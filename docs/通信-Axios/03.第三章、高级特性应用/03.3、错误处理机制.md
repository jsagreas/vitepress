---
title: 3、错误处理机制
---
## 📚 目录

1. [什么是错误处理](#1-什么是错误处理)
2. [Axios错误对象结构](#2-Axios错误对象结构)
3. [错误类型分类](#3-错误类型分类)
4. [错误捕获方法](#4-错误捕获方法)
5. [统一错误处理策略](#5-统一错误处理策略)
6. [用户友好的错误提示](#6-用户友好的错误提示)
7. [重试机制实现](#7-重试机制实现)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🤔 什么是错误处理


### 1.1 为什么需要错误处理

**现实场景理解**：
```
想象你在网上购物：
- 网络突然断了 → 页面转圈圈，啥都显示不出来
- 服务器坏了 → 显示一堆英文错误，用户看不懂
- 商品下架了 → 直接白屏，用户不知道发生了什么

这些就是没有错误处理的糟糕体验！
```

**错误处理的目的**：
- 🎯 **给用户清楚的提示**：告诉用户发生了什么
- 🛡️ **防止程序崩溃**：出错了程序还能继续运行
- 🔄 **提供解决方案**：告诉用户怎么解决问题

### 1.2 什么时候会出错

**常见出错场景**：
```
网络问题：
😵 网络断开 → 发不出请求
⏰ 请求超时 → 等了很久没响应
🚫 被防火墙拦截 → 连不上服务器

服务器问题：
💥 服务器崩溃 → 500错误
🔒 没有权限 → 401/403错误
❓ 页面不存在 → 404错误

数据问题：
📝 参数错误 → 服务器返回错误信息
🔢 数据格式不对 → 解析失败
```

---

## 2. 📋 Axios错误对象结构


### 2.1 错误对象的组成

**Axios给我们的错误信息包含三部分**：

```javascript
// 当请求出错时，axios会给我们一个error对象
axios.get('/api/user')
  .catch(error => {
    console.log('错误对象：', error);
    
    // error对象包含三个重要属性：
    // 1. error.response - HTTP响应错误
    // 2. error.request - 网络请求错误  
    // 3. error.message - 错误描述信息
  });
```

### 2.2 error.response - HTTP响应错误

**什么是response错误**：服务器收到了请求，但返回了错误状态码

```javascript
// 示例：访问不存在的页面
axios.get('/api/not-found')
  .catch(error => {
    if (error.response) {
      // 服务器返回了错误状态码
      console.log('状态码：', error.response.status);     // 404
      console.log('错误数据：', error.response.data);     // 服务器返回的错误信息
      console.log('响应头：', error.response.headers);    // 响应头信息
    }
  });
```

**常见的response错误**：
| 状态码 | **含义** | **通俗解释** |
|--------|----------|-------------|
| `400` | Bad Request | 请求参数有问题，就像填表格填错了 |
| `401` | Unauthorized | 没有登录，需要先登录 |
| `403` | Forbidden | 没有权限，就像没钥匙进不了门 |
| `404` | Not Found | 页面不存在，就像找不到这个地址 |
| `500` | Server Error | 服务器出故障了，不是你的问题 |

### 2.3 error.request - 网络请求错误

**什么是request错误**：请求发出去了，但没有收到任何响应

```javascript
// 示例：网络断开的情况
axios.get('/api/data')
  .catch(error => {
    if (error.request) {
      // 请求发出去了，但没有响应
      console.log('请求对象：', error.request);
      // 这种情况通常是：
      // - 网络断开
      // - 服务器完全没响应
      // - 请求被防火墙拦截
    }
  });
```

### 2.4 error.message - 错误描述

**什么是message**：axios给出的错误描述，通常是英文

```javascript
axios.get('/api/data', { timeout: 3000 })
  .catch(error => {
    console.log('错误描述：', error.message);
    // 常见的错误描述：
    // "Network Error" - 网络错误
    // "timeout of 3000ms exceeded" - 请求超时
    // "Request failed with status code 404" - 请求失败
  });
```

---

## 3. 🏷️ 错误类型分类


### 3.1 网络错误处理

**网络错误的特点**：发不出请求或收不到响应

```javascript
// 判断是否为网络错误
function handleNetworkError(error) {
  if (error.request && !error.response) {
    // 这就是网络错误
    console.log('网络错误，可能是：');
    console.log('- 网络断开了');
    console.log('- 服务器关机了'); 
    console.log('- 防火墙拦截了');
    
    // 给用户友好的提示
    alert('网络连接有问题，请检查网络后重试');
    return true;
  }
  return false;
}
```

### 3.2 HTTP错误处理

**HTTP错误的特点**：服务器收到请求，但返回错误状态码

```javascript
// 根据状态码处理不同错误
function handleHttpError(error) {
  if (error.response) {
    const status = error.response.status;
    
    switch (status) {
      case 401:
        alert('登录过期，请重新登录');
        // 跳转到登录页
        window.location.href = '/login';
        break;
        
      case 403:
        alert('没有权限访问此功能');
        break;
        
      case 404:
        alert('请求的内容不存在');
        break;
        
      case 500:
        alert('服务器出现故障，请稍后重试');
        break;
        
      default:
        alert('请求失败，请稍后重试');
    }
    return true;
  }
  return false;
}
```

### 3.3 业务错误处理

**什么是业务错误**：服务器正常响应，但业务逻辑有问题

```javascript
// 示例：服务器返回自定义错误码
axios.post('/api/login', { username, password })
  .then(response => {
    const { code, message, data } = response.data;
    
    if (code === 0) {
      // 成功
      console.log('登录成功');
    } else {
      // 业务错误
      switch (code) {
        case 1001:
          alert('用户名不存在');
          break;
        case 1002:
          alert('密码错误');
          break;
        case 1003:
          alert('账号被锁定');
          break;
        default:
          alert(message || '登录失败');
      }
    }
  });
```

---

## 4. 🎯 错误捕获方法


### 4.1 Promise的catch方法

**最基本的错误捕获方式**：

```javascript
// 链式调用的错误捕获
axios.get('/api/user')
  .then(response => {
    console.log('获取用户信息成功：', response.data);
  })
  .catch(error => {
    console.log('获取用户信息失败：', error);
    // 在这里处理错误
  });
```

### 4.2 async/await的try-catch

**现代化的错误捕获方式**：

```javascript
// 使用async/await捕获错误
async function getUserInfo() {
  try {
    const response = await axios.get('/api/user');
    console.log('用户信息：', response.data);
    return response.data;
  } catch (error) {
    console.log('获取用户信息出错：', error);
    // 处理错误
    return null;
  }
}
```

### 4.3 判断取消请求

**什么是取消请求**：用户主动取消了正在进行的请求

```javascript
// 创建取消令牌
const source = axios.CancelToken.source();

// 发起请求
axios.get('/api/data', {
  cancelToken: source.token
}).catch(error => {
  // 判断是否为取消请求
  if (axios.isCancel(error)) {
    console.log('请求被取消了：', error.message);
    // 不需要显示错误提示，因为是用户主动取消的
  } else {
    // 其他错误
    console.log('请求出错：', error);
  }
});

// 取消请求
source.cancel('用户取消了请求');
```

---

## 5. 🛠️ 统一错误处理策略


### 5.1 创建错误处理函数

**把错误处理逻辑集中管理**：

```javascript
// 统一的错误处理函数
function handleError(error) {
  // 1. 判断是否为取消请求
  if (axios.isCancel(error)) {
    console.log('请求被取消');
    return;
  }
  
  // 2. 网络错误
  if (error.request && !error.response) {
    showToast('网络连接失败，请检查网络');
    return;
  }
  
  // 3. HTTP错误
  if (error.response) {
    const status = error.response.status;
    
    if (status >= 500) {
      showToast('服务器出现故障，请稍后重试');
    } else if (status === 404) {
      showToast('请求的资源不存在');
    } else if (status === 401) {
      showToast('请先登录');
      // 跳转登录页
      goToLogin();
    } else {
      showToast('请求失败，请重试');
    }
    return;
  }
  
  // 4. 其他错误
  showToast('发生未知错误');
}

// 简单的提示函数
function showToast(message) {
  alert(message); // 实际项目中用Toast组件
}
```

### 5.2 响应拦截器统一处理

**在拦截器中统一处理错误**：

```javascript
// 响应拦截器
axios.interceptors.response.use(
  // 成功的响应
  response => {
    return response;
  },
  // 错误的响应
  error => {
    handleError(error);
    return Promise.reject(error);
  }
);

// 现在所有请求的错误都会被统一处理
axios.get('/api/user'); // 出错会自动显示提示
axios.post('/api/login', data); // 出错会自动显示提示
```

---

## 6. 💬 用户友好的错误提示


### 6.1 错误信息翻译

**把技术错误翻译成用户能理解的话**：

```javascript
// 错误信息映射表
const errorMessages = {
  'Network Error': '网络连接失败，请检查网络',
  'timeout': '请求超时，请重试',
  'Request failed with status code 404': '请求的内容不存在',
  'Request failed with status code 500': '服务器繁忙，请稍后重试'
};

// 获取友好的错误提示
function getFriendlyMessage(error) {
  // 优先使用服务器返回的错误信息
  if (error.response && error.response.data && error.response.data.message) {
    return error.response.data.message;
  }
  
  // 使用映射表翻译
  const message = error.message;
  for (let key in errorMessages) {
    if (message.includes(key)) {
      return errorMessages[key];
    }
  }
  
  // 默认提示
  return '操作失败，请重试';
}
```

### 6.2 不同场景的提示方式

**根据场景选择合适的提示方式**：

```javascript
// 错误提示的封装
const ErrorHandler = {
  // 静默处理（不显示提示）
  silent(error) {
    console.log('静默处理错误：', error);
  },
  
  // Toast提示（轻量级）
  toast(error) {
    const message = getFriendlyMessage(error);
    // 假设有Toast组件
    // Toast.show(message);
    console.log('Toast提示：', message);
  },
  
  // 弹窗提示（重要错误）
  modal(error) {
    const message = getFriendlyMessage(error);
    alert(message);
  },
  
  // 页面提示（严重错误）
  page(error) {
    const message = getFriendlyMessage(error);
    // 跳转到错误页面
    window.location.href = `/error?message=${encodeURIComponent(message)}`;
  }
};
```

---

## 7. 🔄 重试机制实现


### 7.1 简单的重试机制

**自动重试失败的请求**：

```javascript
// 重试函数
async function requestWithRetry(url, options = {}, maxRetry = 3) {
  let retryCount = 0;
  
  while (retryCount < maxRetry) {
    try {
      const response = await axios(url, options);
      return response; // 成功就返回
    } catch (error) {
      retryCount++;
      
      // 判断是否需要重试
      if (shouldRetry(error) && retryCount < maxRetry) {
        console.log(`请求失败，正在重试... (${retryCount}/${maxRetry})`);
        // 等待一段时间再重试
        await delay(1000 * retryCount); // 递增延迟
      } else {
        throw error; // 重试次数用完或不需要重试
      }
    }
  }
}

// 判断是否需要重试
function shouldRetry(error) {
  // 网络错误可以重试
  if (error.request && !error.response) {
    return true;
  }
  
  // 服务器错误可以重试
  if (error.response && error.response.status >= 500) {
    return true;
  }
  
  // 其他错误不重试
  return false;
}

// 延迟函数
function delay(ms) {
  return new Promise(resolve => setTimeout(resolve, ms));
}
```

### 7.2 使用重试机制

**在实际项目中使用重试**：

```javascript
// 使用重试机制发起请求
async function fetchUserData() {
  try {
    const response = await requestWithRetry('/api/user', {
      method: 'GET',
      timeout: 5000
    }, 3);
    
    console.log('用户数据：', response.data);
    return response.data;
  } catch (error) {
    console.log('获取用户数据最终失败：', error);
    showToast('获取用户数据失败');
    return null;
  }
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念

```
🔸 错误处理目的：给用户友好提示，防止程序崩溃
🔸 三种错误类型：网络错误、HTTP错误、业务错误
🔸 错误对象结构：response(响应错误)、request(网络错误)、message(描述)
🔸 捕获方法：catch()、try-catch、拦截器
🔸 统一处理：集中管理错误逻辑，提高代码复用性
```

### 8.2 实用的错误处理模式

```javascript
// 标准的错误处理模式
async function apiRequest() {
  try {
    const response = await axios.get('/api/data');
    return response.data;
  } catch (error) {
    // 1. 判断错误类型
    if (axios.isCancel(error)) return;
    
    // 2. 统一错误处理
    handleError(error);
    
    // 3. 返回默认值或重新抛出
    return null;
  }
}
```

### 8.3 最佳实践建议

- 🎯 **用户至上**：错误提示要用户能理解
- 🛡️ **分类处理**：不同错误用不同的处理方式
- 🔄 **适度重试**：网络错误可以重试，业务错误不要重试
- 📊 **日志记录**：错误要记录日志，方便排查问题
- 🎨 **体验优化**：加载状态、错误状态都要考虑用户体验

**核心记忆**：
- **错误处理不是为了程序员，是为了用户**
- **网络问题重试，权限问题跳转，服务器问题提示**
- **错误信息要说人话，不要显示技术术语**
- **统一处理比分散处理更好维护**