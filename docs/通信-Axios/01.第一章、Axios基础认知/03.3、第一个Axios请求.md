---
title: 3、第一个Axios请求
---
## 📚 目录

1. [什么是Axios请求](#1-什么是Axios请求)
2. [第一个Hello World请求](#2-第一个Hello-World请求)
3. [Promise的通俗理解](#3-Promise的通俗理解)
4. [Axios的几种写法](#4-Axios的几种写法)
5. [配置对象详解](#5-配置对象详解)
6. [async/await语法糖](#6-async-await语法糖)
7. [常见错误处理](#7-常见错误处理)
8. [完整可运行示例](#8-完整可运行示例)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🤔 什么是Axios请求


### 1.1 通俗理解Axios

**简单类比**：把Axios想象成一个"网络邮递员"

```
你的网页 📧 ————————————— 🚚 Axios ————————————— 📦 服务器
       "请帮我取数据"              "好的，马上去"            "这是数据"
```

**实际含义**：
- **你的代码**：告诉Axios要什么数据
- **Axios**：负责跑腿，去服务器取数据
- **服务器**：提供数据的地方

### 1.2 为什么需要Axios

**没有Axios的时代** 🏃‍♂️ 原生写法很麻烦：
```javascript
// 原生写法 - 复杂难懂
let xhr = new XMLHttpRequest();
xhr.open('GET', 'https://api.example.com/users');
xhr.onreadystatechange = function() {
  if (xhr.readyState === 4 && xhr.status === 200) {
    console.log(xhr.responseText);
  }
}
xhr.send();
```

**有了Axios** ✨ 一行代码搞定：
```javascript
// Axios写法 - 简单明了
axios.get('https://api.example.com/users');
```

> 💡 **核心理念**
> 
> Axios就是把复杂的网络请求变简单，让你专注业务逻辑而不是底层细节

---

## 2. 👋 第一个Hello World请求


### 2.1 最简单的GET请求

**概念解释**：GET请求就像"问服务器要数据"

```javascript
// 🎯 核心语法
axios.get('网址')
  .then(response => {
    console.log('服务器返回的数据:', response.data);
  })
  .catch(error => {
    console.log('出错了:', error);
  });
```

### 2.2 真实示例 - 获取用户信息

```javascript
// 📱 实际代码示例
axios.get('https://jsonplaceholder.typicode.com/users/1')
  .then(response => {
    console.log('用户姓名:', response.data.name);
    console.log('用户邮箱:', response.data.email);
  })
  .catch(error => {
    console.log('获取失败:', error.message);
  });
```

**执行流程**：
```
1. 发送请求 → https://jsonplaceholder.typicode.com/users/1
2. 等待响应 → 服务器处理请求
3. 收到数据 → { "name": "张三", "email": "zhangsan@example.com" }
4. 显示结果 → 在控制台打印出来
```

### 2.3 在浏览器中看效果


**步骤1**：打开浏览器控制台
- <kbd>F12</kbd> 或 <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>I</kbd>

**步骤2**：粘贴代码到Console
```javascript
axios.get('https://jsonplaceholder.typicode.com/users/1')
  .then(response => console.log('数据:', response.data));
```

**步骤3**：按 <kbd>Enter</kbd> 执行，看到结果：
```
数据: {id: 1, name: "Leanne Graham", email: "Sincere@april.biz", ...}
```

---

## 3. 🎪 Promise的通俗理解


### 3.1 什么是Promise

**生活类比**：Promise就像"承诺"

```
你对朋友说："我承诺明天给你带午餐"

可能的结果：
✅ 成功 (resolved) - 你真的带了午餐
❌ 失败 (rejected) - 你忘记了或出了意外
⏳ 等待 (pending) - 现在还是明天之前，结果未知
```

### 3.2 Promise在Axios中的应用

```javascript
// 🔄 Promise的三种状态
axios.get('https://api.example.com/data')
  // ⏳ pending - 请求发出去了，等待结果
  .then(response => {
    // ✅ resolved - 成功收到数据
    console.log('成功:', response.data);
  })
  .catch(error => {
    // ❌ rejected - 出错了
    console.log('失败:', error.message);
  })
  .finally(() => {
    // 🏁 无论成功失败都会执行
    console.log('请求结束');
  });
```

### 3.3 then和catch的含义

```javascript
// 📋 详细解释
axios.get('网址')
  .then(response => {
    // then = "然后" - 成功后做什么
    console.log('拿到数据了!', response.data);
  })
  .catch(error => {
    // catch = "捕获" - 出错后做什么  
    console.log('哎呀出错了!', error);
  });
```

> 💡 **记忆技巧**
> 
> - **then** = 然后（成功后然后做什么）
> - **catch** = 抓住（抓住错误来处理）
> - **finally** = 最终（最终都要做的事）

---

## 4. 🛠️ Axios的几种写法


### 4.1 基础写法对比


| 写法类型 | 代码示例 | 使用场景 |
|---------|----------|----------|
| **🎯 便捷方法** | `axios.get(url)` | 📱 日常开发推荐 |
| **⚙️ 通用方法** | `axios({method: 'get', url})` | 🔧 需要更多配置 |
| **🚀 实例方法** | `instance.get(url)` | 🏢 企业项目常用 |

### 4.2 GET请求的多种写法

```javascript
// 🎯 方式1：便捷方法 (推荐新手)
axios.get('https://api.example.com/users');

// ⚙️ 方式2：配置对象
axios({
  method: 'get',
  url: 'https://api.example.com/users'
});

// 📝 方式3：带参数
axios.get('https://api.example.com/users', {
  params: { page: 1, limit: 10 }
});
```

### 4.3 POST请求的写法

```javascript
// 📤 发送数据到服务器
const userData = { name: '张三', age: 25 };

// 方式1：便捷方法
axios.post('https://api.example.com/users', userData);

// 方式2：配置对象
axios({
  method: 'post',
  url: 'https://api.example.com/users',
  data: userData
});
```

### 4.4 常用HTTP方法速览

```javascript
// 📋 增删改查对应的方法
axios.get(url)      // 👀 查询数据 (Read)
axios.post(url, data)    // ➕ 创建数据 (Create) 
axios.put(url, data)     // ✏️ 更新数据 (Update)
axios.delete(url)        // 🗑️ 删除数据 (Delete)
```

---

## 5. ⚙️ 配置对象详解


### 5.1 什么是配置对象

**通俗解释**：配置对象就像"订餐单"，告诉服务员你要什么

```javascript
// 🍽️ 就像点餐一样
const 订餐单 = {
  菜品: '宫保鸡丁',      // → url: 'api/dishes'
  数量: 1,             // → params: { count: 1 }
  备注: '少放辣椒',      // → headers: { note: '少辣' }
  支付方式: '微信',      // → method: 'post'
};
```

### 5.2 核心配置项

```javascript
// 🔧 完整配置示例
const config = {
  method: 'get',                    // 📋 请求方法
  url: 'https://api.example.com',   // 🎯 请求地址  
  params: { id: 1 },               // 📝 URL参数 (?id=1)
  data: { name: '张三' },           // 📦 请求体数据
  headers: {                       // 📋 请求头
    'Content-Type': 'application/json'
  },
  timeout: 5000,                   // ⏰ 超时时间(毫秒)
};

axios(config);
```

### 5.3 参数类型详解

```javascript
// 📝 params vs data 的区别

// GET请求 - 用params (出现在URL里)
axios.get('/users', {
  params: { page: 1, size: 10 }
});
// 实际请求: /users?page=1&size=10

// POST请求 - 用data (在请求体里)  
axios.post('/users', {
  data: { name: '张三', age: 25 }
});
// 数据在请求体中，URL不变
```

> ⚠️ **新手提醒**
> 
> - **params** = URL参数，所有人都能看到
> - **data** = 请求体数据，相对安全一些

---

## 6. 🚀 async/await语法糖


### 6.1 什么是async/await

**通俗解释**：把异步代码写得像同步代码一样

```javascript
// 🔄 传统Promise写法 - 嵌套复杂
axios.get('/users')
  .then(response => {
    console.log(response.data);
    return axios.get('/posts');
  })
  .then(response => {
    console.log(response.data);  
  })
  .catch(error => {
    console.log(error);
  });

// ✨ async/await写法 - 清晰直观  
async function fetchData() {
  try {
    const users = await axios.get('/users');
    console.log(users.data);
    
    const posts = await axios.get('/posts');
    console.log(posts.data);
  } catch (error) {
    console.log(error);
  }
}
```

### 6.2 async/await基本用法

```javascript
// 🎯 基本语法规则
async function getData() {
  // await 必须在 async 函数内使用
  const response = await axios.get('https://api.example.com/users');
  console.log('用户数据:', response.data);
  return response.data;
}

// 调用async函数
getData();
```

### 6.3 错误处理

```javascript
// 🛡️ 完整的错误处理
async function safeRequest() {
  try {
    const response = await axios.get('/api/data');
    console.log('成功:', response.data);
    return response.data;
  } catch (error) {
    console.log('失败:', error.message);
    return null;
  } finally {
    console.log('请求结束');
  }
}
```

> 💡 **使用建议**
> 
> - 新手先学会Promise的then/catch
> - 理解后再使用async/await  
> - async/await让代码更好读，但本质还是Promise

---

## 7. 🐛 常见错误处理


### 7.1 网络错误

```javascript
// ❌ 常见错误：网络连接失败
axios.get('https://不存在的网址.com')
  .catch(error => {
    if (error.code === 'NETWORK_ERROR') {
      console.log('🌐 网络连接失败，请检查网络');
    }
  });
```

### 7.2 HTTP状态码错误

```javascript
// ❌ 服务器返回错误状态码
axios.get('https://api.example.com/notfound')
  .catch(error => {
    if (error.response) {
      // 服务器响应了，但状态码不是2xx
      console.log('❌ 状态码:', error.response.status);
      console.log('📝 错误信息:', error.response.data);
    }
  });
```

### 7.3 超时错误

```javascript
// ⏰ 请求超时处理
axios.get('https://api.example.com/slow', { 
  timeout: 3000  // 3秒超时
})
.catch(error => {
  if (error.code === 'TIMEOUT') {
    console.log('⏰ 请求超时，服务器响应太慢');
  }
});
```

### 7.4 调试技巧

```javascript
// 🔍 完整的错误信息查看
axios.get('/api/data')
  .catch(error => {
    console.log('🐛 完整错误对象:', error);
    console.log('📝 错误消息:', error.message);
    console.log('🌐 请求配置:', error.config);
    if (error.response) {
      console.log('📊 响应状态:', error.response.status);
      console.log('📋 响应数据:', error.response.data);
    }
  });
```

**错误排查流程**：
```
1. 检查网址是否正确 ✅
2. 检查网络连接 🌐  
3. 查看控制台错误信息 🔍
4. 检查服务器状态 📊
5. 确认请求参数格式 📝
```

---

## 8. 🎯 完整可运行示例


### 8.1 HTML页面示例

```html
<!DOCTYPE html>
<html>
<head>
    <title>Axios入门示例</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>
<body>
    <h1>🚀 我的第一个Axios请求</h1>
    <button onclick="fetchUser()">获取用户信息</button>
    <button onclick="createUser()">创建用户</button>
    <div id="result"></div>

    <script>
        // 📱 获取用户信息
        async function fetchUser() {
            try {
                const response = await axios.get('https://jsonplaceholder.typicode.com/users/1');
                document.getElementById('result').innerHTML = 
                    `<h3>✅ 获取成功</h3>
                     <p><strong>姓名:</strong> ${response.data.name}</p>
                     <p><strong>邮箱:</strong> ${response.data.email}</p>`;
            } catch (error) {
                document.getElementById('result').innerHTML = 
                    `<h3>❌ 获取失败</h3><p>${error.message}</p>`;
            }
        }

        // ➕ 创建用户
        async function createUser() {
            try {
                const newUser = {
                    name: '张三',
                    email: 'zhangsan@example.com'
                };
                
                const response = await axios.post('https://jsonplaceholder.typicode.com/users', newUser);
                document.getElementById('result').innerHTML = 
                    `<h3>✅ 创建成功</h3>
                     <p><strong>ID:</strong> ${response.data.id}</p>
                     <p><strong>姓名:</strong> ${response.data.name}</p>`;
            } catch (error) {
                document.getElementById('result').innerHTML = 
                    `<h3>❌ 创建失败</h3><p>${error.message}</p>`;
            }
        }
    </script>
</body>
</html>
```

### 8.2 Node.js示例

```javascript
// 📦 安装: npm install axios
const axios = require('axios');

// 🎯 简单的数据获取
async function main() {
    try {
        console.log('🚀 开始请求...');
        
        // 获取用户列表
        const users = await axios.get('https://jsonplaceholder.typicode.com/users');
        console.log(`✅ 获取到 ${users.data.length} 个用户`);
        
        // 显示前3个用户
        users.data.slice(0, 3).forEach((user, index) => {
            console.log(`${index + 1}. ${user.name} - ${user.email}`);
        });
        
    } catch (error) {
        console.log('❌ 请求失败:', error.message);
    }
}

main();
```

### 8.3 Vue.js集成示例

```vue
<template>
  <div>
    <h2>📱 用户管理</h2>
    <button @click="loadUsers">加载用户</button>
    <ul>
      <li v-for="user in users" :key="user.id">
        {{ user.name }} - {{ user.email }}
      </li>
    </ul>
  </div>
</template>

<script>
import axios from 'axios';

export default {
  data() {
    return {
      users: []
    };
  },
  methods: {
    async loadUsers() {
      try {
        const response = await axios.get('https://jsonplaceholder.typicode.com/users');
        this.users = response.data;
      } catch (error) {
        console.error('加载用户失败:', error);
      }
    }
  }
};
</script>
```

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的概念

```
🔸 Axios本质：网络请求的便捷工具，把复杂变简单
🔸 Promise理解：承诺机制，then处理成功，catch处理失败  
🔸 基本语法：axios.get() / axios.post() 等便捷方法
🔸 配置对象：告诉Axios具体怎么发请求
🔸 错误处理：网络错误、状态码错误、超时错误
```

### 9.2 语法对比速查

| 场景 | Promise写法 | async/await写法 |
|------|-------------|----------------|
| **📱 获取数据** | `axios.get(url).then(res => {})` | `const res = await axios.get(url)` |
| **❌ 错误处理** | `.catch(err => {})` | `try/catch` |
| **🔄 多个请求** | `.then().then()` | `await axios.get(); await axios.get();` |

### 9.3 记忆口诀

```
🧠 记忆要点：
"axios发请求，promise来承诺"
"then是成功，catch是失败"  
"get是获取，post是提交"
"async/await让代码更直白"
```

### 9.4 学习建议

- **🎯 先理解概念**：知道Axios是做什么的
- **👋 从简单开始**：先会发GET请求看结果
- **🔄 逐步进阶**：掌握POST、配置、错误处理
- **💪 多练习**：复制示例代码亲自运行
- **🐛 学会调试**：看控制台错误信息

> 💡 **学习心态**
> 
> 不要急于求成，先把基本的GET/POST请求弄明白，
> 后面的高级功能都是在这个基础上的扩展！