---
title: 6、模板引擎与视图渲染
---
## 📚 目录

1. [模板引擎核心概念](#1-模板引擎核心概念)
2. [EJS模板引擎实战](#2-EJS模板引擎实战)
3. [Pug模板引擎入门](#3-Pug模板引擎入门)
4. [Express视图配置](#4-Express视图配置)
5. [模板数据传递与渲染](#5-模板数据传递与渲染)
6. [模板高级特性](#6-模板高级特性)
7. [前后端渲染方案选择](#7-前后端渲染方案选择)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎨 模板引擎核心概念


### 1.1 什么是模板引擎


**🔸 简单理解**
```
模板引擎就像一个智能的"填空题生成器"：

普通HTML：写死的内容，不能变化
<h1>欢迎用户</h1>

模板HTML：带"空白"的模板，可以填入动态内容
<h1>欢迎 <%= userName %></h1>

最终结果：根据数据生成完整的HTML
<h1>欢迎 张三</h1>
```

**💡 核心作用**
- **动态内容生成**：把数据和HTML模板结合，生成完整页面
- **代码复用**：一个模板可以填入不同数据，生成多个页面
- **逻辑分离**：把页面结构和数据处理分开

### 1.2 为什么需要模板引擎


**🚫 没有模板引擎的问题**
```javascript
// 用字符串拼接HTML - 麻烦且容易出错
app.get('/user', (req, res) => {
  const html = `
    <html>
      <head><title>用户信息</title></head>
      <body>
        <h1>用户：` + user.name + `</h1>
        <p>邮箱：` + user.email + `</p>
      </body>
    </html>
  `;
  res.send(html);
});
```

**✅ 使用模板引擎的优势**
```javascript
// 清晰简洁的模板文件
app.get('/user', (req, res) => {
  res.render('user', { 
    name: user.name, 
    email: user.email 
  });
});
```

### 1.3 常见模板引擎对比


| 模板引擎 | **语法特点** | **学习难度** | **适用场景** |
|----------|-------------|-------------|-------------|
| **EJS** | `类似HTML，<%= %>标签` | `⭐⭐ 简单` | `新手入门，快速开发` |
| **Pug** | `缩进语法，简洁` | `⭐⭐⭐ 中等` | `追求简洁，有经验开发者` |
| **Handlebars** | `{{}} 语法` | `⭐⭐ 简单` | `逻辑简单的模板` |

---

## 2. 📝 EJS模板引擎实战


### 2.1 EJS基础语法


**🔸 EJS核心标签**
```ejs
<!-- 输出变量（转义HTML） -->
<h1>用户名：<%= userName %></h1>

<!-- 输出变量（不转义HTML） -->
<div>内容：<%- htmlContent %></div>

<!-- JavaScript逻辑代码 -->
<% if (isLogin) { %>
  <p>欢迎回来！</p>
<% } else { %>
  <p>请先登录</p>
<% } %>

<!-- 循环渲染 -->
<ul>
<% users.forEach(user => { %>
  <li><%= user.name %></li>
<% }); %>
</ul>
```

### 2.2 EJS项目搭建


**🛠️ 安装和配置**
```bash
# 安装EJS
npm install ejs
```

```javascript
// app.js - Express配置
const express = require('express');
const app = express();

// 设置模板引擎
app.set('view engine', 'ejs');
app.set('views', './views'); // 模板文件目录

app.listen(3000);
```

**📁 项目结构**
```
project/
├── 📄 app.js
├── 📁 views/          ← 模板文件夹
│   ├── 📄 index.ejs
│   └── 📄 user.ejs
└── 📄 package.json
```

### 2.3 EJS实际案例


**📄 views/user.ejs**
```ejs
<!DOCTYPE html>
<html>
<head>
    <title>用户信息</title>
    <style>
        .user-card { border: 1px solid #ddd; padding: 20px; }
    </style>
</head>
<body>
    <div class="user-card">
        <h2>用户：<%= name %></h2>
        <p>邮箱：<%= email %></p>
        <p>注册时间：<%= joinDate %></p>
        
        <% if (isVip) { %>
            <span style="color: gold;">🎖️ VIP用户</span>
        <% } %>
    </div>
</body>
</html>
```

**📄 路由处理**
```javascript
app.get('/user/:id', (req, res) => {
  // 模拟用户数据
  const userData = {
    name: '张三',
    email: 'zhangsan@example.com',
    joinDate: '2024-01-15',
    isVip: true
  };
  
  // 渲染模板
  res.render('user', userData);
});
```

---

## 3. 🎯 Pug模板引擎入门


### 3.1 Pug语法特点


**🔸 Pug vs HTML对比**
```pug
// Pug语法 - 简洁
doctype html
html
  head
    title 用户信息
  body
    .user-card
      h2= name
      p 邮箱：#{email}
      if isVip
        span.vip-badge VIP用户
```

```html
<!-- 对应的HTML -->
<!DOCTYPE html>
<html>
  <head>
    <title>用户信息</title>
  </head>
  <body>
    <div class="user-card">
      <h2>张三</h2>
      <p>邮箱：zhangsan@example.com</p>
      <span class="vip-badge">VIP用户</span>
    </div>
  </body>
</html>
```

### 3.2 Pug基础配置


```bash
# 安装Pug
npm install pug
```

```javascript
// 配置Pug
app.set('view engine', 'pug');
app.set('views', './views');
```

### 3.3 Pug核心语法


**🔸 变量和表达式**
```pug
// 变量输出
h1= userName              // <h1>张三</h1>
p 用户名：#{userName}      // <p>用户名：张三</p>

// 条件判断
if isLogin
  p 欢迎回来
else
  p 请登录

// 循环
ul
  each user in users
    li= user.name
```

---

## 4. ⚙️ Express视图配置


### 4.1 app.set()视图引擎配置


**🔧 核心配置方法**
```javascript
const express = require('express');
const app = express();

// 1. 设置模板引擎类型
app.set('view engine', 'ejs');  // 或 'pug', 'hbs'等

// 2. 设置模板文件目录
app.set('views', './views');    // 默认是 './views'

// 3. 设置模板缓存（生产环境推荐开启）
app.set('view cache', true);
```

**💡 配置选项说明**
```javascript
// 详细配置示例
app.set('view engine', 'ejs');        // 模板引擎
app.set('views', path.join(__dirname, 'templates')); // 自定义模板目录
app.set('view cache', process.env.NODE_ENV === 'production'); // 生产环境缓存
```

### 4.2 多模板引擎支持


```javascript
// 注册多个模板引擎
app.engine('html', require('ejs').renderFile);  // .html文件用EJS渲染
app.engine('pug', require('pug').__express);    // .pug文件用Pug渲染

app.set('view engine', 'html'); // 默认引擎
```

---

## 5. 📊 模板数据传递与渲染


### 5.1 res.render()渲染方法


**🔸 基本用法**
```javascript
// 语法：res.render(模板名, 数据对象, 回调函数)
app.get('/home', (req, res) => {
  res.render('index', {
    title: '首页',
    message: '欢迎来到我的网站'
  });
});
```

**🔸 完整参数说明**
```javascript
app.get('/user', (req, res) => {
  const data = {
    user: { name: '张三', age: 25 },
    products: ['手机', '电脑', '平板']
  };
  
  // 渲染模板
  res.render('user', data, (err, html) => {
    if (err) {
      console.error('渲染错误:', err);
      res.status(500).send('页面渲染失败');
    } else {
      res.send(html);
    }
  });
});
```

### 5.2 数据传递最佳实践


**🎯 数据组织技巧**
```javascript
app.get('/dashboard', (req, res) => {
  const pageData = {
    // 页面基础信息
    title: '控制面板',
    description: '用户数据管理',
    
    // 用户信息
    currentUser: {
      name: req.user.name,
      role: req.user.role
    },
    
    // 页面数据
    stats: {
      totalUsers: 1250,
      todayVisits: 89,
      revenue: 12580
    },
    
    // 页面状态
    showNotification: true,
    theme: 'dark'
  };
  
  res.render('dashboard', pageData);
});
```

### 5.3 模板中的数据使用


**📄 EJS模板示例**
```ejs
<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
</head>
<body>
    <header>
        <h1><%= title %></h1>
        <p>当前用户：<%= currentUser.name %></p>
    </header>
    
    <main>
        <div class="stats">
            <div>总用户：<%= stats.totalUsers %></div>
            <div>今日访问：<%= stats.todayVisits %></div>
            <div>收入：¥<%= stats.revenue %></div>
        </div>
        
        <% if (showNotification) { %>
            <div class="notification">
                您有新消息！
            </div>
        <% } %>
    </main>
</body>
</html>
```

---

## 6. 🏗️ 模板高级特性


### 6.1 模板继承和包含


**🔸 EJS包含功能**
```ejs
<!-- views/layout/header.ejs -->
<header>
    <h1><%= siteName %></h1>
    <nav>
        <a href="/">首页</a>
        <a href="/about">关于</a>
    </nav>
</header>

<!-- views/index.ejs -->
<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
</head>
<body>
    <%- include('layout/header', { siteName: '我的网站' }) %>
    
    <main>
        <h2><%= pageTitle %></h2>
        <p><%= content %></p>
    </main>
    
    <%- include('layout/footer') %>
</body>
</html>
```

**🔸 Pug继承功能**
```pug
// views/layout/base.pug
doctype html
html
  head
    title= title
  body
    header
      h1= siteName
    
    block content
    
    footer
      p © 2024 我的网站

// views/index.pug
extends layout/base

block content
  main
    h2= pageTitle
    p= content
```

### 6.2 模板缓存配置


**⚡ 性能优化配置**
```javascript
// 开发环境 - 不缓存，便于调试
if (process.env.NODE_ENV === 'development') {
  app.set('view cache', false);
}

// 生产环境 - 开启缓存，提升性能
if (process.env.NODE_ENV === 'production') {
  app.set('view cache', true);
}
```

### 6.3 静态资源在模板中的使用


**🎨 CSS和JS引入**
```ejs
<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <!-- 静态CSS文件 -->
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body>
    <!-- 页面内容 -->
    
    <!-- 静态JS文件 -->
    <script src="/js/jquery.min.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>
```

```javascript
// 配置静态资源目录
app.use(express.static('public'));

// 目录结构
// public/
// ├── css/
// │   └── style.css
// └── js/
//     └── app.js
```

---

## 7. 🤔 前后端渲染方案选择


### 7.1 服务端渲染(SSR) vs 前后端分离


**📊 方案对比表**

| 对比项 | **服务端渲染(SSR)** | **前后端分离(SPA)** |
|--------|-------------------|-------------------|
| **SEO优化** | `🟢 优秀` | `🟡 需要额外处理` |
| **首屏加载** | `🟢 快速` | `🟡 相对较慢` |
| **开发复杂度** | `🟢 简单` | `🟡 相对复杂` |
| **前端交互** | `🟡 有限` | `🟢 丰富` |
| **服务器压力** | `🟡 较大` | `🟢 较小` |

### 7.2 选择建议


**✅ 适合服务端渲染的场景**
```
🔸 企业官网、博客 - SEO要求高
🔸 电商产品页 - 需要搜索引擎收录
🔸 新闻资讯站 - 内容为主
🔸 快速开发原型 - 简单直接
```

**✅ 适合前后端分离的场景**
```
🔸 管理后台 - 交互复杂
🔸 移动端APP - 性能要求高
🔸 实时应用 - 如聊天室、游戏
🔸 大型项目 - 团队分工明确
```

### 7.3 混合方案


**🎯 最佳实践**
```javascript
// 不同页面使用不同渲染方式
app.get('/', (req, res) => {
  // 首页用服务端渲染 - 利于SEO
  res.render('index', { products: getProducts() });
});

app.get('/admin/*', (req, res) => {
  // 管理后台用前端路由 - 交互丰富
  res.sendFile(path.join(__dirname, 'public/admin.html'));
});

// API接口供前端调用
app.get('/api/products', (req, res) => {
  res.json(getProducts());
});
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 模板引擎本质：HTML模板 + 动态数据 = 完整页面
🔸 Express配置：app.set() 设置引擎和目录
🔸 数据渲染：res.render() 传递数据到模板
🔸 模板语法：EJS的 <%= %> 和 Pug的缩进语法
🔸 模板继承：复用页面结构，减少重复代码
```

### 8.2 关键理解要点


**🔹 模板引擎的价值**
```
解决问题：
- 避免字符串拼接HTML的麻烦
- 实现代码复用和维护性
- 分离页面结构和数据逻辑

核心优势：
- 开发效率高
- 代码维护性好
- 天然支持SEO
```

**🔹 EJS vs Pug选择**
```
选择EJS：
✅ 新手友好，语法接近HTML
✅ 学习成本低
✅ 团队易于接受

选择Pug：
✅ 代码简洁，开发效率高
✅ 避免HTML标签不匹配错误
✅ 适合有经验的开发者
```

### 8.3 实际应用指导


**🎯 开发建议**
- **新手推荐**：从EJS开始学习，语法简单容易上手
- **项目选择**：根据团队技能和项目需求选择模板引擎
- **性能优化**：生产环境开启模板缓存
- **架构选择**：简单项目用SSR，复杂交互用前后端分离

**🔧 常见配置**
```javascript
// 推荐的基础配置
app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views'));
app.set('view cache', process.env.NODE_ENV === 'production');
app.use(express.static('public'));
```

**核心记忆**：
- 模板引擎让HTML变"活"了，能根据数据动态生成页面
- EJS简单易学，Pug简洁高效，根据团队情况选择
- 服务端渲染适合SEO，前后端分离适合交互
- 模板继承和包含让代码更好维护