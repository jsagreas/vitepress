---
title: 5、操作系统与常用工具模块
---
## 📚 目录

1. [os模块 - 获取系统信息](#1-os模块---获取系统信息)
2. [util模块 - 实用工具集](#2-util模块---实用工具集)
3. [crypto模块 - 安全加密](#3-crypto模块---安全加密)
4. [Buffer缓冲区操作](#4-buffer缓冲区操作)
5. [定时器函数详解](#5-定时器函数详解)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🖥️ os模块 - 获取系统信息


### 1.1 什么是os模块

**简单理解**：os模块就像是一个"系统探测器"，帮你了解运行Node.js的电脑基本情况

```
想象一下：
你的程序 = 一个客人
电脑系统 = 房子
os模块 = 房子的说明书

通过os模块，客人可以知道：
- 这个房子是什么类型（Windows/Mac/Linux）
- 有几个房间（CPU核心数）
- 还有多少空间（内存剩余）
- 房主是谁（用户信息）
```

### 1.2 获取系统基础信息


**🔍 系统平台检测**
```javascript
const os = require('os');

// 获取操作系统类型
console.log('系统平台:', os.platform());
// 输出: 'win32' (Windows) 或 'darwin' (Mac) 或 'linux' (Linux)

// 获取CPU架构
console.log('CPU架构:', os.arch());
// 输出: 'x64' 或 'arm64' 等

// 获取系统版本
console.log('系统版本:', os.release());
```

> 💡 **实际应用**：根据不同系统执行不同逻辑，比如Windows用反斜杠路径，Linux用正斜杠

**📊 硬件信息获取**
```javascript
// 获取CPU信息
const cpus = os.cpus();
console.log('CPU核心数:', cpus.length);
console.log('CPU型号:', cpus[0].model);

// 实用示例：根据CPU核心数决定并发任务数
const maxWorkers = Math.min(os.cpus().length, 4); // 最多4个工作进程
```

### 1.3 内存信息监控


**💾 内存使用情况**
```javascript
// 获取内存信息（单位：字节）
const totalMem = os.totalmem();     // 总内存
const freeMem = os.freemem();       // 空闲内存
const usedMem = totalMem - freeMem; // 已使用内存

// 转换为更友好的单位
function formatBytes(bytes) {
    const gb = (bytes / 1024 / 1024 / 1024).toFixed(2);
    return `${gb} GB`;
}

console.log('总内存:', formatBytes(totalMem));
console.log('空闲内存:', formatBytes(freeMem));
console.log('已用内存:', formatBytes(usedMem));
console.log('内存使用率:', ((usedMem / totalMem) * 100).toFixed(1) + '%');
```

> ⚠️ **注意**：这些数字是实时变化的，可以用来监控程序是否占用过多内存

### 1.4 用户和网络信息


**👤 用户信息获取**
```javascript
// 获取当前用户信息
const userInfo = os.userInfo();
console.log('用户名:', userInfo.username);
console.log('用户目录:', userInfo.homedir);

// 获取主机名
console.log('电脑名称:', os.hostname());
```

**🌐 网络接口信息**
```javascript
// 获取网络接口信息
const networkInterfaces = os.networkInterfaces();

// 找到本机IP地址
function getLocalIP() {
    const interfaces = os.networkInterfaces();
    for (let name in interfaces) {
        for (let info of interfaces[name]) {
            // 跳过内部地址和IPv6
            if (!info.internal && info.family === 'IPv4') {
                return info.address;
            }
        }
    }
    return '127.0.0.1';
}

console.log('本机IP:', getLocalIP());
```

---

## 2. 🛠️ util模块 - 实用工具集


### 2.1 什么是util模块

**简单理解**：util模块是Node.js的"工具箱"，里面装着各种日常开发用得到的小工具

```
util模块 = 开发者的瑞士军刀
包含：
- 格式化工具（让数据更好看）
- 转换工具（把旧式代码变新式）
- 调试工具（查看复杂对象内容）
```

### 2.2 promisify - 旧代码现代化


**🔄 回调函数转Promise**
```javascript
const util = require('util');
const fs = require('fs');

// 传统的回调方式
fs.readFile('data.txt', 'utf8', (err, data) => {
    if (err) console.error(err);
    else console.log(data);
});

// 使用promisify转换
const readFileAsync = util.promisify(fs.readFile);

// 现在可以用async/await了
async function readData() {
    try {
        const data = await readFileAsync('data.txt', 'utf8');
        console.log(data);
    } catch (err) {
        console.error(err);
    }
}
```

> 💡 **为什么有用**：很多老的Node.js API都是回调风格，promisify让它们支持现代的async/await语法

### 2.3 inspect - 对象深度查看


**🔍 对象检查工具**
```javascript
const util = require('util');

const complexObj = {
    name: '张三',
    age: 25,
    hobbies: ['编程', '游戏'],
    address: {
        city: '北京',
        district: '朝阳区'
    },
    getData: function() { return 'some data'; }
};

// 普通console.log看不全
console.log(complexObj); // [Object object] 😵

// 使用inspect看清楚结构
console.log(util.inspect(complexObj, {
    depth: null,    // 显示所有层级
    colors: true    // 有颜色更清楚
}));
```

**🎨 inspect选项说明**
```javascript
util.inspect(对象, {
    depth: 2,        // 只显示2层深度，防止太复杂
    colors: true,    // 不同类型用不同颜色
    showHidden: false, // 不显示隐藏属性
    maxArrayLength: 10 // 数组最多显示10个元素
});
```

### 2.4 format - 字符串格式化


**📝 字符串格式化工具**
```javascript
const util = require('util');

// 类似printf的格式化
const name = '李四';
const age = 30;
const score = 95.5;

const message = util.format('用户 %s，年龄 %d，成绩 %.1f', name, age, score);
console.log(message); // 用户 李四，年龄 30，成绩 95.5

// 常用占位符
util.format('%s', '字符串');     // 字符串
util.format('%d', 123);         // 整数
util.format('%f', 123.456);     // 浮点数
util.format('%j', {a: 1});      // JSON格式
```

---

## 3. 🔒 crypto模块 - 安全加密


### 3.1 什么是crypto模块

**简单理解**：crypto模块是Node.js的"保险箱"，专门用来保护数据安全

```
数据安全的需求：
原始数据 = 一张纸条上的秘密
哈希加密 = 把纸条撕碎磨成粉，无法还原但可以验证
随机数 = 生成不可预测的密码
```

### 3.2 哈希计算 - 数据指纹


**🔐 生成数据哈希值**
```javascript
const crypto = require('crypto');

// 创建哈希计算器
function createHash(data, algorithm = 'md5') {
    return crypto.createHash(algorithm)
                 .update(data)
                 .digest('hex'); // 输出16进制字符串
}

// 实际使用
const password = '123456';
const hashedPassword = createHash(password, 'sha256');
console.log('原密码:', password);
console.log('加密后:', hashedPassword);

// 验证密码
function verifyPassword(inputPassword, storedHash) {
    const inputHash = createHash(inputPassword, 'sha256');
    return inputHash === storedHash;
}
```

> 💡 **实际应用**：密码加密存储、文件完整性校验、生成唯一标识

**🔄 常用哈希算法对比**
```javascript
const data = 'Hello World';

console.log('MD5:   ', createHash(data, 'md5'));     // 32字符
console.log('SHA1:  ', createHash(data, 'sha1'));    // 40字符  
console.log('SHA256:', createHash(data, 'sha256'));  // 64字符
```

| 算法 | 输出长度 | 安全性 | 速度 | 适用场景 |
|------|----------|--------|------|----------|
| **MD5** | 32字符 | ⚠️ 较低 | 🚀 很快 | 文件校验、非安全场景 |
| **SHA1** | 40字符 | ⚠️ 一般 | ⚡ 快 | 版本控制、简单验证 |
| **SHA256** | 64字符 | ✅ 高 | 📊 中等 | 密码存储、安全应用 |

### 3.3 随机数生成


**🎲 生成安全随机数**
```javascript
// 生成随机字节
const randomBytes = crypto.randomBytes(16); // 16字节随机数据
console.log('随机字节:', randomBytes);
console.log('十六进制:', randomBytes.toString('hex'));
console.log('Base64:', randomBytes.toString('base64'));

// 生成随机密码
function generatePassword(length = 12) {
    const charset = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
    const randomBytes = crypto.randomBytes(length);
    let password = '';
    
    for (let i = 0; i < length; i++) {
        password += charset[randomBytes[i] % charset.length];
    }
    
    return password;
}

console.log('随机密码:', generatePassword(16));
```

---

## 4. 📦 Buffer缓冲区操作


### 4.1 什么是Buffer

**简单理解**：Buffer就像一个"临时仓库"，专门存放二进制数据

```
Buffer的作用：
文字 "Hello" → [72, 101, 108, 108, 111] → 二进制数据
图片文件 → [字节1, 字节2, 字节3...] → 二进制数据

Buffer = 能装各种二进制数据的盒子
```

### 4.2 Buffer基础操作


**📝 创建和操作Buffer**
```javascript
// 创建Buffer的几种方式
const buf1 = Buffer.from('Hello');           // 从字符串创建
const buf2 = Buffer.alloc(10);               // 创建10字节的空Buffer
const buf3 = Buffer.from([72, 101, 108]);    // 从数组创建

console.log('buf1:', buf1);                  // <Buffer 48 65 6c 6c 6f>
console.log('转为字符串:', buf1.toString()); // Hello
console.log('Buffer长度:', buf1.length);     // 5

// Buffer操作
buf2.write('Node.js');                       // 写入数据
console.log('写入后:', buf2.toString());
```

### 4.3 编码转换详解


**🔄 不同编码格式转换**
```javascript
const text = '你好世界';

// 不同编码的Buffer
const utf8Buffer = Buffer.from(text, 'utf8');
const base64Buffer = Buffer.from(text, 'utf8');

console.log('原文:', text);
console.log('UTF-8字节数:', utf8Buffer.length);
console.log('转Base64:', utf8Buffer.toString('base64'));
console.log('转十六进制:', utf8Buffer.toString('hex'));

// 编码转换实用函数
function convertEncoding(text, fromEncoding, toEncoding) {
    const buffer = Buffer.from(text, fromEncoding);
    return buffer.toString(toEncoding);
}

// Base64编码/解码
const encoded = convertEncoding('机密信息', 'utf8', 'base64');
const decoded = convertEncoding(encoded, 'base64', 'utf8');
console.log('编码:', encoded);
console.log('解码:', decoded);
```

---

## 5. ⏰ 定时器函数详解


### 5.1 定时器函数概述

**简单理解**：定时器就像"闹钟"，帮你在特定时间执行任务

```
定时器类型：
setTimeout = 单次闹钟（响一次就停）
setInterval = 重复闹钟（每隔一段时间响一次）
setImmediate = 立即闹钟（当前任务完成后立即响）
```

### 5.2 setTimeout - 延迟执行


**⏰ 延迟执行任务**
```javascript
// 基础用法
console.log('开始');

const timer = setTimeout(() => {
    console.log('3秒后执行');
}, 3000);

console.log('设置完定时器');

// 取消定时器
// clearTimeout(timer);

// 带参数的定时器
setTimeout((name, age) => {
    console.log(`用户: ${name}, 年龄: ${age}`);
}, 1000, '张三', 25);
```

> 💡 **实际应用**：延迟发送请求、倒计时功能、超时处理

### 5.3 setInterval - 重复执行


**🔄 重复执行任务**
```javascript
let count = 0;

const interval = setInterval(() => {
    count++;
    console.log(`执行第 ${count} 次`);
    
    // 执行5次后停止
    if (count >= 5) {
        clearInterval(interval);
        console.log('定时器已停止');
    }
}, 1000);

// 实用示例：简单的倒计时
function countdown(seconds) {
    console.log(`倒计时开始: ${seconds}秒`);
    
    const timer = setInterval(() => {
        seconds--;
        if (seconds > 0) {
            console.log(`剩余: ${seconds}秒`);
        } else {
            console.log('倒计时结束！');
            clearInterval(timer);
        }
    }, 1000);
}

countdown(5);
```

### 5.4 setImmediate - 立即执行


**⚡ 下一轮事件循环执行**
```javascript
console.log('1: 同步代码');

setImmediate(() => {
    console.log('3: setImmediate');
});

setTimeout(() => {
    console.log('4: setTimeout');
}, 0);

console.log('2: 同步代码结束');

// 输出顺序：1 → 2 → 3 → 4
```

> 💡 **区别说明**：setImmediate比setTimeout(0)更早执行，用于需要在当前事件循环结束后立即执行的任务

---

## 6. 📋 核心要点总结


### 6.1 模块功能总览


```
┌─ Node.js 常用工具模块 ──────────────────────────┐
│                                              │
│ ┌─ os模块 ─────────┐ ┌─ util模块 ──────────┐    │
│ │ • 系统信息查看    │ │ • promisify转换     │    │
│ │ • 硬件信息获取    │ │ • 对象检查工具      │    │
│ │ • 内存监控        │ │ • 字符串格式化      │    │
│ │ • 网络接口信息    │ └───────────────────┘    │
│ └─────────────────┘                          │
│                                              │
│ ┌─ crypto模块 ─────┐ ┌─ Buffer缓冲区 ──────┐    │
│ │ • 哈希计算        │ │ • 二进制数据处理    │    │
│ │ • 随机数生成      │ │ • 编码格式转换      │    │
│ │ • 数据加密        │ │ • 字节操作          │    │
│ └─────────────────┘ └───────────────────┘    │
│                                              │
│ ┌─ 定时器函数 ─────────────────────────────┐    │
│ │ • setTimeout（延迟执行）               │    │
│ │ • setInterval（重复执行）              │    │
│ │ • setImmediate（立即执行）             │    │
│ └─────────────────────────────────────┘    │
└──────────────────────────────────────────┘
```

### 6.2 必须掌握的核心要点


**🔸 os模块核心**
```
系统检测：os.platform()、os.arch()
硬件信息：os.cpus()、os.totalmem()、os.freemem()
用户信息：os.userInfo()、os.hostname()
网络信息：os.networkInterfaces()
```

**🔸 util模块核心**
```
现代化转换：util.promisify() - 回调转Promise
调试工具：util.inspect() - 深度查看对象
格式化：util.format() - 字符串格式化
```

**🔸 crypto模块核心**
```
数据哈希：crypto.createHash() - 生成数据指纹
随机数：crypto.randomBytes() - 安全随机数生成
常用算法：MD5、SHA1、SHA256
```

**🔸 Buffer核心**
```
数据转换：Buffer.from()、toString()
编码支持：utf8、base64、hex
二进制处理：处理文件、网络数据
```

**🔸 定时器核心**
```
延迟执行：setTimeout() + clearTimeout()
重复执行：setInterval() + clearInterval()  
立即执行：setImmediate()
```

### 6.3 实际应用场景


| 模块 | **典型应用场景** | **实用价值** |
|------|----------------|-------------|
| **os模块** | `系统兼容性检查、资源监控、集群配置` | `根据系统环境做适配` |
| **util模块** | `代码重构、调试分析、日志格式化` | `提升开发效率` |
| **crypto模块** | `密码加密、数据校验、随机ID生成` | `保障数据安全` |
| **Buffer** | `文件处理、网络传输、图片处理` | `处理二进制数据` |
| **定时器** | `延迟任务、定时任务、超时控制` | `时间控制功能` |

### 6.4 新手学习建议


> 📖 **学习重点**：这些模块都是Node.js开发的基础工具，重点理解每个模块解决什么问题

> 🔧 **实践建议**：
> - os模块：写个系统信息查看工具
> - util模块：把现有回调代码改造成async/await
> - crypto模块：实现简单的密码加密功能
> - Buffer：处理文件读写操作
> - 定时器：制作倒计时或提醒功能

> ⚠️ **注意事项**：
> - os模块的内存信息会实时变化
> - crypto的哈希是不可逆的，用于验证而非解密
> - Buffer处理的是二进制数据，注意编码转换
> - 定时器使用后记得清理，避免内存泄漏

**核心记忆要点**：
- os模块看系统，util工具来帮忙
- crypto保安全，Buffer装数据
- 定时器控时间，开发效率大提升