---
title: 3、HTTP 服务器开发
---
## 📚 目录

1. [HTTP模块基础概念](#1-HTTP模块基础概念)
2. [创建第一个HTTP服务器](#2-创建第一个HTTP服务器)
3. [请求对象req详解](#3-请求对象req详解)
4. [响应对象res详解](#4-响应对象res详解)
5. [处理不同HTTP方法](#5-处理不同HTTP方法)
6. [URL解析和参数获取](#6-URL解析和参数获取)
7. [简单路由实现](#7-简单路由实现)
8. [返回不同类型响应](#8-返回不同类型响应)
9. [处理请求体数据](#9-处理请求体数据)
10. [高级特性与最佳实践](#10-高级特性与最佳实践)
11. [核心要点总结](#11-核心要点总结)

---

## 1. 🌐 HTTP模块基础概念


### 1.1 什么是HTTP模块


**简单理解**：HTTP模块就像一个"邮局"，负责接收和发送网络请求

```
浏览器发送请求 → HTTP模块接收 → 处理业务逻辑 → HTTP模块返回响应 → 浏览器显示结果
```

**HTTP模块的作用**：
- 🔸 **创建Web服务器**：让你的电脑能接收网页请求
- 🔸 **处理用户请求**：分析用户想要什么
- 🔸 **返回响应内容**：给用户发送网页、数据或文件
- 🔸 **管理网络连接**：处理多个用户同时访问

### 1.2 HTTP模块在实际中的应用


```
现实场景对比：

餐厅服务               HTTP服务器
- 顾客点餐        →    用户发送请求
- 服务员记录      →    解析请求内容  
- 厨房制作        →    处理业务逻辑
- 服务员上菜      →    返回响应结果
```

**为什么需要HTTP模块**：
- ✅ **创建网站后端**：提供数据给前端页面
- ✅ **开发API接口**：让其他程序调用你的功能
- ✅ **文件服务器**：提供图片、文档下载
- ✅ **数据处理中心**：接收、处理、返回数据

---

## 2. 🚀 创建第一个HTTP服务器


### 2.1 基础服务器代码


```javascript
// 引入http模块 - 这是Node.js内置的，不需要安装
const http = require('http');

// 创建服务器 - 就像开一家店
const server = http.createServer((req, res) => {
    // 这里是处理请求的地方
    res.writeHead(200, {'Content-Type': 'text/plain; charset=utf-8'});
    res.end('你好！这是我的第一个服务器');
});

// 监听端口 - 就像告诉别人你的店在哪条街几号
server.listen(3000, () => {
    console.log('服务器启动成功！访问地址：http://localhost:3000');
});
```

### 2.2 代码分解理解


**🔸 http.createServer() 的含义**：
```javascript
// 创建服务器就像建立一个"接待处"
const server = http.createServer(回调函数);

// 回调函数有两个参数：
// req（request） - 顾客的需求单
// res（response）- 给顾客的回复单
```

**🔸 server.listen() 的含义**：
```javascript
// 监听端口就像选择门牌号
server.listen(端口号, 回调函数);

// 端口号：就像门牌号，告诉别人在哪找到你
// 常用端口：3000, 8000, 8080（开发用）, 80（网站标准）, 443（安全网站）
```

### 2.3 启动服务器步骤


```
步骤1：保存代码为 server.js
步骤2：打开命令行，进入文件目录
步骤3：运行 node server.js
步骤4：打开浏览器访问 http://localhost:3000
步骤5：看到"你好！这是我的第一个服务器"表示成功
```

> 💡 **新手提示**：localhost 就是"本机"的意思，3000是端口号。就像你家地址是"本地小区3000号"

---

## 3. 📨 请求对象req详解


### 3.1 req是什么


**简单理解**：req就像顾客填写的"需求单"，包含了用户想要什么的所有信息

```javascript
const server = http.createServer((req, res) => {
    // req 包含用户请求的所有信息
    console.log('用户访问了：', req.url);        // 用户想去哪个页面
    console.log('访问方式：', req.method);      // 用户怎么访问的
    console.log('用户设备信息：', req.headers); // 用户用什么浏览器等
});
```

### 3.2 常用的req属性


**🔸 req.url - 用户想访问的页面**：
```javascript
// 用户访问不同地址，req.url会显示不同内容
// 访问 http://localhost:3000/home     → req.url = '/home'
// 访问 http://localhost:3000/about    → req.url = '/about'
// 访问 http://localhost:3000/user/123 → req.url = '/user/123'

const server = http.createServer((req, res) => {
    if (req.url === '/home') {
        res.end('这是首页');
    } else if (req.url === '/about') {
        res.end('这是关于页面');
    } else {
        res.end('页面不存在');
    }
});
```

**🔸 req.method - 请求方式**：
```javascript
// 不同的请求方式代表不同的操作意图
const server = http.createServer((req, res) => {
    if (req.method === 'GET') {
        res.end('用户想要获取信息');
    } else if (req.method === 'POST') {
        res.end('用户想要提交信息');
    }
});
```

**HTTP方法简单理解**：
- **GET**：我想要看/获取东西（浏览网页）
- **POST**：我想要提交/发送东西（提交表单）
- **PUT**：我想要更新/修改东西
- **DELETE**：我想要删除东西

**🔸 req.headers - 请求头信息**：
```javascript
const server = http.createServer((req, res) => {
    // headers包含用户的浏览器信息、语言设置等
    console.log('用户的浏览器：', req.headers['user-agent']);
    console.log('用户接受的内容类型：', req.headers['accept']);
    
    res.end('已收到你的信息');
});
```

### 3.3 实用的req信息获取


```javascript
const server = http.createServer((req, res) => {
    // 获取用户信息的实用方法
    const userInfo = {
        访问页面: req.url,
        访问方式: req.method,
        用户浏览器: req.headers['user-agent'],
        访问时间: new Date().toLocaleString()
    };
    
    console.log('用户访问信息：', userInfo);
    res.end('访问记录已保存');
});
```

---

## 4. 📤 响应对象res详解


### 4.1 res是什么


**简单理解**：res就像"回复单"，用来告诉用户你给他什么内容

```javascript
const server = http.createServer((req, res) => {
    // res 用来给用户发送回复
    res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
    res.end('<h1>你好！这是服务器的回复</h1>');
});
```

### 4.2 res.writeHead() - 设置响应头


**🔸 状态码的含义**：
```javascript
// 状态码就像"办事结果通知"
res.writeHead(200); // 200 = 成功办完了
res.writeHead(404); // 404 = 找不到你要的东西  
res.writeHead(500); // 500 = 服务器出错了
```

**常用状态码记忆**：
- **200**：成功了 ✅
- **404**：找不到 ❌  
- **500**：服务器坏了 💥
- **301/302**：页面搬家了 📦

**🔸 设置内容类型**：
```javascript
// Content-Type 告诉浏览器你发的是什么类型的内容
res.writeHead(200, {
    'Content-Type': 'text/html; charset=utf-8'        // HTML网页
});

res.writeHead(200, {
    'Content-Type': 'application/json; charset=utf-8'  // JSON数据
});

res.writeHead(200, {
    'Content-Type': 'text/plain; charset=utf-8'       // 纯文本
});
```

### 4.3 res.setHeader() - 单独设置响应头


```javascript
const server = http.createServer((req, res) => {
    // 可以分别设置不同的头信息
    res.setHeader('Content-Type', 'text/html; charset=utf-8');
    res.setHeader('Server', 'My-Node-Server');
    res.setHeader('Access-Control-Allow-Origin', '*'); // 允许跨域
    
    res.end('<h1>设置了多个响应头</h1>');
});
```

### 4.4 res.end() - 结束响应


```javascript
const server = http.createServer((req, res) => {
    // res.end() 就像"对话结束"，必须调用
    res.writeHead(200, {'Content-Type': 'text/plain; charset=utf-8'});
    res.end('对话结束，再见！'); // 发送内容并结束响应
});
```

> ⚠️ **重要提醒**：每个请求都必须调用 `res.end()`，否则浏览器会一直等待

---

## 5. 🔄 处理不同HTTP方法


### 5.1 HTTP方法的实际应用


```
生活场景类比：

GET请求    = 去图书馆借书（只看不改）
POST请求   = 去邮局寄包裹（提交新内容）
PUT请求    = 去银行更新账户信息（修改现有内容）
DELETE请求 = 去商店退货（删除内容）
```

### 5.2 处理不同方法的代码


```javascript
const server = http.createServer((req, res) => {
    // 根据不同的请求方法执行不同操作
    if (req.method === 'GET') {
        // 处理GET请求 - 用户想要获取数据
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
        res.end('<h2>这是GET请求的响应</h2><p>用户想要查看内容</p>');
        
    } else if (req.method === 'POST') {
        // 处理POST请求 - 用户想要提交数据
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
        res.end('<h2>这是POST请求的响应</h2><p>用户提交了数据</p>');
        
    } else if (req.method === 'PUT') {
        // 处理PUT请求 - 用户想要更新数据
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
        res.end('<h2>这是PUT请求的响应</h2><p>用户更新了数据</p>');
        
    } else if (req.method === 'DELETE') {
        // 处理DELETE请求 - 用户想要删除数据
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
        res.end('<h2>这是DELETE请求的响应</h2><p>用户删除了数据</p>');
        
    } else {
        // 处理不支持的请求方法
        res.writeHead(405, {'Content-Type': 'text/plain; charset=utf-8'});
        res.end('不支持的请求方法');
    }
});
```

### 5.3 实际应用示例


```javascript
const server = http.createServer((req, res) => {
    const url = req.url;
    const method = req.method;
    
    // 用户管理API示例
    if (url === '/users' && method === 'GET') {
        // 获取用户列表
        res.writeHead(200, {'Content-Type': 'application/json'});
        res.end(JSON.stringify({message: '返回用户列表'}));
        
    } else if (url === '/users' && method === 'POST') {
        // 创建新用户
        res.writeHead(200, {'Content-Type': 'application/json'});
        res.end(JSON.stringify({message: '创建新用户成功'}));
        
    } else if (url.startsWith('/users/') && method === 'PUT') {
        // 更新指定用户
        res.writeHead(200, {'Content-Type': 'application/json'});
        res.end(JSON.stringify({message: '更新用户成功'}));
        
    } else if (url.startsWith('/users/') && method === 'DELETE') {
        // 删除指定用户
        res.writeHead(200, {'Content-Type': 'application/json'});
        res.end(JSON.stringify({message: '删除用户成功'}));
        
    } else {
        // 404错误
        res.writeHead(404, {'Content-Type': 'text/plain; charset=utf-8'});
        res.end('页面不存在');
    }
});
```

---

## 6. 🔗 URL解析和参数获取


### 6.1 什么是URL参数


**简单理解**：URL参数就像"订单详情"，告诉服务器具体要什么

```
URL示例解析：
http://localhost:3000/search?keyword=手机&price=1000

分解：
- /search         = 要访问的页面（路径）
- ?               = 参数开始的标志
- keyword=手机    = 搜索关键词参数
- &               = 参数分隔符  
- price=1000      = 价格参数
```

### 6.2 使用url模块解析参数


```javascript
const http = require('http');
const url = require('url'); // 引入url模块来解析URL

const server = http.createServer((req, res) => {
    // 解析URL和参数
    const parsedUrl = url.parse(req.url, true);
    const pathname = parsedUrl.pathname; // 获取路径
    const query = parsedUrl.query;       // 获取参数对象
    
    console.log('访问路径：', pathname);
    console.log('参数对象：', query);
    
    // 根据路径和参数处理请求
    if (pathname === '/search') {
        const keyword = query.keyword || '无';
        const price = query.price || '无';
        
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
        res.end(`
            <h2>搜索结果</h2>
            <p>搜索关键词：${keyword}</p>
            <p>价格范围：${price}</p>
        `);
    } else {
        res.writeHead(404, {'Content-Type': 'text/plain; charset=utf-8'});
        res.end('页面不存在');
    }
});

server.listen(3000, () => {
    console.log('服务器启动！测试地址：http://localhost:3000/search?keyword=手机&price=1000');
});
```

### 6.3 更实用的参数处理


```javascript
const server = http.createServer((req, res) => {
    const parsedUrl = url.parse(req.url, true);
    const pathname = parsedUrl.pathname;
    const query = parsedUrl.query;
    
    if (pathname === '/user') {
        // 获取用户信息 - 示例：/user?id=123&name=张三
        const userId = query.id;
        const userName = query.name;
        
        if (!userId) {
            res.writeHead(400, {'Content-Type': 'text/plain; charset=utf-8'});
            res.end('缺少用户ID参数');
            return;
        }
        
        res.writeHead(200, {'Content-Type': 'application/json; charset=utf-8'});
        res.end(JSON.stringify({
            id: userId,
            name: userName || '未知用户',
            message: '获取用户信息成功'
        }));
        
    } else if (pathname === '/product') {
        // 商品搜索 - 示例：/product?category=手机&minPrice=1000&maxPrice=5000
        const category = query.category || '全部';
        const minPrice = parseInt(query.minPrice) || 0;
        const maxPrice = parseInt(query.maxPrice) || 999999;
        
        res.writeHead(200, {'Content-Type': 'application/json; charset=utf-8'});
        res.end(JSON.stringify({
            searchCondition: {
                category: category,
                priceRange: `${minPrice} - ${maxPrice}元`
            },
            message: '搜索条件已接收'
        }));
        
    } else {
        res.writeHead(404, {'Content-Type': 'text/plain; charset=utf-8'});
        res.end('页面不存在');
    }
});
```

---

## 7. 🛣️ 简单路由实现


### 7.1 什么是路由


**简单理解**：路由就像"导航地图"，告诉服务器用户想去哪里，应该给什么内容

```
路由示例：

用户访问 /home     → 显示首页
用户访问 /about    → 显示关于页面  
用户访问 /contact  → 显示联系方式
用户访问 /api/data → 返回JSON数据
```

### 7.2 基础路由实现


```javascript
const http = require('http');
const url = require('url');

const server = http.createServer((req, res) => {
    const pathname = url.parse(req.url).pathname;
    
    // 设置通用响应头
    res.setHeader('Content-Type', 'text/html; charset=utf-8');
    
    // 路由分发
    switch(pathname) {
        case '/':
        case '/home':
            res.writeHead(200);
            res.end(`
                <h1>欢迎来到首页</h1>
                <nav>
                    <a href="/about">关于我们</a> | 
                    <a href="/contact">联系我们</a> | 
                    <a href="/api/users">用户API</a>
                </nav>
            `);
            break;
            
        case '/about':
            res.writeHead(200);
            res.end(`
                <h1>关于我们</h1>
                <p>这是一个Node.js学习项目</p>
                <a href="/">返回首页</a>
            `);
            break;
            
        case '/contact':
            res.writeHead(200);
            res.end(`
                <h1>联系我们</h1>
                <p>邮箱：contact@example.com</p>
                <p>电话：123-456-7890</p>
                <a href="/">返回首页</a>
            `);
            break;
            
        case '/api/users':
            // API路由返回JSON数据
            res.setHeader('Content-Type', 'application/json; charset=utf-8');
            res.writeHead(200);
            res.end(JSON.stringify({
                users: [
                    {id: 1, name: '张三', age: 25},
                    {id: 2, name: '李四', age: 30}
                ],
                total: 2
            }));
            break;
            
        default:
            // 404页面
            res.writeHead(404);
            res.end(`
                <h1>页面不存在 (404)</h1>
                <p>你访问的页面 ${pathname} 不存在</p>
                <a href="/">返回首页</a>
            `);
    }
});

server.listen(3000, () => {
    console.log('服务器启动！访问 http://localhost:3000');
});
```

### 7.3 带参数的动态路由


```javascript
const server = http.createServer((req, res) => {
    const parsedUrl = url.parse(req.url, true);
    const pathname = parsedUrl.pathname;
    const query = parsedUrl.query;
    
    // 动态路由匹配
    if (pathname.startsWith('/user/')) {
        // 提取用户ID：/user/123 → userId = 123
        const userId = pathname.split('/')[2];
        
        res.setHeader('Content-Type', 'application/json; charset=utf-8');
        res.writeHead(200);
        res.end(JSON.stringify({
            userId: userId,
            userInfo: `这是用户${userId}的信息`,
            timestamp: new Date().toISOString()
        }));
        
    } else if (pathname.startsWith('/product/')) {
        // 提取商品ID：/product/456 → productId = 456
        const productId = pathname.split('/')[2];
        
        res.setHeader('Content-Type', 'application/json; charset=utf-8');
        res.writeHead(200);
        res.end(JSON.stringify({
            productId: productId,
            productName: `商品${productId}`,
            price: Math.floor(Math.random() * 1000) + 100 // 随机价格
        }));
        
    } else {
        // 其他路由处理...
        res.writeHead(404, {'Content-Type': 'text/plain; charset=utf-8'});
        res.end('页面不存在');
    }
});
```

---

## 8. 📋 返回不同类型响应


### 8.1 常见响应类型


**响应类型就像"包装盒"，不同内容需要不同包装**：

```
HTML响应  = 网页内容（给人看的）
JSON响应  = 数据内容（给程序用的）
文件响应  = 图片、文档等（下载用的）
文本响应  = 纯文字（简单信息）
```

### 8.2 HTML响应


```javascript
const server = http.createServer((req, res) => {
    if (req.url === '/page') {
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
        res.end(`
            <!DOCTYPE html>
            <html>
            <head>
                <title>我的网页</title>
                <style>
                    body { font-family: Arial, sans-serif; margin: 40px; }
                    h1 { color: #333; }
                </style>
            </head>
            <body>
                <h1>欢迎访问我的网站</h1>
                <p>这是一个完整的HTML页面</p>
                <button onclick="alert('Hello!')">点击我</button>
            </body>
            </html>
        `);
    }
});
```

### 8.3 JSON响应（API数据）


```javascript
const server = http.createServer((req, res) => {
    if (req.url === '/api/data') {
        // 模拟数据库查询结果
        const data = {
            success: true,
            message: '数据获取成功',
            data: [
                {id: 1, name: '苹果', price: 5.5},
                {id: 2, name: '香蕉', price: 3.2},
                {id: 3, name: '橙子', price: 4.8}
            ],
            timestamp: new Date().toISOString()
        };
        
        res.writeHead(200, {'Content-Type': 'application/json; charset=utf-8'});
        res.end(JSON.stringify(data, null, 2)); // null, 2 让JSON格式更好看
    }
});
```

### 8.4 文件响应


```javascript
const fs = require('fs');
const path = require('path');

const server = http.createServer((req, res) => {
    if (req.url === '/download') {
        // 读取文件并返回
        const filePath = path.join(__dirname, 'example.txt');
        
        fs.readFile(filePath, (err, data) => {
            if (err) {
                res.writeHead(404, {'Content-Type': 'text/plain; charset=utf-8'});
                res.end('文件不存在');
                return;
            }
            
            // 设置文件下载头
            res.writeHead(200, {
                'Content-Type': 'application/octet-stream',
                'Content-Disposition': 'attachment; filename="example.txt"'
            });
            res.end(data);
        });
    }
});
```

### 8.5 图片响应


```javascript
const server = http.createServer((req, res) => {
    if (req.url === '/image') {
        const imagePath = path.join(__dirname, 'photo.jpg');
        
        fs.readFile(imagePath, (err, data) => {
            if (err) {
                res.writeHead(404, {'Content-Type': 'text/plain'});
                res.end('图片不存在');
                return;
            }
            
            res.writeHead(200, {'Content-Type': 'image/jpeg'});
            res.end(data);
        });
    }
});
```

---

## 9. 📦 处理请求体数据


### 9.1 什么是请求体


**简单理解**：请求体就像"包裹里的东西"，用户通过POST请求发送的数据

```
GET请求：数据在URL里（地址栏能看到）
POST请求：数据在请求体里（隐藏在"包裹"中）

例如用户提交表单：
- 用户名：张三
- 密码：123456
这些数据就在请求体里
```

### 9.2 读取POST数据


```javascript
const server = http.createServer((req, res) => {
    if (req.method === 'POST' && req.url === '/submit') {
        let body = '';
        
        // 监听数据接收事件（数据可能分多次传输）
        req.on('data', chunk => {
            body += chunk.toString(); // 将数据拼接起来
        });
        
        // 监听数据接收完成事件
        req.on('end', () => {
            console.log('接收到的数据：', body);
            
            res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
            res.end(`
                <h2>数据提交成功</h2>
                <p>你提交的数据是：${body}</p>
            `);
        });
        
    } else if (req.method === 'GET' && req.url === '/form') {
        // 显示提交表单的页面
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
        res.end(`
            <form method="POST" action="/submit">
                <p>姓名：<input type="text" name="name"></p>
                <p>邮箱：<input type="email" name="email"></p>
                <button type="submit">提交</button>
            </form>
        `);
    }
});
```

### 9.3 处理JSON数据


```javascript
const server = http.createServer((req, res) => {
    if (req.method === 'POST' && req.url === '/api/user') {
        let body = '';
        
        req.on('data', chunk => {
            body += chunk.toString();
        });
        
        req.on('end', () => {
            try {
                // 解析JSON数据
                const userData = JSON.parse(body);
                console.log('用户数据：', userData);
                
                // 简单验证
                if (!userData.name || !userData.email) {
                    res.writeHead(400, {'Content-Type': 'application/json'});
                    res.end(JSON.stringify({
                        success: false,
                        message: '姓名和邮箱都是必填的'
                    }));
                    return;
                }
                
                // 模拟保存数据
                const newUser = {
                    id: Date.now(), // 简单的ID生成
                    name: userData.name,
                    email: userData.email,
                    createTime: new Date().toISOString()
                };
                
                res.writeHead(200, {'Content-Type': 'application/json'});
                res.end(JSON.stringify({
                    success: true,
                    message: '用户创建成功',
                    user: newUser
                }));
                
            } catch (error) {
                res.writeHead(400, {'Content-Type': 'application/json'});
                res.end(JSON.stringify({
                    success: false,
                    message: 'JSON格式错误'
                }));
            }
        });
    }
});
```

---

## 10. 🚀 高级特性与最佳实践


### 10.1 流式响应处理


**什么是流式响应**：就像"边做边给"，不用等全部做完再一次性给用户

```javascript
const server = http.createServer((req, res) => {
    if (req.url === '/stream') {
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
        
        // 流式发送数据
        res.write('<h1>数据加载中...</h1>');
        
        let count = 0;
        const timer = setInterval(() => {
            count++;
            res.write(`<p>第${count}条数据已加载</p>`);
            
            if (count >= 5) {
                res.write('<p><strong>所有数据加载完成！</strong></p>');
                res.end(); // 结束响应
                clearInterval(timer);
            }
        }, 1000); // 每秒发送一条数据
    }
});
```

### 10.2 长连接和Keep-Alive


```javascript
const server = http.createServer((req, res) => {
    // 启用长连接
    res.setHeader('Connection', 'keep-alive');
    res.setHeader('Keep-Alive', 'timeout=60'); // 60秒超时
    
    res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
    res.end('这个连接会保持一段时间，提高后续请求效率');
});

// 设置服务器超时时间
server.setTimeout(60000); // 60秒
```

### 10.3 基本错误处理


```javascript
const server = http.createServer((req, res) => {
    try {
        // 你的正常业务逻辑
        if (req.url === '/error-test') {
            throw new Error('这是一个测试错误');
        }
        
        res.writeHead(200, {'Content-Type': 'text/plain; charset=utf-8'});
        res.end('正常响应');
        
    } catch (error) {
        console.error('服务器错误：', error.message);
        
        res.writeHead(500, {'Content-Type': 'text/html; charset=utf-8'});
        res.end(`
            <h1>服务器内部错误</h1>
            <p>抱歉，服务器遇到了问题</p>
            <p>错误信息：${error.message}</p>
        `);
    }
});

// 处理未捕获的错误
server.on('error', (error) => {
    console.error('服务器启动错误：', error);
});
```

### 10.4 HTTPS服务器基础


```javascript
const https = require('https');
const fs = require('fs');

// HTTPS需要SSL证书（开发环境可以用自签名证书）
const options = {
    key: fs.readFileSync('private-key.pem'),   // 私钥文件
    cert: fs.readFileSync('certificate.pem')   // 证书文件
};

const server = https.createServer(options, (req, res) => {
    res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
    res.end('<h1>这是安全的HTTPS连接</h1>');
});

server.listen(443, () => {
    console.log('HTTPS服务器启动在 https://localhost:443');
});
```

> 💡 **新手提示**：HTTPS需要SSL证书，在学习阶段可以先用HTTP，实际项目部署时再配置HTTPS

---

## 11. 📋 核心要点总结


### 11.1 必须掌握的核心概念


```
🔸 HTTP模块：Node.js内置的Web服务器核心
🔸 createServer()：创建服务器的方法
🔸 req对象：包含用户请求的所有信息（url、method、headers）
🔸 res对象：用于给用户发送响应的工具
🔸 listen()：让服务器监听端口，开始工作
🔸 HTTP方法：GET（获取）、POST（提交）、PUT（更新）、DELETE（删除）
🔸 状态码：200（成功）、404（不存在）、500（服务器错误）
🔸 Content-Type：告诉浏览器内容类型（HTML、JSON、图片等）
```

### 11.2 关键理解要点


**🔹 服务器工作流程**：
```
用户发送请求 → 服务器接收(req) → 处理业务逻辑 → 发送响应(res) → 完成
```

**🔹 请求响应的对应关系**：
```
每个请求(req)都必须有对应的响应(res.end())
就像有问必答，不能让用户一直等待
```

**🔹 URL和路由的概念**：
```
URL = 地址（用户想去哪）
路由 = 导航（告诉服务器该给什么）
参数 = 详细要求（具体想要什么）
```

### 11.3 实际应用价值


**🎯 Web服务器开发**：
- 创建网站后端API
- 处理用户登录、注册等功能
- 提供数据给前端页面

**🎯 文件服务器**：
- 提供图片、文档下载
- 搭建简单的静态网站
- 共享文件资源

**🎯 API接口服务**：
- 为移动APP提供数据接口
- 与其他系统进行数据交换
- 微服务架构的基础

### 11.4 学习建议


**🔸 循序渐进的练习**：
1. 先创建最简单的"Hello World"服务器
2. 然后学会处理不同的URL路径
3. 接着处理不同的HTTP方法
4. 最后学会处理参数和请求体数据

**🔸 实践项目建议**：
- 个人博客系统（增删改查文章）
- 简单的用户管理系统
- 文件上传下载服务
- 待办事项API

**🔸 调试技巧**：
- 多用`console.log()`查看req的内容
- 用浏览器开发者工具查看网络请求
- 先用GET请求测试，再用POST请求
- 用Postman等工具测试API

**核心记忆口诀**：
- HTTP模块建服务，createServer是核心
- req是请求res响应，listen监听端口行
- 方法路径和参数，状态内容要设定
- 有问必答别忘记，res.end()来完成