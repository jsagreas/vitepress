---
title: 20、存储区操作
---
## 📚 目录

1. [存储区基本概念](#1-存储区基本概念)
2. [为什么需要存储区](#2-为什么需要存储区)
3. [存储工作现场](#3-存储工作现场)
4. [查看和管理存储](#4-查看和管理存储)
5. [恢复存储内容](#5-恢复存储内容)
6. [存储区高级操作](#6-存储区高级操作)
7. [实际应用场景](#7-实际应用场景)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🗃️ 存储区基本概念


### 1.1 什么是Git存储区


**🔸 存储区的本质**
```
Git存储区(Stash)就像一个临时的保险箱
把你当前正在做的工作暂时"收起来"
让工作区变得干净，可以去做其他事情
需要的时候再把工作"拿出来"继续
```

**💡 生活中的类比**
```
就像你在书桌上写作业：
📝 正在写数学作业（当前工作）
📞 突然老师叫你去办公室
📦 你把作业收进抽屉里（stash）
🚶 去办公室处理事情（切换分支/拉取代码）
📝 回来后从抽屉拿出作业继续写（恢复stash）
```

### 1.2 存储区的工作原理


**🔧 存储机制图解**
```
工作区状态：                     存储后状态：
┌─────────────────┐             ┌─────────────────┐
│  修改的文件     │             │  干净的工作区   │
│  ├─ file1.txt   │    stash    │  ├─ file1.txt   │
│  ├─ file2.js    │   ────────> │  ├─ file2.js    │
│  └─ file3.css   │             │  └─ file3.css   │
└─────────────────┘             └─────────────────┘
                                         │
                                         ▼
                                ┌─────────────────┐
                                │   存储区(Stash) │
                                │  📦 stash@{0}   │
                                │     保存的修改   │
                                └─────────────────┘
```

### 1.3 存储区与其他区域的关系


**📊 Git区域关系图**
```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   工作区     │    │   暂存区     │    │   本地仓库   │    │   存储区     │
│ (Working)   │    │  (Staging)  │    │ (Repository)│    │   (Stash)   │
│             │    │             │    │             │    │             │
│ file1.txt   │────│ file1.txt   │────│   commit    │    │  临时保存   │
│ file2.js    │    │ file2.js    │    │   history   │    │   修改内容   │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
       │                                                        ▲
       └────────────── stash save ──────────────────────────────┘
```

---

## 2. 🤔 为什么需要存储区


### 2.1 常见的实际场景


**🎯 紧急任务场景**
```
情况：你正在开发新功能，写了一半代码
问题：突然有紧急bug需要立即修复
困难：当前代码还没完成，不想提交
解决：使用stash临时保存，处理完紧急任务再恢复
```

**🔄 分支切换场景**
```
情况：在feature分支开发，代码修改了一部分
需求：需要切换到master分支查看或修改代码
问题：Git不允许带着未提交的修改切换分支
解决：先stash保存修改，切换分支，完成后再恢复
```

### 2.2 不使用存储区的后果


**❌ 强制提交的问题**
```
# 不好的做法
git add .
git commit -m "临时提交，稍后修改"  # 污染提交历史

# 后果：
× 提交历史变得混乱
× 需要额外的revert或reset操作
× 团队协作时造成困扰
```

**❌ 不切换分支的问题**
```
# 问题场景
- 紧急bug无法及时修复
- 错过重要的代码review时间
- 影响团队协作效率
```

### 2.3 存储区的优势


**✅ 保持工作流程灵活**
- 🔸 **无需强制提交**：避免无意义的提交记录
- 🔸 **快速切换任务**：在不同工作之间自由切换
- 🔸 **保护未完成工作**：不会丢失当前的修改内容
- 🔸 **维护代码质量**：确保每次提交都是完整功能

---

## 3. 💾 存储工作现场


### 3.1 基本存储操作


#### 🎯 快速存储：git stash


**命令格式**
```bash
git stash
```

**🔸 操作效果**
```
执行前：
工作区：有修改的文件
暂存区：可能有已add的文件

执行后：
工作区：恢复到干净状态
暂存区：被清空
存储区：新增一个stash条目
```

**💡 实际操作示例**
```bash
# 当前状态：修改了几个文件
$ git status
On branch feature-login
Changes not staged for commit:
  modified:   src/login.js
  modified:   src/style.css

# 快速存储
$ git stash
Saved working directory and index state WIP on feature-login: a1b2c3d Add login form

# 检查状态：工作区变干净了
$ git status
On branch feature-login
nothing to commit, working tree clean
```

#### 📝 带消息存储：git stash save


**命令格式**
```bash
git stash save "存储说明信息"
# 或者使用新语法
git stash push -m "存储说明信息"
```

**🔸 为什么要写说明**
```
没有说明的stash：
stash@{0}: WIP on feature-login: a1b2c3d Add login form

有说明的stash：
stash@{0}: On feature-login: 登录功能开发到一半，还需要添加验证
```

**💡 实际应用示例**
```bash
# 为存储添加详细说明
$ git stash save "登录页面样式调整，完成了50%，还需要适配移动端"
Saved working directory and index state On feature-login: 登录页面样式调整，完成了50%，还需要适配移动端

# 或使用新语法
$ git stash push -m "修复用户头像显示bug，已定位问题但未完成修复"
```

### 3.2 选择性存储


#### 🎯 只存储工作区：保留暂存区


**命令格式**
```bash
git stash --keep-index
```

**📊 操作对比**
```
普通stash：                     --keep-index：
┌─────────────┐               ┌─────────────┐
│   工作区     │ ──────存储──────> │   工作区     │
│ file1.txt   │               │    干净      │
│ file2.js    │               └─────────────┘
└─────────────┘               ┌─────────────┐
┌─────────────┐               │   暂存区     │
│   暂存区     │ ──────存储──────> │ file1.txt   │ ← 保留
│ file1.txt   │               │             │
└─────────────┘               └─────────────┘
```

#### 📁 只存储特定文件


**命令格式**
```bash
git stash push -m "说明" -- 文件路径
```

**💡 使用场景**
```bash
# 只存储特定文件
$ git stash push -m "暂存登录页面修改" -- src/login.js src/login.css

# 存储多个指定文件
$ git stash push -m "API接口调整" -- src/api/ src/utils/request.js
```

### 3.3 存储未跟踪文件


**🔸 默认行为**
```
git stash 默认只存储：
✅ 已跟踪的修改文件
✅ 已添加到暂存区的文件
❌ 新建的未跟踪文件（被忽略）
```

**🔧 包含未跟踪文件**
```bash
git stash -u
# 或
git stash --include-untracked
```

**💡 实际示例**
```bash
# 查看当前状态
$ git status
On branch feature
Changes not staged for commit:
  modified:   existing-file.js
Untracked files:
  new-file.js
  temp-config.json

# 只存储已跟踪文件
$ git stash
# new-file.js 和 temp-config.json 仍然在工作区

# 存储包括未跟踪文件
$ git stash -u -m "包含新建文件的修改"
# 所有文件都被存储，工作区完全干净
```

---

## 4. 📋 查看和管理存储


### 4.1 查看存储列表


#### 📊 列出所有存储：git stash list


**命令格式**
```bash
git stash list
```

**🔸 输出格式解析**
```bash
$ git stash list
stash@{0}: On feature-login: 登录功能样式调整
stash@{1}: WIP on master: a1b2c3d Fix header bug  
stash@{2}: On feature-api: API接口重构进行中

格式说明：
stash@{索引}: On 分支名: 提交信息或自定义说明
├─ 索引：0是最新的，数字越大越旧
├─ 分支：存储时所在的分支
└─ 说明：自动生成或自定义的说明
```

#### 🔍 查看存储详情：git stash show


**基本查看**
```bash
git stash show
# 默认显示最新存储(stash@{0})的文件统计

git stash show stash@{1}
# 显示指定存储的文件统计
```

**详细查看**
```bash
git stash show -p
# 显示详细的差异内容，类似于git diff

git stash show -p stash@{1}
# 显示指定存储的详细差异
```

**💡 实际输出示例**
```bash
# 文件统计信息
$ git stash show
 src/login.js    | 15 ++++++++++++---
 src/style.css   |  8 ++++++--
 2 files changed, 18 insertions(+), 5 deletions(-)

# 详细差异内容
$ git stash show -p
diff --git a/src/login.js b/src/login.js
index 1234567..abcdefg 100644
--- a/src/login.js
+++ b/src/login.js
$$ -10,6 +10,12 $$ function validateLogin() {
+    // 添加的新代码
+    if (!username.trim()) {
+        showError('用户名不能为空');
+        return false;
+    }
```

### 4.2 存储管理策略


**🏷️ 存储命名规范**
```bash
# 推荐的命名方式
git stash save "功能模块-具体内容-进度"

# 实际示例
git stash save "登录页面-表单验证功能-已完成基础验证"
git stash save "用户中心-头像上传-修复了预览问题待测试"
git stash save "首页优化-轮播图组件-重构中断需紧急修bug"
```

**📅 存储整理建议**
```
定期清理原则：
🔸 超过一周的stash及时处理
🔸 已合并到主分支的功能相关stash可删除
🔸 实验性修改的stash要么应用要么删除
🔸 保持stash列表简洁明了
```

---

## 5. 🔄 恢复存储内容


### 5.1 应用并删除：git stash pop


**🔸 pop操作的含义**
```
pop = apply + drop
即：应用存储内容 + 删除该存储记录
类似于栈的弹出操作，"取出"存储
```

**命令格式**
```bash
git stash pop              # 应用最新存储(stash@{0})
git stash pop stash@{1}    # 应用指定存储
```

**💡 成功应用的示例**
```bash
# 应用最新存储
$ git stash pop
On branch feature-login
Changes not staged for commit:
  modified:   src/login.js
  modified:   src/style.css
Dropped refs/stash@{0} (a1b2c3d...)

# 结果：
✅ 存储的修改被恢复到工作区
✅ stash@{0}被自动删除
✅ 其他存储的索引向前移动
```

### 5.2 仅应用不删除：git stash apply


**🔸 apply操作的特点**
```
apply = 只应用内容，保留存储记录
适合需要在多个分支使用相同修改的情况
```

**命令格式**
```bash
git stash apply            # 应用最新存储
git stash apply stash@{2}  # 应用指定存储
```

**💡 使用场景示例**
```bash
# 场景：需要在多个分支应用相同的配置修改
$ git checkout feature-a
$ git stash apply stash@{0}  # 应用配置修改

$ git checkout feature-b  
$ git stash apply stash@{0}  # 在另一个分支也应用相同修改

# stash@{0}仍然存在，可以继续在其他分支使用
```

### 5.3 处理恢复冲突


**🚨 什么时候会产生冲突**
```
存储时的代码：     当前代码：        结果：
file.js line 10    file.js line 10   
- old content      - different content  ⚠️ 冲突！
```

**🔧 冲突解决流程**
```bash
# 1. 尝试应用存储
$ git stash pop
Auto-merging src/login.js
CONFLICT (content): Merge conflict in src/login.js

# 2. 查看冲突文件
$ git status
On branch feature-login
Unmerged paths:
  both modified:   src/login.js

# 3. 手动解决冲突（编辑文件，选择保留的内容）
# 4. 添加解决后的文件
$ git add src/login.js

# 5. 继续（如果使用的是pop，存储会被自动删除）
```

**⚡ 冲突预防技巧**
```
🔸 存储前确保当前分支是最新的
🔸 尽快应用存储，避免代码差异过大
🔸 使用apply而不是pop来保留存储备份
🔸 在干净的分支上应用存储
```

---

## 6. 🧹 存储区高级操作


### 6.1 删除存储


#### 🗑️ 删除单个存储：git stash drop


**命令格式**
```bash
git stash drop              # 删除最新存储
git stash drop stash@{2}    # 删除指定存储
```

**💡 使用示例**
```bash
# 查看当前存储
$ git stash list
stash@{0}: On feature: 已完成的功能修改
stash@{1}: On master: 实验性修改
stash@{2}: On feature: 过时的修改

# 删除过时的存储
$ git stash drop stash@{2}
Dropped stash@{2} (c3d4e5f...)

# 再次查看：索引会重新排列
$ git stash list
stash@{0}: On feature: 已完成的功能修改
stash@{1}: On master: 实验性修改
```

#### 🧽 清空所有存储：git stash clear


**命令格式**
```bash
git stash clear
```

**⚠️ 注意事项**
```
这个操作会删除所有存储且无法恢复！
建议在执行前：
1. 用git stash list确认要删除的内容
2. 对重要的存储先用git stash show -p备份
3. 考虑是否真的需要全部删除
```

### 6.2 存储重命名技巧


**🔸 Git本身不支持直接重命名stash**
但可以通过组合操作实现：

```bash
# 方法：apply + save + drop
# 1. 应用要重命名的存储
git stash apply stash@{1}

# 2. 用新名称重新保存
git stash save "新的更清晰的描述"

# 3. 删除旧的存储
git stash drop stash@{2}  # 注意索引可能变化
```

### 6.3 存储分支操作


**🌿 从存储创建分支**
```bash
git stash branch 新分支名 stash@{0}
```

**💡 使用场景**
```
当存储的修改比较重要，想要：
🔸 单独开一个分支来完善这些修改
🔸 避免在当前分支产生冲突
🔸 更好地管理实验性功能
```

**实际操作示例**
```bash
# 从存储创建新分支
$ git stash branch experimental-feature stash@{0}
Switched to a new branch 'experimental-feature'
On branch experimental-feature
Changes not staged for commit:
  modified:   src/experiment.js

# 结果：
✅ 创建并切换到新分支
✅ 存储内容被应用到新分支
✅ 原存储被删除
```

---

## 7. 🎯 实际应用场景


### 7.1 紧急Bug修复工作流


**📋 完整操作流程**
```bash
# 场景：正在开发新功能，突然需要修复紧急bug

# 1. 保存当前工作
$ git stash save "新功能开发进行中-完成了登录验证部分"

# 2. 切换到主分支
$ git checkout master

# 3. 创建bug修复分支
$ git checkout -b hotfix-login-error

# 4. 修复bug并提交
$ git add .
$ git commit -m "Fix: 修复登录时密码验证错误"

# 5. 合并到主分支
$ git checkout master
$ git merge hotfix-login-error

# 6. 回到原来的功能分支
$ git checkout feature-new-login

# 7. 恢复之前的工作
$ git stash pop
```

### 7.2 多任务并行开发


**🔄 任务切换模式**
```
任务A（进行中）→ stash → 任务B → 完成 → 恢复任务A
```

**实际示例**
```bash
# 正在开发任务A
$ git stash save "任务A-用户注册功能-已完成表单设计"

# 切换到任务B
$ git checkout feature-task-b
# 完成任务B...

# 回到任务A继续开发
$ git checkout feature-task-a
$ git stash pop
```

### 7.3 代码实验与回滚


**🧪 安全实验模式**
```bash
# 1. 保存当前稳定状态
$ git stash save "稳定版本-准备尝试新的实现方案"

# 2. 进行实验性修改
# ... 各种尝试 ...

# 3. 如果实验失败，快速回滚
$ git reset --hard HEAD
$ git stash pop  # 恢复到实验前状态

# 4. 如果实验成功，清理存储
$ git stash drop
```

### 7.4 团队协作中的存储使用


**👥 Pull Request工作流**
```bash
# 1. 保存本地修改
$ git stash save "功能开发中-等待上游更新"

# 2. 拉取最新代码
$ git pull origin master

# 3. 处理可能的合并冲突后，恢复工作
$ git stash pop

# 4. 继续开发并提交
```

**🔄 代码Review流程**
```bash
# 准备提交review时，临时保存未完成的后续工作
$ git stash save "下一阶段工作-等当前PR通过后继续"

# 完善当前功能并提交PR
$ git add .
$ git commit -m "完成功能X的核心实现"

# PR通过后继续后续工作
$ git stash pop
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心命令


```bash
🔸 git stash              # 快速存储当前修改
🔸 git stash save "说明"  # 带说明的存储
🔸 git stash list         # 查看所有存储
🔸 git stash pop          # 应用并删除最新存储
🔸 git stash apply        # 仅应用存储内容
🔸 git stash drop         # 删除指定存储
🔸 git stash clear        # 清空所有存储
🔸 git stash show         # 查看存储内容
```

### 8.2 关键理解要点


**🔹 存储区的本质**
```
存储区就是一个临时保险箱：
✅ 临时保存：不是永久存储方案
✅ 工作现场：保存完整的修改状态
✅ 快速切换：支持任务间快速切换
✅ 恢复能力：可以随时恢复到存储状态
```

**🔹 pop vs apply的选择**
```
使用pop的情况：
- 确定这个存储只用一次
- 希望自动清理存储列表
- 正常的工作流程恢复

使用apply的情况：
- 需要在多个分支使用相同修改
- 不确定是否还需要这个存储
- 作为备份保留存储
```

**🔹 存储的最佳实践**
```
命名规范：
✅ 使用描述性的存储消息
✅ 包含功能模块和完成进度
✅ 注明预计的处理时间

管理策略：
✅ 定期清理过期的存储
✅ 避免存储过多内容
✅ 重要修改要及时提交而不是长期存储
```

### 8.3 常见使用误区


**❌ 错误用法**
```
× 把stash当作备份工具长期保存
× 存储后忘记处理，积累过多stash
× 在不干净的工作区应用stash导致冲突
× 误用clear命令丢失重要修改
```

**✅ 正确做法**
```
✓ 把stash作为短期的任务切换工具
✓ 及时处理存储，保持列表简洁
✓ 应用stash前确保工作区干净
✓ 重要修改优先提交而不是存储
```

### 8.4 工作流程建议


**🚀 高效存储工作流**
```
1. 工作中断时：
   git stash save "功能-内容-进度"

2. 处理紧急任务：
   切换分支 → 处理问题 → 回到原分支

3. 恢复工作：
   git stash pop  (确认无冲突)

4. 定期清理：
   git stash list → 检查过期存储 → 及时清理
```

**💡 记忆口诀**
```
临时保存用stash，工作切换不慌张
save带上说明词，list查看很方便
pop取出就删除，apply保留可重用
冲突处理要小心，清理及时保整洁
```

**核心理念**：Git存储区是你的工作助手，帮你在多任务间灵活切换，但不要依赖它做长期存储。保持简洁、及时处理、合理命名是使用存储区的三大原则。