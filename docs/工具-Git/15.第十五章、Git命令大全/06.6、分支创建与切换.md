---
title: 6、分支创建与切换
---
## 📚 目录

1. [分支是什么？为什么需要分支？](#1-分支是什么为什么需要分支)
2. [查看分支状态](#2-查看分支状态)
3. [创建新分支](#3-创建新分支)
4. [切换分支操作](#4-切换分支操作)
5. [一步到位：创建并切换](#5-一步到位创建并切换)
6. [新版本命令对比](#6-新版本命令对比)
7. [实际工作场景应用](#7-实际工作场景应用)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🌳 分支是什么？为什么需要分支？


### 1.1 什么是Git分支


**🔸 简单理解**  
分支就像是**平行宇宙**，你可以在不同的"宇宙"里独立开发，互不干扰。

```
想象你在写一本小说：

主线剧情（主分支main）：男主角走向成功
分支剧情1（功能分支feature1）：如果男主角失败会怎样？
分支剧情2（功能分支feature2）：如果换个女主角会怎样？

每个分支独立发展，最后可以选择合并或丢弃
```

**🔸 技术角度**  
分支是指向某个提交(commit)的**可移动指针**，让你可以在同一个项目中同时进行多条开发线。

### 1.2 为什么需要分支


**💡 现实工作场景**

```
🏢 公司项目开发：
主分支(main)     ：稳定的线上版本
开发分支(develop)：正在开发的新功能
功能分支(feature)：某个具体功能开发
修复分支(hotfix) ：紧急修复线上问题

👥 团队协作：
小明负责用户登录功能 → feature/login分支
小红负责商品管理功能 → feature/product分支
小李修复支付bug    → hotfix/payment分支

各自在自己分支开发，完成后合并到主分支
```

### 1.3 分支的核心优势


| 优势 | **说明** | **实际效果** |
|------|---------|-------------|
| 🔸 **隔离开发** | `不同功能独立开发` | `不会相互影响，减少冲突` |
| 🔸 **并行工作** | `多人同时开发不同功能` | `提高团队开发效率` |
| 🔸 **版本控制** | `随时切换到不同版本` | `快速回退或前进` |
| 🔸 **实验安全** | `在分支上试验新想法` | `失败了直接删除分支` |

---

## 2. 👀 查看分支状态


### 2.1 查看本地分支列表


**命令：`git branch`**

```bash
$ git branch
  develop
* main
  feature/login
```

**🔍 输出解读**：
- `*` 星号表示**当前所在分支**
- 其他行显示**本地所有分支**
- 当前在 `main` 分支上

### 2.2 查看所有分支（包含远程）


**命令：`git branch -a`**

```bash
$ git branch -a
  develop
* main
  feature/login
  remotes/origin/main
  remotes/origin/develop
  remotes/origin/feature/payment
```

**🔍 解读说明**：
- **本地分支**：develop, main, feature/login
- **远程分支**：remotes/origin/xxx（远程仓库的分支）
- `origin` 是远程仓库的默认名称

### 2.3 只查看远程分支


**命令：`git branch -r`**

```bash
$ git branch -r
  origin/main
  origin/develop
  origin/feature/payment
```

> 💡 **新手提示**  
> `-r` 是 `--remotes` 的简写，只显示远程仓库的分支。远程分支是存在服务器上的分支副本。

### 2.4 查看分支详细信息


**命令：`git branch -v`**

```bash
$ git branch -v
  develop       a1b2c3d 添加用户注册功能
* main          d4e5f6g 修复登录bug
  feature/login h7i8j9k 完成登录界面
```

**🔍 信息说明**：
- **分支名** + **最新提交ID** + **提交消息**
- 帮助你快速了解各分支的最新状态

---

## 3. 🆕 创建新分支


### 3.1 基础创建分支


**命令：`git branch <分支名>`**

```bash
# 创建一个名为 feature/user-profile 的新分支

$ git branch feature/user-profile

# 查看分支列表确认

$ git branch
  feature/user-profile
* main
```

**🔸 重要理解**：
- 只是**创建**分支，**不会自动切换**
- 新分支基于**当前分支**创建
- 新分支包含当前分支的**所有历史记录**

### 3.2 基于特定提交创建分支


**命令：`git branch <分支名> <提交ID>`**

```bash
# 基于特定提交创建分支

$ git branch feature/rollback a1b2c3d

# 基于标签创建分支

$ git branch hotfix/urgent v1.2.0
```

**🎯 使用场景**：
- **回滚功能**：基于历史版本创建修复分支
- **版本维护**：基于发布版本创建维护分支

### 3.3 分支命名规范建议


```
🏷️ 常见命名规范：

功能开发：
feature/user-login      ✅ 清晰明了
feature/payment-system  ✅ 描述具体功能
new-feature            ❌ 太模糊

bug修复：
hotfix/login-error     ✅ 明确问题
bugfix/payment-fail    ✅ 描述错误
fix                    ❌ 不具体

实验性：
experiment/new-ui      ✅ 实验性质
test/performance       ✅ 测试目的
temp                   ❌ 临时性不明确
```

> ⚠️ **命名注意事项**  
> - 使用小写字母和连字符
> - 避免空格和特殊字符
> - 保持简洁但有意义

---

## 4. 🔄 切换分支操作


### 4.1 使用 checkout 切换分支


**命令：`git checkout <分支名>`**

```bash
# 切换到 develop 分支

$ git checkout develop
Switched to branch 'develop'

# 确认当前分支

$ git branch
* develop
  main
  feature/user-profile
```

**🔸 切换过程**：
1. **保存当前工作**：确保工作区干净
2. **更新文件**：将文件更新为目标分支的状态
3. **移动HEAD**：HEAD指针指向新分支

### 4.2 切换分支的前置条件


**工作区必须干净**：

```bash
# 情况1：有未提交的修改

$ git checkout develop
error: Your local changes to the following files would be overwritten by checkout:
    README.md
Please commit your changes or stash them before you switch branches.

# 解决方案1：提交修改

$ git add .
$ git commit -m "保存当前修改"
$ git checkout develop

# 解决方案2：暂存修改

$ git stash
$ git checkout develop
$ git stash pop  # 切换后恢复修改
```

### 4.3 切换到远程分支


**场景**：团队成员创建了新分支，你需要切换过去

```bash
# 先获取最新的远程分支信息

$ git fetch

# 方法1：基于远程分支创建本地分支并切换

$ git checkout -b feature/payment origin/feature/payment

# 方法2：如果分支名相同，可以简化

$ git checkout feature/payment
```

**🔍 工作原理**：
```
远程仓库                本地仓库
origin/feature/payment → feature/payment
(远程分支)               (本地分支)
```

---

## 5. ⚡ 一步到位：创建并切换


### 5.1 使用 checkout -b


**命令：`git checkout -b <新分支名>`**

```bash
# 创建并切换到新分支（一步完成）

$ git checkout -b feature/shopping-cart
Switched to a new branch 'feature/shopping-cart'

# 相当于执行了：

# git branch feature/shopping-cart

# git checkout feature/shopping-cart

```

**🎯 实际工作流程**：
```bash
# 开发新功能的典型流程

$ git checkout main              # 1. 切换到主分支
$ git pull                       # 2. 拉取最新代码
$ git checkout -b feature/search # 3. 创建并切换到功能分支
# 现在可以开始开发搜索功能了

```

### 5.2 基于特定分支创建


```bash
# 基于 develop 分支创建并切换到新分支

$ git checkout -b feature/user-auth develop

# 基于远程分支创建并切换

$ git checkout -b feature/payment origin/develop
```

### 5.3 常见使用场景


**🔸 日常开发场景**：

```bash
# 场景1：开发新功能

$ git checkout main
$ git checkout -b feature/email-notification

# 场景2：修复紧急bug

$ git checkout main
$ git checkout -b hotfix/critical-security-fix

# 场景3：实验新想法

$ git checkout develop
$ git checkout -b experiment/new-algorithm
```

---

## 6. 🆙 新版本命令对比


### 6.1 新命令 switch 介绍


Git 2.23+ 引入了更明确的命令，让操作更清晰：

**旧命令问题**：
- `git checkout` 功能太多：切换分支、恢复文件、创建分支
- 新手容易混淆，不知道具体做什么

**新命令优势**：
- `git switch`：专门用于分支切换
- `git restore`：专门用于文件恢复
- 功能更专一，更好理解

### 6.2 switch 基础用法


**命令：`git switch <分支名>`**

```bash
# 切换到现有分支

$ git switch main
Switched to branch 'main'

# 切换到 develop 分支

$ git switch develop
Switched to branch 'develop'
```

### 6.3 switch 创建并切换


**命令：`git switch -c <新分支名>`**

```bash
# 创建并切换到新分支

$ git switch -c feature/notification
Switched to a new branch 'feature/notification'

# 基于特定分支创建

$ git switch -c hotfix/urgent main
```

### 6.4 新旧命令对比表


| 操作 | **旧命令 (checkout)** | **新命令 (switch)** | **推荐** |
|------|---------------------|-------------------|---------|
| 🔸 **切换分支** | `git checkout main` | `git switch main` | `✅ 新命令` |
| 🔸 **创建并切换** | `git checkout -b new` | `git switch -c new` | `✅ 新命令` |
| 🔸 **切换远程分支** | `git checkout -b local origin/remote` | `git switch -c local origin/remote` | `✅ 新命令` |

> 💡 **学习建议**  
> 新手建议学习新命令 `git switch`，语义更清晰。老命令 `git checkout` 仍然有效，但功能过于复杂。

### 6.5 switch 的额外功能


```bash
# 切换到上一个分支（类似 cd -）

$ git switch -

# 从远程分支创建本地分支

$ git switch -c local-name origin/remote-name

# 强制切换（丢弃本地修改）

$ git switch --force main
```

---

## 7. 🎯 实际工作场景应用


### 7.1 典型开发工作流


**🔸 新功能开发流程**：

```bash
# 第1步：确保主分支是最新的

$ git switch main
$ git pull origin main

# 第2步：创建功能分支

$ git switch -c feature/user-dashboard

# 第3步：开发过程中

# ... 编写代码 ...

$ git add .
$ git commit -m "添加用户仪表板基本布局"

# ... 继续开发 ...

$ git add .
$ git commit -m "实现数据图表显示"

# 第4步：功能完成，准备合并

$ git switch main
$ git pull origin main
$ git merge feature/user-dashboard
```

### 7.2 团队协作场景


**🔸 多人协作开发**：

```
团队协作分支策略：

main分支      ←─── 稳定的生产代码
   ↑
develop分支   ←─── 集成开发分支
   ↑    ↑
   │    └─ feature/payment   (小明开发)
   │
   └─────── feature/search   (小红开发)
```

**实际操作**：
```bash
# 小明开发支付功能

$ git switch develop
$ git switch -c feature/payment
# ... 开发支付功能 ...


# 小红开发搜索功能

$ git switch develop  
$ git switch -c feature/search
# ... 开发搜索功能 ...


# 功能完成后都合并到 develop

# 测试通过后，develop 合并到 main

```

### 7.3 紧急修复场景


**🔸 线上bug紧急修复**：

```bash
# 发现线上有紧急bug需要修复

$ git switch main          # 切换到稳定版本
$ git switch -c hotfix/critical-login-bug

# 快速修复bug

# ... 修改代码 ...

$ git add .
$ git commit -m "修复登录验证逻辑错误"

# 修复完成，合并到main并部署

$ git switch main
$ git merge hotfix/critical-login-bug
$ git push origin main

# 同时合并到develop，避免bug重现

$ git switch develop
$ git merge hotfix/critical-login-bug
```

### 7.4 实验性开发


**🔸 试验新技术或方案**：

```bash
# 试验新的UI框架

$ git switch -c experiment/new-ui-framework

# 尝试新的架构方案

$ git switch -c experiment/microservice-refactor

# 如果实验成功，可以合并

# 如果实验失败，直接删除分支

$ git branch -d experiment/failed-approach
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的命令


```bash
# 🔸 查看分支

git branch          # 查看本地分支
git branch -a       # 查看所有分支
git branch -r       # 查看远程分支

# 🔸 创建分支

git branch <name>   # 创建分支（不切换）

# 🔸 切换分支

git checkout <name> # 切换到现有分支
git switch <name>   # 新版切换命令

# 🔸 创建并切换

git checkout -b <name>  # 创建并切换（旧版）
git switch -c <name>    # 创建并切换（新版）
```

### 8.2 关键理解要点


**🔹 分支的本质**
- 分支是**指向提交的指针**，不是复制代码
- 创建分支**非常轻量**，几乎没有性能开销
- 切换分支会**改变工作区文件**

**🔹 工作区状态**
- 切换分支前要确保**工作区干净**
- 未提交的修改可能**阻止分支切换**
- 使用 `git stash` 临时保存未完成的工作

**🔹 分支策略**
- **main/master**：稳定的生产代码
- **develop**：开发集成分支
- **feature/xxx**：具体功能开发
- **hotfix/xxx**：紧急修复

### 8.3 最佳实践建议


**🎯 分支命名**
```
✅ 推荐：
feature/user-authentication
hotfix/payment-bug
experiment/new-search-algorithm

❌ 避免：
branch1, temp, test, fix
```

**🎯 工作流程**
```
1. 从最新的主分支创建功能分支
2. 在功能分支上完成开发
3. 定期提交代码到功能分支
4. 功能完成后合并到主分支
5. 删除已合并的功能分支
```

**🎯 团队协作**
```
- 功能分支保持专一性（一个分支一个功能）
- 定期同步主分支的最新更改
- 使用有意义的提交消息
- 代码review后再合并到主分支
```

### 8.4 新手避坑指南


> ⚠️ **常见错误1：工作区不干净时切换分支**  
> 解决：先提交或stash保存当前修改

> ⚠️ **常见错误2：分支名混乱没有规范**  
> 解决：采用统一的命名约定

> ⚠️ **常见错误3：在错误的分支上开发**  
> 解决：开发前先确认当前分支 `git branch`

> ⚠️ **常见错误4：忘记拉取最新代码就创建分支**  
> 解决：创建分支前先 `git pull`

**🧠 记忆口诀**：
- 分支如平行宇宙，隔离开发不冲突
- 创建分支用branch，切换分支用switch  
- 一步到位加-c，查看状态用-a
- 工作区净才能切，命名规范要清晰