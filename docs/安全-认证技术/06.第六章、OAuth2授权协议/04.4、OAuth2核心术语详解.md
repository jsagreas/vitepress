---
title: 4、OAuth2核心术语详解
---
## 📚 目录

1. [OAuth2核心术语概览](#1-OAuth2核心术语概览)
2. [Authorization Grant - 授权许可](#2-Authorization-Grant-授权许可)
3. [Access Token - 访问令牌](#3-Access-Token-访问令牌)
4. [Refresh Token - 刷新令牌](#4-Refresh-Token-刷新令牌)
5. [Scope - 授权范围](#5-Scope-授权范围)
6. [State - 状态参数](#6-State-状态参数)
7. [Redirect URI - 重定向URI](#7-Redirect-URI-重定向URI)
8. [Client ID & Client Secret - 客户端凭证](#8-Client-ID-Client-Secret-客户端凭证)
9. [Bearer Token - 承载令牌](#9-Bearer-Token-承载令牌)
10. [术语关系总结](#10-术语关系总结)

---

## 1. 🌟 OAuth2核心术语概览


### 1.1 什么是OAuth2术语

**简单理解**：OAuth2的专业名词就像是**酒店服务系统**的各种凭证和规则。

```
生活化类比 - 酒店入住系统：
🏨 酒店前台    = Authorization Server（授权服务器）
🔑 房卡        = Access Token（访问令牌）
📋 登记表      = Authorization Grant（授权许可）
🚪 房间权限    = Scope（授权范围）
👤 客人身份    = Client ID（客户端ID）
🔐 房间密码    = Client Secret（客户端密钥）
```

### 1.2 为什么要学这些术语

> 💡 **学习重点**：理解每个术语的**实际含义**，而不是死记硬背定义

**核心作用**：
- 🎯 **理解OAuth2流程** - 知道每一步在做什么
- 🔧 **实际开发应用** - 能正确使用各种参数
- 🐛 **问题排查定位** - 快速找到出错环节
- 📚 **技术文档理解** - 看懂官方文档说明

---

## 2. 📋 Authorization Grant - 授权许可


### 2.1 Authorization Grant是什么


**通俗解释**：就像你去银行取钱时的**"取款凭证"**，证明你有权限做某件事。

```
生活化类比：
👤 你想从银行取钱
📋 需要填写取款单（Authorization Grant）
🏦 银行验证取款单有效
💰 银行给你现金（Access Token）

取款单就是你的"授权许可"
```

### 2.2 Authorization Grant的作用


**核心含义**：
- 🔸 **证明身份合法** - 我确实是这个账户的主人
- 🔸 **说明要做什么** - 我要访问哪些资源
- 🔸 **获取访问权限** - 用来换取真正的访问令牌

### 2.3 Authorization Grant的类型


| 类型 | **生活化比喻** | **实际场景** | **特点** |
|------|-------------|-------------|----------|
| 🔐 **授权码模式** | `银行柜台取款` | `网站第三方登录` | `最安全，需要多步验证` |
| 🔑 **密码模式** | `ATM直接取款` | `自家APP登录` | `简单直接，需要密码` |
| 📱 **简化模式** | `手机银行转账` | `单页面应用` | `快速便捷，安全性中等` |
| 🏢 **客户端模式** | `企业批量转账` | `服务器间调用` | `无用户参与，自动化` |

### 2.4 代码示例


```javascript
// 获取授权码（Authorization Grant）
const authUrl = `https://auth.example.com/oauth/authorize?
  response_type=code&           // 我要授权码类型
  client_id=your_app_id&        // 我的应用身份
  scope=read_profile&           // 我要读取用户资料权限
  redirect_uri=https://yourapp.com/callback`;  // 成功后跳转地址

// 用授权码换取访问令牌
const tokenResponse = await fetch('https://auth.example.com/oauth/token', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    grant_type: 'authorization_code',  // 使用授权码换令牌
    code: 'received_auth_code',        // 刚才获得的授权码
    client_id: 'your_app_id',
    client_secret: 'your_app_secret'
  })
});
```

---

## 3. 🔑 Access Token - 访问令牌


### 3.1 Access Token是什么


**通俗解释**：就像你的**"银行卡"**，有了它就能直接使用银行服务。

```
生活化类比：
🏨 酒店房卡    = Access Token
🚪 刷卡开门    = 使用Token访问API
⏰ 房卡有效期  = Token过期时间
🔒 房卡权限    = Token包含的权限范围

拿着房卡，你就能进入被授权的房间
```

### 3.2 Access Token的特点


**核心特征**：
- ⏱️ **有时效性** - 通常1-2小时就过期
- 🎯 **权限明确** - 只能做被授权的事情  
- 🔒 **相对安全** - 即使泄露影响也有限
- 📱 **使用简单** - 带上token就能访问API

### 3.3 Access Token的使用方式


```javascript
// 在API请求中使用Access Token
const userProfile = await fetch('https://api.example.com/user/profile', {
  headers: {
    'Authorization': 'Bearer your_access_token_here'  // 标准用法
  }
});

// Token通常是这样的格式
const accessToken = {
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "Bearer",                    // 令牌类型
  "expires_in": 3600,                       // 1小时后过期
  "scope": "read_profile read_posts"        // 权限范围
};
```

### 3.4 Token过期怎么办


> ⚠️ **重要提醒**：Access Token过期是正常的，这是安全设计！

```
Token过期处理流程：
1️⃣ 发现API返回401未授权错误
2️⃣ 检查是否因为Token过期
3️⃣ 使用Refresh Token获取新的Access Token
4️⃣ 重新发起原来的API请求
```

---

## 4. 🔄 Refresh Token - 刷新令牌


### 4.1 Refresh Token是什么


**通俗解释**：就像你的**"身份证"**，当临时卡片过期时，用它可以重新办理新卡片。

```
生活化类比：
🆔 身份证        = Refresh Token（长期有效）
🎫 临时通行证    = Access Token（短期有效）
🏢 办证大厅      = Authorization Server

临时通行证过期了，拿身份证可以重新办理新的
```

### 4.2 为什么需要Refresh Token


**设计思路**：
- 🔐 **提高安全性** - Access Token短期过期，降低泄露风险
- ⚡ **提升用户体验** - 无需重复登录，自动续期
- 🎯 **权限控制** - 可以随时撤销长期权限

### 4.3 Refresh Token的使用


```javascript
// 当Access Token过期时，使用Refresh Token获取新token
async function refreshAccessToken(refreshToken) {
  const response = await fetch('https://auth.example.com/oauth/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      grant_type: 'refresh_token',      // 使用刷新令牌模式
      refresh_token: refreshToken,      // 提供刷新令牌
      client_id: 'your_app_id',
      client_secret: 'your_app_secret'
    })
  });
  
  const newTokens = await response.json();
  // 通常会返回新的access_token，有时也会返回新的refresh_token
  return newTokens;
}

// 实际使用示例
try {
  const apiData = await callAPI(accessToken);
} catch (error) {
  if (error.status === 401) {  // Token过期
    const newTokens = await refreshAccessToken(refreshToken);
    const apiData = await callAPI(newTokens.access_token);  // 重试
  }
}
```

### 4.4 Refresh Token的安全考虑


> 🛡️ **安全提醒**：Refresh Token更重要，一定要妥善保存！

**存储建议**：
- ✅ **服务器端**：存储在数据库中，加密保存
- ✅ **移动APP**：存储在系统钥匙串中
- ❌ **浏览器**：不要存储在localStorage中
- ❌ **明文传输**：传输时必须使用HTTPS

---

## 5. 🎯 Scope - 授权范围


### 5.1 Scope是什么


**通俗解释**：就像给保姆的**"工作清单"**，明确规定她可以做哪些事情。

```
生活化类比：
🏠 雇佣保姆      = 授权第三方应用
📝 工作清单      = Scope授权范围
✅ 可以打扫卫生  = read_profile（读取资料）
✅ 可以做饭      = write_posts（发布内容）
❌ 不能碰保险柜  = admin_access（管理权限）

明确的工作范围，保护主人隐私安全
```

### 5.2 常见的Scope类型


```javascript
// 典型的Scope示例
const commonScopes = {
  // 基础权限
  'read_profile': '读取用户基本资料',
  'read_email': '获取用户邮箱地址',
  
  // 内容权限  
  'read_posts': '查看用户发布的内容',
  'write_posts': '代替用户发布内容',
  
  // 社交权限
  'read_friends': '查看好友列表',
  'read_messages': '读取私信',
  
  // 高级权限
  'admin': '管理员权限（危险）'
};

// 请求多个权限
const requestUrl = `https://auth.example.com/oauth/authorize?
  client_id=your_app&
  scope=read_profile read_email write_posts&  // 空格分隔多个权限
  response_type=code`;
```

### 5.3 Scope的最佳实践


> 💡 **重要原则**：**最小权限原则** - 只申请真正需要的权限

**申请策略**：
- 🎯 **按需申请** - 功能需要什么权限就申请什么
- ⏰ **渐进式申请** - 开始申请基础权限，后续再申请高级权限
- 📝 **权限说明** - 向用户解释为什么需要这些权限

```javascript
// ❌ 错误做法：申请过多权限
const badScope = 'read_profile read_email read_posts write_posts read_friends admin';

// ✅ 正确做法：按功能分步申请
const goodScope = 'read_profile';  // 登录只需要基本资料
// 需要发布功能时再申请：'write_posts'
// 需要导入好友时再申请：'read_friends'
```

---

## 6. 🛡️ State - 状态参数


### 6.1 State是什么


**通俗解释**：就像你去银行办事时的**"排队号码"**，防止别人冒充你的办事结果。

```
生活化类比：
🎫 银行排队号    = State参数
👤 你拿着A123号  = 发送state=random_string_xyz
🏦 银行叫A123号  = 授权服务器返回相同的state
✅ 号码对得上    = 确认是你的授权结果
❌ 号码对不上    = 可能有人伪造结果

防止有人拿假的办事结果来骗你
```

### 6.2 State的安全作用


**防护目标**：
- 🚫 **CSRF攻击防护** - 防止跨站请求伪造
- 🔍 **请求匹配验证** - 确认返回结果对应你的请求
- 🎯 **状态维持** - 记录用户原本要做什么

### 6.3 State的使用方法


```javascript
// 1. 生成随机state参数
function generateState() {
  return Math.random().toString(36).substring(2, 15) + 
         Math.random().toString(36).substring(2, 15);
}

// 2. 发起授权请求时带上state
const state = generateState();
sessionStorage.setItem('oauth_state', state);  // 记住state值

const authUrl = `https://auth.example.com/oauth/authorize?
  client_id=your_app&
  response_type=code&
  scope=read_profile&
  state=${state}&                              // 带上随机state
  redirect_uri=https://yourapp.com/callback`;

// 3. 在回调页面验证state
function handleCallback(urlParams) {
  const returnedState = urlParams.get('state');
  const savedState = sessionStorage.getItem('oauth_state');
  
  if (returnedState !== savedState) {
    throw new Error('State参数不匹配，可能存在安全风险！');
  }
  
  // 验证通过，继续处理授权码...
  const authCode = urlParams.get('code');
}
```

### 6.4 State的实际应用场景


```javascript
// 场景1：记录用户原始意图
const state = JSON.stringify({
  action: 'login',
  redirect_to: '/dashboard',
  timestamp: Date.now()
});

// 场景2：多平台授权时的平台识别
const state = btoa(JSON.stringify({
  platform: 'wechat',      // 微信登录
  device: 'mobile'         // 移动设备
}));

// 回调时解析state
function parseState(stateStr) {
  try {
    return JSON.parse(atob(stateStr));
  } catch {
    return JSON.parse(stateStr);
  }
}
```

---

## 7. 🔗 Redirect URI - 重定向URI


### 7.1 Redirect URI是什么


**通俗解释**：就像你给快递员的**"收货地址"**，告诉授权服务器成功后把结果送到哪里。

```
生活化类比：
📦 网购下单        = 发起OAuth授权请求
📍 填写收货地址    = 设置Redirect URI
🚚 快递送货上门    = 授权服务器跳转回应用
📋 签收包裹        = 应用接收授权码

地址写错了，快递就送不到你手里
```

### 7.2 Redirect URI的作用


**核心功能**：
- 🎯 **接收授权结果** - 用户同意/拒绝授权后的跳转地址
- 🔐 **安全防护** - 防止授权码被发送到恶意网站
- 📱 **多端支持** - 支持网页、移动应用等不同平台

### 7.3 不同平台的Redirect URI


```javascript
// 网页应用
const webRedirectUri = 'https://yourapp.com/oauth/callback';

// 移动应用（自定义scheme）
const mobileRedirectUri = 'yourapp://oauth/callback';

// 本地开发
const devRedirectUri = 'http://localhost:3000/auth/callback';

// 桌面应用
const desktopRedirectUri = 'http://127.0.0.1:8080/callback';
```

### 7.4 回调处理示例


```javascript
// 回调页面的处理逻辑
function handleOAuthCallback() {
  const urlParams = new URLSearchParams(window.location.search);
  
  // 检查是否有错误
  const error = urlParams.get('error');
  if (error) {
    console.error('授权失败:', error);
    alert('登录失败: ' + urlParams.get('error_description'));
    return;
  }
  
  // 获取授权码
  const authCode = urlParams.get('code');
  const state = urlParams.get('state');
  
  if (!authCode) {
    console.error('未获取到授权码');
    return;
  }
  
  // 验证state参数（防CSRF）
  const savedState = sessionStorage.getItem('oauth_state');
  if (state !== savedState) {
    console.error('State参数不匹配');
    return;
  }
  
  // 用授权码换取访问令牌
  exchangeCodeForToken(authCode);
}

// 页面加载时自动处理回调
window.addEventListener('load', handleOAuthCallback);
```

### 7.5 Redirect URI的安全要求


> 🔒 **安全规范**：Redirect URI必须提前注册，不能随意修改！

**安全考虑**：
- ✅ **HTTPS协议** - 生产环境必须使用HTTPS
- ✅ **精确匹配** - 授权服务器会严格验证URI
- ✅ **白名单机制** - 只允许预先注册的URI
- ❌ **通配符** - 不支持*.example.com这样的通配符

---

## 8. 🆔 Client ID & Client Secret - 客户端凭证


### 8.1 什么是Client ID和Client Secret


**通俗解释**：就像你的**"银行卡号"**和**"密码"**，用来证明你的应用身份。

```
生活化类比：
🏪 开店申请       = 应用注册
🆔 营业执照号     = Client ID（公开身份标识）
🔐 保险柜密码     = Client Secret（私密认证密钥）
🏦 银行认证       = 授权服务器验证
💰 开通收款功能   = 获得OAuth权限

营业执照号可以公开，保险柜密码绝对保密
```

### 8.2 Client ID - 客户端标识


**特点说明**：
- 🌍 **公开信息** - 可以出现在前端代码中
- 🎯 **身份标识** - 告诉授权服务器"我是谁"
- 📱 **多端统一** - 同一应用在不同平台可用同一个ID
- 🔍 **用户可见** - 用户在授权页面能看到应用名称

```javascript
// Client ID的典型使用
const clientId = 'your_app_12345';  // 这个可以放在前端

const authUrl = `https://auth.example.com/oauth/authorize?
  client_id=${clientId}&              // 公开的应用标识
  response_type=code&
  scope=read_profile`;
```

### 8.3 Client Secret - 客户端密钥


**特点说明**：
- 🔒 **绝对保密** - 只能存在服务器端，不能泄露
- 🛡️ **安全认证** - 证明你确实是这个应用的拥有者
- ⚠️ **高度敏感** - 泄露后可能被恶意利用
- 🔄 **可以重置** - 怀疑泄露时可以申请新的密钥

```javascript
// ❌ 错误：不能把Secret放在前端
const clientSecret = 'your_secret_abc123';  // 危险！会泄露

// ✅ 正确：Secret只在服务器端使用
// 后端代码 - 用授权码换取访问令牌
async function exchangeToken(authCode) {
  const response = await fetch('https://auth.example.com/oauth/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      grant_type: 'authorization_code',
      code: authCode,
      client_id: process.env.CLIENT_ID,        // 从环境变量读取
      client_secret: process.env.CLIENT_SECRET, // 从环境变量读取，保密
      redirect_uri: 'https://yourapp.com/callback'
    })
  });
  
  return response.json();
}
```

### 8.4 不同应用类型的安全处理


| 应用类型 | **Client Secret** | **安全处理方式** |
|---------|------------------|-----------------|
| 🌐 **网页应用** | `必须使用` | `服务器端处理，前端不接触` |
| 📱 **移动应用** | `可选/不推荐` | `使用PKCE等安全扩展` |
| 🖥️ **单页应用** | `无法使用` | `使用授权码+PKCE模式` |
| 🔧 **服务端应用** | `必须使用` | `客户端凭证模式` |

```javascript
// 单页应用（SPA）的安全做法 - 不使用Client Secret
const authUrl = `https://auth.example.com/oauth/authorize?
  client_id=spa_client_id&
  response_type=code&
  code_challenge=${codeChallenge}&      // PKCE安全扩展
  code_challenge_method=S256&
  scope=read_profile`;
```

---

## 9. 🎫 Bearer Token - 承载令牌


### 9.1 Bearer Token是什么


**通俗解释**：就像**"VIP通行证"**，谁拿着它谁就能享受相应的服务。

```
生活化类比：
🎫 演唱会门票     = Bearer Token
🚪 检票入场       = API验证Token  
🎤 观看演出       = 访问受保护资源
👮 保安验票       = 服务器验证Token有效性

只要门票真实有效，不管谁拿着都能进场
```

### 9.2 Bearer的含义


**"Bearer"翻译**：
- 📝 **英文含义** - Bearer = "持有者"、"承载者"
- 💡 **实际意思** - 谁持有这个Token，谁就有权限
- ⚠️ **安全特点** - Token本身就是凭证，不需要额外证明身份

### 9.3 Bearer Token的使用格式


```javascript
// 标准的Bearer Token使用方式
const apiCall = await fetch('https://api.example.com/user/profile', {
  headers: {
    'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
    //                ↑      ↑
    //             固定格式   实际的Token内容
  }
});

// Bearer Token的完整格式
Authorization: Bearer <token_value>
```

### 9.4 Bearer Token的安全考虑


> 🔐 **重要提醒**：Bearer Token就像现金，谁拿到谁就能用！

**安全措施**：
- 🔒 **HTTPS传输** - 必须使用加密传输
- ⏰ **设置过期时间** - Token不要永久有效
- 🚫 **避免存储在不安全地方** - 不要存localStorage
- 🔍 **服务端验证** - 每次使用都要验证有效性

```javascript
// ✅ 安全的Token使用示例
class TokenManager {
  constructor() {
    this.token = null;
    this.refreshToken = null;
  }
  
  // 安全存储Token
  setTokens(accessToken, refreshToken) {
    this.token = accessToken;
    this.refreshToken = refreshToken;
    // 生产环境建议存储在httpOnly cookie中
  }
  
  // 带Token的API请求
  async apiRequest(url, options = {}) {
    const response = await fetch(url, {
      ...options,
      headers: {
        'Authorization': `Bearer ${this.token}`,
        'Content-Type': 'application/json',
        ...options.headers
      }
    });
    
    // Token过期处理
    if (response.status === 401) {
      await this.refreshAccessToken();
      // 重试请求...
    }
    
    return response;
  }
}
```

### 9.5 其他Token类型对比


```javascript
// Bearer Token（最常用）
'Authorization': 'Bearer abc123'

// Basic Token（用户名密码）  
'Authorization': 'Basic dXNlcjpwYXNz'  // user:pass的base64编码

// API Key（简单应用）
'X-API-Key': 'your_api_key_here'

// JWT Token（Bearer的一种具体实现）
'Authorization': 'Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...'
```

---

## 10. 🔗 术语关系总结


### 10.1 OAuth2术语关系图


```
OAuth2授权流程中的术语关系：

👤 用户                    🏢 第三方应用                🔐 授权服务器
  |                        |                           |
  |--[1]点击登录----------->|                           |
  |                        |--[2]跳转到授权页--------->|
  |<-------[3]显示授权页面(带Client ID, Scope, State)----|
  |                        |                           |
  |--[4]同意授权----------->|                           |
  |                        |<--[5]返回Authorization Grant-|
  |                        |                           |
  |                        |--[6]用Grant换Token------->|
  |                        |   (需要Client Secret)     |
  |                        |<--[7]返回Access Token------|
  |                        |   (同时返回Refresh Token)  |
  |                        |                           |
  |<--[8]登录成功----------|                           |
  |                        |                           |
  |                        |--[9]用Bearer Token访问API->|
  |                        |<--[10]返回用户数据--------|

关键术语在流程中的作用：
🔸 Client ID & Secret: 证明应用身份[步骤2,6]
🔸 Scope: 定义权限范围[步骤2,3] 
🔸 State: 防CSRF攻击[步骤2,5]
🔸 Authorization Grant: 授权凭证[步骤5,6]
🔸 Access Token: 访问凭证[步骤7,9]
🔸 Refresh Token: 续期凭证[步骤7]
🔸 Redirect URI: 回调地址[步骤5]
🔸 Bearer Token: Token使用格式[步骤9]
```

### 10.2 术语重要程度排序


| 优先级 | **术语** | **重要程度** | **学习建议** |
|-------|---------|-------------|-------------|
| 🥇 **P0** | `Access Token` | ⭐⭐⭐⭐⭐ | `必须完全理解，最常用` |
| 🥇 **P0** | `Client ID/Secret` | ⭐⭐⭐⭐⭐ | `应用注册的基础，必会` |
| 🥈 **P1** | `Authorization Grant` | ⭐⭐⭐⭐ | `理解授权流程的关键` |
| 🥈 **P1** | `Scope` | ⭐⭐⭐⭐ | `权限控制的核心概念` |
| 🥈 **P1** | `Bearer Token` | ⭐⭐⭐⭐ | `Token使用的标准格式` |
| 🥉 **P2** | `Refresh Token` | ⭐⭐⭐ | `长期应用必需理解` |
| 🥉 **P2** | `Redirect URI` | ⭐⭐⭐ | `回调处理的基础` |
| 🔧 **P3** | `State` | ⭐⭐ | `安全防护，进阶内容` |

### 10.3 实际开发中的使用频率


```javascript
// 高频使用（每个OAuth项目都会用到）
const accessToken = 'eyJ...';           // ⭐⭐⭐⭐⭐ 每次API调用
const clientId = 'your_app_id';         // ⭐⭐⭐⭐⭐ 应用配置  
const scope = 'read_profile';           // ⭐⭐⭐⭐ 权限申请
const bearerToken = `Bearer ${token}`;  // ⭐⭐⭐⭐ API请求头

// 中频使用（大多数项目会用到）
const refreshToken = 'refresh_abc';     // ⭐⭐⭐ 长期应用
const redirectUri = 'https://app.com/callback';  // ⭐⭐⭐ 回调配置

// 低频使用（安全要求高的项目）
const state = generateRandomString();   // ⭐⭐ 安全防护
const clientSecret = process.env.SECRET; // ⭐⭐ 服务端应用
```

### 10.4 记忆口诀


> 💡 **OAuth2术语记忆口诀**：
> 
> 🎯 **Client ID身份证，Secret密码要保密**
> 🔑 **Grant是通行证，Token是钥匙**  
> 📝 **Scope定权限，State防攻击**
> 🔄 **Bearer拿就用，Refresh能续期**
> 🏠 **Redirect回家门，八个术语要记清**

### 10.5 学习建议


**阶段性学习路径**：

```
📚 第一阶段：理解基础概念
✅ Access Token - 什么是访问令牌
✅ Client ID - 应用身份标识  
✅ Scope - 权限范围概念

📚 第二阶段：掌握核心流程  
✅ Authorization Grant - 授权许可流程
✅ Bearer Token - 令牌使用格式
✅ Redirect URI - 回调处理

📚 第三阶段：学习高级特性
✅ Refresh Token - 令牌刷新机制
✅ Client Secret - 安全认证
✅ State - 安全防护参数
```

**实践建议**：
- 🔧 **动手实验** - 注册一个OAuth应用，实际体验各个术语
- 📖 **阅读文档** - 查看GitHub、Google等平台的OAuth文档  
- 🐛 **调试工具** - 使用浏览器开发者工具观察授权流程
- 💻 **编写代码** - 实现一个简单的OAuth客户端

---

> 🎉 **总结**：OAuth2的这8个核心术语就像是一套完整的**身份认证体系**。理解了它们的含义和关系，你就掌握了OAuth2的精髓！记住：**概念比代码更重要**，先理解"为什么"，再学习"怎么做"。