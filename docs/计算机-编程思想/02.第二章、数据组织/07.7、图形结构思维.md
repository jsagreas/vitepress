---
title: 7、图形结构思维
---
## 📚 目录

1. [顶点与边的关系](#1-顶点与边的关系)
2. [图的表示方法](#2-图的表示方法)
3. [有向图vs无向图](#3-有向图vs无向图)
4. [图的遍历算法](#4-图的遍历算法)
5. [最短路径问题](#5-最短路径问题)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔗 顶点与边的关系


### 1.1 什么是图形结构


**简单理解**：图形结构就像现实生活中的人际关系网络

```
朋友圈关系：
小明 - 小红 - 小李
 |        |
小王 - 小张 - 小赵

每个人是顶点(Vertex)，朋友关系是边(Edge)
```

**💡 核心概念**
- **顶点(Vertex/Node)**：图中的基本单位，就像网络中的节点
- **边(Edge)**：连接两个顶点的线，表示它们之间的关系
- **图(Graph)**：由顶点和边组成的整体结构

### 1.2 顶点与边的关系本质


**关系类型理解**：
```
现实例子                     图形表示
城市间的交通路线              顶点=城市，边=道路
网页间的链接关系              顶点=网页，边=超链接
社交网络的朋友关系            顶点=用户，边=好友关系
电路中的元件连接              顶点=元件，边=导线
```

**🔸 度的概念**
```
度(Degree) = 连接到某个顶点的边的数量

示例：
    A
   / \
  B   C
 /     \
D       E

顶点A的度：2 (连接B和C)
顶点B的度：2 (连接A和D)  
顶点C的度：2 (连接A和E)
顶点D的度：1 (只连接B)
顶点E的度：1 (只连接C)
```

### 1.3 图的基本特征


**🔹 与树的区别**
```
树的特点：
- 有明确的根节点
- 父子关系清晰
- 不能有环路

图的特点：  
- 没有固定的根节点
- 关系是平等的
- 可以有环路
- 更加灵活
```

**🔹 环路概念**
```
无环图：
A → B → C

有环图：
A → B
↑   ↓
D ← C

环路就是从一个点出发，能够回到自己的路径
```

---

## 2. 📊 图的表示方法


### 2.1 邻接矩阵表示法


**💡 基本概念**：用二维数组来表示图中顶点之间的连接关系

```
图的结构：
  A - B
  |   |
  C - D

顶点编号：A=0, B=1, C=2, D=3

邻接矩阵：
    A B C D
A [ 0 1 1 0 ]
B [ 1 0 0 1 ]  
C [ 1 0 0 1 ]
D [ 0 1 1 0 ]

解读：矩阵[i][j] = 1 表示顶点i和顶点j之间有边
```

**🔸 代码实现**
```java
// 邻接矩阵实现
class GraphMatrix {
    private int[][] matrix;
    private int vertexCount;
    
    public GraphMatrix(int count) {
        vertexCount = count;
        matrix = new int[count][count];
    }
    
    // 添加边
    public void addEdge(int from, int to) {
        matrix[from][to] = 1;
        matrix[to][from] = 1; // 无向图需要双向
    }
    
    // 检查是否有边
    public boolean hasEdge(int from, int to) {
        return matrix[from][to] == 1;
    }
}
```

**✅ 邻接矩阵优点**
- 查找两点间是否有边：O(1)时间
- 实现简单，容易理解
- 适合边比较密集的图

**❌ 邻接矩阵缺点**
- 空间占用：O(V²)，V是顶点数
- 稀疏图浪费空间
- 遍历某个顶点的邻居需要O(V)时间

### 2.2 邻接表表示法


**💡 基本概念**：每个顶点维护一个列表，记录它的邻居

```
同样的图：
  A - B
  |   |
  C - D

邻接表表示：
A: [B, C]
B: [A, D]
C: [A, D]  
D: [B, C]

每个顶点只记录实际连接的邻居
```

**🔸 代码实现**
```java
// 邻接表实现
class GraphList {
    private List<List<Integer>> adjList;
    private int vertexCount;
    
    public GraphList(int count) {
        vertexCount = count;
        adjList = new ArrayList<>();
        for (int i = 0; i < count; i++) {
            adjList.add(new ArrayList<>());
        }
    }
    
    // 添加边
    public void addEdge(int from, int to) {
        adjList.get(from).add(to);
        adjList.get(to).add(from); // 无向图需要双向
    }
    
    // 获取邻居列表
    public List<Integer> getNeighbors(int vertex) {
        return adjList.get(vertex);
    }
}
```

**✅ 邻接表优点**
- 空间效率：O(V+E)，E是边数
- 遍历邻居快速：O(度数)
- 适合稀疏图

**❌ 邻接表缺点**
- 检查两点间是否有边：O(度数)时间
- 实现稍微复杂

### 2.3 如何选择表示方法


**🎯 选择指南**
```
使用邻接矩阵的情况：
✅ 图比较密集（边数接近V²）
✅ 经常需要快速查询两点间是否有边
✅ 图的大小相对固定

使用邻接表的情况：
✅ 图比较稀疏（边数远小于V²）
✅ 经常需要遍历某个顶点的所有邻居
✅ 节省内存空间
✅ 大部分实际应用场景
```

**📊 性能对比表**
| 操作 | 邻接矩阵 | 邻接表 |
|------|----------|---------|
| **空间复杂度** | O(V²) | O(V+E) |
| **添加边** | O(1) | O(1) |
| **检查边存在** | O(1) | O(度数) |
| **遍历邻居** | O(V) | O(度数) |
| **删除边** | O(1) | O(度数) |

---

## 3. 🔄 有向图vs无向图


### 3.1 无向图：双向关系


**💡 基本概念**：边没有方向，关系是相互的

```
朋友关系（无向图）：
小明 ←→ 小红
小红 ←→ 小李
小明 ←→ 小李

如果A是B的朋友，那么B也是A的朋友
```

**🔸 无向图特点**
```
特征：
- 边是双向的
- 如果有边(A,B)，就一定有边(B,A)
- 适合表示对称关系

应用场景：
- 社交网络的好友关系
- 城市间的双向道路
- 网络通信中的双向连接
- 分子结构中的化学键
```

**🔸 代码实现要点**
```java
// 无向图添加边
public void addUndirectedEdge(int a, int b) {
    // 需要添加双向边
    matrix[a][b] = 1;
    matrix[b][a] = 1;
}
```

### 3.2 有向图：单向关系


**💡 基本概念**：边有明确的方向，关系是单向的

```
关注关系（有向图）：
小明 → 明星A
明星A ← 小红
小李 → 明星A

小明关注明星A，但明星A不一定关注小明
```

**🔸 有向图特点**
```
特征：
- 边是单向的
- 有边(A,B)不意味着有边(B,A)
- 适合表示非对称关系

应用场景：
- 网页间的链接关系
- 社交媒体的关注关系
- 工作流程的依赖关系
- 程序调用关系
```

**🔸 入度和出度概念**
```
有向图中的度分为两种：

出度(Out-degree)：从该顶点出发的边数
入度(In-degree)：指向该顶点的边数

示例：
A → B ← C
    ↓
    D

顶点B：入度=2(来自A和C)，出度=1(指向D)
```

### 3.3 应用场景对比


**📋 场景选择指南**
```
使用无向图：
🔸 朋友/好友关系
🔸 城市间的双向道路
🔸 网络设备间的连接
🔸 分子化学键连接
🔸 协作关系网络

使用有向图：
🔸 网页链接关系
🔸 社交媒体关注
🔸 邮件发送关系
🔸 程序函数调用
🔸 工作流程依赖
🔸 食物链关系
```

**🎯 判断标准**
```
问自己这个问题：
"如果A和B有关系，B和A一定也有同样的关系吗？"

如果答案是"是" → 用无向图
如果答案是"不一定" → 用有向图
```

---

## 4. 🚶 图的遍历算法


### 4.1 深度优先搜索(DFS)


**💡 基本思想**：就像走迷宫，沿着一条路走到底，走不通了再回头

```
图结构：
    A
   / \
  B   C
 /   / \
D   E   F

DFS遍历过程：
1. 从A开始
2. 选择B，继续深入
3. 到达D，没有更深的了
4. 回到B，B的其他路径已遍历
5. 回到A，选择C
6. 从C到E，再回到C
7. 从C到F
8. 完成遍历

遍历顺序：A → B → D → C → E → F
```

**🔸 DFS代码实现**
```java
public class DFS {
    private boolean[] visited;
    
    // 递归实现DFS
    public void dfs(GraphList graph, int vertex) {
        visited[vertex] = true;
        System.out.print(vertex + " ");
        
        // 遍历所有邻居
        for (int neighbor : graph.getNeighbors(vertex)) {
            if (!visited[neighbor]) {
                dfs(graph, neighbor);
            }
        }
    }
    
    // 栈实现DFS（非递归）
    public void dfsIterative(GraphList graph, int start) {
        Stack<Integer> stack = new Stack<>();
        boolean[] visited = new boolean[graph.getVertexCount()];
        
        stack.push(start);
        
        while (!stack.isEmpty()) {
            int vertex = stack.pop();
            
            if (!visited[vertex]) {
                visited[vertex] = true;
                System.out.print(vertex + " ");
                
                // 将邻居加入栈
                for (int neighbor : graph.getNeighbors(vertex)) {
                    if (!visited[neighbor]) {
                        stack.push(neighbor);
                    }
                }
            }
        }
    }
}
```

**🔸 DFS特点和应用**
```
特点：
- 使用栈的数据结构（递归本质上也是栈）
- 尽可能深地搜索
- 空间复杂度：O(V)
- 时间复杂度：O(V+E)

应用场景：
✅ 检测图中是否有环
✅ 拓扑排序
✅ 查找连通分量
✅ 求解迷宫问题
✅ 查找路径
```

### 4.2 广度优先搜索(BFS)


**💡 基本思想**：就像水波扩散，一层一层地向外搜索

```
同样的图：
    A
   / \
  B   C
 /   / \
D   E   F

BFS遍历过程：
1. 从A开始（第0层）
2. 遍历A的邻居B、C（第1层）
3. 遍历B的邻居D和C的邻居E、F（第2层）

遍历顺序：A → B → C → D → E → F
```

**🔸 BFS代码实现**
```java
public class BFS {
    
    // 队列实现BFS
    public void bfs(GraphList graph, int start) {
        Queue<Integer> queue = new LinkedList<>();
        boolean[] visited = new boolean[graph.getVertexCount()];
        
        queue.offer(start);
        visited[start] = true;
        
        while (!queue.isEmpty()) {
            int vertex = queue.poll();
            System.out.print(vertex + " ");
            
            // 将未访问的邻居加入队列
            for (int neighbor : graph.getNeighbors(vertex)) {
                if (!visited[neighbor]) {
                    visited[neighbor] = true;
                    queue.offer(neighbor);
                }
            }
        }
    }
    
    // 分层BFS（记录层数）
    public void bfsWithLevel(GraphList graph, int start) {
        Queue<Integer> queue = new LinkedList<>();
        boolean[] visited = new boolean[graph.getVertexCount()];
        int[] level = new int[graph.getVertexCount()];
        
        queue.offer(start);
        visited[start] = true;
        level[start] = 0;
        
        while (!queue.isEmpty()) {
            int vertex = queue.poll();
            System.out.println("顶点 " + vertex + "，层数：" + level[vertex]);
            
            for (int neighbor : graph.getNeighbors(vertex)) {
                if (!visited[neighbor]) {
                    visited[neighbor] = true;
                    level[neighbor] = level[vertex] + 1;
                    queue.offer(neighbor);
                }
            }
        }
    }
}
```

**🔸 BFS特点和应用**
```
特点：
- 使用队列的数据结构
- 按层次逐步搜索
- 空间复杂度：O(V)
- 时间复杂度：O(V+E)

应用场景：
✅ 查找最短路径（无权图）
✅ 层次遍历
✅ 查找连通分量
✅ 计算图的直径
✅ 社交网络中的"六度分离"
```

### 4.3 DFS vs BFS 对比


**📊 两种算法对比**
| 特征 | DFS | BFS |
|------|-----|-----|
| **数据结构** | 栈(Stack) | 队列(Queue) |
| **搜索策略** | 深度优先 | 广度优先 |
| **内存使用** | 较少 | 较多 |
| **最短路径** | 不保证 | 保证找到 |
| **实现方式** | 递归/栈 | 队列 |

**🎯 选择指南**
```
选择DFS的情况：
✅ 需要遍历所有可能的路径
✅ 内存空间有限
✅ 查找任意一条路径即可
✅ 拓扑排序

选择BFS的情况：
✅ 需要找最短路径
✅ 需要按层次处理
✅ 查找最近的目标
✅ 社交网络分析
```

---

## 5. 🛣️ 最短路径问题


### 5.1 什么是最短路径问题


**💡 生活中的例子**：就像用导航软件找最短的开车路线

```
城市图：
A ---5--- B
|         |
3         2
|         |  
C ---4--- D

从A到D的路径：
路径1：A → B → D，总距离 = 5 + 2 = 7
路径2：A → C → D，总距离 = 3 + 4 = 7  
路径3：A → C → B → D，总距离 = 3 + 4 + 2 = 9 (实际不存在C→B边)

最短路径：A → B → D 或 A → C → D（都是7）
```

**🔸 最短路径问题分类**
```
单源最短路径：
- 从一个顶点到所有其他顶点的最短路径
- 例：从你家到城市中所有地点的最短路线

所有顶点对最短路径：
- 任意两个顶点之间的最短路径
- 例：城市中任意两点间的最短距离

特定顶点对最短路径：
- 两个特定顶点之间的最短路径
- 例：从家到公司的最短路线
```

### 5.2 Dijkstra算法（单源最短路径）


**💡 基本思想**：就像一滴墨水在纸上扩散，总是先到达最近的地方

```
算法步骤（通俗理解）：
1. 从起点开始，标记距离为0
2. 选择当前未访问中距离最小的顶点
3. 更新它所有邻居的距离（如果通过它能得到更短距离）
4. 标记该顶点为已访问
5. 重复2-4步，直到所有顶点都被访问
```

**🔸 Dijkstra算法实现**
```java
public class Dijkstra {
    
    public int[] dijkstra(int[][] graph, int start) {
        int n = graph.length;
        int[] dist = new int[n];      // 距离数组
        boolean[] visited = new boolean[n]; // 访问标记
        
        // 初始化距离数组
        Arrays.fill(dist, Integer.MAX_VALUE);
        dist[start] = 0;
        
        for (int i = 0; i < n; i++) {
            // 找到未访问中距离最小的顶点
            int u = findMinDistance(dist, visited);
            visited[u] = true;
            
            // 更新邻居的距离
            for (int v = 0; v < n; v++) {
                if (!visited[v] && graph[u][v] != 0) {
                    int newDist = dist[u] + graph[u][v];
                    if (newDist < dist[v]) {
                        dist[v] = newDist;
                    }
                }
            }
        }
        
        return dist;
    }
    
    private int findMinDistance(int[] dist, boolean[] visited) {
        int min = Integer.MAX_VALUE;
        int minIndex = -1;
        
        for (int i = 0; i < dist.length; i++) {
            if (!visited[i] && dist[i] < min) {
                min = dist[i];
                minIndex = i;
            }
        }
        
        return minIndex;
    }
}
```

**⚠️ Dijkstra算法限制**
```
重要限制：
❌ 不能处理负权边
❌ 只适用于非负权重的图

为什么不能有负权边？
- 算法假设一旦确定了到某点的最短距离，就不会再改变
- 负权边可能让已确定的距离变得更短
```

### 5.3 BFS求最短路径（无权图）


**💡 基本概念**：对于无权图（所有边的权重都是1），BFS就能找到最短路径

```java
public class UnweightedShortestPath {
    
    public int[] bfsShortestPath(GraphList graph, int start) {
        Queue<Integer> queue = new LinkedList<>();
        int[] distance = new int[graph.getVertexCount()];
        boolean[] visited = new boolean[graph.getVertexCount()];
        
        Arrays.fill(distance, -1); // -1表示不可达
        
        queue.offer(start);
        visited[start] = true;
        distance[start] = 0;
        
        while (!queue.isEmpty()) {
            int vertex = queue.poll();
            
            for (int neighbor : graph.getNeighbors(vertex)) {
                if (!visited[neighbor]) {
                    visited[neighbor] = true;
                    distance[neighbor] = distance[vertex] + 1;
                    queue.offer(neighbor);
                }
            }
        }
        
        return distance;
    }
    
    // 获取具体路径
    public List<Integer> getPath(GraphList graph, int start, int end) {
        Queue<Integer> queue = new LinkedList<>();
        int[] parent = new int[graph.getVertexCount()];
        boolean[] visited = new boolean[graph.getVertexCount()];
        
        Arrays.fill(parent, -1);
        
        queue.offer(start);
        visited[start] = true;
        
        while (!queue.isEmpty()) {
            int vertex = queue.poll();
            
            if (vertex == end) {
                // 重构路径
                return reconstructPath(parent, start, end);
            }
            
            for (int neighbor : graph.getNeighbors(vertex)) {
                if (!visited[neighbor]) {
                    visited[neighbor] = true;
                    parent[neighbor] = vertex;
                    queue.offer(neighbor);
                }
            }
        }
        
        return new ArrayList<>(); // 无路径
    }
    
    private List<Integer> reconstructPath(int[] parent, int start, int end) {
        List<Integer> path = new ArrayList<>();
        int current = end;
        
        while (current != -1) {
            path.add(current);
            current = parent[current];
        }
        
        Collections.reverse(path);
        return path;
    }
}
```

### 5.4 最短路径算法对比


**📊 算法选择指南**
| 算法 | 适用图类型 | 时间复杂度 | 空间复杂度 | 特点 |
|------|------------|------------|------------|------|
| **BFS** | 无权图 | O(V+E) | O(V) | 简单，保证最短 |
| **Dijkstra** | 非负权图 | O(V²) | O(V) | 经典，应用广泛 |
| **Bellman-Ford** | 有负权图 | O(VE) | O(V) | 能检测负环 |
| **Floyd-Warshall** | 所有顶点对 | O(V³) | O(V²) | 求任意两点距离 |

**🎯 实际应用场景**
```
BFS最短路径：
✅ 社交网络中的好友度数
✅ 网页链接的跳转次数
✅ 游戏中的移动步数

Dijkstra算法：
✅ GPS导航系统
✅ 网络路由协议
✅ 航班路线规划
✅ 游戏中的AI寻路
```

---

## 6. 📋 核心要点总结


### 6.1 图形结构思维要点


**🔸 核心概念理解**
```
图 = 顶点 + 边的关系网络
- 顶点：实体对象（人、城市、网页等）
- 边：关系连接（朋友、道路、链接等）
- 度：连接数量的衡量标准
```

**🔸 表示方法选择**
```
邻接矩阵：
✅ 密集图，快速查询边
❌ 稀疏图，浪费空间

邻接表：
✅ 稀疏图，节省空间
❌ 查询边稍慢，但通常是首选
```

**🔸 有向vs无向判断**
```
关键问题：关系是否对称？
- 对称关系 → 无向图（朋友、道路）
- 非对称关系 → 有向图（关注、链接）
```

### 6.2 遍历算法应用指南


**🔸 DFS使用场景**
```
深度优先适合：
- 路径查找问题
- 环检测
- 拓扑排序
- 连通性分析
```

**🔸 BFS使用场景**
```
广度优先适合：
- 最短路径（无权图）
- 层次遍历
- 最近邻查找
- 社交网络分析
```

### 6.3 最短路径问题总结


**🔸 算法选择策略**
```
无权图 → BFS
非负权图 → Dijkstra
有负权图 → Bellman-Ford
所有顶点对 → Floyd-Warshall
```

**🔸 实际应用思维**
```
问自己这些问题：
1. 这个问题能用图来建模吗？
2. 关系是单向还是双向的？
3. 边有权重吗？权重代表什么？
4. 我需要找什么？（路径、距离、连通性）
5. 数据规模多大？（选择合适算法）
```

### 6.4 图形思维的实际价值


**🚀 解决现实问题的思维模式**
```
看到网络关系问题 → 想到图
看到层次扩散问题 → 想到BFS  
看到深入探索问题 → 想到DFS
看到路径优化问题 → 想到最短路径算法
```

**💡 培养图形思维的方法**
- 多观察生活中的网络关系
- 练习将实际问题抽象成图
- 理解算法的本质思想，不要死记硬背
- 多做实际项目，加深理解

**核心记忆口诀**：
```
图是关系网络化，顶点连边表达它
遍历分为深和广，路径问题算法佳
矩阵密集表适疏，有向无向看对称
思维训练多实践，抽象建模解难题
```