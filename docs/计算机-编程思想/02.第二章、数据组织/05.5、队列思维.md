---
title: 5、队列思维
---
## 📚 目录

1. [先进先出原理](#1-先进先出原理)
2. [循环队列设计](#2-循环队列设计)
3. [优先级队列](#3-优先级队列)
4. [双端队列](#4-双端队列)
5. [队列在算法中的应用](#5-队列在算法中的应用)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔄 先进先出原理


### 1.1 什么是队列

**🔸 生活中的队列**
```
排队买咖啡的场景：
┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐
│ 张三 │  │ 李四 │  │ 王五 │  │ 赵六 │  ← 队尾(新人排队)
└─────┘  └─────┘  └─────┘  └─────┘
   ↑                                ↑
队头(先买到)                      队尾(后排队)

特点：先排队的人先买到咖啡，后排队的人后买到
```

**🔸 程序中的队列**
```
队列就像一根管子，从一端放入数据，从另一端取出数据
                    
放入数据 ──▶ [  3  2  1  ] ──▶ 取出数据
           队尾        队头
         (enqueue)   (dequeue)
```

### 1.2 先进先出(FIFO)原理详解


**💡 核心概念**: FIFO = First In, First Out(先进先出)

```java
// 队列的基本操作演示
public class SimpleQueue {
    private int[] data;
    private int front = 0;  // 队头指针
    private int rear = 0;   // 队尾指针
    
    // 入队：在队尾添加元素
    public void enqueue(int value) {
        data[rear] = value;
        rear++;
        System.out.println("入队: " + value);
    }
    
    // 出队：从队头移除元素
    public int dequeue() {
        int value = data[front];
        front++;
        System.out.println("出队: " + value);
        return value;
    }
}
```

**🌰 生活类比理解**
> 💡 **记忆技巧**  
> 队列就像食堂排队打饭：
> - 新同学从队尾排队(入队)
> - 轮到的同学从队头打饭(出队)
> - 绝对不能插队！先来的先服务

### 1.3 队列操作的时间复杂度


| 操作 | **时间复杂度** | **说明** |
|------|--------------|---------|
| 🔸 **入队(enqueue)** | `O(1)` | `在队尾直接添加` |
| 🔸 **出队(dequeue)** | `O(1)` | `从队头直接移除` |
| 🔸 **查看队头** | `O(1)` | `只读取不移除` |
| 🔸 **判断空队列** | `O(1)` | `检查队头队尾指针` |

---

## 2. 🔄 循环队列设计


### 2.1 为什么需要循环队列


**🚨 普通队列的问题**
```
初始状态：[  空  空  空  空  空  ]
         front=0, rear=0

依次入队1,2,3：[  1   2   3  空  空  ]
              front=0, rear=3

出队两次：[  空  空   3  空  空  ]
        front=2, rear=3

问题：前面有空间，但rear指针无法再前进！
空间浪费严重，队列"假满"
```

**✅ 循环队列的解决方案**
```
把数组想象成一个圆环：
┌─────┐
│  0  │ ──▶ ┌─────┐
└─────┘     │  1  │
┌─────┐     └─────┘
│  4  │ ◀── ┌─────┐
└─────┘     │  2  │
    ▲       └─────┘
    │       ┌─────┐
    └────── │  3  │
            └─────┘

当指针到达末尾时，自动回到开头继续使用！
```

### 2.2 循环队列实现原理


**🔧 核心技巧**: 使用模运算实现指针的循环移动

```java
public class CircularQueue {
    private int[] data;
    private int front = 0;
    private int rear = 0;
    private int size = 0;     // 当前元素个数
    private int capacity;     // 队列容量
    
    public CircularQueue(int capacity) {
        this.capacity = capacity;
        this.data = new int[capacity];
    }
    
    // 入队：使用模运算实现循环
    public boolean enqueue(int value) {
        if (size == capacity) {
            return false; // 队列已满
        }
        
        data[rear] = value;
        rear = (rear + 1) % capacity;  // 🔑关键：模运算实现循环
        size++;
        return true;
    }
    
    // 出队：同样使用模运算
    public int dequeue() {
        if (size == 0) {
            throw new RuntimeException("队列为空");
        }
        
        int value = data[front];
        front = (front + 1) % capacity;  // 🔑关键：模运算实现循环
        size--;
        return value;
    }
    
    // 判断队列是否已满
    public boolean isFull() {
        return size == capacity;
    }
    
    // 判断队列是否为空
    public boolean isEmpty() {
        return size == 0;
    }
}
```

**🧠 理解要点**
> **模运算的魔法**: `(rear + 1) % capacity`
> - 当rear=4, capacity=5时: (4+1) % 5 = 0 (回到开头)
> - 当rear=2, capacity=5时: (2+1) % 5 = 3 (正常前进)

### 2.3 循环队列的优势


**📈 空间利用率对比**
```
普通队列：
使用过程: [1][2][3][空][空] → [空][空][3][空][空] → 无法继续使用
空间利用率: 60% → 20% → 无法使用

循环队列：
使用过程: [1][2][3][空][空] → [4][5][3][空][空] → [4][5][6][7][8]
空间利用率: 60% → 60% → 100%
```

**⚡ 性能优势**
- ✅ **空间效率**: 100%利用数组空间
- ✅ **时间效率**: 所有操作仍然是O(1)
- ✅ **内存友好**: 避免频繁申请释放内存

---

## 3. 🎯 优先级队列


### 3.1 什么是优先级队列


**🔸 生活中的优先级概念**
```
医院急诊科排队：
┌─────────┐  ┌─────────┐  ┌─────────┐
│ 重症患者 │  │ 普通患者 │  │ 体检患者 │
│ 优先级=1 │  │ 优先级=2 │  │ 优先级=3 │
└─────────┘  └─────────┘  └─────────┘
      ↑            ↑            ↑
  立即处理      正常等待      最后处理

不是按到达顺序，而是按紧急程度排序！
```

**💡 程序中的优先级队列**
```
普通队列: 先进先出 (FIFO)
优先级队列: 优先级高的先出 (Priority First)

操作系统任务调度：
┌──────────────┐
│ 系统任务(高)  │ ──▶ 立即执行
├──────────────┤
│ 用户任务(中)  │ ──▶ 正常执行  
├──────────────┤
│ 后台任务(低)  │ ──▶ 空闲时执行
└──────────────┘
```

### 3.2 基于堆的优先级队列实现


**🏗️ 堆的基本概念**
```
最大堆示例 (父节点 ≥ 子节点):
        9
       / \
      7   8
     / \ / \
    3  5 6  2

数组表示: [9, 7, 8, 3, 5, 6, 2]
索引关系:
- 父节点: index/2
- 左子节点: index*2
- 右子节点: index*2+1
```

**🔧 优先级队列核心实现**
```java
public class PriorityQueue {
    private int[] heap;
    private int size = 0;
    
    // 入队：添加元素并维护堆性质
    public void enqueue(int value) {
        heap[++size] = value;
        swim(size);  // 上浮操作
    }
    
    // 出队：移除最高优先级元素
    public int dequeue() {
        int max = heap[1];      // 堆顶是最大值
        heap[1] = heap[size--]; // 用最后元素替换堆顶
        sink(1);                // 下沉操作
        return max;
    }
    
    // 上浮：新元素可能破坏堆性质，向上调整
    private void swim(int k) {
        while (k > 1 && heap[k] > heap[k/2]) {
            swap(k, k/2);
            k = k/2;
        }
    }
    
    // 下沉：堆顶改变后，向下调整
    private void sink(int k) {
        while (2*k <= size) {
            int j = 2*k;
            if (j < size && heap[j] < heap[j+1]) j++;
            if (heap[k] >= heap[j]) break;
            swap(k, j);
            k = j;
        }
    }
}
```

### 3.3 优先级队列的应用场景


**🎯 实际应用案例**

| 应用场景 | **优先级规则** | **实际作用** |
|---------|--------------|-------------|
| 🔸 **任务调度** | `紧急程度` | `重要任务优先执行` |
| 🔸 **网络路由** | `路径成本` | `选择最短路径` |
| 🔸 **游戏AI** | `威胁等级` | `优先处理高威胁` |
| 🔸 **数据压缩** | `字符频率` | `高频字符优先编码` |

**🌰 任务调度示例**
```java
// 任务类定义
class Task {
    String name;
    int priority;  // 数字越小优先级越高
    
    public Task(String name, int priority) {
        this.name = name;
        this.priority = priority;
    }
}

// 使用优先级队列管理任务
PriorityQueue<Task> taskQueue = new PriorityQueue<>(
    (a, b) -> a.priority - b.priority  // 按优先级排序
);

// 添加任务
taskQueue.offer(new Task("发送邮件", 3));
taskQueue.offer(new Task("系统备份", 1));    // 最高优先级
taskQueue.offer(new Task("数据统计", 2));

// 按优先级执行
while (!taskQueue.isEmpty()) {
    Task task = taskQueue.poll();
    System.out.println("执行任务: " + task.name);
}
// 输出: 系统备份 → 数据统计 → 发送邮件
```

---

## 4. 🔄 双端队列


### 4.1 双端队列的概念


**🔸 传统队列 vs 双端队列**
```
传统队列 (单端操作):
入队 ──▶ [  3   2   1  ] ──▶ 出队
        队尾          队头
        只能从尾部进，头部出

双端队列 (双端操作):
   ◀── 出队          出队 ──▶
入队 ──▶ [  3   2   1  ] ◀── 入队  
        队尾          队头
        两端都可以进出！
```

**💡 生活类比**
> 🌰 **想象一个特殊的电梯**  
> - 普通电梯：只有一个门，先进先出
> - 双端队列像有前后两个门的电梯：前门后门都能进出

### 4.2 双端队列实现设计


**🔧 基于数组的双端队列**
```java
public class Deque {
    private int[] data;
    private int front = 0;  // 头部指针
    private int rear = 0;   // 尾部指针
    private int size = 0;   // 当前元素个数
    private int capacity;
    
    public Deque(int capacity) {
        this.capacity = capacity;
        this.data = new int[capacity];
    }
    
    // 从头部添加元素
    public boolean addFirst(int value) {
        if (size == capacity) return false;
        
        front = (front - 1 + capacity) % capacity;  // 🔑向前移动
        data[front] = value;
        size++;
        return true;
    }
    
    // 从尾部添加元素
    public boolean addLast(int value) {
        if (size == capacity) return false;
        
        data[rear] = value;
        rear = (rear + 1) % capacity;  // 🔑向后移动
        size++;
        return true;
    }
    
    // 从头部移除元素
    public int removeFirst() {
        if (size == 0) throw new RuntimeException("队列为空");
        
        int value = data[front];
        front = (front + 1) % capacity;
        size--;
        return value;
    }
    
    // 从尾部移除元素
    public int removeLast() {
        if (size == 0) throw new RuntimeException("队列为空");
        
        rear = (rear - 1 + capacity) % capacity;
        int value = data[rear];
        size--;
        return value;
    }
}
```

### 4.3 双端队列的应用场景


**🎯 典型应用案例**

**🔸 浏览器历史记录**
```java
// 模拟浏览器前进后退功能
Deque<String> browserHistory = new ArrayDeque<>();

// 访问网页 (添加到尾部)
browserHistory.addLast("百度");
browserHistory.addLast("谷歌");
browserHistory.addLast("GitHub");

// 后退操作 (从尾部移除)
String current = browserHistory.removeLast();  // "GitHub"
System.out.println("当前页面: " + current);

// 前进操作 (添加到尾部)
browserHistory.addLast("GitHub");
```

**🔸 滑动窗口问题**
```java
// 找数组中每个大小为k的窗口的最大值
public int[] maxSlidingWindow(int[] nums, int k) {
    Deque<Integer> deque = new ArrayDeque<>();  // 存储索引
    
    for (int i = 0; i < nums.length; i++) {
        // 移除超出窗口范围的元素
        while (!deque.isEmpty() && deque.peekFirst() < i - k + 1) {
            deque.removeFirst();
        }
        
        // 移除比当前元素小的元素（它们不可能是最大值）
        while (!deque.isEmpty() && nums[deque.peekLast()] < nums[i]) {
            deque.removeLast();
        }
        
        deque.addLast(i);
        
        // 窗口形成后，记录最大值
        if (i >= k - 1) {
            result[i - k + 1] = nums[deque.peekFirst()];
        }
    }
}
```

**📊 双端队列操作复杂度**

| 操作 | **时间复杂度** | **说明** |
|------|--------------|---------|
| 🔸 **头部添加/删除** | `O(1)` | `直接操作头指针` |
| 🔸 **尾部添加/删除** | `O(1)` | `直接操作尾指针` |
| 🔸 **访问头尾元素** | `O(1)` | `直接通过指针访问` |
| 🔸 **中间访问/修改** | `O(n)` | `需要遍历查找` |

---

## 5. 🔍 队列在算法中的应用


### 5.1 广度优先搜索(BFS)


**🔸 BFS核心思想**
```
想象你在迷宫中找出口：
层次搜索策略 - 一层层向外扩展搜索

起点 ──▶ 第1层邻居 ──▶ 第2层邻居 ──▶ 第3层邻居
 S        A B C         D E F G       H I J K

队列作用：确保按层次顺序访问节点
```

**🔧 BFS基本模板**
```java
public void bfs(TreeNode root) {
    if (root == null) return;
    
    Queue<TreeNode> queue = new LinkedList<>();
    queue.offer(root);  // 起点入队
    
    while (!queue.isEmpty()) {
        TreeNode node = queue.poll();  // 取出队头节点
        System.out.println("访问: " + node.val);
        
        // 将子节点加入队列 (下一层)
        if (node.left != null) queue.offer(node.left);
        if (node.right != null) queue.offer(node.right);
    }
}
```

**🌰 实际应用：二叉树层序遍历**
```java
// 按层打印二叉树
public List<List<Integer>> levelOrder(TreeNode root) {
    List<List<Integer>> result = new ArrayList<>();
    if (root == null) return result;
    
    Queue<TreeNode> queue = new LinkedList<>();
    queue.offer(root);
    
    while (!queue.isEmpty()) {
        int levelSize = queue.size();  // 🔑记录当前层节点数
        List<Integer> currentLevel = new ArrayList<>();
        
        // 处理当前层的所有节点
        for (int i = 0; i < levelSize; i++) {
            TreeNode node = queue.poll();
            currentLevel.add(node.val);
            
            // 下一层节点入队
            if (node.left != null) queue.offer(node.left);
            if (node.right != null) queue.offer(node.right);
        }
        
        result.add(currentLevel);
    }
    return result;
}
```

### 5.2 最短路径问题


**🔸 无权图最短路径**
```java
// 在无权图中找从start到end的最短路径
public int shortestPath(int[][] graph, int start, int end) {
    Queue<Integer> queue = new LinkedList<>();
    boolean[] visited = new boolean[graph.length];
    int[] distance = new int[graph.length];
    
    queue.offer(start);
    visited[start] = true;
    distance[start] = 0;
    
    while (!queue.isEmpty()) {
        int node = queue.poll();
        
        // 找到目标节点
        if (node == end) {
            return distance[end];
        }
        
        // 探索邻居节点
        for (int neighbor : graph[node]) {
            if (!visited[neighbor]) {
                visited[neighbor] = true;
                distance[neighbor] = distance[node] + 1;
                queue.offer(neighbor);
            }
        }
    }
    
    return -1;  // 无法到达
}
```

### 5.3 生产者消费者模型


**🔸 模型概念**
```
生产者线程 ──▶ [队列缓冲区] ◀── 消费者线程
              (有界队列)

生产者：生产数据，放入队列
消费者：从队列取数据，处理
队列：缓冲区，解耦生产和消费速度
```

**🔧 线程安全的实现**
```java
public class ProducerConsumer {
    private final Queue<Integer> queue = new LinkedList<>();
    private final int CAPACITY = 10;
    
    // 生产者方法
    public void produce() throws InterruptedException {
        int value = 0;
        while (true) {
            synchronized (queue) {
                // 队列满了，生产者等待
                while (queue.size() == CAPACITY) {
                    queue.wait();
                }
                
                System.out.println("生产: " + value);
                queue.offer(value++);
                queue.notifyAll();  // 通知消费者
            }
            Thread.sleep(1000);  // 模拟生产时间
        }
    }
    
    // 消费者方法
    public void consume() throws InterruptedException {
        while (true) {
            synchronized (queue) {
                // 队列空了，消费者等待
                while (queue.isEmpty()) {
                    queue.wait();
                }
                
                int value = queue.poll();
                System.out.println("消费: " + value);
                queue.notifyAll();  // 通知生产者
            }
            Thread.sleep(1500);  // 模拟消费时间
        }
    }
}
```

### 5.4 队列在算法中的选择指南


**📊 不同队列的适用场景**

| 算法场景 | **推荐队列类型** | **选择理由** |
|---------|---------------|-------------|
| 🔸 **BFS遍历** | `普通队列` | `严格按层次访问` |
| 🔸 **任务调度** | `优先级队列` | `按重要性执行` |
| 🔸 **滑动窗口** | `双端队列` | `需要两端操作` |
| 🔸 **缓存系统** | `循环队列` | `固定大小，高效利用` |

---

## 6. 📋 核心要点总结


### 6.1 队列思维的本质理解


**🎯 核心概念梳理**
```
🔸 先进先出(FIFO): 队列的基本工作原则
🔸 循环设计: 提高空间利用率的关键技巧  
🔸 优先级机制: 打破FIFO限制，按重要性排序
🔸 双端操作: 增加灵活性，支持两端进出
🔸 算法应用: BFS、调度、缓存等核心场景
```

### 6.2 队列选择决策树


```
需要队列数据结构？
     │
     ▼
是否需要优先级？ ──Yes──▶ 优先级队列
     │                      (堆实现)
     No
     ▼
是否需要两端操作？ ──Yes──▶ 双端队列
     │                     (ArrayDeque)
     No  
     ▼
是否固定大小？ ──Yes──▶ 循环队列
     │                   (数组实现)
     No
     ▼
普通队列 (LinkedList)
```

### 6.3 队列思维的实践价值


**🚀 解题思维模式**
1. **📋 识别场景**: 看到"层次处理"、"先来先服务"就想到队列
2. **🎯 选择类型**: 根据具体需求选择合适的队列类型
3. **⚡ 优化空间**: 固定大小用循环队列，减少内存浪费
4. **🔧 线程安全**: 多线程环境注意同步控制

**🧠 记忆口诀**
```
队列思维记心间，先进先出是关键
循环设计省空间，优先级别看重点
双端灵活用处广，BFS算法队列帮
生产消费解耦合，缓冲机制效果强
```

**⭐ 学习建议**
- 🔰 **入门**: 掌握基本队列操作和FIFO原理
- 🔸 **进阶**: 理解循环队列的空间优化思想  
- ⭐ **高级**: 熟练运用优先级队列和双端队列
- 🏆 **专家**: 能够根据场景选择最优队列实现

**🔗 扩展学习**
- 相关数据结构: [栈的设计思维](#栈思维) | [堆的原理应用](#堆思维)
- 算法应用: [图的遍历算法](#图算法) | [动态规划优化](#DP优化)
- 系统设计: [消息队列中间件](#消息队列) | [任务调度系统](#调度系统)