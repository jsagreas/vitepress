---
title: 4、对象组合思维
---
## 📚 目录

1. [对象组合思维概述](#1-对象组合思维概述)
2. [适配器模式：接口不兼容问题的解决方案](#2-适配器模式：接口不兼容问题的解决方案)
3. [桥接模式：抽象与实现的分离](#3-桥接模式：抽象与实现的分离)
4. [组合模式：树形结构的统一处理](#4-组合模式：树形结构的统一处理)
5. [装饰器模式：对象功能的动态扩展](#5-装饰器模式：对象功能的动态扩展)
6. [外观模式：复杂子系统的简化接口](#6-外观模式：复杂子系统的简化接口)
7. [组合模式的对比与选择](#7-组合模式的对比与选择)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🧩 对象组合思维概述


### 1.1 什么是对象组合思维


**🔸 核心概念**
对象组合思维是一种编程思想，它的核心理念是：**把不同的对象组合在一起，形成更强大的功能**。就像搭积木一样，用简单的小块组合成复杂的建筑。

```
现实类比：
汽车 = 发动机 + 轮胎 + 方向盘 + 座椅 + ...
每个部件都有自己的功能，组合起来就是一辆完整的车

编程中：
复杂系统 = 简单对象A + 简单对象B + 简单对象C + ...
```

**💡 为什么要用组合思维**
- **降低复杂度**：把大问题分解成小问题
- **提高复用性**：小对象可以在不同地方重复使用
- **增强灵活性**：可以随时调整组合方式
- **便于维护**：修改一个小对象不影响整体

### 1.2 组合思维的基本原则


**🎯 核心原则**
```
1. 单一职责：每个对象只做一件事，但把它做好
2. 松耦合：对象之间的依赖关系要尽量少
3. 高内聚：相关的功能要放在一起
4. 接口统一：组合的对象要有统一的使用方式
```

### 1.3 常见的组合场景


**📋 实际应用场景**
- **接口适配**：旧系统和新系统的对接
- **功能扩展**：给现有对象添加新能力
- **复杂结构**：处理树形、层级结构
- **系统简化**：把复杂的操作变简单
- **抽象分离**：把变化的部分和稳定的部分分开

---

## 2. 🔌 适配器模式：接口不兼容问题的解决方案


### 2.1 什么是适配器模式


**🔸 生活中的适配器**
想象你有一个美国的电器（三脚插头），但你在中国（两脚插座），这时候你需要一个转换器。适配器模式就是这个转换器。

```
现实情况：
美国电器（三脚插头） ←→ 转换器 ←→ 中国插座（两脚）

编程情况：
旧系统接口 ←→ 适配器 ←→ 新系统接口
```

**💡 适配器模式的作用**
让两个不兼容的接口能够一起工作，就像是一个"翻译官"。

### 2.2 适配器模式的实现


**🔧 基本结构**
```java
// 旧系统：老式打印机
class OldPrinter {
    public void oldPrint(String text) {
        System.out.println("老式打印：" + text);
    }
}

// 新系统期望的接口
interface NewPrinter {
    void print(String content);
}

// 适配器：让老打印机适配新接口
class PrinterAdapter implements NewPrinter {
    private OldPrinter oldPrinter;
    
    public PrinterAdapter(OldPrinter oldPrinter) {
        this.oldPrinter = oldPrinter;
    }
    
    @Override
    public void print(String content) {
        // 把新接口的调用转换成老接口的调用
        oldPrinter.oldPrint(content);
    }
}
```

**🎯 使用示例**
```java
public class AdapterExample {
    public static void main(String[] args) {
        // 有一台老式打印机
        OldPrinter oldPrinter = new OldPrinter();
        
        // 通过适配器，让它支持新接口
        NewPrinter printer = new PrinterAdapter(oldPrinter);
        
        // 现在可以用新方式调用了
        printer.print("Hello World");  // 输出：老式打印：Hello World
    }
}
```

### 2.3 适配器模式的应用场景


**📊 常见应用场景**

| 场景 | 说明 | 举例 |
|------|------|------|
| **系统集成** | 新旧系统对接 | 新支付系统对接老银行接口 |
| **第三方库** | 使用外部库 | 把不同数据库的接口统一 |
| **数据格式** | 格式转换 | JSON转XML，XML转对象 |
| **接口升级** | 版本兼容 | API v1.0 适配到 v2.0 |

**💻 实际应用示例**
```java
// 不同数据库的适配
interface Database {
    void save(String data);
    String load(String id);
}

// MySQL数据库
class MySQLDatabase {
    public void insert(String data) { /* MySQL特有的插入方法 */ }
    public String select(String id) { /* MySQL特有的查询方法 */ }
}

// MySQL适配器
class MySQLAdapter implements Database {
    private MySQLDatabase mysql;
    
    public MySQLAdapter(MySQLDatabase mysql) {
        this.mysql = mysql;
    }
    
    @Override
    public void save(String data) {
        mysql.insert(data);  // 转换调用
    }
    
    @Override
    public String load(String id) {
        return mysql.select(id);  // 转换调用
    }
}
```

---

## 3. 🌉 桥接模式：抽象与实现的分离


### 3.1 什么是桥接模式


**🔸 理解桥接模式**
桥接模式就像现实中的桥梁，连接两个独立的部分。在编程中，它把"要做什么"（抽象）和"怎么做"（实现）分开。

```
现实类比：
遥控器（抽象）  ←桥接→  电视机（实现）
遥控器定义操作：开机、关机、换台
电视机实现操作：具体怎么开机、怎么换台

编程中：
图形（抽象）    ←桥接→  绘图API（实现）  
图形定义：画圆、画方
绘图API：OpenGL、DirectX具体怎么画
```

**💡 为什么要分离抽象和实现**
- **独立变化**：抽象和实现可以各自修改，不互相影响
- **平台无关**：同样的抽象可以在不同平台实现
- **复用性强**：一个实现可以支持多个抽象

### 3.2 桥接模式的实现


**🔧 基本结构**
```java
// 实现接口：定义具体怎么做
interface DrawingAPI {
    void drawCircle(int x, int y, int radius);
    void drawRectangle(int x, int y, int width, int height);
}

// 具体实现1：用控制台画图
class ConsoleDrawing implements DrawingAPI {
    @Override
    public void drawCircle(int x, int y, int radius) {
        System.out.println("控制台画圆：中心(" + x + "," + y + ") 半径" + radius);
    }
    
    @Override
    public void drawRectangle(int x, int y, int width, int height) {
        System.out.println("控制台画矩形：起点(" + x + "," + y + ") 大小" + width + "x" + height);
    }
}

// 具体实现2：用图形界面画图
class GraphicDrawing implements DrawingAPI {
    @Override
    public void drawCircle(int x, int y, int radius) {
        System.out.println("图形界面画圆：中心(" + x + "," + y + ") 半径" + radius);
    }
    
    @Override
    public void drawRectangle(int x, int y, int width, int height) {
        System.out.println("图形界面画矩形：起点(" + x + "," + y + ") 大小" + width + "x" + height);
    }
}
```

**🎨 抽象层定义**
```java
// 抽象图形：定义要做什么
abstract class Shape {
    protected DrawingAPI drawingAPI;  // 桥接：连接到具体实现
    
    public Shape(DrawingAPI drawingAPI) {
        this.drawingAPI = drawingAPI;
    }
    
    public abstract void draw();  // 抽象方法：要做什么
}

// 具体图形：圆形
class Circle extends Shape {
    private int x, y, radius;
    
    public Circle(int x, int y, int radius, DrawingAPI drawingAPI) {
        super(drawingAPI);
        this.x = x;
        this.y = y;
        this.radius = radius;
    }
    
    @Override
    public void draw() {
        drawingAPI.drawCircle(x, y, radius);  // 通过桥接调用具体实现
    }
}

// 具体图形：矩形
class Rectangle extends Shape {
    private int x, y, width, height;
    
    public Rectangle(int x, int y, int width, int height, DrawingAPI drawingAPI) {
        super(drawingAPI);
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
    
    @Override
    public void draw() {
        drawingAPI.drawRectangle(x, y, width, height);
    }
}
```

**🎯 使用示例**
```java
public class BridgeExample {
    public static void main(String[] args) {
        // 同样的圆形，可以用不同方式画
        Shape circle1 = new Circle(10, 20, 5, new ConsoleDrawing());
        Shape circle2 = new Circle(10, 20, 5, new GraphicDrawing());
        
        circle1.draw();  // 输出：控制台画圆：中心(10,20) 半径5
        circle2.draw();  // 输出：图形界面画圆：中心(10,20) 半径5
        
        // 同样的矩形，也可以用不同方式画
        Shape rect1 = new Rectangle(0, 0, 100, 50, new ConsoleDrawing());
        Shape rect2 = new Rectangle(0, 0, 100, 50, new GraphicDrawing());
        
        rect1.draw();  // 输出：控制台画矩形：起点(0,0) 大小100x50
        rect2.draw();  // 输出：图形界面画矩形：起点(0,0) 大小100x50
    }
}
```

### 3.3 桥接模式的优势


**✅ 核心优势**
- **解耦**：抽象和实现可以独立修改
- **扩展性**：添加新的抽象或实现都很容易
- **灵活性**：运行时可以切换不同的实现
- **复用性**：一个实现可以被多个抽象使用

---

## 4. 🌳 组合模式：树形结构的统一处理


### 4.1 什么是组合模式


**🔸 理解组合模式**
组合模式就像文件夹和文件的关系。文件夹里可以有文件，也可以有其他文件夹。无论是文件还是文件夹，我们都可以用同样的方式操作它们（比如删除、复制）。

```
文件系统结构：
📁 我的文档/
  ├── 📄 简历.doc
  ├── 📁 照片/
  │   ├── 📄 旅游1.jpg
  │   └── 📄 旅游2.jpg
  └── 📁 工作/
      ├── 📄 报告.pdf
      └── 📁 项目/
          └── 📄 代码.java

统一操作：
- 删除"我的文档" → 删除整个文件夹及其内容
- 删除"简历.doc" → 删除单个文件
- 复制"照片文件夹" → 复制文件夹及其所有内容
```

**💡 组合模式的核心思想**
让客户端以相同的方式处理单个对象和对象的组合，就像对待单个对象一样。

### 4.2 组合模式的实现


**🔧 基本结构**
```java
// 组件接口：定义文件和文件夹的公共操作
interface FileComponent {
    void display(String indent);  // 显示内容
    int getSize();               // 获取大小
    void delete();              // 删除
}

// 叶子节点：文件（不能包含其他内容）
class File implements FileComponent {
    private String name;
    private int size;
    
    public File(String name, int size) {
        this.name = name;
        this.size = size;
    }
    
    @Override
    public void display(String indent) {
        System.out.println(indent + "📄 " + name + " (" + size + "KB)");
    }
    
    @Override
    public int getSize() {
        return size;
    }
    
    @Override
    public void delete() {
        System.out.println("删除文件：" + name);
    }
}

// 组合节点：文件夹（可以包含文件和其他文件夹）
class Folder implements FileComponent {
    private String name;
    private List<FileComponent> children = new ArrayList<>();
    
    public Folder(String name) {
        this.name = name;
    }
    
    // 添加子项（文件或文件夹）
    public void add(FileComponent component) {
        children.add(component);
    }
    
    // 移除子项
    public void remove(FileComponent component) {
        children.remove(component);
    }
    
    @Override
    public void display(String indent) {
        System.out.println(indent + "📁 " + name + "/");
        // 显示所有子项
        for (FileComponent child : children) {
            child.display(indent + "  ");
        }
    }
    
    @Override
    public int getSize() {
        int totalSize = 0;
        // 计算所有子项的总大小
        for (FileComponent child : children) {
            totalSize += child.getSize();
        }
        return totalSize;
    }
    
    @Override
    public void delete() {
        System.out.println("删除文件夹：" + name);
        // 删除所有子项
        for (FileComponent child : children) {
            child.delete();
        }
        children.clear();
    }
}
```

**🎯 使用示例**
```java
public class CompositeExample {
    public static void main(String[] args) {
        // 创建文件
        File resume = new File("简历.doc", 50);
        File photo1 = new File("旅游1.jpg", 200);
        File photo2 = new File("旅游2.jpg", 180);
        File report = new File("报告.pdf", 300);
        File code = new File("代码.java", 20);
        
        // 创建文件夹
        Folder photos = new Folder("照片");
        photos.add(photo1);
        photos.add(photo2);
        
        Folder project = new Folder("项目");
        project.add(code);
        
        Folder work = new Folder("工作");
        work.add(report);
        work.add(project);
        
        Folder documents = new Folder("我的文档");
        documents.add(resume);
        documents.add(photos);
        documents.add(work);
        
        // 统一的操作方式
        System.out.println("=== 显示文件结构 ===");
        documents.display("");
        
        System.out.println("\n=== 计算总大小 ===");
        System.out.println("总大小：" + documents.getSize() + "KB");
        
        System.out.println("\n=== 删除照片文件夹 ===");
        photos.delete();
    }
}
```

### 4.3 组合模式的应用场景


**📊 常见应用场景**

| 场景 | 说明 | 举例 |
|------|------|------|
| **UI组件** | 界面元素的层级结构 | 窗口→面板→按钮 |
| **菜单系统** | 多级菜单 | 主菜单→子菜单→菜单项 |
| **组织架构** | 公司部门结构 | 公司→部门→小组→员工 |
| **表达式** | 数学表达式 | (a+b)*(c-d) |

---

## 5. 🎨 装饰器模式：对象功能的动态扩展


### 5.1 什么是装饰器模式


**🔸 理解装饰器模式**
装饰器模式就像给人穿衣服。一个人可以穿T恤，也可以在T恤外面穿毛衣，再穿外套。每件衣服都给人添加了新的功能（保暖、美观等），但人还是那个人。

```
现实类比：
人 → 穿T恤 → 穿毛衣 → 穿外套 → 戴帽子
每一层都在原有基础上添加新功能

编程中：
基础对象 → 装饰器1 → 装饰器2 → 装饰器3
每个装饰器都为对象添加新功能
```

**💡 装饰器模式的核心思想**
在不改变原对象的前提下，动态地为对象添加新功能。就像是给对象"贴标签"，想贴多少贴多少。

### 5.2 装饰器模式的实现


**🔧 基本结构**
```java
// 咖啡接口：定义基本功能
interface Coffee {
    String getDescription();  // 获取描述
    double getCost();        // 获取价格
}

// 基础咖啡：最简单的实现
class SimpleCoffee implements Coffee {
    @Override
    public String getDescription() {
        return "简单咖啡";
    }
    
    @Override
    public double getCost() {
        return 10.0;  // 基础价格10元
    }
}

// 咖啡装饰器：所有装饰器的基类
abstract class CoffeeDecorator implements Coffee {
    protected Coffee coffee;  // 被装饰的咖啡对象
    
    public CoffeeDecorator(Coffee coffee) {
        this.coffee = coffee;
    }
    
    @Override
    public String getDescription() {
        return coffee.getDescription();
    }
    
    @Override
    public double getCost() {
        return coffee.getCost();
    }
}

// 具体装饰器：加牛奶
class MilkDecorator extends CoffeeDecorator {
    public MilkDecorator(Coffee coffee) {
        super(coffee);
    }
    
    @Override
    public String getDescription() {
        return coffee.getDescription() + " + 牛奶";
    }
    
    @Override
    public double getCost() {
        return coffee.getCost() + 2.0;  // 牛奶加2元
    }
}

// 具体装饰器：加糖
class SugarDecorator extends CoffeeDecorator {
    public SugarDecorator(Coffee coffee) {
        super(coffee);
    }
    
    @Override
    public String getDescription() {
        return coffee.getDescription() + " + 糖";
    }
    
    @Override
    public double getCost() {
        return coffee.getCost() + 1.0;  // 糖加1元
    }
}

// 具体装饰器：加巧克力
class ChocolateDecorator extends CoffeeDecorator {
    public ChocolateDecorator(Coffee coffee) {
        super(coffee);
    }
    
    @Override
    public String getDescription() {
        return coffee.getDescription() + " + 巧克力";
    }
    
    @Override
    public double getCost() {
        return coffee.getCost() + 3.0;  // 巧克力加3元
    }
}
```

**🎯 使用示例**
```java
public class DecoratorExample {
    public static void main(String[] args) {
        // 从一杯简单咖啡开始
        Coffee coffee = new SimpleCoffee();
        System.out.println(coffee.getDescription() + " 价格：" + coffee.getCost() + "元");
        
        // 加牛奶
        coffee = new MilkDecorator(coffee);
        System.out.println(coffee.getDescription() + " 价格：" + coffee.getCost() + "元");
        
        // 再加糖
        coffee = new SugarDecorator(coffee);
        System.out.println(coffee.getDescription() + " 价格：" + coffee.getCost() + "元");
        
        // 再加巧克力
        coffee = new ChocolateDecorator(coffee);
        System.out.println(coffee.getDescription() + " 价格：" + coffee.getCost() + "元");
        
        // 输出：
        // 简单咖啡 价格：10.0元
        // 简单咖啡 + 牛奶 价格：12.0元
        // 简单咖啡 + 牛奶 + 糖 价格：13.0元
        // 简单咖啡 + 牛奶 + 糖 + 巧克力 价格：16.0元
    }
}
```

### 5.3 装饰器模式的优势


**✅ 核心优势**
- **动态扩展**：运行时可以任意组合功能
- **无需修改**：不用改变原有对象的代码
- **灵活组合**：可以按需要叠加多个功能
- **符合开闭原则**：对扩展开放，对修改关闭

**📊 装饰器 vs 继承**

| 特性 | 装饰器模式 | 继承 |
|------|-----------|------|
| **灵活性** | 运行时动态组合 | 编译时静态确定 |
| **扩展性** | 可任意叠加 | 需要创建大量子类 |
| **复用性** | 装饰器可复用 | 子类难以复用 |
| **复杂度** | 对象关系复杂 | 类层次简单 |

---

## 6. 🏢 外观模式：复杂子系统的简化接口


### 6.1 什么是外观模式


**🔸 理解外观模式**
外观模式就像酒店的前台。客人不需要知道酒店内部有多复杂（清洁部、维修部、餐饮部等），只需要告诉前台需要什么服务，前台会协调各部门来完成。

```
现实类比：
客人 → 前台 → 协调各部门
客人：我要入住
前台：好的，我来安排房间、登记、制作房卡

编程中：
客户端 → 外观类 → 协调各子系统
客户端：我要完成某个功能
外观类：好的，我来调用相关的子系统
```

**💡 外观模式的核心思想**
为复杂的子系统提供一个统一的、简化的接口，让客户端更容易使用。

### 6.2 外观模式的实现


**🔧 复杂子系统**
```java
// 子系统1：CPU
class CPU {
    public void start() {
        System.out.println("CPU 启动");
    }
    
    public void shutdown() {
        System.out.println("CPU 关闭");
    }
}

// 子系统2：内存
class Memory {
    public void load() {
        System.out.println("内存 加载数据");
    }
    
    public void clear() {
        System.out.println("内存 清空数据");
    }
}

// 子系统3：硬盘
class HardDisk {
    public void read() {
        System.out.println("硬盘 读取数据");
    }
    
    public void write() {
        System.out.println("硬盘 写入数据");
    }
}

// 子系统4：显示器
class Monitor {
    public void turnOn() {
        System.out.println("显示器 打开");
    }
    
    public void turnOff() {
        System.out.println("显示器 关闭");
    }
}
```

**🎯 外观类：简化接口**
```java
// 计算机外观：提供简单的操作接口
class ComputerFacade {
    private CPU cpu;
    private Memory memory;
    private HardDisk hardDisk;
    private Monitor monitor;
    
    public ComputerFacade() {
        this.cpu = new CPU();
        this.memory = new Memory();
        this.hardDisk = new HardDisk();
        this.monitor = new Monitor();
    }
    
    // 开机：一个简单操作，背后协调多个子系统
    public void startComputer() {
        System.out.println("=== 开始启动计算机 ===");
        monitor.turnOn();
        cpu.start();
        memory.load();
        hardDisk.read();
        System.out.println("=== 计算机启动完成 ===");
    }
    
    // 关机：一个简单操作，背后协调多个子系统
    public void shutdownComputer() {
        System.out.println("=== 开始关闭计算机 ===");
        hardDisk.write();
        memory.clear();
        cpu.shutdown();
        monitor.turnOff();
        System.out.println("=== 计算机关闭完成 ===");
    }
}
```

**🎯 使用示例**
```java
public class FacadeExample {
    public static void main(String[] args) {
        // 客户端不需要了解内部复杂性
        ComputerFacade computer = new ComputerFacade();
        
        // 简单的开机操作
        computer.startComputer();
        
        System.out.println();
        
        // 简单的关机操作
        computer.shutdownComputer();
        
        // 如果没有外观模式，客户端需要这样做：
        // CPU cpu = new CPU();
        // Memory memory = new Memory();
        // HardDisk hardDisk = new HardDisk();
        // Monitor monitor = new Monitor();
        // monitor.turnOn();
        // cpu.start();
        // memory.load();
        // hardDisk.read();
        // ...复杂的协调过程
    }
}
```

### 6.3 外观模式的应用场景


**📊 常见应用场景**

| 场景 | 说明 | 举例 |
|------|------|------|
| **API封装** | 简化复杂API | 数据库操作的简化接口 |
| **框架集成** | 整合多个框架 | Spring整合各种技术 |
| **遗留系统** | 包装老系统 | 为老系统提供现代接口 |
| **微服务** | 服务聚合 | API网关聚合多个服务 |

**💻 实际应用示例**
```java
// 文件处理外观：简化文件操作
class FileProcessorFacade {
    private FileReader reader;
    private FileValidator validator;
    private FileConverter converter;
    private FileWriter writer;
    
    public FileProcessorFacade() {
        this.reader = new FileReader();
        this.validator = new FileValidator();
        this.converter = new FileConverter();
        this.writer = new FileWriter();
    }
    
    // 一个方法完成复杂的文件处理流程
    public boolean processFile(String inputPath, String outputPath) {
        try {
            // 1. 读取文件
            String content = reader.read(inputPath);
            
            // 2. 验证文件
            if (!validator.validate(content)) {
                return false;
            }
            
            // 3. 转换格式
            String converted = converter.convert(content);
            
            // 4. 写入文件
            writer.write(converted, outputPath);
            
            return true;
        } catch (Exception e) {
            System.out.println("文件处理失败：" + e.getMessage());
            return false;
        }
    }
}
```

---

## 7. ⚖️ 组合模式的对比与选择


### 7.1 五种模式的核心区别


**📊 模式对比表**

| 模式 | 主要目的 | 解决问题 | 使用场景 |
|------|---------|---------|---------|
| **适配器** | 接口转换 | 不兼容接口 | 系统集成、API对接 |
| **桥接** | 分离抽象和实现 | 多维度变化 | 跨平台开发、多实现方式 |
| **组合** | 统一处理整体和部分 | 树形结构 | 文件系统、UI组件 |
| **装饰器** | 动态添加功能 | 功能扩展 | 功能增强、运行时配置 |
| **外观** | 简化复杂接口 | 系统复杂性 | API封装、子系统整合 |

### 7.2 选择指南


**🎯 何时选择适配器模式**
```
✅ 适用情况：
- 需要使用现有类，但接口不匹配
- 想要复用一些现有子类，但缺少公共基类
- 第三方库接口与你的代码不兼容

❌ 不适用情况：
- 可以直接修改现有类的接口
- 接口差异太大，适配成本过高
```

**🎯 何时选择桥接模式**
```
✅ 适用情况：
- 抽象和实现需要独立变化
- 不想在抽象和实现之间建立永久绑定
- 需要在多个平台上实现相同功能

❌ 不适用情况：
- 抽象和实现绑定紧密
- 只有一种实现方式
```

**🎯 何时选择组合模式**
```
✅ 适用情况：
- 需要表示对象的部分-整体层次结构
- 希望统一对待单个对象和组合对象
- 结构是树形的

❌ 不适用情况：
- 结构是线性的或网状的
- 不需要统一处理叶子和容器
```

**🎯 何时选择装饰器模式**
```
✅ 适用情况：
- 需要动态给对象添加功能
- 通过继承扩展功能不现实
- 需要多种功能的组合

❌ 不适用情况：
- 功能是固定的，不需要动态改变
- 只有少量的功能变化
```

**🎯 何时选择外观模式**
```
✅ 适用情况：
- 子系统很复杂，需要简化接口
- 系统有多个入口点
- 需要为遗留系统提供现代接口

❌ 不适用情况：
- 子系统本身就很简单
- 需要暴露子系统的所有功能
```

### 7.3 模式组合使用


**🔗 实际开发中的组合应用**
```java
// 综合应用示例：文档处理系统
public class DocumentSystemExample {
    // 使用外观模式简化复杂操作
    class DocumentFacade {
        private DocumentParser parser;
        private DocumentValidator validator;
        private DocumentConverter converter;
        
        // 使用适配器模式适配不同格式
        public void processDocument(String filePath) {
            DocumentAdapter adapter = getAdapter(filePath);
            Document doc = adapter.parse(filePath);
            // ... 处理逻辑
        }
        
        private DocumentAdapter getAdapter(String filePath) {
            if (filePath.endsWith(".pdf")) {
                return new PDFAdapter();
            } else if (filePath.endsWith(".doc")) {
                return new WordAdapter();
            }
            return new TextAdapter();
        }
    }
    
    // 使用装饰器模式添加处理功能
    class ProcessedDocument {
        Document document;
        
        public ProcessedDocument(Document doc) {
            this.document = doc;
        }
        
        // 可以动态添加加密、压缩等功能
        public ProcessedDocument encrypt() {
            return new EncryptedDocument(this);
        }
        
        public ProcessedDocument compress() {
            return new CompressedDocument(this);
        }
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


**🔸 对象组合思维的本质**
```
核心理念：用简单对象组合成复杂功能
基本原则：单一职责、松耦合、高内聚、接口统一
组合优势：降低复杂度、提高复用性、增强灵活性
```

**🔸 五种组合模式的核心**
```
适配器：让不兼容的接口能够协同工作
桥接：分离"做什么"和"怎么做"
组合：统一处理单个对象和对象集合
装饰器：动态给对象穿"衣服"添加功能
外观：为复杂系统提供简单"门面"
```

### 8.2 关键理解要点


**🔹 组合 vs 继承**
```
继承关系：IS-A（是一个）
- 静态关系，编译时确定
- 白盒复用，子类了解父类细节
- 类爆炸问题

组合关系：HAS-A（有一个）
- 动态关系，运行时确定  
- 黑盒复用，只知道接口
- 更灵活的功能组合
```

**🔹 模式选择的思考路径**
```
1. 问题类型判断：
   - 接口不匹配 → 适配器
   - 多维度变化 → 桥接
   - 树形结构 → 组合
   - 功能扩展 → 装饰器
   - 复杂简化 → 外观

2. 变化频率考虑：
   - 编译时确定 → 继承
   - 运行时变化 → 组合

3. 复杂度权衡：
   - 简单场景 → 直接实现
   - 复杂场景 → 设计模式
```

### 8.3 实际应用指导


**💡 最佳实践建议**
```
1. 优先组合，慎用继承
2. 保持接口简单统一
3. 遵循单一职责原则
4. 考虑未来扩展需求
5. 权衡复杂度和灵活性
```

**⚠️ 常见误区提醒**
```
❌ 过度设计：简单问题复杂化
❌ 模式滥用：为了模式而模式
❌ 忽略性能：过多的间接调用
❌ 违背直觉：让代码难以理解
```

**🎯 学习建议**
```
1. 理解问题本质，再选择模式
2. 多看实际代码，理解应用场景
3. 从简单开始，逐步掌握复杂应用
4. 重点关注模式解决的问题，而不是实现细节
5. 在实际项目中尝试应用，积累经验
```

**🔄 记忆口诀**
```
适配转接口，桥接分两端
组合树结构，装饰动态添
外观简复杂，组合胜继承
模式解问题，实用是关键
```