---
title: 7、二分思维
---
## 📚 目录

1. [二分查找原理](#1-二分查找原理)
2. [边界条件处理](#2-边界条件处理)
3. [二分答案](#3-二分答案)
4. [二分在排序中的应用](#4-二分在排序中的应用)
5. [二分查找变种](#5-二分查找变种)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 二分查找原理


### 1.1 什么是二分思维


**🔸 核心概念**
二分思维就是"**分而治之**"的思想，把一个大问题分成两半来解决。就像我们小时候玩的"**猜数字游戏**"一样：

```
你心里想一个1-100的数字，我来猜：
我猜：50   你说：大了
我猜：25   你说：小了  
我猜：37   你说：大了
我猜：31   你说：对了！

每次都猜中间值，快速缩小范围
```

**💡 二分的本质**
- **前提条件**：数据必须是***有序的***（这是关键！）
- **核心思想**：每次排除一半的数据
- **效率优势**：比一个一个找快得多

### 1.2 二分查找的基本原理


**🔍 工作流程**
```
有序数组：[1, 3, 5, 7, 9, 11, 13, 15]
查找目标：7

第1次：中间位置 = (0+7)/2 = 3，arr[3] = 7
发现就是要找的数！返回位置3

如果查找目标是5：
第1次：中间位置 = (0+7)/2 = 3，arr[3] = 7 > 5
       说明5在左半边，范围缩小为 [1,3,5]
第2次：中间位置 = (0+2)/2 = 1，arr[1] = 3 < 5  
       说明5在右半边，范围缩小为 [5]
第3次：中间位置 = 2，arr[2] = 5，找到了！
```

**⚡ 为什么这么快**
```
线性查找：最坏要看100个数据，时间复杂度O(n)
二分查找：最坏只要看7次（log₂100≈7），时间复杂度O(log n)

比如1000万个数据：
线性查找：最坏1000万次
二分查找：最坏24次（log₂10000000≈24）
```

### 1.3 二分查找的基本实现


```java
// 最简单的二分查找
public int binarySearch(int[] arr, int target) {
    int left = 0;                    // 左边界
    int right = arr.length - 1;      // 右边界
    
    while (left <= right) {
        int mid = left + (right - left) / 2;  // 防止溢出的中间值计算
        
        if (arr[mid] == target) {
            return mid;              // 找到了，返回位置
        } else if (arr[mid] < target) {
            left = mid + 1;          // 目标在右半边
        } else {
            right = mid - 1;         // 目标在左半边
        }
    }
    return -1;                       // 没找到
}
```

**🔧 代码关键点解释**
- `left + (right - left) / 2`：防止`left + right`太大导致整数溢出
- `left <= right`：等号很重要，保证能查找到边界元素
- `mid + 1`和`mid - 1`：避免死循环，一定要移动边界

---

## 2. 🎯 边界条件处理


### 2.1 为什么边界条件这么重要


**🚨 常见的坑**
边界条件是二分查找最容易出错的地方，一不小心就***死循环***或者***漏掉答案***：

```
错误示例：
while (left < right) {        // 少了等号
    // ...
    right = mid;              // 没有-1
}
// 可能导致死循环或找不到答案
```

### 2.2 三种边界处理模板


#### 🔸 模板一：精确查找

```java
// 查找确切的目标值
public int search(int[] arr, int target) {
    int left = 0, right = arr.length - 1;
    
    while (left <= right) {                    // 包含等号
        int mid = left + (right - left) / 2;
        
        if (arr[mid] == target) return mid;
        else if (arr[mid] < target) left = mid + 1;   // +1
        else right = mid - 1;                         // -1
    }
    return -1;
}
```

#### 🔹 模板二：查找左边界

```java
// 查找第一个大于等于target的位置
public int searchLeft(int[] arr, int target) {
    int left = 0, right = arr.length;         // 注意这里是length，不是length-1
    
    while (left < right) {                     // 没有等号
        int mid = left + (right - left) / 2;
        
        if (arr[mid] < target) {
            left = mid + 1;
        } else {
            right = mid;                       // 不是mid-1
        }
    }
    return left;
}
```

#### 🔺 模板三：查找右边界

```java
// 查找最后一个小于等于target的位置
public int searchRight(int[] arr, int target) {
    int left = 0, right = arr.length;
    
    while (left < right) {
        int mid = left + (right - left) / 2;
        
        if (arr[mid] <= target) {
            left = mid + 1;
        } else {
            right = mid;
        }
    }
    return left - 1;
}
```

### 2.3 边界处理的记忆技巧


**💭 记忆口诀**
```
精确查找：左右都减一，条件带等号
左边界：右不减一，条件不带等号  
右边界：最后减一，其他同左边界
```

**🎯 实际应用示例**
```java
数组：[1, 2, 2, 2, 3, 4, 5]
目标：2

精确查找：返回任意一个2的位置（比如位置1）
左边界：返回第一个2的位置（位置1）
右边界：返回最后一个2的位置（位置3）
```

---

## 3. 🔍 二分答案


### 3.1 什么是二分答案


**🔸 核心概念**
二分答案不是在数组中查找元素，而是在***可能的答案范围***中查找最优答案。这是二分思维的高级应用！

**💡 适用场景**
- 求"最大值的最小值"或"最小值的最大值"
- 答案具有***单调性***（答案越大，某个条件越容易满足）

### 3.2 经典例题：分配饼干


**📝 问题描述**
有`n`个小朋友，每个小朋友的胃口值为`g[i]`。有`m`块饼干，每块饼干的尺寸为`s[j]`。一块饼干只能满足一个小朋友，且饼干尺寸要大于等于小朋友的胃口值。问最多能满足多少个小朋友？

**🎯 二分答案思路**
```
答案范围：0 到 min(n, m) 个小朋友
单调性：如果能满足k个小朋友，那么一定能满足k-1个小朋友

二分过程：
1. 猜测能满足mid个小朋友
2. 检验这个猜测是否可行
3. 可行就往更大的方向找，不可行就往小的方向找
```

```java
public int findContentChildren(int[] g, int[] s) {
    Arrays.sort(g);  // 排序很重要
    Arrays.sort(s);
    
    int left = 0, right = Math.min(g.length, s.length);
    
    while (left < right) {
        int mid = left + (right - left + 1) / 2;  // 注意这里+1，避免死循环
        
        if (canSatisfy(g, s, mid)) {
            left = mid;        // 能满足mid个，试试更多的
        } else {
            right = mid - 1;   // 不能满足mid个，减少目标
        }
    }
    return left;
}

// 检验能否满足count个小朋友
private boolean canSatisfy(int[] g, int[] s, int count) {
    int i = 0, j = 0, satisfied = 0;
    
    while (i < g.length && j < s.length && satisfied < count) {
        if (s[j] >= g[i]) {
            satisfied++;
            i++;
        }
        j++;
    }
    return satisfied >= count;
}
```

### 3.3 二分答案的模板


**🔧 标准模板**
```java
public int binaryAnswer(问题参数) {
    int left = 最小可能答案;
    int right = 最大可能答案;
    
    while (left < right) {
        int mid = left + (right - left + 1) / 2;  // 求上界时+1
        
        if (check(mid)) {  // 检验mid是否可行
            left = mid;    // 可行，试试更大的
        } else {
            right = mid - 1;  // 不可行，减小目标
        }
    }
    return left;
}
```

---

## 4. 📊 二分在排序中的应用


### 4.1 插入排序的优化


**🔸 传统插入排序**
```java
// 传统插入排序：线性查找插入位置
public void insertionSort(int[] arr) {
    for (int i = 1; i < arr.length; i++) {
        int key = arr[i];
        int j = i - 1;
        
        // 线性查找插入位置，时间O(n)
        while (j >= 0 && arr[j] > key) {
            arr[j + 1] = arr[j];
            j--;
        }
        arr[j + 1] = key;
    }
}
```

**⚡ 二分插入排序**
```java
// 用二分查找确定插入位置，优化查找时间
public void binaryInsertionSort(int[] arr) {
    for (int i = 1; i < arr.length; i++) {
        int key = arr[i];
        
        // 二分查找插入位置，时间O(log n)
        int pos = binarySearchInsertPos(arr, 0, i - 1, key);
        
        // 移动元素
        for (int j = i - 1; j >= pos; j--) {
            arr[j + 1] = arr[j];
        }
        arr[pos] = key;
    }
}

// 查找插入位置
private int binarySearchInsertPos(int[] arr, int left, int right, int target) {
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (arr[mid] <= target) {
            left = mid + 1;
        } else {
            right = mid - 1;
        }
    }
    return left;
}
```

### 4.2 归并排序中的应用


**🔄 归并过程的二分思想**
```java
// 归并排序本身就体现了二分思想
public void mergeSort(int[] arr, int left, int right) {
    if (left < right) {
        int mid = left + (right - left) / 2;    // 二分分割
        
        mergeSort(arr, left, mid);              // 处理左半部分
        mergeSort(arr, mid + 1, right);         // 处理右半部分
        
        merge(arr, left, mid, right);           // 合并结果
    }
}
```

**💡 二分的威力**
- 把大问题分解成小问题
- 每次都减少一半的工作量
- 时间复杂度从O(n²)降到O(n log n)

---

## 5. 🎨 二分查找变种


### 5.1 旋转数组中的查找


**📝 问题描述**
在一个旋转排序数组中查找目标值。比如`[4,5,6,7,0,1,2]`是`[0,1,2,4,5,6,7]`的一个旋转。

**🎯 解题思路**
虽然整个数组不是有序的，但是分成两半后，***至少有一半是有序的***！

```java
public int searchInRotated(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    
    while (left <= right) {
        int mid = left + (right - left) / 2;
        
        if (nums[mid] == target) return mid;
        
        // 判断哪一半是有序的
        if (nums[left] <= nums[mid]) {  // 左半部分有序
            if (nums[left] <= target && target < nums[mid]) {
                right = mid - 1;  // target在左半部分
            } else {
                left = mid + 1;   // target在右半部分
            }
        } else {  // 右半部分有序
            if (nums[mid] < target && target <= nums[right]) {
                left = mid + 1;   // target在右半部分  
            } else {
                right = mid - 1;  // target在左半部分
            }
        }
    }
    return -1;
}
```

**🔧 关键理解**
- 旋转数组虽然整体不有序，但局部有序
- 先判断哪一半有序，再判断目标在哪一半

### 5.2 查找峰值元素


**📝 问题描述**
峰值元素是指其值大于左右相邻值的元素。数组可能包含多个峰值，返回任何一个峰值的索引即可。

```java
public int findPeakElement(int[] nums) {
    int left = 0, right = nums.length - 1;
    
    while (left < right) {
        int mid = left + (right - left) / 2;
        
        // 比较mid和mid+1
        if (nums[mid] < nums[mid + 1]) {
            left = mid + 1;    // 峰值在右边
        } else {
            right = mid;       // 峰值在左边（包括mid）
        }
    }
    return left;
}
```

**💡 核心洞察**
- 如果`nums[mid] < nums[mid+1]`，说明右边有更大的数，峰值在右边
- 否则峰值在左边（包括mid位置）
- 这样必定能找到一个峰值

### 5.3 在无限数组中查找


**📝 问题描述**
数组无限大，但只能通过API `get(index)`访问元素，查找目标值。

**🎯 两步法**
```java
public int searchInInfiniteArray(ArrayReader reader, int target) {
    // 第一步：确定搜索范围
    int right = 1;
    while (reader.get(right) < target) {
        right *= 2;  // 指数增长，快速定位范围
    }
    
    // 第二步：在范围内二分查找
    int left = right / 2;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        int midVal = reader.get(mid);
        
        if (midVal == target) return mid;
        else if (midVal < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}
```

**⚡ 效率分析**
- 第一步：O(log n)时间确定范围
- 第二步：O(log n)时间精确查找
- 总时间复杂度：O(log n)

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


**🔸 二分思维的本质**
```
分而治之：把大问题分成两半解决
排除性：每次排除一半的可能性  
对数效率：从O(n)优化到O(log n)
前提条件：数据必须有某种单调性
```

**🔸 三大应用场景**
```
1. 在有序数组中查找元素
2. 查找答案的最优值（二分答案）
3. 在局部有序的数据中查找
```

### 6.2 关键理解要点


**🔹 边界条件的处理**
```
关键原则：
- 搞清楚查找的是什么（精确值/左边界/右边界）
- 确定循环条件（<= 还是 <）
- 确定边界更新（+1/-1 还是不变）

常见错误：
- 死循环：边界更新错误
- 漏掉答案：循环条件错误
- 数组越界：边界计算错误
```

**🔹 二分答案的精髓**
```
适用条件：
- 答案有明确的范围
- 答案具有单调性
- 能快速验证某个答案是否可行

解题步骤：
1. 确定答案的取值范围
2. 写出验证函数check()
3. 二分查找最优答案
```

### 6.3 实际应用指导


**🎯 选择合适的模板**
- **精确查找**：查找特定元素，返回位置或-1
- **左边界查找**：查找第一个满足条件的位置
- **右边界查找**：查找最后一个满足条件的位置
- **二分答案**：在答案空间中查找最优解

**🔧 调试技巧**
```java
// 添加调试信息，观察搜索过程
while (left <= right) {
    int mid = left + (right - left) / 2;
    System.out.println("left=" + left + ", right=" + right + ", mid=" + mid);
    
    // 正常的二分逻辑...
}
```

**⚠️ 常见陷阱避免**
- **整数溢出**：用`left + (right - left) / 2`代替`(left + right) / 2`
- **死循环**：确保每次循环都能缩小搜索范围
- **边界错误**：仔细考虑边界元素的处理

### 6.4 学习进阶路径


**📈 掌握层次**
```
🟢 基础级：掌握标准二分查找
🟡 进阶级：熟练处理各种边界条件
🟠 高级级：运用二分答案解决优化问题
🔴 专家级：在复杂数据结构中应用二分思想
```

**🎯 练习建议**
1. **先熟练基础模板**：标准二分查找写100遍
2. **专攻边界处理**：左边界、右边界查找
3. **掌握二分答案**：解决最值优化问题
4. **挑战复杂变种**：旋转数组、二维数组等

**核心记忆口诀**：
```
二分思维核心点，有序单调是关键
每次排除一半量，对数复杂效率高
边界条件要细心，模板选择需准确
答案空间能二分，最值问题迎刃解
```