---
title: 10、算法化归应用
---
## 📚 目录

1. [基础算法应用](#1-基础算法应用)
2. [算法组合策略](#2-算法组合策略)
3. [数据结构映射](#3-数据结构映射)
4. [算法模板化](#4-算法模板化)
5. [性能优化路径](#5-性能优化路径)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔄 基础算法应用


### 1.1 什么是算法化归


**💡 核心概念**
```
算法化归：把复杂问题转换成我们熟悉的基础算法来解决
就像数学中的"化归思想" - 把陌生问题变成熟悉问题

简单理解：
遇到新问题 → 想想能不能用排序解决？
             → 能不能用搜索解决？
             → 能不能用图遍历解决？
```

> 💡 **新手理解**：就像做菜一样，复杂的菜谱最终都是炒、煮、蒸、炸这些基本操作的组合

### 1.2 复杂计算转换成排序


**🎯 核心思路**：很多看起来复杂的问题，本质上是"找规律"，而排序能帮我们发现规律

**典型场景转换**

```java
// 问题：找到数组中出现频率最高的元素
// 复杂思路：要统计、要比较、要记录...
// 化归思路：排序后相同元素聚集，问题变简单

public int findMostFrequent(int[] nums) {
    Arrays.sort(nums);  // 化归为排序问题
    
    int maxCount = 1, currentCount = 1;
    int result = nums[0];
    
    for (int i = 1; i < nums.length; i++) {
        if (nums[i] == nums[i-1]) {
            currentCount++;
        } else {
            if (currentCount > maxCount) {
                maxCount = currentCount;
                result = nums[i-1];
            }
            currentCount = 1;
        }
    }
    return result;
}
```

**排序化归的经典应用**

| 原问题类型 | 化归后的解法 | 核心思想 |
|-----------|-------------|----------|
| **找重复元素** | `排序后相邻比较` | 相同元素聚集在一起 |
| **找缺失数字** | `排序后检查连续性` | 有序序列便于发现断层 |
| **合并区间** | `按起点排序再合并` | 有序便于判断重叠 |
| **最接近的数对** | `排序后双指针` | 有序便于逼近目标 |

### 1.3 复杂计算转换成搜索


**🔍 搜索化归思路**：把"寻找答案"的问题转换成"验证答案"的问题

```java
// 问题：在旋转排序数组中找目标值
// 复杂思路：要处理旋转、要找断点...
// 化归思路：二分搜索的变形

public int searchRotated(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    
    while (left <= right) {
        int mid = left + (right - left) / 2;
        
        if (nums[mid] == target) return mid;
        
        // 关键：确定哪一半是有序的
        if (nums[left] <= nums[mid]) {  // 左半部分有序
            if (target >= nums[left] && target < nums[mid]) {
                right = mid - 1;  // 在左半部分搜索
            } else {
                left = mid + 1;   // 在右半部分搜索
            }
        } else {  // 右半部分有序
            if (target > nums[mid] && target <= nums[right]) {
                left = mid + 1;   // 在右半部分搜索
            } else {
                right = mid - 1;  // 在左半部分搜索
            }
        }
    }
    return -1;
}
```

**搜索化归的思维模式**
```
原问题：在复杂结构中找东西
↓ 化归思考
能否定义一个"搜索空间"？
能否定义一个"判断条件"？
能否缩小搜索范围？
↓ 应用搜索算法
二分搜索、DFS、BFS
```

### 1.4 复杂计算转换成图遍历


**🌐 图遍历化归思路**：把关系问题、路径问题转换成图的遍历

```java
// 问题：判断两个字符串是否可以通过单词替换相互转换
// 复杂思路：要比较字符串、要记录变换路径...
// 化归思路：构建图，问题变成图的连通性

public boolean canTransform(String start, String end, List<String> wordList) {
    Set<String> wordSet = new HashSet<>(wordList);
    if (!wordSet.contains(end)) return false;
    
    Queue<String> queue = new LinkedList<>();
    queue.offer(start);
    wordSet.remove(start);
    
    while (!queue.isEmpty()) {
        String current = queue.poll();
        
        // 尝试改变每一个字符
        for (int i = 0; i < current.length(); i++) {
            char[] chars = current.toCharArray();
            for (char c = 'a'; c <= 'z'; c++) {
                chars[i] = c;
                String newWord = new String(chars);
                
                if (newWord.equals(end)) return true;
                
                if (wordSet.contains(newWord)) {
                    queue.offer(newWord);
                    wordSet.remove(newWord);
                }
            }
        }
    }
    return false;
}
```

**图遍历化归的应用场景**
```
关系型问题：朋友关系 → 社交网络图
路径型问题：迷宫寻路 → 网格图遍历  
依赖型问题：任务依赖 → 有向图拓扑排序
传播型问题：病毒传播 → 图的广度优先遍历
```

---

## 2. 🔧 算法组合策略


### 2.1 多算法协作解决复杂问题


**🎯 核心思想**：复杂问题往往需要多个简单算法配合，而不是一个复杂算法

> 📝 **类比理解**：就像造房子，不是发明一个"造房机器"，而是用挖掘、搬运、切割、焊接等基本操作组合

### 2.2 分治+排序组合


```java
// 问题：计算数组中的逆序对数量
// 策略：分治思想 + 归并排序

public int countInversions(int[] nums) {
    return mergeSort(nums, 0, nums.length - 1);
}

private int mergeSort(int[] nums, int left, int right) {
    if (left >= right) return 0;
    
    int mid = left + (right - left) / 2;
    int count = 0;
    
    // 分治：分别计算左右两部分的逆序对
    count += mergeSort(nums, left, mid);
    count += mergeSort(nums, mid + 1, right);
    
    // 合并：计算跨越两部分的逆序对
    count += merge(nums, left, mid, right);
    
    return count;
}

private int merge(int[] nums, int left, int mid, int right) {
    int[] temp = new int[right - left + 1];
    int i = left, j = mid + 1, k = 0;
    int count = 0;
    
    while (i <= mid && j <= right) {
        if (nums[i] <= nums[j]) {
            temp[k++] = nums[i++];
        } else {
            temp[k++] = nums[j++];
            count += (mid - i + 1);  // 关键：计算逆序对
        }
    }
    
    // 复制剩余元素...
    return count;
}
```

**组合效果**：
- 单纯暴力解法：O(n²) 时间复杂度
- 分治+排序组合：O(n log n) 时间复杂度

### 2.3 哈希表+双指针组合


```java
// 问题：找到数组中和为目标值的两个数
// 策略：哈希表记录 + 一次遍历

public int[] twoSum(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();
    
    for (int i = 0; i < nums.length; i++) {
        int complement = target - nums[i];
        
        if (map.containsKey(complement)) {
            return new int[]{map.get(complement), i};
        }
        
        map.put(nums[i], i);
    }
    
    return new int[]{};
}
```

**组合优势**：
- 哈希表：O(1) 查找速度
- 一次遍历：避免嵌套循环
- 组合结果：O(n) 时间，O(n) 空间

### 2.4 常见组合模式


**📋 高效组合模式总结**

| 组合方式 | 适用场景 | 核心优势 | 典型问题 |
|----------|----------|----------|----------|
| **分治+排序** | `大数据量统计` | 降低时间复杂度 | 逆序对、小和问题 |
| **哈希+遍历** | `查找匹配问题` | 快速定位 | 两数之和、字符统计 |
| **栈+遍历** | `配对匹配问题` | 后进先出特性 | 括号匹配、直方图面积 |
| **队列+层次** | `逐层处理问题` | 广度优先 | 二叉树层序、最短路径 |
| **双指针+排序** | `区间范围问题` | 有序查找 | 三数之和、容器盛水 |

---

## 3. 🗂️ 数据结构映射


### 3.1 问题特征到数据结构的映射规律


**🎯 核心思想**：不同的问题特征天然适合不同的数据结构

> 💡 **新手理解**：就像不同的工具适合不同的工作 - 锤子敲钉子，螺丝刀拧螺丝

### 3.2 问题特征识别与映射


**问题特征识别表**

```
问题特征分析图：

数据访问模式
├── 顺序访问 → 数组/链表
├── 随机访问 → 数组/哈希表  
├── 最值查找 → 堆/优先队列
└── 范围查询 → 线段树/树状数组

操作频率分析  
├── 查找频繁 → 哈希表/平衡树
├── 插入删除频繁 → 链表/动态数组
├── 排序需求 → 数组+排序算法
└── 去重需求 → Set/哈希表

关系复杂度
├── 一对一关系 → 数组/链表
├── 一对多关系 → 图/树结构  
├── 多对多关系 → 图/邻接表
└── 层次关系 → 树/递归结构
```

### 3.3 具体映射规律详解


**📊 访问模式映射**

```java
// 特征：需要快速查找和去重
// 映射：HashSet
public boolean containsDuplicate(int[] nums) {
    Set<Integer> seen = new HashSet<>();
    for (int num : nums) {
        if (!seen.add(num)) {  // add返回false表示已存在
            return true;
        }
    }
    return false;
}

// 特征：需要维护最大值/最小值
// 映射：PriorityQueue（堆）
public int[] topKFrequent(int[] nums, int k) {
    Map<Integer, Integer> count = new HashMap<>();
    for (int num : nums) {
        count.put(num, count.getOrDefault(num, 0) + 1);
    }
    
    // 小顶堆，保持size为k
    PriorityQueue<Map.Entry<Integer, Integer>> heap = 
        new PriorityQueue<>((a, b) -> a.getValue() - b.getValue());
    
    for (Map.Entry<Integer, Integer> entry : count.entrySet()) {
        heap.offer(entry);
        if (heap.size() > k) {
            heap.poll();
        }
    }
    
    // 提取结果
    int[] result = new int[k];
    for (int i = 0; i < k; i++) {
        result[i] = heap.poll().getKey();
    }
    return result;
}
```

**🔗 关系模式映射**

```java
// 特征：层次关系、递归结构
// 映射：树结构
public class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;
}

// 特征：复杂网络关系
// 映射：图结构（邻接表）
public class Graph {
    Map<Integer, List<Integer>> adjList = new HashMap<>();
    
    public void addEdge(int from, int to) {
        adjList.computeIfAbsent(from, k -> new ArrayList<>()).add(to);
    }
}
```

### 3.4 映射决策流程图


```
问题分析决策流程：

输入数据特点
├── 大小固定 → 数组
├── 大小变化 → 动态数组/链表
├── 键值对应 → Map/字典
└── 唯一性要求 → Set

操作需求分析
├── 频繁查找
│   ├── 精确查找 → 哈希表 O(1)
│   ├── 范围查找 → 有序数组+二分 O(log n)  
│   └── 模糊查找 → 字典树/后缀数组
├── 频繁插入删除
│   ├── 头尾操作 → 双端队列
│   ├── 中间操作 → 链表
│   └── 批量操作 → 动态数组
└── 维护顺序
    ├── 全局有序 → 平衡树
    ├── 局部有序 → 堆
    └── 自定义序 → 优先队列
```

---

## 4. 📋 算法模板化


### 4.1 常见问题的算法模板


**💡 模板化思想**：把解决同类问题的思路固化为代码模板，遇到类似问题直接套用

> 📝 **新手理解**：就像做菜的标准菜谱，同类菜品套用相同的基本步骤

### 4.2 二分搜索模板


```java
/**
 * 二分搜索标准模板
 * 适用：在有序数组中查找目标值或插入位置
 */
public int binarySearch(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    
    while (left <= right) {
        int mid = left + (right - left) / 2;  // 防止溢出
        
        if (nums[mid] == target) {
            return mid;
        } else if (nums[mid] < target) {
            left = mid + 1;
        } else {
            right = mid - 1;
        }
    }
    
    return -1;  // 未找到
}

/**
 * 寻找左边界的二分搜索
 * 适用：找到第一个大于等于target的位置
 */
public int searchLeftBound(int[] nums, int target) {
    int left = 0, right = nums.length;
    
    while (left < right) {
        int mid = left + (right - left) / 2;
        
        if (nums[mid] < target) {
            left = mid + 1;
        } else {
            right = mid;
        }
    }
    
    return left;
}
```

### 4.3 双指针模板


```java
/**
 * 对撞指针模板
 * 适用：有序数组中找两数之和、回文判断等
 */
public boolean isPalindrome(String s) {
    int left = 0, right = s.length() - 1;
    
    while (left < right) {
        // 跳过非字母数字字符
        while (left < right && !Character.isLetterOrDigit(s.charAt(left))) {
            left++;
        }
        while (left < right && !Character.isLetterOrDigit(s.charAt(right))) {
            right--;
        }
        
        // 比较字符
        if (Character.toLowerCase(s.charAt(left)) != 
            Character.toLowerCase(s.charAt(right))) {
            return false;
        }
        
        left++;
        right--;
    }
    
    return true;
}

/**
 * 快慢指针模板  
 * 适用：链表环检测、找中点等
 */
public ListNode detectCycle(ListNode head) {
    ListNode slow = head, fast = head;
    
    // 第一阶段：检测是否有环
    while (fast != null && fast.next != null) {
        slow = slow.next;
        fast = fast.next.next;
        
        if (slow == fast) {
            break;  // 发现环
        }
    }
    
    if (fast == null || fast.next == null) {
        return null;  // 无环
    }
    
    // 第二阶段：找环的起点
    slow = head;
    while (slow != fast) {
        slow = slow.next;
        fast = fast.next;
    }
    
    return slow;
}
```

### 4.4 滑动窗口模板


```java
/**
 * 滑动窗口标准模板
 * 适用：子数组/子字符串问题
 */
public int slidingWindowTemplate(String s) {
    Map<Character, Integer> window = new HashMap<>();
    int left = 0, right = 0;
    int result = 0;  // 根据题目要求调整
    
    while (right < s.length()) {
        // 扩大窗口
        char c = s.charAt(right);
        window.put(c, window.getOrDefault(c, 0) + 1);
        right++;
        
        // 缩小窗口（当窗口不满足条件时）
        while (needShrink(window)) {  // 根据具体问题定义
            char d = s.charAt(left);
            window.put(d, window.get(d) - 1);
            if (window.get(d) == 0) {
                window.remove(d);
            }
            left++;
        }
        
        // 更新结果（在合适的位置）
        result = Math.max(result, right - left);
    }
    
    return result;
}

// 具体应用：最长无重复字符子串
public int lengthOfLongestSubstring(String s) {
    Map<Character, Integer> window = new HashMap<>();
    int left = 0, right = 0;
    int maxLen = 0;
    
    while (right < s.length()) {
        char c = s.charAt(right);
        window.put(c, window.getOrDefault(c, 0) + 1);
        right++;
        
        // 当窗口中有重复字符时缩小窗口
        while (window.get(c) > 1) {
            char d = s.charAt(left);
            window.put(d, window.get(d) - 1);
            left++;
        }
        
        maxLen = Math.max(maxLen, right - left);
    }
    
    return maxLen;
}
```

### 4.5 回溯算法模板


```java
/**
 * 回溯算法标准模板
 * 适用：排列组合、N皇后、数独等
 */
public List<List<Integer>> backtrackTemplate(int[] nums) {
    List<List<Integer>> result = new ArrayList<>();
    List<Integer> path = new ArrayList<>();
    boolean[] used = new boolean[nums.length];
    
    backtrack(nums, path, used, result);
    return result;
}

private void backtrack(int[] nums, List<Integer> path, 
                      boolean[] used, List<List<Integer>> result) {
    // 终止条件
    if (path.size() == nums.length) {
        result.add(new ArrayList<>(path));
        return;
    }
    
    // 选择列表
    for (int i = 0; i < nums.length; i++) {
        if (used[i]) continue;  // 跳过已使用的元素
        
        // 做选择
        path.add(nums[i]);
        used[i] = true;
        
        // 递归
        backtrack(nums, path, used, result);
        
        // 撤销选择
        path.remove(path.size() - 1);
        used[i] = false;
    }
}
```

### 4.6 模板使用指南


**📚 模板选择决策树**

```
问题类型识别：

查找类问题
├── 有序数据 → 二分搜索模板
├── 无序数据 → 哈希表模板
└── 特殊结构 → DFS/BFS模板

优化类问题  
├── 连续子数组 → 滑动窗口模板
├── 最值问题 → 动态规划模板
└── 组合问题 → 贪心算法模板

穷举类问题
├── 有约束条件 → 回溯算法模板
├── 无约束条件 → 递归遍历模板
└── 图结构 → DFS/BFS模板
```

> ⚠️ **注意**：模板是思路框架，不是死板套用。要根据具体问题调整细节

---

## 5. 🚀 性能优化路径


### 5.1 从暴力解法到优化算法的演进


**💡 优化思路**：性能优化不是一蹴而就，而是逐步演进的过程

> 📝 **学习策略**：先实现暴力解法确保正确性，再逐步优化性能

### 5.2 优化演进实例：两数之和问题


**第一版：暴力解法**
```java
// 时间复杂度：O(n²)，空间复杂度：O(1)
public int[] twoSumBrute(int[] nums, int target) {
    for (int i = 0; i < nums.length; i++) {
        for (int j = i + 1; j < nums.length; j++) {
            if (nums[i] + nums[j] == target) {
                return new int[]{i, j};
            }
        }
    }
    return new int[]{};
}
```

**第二版：哈希表优化**  
```java
// 时间复杂度：O(n)，空间复杂度：O(n)
// 优化思路：用空间换时间，避免重复查找
public int[] twoSumHash(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();
    
    for (int i = 0; i < nums.length; i++) {
        int complement = target - nums[i];
        if (map.containsKey(complement)) {
            return new int[]{map.get(complement), i};
        }
        map.put(nums[i], i);
    }
    return new int[]{};
}
```

**第三版：双指针优化（有序数组）**
```java
// 时间复杂度：O(n)，空间复杂度：O(1)  
// 优化思路：利用有序性质，双向逼近
public int[] twoSumSorted(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    
    while (left < right) {
        int sum = nums[left] + nums[right];
        if (sum == target) {
            return new int[]{left, right};
        } else if (sum < target) {
            left++;
        } else {
            right--;
        }
    }
    return new int[]{};
}
```

### 5.3 优化演进实例：最大子数组和


**第一版：暴力解法**
```java
// 时间复杂度：O(n³)
public int maxSubArrayBrute(int[] nums) {
    int maxSum = Integer.MIN_VALUE;
    
    for (int i = 0; i < nums.length; i++) {
        for (int j = i; j < nums.length; j++) {
            int sum = 0;
            for (int k = i; k <= j; k++) {
                sum += nums[k];
            }
            maxSum = Math.max(maxSum, sum);
        }
    }
    return maxSum;
}
```

**第二版：前缀和优化**
```java
// 时间复杂度：O(n²)，空间复杂度：O(1)
// 优化思路：避免重复计算子数组和
public int maxSubArrayPrefix(int[] nums) {
    int maxSum = Integer.MIN_VALUE;
    
    for (int i = 0; i < nums.length; i++) {
        int sum = 0;
        for (int j = i; j < nums.length; j++) {
            sum += nums[j];  // 累加而不是重新计算
            maxSum = Math.max(maxSum, sum);
        }
    }
    return maxSum;
}
```

**第三版：动态规划优化（Kadane算法）**
```java
// 时间复杂度：O(n)，空间复杂度：O(1)
// 优化思路：局部最优推导全局最优
public int maxSubArrayDP(int[] nums) {
    int maxSum = nums[0];
    int currentSum = nums[0];
    
    for (int i = 1; i < nums.length; i++) {
        // 核心决策：是否包含前面的子数组
        currentSum = Math.max(nums[i], currentSum + nums[i]);
        maxSum = Math.max(maxSum, currentSum);
    }
    return maxSum;
}
```

### 5.4 性能优化的通用路径


**🛣️ 优化路径图**

```
性能优化演进路径：

第一阶段：暴力解法
├── 目标：实现功能，保证正确性
├── 特点：嵌套循环，直观简单
└── 复杂度：通常O(n²)或O(n³)

第二阶段：空间换时间  
├── 手段：哈希表、缓存、预处理
├── 思路：避免重复计算
└── 效果：降低时间复杂度，增加空间复杂度

第三阶段：算法优化
├── 手段：贪心、动态规划、分治
├── 思路：发现问题的数学性质
└── 效果：时间空间双重优化

第四阶段：工程优化
├── 手段：并行计算、缓存友好、内存池
├── 思路：利用硬件特性
└── 效果：常数级别的性能提升
```

### 5.5 优化决策指南


**📊 优化策略选择表**

| 当前瓶颈 | 优化方向 | 常用技术 | 适用场景 |
|----------|----------|----------|----------|
| **时间复杂度高** | `降低算法复杂度` | 贪心、DP、分治 | 算法密集型任务 |
| **重复计算多** | `缓存中间结果` | 哈希表、记忆化 | 递归、查找频繁 |
| **数据访问慢** | `改善访问模式` | 预排序、索引 | 数据密集型任务 |
| **空间使用大** | `压缩数据结构` | 位运算、原地算法 | 内存受限场景 |

**⚡ 优化效果预期**

```
优化层次与效果：

算法层面优化：10x - 1000x 性能提升
├── O(n²) → O(n log n)：10-100倍提升
├── O(n²) → O(n)：数百倍提升  
└── 指数 → 多项式：成千上万倍提升

实现层面优化：2x - 10x 性能提升
├── 减少内存分配：2-3倍提升
├── 缓存友好访问：2-5倍提升
└── 避免重复计算：3-10倍提升

工程层面优化：1.5x - 5x 性能提升
├── 编译器优化：1.5-2倍提升
├── 并行计算：接近核心数倍提升
└── 硬件加速：3-5倍提升
```

> 💡 **新手建议**：先追求算法层面的大幅优化，再考虑工程层面的细节优化

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 算法化归：将复杂问题转换为熟悉的基础算法问题
🔸 算法组合：多个简单算法协作解决复杂问题  
🔸 数据结构映射：根据问题特征选择合适的数据结构
🔸 算法模板化：将解决同类问题的思路固化为代码模板
🔸 性能优化路径：从暴力解法逐步演进到优化算法
```

### 6.2 关键理解要点


**🔹 化归思维的本质**
```
核心思想：
- 化陌生为熟悉：新问题 → 已知问题
- 化复杂为简单：复杂问题 → 基础算法组合
- 化抽象为具体：抽象关系 → 数据结构表示

实际价值：
- 降低问题难度，提高解题效率
- 复用已有知识，避免重复学习  
- 建立系统思维，形成解题套路
```

**🔹 数据结构选择的决策思路**
```
分析维度：
- 数据特征：大小、类型、关系
- 操作需求：查找、插入、删除、遍历
- 性能要求：时间复杂度、空间复杂度

选择原则：
- 优先匹配操作需求
- 兼顾时间空间平衡
- 考虑实现复杂度
```

**🔹 性能优化的渐进思路**
```
优化阶段：
1. 正确性第一：先实现再优化
2. 瓶颈识别：找出性能瓶颈点
3. 渐进优化：逐步改进，验证效果
4. 权衡取舍：时间vs空间、简单vs复杂

优化重点：
- 算法层面：寻求更优的算法复杂度
- 实现层面：减少不必要的计算和内存操作
- 工程层面：利用语言和硬件特性
```

### 6.3 实际应用指导


**🎯 问题解决流程**
```
1. 问题理解：明确输入输出，理解约束条件
2. 模式识别：判断问题类型，寻找相似问题
3. 算法选择：选择合适的基础算法或组合策略
4. 数据结构设计：根据操作需求选择数据结构
5. 实现编码：套用相应模板，注意边界条件
6. 测试验证：验证正确性，评估性能
7. 优化改进：根据瓶颈进行针对性优化
```

**🛠️ 技能提升建议**
```
基础能力：
- 熟练掌握基础算法：排序、搜索、遍历
- 理解核心数据结构：数组、链表、栈、队列、树、图、哈希表
- 掌握算法模板：二分、双指针、滑动窗口、回溯、动态规划

进阶能力：  
- 培养化归思维：看到问题能想到基础算法
- 练习组合应用：多算法协作解决复杂问题
- 建立优化意识：从暴力解法逐步优化

高级能力：
- 问题抽象：将实际问题抽象为算法问题
- 性能分析：准确分析算法时间空间复杂度
- 工程实践：考虑实际应用中的约束和优化
```

**核心记忆**：
- 复杂问题简单化，基础算法是根基
- 数据结构选择准，问题特征要看清  
- 算法组合威力大，模板套用效率高
- 性能优化分层次，渐进改进最稳妥