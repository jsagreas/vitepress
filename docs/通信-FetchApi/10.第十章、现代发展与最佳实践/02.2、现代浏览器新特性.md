---
title: 2、现代浏览器新特性
---
## 📚 目录

1. [Streaming API支持情况](#1-streaming-api支持情况)
2. [Service Worker中的Fetch应用](#2-service-worker中的fetch应用)
3. [Background Fetch功能介绍](#3-background-fetch功能介绍)
4. [新增配置选项的使用](#4-新增配置选项的使用)
5. [性能优化的新特性](#5-性能优化的新特性)
6. [安全增强功能的应用](#6-安全增强功能的应用)
7. [新特性的兼容性处理](#7-新特性的兼容性处理)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🌊 Streaming API支持情况


### 1.1 什么是Streaming API

**通俗理解**：就是让数据像水流一样一点点传输，而不是等所有数据都准备好了才一次性发送。

```
传统方式（像倒水桶）：        流式传输（像水龙头）：
等待 → 等待 → 等待 → 全部数据    数据片段1 → 数据片段2 → 数据片段3
```

### 1.2 ReadableStream 的实际作用

**核心作用**：让你可以边下载边处理数据，不用等全部下载完

```javascript
// 💡 简单示例：边下载边显示进度
async function downloadWithProgress(url) {
    const response = await fetch(url);
    const reader = response.body.getReader();
    
    let receivedLength = 0;
    const chunks = [];
    
    while(true) {
        const {done, value} = await reader.read();
        
        if (done) break;
        
        chunks.push(value);
        receivedLength += value.length;
        
        // 🔸 实时显示下载进度
        console.log(`已下载：${receivedLength} 字节`);
    }
}
```

### 1.3 实际应用场景

```
✅ 大文件下载：边下载边显示进度条
✅ 在线视频：边下载边播放，不用等全部下载完
✅ 实时日志：服务器日志实时传输到前端
✅ 聊天应用：消息实时流式传输
```

### 1.4 浏览器支持情况


| 浏览器 | **支持版本** | **注意事项** |
|--------|-------------|-------------|
| 🌍 **Chrome** | `43+` | `完整支持` |
| 🦊 **Firefox** | `57+` | `部分限制` |
| 🧭 **Safari** | `14.1+` | `较新支持` |
| 📘 **Edge** | `79+` | `基于Chromium` |

---

## 2. 🔧 Service Worker中的Fetch应用


### 2.1 Service Worker是什么

**通俗解释**：就是在后台运行的一个"小助手"，帮你拦截网络请求，可以做缓存、离线功能等。

```
正常请求流程：
网页 → 直接向服务器请求 → 返回数据

有Service Worker：
网页 → Service Worker拦截 → 
    ↓
检查缓存 → 有缓存就直接返回
         → 没缓存就向服务器请求
```

### 2.2 在Service Worker中使用Fetch

```javascript
// 💡 sw.js - Service Worker文件
self.addEventListener('fetch', event => {
    // 🔸 拦截所有网络请求
    event.respondWith(
        // 先查缓存
        caches.match(event.request)
            .then(response => {
                // 有缓存就返回缓存
                if (response) {
                    return response;
                }
                
                // 没缓存就发起真实请求
                return fetch(event.request);
            })
    );
});
```

### 2.3 缓存策略实现

```javascript
// 🎯 常用策略：缓存优先
self.addEventListener('fetch', event => {
    if (event.request.url.includes('/api/')) {
        // API请求：网络优先
        event.respondWith(
            fetch(event.request)
                .catch(() => caches.match(event.request))
        );
    } else {
        // 静态资源：缓存优先
        event.respondWith(
            caches.match(event.request)
                .then(response => response || fetch(event.request))
        );
    }
});
```

### 2.4 实际应用价值

```
✅ 离线功能：网络断开时依然能用
✅ 加载速度：缓存让页面秒开
✅ 省流量：重复资源不用重新下载
✅ 用户体验：网络差时也能正常使用
```

---

## 3. 📱 Background Fetch功能介绍


### 3.1 Background Fetch是什么

**通俗理解**：就是让下载任务在后台继续进行，即使用户关闭了网页或应用也不会停止。

```
传统下载：
用户打开页面 → 开始下载 → 用户关闭页面 → 下载中断 ❌

Background Fetch：
用户打开页面 → 开始下载 → 用户关闭页面 → 下载继续 ✅
```

### 3.2 基本使用方法

```javascript
// 💡 在Service Worker中启动后台下载
self.addEventListener('backgroundfetch', event => {
    if (event.tag === 'large-file-download') {
        event.waitUntil(
            self.registration.backgroundFetch.fetch(
                'large-file',  // 任务ID
                '/api/large-file.zip',  // 下载URL
                {
                    icons: [{ src: '/icon.png', sizes: '256x256', type: 'image/png' }],
                    title: '正在下载大文件...',
                    downloadTotal: 50 * 1024 * 1024  // 50MB
                }
            )
        );
    }
});
```

### 3.3 监听下载进度

```javascript
// 🔸 监听下载状态变化
self.addEventListener('backgroundfetchsuccess', event => {
    console.log('下载完成:', event.tag);
    
    // 可以向用户发送通知
    self.registration.showNotification('下载完成！', {
        body: '您的文件已下载完毕',
        icon: '/success-icon.png'
    });
});

self.addEventListener('backgroundfetchfail', event => {
    console.log('下载失败:', event.tag);
});
```

### 3.4 适用场景

```
✅ 大文件下载：视频、软件安装包
✅ 批量同步：离线数据同步
✅ 内容预加载：提前下载用户可能需要的内容
✅ 移动应用：PWA应用的重要功能
```

> ⚠️ **注意**: Background Fetch目前支持度较低，主要在Chrome等Chromium内核浏览器中可用

---

## 4. ⚙️ 新增配置选项的使用


### 4.1 Priority 优先级控制

**作用**：告诉浏览器哪些请求更重要，优先处理重要请求。

```javascript
// 🎯 设置请求优先级
fetch('/api/critical-data', {
    priority: 'high'  // 高优先级
});

fetch('/api/background-data', {
    priority: 'low'   // 低优先级
});
```

**优先级选项**：
- `high` - 高优先级（重要数据）
- `normal` - 普通优先级（默认）
- `low` - 低优先级（后台数据）

### 4.2 Referrer Policy 引用策略

**作用**：控制请求时是否发送来源页面信息。

```javascript
fetch('/api/data', {
    referrerPolicy: 'no-referrer'  // 不发送来源信息
});
```

**常用策略**：
```
no-referrer          → 不发送任何来源信息
origin              → 只发送域名，不发送完整URL
same-origin         → 同域才发送来源信息
```

### 4.3 Keepalive 连接保持

**作用**：让请求在页面关闭后依然能完成。

```javascript
// 💡 页面关闭时发送统计数据
window.addEventListener('beforeunload', () => {
    fetch('/api/analytics', {
        method: 'POST',
        body: JSON.stringify({userId: 123, time: Date.now()}),
        keepalive: true  // 🔸 页面关闭后继续发送
    });
});
```

---

## 5. ⚡ 性能优化的新特性


### 5.1 Request Deduplication 请求去重

**作用**：自动合并相同的请求，避免重复发送。

```javascript
// 🎯 多个组件同时请求相同数据
Promise.all([
    fetch('/api/user/123'),  // 第一个请求
    fetch('/api/user/123'),  // 相同请求会被合并
    fetch('/api/user/123')   // 不会真的发送3次
]);
```

### 5.2 HTTP/3 支持

**好处**：更快的连接建立，更好的网络性能。

```
HTTP/1.1：串行请求，容易阻塞
HTTP/2：  并行请求，但TCP层面仍可能阻塞  
HTTP/3：  基于QUIC，几乎消除了阻塞
```

### 5.3 Early Hints 早期提示

**作用**：服务器提前告诉浏览器需要加载哪些资源。

```javascript
// 💡 服务器发送103状态码，提前告知需要的资源
// 浏览器可以提前开始下载CSS、JS等文件
// 无需特殊代码，浏览器自动处理
```

### 5.4 Cache API 增强

```javascript
// 🔸 更精细的缓存控制
const cache = await caches.open('v1');

// 部分匹配缓存
const response = await cache.match(request, {
    ignoreSearch: true,   // 忽略URL参数
    ignoreVary: true      // 忽略Vary头
});
```

---

## 6. 🔐 安全增强功能的应用


### 6.1 Trusted Types 可信类型

**作用**：防止XSS攻击，确保只有安全的内容能被插入到DOM中。

```javascript
// 💡 安全的动态内容插入
if (window.trustedTypes) {
    const policy = trustedTypes.createPolicy('myPolicy', {
        createHTML: (string) => {
            // 清理和验证HTML内容
            return DOMPurify.sanitize(string);
        }
    });
    
    // 安全地插入内容
    element.innerHTML = policy.createHTML(userContent);
}
```

### 6.2 Cross-Origin Isolation 跨域隔离

**作用**：提供更强的安全隔离，支持SharedArrayBuffer等高级功能。

```html
<!-- 🔸 需要设置特殊响应头 -->
Cross-Origin-Opener-Policy: same-origin
Cross-Origin-Embedder-Policy: require-corp
```

### 6.3 Permissions Policy 权限策略

**作用**：精确控制页面可以使用哪些浏览器功能。

```html
<!-- 🎯 限制页面功能使用 -->
<iframe 
    src="/widget" 
    allow="camera 'none'; microphone 'none'">
</iframe>
```

---

## 7. 🔄 新特性的兼容性处理


### 7.1 特性检测

```javascript
// 💡 检测是否支持新特性
function checkFeatureSupport() {
    const support = {
        streamingAPI: 'ReadableStream' in window,
        backgroundFetch: 'serviceWorker' in navigator && 'backgroundFetch' in ServiceWorkerRegistration.prototype,
        priority: 'priority' in Request.prototype
    };
    
    console.log('浏览器支持情况:', support);
    return support;
}
```

### 7.2 渐进增强策略

```javascript
// 🎯 优雅降级的实现
async function enhancedFetch(url, options = {}) {
    // 检查是否支持优先级
    if ('priority' in Request.prototype) {
        options.priority = options.priority || 'normal';
    }
    
    // 检查是否支持流式处理
    const response = await fetch(url, options);
    
    if (response.body && response.body.getReader) {
        // 支持流式处理
        return handleStreamResponse(response);
    } else {
        // 降级到普通处理
        return response.json();
    }
}
```

### 7.3 Polyfill 兼容方案

```javascript
// 🔸 为不支持的浏览器提供备用方案
if (!window.fetch) {
    // 使用axios或其他库作为备用
    window.fetch = require('node-fetch');
}

if (!('serviceWorker' in navigator)) {
    // 提供离线缓存的备用方案
    console.warn('Service Worker不支持，使用localStorage缓存');
}
```

### 7.4 用户体验优化

```javascript
// 💡 根据支持情况调整功能
function adaptFeatures() {
    const support = checkFeatureSupport();
    
    if (support.backgroundFetch) {
        // 显示后台下载选项
        showBackgroundDownloadOption();
    } else {
        // 显示普通下载提示
        showRegularDownloadWarning();
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 现代特性价值总结


```
🌊 Streaming API：
  ✅ 让大数据传输更流畅
  ✅ 实时处理，用户体验更好
  ✅ 节省内存，不用等全部数据

🔧 Service Worker：
  ✅ 离线功能，网络断开也能用
  ✅ 缓存机制，页面加载更快
  ✅ 后台处理，用户无感知

📱 Background Fetch：
  ✅ 后台下载，关闭页面也继续
  ✅ 大文件下载的救星
  ✅ 移动端PWA的重要功能

⚡ 性能优化：
  ✅ 请求去重，避免浪费
  ✅ HTTP/3支持，连接更快
  ✅ 缓存增强，控制更精细
```

### 8.2 实际应用指导


**🎯 选择使用原则**：
- **大文件传输** → 使用Streaming API
- **离线应用** → 配置Service Worker
- **后台任务** → 考虑Background Fetch
- **性能敏感** → 启用新的优化特性

**⚠️ 兼容性考虑**：
- **特性检测** → 先检查浏览器支持
- **渐进增强** → 新特性作为加分项
- **备用方案** → 准备降级处理
- **用户提示** → 告知功能限制

### 8.3 学习建议


```
📚 学习路径：
1️⃣ 先掌握基础Fetch API
2️⃣ 理解Service Worker概念
3️⃣ 体验Streaming API效果
4️⃣ 关注新特性发展趋势

🔧 实践建议：
✅ 在小项目中尝试新特性
✅ 关注浏览器更新日志
✅ 参与开发者社区讨论
✅ 保持技术敏感度
```

**核心记忆**：
- 现代浏览器新特性让网络请求更强大
- 重点关注用户体验提升
- 兼容性处理是工程化必备技能
- 新技术要结合实际需求使用