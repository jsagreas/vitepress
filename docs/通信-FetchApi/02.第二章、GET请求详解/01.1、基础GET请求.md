---
title: 1、基础GET请求
---
## 📚 目录

1. [什么是GET请求](#1-什么是GET请求)
2. [fetch最简化用法](#2-fetch最简化用法)
3. [处理响应数据](#3-处理响应数据)
4. [实际项目场景](#4-实际项目场景)
5. [最佳实践指南](#5-最佳实践指南)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔍 什么是GET请求


### 1.1 GET请求的本质


**通俗理解**：GET请求就像是**去商店问价格**，你想知道某个商品多少钱，就问店员"这个商品多少钱？"，店员告诉你价格。

```
现实生活对比：
👤 顾客："苹果多少钱一斤？"     ← GET请求
🏪 店员："5块钱一斤"           ← 服务器响应

网络请求：
💻 浏览器："给我用户123的信息"   ← GET请求
🖥️ 服务器："用户123叫张三，25岁" ← 服务器响应数据
```

### 1.2 GET请求的特点


**📋 核心特征**
```
🔸 获取数据：专门用来从服务器"要"数据
🔸 不改变数据：只是看看，不会修改服务器上的信息
🔸 参数透明：查询条件直接显示在网址里
🔸 可以缓存：浏览器会记住相同的请求结果
```

**💡 生活化类比**
```
GET请求 = 图书馆借书
- 你去图书馆查书 (发请求)
- 图书管理员告诉你书在哪 (返回数据)
- 你只是查看，不会改变图书馆的书 (不修改数据)
- 你记住书的位置，下次更容易找到 (缓存机制)
```

### 1.3 什么时候用GET请求


**🎯 典型使用场景**
- **查看网页内容**：打开任何网页都是GET请求
- **搜索功能**：在搜索框输入关键词查找
- **获取用户信息**：查看个人资料、好友列表
- **商品展示**：电商网站显示商品列表

---

## 2. ⚡ fetch最简化用法


### 2.1 最简单的GET请求


**🔸 基础语法**
```javascript
// 最简单的写法，就一行代码！
fetch('https://api.example.com/users')
```

**💭 这行代码的含义**
- `fetch()` = 向服务器发请求的工具
- `'网址'` = 你想要数据的地方
- 默认就是GET请求，不用特别说明

### 2.2 完整的基础示例


```javascript
// 1. 发请求并获取数据
fetch('https://jsonplaceholder.typicode.com/users/1')
  .then(response => response.json())  // 2. 把服务器响应转成我们能用的数据
  .then(data => {
    console.log('用户信息：', data);  // 3. 使用获取到的数据
  });
```

**🧠 理解这个过程**
```
步骤分解：
1️⃣ fetch发请求 → 就像打电话给服务器
2️⃣ response.json() → 把服务器说的话翻译成我们懂的
3️⃣ then(data => ...) → 拿到翻译好的数据，想干啥就干啥
```

### 2.3 用async/await写法（推荐）


```javascript
// 现代化写法，更容易理解
async function getUserInfo() {
  try {
    const response = await fetch('https://jsonplaceholder.typicode.com/users/1');
    const userData = await response.json();
    console.log('用户信息：', userData);
  } catch (error) {
    console.log('出错了：', error);
  }
}

// 调用函数
getUserInfo();
```

**✨ 为什么推荐这种写法**
- **更像正常的代码**：从上往下读，逻辑清晰
- **错误处理简单**：try/catch就能搞定
- **容易调试**：一步步执行，问题好找

---

## 3. 📊 处理响应数据


### 3.1 JSON数据处理（最常用）


**📋 什么是JSON数据**
```
JSON = JavaScript Object Notation
通俗理解：就是用大括号{}和方括号[]组织的数据格式
像这样：{"name": "张三", "age": 25}
```

**💻 获取JSON数据完整示例**
```javascript
async function getUser() {
  try {
    // 1. 发请求
    const response = await fetch('https://jsonplaceholder.typicode.com/users/1');
    
    // 2. 转换为JSON格式
    const user = await response.json();
    
    // 3. 使用数据
    document.getElementById('username').textContent = user.name;
    document.getElementById('email').textContent = user.email;
  } catch (error) {
    alert('获取用户信息失败');
  }
}
```

### 3.2 文本数据处理


```javascript
async function getWelcomeMessage() {
  try {
    const response = await fetch('/api/welcome-message');
    const message = await response.text();  // 获取纯文本
    
    document.getElementById('welcome').textContent = message;
  } catch (error) {
    console.log('获取欢迎信息失败');
  }
}
```

### 3.3 检查请求是否成功


```javascript
async function safeGetData() {
  try {
    const response = await fetch('/api/data');
    
    // 检查请求是否成功
    if (!response.ok) {
      throw new Error('请求失败了');
    }
    
    const data = await response.json();
    return data;
  } catch (error) {
    console.log('出错了：', error.message);
    return null;
  }
}
```

**🔍 response.ok的含义**
- `response.ok` = 请求成功了吗？
- 成功 = 服务器正常返回了数据
- 失败 = 服务器出错了（比如404找不到页面）

---

## 4. 🛠️ 实际项目场景


### 4.1 示例1：获取用户数据


**🎯 场景说明**：用户登录后，显示个人信息

```javascript
// HTML结构
/*
<div id="userProfile">
  <h2 id="userName"></h2>
  <p id="userEmail"></p>
  <p id="userPhone"></p>
</div>
*/

async function loadUserProfile(userId) {
  try {
    // 根据用户ID获取信息
    const response = await fetch(`/api/users/${userId}`);
    const user = await response.json();
    
    // 更新页面显示
    document.getElementById('userName').textContent = user.name;
    document.getElementById('userEmail').textContent = `邮箱：${user.email}`;
    document.getElementById('userPhone').textContent = `电话：${user.phone}`;
    
  } catch (error) {
    document.getElementById('userProfile').innerHTML = '加载用户信息失败';
  }
}

// 页面加载后执行
loadUserProfile(123);
```

### 4.2 示例2：获取商品列表并渲染


**🎯 场景说明**：电商页面显示商品列表

```javascript
async function loadProductList() {
  try {
    const response = await fetch('/api/products');
    const products = await response.json();
    
    // 生成商品HTML
    const productHTML = products.map(product => `
      <div class="product-card">
        <h3>${product.name}</h3>
        <p class="price">¥${product.price}</p>
        <button onclick="buyProduct(${product.id})">购买</button>
      </div>
    `).join('');
    
    // 显示在页面上
    document.getElementById('productList').innerHTML = productHTML;
    
  } catch (error) {
    document.getElementById('productList').innerHTML = '<p>商品加载失败，请刷新重试</p>';
  }
}

// 页面加载时获取商品
window.addEventListener('load', loadProductList);
```

### 4.3 示例3：搜索功能


```javascript
async function searchProducts(keyword) {
  if (!keyword.trim()) {
    alert('请输入搜索关键词');
    return;
  }
  
  try {
    // 在URL中传递搜索参数
    const response = await fetch(`/api/products/search?q=${encodeURIComponent(keyword)}`);
    const results = await response.json();
    
    if (results.length === 0) {
      document.getElementById('searchResults').innerHTML = '<p>没有找到相关商品</p>';
    } else {
      displaySearchResults(results);
    }
    
  } catch (error) {
    alert('搜索失败，请重试');
  }
}

// 绑定搜索按钮
document.getElementById('searchBtn').addEventListener('click', () => {
  const keyword = document.getElementById('searchInput').value;
  searchProducts(keyword);
});
```

---

## 5. ✨ 最佳实践指南


### 5.1 错误处理要做好


```javascript
// ❌ 不好的做法 - 没有错误处理
fetch('/api/data').then(response => response.json()).then(data => console.log(data));

// ✅ 好的做法 - 完整的错误处理
async function fetchData() {
  try {
    const response = await fetch('/api/data');
    
    if (!response.ok) {
      throw new Error(`请求失败: ${response.status}`);
    }
    
    const data = await response.json();
    return data;
  } catch (error) {
    console.error('获取数据失败:', error);
    return null;
  }
}
```

### 5.2 加载状态提示


```javascript
async function loadDataWithLoading() {
  // 显示加载中
  document.getElementById('loading').style.display = 'block';
  document.getElementById('content').style.display = 'none';
  
  try {
    const response = await fetch('/api/data');
    const data = await response.json();
    
    // 显示数据
    displayData(data);
    
  } catch (error) {
    // 显示错误信息
    document.getElementById('content').innerHTML = '加载失败，请重试';
  } finally {
    // 隐藏加载中
    document.getElementById('loading').style.display = 'none';
    document.getElementById('content').style.display = 'block';
  }
}
```

### 5.3 合理使用缓存


```javascript
// 简单的内存缓存
const cache = new Map();

async function getCachedData(url) {
  // 先检查缓存
  if (cache.has(url)) {
    console.log('使用缓存数据');
    return cache.get(url);
  }
  
  // 缓存中没有，发请求获取
  try {
    const response = await fetch(url);
    const data = await response.json();
    
    // 存入缓存
    cache.set(url, data);
    return data;
  } catch (error) {
    console.error('获取数据失败:', error);
    return null;
  }
}
```

### 5.4 URL参数处理


```javascript
// 构建带参数的URL
function buildApiUrl(baseUrl, params) {
  const url = new URL(baseUrl);
  Object.keys(params).forEach(key => {
    url.searchParams.append(key, params[key]);
  });
  return url.toString();
}

// 使用示例
async function searchUsers(filters) {
  const apiUrl = buildApiUrl('/api/users', {
    page: filters.page || 1,
    size: filters.size || 10,
    keyword: filters.keyword || ''
  });
  
  const response = await fetch(apiUrl);
  return response.json();
}
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 GET请求本质：从服务器获取数据，不修改任何信息
🔸 fetch基础语法：fetch(url) 就是最简单的GET请求
🔸 异步处理：使用 async/await 让代码更容易理解
🔸 数据转换：response.json() 把服务器数据转成可用格式
🔸 错误处理：try/catch 捕获网络错误和数据解析错误
```

### 6.2 关键理解要点


**🔹 为什么需要异步处理**
```
网络请求需要时间：
- 发请求到服务器需要时间
- 服务器处理需要时间  
- 返回数据也需要时间

如果不用异步：页面会卡住等待
用了异步：页面正常运行，数据回来后再处理
```

**🔹 response.json()的作用**
```
服务器返回的是"原始数据"：
- 就像收到一封外语信件
- response.json() 就是翻译工具
- 翻译后才能在JavaScript中使用
```

**🔹 错误处理的重要性**
```
网络世界不可靠：
- 网络可能断开
- 服务器可能出错
- 数据可能格式错误

不处理错误：用户看到空白页面，不知道发生什么
处理错误：告诉用户"加载失败，请重试"
```

### 6.3 实际应用价值


**🎯 掌握GET请求能做什么**
- **展示数据**：用户信息、商品列表、新闻文章
- **搜索功能**：根据关键词查找内容
- **实时更新**：获取最新的数据显示给用户
- **个性化内容**：根据用户喜好获取推荐内容

### 6.4 学习建议


**📚 循序渐进的学习路径**
```
第1步：理解GET请求的概念和作用
第2步：掌握fetch()的基本用法
第3步：学会处理JSON数据
第4步：添加错误处理机制
第5步：在实际项目中应用
```

**🛠️ 实践建议**
- **多练习**：用不同的API接口练习GET请求
- **看控制台**：使用浏览器开发者工具观察网络请求
- **处理错误**：故意断网测试错误处理是否有效
- **用户体验**：添加加载提示，让用户知道正在获取数据

**核心记忆**：
- GET请求像问路，只是获取信息不改变什么
- fetch()发请求，async/await处理响应
- response.json()翻译数据，try/catch处理错误
- 用户体验很重要，加载和错误提示不能少