---
title: 2、JSONP跨域实现
---
## 📚 目录

1. [什么是跨域问题](#1-什么是跨域问题)
2. [JSONP原理解析](#2-JSONP原理解析)
3. [jQuery中的JSONP实现](#3-jQuery中的JSONP实现)
4. [JSONP参数详解](#4-JSONP参数详解)
5. [实际应用示例](#5-实际应用示例)
6. [注意事项与限制](#6-注意事项与限制)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🚧 什么是跨域问题


### 1.1 跨域问题的通俗解释


想象一下，你住在北京，想要给上海的朋友寄信。如果邮局规定"只能给同城的人寄信"，那你就没法给上海朋友寄信了。

**网页的跨域问题就是这样**：
```
你的网站：http://localhost:3000        (北京)
要访问：http://api.weather.com         (上海)
浏览器说：不行！你们不是同一个域名！
```

### 1.2 什么时候算跨域


**🔸 同源策略三要素**
```
协议 + 域名 + 端口 都相同 = 同源
只要有一个不同 = 跨域

示例分析：
当前页面：http://www.example.com:8080

✅ 同源：
http://www.example.com:8080/api     → 只是路径不同
http://www.example.com:8080/user    → 只是路径不同

❌ 跨域：
https://www.example.com:8080        → 协议不同(http vs https)
http://api.example.com:8080         → 域名不同(www vs api)
http://www.example.com:3000         → 端口不同(8080 vs 3000)
```

### 1.3 为什么会有跨域限制


**🔐 安全考虑**：
- 防止恶意网站窃取其他网站的数据
- 保护用户的隐私信息
- 避免CSRF攻击

---

## 2. 💡 JSONP原理解析


### 2.1 JSONP是什么


**JSONP = JSON with Padding（填充式JSON）**

通俗理解：既然浏览器不让我们直接跨域请求数据，那我们就"借道"一下！

### 2.2 script标签的特殊性


**🔑 关键发现**：
```html
<!-- 这样是被允许的！ -->
<script src="http://other-domain.com/script.js"></script>
<script src="https://cdn.jquery.com/jquery.min.js"></script>
```

**为什么script标签可以跨域？**
- 因为网页经常需要引用其他网站的JS库
- 比如引用jQuery、Bootstrap等
- 浏览器对script标签没有同源限制

### 2.3 JSONP的巧妙思路


**💭 核心思想**：
```
既然script标签可以跨域，
那我们就让服务器返回一段可执行的JavaScript代码，
而不是纯JSON数据！
```

**🔄 工作流程图**：
```
客户端                    服务器端
   |                         |
   |--[1]创建script标签----->|
   |   src=api?callback=fn   |
   |                         |
   |<--[2]返回JS代码---------|
   |   fn({data: "hello"})   |
   |                         |
   |--[3]自动执行回调函数--->|
   |   处理数据               |
```

### 2.4 JSONP实现原理详解


**步骤1：客户端准备回调函数**
```javascript
// 准备一个回调函数来处理数据
function handleWeatherData(data) {
    console.log('收到天气数据：', data);
}
```

**步骤2：动态创建script标签**
```javascript
// 创建script标签，指定回调函数名
var script = document.createElement('script');
script.src = 'http://api.weather.com/get?callback=handleWeatherData';
document.head.appendChild(script);
```

**步骤3：服务器返回可执行代码**
```javascript
// 服务器不返回纯JSON：{"city": "北京", "temp": "25°C"}
// 而是返回函数调用：
handleWeatherData({"city": "北京", "temp": "25°C"});
```

**步骤4：浏览器自动执行**
```javascript
// script加载完成后，浏览器自动执行返回的代码
// 相当于调用了我们预先准备的回调函数
```

---

## 3. 🛠️ jQuery中的JSONP实现


### 3.1 jQuery简化了JSONP


**🎯 jQuery的优势**：
- 自动处理script标签的创建和销毁
- 自动生成回调函数名
- 统一的错误处理机制
- 与普通Ajax请求相似的API

### 3.2 基础语法


**📝 最简单的JSONP请求**：
```javascript
$.ajax({
    url: 'http://api.weather.com/cityweather',
    type: 'GET',
    dataType: 'jsonp',  // 🔑 关键：指定为jsonp
    success: function(data) {
        console.log('天气数据：', data);
    }
});
```

### 3.3 简写方法


**$.getJSON() 支持JSONP**：
```javascript
// 在URL后面加上 ?callback=? 就启用JSONP
$.getJSON('http://api.weather.com/cityweather?callback=?', function(data) {
    console.log('收到数据：', data);
});
```

---

## 4. ⚙️ JSONP参数详解


### 4.1 关键参数说明


| 参数 | 含义 | 默认值 | 说明 |
|------|------|--------|------|
| `dataType` | 数据类型 | - | **必须设为"jsonp"** |
| `jsonp` | 回调参数名 | "callback" | 告诉服务器回调函数名的参数叫什么 |
| `jsonpCallback` | 回调函数名 | 自动生成 | 指定具体的回调函数名 |

### 4.2 jsonp参数详解


**🔸 默认情况**：
```javascript
$.ajax({
    url: 'http://api.example.com/data',
    dataType: 'jsonp'
    // jQuery会发送：http://api.example.com/data?callback=jQuery123456
});
```

**🔸 自定义参数名**：
```javascript
$.ajax({
    url: 'http://api.example.com/data',
    dataType: 'jsonp',
    jsonp: 'cb'  // 改变参数名
    // jQuery会发送：http://api.example.com/data?cb=jQuery123456
});
```

### 4.3 jsonpCallback参数详解


**🔸 自动生成回调函数名（推荐）**：
```javascript
$.ajax({
    url: 'http://api.example.com/data',
    dataType: 'jsonp'
    // jQuery自动生成：jQuery12345678_1234567890
});
```

**🔸 手动指定回调函数名**：
```javascript
// 先定义回调函数
function myWeatherCallback(data) {
    console.log('天气：', data);
}

$.ajax({
    url: 'http://api.example.com/data',
    dataType: 'jsonp',
    jsonpCallback: 'myWeatherCallback'  // 使用自定义函数名
});
```

### 4.4 完整参数示例


```javascript
$.ajax({
    url: 'http://api.weather.com/cityweather',
    type: 'GET',
    dataType: 'jsonp',
    data: {
        city: '北京',
        key: 'your-api-key'
    },
    jsonp: 'callback',           // 回调参数名
    jsonpCallback: 'weatherCb',  // 回调函数名
    timeout: 5000,               // 5秒超时
    success: function(result) {
        console.log('成功：', result);
    },
    error: function(xhr, status, error) {
        console.log('失败：', error);
    }
});
```

---

## 5. 🌤️ 实际应用示例


### 5.1 使用新浪天气API示例


**🔸 新浪天气JSONP接口**：
```
接口地址：http://weather.sina.com.cn/iframe/index/w_cl.php
参数：code (城市代码)
回调参数：_cb
```

**📝 完整示例代码**：
```javascript
// 获取北京天气 (城市代码：101010100)
$.ajax({
    url: 'http://weather.sina.com.cn/iframe/index/w_cl.php',
    type: 'GET',
    dataType: 'jsonp',
    data: {
        code: '101010100',     // 北京城市代码
        day: 0                 // 当天天气
    },
    jsonp: '_cb',             // 新浪接口使用_cb作为回调参数
    timeout: 3000,
    success: function(data) {
        // 解析天气数据
        if (data && data.weather) {
            var weather = data.weather;
            var html = `
                <h3>📍 ${weather.city}</h3>
                <p>🌡️ 温度：${weather.temp}°C</p>
                <p>🌤️ 天气：${weather.weather}</p>
                <p>💨 风向：${weather.wd} ${weather.wp}</p>
            `;
            $('#weather-info').html(html);
        }
    },
    error: function() {
        $('#weather-info').html('<p>❌ 天气数据获取失败</p>');
    }
});
```

### 5.2 百度搜索建议示例


**📝 搜索建议功能**：
```javascript
$('#search-input').on('input', function() {
    var keyword = $(this).val();
    if (keyword.length < 2) return;
    
    $.ajax({
        url: 'https://sp0.baidu.com/5a1Fazu8AA54nxGko9WTAnF6hhy/su',
        dataType: 'jsonp',
        data: {
            wd: keyword
        },
        jsonp: 'cb',
        success: function(data) {
            // 显示搜索建议
            var suggestions = data.s || [];
            var html = suggestions.map(item => 
                `<li class="suggestion-item">${item}</li>`
            ).join('');
            $('#suggestions').html(html);
        }
    });
});
```

### 5.3 实用工具函数


**🔧 封装通用JSONP函数**：
```javascript
// 通用JSONP请求函数
function jsonpRequest(options) {
    var defaults = {
        timeout: 5000,
        dataType: 'jsonp',
        type: 'GET'
    };
    
    var settings = $.extend({}, defaults, options);
    
    return $.ajax(settings).fail(function() {
        console.error('JSONP请求失败：', settings.url);
    });
}

// 使用示例
jsonpRequest({
    url: 'http://api.example.com/data',
    data: { city: '上海' },
    success: function(data) {
        console.log('数据：', data);
    }
});
```

---

## 6. ⚠️ 注意事项与限制


### 6.1 JSONP的限制


**📋 重要限制**：

| 限制 | 说明 | 影响 |
|------|------|------|
| **🚫 仅支持GET请求** | 不能使用POST、PUT、DELETE | 无法提交复杂数据 |
| **🔓 安全性较低** | 依赖服务器返回可执行代码 | 存在XSS风险 |
| **❌ 错误处理有限** | 无法获取详细的HTTP状态码 | 调试困难 |
| **🎯 服务器必须支持** | 需要服务器专门配置JSONP | 不是所有API都支持 |

### 6.2 安全注意事项


**🔐 安全风险**：
```javascript
// ⚠️ 危险：如果服务器返回恶意代码
// 恶意服务器可能返回：alert('你的网站被攻击了！');
// 这段代码会在你的页面中执行！

// ✅ 安全做法：
// 1. 只信任可靠的API服务器
// 2. 对返回的数据进行验证
// 3. 设置合理的超时时间
```

### 6.3 最佳实践


**💡 推荐做法**：

```javascript
// ✅ 好的JSONP请求示例
$.ajax({
    url: 'https://trusted-api.com/data',
    dataType: 'jsonp',
    timeout: 3000,                    // 设置超时
    cache: false,                     // 避免缓存问题
    success: function(data) {
        // ✅ 验证数据格式
        if (data && typeof data === 'object') {
            handleData(data);
        } else {
            console.error('返回数据格式异常');
        }
    },
    error: function(xhr, status, error) {
        // ✅ 完善的错误处理
        if (status === 'timeout') {
            showMessage('请求超时，请稍后重试');
        } else {
            showMessage('网络异常，请检查连接');
        }
    }
});
```

### 6.4 现代替代方案


**🔄 其他跨域解决方案**：

```
🌟 CORS (推荐)：
- 现代浏览器标准
- 支持所有HTTP方法
- 更安全，更灵活

📡 代理服务器：
- 后端代理转发请求
- 避免跨域问题
- 可以添加认证和缓存

🔌 PostMessage：
- iframe间通信
- 适合特定场景
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 跨域问题：协议+域名+端口任一不同就是跨域
🔸 JSONP原理：利用script标签可跨域的特性
🔸 工作机制：服务器返回函数调用而非纯JSON
🔸 jQuery支持：dataType设为"jsonp"即可
🔸 重要限制：仅支持GET请求，存在安全风险
```

### 7.2 关键参数记忆


| 参数 | **必记要点** |
|------|-------------|
| `dataType: 'jsonp'` | **启用JSONP的关键设置** |
| `jsonp: 'callback'` | **回调参数名(默认callback)** |
| `jsonpCallback` | **自定义回调函数名(可选)** |
| `timeout` | **建议设置超时时间** |

### 7.3 实际应用指导


**🎯 什么时候使用JSONP**：
- ✅ 需要获取第三方API数据
- ✅ 对方服务器支持JSONP
- ✅ 只需要GET请求获取数据
- ✅ 对安全性要求不是特别高

**🚫 什么时候不用JSONP**：
- ❌ 需要POST提交数据
- ❌ 对安全性要求很高
- ❌ 需要详细的错误处理
- ❌ 服务器支持CORS

### 7.4 记忆口诀


```
JSONP跨域有妙招，
script标签来帮忙。
服务器返回函数调，
数据包在参数里。
只能GET不能POST，
安全问题要注意。
jQuery设置很简单，
dataType写jsonp！
```

### 7.5 常用代码模板


```javascript
// 🔖 标准JSONP请求模板
$.ajax({
    url: '目标API地址',
    dataType: 'jsonp',
    data: { /* 请求参数 */ },
    timeout: 5000,
    success: function(data) {
        // 处理成功返回的数据
    },
    error: function(xhr, status, error) {
        // 处理错误情况
    }
});
```

**核心理解**：
- JSONP不是Ajax，是借用script标签的"障眼法"
- 它绕过了浏览器的同源策略限制
- 虽然有限制，但在特定场景下非常有用
- 现代开发更推荐使用CORS等方案