---
title: 7、binlog过滤与压缩
---
## 📚 目录

1. [binlog过滤机制](#1-binlog过滤机制)
2. [数据库级过滤配置](#2-数据库级过滤配置)
3. [表级过滤规则](#3-表级过滤规则)
4. [binlog压缩技术](#4-binlog压缩技术)
5. [高级过滤策略](#5-高级过滤策略)
6. [性能优化与监控](#6-性能优化与监控)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 binlog过滤机制


### 1.1 什么是binlog过滤

**简单理解**：就像给binlog装了一个"筛子"，只让我们想要的数据变更记录通过，其他的都过滤掉。

```
未过滤的binlog：               过滤后的binlog：
┌─────────────────┐            ┌─────────────────┐
│ userdb.users    │            │ userdb.users    │ ✓
│ testdb.tmp      │    筛子    │                 │
│ userdb.orders   │     →      │ userdb.orders   │ ✓  
│ sysdb.config    │            │                 │
│ userdb.logs     │            │ userdb.logs     │ ✓
└─────────────────┘            └─────────────────┘
```

**为什么需要过滤**：
- **减少存储**：不记录不重要的表变更，节省磁盘空间
- **提升性能**：减少binlog写入量，提高数据库性能
- **简化备份**：只备份业务相关数据，忽略临时表
- **优化复制**：从库只同步需要的数据

### 1.2 过滤发生的时机

```
数据变更流程：
写入数据 → 过滤检查 → 写入binlog → 传输给从库
         ↗            ↗
    Master端过滤   Slave端过滤
```

**Master端过滤**：在主库写binlog时过滤
**Slave端过滤**：在从库接收binlog时过滤

---

## 2. 🏢 数据库级过滤配置


### 2.1 binlog_do_db：只记录指定数据库

**作用**：告诉MySQL"只记录这些数据库的变更"

```sql
-- 在my.cnf中配置
[mysqld]
binlog_do_db = userdb
binlog_do_db = orderdb
binlog_do_db = productdb
```

**实际效果**：
```
┌─ 配置说明 ─────────────────┐
│ 只有userdb、orderdb、      │
│ productdb的变更会被记录    │
│ 其他数据库的变更都忽略     │
└────────────────────────────┘
```

**测试示例**：
```sql
-- 这些操作会被记录到binlog
USE userdb;
INSERT INTO users (name) VALUES ('张三');

-- 这些操作不会被记录
USE tempdb;
CREATE TABLE test_table (id INT);
```

### 2.2 binlog_ignore_db：忽略指定数据库

**作用**：告诉MySQL"这些数据库的变更不要记录"

```sql
-- 在my.cnf中配置
[mysqld]
binlog_ignore_db = mysql
binlog_ignore_db = information_schema
binlog_ignore_db = performance_schema
binlog_ignore_db = sys
binlog_ignore_db = test
```

**使用场景**：
```
🔸 系统数据库：mysql、sys等不需要备份
🔸 临时数据库：test、temp等测试用数据库
🔸 缓存数据库：cache相关的临时存储
🔸 日志数据库：不重要的日志记录表
```

### 2.3 数据库级过滤注意事项


**重要提醒**：
```
⚠️ 跨数据库操作的坑：
USE db1;
UPDATE db2.table1 SET status = 1;

这种操作可能不会按预期过滤！
因为当前数据库是db1，但操作的是db2
```

**安全配置示例**：
```sql
-- 推荐的配置方式
[mysqld]
# 明确指定要记录的业务数据库
binlog_do_db = business_db
binlog_do_db = user_center
binlog_do_db = order_system

# 同时忽略系统数据库（双重保险）
binlog_ignore_db = mysql
binlog_ignore_db = sys
binlog_ignore_db = performance_schema
```

---

## 3. 📊 表级过滤规则


### 3.1 replicate_wild_do_table：表级包含规则

**作用**：在从库端控制哪些表的变更要执行

```sql
-- 在从库的my.cnf中配置
[mysqld]
replicate_wild_do_table = userdb.user_%
replicate_wild_do_table = orderdb.order_%
replicate_wild_do_table = %.important_%
```

**通配符规则**：
```
userdb.user_%     匹配：userdb.user_info、userdb.user_profile
orderdb.order_%   匹配：orderdb.order_main、orderdb.order_detail  
%.important_%     匹配：任何数据库的important_开头的表
```

### 3.2 replicate_wild_ignore_table：表级排除规则

**作用**：在从库端控制哪些表的变更要忽略

```sql
[mysqld]
replicate_wild_ignore_table = %.tmp_%
replicate_wild_ignore_table = %.cache_%
replicate_wild_ignore_table = %.log_%
replicate_wild_ignore_table = test.%
```

**实际应用场景**：
```sql
-- 这些表的变更会被忽略
CREATE TABLE user_tmp_20241208 (id INT);
INSERT INTO cache_user_session VALUES (1, 'session_data');
UPDATE log_access_record SET count = count + 1;
```

### 3.3 binlog_row_image配置

**作用**：控制行级复制时记录的数据量

```sql
-- 在my.cnf中配置
[mysqld]
binlog_row_image = minimal
```

**三种模式对比**：
```
┌─ binlog_row_image模式对比 ─┐
│ FULL    : 记录完整行数据   │ → 占用空间大，兼容性好
│ MINIMAL : 只记录变更字段   │ → 占用空间小，性能好  
│ NOBLOB  : 不记录BLOB字段   │ → 平衡选择
└────────────────────────────┘
```

**示例对比**：
```sql
-- 原始数据
CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    email VARCHAR(100),
    avatar BLOB,
    created_at TIMESTAMP
);

-- 执行更新
UPDATE users SET email = 'new@email.com' WHERE id = 1;

-- FULL模式记录：id, name, email, avatar, created_at (完整行)
-- MINIMAL模式记录：id, email (只有主键和变更字段)
-- NOBLOB模式记录：id, name, email, created_at (除了BLOB)
```

---

## 4. 🗜️ binlog压缩技术


### 4.1 为什么需要压缩

**空间问题**：
```
未压缩的binlog文件：
├── mysql-bin.000001  (1.2GB)
├── mysql-bin.000002  (1.5GB) 
├── mysql-bin.000003  (1.8GB)
└── ...
总计：每天可能产生10-50GB的binlog

压缩后：
├── mysql-bin.000001.gz  (200MB)
├── mysql-bin.000002.gz  (250MB)
├── mysql-bin.000003.gz  (300MB) 
└── ...
总计：压缩比可达5:1到10:1
```

### 4.2 启用binlog压缩

**MySQL 8.0.20+支持**：
```sql
-- 在my.cnf中配置
[mysqld]
binlog_transaction_compression = ON
binlog_transaction_compression_level_zstd = 3
```

**压缩级别选择**：
```
压缩级别对比：
Level 1: 压缩比3:1，CPU占用低  ← 高并发场景
Level 3: 压缩比5:1，CPU占用中  ← 推荐设置
Level 6: 压缩比7:1，CPU占用高  ← 存储敏感场景
```

### 4.3 手动压缩历史binlog

**压缩脚本示例**：
```bash
#!/bin/bash
# binlog压缩脚本

LOG_DIR="/var/lib/mysql"
DAYS_KEEP=7

# 查找7天前的binlog文件
find $LOG_DIR -name "mysql-bin.*" -mtime +$DAYS_KEEP | while read file; do
    if [[ $file != *.gz ]]; then
        echo "压缩文件: $file"
        gzip "$file"
        echo "压缩完成: $file.gz"
    fi
done
```

**压缩传输优化**：
```sql
-- 从库配置，启用压缩传输
[mysqld]
slave_compressed_protocol = ON
```

---

## 5. 🎛️ 高级过滤策略


### 5.1 动态过滤配置

**运行时修改过滤规则**：
```sql
-- 查看当前过滤配置
SHOW VARIABLES LIKE 'binlog_%db%';
SHOW VARIABLES LIKE 'replicate_%';

-- 动态添加过滤规则（从库端）
CHANGE REPLICATION FILTER 
REPLICATE_WILD_DO_TABLE = ('userdb.user_%', 'orderdb.order_%');

CHANGE REPLICATION FILTER 
REPLICATE_WILD_IGNORE_TABLE = ('%.tmp_%', '%.cache_%');
```

### 5.2 基于GTID的过滤

**场景**：只恢复特定事务
```sql
-- 跳过特定的GTID事务
SET GTID_NEXT = 'server-uuid:transaction-id';
BEGIN; COMMIT;
SET GTID_NEXT = 'AUTOMATIC';
```

### 5.3 混合过滤策略设计

**企业级配置示例**：
```sql
[mysqld]
# 数据库级过滤
binlog_do_db = business_core
binlog_do_db = user_center  
binlog_do_db = order_system

# 表级细化过滤（从库）
replicate_wild_do_table = business_core.%
replicate_wild_ignore_table = %.temp_%
replicate_wild_ignore_table = %.cache_%
replicate_wild_ignore_table = %.session_%

# 行级优化
binlog_row_image = minimal

# 压缩优化
binlog_transaction_compression = ON
binlog_transaction_compression_level_zstd = 3
```

---

## 6. ⚡ 性能优化与监控


### 6.1 过滤规则性能影响


**性能对比测试**：
```
测试环境：1000万行数据插入

无过滤：      ████████████ 100%时间
数据库过滤：   ██████████░░  85%时间  ← 节省15%
表级过滤：     ████████░░░░  70%时间  ← 节省30% 
行级优化：     ██████░░░░░░  50%时间  ← 节省50%
```

### 6.2 监控binlog状态

**关键监控指标**：
```sql
-- 查看binlog文件大小
SELECT 
    file_name,
    file_size,
    ROUND(file_size/1024/1024, 2) AS size_mb
FROM information_schema.files 
WHERE file_name LIKE '%binlog%';

-- 查看binlog写入速度
SHOW STATUS LIKE 'Binlog_cache%';
SHOW STATUS LIKE 'Com_insert%';
```

### 6.3 压缩效果监控

**压缩比监控脚本**：
```bash
#!/bin/bash
# 监控binlog压缩效果

original_size=$(du -sb /var/lib/mysql/mysql-bin.* | awk '{sum+=$1} END {print sum}')
compressed_size=$(du -sb /var/lib/mysql/mysql-bin.*.gz | awk '{sum+=$1} END {print sum}')

if [ $original_size -gt 0 ]; then
    ratio=$(echo "scale=2; $compressed_size / $original_size" | bc)
    echo "压缩比: $ratio ($(date))"
fi
```

### 6.4 过滤规则优化建议


**最佳实践**：
```
🎯 优化策略：
1. 优先使用数据库级过滤（性能最好）
2. 表级过滤用于细化控制
3. 避免过于复杂的通配符规则
4. 定期清理不必要的过滤规则

⚠️ 避免的坑：
1. 不要同时使用do和ignore规则
2. 注意跨数据库操作的过滤问题  
3. 测试环境要验证过滤效果
4. 备份恢复时要考虑过滤影响
```

**规则优先级**：
```
过滤规则优先级（从高到低）：
1. replicate_wild_ignore_table
2. replicate_wild_do_table  
3. replicate_ignore_table
4. replicate_do_table
5. binlog_ignore_db
6. binlog_do_db
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念

```
🔸 过滤机制：控制哪些数据变更记录到binlog中
🔸 数据库级过滤：binlog_do_db和binlog_ignore_db控制
🔸 表级过滤：replicate_wild_*_table规则细化控制
🔸 压缩技术：减少binlog存储空间，提升传输效率
🔸 性能影响：合理的过滤规则能显著提升性能
```

### 7.2 实际应用指导

```
适用场景：
✅ 大量临时表不需要备份
✅ 多租户系统需要分别备份
✅ 存储空间紧张需要压缩
✅ 网络带宽有限需要优化传输

配置建议：
🔧 生产环境：保守的过滤策略，确保数据安全
🔧 测试环境：激进的过滤策略，节省资源
🔧 备份场景：根据备份需求定制过滤规则
🔧 主从复制：在从库端进行细化过滤
```

### 7.3 常见问题解决

```
❓ 问题：过滤后数据恢复不完整
💡 解决：检查过滤规则，确保业务表没有被误过滤

❓ 问题：压缩后性能下降
💡 解决：调整压缩级别，平衡压缩比和CPU使用

❓ 问题：跨数据库操作被误过滤
💡 解决：使用表级过滤代替数据库级过滤

❓ 问题：从库数据不一致
💡 解决：检查主从过滤规则配置，确保一致性
```

**核心记忆**：
- binlog过滤像筛子，控制记录哪些变更
- 数据库级粗过滤，表级过滤更精细
- 压缩技术省空间，传输效率大提升
- 过滤规则要谨慎，数据安全是前提