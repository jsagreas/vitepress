---
title: 14、慢查询性能监控与告警
---
## 📚 目录

1. [慢查询监控基础概念](#1-慢查询监控基础概念)
2. [实时监控方案设计](#2-实时监控方案设计)
3. [监控数据源详解](#3-监控数据源详解)
4. [告警策略与配置](#4-告警策略与配置)
5. [可视化监控Dashboard](#5-可视化监控Dashboard)
6. [监控脚本开发实战](#6-监控脚本开发实战)
7. [监控系统集成方案](#7-监控系统集成方案)
8. [性能影响评估与优化](#8-性能影响评估与优化)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🎯 慢查询监控基础概念


### 1.1 什么是慢查询监控


**基本定义**：慢查询监控就是实时跟踪MySQL数据库中执行时间超过阈值的SQL语句，并在出现问题时及时发出告警。

> 💡 **通俗理解**：
> 
> 就像给数据库装了一个"监控摄像头"，专门盯着那些执行慢的SQL语句，一旦发现有问题立即"报警"。

```
监控流程简图：
SQL执行 → 时间检测 → 超时判断 → 记录收集 → 分析处理 → 告警通知
    ↓         ↓         ↓         ↓         ↓         ↓
   正常      <阈值      ≥阈值      存储      统计      邮件/短信
```

### 1.2 为什么需要慢查询监控


**核心价值**：
- 🚨 **提前预警**：在性能问题影响用户前发现问题
- 📊 **数据驱动**：用真实数据指导优化决策
- 🔍 **问题定位**：快速找到性能瓶颈的根源
- 📈 **趋势分析**：了解数据库性能变化趋势

**实际场景举例**：
```
❌ 没有监控的情况：
用户投诉网站慢 → 开发排查 → 发现慢SQL → 临时优化 → 问题反复

✅ 有监控的情况：
监控发现慢SQL → 自动告警 → 主动优化 → 性能稳定
```

### 1.3 监控关键指标


| 指标类型 | **具体指标** | **含义解释** | **正常范围** |
|---------|-------------|-------------|-------------|
| **执行时间** | `Query_time` | 单条SQL执行耗时 | `< 1秒` |
| **锁等待** | `Lock_time` | 等待锁的时间 | `< 0.1秒` |
| **扫描行数** | `Rows_examined` | 检查的数据行数 | `< 10万行` |
| **返回行数** | `Rows_sent` | 返回给客户端的行数 | `< 1000行` |
| **频率统计** | `执行次数/分钟` | 相同SQL的执行频率 | `< 100次/分钟` |

---

## 2. ⚡ 实时监控方案设计


### 2.1 监控架构总览


```
监控架构图：
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   MySQL数据库    │───▶│   数据收集层     │───▶│   处理分析层     │
│                │    │                │    │                │
│ • 慢查询日志     │    │ • 日志采集      │    │ • 数据清洗      │
│ • 性能模式表     │    │ • 实时监控      │    │ • 指标计算      │
│ • 状态变量      │    │ • 缓存存储      │    │ • 异常检测      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         │                       │                       ▼
         │                       │              ┌─────────────────┐
         │                       │              │   告警通知层     │
         │                       │              │                │
         │                       │              │ • 邮件告警      │
         │                       │              │ • 短信通知      │
         │                       │              │ • 钉钉/企微     │
         │                       │              └─────────────────┘
         │                       ▼
         │              ┌─────────────────┐
         │              │   可视化展示     │
         │              │                │
         └─────────────▶│ • Grafana      │
                        │ • 自定义Dashboard│
                        │ • 实时图表      │
                        └─────────────────┘
```

### 2.2 数据收集策略


**📊 多源数据采集**：

```sql
-- 1. 慢查询日志实时监控
SHOW VARIABLES LIKE 'slow_query_log%';
SHOW VARIABLES LIKE 'long_query_time';

-- 2. Performance Schema实时数据
SELECT * FROM performance_schema.events_statements_history_long 
WHERE timer_wait > 1000000000;  -- 大于1秒的查询

-- 3. 进程列表监控
SHOW PROCESSLIST;
```

**实时采集频率设置**：
- 🔥 **高频采集**：每5秒检查一次进程列表
- ⚡ **中频采集**：每1分钟统计慢查询数量
- 💡 **低频采集**：每5分钟分析趋势变化

### 2.3 监控数据流转


```
数据流转过程：
原始SQL → 标准化处理 → 指纹提取 → 聚合统计 → 阈值判断 → 告警触发

详细说明：
1. 原始SQL: SELECT * FROM users WHERE age > 25 AND city = 'Shanghai'
2. 标准化: SELECT * FROM users WHERE age > ? AND city = ?
3. 指纹提取: MD5('SELECT_users_WHERE_age_city')
4. 聚合统计: 统计相同指纹SQL的执行次数、平均时间等
5. 阈值判断: 检查是否超过预设的告警阈值
6. 告警触发: 满足条件时发送告警通知
```

---

## 3. 📋 监控数据源详解


### 3.1 PERFORMANCE_SCHEMA核心表


**🔍 events_statements_summary_by_digest表**：

> 💡 **作用说明**：这张表记录了按SQL指纹聚合的执行统计信息，是慢查询分析的核心数据源。

```sql
-- 查看当前最慢的SQL语句
SELECT 
    SCHEMA_NAME as '数据库',
    DIGEST_TEXT as 'SQL模板',
    COUNT_STAR as '执行次数',
    AVG_TIMER_WAIT/1000000000 as '平均执行时间(秒)',
    MAX_TIMER_WAIT/1000000000 as '最大执行时间(秒)',
    SUM_ROWS_EXAMINED as '总扫描行数',
    SUM_ROWS_SENT as '总返回行数'
FROM performance_schema.events_statements_summary_by_digest 
WHERE AVG_TIMER_WAIT > 1000000000  -- 平均执行时间大于1秒
ORDER BY AVG_TIMER_WAIT DESC 
LIMIT 10;
```

**📊 重要字段解释**：

| 字段名 | **含义** | **单位** | **用途** |
|--------|---------|---------|---------|
| `DIGEST_TEXT` | SQL语句模板 | 文本 | SQL识别和分类 |
| `COUNT_STAR` | 执行总次数 | 次数 | 频率分析 |
| `AVG_TIMER_WAIT` | 平均执行时间 | 纳秒 | 性能评估 |
| `MAX_TIMER_WAIT` | 最大执行时间 | 纳秒 | 最坏情况分析 |
| `SUM_LOCK_TIME` | 总锁等待时间 | 纳秒 | 锁竞争分析 |

### 3.2 INFORMATION_SCHEMA相关视图


**🔍 PROCESSLIST表监控**：

```sql
-- 实时监控正在执行的慢查询
SELECT 
    ID as '连接ID',
    USER as '用户',
    HOST as '来源主机',
    DB as '数据库',
    COMMAND as '命令类型',
    TIME as '执行时间(秒)',
    STATE as '状态',
    LEFT(INFO, 100) as 'SQL语句(前100字符)'
FROM INFORMATION_SCHEMA.PROCESSLIST 
WHERE TIME > 5  -- 执行时间超过5秒
  AND COMMAND = 'Query'
  AND STATE NOT LIKE '%Sleep%'
ORDER BY TIME DESC;
```

**实时监控价值**：
- ✅ **立即发现**：正在执行的慢查询
- ✅ **及时干预**：可以kill掉异常查询
- ✅ **状态跟踪**：了解查询执行状态

### 3.3 慢查询日志解析


**📝 日志格式解读**：

```sql
# Time: 2025-09-11T14:30:45.123456Z
# User@Host: webapp[webapp] @ [192.168.1.100]
# Thread_id: 12345  Schema: ecommerce  QC_hit: No
# Query_time: 3.456789  Lock_time: 0.000123  Rows_sent: 156  Rows_examined: 250000
# Rows_affected: 0  Bytes_sent: 15600
SET timestamp=1725978645;
SELECT u.username, u.email, p.product_name, o.order_date 
FROM users u 
JOIN orders o ON u.user_id = o.user_id 
JOIN products p ON o.product_id = p.product_id 
WHERE o.order_date >= '2025-01-01' 
ORDER BY o.order_date DESC;
```

**关键信息解释**：
- 🕐 **Query_time**: `3.456789秒` - SQL实际执行时间
- 🔒 **Lock_time**: `0.000123秒` - 等待锁的时间
- 📤 **Rows_sent**: `156行` - 返回给客户端的数据行数
- 🔍 **Rows_examined**: `250000行` - 扫描检查的数据行数

---

## 4. 🚨 告警策略与配置


### 4.1 告警阈值设置策略


**⭐ 多级告警机制**：

| 告警级别 | **执行时间阈值** | **触发条件** | **通知方式** | **处理时效** |
|---------|----------------|-------------|-------------|-------------|
| 🟢 **信息** | `2-5秒` | 连续出现5次 | 日志记录 | 24小时内 |
| 🟡 **警告** | `5-10秒` | 连续出现3次 | 邮件通知 | 4小时内 |
| 🟠 **严重** | `10-30秒` | 连续出现2次 | 邮件+短信 | 1小时内 |
| 🔴 **紧急** | `>30秒` | 立即触发 | 电话+钉钉 | 15分钟内 |

**智能阈值设置**：

```python
# 基于历史数据的动态阈值计算
def calculate_dynamic_threshold(historical_data):
    """
    根据历史数据计算动态告警阈值
    """
    avg_time = np.mean(historical_data)
    std_time = np.std(historical_data)
    
    # 使用3σ原则设置阈值
    warning_threshold = avg_time + 2 * std_time
    critical_threshold = avg_time + 3 * std_time
    
    return {
        'warning': warning_threshold,
        'critical': critical_threshold,
        'baseline': avg_time
    }
```

### 4.2 告警降噪策略


**🔇 防止告警风暴**：

```python
class AlertManager:
    def __init__(self):
        self.alert_history = {}
        self.silence_period = 300  # 5分钟静默期
    
    def should_send_alert(self, sql_fingerprint, severity):
        """
        判断是否应该发送告警（防止重复告警）
        """
        now = time.time()
        key = f"{sql_fingerprint}_{severity}"
        
        # 检查静默期
        if key in self.alert_history:
            last_alert_time = self.alert_history[key]
            if now - last_alert_time < self.silence_period:
                return False
        
        # 记录本次告警时间
        self.alert_history[key] = now
        return True
```

**告警聚合规则**：
- 📊 **相同SQL模板**：5分钟内只发送一次告警
- 📈 **批量告警**：10个以上慢查询合并为一条告警
- ⏰ **时间窗口**：非工作时间降低告警频率

### 4.3 邮件告警配置


**📧 邮件模板设计**：

```html
<!-- 告警邮件模板 -->
<!DOCTYPE html>
<html>
<head>
    <style>
        .alert-critical { background-color: #ff4444; color: white; }
        .alert-warning { background-color: #ffaa00; color: white; }
        .sql-block { background-color: #f5f5f5; padding: 10px; font-family: monospace; }
    </style>
</head>
<body>
    <div class="alert-critical">
        <h2>🚨 MySQL慢查询告警 - 紧急</h2>
    </div>
    
    <h3>告警详情</h3>
    <ul>
        <li><strong>数据库实例：</strong>prod-mysql-01</li>
        <li><strong>发生时间：</strong>2025-09-11 14:30:45</li>
        <li><strong>执行时间：</strong>35.67秒</li>
        <li><strong>影响行数：</strong>250,000行</li>
    </ul>
    
    <h3>SQL语句</h3>
    <div class="sql-block">
        SELECT * FROM orders o 
        JOIN users u ON o.user_id = u.id 
        WHERE o.create_time > ?
        ORDER BY o.create_time DESC
    </div>
    
    <h3>建议处理方案</h3>
    <ol>
        <li>检查是否缺少索引</li>
        <li>考虑添加时间范围限制</li>
        <li>优化JOIN条件</li>
    </ol>
</body>
</html>
```

---

## 5. 📊 可视化监控Dashboard


### 5.1 Grafana Dashboard设计


**🎯 核心监控面板**：

```json
{
  "dashboard": {
    "title": "MySQL慢查询监控大屏",
    "panels": [
      {
        "title": "慢查询数量趋势",
        "type": "graph",
        "targets": [
          {
            "query": "SELECT time, slow_query_count FROM mysql_monitor WHERE time >= now() - 1h"
          }
        ]
      },
      {
        "title": "TOP 10慢查询",
        "type": "table",
        "targets": [
          {
            "query": "SELECT sql_template, avg_exec_time, exec_count FROM slow_queries ORDER BY avg_exec_time DESC LIMIT 10"
          }
        ]
      },
      {
        "title": "数据库负载指标",
        "type": "singlestat",
        "targets": [
          {
            "query": "SELECT avg(cpu_usage), avg(io_usage) FROM system_metrics"
          }
        ]
      }
    ]
  }
}
```

**面板配置说明**：

| 面板类型 | **用途** | **更新频率** | **数据源** |
|---------|---------|-------------|-----------|
| **折线图** | 趋势分析 | 10秒 | 时序数据 |
| **表格** | 详细列表 | 30秒 | SQL统计 |
| **仪表盘** | 实时状态 | 5秒 | 系统指标 |
| **热力图** | 分布展示 | 1分钟 | 执行时间分布 |

### 5.2 自定义监控指标


**📈 关键业务指标**：

```sql
-- 创建自定义监控视图
CREATE VIEW slow_query_metrics AS
SELECT 
    DATE_FORMAT(ts, '%Y-%m-%d %H:%i:00') as time_bucket,
    COUNT(*) as slow_query_count,
    AVG(query_time) as avg_query_time,
    MAX(query_time) as max_query_time,
    SUM(rows_examined) as total_rows_examined,
    COUNT(DISTINCT sql_template) as unique_sql_count
FROM slow_query_log 
WHERE ts >= NOW() - INTERVAL 1 HOUR
GROUP BY time_bucket
ORDER BY time_bucket;
```

**实时指标计算**：
- 🎯 **QPS压力**：每秒慢查询数量
- 📊 **响应时间分布**：P50、P95、P99耗时
- 🔍 **扫描效率**：扫描行数与返回行数比例
- 📈 **趋势对比**：同比、环比变化趋势

---

## 6. 🛠️ 监控脚本开发实战


### 6.1 Python监控脚本


**🐍 完整监控脚本示例**：

```python
#!/usr/bin/env python3
"""
MySQL慢查询监控脚本
功能：实时监控慢查询并发送告警
"""

import pymysql
import time
import json
import smtplib
from email.mime.text import MimeText
from datetime import datetime
import logging

class MySQLSlowQueryMonitor:
    def __init__(self, config_file='monitor_config.json'):
        """初始化监控器"""
        with open(config_file, 'r') as f:
            self.config = json.load(f)
        
        self.setup_logging()
        self.db_connection = None
        self.last_check_time = datetime.now()
    
    def setup_logging(self):
        """设置日志记录"""
        logging.basicConfig(
            level=logging.INFO,
            format='%(asctime)s - %(levelname)s - %(message)s',
            handlers=[
                logging.FileHandler('slow_query_monitor.log'),
                logging.StreamHandler()
            ]
        )
        self.logger = logging.getLogger(__name__)
    
    def connect_database(self):
        """连接数据库"""
        try:
            self.db_connection = pymysql.connect(
                host=self.config['database']['host'],
                port=self.config['database']['port'],
                user=self.config['database']['user'],
                password=self.config['database']['password'],
                database='performance_schema',
                charset='utf8mb4'
            )
            self.logger.info("数据库连接成功")
        except Exception as e:
            self.logger.error(f"数据库连接失败: {e}")
            raise
    
    def check_slow_queries(self):
        """检查慢查询"""
        query = """
        SELECT 
            SCHEMA_NAME,
            DIGEST_TEXT,
            COUNT_STAR,
            AVG_TIMER_WAIT/1000000000 as avg_time_seconds,
            MAX_TIMER_WAIT/1000000000 as max_time_seconds,
            SUM_ROWS_EXAMINED,
            SUM_ROWS_SENT,
            LAST_SEEN
        FROM performance_schema.events_statements_summary_by_digest 
        WHERE AVG_TIMER_WAIT > %s
          AND LAST_SEEN >= %s
        ORDER BY AVG_TIMER_WAIT DESC
        """
        
        threshold_ns = self.config['thresholds']['warning_seconds'] * 1000000000
        
        try:
            with self.db_connection.cursor() as cursor:
                cursor.execute(query, (threshold_ns, self.last_check_time))
                results = cursor.fetchall()
                
                slow_queries = []
                for row in results:
                    slow_query = {
                        'schema': row[0],
                        'sql_template': row[1][:200],  # 截取前200字符
                        'exec_count': row[2],
                        'avg_time': round(row[3], 3),
                        'max_time': round(row[4], 3),
                        'rows_examined': row[5],
                        'rows_sent': row[6],
                        'last_seen': row[7]
                    }
                    slow_queries.append(slow_query)
                
                return slow_queries
        except Exception as e:
            self.logger.error(f"查询慢查询失败: {e}")
            return []
    
    def send_alert(self, slow_queries):
        """发送告警"""
        if not slow_queries:
            return
        
        # 构建告警内容
        alert_content = self.build_alert_message(slow_queries)
        
        # 发送邮件
        if self.config['alerts']['email']['enabled']:
            self.send_email_alert(alert_content)
        
        # 记录日志
        self.logger.warning(f"发现 {len(slow_queries)} 个慢查询，已发送告警")
    
    def build_alert_message(self, slow_queries):
        """构建告警消息"""
        message = f"""
🚨 MySQL慢查询告警通知

检测时间: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}
慢查询数量: {len(slow_queries)}

详细信息:
"""
        for i, query in enumerate(slow_queries[:5], 1):  # 只显示前5个
            message += f"""
{i}. 数据库: {query['schema']}
   平均耗时: {query['avg_time']}秒
   最大耗时: {query['max_time']}秒
   执行次数: {query['exec_count']}
   扫描行数: {query['rows_examined']:,}
   SQL模板: {query['sql_template']}...
   
"""
        
        return message
    
    def send_email_alert(self, content):
        """发送邮件告警"""
        try:
            msg = MimeText(content, 'plain', 'utf-8')
            msg['Subject'] = '🚨 MySQL慢查询告警'
            msg['From'] = self.config['alerts']['email']['from']
            msg['To'] = ', '.join(self.config['alerts']['email']['to'])
            
            server = smtplib.SMTP(
                self.config['alerts']['email']['smtp_host'],
                self.config['alerts']['email']['smtp_port']
            )
            server.starttls()
            server.login(
                self.config['alerts']['email']['username'],
                self.config['alerts']['email']['password']
            )
            server.send_message(msg)
            server.quit()
            
            self.logger.info("邮件告警发送成功")
        except Exception as e:
            self.logger.error(f"邮件发送失败: {e}")
    
    def run_monitor(self):
        """运行监控主循环"""
        self.logger.info("启动MySQL慢查询监控")
        
        while True:
            try:
                if not self.db_connection:
                    self.connect_database()
                
                # 检查慢查询
                slow_queries = self.check_slow_queries()
                
                # 发送告警
                if slow_queries:
                    self.send_alert(slow_queries)
                
                # 更新检查时间
                self.last_check_time = datetime.now()
                
                # 等待下次检查
                time.sleep(self.config['monitor']['check_interval'])
                
            except KeyboardInterrupt:
                self.logger.info("监控停止")
                break
            except Exception as e:
                self.logger.error(f"监控过程中出现错误: {e}")
                time.sleep(10)  # 出错后等待10秒再重试

if __name__ == '__main__':
    monitor = MySQLSlowQueryMonitor()
    monitor.run_monitor()
```

### 6.2 配置文件示例


**📝 monitor_config.json**：

```json
{
  "database": {
    "host": "localhost",
    "port": 3306,
    "user": "monitor_user",
    "password": "monitor_password"
  },
  "thresholds": {
    "warning_seconds": 2.0,
    "critical_seconds": 5.0,
    "emergency_seconds": 10.0
  },
  "monitor": {
    "check_interval": 60,
    "max_queries_per_alert": 5
  },
  "alerts": {
    "email": {
      "enabled": true,
      "smtp_host": "smtp.company.com",
      "smtp_port": 587,
      "username": "monitor@company.com",
      "password": "email_password",
      "from": "monitor@company.com",
      "to": ["dba@company.com", "dev@company.com"]
    }
  }
}
```

---

## 7. 🔗 监控系统集成方案


### 7.1 Zabbix集成配置


**🔧 Zabbix监控项配置**：

```bash
# 创建Zabbix自定义脚本
# /etc/zabbix/scripts/mysql_slow_query_check.sh

#!/bin/bash
MYSQL_HOST="localhost"
MYSQL_USER="zabbix"
MYSQL_PASS="password"

# 查询当前慢查询数量
SLOW_QUERIES=$(mysql -h$MYSQL_HOST -u$MYSQL_USER -p$MYSQL_PASS -e "
SHOW GLOBAL STATUS LIKE 'Slow_queries';" | awk 'NR==2{print $2}')

echo $SLOW_QUERIES
```

**Zabbix模板配置**：

```xml
<!-- Zabbix模板片段 -->
<item>
    <name>MySQL慢查询数量</name>
    <key>mysql.slow_queries</key>
    <type>EXTERNAL</type>
    <delay>60s</delay>
    <command>/etc/zabbix/scripts/mysql_slow_query_check.sh</command>
    <triggers>
        <trigger>
            <name>慢查询数量异常增长</name>
            <expression>{mysql.slow_queries.delta(5m)} > 100</expression>
            <priority>WARNING</priority>
        </trigger>
    </triggers>
</item>
```

### 7.2 Prometheus + Grafana集成


**📊 Prometheus配置**：

```yaml
# prometheus.yml 配置片段
scrape_configs:
  - job_name: 'mysql-exporter'
    static_configs:
      - targets: ['localhost:9104']
    scrape_interval: 15s
    metrics_path: /metrics
```

**MySQL Exporter配置**：

```bash
# 启动mysql_exporter
./mysqld_exporter \
  --config.my-cnf=/etc/mysql_exporter/.my.cnf \
  --collect.perf_schema.eventsstatements \
  --collect.perf_schema.indexiowaits \
  --web.listen-address=0.0.0.0:9104
```

### 7.3 钉钉/企业微信集成


**📱 钉钉机器人告警**：

```python
def send_dingtalk_alert(webhook_url, message):
    """发送钉钉告警消息"""
    headers = {'Content-Type': 'application/json'}
    data = {
        "msgtype": "markdown",
        "markdown": {
            "title": "MySQL慢查询告警",
            "text": f"""
## 🚨 MySQL慢查询告警


**告警时间：** {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}

**告警内容：**
{message}

**处理建议：**
1. 立即检查相关SQL语句
2. 分析执行计划
3. 考虑添加索引或优化查询
"""
        }
    }
    
    response = requests.post(webhook_url, headers=headers, json=data)
    return response.status_code == 200
```

---

## 8. ⚖️ 性能影响评估与优化


### 8.1 监控系统性能影响


**📊 性能开销分析**：

| 监控方式 | **CPU开销** | **内存开销** | **磁盘IO** | **网络开销** |
|---------|------------|-------------|-----------|-------------|
| **慢查询日志** | `< 1%` | `10-50MB` | `中等` | `无` |
| **Performance Schema** | `1-3%` | `100-500MB` | `低` | `无` |
| **实时进程监控** | `2-5%` | `< 10MB` | `极低` | `低` |
| **第三方工具** | `5-10%` | `50-200MB` | `中等` | `中等` |

**🎯 优化建议**：

```sql
-- 优化Performance Schema配置
-- 只启用必要的instruments
UPDATE performance_schema.setup_instruments 
SET ENABLED = 'NO' 
WHERE NAME NOT LIKE '%statement%';

-- 限制历史记录数量
UPDATE performance_schema.setup_consumers 
SET ENABLED = 'NO' 
WHERE NAME LIKE '%history_long%';

-- 设置合理的表大小
SET GLOBAL performance_schema_events_statements_history_long_size = 1000;
```

### 8.2 监控数据存储优化


**🗄️ 存储策略**：

```sql
-- 创建监控数据表（按天分区）
CREATE TABLE slow_query_archive (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    query_date DATE NOT NULL,
    query_time DECIMAL(10,6) NOT NULL,
    sql_template TEXT NOT NULL,
    exec_count INT NOT NULL,
    avg_time DECIMAL(10,6) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_date_time (query_date, avg_time)
) PARTITION BY RANGE (TO_DAYS(query_date)) (
    PARTITION p_2025_09 VALUES LESS THAN (TO_DAYS('2025-10-01')),
    PARTITION p_2025_10 VALUES LESS THAN (TO_DAYS('2025-11-01')),
    PARTITION p_future VALUES LESS THAN MAXVALUE
);

-- 定期清理历史数据
EVENT slow_query_cleanup
ON SCHEDULE EVERY 1 DAY
DO
  DELETE FROM slow_query_archive 
  WHERE query_date < DATE_SUB(CURDATE(), INTERVAL 30 DAY);
```

### 8.3 告警频率控制


**🔇 智能降噪算法**：

```python
class IntelligentAlerting:
    def __init__(self):
        self.baseline_metrics = {}
        self.alert_frequency = {}
    
    def calculate_anomaly_score(self, current_metrics):
        """计算异常评分"""
        score = 0
        
        for metric, value in current_metrics.items():
            if metric in self.baseline_metrics:
                baseline = self.baseline_metrics[metric]
                # 计算偏离程度
                deviation = abs(value - baseline['mean']) / baseline['std']
                score += deviation
        
        return score / len(current_metrics)
    
    def should_alert(self, anomaly_score, sql_fingerprint):
        """基于异常评分决定是否告警"""
        # 动态阈值：基于历史告警频率调整
        base_threshold = 2.0
        
        if sql_fingerprint in self.alert_frequency:
            # 频繁告警的SQL提高阈值
            freq_factor = min(self.alert_frequency[sql_fingerprint] / 10, 2.0)
            threshold = base_threshold * (1 + freq_factor)
        else:
            threshold = base_threshold
        
        return anomaly_score > threshold
```

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的核心概念


```
🔸 监控目标：实时发现和处理慢查询问题，提升数据库性能
🔸 数据来源：慢查询日志、Performance Schema、进程列表
🔸 关键指标：执行时间、锁等待、扫描行数、执行频率
🔸 告警策略：多级阈值、智能降噪、及时通知
🔸 可视化：Grafana Dashboard、实时监控大屏
```

### 9.2 监控实施要点


**🔹 监控部署原则**：
```
渐进式部署：
1. 先启用基础慢查询日志
2. 再配置Performance Schema
3. 最后集成可视化和告警

分层监控：
• 基础层：MySQL内置监控功能
• 采集层：数据收集和预处理
• 分析层：指标计算和异常检测
• 展示层：可视化和告警通知
```

**🔹 告警配置技巧**：
```
阈值设置：
• 基于业务特点设置不同的阈值
• 使用历史数据计算动态阈值
• 区分不同时间段的告警策略

降噪策略：
• 相同SQL的告警合并
• 设置告警静默期
• 基于异常程度决定告警级别
```

### 9.3 实际应用价值


**💼 业务价值体现**：
- **🎯 预防性维护**：在问题影响用户前主动发现
- **📊 数据驱动决策**：基于真实监控数据优化数据库
- **⚡ 快速响应**：缩短问题发现和解决时间
- **📈 性能提升**：持续优化数据库整体性能

**🛠️ 运维实践**：
- **监控部署**：选择合适的监控方案和工具
- **阈值调优**：根据业务特点调整告警阈值
- **流程规范**：建立慢查询处理的标准流程
- **团队协作**：DBA、开发、运维的协同工作机制

**核心记忆要点**：
- 慢查询监控是数据库性能管理的重要手段
- 多数据源结合使用能获得更全面的监控视角
- 智能告警策略比简单阈值告警更有效
- 监控系统本身的性能影响需要重点关注
- 可视化展示有助于快速发现和分析问题