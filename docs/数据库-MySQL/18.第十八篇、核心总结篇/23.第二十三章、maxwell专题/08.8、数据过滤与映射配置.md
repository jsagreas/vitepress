---
title: 8、数据过滤与映射配置
---
## 📚 目录

1. [数据过滤基础概念](#1-数据过滤基础概念)
2. [数据库级别过滤配置](#2-数据库级别过滤配置)
3. [表级别过滤配置](#3-表级别过滤配置)
4. [列级别过滤配置](#4-列级别过滤配置)
5. [正则表达式过滤](#5-正则表达式过滤)
6. [映射规则配置](#6-映射规则配置)
7. [实际应用案例](#7-实际应用案例)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 数据过滤基础概念


### 1.1 什么是Maxwell数据过滤

**通俗理解**：就像过筛子一样，只让我们需要的数据通过

```
想象一个场景：
你的数据库有100个表，但你只关心其中5个表的变化
没有过滤 → Maxwell会把所有100个表的变化都发送出去
有了过滤 → Maxwell只发送你关心的5个表的变化

好处：
✅ 减少网络传输量
✅ 降低下游系统压力  
✅ 提高数据处理效率
✅ 节省存储空间
```

### 1.2 过滤的三个层级

```
数据过滤层级：

📁 数据库级别（Database Level）
   ├── 🗃️ 表级别（Table Level）  
   │   └── 📄 列级别（Column Level）
   
举例说明：
数据库：电商系统有 user_db、order_db、log_db
表：order_db 里有 orders、order_items、payments
列：orders 表里有 id、user_id、amount、created_time、internal_notes

我们可以：
- 只同步 order_db 数据库（排除 log_db）
- 只同步 orders 和 order_items 表（排除 payments）  
- 只同步 id、user_id、amount 列（排除 internal_notes）
```

### 1.3 Include vs Exclude 策略

**核心概念**：白名单 vs 黑名单思维

```
Include（包含）- 白名单策略：
🔸 明确指定要同步什么
🔸 适合场景：只需要少数几个库/表/列
🔸 安全性高：新增的库/表默认不会被同步

Exclude（排除）- 黑名单策略：  
🔸 明确指定不要同步什么
🔸 适合场景：大部分数据都需要，只排除少数
🔸 需谨慎：新增的库/表默认会被同步

实际建议：
优先使用 Include 策略，更加安全可控
```

---

## 2. 🗄️ 数据库级别过滤配置


### 2.1 include_dbs 数据库白名单

**作用**：只同步指定的数据库

```properties
# 基础配置示例
include_dbs=order_db,user_db,product_db

# 实际效果
同步：order_db, user_db, product_db 的所有变化
忽略：log_db, temp_db, system_db 等其他数据库
```

**配置详解**：
```properties
# 单个数据库
include_dbs=order_db

# 多个数据库（用逗号分隔）
include_dbs=order_db,user_db,product_db

# 注意事项
# ❌ 错误：include_dbs = order_db, user_db  (有空格)
# ✅ 正确：include_dbs=order_db,user_db    (无空格)
```

### 2.2 exclude_dbs 数据库黑名单

**作用**：排除指定的数据库，同步其他所有数据库

```properties
# 排除系统库和日志库
exclude_dbs=information_schema,performance_schema,mysql,log_db

# 实际场景示例
假设数据库有：order_db, user_db, product_db, log_db, temp_db
配置：exclude_dbs=log_db,temp_db
结果：同步 order_db, user_db, product_db
```

### 2.3 数据库过滤实战案例

```properties
# 案例1：电商系统只同步核心业务库
include_dbs=order_db,user_db,product_db,payment_db

# 案例2：排除系统库和测试库  
exclude_dbs=mysql,information_schema,performance_schema,test_db,temp_db

# 案例3：开发环境只同步单个库
include_dbs=dev_order_db
```

**选择建议**：
```
使用 include_dbs 的场景：
✅ 微服务架构，每个服务只关心特定几个库
✅ 数据安全要求高，严格控制同步范围
✅ 测试环境，只需要部分数据

使用 exclude_dbs 的场景：  
✅ 传统单体应用，大部分库都需要同步
✅ 只是想排除系统库和日志库
✅ 快速配置，不想逐一列举要同步的库
```

---

## 3. 📋 表级别过滤配置


### 3.1 include_tables 表白名单

**作用**：只同步指定的表

```properties
# 语法格式：数据库.表名
include_tables=order_db.orders,order_db.order_items,user_db.users

# 通配符支持
include_tables=order_db.*,user_db.user_*
```

**配置示例详解**：
```properties
# 精确匹配多个表
include_tables=order_db.orders,order_db.order_items,user_db.users,user_db.user_profiles

# 同步某个库的所有表
include_tables=order_db.*

# 同步多个库的所有表  
include_tables=order_db.*,user_db.*

# 同步特定前缀的表
include_tables=order_db.order_*,user_db.user_*
```

### 3.2 exclude_tables 表黑名单

**作用**：排除指定的表

```properties
# 排除日志表和临时表
exclude_tables=order_db.order_logs,order_db.temp_orders,user_db.user_logs

# 排除所有日志相关表
exclude_tables=*.log_*,*.*_logs,*.*_temp
```

### 3.3 表过滤进阶技巧

```properties
# 技巧1：结合数据库和表过滤
include_dbs=order_db,user_db
include_tables=order_db.orders,order_db.order_items,user_db.users
# 效果：只同步指定库的指定表

# 技巧2：排除系统表
exclude_tables=*.*_log,*.*_temp,*.*_backup,*.*_history

# 技巧3：只同步主要业务表
include_tables=order_db.orders,order_db.order_items,user_db.users,product_db.products
```

**实际应用场景**：
```
电商系统表过滤示例：

核心业务表（需要同步）：
✅ orders          - 订单主表
✅ order_items     - 订单明细  
✅ users           - 用户信息
✅ products        - 商品信息

辅助表（可以排除）：
❌ order_logs      - 订单操作日志
❌ user_sessions   - 用户会话信息  
❌ temp_calculations - 临时计算表
❌ cache_data      - 缓存数据表

配置：
include_tables=order_db.orders,order_db.order_items,user_db.users,product_db.products
```

---

## 4. 📄 列级别过滤配置


### 4.1 include_columns 列白名单

**作用**：只同步指定的列

```properties
# 语法：数据库.表名.列名
include_columns=order_db.orders.id,order_db.orders.user_id,order_db.orders.amount,order_db.orders.status
```

**实际配置案例**：
```properties
# 用户表只同步基本信息，不同步敏感信息
include_columns=user_db.users.id,user_db.users.username,user_db.users.email,user_db.users.created_time

# 订单表只同步业务字段
include_columns=order_db.orders.id,order_db.orders.user_id,order_db.orders.amount,order_db.orders.status,order_db.orders.created_time
```

### 4.2 exclude_columns 列黑名单

**作用**：排除指定的列

```properties
# 排除敏感信息列
exclude_columns=user_db.users.password,user_db.users.phone,user_db.users.id_card

# 排除内部字段
exclude_columns=*.internal_*,*.debug_*,*.*_temp
```

### 4.3 列过滤最佳实践

```properties
# 场景1：用户表脱敏
exclude_columns=user_db.users.password,user_db.users.salt,user_db.users.phone,user_db.users.id_card,user_db.users.bank_account

# 场景2：只同步业务关键字段  
include_columns=order_db.orders.id,order_db.orders.user_id,order_db.orders.amount,order_db.orders.status,order_db.orders.created_time,order_db.orders.updated_time

# 场景3：排除大字段
exclude_columns=product_db.products.description,product_db.products.detail_images,order_db.orders.remark
```

**列过滤注意事项**：
```
⚠️ 重要提醒：

1. 主键字段建议保留
   - 下游系统通常需要主键来标识记录
   
2. 时间字段建议保留  
   - created_time, updated_time 对数据分析很重要
   
3. 状态字段建议保留
   - status, state 等字段对业务逻辑很关键
   
4. 外键字段建议保留
   - user_id, order_id 等关联字段对数据完整性重要
```

---

## 5. 🔍 正则表达式过滤


### 5.1 正则表达式基础语法

**Maxwell支持的正则模式**：

```
基本通配符：
*  → 匹配任意字符
?  → 匹配单个字符

常用正则表达式：
^  → 行开始
$  → 行结束  
.  → 任意单个字符
+  → 一个或多个
*  → 零个或多个
```

### 5.2 数据库正则过滤

```properties
# 匹配所有以 order 开头的数据库
include_dbs=order.*

# 匹配开发和测试环境数据库
include_dbs=.*_(dev|test)$

# 排除所有临时库
exclude_dbs=.*_temp$,.*_backup$
```

### 5.3 表正则过滤实例

```properties
# 只同步主要业务表（以 main_ 开头）
include_tables=.*\.main_.*

# 排除所有日志和历史表
exclude_tables=.*\.(.*_log|.*_history|.*_backup|.*_temp)$

# 只同步用户相关表
include_tables=user_db\.(user.*|profile.*|account.*)
```

### 5.4 列正则过滤实例

```properties
# 排除所有内部字段
exclude_columns=.*\.internal_.*,.*\._.*,.*\.debug_.*

# 只同步ID和时间相关字段
include_columns=.*\.(.*id|.*_time|created_at|updated_at)$

# 排除敏感信息字段
exclude_columns=.*\.(password|secret|token|salt|phone|email)$
```

### 5.5 正则表达式实战案例

```properties
# 复杂场景：微服务架构
# 只同步生产环境的核心服务数据库
include_dbs=prod_(order|user|product|payment)_service$

# 排除所有监控、日志、缓存相关表
exclude_tables=.*\.(monitor_.*|log_.*|cache_.*|temp_.*|backup_.*)$

# 只同步业务核心字段，排除调试和内部字段
exclude_columns=.*\.(debug_.*|internal_.*|_temp|_cache|password|secret)$
```

---

## 6. 🔄 映射规则配置


### 6.1 什么是数据映射

**通俗解释**：就像翻译一样，把数据从一种格式转换成另一种格式

```
为什么需要映射？

原始数据格式：
{
  "database": "order_db",
  "table": "orders", 
  "data": {"id": 1, "amount": 100.50}
}

下游系统需要的格式：
{
  "source": "订单系统",
  "table_name": "订单表",
  "order_id": 1,
  "order_amount": 100.50,
  "currency": "CNY"
}

映射规则就是定义这种转换关系
```

### 6.2 字段名映射

```json
# mapping.json 配置文件
{
  "order_db.orders": {
    "id": "order_id",
    "amount": "order_amount", 
    "user_id": "customer_id",
    "created_time": "order_time"
  },
  "user_db.users": {
    "id": "user_id",
    "username": "user_name",
    "created_time": "register_time"
  }
}
```

### 6.3 数据类型映射

```json
{
  "type_mappings": {
    "DECIMAL": "DOUBLE",
    "VARCHAR": "STRING", 
    "DATETIME": "TIMESTAMP",
    "TINYINT": "BOOLEAN"
  }
}
```

### 6.4 值转换映射

```json
{
  "value_mappings": {
    "order_db.orders.status": {
      "0": "pending",
      "1": "paid", 
      "2": "shipped",
      "3": "completed",
      "4": "cancelled"
    },
    "user_db.users.gender": {
      "M": "male",
      "F": "female", 
      "U": "unknown"
    }
  }
}
```

### 6.5 高级映射规则

```json
{
  "advanced_mappings": {
    "order_db.orders": {
      "computed_fields": {
        "order_year": "YEAR(created_time)",
        "amount_usd": "amount * exchange_rate",
        "is_large_order": "amount > 1000"
      },
      "default_values": {
        "currency": "CNY",
        "source_system": "order_service"
      }
    }
  }
}
```

---

## 7. 🛠️ 实际应用案例


### 7.1 电商系统完整配置

```properties
# Maxwell配置文件：config.properties

# 数据库连接
host=192.168.1.100
port=3306
user=maxwell
password=maxwell123

# 只同步核心业务库
include_dbs=order_db,user_db,product_db,payment_db

# 排除系统库
exclude_dbs=information_schema,performance_schema,mysql

# 只同步主要业务表
include_tables=order_db.orders,order_db.order_items,user_db.users,user_db.user_profiles,product_db.products,payment_db.payments

# 排除敏感字段
exclude_columns=user_db.users.password,user_db.users.salt,user_db.users.phone,user_db.users.id_card,payment_db.payments.card_number

# 输出配置
producer=kafka
kafka.bootstrap.servers=localhost:9092
kafka_topic=maxwell
```

### 7.2 数据脱敏场景配置

```properties
# 场景：同步数据到数据仓库，需要脱敏

# 用户表脱敏
exclude_columns=user_db.users.password,user_db.users.phone,user_db.users.email,user_db.users.id_card,user_db.users.real_name

# 订单表保留业务字段
include_columns=order_db.orders.id,order_db.orders.user_id,order_db.orders.amount,order_db.orders.status,order_db.orders.created_time,order_db.orders.updated_time

# 支付表排除敏感信息
exclude_columns=payment_db.payments.card_number,payment_db.payments.cvv,payment_db.payments.bank_account
```

### 7.3 微服务拆分场景

```properties
# 场景：从单体应用拆分微服务，只同步相关数据

# 订单服务只需要订单相关数据
include_tables=order_db.orders,order_db.order_items,order_db.order_status_history

# 用户服务只需要用户相关数据  
include_tables=user_db.users,user_db.user_profiles,user_db.user_addresses

# 商品服务只需要商品相关数据
include_tables=product_db.products,product_db.categories,product_db.product_images
```

### 7.4 开发测试环境配置

```properties
# 开发环境：只同步核心表的核心字段

# 表过滤
include_tables=order_db.orders,user_db.users,product_db.products

# 字段过滤（只保留必要字段）
include_columns=order_db.orders.id,order_db.orders.user_id,order_db.orders.amount,order_db.orders.status,user_db.users.id,user_db.users.username,product_db.products.id,product_db.products.name,product_db.products.price

# 排除大字段（节省带宽）
exclude_columns=product_db.products.description,product_db.products.images,order_db.orders.remark
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念

```
🔸 过滤层级：数据库 → 表 → 列，三个层级可以组合使用
🔸 策略选择：Include（白名单）vs Exclude（黑名单）
🔸 配置语法：数据库.表名.列名 的层级语法
🔸 正则支持：支持通配符和正则表达式匹配
🔸 映射转换：字段名、数据类型、值的映射转换
```

### 8.2 关键配置参数

| 参数 | **作用** | **示例** | **使用场景** |
|------|---------|---------|-------------|
| `include_dbs` | 数据库白名单 | `order_db,user_db` | 只同步特定业务库 |
| `exclude_dbs` | 数据库黑名单 | `log_db,temp_db` | 排除系统库/日志库 |
| `include_tables` | 表白名单 | `order_db.orders` | 只同步核心业务表 |
| `exclude_tables` | 表黑名单 | `*.*_log,*.*_temp` | 排除日志临时表 |
| `include_columns` | 列白名单 | `users.id,users.name` | 只同步必要字段 |
| `exclude_columns` | 列黑名单 | `users.password` | 排除敏感信息 |

### 8.3 配置最佳实践

```
✅ 推荐做法：

1. 优先使用 include 策略（白名单）
   - 更安全，新增数据不会意外同步
   
2. 分层配置，逐步细化
   - 先配置数据库级别
   - 再配置表级别  
   - 最后配置列级别
   
3. 敏感信息必须排除
   - 密码、手机号、身份证等
   - 内部调试字段、临时字段
   
4. 保留关键业务字段
   - 主键、外键、时间戳
   - 状态字段、金额字段

❌ 避免的错误：

1. 配置语法错误
   - 多余空格：include_dbs = order_db, user_db ❌
   - 正确格式：include_dbs=order_db,user_db ✅
   
2. 过度配置
   - 不要配置太复杂的正则表达式
   - 优先使用简单明确的配置
   
3. 遗漏关键字段
   - 主键字段被排除会导致下游问题
   - 时间戳字段对数据分析很重要
```

### 8.4 故障排查要点

```
常见问题及解决方案：

🔍 数据没有同步：
- 检查 include_dbs 是否包含对应数据库
- 检查 include_tables 是否包含对应表
- 查看 Maxwell 日志是否有错误信息

🔍 同步了不该同步的数据：
- 检查是否使用了 exclude 配置
- 检查正则表达式是否正确
- 优先改用 include 白名单策略

🔍 字段缺失：
- 检查 exclude_columns 是否误排除了必要字段
- 检查 include_columns 是否遗漏了字段
- 确认字段名大小写是否正确

🔍 性能问题：
- 过滤配置不当导致同步数据量过大
- 增加更精确的过滤规则
- 排除不必要的大字段
```

**记忆口诀**：
```
数据过滤三层级，库表列来逐步细
白名单策略更安全，黑名单使用需谨慎  
敏感信息必排除，核心字段要保留
配置语法要规范，正则表达式助力强
```