---
title: 11、MySQL权限表基础结构
---
## 📚 目录

1. [MySQL权限系统概述](#1-MySQL权限系统概述)
2. [权限表层次结构](#2-权限表层次结构)
3. [mysql.user表详解](#3-mysql-user表详解)
4. [mysql.db数据库权限表](#4-mysql-db数据库权限表)
5. [mysql.tables_priv表权限管理](#5-mysql-tables_priv表权限管理)
6. [mysql.columns_priv列权限控制](#6-mysql-columns_priv列权限控制)
7. [权限查询与管理方法](#7-权限查询与管理方法)
8. [权限系统工作机制](#8-权限系统工作机制)
9. [实际应用与最佳实践](#9-实际应用与最佳实践)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🔐 MySQL权限系统概述


### 1.1 什么是MySQL权限系统

**权限系统**就像是数据库的"门卫系统"，决定谁能进来、能做什么操作。

```
现实类比：
权限系统 = 公司的门禁卡系统
├── 身份验证：验证你是谁（用户名+密码）
├── 权限检查：检查你能访问哪些区域
└── 操作控制：控制你在每个区域能做什么

MySQL权限系统：
├── 用户身份：存储在mysql.user表
├── 数据库权限：存储在mysql.db表  
├── 表权限：存储在mysql.tables_priv表
└── 列权限：存储在mysql.columns_priv表
```

### 1.2 权限系统基础架构

**🔸 权限控制的两个阶段**
```
第一阶段：连接验证
├── 检查用户是否存在
├── 验证密码是否正确
├── 检查连接来源IP是否允许
└── 决定是否允许连接

第二阶段：操作权限检查  
├── 检查用户对特定数据库的权限
├── 检查用户对特定表的权限
├── 检查用户对特定列的权限
└── 决定是否允许执行操作
```

### 1.3 权限系统存储位置

**🔸 系统数据库mysql**
```
MySQL的权限信息存储在系统数据库mysql中：

mysql数据库
├── user              # 用户账户和全局权限
├── db                # 数据库级别权限
├── tables_priv       # 表级别权限
├── columns_priv      # 列级别权限
├── procs_priv        # 存储过程权限
└── proxies_priv      # 代理用户权限
```

---

## 2. 🏗️ 权限表层次结构


### 2.1 权限层次架构图

```
权限层次结构（从大到小）：

全局权限
    ↓
数据库权限  
    ↓
表权限
    ↓  
列权限

检查顺序：
用户连接 → 全局权限检查 → 数据库权限检查 → 表权限检查 → 列权限检查
```

### 2.2 权限表关系图

```
权限表层次关系：

mysql.user（用户表）
├── 存储用户基本信息
├── 存储全局权限（适用于所有数据库）
└── 一对多关系指向其他权限表

    ↓ 细化到数据库级别
    
mysql.db（数据库权限表）
├── 存储用户对特定数据库的权限
└── 一对多关系指向表权限

    ↓ 细化到表级别
    
mysql.tables_priv（表权限表）
├── 存储用户对特定表的权限
└── 一对多关系指向列权限

    ↓ 细化到列级别
    
mysql.columns_priv（列权限表）
└── 存储用户对特定列的权限
```

### 2.3 权限检查原则

**🔸 权限继承与覆盖**
```
权限检查逻辑：

如果全局权限允许 → 直接允许操作
    ↓
如果数据库权限允许 → 直接允许操作  
    ↓
如果表权限允许 → 直接允许操作
    ↓
如果列权限允许 → 直接允许操作
    ↓
否则拒绝操作

简单理解：
只要任何一个层级的权限允许，就可以执行操作
权限是"或"的关系，不是"且"的关系
```

---

## 3. 👤 mysql.user表详解


### 3.1 user表的核心作用

**user表**是MySQL权限系统的**核心基础表**，存储所有用户账户信息和全局权限。

```
user表的作用：
1. 身份验证：用户名、密码、连接来源
2. 全局权限：影响所有数据库的权限
3. 用户属性：账户状态、资源限制等
```

### 3.2 user表主要字段结构

**🔸 身份标识字段**
```sql
-- 查看user表的身份字段
SELECT Host, User, authentication_string 
FROM mysql.user;

字段说明：
Host                  # 允许连接的主机（IP/主机名/%）
User                  # 用户名
authentication_string # 加密后的密码
```

**🔸 全局权限字段**
```sql
-- user表中的主要权限字段
SELECT User, Host, 
       Select_priv,     # 全局SELECT权限
       Insert_priv,     # 全局INSERT权限  
       Update_priv,     # 全局UPDATE权限
       Delete_priv,     # 全局DELETE权限
       Create_priv,     # 全局CREATE权限
       Drop_priv,       # 全局DROP权限
       Super_priv       # 超级用户权限
FROM mysql.user;
```

### 3.3 user表字段分类

| 字段类型 | 主要字段 | 作用说明 |
|---------|----------|----------|
| **身份字段** | `Host, User, authentication_string` | 用户身份验证 |
| **数据权限** | `Select_priv, Insert_priv, Update_priv, Delete_priv` | 基本数据操作 |
| **结构权限** | `Create_priv, Drop_priv, Alter_priv, Index_priv` | 数据库结构操作 |
| **管理权限** | `Super_priv, Process_priv, Reload_priv` | 服务器管理 |
| **文件权限** | `File_priv` | 文件导入导出 |
| **复制权限** | `Repl_slave_priv, Repl_client_priv` | 主从复制 |

### 3.4 user表实际查看示例

```sql
-- 查看所有用户及其主机信息
SELECT User, Host FROM mysql.user;

-- 典型输出：
+------------------+-----------+
| User             | Host      |
+------------------+-----------+
| root             | localhost |
| root             | %         |
| app_user         | 10.0.%.%  |
| backup_user      | localhost |
+------------------+-----------+

解释：
root@localhost  # root用户只能从本机连接
root@%          # root用户可以从任何主机连接
app_user@10.0.%.% # app_user只能从10.0网段连接
```

---

## 4. 🗃️ mysql.db数据库权限表


### 4.1 db表的作用

**db表**专门存储用户对**特定数据库**的权限，比全局权限更细化。

```
数据库权限的特点：
- 只影响指定的数据库
- 比全局权限更精确
- 不影响其他数据库的访问
```

### 4.2 db表基本结构

**🔸 主要字段组成**
```sql
-- 查看db表结构
DESC mysql.db;

核心字段：
Host            # 主机名
Db              # 数据库名  
User            # 用户名
Select_priv     # 对该数据库的SELECT权限
Insert_priv     # 对该数据库的INSERT权限
Update_priv     # 对该数据库的UPDATE权限
Delete_priv     # 对该数据库的DELETE权限
Create_priv     # 对该数据库的CREATE权限
Drop_priv       # 对该数据库的DROP权限
```

### 4.3 db表权限查看

```sql
-- 查看数据库权限分配情况
SELECT Host, Db, User, 
       Select_priv, Insert_priv, Update_priv, Delete_priv,
       Create_priv, Drop_priv
FROM mysql.db;

-- 示例输出：
+------+----------+----------+-------------+-------------+
| Host | Db       | User     | Select_priv | Insert_priv |
+------+----------+----------+-------------+-------------+
| %    | test_db  | app_user | Y           | Y           |
| %    | prod_db  | app_user | Y           | N           |
+------+----------+----------+-------------+-------------+

解释：
app_user@% 对 test_db 有SELECT和INSERT权限
app_user@% 对 prod_db 只有SELECT权限（生产库只读）
```

### 4.4 实际应用场景

**🔸 典型的数据库权限分配**
```
生产环境常见权限分配：

开发人员：
├── dev_database：完全权限（增删改查+结构修改）
├── test_database：完全权限  
└── prod_database：只读权限

应用程序：
├── app_database：数据操作权限（增删改查）
└── 禁止结构修改权限（DROP、ALTER等）

备份账户：
├── 所有数据库：SELECT权限
└── mysql数据库：特殊备份权限
```

---

## 5. 🗂️ mysql.tables_priv表权限管理


### 5.1 tables_priv表的作用

**tables_priv表**存储用户对**特定表**的权限，比数据库权限更加精细。

```
表权限的应用场景：
- 敏感表只给特定用户访问
- 日志表只给特定程序写入
- 配置表只给管理员修改
```

### 5.2 tables_priv表结构

**🔸 核心字段解释**
```sql
-- 查看tables_priv表结构
DESC mysql.tables_priv;

重要字段：
Host            # 主机名
Db              # 数据库名
User            # 用户名
Table_name      # 表名
Grantor         # 授权者（谁给的权限）
Timestamp       # 权限授予时间
Table_priv      # 表级权限（Select,Insert,Update,Delete等）
Column_priv     # 列级权限（哪些列可以访问）
```

### 5.3 Table_priv字段详解

**🔸 表权限类型**
```
Table_priv字段存储的权限类型：

数据操作权限：
├── Select    # 查询表数据
├── Insert    # 插入新数据
├── Update    # 修改现有数据  
└── Delete    # 删除数据

表结构权限：
├── Create    # 创建表
├── Drop      # 删除表
├── Alter     # 修改表结构
└── Index     # 创建/删除索引

其他权限：
├── References # 创建外键引用
└── Trigger    # 创建触发器
```

### 5.4 表权限查看示例

```sql
-- 查看特定表的权限分配
SELECT Host, Db, User, Table_name, Table_priv
FROM mysql.tables_priv 
WHERE Db = 'ecommerce' AND Table_name = 'orders';

-- 示例结果：
+------+-----------+-------------+------------+------------------+
| Host | Db        | User        | Table_name | Table_priv       |
+------+-----------+-------------+------------+------------------+
| %    | ecommerce | order_app   | orders     | Select,Insert    |
| %    | ecommerce | report_user | orders     | Select           |
| %    | ecommerce | admin_user  | orders     | Select,Insert,Update,Delete |
+------+-----------+-------------+------------+------------------+

权限解读：
order_app：只能查询和插入订单（业务应用）
report_user：只能查询订单（报表系统）
admin_user：完全的数据操作权限（管理员）
```

---

## 6. 📊 mysql.columns_priv列权限控制


### 6.1 列权限的作用

**列权限**是MySQL中**最精细**的权限控制，可以控制用户对表中**特定列**的访问。

```
列权限的典型应用：
员工表（employees）
├── id, name, department → 所有人可见
├── salary → 只有HR可见  
├── performance_score → 只有主管可见
└── ssn → 只有系统管理员可见
```

### 6.2 columns_priv表结构

**🔸 核心字段说明**
```sql
-- 查看columns_priv表结构
DESC mysql.columns_priv;

关键字段：
Host           # 主机名
Db             # 数据库名
User           # 用户名  
Table_name     # 表名
Column_name    # 列名（重点：具体到某一列）
Timestamp      # 授权时间
Column_priv    # 列权限（Select,Insert,Update,References）
```

### 6.3 列权限类型

**🔸 Column_priv字段的权限类型**
```
列级权限只有4种：

Select      # 可以查询该列的数据
├── SELECT name FROM employees  # 允许
└── SELECT * FROM employees     # 如果没有其他列权限则报错

Insert      # 可以在该列插入数据
├── INSERT INTO employees(name) VALUES('张三')  # 允许
└── INSERT INTO employees(name,salary) VALUES('张三',5000)  # salary列无权限则报错

Update      # 可以修改该列的数据
├── UPDATE employees SET name='李四' WHERE id=1  # 允许
└── UPDATE employees SET salary=6000 WHERE id=1  # salary列无权限则报错

References  # 可以在该列上创建外键引用
└── 相对较少使用
```

### 6.4 列权限实际应用示例

```sql
-- 给HR用户授予员工表的部分列权限
GRANT SELECT(id, name, department, salary) 
ON company.employees 
TO 'hr_user'@'%';

-- 给普通员工只能查看基本信息
GRANT SELECT(id, name, department) 
ON company.employees 
TO 'employee_user'@'%';

-- 查看列权限分配结果
SELECT Host, Db, User, Table_name, Column_name, Column_priv
FROM mysql.columns_priv 
WHERE Db = 'company' AND Table_name = 'employees';
```

---

## 7. 🔍 权限查询与管理方法


### 7.1 查看用户权限的方法

**🔸 SHOW GRANTS查看权限**
```sql
-- 查看当前用户的权限
SHOW GRANTS;

-- 查看指定用户的权限
SHOW GRANTS FOR 'app_user'@'%';

-- 示例输出：
+--------------------------------------------------+
| Grants for app_user@%                           |
+--------------------------------------------------+
| GRANT USAGE ON *.* TO `app_user`@`%`            |
| GRANT SELECT ON `test_db`.* TO `app_user`@`%`   |
| GRANT SELECT, INSERT ON `prod_db`.`orders` TO `app_user`@`%` |
+--------------------------------------------------+

解读：
第1行：基本连接权限（USAGE表示可以连接）
第2行：对test_db数据库的SELECT权限
第3行：对prod_db.orders表的SELECT和INSERT权限
```

### 7.2 直接查询权限表

**🔸 查询user表权限**
```sql
-- 查看用户的全局权限
SELECT Host, User, 
       Select_priv, Insert_priv, Update_priv, Delete_priv,
       Create_priv, Drop_priv, Super_priv
FROM mysql.user 
WHERE User = 'app_user';
```

**🔸 查询db表权限**
```sql
-- 查看用户的数据库权限
SELECT Host, Db, User,
       Select_priv, Insert_priv, Update_priv, Delete_priv
FROM mysql.db 
WHERE User = 'app_user';
```

### 7.3 权限查询实用SQL

**🔸 综合权限查询**
```sql
-- 查看某个用户的所有权限情况
-- 1. 全局权限
SELECT 'GLOBAL' as Level, '*' as Object_name, 
       Group_concat(
         CASE WHEN Select_priv='Y' THEN 'SELECT' END,
         CASE WHEN Insert_priv='Y' THEN 'INSERT' END,
         CASE WHEN Update_priv='Y' THEN 'UPDATE' END,
         CASE WHEN Delete_priv='Y' THEN 'DELETE' END
       ) as Privileges
FROM mysql.user 
WHERE User = 'app_user' AND Host = '%'

UNION ALL

-- 2. 数据库权限
SELECT 'DATABASE' as Level, Db as Object_name,
       Group_concat(
         CASE WHEN Select_priv='Y' THEN 'SELECT' END,
         CASE WHEN Insert_priv='Y' THEN 'INSERT' END,
         CASE WHEN Update_priv='Y' THEN 'UPDATE' END,
         CASE WHEN Delete_priv='Y' THEN 'DELETE' END
       ) as Privileges  
FROM mysql.db
WHERE User = 'app_user' AND Host = '%';
```

---

## 8. 🔄 权限系统工作机制


### 8.1 用户连接验证流程

```
用户连接MySQL时的验证过程：

步骤1：连接阶段验证
├── 检查mysql.user表
├── 验证 User@Host 组合是否存在
├── 验证密码是否正确
└── 决定是否允许连接

步骤2：权限加载
├── 将该用户的所有权限加载到内存
├── 包括全局权限、数据库权限、表权限、列权限
└── 形成该连接的权限缓存
```

### 8.2 操作权限检查流程

```
用户执行SQL时的权限检查：

执行 SELECT * FROM db1.table1
    ↓
1. 检查全局SELECT权限（mysql.user.Select_priv）
   如果有 → 允许操作
    ↓
2. 检查数据库SELECT权限（mysql.db）
   如果有 → 允许操作  
    ↓
3. 检查表SELECT权限（mysql.tables_priv）
   如果有 → 允许操作
    ↓  
4. 检查列SELECT权限（mysql.columns_priv）
   如果所有列都有权限 → 允许操作
    ↓
5. 拒绝操作
```

### 8.3 权限缓存机制

**🔸 权限信息缓存**
```
MySQL权限缓存机制：

连接建立时：
├── 从权限表读取用户权限
├── 加载到内存缓存中
└── 后续权限检查直接使用缓存

权限表修改后：
├── 内存缓存不会自动更新
├── 需要执行 FLUSH PRIVILEGES
└── 或者等待用户重新连接

为什么要缓存：
├── 避免每次操作都查询权限表
├── 提高权限检查性能
└── 减少系统表的访问压力
```

### 8.4 权限刷新命令

```sql
-- 重新加载权限表到内存
FLUSH PRIVILEGES;

-- 什么时候需要执行：
-- 1. 直接修改权限表后（不推荐）
-- 2. 导入权限数据后
-- 3. 权限表损坏修复后

-- 注意：使用GRANT/REVOKE命令不需要手动FLUSH
```

---

## 9. 🛠️ 实际应用与最佳实践


### 9.1 权限设计原则

**🔸 最小权限原则**
```
权限分配策略：

应用程序账户：
├── 只给必需的数据操作权限
├── 不给结构修改权限（DROP、ALTER）
├── 不给全局权限
└── 限制连接来源IP

管理员账户：
├── 开发环境：可以给较高权限
├── 生产环境：严格限制，分权管理
└── 关键操作需要审计

备份账户：
├── 全局SELECT权限或BACKUP_ADMIN权限
├── 限制只能从备份服务器连接
└── 定期更换密码
```

### 9.2 常用权限管理命令

**🔸 创建用户并授权**
```sql
-- 创建应用程序用户
CREATE USER 'app_user'@'10.0.%.%' IDENTIFIED BY 'strong_password';

-- 授予数据库权限
GRANT SELECT, INSERT, UPDATE, DELETE 
ON ecommerce.* 
TO 'app_user'@'10.0.%.%';

-- 授予特定表权限
GRANT SELECT, INSERT 
ON ecommerce.orders 
TO 'order_service'@'%';

-- 授予列权限
GRANT SELECT(id, name, email), UPDATE(email) 
ON users.user_info 
TO 'profile_service'@'%';
```

**🔸 权限回收**
```sql
-- 回收数据库权限
REVOKE INSERT, UPDATE, DELETE 
ON ecommerce.* 
FROM 'app_user'@'10.0.%.%';

-- 回收表权限
REVOKE ALL PRIVILEGES 
ON ecommerce.orders 
FROM 'order_service'@'%';
```

### 9.3 权限表维护

**🔸 权限表备份**
```bash
# 备份权限相关的系统表
mysqldump -u root -p mysql user db tables_priv columns_priv > mysql_privileges.sql

# 权限表恢复
mysql -u root -p mysql < mysql_privileges.sql
# 恢复后必须执行
mysql -u root -p -e "FLUSH PRIVILEGES;"
```

### 9.4 权限安全检查

**🔸 安全审计查询**
```sql
-- 检查有全局权限的用户（安全风险）
SELECT Host, User, 
       CASE WHEN Select_priv='Y' THEN 'SELECT ' ELSE '' END,
       CASE WHEN Insert_priv='Y' THEN 'INSERT ' ELSE '' END,
       CASE WHEN Super_priv='Y' THEN 'SUPER ' ELSE '' END
FROM mysql.user 
WHERE Select_priv='Y' OR Insert_priv='Y' OR Super_priv='Y';

-- 检查空密码用户（严重安全风险）
SELECT Host, User 
FROM mysql.user 
WHERE authentication_string = '';

-- 检查从任意主机连接的用户
SELECT Host, User
FROM mysql.user  
WHERE Host = '%';
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的基本概念

```
🔸 权限表层次：user(全局) → db(数据库) → tables_priv(表) → columns_priv(列)
🔸 权限检查原则：任何一个层级允许就可以执行操作
🔸 权限表位置：所有权限信息存储在mysql系统数据库中
🔸 权限缓存机制：连接时加载权限到内存，提高检查效率
🔸 权限作用域：全局权限影响所有数据库，其他权限有特定范围
```

### 10.2 关键理解要点


**🔹 权限表的层次关系**
```
理解要点：
- user表是基础，存储用户身份和全局权限
- db表是细化，针对特定数据库的权限
- tables_priv表更精细，针对特定表的权限
- columns_priv表最精细，针对特定列的权限

实际意义：
- 层次越低，控制越精细
- 可以实现非常灵活的权限控制
- 不同层次的权限可以组合使用
```

**🔹 权限字段的Y/N含义**
```
权限表中的字段值：
Y = Yes，表示有该权限
N = No，表示无该权限

示例：
Select_priv = 'Y'  # 有SELECT权限
Insert_priv = 'N'  # 无INSERT权限

注意：
- 只有Y和N两种值
- 空值会被当作N处理
- 不区分大小写，但建议使用大写
```

**🔹 权限检查的"或"逻辑**
```
权限检查特点：
- 不是所有层级都要有权限
- 只要任何一个层级有权限就允许
- 从全局到列逐层检查，有权限就停止

实际案例：
用户A在user表没有SELECT权限
但在db表对database1有SELECT权限
→ 可以查询database1中的所有表

用户B在全局有SELECT权限
→ 可以查询任何数据库的任何表
```

### 10.3 实际应用指导


**🔸 权限设计建议**
```
生产环境权限设计：

应用程序用户：
✅ 使用db级别权限（对特定数据库）
✅ 限制连接来源IP
✅ 不给DROP、ALTER等结构权限
❌ 避免全局权限
❌ 避免SUPER权限

开发人员用户：
✅ 开发库给完整权限
✅ 测试库给完整权限  
✅ 生产库只给SELECT权限
❌ 生产库不给DELETE权限

管理员用户：
✅ 谨慎使用SUPER权限
✅ 重要操作要有审计记录
✅ 定期检查权限分配
```

**🔸 权限故障排查**
```
权限问题排查步骤：

1. 确认用户是否存在
SELECT User, Host FROM mysql.user WHERE User = '用户名';

2. 检查连接权限
SHOW GRANTS FOR '用户名'@'主机';

3. 逐层检查权限表
-- 检查全局权限
SELECT * FROM mysql.user WHERE User = '用户名';
-- 检查数据库权限  
SELECT * FROM mysql.db WHERE User = '用户名';
-- 检查表权限
SELECT * FROM mysql.tables_priv WHERE User = '用户名';

4. 确认权限是否生效
FLUSH PRIVILEGES;  -- 如果直接修改了权限表
```

**🔸 权限管理工具**
```sql
-- 查看所有用户列表
SELECT DISTINCT User, Host FROM mysql.user;

-- 查看用户数量统计
SELECT Host, COUNT(*) as user_count 
FROM mysql.user 
GROUP BY Host;

-- 查看权限分布情况
SELECT 
  SUM(CASE WHEN Super_priv='Y' THEN 1 ELSE 0 END) as super_users,
  SUM(CASE WHEN Create_priv='Y' THEN 1 ELSE 0 END) as create_users,
  COUNT(*) as total_users
FROM mysql.user;
```

### 10.4 安全最佳实践

```
权限安全要点：

账户管理：
✅ 删除不必要的默认账户
✅ 定期检查空密码账户
✅ 使用强密码策略
✅ 限制root用户远程连接

权限分配：
✅ 遵循最小权限原则
✅ 定期审计权限分配
✅ 敏感操作要有审批流程
✅ 权限变更要有记录

监控审计：
✅ 记录权限变更操作
✅ 监控权限异常使用
✅ 定期备份权限表
✅ 建立权限恢复机制
```

**核心记忆口诀**：
```
权限表有四层次，用户数据库表列
检查遵循或逻辑，有权即可执行行
用户表是总基础，全局权限最关键
细化控制用专表，安全管理要规范
```