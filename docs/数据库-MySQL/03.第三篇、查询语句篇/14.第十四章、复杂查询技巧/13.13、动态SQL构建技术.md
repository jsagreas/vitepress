---
title: 13、动态SQL构建技术
---
## 📚 目录

1. [动态SQL概述与应用场景](#1-动态SQL概述与应用场景)
2. [动态查询构建技术](#2-动态查询构建技术)
3. [条件动态组装策略](#3-条件动态组装策略)
4. [参数化动态SQL实现](#4-参数化动态SQL实现)
5. [动态查询安全性防护](#5-动态查询安全性防护)
6. [动态SQL性能优化](#6-动态SQL性能优化)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 动态SQL概述与应用场景


### 1.1 什么是动态SQL


**🔸 简单理解**
动态SQL就像一个**可变化的查询模板**。根据用户不同的查询条件，程序自动拼接生成不同的SQL语句。比如用户搜索商品时，可能按价格筛选，也可能按品牌筛选，还可能两个条件都用。

**🔸 静态SQL vs 动态SQL对比**
```
静态SQL（固定查询）：
SELECT * FROM products WHERE price > 100 AND brand = 'Apple';
特点：查询条件固定，无法灵活变化

动态SQL（灵活查询）：
根据用户输入动态生成：
• 只按价格：SELECT * FROM products WHERE price > ?
• 只按品牌：SELECT * FROM products WHERE brand = ?  
• 两个条件：SELECT * FROM products WHERE price > ? AND brand = ?
特点：根据实际需求灵活组装查询条件
```

### 1.2 动态SQL的核心应用场景


**📊 典型业务场景**
```
报表查询系统 ─────────┐
                    ├─── 用户选择不同维度和条件
                    └─── 系统动态生成查询SQL

搜索功能 ───────────┐  
                    ├─── 用户输入关键词和筛选条件
                    └─── 动态组装WHERE条件

数据导出功能 ────────┐
                    ├─── 用户选择导出字段和范围
                    └─── 动态构建SELECT和WHERE子句

权限控制查询 ────────┐
                    ├─── 根据用户角色动态添加权限条件
                    └─── 确保数据安全访问
```

### 1.3 动态SQL的优势与挑战


**✅ 主要优势**
- **灵活性强**：一套代码应对多种查询需求
- **用户体验好**：支持复杂的多条件组合查询
- **代码复用**：避免为每种查询组合写单独的SQL
- **维护性好**：查询逻辑集中管理

**⚠️ 面临挑战**
- **SQL注入风险**：不当拼接可能导致安全漏洞
- **性能问题**：动态生成的SQL可能无法充分利用索引
- **调试困难**：运行时才能看到最终的SQL语句
- **代码复杂**：条件组合逻辑容易变得复杂

---

## 2. 🔧 动态查询构建技术


### 2.1 基础动态查询构建


**🔸 字符串拼接方式**
```java
// Java中的基础动态SQL构建
public class DynamicQueryBuilder {
    
    public String buildProductQuery(String brand, Double minPrice, Double maxPrice, String category) {
        StringBuilder sql = new StringBuilder("SELECT * FROM products WHERE 1=1");
        
        // 动态添加品牌条件
        if (brand != null && !brand.isEmpty()) {
            sql.append(" AND brand = '").append(brand).append("'");
        }
        
        // 动态添加价格条件
        if (minPrice != null) {
            sql.append(" AND price >= ").append(minPrice);
        }
        if (maxPrice != null) {
            sql.append(" AND price <= ").append(maxPrice);
        }
        
        // 动态添加分类条件
        if (category != null && !category.isEmpty()) {
            sql.append(" AND category = '").append(category).append("'");
        }
        
        return sql.toString();
    }
}

// 使用示例
String query1 = buildProductQuery("Apple", 1000.0, null, "手机");
// 结果：SELECT * FROM products WHERE 1=1 AND brand = 'Apple' AND price >= 1000.0 AND category = '手机'

String query2 = buildProductQuery(null, null, 5000.0, null);  
// 结果：SELECT * FROM products WHERE 1=1 AND price <= 5000.0
```

> ⚠️ **安全警告**：上面的代码存在SQL注入风险，仅用于演示拼接逻辑，实际使用需要参数化处理

### 2.2 条件构建器模式


**🔸 面向对象的查询构建**
```java
// 查询条件构建器
public class QueryConditionBuilder {
    private List<String> conditions = new ArrayList<>();
    private List<Object> parameters = new ArrayList<>();
    
    // 添加等值条件
    public QueryConditionBuilder addEquals(String field, Object value) {
        if (value != null) {
            conditions.add(field + " = ?");
            parameters.add(value);
        }
        return this;
    }
    
    // 添加范围条件
    public QueryConditionBuilder addRange(String field, Object min, Object max) {
        if (min != null) {
            conditions.add(field + " >= ?");
            parameters.add(min);
        }
        if (max != null) {
            conditions.add(field + " <= ?");
            parameters.add(max);
        }
        return this;
    }
    
    // 添加IN条件
    public QueryConditionBuilder addIn(String field, List<?> values) {
        if (values != null && !values.isEmpty()) {
            String placeholders = String.join(",", Collections.nCopies(values.size(), "?"));
            conditions.add(field + " IN (" + placeholders + ")");
            parameters.addAll(values);
        }
        return this;
    }
    
    // 构建最终SQL
    public String buildQuery(String baseQuery) {
        if (conditions.isEmpty()) {
            return baseQuery;
        }
        return baseQuery + " WHERE " + String.join(" AND ", conditions);
    }
    
    public List<Object> getParameters() {
        return parameters;
    }
}

// 使用示例
QueryConditionBuilder builder = new QueryConditionBuilder()
    .addEquals("brand", "Apple")
    .addRange("price", 1000, 5000)
    .addIn("category", Arrays.asList("手机", "平板"));

String sql = builder.buildQuery("SELECT * FROM products");
List<Object> params = builder.getParameters();
```

### 2.3 模板化动态查询


**🔸 SQL模板方式**
```java
// 使用模板引擎构建动态SQL
public class TemplateQueryBuilder {
    
    // 查询模板
    private static final String PRODUCT_QUERY_TEMPLATE = """
        SELECT p.*, c.category_name
        FROM products p
        LEFT JOIN categories c ON p.category_id = c.id
        WHERE 1=1
        {{#if brand}}
        AND p.brand = #{brand}
        {{/if}}
        {{#if minPrice}}
        AND p.price >= #{minPrice}
        {{/if}}
        {{#if maxPrice}}
        AND p.price <= #{maxPrice}
        {{/if}}
        {{#if categories}}
        AND p.category_id IN ({{#each categories}}#{this}{{#unless @last}},{{/unless}}{{/each}})
        {{/if}}
        ORDER BY p.price DESC
        """;
    
    public String buildQuery(Map<String, Object> params) {
        // 使用模板引擎处理（如Handlebars、Freemarker等）
        return processTemplate(PRODUCT_QUERY_TEMPLATE, params);
    }
}
```

---

## 3. ⚙️ 条件动态组装策略


### 3.1 复杂条件组装


**🔸 多层级条件组装**
```java
// 复杂条件组装器
public class ComplexConditionBuilder {
    
    // AND条件组
    public static class AndConditionGroup {
        private List<String> conditions = new ArrayList<>();
        private List<Object> parameters = new ArrayList<>();
        
        public AndConditionGroup add(String condition, Object... params) {
            conditions.add(condition);
            parameters.addAll(Arrays.asList(params));
            return this;
        }
        
        public String build() {
            return conditions.isEmpty() ? "" : "(" + String.join(" AND ", conditions) + ")";
        }
    }
    
    // OR条件组
    public static class OrConditionGroup {
        private List<String> conditions = new ArrayList<>();
        private List<Object> parameters = new ArrayList<>();
        
        public OrConditionGroup add(String condition, Object... params) {
            conditions.add(condition);
            parameters.addAll(Arrays.asList(params));
            return this;
        }
        
        public String build() {
            return conditions.isEmpty() ? "" : "(" + String.join(" OR ", conditions) + ")";
        }
    }
    
    // 使用示例：构建复杂搜索条件
    public String buildComplexProductQuery(ProductSearchCriteria criteria) {
        List<String> mainConditions = new ArrayList<>();
        List<Object> allParams = new ArrayList<>();
        
        // 基础信息条件
        AndConditionGroup basicInfo = new AndConditionGroup();
        if (criteria.getBrand() != null) {
            basicInfo.add("brand = ?", criteria.getBrand());
        }
        if (criteria.getCategory() != null) {
            basicInfo.add("category = ?", criteria.getCategory());
        }
        
        // 价格范围条件
        AndConditionGroup priceRange = new AndConditionGroup();
        if (criteria.getMinPrice() != null) {
            priceRange.add("price >= ?", criteria.getMinPrice());
        }
        if (criteria.getMaxPrice() != null) {
            priceRange.add("price <= ?", criteria.getMaxPrice());
        }
        
        // 关键词搜索条件（支持多个关键词OR搜索）
        if (criteria.getKeywords() != null && !criteria.getKeywords().isEmpty()) {
            OrConditionGroup keywordSearch = new OrConditionGroup();
            for (String keyword : criteria.getKeywords()) {
                keywordSearch.add("name LIKE ?", "%" + keyword + "%");
                keywordSearch.add("description LIKE ?", "%" + keyword + "%");
            }
            mainConditions.add(keywordSearch.build());
        }
        
        // 组装最终条件
        if (!basicInfo.build().isEmpty()) {
            mainConditions.add(basicInfo.build());
        }
        if (!priceRange.build().isEmpty()) {
            mainConditions.add(priceRange.build());
        }
        
        String whereClause = mainConditions.isEmpty() ? 
            "" : " WHERE " + String.join(" AND ", mainConditions);
            
        return "SELECT * FROM products" + whereClause + " ORDER BY create_time DESC";
    }
}
```

### 3.2 条件优先级处理


**🔸 查询条件优先级策略**
```java
// 查询条件优先级管理
public class ConditionPriorityManager {
    
    public enum ConditionPriority {
        HIGH(1),      // 高优先级：精确匹配条件
        MEDIUM(2),    // 中优先级：范围条件
        LOW(3);       // 低优先级：模糊搜索条件
        
        private final int level;
        ConditionPriority(int level) { this.level = level; }
    }
    
    public String buildOptimizedQuery(Map<String, Object> searchParams) {
        StringBuilder sql = new StringBuilder("SELECT * FROM products WHERE 1=1");
        
        // 高优先级条件（精确匹配，利用索引）
        addHighPriorityConditions(sql, searchParams);
        
        // 中优先级条件（范围查询）  
        addMediumPriorityConditions(sql, searchParams);
        
        // 低优先级条件（全文搜索）
        addLowPriorityConditions(sql, searchParams);
        
        return sql.toString();
    }
    
    private void addHighPriorityConditions(StringBuilder sql, Map<String, Object> params) {
        // ID精确匹配
        if (params.get("productId") != null) {
            sql.append(" AND id = ").append(params.get("productId"));
        }
        // 状态精确匹配
        if (params.get("status") != null) {
            sql.append(" AND status = '").append(params.get("status")).append("'");
        }
    }
}
```

### 3.3 动态排序和分页


**🔸 动态ORDER BY构建**
```java
// 动态排序构建器
public class DynamicSortBuilder {
    
    // 支持的排序字段（白名单机制）
    private static final Set<String> ALLOWED_SORT_FIELDS = Set.of(
        "price", "create_time", "sales_count", "rating", "name"
    );
    
    public String buildSortClause(List<SortCriteria> sortList) {
        if (sortList == null || sortList.isEmpty()) {
            return " ORDER BY create_time DESC";  // 默认排序
        }
        
        List<String> sortClauses = new ArrayList<>();
        
        for (SortCriteria sort : sortList) {
            // 安全检查：只允许预定义的字段
            if (ALLOWED_SORT_FIELDS.contains(sort.getField())) {
                String direction = "ASC".equalsIgnoreCase(sort.getDirection()) ? "ASC" : "DESC";
                sortClauses.add(sort.getField() + " " + direction);
            }
        }
        
        return sortClauses.isEmpty() ? 
            " ORDER BY create_time DESC" : 
            " ORDER BY " + String.join(", ", sortClauses);
    }
    
    // 动态分页构建
    public String buildPaginationClause(Integer page, Integer pageSize) {
        if (page == null || pageSize == null) {
            return "";
        }
        
        // 参数验证
        page = Math.max(1, page);  // 页码最小为1
        pageSize = Math.min(Math.max(1, pageSize), 100);  // 页大小限制在1-100
        
        int offset = (page - 1) * pageSize;
        return " LIMIT " + pageSize + " OFFSET " + offset;
    }
}

// 使用示例
DynamicSortBuilder sortBuilder = new DynamicSortBuilder();

List<SortCriteria> sorts = Arrays.asList(
    new SortCriteria("price", "ASC"),
    new SortCriteria("rating", "DESC")
);

String sortClause = sortBuilder.buildSortClause(sorts);
// 结果：ORDER BY price ASC, rating DESC

String pageClause = sortBuilder.buildPaginationClause(2, 20);
// 结果：LIMIT 20 OFFSET 20
```

---

## 4. 🔒 参数化动态SQL实现


### 4.1 参数化查询的重要性


**🔸 为什么要参数化**
参数化就是把**用户输入的值**和**SQL结构**分开处理。这样既能实现动态查询，又能防止SQL注入攻击。

**🔸 参数化 vs 字符串拼接**
```
❌ 危险的字符串拼接：
String sql = "SELECT * FROM users WHERE name = '" + userName + "'";
// 如果userName = "'; DROP TABLE users; --"
// 最终SQL：SELECT * FROM users WHERE name = ''; DROP TABLE users; --'
// 后果：删除整个用户表！

✅ 安全的参数化查询：
String sql = "SELECT * FROM users WHERE name = ?";
PreparedStatement stmt = conn.prepareStatement(sql);
stmt.setString(1, userName);
// 无论userName输入什么，都会被当作字符串值处理，不会影响SQL结构
```

### 4.2 参数化动态SQL实现


**🔥 安全的动态查询构建**
```java
// 参数化动态SQL构建器
public class SafeDynamicQueryBuilder {
    
    public static class QueryResult {
        private String sql;
        private List<Object> parameters;
        
        public QueryResult(String sql, List<Object> parameters) {
            this.sql = sql;
            this.parameters = parameters;
        }
        
        // getter方法省略
    }
    
    public QueryResult buildSafeProductQuery(ProductSearchRequest request) {
        StringBuilder sql = new StringBuilder("SELECT * FROM products WHERE 1=1");
        List<Object> params = new ArrayList<>();
        
        // 品牌条件
        if (request.getBrand() != null && !request.getBrand().trim().isEmpty()) {
            sql.append(" AND brand = ?");
            params.add(request.getBrand().trim());
        }
        
        // 价格范围条件
        if (request.getMinPrice() != null) {
            sql.append(" AND price >= ?");
            params.add(request.getMinPrice());
        }
        if (request.getMaxPrice() != null) {
            sql.append(" AND price <= ?");
            params.add(request.getMaxPrice());
        }
        
        // 分类条件（支持多选）
        if (request.getCategories() != null && !request.getCategories().isEmpty()) {
            String placeholders = String.join(",", Collections.nCopies(request.getCategories().size(), "?"));
            sql.append(" AND category_id IN (").append(placeholders).append(")");
            params.addAll(request.getCategories());
        }
        
        // 关键词搜索
        if (request.getKeyword() != null && !request.getKeyword().trim().isEmpty()) {
            sql.append(" AND (name LIKE ? OR description LIKE ?)");
            String likePattern = "%" + request.getKeyword().trim() + "%";
            params.add(likePattern);
            params.add(likePattern);
        }
        
        // 添加排序
        sql.append(" ORDER BY create_time DESC");
        
        return new QueryResult(sql.toString(), params);
    }
}

// 执行查询的安全方式
public List<Product> searchProducts(ProductSearchRequest request) {
    QueryResult queryResult = new SafeDynamicQueryBuilder().buildSafeProductQuery(request);
    
    try (PreparedStatement stmt = connection.prepareStatement(queryResult.getSql())) {
        // 设置参数
        for (int i = 0; i < queryResult.getParameters().size(); i++) {
            stmt.setObject(i + 1, queryResult.getParameters().get(i));
        }
        
        ResultSet rs = stmt.executeQuery();
        return mapResultSetToProducts(rs);
    }
}
```

### 4.3 MyBatis动态SQL


**🔸 MyBatis的动态SQL标签**
```xml
<!-- MyBatis动态SQL示例 -->
<select id="searchProducts" resultType="Product">
    SELECT * FROM products
    <where>
        <if test="brand != null and brand != ''">
            AND brand = #{brand}
        </if>
        <if test="minPrice != null">
            AND price >= #{minPrice}
        </if>
        <if test="maxPrice != null">
            AND price <= #{maxPrice}
        </if>
        <if test="categories != null and categories.size() > 0">
            AND category_id IN
            <foreach collection="categories" item="categoryId" open="(" close=")" separator=",">
                #{categoryId}
            </foreach>
        </if>
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') 
                 OR description LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </where>
    <choose>
        <when test="sortField != null and sortField == 'price'">
            ORDER BY price ${sortDirection}
        </when>
        <when test="sortField != null and sortField == 'sales'">
            ORDER BY sales_count ${sortDirection}  
        </when>
        <otherwise>
            ORDER BY create_time DESC
        </otherwise>
    </choose>
</select>
```

**🔸 MyBatis优势**
- **自动参数化**：所有`#{}`参数自动防SQL注入
- **条件智能处理**：`<where>`标签自动处理AND/OR逻辑
- **代码简洁**：XML配置比Java代码更清晰

---

## 5. 🛡️ 动态查询安全性防护


### 5.1 SQL注入防护策略


**🔸 SQL注入的危害**
SQL注入就像给坏人提供了**直接操作数据库的钥匙**，可能导致数据泄露、数据被篡改，甚至整个数据库被删除。

**🔸 常见SQL注入攻击方式**
```
1. 恶意输入示例：
用户输入用户名：admin'; DROP TABLE users; --
拼接后的SQL：SELECT * FROM users WHERE username = 'admin'; DROP TABLE users; --'
后果：用户表被删除

2. 绕过认证示例：
用户输入密码：' OR '1'='1
拼接后的SQL：SELECT * FROM users WHERE username = 'admin' AND password = '' OR '1'='1'  
后果：绕过密码验证成功登录
```

**🔥 防护策略实现**
```java
// SQL注入防护最佳实践
public class SqlInjectionProtection {
    
    // 1. 参数化查询（最重要）
    public List<User> safeUserSearch(String username, String email) {
        String sql = "SELECT * FROM users WHERE username = ? AND email = ?";
        
        try (PreparedStatement stmt = connection.prepareStatement(sql)) {
            stmt.setString(1, username);  // 自动转义特殊字符
            stmt.setString(2, email);
            return executeQuery(stmt);
        }
    }
    
    // 2. 输入验证和清理
    public String sanitizeInput(String input) {
        if (input == null) return null;
        
        // 移除危险字符
        return input
            .replaceAll("['\";\\\\]", "")  // 移除引号和反斜杠
            .replaceAll("(?i)(DROP|DELETE|UPDATE|INSERT|ALTER|CREATE)", "")  // 移除SQL关键词
            .trim();
    }
    
    // 3. 字段名白名单验证
    private static final Set<String> ALLOWED_SORT_FIELDS = Set.of(
        "id", "name", "price", "create_time", "update_time"
    );
    
    public String validateSortField(String sortField) {
        if (sortField == null || !ALLOWED_SORT_FIELDS.contains(sortField.toLowerCase())) {
            return "create_time";  // 默认排序字段
        }
        return sortField;
    }
    
    // 4. 动态表名安全处理
    public String buildTableQuery(String tableName, Map<String, Object> conditions) {
        // 表名白名单验证
        Set<String> allowedTables = Set.of("products", "orders", "customers", "categories");
        if (!allowedTables.contains(tableName.toLowerCase())) {
            throw new SecurityException("不允许访问的表：" + tableName);
        }
        
        // 构建安全的查询
        return "SELECT * FROM " + tableName + " WHERE id = ?";
    }
}
```

### 5.2 权限控制集成


**🔸 基于角色的动态权限过滤**
```java
// 权限集成的动态查询
public class PermissionAwareDynamicQuery {
    
    public QueryResult buildUserAwareQuery(String baseTable, Map<String, Object> conditions, User currentUser) {
        StringBuilder sql = new StringBuilder("SELECT * FROM " + baseTable + " WHERE 1=1");
        List<Object> params = new ArrayList<>();
        
        // 根据用户角色添加权限过滤条件
        switch (currentUser.getRole()) {
            case "ADMIN":
                // 管理员可以查看所有数据，不添加额外条件
                break;
                
            case "MANAGER":
                // 经理只能查看自己部门的数据
                sql.append(" AND department_id = ?");
                params.add(currentUser.getDepartmentId());
                break;
                
            case "USER":
                // 普通用户只能查看自己的数据
                sql.append(" AND created_by = ? OR assigned_to = ?");
                params.add(currentUser.getId());
                params.add(currentUser.getId());
                break;
                
            default:
                // 未知角色拒绝访问
                throw new SecurityException("无效的用户角色");
        }
        
        // 添加业务查询条件
        addBusinessConditions(sql, params, conditions);
        
        return new QueryResult(sql.toString(), params);
    }
    
    private void addBusinessConditions(StringBuilder sql, List<Object> params, Map<String, Object> conditions) {
        // 安全地添加业务查询条件
        for (Map.Entry<String, Object> entry : conditions.entrySet()) {
            String field = entry.getKey();
            Object value = entry.getValue();
            
            // 字段名验证
            if (isValidField(field) && value != null) {
                sql.append(" AND ").append(field).append(" = ?");
                params.add(value);
            }
        }
    }
    
    private boolean isValidField(String field) {
        // 实现字段名白名单验证逻辑
        return field.matches("^[a-zA-Z_][a-zA-Z0-9_]*$") && 
               !field.toLowerCase().contains("password");
    }
}
```

---

## 6. ⚡ 动态SQL性能优化


### 6.1 动态SQL性能问题分析


**🔸 性能问题的根源**
动态SQL的性能问题主要来自于：**执行计划不稳定**、**索引利用率低**、**缓存命中率差**。

**🔸 常见性能问题**
```
问题1：条件顺序影响索引使用
❌ 低效：WHERE category = ? AND price > ? AND brand = ?
✅ 高效：WHERE brand = ? AND category = ? AND price > ?
原因：brand字段索引选择性更高

问题2：可选条件导致执行计划不稳定  
动态SQL：WHERE (brand = ? OR ? IS NULL) AND (category = ? OR ? IS NULL)
问题：数据库难以选择最优执行计划

问题3：动态IN条件长度变化
WHERE category_id IN (?) -- 有时1个参数，有时10个参数
问题：不同参数数量导致不同的执行计划
```

### 6.2 索引优化策略


**🔥 动态查询索引设计**
```sql
-- 针对动态查询的索引设计策略

-- 1. 复合索引设计（按选择性排序）
CREATE INDEX idx_products_search ON products (
    brand,           -- 高选择性字段在前
    category_id,     -- 中选择性字段
    status,          -- 低选择性字段在后
    price           -- 范围查询字段最后
);

-- 2. 覆盖索引设计（避免回表）
CREATE INDEX idx_products_list ON products (
    status, category_id, brand,    -- 查询条件字段
    price, name, create_time      -- 返回结果字段
);

-- 3. 部分索引（针对特定条件）
CREATE INDEX idx_products_active ON products (brand, price) 
WHERE status = 'ACTIVE';
```

**🔸 索引使用优化技巧**
```java
// 索引友好的动态查询构建
public class IndexOptimizedQueryBuilder {
    
    public QueryResult buildIndexFriendlyQuery(ProductSearchRequest request) {
        StringBuilder sql = new StringBuilder("SELECT * FROM products WHERE 1=1");
        List<Object> params = new ArrayList<>();
        
        // 按索引字段顺序添加条件（提高索引利用率）
        
        // 1. 先添加精确匹配条件（索引效果最好）
        if (request.getStatus() != null) {
            sql.append(" AND status = ?");
            params.add(request.getStatus());
        }
        
        if (request.getBrand() != null) {
            sql.append(" AND brand = ?");
            params.add(request.getBrand());
        }
        
        // 2. 再添加范围条件（索引效果较好）
        if (request.getMinPrice() != null) {
            sql.append(" AND price >= ?");
            params.add(request.getMinPrice());
        }
        
        // 3. 最后添加模糊搜索条件（索引效果最差）
        if (request.getKeyword() != null && !request.getKeyword().trim().isEmpty()) {
            sql.append(" AND name LIKE ?");
            params.add("%" + request.getKeyword().trim() + "%");
        }
        
        return new QueryResult(sql.toString(), params);
    }
}
```

### 6.3 查询缓存优化


**🔸 动态SQL缓存策略**
```java
// 动态SQL结果缓存
public class DynamicQueryCache {
    
    private final Cache<String, List<Object>> queryResultCache;
    
    public DynamicQueryCache() {
        this.queryResultCache = CacheBuilder.newBuilder()
            .maximumSize(1000)                    // 最大缓存1000个查询
            .expireAfterWrite(5, TimeUnit.MINUTES) // 5分钟过期
            .build();
    }
    
    public List<Product> executeWithCache(QueryResult queryResult) {
        // 生成缓存键（SQL + 参数）
        String cacheKey = generateCacheKey(queryResult.getSql(), queryResult.getParameters());
        
        // 尝试从缓存获取
        List<Object> cachedResult = queryResultCache.getIfPresent(cacheKey);
        if (cachedResult != null) {
            return (List<Product>) cachedResult;
        }
        
        // 执行查询
        List<Product> result = executeQuery(queryResult);
        
        // 缓存结果
        queryResultCache.put(cacheKey, (List<Object>) result);
        
        return result;
    }
    
    private String generateCacheKey(String sql, List<Object> params) {
        return Hashing.sha256()
            .hashString(sql + ":" + params.toString(), StandardCharsets.UTF_8)
            .toString();
    }
}
```

### 6.4 查询性能监控


**🔸 动态SQL性能监控**
```java
// 动态SQL性能监控
public class DynamicQueryMonitor {
    
    private final Map<String, QueryStats> queryStats = new ConcurrentHashMap<>();
    
    public static class QueryStats {
        private int executionCount;
        private long totalExecutionTime;
        private long maxExecutionTime;
        private String slowestQueryExample;
        
        // 统计方法省略
    }
    
    public <T> T executeWithMonitoring(String sqlTemplate, List<Object> params, 
                                      Supplier<T> queryExecutor) {
        long startTime = System.currentTimeMillis();
        String queryKey = extractQueryPattern(sqlTemplate);
        
        try {
            T result = queryExecutor.get();
            
            // 记录执行统计
            long executionTime = System.currentTimeMillis() - startTime;
            updateQueryStats(queryKey, executionTime, sqlTemplate);
            
            // 慢查询告警
            if (executionTime > 1000) {  // 超过1秒
                logSlowQuery(sqlTemplate, params, executionTime);
            }
            
            return result;
            
        } catch (Exception e) {
            logQueryError(sqlTemplate, params, e);
            throw e;
        }
    }
    
    // 提取查询模式（去除具体参数值）
    private String extractQueryPattern(String sql) {
        return sql.replaceAll("'[^']*'", "'?'")
                  .replaceAll("\\d+", "?")
                  .replaceAll("\\s+", " ")
                  .trim();
    }
    
    // 获取性能统计报告
    public Map<String, QueryStats> getPerformanceReport() {
        return new HashMap<>(queryStats);
    }
}
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 动态SQL本质：根据运行时条件灵活构建查询语句
🔸 构建策略：字符串拼接、条件构建器、模板引擎三种主要方式
🔸 参数化原则：所有用户输入必须参数化处理，避免SQL注入
🔸 安全防护：输入验证、白名单控制、权限集成三道防线
🔸 性能优化：索引设计、查询缓存、执行监控三个层面
```

### 7.2 关键技术应用要点


**🔹 动态查询设计原则**
```
安全第一：
• 所有用户输入必须参数化
• 字段名和表名使用白名单验证
• 集成权限控制机制

性能考虑：
• 条件顺序按索引设计排列
• 避免过于复杂的动态条件
• 合理使用查询缓存

可维护性：
• 使用构建器模式组织代码
• 条件逻辑模块化设计
• 添加完善的日志和监控
```

**🔹 技术选型指导**
```
简单场景：
✅ 直接使用PreparedStatement参数化
✅ 适合条件较少、逻辑简单的查询

复杂场景：
✅ 使用MyBatis动态SQL标签
✅ 适合条件复杂、业务逻辑多变的查询

高性能场景：
✅ 自定义构建器 + 查询缓存
✅ 适合高并发、性能要求严格的场景
```

### 7.3 实际应用指导


**🎯 常见应用场景**
- **搜索功能**：电商网站的商品搜索、内容管理系统的文章搜索
- **报表系统**：根据用户选择的维度和条件动态生成报表
- **数据导出**：用户自定义导出字段和筛选条件
- **管理后台**：不同角色用户看到不同权限范围的数据

**🔧 实施建议**
- **从简单开始**：先实现基础的动态查询，再逐步优化
- **安全优先**：优先考虑安全性，再考虑性能和功能
- **测试充分**：动态SQL要测试各种条件组合情况
- **监控完善**：建立性能监控和异常告警机制

**⚠️ 注意事项**
- **避免过度动态化**：不是所有查询都需要动态，简单查询用静态SQL
- **控制复杂度**：动态条件过多会增加维护成本
- **性能测试**：动态SQL要在真实数据量下测试性能
- **安全审计**：定期审计动态SQL的安全性

**核心记忆要点**：
- 动态SQL是灵活查询的利器，但安全性和性能需要特别关注
- 参数化查询是防止SQL注入的基础，字段白名单是深度防护
- 索引设计要考虑动态条件的组合，查询缓存能显著提升性能
- 技术选型要平衡功能需求、性能要求和维护成本