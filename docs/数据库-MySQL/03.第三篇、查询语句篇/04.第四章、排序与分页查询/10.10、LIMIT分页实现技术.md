---
title: 10、LIMIT分页实现技术
---
## 📚 目录

1. [分页查询基础概念](#1-分页查询基础概念)
2. [LIMIT基础语法详解](#2-limit基础语法详解)
3. [OFFSET偏移量分页机制](#3-offset偏移量分页机制)
4. [分页参数计算与处理](#4-分页参数计算与处理)
5. [分页性能优化策略](#5-分页性能优化策略)
6. [分页边界与一致性](#6-分页边界与一致性)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 📋 分页查询基础概念


### 1.1 什么是分页查询


**🔸 分页的本质**
```
分页查询：将大量数据分成若干页显示，每次只显示一页数据
目的：提升用户体验，减少数据传输，降低系统负载
原理：通过LIMIT和OFFSET控制返回的数据范围
```

**💡 分页的实际意义**
```
用户体验角度：
• 避免页面加载时间过长
• 提供清晰的数据浏览方式
• 支持快速跳转到指定页面

系统性能角度：
• 减少单次查询的数据量
• 降低网络传输压力
• 减少客户端内存占用
• 提高数据库响应速度
```

### 1.2 分页查询的应用场景


**🎯 典型应用场景**
```
数据展示类：
├── 商品列表：电商网站的商品浏览
├── 文章列表：博客、新闻网站的内容展示
├── 用户列表：管理后台的用户管理
└── 订单记录：历史订单的查看

搜索结果类：
├── 搜索引擎：百度、Google的搜索结果
├── 站内搜索：网站内部的内容搜索
├── 数据筛选：复杂条件下的数据过滤
└── 报表查询：大数据量的报表分页显示
```

### 1.3 分页查询的基本要素


**📊 分页参数说明**
| 参数名称 | **含义** | **常用名称** | **示例值** |
|---------|---------|-------------|-----------|
| **页码** | `当前显示第几页` | `page, pageNum, current` | `1, 2, 3...` |
| **页大小** | `每页显示多少条记录` | `pageSize, limit, size` | `10, 20, 50` |
| **总记录数** | `数据表中符合条件的总行数` | `total, totalCount` | `1250` |
| **总页数** | `根据总记录数计算的页数` | `totalPages, pageCount` | `63` |

---

## 2. 🔧 LIMIT基础语法详解


### 2.1 LIMIT语法格式


**📝 基本语法**
```sql
-- 格式1：只指定返回行数
SELECT * FROM table_name LIMIT row_count;

-- 格式2：指定偏移量和行数
SELECT * FROM table_name LIMIT offset, row_count;

-- 格式3：标准SQL语法（推荐）
SELECT * FROM table_name LIMIT row_count OFFSET offset;
```

### 2.2 LIMIT基础用法示例


**💡 简单LIMIT使用**
```sql
-- 获取前10条员工记录
SELECT employee_id, employee_name, salary
FROM employees
ORDER BY employee_id
LIMIT 10;

执行结果：返回按ID排序的前10名员工
等效理解：取排序后结果的第1-10行
```

**🔢 LIMIT的数值含义**
```sql
-- LIMIT 0：返回0条记录（用于测试SQL语法）
SELECT * FROM employees LIMIT 0;

-- LIMIT 1：只返回1条记录（常用于判断存在性）
SELECT * FROM employees WHERE dept_id = 5 LIMIT 1;

-- 大数值LIMIT：如果指定的行数超过实际数据，返回所有数据
SELECT * FROM employees LIMIT 999999;  -- 表只有100行，返回100行
```

### 2.3 ORDER BY与LIMIT的配合


**⚠️ 重要：LIMIT必须配合ORDER BY使用**
```sql
-- ❌ 问题写法：没有ORDER BY的LIMIT
SELECT * FROM employees LIMIT 10;
-- 问题：每次执行可能返回不同的10条记录，结果不可预测

-- ✅ 正确写法：ORDER BY确保结果稳定
SELECT * FROM employees 
ORDER BY employee_id
LIMIT 10;
-- 结果：每次都返回相同的前10条记录
```

**🎯 排序字段的选择原则**
```
排序字段建议：
1️⃣ 唯一性强：主键、唯一索引字段
2️⃣ 业务意义：创建时间、更新时间、重要性等级
3️⃣ 索引支持：排序字段应该有索引支持
4️⃣ 数据稳定：排序字段值相对稳定

示例：
ORDER BY create_time DESC, id DESC  -- 时间倒序，ID作为二级排序
```

---

## 3. 📖 OFFSET偏移量分页机制


### 3.1 OFFSET工作原理


**🔸 偏移量分页的基本概念**
```
OFFSET偏移量：跳过多少行数据后开始返回
LIMIT行数：从偏移位置开始返回多少行

分页计算公式：
页码从1开始：OFFSET = (page - 1) * pageSize
页码从0开始：OFFSET = page * pageSize
```

**📊 分页参数对应关系**
```
分页参数示例：
• 第1页，每页10条：LIMIT 10 OFFSET 0   (第1-10行)
• 第2页，每页10条：LIMIT 10 OFFSET 10  (第11-20行)
• 第3页，每页10条：LIMIT 10 OFFSET 20  (第21-30行)

计算公式：
当前页码 = page
每页大小 = pageSize  
偏移量 = (page - 1) * pageSize
查询行数 = pageSize
```

### 3.2 OFFSET分页实现示例


**💻 完整分页查询实现**
```sql
-- 分页查询模板
SELECT employee_id, employee_name, department, salary
FROM employees
WHERE status = 'active'           -- 业务过滤条件
ORDER BY create_time DESC        -- 排序保证结果稳定
LIMIT 20 OFFSET 40;              -- 第3页，每页20条

-- 计算过程：
-- 第3页 → page = 3
-- 每页20条 → pageSize = 20
-- 偏移量 = (3-1) * 20 = 40
-- 查询 = 跳过前40行，返回接下来的20行
```

**🔍 分页查询的完整SQL**
```sql
-- 查询第5页数据（每页15条）
-- 计算：OFFSET = (5-1) * 15 = 60

SELECT 
    product_id,
    product_name,
    price,
    category_name
FROM products p
JOIN categories c ON p.category_id = c.id
WHERE p.status = 'active'
  AND p.price > 100
ORDER BY p.create_time DESC, p.id DESC
LIMIT 15 OFFSET 60;
```

### 3.3 总记录数查询


**📊 获取总数据量**
```sql
-- 分页查询通常需要两个SQL：

-- 1. 查询总记录数（用于计算总页数）
SELECT COUNT(*) as total_count
FROM products p  
JOIN categories c ON p.category_id = c.id
WHERE p.status = 'active'
  AND p.price > 100;

-- 2. 查询当前页数据
SELECT p.product_id, p.product_name, p.price
FROM products p
JOIN categories c ON p.category_id = c.id  
WHERE p.status = 'active'
  AND p.price > 100
ORDER BY p.create_time DESC
LIMIT 15 OFFSET 60;
```

**⚠️ COUNT查询的性能注意**
```
COUNT(*)性能问题：
• 大表的COUNT(*)查询可能很慢
• 特别是带WHERE条件的COUNT查询
• 需要扫描大量数据或索引

优化方案：
✅ 缓存总数：定期更新总数缓存
✅ 近似计算：使用EXPLAIN估算行数
✅ 分段缓存：按时间段缓存统计数据
```

---

## 4. 🧮 分页参数计算与处理


### 4.1 分页算法设计


**🔑 标准分页算法**
```java
public class PaginationCalculator {
    
    /**
     * 分页参数计算
     */
    public static class PageInfo {
        private int pageNum;        // 当前页码
        private int pageSize;       // 每页大小
        private long total;         // 总记录数
        private int totalPages;     // 总页数
        private long offset;        // SQL偏移量
        
        public PageInfo(int pageNum, int pageSize, long total) {
            this.pageNum = Math.max(1, pageNum);           // 页码最小为1
            this.pageSize = Math.max(1, pageSize);         // 页大小最小为1
            this.total = Math.max(0, total);               // 总数最小为0
            
            // 计算总页数
            this.totalPages = (int) Math.ceil((double) total / pageSize);
            
            // 修正页码范围
            if (this.pageNum > totalPages && totalPages > 0) {
                this.pageNum = totalPages;
            }
            
            // 计算SQL偏移量
            this.offset = (long) (this.pageNum - 1) * this.pageSize;
        }
        
        // 获取SQL偏移量
        public long getOffset() {
            return offset;
        }
        
        // 是否有下一页
        public boolean hasNextPage() {
            return pageNum < totalPages;
        }
        
        // 是否有上一页
        public boolean hasPreviousPage() {
            return pageNum > 1;
        }
    }
}
```

### 4.2 总页数计算


**🧮 页数计算的数学原理**
```
总页数公式：
totalPages = Math.ceil(total / pageSize)

计算示例：
总记录数 = 103
每页大小 = 10
总页数 = ceil(103 / 10) = ceil(10.3) = 11页

分页分布：
第1页：第1-10条   (10条)
第2页：第11-20条  (10条)  
...
第10页：第91-100条 (10条)
第11页：第101-103条 (3条)  ← 最后一页可能不足pageSize
```

**💡 边界情况处理**
```java
// 处理各种边界情况的计算
public static int calculateTotalPages(long total, int pageSize) {
    if (total <= 0) return 0;           // 无数据
    if (pageSize <= 0) return 0;        // 无效页大小
    
    return (int) Math.ceil((double) total / pageSize);
}

// 修正页码范围
public static int validatePageNum(int pageNum, int totalPages) {
    if (pageNum < 1) return 1;                    // 最小页码
    if (totalPages > 0 && pageNum > totalPages) {
        return totalPages;                        // 最大页码
    }
    return pageNum;
}
```

### 4.3 分页参数验证与处理


**🔒 输入参数验证**
```java
public class PaginationValidator {
    
    // 验证和处理分页参数
    public static PageRequest validateParams(Integer pageNum, Integer pageSize) {
        // 设置默认值
        int validPageNum = (pageNum != null && pageNum > 0) ? pageNum : 1;
        int validPageSize = (pageSize != null && pageSize > 0) ? pageSize : 10;
        
        // 限制页大小范围（防止恶意查询）
        if (validPageSize > 100) {
            validPageSize = 100;  // 限制最大页大小
        }
        
        return new PageRequest(validPageNum, validPageSize);
    }
    
    // 分页请求参数类
    public static class PageRequest {
        private int pageNum;
        private int pageSize;
        
        public PageRequest(int pageNum, int pageSize) {
            this.pageNum = pageNum;
            this.pageSize = pageSize;
        }
        
        // 计算SQL偏移量
        public long getOffset() {
            return (long) (pageNum - 1) * pageSize;
        }
        
        // 获取SQL语句
        public String toLimitSql() {
            return String.format("LIMIT %d OFFSET %d", pageSize, getOffset());
        }
    }
}
```

### 4.4 分页响应结果设计


**📦 分页结果封装**
```java
// 标准分页响应格式
public class PageResult<T> {
    private List<T> data;           // 当前页数据
    private int pageNum;            // 当前页码
    private int pageSize;           // 每页大小
    private long total;             // 总记录数
    private int totalPages;         // 总页数
    
    // 分页导航信息
    private boolean hasNext;        // 是否有下一页
    private boolean hasPrevious;    // 是否有上一页
    private boolean isFirstPage;    // 是否首页
    private boolean isLastPage;     // 是否末页
    
    public PageResult(List<T> data, int pageNum, int pageSize, long total) {
        this.data = data;
        this.pageNum = pageNum;
        this.pageSize = pageSize;
        this.total = total;
        this.totalPages = (int) Math.ceil((double) total / pageSize);
        
        // 计算导航状态
        this.hasNext = pageNum < totalPages;
        this.hasPrevious = pageNum > 1;
        this.isFirstPage = pageNum == 1;
        this.isLastPage = pageNum == totalPages || totalPages == 0;
    }
}
```

---

## 5. ⚡ 分页性能优化策略


### 5.1 分页性能影响分析


**📊 OFFSET性能问题**
```
OFFSET性能特点：
• 偏移量越大，性能越差
• 数据库需要跳过OFFSET行数据
• 大偏移量查询会成为性能瓶颈

性能测试示例（100万条记录）：
┌────────┬──────────┬────────────┐
│ 页码   │ OFFSET值  │ 查询耗时    │
├────────┼──────────┼────────────┤
│ 第1页  │ 0        │ 10ms       │
│ 第100页│ 1000     │ 50ms       │
│ 第1000页│10000    │ 200ms      │
│ 第5000页│50000    │ 800ms      │
│ 第1万页 │100000   │ 2000ms     │
└────────┴──────────┴────────────┘

性能下降原因：
数据库必须扫描并跳过前面的所有行
```

### 5.2 游标分页优化方案


**🚀 基于游标的分页（推荐大数据量）**
```sql
-- 传统OFFSET分页（性能差）
SELECT * FROM products
WHERE status = 'active'
ORDER BY id
LIMIT 20 OFFSET 10000;  -- 需要跳过1万行

-- 游标分页（性能好）  
SELECT * FROM products
WHERE status = 'active'
  AND id > 12345        -- 使用上页最后一条记录的ID
ORDER BY id
LIMIT 20;               -- 无需OFFSET

优势对比：
传统方式：扫描10020行，返回20行
游标方式：直接定位，扫描20行
性能提升：10-100倍（取决于偏移量大小）
```

**💡 游标分页实现要点**
```
实现原则：
• 使用上页最后一条记录的标识字段作为起点
• 标识字段必须有序且唯一（如主键、时间戳）
• 适合无限滚动的场景

局限性：
• 只能顺序翻页，不能随机跳转
• 需要客户端记住游标位置
• 排序字段必须是唯一且有序的
```

### 5.3 索引优化分页查询


**🔍 分页查询的索引设计**
```sql
-- 为分页查询建立合适索引
-- 查询语句：
SELECT product_id, product_name, price
FROM products  
WHERE category_id = 5
  AND status = 'active'
ORDER BY create_time DESC
LIMIT 20 OFFSET 100;

-- 最优索引设计：
CREATE INDEX idx_products_page 
ON products (category_id, status, create_time DESC);

索引覆盖优化：
CREATE INDEX idx_products_cover
ON products (category_id, status, create_time DESC, 
             product_id, product_name, price);
-- 包含所有查询字段，避免回表查询
```

### 5.4 分页缓存策略


**💾 缓存分页结果**
```java
// 分页缓存实现策略
@Service
public class ProductPageService {
    
    @Autowired
    private RedisTemplate redisTemplate;
    
    public PageResult<Product> getProducts(int pageNum, int pageSize) {
        String cacheKey = String.format("products:page:%d:%d", pageNum, pageSize);
        
        // 尝试从缓存获取
        PageResult<Product> cached = redisTemplate.opsForValue().get(cacheKey);
        if (cached != null) {
            return cached;
        }
        
        // 缓存未命中，查询数据库
        PageResult<Product> result = queryFromDatabase(pageNum, pageSize);
        
        // 缓存查询结果（设置5分钟过期）
        redisTemplate.opsForValue().set(cacheKey, result, 300, TimeUnit.SECONDS);
        
        return result;
    }
}

缓存策略：
✅ 缓存热点页面：前几页访问频率高
✅ 设置过期时间：避免数据过期
✅ 缓存失效机制：数据更新时清除相关缓存
❌ 避免缓存所有页面：内存浪费
```

---

## 6. 🛡️ 分页边界与一致性


### 6.1 边界情况处理


**🔧 各种边界情况的处理方案**
```
空数据处理：
┌─────────────────────────────┐
│ 总记录数 = 0                 │
│ 总页数 = 0                  │  
│ 当前页 = 1（显示空页面）      │
│ 返回空列表                   │
└─────────────────────────────┘

页码超出范围：
┌─────────────────────────────┐
│ 用户访问第999页              │
│ 实际总页数只有50页            │
│ 处理：重定向到第50页          │
│ 或返回空结果并提示            │
└─────────────────────────────┘

页大小异常：
┌─────────────────────────────┐
│ 用户设置pageSize = 0         │
│ 处理：默认设置为10           │
│ 用户设置pageSize = 999999    │  
│ 处理：限制最大值为100        │
└─────────────────────────────┘
```

### 6.2 分页一致性保证


**🔒 数据一致性挑战**
```
分页一致性问题：
用户正在浏览第2页时，有新数据插入到第1页
结果：第2页的数据可能发生变化，用户看到重复数据

问题示意：
时刻1 - 用户查看第1页(1-10条)
时刻2 - 新增一条数据到第1位  
时刻3 - 用户查看第2页(11-20条)
问题：第2页显示的是原来第1页的最后一条数据
```

**💡 一致性解决方案**
```
快照读取：
• 使用数据库事务的一致性读
• 在事务开始时确定数据版本
• 整个分页浏览过程使用相同快照

时间戳过滤：
SELECT * FROM products
WHERE create_time <= '2024-01-01 10:00:00'  -- 固定时间点
ORDER BY create_time DESC, id DESC
LIMIT 20 OFFSET 40;

版本标识：
• 在分页请求中包含数据版本号
• 检测到版本变化时提示用户刷新
• 适用于数据变化频率较低的场景
```

### 6.3 分页边界检查


**🔍 完善的边界检查实现**
```java
public class PaginationBoundaryHandler {
    
    // 边界检查和修正
    public static PageInfo handleBoundaries(int requestedPage, int pageSize, long total) {
        
        // 1. 参数基础验证
        pageSize = Math.max(1, Math.min(pageSize, 100));  // 限制1-100
        total = Math.max(0, total);
        
        // 2. 计算总页数
        int totalPages = total == 0 ? 0 : (int) Math.ceil((double) total / pageSize);
        
        // 3. 页码范围修正
        int validPage;
        if (totalPages == 0) {
            validPage = 1;          // 无数据时显示第1页
        } else {
            validPage = Math.max(1, Math.min(requestedPage, totalPages));
        }
        
        return new PageInfo(validPage, pageSize, total, totalPages);
    }
    
    // 检查分页结果的合法性
    public static void validatePageResult(PageResult<?> result) {
        if (result.getData().size() > result.getPageSize()) {
            throw new IllegalStateException("返回数据超过页大小限制");
        }
        
        if (result.getPageNum() > result.getTotalPages() && result.getTotalPages() > 0) {
            throw new IllegalStateException("当前页码超出总页数范围");
        }
    }
}
```

## 6.4 分页URL参数设计


**🔗 RESTful分页接口设计**
```
标准分页URL格式：
GET /api/products?page=2&size=20&sort=createTime,desc

参数说明：
• page：页码，从1开始（符合用户习惯）
• size：页大小，默认10-20
• sort：排序参数，字段名+方向

响应格式：
{
  "data": [...],              // 当前页数据
  "page": 2,                  // 当前页码
  "size": 20,                 // 页大小
  "total": 1250,              // 总记录数
  "totalPages": 63,           // 总页数
  "hasNext": true,            // 是否有下一页
  "hasPrevious": true         // 是否有上一页
}
```

---

## 7. 🎯 分页实现标准方案


### 7.1 完整分页实现模板


**🔑 生产级分页实现**
```java
@Service
public class StandardPaginationService {
    
    @Autowired
    private ProductMapper productMapper;
    
    public PageResult<Product> getProductPage(ProductQuery query, int pageNum, int pageSize) {
        
        // 1. 参数验证和修正
        PageRequest request = PaginationValidator.validateParams(pageNum, pageSize);
        
        // 2. 查询总记录数
        long total = productMapper.countProducts(query);
        if (total == 0) {
            return PageResult.empty(pageNum, pageSize);
        }
        
        // 3. 计算分页信息
        PageInfo pageInfo = new PageInfo(pageNum, pageSize, total);
        
        // 4. 查询当前页数据
        List<Product> products = productMapper.selectProductsPage(
            query, 
            pageInfo.getPageSize(), 
            pageInfo.getOffset()
        );
        
        // 5. 封装返回结果
        return new PageResult<>(products, pageInfo);
    }
}
```

**🗄️ MyBatis分页SQL实现**
```xml
<!-- 查询总数的SQL -->
<select id="countProducts" resultType="long">
    SELECT COUNT(*)
    FROM products p
    <where>
        <if test="categoryId != null">
            AND p.category_id = #{categoryId}
        </if>
        <if test="status != null">
            AND p.status = #{status}  
        </if>
        <if test="keyword != null">
            AND p.product_name LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </where>
</select>

<!-- 分页查询数据的SQL -->
<select id="selectProductsPage" resultType="Product">
    SELECT 
        p.product_id,
        p.product_name,
        p.price,
        p.status,
        p.create_time
    FROM products p
    <where>
        <if test="query.categoryId != null">
            AND p.category_id = #{query.categoryId}
        </if>
        <if test="query.status != null">
            AND p.status = #{query.status}
        </if>
        <if test="query.keyword != null">
            AND p.product_name LIKE CONCAT('%', #{query.keyword}, '%')
        </if>
    </where>
    ORDER BY p.create_time DESC, p.product_id DESC
    LIMIT #{pageSize} OFFSET #{offset}
</select>
```

### 7.2 分页组件前端实现


**🎨 前端分页组件设计**
```javascript
// Vue分页组件实现
export default {
  name: 'Pagination',
  props: {
    current: { type: Number, default: 1 },      // 当前页
    pageSize: { type: Number, default: 10 },    // 页大小
    total: { type: Number, default: 0 },        // 总数
    showQuickJumper: { type: Boolean, default: true },  // 快速跳转
    showSizeChanger: { type: Boolean, default: true }   // 页大小选择
  },
  
  computed: {
    // 计算总页数
    totalPages() {
      return Math.ceil(this.total / this.pageSize);
    },
    
    // 生成页码列表
    pageNumbers() {
      const pages = [];
      const maxVisible = 7;  // 最多显示7个页码
      
      let start = Math.max(1, this.current - 3);
      let end = Math.min(this.totalPages, start + maxVisible - 1);
      
      for (let i = start; i <= end; i++) {
        pages.push(i);
      }
      
      return pages;
    }
  },
  
  methods: {
    // 页码改变事件
    onPageChange(page) {
      if (page >= 1 && page <= this.totalPages) {
        this.$emit('change', page);
      }
    },
    
    // 页大小改变事件
    onPageSizeChange(size) {
      this.$emit('size-change', size);
    }
  }
}
```

### 7.3 分页实现的完整流程


**🔄 前后端分页交互流程**
```
用户操作：点击第3页
       │
       ▼
┌─────────────────┐
│ 前端计算参数     │ ← page=3, size=20
└─────────────────┘
       │
       ▼
┌─────────────────┐
│ 发送API请求     │ ← GET /api/products?page=3&size=20
└─────────────────┘
       │
       ▼
┌─────────────────┐
│ 后端参数验证     │ ← 验证page和size的合法性
└─────────────────┘
       │
       ▼
┌─────────────────┐
│ 查询总记录数     │ ← SELECT COUNT(*)
└─────────────────┘
       │
       ▼
┌─────────────────┐
│ 计算OFFSET      │ ← offset = (3-1) * 20 = 40
└─────────────────┘
       │
       ▼  
┌─────────────────┐
│ 执行分页查询     │ ← LIMIT 20 OFFSET 40
└─────────────────┘
       │
       ▼
┌─────────────────┐
│ 封装返回结果     │ ← PageResult包装
└─────────────────┘
       │
       ▼
┌─────────────────┐
│ 前端渲染页面     │ ← 显示数据和分页导航
└─────────────────┘
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的分页概念


```
🔸 LIMIT语法：控制返回行数的基本语法，必须配合ORDER BY使用
🔸 OFFSET机制：偏移量分页的原理和性能特点
🔸 分页计算：页码、页大小、总页数、偏移量的计算关系
🔸 边界处理：空数据、页码超范围、参数异常的处理方案
🔸 性能优化：大偏移量的性能问题和游标分页解决方案
```

### 8.2 关键理解要点


**🔹 分页的本质是数据切片**
```
数据切片过程：
完整结果集 → 排序 → 跳过OFFSET行 → 取LIMIT行 → 返回结果

性能关键：
• OFFSET越大，跳过的数据越多，性能越差
• ORDER BY决定了分页结果的稳定性
• 索引可以加速排序和定位过程
```

**🔹 分页参数的相互依赖**
```
参数关系：
页码(page) + 页大小(pageSize) → 确定数据范围
总记录数(total) + 页大小(pageSize) → 计算总页数  
当前页码 + 总页数 → 确定导航状态

边界约束：
1 ≤ 当前页码 ≤ 总页数
1 ≤ 页大小 ≤ 最大限制（如100）
```

### 8.3 实际应用指导


**🎯 分页方案选择**
```
数据量小(<1万)：
推荐：OFFSET分页
原因：实现简单，性能足够

数据量中等(1万-100万)：
推荐：OFFSET + 缓存
原因：前几页用OFFSET，热点数据缓存

数据量大(>100万)：
推荐：游标分页
原因：性能稳定，不受数据量影响

无限滚动场景：
推荐：游标分页
原因：只需顺序加载，性能最优
```

**🔧 分页性能优化检查清单**
```
☑️ ORDER BY字段是否有索引支持
☑️ WHERE条件是否能有效过滤数据
☑️ 是否考虑了索引覆盖优化
☑️ 大偏移量是否使用了游标分页
☑️ 热点页面是否进行了缓存
☑️ COUNT查询是否有性能问题
☑️ 分页参数是否有合理的边界限制
```

**核心记忆**：
- 分页是大数据展示的基础技术，LIMIT控制返回行数
- OFFSET偏移量大时性能差，游标分页是大数据量的最优方案  
- 分页必须配合ORDER BY确保结果稳定性
- 边界情况处理和参数验证是分页实现的重要环节