---
title: 8、复合索引设计
---
## 📚 目录

1. [复合索引基础概念](#1-复合索引基础概念)
2. [最左前缀原则详解](#2-最左前缀原则详解)
3. [索引字段顺序设计](#3-索引字段顺序设计)
4. [索引覆盖策略](#4-索引覆盖策略)
5. [多查询模式索引设计](#5-多查询模式索引设计)
6. [复合索引优化实践](#6-复合索引优化实践)
7. [索引维护与监控](#7-索引维护与监控)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🏗️ 复合索引基础概念


### 1.1 什么是复合索引


**🔸 复合索引的定义**
复合索引（也叫组合索引）就是包含多个字段的索引，就像给一本书建立"多关键词"目录：
- **单列索引**：按姓名查找 → 只能通过姓名快速定位
- **复合索引**：按姓名+年龄+城市查找 → 可以组合条件快速定位

```sql
-- 单列索引示例
CREATE INDEX idx_name ON users(name);

-- 复合索引示例
CREATE INDEX idx_name_age_city ON users(name, age, city);
```

### 1.2 复合索引存储结构


**📊 复合索引在B+树中的存储**
```
复合索引 (name, age, city) 的存储方式：

根节点
├── (张三,25,北京) → 指向子节点
├── (李四,30,上海) → 指向子节点  
└── (王五,35,深圳) → 指向子节点

叶子节点数据排列（按字段顺序）：
(李明,20,北京) → (李明,22,上海) → (李明,25,深圳)
(张三,18,北京) → (张三,20,上海) → (张三,25,深圳)
(王五,30,北京) → (王五,32,上海) → (王五,35,深圳)

排序规律：
1. 先按name排序
2. name相同时按age排序  
3. name和age都相同时按city排序
```

### 1.3 复合索引与单列索引的区别


**💡 对比分析**

| 方面 | **单列索引** | **复合索引** | **适用场景** |
|------|------------|------------|-------------|
| **查询灵活性** | `只支持单字段查询` | `支持多字段组合查询` | `复杂查询条件` |
| **存储空间** | `占用较少` | `占用较多` | `权衡空间与性能` |
| **维护成本** | `更新简单` | `更新复杂` | `写入频繁的表谨慎使用` |
| **查询覆盖** | `覆盖范围有限` | `可实现索引覆盖` | `减少回表查询` |

---

## 2. 🔍 最左前缀原则详解


### 2.1 最左前缀原则的含义


**🔸 核心概念**
最左前缀原则是复合索引使用的**黄金法则**：
- 复合索引只能从最左边的字段开始使用
- 中间不能跳过字段
- 一旦遇到范围查询，后续字段索引失效

**💡 生活化理解**
就像查字典一样：
- 字典按拼音排序：先按声母，再按韵母，最后按声调
- 你必须先知道声母，才能快速定位
- 如果只知道韵母，就只能从头翻页查找

### 2.2 最左前缀使用示例


**🔧 建立测试索引**
```sql
-- 创建复合索引
CREATE INDEX idx_name_age_city ON users(name, age, city);

-- 表结构
users: id, name, age, city, phone, email
```

**✅ 能使用索引的查询**
```sql
-- 使用索引：完整匹配
SELECT * FROM users WHERE name='张三' AND age=25 AND city='北京';

-- 使用索引：左前缀匹配  
SELECT * FROM users WHERE name='张三' AND age=25;
SELECT * FROM users WHERE name='张三';

-- 使用索引：左前缀+范围查询
SELECT * FROM users WHERE name='张三' AND age > 20;
```

**❌ 无法使用索引的查询**
```sql
-- 跳过最左字段
SELECT * FROM users WHERE age=25 AND city='北京';

-- 中间跳过字段
SELECT * FROM users WHERE name='张三' AND city='北京';

-- 范围查询后的字段
SELECT * FROM users WHERE name='张三' AND age > 20 AND city='北京';
-- ↑ city字段无法使用索引
```

### 2.3 最左前缀原则验证


**🔍 执行计划验证**
```sql
-- 查看索引使用情况
EXPLAIN SELECT * FROM users WHERE name='张三' AND age=25;

典型输出：
+----+-------+-------+------+---------------------+---------+
| id | type  | key   | ref  | key_len             | rows    |
+----+-------+-------+------+---------------------+---------+
| 1  | ref   | idx_  | const| 152                 | 1       |
|    |       | name_ | ,const|                    |         |
|    |       | age_  |      |                     |         |
|    |       | city  |      |                     |         |
+----+-------+-------+------+---------------------+---------+

key_len分析：
• name字段长度 + age字段长度 = 使用的索引长度
• 通过key_len可以判断使用了哪几个字段
```

---

## 3. 📊 索引字段顺序设计


### 3.1 索引字段顺序决策树 🔥


```
复合索引字段排序决策流程

开始：确定需要建立复合索引
    ↓
第一步：识别查询模式
    ├── 单字段等值查询多？ → 该字段优先级+1
    ├── 多字段组合查询多？ → 分析组合频率
    └── 范围查询字段？ → 降低优先级
    ↓
第二步：计算字段选择性
    ├── 唯一值多的字段 → 选择性高，排前面
    ├── 重复值多的字段 → 选择性低，排后面  
    └── 计算公式：选择性 = DISTINCT(列值) / 总行数
    ↓
第三步：考虑字段稳定性
    ├── 频繁更新的字段 → 尽量排后面
    ├── 稳定不变的字段 → 可以排前面
    └── 权衡更新成本
    ↓
第四步：分析业务优先级
    ├── 核心业务查询 → 对应字段排前面
    ├── 辅助查询 → 对应字段排后面
    └── 考虑查询频率权重
    ↓
输出：最终字段顺序方案
```

### 3.2 字段选择性分析


**🔸 选择性计算方法**
选择性越高，索引效果越好：
```sql
-- 计算各字段的选择性
SELECT 
    'name' as column_name,
    COUNT(DISTINCT name) / COUNT(*) as selectivity,
    COUNT(DISTINCT name) as unique_values,
    COUNT(*) as total_rows
FROM users

UNION ALL

SELECT 
    'age' as column_name,
    COUNT(DISTINCT age) / COUNT(*) as selectivity,
    COUNT(DISTINCT age) as unique_values,
    COUNT(*) as total_rows  
FROM users

UNION ALL

SELECT 
    'city' as column_name,
    COUNT(DISTINCT city) / COUNT(*) as selectivity,
    COUNT(DISTINCT city) as unique_values,
    COUNT(*) as total_rows
FROM users;
```

**📊 选择性分析示例**
```
分析结果：
name字段：选择性0.95（95%的值唯一）→ 排第一
age字段：选择性0.15（年龄范围有限）→ 排第二  
city字段：选择性0.05（城市种类少）→ 排第三

推荐顺序：(name, age, city)
```

### 3.3 查询频率驱动字段排序 🔸


**🎯 查询模式分析**
```sql
-- 分析实际查询模式（模拟）
查询模式统计：
• WHERE name = ? AND age = ? AND city = ?    (40%)
• WHERE name = ? AND age = ?                 (30%)  
• WHERE name = ?                             (20%)
• WHERE name = ? AND city = ?                (8%)
• WHERE age = ? AND city = ?                 (2%)

分析结论：
1. name字段出现在98%的查询中 → 必须排第一
2. age字段出现在70%的查询中 → 应该排第二
3. city字段出现在50%的查询中 → 排第三
4. 有8%的查询跳过age直接用city → 可考虑额外单独索引
```

### 3.4 复合索引字段权重分析 🔸


**⚖️ 权重计算公式**
```
字段权重 = 查询频率权重 × 选择性权重 × 稳定性权重

权重计算示例：
name字段：
• 查询频率权重：0.98 (出现在98%查询中)
• 选择性权重：0.95 (95%唯一值)  
• 稳定性权重：0.8 (偶尔更新)
• 综合权重：0.98 × 0.95 × 0.8 = 0.74

age字段：
• 查询频率权重：0.70
• 选择性权重：0.15
• 稳定性权重：0.9 (较少更新)
• 综合权重：0.70 × 0.15 × 0.9 = 0.09

city字段：
• 查询频率权重：0.50  
• 选择性权重：0.05
• 稳定性权重：0.95 (很少更新)
• 综合权重：0.50 × 0.05 × 0.95 = 0.02

最终排序：name → age → city
```

---

## 4. 📝 索引覆盖策略


### 4.1 索引覆盖的概念


**🔸 什么是索引覆盖**
索引覆盖是指查询所需的数据完全可以从索引中获取，不需要访问表数据：
- **优点**：避免回表查询，减少IO操作
- **原理**：索引叶子节点包含了查询需要的所有字段
- **效果**：查询性能显著提升

**💡 通俗理解**
就像在图书馆查资料：
- **普通查询**：先看目录找到页码，再翻到那一页读内容
- **覆盖查询**：目录本身就包含了你要的信息，不用翻书

### 4.2 复合索引覆盖优化 🔥


**🎯 设计覆盖索引的策略**
```sql
-- 原始查询（需要回表）
SELECT id, name, age FROM users WHERE name='张三' AND age > 25;

-- 当前索引：idx_name_age (name, age)
-- 问题：需要回表获取id字段

-- 优化方案1：扩展索引字段
CREATE INDEX idx_name_age_id ON users(name, age, id);

-- 优化方案2：调整查询字段顺序
CREATE INDEX idx_name_age_extra ON users(name, age) 
INCLUDE (id);  -- MySQL 8.0.13+支持INCLUDE语法
```

**⚡ 覆盖索引效果验证**
```sql
-- 验证是否使用覆盖索引
EXPLAIN SELECT name, age FROM users WHERE name='张三';

期望结果：
• Extra列显示：Using index  ← 表示使用了覆盖索引
• type列显示：ref 或 range
• 没有Using temporary或Using filesort
```

### 4.3 索引长度限制处理 🔥


**🔸 索引长度限制问题**
```
InnoDB索引限制：
• 单个索引最大长度：3072字节（MySQL 5.7+）
• 单个字段最大长度：767字节（默认）
• 复合索引字段数：最多16个

常见问题：
VARCHAR(500) + VARCHAR(500) + INT = 超过长度限制
```

**💡 解决策略**
```sql
-- 方案1：使用前缀索引
CREATE INDEX idx_title_content_prefix ON articles(
    title(50),        -- 只索引前50个字符
    content(100),     -- 只索引前100个字符
    category
);

-- 方案2：选择关键字段
-- 去掉不必要的长字段，保留核心查询字段
CREATE INDEX idx_core_fields ON articles(category, status, create_time);

-- 方案3：分拆索引
-- 将长索引拆分为多个短索引
CREATE INDEX idx_title_category ON articles(title(50), category);
CREATE INDEX idx_content_status ON articles(content(100), status);
```

### 4.4 前缀索引优化算法 🔸


**🔧 前缀长度选择算法**
```sql
-- 分析前缀选择性的SQL
SELECT 
    LENGTH(title) as avg_length,
    COUNT(DISTINCT LEFT(title, 10)) / COUNT(*) as sel_10,
    COUNT(DISTINCT LEFT(title, 20)) / COUNT(*) as sel_20,
    COUNT(DISTINCT LEFT(title, 30)) / COUNT(*) as sel_30,
    COUNT(DISTINCT LEFT(title, 50)) / COUNT(*) as sel_50,
    COUNT(DISTINCT title) / COUNT(*) as sel_full
FROM articles;

选择性分析结果：
前缀长度10：选择性0.3  
前缀长度20：选择性0.6
前缀长度30：选择性0.85  ← 接近完整选择性，选择此长度
前缀长度50：选择性0.87
完整字段：选择性0.9

结论：使用30字符前缀达到85%选择性，性价比最高
```

---

## 5. 🎯 多查询模式索引设计


### 5.1 查询模式分析方法


**🔸 查询模式收集**
分析实际业务中的查询模式，找出最常用的查询组合：
```sql
-- 常见查询模式示例
模式1：用户登录查询（80%频率）
SELECT id, name, status FROM users WHERE email=? AND status='active';

模式2：用户信息查询（60%频率）  
SELECT * FROM users WHERE name=? AND age BETWEEN ? AND ?;

模式3：地区统计查询（40%频率）
SELECT city, COUNT(*) FROM users WHERE age > ? GROUP BY city;

模式4：管理员查询（10%频率）
SELECT id, name, email FROM users WHERE city=? AND status=?;
```

### 5.2 多查询模式索引策略 🔥


**🎯 索引设计策略**

> 💡 **策略原则**：优先覆盖高频查询，兼顾低频查询

```sql
-- 主索引：覆盖模式1和模式2（高频查询）
CREATE INDEX idx_main ON users(email, status, name, age);
-- 覆盖登录查询和部分用户查询

-- 辅助索引1：覆盖模式3（地区统计）  
CREATE INDEX idx_age_city ON users(age, city);
-- 支持按年龄范围的地区统计

-- 辅助索引2：覆盖模式4（管理查询）
CREATE INDEX idx_city_status_covering ON users(city, status, id, name, email);
-- 实现完全覆盖，避免回表

-- 评估索引效果
各查询模式的索引命中情况：
模式1：主索引完全命中 ✅
模式2：主索引部分命中 ✅  
模式3：辅助索引1命中 ✅
模式4：辅助索引2完全命中 ✅
```

### 5.3 索引字段相关性分析 🔥


**🔸 字段相关性检测**
```sql
-- 分析字段间的相关性
WITH field_combinations AS (
    SELECT 
        name, age, city,
        COUNT(*) as combination_count
    FROM users  
    GROUP BY name, age, city
)
SELECT 
    AVG(combination_count) as avg_combinations,
    MAX(combination_count) as max_combinations,
    MIN(combination_count) as min_combinations,
    COUNT(*) as unique_combinations
FROM field_combinations;

相关性分析：
• 如果unique_combinations接近总行数 → 字段相关性低
• 如果avg_combinations很高 → 字段相关性高，存在数据倾斜
```

**💡 相关性对索引设计的影响**
```
高相关性字段：
• 例：province和city高度相关
• 索引设计：(province, city) 效率不高
• 优化策略：考虑只索引更具体的字段city

低相关性字段：
• 例：name和age相关性低
• 索引设计：(name, age) 效果很好
• 每个字段都能有效过滤数据
```

### 5.4 多维查询索引策略 🔸


**🔸 多维查询特点**
多维查询指的是查询条件涉及多个维度，每个维度都可能有不同的查询方式：
```sql
-- 电商订单查询场景
SELECT * FROM orders WHERE 
    user_id = 12345                    -- 精确匹配
    AND create_time > '2025-01-01'     -- 范围查询
    AND status IN ('paid', 'shipped') -- 列表匹配
    AND amount > 100;                  -- 范围查询
```

**🎯 多维索引设计策略**
```sql
-- 策略1：主要维度索引
CREATE INDEX idx_orders_main ON orders(user_id, status, create_time);
-- 适用：按用户查询其订单状态和时间

-- 策略2：时间维度索引  
CREATE INDEX idx_orders_time ON orders(create_time, status, amount);
-- 适用：按时间范围统计订单

-- 策略3：金额维度索引
CREATE INDEX idx_orders_amount ON orders(amount, user_id, create_time);
-- 适用：大额订单分析

验证方法：
-- 查看各索引的使用频率
SELECT 
    index_name,
    table_rows,
    avg_row_length,
    data_length/1024/1024 as size_mb
FROM information_schema.tables t
WHERE table_name = 'orders';
```

---

## 6. 🔧 复合索引优化实践


### 6.1 复合索引拆分合并决策 🔸


**🔸 什么时候拆分索引**
```
拆分场景：
• 索引过长：超过长度限制需要拆分
• 更新频繁：避免大索引的频繁重建
• 查询模式分化：不同查询使用不同字段组合

拆分示例：
原索引：(name, age, city, phone, email)  -- 太长
拆分为：
• 主索引：(name, age, city)      -- 核心查询
• 辅助索引：(phone), (email)     -- 联系方式查询
```

**🔸 什么时候合并索引**
```
合并场景：
• 查询模式重叠：多个查询使用相似字段
• 维护成本高：过多索引增加维护负担
• 空间浪费：多个索引占用过多存储

合并示例：  
原索引：
• idx_name (name)
• idx_name_age (name, age)  
• idx_name_age_city (name, age, city)

优化合并：
• 保留 idx_name_age_city，删除其他
• 原因：复合索引可以支持所有左前缀查询
```

### 6.2 复合索引设计模式库 🔑


**🏗️ 常用设计模式**

**模式1：用户信息查询模式**
```sql
-- 场景：用户管理系统
业务查询：
• 按用户名查询：WHERE username=?
• 按用户名+状态：WHERE username=? AND status=?
• 按邮箱查询：WHERE email=?

索引设计：
CREATE INDEX idx_user_main ON users(username, status, email, id);
-- 支持前3种查询，第4个字段用于覆盖

优化效果：
• username查询：使用索引前缀
• username+status查询：使用索引前缀
• email查询：需要额外建立单独索引
```

**模式2：订单管理模式**
```sql  
-- 场景：电商订单系统
业务查询：
• 用户订单查询：WHERE user_id=? ORDER BY create_time DESC
• 状态统计：WHERE status=? AND create_time > ?
• 金额分析：WHERE amount > ? AND create_time BETWEEN ? AND ?

索引设计：
CREATE INDEX idx_order_user ON orders(user_id, create_time DESC, status);
CREATE INDEX idx_order_time ON orders(create_time, status, amount);

优化要点：
• create_time使用DESC排序，匹配ORDER BY
• 时间字段放在不同位置满足不同查询
```

**模式3：内容管理模式**
```sql
-- 场景：文章管理系统  
业务查询：
• 分类浏览：WHERE category=? AND status='published' ORDER BY create_time DESC
• 作者文章：WHERE author_id=? AND status='published'
• 关键词搜索：WHERE title LIKE '%keyword%' AND category=?

索引设计：
CREATE INDEX idx_content_category ON articles(category, status, create_time DESC);
CREATE INDEX idx_content_author ON articles(author_id, status, create_time DESC);
CREATE INDEX idx_content_search ON articles(category, title(50));

设计理念：
• 按主要查询维度设计索引
• 考虑排序需求
• 平衡覆盖度和维护成本
```

### 6.3 复合索引性能调优方法 🔑


**📊 性能监控SQL**
```sql
-- 监控索引使用情况
SELECT 
    table_schema,
    table_name, 
    index_name,
    seq_in_index,
    column_name,
    cardinality,
    index_type
FROM information_schema.statistics 
WHERE table_name = 'users' 
ORDER BY table_name, index_name, seq_in_index;

-- 查看索引大小和效率
SELECT 
    index_name,
    round(stat_value * $$innodb_page_size / 1024 / 1024, 2) as size_mb
FROM mysql.innodb_index_stats 
WHERE table_name = 'users' AND stat_name = 'size';

-- 分析查询性能
EXPLAIN FORMAT=JSON 
SELECT * FROM users WHERE name='张三' AND age=25;
```

### 6.4 索引效果评估体系 🔑


**📈 评估指标体系**
```sql
-- 1. 索引命中率评估
SELECT 
    ROUND((
        SELECT COUNT(*) FROM performance_schema.events_statements_summary_by_digest 
        WHERE digest_text LIKE '%users%' AND avg_rows_examined <= 100
    ) * 100.0 / (
        SELECT COUNT(*) FROM performance_schema.events_statements_summary_by_digest 
        WHERE digest_text LIKE '%users%'
    ), 2) as index_hit_rate_percent;

-- 2. 平均扫描行数评估
SELECT 
    digest_text,
    count_star as execution_count,
    avg_rows_examined,
    avg_rows_sent,
    avg_timer_wait/1000000 as avg_ms
FROM performance_schema.events_statements_summary_by_digest 
WHERE digest_text LIKE '%users%' 
ORDER BY avg_rows_examined DESC;

-- 3. 索引空间利用率
SELECT 
    table_name,
    round(data_length/1024/1024, 2) as table_size_mb,
    round(index_length/1024/1024, 2) as index_size_mb,
    round(index_length/data_length*100, 2) as index_ratio_percent
FROM information_schema.tables 
WHERE table_name = 'users';
```

---

## 7. 🛠️ 索引维护与监控


### 7.1 复合索引维护最佳实践 🔑


**🔧 日常维护任务**
```sql
-- 1. 定期收集索引统计信息
ANALYZE TABLE users;

-- 2. 检查索引碎片化程度
SELECT 
    table_name,
    engine,
    round(data_length/1024/1024,2) as data_mb,
    round(index_length/1024/1024,2) as index_mb,
    round(data_free/1024/1024,2) as free_mb,
    round(data_free/(data_length+index_length)*100,2) as fragment_percent
FROM information_schema.tables 
WHERE table_schema='your_database' 
ORDER BY fragment_percent DESC;

-- 3. 重建高碎片化的索引
ALTER TABLE users DROP INDEX idx_name_age_city;
ALTER TABLE users ADD INDEX idx_name_age_city (name, age, city);

-- 4. 在线重建索引（MySQL 5.6+）
ALTER TABLE users DROP INDEX idx_name_age_city, 
ADD INDEX idx_name_age_city (name, age, city), ALGORITHM=INPLACE, LOCK=NONE;
```

### 7.2 索引监控告警机制


**📊 关键监控指标**
```sql
-- 监控慢查询中的索引使用情况
SELECT 
    digest_text,
    count_star,
    sum_lock_time/1000000 as total_lock_ms,
    sum_rows_examined,
    sum_rows_sent,
    first_seen,
    last_seen
FROM performance_schema.events_statements_summary_by_digest 
WHERE avg_timer_wait > 1000000000  -- 超过1秒的查询
ORDER BY sum_timer_wait DESC;

-- 监控索引未使用情况
SELECT 
    object_schema,
    object_name, 
    index_name
FROM performance_schema.table_io_waits_summary_by_index_usage 
WHERE count_star = 0 
AND object_schema NOT IN ('mysql', 'performance_schema', 'sys');
```

### 7.3 索引维护成本权衡


**⚖️ 维护成本分析**
```
写入成本：
• 每次INSERT：需要更新所有相关索引
• 每次UPDATE：可能触发索引重排
• 每次DELETE：需要从索引中删除条目

存储成本：
• 索引占用额外存储空间
• 复合索引通常比单列索引大2-5倍
• 需要定期维护和重建

查询收益：
• 查询速度提升：10倍到1000倍不等
• 减少全表扫描
• 提高并发处理能力

权衡原则：
✅ 读多写少的表：多建索引
❌ 写多读少的表：少建索引
⚖️ 读写并重的表：精选关键索引
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 复合索引本质：多字段组合索引，按字段顺序排序存储
🔸 最左前缀原则：索引使用必须从最左字段开始，中间不能跳跃
🔸 字段顺序重要性：顺序决定索引效果，需要综合分析确定
🔸 索引覆盖优化：让索引包含查询所需所有字段，避免回表
🔸 查询模式驱动：根据实际查询模式设计索引，不是凭感觉
🔸 维护成本控制：平衡查询性能和维护开销
```

### 8.2 设计决策要点


**🔹 字段顺序决策要素**
```
选择性优先：
• 唯一值多的字段排前面
• 重复值多的字段排后面
• 提高索引过滤效率

查询频率优先：
• 高频查询字段排前面
• 低频查询字段排后面  
• 确保主要查询能命中索引

稳定性考虑：
• 更新频繁的字段尽量排后面
• 稳定的字段可以排前面
• 减少索引维护开销
```

**🔹 索引设计模式**
```
单表多索引模式：
• 针对不同查询模式建立不同索引
• 适用：查询模式差异较大的场景

覆盖索引模式：
• 索引包含查询所需的所有字段
• 适用：查询字段固定的场景

前缀索引模式：  
• 对长字段建立前缀索引
• 适用：字符串字段较长的场景
```

### 8.3 实际应用指导


**💡 设计步骤总结**
```
步骤1：收集查询模式
• 分析慢查询日志
• 统计查询频率
• 识别核心业务查询

步骤2：分析字段特征
• 计算字段选择性
• 评估字段相关性
• 考虑字段更新频率

步骤3：设计索引方案
• 应用最左前缀原则
• 优化字段顺序
• 考虑索引覆盖

步骤4：验证优化效果
• 执行计划分析
• 性能测试对比
• 监控索引使用情况

步骤5：持续优化维护
• 定期分析索引效果
• 根据查询变化调整索引
• 清理无用索引
```

### 8.4 常见误区警示


> ⚠️ **设计误区**
> - **误区1**：认为字段越多索引越好 → 实际上增加维护成本
> - **误区2**：不分析查询直接建索引 → 可能建立无用索引
> - **误区3**：忽略最左前缀原则 → 索引无法正常使用
> - **误区4**：不考虑字段更新频率 → 导致性能问题

> 💡 **设计原则**  
> - **需求驱动**：基于实际查询需求设计索引
> - **效果验证**：通过执行计划验证索引效果
> - **持续优化**：根据业务变化调整索引策略
> - **成本控制**：在性能提升和维护成本间找平衡

**核心记忆**：
- 复合索引字段顺序是关键，选择性高的排前面
- 最左前缀原则决定索引能否使用
- 索引覆盖可以显著提升查询性能
- 设计要基于实际查询模式，不能想当然