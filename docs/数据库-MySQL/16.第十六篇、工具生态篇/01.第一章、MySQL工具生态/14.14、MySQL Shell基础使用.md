---
title: 14、MySQL Shell基础使用
---
## 📚 目录

1. [MySQL Shell概述](#1-mysql-shell概述)
2. [安装与配置](#2-安装与配置)
3. [连接方式管理](#3-连接方式管理)
4. [交互模式详解](#4-交互模式详解)
5. [基本操作命令](#5-基本操作命令)
6. [会话与配置管理](#6-会话与配置管理)
7. [高级功能应用](#7-高级功能应用)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 MySQL Shell概述


### 1.1 什么是MySQL Shell


**通俗理解**：
MySQL Shell就像是MySQL的"超级遥控器"，比传统的mysql命令行工具更强大、更智能。

```
传统方式：           MySQL Shell方式：
mysql -u root -p    mysqlsh root@localhost
只能执行SQL         SQL + JavaScript + Python
功能单一            多模式切换，功能丰富
```

**🔸 核心特点**
- **多语言支持**：SQL、JavaScript、Python三种模式
- **现代化界面**：自动补全、语法高亮、格式化输出  
- **高级功能**：InnoDB Cluster管理、数据导入导出
- **API丰富**：AdminAPI管理集群，X DevAPI开发应用

### 1.2 为什么要用MySQL Shell


**解决的核心问题**：
```
问题1：传统命令行功能有限
解决：提供丰富的管理和开发API

问题2：集群管理复杂
解决：内置InnoDB Cluster管理功能

问题3：数据迁移繁琐  
解决：提供便捷的导入导出工具

问题4：开发体验差
解决：现代化的交互界面和自动补全
```

---

## 2. 🛠️ 安装与配置


### 2.1 Shell安装配置


**安装方式对比**

| 安装方式 | 优势 | 适用场景 | 命令示例 |
|---------|------|---------|----------|
| **包管理器** | 简单快捷 | 开发环境 | `apt install mysql-shell` |
| **官方包** | 版本稳定 | 生产环境 | 下载.deb/.rpm安装 |
| **源码编译** | 自定义配置 | 特殊需求 | `cmake + make` |

**🔧 Linux安装示例**
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install mysql-shell

# CentOS/RHEL  
sudo yum install mysql-shell

# 验证安装
mysqlsh --version
# 输出：mysqlsh Ver 8.0.35
```

### 2.2 环境变量设置


**核心配置说明**：
```bash
# 设置默认连接
export MYSQLSH_USER=root
export MYSQLSH_HOST=localhost  
export MYSQLSH_PORT=3306

# 设置Shell行为
export MYSQLSH_TERM_COLOR_MODE=rgb    # 彩色输出
export MYSQLSH_PROMPT_THEME=modern    # 现代主题
```

**配置文件位置**：
```
Linux/Mac：~/.mysqlsh/mysqlsh.conf
Windows：%APPDATA%\MySQL\mysqlsh\mysqlsh.conf
```

---

## 3. 🔌 连接方式管理


### 3.1 基本连接方法


**连接语法格式**：
```
mysqlsh [用户名[:密码]@]主机名[:端口][/数据库][?选项]
```

**🔸 常用连接示例**
```bash
# 基本连接（交互式输入密码）
mysqlsh root@localhost

# 指定端口和数据库
mysqlsh root@localhost:3307/testdb

# TCP连接（默认）
mysqlsh --uri mysql://root@localhost:3306

# SSL连接
mysqlsh --uri mysql://root@localhost:3306?ssl-mode=required

# Socket连接
mysqlsh --socket /tmp/mysql.sock
```

### 3.2 多实例连接管理


**场景应用**：当你有多个MySQL实例时，如何快速切换？

```bash
# 定义连接别名（在Shell内执行）
\connect --mc production mysql://root@prod-server:3306
\connect --mc development mysql://root@dev-server:3307  
\connect --mc testing mysql://root@test-server:3308

# 快速切换
\connect production    # 切换到生产环境
\connect development   # 切换到开发环境
```

**连接状态查看**：
```javascript
// 查看当前连接信息
session.getUri()        // 获取连接URI
session.isOpen()        // 检查连接状态
```

### 3.3 连接安全配置


**SSL连接模式**：

| 模式 | 说明 | 安全级别 | 使用场景 |
|------|------|---------|----------|
| `DISABLED` | 不使用SSL | 低 | 本地开发 |
| `PREFERRED` | 优先SSL | 中 | 混合环境 |
| `REQUIRED` | 强制SSL | 高 | 生产环境 |
| `VERIFY_CA` | 验证证书 | 很高 | 企业级 |

```bash
# 强制SSL连接
mysqlsh --ssl-mode=REQUIRED root@remote-server

# 证书验证连接
mysqlsh --ssl-mode=VERIFY_CA \
  --ssl-ca=/path/to/ca.pem \
  root@secure-server
```

---

## 4. 🔄 交互模式详解


### 4.1 三种工作模式


**模式切换示意图**：
```
MySQL Shell
├── SQL模式 (\sql)
│   └── 执行标准SQL语句
├── JavaScript模式 (\js)  
│   └── 使用X DevAPI和AdminAPI
└── Python模式 (\py)
    └── Python脚本和MySQL交互
```

**🔸 模式切换实践**
```bash
# 启动后默认JavaScript模式
MySQL localhost:3306 ssl JS>

# 切换到SQL模式
\sql
MySQL localhost:3306 ssl SQL> 

# 切换到Python模式
\py  
MySQL localhost:3306 ssl Py>

# 切换回JavaScript模式
\js
MySQL localhost:3306 ssl JS>
```

### 4.2 SQL模式应用


**使用特点**：就像传统mysql命令行，但界面更友好

```sql
-- 基本查询（有语法高亮）
SELECT user, host FROM mysql.user;

-- 查看执行计划（格式化输出）
EXPLAIN SELECT * FROM employees WHERE department='IT';

-- 多行语句支持
SELECT 
    employee_id,
    name,
    salary
FROM employees 
WHERE salary > 50000
ORDER BY salary DESC;
```

### 4.3 JavaScript模式应用


**X DevAPI使用**：现代化的数据库操作方式
```javascript
// 获取数据库session
var db = session.getSchema('testdb');

// 集合操作（类似MongoDB）
var users = db.getCollection('users');

// 添加文档
users.add({
    name: 'John Doe', 
    email: 'john@example.com',
    age: 30
});

// 查询文档  
users.find('age > 25').execute();
```

### 4.4 Python模式应用


```python
# 获取schema
db = session.get_schema('testdb')

# 表操作
employees = db.get_table('employees')

# 查询数据
result = employees.select(['name', 'salary']).where('department = "IT"').execute()

# 遍历结果
for row in result:
    print(f"Name: {row.name}, Salary: {row.salary}")
```

---

## 5. ⚡ 基本操作命令


### 5.1 Shell内置命令


**命令分类图**：
```
Shell命令
├── 连接管理
│   ├── \connect - 连接数据库
│   ├── \disconnect - 断开连接  
│   └── \status - 查看状态
├── 模式控制
│   ├── \sql - SQL模式
│   ├── \js - JavaScript模式
│   └── \py - Python模式
├── 输出控制
│   ├── \show - 显示设置
│   └── \format - 格式化选项
└── 系统操作
    ├── \system - 执行系统命令
    ├── \source - 执行脚本
    └── \quit - 退出Shell
```

**🔸 常用命令实践**
```bash
# 查看帮助
\help           # 显示所有命令
\help connect   # 显示特定命令帮助

# 连接管理
\status         # 显示连接状态
\reconnect      # 重新连接

# 输出格式控制
\show           # 显示当前设置
\format json    # 设置JSON输出格式
\format table   # 设置表格输出格式
```

### 5.2 输出格式配置


**格式对比展示**：

**表格格式**（默认）：
```
+----+----------+--------+
| id | name     | salary |
+----+----------+--------+
|  1 | John Doe |  50000 |
|  2 | Jane Lee |  60000 |
+----+----------+--------+
```

**JSON格式**：
```json
[
    {"id": 1, "name": "John Doe", "salary": 50000},
    {"id": 2, "name": "Jane Lee", "salary": 60000}
]
```

**格式设置命令**：
```bash
# 设置输出格式
\format table    # 表格格式（默认）
\format tabbed   # 制表符分隔
\format json     # JSON格式  
\format ndjson   # 换行分隔的JSON
\format csv      # CSV格式
```

---

## 6. 📁 会话与配置管理


### 6.1 会话管理


**会话状态图**：
```
会话生命周期
    ┌─────────────┐
    │  连接建立    │
    └──────┬──────┘
           │
    ┌──────▼──────┐
    │  会话活跃    │ ← 执行命令、查询数据
    └──────┬──────┘
           │
    ┌──────▼──────┐
    │  会话结束    │
    └─────────────┘
```

**会话操作实践**：
```javascript
// 查看会话信息
session.getUri()                    // 连接URI
session.getCurrentSchema()          // 当前数据库
session.getDefaultSchema()          // 默认数据库

// 会话配置
session.setCurrentSchema('mydb')    // 切换数据库
```

### 6.2 会话持久化


**配置保存位置**：
```
全局配置：~/.mysqlsh/mysqlsh.conf
会话历史：~/.mysqlsh/history
连接缓存：~/.mysqlsh/connections.json
```

**持久化设置**：
```bash
# 设置持久化选项（重启后保持）
\option --persist defaultMode=sql
\option --persist outputFormat=json
\option --persist history.autoSave=true
```

### 6.3 配置文件管理


**配置文件结构**：
```json
{
    "defaultMode": "sql",
    "outputFormat": "table",
    "history": {
        "autoSave": true,
        "maxSize": 1000
    },
    "autocomplete": {
        "enabled": true,
        "nameCache": true
    }
}
```

**常用配置项**：
```bash
# 查看所有配置
\option

# 修改特定配置
\option outputFormat json
\option autocomplete.enabled false
\option history.maxSize 2000
```

---

## 7. 🚀 高级功能应用


### 7.1 批处理模式


**批处理应用场景**：
- 自动化脚本执行
- 定时任务处理  
- 大量数据操作

```bash
# 执行SQL文件
mysqlsh --sql --file=/path/to/script.sql root@localhost

# 执行JavaScript脚本
mysqlsh --js --file=/path/to/script.js root@localhost

# 批量执行（无交互）
mysqlsh --batch-continue-on-error \
  --file=batch_operations.sql \
  root@localhost
```

**批处理脚本示例**：
```sql
-- batch_operations.sql
USE testdb;
SELECT 'Starting batch operations' AS status;

CREATE TABLE IF NOT EXISTS temp_data (
    id INT PRIMARY KEY,
    data VARCHAR(100)
);

INSERT INTO temp_data VALUES (1, 'Batch data 1');
INSERT INTO temp_data VALUES (2, 'Batch data 2');

SELECT 'Batch operations completed' AS status;
```

### 7.2 Shell主题定制


**主题配置选项**：
```
可用主题：
├── default - 默认主题
├── modern  - 现代主题  
├── classic - 经典主题
└── minimal - 简洁主题
```

```bash
# 设置主题
\option --persist promptTheme modern

# 自定义提示符颜色
export MYSQLSH_PROMPT_THEME=modern
export MYSQLSH_TERM_COLOR_MODE=rgb
```

### 7.3 命令历史管理


**历史记录功能**：
```bash
# 查看历史命令  
\history

# 搜索历史（Ctrl+R）
# 清空历史
\history clear

# 保存历史到文件
\history save /path/to/backup.txt
```

**历史配置**：
```javascript
// JavaScript模式设置
shell.options.set('history.autoSave', true);
shell.options.set('history.maxSize', 5000);
```

### 7.4 Shell性能调优


**性能优化配置**：

| 配置项 | 作用 | 推荐值 | 说明 |
|--------|------|--------|------|
| `nameCache` | 名称缓存 | `true` | 加速自动补全 |
| `queryTimeout` | 查询超时 | `600` | 10分钟超时 |
| `connectTimeout` | 连接超时 | `10` | 10秒连接超时 |
| `maxBinaryLength` | 二进制长度 | `1024` | 限制显示长度 |

```bash
# 性能优化设置
\option autocomplete.nameCache true
\option connectTimeout 10  
\option queryTimeout 600
\option maxBinaryLength 1024
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 MySQL Shell：现代化的MySQL管理工具
🔸 三种模式：SQL、JavaScript、Python可灵活切换  
🔸 连接管理：支持多种连接方式和安全配置
🔸 会话持久化：配置和历史可保存重用
🔸 批处理：支持自动化脚本执行
```

### 8.2 关键操作记忆


**🔹 连接切换口诀**
```
连接用connect，状态用status
模式用反斜杠，sql js py
格式用format，选项用option
```

**🔹 常用命令速查**
```bash
# 基础操作
\connect user@host    # 连接数据库
\status              # 查看状态  
\sql \js \py         # 切换模式
\format table        # 设置格式

# 配置管理  
\option              # 查看配置
\option key value    # 设置配置
\help command        # 获取帮助
```

### 8.3 实际应用价值


**💼 日常开发**
- 替代传统mysql命令行，体验更好
- 多种编程语言支持，开发更灵活
- 自动补全和语法高亮提升效率

**🏢 生产运维**  
- 批处理模式支持自动化脚本
- 丰富的连接选项适应各种环境
- 会话管理便于多环境操作

**🔧 集群管理**
- 内置AdminAPI简化集群操作
- 支持InnoDB Cluster管理
- 提供数据导入导出工具

**核心记忆**：
- MySQL Shell是MySQL的现代化管理界面
- 三模式切换让操作更灵活高效  
- 丰富的配置选项支持个性化定制
- 批处理和API支持自动化运维