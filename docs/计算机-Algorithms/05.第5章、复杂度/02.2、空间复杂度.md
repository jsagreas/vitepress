---
title: 2、空间复杂度
---
## 📚 目录

1. [空间复杂度基本概念](#1-空间复杂度基本概念)
2. [空间复杂度分析方法](#2-空间复杂度分析方法)
3. [常见空间复杂度类型](#3-常见空间复杂度类型)
4. [递归与栈空间](#4-递归与栈空间)
5. [数据结构空间开销](#5-数据结构空间开销)
6. [时间与空间权衡](#6-时间与空间权衡)
7. [空间优化技巧](#7-空间优化技巧)
8. [实战应用总结](#8-实战应用总结)

---

## 1. 🧠 空间复杂度基本概念


### 1.1 什么是空间复杂度

空间复杂度（Space Complexity）衡量算法在执行过程中所需要的**额外内存空间**随输入规模增长的趋势。

### 1.2 核心要点

```
🔸 只计算额外空间: 不包括输入数据本身占用的空间
🔸 关注增长趋势: 使用大O表示法描述
🔸 包含所有内存: 栈空间、堆空间、临时变量等
🔸 与时间复杂度独立: 可以分别分析和优化
```

### 1.3 空间组成分析

```
总内存使用 = 输入空间 + 辅助空间 + 输出空间

输入空间: 存储输入数据 (通常不计入分析)
辅助空间: 算法执行时的额外空间 (分析重点)
输出空间: 存储结果数据 (根据问题决定是否计入)
```

### 1.4 基础示例

```java
// O(1) 空间复杂度
public int sum(int[] arr) {
    int total = 0;  // 固定的额外变量
    for (int num : arr) {
        total += num;
    }
    return total;
    // 只使用常数个额外变量，空间复杂度 O(1)
}

// O(n) 空间复杂度
public int[] reverse(int[] arr) {
    int n = arr.length;
    int[] result = new int[n];  // 与输入同等大小的额外数组
    for (int i = 0; i < n; i++) {
        result[i] = arr[n - 1 - i];
    }
    return result;
    // 额外数组大小与输入成正比，空间复杂度 O(n)
}
```

---

## 2. 🔍 空间复杂度分析方法


### 2.1 基本分析步骤

```
步骤1: 识别所有额外内存分配
- 局部变量
- 临时数组/对象
- 递归调用栈
- 数据结构开销

步骤2: 计算各部分空间大小
- 与输入规模的关系
- 最大同时使用量

步骤3: 确定主导项
- 忽略常数因子
- 关注最高阶项
```

### 2.2 不同情况的分析

```java
// 情况1: 局部变量
public void example1(int n) {
    int a = 0, b = 0;  // O(1)
    // 空间复杂度: O(1)
}

// 情况2: 临时数组
public void example2(int n) {
    int[] temp = new int[n];  // O(n)
    // 空间复杂度: O(n)
}

// 情况3: 嵌套结构
public void example3(int n) {
    int[][] matrix = new int[n][n];  // O(n²)
    // 空间复杂度: O(n²)
}

// 情况4: 多个数组
public void example4(int n) {
    int[] arr1 = new int[n];     // O(n)
    int[] arr2 = new int[n];     // O(n)
    // 同时存在，空间复杂度: O(n) + O(n) = O(n)
}
```

### 2.3 递归调用栈分析

```java
// 线性递归
public int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
    // 递归深度: n，每层O(1)空间，总计O(n)
}

// 二分递归
public void mergeSort(int[] arr, int left, int right) {
    if (left < right) {
        int mid = (left + right) / 2;
        mergeSort(arr, left, mid);        // 递归深度log(n)
        mergeSort(arr, mid + 1, right);   // 同上
        merge(arr, left, mid, right);     // O(n)临时空间
    }
    // 栈空间: O(log n), 临时空间: O(n), 总计: O(n)
}
```

---

## 3. 📊 常见空间复杂度类型


### 3.1 常数空间 O(1)

```
特点: 使用固定数量的额外变量
应用: 原地算法、简单计算
```

```java
// 原地排序 - 冒泡排序
public void bubbleSort(int[] arr) {
    int n = arr.length;
    for (int i = 0; i < n - 1; i++) {
        for (int j = 0; j < n - 1 - i; j++) {
            if (arr[j] > arr[j + 1]) {
                // 原地交换，只用常数额外空间
                int temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
    }
    // 空间复杂度: O(1)
}

// 数学运算
public int gcd(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
    // 空间复杂度: O(1)
}
```

### 3.2 线性空间 O(n)

```
特点: 额外空间与输入规模成正比
应用: 复制数组、哈希表、动态规划
```

```java
// 归并排序
public void mergeSort(int[] arr) {
    if (arr.length <= 1) return;
    
    int mid = arr.length / 2;
    int[] left = Arrays.copyOfRange(arr, 0, mid);     // O(n)
    int[] right = Arrays.copyOfRange(arr, mid, arr.length);  // O(n)
    
    mergeSort(left);
    mergeSort(right);
    merge(arr, left, right);
    // 临时数组空间: O(n)
}

// 哈希表去重
public List<Integer> removeDuplicates(int[] arr) {
    Set<Integer> seen = new HashSet<>();        // O(n)最坏情况
    List<Integer> result = new ArrayList<>();   // O(n)最坏情况
    
    for (int num : arr) {
        if (!seen.contains(num)) {
            seen.add(num);
            result.add(num);
        }
    }
    return result;
    // 空间复杂度: O(n)
}
```

### 3.3 平方空间 O(n²)

```
特点: 二维结构、图的邻接矩阵
应用: 动态规划、图算法
```

```java
// 动态规划 - 最长公共子序列
public int longestCommonSubsequence(String text1, String text2) {
    int m = text1.length(), n = text2.length();
    int[][] dp = new int[m + 1][n + 1];  // O(m×n)空间
    
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (text1.charAt(i - 1) == text2.charAt(j - 1)) {
                dp[i][j] = dp[i - 1][j - 1] + 1;
            } else {
                dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
            }
        }
    }
    return dp[m][n];
    // 空间复杂度: O(m×n)
}

// 图的邻接矩阵
public class Graph {
    private boolean[][] adjMatrix;  // O(V²)空间
    
    public Graph(int vertices) {
        adjMatrix = new boolean[vertices][vertices];
    }
    // 空间复杂度: O(V²)
}
```

### 3.4 指数空间 O(2ⁿ)

```
特点: 存储所有可能的状态或组合
应用: 生成所有子集、排列
```

```java
// 生成所有子集
public List<List<Integer>> subsets(int[] nums) {
    List<List<Integer>> result = new ArrayList<>();
    generateSubsets(nums, 0, new ArrayList<>(), result);
    return result;
    // 2^n个子集，每个平均长度n/2，总空间O(n×2^n)
}

private void generateSubsets(int[] nums, int index, 
                           List<Integer> current, 
                           List<List<Integer>> result) {
    if (index == nums.length) {
        result.add(new ArrayList<>(current));
        return;
    }
    
    // 不包含当前元素
    generateSubsets(nums, index + 1, current, result);
    
    // 包含当前元素
    current.add(nums[index]);
    generateSubsets(nums, index + 1, current, result);
    current.remove(current.size() - 1);
}
```

---

## 4. 🔄 递归与栈空间


### 4.1 递归栈空间基础

```
递归调用栈的组成:
- 函数参数
- 局部变量  
- 返回地址
- 栈帧元数据

计算公式:
总栈空间 = 栈帧大小 × 最大递归深度
```

### 4.2 不同递归模式的空间分析

```java
// 线性递归
public int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
    // 递归深度: n, 空间: O(n)
}

// 二分递归 (分治)
public int binarySearch(int[] arr, int target, int left, int right) {
    if (left > right) return -1;
    int mid = left + (right - left) / 2;
    if (arr[mid] == target) return mid;
    else if (arr[mid] < target) 
        return binarySearch(arr, target, mid + 1, right);
    else 
        return binarySearch(arr, target, left, mid - 1);
    // 递归深度: log(n), 空间: O(log n)
}

// 多分支递归
public int fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
    // 最大递归深度: n (沿左分支), 空间: O(n)
    // 注意: 时间复杂度O(2^n)，但空间复杂度只有O(n)
}
```

### 4.3 尾递归与优化

```java
// 非尾递归
public int factorialNormal(int n) {
    if (n <= 1) return 1;
    return n * factorialNormal(n - 1);  // 递归后还要做乘法
    // 空间: O(n)
}

// 尾递归
public int factorialTail(int n) {
    return factorialHelper(n, 1);
}

private int factorialHelper(int n, int acc) {
    if (n <= 1) return acc;
    return factorialHelper(n - 1, n * acc);  // 尾递归
    // Java不支持尾递归优化，仍然O(n)
    // 支持优化的语言可以优化为O(1)
}

// 迭代替代 (手动优化)
public int factorialIterative(int n) {
    int result = 1;
    for (int i = 2; i <= n; i++) {
        result *= i;
    }
    return result;
    // 空间: O(1)
}
```

---

## 5. 🏗️ 数据结构空间开销


### 5.1 线性结构对比

```
数组 (Array):
- 内存连续，缓存友好
- 空间: n × 元素大小
- 无额外指针开销

动态数组 (ArrayList):
- 预分配空间，扩容时浪费
- 空间利用率: ~67% (1.5倍扩容)
- 额外开销: 对象头 + 容量信息

链表 (LinkedList):
- 每个节点额外指针开销
- 空间: n × (元素大小 + 指针大小)
- 内存不连续，缓存不友好
```

```java
// 空间效率对比示例
public void spaceComparison() {
    int n = 1000;
    
    // 原始数组: 1000 × 4 = 4000字节
    int[] array = new int[n];
    
    // ArrayList: ~4000 × 1.5 + 对象开销 ≈ 6024字节
    List<Integer> arrayList = new ArrayList<>();
    for (int i = 0; i < n; i++) {
        arrayList.add(i);  // 还要算Integer对象开销
    }
    
    // LinkedList: 1000 × (节点40字节 + Integer16字节) = 56000字节
    List<Integer> linkedList = new LinkedList<>();
    for (int i = 0; i < n; i++) {
        linkedList.add(i);
    }
}
```

### 5.2 树形结构空间分析

```java
// 二叉搜索树
class TreeNode {
    int val;                    // 4字节
    TreeNode left, right;       // 16字节(两个引用)
    // 对象头: 16字节
    // 总计: 36字节每个节点
}
// n个节点的BST: 36n字节，空间复杂度O(n)

// 平衡树 (如AVL)
class AVLNode {
    int val;                    // 4字节
    int height;                 // 4字节 (额外的平衡信息)
    AVLNode left, right;        // 16字节
    // 对象头: 16字节
    // 总计: 40字节每个节点
}
// 额外空间换取平衡保证

// 字典树 (Trie)
class TrieNode {
    boolean isEnd;              // 1字节
    TrieNode[] children = new TrieNode[26];  // 26×8 = 208字节
    // 对象头: 16字节
    // 总计: 225字节每个节点
}
// 大量空指针，空间效率低
```

### 5.3 哈希表空间效率

```java
// HashMap核心参数
public void hashMapSpace() {
    /*
    HashMap空间组成:
    1. 桶数组: capacity × 8字节 (引用数组)
    2. Entry节点: size × 44字节 (节点对象)
    3. 键值对象: 取决于具体类型
    
    负载因子影响:
    - 默认负载因子: 0.75
    - 实际容量: size / 0.75
    - 空间利用率: 75%
    
    扩容开销:
    - 扩容时需要2倍空间
    - 重新哈希所有元素
    */
    
    Map<String, Integer> map = new HashMap<>();
    // 每个Entry约100字节 (包含String和Integer对象)
    // 桶数组开销: (n/0.75) × 8字节
    // 总空间复杂度: O(n)
}
```

---

## 6. ⚖️ 时间与空间权衡


### 6.1 经典权衡场景

```
🔸 空间换时间:
- 缓存/记忆化: 存储计算结果避免重复计算
- 预处理: 预计算结果快速查询
- 哈希表: 用额外空间实现O(1)查找

🔸 时间换空间:
- 重新计算: 不存储中间结果，需要时重算
- 压缩算法: 减少存储但增加计算时间
- 原地算法: 避免额外空间但可能增加时间
```

### 6.2 具体示例对比

```java
// 斐波那契数列的不同实现

// 1. 朴素递归: 时间O(2^n), 空间O(n)
public int fibRecursive(int n) {
    if (n <= 1) return n;
    return fibRecursive(n - 1) + fibRecursive(n - 2);
}

// 2. 记忆化递归: 时间O(n), 空间O(n)
private Map<Integer, Integer> memo = new HashMap<>();
public int fibMemo(int n) {
    if (n <= 1) return n;
    if (memo.containsKey(n)) return memo.get(n);
    
    int result = fibMemo(n - 1) + fibMemo(n - 2);
    memo.put(n, result);
    return result;
}

// 3. 动态规划: 时间O(n), 空间O(n)
public int fibDP(int n) {
    if (n <= 1) return n;
    int[] dp = new int[n + 1];
    dp[1] = 1;
    for (int i = 2; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];
    }
    return dp[n];
}

// 4. 空间优化: 时间O(n), 空间O(1)
public int fibOptimized(int n) {
    if (n <= 1) return n;
    int prev = 0, curr = 1;
    for (int i = 2; i <= n; i++) {
        int next = prev + curr;
        prev = curr;
        curr = next;
    }
    return curr;
}
```

### 6.3 动态规划的空间优化

```java
// 二维DP优化为一维
// 原始版本: O(mn)空间
public int uniquePaths2D(int m, int n) {
    int[][] dp = new int[m][n];
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            if (i == 0 || j == 0) {
                dp[i][j] = 1;
            } else {
                dp[i][j] = dp[i-1][j] + dp[i][j-1];
            }
        }
    }
    return dp[m-1][n-1];
}

// 优化版本: O(n)空间
public int uniquePaths1D(int m, int n) {
    int[] dp = new int[n];
    Arrays.fill(dp, 1);
    
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            dp[j] += dp[j-1];  // 只需要当前行和上一行
        }
    }
    return dp[n-1];
}
```

---

## 7. 🚀 空间优化技巧


### 7.1 原地算法技巧

```java
// 原地反转数组
public void reverseArray(int[] arr) {
    int left = 0, right = arr.length - 1;
    while (left < right) {
        int temp = arr[left];
        arr[left] = arr[right];
        arr[right] = temp;
        left++;
        right--;
    }
    // 空间: O(1)
}

// 原地移除元素
public int removeElement(int[] nums, int val) {
    int writeIndex = 0;
    for (int readIndex = 0; readIndex < nums.length; readIndex++) {
        if (nums[readIndex] != val) {
            nums[writeIndex++] = nums[readIndex];
        }
    }
    return writeIndex;
    // 空间: O(1)
}
```

### 7.2 滚动数组技巧

```java
// 动态规划空间优化
public int minPathSum(int[][] grid) {
    int m = grid.length, n = grid[0].length;
    
    // 只需要上一行的信息
    int[] dp = new int[n];
    dp[0] = grid[0][0];
    
    // 初始化第一行
    for (int j = 1; j < n; j++) {
        dp[j] = dp[j-1] + grid[0][j];
    }
    
    // 逐行更新
    for (int i = 1; i < m; i++) {
        dp[0] += grid[i][0];  // 第一列
        for (int j = 1; j < n; j++) {
            dp[j] = Math.min(dp[j], dp[j-1]) + grid[i][j];
        }
    }
    
    return dp[n-1];
    // 从O(mn)优化为O(n)
}
```

### 7.3 位操作优化

```java
// 用位标记代替布尔数组
public List<Integer> findDuplicates(int[] nums) {
    List<Integer> result = new ArrayList<>();
    
    for (int num : nums) {
        int index = Math.abs(num) - 1;
        if (nums[index] < 0) {
            result.add(Math.abs(num));  // 已经标记过，是重复元素
        } else {
            nums[index] = -nums[index];  // 标记为负数
        }
    }
    
    return result;
    // 利用原数组空间，额外空间O(1)
}

// 位图优化
public class BitSet {
    private int[] bits;
    
    public BitSet(int size) {
        bits = new int[(size + 31) / 32];  // 每个int存32位
    }
    
    public void set(int index) {
        int intIndex = index / 32;
        int bitIndex = index % 32;
        bits[intIndex] |= (1 << bitIndex);
    }
    
    // 比boolean数组节省32倍空间
}
```

### 7.4 数据结构选择优化

```java
// 根据使用模式选择合适的数据结构
public class SpaceOptimizedCollections {
    
    // 稀疏数组优化
    public class SparseArray {
        private Map<Integer, Integer> data = new HashMap<>();
        private int size;
        
        public void set(int index, int value) {
            if (value == 0) {
                data.remove(index);  // 0值不存储
            } else {
                data.put(index, value);
            }
        }
        // 只存储非零元素，节省大量空间
    }
    
    // 字符串优化
    public void stringOptimization() {
        // 避免字符串拼接创建大量临时对象
        StringBuilder sb = new StringBuilder();  // 预分配容量
        for (int i = 0; i < 1000; i++) {
            sb.append(i).append(",");
        }
        String result = sb.toString();
        // 避免O(n²)空间的字符串拼接
    }
}
```

---

## 8. 📋 实战应用总结


### 8.1 空间复杂度分析检查清单

```
✅ 分析要点:
1. 识别所有额外内存分配
2. 计算递归调用栈深度
3. 考虑数据结构的内在开销
4. 分析最坏情况的空间使用
5. 区分输入、辅助、输出空间

✅ 优化策略:
1. 使用原地算法避免额外空间
2. 滚动数组优化多维DP
3. 选择空间效率高的数据结构
4. 考虑时间空间权衡
5. 利用位操作节省空间
```

### 8.2 常见应用场景总结

```
🔸 系统设计场景:
- 内存受限的嵌入式系统: 优先O(1)空间算法
- 大数据处理: 流式处理避免O(n)空间
- 缓存系统: 合理的空间时间权衡
- 数据库索引: 考虑存储开销

🔸 算法竞赛场景:
- 空间限制题目: 必须使用原地算法
- DP优化: 降维减少空间复杂度
- 图算法: 选择邻接表vs邻接矩阵
- 字符串处理: 避免不必要的字符串复制
```

### 8.3 性能权衡指导

```java
// 决策框架
public class SpaceTimeTradeoff {
    
    /*
    选择标准:
    
    1. 内存充足，追求速度:
       - 使用哈希表、缓存
       - 预处理数据
       - 空间换时间
    
    2. 内存受限，可接受较慢速度:
       - 原地算法
       - 重新计算而非存储
       - 时间换空间
    
    3. 平衡考虑:
       - 分析访问频率
       - 考虑缓存效果
       - 评估实际性能影响
    */
    
    // 示例: 根据数据规模选择策略
    public void adaptiveStrategy(int[] data) {
        if (data.length < 1000) {
            // 小数据量: 简单算法，重点考虑代码简洁性
            bubbleSort(data);
        } else if (availableMemory() > data.length * 4) {
            // 内存充足: 使用额外空间换取效率
            mergeSort(data);
        } else {
            // 内存受限: 使用原地算法
            heapSort(data);
        }
    }
}
```

### 8.4 核心原则总结

```
🔸 分析原则:
- 只计算额外空间，不含输入
- 考虑递归栈空间
- 关注最坏情况
- 使用大O表示法

🔸 优化原则:
- 优先考虑原地算法
- 合理利用输入空间
- 选择合适的数据结构
- 平衡时间和空间需求

🔸 实践原则:
- 根据实际约束选择算法
- 测试验证理论分析
- 考虑系统整体性能
- 保持代码可读性和可维护性
```