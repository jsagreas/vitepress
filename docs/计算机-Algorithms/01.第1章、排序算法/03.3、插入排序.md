---
title: 3、插入排序
---
## 📚 目录

1. [算法概述](#1-算法概述)
2. [基本思想](#2-基本思想)
3. [算法原理](#3-算法原理)
4. [实现方法](#4-实现方法)
5. [算法分析](#5-算法分析)
6. [优化变种](#6-优化变种)
7. [应用场景](#7-应用场景)
8. [对比分析](#8-对比分析)
9. [实际应用](#9-实际应用)
10. [总结](#10-总结)

---

## 1. 📖 算法概述


### 1.1 定义

插入排序是一种简单直观的排序算法，它的工作原理是通过构建有序序列，对于未排序数据，在已排序序列中从后向前扫描，找到相应位置并插入。

### 1.2 核心特征

- **🔹 稳定性**：稳定排序算法
- **🔹 原地排序**：只需要 O(1) 的额外内存空间
- **🔹 自适应性**：对于基本有序的数组效率很高
- **🔹 在线算法**：可以在接收数据的同时进行排序

### 1.3 算法分类

- 比较排序算法
- 内部排序算法
- 增量式排序算法

---

## 2. 💡 基本思想


### 2.1 核心思想

插入排序的基本思想类似于整理扑克牌：
1. 从第一张牌开始，这张牌可以认为已经被排序
2. 取出下一张牌，在已经排序的牌序列中从后向前扫描
3. 如果该牌小于前一张牌，则交换位置
4. 重复步骤3，直到找到适合的位置
5. 将新牌插入到该位置
6. 重复步骤2-5，直到所有牌都被排序

### 2.2 排序过程示例

以数组 [5, 2, 4, 6, 1, 3] 为例：

```
初始:     [5, 2, 4, 6, 1, 3]
第1轮:    [2, 5, 4, 6, 1, 3]  (将2插入到5前面)
第2轮:    [2, 4, 5, 6, 1, 3]  (将4插入到2和5之间)
第3轮:    [2, 4, 5, 6, 1, 3]  (6已经在正确位置)
第4轮:    [1, 2, 4, 5, 6, 3]  (将1插入到最前面)
第5轮:    [1, 2, 3, 4, 5, 6]  (将3插入到2和4之间)
```

---

## 3. ⚙️ 算法原理


### 3.1 工作机制

1. **分割数组**：将数组分为已排序和未排序两部分
2. **逐个处理**：从未排序部分取出第一个元素
3. **寻找位置**：在已排序部分找到合适的插入位置
4. **元素移动**：将元素移动到正确位置
5. **重复过程**：直到所有元素都被处理

### 3.2 关键步骤

- **🔸 初始化**：假设第一个元素已排序
- **🔸 外层循环**：遍历未排序的元素
- **🔸 内层循环**：在已排序部分寻找插入位置
- **🔸 元素插入**：将当前元素插入到正确位置

### 3.3 不变式

- 数组的前 i 个元素始终是排序的
- 在第 i 次迭代开始时，数组的前 i-1 个元素已经排序完成

---

## 4. 🔨 实现方法


### 4.1 基础实现（Python）


```python
def insertion_sort(arr):
    """
    插入排序的基本实现
    时间复杂度: O(n²)
    空间复杂度: O(1)
    """
    n = len(arr)
    
    # 从第二个元素开始，因为第一个元素可以认为已经排序
    for i in range(1, n):
        key = arr[i]  # 当前要插入的元素
        j = i - 1     # 已排序部分的最后一个元素索引
        
        # 向前搜索，寻找插入位置
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]  # 元素后移
            j -= 1
        
        # 插入元素
        arr[j + 1] = key
    
    return arr
```

### 4.2 Java 实现


```java
public class InsertionSort {
    public static void insertionSort(int[] arr) {
        int n = arr.length;
        
        for (int i = 1; i < n; i++) {
            int key = arr[i];
            int j = i - 1;
            
            // 移动元素，为key腾出空间
            while (j >= 0 && arr[j] > key) {
                arr[j + 1] = arr[j];
                j--;
            }
            
            arr[j + 1] = key;
        }
    }
}
```

### 4.3 C++ 实现


```cpp
#include <vector>
using namespace std;

void insertionSort(vector<int>& arr) {
    int n = arr.size();
    
    for (int i = 1; i < n; i++) {
        int key = arr[i];
        int j = i - 1;
        
        while (j >= 0 && arr[j] > key) {
            arr[j + 1] = arr[j];
            j--;
        }
        
        arr[j + 1] = key;
    }
}
```

### 4.4 递归实现


```python
def insertion_sort_recursive(arr, n):
    """
    插入排序的递归实现
    """
    # 基准情况
    if n <= 1:
        return
    
    # 递归排序前n-1个元素
    insertion_sort_recursive(arr, n - 1)
    
    # 将第n个元素插入到正确位置
    last = arr[n - 1]
    j = n - 2
    
    while j >= 0 and arr[j] > last:
        arr[j + 1] = arr[j]
        j -= 1
    
    arr[j + 1] = last
```

---

## 5. 📊 算法分析


### 5.1 时间复杂度


##### 5.1.1 最好情况

- **条件**：数组已经有序
- **复杂度**：`O(n)` <span style="color:#22c55e">✅ 最优</span>
- **原因**：每个元素只需要与前一个元素比较一次

##### 5.1.2 最坏情况

- **条件**：数组逆序排列
- **复杂度**：`O(n²)` <span style="color:#ef4444">❌ 较差</span>
- **原因**：每个元素都需要与前面所有元素比较

##### 5.1.3 平均情况

- **复杂度**：`O(n²)` <span style="color:#f59e0b">⚠️ 一般</span>
- **分析**：平均每个元素需要与前面一半的元素比较

### 5.2 空间复杂度

- **复杂度**：`O(1)` <span style="color:#22c55e">✅ 优秀</span>
- **原因**：只使用了常数个额外变量

### 5.3 稳定性分析

- **稳定排序**：相等元素的相对位置不会改变
- **原因**：只有当 arr[j] > key 时才进行交换

### 5.4 自适应性

- **高自适应性**：对于基本有序的数组效率很高
- **性能提升**：当数组接近有序时，时间复杂度趋近于 O(n)

---

## 6. 🚀 优化变种


### 6.1 二分插入排序


```python
def binary_insertion_sort(arr):
    """
    使用二分查找优化的插入排序
    查找时间复杂度: O(log n)
    总时间复杂度: O(n²) (移动元素仍需O(n))
    """
    for i in range(1, len(arr)):
        key = arr[i]
        # 二分查找插入位置
        left, right = 0, i
        
        while left < right:
            mid = (left + right) // 2
            if arr[mid] > key:
                right = mid
            else:
                left = mid + 1
        
        # 移动元素
        for j in range(i, left, -1):
            arr[j] = arr[j - 1]
        
        arr[left] = key
    
    return arr
```

### 6.2 链表插入排序


```python
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

def insertion_sort_list(head):
    """
    链表的插入排序
    时间复杂度: O(n²)
    空间复杂度: O(1)
    """
    dummy = ListNode(0)
    current = head
    
    while current:
        # 保存下一个节点
        next_node = current.next
        
        # 在已排序部分寻找插入位置
        prev = dummy
        while prev.next and prev.next.val < current.val:
            prev = prev.next
        
        # 插入节点
        current.next = prev.next
        prev.next = current
        
        # 移动到下一个节点
        current = next_node
    
    return dummy.next
```

### 6.3 希尔排序（Shell Sort）

希尔排序是插入排序的改进版本，通过设置增量序列来减少数据移动。

```python
def shell_sort(arr):
    """
    希尔排序 - 插入排序的改进版
    """
    n = len(arr)
    gap = n // 2
    
    while gap > 0:
        for i in range(gap, n):
            key = arr[i]
            j = i
            
            while j >= gap and arr[j - gap] > key:
                arr[j] = arr[j - gap]
                j -= gap
            
            arr[j] = key
        
        gap //= 2
    
    return arr
```

---

## 7. 🎯 应用场景


### 7.1 适用情况

- **🔸 小规模数据**：数据量较小时（通常 < 50）
- **🔸 基本有序**：数据已经基本有序
- **🔸 在线排序**：数据逐个到达需要实时排序
- **🔸 稳定排序需求**：需要保持相等元素的相对位置

### 7.2 实际应用

- **TimSort**：Python 和 Java 的内置排序算法中用作小数组排序
- **快速排序优化**：当分区大小小于阈值时切换到插入排序
- **归并排序优化**：小数组使用插入排序提高效率
- **数据库索引**：B+ 树节点内部的排序

### 7.3 混合排序算法

```python
def hybrid_sort(arr, threshold=10):
    """
    混合排序：大数组用快速排序，小数组用插入排序
    """
    if len(arr) <= threshold:
        return insertion_sort(arr)
    else:
        return quicksort(arr)
```

---

## 8. ⚖️ 对比分析


### 8.1 与其他O(n²)排序算法对比


| 算法 | 时间复杂度 | 空间复杂度 | 稳定性 | 自适应性 |
|------|------------|------------|---------|----------|
| 插入排序 | O(n²) | O(1) | <span style="color:#22c55e">✅ 稳定</span> | <span style="color:#22c55e">✅ 高</span> |
| 选择排序 | O(n²) | O(1) | <span style="color:#ef4444">❌ 不稳定</span> | <span style="color:#ef4444">❌ 低</span> |
| 冒泡排序 | O(n²) | O(1) | <span style="color:#22c55e">✅ 稳定</span> | <span style="color:#22c55e">✅ 高</span> |

### 8.2 性能特点

- **插入排序 vs 选择排序**：插入排序在最好情况下为O(n)，选择排序始终为O(n²)
- **插入排序 vs 冒泡排序**：插入排序的常数因子更小，实际性能更好
- **插入排序 vs 快速排序**：小数组时插入排序性能更好

### 8.3 实际测试结果

```python
import time
import random

def performance_test():
    sizes = [10, 100, 1000, 5000]
    
    for size in sizes:
        # 随机数组
        arr = [random.randint(1, 1000) for _ in range(size)]
        
        start = time.time()
        insertion_sort(arr.copy())
        end = time.time()
        
        print(f"Size: {size}, Time: {end - start:.6f}s")
```

---

## 9. 🛠️ 实际应用


### 9.1 库函数中的应用

- **Python**: Timsort 中用于小数组排序
- **Java**: Arrays.sort() 中用于小数组排序
- **C++**: std::sort() 的 introsort 实现中用于小数组

### 9.2 数据结构中的应用

- **优先队列**：维护小顶堆时的局部排序
- **B树/B+树**：节点内部元素的排序
- **跳表**：层级结构的维护

### 9.3 特定场景应用

```python
def sort_playing_cards(cards):
    """
    扑克牌排序 - 插入排序的经典应用
    """
    def card_value(card):
        values = {'A': 1, 'J': 11, 'Q': 12, 'K': 13}
        return values.get(card, int(card))
    
    for i in range(1, len(cards)):
        key = cards[i]
        j = i - 1
        
        while j >= 0 and card_value(cards[j]) > card_value(key):
            cards[j + 1] = cards[j]
            j -= 1
        
        cards[j + 1] = key
    
    return cards
```

---

## 10. 📋 总结


### 10.1 核心优势

1. **🔸 简单直观**：算法逻辑简单，易于理解和实现
2. **🔸 原地排序**：只需要常数级额外空间
3. **🔸 稳定排序**：保持相等元素的相对位置
4. **🔸 自适应性强**：对于基本有序的数组效率很高
5. **🔸 在线算法**：可以边接收数据边排序

### 10.2 主要劣势

1. **🔸 时间复杂度高**：平均和最坏情况都是O(n²)
2. **🔸 大数据集性能差**：不适合大规模数据排序
3. **🔸 元素移动频繁**：需要大量的数据移动操作

### 10.3 使用建议

- **✅ 适用场景**：小数组、基本有序数据、需要稳定排序
- **❌ 避免场景**：大规模数据、完全随机数据
- **🔧 优化策略**：结合其他排序算法使用（如混合排序）


