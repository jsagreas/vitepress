---
title: 8、计数排序
---
## 📚 目录

1. [算法概述](#1-算法概述)
2. [基本思想](#2-基本思想)
3. [算法原理](#3-算法原理)
4. [实现方法](#4-实现方法)
5. [算法分析](#5-算法分析)
6. [应用场景](#6-应用场景)
7. [对比分析](#7-对比分析)
8. [总结](#8-总结)

---

## 1. 📖 算法概述


### 1.1 定义

计数排序（Counting Sort）是一种非比较型的线性时间排序算法。它通过统计数组中每个不同元素的出现次数，然后根据统计信息将元素按序放入结果数组中，从而实现排序。

### 1.2 历史背景

- **理论基础**：基于键值统计的思想
- **意义**：突破了比较排序算法O(n log n)的时间复杂度下界
- **地位**：线性时间排序算法的代表之一

### 1.3 核心特征

- **🔹 非比较排序**：不通过比较元素大小来排序
- **🔹 稳定排序**：相等元素的相对位置保持不变
- **🔹 线性时间**：时间复杂度为O(n+k)
- **🔹 空间换时间**：需要额外的计数数组空间

### 1.4 算法分类

- 非比较排序算法
- 线性时间排序算法
- 稳定排序算法
- 分布排序算法

---

## 2. 💡 基本思想


### 2.1 核心思想

计数排序的基本思想是：
1. **统计频次**：统计数组中每个元素的出现次数
2. **累积计算**：计算每个元素在排序后数组中的位置
3. **重新排列**：根据位置信息重新排列元素

### 2.2 为什么有效？

- **避免比较**：通过统计而非比较来确定元素位置
- **直接定位**：每个元素可以直接确定其最终位置
- **线性时间**：统计和重排都是线性时间操作
- **稳定性保证**：从后向前处理保证稳定性

### 2.3 排序过程示例

以数组 [4, 2, 2, 8, 3, 3, 1] 为例：

```
原始数组: [4, 2, 2, 8, 3, 3, 1]
元素范围: 1-8

第一步：统计每个元素的出现次数
计数数组: [1, 2, 2, 1, 0, 0, 0, 1]
索引映射:  1  2  3  4  5  6  7  8
含义:     1出现1次, 2出现2次, 3出现2次, 4出现1次, 8出现1次

第二步：计算累积频次（确定位置范围）
累积数组: [1, 3, 5, 6, 6, 6, 6, 7]
含义:     ≤1的有1个, ≤2的有3个, ≤3的有5个, ≤4的有6个...

第三步：从后向前重新排列（保证稳定性）
处理 arr[6]=1: 位置=count[1]-1=0, result[0]=1, count[1]=0
处理 arr[5]=3: 位置=count[3]-1=4, result[4]=3, count[3]=4
处理 arr[4]=3: 位置=count[3]-1=3, result[3]=3, count[3]=3
处理 arr[3]=8: 位置=count[8]-1=6, result[6]=8, count[8]=6
处理 arr[2]=2: 位置=count[2]-1=2, result[2]=2, count[2]=2
处理 arr[1]=2: 位置=count[2]-1=1, result[1]=2, count[2]=1
处理 arr[0]=4: 位置=count[4]-1=5, result[5]=4, count[4]=5

最终结果: [1, 2, 2, 3, 3, 4, 8]
```

---

## 3. ⚙️ 算法原理


### 3.1 工作机制

1. **确定范围**：找出数组中的最大值和最小值，确定计数数组的大小
2. **统计频次**：遍历原数组，统计每个元素的出现次数
3. **累积计算**：将计数数组转换为累积数组，确定每个元素的位置范围
4. **构建结果**：从后向前遍历原数组，根据累积数组确定每个元素的位置

### 3.2 关键概念


##### 3.2.1 计数数组（Count Array）

- **定义**：用于统计每个元素出现次数的辅助数组
- **大小**：max - min + 1
- **索引映射**：count[value - min] 对应 value 的出现次数

##### 3.2.2 累积数组（Cumulative Array）

- **定义**：计数数组的前缀和
- **含义**：count[i] 表示小于等于 (i + min) 的元素个数
- **作用**：确定每个元素在排序后数组中的位置

### 3.3 稳定性保证

- **关键**：从后向前处理原数组
- **原理**：相同元素中，后出现的会被放在前面位置，从而保持相对顺序
- **重要性**：使计数排序成为稳定排序算法

---

## 4. 🔨 实现方法


### 4.1 基础实现（Python）


```python
def counting_sort(arr):
    """
    计数排序基础实现
    """
    if not arr:
        return arr
    
    # 找出最大值和最小值
    min_val = min(arr)
    max_val = max(arr)
    
    # 计算计数数组的大小
    range_size = max_val - min_val + 1
    
    # 初始化计数数组
    count = [0] * range_size
    
    # 统计每个元素的出现次数
    for num in arr:
        count[num - min_val] += 1
    
    # 计算累积频次
    for i in range(1, range_size):
        count[i] += count[i - 1]
    
    # 构建结果数组
    result = [0] * len(arr)
    
    # 从后向前处理，保证稳定性
    for i in range(len(arr) - 1, -1, -1):
        num = arr[i]
        index = count[num - min_val] - 1
        result[index] = num
        count[num - min_val] -= 1
    
    return result
```

### 4.2 简化实现（非稳定版本）


```python
def counting_sort_simple(arr):
    """
    计数排序简化实现（不保证稳定性）
    """
    if not arr:
        return arr
    
    min_val = min(arr)
    max_val = max(arr)
    range_size = max_val - min_val + 1
    
    # 统计频次
    count = [0] * range_size
    for num in arr:
        count[num - min_val] += 1
    
    # 直接构建结果
    result = []
    for i in range(range_size):
        value = i + min_val
        result.extend([value] * count[i])
    
    return result
```

### 4.3 Java 实现


```java
public class CountingSort {
    public static int[] countingSort(int[] arr) {
        if (arr.length == 0) return arr;
        
        // 找出最值
        int min = arr[0], max = arr[0];
        for (int num : arr) {
            min = Math.min(min, num);
            max = Math.max(max, num);
        }
        
        int range = max - min + 1;
        int[] count = new int[range];
        
        // 统计频次
        for (int num : arr) {
            count[num - min]++;
        }
        
        // 计算累积频次
        for (int i = 1; i < range; i++) {
            count[i] += count[i - 1];
        }
        
        // 构建结果数组
        int[] result = new int[arr.length];
        for (int i = arr.length - 1; i >= 0; i--) {
            int num = arr[i];
            int index = count[num - min] - 1;
            result[index] = num;
            count[num - min]--;
        }
        
        return result;
    }
}
```

### 4.4 C++ 实现


```cpp
#include <vector>
#include <algorithm>
using namespace std;

vector<int> countingSort(vector<int>& arr) {
    if (arr.empty()) return arr;
    
    // 找出最值
    int minVal = *min_element(arr.begin(), arr.end());
    int maxVal = *max_element(arr.begin(), arr.end());
    
    int range = maxVal - minVal + 1;
    vector<int> count(range, 0);
    
    // 统计频次
    for (int num : arr) {
        count[num - minVal]++;
    }
    
    // 计算累积频次
    for (int i = 1; i < range; i++) {
        count[i] += count[i - 1];
    }
    
    // 构建结果数组
    vector<int> result(arr.size());
    for (int i = arr.size() - 1; i >= 0; i--) {
        int num = arr[i];
        int index = count[num - minVal] - 1;
        result[index] = num;
        count[num - minVal]--;
    }
    
    return result;
}
```

---

## 5. 📊 算法分析


### 5.1 时间复杂度


##### 5.1.1 各阶段复杂度

- **找最值**：`O(n)` <span style="color:#22c55e">✅ 线性</span>
- **统计频次**：`O(n)` <span style="color:#22c55e">✅ 线性</span>
- **累积计算**：`O(k)` <span style="color:#22c55e">✅ 线性</span>
- **构建结果**：`O(n)` <span style="color:#22c55e">✅ 线性</span>

##### 5.1.2 总时间复杂度

- **复杂度**：`O(n + k)` <span style="color:#22c55e">✅ 线性</span>
- **说明**：n是数组长度，k是数据范围（max - min + 1）
- **最优情况**：当k = O(n)时，时间复杂度为O(n)

### 5.2 空间复杂度

- **计数数组**：`O(k)` <span style="color:#f59e0b">⚠️ 依赖数据范围</span>
- **结果数组**：`O(n)` <span style="color:#22c55e">✅ 线性</span>
- **总空间复杂度**：`O(n + k)`

### 5.3 稳定性分析

- **稳定排序**：<span style="color:#22c55e">✅ 稳定</span>
- **保证方法**：从后向前处理原数组，确保相等元素的相对位置不变

### 5.4 性能特点

- **🔸 线性时间**：在k不是很大的情况下，接近O(n)
- **🔸 稳定排序**：保持相等元素的相对位置
- **🔸 非比较**：不依赖元素间的比较
- **🔸 空间敏感**：空间复杂度依赖于数据范围

---

## 6. 🎯 应用场景


### 6.1 适用情况

- **🔸 数据范围小**：数据范围相对较小（k 不远大于 n）
- **🔸 整数排序**：主要用于整数或可映射为整数的数据
- **🔸 稳定性要求**：需要保持相等元素的相对位置
- **🔸 线性时间需求**：需要突破比较排序的O(n log n)限制
- **🔸 基数排序组件**：作为基数排序的基础算法

### 6.2 不适用情况

- **🔸 数据范围过大**：当k >> n时，空间和时间开销过大
- **🔸 浮点数排序**：不适合直接排序浮点数
- **🔸 通用排序**：不如比较排序算法通用
- **🔸 内存受限**：无法提供O(k)额外空间时

### 6.3 典型应用示例


```python
# 年龄排序（范围0-150）
def sort_ages(ages):
    """对年龄进行排序"""
    count = [0] * 151
    for age in ages:
        count[age] += 1
    
    result = []
    for age in range(151):
        result.extend([age] * count[age])
    return result

# 成绩排序（范围0-100）
def sort_scores(scores):
    """对成绩进行排序"""
    return counting_sort_simple(scores)

# 字符计数排序
def sort_chars(chars):
    """对单个字符进行排序"""
    count = [0] * 256  # ASCII字符
    for char in chars:
        count[ord(char)] += 1
    
    result = []
    for i in range(256):
        result.extend([chr(i)] * count[i])
    return result
```

---

## 7. ⚖️ 对比分析


### 7.1 与其他排序算法对比


| 算法 | 时间复杂度 | 空间复杂度 | 稳定性 | 数据要求 |
|------|------------|------------|---------|----------|
| 计数排序 | O(n + k) | O(n + k) | <span style="color:#22c55e">✅ 稳定</span> | <span style="color:#f59e0b">⚠️ 整数范围小</span> |
| 快速排序 | O(n log n) | O(log n) | <span style="color:#ef4444">❌ 不稳定</span> | <span style="color:#22c55e">✅ 通用</span> |
| 归并排序 | O(n log n) | O(n) | <span style="color:#22c55e">✅ 稳定</span> | <span style="color:#22c55e">✅ 通用</span> |
| 堆排序 | O(n log n) | O(1) | <span style="color:#ef4444">❌ 不稳定</span> | <span style="color:#22c55e">✅ 通用</span> |
| 基数排序 | O(d(n + k)) | O(n + k) | <span style="color:#22c55e">✅ 稳定</span> | <span style="color:#f59e0b">⚠️ 整数</span> |

### 7.2 优缺点总结


##### 7.2.1 主要优点

- **线性时间**：在合适条件下时间复杂度为O(n)
- **稳定排序**：保持相等元素的相对位置
- **非比较**：不依赖于元素间的比较操作
- **简单实现**：算法逻辑简单易懂
- **可预测性**：性能不依赖于输入数据的分布

##### 7.2.2 主要缺点

- **空间依赖**：空间复杂度依赖于数据范围
- **数据限制**：只适用于整数或可映射为整数的数据
- **范围敏感**：当数据范围很大时效率很低
- **不够通用**：不如比较排序算法通用

### 7.3 选择建议


**推荐使用计数排序的情况**：
- ✅ 数据为小范围整数
- ✅ 需要稳定排序
- ✅ 需要线性时间复杂度
- ✅ 有足够的内存空间
- ✅ 数据范围 k 相对较小

**不推荐使用计数排序的情况**：
- ❌ 数据范围很大（k >> n）
- ❌ 排序浮点数
- ❌ 内存空间受限
- ❌ 需要通用排序算法

---

## 8. 📋 总结


### 8.1 核心要点

1. **🔸 非比较排序**：通过统计而非比较来实现排序
2. **🔸 线性时间**：在合适条件下达到O(n + k)的时间复杂度
3. **🔸 稳定性保证**：通过从后向前处理保证稳定性
4. **🔸 空间换时间**：用额外空间换取时间效率
5. **🔸 应用限制**：适用于特定类型和范围的数据

### 8.2 关键技术点

- **计数统计**：统计每个元素的出现频次
- **累积计算**：通过前缀和确定元素位置
- **稳定性实现**：从后向前处理保证稳定性
- **范围控制**：合理控制数据范围以优化性能
- **内存管理**：注意计数数组的内存消耗

### 8.3 适用原则


##### 8.3.1 何时选择计数排序

- ✅ 数据为非负整数且范围较小
- ✅ 需要稳定排序
- ✅ 追求线性时间复杂度
- ✅ 内存充足

##### 8.3.2 何时避免计数排序

- ❌ 数据范围过大
- ❌ 数据类型复杂
- ❌ 内存严格受限
- ❌ 需要通用解决方案

### 8.4 学习价值

- **算法思想**：展示了非比较排序的思路
- **复杂度突破**：突破了比较排序的O(n log n)下界
- **稳定性理解**：深入理解稳定排序的实现方法
- **空间时间权衡**：体现了算法设计中的权衡思想
