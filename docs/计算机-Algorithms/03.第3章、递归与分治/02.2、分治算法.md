---
title: 2、分治算法
---
## 📚 目录

1. [算法概述](#1-算法概述)
2. [基本思想](#2-基本思想)
3. [算法原理](#3-算法原理)
4. [实现方法](#4-实现方法)
5. [算法分析](#5-算法分析)
6. [典型应用](#6-典型应用)
7. [优化策略](#7-优化策略)
8. [总结](#8-总结)

---

## 1. 📖 算法概述


### 1.1 定义

分治算法（Divide and Conquer）是一种重要的算法设计策略，它将一个复杂的问题分解为若干个规模较小但结构相似的子问题，递归地解决这些子问题，然后将子问题的解合并得到原问题的解。

### 1.2 历史背景

- **数学基础**：源于古希腊的"分而治之"思想
- **计算机发展**：20世纪60年代开始广泛应用于算法设计
- **理论奠基**：冯·诺依曼在归并排序中的应用
- **现代应用**：成为并行计算和大数据处理的核心思想

### 1.3 核心特征

- **🔹 问题分解**：将大问题分解为小问题
- **🔹 递归求解**：递归地解决子问题
- **🔹 结果合并**：将子问题的解合并
- **🔹 独立性**：子问题之间相互独立

### 1.4 算法分类

- **二分分治**：每次分解为两个子问题（归并排序、快速排序）
- **多分分治**：每次分解为多个子问题（Strassen矩阵乘法）
- **平衡分治**：子问题规模基本相等
- **非平衡分治**：子问题规模不均匀

### 1.5 适用条件

- 问题的规模缩小到一定程度时可以容易地解决
- 问题可以分解为若干个规模较小的相同问题
- 子问题的解可以合并为原问题的解
- 子问题之间相互独立

---

## 2. 💡 基本思想


### 2.1 核心思想

分治算法的基本思想可以概括为"分而治之"：
1. **分解（Divide）**：将原问题分解为若干个规模较小的子问题
2. **解决（Conquer）**：递归地解决这些子问题
3. **合并（Combine）**：将子问题的解合并为原问题的解

### 2.2 分治的比喻理解

**分治就像团队项目管理：**
- 项目经理将大项目分解为小任务
- 每个小组独立完成自己的任务
- 最后将所有成果整合成最终项目

**分治就像分工协作：**
- 一个人无法完成的大工作
- 分配给多个人同时进行
- 最后汇总所有人的工作成果

### 2.3 分治过程示例

以归并排序为例：对数组 [8, 4, 2, 1, 3, 5, 7, 6] 进行排序

```
分治过程：

第1步：[8, 4, 2, 1, 3, 5, 7, 6] 分解为
       [8, 4, 2, 1] 和 [3, 5, 7, 6]

第2步：[8, 4, 2, 1] 分解为 [8, 4] 和 [2, 1]
       [3, 5, 7, 6] 分解为 [3, 5] 和 [7, 6]

第3步：[8, 4] 分解为 [8] 和 [4]
       [2, 1] 分解为 [2] 和 [1]
       [3, 5] 分解为 [3] 和 [5]
       [7, 6] 分解为 [7] 和 [6]

合并过程：
第1步：[8] 和 [4] 合并为 [4, 8]
       [2] 和 [1] 合并为 [1, 2]
       [3] 和 [5] 合并为 [3, 5]
       [7] 和 [6] 合并为 [6, 7]

第2步：[4, 8] 和 [1, 2] 合并为 [1, 2, 4, 8]
       [3, 5] 和 [6, 7] 合并为 [3, 5, 6, 7]

第3步：[1, 2, 4, 8] 和 [3, 5, 6, 7] 合并为 [1, 2, 3, 4, 5, 6, 7, 8]

递归树结构：
                [8, 4, 2, 1, 3, 5, 7, 6]
                      /        \
            [8, 4, 2, 1]        [3, 5, 7, 6]
             /      \             /        \
        [8, 4]    [2, 1]     [3, 5]    [7, 6]
         /  \      /  \       /  \      /  \
       [8] [4]   [2] [1]   [3] [5]   [7] [6]
```

---

## 3. ⚙️ 算法原理


### 3.1 分治的三个步骤


#### 3.1.1 分解（Divide）

```
将原问题分解为若干个规模较小的子问题

关键要求：
- 子问题的类型与原问题相同
- 子问题的规模比原问题小
- 子问题之间相互独立

常见分解方式：
- 二分分解：如归并排序、快速排序
- 均匀分解：如Strassen矩阵乘法
- 按结构分解：如树的分治遍历
```

#### 3.1.2 解决（Conquer）

```
递归地求解各个子问题

处理方式：
- 如果子问题规模足够小，直接求解
- 否则递归调用分治算法求解

递归终止条件：
- 问题规模达到阈值
- 问题可以直接求解
- 边界情况处理
```

#### 3.1.3 合并（Combine）

```
将子问题的解合并为原问题的解

合并策略：
- 简单合并：直接拼接结果
- 复杂合并：需要额外的合并算法
- 分层合并：逐层向上合并

合并复杂度：
- 直接合并：O(1)
- 线性合并：O(n)
- 复杂合并：O(n log n)
```

### 3.2 分治算法的数学模型


#### 3.2.1 递推关系

```
分治算法的时间复杂度可以用递推关系表示：

T(n) = aT(n/b) + f(n)

其中：
- a：子问题的数量
- n/b：每个子问题的规模
- f(n)：分解和合并的时间复杂度

常见模式：
- 二分分治：T(n) = 2T(n/2) + f(n)
- 三分分治：T(n) = 3T(n/3) + f(n)
- 不均匀分治：T(n) = T(n/3) + T(2n/3) + f(n)
```

#### 3.2.2 主定理（Master Theorem）

```
用于求解分治算法的时间复杂度

T(n) = aT(n/b) + f(n)，其中 a ≥ 1, b > 1

设 k = log_b(a)，则：

情况1：如果 f(n) = O(n^c)，其中 c < k
       则 T(n) = Θ(n^k)

情况2：如果 f(n) = O(n^k)
       则 T(n) = Θ(n^k * log n)

情况3：如果 f(n) = O(n^c)，其中 c > k
       则 T(n) = Θ(f(n))

应用示例：
- 归并排序：T(n) = 2T(n/2) + O(n) → T(n) = O(n log n)
- 快速排序：T(n) = 2T(n/2) + O(n) → T(n) = O(n log n)
- 二分查找：T(n) = T(n/2) + O(1) → T(n) = O(log n)
```

### 3.3 分治算法的设计要点


#### 3.3.1 问题可分解性

```
问题必须能够分解为相同类型的子问题

示例：
✓ 排序问题：可以分解为对子数组的排序
✓ 查找问题：可以分解为在子区间中查找
✓ 矩阵运算：可以分解为子矩阵的运算
✗ 图的最短路径：子图的最短路径不能直接合并
```

#### 3.3.2 子问题独立性

```
子问题之间不能有重叠，必须相互独立

独立性检查：
- 子问题的解决不依赖于其他子问题
- 子问题的输入数据不重叠
- 子问题可以并行求解

反例：
- 斐波那契数列：存在重叠子问题
- 背包问题：子问题之间有依赖关系
```

#### 3.3.3 合并可行性

```
子问题的解必须能够有效合并

合并要求：
- 合并算法的复杂度不能太高
- 合并结果能够正确表示原问题的解
- 合并过程不能破坏解的性质

合并类型：
- 直接拼接：如数组的合并
- 比较选择：如求最大值、最小值
- 复杂运算：如矩阵乘法的合并
```

---

## 4. 🔨 实现方法


### 4.1 分治算法通用模板


```java
public class DivideAndConquer {
    
    /**
     * 分治算法通用模板
     * @param problem 待解决的问题
     * @return 问题的解
     */
    public static <T, R> R divideAndConquer(T problem) {
        // 基础情况：问题规模足够小，直接求解
        if (isSmallEnough(problem)) {
            return solveDirectly(problem);
        }
        
        // 分解：将问题分解为子问题
        T[] subProblems = divide(problem);
        
        // 解决：递归求解子问题
        R[] subSolutions = new R[subProblems.length];
        for (int i = 0; i < subProblems.length; i++) {
            subSolutions[i] = divideAndConquer(subProblems[i]);
        }
        
        // 合并：将子问题的解合并为原问题的解
        return combine(subSolutions);
    }
    
    private static <T> boolean isSmallEnough(T problem) {
        // 判断问题规模是否足够小
        return false;
    }
    
    private static <T, R> R solveDirectly(T problem) {
        // 直接求解小规模问题
        return null;
    }
    
    private static <T> T[] divide(T problem) {
        // 分解问题为子问题
        return null;
    }
    
    private static <R> R combine(R[] subSolutions) {
        // 合并子问题的解
        return null;
    }
}
```

### 4.2 归并排序详细实现


```java
public class MergeSort {
    
    /**
     * 归并排序主函数
     * @param arr 待排序数组
     */
    public static void mergeSort(int[] arr) {
        if (arr == null || arr.length <= 1) {
            return;
        }
        mergeSort(arr, 0, arr.length - 1);
    }
    
    /**
     * 递归归并排序
     * @param arr 数组
     * @param left 左边界
     * @param right 右边界
     */
    private static void mergeSort(int[] arr, int left, int right) {
        // 基础情况：只有一个元素或没有元素
        if (left >= right) {
            return;
        }
        
        // 分解：找到中点，分为两个子数组
        int mid = left + (right - left) / 2;
        
        // 递归解决子问题
        mergeSort(arr, left, mid);        // 排序左半部分
        mergeSort(arr, mid + 1, right);   // 排序右半部分
        
        // 合并：将两个有序子数组合并
        merge(arr, left, mid, right);
    }
    
    /**
     * 合并两个有序子数组
     * @param arr 数组
     * @param left 左边界
     * @param mid 中点
     * @param right 右边界
     */
    private static void merge(int[] arr, int left, int mid, int right) {
        // 创建临时数组存储合并结果
        int[] temp = new int[right - left + 1];
        int i = left;      // 左子数组指针
        int j = mid + 1;   // 右子数组指针
        int k = 0;         // 临时数组指针
        
        // 比较并合并两个子数组
        while (i <= mid && j <= right) {
            if (arr[i] <= arr[j]) {
                temp[k++] = arr[i++];
            } else {
                temp[k++] = arr[j++];
            }
        }
        
        // 处理剩余元素
        while (i <= mid) {
            temp[k++] = arr[i++];
        }
        while (j <= right) {
            temp[k++] = arr[j++];
        }
        
        // 将合并结果复制回原数组
        for (i = left, k = 0; i <= right; i++, k++) {
            arr[i] = temp[k];
        }
    }
    
    /**
     * 带跟踪的归并排序（用于理解过程）
     */
    public static void mergeSortWithTrace(int[] arr, int left, int right, int depth) {
        String indent = "  ".repeat(depth);
        System.out.println(indent + "→ 排序 [" + left + "," + right + "]: " + 
                          arrayToString(arr, left, right));
        
        if (left >= right) {
            return;
        }
        
        int mid = left + (right - left) / 2;
        
        // 递归排序左右子数组
        mergeSortWithTrace(arr, left, mid, depth + 1);
        mergeSortWithTrace(arr, mid + 1, right, depth + 1);
        
        // 合并
        merge(arr, left, mid, right);
        
        System.out.println(indent + "← 合并完成: " + arrayToString(arr, left, right));
    }
    
    private static String arrayToString(int[] arr, int left, int right) {
        StringBuilder sb = new StringBuilder();
        for (int i = left; i <= right; i++) {
            sb.append(arr[i]).append(i < right ? ", " : "");
        }
        return sb.toString();
    }
}
```

### 4.3 快速排序详细实现


```java
public class QuickSort {
    
    /**
     * 快速排序主函数
     * @param arr 待排序数组
     */
    public static void quickSort(int[] arr) {
        if (arr == null || arr.length <= 1) {
            return;
        }
        quickSort(arr, 0, arr.length - 1);
    }
    
    /**
     * 递归快速排序
     * @param arr 数组
     * @param low 左边界
     * @param high 右边界
     */
    private static void quickSort(int[] arr, int low, int high) {
        // 基础情况：子数组长度小于等于1
        if (low >= high) {
            return;
        }
        
        // 分解：选择基准元素并分区
        int pivotIndex = partition(arr, low, high);
        
        // 递归解决子问题
        quickSort(arr, low, pivotIndex - 1);    // 排序左半部分
        quickSort(arr, pivotIndex + 1, high);   // 排序右半部分
        
        // 注意：快速排序不需要显式的合并步骤
        // 因为分区后元素已经在正确位置
    }
    
    /**
     * 分区函数：将数组分为小于基准和大于基准的两部分
     * @param arr 数组
     * @param low 左边界
     * @param high 右边界
     * @return 基准元素的最终位置
     */
    private static int partition(int[] arr, int low, int high) {
        // 选择最后一个元素作为基准
        int pivot = arr[high];
        int i = low - 1;  // 小于基准的元素的边界
        
        for (int j = low; j < high; j++) {
            // 如果当前元素小于或等于基准
            if (arr[j] <= pivot) {
                i++;
                swap(arr, i, j);
            }
        }
        
        // 将基准元素放到正确位置
        swap(arr, i + 1, high);
        return i + 1;
    }
    
    /**
     * 三路快排：处理大量重复元素的情况
     */
    public static void quickSort3Way(int[] arr, int low, int high) {
        if (low >= high) {
            return;
        }
        
        int pivot = arr[low];
        int lt = low;      // 小于基准的边界
        int gt = high;     // 大于基准的边界
        int i = low + 1;   // 当前检查的元素
        
        while (i <= gt) {
            if (arr[i] < pivot) {
                swap(arr, lt++, i++);
            } else if (arr[i] > pivot) {
                swap(arr, i, gt--);
            } else {
                i++;
            }
        }
        
        // 递归处理小于和大于基准的部分
        quickSort3Way(arr, low, lt - 1);
        quickSort3Way(arr, gt + 1, high);
    }
    
    private static void swap(int[] arr, int i, int j) {
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}
```

### 4.4 二分查找详细实现


```java
public class BinarySearch {
    
    /**
     * 二分查找（迭代版本）
     * @param arr 有序数组
     * @param target 目标值
     * @return 目标值的索引，未找到返回-1
     */
    public static int binarySearchIterative(int[] arr, int target) {
        int left = 0;
        int right = arr.length - 1;
        
        while (left <= right) {
            int mid = left + (right - left) / 2;
            
            if (arr[mid] == target) {
                return mid;
            } else if (arr[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        
        return -1;
    }
    
    /**
     * 二分查找（递归版本）
     * @param arr 有序数组
     * @param target 目标值
     * @return 目标值的索引，未找到返回-1
     */
    public static int binarySearchRecursive(int[] arr, int target) {
        return binarySearchRecursive(arr, target, 0, arr.length - 1);
    }
    
    private static int binarySearchRecursive(int[] arr, int target, int left, int right) {
        // 基础情况：查找区间为空
        if (left > right) {
            return -1;
        }
        
        // 分解：计算中点
        int mid = left + (right - left) / 2;
        
        // 检查中点是否为目标值
        if (arr[mid] == target) {
            return mid;
        }
        
        // 递归解决子问题
        if (arr[mid] < target) {
            // 在右半部分查找
            return binarySearchRecursive(arr, target, mid + 1, right);
        } else {
            // 在左半部分查找
            return binarySearchRecursive(arr, target, left, mid - 1);
        }
    }
    
    /**
     * 查找第一个大于等于目标值的元素
     */
    public static int binarySearchLowerBound(int[] arr, int target) {
        int left = 0;
        int right = arr.length;
        
        while (left < right) {
            int mid = left + (right - left) / 2;
            
            if (arr[mid] < target) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }
        
        return left;
    }
}
```

### 4.5 大整数乘法（Karatsuba算法）


```java
import java.math.BigInteger;

public class Karatsuba {
    
    /**
     * Karatsuba大整数乘法
     * @param x 第一个数
     * @param y 第二个数
     * @return 乘积
     */
    public static BigInteger multiply(BigInteger x, BigInteger y) {
        // 基础情况：数字较小时使用普通乘法
        if (x.toString().length() < 10 || y.toString().length() < 10) {
            return x.multiply(y);
        }
        
        // 计算位数
        int n = Math.max(x.toString().length(), y.toString().length());
        int half = n / 2;
        
        // 分解：将大数分为高位和低位
        BigInteger power = BigInteger.TEN.pow(half);
        
        BigInteger a = x.divide(power);           // x的高位
        BigInteger b = x.remainder(power);        // x的低位
        BigInteger c = y.divide(power);           // y的高位
        BigInteger d = y.remainder(power);        // y的低位
        
        // 递归计算三个乘积
        BigInteger ac = multiply(a, c);           // 高位乘积
        BigInteger bd = multiply(b, d);           // 低位乘积
        BigInteger adbc = multiply(a.add(b), c.add(d)).subtract(ac).subtract(bd);
        
        // 合并：计算最终结果
        BigInteger result = ac.multiply(power.pow(2))
                           .add(adbc.multiply(power))
                           .add(bd);
        
        return result;
    }
}
```

---

## 5. 📊 算法分析


### 5.1 时间复杂度分析

- **归并排序**：T(n) = 2T(n/2) + O(n) = O(n log n)
- **快速排序**：平均 O(n log n)，最坏 O(n²)
- **二分查找**：T(n) = T(n/2) + O(1) = O(log n)
- **Karatsuba**：T(n) = 3T(n/2) + O(n) = O(n^1.585)

### 5.2 空间复杂度分析

- **归并排序**：O(n)（需要临时数组）
- **快速排序**：O(log n)（递归栈）
- **二分查找**：O(log n)（递归）或 O(1)（迭代）

---

## 6. 🎯 典型应用


### 6.1 排序算法

- 归并排序、快速排序、堆排序

### 6.2 查找算法

- 二分查找、查找第K小元素、在旋转数组中查找

### 6.3 数值计算

- 大整数乘法、矩阵乘法、快速幂

### 6.4 几何问题

- 最近点对问题、凸包问题

### 6.5 字符串算法

- 字符串匹配、最长公共子序列

---

## 7. 🚀 优化策略


### 7.1 减少递归深度

- 尾递归优化
- 迭代替代递归
- 混合算法（小数组使用简单算法）

### 7.2 内存优化

- 原地算法
- 循环缓冲区
- 减少临时空间使用

### 7.3 并行化

- 子问题独立性适合并行处理
- Fork-Join框架实现并行分治

---

## 8. 📋 总结


### 8.1 核心要点

1. **🔸 三步骤**：分解、解决、合并
2. **🔸 适用条件**：问题可分解、子问题独立、可有效合并
3. **🔸 复杂度分析**：使用主定理分析时间复杂度
4. **🔸 实现关键**：确定基础情况、递归关系、合并策略
5. **🔸 优化方向**：减少递归深度、内存优化、并行化

### 8.2 设计原则

- **正确性**：确保分解和合并的正确性
- **效率性**：选择合适的分解策略
- **稳定性**：注意算法的稳定性要求
- **实用性**：考虑实际应用中的优化

