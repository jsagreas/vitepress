---
title: 1、哈希基础
---
## 📚 目录

1. [什么是哈希](#1-什么是哈希)
2. [哈希函数原理](#2-哈希函数原理)
3. [哈希的基本特性](#3-哈希的基本特性)
4. [简单哈希函数](#4-简单哈希函数)
5. [哈希的时间空间复杂度](#5-哈希的时间空间复杂度)
6. [哈希的核心应用](#6-哈希的核心应用)

---

## 1. 🎯 什么是哈希


### 1.1 基本定义

哈希（Hash）是一种将任意长度的输入数据映射为固定长度输出的函数。这个过程叫做哈希化，输出结果叫做哈希值。

```
简单理解：
输入：任意数据（字符串、数字、文件等）
哈希函数：数学计算过程
输出：固定长度的数值（通常是整数）

例子：
"hello" → hash() → 99162322
"world" → hash() → 113318802
```

### 1.2 为什么需要哈希

```
🔸 快速查找：将O(n)的查找降低到O(1)
🔸 数据索引：为大量数据建立快速访问索引
🔸 数据验证：快速检查数据是否被修改
🔸 唯一标识：为数据生成相对唯一的标识符
```

### 1.3 哈希的直观理解

```java
// 传统查找：需要逐个比较
public int findInArray(String[] arr, String target) {
    for (int i = 0; i < arr.length; i++) {
        if (arr[i].equals(target)) {
            return i;  // 找到了，返回位置
        }
    }
    return -1;  // 没找到
}
// 时间复杂度：O(n)

// 哈希查找：直接定位
Map<String, Integer> hashMap = new HashMap<>();
hashMap.put("apple", 1);
hashMap.put("banana", 2);

int result = hashMap.get("apple");  // 直接获取
// 时间复杂度：O(1)
```

---

## 2. ⚙️ 哈希函数原理


### 2.1 哈希函数的数学表示

```
哈希函数：H(key) = hash_value

特点：
- 输入域：无限大（所有可能的输入）
- 输出域：有限的（0到m-1，m是哈希表大小）
- 映射：多对一的映射关系

数学表示：
H: U → {0, 1, 2, ..., m-1}
```

### 2.2 哈希函数的工作过程

```java
public class HashProcess {
    
    // 简单的字符串哈希过程演示
    public static int simpleStringHash(String str) {
        System.out.println("哈希计算过程：");
        System.out.println("输入字符串: " + str);
        
        int hash = 0;
        for (int i = 0; i < str.length(); i++) {
            char c = str.charAt(i);
            hash = hash * 31 + c;  // 31是常用的质数
            System.out.println("处理字符 '" + c + "': hash = " + hash);
        }
        
        System.out.println("最终哈希值: " + hash);
        return hash;
    }
    
    // 演示
    public static void main(String[] args) {
        simpleStringHash("abc");
    }
}

/*
输出示例：
哈希计算过程：
输入字符串: abc
处理字符 'a': hash = 97
处理字符 'b': hash = 3105
处理字符 'c': hash = 96354
最终哈希值: 96354
*/
```

### 2.3 从哈希值到数组索引

```java
public class HashToIndex {
    
    public static void demonstrateHashToIndex() {
        String[] keys = {"apple", "banana", "cherry"};
        int tableSize = 7;  // 哈希表大小
        
        System.out.println("从哈希值到数组索引：");
        System.out.println("哈希表大小: " + tableSize);
        System.out.println();
        
        for (String key : keys) {
            int hashValue = key.hashCode();
            int index = Math.abs(hashValue) % tableSize;
            
            System.out.println("键: " + key);
            System.out.println("  哈希值: " + hashValue);
            System.out.println("  数组索引: " + index);
            System.out.println();
        }
    }
}
```

---

## 3. 📐 哈希的基本特性


### 3.1 确定性

相同的输入总是产生相同的输出。

```java
public class HashDeterminism {
    
    public static void testDeterminism() {
        String input = "hello";
        
        System.out.println("确定性测试：");
        for (int i = 0; i < 5; i++) {
            int hash = input.hashCode();
            System.out.println("第" + (i+1) + "次计算: " + hash);
        }
        // 所有结果都应该相同
    }
}
```

### 3.2 均匀分布

好的哈希函数应该让输出值均匀分布在整个输出空间。

```java
public class HashDistribution {
    
    public static void testDistribution() {
        int tableSize = 10;
        int[] buckets = new int[tableSize];
        
        // 测试1000个连续数字的分布
        for (int i = 0; i < 1000; i++) {
            int hash = Integer.hashCode(i);
            int index = Math.abs(hash) % tableSize;
            buckets[index]++;
        }
        
        System.out.println("分布测试结果：");
        for (int i = 0; i < tableSize; i++) {
            System.out.println("桶" + i + ": " + buckets[i] + "个元素");
        }
        
        // 理想情况下每个桶应该有大约100个元素
    }
}
```

### 3.3 快速计算

哈希函数必须能够快速计算，否则失去了优化的意义。

```java
public class HashSpeed {
    
    public static void compareHashSpeed() {
        String testString = "这是一个测试字符串";
        int iterations = 1000000;
        
        // 测试内置hashCode方法
        long start = System.nanoTime();
        for (int i = 0; i < iterations; i++) {
            int hash = testString.hashCode();
        }
        long builtinTime = System.nanoTime() - start;
        
        // 测试简单哈希
        start = System.nanoTime();
        for (int i = 0; i < iterations; i++) {
            int hash = simpleHash(testString);
        }
        long simpleTime = System.nanoTime() - start;
        
        System.out.println("哈希速度对比：");
        System.out.println("内置hashCode: " + builtinTime + " 纳秒");
        System.out.println("简单哈希: " + simpleTime + " 纳秒");
    }
    
    private static int simpleHash(String str) {
        int hash = 0;
        for (char c : str.toCharArray()) {
            hash += c;
        }
        return hash;
    }
}
```

### 3.4 雪崩效应

输入的微小变化应该导致输出的巨大变化。

```java
public class AvalancheEffect {
    
    public static void demonstrateAvalanche() {
        String original = "hello";
        String modified = "hallo";  // 只改变一个字符
        
        int hash1 = original.hashCode();
        int hash2 = modified.hashCode();
        
        System.out.println("雪崩效应演示：");
        System.out.println("原始字符串: " + original + " → " + hash1);
        System.out.println("修改字符串: " + modified + " → " + hash2);
        
        // 计算二进制位的差异
        int diff = hash1 ^ hash2;
        int bitCount = Integer.bitCount(diff);
        
        System.out.println("不同的位数: " + bitCount + "/32");
        System.out.println("变化百分比: " + (bitCount * 100.0 / 32) + "%");
    }
}
```

---

## 4. 🔧 简单哈希函数


### 4.1 除法哈希

最简单的哈希函数：`h(k) = k mod m`

```java
public class DivisionHash {
    
    public static int hash(int key, int tableSize) {
        return Math.abs(key) % tableSize;
    }
    
    public static int hashString(String str, int tableSize) {
        int sum = 0;
        for (char c : str.toCharArray()) {
            sum += c;
        }
        return sum % tableSize;
    }
    
    public static void example() {
        int tableSize = 7;
        int[] keys = {10, 22, 31, 4, 15, 28, 17, 88, 59};
        
        System.out.println("除法哈希示例（表大小=" + tableSize + "）：");
        for (int key : keys) {
            int index = hash(key, tableSize);
            System.out.println(key + " → " + index);
        }
    }
}
```

### 4.2 乘法哈希

使用乘法和小数部分：`h(k) = ⌊m(kA mod 1)⌋`

```java
public class MultiplicationHash {
    
    private static final double A = 0.6180339887;  // (√5 - 1) / 2
    
    public static int hash(int key, int tableSize) {
        double temp = key * A;
        double fractionalPart = temp - Math.floor(temp);
        return (int) Math.floor(tableSize * fractionalPart);
    }
    
    public static void example() {
        int tableSize = 8;
        int[] keys = {10, 22, 31, 4, 15, 28, 17, 88, 59};
        
        System.out.println("乘法哈希示例（表大小=" + tableSize + "）：");
        for (int key : keys) {
            int index = hash(key, tableSize);
            System.out.println(key + " → " + index);
        }
    }
}
```

### 4.3 字符串哈希

专门处理字符串的哈希函数。

```java
public class StringHash {
    
    // 多项式哈希
    public static int polynomialHash(String str, int tableSize) {
        int hash = 0;
        int prime = 31;  // 使用质数
        
        for (char c : str.toCharArray()) {
            hash = hash * prime + c;
        }
        
        return Math.abs(hash) % tableSize;
    }
    
    // 简单累加哈希
    public static int sumHash(String str, int tableSize) {
        int sum = 0;
        for (char c : str.toCharArray()) {
            sum += c;
        }
        return sum % tableSize;
    }
    
    public static void compareStringHash() {
        String[] words = {"cat", "dog", "rat", "bat", "hat"};
        int tableSize = 7;
        
        System.out.println("字符串哈希对比：");
        System.out.println("单词\t\t累加哈希\t多项式哈希");
        
        for (String word : words) {
            int sum = sumHash(word, tableSize);
            int poly = polynomialHash(word, tableSize);
            System.out.println(word + "\t\t" + sum + "\t\t" + poly);
        }
    }
}
```

---

## 5. ⏱️ 哈希的时间空间复杂度


### 5.1 时间复杂度分析

```
理想情况（无冲突）：
- 查找：O(1)
- 插入：O(1)  
- 删除：O(1)

最坏情况（所有元素冲突）：
- 查找：O(n)
- 插入：O(n)
- 删除：O(n)

平均情况：
- 查找：O(1 + α)，其中α是负载因子
- 插入：O(1 + α)
- 删除：O(1 + α)
```

### 5.2 负载因子的影响

```java
public class LoadFactorImpact {
    
    public static void analyzeLoadFactor() {
        System.out.println("负载因子对性能的影响：");
        System.out.println("负载因子 = 元素数量 / 哈希表大小");
        System.out.println();
        
        double[] loadFactors = {0.5, 0.7, 0.9, 1.0, 1.5};
        
        for (double lf : loadFactors) {
            double avgProbes = 1 + lf / 2;  // 线性探测的平均探测次数
            System.out.printf("负载因子 %.1f: 平均探测次数 %.2f\n", lf, avgProbes);
        }
        
        System.out.println();
        System.out.println("结论：负载因子越高，性能越差");
        System.out.println("建议：保持负载因子在0.7以下");
    }
}
```

### 5.3 空间复杂度

```java
public class SpaceComplexity {
    
    public static void analyzeSpace() {
        System.out.println("哈希表空间复杂度分析：");
        
        int n = 1000;  // 要存储的元素数量
        
        // 不同负载因子对应的空间需求
        double[] loadFactors = {0.5, 0.7, 0.9};
        
        for (double lf : loadFactors) {
            int tableSize = (int) Math.ceil(n / lf);
            int wastedSpace = tableSize - n;
            double wastePercent = wastedSpace * 100.0 / tableSize;
            
            System.out.printf("负载因子 %.1f:\n", lf);
            System.out.printf("  表大小: %d\n", tableSize);
            System.out.printf("  浪费空间: %d (%.1f%%)\n", wastedSpace, wastePercent);
            System.out.println();
        }
        
        System.out.println("总结：空间复杂度为O(m)，其中m是哈希表大小");
        System.out.println("实际空间 = O(n/α)，α是负载因子");
    }
}
```

---

## 6. 🚀 哈希的核心应用


### 6.1 快速查找：HashMap

```java
public class HashMapUsage {
    
    public static void demonstrateHashMap() {
        // 创建学生成绩管理系统
        Map<String, Integer> studentScores = new HashMap<>();
        
        // 插入数据：O(1)
        studentScores.put("张三", 85);
        studentScores.put("李四", 92);
        studentScores.put("王五", 78);
        studentScores.put("赵六", 95);
        
        // 查找数据：O(1)
        System.out.println("张三的成绩: " + studentScores.get("张三"));
        System.out.println("李四的成绩: " + studentScores.get("李四"));
        
        // 检查是否存在：O(1)
        if (studentScores.containsKey("王五")) {
            System.out.println("找到王五的记录");
        }
        
        // 遍历所有数据
        System.out.println("所有学生成绩：");
        for (Map.Entry<String, Integer> entry : studentScores.entrySet()) {
            System.out.println(entry.getKey() + ": " + entry.getValue());
        }
    }
}
```

### 6.2 去重：HashSet

```java
public class HashSetUsage {
    
    public static void demonstrateDeduplication() {
        // 原始数据（包含重复）
        String[] words = {"apple", "banana", "apple", "cherry", "banana", "date"};
        
        // 使用HashSet去重
        Set<String> uniqueWords = new HashSet<>();
        for (String word : words) {
            uniqueWords.add(word);
        }
        
        System.out.println("原始数据: " + Arrays.toString(words));
        System.out.println("去重后: " + uniqueWords);
        System.out.println("原始数量: " + words.length);
        System.out.println("去重数量: " + uniqueWords.size());
    }
}
```

### 6.3 计数统计

```java
public class CountingWithHash {
    
    public static void wordFrequency() {
        String text = "hello world hello java world hello";
        String[] words = text.split(" ");
        
        // 使用HashMap统计词频
        Map<String, Integer> frequency = new HashMap<>();
        
        for (String word : words) {
            frequency.put(word, frequency.getOrDefault(word, 0) + 1);
        }
        
        System.out.println("词频统计：");
        for (Map.Entry<String, Integer> entry : frequency.entrySet()) {
            System.out.println(entry.getKey() + ": " + entry.getValue() + "次");
        }
    }
}
```

### 6.4 缓存实现

```java
public class SimpleCache {
    
    private Map<String, String> cache;
    private final int maxSize;
    
    public SimpleCache(int maxSize) {
        this.maxSize = maxSize;
        this.cache = new HashMap<>();
    }
    
    public String get(String key) {
        return cache.get(key);
    }
    
    public void put(String key, String value) {
        if (cache.size() >= maxSize && !cache.containsKey(key)) {
            // 简单策略：删除第一个元素
            String firstKey = cache.keySet().iterator().next();
            cache.remove(firstKey);
        }
        cache.put(key, value);
    }
    
    public static void demonstrateCache() {
        SimpleCache cache = new SimpleCache(3);
        
        System.out.println("缓存演示：");
        
        cache.put("user1", "张三的数据");
        cache.put("user2", "李四的数据");
        cache.put("user3", "王五的数据");
        
        System.out.println("查询user1: " + cache.get("user1"));
        System.out.println("查询user2: " + cache.get("user2"));
        
        // 添加新数据，触发淘汰
        cache.put("user4", "赵六的数据");
        System.out.println("添加user4后，查询user1: " + cache.get("user1"));
    }
}
```

### 6.5 数据完整性验证

```java
public class DataIntegrity {
    
    // 简单的数据校验
    public static int computeChecksum(String data) {
        int checksum = 0;
        for (char c : data.toCharArray()) {
            checksum = checksum * 31 + c;
        }
        return Math.abs(checksum);
    }
    
    public static boolean verifyData(String data, int expectedChecksum) {
        int actualChecksum = computeChecksum(data);
        return actualChecksum == expectedChecksum;
    }
    
    public static void demonstrateIntegrity() {
        String originalData = "重要的数据内容";
        int checksum = computeChecksum(originalData);
        
        System.out.println("数据完整性验证：");
        System.out.println("原始数据: " + originalData);
        System.out.println("校验码: " + checksum);
        
        // 验证原始数据
        boolean isValid = verifyData(originalData, checksum);
        System.out.println("原始数据验证: " + (isValid ? "通过" : "失败"));
        
        // 验证被篡改的数据
        String tamperedData = originalData + "被修改";
        isValid = verifyData(tamperedData, checksum);
        System.out.println("篡改数据验证: " + (isValid ? "通过" : "失败"));
    }
}
```

---

## 🎯 总结


### 核心要点

```
🔸 哈希是什么：
- 将任意数据映射为固定长度数值的函数
- 实现从O(n)到O(1)的查找优化

🔸 关键特性：
- 确定性：相同输入产生相同输出
- 快速计算：哈希函数本身要高效
- 均匀分布：避免大量冲突
- 雪崩效应：微小变化产生巨大差异

🔸 性能特点：
- 平均时间复杂度：O(1)
- 空间复杂度：O(m)，m是表大小
- 性能受负载因子影响

🔸 主要应用：
- 快速查找：HashMap、HashSet
- 数据去重和计数统计
- 缓存系统实现
- 数据完整性验证
```

### 使用建议

```
✅ 适合使用哈希的场景：
- 需要快速查找、插入、删除
- 数据不需要保持有序
- 内存充足，可以用空间换时间

❌ 不适合使用哈希的场景：
- 需要范围查询（如查找某个区间内的数据）
- 需要有序遍历
- 内存非常紧张
```
