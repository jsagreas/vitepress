---
title: 2、Wireshark抓包分析
---
## 📚 目录

1. [连通性测试命令](#1-连通性测试命令)
2. [路径跟踪命令](#2-路径跟踪命令)
3. [网络接口查看命令](#3-网络接口查看命令)
4. [网络连接状态命令](#4-网络连接状态命令)
5. [DNS查询命令](#5-DNS查询命令)
6. [其他实用命令](#6-其他实用命令)
7. [命令组合使用技巧](#7-命令组合使用技巧)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🏓 连通性测试命令


### 1.1 ping命令详解


**🔸 基本原理**
```
ping基于ICMP协议（Internet Control Message Protocol）
工作原理：发送ICMP Echo Request，等待ICMP Echo Reply
测试内容：网络连通性、往返时延（RTT）、丢包率
```

**💻 基本语法**
```bash
# Windows/Linux通用格式
ping [选项] 目标主机

# 基本使用
ping www.baidu.com
ping 8.8.8.8
ping 192.168.1.1
```

**⚙️ 常用参数详解**

| 参数 | **Windows** | **Linux** | **功能说明** |
|------|------------|----------|-------------|
| 🔢 **指定次数** | `-n 4` | `-c 4` | `发送4个ping包` |
| 📏 **包大小** | `-l 1024` | `-s 1024` | `设置数据包大小` |
| ⏱️ **超时时间** | `-w 5000` | `-W 5` | `等待超时（毫秒/秒）` |
| 🔄 **持续ping** | `-t` | `默认持续` | `持续发送直到手动停止` |
| 📊 **间隔时间** | `无` | `-i 2` | `设置发送间隔（秒）` |

**🎯 实战示例**
```bash
# 测试基本连通性
ping www.google.com

# Windows：发送10个包，每个1000字节，超时3秒
ping -n 10 -l 1000 -w 3000 192.168.1.1

# Linux：发送5个包，每个500字节，间隔2秒
ping -c 5 -s 500 -i 2 192.168.1.1

# 持续ping测试网络稳定性
ping -t 8.8.8.8  # Windows
ping 8.8.8.8     # Linux（Ctrl+C停止）
```

**📊 结果分析**
```
PING www.baidu.com (36.152.44.95): 56 data bytes
64 bytes from 36.152.44.95: icmp_seq=1 ttl=55 time=28.3 ms
64 bytes from 36.152.44.95: icmp_seq=2 ttl=55 time=27.9 ms
64 bytes from 36.152.44.95: icmp_seq=3 ttl=55 time=28.1 ms

关键信息解读：
• ttl=55：数据包生存时间（Time To Live）
• time=28.3ms：往返时延
• icmp_seq=1：ICMP序列号
• 0% packet loss：丢包率
```

### 1.2 高级ping技巧


**🔍 故障诊断场景**
```bash
# 测试本地回环
ping 127.0.0.1          # 测试TCP/IP协议栈

# 测试本机网卡
ping 本机IP地址          # 测试网卡驱动

# 测试网关连通性
ping 192.168.1.1        # 测试到网关的连通性

# 测试DNS解析
ping www.baidu.com      # 同时测试DNS和连通性
ping 220.181.38.148     # 直接IP，排除DNS问题
```

---

## 2. 🗺️ 路径跟踪命令


### 2.1 traceroute/tracert详解


**🔸 基本原理**
```
通过递增TTL值来发现数据包经过的路径
TTL=1时，第一跳路由器返回ICMP Time Exceeded
TTL=2时，第二跳路由器返回ICMP Time Exceeded
以此类推，直到到达目标主机
```

**💻 基本语法**
```bash
# Windows
tracert [选项] 目标主机

# Linux/macOS
traceroute [选项] 目标主机
```

**⚙️ 常用参数**
```bash
# Windows tracert
tracert -h 15 www.baidu.com    # 最大跳数15
tracert -w 5000 www.baidu.com  # 超时5秒

# Linux traceroute
traceroute -m 15 www.baidu.com # 最大跳数15
traceroute -w 5 www.baidu.com  # 超时5秒
traceroute -n www.baidu.com    # 不解析主机名
```

**📊 结果分析示例**
```
C:\>tracert www.baidu.com
跟踪到 www.a.shifen.com [36.152.44.95] 的路由，最多 30 个跃点：

  1     2 ms     1 ms     2 ms  192.168.1.1
  2    15 ms    12 ms    14 ms  10.0.0.1
  3    28 ms    25 ms    27 ms  61.148.143.1
  4    26 ms    28 ms    25 ms  36.152.44.95

分析说明：
• 第1跳：本地路由器/网关（2ms）
• 第2跳：ISP接入路由器（14ms）
• 第3跳：ISP骨干网路由器（27ms）
• 第4跳：目标服务器（26ms）
```

### 2.2 mtr命令（Linux）


**🔸 mtr优势**
```
mtr = My Traceroute，结合了ping和traceroute的功能
实时显示：持续监控，动态更新统计信息
丰富统计：显示丢包率、延迟统计、最佳/最差/平均延迟
```

**💻 基本使用**
```bash
# 基本使用
mtr www.baidu.com

# 指定发送包数后退出
mtr -c 10 www.baidu.com

# 不解析主机名
mtr -n www.baidu.com

# 报告模式（适合脚本）
mtr -r -c 10 www.baidu.com
```

**📊 mtr输出解读**
```
                             My traceroute  [v0.92]
Host                    Loss%   Snt   Last   Avg  Best  Wrst StDev
1. 192.168.1.1           0.0%    10    1.2   1.4   1.1   2.1   0.3
2. 10.0.0.1              0.0%    10   12.3  13.1  11.8  15.2   1.1
3. 61.148.143.1          0.0%    10   26.8  27.2  25.9  29.1   1.0
4. 36.152.44.95          0.0%    10   28.1  28.5  27.6  30.2   0.8

字段说明：
• Loss%：丢包率
• Snt：发送包数
• Last：最后一次延迟
• Avg：平均延迟
• Best/Wrst：最佳/最差延迟
• StDev：标准差（稳定性指标）
```

---

## 3. 🔌 网络接口查看命令


### 3.1 ipconfig（Windows）


**💻 基本使用**
```cmd
# 显示所有网络接口的基本信息
ipconfig

# 显示详细配置信息
ipconfig /all

# 释放IP地址
ipconfig /release

# 续租IP地址
ipconfig /renew

# 刷新DNS缓存
ipconfig /flushdns

# 显示DNS缓存
ipconfig /displaydns
```

**📊 输出信息解读**
```
Windows IP 配置

以太网适配器 本地连接:
   连接特定的 DNS 后缀:
   本地链接 IPv6 地址: fe80::a1b2:c3d4:e5f6:7890%11
   IPv4 地址: 192.168.1.100
   子网掩码: 255.255.255.0
   默认网关: 192.168.1.1

关键信息：
• 网络适配器名称和状态
• IP地址、子网掩码、默认网关
• DNS服务器地址
• DHCP配置信息
```

### 3.2 ifconfig（Linux）


**💻 基本使用**
```bash
# 显示所有活跃接口
ifconfig

# 显示指定接口
ifconfig eth0

# 配置IP地址
sudo ifconfig eth0 192.168.1.100 netmask 255.255.255.0

# 启用/禁用接口
sudo ifconfig eth0 up
sudo ifconfig eth0 down

# 配置别名接口
sudo ifconfig eth0:1 192.168.1.101
```

### 3.3 ip命令（现代Linux）


**💻 替代ifconfig的现代命令**
```bash
# 显示所有接口
ip addr show
ip a  # 简写

# 显示路由表
ip route show
ip r  # 简写

# 显示邻居表（ARP表）
ip neighbor show
ip n  # 简写

# 添加IP地址
sudo ip addr add 192.168.1.100/24 dev eth0

# 启用/禁用接口
sudo ip link set eth0 up
sudo ip link set eth0 down
```

---

## 4. 📊 网络连接状态命令


### 4.1 netstat命令详解


**🔸 基本功能**
```
显示网络连接、路由表、接口统计、masquerade连接等信息
是网络故障排查的重要工具
支持TCP、UDP、UNIX套接字等多种协议
```

**💻 常用参数组合**
```bash
# 显示所有连接和监听端口
netstat -a

# 显示数字地址而非主机名
netstat -n

# 显示TCP连接
netstat -t

# 显示UDP连接
netstat -u

# 显示监听状态的端口
netstat -l

# 显示进程ID和名称
netstat -p

# 组合使用：显示所有TCP连接的数字形式
netstat -ant

# 显示监听端口及对应进程
netstat -tlnp
```

**🎯 实战示例**
```bash
# 查看特定端口是否被占用
netstat -an | grep :80
netstat -an | grep :22

# 查看established连接
netstat -ant | grep ESTABLISHED

# 查看监听端口及进程
netstat -tlnp | grep :3306

# 统计连接状态
netstat -ant | awk '{print $6}' | sort | uniq -c
```

**📊 连接状态解读**
```
TCP连接状态：
• LISTEN：监听状态，等待连接
• ESTABLISHED：已建立连接
• TIME_WAIT：连接关闭后的等待状态
• CLOSE_WAIT：等待应用程序关闭连接
• FIN_WAIT1/FIN_WAIT2：主动关闭连接的状态
• SYN_SENT：正在尝试建立连接
• SYN_RECV：收到连接请求，等待确认
```

### 4.2 ss命令（现代替代）


**🔸 ss优势**
```
ss（Socket Statistics）是netstat的现代替代品
性能更好：处理大量连接时速度更快
功能更强：支持更多过滤选项
输出更清晰：格式更易读
```

**💻 基本使用**
```bash
# 显示所有套接字
ss -a

# 显示TCP连接
ss -t

# 显示UDP连接
ss -u

# 显示监听端口
ss -l

# 显示进程信息
ss -p

# 组合：显示所有TCP监听端口及进程
ss -tlnp

# 过滤特定端口
ss -tlnp | grep :80
ss dst :80
ss sport = :22
```

---

## 5. 🔍 DNS查询命令


### 5.1 nslookup命令


**🔸 基本功能**
```
查询DNS记录信息
支持正向解析（域名→IP）和反向解析（IP→域名）
可以指定查询的DNS服务器
支持多种记录类型查询
```

**💻 基本使用**
```bash
# 基本域名解析
nslookup www.baidu.com

# 反向DNS查询
nslookup 8.8.8.8

# 指定DNS服务器查询
nslookup www.baidu.com 8.8.8.8

# 交互模式
nslookup
> www.baidu.com
> server 8.8.8.8
> www.google.com
> exit
```

**🔍 高级查询**
```bash
# 查询特定记录类型
nslookup -type=A www.baidu.com      # A记录
nslookup -type=MX baidu.com         # 邮件交换记录
nslookup -type=NS baidu.com         # 名称服务器记录
nslookup -type=CNAME www.baidu.com  # 别名记录
nslookup -type=TXT baidu.com        # 文本记录
```

### 5.2 dig命令（Linux推荐）


**🔸 dig优势**
```
dig（Domain Information Groper）功能更强大
输出格式更清晰易读
支持更多查询选项
批量查询能力强
```

**💻 基本使用**
```bash
# 基本查询
dig www.baidu.com

# 简洁输出
dig www.baidu.com +short

# 指定DNS服务器
dig @8.8.8.8 www.baidu.com

# 反向查询
dig -x 8.8.8.8

# 查询特定记录类型
dig www.baidu.com A
dig baidu.com MX
dig baidu.com NS
dig baidu.com TXT
```

**📊 dig输出解读**
```
; <<>> DiG 9.16.1 <<>> www.baidu.com
;; global options: +cmd
;; Got answer:
;; ->>HEADER<<- opcode: QUERY, status: NOERROR, id: 12345
;; flags: qr rd ra; QUERY: 1, ANSWER: 3, AUTHORITY: 0, ADDITIONAL: 1

;; QUESTION SECTION:
;www.baidu.com.			IN	A

;; ANSWER SECTION:
www.baidu.com.		1200	IN	CNAME	www.a.shifen.com.
www.a.shifen.com.	300	IN	A	36.152.44.95
www.a.shifen.com.	300	IN	A	36.152.44.96

;; Query time: 23 msec
;; SERVER: 192.168.1.1#53(192.168.1.1)
;; WHEN: Mon Jul 16 10:30:45 CST 2025
;; MSG SIZE  rcvd: 90

关键信息：
• ANSWER SECTION：查询结果
• Query time：查询耗时
• SERVER：使用的DNS服务器
• TTL：记录缓存时间
```

---

## 6. 🛠️ 其他实用命令


### 6.1 telnet命令


**🔸 基本功能**
```
测试TCP端口连通性
简单的远程登录工具
应用层连通性测试
协议测试和调试
```

**💻 基本使用**
```bash
# 测试端口连通性
telnet www.baidu.com 80
telnet 192.168.1.1 22
telnet smtp.gmail.com 25

# 测试本地服务
telnet localhost 3306
telnet 127.0.0.1 8080
```

**🎯 实战场景**
```bash
# 测试Web服务器
telnet www.example.com 80
# 连接成功后可以手动输入HTTP请求：
GET / HTTP/1.1
Host: www.example.com

# 测试邮件服务器
telnet smtp.gmail.com 587

# 测试数据库端口
telnet db.example.com 3306
```

### 6.2 curl命令


**🔸 基本功能**
```
强大的URL工具和库
支持多种协议：HTTP, HTTPS, FTP, FTPS等
可以发送各种HTTP请求
支持认证、代理、SSL等高级功能
```

**💻 基本使用**
```bash
# 基本GET请求
curl http://www.baidu.com

# 显示详细信息
curl -v http://www.baidu.com

# 只显示响应头
curl -I http://www.baidu.com

# POST请求
curl -X POST -d "key=value" http://api.example.com

# 发送JSON数据
curl -X POST -H "Content-Type: application/json" \
     -d '{"name":"test"}' http://api.example.com

# 下载文件
curl -O http://example.com/file.zip

# 测试响应时间
curl -w "@curl-format.txt" -o /dev/null -s http://www.baidu.com
```

---

## 7. 🎯 命令组合使用技巧


### 7.1 系统化故障排查流程


**🔍 网络连通性排查步骤**
```bash
# 1. 检查本地网络配置
ipconfig                    # Windows
ip addr show               # Linux

# 2. 测试本地回环
ping 127.0.0.1

# 3. 测试本机IP
ping [本机IP]

# 4. 测试网关连通性
ping [网关IP]

# 5. 测试外网连通性
ping 8.8.8.8               # 测试IP连通性
ping www.baidu.com         # 测试DNS解析

# 6. 路径跟踪分析
tracert www.baidu.com      # Windows
traceroute www.baidu.com   # Linux
```

### 7.2 端口和服务排查


**🔍 端口连通性测试流程**
```bash
# 1. 检查本地端口状态
netstat -tlnp | grep :80   # Linux
netstat -an | findstr :80  # Windows

# 2. 测试端口连通性
telnet target_host 80
nmap -p 80 target_host

# 3. 应用层测试
curl -I http://target_host
curl -v http://target_host:8080
```

### 7.3 DNS问题排查


**🔍 DNS解析问题诊断**
```bash
# 1. 测试DNS解析
nslookup www.example.com
dig www.example.com

# 2. 使用不同DNS服务器测试
nslookup www.example.com 8.8.8.8
dig @1.1.1.1 www.example.com

# 3. 清除DNS缓存
ipconfig /flushdns         # Windows
sudo systemctl restart systemd-resolved  # Linux

# 4. 检查DNS配置
cat /etc/resolv.conf       # Linux
ipconfig /all              # Windows
```

### 7.4 性能问题分析


**📊 网络性能分析组合**
```bash
# 1. 基础连通性和延迟
ping -c 100 target_host

# 2. 路径质量分析
mtr -c 50 target_host      # Linux
pathping target_host       # Windows

# 3. 端口响应时间
time telnet target_host 80

# 4. HTTP响应时间分析
curl -w "@curl-format.txt" -o /dev/null -s http://target_host
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的基本命令


```
🔸 ping：最基础的连通性测试工具，必须熟练掌握各种参数
🔸 traceroute/tracert：路径分析的核心工具，故障定位必备
🔸 netstat/ss：网络连接状态查看，端口占用检查
🔸 ipconfig/ifconfig：网络接口配置查看和管理
🔸 nslookup/dig：DNS查询和域名解析问题排查
```

### 8.2 故障排查思维模式


**🔹 分层排查原则**
```
物理层 → 数据链路层 → 网络层 → 传输层 → 应用层
从底层向上逐层排查，确定故障所在层次
```

**🔹 对比测试方法**
```
本地 vs 远程：确定问题范围
内网 vs 外网：确定网络边界问题
不同DNS服务器：排除DNS问题
不同时间段：确定是否为间歇性问题
```

**🔹 工具组合使用**
```
单一工具有局限性，需要多工具组合验证
交叉验证结果，避免工具本身的问题
结合日志分析，形成完整的故障画像
```

### 8.3 实际应用技巧


**🎯 日常运维场景**
- **服务部署验证**：使用telnet测试端口，curl测试服务
- **网络故障排查**：ping+traceroute组合定位问题
- **性能问题分析**：mtr持续监控，识别网络瓶颈
- **DNS问题处理**：多DNS服务器对比，清除缓存

**🎯 开发调试场景**
- **API测试**：curl发送各种HTTP请求
- **数据库连接**：telnet测试数据库端口连通性
- **负载均衡验证**：多次curl请求验证分发策略
- **SSL证书检查**：curl -v查看证书信息

### 8.4 最佳实践建议


**📝 建立标准化流程**
- 制定标准的故障排查检查单
- 记录常用命令和参数组合
- 建立故障案例库和解决方案

**🔧 工具效率提升**
- 学习命令别名和快捷方式
- 掌握管道和过滤技巧
- 使用脚本自动化重复性工作

**核心记忆**：
- 网络调试遵循分层排查原则
- 工具组合使用效果更好
- 实践是掌握命令的最好方法
- 建立系统化的排查思维模式