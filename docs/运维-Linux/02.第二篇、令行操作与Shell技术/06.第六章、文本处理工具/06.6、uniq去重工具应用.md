---
title: 6、uniq去重工具应用
---
## 📚 目录

1. [uniq工具基础概念](#1-uniq工具基础概念)
2. [连续重复行去重基础用法](#2-连续重复行去重基础用法)
3. [计数统计功能详解](#3-计数统计功能详解)
4. [重复行与唯一行筛选](#4-重复行与唯一行筛选)
5. [字段与字符处理选项](#5-字段与字符处理选项)
6. [比较控制选项详解](#6-比较控制选项详解)
7. [uniq与sort组合应用](#7-uniq与sort组合应用)
8. [数据统计分析实战](#8-数据统计分析实战)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🔧 uniq工具基础概念


### 1.1 什么是uniq工具


> 💡 **核心理解**：uniq就像一个"连续重复检查器"，专门处理相邻的重复行

```
简单理解：
想象你有一摞试卷要检查，uniq就是帮你找出"连续相同"的试卷
注意：只能检查相邻的，不相邻的重复它发现不了！
```

**🎯 uniq的工作原理**
```
输入文件：              uniq处理后：
apple                   apple
apple          →        banana  
banana                  apple
apple                   orange
orange                  

解释：两个连续的apple变成1个，但后面单独的apple保留
```

### 1.2 为什么需要uniq


**实际应用场景**
- 📊 **日志分析**：去除重复的错误日志
- 📈 **数据统计**：统计重复出现的次数
- 🔍 **文件对比**：找出数据中的重复项
- 📋 **报表生成**：清理重复的数据记录

> ⚠️ **重要提醒**：uniq只能处理**连续相邻**的重复行，要处理所有重复行需要先用sort排序

---

## 2. 📝 连续重复行去重基础用法


### 2.1 基础语法格式


```bash
uniq [选项] [输入文件] [输出文件]
```

**🔸 最简单的使用方式**
```bash
# 直接去除连续重复行
uniq filename.txt

# 从标准输入处理
cat filename.txt | uniq
```

### 2.2 基础去重实例


**📋 创建测试文件**
```bash
cat > test.txt << EOF
apple
apple
banana
banana
banana
orange
apple
apple
EOF
```

**🔍 基础去重操作**
```bash
# 去除连续重复行
uniq test.txt
```

**输出结果**
```
apple
banana
orange
apple
```

> 💭 **理解要点**：最后的两个apple被保留了，因为它们和前面的apple不相邻

### 2.3 保存结果到文件


```bash
# 方法1：重定向输出
uniq test.txt > result.txt

# 方法2：直接指定输出文件
uniq test.txt result.txt

# 方法3：管道处理
cat test.txt | uniq > result.txt
```

---

## 3. 📊 计数统计功能详解


### 3.1 -c选项：显示重复次数


> 🎯 **功能解释**：-c选项会在每行前面显示该行连续出现的次数

**基础语法**
```bash
uniq -c filename.txt
```

**📈 实际示例**
```bash
# 使用测试文件
uniq -c test.txt
```

**输出结果**
```
   2 apple
   3 banana
   1 orange
   2 apple
```

**结果解读**
```
数字含义：
2 apple  ← apple连续出现2次
3 banana ← banana连续出现3次
1 orange ← orange出现1次
2 apple  ← 最后的apple连续出现2次
```

### 3.2 计数统计的实际应用


**🔍 日志分析示例**
```bash
# 创建日志文件
cat > access.log << EOF
192.168.1.1
192.168.1.1
192.168.1.2
192.168.1.2
192.168.1.2
192.168.1.3
EOF

# 统计连续访问次数
uniq -c access.log
```

**输出结果**
```
   2 192.168.1.1
   3 192.168.1.2
   1 192.168.1.3
```

**💡 实用技巧**
```bash
# 按访问次数排序（降序）
uniq -c access.log | sort -nr

# 只显示访问次数超过2次的IP
uniq -c access.log | awk '$1 > 2 {print $2}'
```

---

## 4. 🔍 重复行与唯一行筛选


### 4.1 -d选项：只显示重复行


> 🎯 **功能说明**：-d选项只显示那些**连续重复**的行，且只显示一次

**基础用法**
```bash
uniq -d filename.txt
```

**📋 示例演示**
```bash
# 使用测试文件
uniq -d test.txt
```

**输出结果**
```
apple
banana
apple
```

**解释**
```
为什么是这个结果：
- 第1个apple连续出现2次 → 显示apple
- banana连续出现3次 → 显示banana  
- orange只出现1次 → 不显示
- 最后apple连续出现2次 → 显示apple
```

### 4.2 -u选项：只显示唯一行


> 🎯 **功能说明**：-u选项只显示那些**没有重复**的行

**基础用法**
```bash
uniq -u filename.txt
```

**📋 示例演示**
```bash
uniq -u test.txt
```

**输出结果**
```
orange
```

**解释**：只有orange没有连续重复，所以只显示orange

### 4.3 重复行筛选的实际应用


**🔍 找出重复的配置项**
```bash
# 配置文件示例
cat > config.txt << EOF
server=web1
port=8080
port=8080
database=mysql
server=web2
timeout=30
EOF

# 找出重复的配置
uniq -d config.txt
```

**🔧 数据质量检查**
```bash
# 检查数据文件中的重复记录
sort data.csv | uniq -d

# 找出唯一的记录
sort data.csv | uniq -u
```

---

## 5. ⚙️ 字段与字符处理选项


### 5.1 -f选项：跳过字段


> 🎯 **功能解释**：-f选项让uniq在比较时跳过前面的N个字段（字段以空格分隔）

**基础语法**
```bash
uniq -f N filename.txt
```

**📋 实际示例**
```bash
# 创建含多字段的数据
cat > users.txt << EOF
001 john male
002 john male
003 mary female
004 mary female
005 bob male
EOF

# 跳过第1个字段（编号），基于姓名和性别去重
uniq -f 1 users.txt
```

**输出结果**
```
001 john male
003 mary female
005 bob male
```

**🔍 应用场景**
```bash
# 日志格式：时间戳 IP地址 操作
# 想基于IP和操作去重，跳过时间戳
uniq -f 1 access.log

# 文件格式：序号 用户名 邮箱
# 基于用户名和邮箱去重，跳过序号
uniq -f 1 user_list.txt
```

### 5.2 -s选项：跳过字符


> 🎯 **功能解释**：-s选项让uniq在比较时跳过每行开头的N个字符

**基础语法**
```bash
uniq -s N filename.txt
```

**📋 实际示例**
```bash
# 创建带前缀的数据
cat > items.txt << EOF
[INFO] Connection established
[INFO] Connection established
[ERROR] Connection failed
[ERROR] Connection timeout
[WARN] Low memory
EOF

# 跳过前7个字符（跳过日志级别），基于消息内容去重
uniq -s 7 items.txt
```

**输出结果**
```
[INFO] Connection established
[ERROR] Connection failed
[ERROR] Connection timeout
[WARN] Low memory
```

### 5.3 组合使用字段和字符选项


```bash
# 同时跳过2个字段和5个字符
uniq -f 2 -s 5 complex_data.txt

# 实际应用：处理复杂格式的日志
# 格式：日期 时间 [级别] 消息
# 跳过日期时间，基于级别和消息去重
uniq -f 2 server.log
```

---

## 6. 🔧 比较控制选项详解


### 6.1 -i选项：忽略大小写


> 🎯 **功能说明**：-i选项让uniq在比较时不区分大小写

**基础用法**
```bash
uniq -i filename.txt
```

**📋 示例演示**
```bash
# 创建大小写混合的数据
cat > mixed_case.txt << EOF
Apple
apple
APPLE
Banana
banana
Orange
EOF

# 不忽略大小写（默认）
uniq mixed_case.txt
```

**默认输出**
```
Apple
apple
APPLE
Banana
banana
Orange
```

**忽略大小写处理**
```bash
uniq -i mixed_case.txt
```

**输出结果**
```
Apple
Banana
Orange
```

### 6.2 -w选项：指定比较字符数


> 🎯 **功能说明**：-w选项只比较每行的前N个字符

**基础语法**
```bash
uniq -w N filename.txt
```

**📋 实际示例**
```bash
# 创建测试数据
cat > prefixes.txt << EOF
test001_file
test002_file
test003_data
prod001_file
prod002_file
EOF

# 只比较前4个字符
uniq -w 4 prefixes.txt
```

**输出结果**
```
test001_file
prod001_file
```

**解释**：
- `test`开头的行被当作相同（只比较前4字符）
- `prod`开头的行被当作相同

### 6.3 多选项组合使用


```bash
# 组合示例：忽略大小写 + 计数统计
uniq -ic mixed_case.txt

# 组合示例：跳过字段 + 计数统计
uniq -f 1 -c users.txt

# 组合示例：指定字符数 + 只显示重复行
uniq -w 3 -d data.txt
```

---

## 7. 🔄 uniq与sort组合应用


### 7.1 为什么需要sort配合


> ⚠️ **核心问题**：uniq只能处理相邻的重复行，要处理所有重复行必须先排序

**📋 问题演示**
```bash
# 创建分散重复的数据
cat > scattered.txt << EOF
apple
banana
apple
orange
banana
apple
EOF

# 单独使用uniq（效果不好）
uniq scattered.txt
```

**输出结果**
```
apple
banana
apple
orange
banana
apple
```

**说明**：因为重复行不相邻，uniq无法处理

### 7.2 sort + uniq 完整去重


**🔧 正确的处理方式**
```bash
# 先排序再去重
sort scattered.txt | uniq
```

**输出结果**
```
apple
banana
orange
```

**💡 常用组合命令**
```bash
# 1. 完全去重
sort file.txt | uniq

# 2. 去重并计数
sort file.txt | uniq -c

# 3. 去重并按计数排序
sort file.txt | uniq -c | sort -nr

# 4. 只显示重复的项
sort file.txt | uniq -d

# 5. 只显示唯一的项
sort file.txt | uniq -u
```

### 7.3 数值排序配合uniq


```bash
# 数值内容排序去重
cat > numbers.txt << EOF
10
2
10
1
2
100
EOF

# 按数值大小排序去重
sort -n numbers.txt | uniq -c
```

**输出结果**
```
   1 1
   2 2
   2 10
   1 100
```

---

## 8. 📈 数据统计分析实战


### 8.1 网站访问日志分析


**📋 创建模拟日志**
```bash
cat > access_log.txt << EOF
192.168.1.1 GET /index.html
192.168.1.1 GET /index.html
192.168.1.2 GET /about.html
192.168.1.1 POST /login
192.168.1.2 GET /about.html
192.168.1.3 GET /index.html
192.168.1.2 GET /contact.html
192.168.1.3 GET /index.html
EOF
```

**🔍 分析访问最频繁的IP**
```bash
# 提取IP地址，排序，统计
awk '{print $1}' access_log.txt | sort | uniq -c | sort -nr
```

**输出结果**
```
   3 192.168.1.1
   3 192.168.1.2
   2 192.168.1.3
```

**🔍 分析最受欢迎的页面**
```bash
# 提取页面路径，排序，统计
awk '{print $3}' access_log.txt | sort | uniq -c | sort -nr
```

### 8.2 用户行为分析


**📊 统计用户操作类型**
```bash
# 提取HTTP方法，统计频次
awk '{print $2}' access_log.txt | sort | uniq -c
```

**🔧 找出异常访问模式**
```bash
# 找出只访问一次的IP（可能是异常）
awk '{print $1}' access_log.txt | sort | uniq -u

# 找出重复访问的IP
awk '{print $1}' access_log.txt | sort | uniq -d
```

### 8.3 系统监控数据处理


**📋 CPU使用率统计**
```bash
# 模拟CPU监控数据
cat > cpu_usage.txt << EOF
server1 85%
server1 85%
server1 90%
server2 75%
server2 75%
server2 80%
server3 95%
EOF

# 去重并显示最后状态
sort cpu_usage.txt | uniq
```

**⚡ 高级数据分析**
```bash
# 统计每台服务器的监控记录数
awk '{print $1}' cpu_usage.txt | sort | uniq -c

# 找出CPU使用率变化的服务器
sort cpu_usage.txt | uniq -u
```

### 8.4 文件内容去重与分析


**📁 代码文件去重**
```bash
# 去除代码文件中的重复行（常用于配置文件清理）
sort config.ini | uniq > config_clean.ini

# 找出配置文件中的重复配置项
sort config.ini | uniq -d
```

**🔍 数据质量检查**
```bash
# 检查CSV文件中的重复记录
sort data.csv | uniq -d > duplicate_records.csv

# 生成清理后的数据文件
sort data.csv | uniq > clean_data.csv

# 统计清理前后的记录数差异
echo "原始记录数: $(wc -l < data.csv)"
echo "去重后记录数: $(sort data.csv | uniq | wc -l)"
echo "重复记录数: $(sort data.csv | uniq -d | wc -l)"
```

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的核心概念


```
🔸 uniq本质：只处理连续相邻的重复行
🔸 基础去重：直接使用 uniq filename.txt
🔸 计数统计：使用 -c 选项显示重复次数
🔸 重复筛选：-d 显示重复行，-u 显示唯一行
🔸 字段控制：-f 跳过字段，-s 跳过字符
🔸 比较选项：-i 忽略大小写，-w 指定比较字符数
🔸 完整去重：必须 sort + uniq 组合使用
🔸 数据分析：统计、排序、筛选的综合应用
```

### 9.2 关键理解要点


**🔹 uniq的工作机制**
```
uniq的限制：只能检查相邻行
解决方案：先用sort排序，再用uniq去重
记忆要点：uniq = "相邻行去重器"
```

**🔹 选项组合使用**
```
单一功能：每个选项解决特定问题
组合威力：多选项组合解决复杂需求
常用组合：-c计数、-d重复、-u唯一
实际应用：根据需求灵活选择和组合
```

**🔹 实际应用价值**
- **日志分析**：找出重复的错误、统计访问频次
- **数据清理**：去除重复记录、检查数据质量
- **系统监控**：分析重复告警、统计状态变化
- **文本处理**：配置文件去重、内容统计分析

### 9.3 使用技巧与最佳实践


**💡 记忆技巧**
```
选项记忆法：
-c = count（计数）
-d = duplicate（重复）
-u = unique（唯一）
-f = fields（字段）
-s = skip（跳过）
-i = ignore case（忽略大小写）
-w = width（宽度/字符数）
```

**⚡ 实用命令模板**
```bash
# 完全去重模板
sort file.txt | uniq

# 统计重复模板  
sort file.txt | uniq -c | sort -nr

# 数据分析模板
awk '{print $N}' file.txt | sort | uniq -c | sort -nr

# 质量检查模板
sort file.txt | uniq -d  # 找重复
sort file.txt | uniq -u  # 找唯一
```

**🔧 常见问题解决**
```
问题1：uniq没有去重所有重复行
解决：先用sort排序

问题2：需要统计重复次数
解决：使用-c选项

问题3：需要基于部分内容去重
解决：使用-f跳过字段或-s跳过字符

问题4：大小写导致的重复识别问题
解决：使用-i忽略大小写
```

**核心记忆口诀**：
- uniq去重看相邻，sort配合才完整
- 计数统计用-c，重复唯一-d和-u  
- 跳过字段用-f，跳过字符用-s
- 忽略大小写-i，指定字符数-w
- 数据分析组合用，统计排序显威力