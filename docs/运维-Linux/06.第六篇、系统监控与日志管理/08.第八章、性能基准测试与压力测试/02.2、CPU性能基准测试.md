---
title: 2、CPU性能基准测试
---
## 📚 目录

1. [CPU性能基准测试概述](#1-CPU性能基准测试概述)
2. [sysbench CPU测试模块](#2-sysbench-CPU测试模块)
3. [stress/stress-ng CPU压力测试](#3-stress-stress-ng-CPU压力测试)
4. [UnixBench综合CPU测试](#4-UnixBench综合CPU测试)
5. [prime95质数计算测试](#5-prime95质数计算测试)
6. [CPU核心数与频率测试](#6-CPU核心数与频率测试)
7. [浮点运算性能测试](#7-浮点运算性能测试)
8. [多线程并发性能测试](#8-多线程并发性能测试)
9. [CPU缓存性能评估](#9-CPU缓存性能评估)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🔍 CPU性能基准测试概述


### 1.1 什么是CPU性能基准测试


**基本概念**：CPU性能基准测试就是通过运行特定的程序和工作负载，来测量和评估CPU的各项性能指标。

```
简单理解：
就像给汽车测试马力一样，CPU基准测试是给处理器"跑分"
看看它在各种计算任务下的表现如何
```

**核心目标**：
- **性能评估**：了解CPU的真实计算能力
- **瓶颈识别**：找出系统性能瓶颈所在
- **配置优化**：为系统调优提供数据支撑
- **硬件选型**：为硬件采购提供参考依据

### 1.2 CPU性能测试的维度


**🔸 计算密集型测试**
```
测试内容：纯CPU计算能力
典型场景：数学运算、算法执行、数据处理
测试工具：sysbench、stress等
```

**🔸 多线程并发测试**
```
测试内容：多核心协同工作能力
典型场景：并行计算、多任务处理
测试工具：stress-ng、UnixBench等
```

**🔸 缓存性能测试**
```
测试内容：CPU缓存系统效率
典型场景：内存访问模式、数据局部性
测试工具：专用缓存测试程序
```

### 1.3 测试环境准备


**🛠️ 环境要求**
- **稳定的系统环境**：避免其他程序干扰
- **充足的散热条件**：防止CPU过热降频
- **足够的运行时间**：确保测试结果稳定可靠

**⚠️ 注意事项**
- 测试前关闭不必要的后台服务
- 确保系统有充足的内存空间
- 注意CPU温度，避免过热保护触发

---

## 2. ⚡ sysbench CPU测试模块


### 2.1 sysbench简介


**工具定位**：sysbench是一个多功能的系统性能测试工具，其中CPU模块专门用于测试处理器的计算性能。

```
sysbench的优势：
✅ 使用简单，命令行操作
✅ 结果标准化，便于对比
✅ 支持多线程测试
✅ 跨平台兼容性好
```

### 2.2 安装sysbench


```bash
# CentOS/RHEL系统
yum install sysbench

# Ubuntu/Debian系统  
apt-get install sysbench

# 验证安装
sysbench --version
```

### 2.3 基本CPU测试


**🔸 单线程CPU测试**
```bash
# 基本语法
sysbench cpu run

# 指定测试时间（30秒）
sysbench cpu --time=30 run

# 指定运算次数
sysbench cpu --cpu-max-prime=10000 run
```

**测试结果解读**：
```
测试输出示例：
CPU speed:
    events per second: 1524.67    ← 每秒完成的运算次数
    
General statistics:
    total time: 10.0002s          ← 总测试时间
    total number of events: 15248 ← 总完成运算次数
    
Latency (ms):
    min: 0.65    ← 最小延迟
    avg: 0.66    ← 平均延迟  
    max: 1.23    ← 最大延迟
```

### 2.4 多线程CPU测试


**🔸 多线程并发测试**
```bash
# 2个线程并发测试
sysbench cpu --threads=2 --time=60 run

# 等于CPU核心数的线程测试
sysbench cpu --threads=$(nproc) --time=60 run

# 测试不同线程数的性能
for i in 1 2 4 8; do
  echo "Testing with $i threads:"
  sysbench cpu --threads=$i --time=30 run | grep "events per second"
done
```

**性能分析要点**：
- **线性扩展**：理想情况下，线程数翻倍，性能也应该翻倍
- **扩展瓶颈**：当增加线程不再提升性能时，说明遇到瓶颈
- **最佳线程数**：通常等于或略大于CPU核心数

### 2.5 高级测试选项


**🔧 详细配置参数**
```bash
# 完整的测试命令示例
sysbench cpu \
  --cpu-max-prime=20000 \    # 设置质数计算上限
  --threads=4 \              # 使用4个线程
  --time=120 \               # 运行120秒
  --report-interval=10 \     # 每10秒报告一次进度
  run
```

| 参数 | 说明 | 默认值 | 建议设置 |
|------|------|--------|----------|
| `--cpu-max-prime` | 质数计算上限 | 10000 | 10000-50000 |
| `--threads` | 并发线程数 | 1 | CPU核心数 |
| `--time` | 测试时长(秒) | 10 | 60-300 |
| `--report-interval` | 报告间隔(秒) | 0 | 10-30 |

---

## 3. 💪 stress/stress-ng CPU压力测试


### 3.1 stress与stress-ng对比


**🔸 工具定位差异**
```
stress（传统版本）：
- 基础的压力测试工具
- 功能相对简单
- 适合基本的CPU负载测试

stress-ng（新一代版本）：
- 功能更强大，测试种类更多
- 支持更多的压力测试模式
- 提供更详细的测试报告
```

### 3.2 安装压力测试工具


```bash
# 安装stress
yum install stress          # CentOS/RHEL
apt-get install stress      # Ubuntu/Debian

# 安装stress-ng（推荐）
yum install stress-ng       # CentOS/RHEL
apt-get install stress-ng   # Ubuntu/Debian
```

### 3.3 基础CPU压力测试


**🔸 使用stress进行基本测试**
```bash
# 启动4个CPU密集型进程，运行60秒
stress --cpu 4 --timeout 60s

# 同时测试CPU和内存
stress --cpu 2 --vm 1 --vm-bytes 1G --timeout 60s

# 查看系统负载变化
top    # 在另一个终端监控
```

**🔸 使用stress-ng进行高级测试**
```bash
# 基本CPU压力测试
stress-ng --cpu 4 --timeout 60s

# 指定特定的CPU测试方法
stress-ng --cpu 4 --cpu-method pi --timeout 60s

# 显示详细信息
stress-ng --cpu 4 --metrics-brief --timeout 60s
```

### 3.4 CPU测试方法详解


**stress-ng支持的CPU测试方法**：

| 测试方法 | 说明 | 适用场景 |
|----------|------|----------|
| `pi` | π值计算 | 浮点运算测试 |
| `prime` | 质数计算 | 整数运算测试 |
| `fibonacci` | 斐波那契数列 | 递归计算测试 |
| `matrix` | 矩阵运算 | 内存+计算综合测试 |
| `sqrt` | 平方根计算 | 数学函数测试 |

```bash
# 测试不同的CPU计算方法
stress-ng --cpu 2 --cpu-method pi --timeout 30s
stress-ng --cpu 2 --cpu-method prime --timeout 30s
stress-ng --cpu 2 --cpu-method matrix --timeout 30s
```

### 3.5 监控CPU使用率


**🔍 实时监控命令**
```bash
# 方法1：使用top命令
top -p $(pgrep stress)

# 方法2：使用htop（更直观）
htop

# 方法3：使用sar监控
sar -u 1 10    # 每秒采样，共10次

# 方法4：使用vmstat
vmstat 1 10    # 每秒采样，共10次
```

**监控要点**：
- **CPU使用率**：应该接近100%（压力测试目标）
- **负载平均值**：应该接近或等于CPU核心数
- **温度监控**：确保CPU不会过热

---

## 4. 📊 UnixBench综合CPU测试


### 4.1 UnixBench测试套件介绍


**工具特点**：UnixBench是一个综合性的系统性能测试套件，包含多个测试项目，能够全面评估CPU的各项性能。

```
UnixBench测试项目：
🔸 Dhrystone：整数运算性能
🔸 Whetstone：浮点运算性能  
🔸 Execl Throughput：进程创建性能
🔸 File Copy：文件操作性能
🔸 Pipe Throughput：管道通信性能
🔸 Context Switching：上下文切换性能
🔸 System Call Overhead：系统调用开销
```

### 4.2 安装UnixBench


```bash
# 下载源码
wget https://github.com/kdlucas/byte-unixbench/archive/v5.1.3.tar.gz
tar -xzf v5.1.3.tar.gz
cd byte-unixbench-5.1.3/UnixBench

# 编译安装
make

# 或者使用包管理器（某些发行版）
yum install unixbench
```

### 4.3 运行UnixBench测试


**🔸 基本测试命令**
```bash
# 运行完整测试套件
./Run

# 指定并发线程数
./Run -c 4

# 运行特定测试项目
./Run dhry2reg whetstone-double
```

**🔸 测试结果解读**
```
典型输出格式：
========================================================================
   BYTE UNIX Benchmarks (Version 5.1.3)

   System: hostname: 4 CPUs, Intel Core i7-8700 @ 3.20GHz
   
   Benchmark Run: 2025-09-15 14:30:00
   4 CPUs in system; running 1 parallel copy of tests

Benchmark                     Result     Index    Baseline   
Dhrystone 2 using register   45123.5    1234.5   (116700.0)
Double-Precision Whetstone    3456.7     890.2    (55.0)    
Execl Throughput              2345.6     543.2    (43.0)    
...
========================================================================
FINAL SCORE                            876.4
```

### 4.4 性能指标分析


**🔍 关键指标含义**：

| 测试项目 | 测量内容 | 单位 | 说明 |
|----------|----------|------|------|
| **Dhrystone** | 整数运算速度 | lps | 每秒循环次数，越高越好 |
| **Whetstone** | 浮点运算速度 | MWIPS | 百万指令/秒，越高越好 |
| **Execl Throughput** | 进程创建效率 | lps | 每秒执行次数，越高越好 |
| **System Call** | 系统调用开销 | lps | 每秒调用次数，越高越好 |

**性能对比方法**：
- **Index值**：相对于基准系统的性能倍数
- **Final Score**：综合性能得分
- **基线对比**：与标准配置的性能对比

---

## 5. 🧮 prime95质数计算测试


### 5.1 prime95测试原理


**工具背景**：prime95原本是用于寻找梅森质数的分布式计算项目，因其对CPU要求极高，成为了事实上的CPU稳定性和性能测试标准。

```
prime95的测试特点：
🔸 极高的CPU利用率（接近100%）
🔸 长时间稳定性测试
🔸 温度压力测试
🔸 超频稳定性验证
```

### 5.2 安装和配置prime95


```bash
# 下载prime95 Linux版本
wget https://www.mersenne.org/download/software/v30/30.8/p95v308b15.linux64.tar.gz
tar -xzf p95v308b15.linux64.tar.gz
cd p95v308b15.linux64

# 运行prime95
./mprime
```

**🔧 基本配置选项**：
```
初次运行配置向导：
1. Join GIMPS? [Y]            ← 是否加入分布式计算项目
2. Use PrimeNet? [Y]          ← 是否使用网络功能  
3. CPU type? [1-8]            ← 选择CPU类型
4. Memory to use? [MB]        ← 设置使用内存大小
5. Time limit? [minutes]      ← 设置测试时间限制
```

### 5.3 不同测试模式


**🔸 测试模式选择**
```
1. Small FFTs             ← 测试CPU和缓存，功耗最高
2. Large FFTs             ← 测试CPU、缓存和内存控制器
3. Blend (default)        ← 综合测试，平衡CPU和内存
4. Custom                 ← 自定义测试参数
```

**实际测试命令**：
```bash
# 启动测试（交互式）
./mprime

# 非交互式测试（需要预先配置）
./mprime -t    # 运行torture test
```

### 5.4 监控和结果分析


**🔍 监控要点**
```bash
# 监控CPU温度
sensors    # 需要安装lm-sensors

# 监控系统负载
watch -n 1 'cat /proc/loadavg'

# 监控内存使用
free -h

# 监控频率和温度
watch -n 1 'cat /proc/cpuinfo | grep MHz'
```

**测试结果判断**：
- **通过标准**：运行指定时间无错误、无重启
- **性能指标**：完成的迭代次数和速度
- **稳定性**：温度控制和频率稳定性

---

## 6. ⚙️ CPU核心数与频率测试


### 6.1 CPU基本信息查看


**🔍 核心硬件信息获取**
```bash
# 查看CPU详细信息
cat /proc/cpuinfo

# 查看CPU核心数
nproc                          # 逻辑CPU数量
nproc --all                    # 所有CPU数量
lscpu                          # 详细CPU架构信息

# 查看物理CPU信息
lscpu | grep -E 'CPU\(s\)|Thread|Core|Socket'
```

**输出信息解读**：
```
lscpu输出示例：
Architecture:        x86_64           ← CPU架构
CPU(s):             8                ← 逻辑CPU总数
Thread(s) per core: 2                ← 每核心线程数（超线程）
Core(s) per socket: 4                ← 每插槽核心数
Socket(s):          1                ← CPU插槽数

实际核心数 = Socket × Core per socket = 1 × 4 = 4个物理核心
逻辑核心数 = 物理核心 × 超线程 = 4 × 2 = 8个逻辑核心
```

### 6.2 CPU频率监控


**🔸 实时频率查看**
```bash
# 查看当前频率
cat /proc/cpuinfo | grep MHz

# 实时监控频率变化
watch -n 1 'cat /proc/cpuinfo | grep MHz'

# 使用cpufreq工具
cpufreq-info                   # 显示频率信息
cpufreq-info -f                # 只显示当前频率
```

**🔸 频率调节策略**
```bash
# 查看当前调节策略
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor

# 查看支持的调节策略
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_available_governors

# 常见调节策略：
# performance  ← 性能优先，固定最高频率
# powersave    ← 节能优先，固定最低频率  
# ondemand     ← 按需调节，根据负载自动调整
# conservative ← 保守调节，频率变化较缓慢
```

### 6.3 核心性能测试


**🔸 单核心vs多核心性能对比**
```bash
# 单核心测试
taskset -c 0 sysbench cpu --time=30 run

# 多核心测试
sysbench cpu --threads=$(nproc) --time=30 run

# 逐核心测试
for i in $(seq 0 $(($(nproc)-1))); do
  echo "Testing CPU core $i:"
  taskset -c $i sysbench cpu --time=10 run | grep "events per second"
done
```

**性能扩展分析**：
```
理想情况下的性能扩展：
单核心性能：1000 events/sec
双核心理论性能：2000 events/sec  
四核心理论性能：4000 events/sec

实际扩展效率 = 实际性能 / 理论性能 × 100%
```

---

## 7. 🧮 浮点运算性能测试


### 7.1 浮点运算基础概念


**浮点运算重要性**：浮点运算能力直接影响科学计算、图形处理、机器学习等应用的性能表现。

```
浮点运算类型：
🔸 单精度浮点（float）：32位，精度较低，速度较快
🔸 双精度浮点（double）：64位，精度较高，计算更精确
🔸 SIMD指令：同时处理多个浮点数，提升并行效率
```

### 7.2 专用浮点性能测试


**🔸 使用sysbench测试浮点性能**
```bash
# 虽然sysbench主要测试整数，但可以观察浮点表现
sysbench cpu --cpu-max-prime=20000 --time=60 run
```

**🔸 使用stress-ng测试浮点运算**
```bash
# 专门的浮点运算测试
stress-ng --cpu 4 --cpu-method pi --timeout 60s
stress-ng --cpu 4 --cpu-method euler --timeout 60s

# 矩阵运算（浮点密集型）
stress-ng --matrix 4 --timeout 60s
```

### 7.3 自定义浮点测试脚本


**🔧 简单的浮点测试程序**
```bash
# 创建浮点测试脚本
cat > float_test.c << 'EOF'
#include <stdio.h>
#include <time.h>
#include <math.h>

int main() {
    int iterations = 10000000;
    double result = 0.0;
    clock_t start = clock();
    
    for(int i = 0; i < iterations; i++) {
        result += sqrt(i * 3.14159) * sin(i);
    }
    
    clock_t end = clock();
    double time_spent = ((double)(end - start)) / CLOCKS_PER_SEC;
    
    printf("浮点运算测试完成\n");
    printf("迭代次数: %d\n", iterations);
    printf("耗时: %.2f 秒\n", time_spent);
    printf("每秒运算次数: %.0f\n", iterations / time_spent);
    
    return 0;
}
EOF

# 编译和运行
gcc -O2 -lm float_test.c -o float_test
./float_test
```

### 7.4 SIMD指令集支持检查


**🔍 检查CPU支持的指令集**
```bash
# 查看支持的指令集
cat /proc/cpuinfo | grep flags

# 检查特定指令集支持
grep -o 'sse\|sse2\|sse3\|ssse3\|sse4\|avx\|avx2\|fma' /proc/cpuinfo | sort | uniq

# 使用lscpu查看
lscpu | grep -i flags
```

**常见指令集说明**：
- **SSE/SSE2**：基础SIMD指令集
- **AVX/AVX2**：高级向量扩展，提升浮点性能
- **FMA**：融合乘加运算，提高浮点精度和速度

---

## 8. 🔄 多线程并发性能测试


### 8.1 并发性能测试的意义


**为什么要测试并发性能**：现代应用大多采用多线程架构，CPU的多核心协同工作能力直接影响整体性能。

```
并发性能影响因素：
🔸 核心数量：物理核心数决定真正的并行能力
🔸 超线程技术：逻辑核心可以提升某些场景的性能
🔸 缓存共享：核心间缓存共享机制影响协同效率
🔸 内存带宽：多核心同时访问内存的带宽限制
```

### 8.2 多线程压力测试


**🔸 使用sysbench进行阶梯式测试**
```bash
#!/bin/bash
# 多线程性能测试脚本

echo "多线程CPU性能测试"
echo "=================="

for threads in 1 2 4 8 16; do
    echo "测试线程数: $threads"
    result=$(sysbench cpu --threads=$threads --time=30 run | grep "events per second")
    echo "$result"
    echo "----------------"
    sleep 2
done
```

**🔸 使用stress-ng测试并发负载**
```bash
# 测试不同并发级别
stress-ng --cpu 1 --timeout 30s --metrics-brief
stress-ng --cpu 2 --timeout 30s --metrics-brief  
stress-ng --cpu 4 --timeout 30s --metrics-brief
stress-ng --cpu 8 --timeout 30s --metrics-brief
```

### 8.3 并发性能分析


**🔍 性能扩展效率计算**
```
计算公式：
扩展效率 = (N线程性能 / 单线程性能) / N × 100%

示例分析：
单线程：1000 events/sec
双线程：1800 events/sec
扩展效率 = (1800/1000) / 2 × 100% = 90%

理想情况：100%（完美线性扩展）
实际情况：70-90%（受限于各种因素）
```

**性能瓶颈分析**：
- **90%以上**：优秀的扩展性
- **70-90%**：良好的扩展性  
- **50-70%**：一般的扩展性
- **50%以下**：存在明显瓶颈

### 8.4 线程亲和性测试


**🔧 CPU亲和性绑定测试**
```bash
# 绑定到特定CPU核心
taskset -c 0,1 stress-ng --cpu 2 --timeout 30s

# 绑定到指定NUMA节点
numactl --cpunodebind=0 stress-ng --cpu 4 --timeout 30s

# 查看NUMA拓扑
numactl --hardware
lscpu | grep NUMA
```

---

## 9. 🗃️ CPU缓存性能评估


### 9.1 CPU缓存架构理解


**缓存层次结构**：
```
CPU缓存层次（从快到慢）：
L1 Cache (Level 1)
├── L1i Cache：指令缓存（32-64KB）
└── L1d Cache：数据缓存（32-64KB）
    ↓
L2 Cache (Level 2)：统一缓存（256KB-1MB）
    ↓  
L3 Cache (Level 3)：共享缓存（8-32MB）
    ↓
主内存（DDR4/DDR5）
```

**缓存性能指标**：
- **缓存命中率**：访问数据在缓存中找到的比例
- **缓存延迟**：从缓存读取数据的时间
- **缓存带宽**：单位时间内缓存传输的数据量

### 9.2 查看缓存信息


**🔍 获取缓存架构信息**
```bash
# 查看缓存详细信息
lscpu | grep -i cache

# 查看缓存大小
cat /sys/devices/system/cpu/cpu0/cache/index*/size

# 查看缓存类型和级别
for i in /sys/devices/system/cpu/cpu0/cache/index*; do
    echo "缓存级别: $(cat $i/level)"
    echo "缓存类型: $(cat $i/type)"  
    echo "缓存大小: $(cat $i/size)"
    echo "------------"
done
```

### 9.3 缓存性能测试工具


**🔸 使用stream测试内存带宽**
```bash
# 下载和编译stream
wget https://www.cs.virginia.edu/stream/FTP/Code/stream.c
gcc -O2 -fopenmp stream.c -o stream

# 运行测试
export OMP_NUM_THREADS=4
./stream
```

**🔸 缓存延迟测试**
```bash
# 使用lat_mem_rd测试（lmbench套件）
lat_mem_rd 1M 128    # 测试1MB内存，128字节步长
lat_mem_rd 8M 128    # 测试8MB内存
lat_mem_rd 32M 128   # 测试32MB内存
```

### 9.4 缓存友好性测试


**🔧 数据访问模式测试**
```c
// 缓存友好访问模式测试
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define SIZE 1024*1024*10  // 10MB数据

int main() {
    int *array = malloc(SIZE * sizeof(int));
    clock_t start, end;
    
    // 顺序访问（缓存友好）
    start = clock();
    for(int i = 0; i < SIZE; i++) {
        array[i] = i;
    }
    end = clock();
    printf("顺序访问时间: %.2f ms\n", 
           ((double)(end-start) / CLOCKS_PER_SEC) * 1000);
    
    // 随机访问（缓存不友好）
    start = clock();
    for(int i = 0; i < SIZE; i++) {
        int idx = rand() % SIZE;
        array[idx] = idx;
    }
    end = clock();
    printf("随机访问时间: %.2f ms\n", 
           ((double)(end-start) / CLOCKS_PER_SEC) * 1000);
    
    free(array);
    return 0;
}
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


```
🔸 基准测试目的：评估CPU性能，识别瓶颈，指导优化
🔸 sysbench：标准化的CPU计算性能测试工具
🔸 stress/stress-ng：CPU压力测试和稳定性验证工具
🔸 UnixBench：综合性能测试套件，多维度评估
🔸 prime95：极限稳定性测试，超频验证标准
🔸 多线程测试：评估多核心协同工作能力
🔸 缓存性能：影响实际应用性能的关键因素
```

### 10.2 关键理解要点


**🔹 测试工具选择原则**
```
日常性能评估：
→ sysbench（标准化、易比较）

压力测试：  
→ stress-ng（功能丰富、测试全面）

综合评估：
→ UnixBench（多维度、行业标准）

稳定性验证：
→ prime95（极限测试、超频验证）
```

**🔹 性能指标理解**
```
单线程性能：
- 反映CPU单核心计算能力
- 影响单任务应用性能

多线程性能：
- 反映多核心协同能力  
- 影响并发应用性能

扩展效率：
- 多线程性能提升比例
- 理想值接近100%

缓存性能：
- 影响实际应用表现
- 比纯计算能力更重要
```

**🔹 测试环境要求**
```
环境准备：
✅ 关闭不必要的后台服务
✅ 确保充足的散热条件
✅ 稳定的电源供应

监控要点：
✅ CPU使用率和负载
✅ 温度和频率变化
✅ 内存使用情况
✅ 系统稳定性
```

### 10.3 实际应用指导


**🎯 性能基准建立流程**
1. **环境准备**：清理系统，确保测试环境干净
2. **基础测试**：使用sysbench建立性能基线
3. **压力测试**：使用stress-ng验证稳定性
4. **综合评估**：运行UnixBench获得综合得分
5. **专项测试**：针对特定需求进行专项测试

**⚙️ 性能优化方向**
```
硬件层面：
- CPU频率调节策略优化
- 内存和缓存配置优化
- 散热系统改善

软件层面：
- 编译器优化选项
- 程序多线程架构优化
- 数据访问模式优化
```

**📊 性能对比分析**
```
横向对比：
- 不同硬件配置性能差异
- 同类产品性能排名

纵向对比：
- 优化前后性能提升
- 系统升级效果评估

场景对比：
- 不同工作负载下的表现
- 实际应用性能相关性
```

### 10.4 常见问题与解决


**❗ 测试过程常见问题**
```
测试结果不稳定：
→ 检查后台进程干扰
→ 确保充足测试时间
→ 监控温度和频率变化

性能低于预期：
→ 检查CPU频率策略设置
→ 确认BIOS设置正确
→ 检查散热和电源供应

多线程扩展性差：
→ 检查NUMA配置
→ 确认超线程设置
→ 分析缓存共享瓶颈
```

**核心记忆要点**：
- CPU基准测试是性能优化的基础，选对工具事半功倍
- 单线程看核心能力，多线程看协同效率，缓存看实际表现
- 测试环境要干净，监控数据要全面，结果分析要客观
- 性能数字要结合实际应用场景，避免为了跑分而跑分