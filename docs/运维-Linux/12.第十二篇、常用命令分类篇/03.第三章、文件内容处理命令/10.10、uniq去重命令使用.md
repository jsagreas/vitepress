---
title: 10、uniq去重命令使用
---
## 📚 目录


1. [uniq命令基础概念](#1-uniq命令基础概念)
2. [相邻重复行去重](#2-相邻重复行去重)
3. [显示重复行统计](#3-显示重复行统计)
4. [忽略大小写选项](#4-忽略大小写选项)
5. [指定比较字段](#5-指定比较字段)
6. [与sort命令配合使用](#6-与sort命令配合使用)
7. [重复行计数显示](#7-重复行计数显示)
8. [实际数据处理场景](#8-实际数据处理场景)
9. [核心要点总结](#9-核心要点总结)

---

# 🎯 **学习路径导航**


**前置知识**：需要掌握基本文件操作、管道概念 → **当前内容**：uniq去重命令 → **后续学习**：建议学习awk文本处理、grep模式匹配

⏱️ **预计学习时间**：本章预计40分钟 | 实践练习20分钟

---

## 1. 🔧 uniq命令基础概念



### 1.1 什么是uniq命令



**🔸 核心定义**
uniq命令是Linux中专门用于**去除相邻重复行**的文本处理工具。注意关键词是"相邻"，这个特点决定了uniq的使用方式。

**💭 生活化理解**
想象你在整理一摞相同主题的报纸：
- **uniq的作用**：把紧挨着的重复报纸拿掉，只留一份
- **重要限制**：只能处理挨在一起的重复，分开的重复无法处理
- **实际应用**：通常需要先排序，让重复内容聚集到一起

### 1.2 uniq命令的基本语法



**📋 命令格式**
```bash
uniq [选项] [输入文件] [输出文件]
```

**🔹 常用选项一览**

| **选项** | **作用** | **使用场景** |
|---------|---------|-------------|
| `-c` | 显示每行出现次数 | 统计重复频率 |
| `-d` | 只显示重复的行 | 找出重复内容 |
| `-u` | 只显示唯一的行 | 过滤重复项 |
| `-i` | 忽略大小写 | 不区分大小写去重 |
| `-f N` | 跳过前N个字段 | 按指定字段比较 |
| `-s N` | 跳过前N个字符 | 按字符位置比较 |

### 1.3 uniq的工作原理



**🔄 处理流程图示**
```
原始数据：         uniq处理：        结果：
apple             ┌─────────┐       apple
apple      ────→  │  uniq   │ ────→ banana  
banana            │  处理   │       orange
orange            └─────────┘       apple
apple
```

**⚠️ 重要提醒**
uniq只能去除**相邻**的重复行！上面例子中最后的apple不会被去除，因为它与前面的apple不相邻。

---

## 2. 🗂️ 相邻重复行去重



### 2.1 基本去重操作



**📝 创建测试文件**
```bash
# 创建包含重复行的文件

cat > fruits.txt << EOF
apple
apple
banana
banana
banana
orange
apple
apple
EOF
```

**🔧 基本去重命令**
```bash
# 去除相邻重复行

uniq fruits.txt
```

**📊 输出结果**
```
apple
banana
orange
apple
```

> 💡 **理解要点**：注意最后的apple保留了，因为它与前面的apple行不相邻

### 2.2 直接输出到文件



**📁 保存去重结果**
```bash
# 将去重结果保存到新文件

uniq fruits.txt unique_fruits.txt

# 或使用重定向

uniq fruits.txt > clean_fruits.txt
```

### 2.3 管道配合使用



**🔗 与其他命令结合**
```bash
# 从命令输出中去重

cat fruits.txt | uniq

# 多重处理

cat fruits.txt | sort | uniq
```

---

## 3. 📊 显示重复行统计



### 3.1 统计每行出现次数



**🔢 使用-c选项进行计数**
```bash
# 显示每行的出现次数

uniq -c fruits.txt
```

**📋 输出格式解读**
```
      2 apple      ← 连续出现2次
      3 banana     ← 连续出现3次  
      1 orange     ← 出现1次
      2 apple      ← 又连续出现2次
```

> 📌 **格式说明**：输出格式为"次数 内容"，次数右对齐显示

### 3.2 统计结果的进一步处理



**📈 按出现频率排序**
```bash
# 按出现次数从高到低排序

uniq -c fruits.txt | sort -nr

# 按出现次数从低到高排序  

uniq -c fruits.txt | sort -n
```

**🔍 找出出现最多的行**
```bash
# 显示出现次数最多的行

uniq -c fruits.txt | sort -nr | head -1
```

---

## 4. 🔤 忽略大小写选项



### 4.1 大小写敏感的默认行为



**📝 创建大小写混合文件**
```bash
cat > mixed_case.txt << EOF
Apple
apple
APPLE
Banana
banana
EOF
```

**🔍 默认处理结果**
```bash
uniq mixed_case.txt
# 输出：全部保留，因为大小写不同被视为不同行

```

### 4.2 忽略大小写去重



**🔤 使用-i选项**
```bash
# 忽略大小写进行去重

uniq -i mixed_case.txt
```

**📊 输出结果**
```
Apple     ← 保留第一个遇到的形式
Banana    ← 保留第一个遇到的形式
```

### 4.3 大小写统计应用



**📈 不区分大小写的频率统计**
```bash
# 统计时忽略大小写

uniq -i -c mixed_case.txt
```

**💼 实际应用场景**
- **日志分析**：统计错误信息时不区分大小写
- **用户输入**：处理用户名或邮箱时的大小写变体
- **文本清理**：标准化文本格式

---

## 5. 🎯 指定比较字段



### 5.1 跳过字段进行比较



**📝 创建多字段测试文件**
```bash
cat > users.txt << EOF
2023-01-01 john admin
2023-01-02 john user
2023-01-03 mary admin
2023-01-04 mary admin
2023-01-05 bob user
EOF
```

**🔧 按用户名去重（跳过日期字段）**
```bash
# -f 1: 跳过第1个字段（日期），按用户名比较

uniq -f 1 users.txt
```

**📊 输出结果分析**
```
2023-01-01 john admin    ← 保留第一个john
2023-01-03 mary admin    ← 保留第一个mary  
2023-01-05 bob user      ← 保留bob
```

### 5.2 跳过字符进行比较



**🔤 使用-s选项跳过字符**
```bash
cat > codes.txt << EOF
ID001-apple
ID002-apple
ID003-banana
ID004-banana
EOF

# -s 6: 跳过前6个字符（ID和编号），按后面内容比较

uniq -s 6 codes.txt
```

### 5.3 组合字段和字符跳过



**🎛️ 复杂比较条件**
```bash
# 既跳过字段又跳过字符

uniq -f 1 -s 3 complex_data.txt

# 结合计数功能

uniq -f 1 -c users.txt  # 按用户名统计
```

---

## 6. 🔗 与sort命令配合使用



### 6.1 为什么需要配合sort



**❌ 单独使用uniq的局限性**
```bash
cat > scattered.txt << EOF
apple
banana
apple
orange
banana
apple
EOF

# 只使用uniq - 无法完全去重

uniq scattered.txt
# 输出：apple, banana, apple, orange, banana, apple

```

**✅ 配合sort的完整去重**
```bash
# 先排序再去重 - 完全去重

sort scattered.txt | uniq
# 输出：apple, banana, orange

```

### 6.2 常用组合模式



**🔄 标准去重流程**
```bash
# 完全去重并统计

sort scattered.txt | uniq -c

# 完全去重并按频率排序

sort scattered.txt | uniq -c | sort -nr

# 只显示重复的行

sort scattered.txt | uniq -d
```

### 6.3 处理大文件的技巧



**⚡ 高效处理策略**
```bash
# 对大文件先排序再去重

sort large_file.txt | uniq > cleaned_file.txt

# 显示处理进度（仅作示意）

sort large_file.txt | pv | uniq > cleaned_file.txt
```

**💾 内存优化**
```bash
# 使用临时文件处理超大文件

sort -T /tmp large_file.txt | uniq > result.txt
```

---

## 7. 🔢 重复行计数显示



### 7.1 基础计数功能



**📊 使用-c选项详解**
```bash
cat > access.log << EOF
192.168.1.100 GET /home
192.168.1.100 GET /home  
192.168.1.101 GET /about
192.168.1.100 POST /login
192.168.1.101 GET /about
EOF

# 统计相邻重复行

uniq -c access.log
```

**📋 输出解读**
```
      2 192.168.1.100 GET /home     ← 连续出现2次
      1 192.168.1.101 GET /about    ← 出现1次
      1 192.168.1.100 POST /login   ← 出现1次  
      1 192.168.1.101 GET /about    ← 又出现1次
```

### 7.2 完整统计分析



**📈 完整访问统计**
```bash
# 完整统计（先排序）

sort access.log | uniq -c
```

**🔍 高级分析命令**
```bash
# 找出访问最频繁的IP

sort access.log | uniq -c | sort -nr | head -5

# 统计唯一访问者数量

sort access.log | uniq | wc -l

# 只显示重复访问的记录

sort access.log | uniq -c | grep -v "      1 "
```

### 7.3 格式化输出



**🎨 美化统计结果**
```bash
# 添加表头和格式化

echo "访问次数 访问记录" > report.txt
sort access.log | uniq -c | sort -nr >> report.txt

# 使用awk进一步格式化

sort access.log | uniq -c | awk '{printf "%-8s %s\n", $1"次", substr($0, index($0, $2))}'
```

---

## 8. 🌟 实际数据处理场景



### 8.1 日志分析场景



**📋 Web服务器日志去重**
```bash
# 分析nginx访问日志

cat /var/log/nginx/access.log | \
    awk '{print $1}' | \          # 提取IP地址
    sort | uniq -c | \             # 统计访问次数  
    sort -nr | \                   # 按次数排序
    head -10                       # 显示前10名
```

**🔍 错误日志分析**
```bash
# 统计错误类型

grep "ERROR" application.log | \
    awk '{print $4}' | \           # 提取错误类型
    sort | uniq -c | \             # 统计出现次数
    sort -nr                       # 按频率排序
```

### 8.2 用户数据处理



**👥 用户行为分析**
```bash
cat > user_actions.txt << EOF
user001,login,2023-09-18
user001,login,2023-09-18  
user002,logout,2023-09-18
user001,view_page,2023-09-18
user002,logout,2023-09-18
EOF

# 统计用户操作频率

cut -d',' -f1,2 user_actions.txt | \  # 提取用户和操作
    sort | uniq -c                      # 统计频率
```

### 8.3 数据清理场景



**🧹 CSV文件去重**
```bash
# 保留表头，对数据行去重

head -1 data.csv > clean_data.csv              # 保存表头
tail -n +2 data.csv | sort | uniq >> clean_data.csv  # 去重数据
```

**📧 邮件列表清理**
```bash
cat > emails.txt << EOF
john@example.com
John@Example.com  
mary@test.org
JOHN@EXAMPLE.COM
admin@site.net
EOF

# 不区分大小写去重

sort emails.txt | uniq -i > clean_emails.txt
```

### 8.4 系统监控应用



**🖥️ 进程监控**
```bash
# 统计当前运行的进程类型

ps aux | awk '{print $11}' | \     # 提取进程命令
    sort | uniq -c | \              # 统计数量
    sort -nr | head -10             # 显示前10个
```

**💾 磁盘使用分析**
```bash
# 分析文件扩展名分布

find /path/to/directory -type f | \
    sed 's/.*\.//' | \              # 提取扩展名
    sort | uniq -c | \              # 统计数量
    sort -nr                        # 按数量排序
```

---

## 9. 📋 核心要点总结



### 9.1 必须掌握的核心概念



```
🔸 uniq本质：专门处理相邻重复行的文本工具
🔸 关键限制：只能处理紧挨着的重复，不相邻的无法处理
🔸 常见用法：通常与sort命令配合使用实现完全去重
🔸 计数功能：-c选项显示每行出现次数，便于统计分析
🔸 字段控制：可以指定比较的字段或字符位置
```

### 9.2 关键理解要点



**🔹 uniq vs sort的配合**
```
单独使用uniq：
- 只处理相邻重复行
- 适合已经排序的数据
- 处理速度快

sort + uniq组合：
- 实现完全去重
- 先排序后去重
- 是最常用的模式
```

**🔹 选项组合的威力**
```
基础去重：uniq file.txt
统计分析：sort file.txt | uniq -c | sort -nr
找重复项：sort file.txt | uniq -d  
找唯一项：sort file.txt | uniq -u
不区分大小写：sort file.txt | uniq -i
```

### 9.3 实际应用价值



**📊 数据分析应用**
- **日志分析**：统计访问频率、错误类型
- **用户行为**：分析用户操作模式
- **系统监控**：统计进程、文件类型分布
- **数据清理**：去除重复记录，标准化数据

**🛠️ 最佳实践指南**
```
数据去重标准流程：
1. sort排序：让重复行相邻
2. uniq去重：移除重复行
3. 结果验证：检查去重效果

性能优化建议：
- 大文件先sort再uniq
- 使用管道避免临时文件
- 合理使用-f和-s选项
```

### 9.4 常见问题与解决



**❓ 常见疑问解答**

> **Q**: 为什么uniq没有完全去重？
> **A**: uniq只处理相邻行，需要先用sort排序

> **Q**: 如何统计文件中不重复的行数？
> **A**: `sort file.txt | uniq | wc -l`

> **Q**: 如何找出重复次数超过N次的行？
> **A**: `sort file.txt | uniq -c | awk '$1 > N'`

### 9.5 学习检查清单



- [ ] 理解uniq只处理相邻重复行的特点
- [ ] 掌握-c选项进行频率统计
- [ ] 会使用-i选项忽略大小写
- [ ] 了解-f和-s选项指定比较位置
- [ ] 熟练使用sort+uniq组合
- [ ] 能够应用于实际数据处理场景

**🔑 核心记忆口诀**
> uniq去重有限制，只处理相邻重复行
> 配合sort更完美，统计分析用-c
> 大小写忽略用-i，字段跳过有妙招

**💡 延伸学习建议**
- 学习awk进行更复杂的文本处理
- 了解sed流式编辑器的使用
- 掌握grep正则表达式匹配
- 学习数据库去重的SQL语句