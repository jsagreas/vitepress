---
title: 7、target目标管理命令
---
## 📚 目录

1. [target目标基本概念](#1-target目标基本概念)
2. [target模式分类详解](#2-target模式分类详解)
3. [target查看和管理命令](#3-target查看和管理命令)
4. [target切换操作实践](#4-target切换操作实践)
5. [应用场景和故障处理](#5-应用场景和故障处理)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 target目标基本概念


### 1.1 什么是target目标


**简单理解**：target就像是Linux系统的不同"工作模式"，决定系统启动后处于什么状态

```
想象成手机的不同模式：
普通模式    → multi-user.target (命令行模式)
图形模式    → graphical.target (桌面环境)
飞行模式    → rescue.target (救援模式)
安全模式    → emergency.target (紧急模式)
```

**🔸 核心概念**
```
target目标：
• 本质：一组服务的集合，定义系统运行状态
• 作用：决定系统启动后加载哪些服务
• 替代：取代了传统的运行级别(runlevel)
• 管理：通过systemd统一管理
```

### 1.2 target与传统运行级别对比


**传统运行级别 vs 现代target**

| 传统runlevel | 对应target | **含义说明** |
|-------------|-----------|------------|
| `0` | `poweroff.target` | `关机状态` |
| `1` | `rescue.target` | `单用户救援模式` |
| `2,3,4` | `multi-user.target` | `多用户命令行模式` |
| `5` | `graphical.target` | `图形界面模式` |
| `6` | `reboot.target` | `重启状态` |

**🔹 优势对比**
```
传统运行级别：
❌ 数字不够直观
❌ 功能相对固定
❌ 扩展性有限

现代target：
✅ 名称直观易懂
✅ 功能更加灵活
✅ 可以自定义组合
```

### 1.3 target的工作机制


**📊 target依赖关系图**
```
graphical.target (图形模式)
    ↓ 依赖
multi-user.target (多用户模式)
    ↓ 依赖
basic.target (基础服务)
    ↓ 依赖
sysinit.target (系统初始化)
```

**💡 工作原理**
- **层级依赖**：高级target依赖低级target的服务
- **服务集合**：每个target包含一组相关服务
- **状态切换**：可以在不同target间动态切换

---

## 2. 🏗️ target模式分类详解


### 2.1 multi-user.target多用户模式


**🔸 基本特征**
```
多用户命令行模式：
• 界面：纯命令行界面，无图形桌面
• 网络：网络服务正常启动
• 用户：支持多用户同时登录
• 服务：启动系统核心服务和网络服务
• 资源：占用系统资源较少
```

**适用场景**
- 🖥️ **服务器环境**：大多数Linux服务器默认模式
- ⚡ **资源节约**：不需要图形界面时节省内存
- 🛠️ **运维管理**：便于脚本自动化管理
- 🔧 **故障排查**：系统出问题时的稳定模式

### 2.2 graphical.target图形界面模式


**🔸 基本特征**
```
图形桌面模式：
• 界面：完整的图形用户界面(GUI)
• 功能：包含multi-user.target的所有功能
• 桌面：启动桌面环境(GNOME、KDE等)
• 应用：支持图形应用程序
• 资源：占用更多系统资源
```

**包含的核心组件**
- 🎨 **显示管理器**：gdm、lightdm等登录界面
- 🖥️ **桌面环境**：GNOME、KDE、XFCE等
- 🎮 **图形服务**：X Server或Wayland
- 📱 **系统应用**：文件管理器、设置程序等

### 2.3 rescue.target救援模式


**🔸 基本特征**
```
单用户救援模式：
• 用户：只允许root用户登录
• 服务：启动最基本的系统服务
• 网络：通常不启动网络服务
• 文件系统：以读写方式挂载根文件系统
• 用途：系统维护和故障修复
```

**🚨 使用场景**
- 🔧 **密码重置**：忘记root密码时修改
- 🛠️ **文件系统修复**：磁盘错误检查和修复
- ⚙️ **配置修复**：修改错误的系统配置
- 📋 **日志分析**：查看系统启动失败原因

### 2.4 emergency.target紧急模式


**🔸 基本特征**
```
紧急模式：
• 启动：最小化启动，只加载必要驱动
• 文件系统：根文件系统只读挂载
• 服务：几乎不启动任何系统服务
• 用途：系统严重损坏时的最后手段
```

**⚠️ 关键区别**
```
rescue vs emergency：

救援模式(rescue)：
• 文件系统读写挂载
• 启动基本系统服务
• 适合一般故障修复

紧急模式(emergency)：
• 文件系统只读挂载
• 几乎不启动服务
• 适合严重系统损坏
```

---

## 3. 🔍 target查看和管理命令


### 3.1 查看target状态命令


**📋 获取当前默认target**
```bash
# 查看系统默认启动target
systemctl get-default
```

**输出示例**：
```
graphical.target  # 系统默认以图形模式启动
```

**📋 查看当前运行target**
```bash
# 查看当前正在运行的target
systemctl list-units --type=target
```

**输出解读**：
```
UNIT                   LOAD   ACTIVE SUB    DESCRIPTION
basic.target          loaded active active Basic System
graphical.target      loaded active active Graphical Interface
multi-user.target     loaded active active Multi-User System
network.target        loaded active active Network

LOAD   : 是否正确加载
ACTIVE : 当前活动状态  
SUB    : 详细子状态
```

### 3.2 target设置命令详解


**⚙️ 设置默认target**
```bash
# 设置系统默认启动为命令行模式
systemctl set-default multi-user.target

# 设置系统默认启动为图形模式  
systemctl set-default graphical.target
```

**💡 实际效果**
- 修改系统启动配置，下次重启生效
- 实际上是创建符号链接：`/etc/systemd/system/default.target`

**🔍 验证设置结果**
```bash
# 确认设置是否生效
systemctl get-default
ls -l /etc/systemd/system/default.target
```

### 3.3 查看所有可用target


**📊 列出所有target**
```bash
# 查看系统中所有可用的target
systemctl list-unit-files --type=target
```

**常见target列表**：
```
UNIT FILE                 STATE    
basic.target             static   基础系统target
emergency.target         static   紧急模式target
graphical.target         enabled  图形界面target
multi-user.target        enabled  多用户target
network.target           static   网络target
poweroff.target          disabled 关机target
reboot.target            disabled 重启target
rescue.target            disabled 救援target
```

**🔹 状态说明**
- **enabled**：可以被启用的target
- **disabled**：被禁用的target
- **static**：不能直接启用，被其他单元依赖

---

## 4. 🔄 target切换操作实践


### 4.1 临时切换target


**⚡ isolate命令详解**
```bash
# 立即切换到命令行模式(不重启)
systemctl isolate multi-user.target

# 立即切换到图形模式
systemctl isolate graphical.target

# 切换到救援模式
systemctl isolate rescue.target
```

**🔸 isolate的含义**
- **隔离激活**：只启动指定target及其依赖服务
- **停止其他**：停止不相关的服务
- **立即生效**：无需重启系统

**⚠️ 切换注意事项**
```
切换前准备：
• 保存当前工作内容
• 关闭不必要的应用程序
• 确保没有重要任务在运行

切换可能影响：
• 正在运行的图形应用会关闭
• 网络连接可能短暂中断
• 某些服务可能重启
```

### 4.2 切换操作实战演示


**🎯 场景1：从图形切换到命令行**
```bash
# 当前状态检查
systemctl get-default          # 查看默认target
who                            # 查看当前登录用户

# 执行切换
systemctl isolate multi-user.target

# 切换后验证
systemctl list-units --type=target --state=active
```

**🎯 场景2：故障时进入救援模式**
```bash
# 紧急情况下进入救援模式
systemctl isolate rescue.target

# 在救援模式中的操作
passwd root                    # 重置密码
fsck /dev/sda1                # 检查文件系统
nano /etc/fstab               # 修复配置文件

# 修复完成后返回正常模式
systemctl isolate graphical.target
```

### 4.3 重启时指定target


**🔄 重启到指定模式**
```bash
# 重启并进入命令行模式
systemctl reboot --boot-loader-entry=multi-user

# 或者设置默认后重启
systemctl set-default multi-user.target
reboot
```

---

## 5. 🛠️ 应用场景和故障处理


### 5.1 典型应用场景


**🖥️ 服务器管理场景**
```
场景：配置Linux服务器
目标：节省资源，提高性能
操作：
1. systemctl set-default multi-user.target
2. systemctl disable gdm
3. reboot

结果：服务器以命令行模式启动，节省内存资源
```

**🔧 系统维护场景**
```
场景：系统出现图形界面问题
目标：进入命令行模式排查
操作：
1. Ctrl+Alt+F2 进入控制台
2. systemctl isolate multi-user.target
3. 检查和修复图形服务问题
4. systemctl isolate graphical.target

结果：在稳定的命令行环境下解决图形问题
```

### 5.2 常见故障排查


**❌ 问题1：图形界面启动失败**

**故障现象**：
```
系统启动后停留在命令行界面
图形登录管理器无法启动
错误信息：Failed to start GNOME Display Manager
```

**排查步骤**：
```bash
# 1. 检查当前target状态
systemctl get-default
systemctl list-units --type=target --state=active

# 2. 检查图形服务状态
systemctl status gdm
systemctl status graphical.target

# 3. 查看详细错误日志
journalctl -u gdm -f
journalctl -u graphical.target

# 4. 尝试手动启动图形服务
systemctl start gdm
systemctl isolate graphical.target
```

**❌ 问题2：无法切换target**

**故障现象**：
```
执行systemctl isolate命令无响应
切换后服务状态异常
系统资源占用过高
```

**解决方案**：
```bash
# 1. 检查服务依赖关系
systemctl list-dependencies graphical.target

# 2. 强制重新加载systemd配置
systemctl daemon-reload

# 3. 检查冲突的服务
systemctl --failed
systemctl reset-failed

# 4. 安全模式下重启
systemctl rescue
```

### 5.3 target管理最佳实践


**📋 日常管理建议**

**生产环境设置**：
```bash
# 服务器环境推荐配置
systemctl set-default multi-user.target  # 默认命令行
systemctl mask graphical.target          # 禁用图形模式
systemctl enable sshd                    # 启用SSH服务
```

**开发环境设置**：
```bash
# 开发机推荐配置
systemctl set-default graphical.target   # 默认图形模式
systemctl enable NetworkManager          # 网络管理
systemctl enable bluetooth               # 蓝牙支持
```

**⚠️ 安全注意事项**
```
权限控制：
• 只有root用户可以切换target
• 普通用户需要sudo权限
• 远程连接时谨慎切换模式

备份策略：
• 修改默认target前做好备份
• 记录当前系统配置
• 准备应急恢复方案
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的基本概念


```
🔸 target本质：系统运行模式，决定启动哪些服务
🔸 主要模式：multi-user(命令行)、graphical(图形)、rescue(救援)、emergency(紧急)
🔸 核心命令：get-default、set-default、isolate、list-units
🔸 切换方式：临时切换(isolate)、永久设置(set-default)
```

### 6.2 关键命令使用要点


**🔹 查看类命令**
```bash
systemctl get-default                    # 获取默认target
systemctl list-units --type=target       # 查看当前活跃target
systemctl list-unit-files --type=target  # 查看所有可用target
```

**🔹 管理类命令**
```bash
systemctl set-default [target名称]       # 设置默认target
systemctl isolate [target名称]           # 临时切换target
```

### 6.3 实际应用指导


**🎯 使用场景选择**
```
命令行模式(multi-user)：
✅ 服务器环境部署
✅ 资源受限的系统
✅ 自动化脚本运行
✅ 远程管理维护

图形模式(graphical)：
✅ 桌面办公环境
✅ 开发调试工作
✅ 多媒体应用
✅ 用户交互操作

救援模式(rescue)：
✅ 系统故障修复
✅ 密码重置操作
✅ 配置文件修复
✅ 文件系统检查
```

**🔧 故障处理思路**
```
问题诊断步骤：
1. 检查当前target状态
2. 查看服务运行情况
3. 分析系统日志信息
4. 尝试切换到稳定模式
5. 在安全环境下修复
```

### 6.4 学习记忆要点


**📝 核心概念记忆**
- **target = 运行模式**：不同的系统状态
- **isolate = 立即切换**：不重启直接改变模式
- **set-default = 永久设置**：修改开机默认模式

**🎯 命令记忆技巧**
```
get-default  → "获取"默认设置
set-default  → "设置"默认配置  
isolate      → "隔离"激活指定target
list-units   → "列出"系统单元状态
```

**⚡ 模式选择原则**
- **服务器优先命令行**：节省资源，提高稳定性
- **桌面优先图形模式**：提供完整用户体验
- **故障优先救援模式**：提供安全修复环境

**核心记忆口诀**：
- target管理系统状态，get查看set设置
- isolate立即能切换，rescue救援emergency急
- 命令行省资源稳定，图形界面用户友好