---
title: 1、磁盘信息查看命令
---
## 📚 目录

1. [磁盘信息查看基础概念](#1-磁盘信息查看基础概念)
2. [lsblk - 树形显示块设备](#2-lsblk-树形显示块设备)
3. [blkid - 设备标识符查看](#3-blkid-设备标识符查看)
4. [fdisk -l - 分区表查看](#4-fdisk-l-分区表查看)
5. [lshw - 硬件信息查看](#5-lshw-硬件信息查看)
6. [hdparm - 硬盘参数管理](#6-hdparm-硬盘参数管理)
7. [smartctl - 硬盘健康检测](#7-smartctl-硬盘健康检测)
8. [系统日志与内核信息](#8-系统日志与内核信息)
9. [实战应用与最佳实践](#9-实战应用与最佳实践)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 💾 磁盘信息查看基础概念


### 1.1 什么是磁盘信息查看


**通俗解释**：就像你想了解自己电脑有几块硬盘、多大容量、分了几个区一样，Linux提供了多种命令帮你查看这些信息。

**核心作用**：
- 🔍 **设备识别**：查看系统中有哪些存储设备
- 📊 **容量查看**：了解磁盘大小和使用情况  
- 🗂️ **分区信息**：查看磁盘是如何分区的
- ⚕️ **健康状态**：检测磁盘是否正常工作

### 1.2 Linux中的块设备概念


**什么是块设备**？
简单理解：块设备就是可以存储数据的设备，比如硬盘、U盘、光盘等。

```
Linux设备分类：
┌─────────────┐
│   块设备     │ ← 硬盘、SSD、U盘（可存储数据）
├─────────────┤
│  字符设备    │ ← 键盘、鼠标、串口（按字符传输）
├─────────────┤
│  网络设备    │ ← 网卡（网络通信）
└─────────────┘
```

**设备命名规则**：
- `sda, sdb, sdc...` → SATA/SAS硬盘
- `nvme0n1, nvme0n2...` → NVMe SSD硬盘
- `sda1, sda2, sda3...` → sda硬盘的第1、2、3个分区

---

## 2. 🌳 lsblk - 树形显示块设备


### 2.1 命令基本含义


**lsblk** = **l**i**s**t **bl**oc**k** devices（列出块设备）

**作用**：以树形结构显示系统中所有块设备的层次关系，让你一目了然地看到磁盘和分区的组织结构。

### 2.2 基本语法和常用选项


```bash
# 基本用法
lsblk

# 显示更多信息
lsblk -f    # 显示文件系统信息
lsblk -m    # 显示权限信息
lsblk -o +UUID,LABEL    # 自定义显示列
```

### 2.3 输出信息详解


```
典型输出示例：
NAME   MAJ:MIN RM  SIZE RO TYPE MOUNTPOINT
sda      8:0    0   1TB  0  disk
├─sda1   8:1    0  512M  0  part /boot/efi
├─sda2   8:2    0   1GB  0  part /boot
└─sda3   8:3    0  998G  0  part /
sr0     11:0    1  1024M 0  rom
```

**字段含义解释**：
- **NAME** → 设备名称（如sda表示第一块SATA硬盘）
- **MAJ:MIN** → 主设备号:次设备号（内核用来识别设备）
- **RM** → 是否可移动（0=固定，1=可移动如U盘）
- **SIZE** → 设备容量大小
- **RO** → 是否只读（0=可写，1=只读）
- **TYPE** → 设备类型（disk=磁盘，part=分区，rom=光驱）
- **MOUNTPOINT** → 挂载点（设备挂载到哪个目录）

### 2.4 实用技巧


| 使用场景 | 命令 | 说明 |
|---------|------|------|
| 🔍 **查看文件系统** | `lsblk -f` | 显示每个分区的文件系统类型 |
| 📊 **查看详细信息** | `lsblk -o +UUID,LABEL,FSTYPE` | 显示UUID、标签、文件系统 |
| 🎯 **指定设备** | `lsblk /dev/sda` | 只查看指定磁盘的信息 |

---

## 3. 🏷️ blkid - 设备标识符查看


### 3.1 命令基本含义


**blkid** = **bl**oc**k** **id**entifier（块设备标识符）

**通俗解释**：每个分区都有自己的"身份证"，包括UUID（唯一编号）、文件系统类型等。blkid就是用来查看这些"身份信息"的。

### 3.2 为什么需要UUID


**什么是UUID**？
UUID = Universally Unique Identifier（全局唯一标识符）

**为什么重要**？
设备名称可能会变化（比如硬盘顺序改变，sda可能变成sdb），但UUID永远不变，所以在配置文件中使用UUID更可靠。

### 3.3 基本用法


```bash
# 显示所有设备的UUID和文件系统信息
blkid

# 查看指定设备
blkid /dev/sda1

# 只显示UUID
blkid -s UUID /dev/sda1
```

### 3.4 输出信息解读


```
典型输出：
/dev/sda1: UUID="12AB-34CD" TYPE="vfat" PARTUUID="abcd1234-01"
/dev/sda2: UUID="ab12cd34-ef56-7890-abcd-123456789abc" TYPE="ext4"
```

**字段解释**：
- **UUID** → 分区的唯一标识符
- **TYPE** → 文件系统类型（ext4、vfat、xfs等）
- **PARTUUID** → 分区的UUID（GPT分区表特有）

---

## 4. 💽 fdisk -l - 分区表查看


### 4.1 命令基本含义


**fdisk** = **f**ixed **disk**（固定磁盘）

**通俗解释**：fdisk是Linux的分区管理工具，`-l`选项让它只查看不修改，展示磁盘的分区表信息。

### 4.2 基本语法


```bash
# 查看所有磁盘分区信息
sudo fdisk -l

# 查看指定磁盘
sudo fdisk -l /dev/sda
```

**⚠️ 注意**：某些系统需要root权限才能查看完整信息。

### 4.3 输出信息详解


```
磁盘基本信息：
Disk /dev/sda: 1 TiB, 1099511627776 bytes, 2147483648 sectors
Disk model: Samsung SSD 980
Units: sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 4096 bytes

分区表信息：
Device     Start       End   Sectors  Size Type
/dev/sda1   2048   1050623   1048576  512M EFI System
/dev/sda2 1050624   3147775   2097152    1G Linux filesystem
/dev/sda3 3147776 2147483614 2144335839 1023G Linux filesystem
```

**关键信息解读**：
- **磁盘容量** → 1 TiB表示磁盘总大小
- **扇区大小** → 通常是512字节
- **分区类型** → EFI System（启动分区）、Linux filesystem（普通分区）

### 4.4 分区表类型识别


**MBR vs GPT**：

```
MBR分区表特点：
- 最多支持4个主分区
- 单个分区最大2TB
- 兼容性好，适合老系统

GPT分区表特点：
- 支持128个分区
- 单个分区可达9.4ZB
- 现代系统推荐使用
```

---

## 5. 🔧 lshw - 硬件信息查看


### 5.1 命令基本含义


**lshw** = **l**i**s**t **h**ard**w**are（列出硬件）

**通俗解释**：lshw是Linux的硬件信息查看工具，可以显示CPU、内存、磁盘等各种硬件的详细信息。

### 5.2 磁盘相关用法


```bash
# 只显示磁盘硬件信息
sudo lshw -class disk

# 以简短格式显示
sudo lshw -class disk -short

# 以HTML格式输出（可保存到文件）
sudo lshw -html > hardware_info.html
```

### 5.3 输出信息解读


```
典型输出：
*-disk                    
       description: ATA Disk
       product: Samsung SSD 980
       vendor: Samsung
       physical id: 0.0.0
       bus info: scsi@1:0.0.0
       logical name: /dev/sda
       version: 2B2Q
       size: 1TB (1000204886016 bytes)
       configuration: ansiversion=5 logicalsectorsize=512 sectorsize=4096
```

**重要字段**：
- **product** → 硬盘型号
- **vendor** → 制造商
- **size** → 容量大小
- **logical name** → 在Linux系统中的设备名

---

## 6. ⚡ hdparm - 硬盘参数管理


### 6.1 命令基本含义


**hdparm** = **h**ard **d**isk **param**eters（硬盘参数）

**通俗解释**：hdparm用来查看和设置硬盘的各种参数，比如转速、缓存、电源管理等。

### 6.2 常用查看功能


```bash
# 查看硬盘基本信息
sudo hdparm -I /dev/sda

# 测试硬盘读取速度
sudo hdparm -t /dev/sda

# 查看硬盘是否支持SMART
sudo hdparm -I /dev/sda | grep -i smart
```

### 6.3 性能测试


```bash
# 测试缓冲读取速度
sudo hdparm -T /dev/sda

# 测试直接读取速度  
sudo hdparm -t /dev/sda
```

**结果解读**：
- **缓冲读取** → 测试内存到CPU的速度
- **直接读取** → 测试硬盘的真实读取速度

---

## 7. 🏥 smartctl - 硬盘健康检测


### 7.1 SMART技术介绍


**什么是SMART**？
SMART = Self-Monitoring, Analysis and Reporting Technology
（自我监测、分析和报告技术）

**通俗解释**：现代硬盘都内置了SMART功能，能监测自己的健康状况，就像人体检查一样。

### 7.2 基本使用


```bash
# 查看SMART信息总览
sudo smartctl -i /dev/sda

# 查看SMART健康状态
sudo smartctl -H /dev/sda

# 查看详细SMART属性
sudo smartctl -A /dev/sda
```

### 7.3 健康状态判断


**SMART状态**：
- ✅ **PASSED** → 硬盘健康良好
- ⚠️ **FAILING** → 硬盘可能即将故障
- ❌ **FAILED** → 硬盘已经损坏

**关键参数**：
- **Temperature** → 硬盘温度（通常应低于50°C）
- **Power-On Hours** → 累计通电时间
- **Reallocated Sectors** → 重分配扇区数（越少越好）

---

## 8. 📋 系统日志与内核信息


### 8.1 dmesg查看磁盘日志


**dmesg含义**：显示内核环形缓冲区的信息，记录了系统启动和硬件检测的过程。

```bash
# 查看磁盘相关的内核日志
dmesg | grep -i disk

# 查看SCSI设备日志（包括SATA硬盘）
dmesg | grep sd

# 查看最近的磁盘日志
dmesg -T | grep sd | tail -10
```

### 8.2 /proc/partitions文件


**作用**：显示内核识别的所有分区信息。

```bash
# 查看内核识别的分区
cat /proc/partitions

# 输出示例：
major minor  #blocks  name
   8     0 1953514584 sda
   8     1     524288 sda1
   8     2    1048576 sda2
   8     3 1951941632 sda3
```

**字段说明**：
- **major** → 主设备号
- **minor** → 次设备号  
- **#blocks** → 块数量（1块=1024字节）
- **name** → 设备名称

---

## 9. 🎯 实战应用与最佳实践


### 9.1 新硬盘检测流程


**Step ①** 检测系统是否识别新硬盘：
```bash
lsblk
# 或者
dmesg | grep sd | tail -5
```

**Step ②** 查看硬盘详细信息：
```bash
sudo fdisk -l /dev/sdX  # X为新硬盘字母
```

**Step ③** 检测硬盘健康状况：
```bash
sudo smartctl -H /dev/sdX
```

### 9.2 分区信息备份


**为什么需要备份分区信息**？
防止意外删除分区时能够恢复。

```bash
# 备份分区表
sudo sfdisk -d /dev/sda > sda_partition_backup.txt

# 恢复分区表（谨慎操作！）
sudo sfdisk /dev/sda < sda_partition_backup.txt
```

### 9.3 常见问题排查


| 问题现象 | 可能原因 | 排查命令 |
|---------|----------|---------|
| 🔍 **系统识别不到新硬盘** | 连接问题/驱动问题 | `lsblk`, `dmesg \| grep sd` |
| ⚠️ **硬盘读写很慢** | 硬盘故障/接口问题 | `hdparm -t`, `smartctl -A` |
| ❌ **分区丢失** | 分区表损坏 | `fdisk -l`, 恢复备份 |

### 9.4 系统管理员日常检查


**每日检查清单**：
- [ ] `df -h` 检查磁盘空间使用率
- [ ] `smartctl -H /dev/sd*` 检查硬盘健康
- [ ] `dmesg | grep -i error` 查看是否有硬件错误

---

## 10. 📝 核心要点总结


### 10.1 必须掌握的核心命令


```
🔸 lsblk     → 树形查看磁盘和分区结构（最常用）
🔸 blkid     → 查看分区UUID和文件系统类型
🔸 fdisk -l  → 查看分区表详细信息
🔸 smartctl  → 检查硬盘健康状态（重要！）
🔸 dmesg     → 查看系统硬件检测日志
```

### 10.2 关键概念理解


**🔹 设备命名规则**
```
SATA硬盘：sda, sdb, sdc...
分区编号：sda1, sda2, sda3...
NVMe SSD：nvme0n1, nvme0n2...
```

**🔹 UUID的重要性**
- 设备名可能变化，UUID永不变
- 配置文件中使用UUID更可靠
- 通过`blkid`命令获取UUID

**🔹 分区表类型**
- MBR：老式，最多4个主分区，最大2TB
- GPT：现代，支持128个分区，几乎无容量限制

### 10.3 实际运维价值


**🎯 系统管理**
- **容量监控**：定期检查磁盘使用情况
- **健康检查**：使用SMART监测硬盘状态
- **故障预防**：及时发现硬盘异常

**🔧 故障排查**
- **新硬盘安装**：确认系统识别和分区
- **性能问题**：测试磁盘读写速度
- **数据恢复**：分区表备份和恢复

**核心记忆口诀**：
- lsblk看结构，blkid查身份
- fdisk观分区，smart测健康
- dmesg看日志，问题早发现

**⚡ 快速参考卡片**

| 需求 | 命令 | 作用 |
|------|------|------|
| 🔍 查看磁盘结构 | `lsblk` | 树形显示所有磁盘分区 |
| 🏷️ 获取UUID | `blkid` | 查看分区标识符 |
| 📊 详细分区信息 | `sudo fdisk -l` | 显示分区表详情 |
| 🏥 检查硬盘健康 | `sudo smartctl -H /dev/sda` | SMART健康检测 |
| 📋 查看识别日志 | `dmesg \| grep sd` | 内核硬盘检测日志 |