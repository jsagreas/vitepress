---
title: 3、Docker安装配置管理
---
## 📚 目录

1. [Docker版本选择](#1-Docker版本选择)
2. [系统环境准备](#2-系统环境准备)
3. [Docker安装过程](#3-Docker安装过程)
4. [服务管理配置](#4-服务管理配置)
5. [用户权限配置](#5-用户权限配置)
6. [守护进程配置](#6-守护进程配置)
7. [安装验证测试](#7-安装验证测试)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔄 Docker版本选择


### 1.1 Docker CE vs Docker EE


**什么是Docker CE和EE？**

Docker就像是一个软件打包工具，把应用程序和它需要的所有东西（代码、运行环境、依赖库等）打包成一个"容器"，这个容器可以在任何支持Docker的机器上运行。

```
简单理解：
Docker = 集装箱系统
应用程序 = 货物
容器 = 标准集装箱
任何支持的机器 = 港口/运输工具
```

**版本区别对比：**

| 特性对比 | **Docker CE（社区版）** | **Docker EE（企业版）** |
|---------|------------------------|------------------------|
| 🏷️ **价格** | `完全免费` | `付费订阅` |
| 👥 **适用人群** | `个人学习、小团队开发` | `大型企业、商业项目` |
| 🔧 **功能特性** | `核心容器功能` | `企业级安全、管理功能` |
| 📞 **技术支持** | `社区支持` | `官方技术支持` |
| ⏱️ **更新频率** | `月度更新` | `季度稳定版本` |

**💡 新手选择建议**：
- 学习阶段：选择 **Docker CE**
- 个人项目：选择 **Docker CE**  
- 小公司开发：选择 **Docker CE**
- 大企业生产：考虑 **Docker EE**

### 1.2 Docker与Podman的区别


**Podman是什么？**

Podman是Docker的替代品，功能类似但设计理念不同。

```
架构对比图：

Docker架构：
用户命令 → Docker客户端 → Docker守护进程 → 容器运行
          (需要root权限)    (后台服务)

Podman架构：
用户命令 → Podman → 容器运行
        (无需守护进程，更安全)
```

| 对比项目 | **Docker** | **Podman** |
|---------|------------|------------|
| 🏗️ **架构** | `需要守护进程` | `无守护进程` |
| 🔐 **权限** | `需要root权限` | `支持无root运行` |
| 📱 **兼容性** | `Docker生态完整` | `命令兼容Docker` |
| 📈 **学习曲线** | `生态成熟，资料多` | `相对较新` |

**选择建议**：新手建议先学Docker，因为：
- 📚 学习资料更丰富
- 🌐 社区支持更完善  
- 💼 工作中使用更广泛

---

## 2. 🛠️ 系统环境准备


### 2.1 系统要求检查


**Docker对系统的基本要求：**

```
硬件要求：
- CPU：64位处理器
- 内存：至少2GB（推荐4GB以上）
- 存储：至少20GB可用空间

软件要求：
- Linux内核：3.10及以上
- 系统版本：CentOS 7+、Ubuntu 16.04+、Debian 8+等
```

**⚠️ 重要提醒**：Docker不能在32位系统上运行！

### 2.2 检查系统信息


**系统版本检查命令：**

```bash
# 查看系统版本
cat /etc/os-release

# 查看内核版本  
uname -r

# 查看系统架构
uname -m
```

**输出示例解读：**
```
# CentOS系统输出示例
NAME="CentOS Linux"
VERSION="7 (Core)"
# 说明：CentOS 7版本，满足要求

# 内核版本输出示例  
3.10.0-1160.el7.x86_64
# 说明：3.10内核，满足最低要求

# 架构输出示例
x86_64
# 说明：64位系统，满足要求
```

### 2.3 清理旧版本Docker


**为什么要清理旧版本？**

旧版本Docker可能会与新版本冲突，导致安装失败或运行异常。

```bash
# 卸载旧版本Docker（如果存在）
sudo yum remove docker \
    docker-client \
    docker-client-latest \
    docker-common \
    docker-latest \
    docker-latest-logrotate \
    docker-logrotate \
    docker-engine
```

**💡 小贴士**：如果系统提示"没有安装包"，这是正常的，说明没有旧版本需要清理。

---

## 3. 📦 Docker安装过程


### 3.1 配置Docker官方仓库


**什么是软件仓库？**

软件仓库就像是一个"应用商店"，系统可以从这里下载和安装软件。配置Docker官方仓库能确保下载到最新、最安全的Docker版本。

**CentOS/RHEL系统配置：**

```bash
# 1. 安装必要的工具
sudo yum install -y yum-utils

# 2. 添加Docker官方仓库
sudo yum-config-manager \
    --add-repo \
    https://download.docker.com/linux/centos/docker-ce.repo
```

**Ubuntu/Debian系统配置：**

```bash
# 1. 更新包索引
sudo apt-get update

# 2. 安装必要工具
sudo apt-get install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# 3. 添加Docker官方GPG密钥
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# 4. 添加仓库
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

### 3.2 安装Docker CE


**CentOS/RHEL安装命令：**

```bash
# 安装Docker CE最新版本
sudo yum install -y docker-ce docker-ce-cli containerd.io
```

**Ubuntu/Debian安装命令：**

```bash
# 更新包索引
sudo apt-get update

# 安装Docker CE
sudo apt-get install -y docker-ce docker-ce-cli containerd.io
```

**🔍 安装过程说明**：
- `docker-ce`：Docker核心引擎
- `docker-ce-cli`：Docker命令行工具
- `containerd.io`：容器运行时环境

### 3.3 验证安装结果


```bash
# 检查Docker版本
docker --version

# 查看详细信息
docker version

# 查看系统信息
docker info
```

**成功安装的输出示例：**
```
Docker version 20.10.21, build baeda1f
# 显示版本信息说明安装成功
```

---

## 4. ⚙️ 服务管理配置


### 4.1 systemctl服务管理


**什么是systemctl？**

systemctl是Linux系统的服务管理工具，可以启动、停止、重启各种系统服务。Docker安装后会注册为系统服务，需要通过systemctl来管理。

**Docker服务管理命令：**

```bash
# 启动Docker服务
sudo systemctl start docker

# 停止Docker服务  
sudo systemctl stop docker

# 重启Docker服务
sudo systemctl restart docker

# 查看服务状态
sudo systemctl status docker

# 查看服务日志
sudo journalctl -u docker
```

**服务状态解读：**

```
● docker.service - Docker Application Container Engine
   Loaded: loaded (/usr/lib/systemd/system/docker.service; enabled; vendor preset: disabled)
   Active: active (running) since Mon 2025-01-17 10:00:00 CST; 5min ago
```

- ✅ `Active: active (running)`：服务正在运行
- ❌ `Active: inactive (dead)`：服务已停止
- ⚠️ `Active: failed`：服务启动失败

### 4.2 开机自启动设置


**为什么要设置开机自启？**

设置开机自启后，系统重启时Docker会自动启动，不需要手动操作。

```bash
# 设置开机自启动
sudo systemctl enable docker

# 取消开机自启动  
sudo systemctl disable docker

# 查看是否已设置开机自启
sudo systemctl is-enabled docker
```

**验证自启动设置：**
```bash
# 重启系统测试
sudo reboot

# 系统启动后检查Docker状态
sudo systemctl status docker
```

---

## 5. 👥 用户权限配置


### 5.1 Docker用户组配置


**为什么需要配置用户组？**

默认情况下，只有root用户可以运行Docker命令。为了让普通用户也能使用Docker，需要将用户添加到docker用户组。

```
权限原理图：

默认情况：
普通用户 → Docker命令 → ❌ 权限不足

配置后：
普通用户 → docker用户组 → Docker命令 → ✅ 正常执行
```

**配置步骤：**

```bash
# 1. 创建docker用户组（通常安装时已自动创建）
sudo groupadd docker

# 2. 将当前用户添加到docker组
sudo usermod -aG docker $USER

# 3. 重新登录生效（或使用以下命令）
newgrp docker
```

### 5.2 权限验证测试


**测试无sudo运行Docker：**

```bash
# 测试普通用户权限（不使用sudo）
docker run hello-world
```

**成功输出示例：**
```
Hello from Docker!
This message shows that your installation appears to be working correctly.
```

**❌ 如果出现权限错误：**
```
Got permission denied while trying to connect to the Docker daemon socket
```

**解决方法：**
1. 确认用户已添加到docker组
2. 注销重新登录
3. 或重启系统

---

## 6. 🔧 守护进程配置


### 6.1 Docker守护进程配置文件


**什么是守护进程？**

守护进程就是在后台持续运行的程序，Docker daemon（守护进程）负责管理所有的Docker容器和镜像。

**配置文件位置：**
- 主配置文件：`/etc/docker/daemon.json`
- 服务配置：`/lib/systemd/system/docker.service`

### 6.2 常用配置选项


**创建配置文件：**

```bash
# 创建配置目录（如果不存在）
sudo mkdir -p /etc/docker

# 创建配置文件
sudo tee /etc/docker/daemon.json <<-'EOF'
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "storage-driver": "overlay2"
}
EOF
```

**配置项说明：**

| 配置项 | **作用说明** | **推荐值** |
|--------|-------------|-----------|
| 🌐 `registry-mirrors` | `镜像加速器，提升下载速度` | `国内镜像源` |
| 📝 `log-driver` | `日志驱动类型` | `json-file` |
| 📏 `max-size` | `单个日志文件最大大小` | `10m` |
| 📂 `max-file` | `保留的日志文件数量` | `3` |
| 💾 `storage-driver` | `存储驱动类型` | `overlay2` |

**💡 镜像加速器说明**：
由于Docker Hub在国外，下载速度较慢，配置国内镜像源可以大幅提升下载速度。

### 6.3 重启服务生效


```bash
# 重新加载systemd配置
sudo systemctl daemon-reload

# 重启Docker服务
sudo systemctl restart docker

# 验证配置是否生效
docker info | grep -i "registry mirrors"
```

---

## 7. ✅ 安装验证测试


### 7.1 基础功能测试


**1️⃣ 版本信息检查：**

```bash
# 检查Docker版本
docker --version

# 查看详细版本信息
docker version

# 查看系统配置信息
docker info
```

**2️⃣ 运行测试容器：**

```bash
# 运行官方测试镜像
docker run hello-world
```

**成功运行流程图：**

```
执行命令 → 检查本地镜像 → 下载hello-world镜像 → 创建容器 → 运行程序 → 显示成功信息
    |            |               |                |         |           |
   开始      本地没有        从仓库下载         启动容器    执行完成      测试通过
```

### 7.2 进阶功能测试


**3️⃣ 运行交互式容器：**

```bash
# 运行Ubuntu容器并进入交互模式
docker run -it ubuntu:20.04 bash

# 在容器内执行命令测试
ls /
cat /etc/os-release
exit
```

**4️⃣ 后台运行测试：**

```bash
# 后台运行nginx容器
docker run -d -p 8080:80 --name test-nginx nginx

# 查看运行中的容器
docker ps

# 测试访问
curl http://localhost:8080

# 停止并删除容器
docker stop test-nginx
docker rm test-nginx
```

### 7.3 常见问题排查


**问题1：权限被拒绝**
```bash
# 错误信息
Got permission denied while trying to connect to the Docker daemon socket

# 解决方案
sudo usermod -aG docker $USER
newgrp docker
```

**问题2：服务未启动**
```bash
# 错误信息
Cannot connect to the Docker daemon

# 解决方案
sudo systemctl start docker
sudo systemctl enable docker
```

**问题3：镜像下载慢**
```bash
# 解决方案：配置镜像加速器（见6.2章节）
# 验证加速器生效
docker info | grep -i "registry mirrors"
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 Docker版本：新手选择Docker CE，免费且功能完整
🔸 系统要求：64位系统，内核3.10+，至少2GB内存
🔸 官方仓库：确保下载最新最安全的Docker版本
🔸 服务管理：使用systemctl管理Docker服务
🔸 用户权限：将用户添加到docker组，避免频繁使用sudo
🔸 守护进程：通过daemon.json配置Docker运行参数
🔸 安装验证：运行hello-world容器验证安装成功
```

### 8.2 关键理解要点


**🔹 Docker的本质作用**
```
打包应用：将应用和环境打包成容器
标准化：容器可以在任何支持Docker的机器上运行  
隔离性：每个容器都是独立的运行环境
便携性：开发环境和生产环境保持一致
```

**🔹 权限配置的重要性**
```
安全考虑：Docker需要较高权限来管理容器
便利性：配置用户组后可以避免频繁使用sudo
最佳实践：生产环境中谨慎分配Docker权限
```

**🔹 配置优化的意义**
```
镜像加速：大幅提升镜像下载速度
日志管理：防止日志文件占用过多磁盘空间
存储优化：选择合适的存储驱动提升性能
```

### 8.3 实际应用价值


**💼 工作场景应用**
- **开发环境**：快速搭建统一的开发环境
- **测试环境**：容器化测试，提高测试效率
- **部署环境**：容器化部署，简化运维工作
- **学习环境**：快速体验各种技术栈

**🚀 技能发展路径**
1. **基础阶段**：掌握Docker安装和基本使用
2. **进阶阶段**：学习容器编排、网络配置  
3. **实战阶段**：项目容器化改造
4. **专业阶段**：生产环境容器管理

### 8.4 学习建议


**📚 学习重点**
- 理解容器化概念和价值
- 熟练掌握Docker基本命令
- 了解镜像和容器的关系
- 掌握常见问题的排查方法

**🔧 实践建议**
- 多动手操作，在实践中学习
- 搭建个人的容器化开发环境
- 尝试容器化自己的项目
- 关注Docker官方文档和最佳实践

**核心记忆口诀**：
```
Docker安装不复杂，版本选择CE就够
系统环境要检查，官方仓库配置好
服务管理用systemctl，用户权限要配置
守护进程有配置，镜像加速不可少
验证测试很重要，hello-world跑一遍
```