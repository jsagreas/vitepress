---
title: 10、Maven与Gradle构建工具代理
---
## 📚 目录

1. [构建工具代理概述](#1-构建工具代理概述)
2. [Maven代理配置详解](#2-Maven代理配置详解)
3. [Gradle代理配置详解](#3-Gradle代理配置详解)
4. [中央仓库代理访问配置](#4-中央仓库代理访问配置)
5. [私有仓库代理设置](#5-私有仓库代理设置)
6. [构建依赖下载优化](#6-构建依赖下载优化)
7. [企业级配置模板](#7-企业级配置模板)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🌐 构建工具代理概述


### 1.1 什么是构建工具代理


**🏠 生活类比**
> 想象你在海外网购，需要通过转运公司来获取商品。构建工具代理就像转运公司，帮助你的项目从远程仓库获取依赖包。

**核心概念**
```
构建工具代理 = 依赖下载的"中转站"

作用原理：
开发者项目 ──HTTP请求──> 代理服务器 ──转发──> Maven中央仓库
           <──依赖包────  代理服务器 <──依赖包── Maven中央仓库
```

### 1.2 为什么需要代理配置


**🎯 主要原因**

| **问题场景** | **具体表现** | **代理解决方案** |
|-------------|-------------|-----------------|
| 🌍 **网络限制** | `访问国外仓库缓慢` | 使用国内镜像代理 |
| 🏢 **企业环境** | `公司防火墙阻挡` | 配置企业代理服务器 |
| 🔒 **安全要求** | `需要认证访问` | 设置代理认证信息 |
| ⚡ **速度优化** | `下载依赖太慢` | 选择就近代理节点 |

**💡 关键洞察**
> 构建工具代理不仅解决网络问题，更是提升开发效率的重要工具。配置得当可以让依赖下载速度提升10倍以上。

### 1.3 代理配置的核心要素


**📊 配置要素对比**
```
┌─────────────────┬──────────────┬──────────────┐
│   配置要素      │   Maven      │   Gradle     │
├─────────────────┼──────────────┼──────────────┤
│ 代理服务器地址   │ settings.xml │ gradle.properties│
│ 端口号          │ <port>       │ systemProp   │
│ 用户认证        │ <username>   │ .username    │
│ 仓库镜像        │ <mirrors>    │ repositories │
└─────────────────┴──────────────┴──────────────┘
```

---

## 2. 🔧 Maven代理配置详解


### 2.1 settings.xml文件位置


**📂 配置文件位置**
```
全局配置（推荐）：
Linux/Mac: ~/.m2/settings.xml
Windows: C:\Users\{用户名}\.m2\settings.xml

项目配置：
项目根目录/.m2/settings.xml
```

**🔍 优先级顺序**
```
项目级settings.xml > 用户级settings.xml > 全局settings.xml
```

### 2.2 HTTP代理基础配置


**⚙️ 基本HTTP代理配置**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0">
  <proxies>
    <proxy>
      <id>http-proxy</id>
      <active>true</active>
      <protocol>http</protocol>
      <host>proxy.company.com</host>
      <port>8080</port>
      <!-- 可选：需要认证时配置 -->
      <username>your-username</username>
      <password>your-password</password>
      <!-- 不走代理的主机列表 -->
      <nonProxyHosts>localhost|127.0.0.1|*.local</nonProxyHosts>
    </proxy>
  </proxies>
</settings>
```

**📝 配置说明**

| **参数** | **含义** | **示例值** |
|---------|---------|------------|
| `<id>` | 代理标识符 | `http-proxy` |
| `<active>` | 是否启用 | `true/false` |
| `<protocol>` | 协议类型 | `http/https` |
| `<host>` | 代理服务器地址 | `proxy.company.com` |
| `<port>` | 代理端口 | `8080` |
| `<nonProxyHosts>` | 排除列表 | `localhost\|*.internal` |

### 2.3 HTTPS代理配置


**🔒 HTTPS代理设置**
```xml
<proxies>
  <!-- HTTP代理 -->
  <proxy>
    <id>http-proxy</id>
    <active>true</active>
    <protocol>http</protocol>
    <host>proxy.company.com</host>
    <port>8080</port>
  </proxy>
  
  <!-- HTTPS代理 -->
  <proxy>
    <id>https-proxy</id>
    <active>true</active>
    <protocol>https</protocol>
    <host>proxy.company.com</host>
    <port>8443</port>
    <username>your-username</username>
    <password>your-password</password>
  </proxy>
</proxies>
```

### 2.4 仓库镜像配置


**🪞 国内镜像加速**
```xml
<mirrors>
  <!-- 阿里云镜像 -->
  <mirror>
    <id>aliyun-maven</id>
    <mirrorOf>central</mirrorOf>
    <name>阿里云Maven中央仓库</name>
    <url>https://maven.aliyun.com/repository/central</url>
  </mirror>
  
  <!-- 华为云镜像 -->
  <mirror>
    <id>huawei-maven</id>
    <mirrorOf>*</mirrorOf>
    <name>华为云Maven仓库</name>
    <url>https://repo.huaweicloud.com/repository/maven/</url>
  </mirror>
</mirrors>
```

**⚠️ 注意事项**
- `mirrorOf="central"`: 仅镜像中央仓库
- `mirrorOf="*"`: 镜像所有仓库（慎用）
- `mirrorOf="external:*"`: 镜像所有外部仓库

---

## 3. 🛠️ Gradle代理配置详解


### 3.1 gradle.properties配置


**📍 配置文件位置**
```
全局配置：
Linux/Mac: ~/.gradle/gradle.properties
Windows: C:\Users\{用户名}\.gradle\gradle.properties

项目配置：
项目根目录/gradle.properties
```

### 3.2 HTTP/HTTPS代理配置


**⚙️ 基础代理配置**
```properties
# HTTP代理设置
systemProp.http.proxyHost=proxy.company.com
systemProp.http.proxyPort=8080
systemProp.http.proxyUser=username
systemProp.http.proxyPassword=password

# HTTPS代理设置
systemProp.https.proxyHost=proxy.company.com
systemProp.https.proxyPort=8443
systemProp.https.proxyUser=username
systemProp.https.proxyPassword=password

# 不走代理的主机
systemProp.http.nonProxyHosts=localhost|127.0.0.1|*.local
```

### 3.3 Socks代理配置


**🔌 Socks代理设置**
```properties
# Socks代理配置
systemProp.socksProxyHost=proxy.company.com
systemProp.socksProxyPort=1080
systemProp.socksProxyVersion=5

# 如需认证
systemProp.java.net.socks.username=username
systemProp.java.net.socks.password=password
```

### 3.4 仓库镜像配置


**🏪 仓库配置（build.gradle）**
```gradle
repositories {
    // 使用阿里云镜像
    maven {
        url 'https://maven.aliyun.com/repository/central'
    }
    
    // 华为云镜像
    maven {
        url 'https://repo.huaweicloud.com/repository/maven/'
    }
    
    // 腾讯云镜像
    maven {
        url 'https://mirrors.cloud.tencent.com/nexus/repository/maven-public/'
    }
    
    // 备用：官方仓库
    mavenCentral()
}
```

---

## 4. 🎯 中央仓库代理访问配置


### 4.1 常用镜像仓库对比


**📊 镜像仓库性能对比**

| **镜像源** | **速度** | **稳定性** | **同步延迟** | **推荐指数** |
|-----------|---------|-----------|-------------|-------------|
| 🟢 **阿里云** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 2-4小时 | ⭐⭐⭐⭐⭐ |
| 🟢 **华为云** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 4-6小时 | ⭐⭐⭐⭐ |
| 🟡 **腾讯云** | ⭐⭐⭐ | ⭐⭐⭐ | 6-12小时 | ⭐⭐⭐ |
| 🔴 **官方源** | ⭐ | ⭐⭐⭐⭐⭐ | 实时 | ⭐⭐ |

### 4.2 动态代理切换


**🔄 Maven多镜像配置**
```xml
<mirrors>
  <!-- 主镜像：阿里云 -->
  <mirror>
    <id>aliyun-primary</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven Primary</name>
    <url>https://maven.aliyun.com/repository/central</url>
  </mirror>
  
  <!-- 备用镜像：华为云 -->
  <mirror>
    <id>huawei-backup</id>
    <mirrorOf>central</mirrorOf>
    <name>Huawei Maven Backup</name>
    <url>https://repo.huaweicloud.com/repository/maven/</url>
  </mirror>
</mirrors>
```

**⚡ Gradle优雅降级配置**
```gradle
repositories {
    maven {
        url 'https://maven.aliyun.com/repository/central'
        // 连接失败时的降级策略
        allowInsecureProtocol = false
    }
    
    maven {
        url 'https://repo.huaweicloud.com/repository/maven/'
    }
    
    // 最后备选
    mavenCentral()
}
```

---

## 5. 🏢 私有仓库代理设置


### 5.1 企业私有仓库访问


**🔐 私有仓库认证配置**

**Maven私有仓库配置**
```xml
<servers>
  <server>
    <id>company-nexus</id>
    <username>dev-user</username>
    <password>dev-password</password>
  </server>
</servers>

<repositories>
  <repository>
    <id>company-nexus</id>
    <name>Company Nexus Repository</name>
    <url>http://nexus.company.com:8081/repository/maven-public/</url>
    <releases>
      <enabled>true</enabled>
    </releases>
    <snapshots>
      <enabled>true</enabled>
    </snapshots>
  </repository>
</repositories>
```

**Gradle私有仓库配置**
```gradle
repositories {
    maven {
        url 'http://nexus.company.com:8081/repository/maven-public/'
        credentials {
            username 'dev-user'
            password 'dev-password'
        }
    }
}
```

### 5.2 私有仓库与代理结合


**🔗 混合配置策略**
```
访问优先级：
私有仓库（内网直连）> 国内镜像（代理加速）> 官方仓库（代理访问）
```

**Maven混合配置**
```xml
<mirrors>
  <!-- 仅对外部仓库使用镜像 -->
  <mirror>
    <id>aliyun-external</id>
    <mirrorOf>external:*,!company-nexus</mirrorOf>
    <url>https://maven.aliyun.com/repository/central</url>
  </mirror>
</mirrors>

<repositories>
  <!-- 私有仓库直接访问 -->
  <repository>
    <id>company-nexus</id>
    <url>http://nexus.internal.com:8081/repository/maven-public/</url>
  </repository>
</repositories>
```

---

## 6. ⚡ 构建依赖下载优化


### 6.1 依赖版本锁定策略


**🔒 版本锁定最佳实践**

**Maven版本锁定**
```xml
<dependencyManagement>
  <dependencies>
    <!-- Spring Boot BOM -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-dependencies</artifactId>
      <version>2.7.0</version>
      <type>pom</type>
      <scope>import</scope>
    </dependency>
  </dependencies>
</dependencyManagement>
```

**Gradle版本锁定**
```gradle
dependencyLocking {
    lockAllConfigurations()
}

// 生成锁定文件
gradle dependencies --write-locks
```

### 6.2 本地缓存优化


**📦 缓存策略配置**

| **构建工具** | **缓存路径** | **清理命令** |
|-------------|-------------|-------------|
| **Maven** | `~/.m2/repository` | `mvn dependency:purge-local-repository` |
| **Gradle** | `~/.gradle/caches` | `gradle clean --delete-cache` |

**💾 缓存空间管理**
```bash
# 查看Maven缓存大小
du -sh ~/.m2/repository

# 清理Gradle缓存
gradle clean
gradle --stop  # 停止守护进程
rm -rf ~/.gradle/caches
```

### 6.3 并行下载配置


**🚀 Maven并行下载**
```xml
<properties>
  <!-- 并行线程数 -->
  <maven.artifact.threads>10</maven.artifact.threads>
</properties>
```

**⚡ Gradle并行配置**
```properties
# gradle.properties
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.daemon=true
org.gradle.workers.max=4
```

---

## 7. 🏗️ 企业级配置模板


### 7.1 企业Maven配置模板


**🏢 完整企业配置**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0">
  <!-- 代理配置 -->
  <proxies>
    <proxy>
      <id>company-proxy</id>
      <active>true</active>
      <protocol>http</protocol>
      <host>proxy.company.com</host>
      <port>8080</port>
      <username>${env.PROXY_USER}</username>
      <password>${env.PROXY_PASS}</password>
      <nonProxyHosts>*.company.com|localhost|127.0.0.1</nonProxyHosts>
    </proxy>
  </proxies>

  <!-- 服务器认证 -->
  <servers>
    <server>
      <id>company-nexus</id>
      <username>${env.NEXUS_USER}</username>
      <password>${env.NEXUS_PASS}</password>
    </server>
  </servers>

  <!-- 镜像配置 -->
  <mirrors>
    <mirror>
      <id>company-mirror</id>
      <mirrorOf>external:*,!company-nexus</mirrorOf>
      <url>https://maven.aliyun.com/repository/central</url>
    </mirror>
  </mirrors>

  <!-- 配置文件 -->
  <profiles>
    <profile>
      <id>company-repos</id>
      <repositories>
        <repository>
          <id>company-nexus</id>
          <url>http://nexus.company.com:8081/repository/maven-public/</url>
          <releases><enabled>true</enabled></releases>
          <snapshots><enabled>true</enabled></snapshots>
        </repository>
      </repositories>
    </profile>
  </profiles>

  <activeProfiles>
    <activeProfile>company-repos</activeProfile>
  </activeProfiles>
</settings>
```

### 7.2 企业Gradle配置模板


**🏭 完整企业配置**
```properties
# gradle.properties

# 代理配置
systemProp.http.proxyHost=proxy.company.com
systemProp.http.proxyPort=8080
systemProp.https.proxyHost=proxy.company.com
systemProp.https.proxyPort=8080
systemProp.http.nonProxyHosts=*.company.com|localhost|127.0.0.1

# 性能优化
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.daemon=true
org.gradle.workers.max=4

# 内存设置
org.gradle.jvmargs=-Xmx2g -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError

# 缓存配置
org.gradle.caching=true
```

### 7.3 CI/CD环境配置


**🔄 持续集成配置**
```yaml
# .gitlab-ci.yml Maven示例
build:
  stage: build
  before_script:
    - mkdir -p ~/.m2
    - cp ci/settings.xml ~/.m2/settings.xml
  script:
    - mvn clean package -DskipTests
  cache:
    paths:
      - ~/.m2/repository/
```

**📋 学习检查点**
- [ ] 理解代理配置的基本原理
- [ ] 掌握Maven settings.xml配置
- [ ] 学会Gradle代理设置方法
- [ ] 了解企业级配置最佳实践

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 代理作用：解决网络访问限制，加速依赖下载
🔸 配置文件：Maven用settings.xml，Gradle用gradle.properties  
🔸 镜像优势：使用国内镜像可大幅提升下载速度
🔸 认证配置：企业环境需要正确配置用户认证信息
🔸 缓存管理：合理管理本地缓存，避免磁盘空间浪费
```

### 8.2 关键配置要点


**🔹 Maven配置核心**
```
settings.xml位置：~/.m2/settings.xml
代理配置：<proxies>标签
镜像配置：<mirrors>标签  
认证配置：<servers>标签
仓库配置：<repositories>标签
```

**🔹 Gradle配置核心**
```
配置文件：gradle.properties
代理设置：systemProp.http.proxyHost等
仓库配置：repositories块
性能优化：parallel、daemon等开关
```

**🔹 企业级配置要点**
```
环境变量：使用${env.VAR}引用敏感信息
多环境：通过profile/环境区分配置
安全性：避免明文密码，使用加密存储
监控：配置下载日志，便于问题排查
```

### 8.3 实际应用价值


**🎯 解决的核心问题**
- **网络问题**：解决依赖下载缓慢或失败
- **企业限制**：突破公司网络访问限制  
- **开发效率**：大幅提升构建速度
- **团队协作**：统一团队构建环境

**🚀 性能提升效果**
- **下载速度**：国内镜像比官方源快5-10倍
- **构建时间**：首次构建时间可缩短50-80%
- **稳定性**：减少网络问题导致的构建失败

### 8.4 最佳实践建议


**🎯 一分钟掌握**
1. **优先使用国内镜像**：阿里云、华为云等
2. **配置合理降级**：主镜像+备用镜像+官方源
3. **环境变量管理**：敏感信息不要硬编码

**⚠️ 注意事项**
- 代理配置后要重启IDE才能生效
- 企业网络可能需要IT部门协助配置
- 定期清理本地缓存避免磁盘空间不足
- 版本锁定可以避免依赖冲突问题

**🔥 面试高频**
- Maven和Gradle代理配置的区别
- 如何解决依赖下载失败问题
- 企业级构建环境的最佳实践
- 如何优化大型项目的构建速度

**💎 核心记忆**
```
代理配置三要素：地址端口认证信息
镜像选择重速度：国内优先官方备用  
企业配置讲安全：环境变量密码加密
构建优化抓缓存：并行下载本地存储
```

**🎯 记忆口诀**
> 代理镜像加速跑，Maven Gradle配置好  
> 企业网络有限制，认证密码不能少  
> 缓存清理要及时，构建效率步步高