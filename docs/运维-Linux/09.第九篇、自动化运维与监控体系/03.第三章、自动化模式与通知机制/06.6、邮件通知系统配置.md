---
title: 6、邮件通知系统配置
---
## 📚 目录

1. [邮件通知系统概述](#1-邮件通知系统概述)
2. [mail/mailx命令基础配置](#2-mailmailx命令基础配置)
3. [SMTP服务器连接设置](#3-smtp服务器连接设置)
4. [邮件模板与格式化](#4-邮件模板与格式化)
5. [附件发送机制](#5-附件发送机制)
6. [邮件发送状态检查](#6-邮件发送状态检查)
7. [批量邮件发送控制](#7-批量邮件发送控制)
8. [邮件队列管理](#8-邮件队列管理)
9. [反垃圾邮件配置](#9-反垃圾邮件配置)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 📧 邮件通知系统概述


### 1.1 什么是Linux邮件通知系统


**简单理解**：就像给你的Linux服务器配置一个"消息助手"，当系统出现问题或完成任务时，自动发邮件通知你。

```
现实场景类比：
家里的安防系统 → 发现异常 → 发短信通知你
Linux服务器   → 系统异常   → 发邮件通知你

核心作用：
• 系统监控告警：磁盘空间不足、CPU过高
• 任务完成通知：定时备份完成、脚本执行结果
• 安全事件报告：登录异常、权限变更
• 自动化运维：无人值守时的状态汇报
```

### 1.2 邮件通知系统的组成架构


```
Linux邮件通知系统架构图：

┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   应用程序      │ →  │  邮件客户端      │ →  │   SMTP服务器    │
│ (脚本/监控工具) │    │ (mail/mailx)     │    │ (Gmail/企业邮箱) │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                              ↓
                       ┌──────────────────┐
                       │   本地队列       │
                       │ (/var/spool/     │
                       │     mail)        │
                       └──────────────────┘

组件说明：
🔸 应用程序：产生通知需求的源头
🔸 邮件客户端：处理邮件发送的工具
🔸 SMTP服务器：实际投递邮件的服务
🔸 本地队列：临时存储待发送邮件
```

### 1.3 常见应用场景


**实际运维场景**：
- **系统监控**：磁盘使用率超过90%时发送警告
- **备份通知**：每日凌晨数据库备份完成后发送结果
- **安全告警**：检测到SSH暴力破解时立即通知
- **服务状态**：关键服务停止时发送紧急邮件
- **定时报告**：每周系统健康状态汇总报告

---

## 2. 📮 mail/mailx命令基础配置


### 2.1 mail与mailx的区别理解


**简单区分**：
- **mail**：传统的基础邮件工具，功能简单
- **mailx**：增强版邮件工具，支持更多功能（推荐使用）

```bash
# 检查系统中安装的邮件工具
which mail mailx
# 查看版本信息
mail -V 2>/dev/null || mailx -V 2>/dev/null
```

> 💡 **实用建议**：现代Linux系统建议使用`mailx`，它向后兼容`mail`命令，但功能更强大。

### 2.2 mailx基础使用方法


**最简单的发送方式**：
```bash
# 方式1：交互式发送
mailx user@example.com
Subject: 测试邮件
这是邮件正文内容
.
# 输入点号(.)表示结束

# 方式2：非交互式发送（推荐）
echo "服务器状态正常" | mailx -s "系统监控报告" admin@company.com

# 方式3：从文件读取内容
mailx -s "日志报告" admin@company.com < /var/log/system-report.txt
```

**常用参数说明**：
- `-s`：指定邮件主题
- `-a`：添加附件
- `-r`：指定发件人地址
- `-c`：添加抄送收件人
- `-b`：添加密送收件人

### 2.3 mailx配置文件设置


**全局配置文件位置**：`/etc/mail.rc` 或 `/etc/mailx.rc`

```bash
# 编辑全局配置
sudo vim /etc/mail.rc

# 基础配置内容
set smtp=smtps://smtp.gmail.com:465
set smtp-auth=login
set smtp-auth-user=your-email@gmail.com
set smtp-auth-password=your-app-password
set from="监控系统 <monitor@yourcompany.com>"
set ssl-verify=ignore
set nss-config-dir=/etc/pki/nssdb/
```

**用户级配置文件**：`~/.mailrc`

```bash
# 用户个人配置（优先级高于全局配置）
cat > ~/.mailrc << 'EOF'
# 发送方配置
set from="服务器监控 <monitor@example.com>"
set smtp=smtp://localhost:25

# 编码设置
set charset=utf-8
set sendcharsets=utf-8

# 界面设置
set verbose
EOF
```

---

## 3. 🌐 SMTP服务器连接设置


### 3.1 常见邮箱服务商SMTP配置


**Gmail邮箱配置**：
```bash
# Gmail SMTP设置
set smtp=smtps://smtp.gmail.com:465
set smtp-auth=login
set smtp-auth-user=your-account@gmail.com
set smtp-auth-password=your-app-password
set ssl-verify=ignore
set from="服务器监控 <your-account@gmail.com>"
```

> ⚠️ **安全提醒**：Gmail需要使用应用专用密码，不能使用普通登录密码。

**企业邮箱配置示例**：
```bash
# 腾讯企业邮箱
set smtp=smtp://smtp.exmail.qq.com:587
set smtp-auth=login
set smtp-auth-user=admin@company.com
set smtp-auth-password=email-password
set from="系统管理员 <admin@company.com>"

# 阿里云企业邮箱
set smtp=smtps://smtp.mxhichina.com:465
set smtp-auth=login
set smtp-auth-user=service@company.com  
set smtp-auth-password=email-password
set ssl-verify=ignore
```

### 3.2 SMTP连接测试方法


**基础连接测试**：
```bash
# 测试邮件发送
echo "SMTP连接测试" | mailx -s "测试邮件" test@example.com

# 带详细输出的测试
echo "详细测试" | mailx -v -s "调试测试" test@example.com
```

**连接故障诊断**：
```bash
# 检查网络连通性
telnet smtp.gmail.com 465

# 检查DNS解析
nslookup smtp.gmail.com

# 查看详细日志
tail -f /var/log/maillog
```

### 3.3 SSL/TLS安全连接配置


**安全连接参数详解**：
```bash
# SMTPS（SSL加密，端口465）
set smtp=smtps://smtp.example.com:465
set ssl-verify=ignore               # 忽略证书验证
set nss-config-dir=/etc/pki/nssdb/  # 证书数据库路径

# SMTP+STARTTLS（端口587）
set smtp=smtp://smtp.example.com:587
set smtp-use-starttls=yes           # 启用STARTTLS
set ssl-verify=ignore
```

---

## 4. 📝 邮件模板与格式化


### 4.1 HTML格式邮件发送


**发送HTML格式邮件**：
```bash
# 创建HTML邮件模板
cat > /tmp/report.html << 'EOF'
<html>
<head><meta charset="utf-8"></head>
<body>
<h2>🖥️ 服务器监控报告</h2>
<table border="1" style="border-collapse:collapse;">
<tr><th>监控项</th><th>状态</th><th>数值</th></tr>
<tr><td>CPU使用率</td><td>✅正常</td><td>15%</td></tr>
<tr><td>内存使用率</td><td>⚠️警告</td><td>85%</td></tr>
<tr><td>磁盘使用率</td><td>❌异常</td><td>95%</td></tr>
</table>
<p>详细信息请登录服务器查看。</p>
</body>
</html>
EOF

# 发送HTML邮件
mailx -a "Content-Type: text/html; charset=utf-8" \
      -s "服务器监控报告" \
      admin@company.com < /tmp/report.html
```

### 4.2 邮件模板脚本化


**动态生成邮件内容**：
```bash
#!/bin/bash
# 文件：generate-system-report.sh

# 获取系统信息
CPU_USAGE=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'%' -f1)
MEMORY_USAGE=$(free | grep Mem | awk '{printf "%.1f", $3/$2 * 100.0}')
DISK_USAGE=$(df -h / | awk 'NR==2{print $5}' | cut -d'%' -f1)

# 生成邮件内容
generate_email_content() {
cat << EOF
系统监控报告 - $(date '+%Y-%m-%d %H:%M')

📊 系统状态概览：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔸 CPU使用率：    ${CPU_USAGE}%
🔸 内存使用率：   ${MEMORY_USAGE}%  
🔸 磁盘使用率：   ${DISK_USAGE}%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

$([ $DISK_USAGE -gt 90 ] && echo "⚠️ 警告：磁盘空间不足！" || echo "✅ 系统运行正常")

详细监控数据请查看附件。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
发送时间：$(date)
服务器：$(hostname)
EOF
}

# 发送邮件
generate_email_content | mailx -s "【$(hostname)】系统监控报告" admin@company.com
```

### 4.3 中文邮件编码处理


**中文邮件配置**：
```bash
# 在.mailrc中添加中文支持
echo "set charset=utf-8" >> ~/.mailrc
echo "set sendcharsets=utf-8" >> ~/.mailrc

# 发送中文邮件示例
echo "服务器运行正常，CPU使用率12%" | \
mailx -s "$(echo '服务器状态报告' | iconv -t utf-8)" \
      -S charset=utf-8 \
      admin@company.com
```

---

## 5. 📎 附件发送机制


### 5.1 单个附件发送


**基本附件发送语法**：
```bash
# 发送单个附件
echo "请查看附件中的系统日志" | \
mailx -s "系统日志报告" \
      -a /var/log/system.log \
      admin@company.com

# 指定附件显示名称
echo "服务器备份完成" | \
mailx -s "备份通知" \
      -a /backup/db-backup-$(date +%Y%m%d).tar.gz \
      ops-team@company.com
```

### 5.2 多个附件发送


**发送多个附件**：
```bash
# 方式1：多个-a参数
echo "系统维护报告" | \
mailx -s "维护报告" \
      -a /var/log/system.log \
      -a /var/log/security.log \
      -a /tmp/performance-report.txt \
      admin@company.com

# 方式2：使用脚本批量处理
#!/bin/bash
SUBJECT="每日系统报告"
RECIPIENT="admin@company.com"
ATTACH_DIR="/var/reports/daily"

# 构建附件参数
ATTACHMENTS=""
for file in ${ATTACH_DIR}/*.log; do
    [ -f "$file" ] && ATTACHMENTS="$ATTACHMENTS -a $file"
done

# 发送邮件
echo "请查看附件中的每日系统报告" | \
mailx -s "$SUBJECT" $ATTACHMENTS "$RECIPIENT"
```

### 5.3 附件大小与格式控制


**附件大小检查**：
```bash
#!/bin/bash
check_and_send_attachment() {
    local file="$1"
    local max_size=$((10 * 1024 * 1024))  # 10MB限制
    
    if [ ! -f "$file" ]; then
        echo "文件不存在: $file"
        return 1
    fi
    
    file_size=$(stat -c%s "$file")
    if [ $file_size -gt $max_size ]; then
        echo "文件过大，压缩后发送"
        gzip -c "$file" > "${file}.gz"
        echo "附件已压缩" | \
        mailx -s "压缩日志报告" -a "${file}.gz" admin@company.com
    else
        echo "发送原文件" | \
        mailx -s "日志报告" -a "$file" admin@company.com
    fi
}

check_and_send_attachment "/var/log/large-system.log"
```

---

## 6. ✅ 邮件发送状态检查


### 6.1 发送状态监控方法


**检查邮件发送状态**：
```bash
# 查看邮件队列状态
mailq

# 查看邮件日志
tail -f /var/log/maillog

# 检查特定邮件的发送状态
grep "your-email@domain.com" /var/log/maillog | tail -5
```

**邮件发送状态判断**：
```bash
#!/bin/bash
send_email_with_status() {
    local recipient="$1"
    local subject="$2"
    local message="$3"
    
    # 发送邮件并检查返回值
    if echo "$message" | mailx -s "$subject" "$recipient"; then
        echo "✅ 邮件发送成功: $subject -> $recipient"
        logger "EMAIL_SENT: $subject to $recipient"
    else
        echo "❌ 邮件发送失败: $subject -> $recipient"
        logger "EMAIL_FAILED: $subject to $recipient"
        return 1
    fi
}

# 使用示例
send_email_with_status "admin@company.com" "测试邮件" "这是测试内容"
```

### 6.2 邮件发送重试机制


**自动重试发送**：
```bash
#!/bin/bash
send_email_with_retry() {
    local max_attempts=3
    local attempt=1
    local recipient="$1"
    local subject="$2" 
    local message="$3"
    
    while [ $attempt -le $max_attempts ]; do
        echo "尝试发送邮件 (第${attempt}次)"
        
        if echo "$message" | mailx -s "$subject" "$recipient" 2>/dev/null; then
            echo "✅ 邮件发送成功"
            return 0
        else
            echo "❌ 第${attempt}次发送失败"
            attempt=$((attempt + 1))
            [ $attempt -le $max_attempts ] && sleep 10
        fi
    done
    
    echo "🚫 邮件发送彻底失败，已重试${max_attempts}次"
    return 1
}
```

### 6.3 发送结果日志记录


**详细日志记录**：
```bash
#!/bin/bash
LOG_FILE="/var/log/email-notifications.log"

log_email_activity() {
    local action="$1"
    local recipient="$2"
    local subject="$3"
    local status="$4"
    
    echo "$(date '+%Y-%m-%d %H:%M:%S') [$action] TO:$recipient SUBJECT:$subject STATUS:$status" >> "$LOG_FILE"
}

# 发送邮件并记录日志
send_and_log_email() {
    local recipient="$1"
    local subject="$2"
    local message="$3"
    
    if echo "$message" | mailx -s "$subject" "$recipient"; then
        log_email_activity "SEND" "$recipient" "$subject" "SUCCESS"
    else
        log_email_activity "SEND" "$recipient" "$subject" "FAILED"
    fi
}
```

---

## 7. 📊 批量邮件发送控制


### 7.1 批量发送基础实现


**简单批量发送脚本**：
```bash
#!/bin/bash
# 文件：bulk-email-sender.sh

RECIPIENT_LIST="/etc/email-lists/admin-team.txt"
SUBJECT="系统维护通知"
MESSAGE="系统将于今晚22:00进行维护，预计维护时间2小时。"

# 读取收件人列表并逐个发送
while IFS= read -r email; do
    # 跳过空行和注释行
    [[ -z "$email" || "$email" =~ ^# ]] && continue
    
    echo "发送邮件到: $email"
    echo "$MESSAGE" | mailx -s "$SUBJECT" "$email"
    
    # 发送间隔，避免被认为是垃圾邮件
    sleep 2
done < "$RECIPIENT_LIST"
```

**收件人列表文件示例**：
```bash
# 文件：/etc/email-lists/admin-team.txt
# 管理员邮箱列表
admin1@company.com
admin2@company.com
ops@company.com
# support@company.com  # 临时禁用
backup-admin@company.com
```

### 7.2 发送频率控制


**智能发送频率控制**：
```bash
#!/bin/bash
RATE_LIMIT=5    # 每分钟最多发送5封
SENT_COUNT=0
START_TIME=$(date +%s)

send_with_rate_limit() {
    local current_time=$(date +%s)
    local elapsed=$((current_time - START_TIME))
    
    # 如果一分钟内发送达到限制，等待
    if [ $SENT_COUNT -ge $RATE_LIMIT ] && [ $elapsed -lt 60 ]; then
        local wait_time=$((60 - elapsed))
        echo "⏳ 达到发送频率限制，等待 ${wait_time} 秒..."
        sleep $wait_time
        SENT_COUNT=0
        START_TIME=$(date +%s)
    fi
    
    # 发送邮件
    echo "$2" | mailx -s "$3" "$1"
    SENT_COUNT=$((SENT_COUNT + 1))
    echo "✅ 已发送 $1 (本轮第 $SENT_COUNT 封)"
}
```

### 7.3 分组发送管理


**按组发送邮件**：
```bash
#!/bin/bash
# 定义不同的邮件组
declare -A EMAIL_GROUPS

EMAIL_GROUPS[admin]="admin1@company.com admin2@company.com"
EMAIL_GROUPS[dev]="dev1@company.com dev2@company.com"
EMAIL_GROUPS[ops]="ops1@company.com ops2@company.com"

send_to_group() {
    local group_name="$1"
    local subject="$2"
    local message="$3"
    
    if [ -z "${EMAIL_GROUPS[$group_name]}" ]; then
        echo "❌ 未找到邮件组: $group_name"
        return 1
    fi
    
    echo "📧 向 $group_name 组发送邮件: $subject"
    
    for email in ${EMAIL_GROUPS[$group_name]}; do
        echo "$message" | mailx -s "$subject" "$email"
        echo "  ✅ $email"
        sleep 1
    done
}

# 使用示例
send_to_group "admin" "服务器异常告警" "服务器CPU使用率异常，请及时处理"
```

---

## 8. 📬 邮件队列管理


### 8.1 邮件队列基础操作


**查看和管理邮件队列**：
```bash
# 查看当前邮件队列
mailq

# 删除邮件队列中的所有邮件
sudo postsuper -d ALL

# 删除特定收件人的邮件
sudo postsuper -d ALL deferred
sudo postsuper -d ALL active

# 强制处理队列中的邮件
sudo postqueue -f
```

**邮件队列状态解读**：
```bash
# 查看队列状态的详细信息
postqueue -p

# 队列状态说明:
# active    - 正在发送的邮件
# deferred  - 延迟发送的邮件（通常是临时失败）
# hold      - 被管理员暂停的邮件
# incoming  - 刚接收到的邮件
```

### 8.2 队列监控脚本


**邮件队列监控**：
```bash
#!/bin/bash
# 文件：monitor-mail-queue.sh

QUEUE_THRESHOLD=50
LOG_FILE="/var/log/mail-queue-monitor.log"

check_mail_queue() {
    local queue_count=$(mailq | grep -c "^[A-F0-9]")
    local timestamp=$(date '+%Y-%m-%d %H:%M:%S')
    
    if [ "$queue_count" -gt "$QUEUE_THRESHOLD" ]; then
        local alert_msg="⚠️ 邮件队列异常：当前有 $queue_count 封邮件待处理"
        echo "$timestamp $alert_msg" >> "$LOG_FILE"
        
        # 发送告警邮件（注意避免循环）
        echo "$alert_msg" | \
        mailx -s "邮件队列告警" admin@company.com 2>/dev/null || true
        
        echo "$alert_msg"
        return 1
    else
        echo "$timestamp 邮件队列正常：$queue_count 封邮件" >> "$LOG_FILE"
        echo "✅ 邮件队列正常：$queue_count 封邮件"
        return 0
    fi
}

# 执行检查
check_mail_queue
```

### 8.3 队列清理与维护


**定期队列清理**：
```bash
#!/bin/bash
# 文件：cleanup-mail-queue.sh

cleanup_old_mail() {
    local days_old="${1:-7}"  # 默认清理7天前的邮件
    
    echo "🧹 开始清理 $days_old 天前的邮件..."
    
    # 查找并删除老旧邮件
    find /var/spool/postfix/deferred -type f -mtime +$days_old -delete
    find /var/spool/postfix/active -type f -mtime +$days_old -delete
    
    # 重新加载队列
    sudo postqueue -f
    
    # 记录清理结果
    local current_count=$(mailq | grep -c "^[A-F0-9]" || echo "0")
    echo "✅ 清理完成，当前队列中有 $current_count 封邮件"
    
    # 记录到日志
    echo "$(date): 清理了 $days_old 天前的邮件，剩余 $current_count 封" >> /var/log/mail-cleanup.log
}

# 执行清理（保留3天内的邮件）
cleanup_old_mail 3
```

---

## 9. 🛡️ 反垃圾邮件配置


### 9.1 发件人身份验证


**SPF记录配置指导**：
```bash
# DNS TXT记录示例（在域名DNS管理中添加）
# 域名: yourcompany.com
# 类型: TXT
# 值: v=spf1 ip4:192.168.1.100 include:_spf.google.com ~all

# 检查SPF记录
dig TXT yourcompany.com | grep spf

# 测试SPF验证
echo "测试邮件" | mailx -s "SPF测试" -r "monitor@yourcompany.com" test@example.com
```

**DKIM签名配置**：
```bash
# 安装DKIM工具
sudo apt install opendkim opendkim-tools

# 生成DKIM密钥
sudo opendkim-genkey -t -s mail -d yourcompany.com

# 查看生成的公钥（需要添加到DNS）
sudo cat /etc/opendkim/keys/yourcompany.com/mail.txt
```

### 9.2 邮件内容过滤


**避免垃圾邮件特征**：
```bash
#!/bin/bash
# 文件：send-clean-email.sh

validate_email_content() {
    local subject="$1"
    local content="$2"
    
    # 检查垃圾邮件关键词
    local spam_keywords=("FREE" "URGENT" "CLICK HERE" "LIMITED TIME")
    
    for keyword in "${spam_keywords[@]}"; do
        if echo "$subject $content" | grep -qi "$keyword"; then
            echo "⚠️ 警告：邮件内容包含可能的垃圾邮件关键词: $keyword"
        fi
    done
    
    # 检查过多的大写字母
    local caps_ratio=$(echo "$subject" | grep -o '[A-Z]' | wc -l)
    local total_chars=$(echo "$subject" | wc -c)
    
    if [ $total_chars -gt 0 ] && [ $caps_ratio -gt $((total_chars / 2)) ]; then
        echo "⚠️ 警告：主题包含过多大写字母"
    fi
}

send_clean_email() {
    local recipient="$1"
    local subject="$2"
    local message="$3"
    
    # 验证邮件内容
    validate_email_content "$subject" "$message"
    
    # 添加标准邮件头
    local clean_subject="[$(hostname)] $subject"
    
    # 发送邮件
    echo "$message" | mailx -s "$clean_subject" "$recipient"
}
```

### 9.3 发送频率和声誉管理


**维护良好的发送声誉**：
```bash
#!/bin/bash
# 文件：reputation-manager.sh

REPUTATION_LOG="/var/log/email-reputation.log"
DAILY_LIMIT=100
DATE_TODAY=$(date +%Y-%m-%d)

check_daily_limit() {
    local sent_today=$(grep "$DATE_TODAY" "$REPUTATION_LOG" | grep "SENT" | wc -l)
    
    if [ "$sent_today" -ge "$DAILY_LIMIT" ]; then
        echo "🚫 今日发送限制已达到 ($sent_today/$DAILY_LIMIT)"
        return 1
    else
        echo "✅ 今日发送额度正常 ($sent_today/$DAILY_LIMIT)"
        return 0
    fi
}

record_email_sent() {
    local recipient="$1"
    local subject="$2"
    local status="$3"
    
    echo "$(date '+%Y-%m-%d %H:%M:%S') SENT $recipient \"$subject\" $status" >> "$REPUTATION_LOG"
}

# 使用示例
if check_daily_limit; then
    echo "系统监控报告" | mailx -s "监控报告" admin@company.com
    record_email_sent "admin@company.com" "监控报告" "SUCCESS"
else
    echo "发送被限制，跳过非紧急邮件"
fi
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的基本概念


```
🔸 邮件通知系统：Linux服务器的自动化消息助手
🔸 mailx命令：现代Linux推荐使用的邮件发送工具
🔸 SMTP配置：连接外部邮件服务器的桥梁
🔸 队列管理：确保邮件可靠发送的缓冲机制
🔸 反垃圾配置：避免邮件被误判为垃圾邮件的关键
```

### 10.2 关键配置要点


**🔹 SMTP服务器配置核心**
```
正确配置顺序：
1. 选择合适的邮件服务商（Gmail、企业邮箱）
2. 获取正确的SMTP服务器地址和端口
3. 配置认证信息（用户名、应用密码）
4. 设置加密方式（SSL/TLS）
5. 测试连接并验证发送功能
```

**🔹 邮件内容最佳实践**
```
内容规范：
• 主题明确：包含服务器名和事件类型
• 内容简洁：重点信息突出，避免冗余
• 格式友好：使用适当的格式化和符号
• 编码正确：确保中文内容正常显示
• 附件合理：控制大小，压缩大文件
```

**🔹 发送频率控制原则**
```
频率管理：
• 紧急告警：立即发送，但避免重复
• 定期报告：固定时间发送，如每日汇总
• 批量通知：控制发送间隔，避免触发限制
• 队列监控：定期检查队列状态，及时处理堆积
```

### 10.3 实际应用指导


**📊 监控告警邮件模板**
```bash
# 实用的监控告警脚本框架
#!/bin/bash
send_alert() {
    local alert_type="$1"    # CPU/MEMORY/DISK
    local current_value="$2"  # 当前值
    local threshold="$3"     # 阈值
    
    local subject="🚨 [$(hostname)] ${alert_type}告警"
    local message="
📊 系统监控告警报告
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
告警类型: $alert_type
当前值:   $current_value
告警阈值: $threshold
服务器:   $(hostname)
时间:     $(date)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
请及时登录服务器检查处理。
"
    
    echo "$message" | mailx -s "$subject" admin@company.com
}

# 使用示例
send_alert "CPU" "85%" "80%"
```

**🔧 常见问题解决方案**
```
发送失败排查步骤：
1. 检查网络连接：ping smtp.example.com
2. 验证端口通信：telnet smtp.example.com 587
3. 确认认证信息：用户名和密码是否正确
4. 查看详细日志：tail -f /var/log/maillog
5. 测试简单发送：echo "test" | mailx -v user@example.com

中文乱码解决：
1. 设置系统编码：export LANG=zh_CN.UTF-8
2. 配置mailx编码：set charset=utf-8
3. 发送时指定编码：-S charset=utf-8
```

### 10.4 安全注意事项


> ⚠️ **安全提醒**：
> - 不要在配置文件中使用明文密码
> - 使用应用专用密码，不要使用主账户密码  
> - 定期轮换邮箱密码
> - 限制配置文件的访问权限：`chmod 600 ~/.mailrc`

> 💡 **性能建议**：
> - 批量发送时控制频率，避免触发限制
> - 大附件先压缩再发送
> - 定期清理邮件队列，避免堆积
> - 监控发送成功率，及时调整配置

**核心记忆口诀**：
- 邮件通知自动化，监控告警不掉线
- SMTP配置要准确，认证加密都重要  
- 内容格式要规范，频率控制防封号
- 队列管理保可靠，日志监控助排查