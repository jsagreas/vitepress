---
title: 14、容器和云原生监控
---
## 📚 目录

1. [容器监控基础概念](#1-容器监控基础概念)
2. [Docker容器监控](#2-Docker容器监控)
3. [Kubernetes集群监控](#3-Kubernetes集群监控)
4. [Pod和Node监控详解](#4-Pod和Node监控详解)
5. [容器资源使用监控](#5-容器资源使用监控)
6. [服务网格监控](#6-服务网格监控)
7. [云平台监控集成](#7-云平台监控集成)
8. [容器化监控部署](#8-容器化监控部署)
9. [云原生监控最佳实践](#9-云原生监控最佳实践)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🔍 容器监控基础概念


### 1.1 什么是容器监控


**容器监控的本质**：
监控运行在容器中的应用程序和容器本身的性能、资源使用情况、健康状态

```
传统监控 vs 容器监控：

传统服务器监控：
┌─────────────────┐
│   应用程序       │ ← 直接部署在OS上
├─────────────────┤
│   操作系统       │ ← 监控CPU、内存、磁盘
├─────────────────┤
│   物理硬件       │
└─────────────────┘

容器监控：
┌─────────┬─────────┬─────────┐
│ 容器A    │ 容器B    │ 容器C    │ ← 每个容器独立监控
├─────────┼─────────┼─────────┤
│     Docker Engine        │ ← 容器引擎层监控
├─────────────────────────┤
│      主机操作系统         │ ← 宿主机监控
├─────────────────────────┤
│      物理/虚拟硬件        │
└─────────────────────────┘
```

**容器监控的特殊挑战**：
- **动态性**：容器随时可能启动、停止、重启
- **短暂性**：容器生命周期短，日志容易丢失
- **密度高**：单台主机运行大量容器
- **层次多**：需要监控容器、镜像、主机多个层面

### 1.2 监控层次结构


**四层监控体系**：
```
应用层监控：
├── 业务指标（订单量、用户数）
├── 应用性能（响应时间、错误率）
└── 自定义指标

容器层监控：
├── 容器状态（运行/停止/重启）
├── 容器资源（CPU、内存、网络、磁盘）
└── 容器日志

编排层监控（K8s）：
├── 集群状态（节点健康、API服务器）
├── Pod状态（调度、运行、终止）
└── 服务发现（Service、Ingress）

基础设施监控：
├── 主机资源（物理机/虚拟机）
├── 网络连通性
└── 存储性能
```

### 1.3 关键监控指标


**🔸 核心指标分类**：

| 指标类型 | 具体指标 | 作用说明 | 告警阈值建议 |
|---------|---------|---------|-------------|
| **资源指标** | `CPU使用率` | 容器CPU消耗情况 | `>80%` |
| | `内存使用率` | 容器内存占用 | `>85%` |
| | `磁盘I/O` | 容器存储性能 | `IOPS>1000` |
| | `网络流量` | 容器网络负载 | `带宽>80%` |
| **健康指标** | `容器状态` | 运行/停止/异常 | `非Running` |
| | `重启次数` | 容器稳定性 | `>5次/小时` |
| | `存活检查` | 应用响应能力 | `失败>3次` |
| **业务指标** | `请求延迟` | 应用响应速度 | `>500ms` |
| | `错误率` | 应用可靠性 | `>1%` |
| | `并发数` | 应用负载能力 | `>设计值80%` |

---

## 2. 🐳 Docker容器监控


### 2.1 Docker内置监控工具


**docker stats 命令**：
实时查看容器资源使用情况

```bash
# 查看所有运行容器的资源使用
docker stats

# 查看指定容器的资源使用
docker stats nginx-container

# 输出格式化的统计信息
docker stats --format "table {{.Container}}\t{{.CPUPerc}}\t{{.MemUsage}}"
```

**输出解释**：
```
CONTAINER ID   NAME            CPU %    MEM USAGE/LIMIT     MEM %
a1b2c3d4e5f6   nginx-app       2.15%    45.5MiB / 1.952GiB  2.28%
b2c3d4e5f6a1   mysql-db        8.42%    312.1MiB / 1.952GiB 15.62%

字段含义：
- CPU %：CPU使用百分比
- MEM USAGE/LIMIT：内存使用量/限制量
- MEM %：内存使用百分比
- NET I/O：网络输入/输出流量
- BLOCK I/O：磁盘读写流量
```

**docker logs 监控**：
```bash
# 查看容器实时日志
docker logs -f nginx-container

# 查看最近100行日志
docker logs --tail 100 nginx-container

# 带时间戳的日志
docker logs -t nginx-container
```

### 2.2 Docker监控API


**Docker API监控**：
通过Docker API获取详细的容器信息

```bash
# 获取容器详细信息
docker inspect nginx-container

# 获取容器统计信息（JSON格式）
curl --unix-socket /var/run/docker.sock \
  http://localhost/containers/nginx-container/stats
```

**关键监控字段**：
```json
{
  "cpu_stats": {
    "cpu_usage": {
      "total_usage": 25769230000,
      "usage_in_kernelmode": 4570000000
    }
  },
  "memory_stats": {
    "usage": 47710208,
    "limit": 2097152000
  },
  "networks": {
    "eth0": {
      "rx_bytes": 1338,
      "tx_bytes": 648
    }
  }
}
```

### 2.3 容器健康检查


**Dockerfile健康检查**：
```dockerfile
FROM nginx:alpine

# 定义健康检查
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD curl -f http://localhost/ || exit 1

EXPOSE 80
```

**运行时健康检查配置**：
```bash
# 启动容器时配置健康检查
docker run -d \
  --name web-app \
  --health-cmd="curl -f http://localhost:8080/health || exit 1" \
  --health-interval=30s \
  --health-timeout=10s \
  --health-retries=3 \
  my-web-app:latest
```

**健康状态查看**：
```bash
# 查看容器健康状态
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Health}}"

# 健康状态说明：
# healthy：健康检查通过
# unhealthy：健康检查失败
# starting：容器启动中，尚未开始健康检查
```

---

## 3. ☸️ Kubernetes集群监控


### 3.1 K8s监控架构


**Kubernetes监控生态**：
```
┌─────────────────────────────────────────┐
│              监控数据流                  │
├─────────────────────────────────────────┤
│  应用 → cAdvisor → kubelet → API Server │
│    ↓                                   │
│  Prometheus ← metrics-server           │
│    ↓                                   │
│  Grafana                              │
└─────────────────────────────────────────┘

监控组件说明：
• cAdvisor：容器资源使用数据收集
• kubelet：节点级别的资源统计
• metrics-server：集群级别的资源聚合
• kube-state-metrics：K8s对象状态信息
```

### 3.2 核心监控组件


**metrics-server 部署**：
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrics-server
  namespace: kube-system
spec:
  selector:
    matchLabels:
      k8s-app: metrics-server
  template:
    metadata:
      labels:
        k8s-app: metrics-server
    spec:
      containers:
      - name: metrics-server
        image: k8s.gcr.io/metrics-server/metrics-server:v0.6.1
        args:
        - --cert-dir=/tmp
        - --secure-port=4443
        - --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname
        - --kubelet-use-node-status-port
        - --metric-resolution=15s
```

**kube-state-metrics 配置**：
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-state-metrics
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-state-metrics
  template:
    metadata:
      labels:
        app: kube-state-metrics
    spec:
      containers:
      - name: kube-state-metrics
        image: quay.io/coreos/kube-state-metrics:v2.5.0
        ports:
        - containerPort: 8080
          name: http-metrics
        - containerPort: 8081
          name: telemetry
```

### 3.3 集群状态监控


**kubectl监控命令**：
```bash
# 查看节点状态
kubectl get nodes -o wide

# 查看Pod资源使用（需要metrics-server）
kubectl top pods --all-namespaces

# 查看节点资源使用
kubectl top nodes

# 查看集群事件
kubectl get events --sort-by='.metadata.creationTimestamp'

# 查看资源配额使用情况
kubectl describe resourcequota -n production
```

**集群健康检查**：
```bash
# 检查API服务器状态
kubectl get cs

# 检查所有系统Pod状态  
kubectl get pods -n kube-system

# 检查网络插件状态
kubectl get pods -n kube-system -l k8s-app=calico-node
```

---

## 4. 🏠 Pod和Node监控详解


### 4.1 Pod生命周期监控


**Pod状态阶段**：
```
Pod生命周期：
Pending → ContainerCreating → Running → Terminating → Succeeded/Failed

┌──────────┐    ┌─────────────────┐    ┌─────────┐
│ Pending  │ → │ ContainerCreating │ → │ Running │
└──────────┘    └─────────────────┘    └─────────┘
                                           │
                                           ▼
┌───────────────┐    ┌──────────────┐
│ Succeeded     │ ← │ Terminating  │
│ Failed        │    │              │
└───────────────┘    └──────────────┘

状态含义：
• Pending：Pod已创建，但容器镜像未创建或调度中
• ContainerCreating：Pod已调度，正在创建容器
• Running：Pod中至少有一个容器正在运行
• Terminating：Pod正在被删除
• Succeeded：Pod中所有容器已成功终止
• Failed：Pod中至少有一个容器终止失败
```

**Pod监控指标**：
```bash
# 查看Pod详细状态
kubectl describe pod nginx-pod

# 关键状态信息：
# Conditions: 
#   - Ready: True/False (Pod是否准备就绪)
#   - ContainersReady: True/False (容器是否就绪)
#   - PodScheduled: True/False (Pod是否已调度)

# 查看Pod资源使用
kubectl top pod nginx-pod --containers

# 查看Pod事件
kubectl get events --field-selector involvedObject.name=nginx-pod
```

### 4.2 Node节点监控


**节点健康状态**：
```bash
# 查看节点详细信息
kubectl describe node worker-01

# 关键状态指标：
# Conditions:
#   - Ready: 节点是否健康且准备接收Pod
#   - MemoryPressure: 节点内存是否压力过大
#   - DiskPressure: 节点磁盘是否空间不足
#   - PIDPressure: 节点进程数是否过多
#   - NetworkUnavailable: 节点网络是否不可用
```

**节点资源监控**：
```
节点资源分配视图：
┌─────────────────────────────────────┐
│         Node: worker-01              │
├─────────────────────────────────────┤
│ Total CPU: 4 cores                  │
│ Allocated: 2.5 cores (62.5%)       │
│ Available: 1.5 cores                │
├─────────────────────────────────────┤
│ Total Memory: 8Gi                  │ 
│ Allocated: 5.2Gi (65%)             │
│ Available: 2.8Gi                   │
├─────────────────────────────────────┤
│ Total Pods: 110 (max)              │
│ Running Pods: 28                   │
│ Available: 82                      │
└─────────────────────────────────────┘
```

### 4.3 容器级别监控


**容器监控维度**：

| 监控维度 | 关键指标 | 监控方法 | 异常处理 |
|---------|---------|---------|---------|
| **资源使用** | CPU/内存/存储 | `kubectl top` | 扩容或优化 |
| **健康状态** | 存活性/就绪性检查 | Probe配置 | 重启容器 |
| **日志输出** | 应用日志/错误日志 | `kubectl logs` | 问题排查 |
| **网络连接** | 端口监听/服务调用 | 服务监控 | 网络诊断 |

**存活性和就绪性探针**：
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: web-app
spec:
  containers:
  - name: app
    image: nginx
    # 存活性探针 - 判断容器是否需要重启
    livenessProbe:
      httpGet:
        path: /healthz
        port: 8080
      initialDelaySeconds: 15    # 启动后15秒开始检查
      periodSeconds: 20          # 每20秒检查一次
      timeoutSeconds: 5          # 检查超时5秒
      failureThreshold: 3        # 连续失败3次重启
    
    # 就绪性探针 - 判断容器是否准备接收流量
    readinessProbe:
      httpGet:
        path: /ready
        port: 8080
      initialDelaySeconds: 5     # 启动后5秒开始检查
      periodSeconds: 5           # 每5秒检查一次
      timeoutSeconds: 3          # 检查超时3秒
      successThreshold: 1        # 成功1次就认为就绪
      failureThreshold: 3        # 连续失败3次认为未就绪
```

---

## 5. 📊 容器资源使用监控


### 5.1 CPU监控详解


**CPU指标理解**：
```
容器CPU监控指标：

CPU使用率计算：
CPU% = (容器CPU时间 / 系统CPU时间) × 100%

CPU限制和请求：
┌─────────────────────────────────────┐
│     Pod CPU资源模型                  │
├─────────────────────────────────────┤  
│ requests: 500m (0.5 CPU core)      │ ← 保证分配
│ limits: 1000m (1 CPU core)         │ ← 最大使用
├─────────────────────────────────────┤
│ 当前使用: 750m (0.75 CPU core)     │ ← 实际使用
│ 使用率: 75% (相对于limits)          │
└─────────────────────────────────────┘

CPU压力等级：
🟢 正常: < 70%
🟡 警告: 70-85%
🔴 危险: > 85%
```

**CPU监控配置**：
```yaml
# Pod资源配置
apiVersion: v1
kind: Pod
metadata:
  name: cpu-demo
spec:
  containers:
  - name: app
    image: nginx
    resources:
      requests:
        cpu: "500m"        # 请求0.5个CPU核心
        memory: "512Mi"    # 请求512MB内存
      limits:
        cpu: "1"           # 限制1个CPU核心
        memory: "1Gi"      # 限制1GB内存
```

### 5.2 内存监控详解


**内存使用分析**：
```
容器内存监控维度：

┌────────────────────────────────────┐
│        容器内存使用分布             │
├────────────────────────────────────┤
│ RSS (物理内存): 256MB              │ ← 实际占用的物理内存
│ Cache (页面缓存): 128MB            │ ← 文件缓存
│ Swap (交换分区): 0MB               │ ← 交换到磁盘的内存
├────────────────────────────────────┤
│ Working Set: 384MB                 │ ← 活跃使用的内存
│ Memory Limit: 512MB                │ ← 容器内存限制
│ Usage%: 75% (384/512)              │ ← 内存使用率
└────────────────────────────────────┘

内存压力处理：
- 接近limit时触发OOMKilled
- 可以通过水平扩展分散负载
- 或者调整memory limits配置
```

### 5.3 存储和网络监控


**存储I/O监控**：
```bash
# 查看容器磁盘使用情况
docker exec nginx-container df -h

# 监控容器I/O统计
docker stats nginx-container --format \
  "table {{.Container}}\t{{.BlockIO}}"
```

**存储监控指标**：
```
存储性能指标：

磁盘使用率：已用空间/总空间 × 100%
IOPS：每秒I/O操作次数
延迟：I/O操作的平均响应时间
带宽：数据传输速度 (MB/s)

告警阈值建议：
🟢 磁盘使用率 < 80%
🟡 磁盘使用率 80-90% 
🔴 磁盘使用率 > 90%
⚡ IOPS > 基线值 150%
⚡ 延迟 > 50ms
```

**网络监控配置**：
```bash
# 监控容器网络流量
docker stats --format \
  "table {{.Container}}\t{{.NetIO}}" nginx-container

# 在Pod中监控网络连接
kubectl exec -it web-pod -- netstat -tulpn

# 测试Pod网络连通性
kubectl exec -it web-pod -- ping google.com
```

---

## 6. 🕸️ 服务网格监控


### 6.1 服务网格基础


**什么是服务网格监控**：
服务网格(如Istio、Linkerd)为微服务架构提供流量管理、安全、观测性等能力

```
传统微服务 vs 服务网格：

传统微服务架构：
┌─────────┐    ┌─────────┐    ┌─────────┐
│ 服务A   │ → │ 服务B   │ → │ 服务C   │
└─────────┘    └─────────┘    └─────────┘
     ↑              ↑              ↑
 自实现监控    自实现监控      自实现监控

服务网格架构：
┌─────────┐    ┌─────────┐    ┌─────────┐
│ 服务A   │    │ 服务B   │    │ 服务C   │
└─────────┘    └─────────┘    └─────────┘
     │              │              │
┌─────────┐    ┌─────────┐    ┌─────────┐
│ Sidecar │ → │ Sidecar │ → │ Sidecar │ ← 统一监控
└─────────┘    └─────────┘    └─────────┘
```

### 6.2 Istio监控配置


**Istio Telemetry配置**：
```yaml
# 启用Istio监控
apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
metadata:
  name: control-plane
spec:
  values:
    telemetry:
      v2:
        prometheus:
          configOverride:
            metric_relabeling_configs:
            - source_labels: [__name__]
              regex: 'istio_.*'
              target_label: service_mesh
              replacement: 'istio'
```

**关键监控指标**：
```
Istio核心监控指标：

请求指标：
• istio_requests_total: 请求总数
• istio_request_duration_milliseconds: 请求延迟
• istio_request_bytes: 请求大小
• istio_response_bytes: 响应大小

TCP指标：
• istio_tcp_opened_total: TCP连接建立数
• istio_tcp_closed_total: TCP连接关闭数
• istio_tcp_received_bytes_total: 接收字节数
• istio_tcp_sent_bytes_total: 发送字节数
```

### 6.3 服务调用链监控


**分布式追踪配置**：
```yaml
# Jaeger追踪配置
apiVersion: v1
kind: ConfigMap
metadata:
  name: jaeger-configuration
  namespace: istio-system
data:
  span_storage_type: "elasticsearch"
  es_server_urls: "http://elasticsearch:9200"
  es_index_prefix: "jaeger"
---
# 追踪采样配置
apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: tracing-default
  namespace: istio-system
spec:
  tracing:
  - providers:
    - name: jaeger
  - customTags:
      app_name:
        header:
          name: "x-app-name"
```

**调用链分析示例**：
```
微服务调用链示例：

用户请求 → 网关 → 订单服务 → 支付服务 → 库存服务
    ↓        ↓        ↓          ↓          ↓
   10ms    25ms     150ms      300ms      50ms

┌─────────────────────────────────────────────────────┐
│ 总耗时: 535ms                                       │
├─────────────────────────────────────────────────────┤
│ 网关        ████                    (25ms - 4.7%)   │
│ 订单服务    ████████████████        (150ms - 28%)   │  
│ 支付服务    ██████████████████████  (300ms - 56%)   │ ← 瓶颈
│ 库存服务    ██████                  (50ms - 9.3%)   │
└─────────────────────────────────────────────────────┘

性能分析：
✅ 支付服务是性能瓶颈，占总时间56%
⚠️ 可考虑优化支付服务或增加缓存
```

---

## 7. ☁️ 云平台监控集成


### 7.1 云监控服务对接


**主流云平台监控**：

| 云平台 | 监控服务 | 特点 | 集成方式 |
|-------|---------|------|---------|
| **AWS** | `CloudWatch` | 原生服务深度集成 | EKS自动集成 |
| **Azure** | `Monitor` | 统一监控面板 | AKS内置支持 |
| **阿里云** | `云监控` | 中文界面易用 | ACK无缝集成 |
| **腾讯云** | `云监控` | 成本控制精细 | TKE自动配置 |

### 7.2 AWS CloudWatch集成


**EKS集群监控配置**：
```yaml
# CloudWatch Container Insights
apiVersion: v1
kind: ConfigMap
metadata:
  name: cwagentconfig
  namespace: amazon-cloudwatch
data:
  cwagentconfig.json: |
    {
      "metrics": {
        "namespace": "ContainerInsights",
        "metrics_collected": {
          "cpu": {
            "measurement": ["cpu_usage_idle", "cpu_usage_iowait"],
            "metrics_collection_interval": 60
          },
          "disk": {
            "measurement": ["used_percent"],
            "metrics_collection_interval": 60,
            "resources": ["*"]
          },
          "mem": {
            "measurement": ["mem_used_percent"],
            "metrics_collection_interval": 60
          }
        }
      }
    }
```

### 7.3 混合云监控策略


**混合云监控架构**：
```
多云监控统一架构：

┌─────────┐  ┌─────────┐  ┌─────────┐
│ AWS EKS │  │Azure AKS│  │ 阿里ACK  │
└─────────┘  └─────────┘  └─────────┘
     │            │            │
     ▼            ▼            ▼
┌─────────────────────────────────────┐
│       Prometheus联邦            │ ← 统一数据收集
├─────────────────────────────────────┤
│       Grafana统一展示           │ ← 统一监控面板
├─────────────────────────────────────┤
│       AlertManager告警          │ ← 统一告警处理
└─────────────────────────────────────┘

优势：
• 统一监控界面
• 跨云环境对比
• 成本控制统一
• 故障统一处理
```

---

## 8. 🏗️ 容器化监控部署


### 8.1 Prometheus Operator部署


**Prometheus Operator简介**：
简化Kubernetes环境中Prometheus的部署和管理

```bash
# 使用Helm部署Prometheus Operator
helm repo add prometheus-community \
  https://prometheus-community.github.io/helm-charts

helm install monitoring prometheus-community/kube-prometheus-stack \
  --namespace monitoring \
  --create-namespace \
  --set grafana.adminPassword=admin123
```

**核心组件说明**：
```
Prometheus Operator组件：

┌─────────────────────────────────────────┐
│            监控组件生态                  │
├─────────────────────────────────────────┤
│ Prometheus Operator                     │ ← 管理Prometheus实例
│   ├── Prometheus                       │ ← 时序数据库
│   ├── AlertManager                     │ ← 告警管理
│   └── Grafana                          │ ← 数据可视化
├─────────────────────────────────────────┤
│ ServiceMonitor                          │ ← 服务发现配置
│ PodMonitor                              │ ← Pod监控配置  
│ PrometheusRule                          │ ← 告警规则
└─────────────────────────────────────────┘
```

### 8.2 监控配置自动发现


**ServiceMonitor配置**：
```yaml
# 自动发现Spring Boot应用监控
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spring-boot-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: spring-boot-app
  endpoints:
  - port: metrics
    path: /actuator/prometheus
    interval: 30s
    honorLabels: true
```

**PodMonitor配置**：
```yaml
# 监控特定Pod的指标
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: nginx-pod-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: nginx
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 15s
```

### 8.3 数据持久化配置


**Prometheus存储配置**：
```yaml
# Prometheus持久化存储
apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prometheus
  namespace: monitoring
spec:
  replicas: 2
  retention: 15d                    # 数据保留15天
  retentionSize: 50GB              # 最大存储50GB
  storage:
    volumeClaimTemplate:
      spec:
        storageClassName: fast-ssd
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 100Gi
  
  # 远程存储配置(可选)
  remoteWrite:
  - url: "https://prometheus-remote-storage/api/v1/write"
    basicAuth:
      username:
        name: remote-storage-secret
        key: username
      password:
        name: remote-storage-secret
        key: password
```

---

## 9. 🎯 云原生监控最佳实践


### 9.1 监控策略设计


**四个黄金信号**：
Google SRE提出的核心监控维度

```
四个黄金信号详解：

① 延迟 (Latency)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
含义: 请求处理时间
指标: P50、P90、P99响应时间
告警: P99 > 1秒

② 流量 (Traffic)  
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
含义: 系统负载大小
指标: QPS、RPS、并发连接数
告警: QPS > 基线值 200%

③ 错误率 (Errors)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
含义: 请求失败比例
指标: 4xx/5xx错误率
告警: 错误率 > 1%

④ 饱和度 (Saturation)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
含义: 资源使用程度
指标: CPU、内存、磁盘使用率
告警: 资源使用率 > 80%
```

### 9.2 告警规则设计


**分层告警策略**：
```yaml
# Prometheus告警规则
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: container-alerts
  namespace: monitoring
spec:
  groups:
  - name: container.rules
    rules:
    # P0级别告警 - 立即处理
    - alert: ContainerDown
      expr: up == 0
      for: 1m
      labels:
        severity: critical
      annotations:
        summary: "容器 {{ $labels.instance }} 已停止"
        description: "容器已停止超过1分钟"
    
    # P1级别告警 - 紧急处理  
    - alert: HighMemoryUsage
      expr: container_memory_usage_bytes / container_spec_memory_limit_bytes > 0.9
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "容器内存使用过高"
        description: "{{ $labels.container }} 内存使用率超过90%"
    
    # P2级别告警 - 及时关注
    - alert: HighCPUUsage
      expr: rate(container_cpu_usage_seconds_total[5m]) > 0.8
      for: 10m  
      labels:
        severity: info
      annotations:
        summary: "容器CPU使用持续过高"
        description: "{{ $labels.container }} CPU使用率持续10分钟超过80%"
```

### 9.3 可观测性成熟度模型


**监控成熟度评估**：
```
Level 0 - 基础监控
┌──────────────────────────────────────┐
│ ✅ 基础资源监控 (CPU/内存/磁盘)       │
│ ✅ 服务可用性检查                   │  
│ ✅ 基础日志收集                     │
└──────────────────────────────────────┘

Level 1 - 应用监控  
┌──────────────────────────────────────┐
│ ✅ 应用性能指标 (延迟/吞吐量/错误)   │
│ ✅ 业务指标监控                     │
│ ✅ 自定义监控面板                   │
└──────────────────────────────────────┘

Level 2 - 智能监控
┌──────────────────────────────────────┐
│ ✅ 分布式链路追踪                   │
│ ✅ 智能告警降噪                     │
│ ✅ 异常检测算法                     │
└──────────────────────────────────────┘

Level 3 - 自治运维
┌──────────────────────────────────────┐
│ ✅ 自动扩缩容                       │
│ ✅ 智能故障诊断                     │
│ ✅ 预测性运维                       │
└──────────────────────────────────────┘
```

### 9.4 成本优化策略


**监控成本控制**：
```
监控成本优化方案：

数据存储优化：
• 设置合理的数据保留期 (7-30天)
• 配置数据压缩和去重
• 使用分层存储 (热数据SSD，冷数据HDD)

指标采集优化：  
• 调整采集频率 (非关键指标可30s-60s)
• 删除无用指标
• 使用指标聚合减少数据量

告警优化：
• 避免告警风暴
• 设置告警抑制规则
• 分级告警处理

资源使用优化：
• 监控服务右配置资源
• 使用共享存储
• 定期清理历史数据
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


```
🔸 容器监控本质：监控容器应用和基础设施的健康状态
🔸 监控层次：应用层→容器层→编排层→基础设施层
🔸 关键指标：资源使用、健康状态、业务指标
🔸 Docker监控：stats命令、API接口、健康检查
🔸 K8s监控：metrics-server、kube-state-metrics、Pod/Node监控
🔸 云原生监控：Prometheus生态、服务网格、云平台集成
```

### 10.2 关键理解要点


**🔹 容器监控的特殊性**：
```
动态特性：
- 容器生命周期短，需要动态发现
- 监控配置需要自动化
- 需要处理容器漂移问题

资源共享：
- 多容器共享主机资源
- 需要监控资源竞争
- 容器间相互影响

可观测性：
- 日志、指标、链路追踪三位一体
- 需要关联分析多维度数据
```

**🔹 监控工具选择原则**：
```
Docker原生监控：
✅ 适合：开发调试、小规模部署
❌ 限制：功能有限、扩展性差

Kubernetes监控：
✅ 适合：集群环境、自动化运维  
❌ 复杂度：配置复杂、学习成本高

云平台监控：
✅ 适合：快速上线、托管服务
❌ 成本：费用较高、厂商锁定
```

### 10.3 实际应用价值


**📈 业务价值**：
- **故障预警**：提前发现问题，减少服务中断
- **性能优化**：基于数据优化资源配置和应用性能
- **成本控制**：合理分配资源，避免过度配置
- **容量规划**：基于历史数据预测资源需求

**🛠 技术价值**：
- **问题定位**：快速定位故障根因
- **性能分析**：识别系统瓶颈和优化点
- **趋势分析**：了解系统使用趋势
- **自动化运维**：基于监控数据实现自动化操作

### 10.4 学习建议和实践路径


**学习路径**：
```
① 基础阶段 (1-2周)
┌──────────────────────────────────────┐
│ • Docker基础命令和容器概念           │
│ • docker stats、logs等监控命令       │  
│ • 容器健康检查配置                   │
└──────────────────────────────────────┘

② 进阶阶段 (2-4周)  
┌──────────────────────────────────────┐
│ • Kubernetes核心概念和架构           │
│ • Pod、Node监控配置                  │
│ • metrics-server部署和使用           │
└──────────────────────────────────────┘

③ 高级阶段 (4-8周)
┌──────────────────────────────────────┐
│ • Prometheus生态部署和配置           │
│ • Grafana面板制作                    │
│ • 告警规则设计和优化                 │
└──────────────────────────────────────┘

④ 专家阶段 (持续学习)
┌──────────────────────────────────────┐
│ • 服务网格监控                       │
│ • 多云环境监控                       │  
│ • 监控自动化和智能化                 │
└──────────────────────────────────────┘
```

**实践建议**：
- **动手实验**：搭建测试环境，实际操作各种监控工具
- **场景模拟**：模拟故障场景，验证监控告警效果  
- **持续优化**：根据实际使用情况持续优化监控策略
- **社区学习**：关注开源监控项目，学习最佳实践

**💡 核心记忆口诀**：
- 容器监控分四层，应用容器编排基础设施
- Docker原生加K8s，Prometheus生态最完善  
- 四个黄金信号记心间，延迟流量错误饱和度
- 告警分级处理好，P0紧急P1警告P2关注