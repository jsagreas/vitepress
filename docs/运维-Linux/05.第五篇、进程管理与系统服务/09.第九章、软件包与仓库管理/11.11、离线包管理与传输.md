---
title: 11、离线包管理与传输
---
## 📚 目录

1. [离线环境包管理基础](#1-离线环境包管理基础)
2. [包下载与缓存技术](#2-包下载与缓存技术)
3. [离线包依赖解析](#3-离线包依赖解析)
4. [移动存储仓库制作](#4-移动存储仓库制作)
5. [包传输与校验](#5-包传输与校验)
6. [离线仓库同步策略](#6-离线仓库同步策略)
7. [空气隔离环境管理](#7-空气隔离环境管理)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔌 离线环境包管理基础


### 1.1 什么是离线包管理


**离线包管理**：在没有网络连接的环境下，管理和安装软件包的技术方案。

```
网络环境对比：
在线环境：    服务器 ←→ 网络 ←→ 仓库服务器
离线环境：    服务器 ←→ 本地介质（U盘/光盘/硬盘）
```

**为什么需要离线包管理**：
- **安全要求**：生产服务器禁止联网
- **网络限制**：内网环境无法访问外部仓库
- **带宽考虑**：网络带宽有限，批量下载更高效
- **稳定性要求**：避免网络波动影响软件安装

### 1.2 离线包管理的核心挑战


**主要难点**：
```
🔸 依赖关系复杂
• 软件包之间存在复杂的依赖链
• 需要下载完整的依赖包树
• 版本兼容性问题

🔸 包完整性保证
• 确保下载的包没有损坏
• 验证包的数字签名
• 防止恶意包混入

🔸 存储与传输
• 合理组织包的存储结构
• 高效的传输方案
• 版本管理和更新策略
```

### 1.3 离线包管理策略总览


```
策略类型                使用场景               优缺点

包缓存策略              临时离线环境          优点：简单快速
                                             缺点：依赖不完整

完整镜像策略            长期离线环境          优点：包含所有软件
                                             缺点：占用空间大

按需下载策略            部分离线环境          优点：空间占用适中
                                             缺点：需要预先规划

定制仓库策略            特定业务环境          优点：针对性强
                                             缺点：维护成本高
```

---

## 2. 📦 包下载与缓存技术


### 2.1 YUM/DNF 环境下的包下载


**yumdownloader 命令详解**

`yumdownloader` 是专门用于下载RPM包的工具，不会自动安装。

```bash
# 基本下载命令
yumdownloader nginx

# 下载包及其所有依赖
yumdownloader --resolve nginx

# 指定下载目录
yumdownloader --destdir=/tmp/packages nginx

# 下载源码包
yumdownloader --source nginx
```

**实际操作示例**：
```bash
# 创建包下载目录
mkdir -p /opt/offline-packages/nginx

# 下载nginx及全部依赖包
yumdownloader --resolve --destdir=/opt/offline-packages/nginx nginx

# 查看下载的包
ls -la /opt/offline-packages/nginx/
# 输出示例：
# nginx-1.20.1-1.el8.x86_64.rpm
# nginx-filesystem-1.20.1-1.el8.noarch.rpm  
# openssl-libs-1.1.1k-5.el8_5.x86_64.rpm
```

**DNF 下载命令**：
```bash
# DNF 使用 download 插件
dnf download nginx

# 下载依赖包
dnf download --resolve nginx

# 下载到指定目录
dnf download --downloaddir=/tmp/packages nginx
```

### 2.2 APT 环境下的包下载


**apt download 命令使用**

```bash
# 基本下载
apt download nginx

# 下载到指定目录（需要先cd到目标目录）
cd /tmp/packages
apt download nginx

# 只下载包，不解决依赖
apt download --print-uris nginx
```

**使用 apt-get 批量下载**：
```bash
# 下载包但不安装
apt-get download nginx

# 下载包及依赖（到缓存目录）
apt-get install --download-only nginx

# 查看缓存的包
ls /var/cache/apt/archives/
```

**更高级的下载方案**：
```bash
# 使用 apt-rdepends 获取完整依赖列表
apt-rdepends nginx | grep -v "^ " > nginx-deps.txt

# 批量下载依赖包
while read package; do
    apt-get download $package
done < nginx-deps.txt
```

### 2.3 包缓存管理策略


**缓存目录结构设计**：
```
/opt/offline-packages/
├── base/                    # 基础系统包
│   ├── kernel/
│   ├── glibc/
│   └── systemd/
├── web-server/              # Web服务器相关
│   ├── nginx/
│   ├── apache/
│   └── php/
├── database/                # 数据库相关
│   ├── mysql/
│   ├── postgresql/
│   └── redis/
└── development/             # 开发工具
    ├── gcc/
    ├── python3/
    └── nodejs/
```

**缓存清理脚本**：
```bash
#!/bin/bash
# 清理缓存脚本 clean-cache.sh

# 设置缓存目录
CACHE_DIR="/opt/offline-packages"

# 清理30天前的包
find $CACHE_DIR -name "*.rpm" -mtime +30 -delete
find $CACHE_DIR -name "*.deb" -mtime +30 -delete

# 重新生成索引
createrepo --update $CACHE_DIR/
```

---

## 3. 🔗 离线包依赖解析


### 3.1 依赖关系的本质


**什么是软件包依赖**：
软件包依赖就像搭积木，每个软件都需要其他软件提供的功能才能正常运行。

```
依赖示例：
nginx (Web服务器)
├── 依赖 openssl (加密库)
├── 依赖 zlib (压缩库)
├── 依赖 pcre (正则表达式库)
└── 依赖 glibc (基础C库)
    └── 依赖 kernel (操作系统内核)
```

**依赖类型说明**：
- **强依赖（Requires）**：必须安装，否则软件无法运行
- **推荐依赖（Recommends）**：建议安装，增强功能
- **建议依赖（Suggests）**：可选安装，提供额外特性
- **冲突依赖（Conflicts）**：不能同时安装的软件

### 3.2 依赖解析工具使用


**使用 repoquery 分析依赖**：
```bash
# 查看包的直接依赖
repoquery --requires nginx

# 查看包的完整依赖树
repoquery --requires --resolve --recursive nginx

# 查看依赖图（更直观）
repoquery --requires --tree nginx
```

**实际操作示例**：
```bash
# 创建依赖分析脚本
cat > analyze-deps.sh << 'EOF'
#!/bin/bash
PACKAGE=$1
echo "分析 $PACKAGE 的依赖关系..."

# 获取直接依赖
echo "=== 直接依赖 ==="
repoquery --requires $PACKAGE

# 获取完整依赖列表
echo "=== 完整依赖树 ==="  
repoquery --requires --resolve --recursive $PACKAGE | sort -u
EOF

chmod +x analyze-deps.sh
./analyze-deps.sh nginx
```

### 3.3 完整依赖包收集方案


**自动化依赖下载脚本**：
```bash
#!/bin/bash
# download-with-deps.sh - 下载包及完整依赖

PACKAGE=$1
DOWNLOAD_DIR=${2:-/tmp/offline-packages}

# 创建下载目录
mkdir -p $DOWNLOAD_DIR

echo "开始下载 $PACKAGE 及其依赖..."

# 方法1: 使用yumdownloader（推荐）
yumdownloader --resolve --destdir=$DOWNLOAD_DIR $PACKAGE

# 方法2: 手动解析依赖
# 获取依赖列表
DEPS=$(repoquery --requires --resolve --recursive $PACKAGE | sort -u)

# 逐个下载
for dep in $DEPS; do
    echo "下载依赖: $dep"
    yumdownloader --destdir=$DOWNLOAD_DIR $dep
done

echo "下载完成，包存放在: $DOWNLOAD_DIR"
ls -la $DOWNLOAD_DIR/
```

**APT 环境的依赖收集**：
```bash
#!/bin/bash
# apt-download-deps.sh

PACKAGE=$1
DOWNLOAD_DIR=${2:-./packages}

mkdir -p $DOWNLOAD_DIR
cd $DOWNLOAD_DIR

# 获取包及依赖列表
apt-cache depends --recurse --no-recommends --no-suggests \
    --no-conflicts --no-breaks --no-replaces --no-enhances \
    $PACKAGE | grep "^\w" | sort -u > deps-list.txt

# 批量下载
while read package; do
    if [ ! -z "$package" ]; then
        echo "下载: $package"
        apt-get download $package 2>/dev/null
    fi
done < deps-list.txt
```

---

## 4. 💾 移动存储仓库制作


### 4.1 U盘仓库制作步骤


**U盘仓库的核心思路**：
把U盘变成一个"移动的软件仓库"，插到任何Linux机器上都能安装软件。

**制作流程**：
```
步骤1: 格式化U盘 → 步骤2: 复制软件包 → 步骤3: 创建仓库索引 → 步骤4: 配置仓库文件
```

**详细操作步骤**：
```bash
# 1. 准备U盘（假设设备为/dev/sdb1）
sudo mkfs.ext4 /dev/sdb1 -L "OfflineRepo"

# 2. 挂载U盘
sudo mkdir -p /mnt/usb
sudo mount /dev/sdb1 /mnt/usb

# 3. 创建仓库目录结构
sudo mkdir -p /mnt/usb/packages
sudo mkdir -p /mnt/usb/repodata

# 4. 复制软件包到U盘
sudo cp /opt/offline-packages/*.rpm /mnt/usb/packages/

# 5. 创建仓库索引
sudo createrepo /mnt/usb/packages/

# 6. 创建仓库配置文件
sudo cat > /mnt/usb/offline-repo.repo << EOF
[offline-usb]
name=Offline USB Repository
baseurl=file:///mnt/usb/packages
enabled=1
gpgcheck=0
EOF
```

### 4.2 光盘仓库制作


**ISO镜像仓库制作**：
```bash
# 1. 创建临时目录
mkdir -p /tmp/iso-repo/packages
mkdir -p /tmp/iso-repo/isolinux

# 2. 复制软件包
cp /opt/offline-packages/*.rpm /tmp/iso-repo/packages/

# 3. 创建仓库索引
createrepo /tmp/iso-repo/packages/

# 4. 创建启动配置（可选）
cat > /tmp/iso-repo/isolinux/isolinux.cfg << EOF
default local
timeout 30
prompt 1
label local
    menu label Boot from ^Local Drive
    localboot 0
EOF

# 5. 制作ISO镜像
mkisofs -o offline-repo.iso \
    -b isolinux/isolinux.bin \
    -c isolinux/boot.cat \
    -no-emul-boot \
    -boot-load-size 4 \
    -boot-info-table \
    -R -J -v -T \
    /tmp/iso-repo/
```

### 4.3 便携式仓库使用


**U盘仓库使用方法**：
```bash
# 1. 插入U盘并挂载
sudo mkdir -p /mnt/usb
sudo mount /dev/sdb1 /mnt/usb

# 2. 配置临时仓库
sudo cp /mnt/usb/offline-repo.repo /etc/yum.repos.d/

# 3. 更新仓库缓存
sudo yum clean all
sudo yum makecache

# 4. 安装软件
sudo yum install nginx

# 5. 使用完毕后清理
sudo rm /etc/yum.repos.d/offline-repo.repo
sudo umount /mnt/usb
```

**光盘仓库使用**：
```bash
# 1. 挂载ISO文件
sudo mkdir -p /mnt/cdrom
sudo mount -o loop offline-repo.iso /mnt/cdrom

# 2. 配置仓库
sudo cat > /etc/yum.repos.d/cdrom-repo.repo << EOF
[cdrom-repo]
name=CDROM Repository
baseurl=file:///mnt/cdrom/packages
enabled=1
gpgcheck=0
EOF

# 3. 安装软件
sudo yum install package-name
```

---

## 5. 🔐 包传输与校验


### 5.1 包完整性验证原理


**为什么需要校验**：
文件在传输过程中可能出现的问题：
- **数据损坏**：网络传输或存储介质错误
- **版本错误**：下载了错误版本的软件包
- **恶意篡改**：软件包被植入恶意代码

**校验方法对比**：
```
校验方式        安全级别    计算速度    用途
MD5            低          快         基本完整性检查
SHA1           中          中等       一般完整性验证  
SHA256         高          较慢       安全性要求高的场景
GPG签名        最高        慢         官方包验证
```

### 5.2 文件校验实操


**生成校验文件**：
```bash
# 为包目录生成各种校验值
cd /opt/offline-packages

# 生成MD5校验文件
find . -name "*.rpm" -exec md5sum {} \; > packages.md5
find . -name "*.deb" -exec md5sum {} \; >> packages.md5

# 生成SHA256校验文件
find . -name "*.rpm" -exec sha256sum {} \; > packages.sha256
find . -name "*.deb" -exec sha256sum {} \; >> packages.sha256

# 查看校验文件内容
head -3 packages.md5
# 输出示例：
# a1b2c3d4e5f6... nginx-1.20.1-1.el8.x86_64.rpm
# f6e5d4c3b2a1... openssl-libs-1.1.1k-5.el8_5.x86_64.rpm
```

**验证校验和**：
```bash
# 验证MD5校验和
md5sum -c packages.md5

# 验证SHA256校验和  
sha256sum -c packages.sha256

# 输出示例：
# nginx-1.20.1-1.el8.x86_64.rpm: OK
# openssl-libs-1.1.1k-5.el8_5.x86_64.rpm: OK
```

### 5.3 GPG签名验证


**RPM包签名验证**：
```bash
# 导入GPG公钥（以CentOS为例）
rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial

# 验证单个包的签名
rpm --checksig nginx-1.20.1-1.el8.x86_64.rpm

# 批量验证包签名
for rpm in *.rpm; do
    echo "验证: $rpm"
    rpm --checksig "$rpm"
done
```

**DEB包签名验证**：
```bash
# 验证deb包
dpkg-sig --verify package.deb

# 批量验证
for deb in *.deb; do
    echo "验证: $deb"
    dpkg-sig --verify "$deb"
done
```

### 5.4 传输安全方案


**安全传输脚本**：
```bash
#!/bin/bash
# secure-transfer.sh - 安全传输包文件

SOURCE_DIR=$1
TARGET_HOST=$2
TARGET_DIR=$3

# 1. 生成校验文件
echo "生成校验文件..."
cd $SOURCE_DIR
find . -type f -exec sha256sum {} \; > transfer.sha256

# 2. 打包文件
echo "打包文件..."
tar -czf packages.tar.gz * transfer.sha256

# 3. 使用scp传输
echo "传输到目标主机..."
scp packages.tar.gz $TARGET_HOST:$TARGET_DIR/

# 4. 远程解包和验证
echo "远程验证..."
ssh $TARGET_HOST << EOF
cd $TARGET_DIR
tar -xzf packages.tar.gz
sha256sum -c transfer.sha256
echo "验证完成"
EOF
```

---

## 6. 🔄 离线仓库同步策略


### 6.1 仓库同步基本概念


**什么是仓库同步**：
定期将在线仓库的内容复制到本地，保持离线环境的软件包是最新的。

```
同步流程：
在线仓库 → 临时下载 → 本地验证 → 离线仓库 → 目标机器
```

**同步策略类型**：
- **完全同步**：下载整个仓库的所有包
- **增量同步**：只下载新增和更新的包
- **选择性同步**：只同步指定的软件包和分类

### 6.2 YUM仓库同步实现


**使用 reposync 工具**：
```bash
# 安装仓库同步工具
yum install yum-utils

# 同步特定仓库
reposync --repo=epel --download_path=/opt/local-repo/

# 只下载最新版本的包
reposync --repo=epel --newest-only --download_path=/opt/local-repo/

# 同步后创建仓库索引
createrepo /opt/local-repo/epel/
```

**完整的同步脚本**：
```bash
#!/bin/bash
# repo-sync.sh - 仓库同步脚本

REPO_NAME=${1:-epel}
LOCAL_PATH=${2:-/opt/local-repo}
LOG_FILE="/var/log/repo-sync.log"

echo "$(date): 开始同步仓库 $REPO_NAME" >> $LOG_FILE

# 1. 创建本地目录
mkdir -p $LOCAL_PATH/$REPO_NAME

# 2. 同步仓库内容
reposync --repo=$REPO_NAME \
         --newest-only \
         --delete \
         --download_path=$LOCAL_PATH/ \
         --downloadcomps \
         --download-metadata

# 3. 创建/更新仓库索引
createrepo --update $LOCAL_PATH/$REPO_NAME/

# 4. 生成校验文件
cd $LOCAL_PATH/$REPO_NAME
find . -name "*.rpm" -exec sha256sum {} \; > checksums.sha256

echo "$(date): 同步完成，包数量: $(ls *.rpm | wc -l)" >> $LOG_FILE
```

### 6.3 APT仓库同步


**使用 apt-mirror 工具**：
```bash
# 安装apt-mirror
apt-get install apt-mirror

# 配置mirror.list文件
cat > /etc/apt/mirror.list << EOF
set base_path /opt/local-mirror
set mirror_path $base_path/mirror
set skel_path $base_path/skel
set var_path $base_path/var

# 指定要镜像的仓库
deb http://archive.ubuntu.com/ubuntu focal main restricted
deb http://archive.ubuntu.com/ubuntu focal-updates main restricted
deb http://security.ubuntu.com/ubuntu focal-security main restricted

clean http://archive.ubuntu.com/ubuntu
EOF

# 执行同步
apt-mirror
```

### 6.4 自动化同步策略


**定时同步配置**：
```bash
# 创建crontab任务
cat > /etc/cron.d/repo-sync << EOF
# 每天凌晨2点执行仓库同步
0 2 * * * root /opt/scripts/repo-sync.sh epel /opt/local-repo
0 3 * * * root /opt/scripts/repo-sync.sh base /opt/local-repo
EOF
```

**同步状态监控**：
```bash
#!/bin/bash
# monitor-sync.sh - 监控同步状态

REPO_PATH="/opt/local-repo"
ALERT_EMAIL="admin@example.com"

# 检查同步是否成功
check_sync_status() {
    local repo=$1
    local log_file="/var/log/repo-sync.log"
    
    # 检查最近24小时的同步记录
    if grep -q "$(date --date='1 day ago' '+%Y-%m-%d')" $log_file; then
        echo "$repo: 同步正常"
        return 0
    else
        echo "$repo: 同步异常" 
        return 1
    fi
}

# 检查各个仓库
for repo in epel base updates; do
    if ! check_sync_status $repo; then
        echo "警告: $repo 仓库同步失败" | mail -s "仓库同步告警" $ALERT_EMAIL
    fi
done
```

---

## 7. 🏢 空气隔离环境管理


### 7.1 空气隔离环境特点


**什么是空气隔离**：
空气隔离（Air Gap）是指系统完全与外部网络隔离，无法通过网络进行任何形式的数据交换。

```
隔离级别对比：
网络隔离：    内网 ←→ 防火墙 ←╳→ 外网
物理隔离：    内网 ←╳→ 物理断开 ←╳→ 外网  
空气隔离：    独立系统 ←╳→ 完全隔离 ←╳→ 任何网络
```

**空气隔离环境的挑战**：
- **软件更新困难**：无法在线下载更新
- **依赖解析复杂**：需要离线解决所有依赖
- **安全更新滞后**：安全补丁无法及时获取
- **管理成本高**：需要人工传输和管理

### 7.2 空气隔离环境软件管理策略


**分层管理策略**：
```
基础层：操作系统 + 核心组件
├── 基础运行时环境
├── 系统安全组件
└── 基础网络工具

应用层：业务应用 + 支撑工具  
├── 数据库系统
├── Web服务器
├── 应用运行时
└── 监控工具

开发层：开发调试工具
├── 编译器工具链
├── 调试工具
└── 性能分析工具
```

**离线软件包管理流程**：
```bash
#!/bin/bash
# airgap-package-manager.sh - 空气隔离环境包管理

STAGING_DIR="/opt/staging"      # 暂存目录
PRODUCTION_DIR="/opt/packages"  # 生产目录
QUARANTINE_DIR="/opt/quarantine" # 隔离目录

# 1. 包接收和隔离
receive_packages() {
    local source_media=$1
    
    echo "挂载传输介质..."
    mount $source_media /mnt/transfer
    
    echo "隔离新包..."
    cp /mnt/transfer/*.rpm $QUARANTINE_DIR/
    
    umount /mnt/transfer
}

# 2. 安全检查
security_check() {
    echo "执行安全扫描..."
    
    # 病毒扫描
    clamscan -r $QUARANTINE_DIR/
    
    # 包完整性验证
    for pkg in $QUARANTINE_DIR/*.rpm; do
        rpm --checksig "$pkg" || {
            echo "警告: $pkg 签名验证失败"
            mv "$pkg" "$pkg.suspicious"
        }
    done
}

# 3. 依赖检查
dependency_check() {
    echo "检查依赖关系..."
    
    for pkg in $QUARANTINE_DIR/*.rpm; do
        echo "分析 $pkg 的依赖..."
        rpm -qpR "$pkg"
    done
}

# 4. 部署到生产
deploy_to_production() {
    echo "部署到生产环境..."
    
    # 移动验证通过的包
    mv $QUARANTINE_DIR/*.rpm $PRODUCTION_DIR/
    
    # 更新仓库索引
    createrepo --update $PRODUCTION_DIR/
}

# 执行完整流程
main() {
    receive_packages $1
    security_check
    dependency_check  
    deploy_to_production
    
    echo "包管理流程完成"
}
```

### 7.3 空气隔离环境更新策略


**定期批量更新方案**：
```bash
#!/bin/bash
# batch-update.sh - 批量更新脚本

UPDATE_SCHEDULE_FILE="/etc/airgap-update.conf"
TRANSFER_LOG="/var/log/package-transfer.log"

# 配置文件示例
cat > $UPDATE_SCHEDULE_FILE << EOF
# 更新计划配置
CRITICAL_UPDATES_INTERVAL=7    # 关键更新：7天
SECURITY_UPDATES_INTERVAL=14   # 安全更新：14天  
REGULAR_UPDATES_INTERVAL=30    # 常规更新：30天
FEATURE_UPDATES_INTERVAL=90    # 功能更新：90天
EOF

# 执行更新
execute_batch_update() {
    local update_type=$1
    
    case $update_type in
        "critical")
            echo "执行关键更新..."
            install_packages "/opt/updates/critical/"
            ;;
        "security")
            echo "执行安全更新..."
            install_packages "/opt/updates/security/"
            ;;
        "regular")
            echo "执行常规更新..."
            install_packages "/opt/updates/regular/"
            ;;
    esac
}

# 安装包函数
install_packages() {
    local package_dir=$1
    
    if [ -d "$package_dir" ]; then
        rpm -Uvh $package_dir/*.rpm
        echo "$(date): 安装 $package_dir 完成" >> $TRANSFER_LOG
    fi
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 离线包管理：在无网络环境下管理软件包的技术体系
🔸 依赖解析：理解和处理软件包之间的依赖关系
🔸 仓库同步：将在线仓库内容复制到离线环境
🔸 包完整性：通过校验确保包的安全性和完整性
🔸 空气隔离：完全与网络隔离的高安全等级环境
```

### 8.2 关键技术要点


**🔹 包下载核心命令**
```bash
# YUM/DNF环境
yumdownloader --resolve package-name    # 下载包及依赖
dnf download --resolve package-name     # DNF下载包及依赖

# APT环境  
apt download package-name               # 下载单个包
apt-get install --download-only pkg     # 下载到缓存
```

**🔹 仓库制作关键步骤**
```bash
# 创建本地仓库
createrepo /path/to/packages           # RPM仓库
dpkg-scanpackages . /dev/null | gzip > Packages.gz  # DEB仓库
```

**🔹 包校验核心方法**
```bash
# 生成和验证校验和
sha256sum packages/*.rpm > checksums.sha256
sha256sum -c checksums.sha256

# GPG签名验证
rpm --checksig package.rpm
```

### 8.3 实际应用场景


**🎯 典型应用环境**
- **企业内网**：无法访问外部仓库的企业环境
- **生产服务器**：安全要求禁止联网的生产系统
- **嵌入式系统**：资源受限的嵌入式Linux设备
- **科研环境**：需要高安全等级的科研计算环境

**🔧 实施建议**
```
规划阶段：
✅ 调研所需软件清单
✅ 评估存储空间需求
✅ 制定更新策略

实施阶段：
✅ 搭建包下载环境
✅ 建立校验流程
✅ 制作传输介质

维护阶段：
✅ 定期同步更新
✅ 监控包完整性
✅ 优化存储结构
```

### 8.4 常见问题与解决方案


> **💡 问题1**：依赖包下载不完整
> 
> **解决方案**：使用 `--resolve` 参数，并结合 `repoquery` 手动确认依赖关系

> **⚠️ 问题2**：包传输过程中损坏
> 
> **解决方案**：传输前后都进行校验和验证，建立完整的校验流程

> **🔥 问题3**：离线环境包版本冲突
> 
> **解决方案**：建立测试环境，先在测试环境验证包的兼容性

**🧠 记忆要点**：
```
离线包管理三步走：下载-校验-部署
依赖关系要理清：递归解析不能少  
传输安全很重要：校验签名双保险
空气隔离更严格：分层管理策略好
```

**核心记忆口诀**：
> 离线环境包管理，下载依赖要齐全
> 
> 校验签名保安全，仓库同步定时间  
> 
> 空气隔离更严格，分批更新保稳定