---
title: 4、systemd target系统与运行级别
---
## 📚 目录

1. [什么是systemd target系统](#1-什么是systemd-target系统)
2. [target与传统运行级别的关系](#2-target与传统运行级别的关系)
3. [核心target类型详解](#3-核心target类型详解)
4. [系统启动target链机制](#4-系统启动target链机制)
5. [target依赖关系分析](#5-target依赖关系分析)
6. [target管理操作实战](#6-target管理操作实战)
7. [自定义target创建与应用](#7-自定义target创建与应用)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 什么是systemd target系统


### 1.1 target的基本概念


**🔸 什么是target？**
```
简单理解：target就像是系统的"工作模式"
- 就像手机有飞行模式、静音模式、正常模式一样
- Linux系统也有不同的工作模式：图形界面模式、纯命令行模式、紧急模式等
- target就是定义这些模式的配置单元
```

**💡 生活化类比**
```
🏠 家庭模式类比：
普通家庭模式 → multi-user.target (多用户命令行)
聚会模式     → graphical.target (图形界面)  
睡眠模式     → poweroff.target (关机)
紧急模式     → emergency.target (系统修复)
```

### 1.2 target的作用机制


**📊 target工作原理**
```
传统方式：                    systemd方式：
系统启动 → 运行级别3          系统启动 → multi-user.target
         ↓                           ↓
    启动对应服务                  启动目标相关的所有unit
    
核心区别：
• 传统：按数字级别(0-6)管理
• systemd：按目标名称管理，更直观
• 传统：串行启动服务
• systemd：并行启动，效率更高
```

**🔍 target单元的本质**
```
target本身不做具体工作，它是一个"集合"概念：
- 定义了一组需要同时运行的服务
- 类似于"播放列表"，列出要启动的服务清单
- 通过依赖关系管理服务启动顺序
```

### 1.3 为什么需要target系统


**🎯 解决的核心问题**
```
1. 系统状态管理：
   - 不同场景需要不同的服务组合
   - 服务器不需要图形界面，桌面需要
   - 维护模式只需要最基础的服务

2. 启动效率优化：
   - 并行启动相互独立的服务
   - 明确的依赖关系管理
   - 避免不必要的服务启动

3. 系统可靠性：
   - 紧急模式快速进入
   - 服务故障不影响整个系统
   - 灵活的恢复机制
```

---

## 2. 🔄 target与传统运行级别的关系


### 2.1 传统运行级别回顾


**📋 传统SysV运行级别**
```
运行级别对照表：
┌─────────┬─────────────┬─────────────────────┐
│ 级别    │ 名称        │ 说明                │
├─────────┼─────────────┼─────────────────────┤
│ 0       │ halt        │ 关机状态            │
│ 1       │ single      │ 单用户模式(维护)    │
│ 2       │ multi-user  │ 多用户模式(无网络)  │
│ 3       │ multi-user  │ 多用户模式(有网络)  │
│ 4       │ 未定义      │ 用户自定义          │
│ 5       │ X11         │ 图形界面模式        │
│ 6       │ reboot      │ 重启状态            │
└─────────┴─────────────┴─────────────────────┘
```

### 2.2 runlevel到target的映射关系


**🔗 完整映射对照**
```
传统运行级别 → systemd target映射：

runlevel 0 → poweroff.target
    ├─ 系统关机
    └─ 停止所有服务后关闭电源

runlevel 1 → rescue.target  
    ├─ 单用户救援模式
    └─ 仅启动最基础服务，root用户维护

runlevel 2 → multi-user.target
    ├─ 多用户命令行模式
    └─ 完整网络功能(部分发行版)

runlevel 3 → multi-user.target
    ├─ 标准多用户命令行模式  
    └─ 完整网络和服务功能

runlevel 4 → multi-user.target
    ├─ 用户自定义级别
    └─ 默认映射到多用户模式

runlevel 5 → graphical.target
    ├─ 图形界面模式
    └─ 包含桌面环境的完整功能

runlevel 6 → reboot.target
    ├─ 系统重启
    └─ 安全停止服务后重新启动
```

**💭 兼容性说明**
```
systemd提供兼容层：
- 可以继续使用 init 3, init 5 等传统命令
- 内部自动转换为对应的target操作
- 但建议使用新的systemctl命令

示例对比：
传统方式：init 3
新方式：  systemctl isolate multi-user.target
```

### 2.3 target系统的改进之处


**🚀 相比传统运行级别的优势**
```
1. 命名更直观：
   runlevel 5 → graphical.target
   数字含义不明确 → 名称一目了然

2. 并行启动：
   传统：按顺序逐个启动服务
   systemd：独立服务同时启动，速度更快

3. 依赖管理：
   传统：简单的启动脚本顺序
   systemd：复杂的依赖关系图

4. 状态管理：
   传统：难以跟踪服务状态
   systemd：实时监控每个unit状态

5. 灵活性：
   传统：固定的7个级别
   systemd：可以自定义target
```

---

## 3. 🎨 核心target类型详解


### 3.1 multi-user.target - 多用户命令行模式


**🔸 核心特性**
```
用途：标准的服务器运行模式
特点：
• 支持多用户同时登录
• 提供完整的网络功能
• 运行所有系统服务
• 没有图形界面，纯命令行

适用场景：
✅ 服务器系统
✅ 嵌入式设备  
✅ 资源受限环境
✅ 远程管理的系统
```

**🔧 包含的核心服务**
```
网络服务：
├─ NetworkManager.service
├─ sshd.service
├─ systemd-resolved.service
└─ firewalld.service

系统服务：
├─ systemd-logind.service
├─ dbus.service
├─ chronyd.service
└─ rsyslog.service

用户服务：
├─ getty@tty1.service (虚拟控制台)
├─ user@.service (用户会话管理)
└─ systemd-user-sessions.service
```

### 3.2 graphical.target - 图形界面模式


**🔸 核心特性**
```
用途：桌面用户的标准模式
特点：
• 继承multi-user.target所有功能
• 额外启动图形显示服务
• 支持桌面环境(GNOME、KDE等)
• 提供完整的用户交互界面

关系图：
graphical.target
    ├─ multi-user.target (继承)
    │   ├─ basic.target
    │   └─ 所有命令行服务
    └─ 图形相关服务
        ├─ gdm.service (显示管理器)
        ├─ lightdm.service
        └─ 桌面环境服务
```

**💻 图形相关服务示例**
```
显示管理器：
├─ gdm.service (GNOME)
├─ lightdm.service (轻量级)
├─ sddm.service (KDE)
└─ xdm.service (传统X11)

图形系统：
├─ xorg-server
├─ wayland服务
└─ 桌面环境服务
```

### 3.3 rescue.target - 救援模式


**🔸 救援模式特性**
```
用途：系统故障修复和维护
特点：
• 最小化的系统环境
• 仅root用户可登录
• 基础服务运行
• 文件系统可读写

启动条件：
• 系统启动故障
• 手动指定进入
• 密码找回
• 系统配置修复
```

**🛠️ 救援模式包含服务**
```
最小服务集：
├─ systemd核心服务
├─ 基础文件系统服务
├─ emergency shell
└─ 基本设备管理

不包含：
❌ 网络服务
❌ 多用户支持
❌ 非必要系统服务
❌ 图形界面
```

### 3.4 特殊target详解


**⚡ emergency.target - 紧急模式**
```
比rescue更基础的模式：
• 文件系统只读挂载
• 仅启动最核心进程
• 用于严重系统故障
• 手动修复后才能正常启动

使用场景：
🚨 根文件系统损坏
🚨 重要系统文件丢失
🚨 内核模块加载失败
🚨 严重的配置错误
```

**🔌 poweroff.target - 关机目标**
```
关机流程控制：
1. 停止用户进程
2. 卸载文件系统
3. 停止内核服务
4. 关闭硬件电源

相关命令：
systemctl poweroff
systemctl halt
poweroff
```

**🔄 reboot.target - 重启目标**
```
重启流程：
1. 保存系统状态
2. 停止所有服务
3. 卸载文件系统
4. 重新初始化系统

相关命令：
systemctl reboot
reboot
```

---

## 4. ⛓️ 系统启动target链机制


### 4.1 启动链的层次结构


**📊 完整启动链图示**
```
系统启动target依赖链：

    ┌─────────────────┐
    │   kernel init   │ ← 内核初始化
    └─────────────────┘
            │
    ┌─────────────────┐
    │ sysinit.target  │ ← 系统初始化
    └─────────────────┘
            │
    ┌─────────────────┐
    │  basic.target   │ ← 基础服务
    └─────────────────┘
            │
    ┌─────────────────┐
    │multi-user.target│ ← 多用户模式
    └─────────────────┘
            │ (可选)
    ┌─────────────────┐
    │graphical.target │ ← 图形界面
    └─────────────────┘
```

### 4.2 各阶段详细说明


**🔧 sysinit.target - 系统初始化**
```
这个阶段做什么：
• 挂载基本文件系统
• 初始化硬件设备
• 加载内核模块
• 设置系统时钟
• 初始化随机数生成器

包含的关键服务：
├─ systemd-tmpfiles-setup.service
├─ systemd-sysctl.service  
├─ systemd-modules-load.service
├─ systemd-random-seed.service
└─ dev-mqueue.mount
```

**🏗️ basic.target - 基础目标**
```
提供基础系统功能：
• 系统日志服务
• D-Bus消息总线
• 网络基础设施
• 时间同步服务
• 设备管理

核心服务：
├─ systemd-journald.service (日志)
├─ dbus.service (消息总线)
├─ systemd-logind.service (登录管理)
├─ systemd-resolved.service (域名解析)
└─ systemd-networkd.service (网络管理)
```

**👥 multi-user.target - 多用户目标**
```
在basic.target基础上添加：
• 完整网络功能
• 用户登录支持
• SSH远程访问
• 系统服务
• 定时任务支持

新增关键服务：
├─ NetworkManager.service
├─ sshd.service
├─ crond.service
├─ rsyslog.service
└─ getty@.service
```

### 4.3 依赖关系分析


**🔗 target之间的依赖**
```
依赖类型说明：

Requires: 强依赖
• 依赖的target必须成功启动
• 依赖失败时，当前target也失败

Wants: 弱依赖  
• 希望依赖的target启动
• 依赖失败不影响当前target

After: 顺序依赖
• 等待指定target启动完成后再启动
• 不关心依赖是否成功

Before: 前置依赖
• 在指定target之前启动
• 为其他target提供服务
```

**📋 实际依赖关系示例**
```
multi-user.target的依赖：
[Unit]
Description=Multi-User System
Documentation=man:systemd.special(7)
Requires=basic.target        ← 必须有basic.target
Conflicts=rescue.service rescue.target  ← 与救援模式冲突
After=basic.target rescue.service rescue.target  ← 在basic.target之后
AllowIsolate=yes            ← 允许切换到此target
```

---

## 5. 🔍 target依赖关系分析


### 5.1 查看target依赖的方法


**🔧 实用命令集合**

```bash
# 查看target的完整依赖关系
systemctl list-dependencies graphical.target

# 查看反向依赖(哪些依赖这个target)
systemctl list-dependencies --reverse basic.target

# 显示依赖关系树(更详细)
systemctl list-dependencies --all multi-user.target

# 查看target的配置文件内容
systemctl cat multi-user.target
```

**📊 依赖关系输出示例**
```
$ systemctl list-dependencies graphical.target

graphical.target
├─ accounts-daemon.service
├─ gdm.service
├─ network.service  
├─ rtkit-daemon.service
├─ systemd-update-utmp-runlevel.service
├─ udisks2.service
└─ multi-user.target
  ├─ basic.target
  │ ├─ sysinit.target
  │ │ ├─ cryptsetup.target
  │ │ ├─ local-fs.target
  │ │ └─ swap.target
  │ └─ sockets.target
  ├─ getty.target
  └─ remote-fs.target
```

### 5.2 依赖关系故障排查


**🔍 常见依赖问题**

```bash
# 检查failed的unit
systemctl --failed

# 查看具体服务的失败原因  
systemctl status 服务名.service

# 查看启动日志
journalctl -u 服务名.service

# 分析target启动时间
systemd-analyze blame

# 查看启动关键路径
systemd-analyze critical-chain
```

**⚠️ 依赖故障处理策略**
```
故障类型与处理：

1. Requires依赖失败：
   问题：强依赖服务启动失败
   现象：整个target无法启动
   解决：修复依赖服务或临时禁用

2. 循环依赖：
   问题：A依赖B，B也依赖A
   现象：系统启动卡住
   解决：分析依赖链，打破循环

3. 超时问题：
   问题：服务启动时间过长
   现象：target启动缓慢
   解决：调整超时设置或优化服务
```

### 5.3 自定义依赖关系


**🎯 修改依赖的方法**
```bash
# 为target添加依赖服务
systemctl add-wants multi-user.target 自定义服务.service

# 为target添加必需依赖
systemctl add-requires multi-user.target 关键服务.service

# 移除依赖关系
systemctl remove-wants multi-user.target 服务名.service
```

**📝 手动编辑依赖关系**
```bash
# 创建override配置
systemctl edit multi-user.target

# 在编辑器中添加依赖
[Unit]
Wants=my-custom.service
After=my-custom.service
```

---

## 6. 🛠️ target管理操作实战


### 6.1 查看当前系统状态


**📊 系统状态查询命令**

```bash
# 查看当前活动的target
systemctl get-default
systemctl list-units --type=target

# 查看当前target状态
systemctl status multi-user.target

# 查看所有可用target
systemctl list-units --type=target --all
```

**💻 实际输出示例**
```
$ systemctl get-default
graphical.target

$ systemctl list-units --type=target
UNIT                   LOAD   ACTIVE SUB    DESCRIPTION
basic.target           loaded active active Basic System
cryptsetup.target      loaded active active Local Encrypted Volumes  
graphical.target       loaded active active Graphical Interface
local-fs.target        loaded active active Local File Systems
multi-user.target      loaded active active Multi-User System
network.target         loaded active active Network
```

### 6.2 默认target管理


**🔧 设置默认启动target**

```bash
# 设置默认为命令行模式
sudo systemctl set-default multi-user.target

# 设置默认为图形界面模式  
sudo systemctl set-default graphical.target

# 查看设置结果
systemctl get-default
ls -l /etc/systemd/system/default.target
```

**🔄 临时切换target**
```bash
# 切换到命令行模式(不重启)
sudo systemctl isolate multi-user.target

# 切换到图形界面模式
sudo systemctl isolate graphical.target

# 进入救援模式
sudo systemctl isolate rescue.target

# 进入紧急模式
sudo systemctl isolate emergency.target
```

### 6.3 target服务管理


**📋 管理target相关服务**

```bash
# 查看target包含的所有服务
systemctl list-dependencies --plain multi-user.target

# 启动target(及其依赖)
sudo systemctl start graphical.target

# 停止target
sudo systemctl stop graphical.target

# 重启target
sudo systemctl restart multi-user.target
```

**⚡ 快捷管理命令**
```bash
# 快速重启到命令行
sudo systemctl isolate multi-user.target

# 快速重启到图形界面
sudo systemctl isolate graphical.target

# 系统关机
sudo systemctl isolate poweroff.target
# 或简写为
sudo systemctl poweroff

# 系统重启
sudo systemctl isolate reboot.target  
# 或简写为
sudo systemctl reboot
```

---

## 7. 🎨 自定义target创建与应用


### 7.1 创建自定义target的场景


**🎯 什么时候需要自定义target**
```
实际应用场景：

1. 开发环境模式：
   • 启动开发工具服务
   • 关闭生产环境服务
   • 开启调试和监控

2. 维护模式：
   • 停止用户服务
   • 启动备份和清理任务
   • 限制系统访问

3. 特殊应用模式：
   • 游戏服务器模式
   • 数据库专用模式  
   • 容器运行环境
```

### 7.2 自定义target创建步骤


**📝 创建开发环境target示例**

**步骤1：创建target文件**
```bash
# 创建自定义target配置
sudo vim /etc/systemd/system/development.target
```

```ini
[Unit]
Description=Development Environment
Documentation=Custom target for development
Requires=multi-user.target
After=multi-user.target
Conflicts=shutdown.target
AllowIsolate=yes

[Install]  
WantedBy=multi-user.target
```

**步骤2：创建开发相关服务**
```bash
# 创建开发工具服务
sudo vim /etc/systemd/system/dev-tools.service
```

```ini
[Unit]
Description=Development Tools Service
After=network.target

[Service]
Type=forking
ExecStart=/usr/local/bin/start-dev-tools.sh
ExecStop=/usr/local/bin/stop-dev-tools.sh
Restart=on-failure

[Install]
WantedBy=development.target
```

**步骤3：建立服务关联**
```bash
# 让development.target包含开发服务
sudo systemctl add-wants development.target dev-tools.service
sudo systemctl add-wants development.target docker.service
sudo systemctl add-wants development.target nginx.service
```

### 7.3 高级自定义target示例


**🏗️ 数据库专用模式target**

```bash
# 创建数据库target
sudo vim /etc/systemd/system/database.target
```

```ini
[Unit]
Description=Database Server Mode
Documentation=Optimized for database operations
Requires=basic.target
After=basic.target
Conflicts=graphical.target

[Install]
WantedBy=multi-user.target
```

**相关服务配置**
```bash
# 数据库性能优化服务
sudo vim /etc/systemd/system/db-optimization.service
```

```ini
[Unit]
Description=Database Performance Optimization
After=basic.target
Before=database.target

[Service]
Type=oneshot
ExecStart=/opt/scripts/db-tune.sh
RemainAfterExit=yes

[Install]
WantedBy=database.target
```

### 7.4 自定义target管理


**🔧 target维护命令**

```bash
# 重载systemd配置
sudo systemctl daemon-reload

# 启用自定义target
sudo systemctl enable development.target

# 切换到自定义target
sudo systemctl isolate development.target

# 设为默认target
sudo systemctl set-default development.target

# 查看自定义target状态
systemctl status development.target
systemctl list-dependencies development.target
```

**🔍 调试自定义target**
```bash
# 检查target语法
systemd-analyze verify development.target

# 查看target启动时间
systemd-analyze blame | grep development

# 查看配置文件位置
systemctl show development.target | grep FragmentPath
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 target本质：systemd的工作模式定义，类似传统运行级别但更强大
🔸 核心target：multi-user(命令行)、graphical(图形界面)、rescue(救援)
🔸 启动链：sysinit → basic → multi-user → graphical 的依赖链条
🔸 依赖关系：Requires(强依赖)、Wants(弱依赖)、After(顺序依赖)
🔸 target管理：set-default设默认，isolate切换，enable/disable控制
```

### 8.2 关键理解要点


**🔹 target与运行级别的根本差异**
```
传统运行级别：
• 数字标识(0-6)，含义不直观
• 串行启动，效率较低
• 固定模式，扩展性差

systemd target：
• 名称标识，一目了然
• 并行启动，速度更快  
• 灵活定制，可扩展性强
```

**🔹 依赖关系的重要性**
```
理解依赖关系帮助：
• 分析系统启动问题
• 优化启动顺序和时间
• 设计自定义工作模式
• 进行系统故障排查
```

**🔹 实际应用价值**
```
日常运维场景：
• 服务器通常使用multi-user.target
• 桌面系统使用graphical.target  
• 系统维护时切换到rescue.target
• 特殊需求时创建自定义target
```

### 8.3 实用操作速查


**⚡ 常用命令速查表**

| 操作目的 | 命令 | 说明 |
|---------|------|------|
| **查看当前target** | `systemctl get-default` | 显示系统默认target |
| **设置默认target** | `systemctl set-default multi-user.target` | 设置开机默认模式 |
| **临时切换** | `systemctl isolate graphical.target` | 立即切换不重启 |
| **查看依赖** | `systemctl list-dependencies basic.target` | 显示依赖关系树 |
| **查看所有target** | `systemctl list-units --type=target` | 列出所有target状态 |

**🔧 故障排查步骤**
```
1. 检查当前状态：systemctl get-default
2. 查看失败服务：systemctl --failed  
3. 分析依赖关系：systemctl list-dependencies
4. 查看启动日志：journalctl -b
5. 测试切换模式：systemctl isolate rescue.target
```

### 8.4 学习建议与扩展


**📚 深入学习方向**
```
基础掌握后可以探索：
• systemd unit文件编写规范
• 复杂的依赖关系设计
• 性能优化和启动时间分析
• 容器化环境的target应用
• 集群环境的target管理
```

**💡 实践建议**
```
动手练习：
1. 在测试系统上切换不同target
2. 创建一个简单的自定义target
3. 分析自己系统的启动依赖链
4. 模拟故障并练习恢复
5. 优化系统启动时间
```

**🎯 核心记忆**
- **target是系统工作模式**：multi-user命令行，graphical图形界面
- **启动有依赖链条**：sysinit基础，basic核心，multi-user完整
- **管理用systemctl**：get-default查看，set-default设置，isolate切换
- **自定义满足需求**：特殊场景创建专用target，灵活应对不同需要
- **依赖关系很重要**：理解依赖链，才能排查问题和优化性能

**最佳实践原则**：
✅ 服务器优选multi-user.target，节省资源
✅ 桌面系统使用graphical.target，用户友好  
✅ 维护时切换rescue.target，安全可靠
✅ 特殊需求创建自定义target，灵活应对