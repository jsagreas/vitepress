---
title: 3、列表与条件渲染实践
---
## 📚 目录


1. [列表渲染常见错误与解决](#1-列表渲染常见错误与解决)
2. [嵌套列表处理技巧](#2-嵌套列表处理技巧)
3. [条件渲染最佳实践](#3-条件渲染最佳实践)
4. [列表与条件结合使用](#4-列表与条件结合使用)
5. [性能优化技巧](#5-性能优化技巧)
6. [空列表和加载状态处理](#6-空列表和加载状态处理)
7. [核心要点总结](#7-核心要点总结)

---

# 1. 🐛 列表渲染常见错误与解决



## 1.1 缺少key属性 - 最常见的错误



**🔴 错误做法:**
```jsx
// ❌ 没有key，React不知道哪些元素发生了变化
function UserList({ users }) {
  return (
    <ul>
      {users.map(user => 
        <li>{user.name}</li>  // 缺少key属性
      )}
    </ul>
  );
}
```

**🟢 正确做法:**
```jsx
// ✅ 使用唯一ID作为key
function UserList({ users }) {
  return (
    <ul>
      {users.map(user => 
        <li key={user.id}>{user.name}</li>  // 使用唯一ID
      )}
    </ul>
  );
}
```

> 💡 **key的作用是什么？**
> 
> key就像身份证号，帮助React快速识别哪个元素是哪个。没有key，React就像盲人摸象，只能全部重新渲染，性能很差。

## 1.2 使用数组索引做key的陷阱



**🔴 错误做法:**
```jsx
// ❌ 使用索引做key在列表变化时会出问题
function TodoList({ todos }) {
  return (
    <ul>
      {todos.map((todo, index) => 
        <li key={index}>  // 索引会变化
          <input type="text" defaultValue={todo.text} />
        </li>
      )}
    </ul>
  );
}
```

**问题演示:**
```
初始状态: [A, B, C]  →  索引: [0, 1, 2]
删除A后: [B, C]      →  索引: [0, 1] 

结果: B元素的key从1变成0，React认为是新元素！
输入框的内容会错乱或丢失。
```

**🟢 正确做法:**
```jsx
// ✅ 使用稳定不变的唯一标识
function TodoList({ todos }) {
  return (
    <ul>
      {todos.map(todo => 
        <li key={todo.id}>  // 使用不变的ID
          <input type="text" defaultValue={todo.text} />
        </li>
      )}
    </ul>
  );
}
```

## 1.3 在map里面犯的其他错误



**🔴 忘记return:**
```jsx
// ❌ 箭头函数需要return
{users.map(user => {
  <li key={user.id}>{user.name}</li>  // 缺少return
})}

// ✅ 两种正确写法
{users.map(user => (
  <li key={user.id}>{user.name}</li>  // 小括号自动return
))}

{users.map(user => {
  return <li key={user.id}>{user.name}</li>;  // 手动return
})}
```

---

# 2. 🌳 嵌套列表处理技巧



## 2.1 简单嵌套列表



**场景:** 分类商品展示
```jsx
function ProductCategories({ categories }) {
  return (
    <div>
      {categories.map(category => (
        <div key={category.id} className="category">
          <h3>{category.name}</h3>
          <ul>
            {category.products.map(product => (
              <li key={product.id}>
                {product.name} - ¥{product.price}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  );
}
```

> 🎯 **关键点:** 每层列表都要有自己的key，内外层key可以重复（在不同的兄弟层级中）

## 2.2 复杂嵌套结构优化



**🔴 不推荐 - 太复杂:**
```jsx
// ❌ 全部写在一起，难以维护
function ComplexList({ data }) {
  return (
    <div>
      {data.map(dept => (
        <div key={dept.id}>
          <h2>{dept.name}</h2>
          {dept.teams.map(team => (
            <div key={team.id}>
              <h3>{team.name}</h3>
              {team.members.map(member => (
                <div key={member.id}>
                  {member.name} - {member.role}
                </div>
              ))}
            </div>
          ))}
        </div>
      ))}
    </div>
  );
}
```

**🟢 推荐 - 拆分组件:**
```jsx
// ✅ 拆分成小组件，逻辑清晰
function Member({ member }) {
  return (
    <div className="member">
      {member.name} - {member.role}
    </div>
  );
}

function Team({ team }) {
  return (
    <div className="team">
      <h3>{team.name}</h3>
      {team.members.map(member => (
        <Member key={member.id} member={member} />
      ))}
    </div>
  );
}

function Department({ department }) {
  return (
    <div className="department">
      <h2>{department.name}</h2>
      {department.teams.map(team => (
        <Team key={team.id} team={team} />
      ))}
    </div>
  );
}
```

---

# 3. ✨ 条件渲染最佳实践



## 3.1 选择合适的条件渲染方式



**不同场景用不同方法:**

| 场景 | 推荐方法 | 示例 |
|------|----------|------|
| 简单显示/隐藏 | `&&` 操作符 | `{isLoggedIn && <Profile />}` |
| 二选一显示 | 三元运算符 | `{isLoggedIn ? <Profile /> : <Login />}` |
| 多种情况 | 函数返回 | `{renderContent()}` |
| 复杂逻辑 | 提前判断 | `if/else + return` |

## 3.2 && 操作符的正确使用



**🔴 常见陷阱:**
```jsx
// ❌ 数字0会被渲染出来
{items.length && <div>有{items.length}个商品</div>}
// 当items为空数组时，会显示"0"

// ❌ 空字符串也有问题
{user.name && <div>欢迎{user.name}</div>}
// 如果name是空字符串，条件为false，不显示任何内容
```

**🟢 安全的写法:**
```jsx
// ✅ 明确的布尔值判断
{items.length > 0 && <div>有{items.length}个商品</div>}

// ✅ 或者使用三元运算符
{items.length ? <div>有{items.length}个商品</div> : null}

// ✅ 字符串的安全判断
{user.name ? <div>欢迎{user.name}</div> : <div>欢迎访客</div>}
```

## 3.3 提高条件渲染的可读性



**🔴 难以阅读:**
```jsx
// ❌ 嵌套太深，难以理解
return (
  <div>
    {user ? (
      user.isVip ? (
        user.hasDiscount ? (
          <VipDiscountCard />
        ) : (
          <VipCard />
        )
      ) : (
        user.isNewUser ? <NewUserWelcome /> : <RegularUserCard />
      )
    ) : (
      <LoginPrompt />
    )}
  </div>
);
```

**🟢 清晰易读:**
```jsx
// ✅ 使用函数拆分逻辑
function renderUserCard() {
  if (!user) return <LoginPrompt />;
  
  if (user.isVip) {
    return user.hasDiscount ? <VipDiscountCard /> : <VipCard />;
  }
  
  return user.isNewUser ? <NewUserWelcome /> : <RegularUserCard />;
}

return <div>{renderUserCard()}</div>;
```

---

# 4. 🤝 列表与条件结合使用



## 4.1 过滤后再渲染



**场景:** 只显示符合条件的数据
```jsx
function ProductList({ products, showOnlyInStock }) {
  return (
    <div>
      {products
        .filter(product => showOnlyInStock ? product.stock > 0 : true)
        .map(product => (
          <div key={product.id} className="product">
            <h3>{product.name}</h3>
            <p>价格: ¥{product.price}</p>
            {product.stock > 0 ? (
              <span className="in-stock">有货</span>
            ) : (
              <span className="out-of-stock">缺货</span>
            )}
          </div>
        ))
      }
    </div>
  );
}
```

## 4.2 列表中的条件元素



**每个列表项内部有条件逻辑:**
```jsx
function MessageList({ messages, currentUserId }) {
  return (
    <div className="chat">
      {messages.map(message => (
        <div 
          key={message.id} 
          className={`message ${message.senderId === currentUserId ? 'own' : 'other'}`}
        >
          <div className="content">{message.text}</div>
          <div className="meta">
            {message.senderId !== currentUserId && (
              <span className="sender">{message.senderName}</span>
            )}
            <span className="time">{message.timestamp}</span>
            {message.senderId === currentUserId && (
              <button onClick={() => deleteMessage(message.id)}>删除</button>
            )}
          </div>
        </div>
      ))}
    </div>
  );
}
```

## 4.3 分组渲染



**将数据分组后分别渲染:**
```jsx
function TaskList({ tasks }) {
  const pendingTasks = tasks.filter(task => !task.completed);
  const completedTasks = tasks.filter(task => task.completed);
  
  return (
    <div>
      {pendingTasks.length > 0 && (
        <section>
          <h3>待完成 ({pendingTasks.length})</h3>
          {pendingTasks.map(task => (
            <TaskItem key={task.id} task={task} />
          ))}
        </section>
      )}
      
      {completedTasks.length > 0 && (
        <section>
          <h3>已完成 ({completedTasks.length})</h3>
          {completedTasks.map(task => (
            <TaskItem key={task.id} task={task} />
          ))}
        </section>
      )}
      
      {tasks.length === 0 && (
        <div className="empty-state">
          <p>还没有任务，创建一个吧！</p>
        </div>
      )}
    </div>
  );
}
```

---

# 5. ⚡ 性能优化技巧



## 5.1 避免在render中创建新函数



**🔴 性能问题:**
```jsx
// ❌ 每次渲染都创建新函数，子组件会重新渲染
function UserList({ users }) {
  return (
    <ul>
      {users.map(user => (
        <UserItem 
          key={user.id} 
          user={user}
          onEdit={() => editUser(user.id)}  // 新函数
          onDelete={() => deleteUser(user.id)}  // 新函数
        />
      ))}
    </ul>
  );
}
```

**🟢 性能优化:**
```jsx
// ✅ 使用useCallback或将处理逻辑移到子组件
function UserList({ users, onEditUser, onDeleteUser }) {
  return (
    <ul>
      {users.map(user => (
        <UserItem 
          key={user.id} 
          user={user}
          onEdit={onEditUser}  // 传递稳定的函数
          onDelete={onDeleteUser}
        />
      ))}
    </ul>
  );
}

// 在子组件中处理
function UserItem({ user, onEdit, onDelete }) {
  const handleEdit = () => onEdit(user.id);
  const handleDelete = () => onDelete(user.id);
  
  return (
    <li>
      {user.name}
      <button onClick={handleEdit}>编辑</button>
      <button onClick={handleDelete}>删除</button>
    </li>
  );
}
```

## 5.2 使用React.memo优化列表项



```jsx
// ✅ 防止不必要的重渲染
const UserItem = React.memo(function UserItem({ user, onEdit, onDelete }) {
  return (
    <li>
      {user.name}
      <button onClick={() => onEdit(user.id)}>编辑</button>
      <button onClick={() => onDelete(user.id)}>删除</button>
    </li>
  );
});
```

> 💡 **React.memo的作用**
> 
> 就像给组件装了个"变化检测器"，只有props真的改变了才重新渲染，相同的props就复用之前的结果。

## 5.3 大列表虚拟化



**当列表很长时（>1000项）：**
```jsx
// 使用react-window库实现虚拟列表
import { FixedSizeList as List } from 'react-window';

function BigList({ items }) {
  const Row = ({ index, style }) => (
    <div style={style}>
      {items[index].name}
    </div>
  );

  return (
    <List
      height={600}        // 容器高度
      itemCount={items.length}  // 总项数
      itemSize={50}       // 每项高度
    >
      {Row}
    </List>
  );
}
```

---

# 6. 🔄 空列表和加载状态处理



## 6.1 优雅的空状态处理



```jsx
function ProductList({ products, isLoading, error }) {
  // 错误状态
  if (error) {
    return (
      <div className="error-state">
        <p>😔 加载失败了</p>
        <button onClick={retry}>重试</button>
      </div>
    );
  }
  
  // 加载状态
  if (isLoading) {
    return (
      <div className="loading-state">
        <p>🔄 正在加载商品...</p>
      </div>
    );
  }
  
  // 空列表状态
  if (products.length === 0) {
    return (
      <div className="empty-state">
        <p>📦 暂无商品</p>
        <button onClick={addProduct}>添加第一个商品</button>
      </div>
    );
  }
  
  // 正常列表
  return (
    <div className="product-grid">
      {products.map(product => (
        <ProductCard key={product.id} product={product} />
      ))}
    </div>
  );
}
```

## 6.2 搜索结果的空状态



```jsx
function SearchResults({ query, results, isSearching }) {
  if (isSearching) {
    return <div>🔍 搜索中...</div>;
  }
  
  if (query && results.length === 0) {
    return (
      <div className="no-results">
        <p>没有找到"{query}"的相关结果</p>
        <p>试试其他关键词吧</p>
      </div>
    );
  }
  
  if (results.length === 0) {
    return <div>请输入关键词搜索</div>;
  }
  
  return (
    <ul>
      {results.map(item => (
        <li key={item.id}>{item.title}</li>
      ))}
    </ul>
  );
}
```

## 6.3 加载状态的优化体验



**骨架屏加载:**
```jsx
function UserListSkeleton() {
  return (
    <div>
      {[1, 2, 3, 4, 5].map(i => (
        <div key={i} className="user-skeleton">
          <div className="skeleton-avatar"></div>
          <div className="skeleton-text"></div>
        </div>
      ))}
    </div>
  );
}

function UserList({ users, isLoading }) {
  if (isLoading) {
    return <UserListSkeleton />;
  }
  
  return (
    <div>
      {users.map(user => (
        <UserCard key={user.id} user={user} />
      ))}
    </div>
  );
}
```

---

# 7. 📋 核心要点总结



## 7.1 必须掌握的关键点



```
🔸 key属性：使用唯一稳定的ID，不要用数组索引
🔸 条件渲染：&&用于显示隐藏，三元运算符用于二选一
🔸 空状态处理：加载中、错误、空列表都要考虑到
🔸 性能优化：避免render中创建函数，使用React.memo
🔸 代码可读性：复杂逻辑拆分成函数或组件
```

## 7.2 常见错误避免清单



- [ ] ✅ 所有列表项都有key
- [ ] ✅ key使用稳定唯一的标识
- [ ] ✅ && 操作符使用布尔值判断
- [ ] ✅ 复杂条件逻辑已拆分
- [ ] ✅ 处理了空列表状态
- [ ] ✅ 处理了加载和错误状态
- [ ] ✅ 避免了render中创建函数

## 7.3 性能优化检查



```
性能等级评估:
🟢 优秀: 使用memo + 稳定key + 避免重复计算
🟡 良好: 有key + 基本优化
🔴 需改进: 缺少key或有明显性能问题
```

## 7.4 实用模板



**万能列表组件模板:**
```jsx
function ListComponent({ data, isLoading, error, onRetry }) {
  if (error) return <ErrorState onRetry={onRetry} />;
  if (isLoading) return <LoadingState />;
  if (data.length === 0) return <EmptyState />;
  
  return (
    <div>
      {data.map(item => (
        <ListItem key={item.id} item={item} />
      ))}
    </div>
  );
}
```

> 🎯 **核心记忆**
> 
> - **key是身份证** - 帮React识别元素
> - **条件要明确** - 避免0和空字符串陷阱  
> - **空状态要友好** - 用户体验很重要
> - **性能要考虑** - 大列表用虚拟化，频繁更新用memo