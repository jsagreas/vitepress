---
title: 5、非父子通信方案
---
## 📚 目录

1. [非父子通信的概念](#1-非父子通信的概念)
2. [兄弟组件通信策略](#2-兄弟组件通信策略)
3. [全局事件总线实现](#3-全局事件总线实现)
4. [发布订阅模式详解](#4-发布订阅模式详解)
5. [全局状态库预览](#5-全局状态库预览)
6. [第三方通信库](#6-第三方通信库)
7. [通信方案选择指南](#7-通信方案选择指南)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🤔 非父子通信的概念


### 1.1 什么是非父子通信


在React中，**非父子通信**就是指那些没有直接父子关系的组件之间需要交换数据。

```
组件关系图示：
         App
        /   \
   Header     Main
   /          /  \
 Nav       Content Sidebar
           /
        Article

❌ 父子关系：App和Header，Header和Nav
✅ 非父子关系：Nav和Content，Header和Sidebar
```

**通俗理解**：
- 就像家里的兄弟姐妹要聊天，不能直接对话
- 或者楼上楼下的邻居要传话，需要找个中间人
- React组件也一样，距离远的组件通信需要特殊方法

### 1.2 为什么需要非父子通信


**现实场景举例**：
```
🛒 购物网站示例：
- 顶部导航显示购物车数量
- 商品列表中点击"加入购物车"
- 两个组件没有父子关系，但需要通信

🔔 消息通知示例：
- 用户在任何页面操作
- 顶部需要显示通知消息
- 操作组件和通知组件相距很远
```

### 1.3 传统解决方案的问题


> **💡 状态提升的局限性**
> 
> 把共享状态放到最近的共同父组件中，但这会导致：
> - 状态层级过深，管理复杂
> - 中间组件被迫传递不相关的props
> - 代码维护困难

```
传统方案问题图示：
         App (存放共享状态)
        /   \
   Header(传递)  Main(传递)
   /              /  \
 Nav(使用)     Content  Sidebar(使用)

问题：Header和Main只是"传话筒"，增加了复杂度
```

---

## 2. 👫 兄弟组件通信策略


### 2.1 通过共同父组件中转


这是最基础的方法，适合简单的兄弟组件通信。

**实现原理**：
```
通信路径：兄弟A → 父组件 → 兄弟B

   Parent (中转站)
   /           \
Brother1  →  Brother2
```

**代码示例**：
```jsx
// 父组件：负责中转消息
function Parent() {
  const [message, setMessage] = useState('');
  
  return (
    <div>
      <Brother1 onSendMessage={setMessage} />
      <Brother2 message={message} />
    </div>
  );
}

// 兄弟组件1：发送消息
function Brother1({ onSendMessage }) {
  const handleClick = () => {
    onSendMessage('来自兄弟1的问候！');
  };
  
  return <button onClick={handleClick}>发送消息</button>;
}

// 兄弟组件2：接收消息
function Brother2({ message }) {
  return <div>收到消息：{message}</div>;
}
```

**适用场景**：
- ✅ 组件层级不深（2-3层）
- ✅ 通信需求简单
- ✅ 数据变化不频繁

### 2.2 使用React Context


当兄弟组件距离较远时，Context是更好的选择。

**Context通俗理解**：
- 就像家里的"公告栏"
- 任何房间的人都能看到和更新公告
- 不需要一个个房间传话

```jsx
// 创建通信Context
const MessageContext = createContext();

// 祖先组件：提供Context
function App() {
  const [sharedData, setSharedData] = useState('');
  
  return (
    <MessageContext.Provider value={{ sharedData, setSharedData }}>
      <Header />
      <Main />
    </MessageContext.Provider>
  );
}

// 任意后代组件都能使用
function SomeComponent() {
  const { sharedData, setSharedData } = useContext(MessageContext);
  
  return (
    <div>
      <p>共享数据：{sharedData}</p>
      <button onClick={() => setSharedData('新数据')}>
        更新数据
      </button>
    </div>
  );
}
```

---

## 3. 📡 全局事件总线实现


### 3.1 事件总线的概念


**事件总线**就像城市的广播电台：
- 任何人都能发送广播（发布事件）
- 任何人都能收听广播（订阅事件）
- 发送者和接收者不需要知道对方是谁

```
事件总线工作原理：
组件A ──发布──► [事件总线] ◄──订阅── 组件B
组件C ──发布──►              ◄──订阅── 组件D
```

### 3.2 简单事件总线实现


```jsx
// 创建事件总线
class EventBus {
  constructor() {
    this.events = {}; // 存储所有事件监听器
  }
  
  // 订阅事件（听广播）
  on(eventName, callback) {
    if (!this.events[eventName]) {
      this.events[eventName] = [];
    }
    this.events[eventName].push(callback);
  }
  
  // 发布事件（发广播）
  emit(eventName, data) {
    if (this.events[eventName]) {
      this.events[eventName].forEach(callback => callback(data));
    }
  }
  
  // 取消订阅（关闭收音机）
  off(eventName, callback) {
    if (this.events[eventName]) {
      this.events[eventName] = this.events[eventName].filter(
        cb => cb !== callback
      );
    }
  }
}

// 创建全局事件总线实例
const eventBus = new EventBus();
```

### 3.3 在组件中使用事件总线


```jsx
// 发送消息的组件
function Sender() {
  const sendMessage = () => {
    eventBus.emit('userLogin', { 
      username: '张三', 
      time: new Date() 
    });
  };
  
  return <button onClick={sendMessage}>用户登录</button>;
}

// 接收消息的组件
function Receiver() {
  const [userInfo, setUserInfo] = useState(null);
  
  useEffect(() => {
    // 订阅登录事件
    const handleLogin = (data) => {
      setUserInfo(data);
    };
    
    eventBus.on('userLogin', handleLogin);
    
    // 组件卸载时取消订阅
    return () => {
      eventBus.off('userLogin', handleLogin);
    };
  }, []);
  
  return (
    <div>
      {userInfo ? `欢迎 ${userInfo.username}！` : '未登录'}
    </div>
  );
}
```

---

## 4. 📢 发布订阅模式详解


### 4.1 发布订阅模式的核心思想


**发布订阅模式**就像微信朋友圈：
- **发布者**：发朋友圈的人（不关心谁看到）
- **订阅者**：刷朋友圈的人（主动选择关注谁）
- **调度中心**：微信系统（负责分发消息）

```
发布订阅vs观察者模式：

发布订阅（松耦合）：
发布者 → 调度中心 → 订阅者
      （不直接认识）

观察者（紧耦合）：
发布者 ← 直接认识 → 观察者
```

### 4.2 React中的发布订阅实现


```jsx
// 增强版事件管理器
class PubSub {
  constructor() {
    this.subscribers = new Map(); // 用Map存储订阅者
  }
  
  // 订阅频道
  subscribe(channel, callback) {
    if (!this.subscribers.has(channel)) {
      this.subscribers.set(channel, new Set());
    }
    this.subscribers.get(channel).add(callback);
    
    // 返回取消订阅的函数
    return () => {
      this.subscribers.get(channel)?.delete(callback);
    };
  }
  
  // 发布消息到频道
  publish(channel, data) {
    const callbacks = this.subscribers.get(channel);
    if (callbacks) {
      callbacks.forEach(callback => {
        // 异步执行，避免阻塞
        setTimeout(() => callback(data), 0);
      });
    }
  }
  
  // 清空某个频道
  clear(channel) {
    this.subscribers.delete(channel);
  }
}

// 全局发布订阅实例
const pubsub = new PubSub();
```

### 4.3 实际应用示例


```jsx
// 购物车组件
function Cart() {
  const [items, setItems] = useState([]);
  
  useEffect(() => {
    // 订阅添加商品事件
    const unsubscribe = pubsub.subscribe('cart:add', (product) => {
      setItems(prev => [...prev, product]);
    });
    
    return unsubscribe; // 自动清理
  }, []);
  
  return (
    <div>
      购物车商品数量：{items.length}
    </div>
  );
}

// 商品列表组件
function ProductList() {
  const addToCart = (product) => {
    // 发布添加商品事件
    pubsub.publish('cart:add', product);
  };
  
  return (
    <div>
      <button onClick={() => addToCart({ id: 1, name: '苹果' })}>
        加入购物车
      </button>
    </div>
  );
}
```

---

## 5. 🏪 全局状态库预览


### 5.1 状态管理库的作用


**全局状态库**就像银行：
- 把所有重要的"钱"（状态）存在一个安全的地方
- 任何"客户"（组件）都能存取
- 有完整的"账单"（状态变化记录）

```
全局状态库架构：
┌─────────────────────────────────┐
│         全局状态库               │
│  ┌─────┐ ┌─────┐ ┌─────┐       │
│  │用户 │ │购物车│ │主题 │       │
│  │状态 │ │状态 │ │状态 │       │
│  └─────┘ └─────┘ └─────┘       │
└─────────────────────────────────┘
    ↕       ↕       ↕
  组件A    组件B    组件C
```

### 5.2 主流状态管理方案对比


| 方案 | 适用场景 | 学习成本 | 性能 |
|------|----------|----------|------|
| **useState + Context** | 小型项目 | ⭐ | ⭐⭐ |
| **Zustand** | 中型项目 | ⭐⭐ | ⭐⭐⭐ |
| **Redux Toolkit** | 大型项目 | ⭐⭐⭐ | ⭐⭐⭐ |
| **Jotai** | 原子化状态 | ⭐⭐ | ⭐⭐⭐ |

### 5.3 Zustand简单示例


```jsx
// 创建store（商店）
import { create } from 'zustand';

const useStore = create((set) => ({
  // 状态
  count: 0,
  user: null,
  
  // 行为（修改状态的方法）
  increment: () => set((state) => ({ count: state.count + 1 })),
  setUser: (user) => set({ user }),
}));

// 在组件中使用
function Counter() {
  const { count, increment } = useStore();
  
  return (
    <div>
      <p>计数：{count}</p>
      <button onClick={increment}>+1</button>
    </div>
  );
}

function UserInfo() {
  const user = useStore((state) => state.user);
  
  return <div>用户：{user?.name || '未登录'}</div>;
}
```

---

## 6. 🔧 第三方通信库


### 6.1 常用通信库推荐


**简单项目推荐**：
```jsx
// mitt - 轻量级事件发射器（仅2KB）
import mitt from 'mitt';

const emitter = mitt();

// 发送事件
emitter.emit('user-login', { id: 1, name: '张三' });

// 监听事件
emitter.on('user-login', (user) => {
  console.log('用户登录：', user);
});
```

**复杂项目推荐**：
```jsx
// RxJS - 强大的响应式编程库
import { Subject } from 'rxjs';
import { filter, map } from 'rxjs/operators';

const messageStream = new Subject();

// 发送消息
messageStream.next({ type: 'USER_LOGIN', data: { name: '张三' } });

// 订阅特定类型的消息
messageStream.pipe(
  filter(msg => msg.type === 'USER_LOGIN'),
  map(msg => msg.data)
).subscribe(user => {
  console.log('用户登录：', user);
});
```

### 6.2 自定义Hook方案


```jsx
// 创建通用的通信Hook
function useEventCommunication() {
  const listeners = useRef(new Map());
  
  const emit = useCallback((event, data) => {
    const callbacks = listeners.current.get(event) || [];
    callbacks.forEach(callback => callback(data));
  }, []);
  
  const on = useCallback((event, callback) => {
    const callbacks = listeners.current.get(event) || [];
    listeners.current.set(event, [...callbacks, callback]);
    
    // 返回清理函数
    return () => {
      const updated = listeners.current.get(event)?.filter(cb => cb !== callback);
      listeners.current.set(event, updated || []);
    };
  }, []);
  
  return { emit, on };
}

// 使用自定义Hook
function Component1() {
  const { emit } = useEventCommunication();
  
  return (
    <button onClick={() => emit('greeting', 'Hello!')}>
      发送问候
    </button>
  );
}

function Component2() {
  const { on } = useEventCommunication();
  const [message, setMessage] = useState('');
  
  useEffect(() => {
    return on('greeting', setMessage);
  }, [on]);
  
  return <div>收到消息：{message}</div>;
}
```

---

## 7. 🎯 通信方案选择指南


### 7.1 选择决策树


```
项目通信需求分析：

数据共享频率高？
    ├─ 是 → 使用全局状态管理
    │      ├─ 简单项目 → Context + useReducer
    │      ├─ 中等项目 → Zustand
    │      └─ 复杂项目 → Redux Toolkit
    │
    └─ 否 → 偶尔通信
           ├─ 组件距离近 → Props传递
           ├─ 组件距离远 → Context
           └─ 一次性事件 → 事件总线
```

### 7.2 性能与复杂度对比


| 方案 | 性能开销 | 代码复杂度 | 维护成本 | 适用规模 |
|------|----------|------------|----------|----------|
| **Props传递** | 最低 ⭐ | 最低 ⭐ | 低 ⭐⭐ | 小型 |
| **Context** | 低 ⭐⭐ | 低 ⭐⭐ | 中 ⭐⭐⭐ | 中小型 |
| **事件总线** | 中 ⭐⭐⭐ | 中 ⭐⭐⭐ | 中 ⭐⭐⭐ | 中型 |
| **状态库** | 高 ⭐⭐⭐⭐ | 高 ⭐⭐⭐⭐ | 低 ⭐⭐ | 大型 |

### 7.3 最佳实践建议


> **🎯 通信方案选择原则**
> 
> 1. **就近原则**：能用简单方案解决就不用复杂方案
> 2. **按需选择**：根据项目规模和团队能力选择
> 3. **统一标准**：一个项目内保持通信方案的一致性
> 4. **性能优先**：避免不必要的重新渲染

**具体建议**：
```
📝 小型项目（<10个组件）：
   → Props + Context 足够

🏗️ 中型项目（10-50个组件）：
   → Context + 事件总线 + 轻量状态库

🏢 大型项目（>50个组件）：
   → 专业状态管理 + 明确的数据流向
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 非父子通信：没有直接父子关系的组件间数据交换
🔸 兄弟组件通信：同级组件间的数据传递方式
🔸 事件总线：全局事件发布订阅机制
🔸 发布订阅模式：松耦合的消息传递模式
🔸 全局状态：整个应用共享的数据状态
🔸 通信方案选择：根据项目需求选择合适的通信方式
```

### 8.2 关键理解要点


**🔹 为什么需要多种通信方案**
```
不同场景有不同需求：
- 简单数据传递 → Props就够了
- 偶尔的远距离通信 → 事件总线
- 频繁的数据共享 → 全局状态管理
- 复杂的业务逻辑 → 专业状态库
```

**🔹 通信方案的权衡**
```
简单 vs 强大：
- 简单方案易学易用，但功能有限
- 强大方案功能丰富，但学习成本高
- 关键是找到项目需求的平衡点
```

**🔹 性能影响**
```
不同方案对性能的影响：
- Context变化会导致所有消费者重新渲染
- 事件总线是异步的，不会阻塞UI
- 状态库通常有优化机制，按需更新
```

### 8.3 实际应用价值


- **项目架构**：为不同规模项目选择合适的通信架构
- **团队协作**：统一的通信方案便于团队开发
- **代码维护**：清晰的数据流向提高代码可维护性
- **性能优化**：合理的通信方案避免不必要的渲染
- **技术选型**：理解各种方案的优缺点，做出明智选择

**核心记忆口诀**：
```
组件通信有妙招，
距离远近要分好，
简单传值用Context，
复杂状态用状态库，
事件总线解耦合，
按需选择最重要！
```