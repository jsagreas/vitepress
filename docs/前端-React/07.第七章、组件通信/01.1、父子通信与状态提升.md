---
title: 1、父子通信与状态提升
---
## 📚 目录

1. [组件通信概念](#1-组件通信概念)
2. [父子组件通信基础](#2-父子组件通信基础)
3. [Props传值详解](#3-Props传值详解)
4. [回调函数通信](#4-回调函数通信)
5. [状态提升原理](#5-状态提升原理)
6. [兄弟组件通信](#6-兄弟组件通信)
7. [通信最佳实践](#7-通信最佳实践)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔗 组件通信概念


### 1.1 什么是组件通信


**简单理解**：组件通信就是让不同的组件之间能够"说话"，互相传递信息。

```
现实类比：
父母和孩子聊天：
🏠 父母告诉孩子今天的天气（父→子）
👶 孩子告诉父母今天考试成绩（子→父）
👫 兄弟姐妹通过父母传话（兄弟通信）
```

### 1.2 为什么需要组件通信


**核心原因**：
- 🎯 **数据共享**：多个组件需要使用同一份数据
- 🔄 **状态同步**：一个组件的变化需要影响其他组件
- 📡 **事件传递**：用户操作需要传递给相关组件

**实际场景**：
```
购物车应用：
商品列表组件 ←→ 购物车组件 ←→ 总价组件
添加商品时，所有组件都要更新
```

### 1.3 React中的数据流


**单向数据流**：
```
          父组件
         /      \
    子组件1    子组件2
    
数据流向：父 → 子（通过props）
事件流向：子 → 父（通过回调函数）
```

---

## 2. 👨‍👧‍👦 父子组件通信基础


### 2.1 组件关系理解


**父子关系**：
```jsx
// 父组件
function Parent() {
  return (
    <div>
      <Child />  {/* Child是Parent的子组件 */}
    </div>
  );
}

// 子组件
function Child() {
  return <div>我是子组件</div>;
}
```

### 2.2 通信方向与方式


| 通信方向 | 实现方式 | 用途 |
|---------|---------|------|
| 🔽 **父→子** | `props传值` | 传递数据、配置 |
| 🔼 **子→父** | `回调函数` | 传递事件、状态变化 |
| ↔️ **兄弟间** | `状态提升` | 共享数据、同步状态 |

### 2.3 通信数据流向


```
数据流向示意：
┌─────────────┐
│   父组件     │ 
│ state: 数据  │ ──props──┐
└─────────────┘          │
                         ▼
                ┌─────────────┐
                │   子组件     │
                │ 接收props   │
                └─────────────┘
                         │
                     回调函数
                         │
                         ▼
                ┌─────────────┐
                │   父组件     │
                │ 更新state   │
                └─────────────┘
```

---

## 3. 📦 Props传值详解


### 3.1 Props的本质


**通俗理解**：Props就像是"快递包裹"，父组件把数据打包，子组件签收使用。

```jsx
// 父组件"寄快递"
function Parent() {
  const message = "你好，孩子！";
  const count = 100;
  
  return (
    <Child 
      message={message}  // 传递字符串
      count={count}      // 传递数字
      isActive={true}    // 传递布尔值
    />
  );
}

// 子组件"收快递"
function Child({ message, count, isActive }) {
  return (
    <div>
      <p>收到消息：{message}</p>
      <p>数量：{count}</p>
      <p>状态：{isActive ? '激活' : '未激活'}</p>
    </div>
  );
}
```

### 3.2 Props的特点


**🔸 核心特点**：
- ✅ **只读性**：子组件不能修改props
- ✅ **动态性**：props值变化，子组件自动重新渲染
- ✅ **类型丰富**：可传递任何JavaScript数据类型

```jsx
// ❌ 错误：子组件不能修改props
function Child({ count }) {
  // count = 200; // 这样做是错误的！
  
  return <div>{count}</div>;
}

// ✅ 正确：如果需要修改，要通过父组件
function Parent() {
  const [count, setCount] = useState(100);
  
  return (
    <Child 
      count={count} 
      onIncrease={() => setCount(count + 1)} 
    />
  );
}
```

### 3.3 多种数据类型传递


```jsx
function Parent() {
  const user = { name: '张三', age: 25 };
  const hobbies = ['读书', '游戏', '电影'];
  const handleClick = () => console.log('点击了');
  
  return (
    <Child 
      user={user}           // 对象
      hobbies={hobbies}     // 数组
      onClick={handleClick} // 函数
      style={{color: 'red'}} // 样式对象
    />
  );
}

function Child({ user, hobbies, onClick, style }) {
  return (
    <div style={style}>
      <h3>{user.name} ({user.age}岁)</h3>
      <ul>
        {hobbies.map(hobby => (
          <li key={hobby}>{hobby}</li>
        ))}
      </ul>
      <button onClick={onClick}>点击我</button>
    </div>
  );
}
```

---

## 4. 🔄 回调函数通信


### 4.1 回调函数的概念


**通俗理解**：回调函数就像"电话号码"，父组件给子组件一个"号码"，子组件有事就"打电话"给父组件。

```jsx
// 父组件给子组件一个"电话号码"
function Parent() {
  const [message, setMessage] = useState('');
  
  // 这是"电话号码"（回调函数）
  const receiveCall = (data) => {
    setMessage(`子组件说：${data}`);
  };
  
  return (
    <div>
      <p>{message}</p>
      <Child onCall={receiveCall} />
    </div>
  );
}

// 子组件"打电话"
function Child({ onCall }) {
  const handleClick = () => {
    onCall('我被点击了！'); // "拨打电话"
  };
  
  return <button onClick={handleClick}>点击通知父组件</button>;
}
```

### 4.2 事件回调模式


**常见模式**：
```jsx
function TodoApp() {
  const [todos, setTodos] = useState([]);
  
  // 添加任务的回调
  const addTodo = (text) => {
    setTodos([...todos, { id: Date.now(), text, done: false }]);
  };
  
  // 切换完成状态的回调
  const toggleTodo = (id) => {
    setTodos(todos.map(todo => 
      todo.id === id ? { ...todo, done: !todo.done } : todo
    ));
  };
  
  return (
    <div>
      <TodoInput onAdd={addTodo} />
      <TodoList todos={todos} onToggle={toggleTodo} />
    </div>
  );
}
```

### 4.3 子组件调用父组件方法


```jsx
// 父组件提供方法
function Calculator() {
  const [result, setResult] = useState(0);
  
  // 父组件的计算方法
  const calculate = (operation, value) => {
    switch(operation) {
      case 'add':
        setResult(result + value);
        break;
      case 'subtract':
        setResult(result - value);
        break;
      default:
        setResult(0);
    }
  };
  
  return (
    <div>
      <h2>计算结果：{result}</h2>
      <CalculatorButtons onCalculate={calculate} />
    </div>
  );
}

// 子组件调用父组件方法
function CalculatorButtons({ onCalculate }) {
  return (
    <div>
      <button onClick={() => onCalculate('add', 10)}>+10</button>
      <button onClick={() => onCalculate('subtract', 5)}>-5</button>
      <button onClick={() => onCalculate('reset', 0)}>重置</button>
    </div>
  );
}
```

---

## 5. ⬆️ 状态提升原理


### 5.1 什么是状态提升


**通俗理解**：状态提升就像"家庭会议"，把大家都关心的事情拿到客厅（父组件）讨论，而不是在各自房间（子组件）里决定。

### 5.2 为什么需要状态提升


**问题场景**：
```jsx
// ❌ 问题：两个子组件各自管理状态，无法同步
function Counter1() {
  const [count, setCount] = useState(0);
  return <div>计数器1: {count}</div>;
}

function Counter2() {
  const [count, setCount] = useState(0);
  return <div>计数器2: {count}</div>;
}

// 结果：两个计数器互不相关，无法同步
```

**解决方案：状态提升**
```jsx
// ✅ 解决：状态提升到父组件
function App() {
  const [count, setCount] = useState(0); // 状态在父组件
  
  return (
    <div>
      <Counter value={count} onChange={setCount} />
      <Counter value={count} onChange={setCount} />
      <p>两个计数器现在同步了！</p>
    </div>
  );
}

function Counter({ value, onChange }) {
  return (
    <div>
      <span>计数：{value}</span>
      <button onClick={() => onChange(value + 1)}>+1</button>
    </div>
  );
}
```

### 5.3 状态提升实战案例


**温度转换器示例**：
```jsx
function TemperatureApp() {
  const [temperature, setTemperature] = useState('');
  const [scale, setScale] = useState('c');
  
  // 转换函数
  const toCelsius = (fahrenheit) => (fahrenheit - 32) * 5 / 9;
  const toFahrenheit = (celsius) => (celsius * 9 / 5) + 32;
  
  return (
    <div>
      <TemperatureInput
        scale="c"
        temperature={scale === 'c' ? temperature : 
                    temperature === '' ? '' : toCelsius(temperature)}
        onTemperatureChange={(temp) => {
          setTemperature(temp);
          setScale('c');
        }}
      />
      
      <TemperatureInput
        scale="f"
        temperature={scale === 'f' ? temperature : 
                    temperature === '' ? '' : toFahrenheit(temperature)}
        onTemperatureChange={(temp) => {
          setTemperature(temp);
          setScale('f');
        }}
      />
      
      <BoilingVerdict celsius={
        scale === 'c' ? temperature : toCelsius(temperature)
      } />
    </div>
  );
}

function TemperatureInput({ scale, temperature, onTemperatureChange }) {
  const scaleName = scale === 'c' ? '摄氏度' : '华氏度';
  
  return (
    <fieldset>
      <legend>输入{scaleName}温度:</legend>
      <input
        value={temperature}
        onChange={(e) => onTemperatureChange(e.target.value)}
      />
    </fieldset>
  );
}
```

### 5.4 找到共同父组件


**步骤指南**：
```
1. 找出需要共享状态的组件
2. 找到它们的最近公共父组件
3. 将状态放在公共父组件中
4. 通过props向下传递状态
5. 通过回调向上传递更新
```

**组件层级示意**：
```
        App (共同父组件)
       /   \
   Header   Main
           /    \
    Sidebar    Content
              /      \
        TodoList   TodoForm
        
如果TodoList和TodoForm需要共享状态
→ 状态应该提升到Content组件
```

---

## 6. 👫 兄弟组件通信


### 6.1 兄弟组件的通信方式


**核心原理**：兄弟组件不能直接通信，需要通过父组件"传话"。

```jsx
function App() {
  const [selectedUser, setSelectedUser] = useState(null);
  
  return (
    <div style={{ display: 'flex' }}>
      {/* 左侧：用户列表 */}
      <UserList onSelectUser={setSelectedUser} />
      
      {/* 右侧：用户详情 */}
      <UserDetail user={selectedUser} />
    </div>
  );
}

// 兄弟组件1：用户列表
function UserList({ onSelectUser }) {
  const users = [
    { id: 1, name: '张三' },
    { id: 2, name: '李四' }
  ];
  
  return (
    <div>
      <h3>用户列表</h3>
      {users.map(user => (
        <button
          key={user.id}
          onClick={() => onSelectUser(user)}
        >
          {user.name}
        </button>
      ))}
    </div>
  );
}

// 兄弟组件2：用户详情
function UserDetail({ user }) {
  if (!user) {
    return <div>请选择一个用户</div>;
  }
  
  return (
    <div>
      <h3>用户详情</h3>
      <p>姓名：{user.name}</p>
      <p>ID：{user.id}</p>
    </div>
  );
}
```

### 6.2 购物车实战案例


```jsx
function ShoppingApp() {
  const [cartItems, setCartItems] = useState([]);
  
  // 添加商品到购物车
  const addToCart = (product) => {
    const existingItem = cartItems.find(item => item.id === product.id);
    
    if (existingItem) {
      setCartItems(cartItems.map(item =>
        item.id === product.id 
          ? { ...item, quantity: item.quantity + 1 }
          : item
      ));
    } else {
      setCartItems([...cartItems, { ...product, quantity: 1 }]);
    }
  };
  
  // 从购物车移除商品
  const removeFromCart = (productId) => {
    setCartItems(cartItems.filter(item => item.id !== productId));
  };
  
  return (
    <div>
      <ProductList onAddToCart={addToCart} />
      <Cart items={cartItems} onRemove={removeFromCart} />
    </div>
  );
}
```

---

## 7. 🎯 通信最佳实践


### 7.1 避免Props Drilling


**问题**：多层传递props变得复杂
```jsx
// ❌ 问题：props需要层层传递
function App() {
  const [user, setUser] = useState({ name: '张三' });
  return <Level1 user={user} setUser={setUser} />;
}

function Level1({ user, setUser }) {
  return <Level2 user={user} setUser={setUser} />;
}

function Level2({ user, setUser }) {
  return <Level3 user={user} setUser={setUser} />;
}

function Level3({ user, setUser }) {
  return <div>{user.name}</div>;
}
```

**解决方案**：
- 🔸 **重新设计组件结构**
- 🔸 **使用组合模式**
- 🔸 **考虑Context API**（后续章节）

### 7.2 组件组合模式


```jsx
// ✅ 使用组合模式避免不必要的props传递
function App() {
  const [user, setUser] = useState({ name: '张三' });
  
  return (
    <Layout>
      <Header user={user} />
      <Main>
        <UserProfile user={user} onUpdate={setUser} />
      </Main>
    </Layout>
  );
}

function Layout({ children }) {
  return <div className="layout">{children}</div>;
}

function Main({ children }) {
  return <main>{children}</main>;
}
```

### 7.3 通信原则


**🔸 设计原则**：
- ✅ **最小权限**：只传递必要的数据
- ✅ **单一职责**：每个组件职责明确
- ✅ **就近原则**：状态放在最近的公共父组件
- ✅ **命名清晰**：回调函数命名要明确意图

```jsx
// ✅ 好的例子：命名清晰，职责单一
function TodoApp() {
  const [todos, setTodos] = useState([]);
  
  const handleAddTodo = (text) => { /* ... */ };
  const handleToggleTodo = (id) => { /* ... */ };
  const handleDeleteTodo = (id) => { /* ... */ };
  
  return (
    <div>
      <AddTodoForm onSubmit={handleAddTodo} />
      <TodoList 
        todos={todos}
        onToggle={handleToggleTodo}
        onDelete={handleDeleteTodo}
      />
    </div>
  );
}
```

### 7.4 性能优化提示


```jsx
// 🔸 使用React.memo避免不必要的重渲染
const Child = React.memo(function Child({ data, onClick }) {
  return (
    <div onClick={onClick}>
      {data.name}
    </div>
  );
});

// 🔸 使用useCallback缓存回调函数
function Parent() {
  const [items, setItems] = useState([]);
  
  const handleClick = useCallback((id) => {
    setItems(items => items.filter(item => item.id !== id));
  }, []);
  
  return (
    <div>
      {items.map(item => (
        <Child 
          key={item.id}
          data={item}
          onClick={() => handleClick(item.id)}
        />
      ))}
    </div>
  );
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 组件通信：让不同组件能够互相传递信息
🔸 Props传值：父组件向子组件传递数据的方式
🔸 回调函数：子组件向父组件传递信息的方式
🔸 状态提升：将共享状态提升到公共父组件
🔸 单向数据流：React中数据只能从父组件流向子组件
```

### 8.2 关键理解要点


**🔹 通信方向记忆**：
```
父→子：props传值（送快递）
子→父：回调函数（打电话）
兄弟间：通过父组件（传话筒）
```

**🔹 状态提升时机**：
```
何时提升状态？
✅ 多个子组件需要相同数据
✅ 子组件之间需要同步
✅ 子组件数据变化需要影响其他组件
```

**🔹 最佳实践原则**：
```
设计原则：
• 状态尽可能靠近使用的地方
• 避免不必要的props传递
• 保持组件职责单一
• 使用清晰的命名
```

### 8.3 实际应用场景


- **表单组件**：父组件收集子组件的表单数据
- **列表详情**：列表选择影响详情显示
- **购物车**：商品列表和购物车状态同步
- **用户界面**：登录状态在多个组件间共享
- **数据筛选**：筛选条件影响数据显示

### 8.4 常见问题与解决


**❌ 常见错误**：
```javascript
// 1. 直接修改props
function Child({ data }) {
  data.name = '新名字'; // 错误！
}

// 2. 忘记传递key
{items.map(item => <Item data={item} />)} // 缺少key

// 3. 回调函数重复创建
<Child onClick={() => handleClick(item.id)} /> // 每次都创建新函数
```

**✅ 正确做法**：
```javascript
// 1. 通过回调修改状态
function Child({ data, onChange }) {
  onChange({ ...data, name: '新名字' });
}

// 2. 添加key属性
{items.map(item => <Item key={item.id} data={item} />)}

// 3. 使用useCallback优化
const handleClick = useCallback((id) => {
  // 处理逻辑
}, []);
```

**核心记忆**：
- 组件通信像家庭对话，父子兄弟有规矩
- Props向下传数据，回调向上传消息
- 共享状态要提升，找到公共父组件
- 设计清晰职责明，性能优化不能忘