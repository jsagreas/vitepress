---
title: 3、代码分割与懒加载
---
## 📚 目录

1. [代码分割基础概念](#1-代码分割基础概念)
2. [React.lazy懒加载组件](#2-react-lazy懒加载组件)
3. [Suspense组件与加载状态](#3-suspense组件与加载状态)
4. [动态import语法详解](#4-动态import语法详解)
5. [路由级代码分割](#5-路由级代码分割)
6. [组件级代码分割](#6-组件级代码分割)
7. [加载失败处理与预加载策略](#7-加载失败处理与预加载策略)
8. [虚拟化长列表优化](#8-虚拟化长列表优化)
9. [打包体积优化实战](#9-打包体积优化实战)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🎯 代码分割基础概念


### 1.1 什么是代码分割


**🔸 通俗理解**
代码分割就像**吃自助餐**一样 - 不是一开始就把所有菜都装满盘子，而是**需要什么取什么**。

```
传统加载方式（一次性加载）：
浏览器 → 下载整个应用 → 等待全部完成 → 才能使用

代码分割方式（按需加载）：
浏览器 → 下载核心代码 → 立即可用 → 需要时再下载其他部分
```

**🔸 核心好处**
- **⚡ 首屏加载更快**：只下载当前需要的代码
- **💾 节省流量**：用户可能不会访问所有页面
- **🚀 提升体验**：用户能更快看到页面内容

### 1.2 为什么需要代码分割


```
假设一个电商网站包含：
├── 首页 (50KB)
├── 商品列表 (80KB)  
├── 购物车 (30KB)
├── 用户中心 (60KB)
└── 管理后台 (120KB)

❌ 不分割：用户访问首页也要下载 340KB
✅ 分割后：用户访问首页只需下载 50KB
```

**🔸 实际场景对比**

| 场景 | 不分割 | 分割后 | 提升效果 |
|------|--------|--------|----------|
| **首页访问** | `340KB，2-3秒` | `50KB，0.5秒` | `⚡ 快6倍` |
| **手机用户** | `消耗流量多` | `节省85%流量` | `💰 省钱` |
| **跳出率** | `高，等待时间长` | `低，体验流畅` | `📈 提升转化` |

---

## 2. 🧩 React.lazy懒加载组件


### 2.1 什么是React.lazy


**🔸 简单理解**
`React.lazy` 就像**预约餐厅** - 不是现在就去吃饭，而是**需要的时候再去**。

```jsx
// ❌ 传统方式：立即导入
import UserProfile from './UserProfile';

// ✅ 懒加载方式：需要时才导入
const UserProfile = React.lazy(() => import('./UserProfile'));
```

### 2.2 基础使用方法


```jsx
import React, { Suspense, useState } from 'react';

// 🔸 创建懒加载组件
const UserProfile = React.lazy(() => import('./UserProfile'));
const Settings = React.lazy(() => import('./Settings'));

function App() {
  const [currentTab, setCurrentTab] = useState('home');

  return (
    <div>
      <nav>
        <button onClick={() => setCurrentTab('home')}>首页</button>
        <button onClick={() => setCurrentTab('profile')}>个人资料</button>
        <button onClick={() => setCurrentTab('settings')}>设置</button>
      </nav>

      {/* 🔸 只有点击对应按钮时才加载组件 */}
      <Suspense fallback={<div>加载中...</div>}>
        {currentTab === 'home' && <div>首页内容</div>}
        {currentTab === 'profile' && <UserProfile />}
        {currentTab === 'settings' && <Settings />}
      </Suspense>
    </div>
  );
}
```

### 2.3 lazy的工作原理


```
用户操作流程：
1. 用户点击"个人资料"按钮
2. React发现需要UserProfile组件
3. 浏览器开始下载UserProfile.js文件
4. 下载期间显示"加载中..."
5. 下载完成后显示UserProfile组件

┌─────────────┐    点击按钮    ┌─────────────┐
│    用户     │ ──────────→ │   React     │
└─────────────┘              └─────────────┘
                                     │
                                     ▼
                             ┌─────────────┐
                             │ 下载组件文件 │
                             └─────────────┘
                                     │
                                     ▼
                             ┌─────────────┐
                             │ 渲染组件    │
                             └─────────────┘
```

---

## 3. ⏳ Suspense组件与加载状态


### 3.1 Suspense是什么


**🔸 通俗比喻**
`Suspense` 就像**电梯的等待提示** - 当电梯还没到的时候，显示"电梯正在上来"，到了就直接进入。

### 3.2 基本使用方式


```jsx
import React, { Suspense } from 'react';

const LazyComponent = React.lazy(() => import('./LazyComponent'));

function App() {
  return (
    <div>
      {/* 🔸 Suspense包裹懒加载组件 */}
      <Suspense fallback={<div>组件加载中，请稍候...</div>}>
        <LazyComponent />
      </Suspense>
    </div>
  );
}
```

### 3.3 优雅的加载状态设计


```jsx
// 🔸 简单加载提示
const SimpleLoading = () => <div>加载中...</div>;

// 🔸 美观的加载提示
const BeautifulLoading = () => (
  <div style={{ 
    display: 'flex', 
    justifyContent: 'center', 
    alignItems: 'center',
    height: '200px' 
  }}>
    <div>
      <div>🔄 正在加载组件...</div>
      <div style={{ fontSize: '12px', color: '#666' }}>
        首次加载可能需要几秒钟
      </div>
    </div>
  </div>
);

// 🔸 带进度的加载提示
const ProgressLoading = () => {
  const [dots, setDots] = useState('');
  
  useEffect(() => {
    const timer = setInterval(() => {
      setDots(prev => prev.length >= 3 ? '' : prev + '.');
    }, 500);
    return () => clearInterval(timer);
  }, []);

  return <div>加载中{dots}</div>;
};
```

### 3.4 嵌套Suspense的使用


```jsx
function App() {
  return (
    <div>
      {/* 🔸 外层Suspense：整个页面级别 */}
      <Suspense fallback={<div>页面加载中...</div>}>
        <Header />
        
        {/* 🔸 内层Suspense：具体功能模块 */}
        <Suspense fallback={<div>用户模块加载中...</div>}>
          <UserModule />
        </Suspense>
        
        <Suspense fallback={<div>订单模块加载中...</div>}>
          <OrderModule />
        </Suspense>
      </Suspense>
    </div>
  );
}
```

**🔸 嵌套的好处**
- **细粒度控制**：不同模块显示不同的加载提示
- **更好体验**：用户知道具体哪个部分在加载
- **容错能力**：一个模块失败不影响其他模块

---

## 4. 📦 动态import语法详解


### 4.1 什么是动态import


**🔸 生活类比**
动态import就像**网购** - 不是把商店里所有商品都买回家，而是**需要什么下单什么**。

```javascript
// ❌ 静态import：启动时就要导入
import UserService from './services/UserService';

// ✅ 动态import：需要时才导入
const UserService = await import('./services/UserService');
```

### 4.2 基础语法使用


```javascript
// 🔸 基本语法
import('./module.js')
  .then(module => {
    // 使用导入的模块
    console.log(module.default);
  })
  .catch(error => {
    console.error('导入失败:', error);
  });

// 🔸 async/await语法
async function loadModule() {
  try {
    const module = await import('./module.js');
    return module.default;
  } catch (error) {
    console.error('导入失败:', error);
  }
}
```

### 4.3 在React中的实际应用


```jsx
// 🔸 条件加载工具组件
function ConditionalTool({ showAdvanced }) {
  const [AdvancedTool, setAdvancedTool] = useState(null);

  useEffect(() => {
    if (showAdvanced && !AdvancedTool) {
      // 只有需要高级工具时才加载
      import('./AdvancedTool')
        .then(module => setAdvancedTool(() => module.default))
        .catch(error => console.error('加载失败:', error));
    }
  }, [showAdvanced, AdvancedTool]);

  return (
    <div>
      <BasicTool />
      {AdvancedTool && <AdvancedTool />}
    </div>
  );
}
```

### 4.4 动态加载非组件资源


```javascript
// 🔸 动态加载工具库
async function loadUtils() {
  const { formatDate, validateEmail } = await import('./utils');
  return { formatDate, validateEmail };
}

// 🔸 动态加载配置文件
async function loadConfig(env) {
  const config = await import(`./config/${env}.js`);
  return config.default;
}

// 🔸 使用示例
button.onclick = async () => {
  const utils = await loadUtils();
  const formattedDate = utils.formatDate(new Date());
  console.log(formattedDate);
};
```

---

## 5. 🛣️ 路由级代码分割


### 5.1 什么是路由级分割


**🔸 简单理解**
路由级分割就像**分楼层购物** - 你去1楼买衣服，就不需要把2楼的家电、3楼的图书都搬下来。

```
网站结构：
/home     → 首页组件 (只在访问首页时加载)
/products → 商品页组件 (只在访问商品页时加载)  
/profile  → 用户页组件 (只在访问用户页时加载)
/admin    → 管理页组件 (只有管理员访问时加载)
```

### 5.2 React Router + 懒加载


```jsx
import React, { Suspense } from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';

// 🔸 懒加载各个页面组件
const Home = React.lazy(() => import('./pages/Home'));
const Products = React.lazy(() => import('./pages/Products'));
const Profile = React.lazy(() => import('./pages/Profile'));
const Admin = React.lazy(() => import('./pages/Admin'));

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/">首页</Link>
        <Link to="/products">商品</Link>
        <Link to="/profile">个人中心</Link>
      </nav>

      {/* 🔸 所有路由用一个Suspense包裹 */}
      <Suspense fallback={<div>页面加载中...</div>}>
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/products" element={<Products />} />
          <Route path="/profile" element={<Profile />} />
          <Route path="/admin" element={<Admin />} />
        </Routes>
      </Suspense>
    </BrowserRouter>
  );
}
```

### 5.3 更智能的路由分割


```jsx
// 🔸 按功能模块分组的路由
const PublicRoutes = React.lazy(() => import('./routes/PublicRoutes'));
const UserRoutes = React.lazy(() => import('./routes/UserRoutes'));
const AdminRoutes = React.lazy(() => import('./routes/AdminRoutes'));

function App() {
  const { user } = useAuth();

  return (
    <BrowserRouter>
      <Suspense fallback={<PageLoading />}>
        <Routes>
          {/* 公共路由：任何人都可以访问 */}
          <Route path="/*" element={<PublicRoutes />} />
          
          {/* 用户路由：登录后才加载 */}
          {user && (
            <Route path="/user/*" element={<UserRoutes />} />
          )}
          
          {/* 管理员路由：只有管理员才加载 */}
          {user?.role === 'admin' && (
            <Route path="/admin/*" element={<AdminRoutes />} />
          )}
        </Routes>
      </Suspense>
    </BrowserRouter>
  );
}
```

### 5.4 路由级分割的好处


```
实际效果对比：

❌ 不分割路由：
- 首次访问：下载所有页面代码 (500KB)
- 加载时间：3-4秒
- 用户体验：等待时间长

✅ 分割路由：
- 首次访问：只下载首页代码 (80KB)  
- 加载时间：0.5-1秒
- 切换页面：按需加载，体验流畅
```

---

## 6. 🧩 组件级代码分割


### 6.1 什么是组件级分割


**🔸 生活例子**
组件级分割就像**点菜** - 不是把菜单上所有菜都做好端上来，而是**点什么菜做什么菜**。

### 6.2 功能组件的懒加载


```jsx
function Dashboard() {
  const [activeTab, setActiveTab] = useState('overview');

  // 🔸 只有需要时才加载的重型组件
  const ChartComponent = React.lazy(() => import('./components/Chart'));
  const DataTable = React.lazy(() => import('./components/DataTable'));
  const ReportGenerator = React.lazy(() => import('./components/ReportGenerator'));

  return (
    <div>
      <div className="tabs">
        <button onClick={() => setActiveTab('overview')}>概览</button>
        <button onClick={() => setActiveTab('charts')}>图表</button>
        <button onClick={() => setActiveTab('data')}>数据</button>
        <button onClick={() => setActiveTab('reports')}>报告</button>
      </div>

      <div className="tab-content">
        {activeTab === 'overview' && <div>概览内容</div>}
        
        {activeTab === 'charts' && (
          <Suspense fallback={<div>图表加载中...</div>}>
            <ChartComponent />
          </Suspense>
        )}
        
        {activeTab === 'data' && (
          <Suspense fallback={<div>数据表格加载中...</div>}>
            <DataTable />
          </Suspense>
        )}
        
        {activeTab === 'reports' && (
          <Suspense fallback={<div>报告工具加载中...</div>}>
            <ReportGenerator />
          </Suspense>
        )}
      </div>
    </div>
  );
}
```

### 6.3 条件渲染的懒加载


```jsx
function UserProfile({ user }) {
  const [showAdvanced, setShowAdvanced] = useState(false);

  // 🔸 高级功能组件，只有VIP用户才需要
  const AdvancedFeatures = React.lazy(() => 
    import('./components/AdvancedFeatures')
  );

  // 🔸 管理面板，只有管理员才需要
  const AdminPanel = React.lazy(() => 
    import('./components/AdminPanel')
  );

  return (
    <div>
      <h1>用户资料</h1>
      <BasicInfo user={user} />

      {/* 🔸 VIP用户才显示高级功能 */}
      {user.isVip && (
        <Suspense fallback={<div>加载高级功能...</div>}>
          <AdvancedFeatures />
        </Suspense>
      )}

      {/* 🔸 管理员才显示管理面板 */}
      {user.role === 'admin' && (
        <Suspense fallback={<div>加载管理面板...</div>}>
          <AdminPanel />
        </Suspense>
      )}
    </div>
  );
}
```

### 6.4 第三方库的懒加载


```jsx
function DocumentEditor() {
  const [showEditor, setShowEditor] = useState(false);

  // 🔸 富文本编辑器很大，按需加载
  const RichEditor = React.lazy(async () => {
    // 动态导入第三方库
    const [ReactQuill, QuillModule] = await Promise.all([
      import('react-quill'),
      import('quill')
    ]);
    
    // 返回包装后的组件
    return {
      default: function WrappedEditor(props) {
        return <ReactQuill.default {...props} />;
      }
    };
  });

  return (
    <div>
      <button onClick={() => setShowEditor(true)}>
        打开编辑器
      </button>

      {showEditor && (
        <Suspense fallback={<div>编辑器加载中，请稍候...</div>}>
          <RichEditor />
        </Suspense>
      )}
    </div>
  );
}
```

---

## 7. 🛡️ 加载失败处理与预加载策略


### 7.1 加载失败的处理


**🔸 为什么会加载失败**
- 网络连接问题
- 服务器文件缺失  
- 用户中途取消加载
- 代码打包错误

```jsx
// 🔸 基础错误处理
class LazyLoadErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error) {
    return { hasError: true };
  }

  componentDidCatch(error, errorInfo) {
    console.error('懒加载失败:', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return (
        <div>
          <h3>😔 加载失败</h3>
          <p>组件加载出现问题，请检查网络连接</p>
          <button onClick={() => window.location.reload()}>
            刷新页面
          </button>
        </div>
      );
    }

    return this.props.children;
  }
}

// 使用方式
function App() {
  return (
    <LazyLoadErrorBoundary>
      <Suspense fallback={<div>加载中...</div>}>
        <LazyComponent />
      </Suspense>
    </LazyLoadErrorBoundary>
  );
}
```

### 7.2 重试机制


```jsx
// 🔸 带重试的懒加载Hook
function useLazyWithRetry(importFunc, retries = 3) {
  const [component, setComponent] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  const loadComponent = useCallback(async (attemptCount = 0) => {
    try {
      setLoading(true);
      setError(null);
      
      const module = await importFunc();
      setComponent(() => module.default);
    } catch (err) {
      if (attemptCount < retries) {
        console.log(`加载失败，正在重试... (${attemptCount + 1}/${retries})`);
        setTimeout(() => loadComponent(attemptCount + 1), 1000);
      } else {
        setError(err);
      }
    } finally {
      setLoading(false);
    }
  }, [importFunc, retries]);

  return { component, loading, error, loadComponent };
}

// 使用示例
function LazyComponentWrapper() {
  const { component: LazyComponent, loading, error, loadComponent } = 
    useLazyWithRetry(() => import('./HeavyComponent'));

  useEffect(() => {
    loadComponent();
  }, [loadComponent]);

  if (loading) return <div>加载中...</div>;
  if (error) return (
    <div>
      <p>加载失败: {error.message}</p>
      <button onClick={() => loadComponent()}>重试</button>
    </div>
  );
  if (LazyComponent) return <LazyComponent />;
  return null;
}
```

### 7.3 预加载策略


```jsx
// 🔸 鼠标悬停预加载
function NavigationButton({ to, children }) {
  const [isPreloaded, setIsPreloaded] = useState(false);

  const handleMouseEnter = () => {
    if (!isPreloaded) {
      // 预加载页面组件
      import(`./pages/${to}`)
        .then(() => setIsPreloaded(true))
        .catch(console.error);
    }
  };

  return (
    <Link 
      to={to} 
      onMouseEnter={handleMouseEnter}
      className="nav-button"
    >
      {children}
      {isPreloaded && <span className="preloaded">✓</span>}
    </Link>
  );
}

// 🔸 空闲时间预加载
function useIdlePreload(importFunc) {
  useEffect(() => {
    // 检查浏览器是否支持requestIdleCallback
    if ('requestIdleCallback' in window) {
      const timeoutId = requestIdleCallback(() => {
        importFunc().catch(console.error);
      });
      
      return () => cancelIdleCallback(timeoutId);
    } else {
      // 降级方案：延迟加载
      const timeoutId = setTimeout(() => {
        importFunc().catch(console.error);
      }, 2000);
      
      return () => clearTimeout(timeoutId);
    }
  }, [importFunc]);
}

// 使用示例
function App() {
  // 在空闲时预加载用户中心页面
  useIdlePreload(() => import('./pages/UserCenter'));

  return (
    <div>
      <NavigationButton to="home">首页</NavigationButton>
      <NavigationButton to="products">商品</NavigationButton>
      <NavigationButton to="user-center">用户中心</NavigationButton>
    </div>
  );
}
```

---

## 8. 📜 虚拟化长列表优化


### 8.1 什么是虚拟化


**🔸 生活比喻**
虚拟化长列表就像**看书** - 你不会把整本书的所有页面都摊开，而是**只看当前这一页**，需要时再翻页。

```
传统长列表问题：
显示10000条数据 → 创建10000个DOM元素 → 页面卡顿

虚拟化解决方案：
显示10000条数据 → 只创建可视区域的DOM元素 → 页面流畅
```

### 8.2 简单虚拟化实现


```jsx
// 🔸 基础虚拟列表组件
function VirtualList({ items, itemHeight, containerHeight }) {
  const [scrollTop, setScrollTop] = useState(0);

  // 计算可视区域
  const startIndex = Math.floor(scrollTop / itemHeight);
  const endIndex = Math.min(
    startIndex + Math.ceil(containerHeight / itemHeight) + 1,
    items.length
  );
  
  // 只渲染可视区域的项目
  const visibleItems = items.slice(startIndex, endIndex);

  return (
    <div 
      style={{ height: containerHeight, overflow: 'auto' }}
      onScroll={e => setScrollTop(e.target.scrollTop)}
    >
      {/* 占位元素，撑开总高度 */}
      <div style={{ height: items.length * itemHeight, position: 'relative' }}>
        {visibleItems.map((item, index) => (
          <div
            key={startIndex + index}
            style={{
              position: 'absolute',
              top: (startIndex + index) * itemHeight,
              height: itemHeight,
              width: '100%'
            }}
          >
            {item.name} - {item.description}
          </div>
        ))}
      </div>
    </div>
  );
}

// 使用示例
function App() {
  const [items] = useState(() => 
    Array.from({ length: 10000 }, (_, i) => ({
      id: i,
      name: `项目 ${i}`,
      description: `这是第 ${i} 个项目的描述`
    }))
  );

  return (
    <VirtualList 
      items={items}
      itemHeight={50}
      containerHeight={400}
    />
  );
}
```

### 8.3 使用react-window库


```jsx
import { FixedSizeList as List } from 'react-window';

// 🔸 列表项组件
const ListItem = ({ index, style, data }) => (
  <div style={style}>
    <div className="item">
      <h4>{data[index].name}</h4>
      <p>{data[index].description}</p>
    </div>
  </div>
);

// 🔸 虚拟化列表
function VirtualizedList({ items }) {
  return (
    <List
      height={400}        // 容器高度
      itemCount={items.length}  // 总项目数
      itemSize={80}       // 每项高度
      itemData={items}    // 数据
      width="100%"
    >
      {ListItem}
    </List>
  );
}
```

### 8.4 虚拟化的适用场景


**✅ 适合使用虚拟化**
- 列表项目超过100个
- 每个列表项包含复杂内容
- 用户需要滚动浏览大量数据
- 移动设备上的长列表

**❌ 不适合使用虚拟化**
- 列表项目很少（<50个）
- 列表项高度差异很大
- 需要全局搜索功能
- 列表内容经常变化

---

## 9. 📦 打包体积优化实战


### 9.1 分析打包体积


```bash
# 🔸 安装分析工具
npm install --save-dev webpack-bundle-analyzer

# 🔸 生成分析报告
npm run build
npx webpack-bundle-analyzer build/static/js/*.js
```

**🔸 体积分析要点**
- 找出最大的文件块
- 识别重复导入的库
- 发现可以懒加载的模块
- 检查未使用的代码

### 9.2 第三方库优化


```jsx
// ❌ 导入整个库
import _ from 'lodash';
import moment from 'moment';

// ✅ 按需导入
import debounce from 'lodash/debounce';
import format from 'date-fns/format';

// ✅ 动态导入大型库
async function useCharts() {
  const { Chart } = await import('chart.js');
  return Chart;
}

// ✅ 替换为更小的库
// moment.js (67KB) → date-fns (13KB)
// lodash (70KB) → 原生JS或小工具函数
```

### 9.3 Tree Shaking优化


```javascript
// 🔸 确保模块使用ES6导出
// utils.js
export const formatDate = (date) => { /* ... */ };
export const validateEmail = (email) => { /* ... */ };
export const debounce = (func, wait) => { /* ... */ };

// 🔸 只导入需要的函数
import { formatDate, validateEmail } from './utils';
// 不会打包debounce函数

// package.json配置
{
  "sideEffects": false  // 告诉webpack可以安全地进行tree shaking
}
```

### 9.4 首屏加载优化


```jsx
// 🔸 关键资源优先加载
function App() {
  return (
    <div>
      {/* 首屏内容 */}
      <Header />
      <Hero />
      
      {/* 非首屏内容懒加载 */}
      <Suspense fallback={<div>加载中...</div>}>
        <Features />
        <Testimonials />
        <Footer />
      </Suspense>
    </div>
  );
}

// 🔸 预加载关键资源
// index.html
<link rel="preload" href="/fonts/main.woff2" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="/api/critical-data" as="fetch" crossorigin>
```

**🔸 首屏优化检查清单**

| 优化项 | 目标 | 检查方法 |
|--------|------|----------|
| **首屏资源** | `< 100KB` | `检查network面板` |
| **加载时间** | `< 1秒` | `Lighthouse评分` |
| **关键路径** | `最小化CSS/JS` | `覆盖率工具` |
| **图片优化** | `WebP格式` | `图片格式检查` |

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


```
🔸 代码分割本质：按需加载，避免一次性下载所有代码
🔸 React.lazy用法：创建懒加载组件，配合Suspense使用  
🔸 Suspense作用：为懒加载组件提供加载状态显示
🔸 动态import：运行时导入模块，支持条件加载
🔸 路由级分割：按页面分割，用户访问时才加载对应页面
🔸 组件级分割：按功能分割，需要时才加载对应组件
```

### 10.2 实用记忆要点


**🔹 什么时候使用代码分割**
```
✅ 适用场景：
- 页面很多，用户不会全部访问
- 组件很大，首屏不需要显示  
- 第三方库很重，按需使用
- 管理功能，只有特定用户需要

❌ 不适用场景：  
- 应用很小，分割后反而复杂
- 组件经常使用，分割后体验差
- 网络很好，加载速度不是问题
```

**🔹 代码分割最佳实践**
```
路由级分割：
- 按页面分割，粒度适中
- 公共组件不要分割
- 权限页面单独分割

组件级分割：
- 重型组件才分割
- Tab切换内容适合分割  
- 弹窗组件适合分割

加载体验：
- 提供有意义的加载提示
- 添加加载失败处理
- 考虑预加载策略
```

### 10.3 性能优化效果


**🔸 优化前后对比**

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| **首屏加载** | `3-5秒` | `0.5-1秒` | `⚡ 5倍快` |
| **包体积** | `500KB` | `80KB` | `📦 6倍小` |
| **用户体验** | `等待时间长` | `即开即用` | `🚀 显著提升` |
| **加载成功率** | `95%` | `99%` | `📈 更稳定` |

### 10.4 常见问题解决


**🔸 Q: 懒加载组件报错怎么办？**
```jsx
// A: 使用ErrorBoundary + 重试机制
<ErrorBoundary fallback={<RetryButton />}>
  <Suspense fallback={<Loading />}>
    <LazyComponent />
  </Suspense>
</ErrorBoundary>
```

**🔸 Q: 如何判断哪些组件需要分割？**
```
1. 分析工具：webpack-bundle-analyzer
2. 使用频率：低频组件优先分割
3. 组件大小：超过50KB的组件考虑分割
4. 加载时机：非首屏组件适合分割
```

**🔸 Q: 分割过度会有什么问题？**
```
- 请求数量增加，可能影响性能
- 用户体验碎片化，频繁看到加载状态
- 代码维护复杂，调试困难
- 缓存效率降低
```

**核心记忆口诀**：
- 代码分割像点菜，需要什么装什么
- React.lazy配Suspense，懒加载就这么简单
- 路由组件按页分，功能模块看情况
- 错误处理要做好，用户体验最重要