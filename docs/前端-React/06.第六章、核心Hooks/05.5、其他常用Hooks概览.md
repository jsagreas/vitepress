---
title: 5、其他常用Hooks概览
---
## 📚 目录

1. [useContext - 跨组件传递数据](#1-useContext-跨组件传递数据)
2. [useReducer - 复杂状态管理](#2-useReducer-复杂状态管理)
3. [useLayoutEffect - 同步执行的副作用](#3-useLayoutEffect-同步执行的副作用)
4. [自定义Hook - 逻辑复用神器](#4-自定义Hook-逻辑复用神器)
5. [Hook使用规则详解](#5-Hook使用规则详解)
6. [常见Hook组合模式](#6-常见Hook组合模式)
7. [Hook最佳实践](#7-Hook最佳实践)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔗 useContext - 跨组件传递数据


### 1.1 什么是useContext


> 💡 **通俗理解**：想象你在一个大家庭里，爷爷想给孙子传话，不用一层层传递，而是通过"家庭群聊"直接告诉孙子。useContext就是React的"群聊功能"。

**解决的问题**：
- ❌ **层层传递**：父组件 → 子组件 → 孙组件，props一层层往下传
- ✅ **直接获取**：任何组件都能直接获取需要的数据

### 1.2 基本使用方式


```jsx
// 1. 创建Context（创建"群聊"）
const UserContext = React.createContext();

// 2. 提供数据（爷爷发消息到群聊）
function App() {
  const user = { name: "张三", age: 25 };
  
  return (
    <UserContext.Provider value={user}>
      <ParentComponent />
    </UserContext.Provider>
  );
}

// 3. 使用数据（孙子直接看群聊消息）
function GrandChild() {
  const user = useContext(UserContext);
  
  return <div>用户：{user.name}</div>;
}
```

### 1.3 实际应用场景


**主题切换示例**：
```jsx
const ThemeContext = React.createContext();

// 主题提供者
function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light');
  
  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

// 任何组件都能切换主题
function Button() {
  const { theme, setTheme } = useContext(ThemeContext);
  
  return (
    <button 
      style={{ background: theme === 'dark' ? '#333' : '#fff' }}
      onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}
    >
      切换主题
    </button>
  );
}
```

---

## 2. 🔄 useReducer - 复杂状态管理


### 2.1 什么是useReducer


> 💡 **通俗理解**：useState像是一个简单的开关，只能开/关。useReducer像是一个复杂的遥控器，有很多按钮，每个按钮执行不同的操作。

**什么时候用useReducer**：
- ✅ 状态逻辑复杂（多个相关状态）
- ✅ 状态更新依赖之前的状态
- ✅ 需要统一管理多种操作

### 2.2 基本使用模式


```jsx
// 1. 定义reducer（遥控器的说明书）
function counterReducer(state, action) {
  switch (action.type) {
    case 'INCREMENT':
      return { count: state.count + 1 };
    case 'DECREMENT':
      return { count: state.count - 1 };
    case 'RESET':
      return { count: 0 };
    default:
      return state;
  }
}

// 2. 使用useReducer
function Counter() {
  const [state, dispatch] = useReducer(counterReducer, { count: 0 });
  
  return (
    <div>
      <p>计数：{state.count}</p>
      <button onClick={() => dispatch({ type: 'INCREMENT' })}>+1</button>
      <button onClick={() => dispatch({ type: 'DECREMENT' })}>-1</button>
      <button onClick={() => dispatch({ type: 'RESET' })}>重置</button>
    </div>
  );
}
```

### 2.3 复杂状态管理示例


```jsx
// 购物车状态管理
function cartReducer(state, action) {
  switch (action.type) {
    case 'ADD_ITEM':
      return {
        ...state,
        items: [...state.items, action.payload],
        total: state.total + action.payload.price
      };
    case 'REMOVE_ITEM':
      const newItems = state.items.filter(item => item.id !== action.payload);
      return {
        ...state,
        items: newItems,
        total: newItems.reduce((sum, item) => sum + item.price, 0)
      };
    default:
      return state;
  }
}

function ShoppingCart() {
  const [cart, dispatch] = useReducer(cartReducer, { 
    items: [], 
    total: 0 
  });
  
  const addItem = (item) => {
    dispatch({ type: 'ADD_ITEM', payload: item });
  };
  
  return (
    <div>
      <p>商品数量：{cart.items.length}</p>
      <p>总价：¥{cart.total}</p>
    </div>
  );
}
```

---

## 3. ⚡ useLayoutEffect - 同步执行的副作用


### 3.1 useLayoutEffect vs useEffect


> 💡 **通俗理解**：
> - **useEffect**：像是"异步快递"，页面先显示，效果后到达
> - **useLayoutEffect**：像是"同城速达"，页面和效果同时到达

```
页面渲染流程：
DOM更新 → useLayoutEffect执行 → 浏览器绘制 → useEffect执行
```

### 3.2 什么时候用useLayoutEffect


**使用场景**：
- ✅ 需要测量DOM尺寸
- ✅ 需要同步更新样式
- ✅ 避免视觉闪烁

```jsx
function MeasureComponent() {
  const [height, setHeight] = useState(0);
  const divRef = useRef();
  
  // 使用useLayoutEffect避免闪烁
  useLayoutEffect(() => {
    setHeight(divRef.current.offsetHeight);
  }, []);
  
  return (
    <div>
      <div ref={divRef}>需要测量的内容</div>
      <p>高度：{height}px</p>
    </div>
  );
}
```

### 3.3 性能考虑


> ⚠️ **注意**：useLayoutEffect是同步执行的，会阻塞浏览器绘制，所以要谨慎使用，大部分情况下用useEffect就够了。

---

## 4. 🛠️ 自定义Hook - 逻辑复用神器


### 4.1 什么是自定义Hook


> 💡 **通俗理解**：自定义Hook就像是把常用的功能封装成一个"工具包"，需要的时候直接拿来用，不用重复写代码。

**自定义Hook的特点**：
- ✅ 必须以`use`开头
- ✅ 可以调用其他Hook
- ✅ 用于复用状态逻辑

### 4.2 简单的自定义Hook示例


```jsx
// 自定义Hook：管理本地存储
function useLocalStorage(key, initialValue) {
  const [value, setValue] = useState(() => {
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch (error) {
      return initialValue;
    }
  });
  
  const setStoredValue = (newValue) => {
    setValue(newValue);
    window.localStorage.setItem(key, JSON.stringify(newValue));
  };
  
  return [value, setStoredValue];
}

// 使用自定义Hook
function UserProfile() {
  const [name, setName] = useLocalStorage('username', '');
  
  return (
    <input 
      value={name}
      onChange={(e) => setName(e.target.value)}
      placeholder="输入用户名"
    />
  );
}
```

### 4.3 实用的自定义Hook


```jsx
// 网络请求Hook
function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);
  
  useEffect(() => {
    fetch(url)
      .then(response => response.json())
      .then(data => {
        setData(data);
        setLoading(false);
      })
      .catch(error => {
        setError(error);
        setLoading(false);
      });
  }, [url]);
  
  return { data, loading, error };
}

// 使用方式
function UserList() {
  const { data: users, loading, error } = useFetch('/api/users');
  
  if (loading) return <div>加载中...</div>;
  if (error) return <div>出错了</div>;
  
  return (
    <ul>
      {users.map(user => <li key={user.id}>{user.name}</li>)}
    </ul>
  );
}
```

---

## 5. 📋 Hook使用规则详解


### 5.1 Hook的两个重要规则


> ⚠️ **重要**：违反这些规则，React应用会出问题！

**规则1：只在顶层调用Hook**
```jsx
// ❌ 错误：在条件语句中使用Hook
function BadComponent() {
  if (someCondition) {
    const [count, setCount] = useState(0); // 违反规则！
  }
}

// ✅ 正确：始终在顶层使用Hook
function GoodComponent() {
  const [count, setCount] = useState(0);
  
  if (someCondition) {
    // 在这里使用count
  }
}
```

**规则2：只在React函数中调用Hook**
```jsx
// ❌ 错误：在普通函数中使用Hook
function regularFunction() {
  const [count, setCount] = useState(0); // 违反规则！
}

// ✅ 正确：在React组件中使用Hook
function ReactComponent() {
  const [count, setCount] = useState(0); // 正确
}

// ✅ 正确：在自定义Hook中使用Hook
function useCustomHook() {
  const [count, setCount] = useState(0); // 正确
}
```

### 5.2 为什么有这些规则


> 💡 **原因**：React依靠Hook的调用顺序来追踪状态，如果顺序变了，React就乱套了。

```
第一次渲染：     第二次渲染：
Hook 1: useState  Hook 1: useState  ✅ 顺序一致
Hook 2: useEffect Hook 2: useEffect ✅ 顺序一致

如果条件性调用：
Hook 1: useState  Hook 1: useState  
Hook 2: useEffect [跳过了]         ❌ 顺序乱了！
Hook 3: useState  Hook 2: useEffect
```

---

## 6. 🎯 常见Hook组合模式


### 6.1 数据获取 + 加载状态


```jsx
function useDataFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);
  
  const fetchData = useCallback(async () => {
    setLoading(true);
    setError(null);
    
    try {
      const response = await fetch(url);
      const result = await response.json();
      setData(result);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  }, [url]);
  
  useEffect(() => {
    fetchData();
  }, [fetchData]);
  
  return { data, loading, error, refetch: fetchData };
}
```

### 6.2 表单处理组合


```jsx
function useForm(initialValues) {
  const [values, setValues] = useState(initialValues);
  const [errors, setErrors] = useState({});
  
  const handleChange = (name, value) => {
    setValues(prev => ({ ...prev, [name]: value }));
    // 清除对应字段的错误
    if (errors[name]) {
      setErrors(prev => ({ ...prev, [name]: '' }));
    }
  };
  
  const validate = (rules) => {
    const newErrors = {};
    // 验证逻辑...
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };
  
  return { values, errors, handleChange, validate };
}
```

---

## 7. 🎖️ Hook最佳实践


### 7.1 命名规范


```jsx
// ✅ 好的命名
const [isLoading, setIsLoading] = useState(false);
const [userList, setUserList] = useState([]);
const [hasError, setHasError] = useState(false);

// ❌ 不好的命名
const [flag, setFlag] = useState(false);
const [data, setData] = useState([]);
const [x, setX] = useState(false);
```

### 7.2 依赖项管理


> 💡 **原则**：useEffect的依赖项要诚实，用到什么就写什么

```jsx
// ✅ 正确的依赖项
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);
  
  useEffect(() => {
    fetchUser(userId).then(setUser);
  }, [userId]); // userId变化时重新获取
  
  return <div>{user?.name}</div>;
}

// ❌ 错误的依赖项
useEffect(() => {
  fetchUser(userId).then(setUser);
}, []); // 缺少userId依赖，可能导致数据不更新
```

### 7.3 性能优化技巧


```jsx
// 使用useCallback优化函数
const handleClick = useCallback(() => {
  // 处理点击事件
}, [dependency]);

// 使用useMemo优化计算
const expensiveValue = useMemo(() => {
  return expensiveCalculation(data);
}, [data]);

// 合理拆分状态
// ✅ 好的做法：相关状态放一起
const [user, setUser] = useState({ name: '', email: '' });

// ❌ 不好的做法：过度拆分
const [userName, setUserName] = useState('');
const [userEmail, setUserEmail] = useState('');
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 useContext：跨组件传递数据的"群聊功能"
🔸 useReducer：管理复杂状态的"遥控器"  
🔸 useLayoutEffect：同步执行的副作用，避免闪烁
🔸 自定义Hook：复用逻辑的"工具包"
🔸 Hook规则：只在顶层调用，只在React函数中使用
🔸 组合模式：多个Hook配合使用解决复杂问题
```

### 8.2 学习重点理解


**🔹 选择合适的Hook**
```
简单状态 → useState
复杂状态 → useReducer  
跨组件传值 → useContext
逻辑复用 → 自定义Hook
同步DOM操作 → useLayoutEffect
```

**🔹 实际开发建议**
- ✅ 优先使用useState，复杂了再考虑useReducer
- ✅ 多个组件需要相同数据时使用useContext
- ✅ 重复的逻辑封装成自定义Hook
- ✅ 一般情况用useEffect，特殊情况才用useLayoutEffect

### 8.3 常见使用场景


| Hook类型 | **使用场景** | **实际例子** |
|----------|-------------|-------------|
| 🔗 **useContext** | `跨组件数据共享` | `主题切换、用户信息、语言设置` |
| 🔄 **useReducer** | `复杂状态管理` | `购物车、表单验证、游戏状态` |
| ⚡ **useLayoutEffect** | `DOM同步操作` | `测量元素大小、动画效果` |
| 🛠️ **自定义Hook** | `逻辑复用` | `网络请求、本地存储、表单处理` |

**核心记忆要点**：
- Hook让函数组件拥有类组件的能力
- 每个Hook都有特定的使用场景，选对工具很重要
- 遵守Hook规则，避免在条件语句中使用
- 自定义Hook是复用逻辑的最佳方式
- 组合使用多个Hook可以解决复杂问题