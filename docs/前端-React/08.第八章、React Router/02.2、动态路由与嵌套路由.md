---
title: 2、动态路由与嵌套路由
---
## 📚 目录

1. [动态路由基础](#1-动态路由基础)
2. [路由参数获取](#2-路由参数获取)
3. [查询参数处理](#3-查询参数处理)
4. [嵌套路由实现](#4-嵌套路由实现)
5. [路由重定向与404](#5-路由重定向与404)
6. [路由懒加载](#6-路由懒加载)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 动态路由基础


### 1.1 什么是动态路由


**通俗理解**：动态路由就像是一个**模板**，可以适应不同的具体内容。

```
想象一下：
固定路由：/users → 显示用户列表页面
动态路由：/users/123 → 显示ID为123的用户详情
动态路由：/users/456 → 显示ID为456的用户详情

就像一个模具，可以制作不同编号的产品
```

### 1.2 动态路由语法


**核心概念**：用冒号`:` 标记可变的部分

```javascript
// 基础语法
"/users/:id"           // :id 是动态参数
"/products/:category"  // :category 是分类参数
"/posts/:id/comments"  // 可以有多个固定部分

// 实际匹配示例
"/users/123"     ✅ 匹配 /users/:id
"/users/abc"     ✅ 匹配 /users/:id  
"/users"         ❌ 不匹配（缺少参数）
"/users/123/456" ❌ 不匹配（多余部分）
```

### 1.3 基础路由配置


```javascript
import { BrowserRouter, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* 静态路由 */}
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        
        {/* 动态路由 */}
        <Route path="/users/:id" element={<UserDetail />} />
        <Route path="/products/:category" element={<ProductList />} />
      </Routes>
    </BrowserRouter>
  );
}
```

---

## 2. 🔍 路由参数获取


### 2.1 useParams - 获取路径参数


**作用说明**：`useParams` 是React Router提供的钩子，用来获取URL中的动态参数。

```
URL: /users/123
路由: /users/:id
useParams() 返回: { id: "123" }

就像从信封上读取收件人地址
```

### 2.2 useParams 基础用法


```javascript
import { useParams } from 'react-router-dom';

function UserDetail() {
  // 获取URL中的动态参数
  const params = useParams();
  
  return (
    <div>
      <h1>用户详情</h1>
      <p>用户ID: {params.id}</p>
    </div>
  );
}

// 解构写法（更常用）
function UserDetail() {
  const { id } = useParams();
  
  return (
    <div>
      <h1>用户详情</h1>
      <p>用户ID: {id}</p>
    </div>
  );
}
```

### 2.3 多个参数获取


```javascript
// 路由配置：/posts/:postId/comments/:commentId
function CommentDetail() {
  const { postId, commentId } = useParams();
  
  return (
    <div>
      <h1>评论详情</h1>
      <p>文章ID: {postId}</p>
      <p>评论ID: {commentId}</p>
    </div>
  );
}
```

### 2.4 useLocation - 获取完整位置信息


**作用说明**：`useLocation` 获取当前页面的完整位置信息，包括路径、查询参数等。

```javascript
import { useLocation } from 'react-router-dom';

function CurrentPage() {
  const location = useLocation();
  
  console.log(location);
  // 输出类似：
  // {
  //   pathname: '/users/123',
  //   search: '?tab=info&sort=name',
  //   hash: '#section1',
  //   state: null,
  //   key: 'default'
  // }
  
  return <div>当前路径: {location.pathname}</div>;
}
```

---

## 3. 🔗 查询参数处理


### 3.1 什么是查询参数


**通俗理解**：查询参数就是URL中问号`?`后面的部分，用来传递额外信息。

```
完整URL结构：
https://example.com/users/123?tab=info&sort=name#section1
                    ^^^  ^^^  ^^^^^^^^^^^^^^^^^ ^^^^^^^^^^
                    |    |    |                 |
                   域名  路径  查询参数          锚点

查询参数作用：类似于给页面传递"配置选项"
```

### 3.2 useSearchParams - 处理查询参数


**作用说明**：`useSearchParams` 专门用来读取和修改URL中的查询参数。

```javascript
import { useSearchParams } from 'react-router-dom';

function ProductList() {
  const [searchParams, setSearchParams] = useSearchParams();
  
  // 读取查询参数
  const category = searchParams.get('category'); // 获取 ?category=electronics
  const sort = searchParams.get('sort');         // 获取 ?sort=price
  
  // 修改查询参数
  const handleFilterChange = (newCategory) => {
    setSearchParams({ category: newCategory, sort: 'name' });
    // 会更新URL为：/products?category=books&sort=name
  };
  
  return (
    <div>
      <h1>商品列表</h1>
      <p>当前分类: {category || '全部'}</p>
      <p>排序方式: {sort || '默认'}</p>
      
      <button onClick={() => handleFilterChange('electronics')}>
        电子产品
      </button>
      <button onClick={() => handleFilterChange('books')}>
        图书
      </button>
    </div>
  );
}
```

### 3.3 查询参数的实际应用


```javascript
// 搜索页面示例
function SearchPage() {
  const [searchParams, setSearchParams] = useSearchParams();
  
  const keyword = searchParams.get('q') || '';
  const page = searchParams.get('page') || '1';
  
  const handleSearch = (newKeyword) => {
    setSearchParams({ q: newKeyword, page: '1' });
  };
  
  const handlePageChange = (newPage) => {
    setSearchParams({ q: keyword, page: newPage });
  };
  
  return (
    <div>
      <input 
        value={keyword}
        onChange={(e) => handleSearch(e.target.value)}
        placeholder="搜索..."
      />
      <p>搜索词: {keyword}</p>
      <p>当前页: {page}</p>
    </div>
  );
}
```

---

## 4. 🏗️ 嵌套路由实现


### 4.1 什么是嵌套路由


**通俗理解**：嵌套路由就像是**套娃**，一个页面里面还可以有子页面。

```
页面结构示例：
Dashboard（仪表板主页面）
├── Overview（概览子页面）
├── Analytics（分析子页面）
└── Settings（设置子页面）

URL对应关系：
/dashboard          → Dashboard + Overview
/dashboard/overview → Dashboard + Overview  
/dashboard/analytics → Dashboard + Analytics
/dashboard/settings  → Dashboard + Settings
```

### 4.2 嵌套路由配置


```javascript
import { Outlet } from 'react-router-dom';

// 父级组件 - Dashboard
function Dashboard() {
  return (
    <div>
      <h1>仪表板</h1>
      <nav>
        <Link to="/dashboard">概览</Link>
        <Link to="/dashboard/analytics">分析</Link>
        <Link to="/dashboard/settings">设置</Link>
      </nav>
      
      {/* Outlet：子路由内容显示的地方 */}
      <Outlet />
    </div>
  );
}

// 路由配置
function App() {
  return (
    <Routes>
      <Route path="/dashboard" element={<Dashboard />}>
        {/* 嵌套子路由 */}
        <Route index element={<Overview />} />          {/* 默认子路由 */}
        <Route path="analytics" element={<Analytics />} />
        <Route path="settings" element={<Settings />} />
      </Route>
    </Routes>
  );
}
```

### 4.3 Outlet 的作用


**核心概念**：`Outlet` 就像是一个**插座**，子组件会"插"在这个位置显示。

```
Dashboard 组件布局：
┌─────────────────────────┐
│ 仪表板标题               │
├─────────────────────────┤
│ 导航菜单                │
├─────────────────────────┤
│ <Outlet />              │ ← 子组件显示区域
│ (这里显示子路由内容)      │
└─────────────────────────┘
```

### 4.4 嵌套路由的实际应用


```javascript
// 用户管理系统
function UserManagement() {
  return (
    <div className="user-management">
      <aside>
        <nav>
          <Link to="/users">用户列表</Link>
          <Link to="/users/create">新建用户</Link>
          <Link to="/users/roles">角色管理</Link>
        </nav>
      </aside>
      
      <main>
        <Outlet /> {/* 子页面内容显示区 */}
      </main>
    </div>
  );
}

// 路由配置
<Route path="/users" element={<UserManagement />}>
  <Route index element={<UserList />} />
  <Route path="create" element={<CreateUser />} />
  <Route path="roles" element={<RoleManagement />} />
  <Route path=":id" element={<UserDetail />} />
  <Route path=":id/edit" element={<EditUser />} />
</Route>
```

---

## 5. 🚦 路由重定向与404


### 5.1 路由重定向


**应用场景**：当用户访问某个路径时，自动跳转到另一个路径。

```javascript
import { Navigate } from 'react-router-dom';

function App() {
  return (
    <Routes>
      {/* 根路径重定向到首页 */}
      <Route path="/" element={<Navigate to="/home" replace />} />
      
      {/* 旧路径重定向到新路径 */}
      <Route path="/old-page" element={<Navigate to="/new-page" replace />} />
      
      <Route path="/home" element={<Home />} />
      <Route path="/new-page" element={<NewPage />} />
    </Routes>
  );
}
```

**`replace` 属性说明**：
- `replace={true}`：替换历史记录（用户点后退不会回到重定向前的页面）
- `replace={false}`：添加历史记录（默认行为）

### 5.2 条件重定向


```javascript
function ProtectedRoute({ children }) {
  const isLoggedIn = false; // 假设用户未登录
  
  if (!isLoggedIn) {
    return <Navigate to="/login" replace />;
  }
  
  return children;
}

// 使用方式
<Route 
  path="/profile" 
  element={
    <ProtectedRoute>
      <Profile />
    </ProtectedRoute>
  } 
/>
```

### 5.3 404页面处理


**核心概念**：当用户访问不存在的路径时，显示友好的错误页面。

```javascript
function NotFound() {
  return (
    <div>
      <h1>404 - 页面未找到</h1>
      <p>抱歉，您访问的页面不存在。</p>
      <Link to="/">返回首页</Link>
    </div>
  );
}

function App() {
  return (
    <Routes>
      <Route path="/" element={<Home />} />
      <Route path="/about" element={<About />} />
      <Route path="/users/:id" element={<UserDetail />} />
      
      {/* 捕获所有未匹配的路径 */}
      <Route path="*" element={<NotFound />} />
    </Routes>
  );
}
```

**路径匹配优先级**：
```
匹配顺序（从上到下）：
1. 精确匹配：/about
2. 动态匹配：/users/:id  
3. 通配符匹配：*（最后）
```

---

## 6. ⚡ 路由懒加载


### 6.1 什么是懒加载


**通俗理解**：懒加载就像是**按需点菜**，只有用户真正访问某个页面时，才去加载这个页面的代码。

```
传统加载方式：
用户打开网站 → 一次性下载所有页面代码 → 首次加载慢

懒加载方式：
用户打开网站 → 只下载首页代码 → 访问其他页面时再下载 → 首次加载快
```

### 6.2 React.lazy + Suspense 实现


```javascript
import { lazy, Suspense } from 'react';

// 懒加载组件
const Home = lazy(() => import('./pages/Home'));
const About = lazy(() => import('./pages/About'));
const UserDetail = lazy(() => import('./pages/UserDetail'));

// 加载中组件
function Loading() {
  return <div>页面加载中...</div>;
}

function App() {
  return (
    <BrowserRouter>
      <Suspense fallback={<Loading />}>
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/about" element={<About />} />
          <Route path="/users/:id" element={<UserDetail />} />
        </Routes>
      </Suspense>
    </BrowserRouter>
  );
}
```

### 6.3 懒加载的优势与注意事项


**✅ 优势**：
- **首页加载更快**：减少初始代码体积
- **节省流量**：只下载需要的代码
- **更好的用户体验**：避免长时间白屏

**⚠️ 注意事项**：
- **网络延迟**：页面切换时可能有短暂加载
- **错误处理**：需要处理加载失败的情况

```javascript
// 错误边界处理
const Home = lazy(() => 
  import('./pages/Home').catch(() => {
    return { default: () => <div>页面加载失败</div> };
  })
);
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 动态路由：用 :参数名 定义可变路径部分
🔸 useParams：获取URL中的动态参数
🔸 useSearchParams：处理查询参数（?key=value）
🔸 嵌套路由：父组件中用 <Outlet /> 显示子路由
🔸 路由重定向：用 <Navigate /> 实现页面跳转
🔸 404处理：用 path="*" 捕获未匹配路径
🔸 懒加载：用 React.lazy + Suspense 按需加载
```

### 7.2 关键理解要点


**🔹 动态路由的本质**
```
动态路由 = 路径模板 + 具体参数
/users/:id → 模板
/users/123 → 具体实例

就像填空题：/users/___，可以填入任何用户ID
```

**🔹 嵌套路由的结构**
```
页面布局 = 父组件布局 + 子路由内容
父组件：提供整体框架（导航、侧边栏等）
<Outlet />：子路由的显示区域
子路由：具体的页面内容
```

**🔹 路由参数的区别**
```
路径参数：/users/:id → 路径的一部分，必需的
查询参数：?page=1&sort=name → 可选的配置信息
状态参数：通过 state 传递，不显示在URL中
```

### 7.3 实际应用指导


**📱 常见应用场景**：
- **商品详情**：`/products/:id` 显示不同商品
- **用户资料**：`/users/:id` 显示不同用户信息  
- **文章页面**：`/posts/:slug` 显示不同文章
- **管理后台**：嵌套路由构建多级页面
- **搜索结果**：查询参数控制搜索条件

**🎯 选择建议**：
```
路径参数适用于：
- 资源ID（用户ID、商品ID）
- 必需的路径信息

查询参数适用于：
- 搜索关键词
- 分页信息
- 筛选条件
- 排序方式

嵌套路由适用于：
- 多层级页面结构
- 共享布局的页面组
- 管理后台系统
```

**💡 最佳实践**：
- **参数验证**：始终验证从URL获取的参数
- **默认值处理**：为可选参数提供默认值
- **错误处理**：处理参数无效的情况
- **SEO友好**：使用有意义的URL结构
- **性能优化**：合理使用懒加载减少首屏时间

**核心记忆**：
- 动态路由用冒号，参数获取用useParams
- 查询参数问号后，useSearchParams来处理
- 嵌套路由用Outlet，父子组件要分离
- 404用星号捕获，重定向Navigate组件