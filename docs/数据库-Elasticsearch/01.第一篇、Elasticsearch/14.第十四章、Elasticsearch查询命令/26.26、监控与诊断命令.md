---
title: 26、监控与诊断命令
---
## 📚 目录

1. [集群健康监控](#1-集群健康监控)
2. [节点状态检查](#2-节点状态检查)
3. [系统资源监控](#3-系统资源监控)
4. [快照与备份管理](#4-快照与备份管理)
5. [模板与插件管理](#5-模板与插件管理)
6. [性能监控与优化](#6-性能监控与优化)
7. [故障诊断技巧](#7-故障诊断技巧)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🏥 集群健康监控


### 1.1 集群健康基础检查


**🔸 基本健康状态查询**
```bash
# 最简单的健康检查 - 看集群是否正常
GET /_cluster/health

# 返回结果示例
{
  "cluster_name": "my-cluster",
  "status": "green",        # 绿色=健康，黄色=警告，红色=故障
  "number_of_nodes": 3,     # 节点总数
  "active_primary_shards": 10,
  "active_shards": 20
}
```

> 💡 **通俗理解**：就像体检报告，绿色表示身体健康，黄色表示有些小问题但不严重，红色表示有严重问题需要立即处理。

**🔸 等待特定状态 - 确保操作安全**
```bash
# 等待集群变成绿色状态才继续操作
GET /_cluster/health?wait_for_status=green

# 设置超时时间（30秒）
GET /_cluster/health?wait_for_status=green&timeout=30s

# 等待特定数量的节点上线
GET /_cluster/health?wait_for_nodes=3&timeout=60s
```

> ⚠️ **实用场景**：在重启节点或进行维护前，确保集群完全恢复健康状态，避免数据丢失。

### 1.2 详细健康信息


**🔸 获取详细健康报告**
```bash
# 查看每个索引的健康状态
GET /_cluster/health?level=indices

# 查看分片级别的详细信息
GET /_cluster/health?level=shards&pretty
```

**健康状态含义解释**：
```
┌─────────────────────────────────────────┐
│ 🟢 GREEN（绿色）- 完全健康              │
│ • 所有主分片和副本都正常工作             │
│ • 数据完整，可以正常读写                 │
│                                         │
│ 🟡 YELLOW（黄色）- 有警告               │
│ • 主分片正常，但部分副本缺失             │
│ • 数据完整，但缺少备份保护               │
│                                         │
│ 🔴 RED（红色）- 有故障                  │
│ • 部分主分片不可用                       │
│ • 数据丢失，影响正常使用                 │
└─────────────────────────────────────────┘
```

---

## 2. 🖥️ 节点状态检查


### 2.1 主节点信息查询


**🔸 查看当前主节点**
```bash
# 查看哪个节点是主节点（集群大脑）
GET /_cat/master

# 显示更详细的主节点信息
GET /_cat/master?v
```

> 💡 **通俗理解**：Elasticsearch集群就像一个公司，主节点就是CEO，负责做重要决策。这个命令告诉你现在谁是CEO。

**输出示例**：
```
id                     host      ip        node
u_n93zwxThWHi1PDBJAGAg 127.0.0.1 127.0.0.1 node-1
```

### 2.2 节点属性查看


**🔸 查看节点详细属性**
```bash
# 查看所有节点的属性配置
GET /_cat/nodeattrs

# 查看特定属性（比如机架信息）
GET /_cat/nodeattrs?v&h=node,attr,value
```

**节点属性示例**：
```
node   attr            value
node-1 rack            rack-1
node-1 zone            zone-a
node-2 rack            rack-2
node-2 zone            zone-b
```

> 🎯 **实际用途**：帮你了解节点的物理位置和特殊配置，用于分片分配策略和故障域规划。

### 2.3 节点性能监控


**🔸 查看节点基本信息**
```bash
# 查看所有节点的基本状态
GET /_cat/nodes?v

# 查看特定字段（内存、CPU、负载）
GET /_cat/nodes?v&h=name,heap.percent,ram.percent,cpu,load_1m
```

**性能指标解读**：
```
名称          含义                    正常范围
heap.percent  JVM堆内存使用率         < 85%
ram.percent   系统内存使用率          < 90%  
cpu           CPU使用率               < 80%
load_1m       1分钟平均负载           < CPU核数
```

---

## 3. 📊 系统资源监控


### 3.1 线程池监控


**🔸 查看线程池状态**
```bash
# 查看所有线程池概况
GET /_cat/thread_pool?v

# 查看关键线程池详情
GET /_cat/thread_pool?v&h=name,active,rejected,completed

# 监控搜索线程池
GET /_cat/thread_pool/search?v
```

**线程池类型说明**：
```
🔸 search     - 处理搜索请求
🔸 index      - 处理索引写入请求  
🔸 bulk       - 处理批量操作
🔸 refresh    - 处理索引刷新
🔸 flush      - 处理索引持久化
```

> ⚠️ **关键指标**：如果`rejected`（拒绝数）持续增加，说明系统负载过高，需要优化查询或增加资源。

### 3.2 分片分布监控


**🔸 查看分片分配情况**
```bash
# 查看所有分片状态
GET /_cat/shards?v

# 查看未分配的分片
GET /_cat/shards?h=index,shard,prirep,state,unassigned.reason | grep UNASSIGNED

# 按节点查看分片分布
GET /_cat/allocation?v
```

**分片状态含义**：
```
┌─────────────────────────────────────┐
│ STARTED    - 正常工作的分片         │
│ RELOCATING - 正在迁移的分片         │  
│ INITIALIZING - 正在初始化的分片     │
│ UNASSIGNED - 未分配的分片（问题）   │
└─────────────────────────────────────┘
```

---

## 4. 💾 快照与备份管理


### 4.1 仓库管理


**🔸 查看快照仓库**
```bash
# 查看所有备份仓库
GET /_cat/repositories?v

# 查看仓库详细配置
GET /_snapshot
```

> 💡 **通俗理解**：快照仓库就像存储备份的仓库，类似于云盘或移动硬盘，用来保存数据的完整备份。

### 4.2 快照监控


**🔸 查看快照列表**
```bash
# 查看指定仓库中的所有快照
GET /_cat/snapshots/{repository}

# 查看快照详细信息
GET /_cat/snapshots/my_backup?v&s=id
```

**快照状态监控**：
```bash
# 查看正在进行的快照任务
GET /_snapshot/_status

# 查看特定快照的进度
GET /_snapshot/my_backup/snapshot_2024_01_01/_status
```

---

## 5. 🔧 模板与插件管理


### 5.1 模板管理


**🔸 查看索引模板**
```bash
# 查看所有索引模板
GET /_cat/templates?v

# 查看特定模板
GET /_cat/templates/my_template*?v&s=name
```

> 💡 **模板作用**：就像Word文档模板，为新创建的索引预设好格式、字段类型等配置，确保数据结构一致。

### 5.2 插件监控


**🔸 查看已安装插件**
```bash
# 查看所有节点的插件列表
GET /_cat/plugins?v

# 查看特定节点的插件
GET /_cat/plugins?v&h=name,component,version
```

**常用插件类型**：
```
🔸 analysis-* - 分词器插件
🔸 repository-* - 备份仓库插件
🔸 discovery-* - 节点发现插件  
🔸 security - 安全认证插件
```

---

## 6. ⚡ 性能监控与优化


### 6.1 查询性能监控


**🔸 慢查询监控**
```bash
# 查看索引统计信息
GET /_cat/indices?v&h=index,search.query_time_in_millis,search.query_total

# 查看节点级别的查询统计
GET /_nodes/stats/indices/search
```

### 6.2 写入性能监控


**🔸 索引性能统计**
```bash
# 查看索引写入统计
GET /_cat/indices?v&h=index,indexing.index_time_in_millis,indexing.index_total

# 查看刷新和合并统计
GET /_cat/indices?v&h=index,refresh.total_time_in_millis,merges.total_time_in_millis
```

### 6.3 磁盘使用监控


**🔸 磁盘空间检查**
```bash
# 查看各节点磁盘使用情况
GET /_cat/allocation?v&h=node,disk.used_percent,disk.used,disk.total

# 查看索引大小排序
GET /_cat/indices?v&h=index,store.size&s=store.size:desc
```

---

## 7. 🔍 故障诊断技巧


### 7.1 常见问题诊断流程


```
故障诊断步骤：
步骤1️⃣ 检查集群状态 → GET /_cluster/health
           ↓
步骤2️⃣ 查看节点状态 → GET /_cat/nodes?v  
           ↓
步骤3️⃣ 检查分片分配 → GET /_cat/shards?v
           ↓  
步骤4️⃣ 查看错误日志 → GET /_cat/allocation?v
           ↓
步骤5️⃣ 分析线程池 → GET /_cat/thread_pool?v
```

### 7.2 关键监控指标


**🔸 集群健康度监控**
```bash
# 一键检查集群关键指标
GET /_cat/health?v
GET /_cat/nodes?v&h=name,heap.percent,cpu,load_1m
GET /_cat/allocation?v&h=node,disk.used_percent
```

**告警阈值建议**：
```
┌─────────────────────────────────────┐
│ 🔴 紧急告警                        │
│ • 集群状态：RED                     │
│ • 堆内存使用：> 90%                 │
│ • 磁盘使用：> 95%                   │
│                                     │
│ 🟡 警告告警                        │  
│ • 集群状态：YELLOW                  │
│ • 堆内存使用：> 85%                 │
│ • 磁盘使用：> 85%                   │
│ • CPU使用：> 80%                    │
└─────────────────────────────────────┘
```

### 7.3 性能优化建议


**🔸 基于监控数据的优化方向**
```bash
# 如果堆内存使用率高
# 1. 检查查询复杂度和聚合操作
GET /_nodes/stats/jvm

# 2. 调整fielddata和查询缓存
PUT /_cluster/settings
{
  "transient": {
    "indices.fielddata.cache.size": "20%"
  }
}

# 如果磁盘使用率高  
# 1. 清理旧索引或进行索引生命周期管理
DELETE /old_logs_*

# 2. 调整分片大小和副本数量
PUT /my_index/_settings
{
  "number_of_replicas": 1
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的监控命令


```
🔸 集群健康：GET /_cluster/health?wait_for_status=green
🔸 节点状态：GET /_cat/nodes?v&h=name,heap.percent,cpu,load_1m  
🔸 主节点：GET /_cat/master?v
🔸 分片状态：GET /_cat/shards?v
🔸 线程池：GET /_cat/thread_pool?v&h=name,active,rejected,completed
🔸 磁盘使用：GET /_cat/allocation?v
🔸 插件列表：GET /_cat/plugins?v
🔸 快照状态：GET /_cat/snapshots/{repository}?v
🔸 模板列表：GET /_cat/templates?v
```

### 8.2 关键理解要点


**🔹 健康状态的含义**
```
GREEN（绿色）→ 完全正常，可以放心使用
YELLOW（黄色）→ 功能正常，但缺少备份保护  
RED（红色）→ 有数据丢失，需要立即处理
```

**🔹 监控的重点指标**
```
性能指标：
• 堆内存使用率 < 85%
• CPU使用率 < 80%  
• 磁盘使用率 < 85%
• 线程池拒绝数为0

健康指标：
• 集群状态为GREEN
• 所有分片都是STARTED状态
• 没有UNASSIGNED分片
```

**🔹 故障诊断思路**
```
问题排查顺序：
1. 先看整体（集群健康）
2. 再看个体（节点状态）  
3. 深入细节（分片、线程池）
4. 定位根因（日志、指标）
5. 制定方案（优化、修复）
```

### 8.3 实际应用指导


**🎯 日常监控清单**
- **每日检查**：集群健康状态、节点资源使用率
- **每周检查**：分片分布、快照备份状态
- **每月检查**：索引大小增长、性能趋势分析

**🚀 性能优化策略**
- **预防为主**：设置合理告警阈值，及时发现问题
- **容量规划**：基于监控数据预测资源需求
- **定期维护**：清理旧数据、优化索引设置

**核心记忆口诀**：
- 健康监控看三色，绿黄红灯要分明
- 节点资源勤检查，CPU内存不超标  
- 分片状态要正常，线程池中无积压
- 快照备份定期做，模板插件管理好