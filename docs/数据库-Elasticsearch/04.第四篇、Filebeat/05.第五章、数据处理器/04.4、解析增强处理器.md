---
title: 4、解析增强处理器
---
## 📚 目录

1. [解析增强处理器概述](#1-解析增强处理器概述)
2. [JSON数据解析处理器](#2-JSON数据解析处理器)
3. [结构化文本解析处理器](#3-结构化文本解析处理器)
4. [时间戳处理器](#4-时间戳处理器)
5. [数据类型转换处理器](#5-数据类型转换处理器)
6. [脚本处理器](#6-脚本处理器)
7. [指纹生成处理器](#7-指纹生成处理器)
8. [字段截断处理器](#8-字段截断处理器)
9. [地理位置处理器](#9-地理位置处理器)
10. [用户代理解析处理器](#10-用户代理解析处理器)
11. [核心要点总结](#11-核心要点总结)

---

## 1. 🔍 解析增强处理器概述


### 1.1 什么是解析增强处理器


**📖 通俗理解**：
想象你收到一堆乱糟糟的邮件，有些是JSON格式，有些是普通文本，有些包含时间信息，有些有IP地址。解析增强处理器就像一个智能分拣员，能够：
- 把JSON邮件里的内容提取出来分类整理
- 把普通文本按照规律切分成有用信息
- 把时间信息转换成标准格式
- 把IP地址转换成地理位置信息

```
原始日志数据    →    解析增强处理器    →    结构化干净数据
混乱无序              智能分析                便于搜索分析
```

**🎯 核心作用**：
- **数据解析**：把复杂数据变成简单结构
- **内容提取**：从文本中挖掘有用信息
- **格式转换**：统一数据格式便于处理
- **信息增强**：添加额外的有用信息

### 1.2 处理器工作原理


**⚙️ 处理流程**：
```
日志收集 → 初步解析 → 应用处理器 → 字段增强 → 发送到目标
   ↓          ↓          ↓          ↓         ↓
原始文本   基础字段   高级解析    添加信息   最终数据
```

**💡 实际例子**：
假设有一条Web服务器日志：
```
{"timestamp":"2024-01-15T10:30:00Z","ip":"192.168.1.100","user_agent":"Mozilla/5.0...","response_time":"150ms"}
```

经过处理器后变成：
- **timestamp**: 标准时间格式
- **ip**: 包含地理位置信息
- **user_agent**: 解析出浏览器和操作系统
- **response_time**: 转换为数字类型

---

## 2. 📊 JSON数据解析处理器


### 2.1 decode_json_fields 基础概念


**📝 简单理解**：
JSON就像一个装满标签的盒子，每个标签都有名称和内容。`decode_json_fields`处理器就是帮你把这个盒子打开，把里面的内容分类摆放的工具。

**🔸 核心功能**：
- 解析JSON字符串为独立字段
- 支持嵌套JSON结构
- 自动处理数据类型
- 错误处理和容错机制

### 2.2 基础配置与使用


**📋 配置语法**：
```yaml
processors:
  - decode_json_fields:
      fields: ["message"]           # 要解析的字段
      target: ""                   # 目标位置
      overwrite_keys: false        # 是否覆盖现有字段
      add_error_key: true          # 添加错误信息字段
```

**🎯 实际应用示例**：

**场景1：Web应用日志解析**
```yaml
# 原始日志内容
# message: {"user_id":123,"action":"login","ip":"192.168.1.100","status":"success"}

processors:
  - decode_json_fields:
      fields: ["message"]
      target: "json"
      overwrite_keys: true
```

**处理结果**：
```
原始字段：
message: {"user_id":123,"action":"login"...}

解析后字段：
json.user_id: 123
json.action: "login"
json.ip: "192.168.1.100"
json.status: "success"
```

### 2.3 高级配置技巧


**🔧 处理嵌套JSON**：
```yaml
processors:
  - decode_json_fields:
      fields: ["message"]
      target: "app"
      max_depth: 3                 # 最大解析深度
      process_array: true          # 处理数组元素
```

**💡 错误处理最佳实践**：
```yaml
processors:
  - decode_json_fields:
      fields: ["log_data"]
      target: "parsed"
      add_error_key: true
      when:
        contains:
          log_data: "{"            # 只处理包含JSON的日志
```

---

## 3. ✂️ 结构化文本解析处理器


### 3.1 dissect 处理器原理


**📖 通俗解释**：
`dissect`就像一把智能剪刀，能按照你给定的模板把文本切分成小块。比如你有一句话"张三在2024年买了苹果"，你可以设置模板"%{姓名}在%{年份}年买了%{商品}"，它就能自动提取出姓名、年份、商品。

**🎯 核心特点**：
- **性能高效**：比正则表达式快很多
- **语法简单**：不需要复杂的正则知识
- **容错性好**：格式稍有变化也能处理

### 3.2 基础模板语法


**📋 模板格式**：
```
%{字段名}     # 提取内容到指定字段
%{字段名->}   # 提取到结尾
%{->字段名}   # 从开始提取
%{}           # 跳过不需要的内容
```

**🔸 实际示例**：

**示例1：Apache访问日志解析**
```yaml
# 日志格式：192.168.1.100 - - [15/Jan/2024:10:30:00 +0000] "GET /index.html HTTP/1.1" 200 1234

processors:
  - dissect:
      tokenizer: '%{client_ip} - - [%{timestamp}] "%{method} %{url} %{http_version}" %{status_code} %{response_size}'
      field: "message"
      target_prefix: "apache"
```

**解析结果**：
```
apache.client_ip: "192.168.1.100"
apache.timestamp: "15/Jan/2024:10:30:00 +0000"
apache.method: "GET"
apache.url: "/index.html"
apache.http_version: "HTTP/1.1"
apache.status_code: "200"
apache.response_size: "1234"
```

### 3.3 高级解析技巧


**🔧 处理可变格式**：
```yaml
processors:
  - dissect:
      tokenizer: '%{app_name}[%{pid}]: %{level} - %{message}'
      field: "syslog_message"
      ignore_failure: true         # 忽略解析失败
```

**💡 组合使用示例**：
```yaml
processors:
  # 第一步：基础切分
  - dissect:
      tokenizer: '%{timestamp} %{hostname} %{service}: %{json_data}'
      field: "message"
  
  # 第二步：解析JSON部分
  - decode_json_fields:
      fields: ["json_data"]
      target: "app"
```

---

## 4. ⏰ 时间戳处理器


### 4.1 timestamp 处理器作用


**📅 通俗理解**：
时间戳处理器就像一个时间翻译官，能把各种奇怪的时间格式都翻译成Elasticsearch能理解的标准时间格式。

比如：
- `2024-01-15 10:30:00` → `2024-01-15T10:30:00Z`
- `15/Jan/2024:10:30:00` → `2024-01-15T10:30:00Z`
- `1705310200` → `2024-01-15T10:30:00Z`

**🎯 核心功能**：
- **格式统一**：转换各种时间格式
- **时区处理**：自动处理时区转换
- **默认值设置**：处理缺失的时间信息

### 4.2 常用时间格式配置


**📋 基础配置**：
```yaml
processors:
  - timestamp:
      field: "custom_timestamp"     # 源时间字段
      target_field: "@timestamp"   # 目标字段
      layouts:                     # 时间格式模板
        - '2006-01-02 15:04:05'
        - '2006-01-02T15:04:05Z'
      timezone: "Asia/Shanghai"    # 时区设置
```

**🔸 常见时间格式模板**：

| **日志格式** | **模板** | **示例** |
|-------------|----------|----------|
| 标准ISO格式 | `2006-01-02T15:04:05Z` | `2024-01-15T10:30:00Z` |
| 普通日期时间 | `2006-01-02 15:04:05` | `2024-01-15 10:30:00` |
| Unix时间戳 | `UNIX` | `1705310200` |
| Apache日志 | `02/Jan/2006:15:04:05 -0700` | `15/Jan/2024:10:30:00 +0800` |

### 4.3 实际应用场景


**🎯 场景1：处理应用日志时间**
```yaml
processors:
  - timestamp:
      field: "app_time"
      layouts:
        - '2006-01-02 15:04:05.000'  # 包含毫秒
        - '2006-01-02 15:04:05'      # 不含毫秒
      timezone: "Local"              # 使用本地时区
      ignore_missing: true           # 忽略缺失字段
```

**💡 多格式兼容处理**：
```yaml
processors:
  - timestamp:
      field: "log_timestamp"
      layouts:
        - 'UNIX'                     # 优先尝试Unix时间戳
        - '2006-01-02T15:04:05Z'     # ISO格式
        - '2006-01-02 15:04:05'      # 普通格式
      test:
        - '1705310200'
        - '2024-01-15T10:30:00Z'
        - '2024-01-15 10:30:00'
```

---

## 5. 🔄 数据类型转换处理器


### 5.1 convert 处理器功能


**🔢 通俗理解**：
数据类型转换器就像一个格式化工具，能把文本数字变成真正的数字，把字符串"true"变成布尔值true。这样做的好处是Elasticsearch能够更好地理解和处理这些数据。

**🎯 支持的转换类型**：
- **string** → **integer**: `"123"` → `123`
- **string** → **float**: `"12.34"` → `12.34`
- **string** → **boolean**: `"true"` → `true`
- **integer** → **string**: `123` → `"123"`

### 5.2 基础配置语法


**📋 配置格式**：
```yaml
processors:
  - convert:
      fields:
        - {from: "response_time", to: "response_time_ms", type: "integer"}
        - {from: "is_success", type: "boolean"}
        - {from: "user_score", type: "float"}
      ignore_missing: true
      fail_on_error: false
```

**🔸 实际应用示例**：

**示例1：Web性能监控数据**
```yaml
# 原始数据：所有字段都是字符串
# response_time: "150"
# success_rate: "0.95"
# is_cached: "true"

processors:
  - convert:
      fields:
        - {from: "response_time", type: "integer"}
        - {from: "success_rate", type: "float"}
        - {from: "is_cached", type: "boolean"}
      tag: "type_conversion"
```

**转换结果**：
```
response_time: 150        # 整数类型
success_rate: 0.95        # 浮点数类型
is_cached: true           # 布尔类型
```

### 5.3 高级转换技巧


**🔧 批量字段转换**：
```yaml
processors:
  - convert:
      fields:
        # 转换所有以_count结尾的字段为整数
        - {from: "error_count", type: "integer"}
        - {from: "success_count", type: "integer"}
        - {from: "retry_count", type: "integer"}
      mode: "rename"           # 覆盖原字段
      ignore_missing: true
```

**💡 条件转换**：
```yaml
processors:
  - convert:
      fields:
        - {from: "status_code", type: "integer"}
      when:
        regexp:
          status_code: '^[0-9]+$'  # 只转换纯数字
```

---

## 6. 🔧 脚本处理器


### 6.1 script 处理器概念


**💻 通俗理解**：
脚本处理器就像给Filebeat装了一个小型计算器和逻辑处理器，能够执行复杂的数据处理逻辑。当内置处理器无法满足需求时，就可以用脚本来实现自定义功能。

**🎯 核心能力**：
- **数据计算**：数学运算、字符串处理
- **条件判断**：复杂的业务逻辑
- **字段操作**：动态创建、修改字段
- **数据验证**：检查数据完整性

### 6.2 JavaScript脚本基础


**📋 基础语法**：
```yaml
processors:
  - script:
      lang: javascript
      source: >
        function(event) {
          // 在这里编写处理逻辑
          return event;
        }
```

**🔸 实际应用示例**：

**示例1：计算响应时间等级**
```yaml
processors:
  - script:
      lang: javascript
      source: >
        function(event) {
          var responseTime = event.Get("response_time");
          var level;
          
          if (responseTime < 100) {
            level = "fast";
          } else if (responseTime < 500) {
            level = "normal";
          } else {
            level = "slow";
          }
          
          event.Put("performance_level", level);
          return event;
        }
```

### 6.3 高级脚本应用


**🔧 复杂数据处理**：
```yaml
processors:
  - script:
      lang: javascript
      source: >
        function(event) {
          // 获取用户代理字符串
          var userAgent = event.Get("user_agent");
          
          // 简单的浏览器检测
          var browser = "unknown";
          if (userAgent.indexOf("Chrome") > -1) {
            browser = "chrome";
          } else if (userAgent.indexOf("Firefox") > -1) {
            browser = "firefox";
          } else if (userAgent.indexOf("Safari") > -1) {
            browser = "safari";
          }
          
          // 设置新字段
          event.Put("browser_type", browser);
          
          // 计算访问得分
          var score = 0;
          if (event.Get("status_code") === 200) score += 10;
          if (browser !== "unknown") score += 5;
          
          event.Put("visit_score", score);
          return event;
        }
```

**💡 错误处理最佳实践**：
```yaml
processors:
  - script:
      lang: javascript
      source: >
        function(event) {
          try {
            // 数据处理逻辑
            var data = JSON.parse(event.Get("raw_data"));
            event.Put("parsed_data", data);
          } catch (e) {
            // 错误处理
            event.Put("parse_error", e.message);
          }
          return event;
        }
      tag: "custom_parser"
```

---

## 7. 🔐 指纹生成处理器


### 7.1 fingerprint 处理器作用


**🆔 通俗理解**：
指纹生成器就像给每条日志数据生成一个独特的身份证号码。即使两条日志内容完全相同，也能通过这个"指纹"来识别它们是否重复，这对去重和数据完整性检查非常有用。

**🎯 核心功能**：
- **去重标识**：识别重复的日志条目
- **数据完整性**：检测数据是否被修改
- **分片路由**：基于内容进行数据分发
- **缓存键值**：作为缓存系统的唯一标识

### 7.2 基础配置方法


**📋 配置语法**：
```yaml
processors:
  - fingerprint:
      fields: ["host", "message"]    # 用于生成指纹的字段
      target_field: "fingerprint"   # 指纹存储字段
      method: "sha256"               # 哈希算法
      encoding: "hex"                # 编码格式
```

**🔸 支持的哈希算法**：

| **算法** | **安全性** | **性能** | **适用场景** |
|----------|------------|----------|-------------|
| `md5` | 低 | 很快 | 简单去重 |
| `sha1` | 中 | 快 | 一般应用 |
| `sha256` | 高 | 中等 | 安全要求高 |
| `sha512` | 很高 | 较慢 | 最高安全 |

### 7.3 实际应用场景


**🎯 场景1：日志去重**
```yaml
processors:
  - fingerprint:
      fields: ["message", "host", "@timestamp"]
      target_field: "event_fingerprint"
      method: "sha256"
      ignore_missing: true
```

**应用效果**：
```
原始日志1：
message: "User login failed"
host: "web-server-01"
@timestamp: "2024-01-15T10:30:00Z"
event_fingerprint: "a1b2c3d4e5f6..."

原始日志2：(内容相同)
message: "User login failed"
host: "web-server-01"
@timestamp: "2024-01-15T10:30:00Z"
event_fingerprint: "a1b2c3d4e5f6..."  # 相同指纹，可识别为重复
```

**💡 智能去重策略**：
```yaml
processors:
  # 先规范化数据
  - lowercase:
      field: "message"
  
  # 移除时间戳差异
  - dissect:
      tokenizer: '%{timestamp} %{content}'
      field: "message"
      ignore_failure: true
  
  # 基于内容生成指纹
  - fingerprint:
      fields: ["content", "host", "service"]
      target_field: "content_hash"
      method: "md5"
```

---

## 8. ✂️ 字段截断处理器


### 8.1 truncate_fields 处理器功能


**📏 通俗理解**：
字段截断处理器就像一把智能剪刀，能把过长的文本内容剪短到合适的长度。这样做的好处是：
- 节省存储空间
- 提高查询性能
- 避免超长字段导致的问题
- 保护敏感信息

**🎯 核心特性**：
- **长度控制**：精确控制字段长度
- **编码安全**：正确处理中文等多字节字符
- **批量处理**：同时处理多个字段
- **智能截断**：避免截断敏感位置

### 8.2 基础配置语法


**📋 配置格式**：
```yaml
processors:
  - truncate_fields:
      fields:
        - "message"              # 要截断的字段
        - "user_agent"
      max_bytes: 1024            # 最大字节数
      fail_on_error: false       # 错误处理方式
```

**🔸 实际应用示例**：

**示例1：日志消息长度控制**
```yaml
# 原始数据可能很长
# message: "This is a very long error message that contains detailed stack trace information..."

processors:
  - truncate_fields:
      fields: ["message"]
      max_bytes: 200
      fail_on_error: false
```

**处理结果**：
```
message: "This is a very long error message that contains detailed stack trace info..."
# 自动截断到200字节以内
```

### 8.3 高级截断策略


**🔧 多字段分别控制**：
```yaml
processors:
  - truncate_fields:
      fields: ["error_message"]
      max_bytes: 500
  
  - truncate_fields:
      fields: ["user_input"]
      max_bytes: 100              # 用户输入限制更严格
  
  - truncate_fields:
      fields: ["system_log"]
      max_bytes: 2048             # 系统日志可以更长
```

**💡 智能截断技巧**：
```yaml
processors:
  # 先尝试保留重要信息
  - script:
      lang: javascript
      source: >
        function(event) {
          var msg = event.Get("message");
          if (msg && msg.length > 1000) {
            // 保留开头和结尾，中间用...连接
            var start = msg.substring(0, 400);
            var end = msg.substring(msg.length - 400);
            event.Put("message", start + "..." + end);
          }
          return event;
        }
  
  # 最终长度控制
  - truncate_fields:
      fields: ["message"]
      max_bytes: 1024
```

---

## 9. 🌍 地理位置处理器


### 9.1 geoip 处理器概念


**🗺️ 通俗理解**：
GeoIP处理器就像一个地址查询助手，能够根据IP地址告诉你这个IP来自哪个国家、城市，甚至能提供经纬度坐标。这对于分析用户分布、安全监控、内容分发等场景非常有用。

**🎯 核心功能**：
- **地理定位**：IP地址 → 地理位置信息
- **安全分析**：识别异常地区的访问
- **用户分析**：了解用户地理分布
- **内容优化**：基于地区优化服务

### 9.2 基础配置方法


**📋 配置语法**：
```yaml
processors:
  - geoip:
      field: "client_ip"           # 源IP字段
      target_field: "geoip"        # 目标字段
      database_file: "/path/to/GeoLite2-City.mmdb"  # 数据库文件
      properties:                  # 要提取的信息
        - "city_name"
        - "country_name"
        - "location"
        - "region_name"
```

**🔸 可提取的地理信息**：

| **属性** | **含义** | **示例** |
|----------|----------|----------|
| `country_name` | 国家名称 | `"China"` |
| `country_code` | 国家代码 | `"CN"` |
| `city_name` | 城市名称 | `"Beijing"` |
| `region_name` | 省份/州 | `"Beijing"` |
| `location` | 经纬度 | `{"lat": 39.9042, "lon": 116.4074}` |
| `timezone` | 时区 | `"Asia/Shanghai"` |

### 9.3 实际应用示例


**🎯 场景1：Web访问分析**
```yaml
processors:
  # 解析IP地理位置
  - geoip:
      field: "client_ip"
      target_field: "geo"
      properties:
        - "country_name"
        - "city_name"
        - "location"
        - "region_name"
      ignore_missing: true
  
  # 添加地理标签
  - script:
      lang: javascript
      source: >
        function(event) {
          var country = event.Get("geo.country_name");
          if (country) {
            // 设置地区标签
            if (country === "China") {
              event.Put("region_tag", "domestic");
            } else {
              event.Put("region_tag", "international");
            }
          }
          return event;
        }
```

**处理结果**：
```
原始数据：
client_ip: "203.208.60.1"

处理后：
client_ip: "203.208.60.1"
geo.country_name: "China"
geo.city_name: "Beijing"
geo.location: {"lat": 39.9042, "lon": 116.4074}
geo.region_name: "Beijing"
region_tag: "domestic"
```

**💡 安全监控应用**：
```yaml
processors:
  - geoip:
      field: "source_ip"
      target_field: "threat_geo"
      properties: ["country_name", "country_code"]
  
  # 标记可疑地区
  - script:
      lang: javascript
      source: >
        function(event) {
          var country = event.Get("threat_geo.country_code");
          var suspiciousCountries = ["XX", "YY", "ZZ"];  // 高风险国家代码
          
          if (suspiciousCountries.indexOf(country) > -1) {
            event.Put("security_risk", "high");
            event.Put("alert_level", "warning");
          } else {
            event.Put("security_risk", "normal");
          }
          return event;
        }
```

---

## 10. 🌐 用户代理解析处理器


### 10.1 user_agent 处理器作用


**🔍 通俗理解**：
用户代理解析器就像一个侦探，能从浏览器发送的"身份证明"（User-Agent字符串）中解读出用户使用的是什么浏览器、操作系统、设备类型等信息。这些信息对于网站优化、用户体验分析、兼容性测试都很重要。

**🎯 解析信息类型**：
- **浏览器信息**：Chrome、Firefox、Safari等
- **操作系统**：Windows、macOS、Linux、Android等
- **设备类型**：桌面、手机、平板等
- **版本信息**：具体的版本号

### 10.2 基础配置语法


**📋 配置格式**：
```yaml
processors:
  - user_agent:
      field: "user_agent"          # 源字段
      target_field: "ua"           # 目标字段前缀
      regex_file: ""               # 自定义正则文件
      ignore_missing: true
```

**🔸 默认解析字段**：

| **字段** | **含义** | **示例** |
|----------|----------|----------|
| `ua.name` | 浏览器名称 | `"Chrome"` |
| `ua.version` | 浏览器版本 | `"91.0.4472.124"` |
| `ua.os.name` | 操作系统 | `"Windows"` |
| `ua.os.version` | 系统版本 | `"10"` |
| `ua.device.name` | 设备名称 | `"Other"` |

### 10.3 实际应用示例


**🎯 场景1：网站访问统计**
```yaml
processors:
  - user_agent:
      field: "http.request.headers.user-agent"
      target_field: "user_agent"
      ignore_missing: true
  
  # 添加设备类型判断
  - script:
      lang: javascript
      source: >
        function(event) {
          var osName = event.Get("user_agent.os.name");
          var deviceType = "desktop";
          
          if (osName && (osName.indexOf("Android") > -1 || osName.indexOf("iOS") > -1)) {
            deviceType = "mobile";
          } else if (osName && osName.indexOf("iPad") > -1) {
            deviceType = "tablet";
          }
          
          event.Put("device_category", deviceType);
          return event;
        }
```

**处理结果**：
```
原始数据：
user_agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36"

解析结果：
user_agent.name: "Chrome"
user_agent.version: "91.0.4472.124"
user_agent.os.name: "Windows"
user_agent.os.version: "10"
device_category: "desktop"
```

**💡 移动端优化应用**：
```yaml
processors:
  - user_agent:
      field: "user_agent_string"
      target_field: "parsed_ua"
  
  # 移动端特殊处理
  - script:
      lang: javascript
      source: >
        function(event) {
          var browser = event.Get("parsed_ua.name");
          var osName = event.Get("parsed_ua.os.name");
          
          // 移动端浏览器优化建议
          var recommendations = [];
          
          if (osName && osName.indexOf("Android") > -1) {
            if (browser === "Chrome" && parseInt(event.Get("parsed_ua.version")) < 80) {
              recommendations.push("upgrade_browser");
            }
            recommendations.push("mobile_optimized");
          }
          
          if (recommendations.length > 0) {
            event.Put("optimization_suggestions", recommendations);
          }
          
          return event;
        }
```

---

## 11. 📋 核心要点总结


### 11.1 各处理器适用场景总结


🎯 **处理器选择指南**：

| **数据类型** | **推荐处理器** | **应用场景** |
|-------------|---------------|--------------|
| 🏷️ **JSON格式日志** | `decode_json_fields` | API响应、应用日志、结构化数据 |
| 📝 **固定格式文本** | `dissect` | Apache日志、系统日志、CSV数据 |
| ⏰ **时间字段** | `timestamp` | 统一时间格式、时区转换 |
| 🔢 **数字字符串** | `convert` | 性能指标、计数器、布尔值 |
| 🌍 **IP地址** | `geoip` | 用户分析、安全监控、CDN优化 |
| 🌐 **浏览器信息** | `user_agent` | 用户体验、兼容性、设备统计 |
| 🔐 **数据去重** | `fingerprint` | 重复检测、数据完整性 |
| ✂️ **长文本** | `truncate_fields` | 存储优化、性能提升 |
| 🔧 **复杂逻辑** | `script` | 自定义处理、业务逻辑 |

### 11.2 配置最佳实践


**🔸 性能优化原则**：
```yaml
processors:
  # 1. 先过滤，减少后续处理量
  - drop_event:
      when:
        equals:
          log.level: "debug"
  
  # 2. 轻量级处理器在前
  - dissect:
      tokenizer: '%{timestamp} %{level} %{message}'
      field: "message"
  
  # 3. 重量级处理器在后
  - geoip:
      field: "client_ip"
      target_field: "geo"
  
  # 4. 脚本处理器最后
  - script:
      lang: javascript
      source: "function(event) { /* 复杂逻辑 */ }"
```

**💡 错误处理策略**：
```yaml
processors:
  - decode_json_fields:
      fields: ["message"]
      target: "json"
      ignore_failure: true        # 继续处理其他数据
      add_error_key: true         # 记录错误信息
      tag: "json_parsing"         # 添加标签便于调试
  
  - timestamp:
      field: "json.timestamp"
      layouts: ["2006-01-02T15:04:05Z"]
      ignore_missing: true        # 字段不存在时不报错
      test: ["2024-01-15T10:30:00Z"]  # 测试格式
```

### 11.3 组合使用模式


**🔄 典型处理流水线**：
```yaml
processors:
  # 阶段1：基础解析
  - dissect:
      tokenizer: '%{timestamp} %{host} %{service}: %{json_message}'
      field: "message"
  
  # 阶段2：JSON解析
  - decode_json_fields:
      fields: ["json_message"]
      target: "app"
  
  # 阶段3：时间处理
  - timestamp:
      field: "timestamp"
      layouts: ["2006-01-02 15:04:05"]
  
  # 阶段4：数据增强
  - geoip:
      field: "app.client_ip"
      target_field: "geo"
  
  - user_agent:
      field: "app.user_agent"
      target_field: "ua"
  
  # 阶段5：数据清理
  - convert:
      fields:
        - {from: "app.response_time", type: "integer"}
        - {from: "app.status_code", type: "integer"}
  
  # 阶段6：指纹生成
  - fingerprint:
      fields: ["host", "app.request_id"]
      target_field: "event_hash"
      method: "sha256"
```

**🧠 记忆要点**：
- **解析顺序**：先文本切分，再JSON解析，最后数据增强
- **性能考虑**：轻量级在前，重量级在后
- **错误处理**：每个处理器都要考虑失败情况
- **测试验证**：使用样本数据验证处理器配置

**核心记忆口诀**：
```
JSON解析用decode，文本切分选dissect
时间格式要统一，数据类型需转换
IP地址查地理，用户代理解浏览器
指纹生成防重复，字段截断省空间
脚本处理最灵活，组合使用效果佳
```