---
title: 7、syslog-input.yml系统日志配置
---
## 📚 目录

1. [什么是Syslog输入配置](#1-什么是syslog输入配置)
2. [UDP方式接收系统日志](#2-udp方式接收系统日志)
3. [TCP方式接收系统日志](#3-tcp方式接收系统日志)
4. [多端口监听配置](#4-多端口监听配置)
5. [日志格式解析设置](#5-日志格式解析设置)
6. [安全传输配置](#6-安全传输配置)
7. [日志级别过滤](#7-日志级别过滤)
8. [设施代码配置](#8-设施代码配置)
9. [网络缓冲区优化](#9-网络缓冲区优化)
10. [并发连接管理](#10-并发连接管理)
11. [完整配置示例](#11-完整配置示例)
12. [核心要点总结](#12-核心要点总结)

---

## 1. 🌐 什么是Syslog输入配置


### 1.1 Syslog的基本概念


**什么是Syslog？**
```
简单理解：Syslog就是系统日志的标准格式
作用：让不同的设备和软件都能用统一的方式发送日志
好处：无论是路由器、防火墙、服务器，日志格式都一样
```

> 💡 **通俗解释**：
> 
> 想象一下邮件系统，所有人都用统一的邮件格式发邮件
> Syslog就像是日志界的"邮件格式标准"
> 这样Filebeat就能轻松接收各种设备的日志了

### 1.2 Filebeat为什么要接收Syslog


**实际应用场景**：
- 🔥 **网络设备日志**：路由器、交换机发送的运行日志
- 🔥 **安全设备日志**：防火墙、入侵检测系统的告警日志  
- 🔥 **应用系统日志**：各种服务器程序的运行状态日志
- 🔥 **监控告警日志**：系统监控产生的各种告警信息

### 1.3 Syslog传输方式对比


| 传输方式 | **优点** | **缺点** | **适用场景** |
|---------|---------|---------|-------------|
| 📡 **UDP** | `速度快，资源占用少` | `可能丢失日志` | `大量普通日志，允许少量丢失` |
| 🔗 **TCP** | `可靠传输，不丢失` | `速度较慢，资源占用多` | `重要日志，必须保证完整` |

---

## 2. 📡 UDP方式接收系统日志


### 2.1 UDP接收的工作原理


**工作流程图示**：
```
日志发送设备          网络传输          Filebeat接收端
┌─────────┐          ┌─────────┐        ┌─────────────┐
│ 路由器   │─────UDP─→│ 网络    │──────→│ UDP监听器   │
│ 防火墙   │   514端口 │ 传输    │  514端口│ 接收处理    │
│ 服务器   │          │         │        │ 格式解析    │
└─────────┘          └─────────┘        └─────────────┘
```

> ⚠️ **重要提醒**：
> 
> UDP就像寄明信片，速度快但可能丢失
> 适合接收大量不太重要的日常日志

### 2.2 基础UDP配置


```yaml
# 最简单的UDP syslog配置
filebeat.inputs:
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"    # 监听所有网卡的514端口
  fields:
    log_type: "syslog_udp"  # 添加标识字段
```

**配置说明**：
- `type: syslog`：告诉Filebeat这是接收syslog日志
- `protocol.udp`：指定使用UDP协议
- `host: "0.0.0.0:514"`：在所有网卡的514端口监听
- `fields`：给日志添加额外的标识信息

### 2.3 UDP高级配置选项


```yaml
filebeat.inputs:
- type: syslog
  protocol.udp:
    host: "192.168.1.100:1514"  # 指定网卡和端口
    max_message_size: 10240     # 最大消息10KB
    timeout: 300s               # 5分钟超时
  
  # 解析器配置
  parsers:
  - syslog:
      format: rfc3164           # 使用RFC3164格式
      timezone: "Asia/Shanghai" # 设置时区
```

**参数详解**：
- ✅ **max_message_size**：单条日志最大字节数，防止超大日志
- ✅ **timeout**：网络超时时间，太短容易断连
- ✅ **format**：日志解析格式，rfc3164是最常用的
- ✅ **timezone**：时区设置，确保时间显示正确

---

## 3. 🔗 TCP方式接收系统日志


### 3.1 TCP接收的工作原理


**TCP vs UDP对比**：
```
UDP传输（不可靠）：
发送端 ──丢包可能──→ 接收端
  📦✗               收到部分

TCP传输（可靠）：
发送端 ←─确认应答─→ 接收端  
  📦✓               完整接收
```

> 💡 **形象比喻**：
> 
> TCP像快递员，必须签收确认才算送达
> UDP像扔报纸，扔了就走，不管你收到没有

### 3.2 基础TCP配置


```yaml
filebeat.inputs:
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:1514"      # TCP监听端口
    framing: rfc6587          # TCP帧格式
    line_delimiter: "\n"      # 行分隔符
  
  fields:
    log_type: "syslog_tcp"
    transport: "reliable"     # 标记为可靠传输
```

**关键配置说明**：
- `framing: rfc6587`：TCP专用的帧格式，确保消息边界清晰
- `line_delimiter`：每条日志的结束标记
- 端口建议用1514而不是514（需要root权限）

### 3.3 TCP连接管理配置


```yaml
filebeat.inputs:
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:1514"
    max_connections: 100      # 最大并发连接数
    timeout: 600s             # 连接超时10分钟
    
    # 连接保活设置
    keep_alive: true          # 启用长连接
    keep_alive_period: 30s    # 保活探测间隔
```

**连接管理要点**：
- 🔥 **max_connections**：根据发送端数量设置，太小会拒绝连接
- 🔥 **timeout**：太短频繁断连，太长占用资源
- 🔥 **keep_alive**：长连接减少建连开销

---

## 4. 🔀 多端口监听配置


### 4.1 为什么需要多端口


**实际业务场景**：
- 🎯 **设备类型分离**：不同设备用不同端口
- 🎯 **日志级别分离**：普通日志和告警日志分开
- 🎯 **业务系统分离**：不同业务用不同端口便于管理

### 4.2 多端口配置示例


```yaml
filebeat.inputs:
# 网络设备日志 - UDP 514端口
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"
  fields:
    device_type: "network"
    log_source: "router_switch"

# 安全设备日志 - TCP 1514端口  
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:1514"
  fields:
    device_type: "security"
    log_source: "firewall"

# 应用日志 - UDP 2514端口
- type: syslog
  protocol.udp:
    host: "0.0.0.0:2514"
  fields:
    device_type: "application"
    log_source: "app_server"
```

### 4.3 端口规划最佳实践


**端口选择建议**：
```
514   ← 标准syslog端口（需要root权限）
1514  ← 常用备选端口
2514  ← 应用日志端口  
5514  ← 安全日志端口
9514  ← 监控日志端口
```

> ⚠️ **注意事项**：
> 
> - 低于1024的端口需要管理员权限
> - 避免与其他服务冲突（如3306是MySQL）
> - 建议使用防火墙限制访问来源

---

## 5. 📝 日志格式解析设置


### 5.1 Syslog格式标准介绍


**常见格式对比**：

| 格式标准 | **特点** | **示例** | **使用场景** |
|---------|---------|---------|-------------|
| 🔸 **RFC3164** | `传统格式，广泛支持` | `Oct 11 22:14:15 host app[123]: message` | `老设备，兼容性要求高` |
| 🔸 **RFC5424** | `新标准，结构化` | `<165>1 2023-10-11T22:14:15.123Z host app 123 - message` | `新设备，需要精确时间` |

### 5.2 RFC3164格式解析配置


```yaml
filebeat.inputs:
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"
  
  # RFC3164解析设置
  parsers:
  - syslog:
      format: rfc3164                    # 传统syslog格式
      timezone: "Asia/Shanghai"          # 本地时区
      ignore_parse_failures: true       # 忽略解析错误
```

**RFC3164格式解释**：
```
完整日志：Oct 11 22:14:15 web01 nginx[1234]: GET /api/users HTTP/1.1

解析结果：
├── 时间戳：Oct 11 22:14:15    （月 日 时:分:秒）
├── 主机名：web01              （发送日志的主机）  
├── 程序名：nginx              （产生日志的程序）
├── 进程ID：1234               （程序的进程号）
└── 消息内容：GET /api/users HTTP/1.1
```

### 5.3 RFC5424格式解析配置


```yaml
filebeat.inputs:
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:1514"
  
  parsers:
  - syslog:
      format: rfc5424                    # 新标准格式
      timezone: "UTC"                    # RFC5424推荐UTC
      structured_data: true              # 解析结构化数据
```

**RFC5424格式优势**：
- ✅ **精确时间**：支持毫秒级时间戳
- ✅ **结构化数据**：支持键值对数据  
- ✅ **国际化**：支持UTF-8字符编码
- ✅ **扩展性**：预留扩展字段

---

## 6. 🔒 安全传输配置


### 6.1 TLS加密传输


**为什么需要TLS**：
```
普通传输：明文传输，容易被窃听
┌────────┐  明文日志  ┌──────────┐
│ 发送端 │ ────────→ │ 接收端   │
└────────┘           └──────────┘
          ↑ 黑客可以截获

TLS传输：加密传输，安全可靠  
┌────────┐  加密日志  ┌──────────┐
│ 发送端 │ ━━━━━━━→ │ 接收端   │
└────────┘           └──────────┘
          ↑ 黑客无法解读
```

### 6.2 TLS配置示例


```yaml
filebeat.inputs:
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:6514"      # TLS常用6514端口
    tls:
      enabled: true           # 启用TLS
      certificate: "/etc/filebeat/server.crt"    # 服务器证书
      key: "/etc/filebeat/server.key"            # 私钥文件
      certificate_authorities: ["/etc/filebeat/ca.crt"]  # CA证书
      
      # 安全设置
      verification_mode: "strict"     # 严格验证客户端
      supported_protocols: ["TLSv1.2", "TLSv1.3"]  # 支持的TLS版本
```

### 6.3 客户端认证配置


```yaml
filebeat.inputs:
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:6514"
    tls:
      enabled: true
      certificate: "/etc/filebeat/server.crt"
      key: "/etc/filebeat/server.key" 
      certificate_authorities: ["/etc/filebeat/ca.crt"]
      
      # 客户端证书验证
      client_authentication: "required"    # 要求客户端证书
      verification_mode: "strict"          # 严格验证模式
```

> 🔒 **安全最佳实践**：
> 
> - 使用强密码保护私钥文件
> - 定期更新证书（建议每年）
> - 只使用TLS 1.2以上版本
> - 限制证书访问权限（600权限）

---

## 7. 🎯 日志级别过滤


### 7.1 Syslog日志级别介绍


**标准日志级别**：
```
级别  名称      含义                     典型使用
 0    emergency  系统不可用               系统崩溃
 1    alert      必须立即处理             硬件故障  
 2    critical   严重错误                 软件严重错误
 3    error      一般错误                 程序错误
 4    warning    警告信息                 配置问题
 5    notice     正常但重要               用户登录
 6    info       一般信息                 程序启动
 7    debug      调试信息                 开发调试
```

### 7.2 基于级别的过滤配置


```yaml
filebeat.inputs:
# 只接收错误和警告级别
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"
  
  # 使用processors过滤
  processors:
  - drop_event:
      when:
        not:
          or:
            - equals:
                syslog.severity: 0    # emergency
            - equals:  
                syslog.severity: 1    # alert
            - equals:
                syslog.severity: 2    # critical
            - equals:
                syslog.severity: 3    # error
            - equals:
                syslog.severity: 4    # warning
```

### 7.3 简化的级别过滤


```yaml
filebeat.inputs:
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"
  
  processors:
  # 只保留严重级别（0-4）
  - drop_event:
      when:
        range:
          syslog.severity:
            gte: 5    # 丢弃info和debug级别
```

**过滤策略建议**：
- 🔥 **生产环境**：只保留error及以上级别（0-3）
- 🔥 **测试环境**：保留warning及以上级别（0-4）  
- 🔥 **开发环境**：保留所有级别（0-7）

---

## 8. 🏷️ 设施代码配置


### 8.1 什么是设施代码


**设施代码的作用**：
```
设施代码 = 日志来源分类标识

常用设施代码：
0   = kernel       内核消息
1   = user          用户级消息  
2   = mail          邮件系统
3   = daemon        守护进程
4   = security      安全/授权消息
5   = syslogd       syslogd内部消息
16  = local0        本地自定义0
17  = local1        本地自定义1
...
23  = local7        本地自定义7
```

> 💡 **通俗理解**：
> 
> 设施代码就像快递的分拣标签
> 告诉你这个日志是从哪类系统发来的

### 8.2 基于设施代码的分类处理


```yaml
filebeat.inputs:
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"
  
  processors:
  # 给不同设施添加标签
  - add_fields:
      when:
        equals:
          syslog.facility: 4    # security facility
      fields:
        log_category: "security"
        priority: "high"

  - add_fields:
      when:
        equals:
          syslog.facility: 16   # local0 facility  
      fields:
        log_category: "application"
        priority: "medium"
```

### 8.3 设施代码路由配置


```yaml
filebeat.inputs:
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"

output.elasticsearch:
  hosts: ["localhost:9200"]
  
  # 根据设施代码路由到不同索引
  indices:
    - index: "security-logs-%{+yyyy.MM.dd}"
      when.equals:
        syslog.facility: 4      # 安全日志
        
    - index: "system-logs-%{+yyyy.MM.dd}"  
      when.range:
        syslog.facility:
          gte: 0
          lt: 16                # 系统日志
          
    - index: "application-logs-%{+yyyy.MM.dd}"
      when.range:
        syslog.facility:
          gte: 16               # 应用日志
```

---

## 9. 📊 网络缓冲区优化


### 9.1 为什么需要优化缓冲区


**缓冲区作用示意**：
```
数据流向：网络 → 系统缓冲区 → Filebeat处理

缓冲区太小：
网络数据 ──丢失──→ [小缓冲区] ──→ Filebeat
              溢出丢失

缓冲区合适：  
网络数据 ────────→ [适当缓冲区] ──→ Filebeat
              暂存处理
```

### 9.2 UDP缓冲区优化配置


```yaml
filebeat.inputs:
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"
    max_message_size: 65536      # 64KB消息大小
    read_buffer: 1048576         # 1MB读缓冲区
    
  # 处理器缓冲优化
  processors:
  - script:
      lang: javascript
      id: buffer_stats
      source: >
        function process(event) {
          // 添加缓冲区统计信息
          event.Put("buffer.size", "1MB");
          return event;
        }
```

### 9.3 系统级缓冲区调优


**Linux系统优化**：
```bash
# 查看当前UDP缓冲区设置
sysctl net.core.rmem_default    # 默认接收缓冲区
sysctl net.core.rmem_max        # 最大接收缓冲区

# 优化UDP接收缓冲区（临时生效）
sudo sysctl -w net.core.rmem_default=1048576    # 1MB
sudo sysctl -w net.core.rmem_max=134217728      # 128MB

# 永久生效（添加到/etc/sysctl.conf）
net.core.rmem_default = 1048576
net.core.rmem_max = 134217728
```

> ⚡ **性能调优要点**：
> 
> - 高流量环境：增大缓冲区到1-4MB
> - 突发流量：设置最大缓冲区到64-128MB
> - 监控丢包：定期检查`netstat -su`的UDP丢包统计

---

## 10. 🔄 并发连接管理


### 10.1 并发连接的重要性


**并发场景示意**：
```
单连接处理：
设备A ──→ Filebeat ──→ 等待处理
设备B ──→ 排队等待
设备C ──→ 排队等待

多连接并发：
设备A ──→ 连接1 ──→ Filebeat
设备B ──→ 连接2 ──→ 同时处理  
设备C ──→ 连接3 ──→ 提高效率
```

### 10.2 TCP并发连接配置


```yaml
filebeat.inputs:
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:1514"
    max_connections: 200         # 最大200个并发连接
    timeout: 300s                # 连接超时5分钟
    
    # 连接池管理
    keep_alive: true             # 启用长连接
    keep_alive_period: 30s       # 30秒保活探测
    
  # 并发处理优化
  processors:
  - add_fields:
      fields:
        connection_mode: "concurrent"
        max_connections: 200
```

### 10.3 连接监控和告警


```yaml
filebeat.inputs:
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:1514"
    max_connections: 100
    
  processors:
  # 连接数监控
  - script:
      lang: javascript
      source: >
        function process(event) {
          var now = new Date();
          event.Put("connection.timestamp", now.toISOString());
          event.Put("connection.monitoring", true);
          return event;
        }
```

**连接数规划建议**：
- 🎯 **小环境**：10-50个并发连接
- 🎯 **中等环境**：50-200个并发连接
- 🎯 **大型环境**：200-1000个并发连接

> ⚠️ **注意事项**：
> 
> - 过多连接会占用系统资源
> - 需要配合系统`ulimit -n`设置
> - 监控连接使用情况，及时调整

---

## 11. 📋 完整配置示例


### 11.1 生产环境完整配置


```yaml
##################### Filebeat Configuration ######################


# ============================== 全局配置 ==============================
name: "syslog-collector-prod"
tags: ["production", "syslog", "security"]

# ============================== 输入配置 ==============================
filebeat.inputs:

# 网络设备日志 - UDP接收
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"
    max_message_size: 65536
    read_buffer: 1048576
    
  parsers:
  - syslog:
      format: rfc3164
      timezone: "Asia/Shanghai"
      ignore_parse_failures: true
      
  fields:
    log_type: "network_device"
    environment: "production"
    
  processors:
  - add_host_metadata:
      when.not.contains.tags: forwarded
  - drop_event:
      when.range:
        syslog.severity:
          gte: 6    # 只保留notice及以上级别

# 安全设备日志 - TCP TLS接收
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:6514"
    max_connections: 100
    timeout: 600s
    tls:
      enabled: true
      certificate: "/etc/filebeat/certs/server.crt"
      key: "/etc/filebeat/certs/server.key"
      certificate_authorities: ["/etc/filebeat/certs/ca.crt"]
      verification_mode: "strict"
      
  parsers:
  - syslog:
      format: rfc5424
      timezone: "UTC"
      structured_data: true
      
  fields:
    log_type: "security_device"
    environment: "production"
    criticality: "high"

# 应用日志 - TCP接收
- type: syslog
  protocol.tcp:
    host: "0.0.0.0:1514"
    max_connections: 200
    timeout: 300s
    
  parsers:
  - syslog:
      format: rfc3164
      timezone: "Asia/Shanghai"
      
  fields:
    log_type: "application"
    environment: "production"

# ============================== 处理器配置 ==============================
processors:
- add_fields:
    target: "meta"
    fields:
      collector_version: "8.10.0"
      config_version: "1.0"
      
- timestamp:
    field: "@timestamp"
    layouts:
      - '2006-01-02T15:04:05.000Z'
      - '2006-01-02T15:04:05Z'
    test:
      - '2023-10-11T22:14:15.123Z'

# ============================== 输出配置 ==============================
output.elasticsearch:
  hosts: ["es-node1:9200", "es-node2:9200", "es-node3:9200"]
  protocol: "https"
  username: "filebeat_user"
  password: "${ELASTIC_PASSWORD}"
  
  # 基于日志类型的索引路由
  indices:
    - index: "security-logs-%{+yyyy.MM.dd}"
      when.equals:
        fields.log_type: "security_device"
        
    - index: "network-logs-%{+yyyy.MM.dd}"
      when.equals:
        fields.log_type: "network_device"
        
    - index: "application-logs-%{+yyyy.MM.dd}"
      when.equals:
        fields.log_type: "application"

# ============================== 日志配置 ==============================
logging.level: info
logging.to_files: true
logging.files:
  path: /var/log/filebeat
  name: filebeat.log
  keepfiles: 7
  permissions: 0644

# ============================== 监控配置 ==============================  
monitoring.enabled: true
monitoring.elasticsearch:
  hosts: ["es-node1:9200", "es-node2:9200", "es-node3:9200"]
  username: "monitoring_user"
  password: "${MONITORING_PASSWORD}"
```

### 11.2 开发测试环境配置


```yaml
##################### Filebeat Development Configuration ######################


name: "syslog-collector-dev"
tags: ["development", "syslog", "testing"]

filebeat.inputs:
# 开发环境简化配置 - 接收所有级别日志
- type: syslog
  protocol.udp:
    host: "0.0.0.0:514"
    
  parsers:
  - syslog:
      format: rfc3164
      timezone: "Asia/Shanghai"
      ignore_parse_failures: true
      
  fields:
    environment: "development"
    debug_mode: true

# 本地TCP测试
- type: syslog
  protocol.tcp:
    host: "127.0.0.1:1514"
    max_connections: 10
    
  fields:
    environment: "development"
    source: "local_test"

# 控制台输出（开发调试）
output.console:
  pretty: true
  enable: true

# 简化日志
logging.level: debug
logging.to_stderr: true
```

---

## 12. 📋 核心要点总结


### 12.1 必须掌握的基本概念


```
🔸 Syslog本质：统一的系统日志格式标准，让不同设备都能发送标准化日志
🔸 传输协议：UDP快速但可能丢失，TCP可靠但资源占用多
🔸 日志格式：RFC3164传统兼容好，RFC5424新标准功能强
🔸 安全传输：TLS加密保护敏感日志，防止网络窃听
🔸 过滤策略：根据级别和设施代码过滤，减少无用日志
```

### 12.2 关键配置要点


**🔹 端口和协议选择**
```
选择原则：
- 大量普通日志 → UDP + 514端口
- 重要安全日志 → TCP + TLS + 6514端口
- 应用程序日志 → TCP + 1514端口
- 多种设备混合 → 多端口分别接收
```

**🔹 性能优化策略**
```
网络优化：
- 增大UDP缓冲区处理突发流量
- 合理设置TCP连接数和超时
- 使用长连接减少建连开销

处理优化：
- 过滤低级别日志减少处理量
- 分类路由到不同索引便于查询
- 添加字段标识便于后续分析
```

**🔹 安全配置建议**
```
基础安全：
- 使用防火墙限制日志发送源
- 设置合理的连接数限制
- 监控异常连接和大量数据

高级安全：
- 启用TLS加密传输
- 使用客户端证书认证
- 定期更新证书和密钥
```

### 12.3 实际应用指导


**适用场景判断**：
- ✅ **网络设备监控**：路由器、交换机状态监控
- ✅ **安全事件收集**：防火墙告警、入侵检测日志
- ✅ **系统运维监控**：服务器状态、应用程序日志
- ✅ **合规性审计**：操作记录、访问日志收集

**配置选择指导**：
- 🎯 **小型环境**：UDP接收，简单过滤，单一索引
- 🎯 **中型环境**：UDP+TCP混合，级别过滤，分类索引  
- 🎯 **大型环境**：多协议多端口，TLS安全，智能路由
- 🎯 **高安全环境**：全程TLS，客户端认证，严格过滤

**故障排查要点**：
- 🔍 检查端口是否被占用（`netstat -tulpn`）
- 🔍 确认防火墙规则允许对应端口
- 🔍 查看系统UDP缓冲区是否充足
- 🔍 验证TLS证书配置是否正确
- 🔍 监控连接数是否达到上限

**核心记忆**：
- Syslog是日志标准格式，Filebeat是日志收集工具
- UDP速度快可能丢，TCP可靠占资源，TLS安全有保障
- 端口分类接收好管理，级别过滤减负担，索引路由便查询
- 缓冲区大小要合适，连接数量要控制，安全配置不能省