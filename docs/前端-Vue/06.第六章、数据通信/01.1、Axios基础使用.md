---
title: 1、Axios基础使用
---
## 📚 目录

1. [Axios是什么](#1-axios是什么)
2. [安装与基本配置](#2-安装与基本配置)
3. [基础HTTP请求](#3-基础http请求)
4. [请求配置详解](#4-请求配置详解)
5. [响应数据处理](#5-响应数据处理)
6. [错误处理机制](#6-错误处理机制)
7. [实战应用案例](#7-实战应用案例)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🌐 Axios是什么


### 1.1 基本概念


**Axios**是一个专门用来发送HTTP请求的JavaScript库，可以理解为一个**网络请求工具**。

```
简单理解：
前端页面 → Axios → 后端服务器
就像：顾客 → 服务员 → 厨房

顾客(前端)想要菜品(数据)，通过服务员(Axios)向厨房(后端)下单
```

### 1.2 为什么选择Axios


**🔸 相比原生fetch的优势**
```
原生fetch写法复杂：
fetch('/api/users')
  .then(response => response.json())  // 需要手动转换JSON
  .then(data => console.log(data))

Axios写法简洁：
axios.get('/api/users')
  .then(response => console.log(response.data))  // 自动处理JSON
```

**🔸 核心特点**
- ✅ **自动JSON处理**：自动转换请求和响应数据
- ✅ **拦截器功能**：可以统一处理请求和响应
- ✅ **错误处理**：更友好的错误信息
- ✅ **取消请求**：可以中途取消网络请求
- ✅ **兼容性好**：支持各种浏览器

---

## 2. 🔧 安装与基本配置


### 2.1 安装Axios


**📦 NPM安装方式**
```bash
# 项目中安装
npm install axios

# 或者使用yarn
yarn add axios
```

**📁 在Vue项目中引入**
```javascript
// main.js中全局引入
import axios from 'axios'
import { createApp } from 'vue'
import App from './App.vue'

const app = createApp(App)

// 挂载到全局，方便组件中使用
app.config.globalProperties.$axios = axios

app.mount('#app')
```

### 2.2 基本配置


**🔸 创建Axios实例**
```javascript
// api/index.js - 推荐的配置方式
import axios from 'axios'

// 创建专门的axios实例
const api = axios.create({
  baseURL: 'http://localhost:3000',  // 服务器地址
  timeout: 5000,                    // 超时时间5秒
  headers: {
    'Content-Type': 'application/json'
  }
})

export default api
```

**💡 配置说明**
```
baseURL: 基础URL地址
└── 好处：后续请求只需写相对路径
└── 示例：baseURL设置后，'/users' 等同于 'http://localhost:3000/users'

timeout: 超时时间(毫秒)
└── 作用：防止请求时间过长，影响用户体验
└── 建议：一般设置3-10秒

headers: 请求头
└── 作用：告诉服务器我们发送的数据格式
└── 常用：application/json(JSON格式)
```

---

## 3. 🚀 基础HTTP请求


### 3.1 GET请求 - 获取数据


**🔸 基本用法**
```javascript
// 获取用户列表
async getUserList() {
  try {
    const response = await axios.get('/api/users')
    console.log(response.data)  // 服务器返回的数据
  } catch (error) {
    console.log('请求失败:', error)
  }
}
```

**🔸 带参数的GET请求**
```javascript
// 方式1：URL拼接参数
axios.get('/api/users?page=1&size=10')

// 方式2：使用params配置(推荐)
axios.get('/api/users', {
  params: {
    page: 1,
    size: 10,
    keyword: '张三'
  }
})

// 最终请求地址：/api/users?page=1&size=10&keyword=张三
```

### 3.2 POST请求 - 发送数据


**🔸 创建新用户**
```javascript
// 添加用户
async addUser() {
  const newUser = {
    name: '李四',
    age: 25,
    email: 'lisi@example.com'
  }
  
  try {
    const response = await axios.post('/api/users', newUser)
    console.log('添加成功:', response.data)
  } catch (error) {
    console.log('添加失败:', error)
  }
}
```

### 3.3 PUT请求 - 更新数据


**🔸 更新用户信息**
```javascript
// 更新ID为123的用户
async updateUser(userId) {
  const updateData = {
    name: '王五',
    age: 30
  }
  
  try {
    const response = await axios.put(`/api/users/${userId}`, updateData)
    console.log('更新成功:', response.data)
  } catch (error) {
    console.log('更新失败:', error)
  }
}
```

### 3.4 DELETE请求 - 删除数据


**🔸 删除用户**
```javascript
// 删除指定用户
async deleteUser(userId) {
  try {
    const response = await axios.delete(`/api/users/${userId}`)
    console.log('删除成功')
  } catch (error) {
    console.log('删除失败:', error)
  }
}
```

### 3.5 HTTP方法对比表


| 方法 | **用途** | **数据位置** | **示例场景** |
|------|---------|-------------|-------------|
| 🔍 **GET** | `获取数据` | `URL参数` | `查看用户列表、商品详情` |
| 📝 **POST** | `创建数据` | `请求体` | `注册用户、发表评论` |
| ✏️ **PUT** | `更新数据` | `请求体` | `修改个人信息、编辑文章` |
| 🗑️ **DELETE** | `删除数据` | `URL参数` | `删除评论、注销账户` |

---

## 4. ⚙️ 请求配置详解


### 4.1 请求参数详解


**🔸 GET请求参数**
```javascript
// 查询参数 - 显示在URL中
axios.get('/api/search', {
  params: {
    q: 'Vue教程',      // 搜索关键字
    page: 1,          // 页码
    category: 'tech'  // 分类
  }
})
// 实际请求：/api/search?q=Vue教程&page=1&category=tech
```

**🔸 POST请求体**
```javascript
// JSON格式数据
axios.post('/api/users', {
  name: '张三',
  email: 'zhangsan@example.com',
  password: '123456'
})

// 表单格式数据
const formData = new FormData()
formData.append('name', '张三')
formData.append('avatar', file)  // 文件上传

axios.post('/api/upload', formData, {
  headers: {
    'Content-Type': 'multipart/form-data'
  }
})
```

### 4.2 请求头设置


**🔸 常用请求头**
```javascript
// 设置认证信息
axios.get('/api/profile', {
  headers: {
    'Authorization': 'Bearer your-token-here',
    'User-Agent': 'MyApp/1.0'
  }
})

// 全局设置默认请求头
axios.defaults.headers.common['Authorization'] = 'Bearer token'
axios.defaults.headers.post['Content-Type'] = 'application/json'
```

### 4.3 baseURL和timeout配置


**🔸 统一配置**
```javascript
// 创建配置好的axios实例
const api = axios.create({
  baseURL: process.env.VUE_APP_API_URL || 'http://localhost:3000',
  timeout: 10000,  // 10秒超时
  headers: {
    'Content-Type': 'application/json'
  }
})

// 使用配置好的实例
api.get('/users')        // 实际请求：http://localhost:3000/users
api.post('/users', data) // 实际请求：http://localhost:3000/users
```

**💡 配置的好处**
```
集中管理：
└── 所有API请求统一配置
└── 修改服务器地址时只需改一处

环境切换：
└── 开发环境：http://localhost:3000
└── 生产环境：https://api.yoursite.com
```

---

## 5. 📊 响应数据处理


### 5.1 响应对象结构


**🔸 完整的响应对象**
```javascript
axios.get('/api/users/123').then(response => {
  console.log(response)
  
  // 响应对象包含：
  // {
  //   data: {},           // 服务器返回的实际数据
  //   status: 200,        // HTTP状态码
  //   statusText: 'OK',   // 状态描述
  //   headers: {},        // 响应头
  //   config: {},         // 请求配置
  //   request: {}         // 请求对象
  // }
})
```

### 5.2 提取响应数据


**🔸 获取实际数据**
```javascript
// 通常我们只需要data部分
async getUser(id) {
  try {
    const response = await axios.get(`/api/users/${id}`)
    
    // 方式1：完整写法
    const userData = response.data
    
    // 方式2：解构赋值(推荐)
    const { data } = response
    
    // 方式3：直接解构到具体字段
    const { data: { name, email, age } } = response
    
    return userData
  } catch (error) {
    console.log('获取用户失败:', error)
  }
}
```

### 5.3 处理不同格式的响应


**🔸 常见响应格式**
```javascript
// 格式1：直接返回数据
{
  "name": "张三",
  "age": 25
}

// 格式2：包装后的响应
{
  "code": 200,
  "message": "成功",
  "data": {
    "name": "张三",
    "age": 25
  }
}

// 处理包装格式
async getUser(id) {
  const { data } = await axios.get(`/api/users/${id}`)
  
  if (data.code === 200) {
    return data.data  // 返回实际的用户数据
  } else {
    throw new Error(data.message)
  }
}
```

---

## 6. 🚨 错误处理机制


### 6.1 错误类型


**🔸 网络错误分类**
```
HTTP状态码错误：
├── 4xx：客户端错误
│   ├── 400：请求参数错误
│   ├── 401：未授权
│   ├── 403：禁止访问
│   └── 404：资源不存在
└── 5xx：服务器错误
    ├── 500：服务器内部错误
    └── 503：服务不可用

网络连接错误：
├── 超时：请求时间过长
├── 网络断开：无法连接服务器
└── CORS错误：跨域请求被阻止
```

### 6.2 基本错误处理


**🔸 try-catch方式**
```javascript
async fetchUserData() {
  try {
    const { data } = await axios.get('/api/users')
    this.users = data
  } catch (error) {
    // 处理各种错误情况
    if (error.response) {
      // 服务器响应了错误状态码
      const status = error.response.status
      const message = error.response.data.message || '请求失败'
      
      if (status === 401) {
        this.$message.error('请先登录')
        this.$router.push('/login')
      } else if (status === 403) {
        this.$message.error('没有权限访问')
      } else if (status >= 500) {
        this.$message.error('服务器错误，请稍后重试')
      } else {
        this.$message.error(message)
      }
    } else if (error.request) {
      // 网络错误
      this.$message.error('网络连接失败，请检查网络')
    } else {
      // 其他错误
      this.$message.error('请求配置错误')
    }
  }
}
```

### 6.3 Promise方式错误处理


**🔸 .catch()方法**
```javascript
// Promise链式调用
axios.get('/api/users')
  .then(response => {
    // 成功处理
    console.log(response.data)
  })
  .catch(error => {
    // 错误处理
    console.log('请求失败:', error.message)
  })
  .finally(() => {
    // 无论成功失败都会执行
    console.log('请求完成')
  })
```

### 6.4 统一错误处理


**🔸 创建错误处理函数**
```javascript
// utils/errorHandler.js
export function handleApiError(error) {
  let message = '请求失败'
  
  if (error.response) {
    const { status, data } = error.response
    
    switch (status) {
      case 400:
        message = data.message || '请求参数错误'
        break
      case 401:
        message = '请先登录'
        // 跳转到登录页
        break
      case 403:
        message = '没有访问权限'
        break
      case 404:
        message = '请求的资源不存在'
        break
      case 500:
        message = '服务器错误，请稍后重试'
        break
      default:
        message = data.message || `请求失败(${status})`
    }
  } else if (error.request) {
    message = '网络连接失败'
  }
  
  return message
}

// 使用统一错误处理
async getUsers() {
  try {
    const { data } = await axios.get('/api/users')
    return data
  } catch (error) {
    const errorMessage = handleApiError(error)
    this.$message.error(errorMessage)
    throw error
  }
}
```

---

## 7. 💼 实战应用案例


### 7.1 用户管理API封装


**🔸 创建API模块**
```javascript
// api/user.js
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:3000/api',
  timeout: 5000
})

export const userApi = {
  // 获取用户列表
  getUsers(params = {}) {
    return api.get('/users', { params })
  },
  
  // 获取单个用户
  getUserById(id) {
    return api.get(`/users/${id}`)
  },
  
  // 创建用户
  createUser(userData) {
    return api.post('/users', userData)
  },
  
  // 更新用户
  updateUser(id, userData) {
    return api.put(`/users/${id}`, userData)
  },
  
  // 删除用户
  deleteUser(id) {
    return api.delete(`/users/${id}`)
  }
}
```

### 7.2 Vue组件中使用


**🔸 用户列表组件**
```vue
<template>
  <div class="user-list">
    <div v-if="loading">加载中...</div>
    <div v-else>
      <div v-for="user in users" :key="user.id" class="user-item">
        <span>{{ user.name }}</span>
        <span>{{ user.email }}</span>
        <button @click="deleteUser(user.id)">删除</button>
      </div>
    </div>
  </div>
</template>

<script>
import { userApi } from '@/api/user'

export default {
  name: 'UserList',
  data() {
    return {
      users: [],
      loading: false
    }
  },
  
  async created() {
    await this.loadUsers()
  },
  
  methods: {
    // 加载用户列表
    async loadUsers() {
      this.loading = true
      try {
        const { data } = await userApi.getUsers()
        this.users = data
      } catch (error) {
        this.$message.error('加载用户列表失败')
      } finally {
        this.loading = false
      }
    },
    
    // 删除用户
    async deleteUser(id) {
      try {
        await userApi.deleteUser(id)
        this.$message.success('删除成功')
        // 重新加载列表
        await this.loadUsers()
      } catch (error) {
        this.$message.error('删除失败')
      }
    }
  }
}
</script>
```

### 7.3 加载状态与用户体验


**🔸 改善用户体验**
```vue
<template>
  <div class="user-management">
    <!-- 加载状态 -->
    <div v-if="loading" class="loading">
      <span>正在加载...</span>
    </div>
    
    <!-- 错误状态 -->
    <div v-else-if="error" class="error">
      <span>{{ error }}</span>
      <button @click="retry">重试</button>
    </div>
    
    <!-- 正常内容 -->
    <div v-else class="content">
      <!-- 用户列表 -->
    </div>
  </div>
</template>

<script>
export default {
  data() {
    return {
      users: [],
      loading: false,
      error: null
    }
  },
  
  methods: {
    async loadData() {
      this.loading = true
      this.error = null
      
      try {
        const { data } = await userApi.getUsers()
        this.users = data
      } catch (error) {
        this.error = '加载失败，请稍后重试'
      } finally {
        this.loading = false
      }
    },
    
    retry() {
      this.loadData()
    }
  }
}
</script>
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的基本概念


```
🔸 Axios本质：专门用来发送HTTP请求的工具库
🔸 四种请求：GET获取、POST创建、PUT更新、DELETE删除
🔸 请求配置：baseURL基础地址、timeout超时、headers请求头
🔸 响应处理：从response.data中获取实际数据
🔸 错误处理：区分网络错误和HTTP状态码错误
```

### 8.2 关键理解要点


**🔹 请求方法的选择**
```
什么时候用GET：
├── 查看用户列表
├── 获取商品详情
└── 搜索功能

什么时候用POST：
├── 用户注册
├── 发表评论
└── 上传文件

什么时候用PUT：
├── 修改个人资料
├── 更新文章内容
└── 编辑商品信息

什么时候用DELETE：
├── 删除评论
├── 移除收藏
└── 注销账户
```

**🔹 错误处理的重要性**
```
用户体验角度：
├── 网络错误：提示"网络连接失败"
├── 401错误：自动跳转登录页
├── 404错误：提示"内容不存在"
└── 500错误：提示"服务器繁忙"

开发调试角度：
├── 打印详细错误信息
├── 记录错误日志
└── 便于问题排查
```

### 8.3 实际应用建议


**🔹 项目组织结构**
```
src/
├── api/           # API接口封装
│   ├── user.js    # 用户相关接口
│   ├── product.js # 商品相关接口
│   └── index.js   # axios配置
├── utils/
│   └── request.js # 请求拦截器
└── components/    # Vue组件
```

**🔹 最佳实践**
- ✅ **统一配置**：创建axios实例，集中管理配置
- ✅ **接口封装**：按功能模块封装API方法
- ✅ **错误处理**：提供友好的错误提示
- ✅ **加载状态**：显示loading，改善用户体验
- ✅ **参数验证**：发送请求前验证必要参数

**核心记忆**：
- Axios是发请求的工具，GET取数据，POST送数据
- 配置baseURL省事省力，错误处理用户体验好
- 响应取data就够了，async/await写法更清晰