---
title: 4、组件复用与逻辑抽离
---
## 📚 目录

1. [什么是组件复用与逻辑抽离](#1-什么是组件复用与逻辑抽离)
2. [mixins混入的使用](#2-mixins混入的使用)
3. [Composables组合式函数](#3-composables组合式函数)
4. [Composition API基础](#4-composition-api基础)
5. [Hooks概念与实践](#5-hooks概念与实践)
6. [公共组件设计原则](#6-公共组件设计原则)
7. [最佳实践与策略](#7-最佳实践与策略)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔄 什么是组件复用与逻辑抽离


### 1.1 基本概念理解


**🔸 组件复用**：就像搭积木一样，把做好的积木块重复使用
- 一次编写，多处使用
- 减少重复代码
- 提高开发效率

**🔸 逻辑抽离**：把相同的功能代码提取出来，放在一个地方管理
- 相同逻辑只写一遍
- 便于维护和修改
- 代码更加清晰

🌰 **生活类比**：就像厨房里的调料包，调料配好后可以做很多道菜，不用每次都重新配制

### 1.2 为什么需要复用与抽离


**🎯 解决的问题**：
```
❌ 没有复用前：
- 每个页面都写相同的加载逻辑
- 每个表单都重复验证代码  
- 相同功能在多处维护

✅ 复用抽离后：
- 写一次加载逻辑，到处使用
- 统一的表单验证方法
- 功能修改只需改一个地方
```

### 1.3 常见的复用场景


**📋 实际开发中的复用需求**：
- 🔸 **数据请求逻辑**：获取用户信息、商品列表等
- 🔸 **表单验证功能**：邮箱验证、密码强度检查等
- 🔸 **UI交互效果**：弹窗、提示框、加载动画等
- 🔸 **工具方法**：日期格式化、文件上传等

---

## 2. 🔧 mixins混入的使用


### 2.1 什么是mixins


**mixins**：就像调料包，可以把一些功能"混入"到组件中

🌰 **简单理解**：mixins就是把一些通用的功能打包，然后"倒入"到需要的组件里

### 2.2 基础使用示例


**🔸 创建一个通用的加载mixin**：
```javascript
// mixins/loading.js
export const loadingMixin = {
  data() {
    return {
      loading: false
    }
  },
  methods: {
    showLoading() {
      this.loading = true
    },
    hideLoading() {
      this.loading = false
    }
  }
}
```

**🔸 在组件中使用**：
```javascript
// 在组件中使用
export default {
  mixins: [loadingMixin],
  methods: {
    async fetchData() {
      this.showLoading()  // 来自mixin
      try {
        // 获取数据...
      } finally {
        this.hideLoading()  // 来自mixin
      }
    }
  }
}
```

### 2.3 mixins的优缺点


**✅ 优点**：
- 使用简单，直接混入即可
- 可以共享data、methods、生命周期等
- Vue2项目中广泛使用

**❌ 缺点**：
- 命名冲突：mixin和组件有相同名称时会混乱
- 来源不明：不知道某个方法来自哪个mixin
- 维护困难：多个mixin叠加使用时复杂

> ⚠️ **注意事项**  
> Vue3中推荐使用Composition API替代mixins，因为它解决了mixins的这些问题

---

## 3. 🎯 Composables组合式函数


### 3.1 什么是Composables


**Composables**：组合式函数，就是把功能逻辑封装成函数，需要时就调用

🌰 **生活类比**：就像工具箱里的各种工具，需要螺丝刀就拿螺丝刀，需要锤子就拿锤子

### 3.2 创建你的第一个Composable


**🔸 创建一个计数器功能**：
```javascript
// composables/useCounter.js
import { ref } from 'vue'

export function useCounter(initialValue = 0) {
  const count = ref(initialValue)
  
  const increment = () => count.value++
  const decrement = () => count.value--
  const reset = () => count.value = initialValue
  
  return {
    count,
    increment,
    decrement,
    reset
  }
}
```

**🔸 在组件中使用**：
```javascript
<template>
  <div>
    <p>计数：{{ count }}</p>
    <button @click="increment">增加</button>
    <button @click="decrement">减少</button>
    <button @click="reset">重置</button>
  </div>
</template>

<script setup>
import { useCounter } from '@/composables/useCounter'

const { count, increment, decrement, reset } = useCounter(10)
</script>
```

### 3.3 实用的Composables示例


**🔸 数据请求Composable**：
```javascript
// composables/useFetch.js
import { ref } from 'vue'

export function useFetch(url) {
  const data = ref(null)
  const loading = ref(false)
  const error = ref(null)
  
  const fetchData = async () => {
    loading.value = true
    error.value = null
    
    try {
      const response = await fetch(url)
      data.value = await response.json()
    } catch (err) {
      error.value = err.message
    } finally {
      loading.value = false
    }
  }
  
  return { data, loading, error, fetchData }
}
```

**🔸 在组件中使用**：
```javascript
<script setup>
import { useFetch } from '@/composables/useFetch'

const { data, loading, error, fetchData } = useFetch('/api/users')

// 组件挂载时获取数据
fetchData()
</script>
```

---

## 4. 🚀 Composition API基础


### 4.1 什么是Composition API


**Composition API**：Vue3新增的编程方式，让代码组织更灵活

🌰 **简单对比**：
```
传统Options API：    Composition API：
像填表格             像写自由作文
固定格式             灵活组织
分散相关逻辑         相关逻辑放一起
```

### 4.2 基本使用方式


**🔸 setup函数方式**：
```javascript
export default {
  setup() {
    const message = ref('Hello Vue3')
    
    const updateMessage = () => {
      message.value = 'Updated!'
    }
    
    return {
      message,
      updateMessage
    }
  }
}
```

**🔸 script setup方式（推荐）**：
```javascript
<script setup>
import { ref } from 'vue'

const message = ref('Hello Vue3')

const updateMessage = () => {
  message.value = 'Updated!'
}
</script>
```

### 4.3 响应式数据创建


**🔸 ref：基本数据类型**：
```javascript
const count = ref(0)        // 数字
const name = ref('张三')     // 字符串
const visible = ref(false)  // 布尔值

// 使用时需要.value
count.value = 10
```

**🔸 reactive：对象类型**：
```javascript
const user = reactive({
  name: '张三',
  age: 25,
  hobbies: ['读书', '游戏']
})

// 直接使用，不需要.value
user.name = '李四'
user.age = 30
```

> 💡 **记忆技巧**  
> ref像包装盒，需要打开(.value)才能用  
> reactive像透明盒，直接就能看到和使用

---

## 5. 🎪 Hooks概念与实践


### 5.1 什么是Hooks


**Hooks**：钩子函数，就是在特定时机执行的函数

🌰 **生活类比**：就像闹钟，设定好时间到了就响，Hooks就是设定好某个时机到了就执行

### 5.2 Vue中的Hooks


**🔸 生命周期Hooks**：
```javascript
<script setup>
import { onMounted, onUnmounted } from 'vue'

// 组件挂载时执行
onMounted(() => {
  console.log('组件挂载了')
})

// 组件卸载时执行
onUnmounted(() => {
  console.log('组件要卸载了')
})
</script>
```

**🔸 自定义Hooks示例**：
```javascript
// composables/useLocalStorage.js
import { ref, watch } from 'vue'

export function useLocalStorage(key, defaultValue) {
  // 从本地存储读取
  const storedValue = localStorage.getItem(key)
  const value = ref(storedValue ? JSON.parse(storedValue) : defaultValue)
  
  // 监听变化，自动保存
  watch(value, (newValue) => {
    localStorage.setItem(key, JSON.stringify(newValue))
  }, { deep: true })
  
  return value
}
```

**🔸 使用自定义Hook**：
```javascript
<script setup>
import { useLocalStorage } from '@/composables/useLocalStorage'

// 自动从localStorage读取和保存
const theme = useLocalStorage('theme', 'light')
const userSettings = useLocalStorage('userSettings', {})
</script>
```

---

## 6. 🏗️ 公共组件设计原则


### 6.1 设计思路


**🎯 设计目标**：
- 一次开发，多处使用
- 配置灵活，适应不同场景
- 易于理解和维护

### 6.2 组件设计要点


**🔸 属性设计**：
```javascript
// 好的按钮组件设计
<template>
  <button 
    :class="buttonClass" 
    :disabled="disabled"
    @click="handleClick"
  >
    <slot>{{ text }}</slot>
  </button>
</template>

<script setup>
defineProps({
  type: {
    type: String,
    default: 'default',
    validator: value => ['default', 'primary', 'danger'].includes(value)
  },
  size: {
    type: String,
    default: 'medium'
  },
  disabled: {
    type: Boolean,
    default: false
  },
  text: String
})

const emit = defineEmits(['click'])

const handleClick = (event) => {
  emit('click', event)
}
</script>
```

**🔸 插槽使用**：
```javascript
<!-- 支持自定义内容 -->
<MyButton type="primary">
  <Icon name="plus" /> 添加用户
</MyButton>

<!-- 使用默认文字 -->
<MyButton text="保存" />
```

### 6.3 组件分类策略


```
🏗️ 组件分层结构：

基础组件层（原子组件）
├── Button（按钮）
├── Input（输入框）  
├── Icon（图标）
└── Loading（加载）

业务组件层（分子组件）
├── UserCard（用户卡片）
├── ProductItem（商品项）
└── SearchBar（搜索栏）

页面组件层（有机体组件）
├── UserList（用户列表）
├── ProductGrid（商品网格）
└── Header（页面头部）
```

---

## 7. 🎯 最佳实践与策略


### 7.1 何时使用哪种方式


**🔸 选择指南**：

| 场景 | **推荐方案** | **原因** |
|------|------------|---------|
| `简单工具函数` | **普通函数** | `无需响应式，简单直接` |
| `有状态的逻辑` | **Composables** | `响应式，易测试，逻辑清晰` |
| `UI组件` | **独立组件** | `视觉复用，props配置` |
| `Vue2项目` | **mixins** | `兼容性考虑` |

### 7.2 命名规范


**🔸 Composables命名**：
```javascript
// ✅ 好的命名
useCounter()     // 计数器功能
useFetch()       // 数据请求功能  
useForm()        // 表单处理功能
useLocalStorage() // 本地存储功能

// ❌ 避免的命名
counter()        // 不明确是组件还是函数
getData()        // 太泛泛
helper()         // 不知道做什么
```

**🔸 组件命名**：
```javascript
// ✅ 基础组件
BaseButton.vue
BaseInput.vue
BaseModal.vue

// ✅ 业务组件  
UserProfile.vue
ProductCard.vue
OrderList.vue
```

### 7.3 文件组织结构


```
src/
├── components/           # 组件目录
│   ├── base/            # 基础组件
│   ├── business/        # 业务组件
│   └── layout/          # 布局组件
├── composables/         # 组合式函数
│   ├── useAuth.js       # 认证相关
│   ├── useApi.js        # API相关
│   └── useUtils.js      # 工具相关
├── mixins/              # mixins（Vue2）
└── utils/               # 普通工具函数
```

### 7.4 实际开发建议


**🔸 渐进式重构**：
```
第一步：识别重复代码
第二步：提取为函数
第三步：改造为Composable
第四步：优化和测试
```

**🔸 测试策略**：
```javascript
// composables/useCounter.test.js
import { useCounter } from './useCounter'

test('计数器基本功能', () => {
  const { count, increment, decrement } = useCounter(0)
  
  expect(count.value).toBe(0)
  
  increment()
  expect(count.value).toBe(1)
  
  decrement()
  expect(count.value).toBe(0)
})
```

---

## 8. 📋 核心要点总结


### 8.1 关键概念理解


**🔸 复用的本质**：
- 减少重复代码编写
- 提高开发效率
- 便于维护和修改
- 保证功能一致性

**🔸 抽离的价值**：
- 逻辑集中管理
- 易于测试验证
- 代码结构清晰
- 团队协作友好

### 8.2 技术选择建议


```
🎯 Vue3项目推荐路径：

Composables ✅
    ↓
独立组件 ✅  
    ↓
普通函数 ✅
    ↓
mixins ⚠️（仅兼容时使用）
```

### 8.3 开发流程建议


**🔸 设计阶段**：
1. 📋 **需求分析**：明确复用场景和范围
2. 🎯 **接口设计**：定义清晰的API和参数
3. 🧪 **原型验证**：小范围测试可行性

**🔸 实现阶段**：
1. 🏗️ **功能开发**：实现核心逻辑
2. 📝 **文档编写**：说明使用方法和注意事项
3. 🧪 **单元测试**：保证功能正确性

**🔸 应用阶段**：
1. 🚀 **推广使用**：在项目中应用
2. 🔍 **收集反馈**：根据使用情况改进
3. 🔄 **持续优化**：定期更新和维护

### 8.4 注意事项提醒


> ⚠️ **避免过度抽离**  
> 不是所有代码都需要抽离，简单的一次性逻辑直接写在组件中即可

> 💡 **保持简单原则**  
> 复用组件要易于理解，不要为了灵活性而牺牲简洁性

> 🔍 **关注性能影响**  
> 抽离后要注意是否影响性能，避免不必要的计算和渲染

**🧠 记忆要点**：
- Composables是Vue3的主力复用方案
- 组件复用重在接口设计和配置灵活性
- 逻辑抽离要平衡复用性和简洁性
- 命名规范和文件组织很重要
- 渐进式重构，不要一步到位