---
title: 2、项目目录与模块划分
---
## 📚 目录


1. [项目目录结构基础](#1-项目目录结构基础)
2. [核心目录详解](#2-核心目录详解)
3. [命名规范与约定](#3-命名规范与约定)
4. [模块划分策略](#4-模块划分策略)
5. [目录组织最佳实践](#5-目录组织最佳实践)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🏗️ 项目目录结构基础



### 1.1 什么是项目目录结构



**简单理解**：就像整理房间一样，把不同类型的东西放到不同的地方

```
把家里的东西分类整理：
客厅 → 放沙发、电视（展示用的）
卧室 → 放床、衣柜（私人用的）  
厨房 → 放锅碗瓢盆（功能用的）
书房 → 放书、电脑（工作用的）

Vue项目也是一样：
views → 放页面（用户看到的界面）
components → 放组件（可重复使用的零件）
assets → 放静态资源（图片、样式等）
router → 放路由（页面跳转规则）
```

### 1.2 为什么需要良好的目录结构



**🎯 核心作用**：
- **易于查找**：知道什么东西放在哪里
- **便于维护**：修改某个功能时快速定位
- **团队协作**：大家都按同样的规则工作
- **项目扩展**：新增功能时有地方放

> 💡 **生活类比**：就像超市的商品分类，生鲜区、日用品区、服装区，你总能快速找到要买的东西

### 1.3 标准Vue项目目录结构



```
my-vue-project/
├── public/                  # 静态资源（不会被webpack处理）
│   ├── index.html          # 入口HTML文件
│   └── favicon.ico         # 网站图标
├── src/                    # 源代码目录
│   ├── assets/             # 静态资源（会被webpack处理）
│   ├── components/         # 公共组件
│   ├── views/              # 页面组件
│   ├── router/             # 路由配置
│   ├── store/              # 状态管理
│   ├── utils/              # 工具函数
│   ├── api/                # 接口请求
│   ├── App.vue            # 根组件
│   └── main.js            # 入口文件
├── package.json           # 项目配置
└── vue.config.js         # Vue CLI配置
```

---

## 2. 📁 核心目录详解



### 2.1 components 目录 - 组件仓库



**🔸 作用说明**：存放可重复使用的组件，就像是"零件库"

```
src/components/
├── common/                 # 通用组件
│   ├── Header.vue         # 页面头部
│   ├── Footer.vue         # 页面底部
│   └── Loading.vue        # 加载动画
├── form/                  # 表单相关组件
│   ├── MyInput.vue        # 自定义输入框
│   └── MyButton.vue       # 自定义按钮
└── business/              # 业务组件
    ├── UserCard.vue       # 用户卡片
    └── ProductList.vue    # 商品列表
```

**💡 什么放在 components**：
- 在多个页面都会用到的组件
- 可以独立使用的功能模块
- 与具体业务逻辑解耦的通用组件

```vue
<!-- 典型的 components 组件 -->
<template>
  <button :class="type" @click="handleClick">
    <slot></slot>
  </button>
</template>

<script>
export default {
  name: 'MyButton',
  props: {
    type: { type: String, default: 'primary' }
  }
}
</script>
```

### 2.2 views 目录 - 页面集合



**🔸 作用说明**：存放页面级组件，每个文件对应一个路由页面

```
src/views/
├── Home/                  # 首页模块
│   ├── index.vue         # 首页主组件
│   └── components/       # 首页专用组件
│       └── Banner.vue    # 首页轮播图
├── User/                 # 用户模块
│   ├── Profile.vue       # 用户资料页
│   ├── Settings.vue      # 设置页
│   └── components/       # 用户模块专用组件
└── Product/              # 商品模块
    ├── List.vue          # 商品列表页
    └── Detail.vue        # 商品详情页
```

**💡 什么放在 views**：
- 对应路由的页面组件
- 页面级的业务逻辑
- 只在特定页面使用的组件

```vue
<!-- 典型的 views 组件 -->
<template>
  <div class="home">
    <Header />
    <Banner />
    <ProductList :products="products" />
    <Footer />
  </div>
</template>

<script>
import Header from '@/components/common/Header.vue'
import Banner from './components/Banner.vue'

export default {
  name: 'Home',
  components: { Header, Banner }
}
</script>
```

### 2.3 assets 目录 - 静态资源库



**🔸 作用说明**：存放项目中使用的静态资源文件

```
src/assets/
├── images/               # 图片资源
│   ├── logo.png         # 网站Logo
│   ├── icons/           # 图标
│   └── avatars/         # 头像
├── styles/              # 样式文件
│   ├── index.css        # 全局样式
│   ├── variables.css    # CSS变量
│   └── components.css   # 组件样式
├── fonts/               # 字体文件
└── data/                # 静态数据
    └── config.json      # 配置数据
```

**💡 assets vs public 的区别**：
```
assets 文件：
- 会被 webpack 处理（压缩、合并）
- 可以在代码中 import 引入
- 路径可能会被修改

public 文件：
- 不会被 webpack 处理
- 直接复制到输出目录
- 保持原始路径不变
```

### 2.4 router 目录 - 路由配置



**🔸 作用说明**：管理页面间的跳转规则

```
src/router/
├── index.js             # 主路由文件
└── modules/             # 路由模块
    ├── user.js          # 用户相关路由
    ├── product.js       # 商品相关路由
    └── admin.js         # 管理后台路由
```

```javascript
// router/index.js - 简化示例
import { createRouter, createWebHistory } from 'vue-router'
import Home from '@/views/Home/index.vue'

const routes = [
  { path: '/', component: Home },
  { path: '/user', component: () => import('@/views/User/Profile.vue') }
]

export default createRouter({
  history: createWebHistory(),
  routes
})
```

### 2.5 store 目录 - 状态管理



**🔸 作用说明**：管理应用的全局状态数据

```
src/store/
├── index.js             # store 入口
└── modules/             # 状态模块
    ├── user.js          # 用户状态
    ├── cart.js          # 购物车状态
    └── product.js       # 商品状态
```

```javascript
// store/modules/user.js - 简化示例
export default {
  state: {
    userInfo: null,
    isLogin: false
  },
  mutations: {
    SET_USER_INFO(state, userInfo) {
      state.userInfo = userInfo
      state.isLogin = true
    }
  }
}
```

---

## 3. 📝 命名规范与约定



### 3.1 文件命名规范



**🎯 组件文件命名**：
```
✅ 推荐的命名方式：
PascalCase（大驼峰）：UserProfile.vue
kebab-case（短横线）：user-profile.vue

❌ 避免的命名：
userprofile.vue（全小写）
userProfile.vue（小驼峰，容易混淆）
User_Profile.vue（下划线分隔）
```

**🎯 目录命名规范**：
```
✅ 推荐：
views/User/           # 大驼峰
components/common/    # 全小写
api/user/            # 全小写

❌ 避免：
views/user/          # 页面目录建议大写
components/Common/   # 通用目录建议小写
```

### 3.2 目录结构命名约定



| 目录类型 | **命名方式** | **示例** | **说明** |
|---------|------------|---------|---------|
| **页面目录** | `PascalCase` | `User/`, `ProductDetail/` | `页面级目录首字母大写` |
| **组件目录** | `lowercase` | `common/`, `form/` | `组件分类目录小写` |
| **功能目录** | `lowercase` | `api/`, `utils/`, `store/` | `功能性目录小写` |
| **文件名** | `PascalCase` | `UserProfile.vue` | `Vue组件文件大驼峰` |

### 3.3 import 路径约定



```javascript
// ✅ 推荐的导入方式
import UserProfile from '@/views/User/Profile.vue'
import MyButton from '@/components/form/MyButton.vue'
import { getUserInfo } from '@/api/user'

// ❌ 避免的相对路径
import UserProfile from '../../../views/User/Profile.vue'
import MyButton from '../../components/form/MyButton.vue'
```

> 💡 **@符号的含义**：`@` 是路径别名，通常指向 `src` 目录，让路径更简洁清晰

---

## 4. 🔀 模块划分策略



### 4.1 按功能划分 vs 按页面划分



**📊 两种策略对比**：

```
按功能划分（推荐小项目）：
src/
├── components/
│   ├── user/           # 用户相关组件
│   ├── product/        # 商品相关组件
│   └── order/          # 订单相关组件
├── views/
├── api/
│   ├── user.js         # 用户接口
│   ├── product.js      # 商品接口
│   └── order.js        # 订单接口

按页面划分（推荐大项目）：
src/
├── modules/
│   ├── user/           # 用户模块
│   │   ├── views/      # 用户页面
│   │   ├── components/ # 用户组件
│   │   ├── api/        # 用户接口
│   │   └── store/      # 用户状态
│   └── product/        # 商品模块
│       ├── views/
│       ├── components/
│       └── api/
```

### 4.2 按页面模块拆分实践



**🏗️ 大型项目模块化结构**：

```
src/
├── modules/
│   ├── dashboard/           # 仪表盘模块
│   │   ├── views/
│   │   │   ├── Overview.vue
│   │   │   └── Analytics.vue
│   │   ├── components/
│   │   │   ├── Chart.vue
│   │   │   └── DataCard.vue
│   │   ├── api/
│   │   │   └── dashboard.js
│   │   └── store/
│   │       └── dashboard.js
│   │
│   ├── user/                # 用户管理模块
│   │   ├── views/
│   │   │   ├── UserList.vue
│   │   │   └── UserDetail.vue
│   │   ├── components/
│   │   │   └── UserForm.vue
│   │   └── api/
│   │       └── user.js
│   │
│   └── product/             # 商品管理模块
│       ├── views/
│       ├── components/
│       └── api/
│
├── shared/                  # 共享资源
│   ├── components/          # 公共组件
│   ├── utils/              # 工具函数
│   └── styles/             # 公共样式
│
└── core/                   # 核心配置
    ├── router/
    ├── store/
    └── plugins/
```

**💡 模块拆分的好处**：
- 每个模块相对独立，便于团队分工
- 模块内的文件关联性强，维护效率高
- 可以按模块进行代码分割和懒加载

### 4.3 选择拆分策略的建议



```
🔸 小型项目（< 20个页面）：
→ 选择按功能划分
→ 结构简单，容易理解

🔸 中型项目（20-50个页面）：
→ 功能划分 + 适当的子目录
→ 平衡复杂度和清晰度

🔸 大型项目（> 50个页面）：
→ 选择按模块划分
→ 便于团队协作和维护

🔸 微前端项目：
→ 每个模块可独立部署
→ 完全按业务域拆分
```

---

## 5. 🎯 目录组织最佳实践



### 5.1 通用目录组织建议



**📋 推荐的完整项目结构**：

```
my-vue-project/
├── public/
│   ├── index.html
│   └── favicon.ico
│
├── src/
│   ├── assets/                    # 静态资源
│   │   ├── images/
│   │   ├── styles/
│   │   └── fonts/
│   │
│   ├── components/                # 公共组件
│   │   ├── base/                 # 基础组件（按钮、输入框等）
│   │   ├── business/             # 业务组件（用户卡片等）
│   │   └── layout/               # 布局组件（导航、侧边栏等）
│   │
│   ├── views/                    # 页面组件
│   │   ├── Home/
│   │   ├── User/
│   │   └── Product/
│   │
│   ├── router/                   # 路由配置
│   │   ├── index.js
│   │   └── modules/
│   │
│   ├── store/                    # 状态管理
│   │   ├── index.js
│   │   └── modules/
│   │
│   ├── api/                      # 接口请求
│   │   ├── request.js           # axios 封装
│   │   ├── user.js
│   │   └── product.js
│   │
│   ├── utils/                    # 工具函数
│   │   ├── index.js
│   │   ├── format.js            # 格式化工具
│   │   └── validate.js          # 验证工具
│   │
│   ├── plugins/                  # 插件配置
│   │   ├── element-plus.js
│   │   └── chart.js
│   │
│   ├── directives/               # 自定义指令
│   │   └── permission.js
│   │
│   ├── filters/                  # 过滤器（Vue2）
│   │   └── index.js
│   │
│   ├── App.vue                   # 根组件
│   └── main.js                   # 入口文件
│
├── tests/                        # 测试文件
├── docs/                         # 项目文档
├── .env                          # 环境变量
├── .gitignore                    # Git忽略文件
├── package.json                  # 项目配置
├── README.md                     # 项目说明
└── vue.config.js                 # Vue CLI配置
```

### 5.2 文件组织的实用技巧



**🎯 组件文件组织技巧**：

```vue
<!-- 方式1：单文件组件 -->
<!-- UserProfile.vue -->
<template>
  <!-- 模板 -->
</template>

<script>
  // 逻辑
</script>

<style scoped>
  /* 样式 */
</style>
```

```
方式2：多文件组件（复杂组件推荐）
UserProfile/
├── index.vue          # 主组件文件
├── UserProfile.js     # 逻辑文件
├── UserProfile.scss   # 样式文件
└── components/        # 子组件
    ├── Avatar.vue
    └── InfoCard.vue
```

**🎯 路径引用技巧**：

```javascript
// vue.config.js - 配置路径别名
module.exports = {
  configureWebpack: {
    resolve: {
      alias: {
        '@': path.resolve(__dirname, 'src'),
        '@c': path.resolve(__dirname, 'src/components'),
        '@v': path.resolve(__dirname, 'src/views'),
        '@u': path.resolve(__dirname, 'src/utils')
      }
    }
  }
}

// 使用别名
import MyButton from '@c/base/MyButton.vue'
import { formatDate } from '@u/format.js'
```

### 5.3 团队协作约定



**📝 团队开发规范**：

```javascript
// 组件导出统一规范
// components/index.js
export { default as MyButton } from './base/MyButton.vue'
export { default as UserCard } from './business/UserCard.vue'

// 使用时可以统一导入
import { MyButton, UserCard } from '@/components'
```

**📁 README 文件约定**：
```markdown
# 模块说明文档


# User 模块


- views/UserList.vue - 用户列表页
- views/UserDetail.vue - 用户详情页
- components/UserForm.vue - 用户表单组件

# 主要功能


- 用户列表展示
- 用户信息编辑
- 用户权限管理
```

---

## 6. 📋 核心要点总结



### 6.1 必须掌握的核心概念



```
🔸 目录职责：每个目录有明确的存放内容
🔸 命名规范：统一的文件和目录命名约定
🔸 模块划分：按功能或按页面组织代码结构
🔸 路径管理：使用别名简化引用路径
🔸 团队协作：制定统一的开发规范
```

### 6.2 关键理解要点



**🔹 目录结构的本质**：
```
就像整理房间：
- 把相同类型的东西放在一起
- 给每样东西一个固定的位置
- 方便查找和维护
```

**🔹 组件 vs 页面的区别**：
```
组件（components）：
- 可重复使用的"零件"
- 独立的功能模块
- 不依赖特定页面

页面（views）：
- 对应路由的完整页面
- 组合多个组件
- 包含页面级业务逻辑
```

**🔹 模块化的好处**：
```
小项目：按功能分类，结构简单
大项目：按业务模块分离，便于维护
团队项目：每人负责不同模块，并行开发
```

### 6.3 实际应用指导



**🎯 新手建议**：
- 先按标准结构搭建项目
- 理解每个目录的作用
- 养成良好的命名习惯
- 从小项目开始实践

**🎯 项目选择**：
- 个人项目：选择简单的功能划分
- 团队项目：制定统一的目录规范
- 大型项目：考虑模块化拆分

**核心记忆**：
- 目录结构是项目的"骨架"，好的结构让开发事半功倍
- 命名规范是团队的"语言"，统一规范避免混乱
- 模块划分是代码的"分工"，合理划分提高效率
- 工具配置是开发的"助手"，善用别名简化操作