---
title: 2、表单控件与v-model绑定
---
## 📚 目录

1. [v-model双向绑定基础](#1-v-model双向绑定基础)
2. [基础表单控件绑定](#2-基础表单控件绑定)
3. [选择控件绑定详解](#3-选择控件绑定详解)
4. [v-model修饰符使用](#4-v-model修饰符使用)
5. [自定义组件v-model](#5-自定义组件v-model)
6. [表单交互最佳实践](#6-表单交互最佳实践)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🔄 v-model双向绑定基础


### 1.1 什么是双向绑定


**🎯 简单理解**：双向绑定就是"数据变化，界面跟着变；界面变化，数据也跟着变"

```
用户在输入框输入 → 数据自动更新
代码修改数据     → 输入框自动显示新内容

这就像一面镜子，两边总是保持同步！
```

**💡 日常类比**：
> 想象你在手机上编辑文档，你打字的同时云端自动保存，别人也能实时看到你的修改。这就是双向绑定的效果。

### 1.2 v-model的工作原理


**🔧 本质理解**：`v-model` 其实是一个**语法糖**（简化写法）

```vue
<!-- 使用 v-model（简化写法） -->
<input v-model="message">

<!-- 等价于下面的完整写法 -->
<input 
  :value="message" 
  @input="message = $event.target.value"
>
```

**📊 工作流程图**：
```
┌─────────────┐    用户输入    ┌─────────────┐
│   数据变量   │ ◄────────────► │  表单控件   │
│  message    │    界面更新    │   input     │
└─────────────┘                └─────────────┘
      ▲                              │
      │           自动同步            │
      └──────────────────────────────┘
```

### 1.3 基础使用示例


```vue
<template>
  <div>
    <p>你输入的内容：{{ message }}</p>
    <input v-model="message" placeholder="请输入内容">
  </div>
</template>

<script>
export default {
  data() {
    return {
      message: '初始内容'
    }
  }
}
</script>
```

**🎪 效果说明**：
- 页面加载时，输入框显示"初始内容"
- 用户输入时，上方文字实时变化
- 代码修改 `message` 时，输入框内容也会更新

---

## 2. 📝 基础表单控件绑定


### 2.1 文本输入框绑定


**🔸 单行文本输入**
```vue
<template>
  <div>
    <!-- 普通文本输入 -->
    <input v-model="username" placeholder="请输入用户名">
    
    <!-- 密码输入 -->
    <input v-model="password" type="password" placeholder="请输入密码">
    
    <!-- 显示绑定结果 -->
    <p>用户名：{{ username }}</p>
    <p>密码：{{ password }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      username: '',
      password: ''
    }
  }
}
</script>
```

**🔸 多行文本输入**
```vue
<template>
  <div>
    <textarea 
      v-model="description" 
      placeholder="请输入描述"
      rows="4"
    ></textarea>
    
    <p>字符数：{{ description.length }}</p>
    <div>内容预览：{{ description }}</div>
  </div>
</template>

<script>
export default {
  data() {
    return {
      description: ''
    }
  }
}
</script>
```

> 💡 **小提示**：`textarea` 的内容要用 `v-model`，不能在标签内写内容

### 2.2 数字输入控件


```vue
<template>
  <div>
    <input 
      v-model="age" 
      type="number" 
      min="1" 
      max="120"
      placeholder="请输入年龄"
    >
    
    <p>你的年龄：{{ age }} 岁</p>
    <p>数据类型：{{ typeof age }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      age: null
    }
  }
}
</script>
```

> ⚠️ **注意**：即使是 `type="number"`，v-model 绑定的仍然是字符串。如需数字类型，使用 `.number` 修饰符。

---

## 3. 🎛️ 选择控件绑定详解


### 3.1 单选框（Radio）绑定


**🔸 基础单选**
```vue
<template>
  <div>
    <h3>选择你的性别：</h3>
    
    <!-- 同一组单选框要有相同的 name 和 v-model -->
    <label>
      <input type="radio" v-model="gender" value="male">
      男
    </label>
    
    <label>
      <input type="radio" v-model="gender" value="female">
      女
    </label>
    
    <p>你选择的是：{{ gender }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      gender: '' // 初始值为空，也可以设置默认值如 'male'
    }
  }
}
</script>
```

**🔸 选项列表渲染**
```vue
<template>
  <div>
    <h3>选择你喜欢的编程语言：</h3>
    
    <div v-for="lang in languages" :key="lang.value">
      <label>
        <input type="radio" v-model="selectedLang" :value="lang.value">
        {{ lang.name }}
      </label>
    </div>
    
    <p>你选择的语言：{{ selectedLang }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      selectedLang: 'javascript',
      languages: [
        { value: 'javascript', name: 'JavaScript' },
        { value: 'python', name: 'Python' },
        { value: 'java', name: 'Java' },
        { value: 'vue', name: 'Vue.js' }
      ]
    }
  }
}
</script>
```

### 3.2 复选框（Checkbox）绑定


**🔸 单个复选框（布尔值）**
```vue
<template>
  <div>
    <label>
      <input type="checkbox" v-model="agreed">
      我同意用户协议
    </label>
    
    <p>同意状态：{{ agreed }}</p>
    <button :disabled="!agreed">注册</button>
  </div>
</template>

<script>
export default {
  data() {
    return {
      agreed: false
    }
  }
}
</script>
```

**🔸 多个复选框（数组）**
```vue
<template>
  <div>
    <h3>选择你的兴趣爱好：</h3>
    
    <div v-for="hobby in hobbies" :key="hobby">
      <label>
        <input type="checkbox" v-model="selectedHobbies" :value="hobby">
        {{ hobby }}
      </label>
    </div>
    
    <p>已选择：{{ selectedHobbies.join(', ') }}</p>
    <p>选择数量：{{ selectedHobbies.length }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      selectedHobbies: [], // 注意：多选时要用数组
      hobbies: ['读书', '运动', '音乐', '旅行', '编程']
    }
  }
}
</script>
```

### 3.3 下拉选择框（Select）绑定


**🔸 单选下拉框**
```vue
<template>
  <div>
    <h3>选择你的城市：</h3>
    
    <select v-model="selectedCity">
      <option value="">请选择城市</option>
      <option value="beijing">北京</option>
      <option value="shanghai">上海</option>
      <option value="guangzhou">广州</option>
      <option value="shenzhen">深圳</option>
    </select>
    
    <p>选择的城市：{{ selectedCity || '未选择' }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      selectedCity: ''
    }
  }
}
</script>
```

**🔸 多选下拉框**
```vue
<template>
  <div>
    <h3>选择你擅长的技能：</h3>
    
    <!-- 注意：多选要加 multiple 属性 -->
    <select v-model="selectedSkills" multiple style="height: 120px;">
      <option v-for="skill in skills" :key="skill.value" :value="skill.value">
        {{ skill.name }}
      </option>
    </select>
    
    <p>已选择的技能：</p>
    <ul>
      <li v-for="skill in selectedSkills" :key="skill">{{ skill }}</li>
    </ul>
  </div>
</template>

<script>
export default {
  data() {
    return {
      selectedSkills: [], // 多选时绑定数组
      skills: [
        { value: 'html', name: 'HTML' },
        { value: 'css', name: 'CSS' },
        { value: 'js', name: 'JavaScript' },
        { value: 'vue', name: 'Vue.js' },
        { value: 'react', name: 'React' }
      ]
    }
  }
}
</script>
```

---

## 4. 🔧 v-model修饰符使用


### 4.1 .lazy修饰符


**🎯 作用**：延迟同步，只在失去焦点时更新数据

```vue
<template>
  <div>
    <!-- 普通绑定：实时同步 -->
    <input v-model="message1" placeholder="实时同步">
    <p>实时值：{{ message1 }}</p>
    
    <!-- lazy修饰符：失去焦点时同步 -->
    <input v-model.lazy="message2" placeholder="失去焦点时同步">
    <p>延迟值：{{ message2 }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      message1: '',
      message2: ''
    }
  }
}
</script>
```

**💡 使用场景**：
- 🎯 搜索框：避免输入时频繁触发搜索
- 📝 表单验证：减少验证触发次数

### 4.2 .number修饰符


**🎯 作用**：自动将输入值转换为数字类型

```vue
<template>
  <div>
    <!-- 不使用 .number -->
    <input v-model="age1" type="number" placeholder="年龄（字符串）">
    <p>年龄：{{ age1 }}，类型：{{ typeof age1 }}</p>
    
    <!-- 使用 .number -->
    <input v-model.number="age2" type="number" placeholder="年龄（数字）">
    <p>年龄：{{ age2 }}，类型：{{ typeof age2 }}</p>
    
    <!-- 计算示例 -->
    <p>字符串运算：{{ age1 + 10 }}</p>
    <p>数字运算：{{ age2 + 10 }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      age1: '',
      age2: 0
    }
  }
}
</script>
```

> 🔥 **重点**：`age1 + 10` 结果是字符串拼接（如"2010"），`age2 + 10` 是数学运算（如30）

### 4.3 .trim修饰符


**🎯 作用**：自动去除首尾空格

```vue
<template>
  <div>
    <!-- 不使用 .trim -->
    <input v-model="username1" placeholder="用户名（保留空格）">
    <p>用户名："{{ username1 }}"，长度：{{ username1.length }}</p>
    
    <!-- 使用 .trim -->
    <input v-model.trim="username2" placeholder="用户名（去除空格）">
    <p>用户名："{{ username2 }}"，长度：{{ username2.length }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      username1: '',
      username2: ''
    }
  }
}
</script>
```

**💡 使用场景**：
- 🎯 用户名输入：避免意外空格
- 📧 邮箱输入：确保格式正确
- 🔍 搜索框：提高搜索准确性

### 4.4 修饰符组合使用


```vue
<template>
  <div>
    <input 
      v-model.lazy.trim="searchKeyword" 
      placeholder="搜索关键词（延迟+去空格）"
    >
    
    <input 
      v-model.number.lazy="price" 
      type="number" 
      placeholder="商品价格（数字+延迟）"
    >
    
    <p>搜索词：{{ searchKeyword }}</p>
    <p>价格：{{ price }}，类型：{{ typeof price }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      searchKeyword: '',
      price: 0
    }
  }
}
</script>
```

---

## 5. 🔨 自定义组件v-model


### 5.1 基础自定义组件


**🎯 理解**：让你的自定义组件也能使用 `v-model`

```vue
<!-- 父组件 -->
<template>
  <div>
    <h3>自定义输入组件</h3>
    <custom-input v-model="message"></custom-input>
    <p>绑定值：{{ message }}</p>
  </div>
</template>

<script>
import CustomInput from './CustomInput.vue'

export default {
  components: { CustomInput },
  data() {
    return {
      message: '初始值'
    }
  }
}
</script>
```

```vue
<!-- CustomInput.vue 子组件 -->
<template>
  <div class="custom-input">
    <label>{{ label }}</label>
    <input 
      :value="value"
      @input="handleInput"
      :placeholder="placeholder"
    >
  </div>
</template>

<script>
export default {
  name: 'CustomInput',
  props: {
    value: String, // 接收 v-model 的值
    label: String,
    placeholder: String
  },
  methods: {
    handleInput(event) {
      // 触发 input 事件，更新父组件的值
      this.$emit('input', event.target.value)
    }
  }
}
</script>
```

**📊 工作原理**：
```
父组件 v-model="message"
        ↓
子组件接收 props: { value: message }
        ↓
用户输入触发 @input="handleInput"
        ↓
子组件 $emit('input', newValue)
        ↓
父组件 message 自动更新
```

### 5.2 自定义事件名和属性名


```vue
<!-- 子组件：自定义 v-model 行为 -->
<template>
  <div>
    <input 
      :value="checked"
      @change="handleChange"
      type="checkbox"
    >
    {{ label }}
  </div>
</template>

<script>
export default {
  model: {
    prop: 'checked',  // 自定义属性名
    event: 'change'   // 自定义事件名
  },
  props: {
    checked: Boolean, // 对应 prop: 'checked'
    label: String
  },
  methods: {
    handleChange(event) {
      this.$emit('change', event.target.checked) // 对应 event: 'change'
    }
  }
}
</script>
```

### 5.3 Vue 3 的 v-model


> 💡 **Vue 3 变化**：Vue 3 中 v-model 的实现有所不同

```vue
<!-- Vue 3 自定义组件 -->
<template>
  <input 
    :value="modelValue"
    @input="$emit('update:modelValue', $event.target.value)"
  >
</template>

<script>
export default {
  props: ['modelValue'],
  emits: ['update:modelValue']
}
</script>
```

---

## 6. 🎯 表单交互最佳实践


### 6.1 完整表单示例


```vue
<template>
  <div class="user-form">
    <h2>用户注册表单</h2>
    
    <form @submit.prevent="handleSubmit">
      <!-- 基本信息 -->
      <div class="form-group">
        <label>用户名：</label>
        <input v-model.trim="form.username" placeholder="请输入用户名" required>
      </div>
      
      <div class="form-group">
        <label>年龄：</label>
        <input v-model.number="form.age" type="number" min="1" max="120">
      </div>
      
      <!-- 性别选择 -->
      <div class="form-group">
        <label>性别：</label>
        <label><input type="radio" v-model="form.gender" value="male"> 男</label>
        <label><input type="radio" v-model="form.gender" value="female"> 女</label>
      </div>
      
      <!-- 兴趣爱好 -->
      <div class="form-group">
        <label>兴趣爱好：</label>
        <div v-for="hobby in hobbies" :key="hobby">
          <label>
            <input type="checkbox" v-model="form.hobbies" :value="hobby">
            {{ hobby }}
          </label>
        </div>
      </div>
      
      <!-- 城市选择 -->
      <div class="form-group">
        <label>所在城市：</label>
        <select v-model="form.city">
          <option value="">请选择</option>
          <option v-for="city in cities" :key="city" :value="city">
            {{ city }}
          </option>
        </select>
      </div>
      
      <!-- 个人简介 -->
      <div class="form-group">
        <label>个人简介：</label>
        <textarea v-model.trim="form.bio" rows="4"></textarea>
      </div>
      
      <!-- 协议同意 -->
      <div class="form-group">
        <label>
          <input type="checkbox" v-model="form.agreed"> 
          我同意用户协议
        </label>
      </div>
      
      <button type="submit" :disabled="!canSubmit">提交注册</button>
    </form>
    
    <!-- 表单数据预览 -->
    <div class="form-preview">
      <h3>表单数据：</h3>
      <pre>{{ JSON.stringify(form, null, 2) }}</pre>
    </div>
  </div>
</template>

<script>
export default {
  data() {
    return {
      form: {
        username: '',
        age: null,
        gender: '',
        hobbies: [],
        city: '',
        bio: '',
        agreed: false
      },
      hobbies: ['读书', '运动', '音乐', '旅行', '编程'],
      cities: ['北京', '上海', '广州', '深圳']
    }
  },
  computed: {
    canSubmit() {
      return this.form.username && 
             this.form.age && 
             this.form.gender && 
             this.form.agreed
    }
  },
  methods: {
    handleSubmit() {
      if (!this.canSubmit) {
        alert('请完善必填信息')
        return
      }
      
      console.log('提交的表单数据：', this.form)
      alert('注册成功！')
    }
  }
}
</script>

<style scoped>
.form-group {
  margin-bottom: 15px;
}
.form-group label {
  display: block;
  margin-bottom: 5px;
}
.form-preview {
  margin-top: 20px;
  padding: 15px;
  background: #f5f5f5;
  border-radius: 5px;
}
</style>
```

### 6.2 表单验证技巧


```vue
<template>
  <div>
    <input 
      v-model="email"
      @blur="validateEmail"
      :class="{ error: emailError }"
      placeholder="请输入邮箱"
    >
    <p v-if="emailError" class="error-msg">{{ emailError }}</p>
  </div>
</template>

<script>
export default {
  data() {
    return {
      email: '',
      emailError: ''
    }
  },
  methods: {
    validateEmail() {
      const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
      if (!this.email) {
        this.emailError = '邮箱不能为空'
      } else if (!emailPattern.test(this.email)) {
        this.emailError = '邮箱格式不正确'
      } else {
        this.emailError = ''
      }
    }
  }
}
</script>

<style>
.error {
  border-color: red;
}
.error-msg {
  color: red;
  font-size: 12px;
}
</style>
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 v-model本质：语法糖，简化了 :value + @input 的写法
🔸 双向绑定：数据变化↔界面变化，自动同步
🔸 不同控件类型：文本、单选、多选、下拉选择的绑定方式
🔸 修饰符作用：.lazy、.number、.trim 解决实际问题
🔸 自定义组件：让自己的组件也支持 v-model
```

### 7.2 数据类型对应关系


| **控件类型** | **绑定数据类型** | **说明** |
|-------------|----------------|----------|
| 🔤 `input[text]` | `String` | 普通文本 |
| 🔢 `input[number]` | `String/Number` | 配合`.number`修饰符 |
| ☑️ `checkbox`(单个) | `Boolean` | true/false |
| ☑️ `checkbox`(多个) | `Array` | 选中值的数组 |
| 🔘 `radio` | `String` | 选中的value值 |
| 📋 `select`(单选) | `String` | 选中的option值 |
| 📋 `select`(多选) | `Array` | 选中值的数组 |
| 📝 `textarea` | `String` | 多行文本 |

### 7.3 实际应用指导


**🎯 表单设计原则**
- ✅ **合理使用修饰符**：根据数据类型选择合适修饰符
- ✅ **提供默认值**：避免undefined导致的问题  
- ✅ **即时反馈**：配合计算属性提供验证提示
- ✅ **用户体验**：合理使用.lazy避免过度响应

**🔧 常见问题解决**
```javascript
// ❌ 错误：多选绑定到字符串
data() {
  return {
    hobbies: '' // 应该是数组
  }
}

// ✅ 正确：多选绑定到数组
data() {
  return {
    hobbies: []
  }
}
```

**💡 开发技巧**
- 🎯 使用`JSON.stringify()`调试表单数据
- 🎯 配合computed属性做表单验证
- 🎯 自定义组件时记住：prop接收值，$emit更新值
- 🎯 复杂表单可以拆分成多个子组件

**核心记忆**：
- v-model是双向绑定的语法糖
- 不同控件对应不同数据类型
- 修饰符解决具体问题（延迟、类型转换、去空格）
- 自定义组件通过prop+$emit实现v-model