---
title: 4、嵌套路由与重定向
---
## 📚 目录

1. [什么是嵌套路由](#1-什么是嵌套路由)
2. [嵌套路由基础配置](#2-嵌套路由基础配置)
3. [路由重定向机制](#3-路由重定向机制)
4. [路由别名应用](#4-路由别名应用)
5. [多层嵌套路由实战](#5-多层嵌套路由实战)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🏗️ 什么是嵌套路由


### 1.1 核心概念理解


**嵌套路由**就像俄罗斯套娃一样，**页面里面还有页面**。

```
想象一个后台管理系统：
┌─────────────────────────────────┐
│        顶部导航栏               │
├─────────────────────────────────┤
│  侧栏  │     内容区域           │
│  菜单  │  ┌─────────────────┐   │
│        │  │   这里显示       │   │
│        │  │   不同的页面     │   │ ← 这个区域根据路由变化
│        │  │                 │   │
│        │  └─────────────────┘   │
└─────────────────────────────────┘

外层：/admin (管理系统框架)
内层：/admin/users (用户管理页面)
     /admin/products (商品管理页面)
```

### 1.2 为什么需要嵌套路由


**🔸 解决的问题：**
- **布局复用** - 多个页面共享相同的页面框架
- **结构清晰** - URL结构与页面层级对应
- **开发便捷** - 避免重复写相同的页面框架代码

**实际场景举例：**
```
电商网站结构：
/shop           → 商城首页框架
  /shop/list    → 商品列表页 (在框架内显示)
  /shop/detail  → 商品详情页 (在框架内显示)
  /shop/cart    → 购物车页面 (在框架内显示)

个人中心：
/user           → 用户中心框架  
  /user/profile → 个人资料页 (在框架内显示)
  /user/orders  → 订单列表页 (在框架内显示)
  /user/address → 地址管理页 (在框架内显示)
```

### 1.3 嵌套路由的本质


> **核心理解：** 嵌套路由 = 父组件 + 子路由出口(`<router-view>`)

```vue
<!-- 父组件 Layout.vue -->
<template>
  <div class="layout">
    <!-- 固定的页面框架 -->
    <header>网站头部</header>
    <nav>导航菜单</nav>
    
    <!-- 子路由显示区域 -->
    <main>
      <router-view></router-view> ← 子页面在这里显示
    </main>
    
    <footer>网站底部</footer>
  </div>
</template>
```

---

## 2. ⚙️ 嵌套路由基础配置


### 2.1 children配置详解


**`children`** 就是在路由里面**再放一个路由数组**，实现页面套页面。

```javascript
// router/index.js
const routes = [
  {
    path: '/admin',
    component: AdminLayout,  // 父组件(页面框架)
    children: [              // 子路由配置
      {
        path: '',           // 空路径 = 默认子路由
        component: Dashboard // /admin 时显示 Dashboard
      },
      {
        path: 'users',      // /admin/users
        component: UserList
      },
      {
        path: 'products',   // /admin/products  
        component: ProductList
      }
    ]
  }
]
```

### 2.2 路径规则说明


**🔸 子路由路径规则：**

| 子路径写法 | 最终URL | 说明 |
|-----------|---------|------|
| `''` | `/admin` | 空字符串 = 默认子路由 |
| `'users'` | `/admin/users` | 相对路径，自动拼接 |
| `'/dashboard'` | `/dashboard` | 绝对路径，从根开始 |

```javascript
// ✅ 推荐写法 - 相对路径
children: [
  { path: '', component: Dashboard },      // /admin
  { path: 'users', component: UserList },  // /admin/users
  { path: 'settings', component: Settings } // /admin/settings
]

// ❌ 不推荐 - 绝对路径会脱离父路由
children: [
  { path: '/dashboard', component: Dashboard }, // /dashboard (脱离了/admin)
]
```

### 2.3 完整配置示例


```javascript
// 创建管理后台的嵌套路由
const routes = [
  {
    path: '/admin',
    component: () => import('@/layouts/AdminLayout.vue'),
    children: [
      // 默认页面：访问 /admin 时显示
      {
        path: '',
        name: 'AdminHome',
        component: () => import('@/views/admin/Dashboard.vue')
      },
      // 用户管理：/admin/users
      {
        path: 'users',
        name: 'UserManage', 
        component: () => import('@/views/admin/UserList.vue')
      },
      // 商品管理：/admin/products
      {
        path: 'products',
        name: 'ProductManage',
        component: () => import('@/views/admin/ProductList.vue')
      }
    ]
  }
]
```

### 2.4 父组件模板要点


```vue
<!-- AdminLayout.vue - 管理后台布局组件 -->
<template>
  <div class="admin-layout">
    <!-- 固定的顶部导航 -->
    <header class="admin-header">
      <h1>管理后台</h1>
      <nav>
        <router-link to="/admin">首页</router-link>
        <router-link to="/admin/users">用户管理</router-link>
        <router-link to="/admin/products">商品管理</router-link>
      </nav>
    </header>
    
    <!-- 关键：子路由显示区域 -->
    <main class="admin-content">
      <router-view></router-view> ← 子页面组件在这里渲染
    </main>
  </div>
</template>
```

> **⚠️ 重要提醒：** 父组件**必须**包含`<router-view>`，否则子路由无法显示！

---

## 3. 🔄 路由重定向机制


### 3.1 什么是路由重定向


**重定向**就是**自动跳转**，当用户访问A地址时，自动跳到B地址。

```
用户访问：/              
自动跳转：/home    ← 这就是重定向

实际效果：
1. 用户在浏览器输入 example.com/
2. 系统自动跳转到 example.com/home
3. 显示首页内容
```

### 3.2 redirect基础语法


```javascript
const routes = [
  // 🔸 字符串重定向 - 最常用
  {
    path: '/',
    redirect: '/home'  // 访问根路径时跳转到 /home
  },
  
  // 🔸 对象重定向 - 可以指定name
  {
    path: '/old-page',
    redirect: { name: 'HomePage' }
  },
  
  // 🔸 函数重定向 - 动态决定跳转地址
  {
    path: '/user/:id',
    redirect: (to) => {
      // 根据参数决定跳转地址
      return `/profile/${to.params.id}`
    }
  }
]
```

### 3.3 常见重定向应用场景


**🎯 场景1：网站首页重定向**
```javascript
// 用户访问域名根路径时，跳转到首页
{
  path: '/',
  redirect: '/home'
},
{
  path: '/home',
  component: HomePage
}
```

**🎯 场景2：嵌套路由默认页面**
```javascript
{
  path: '/admin',
  component: AdminLayout,
  redirect: '/admin/dashboard', // 进入管理后台时默认显示仪表盘
  children: [
    {
      path: 'dashboard',
      component: Dashboard
    },
    {
      path: 'users', 
      component: UserList
    }
  ]
}
```

**🎯 场景3：旧链接兼容**
```javascript
// 网站改版后，旧链接重定向到新链接
{
  path: '/old-about',
  redirect: '/about'
},
{
  path: '/old-contact', 
  redirect: '/contact'
}
```

### 3.4 重定向 vs 默认子路由


```javascript
// 方式1：使用重定向
{
  path: '/admin',
  component: AdminLayout,
  redirect: '/admin/dashboard',  // 显式重定向
  children: [
    { path: 'dashboard', component: Dashboard }
  ]
}

// 方式2：使用默认子路由 (推荐)
{
  path: '/admin', 
  component: AdminLayout,
  children: [
    { path: '', component: Dashboard }  // 空路径 = 默认路由
  ]
}
```

> **💡 最佳实践：** 嵌套路由优先使用**默认子路由**（空path），重定向用于**跨路由跳转**。

---

## 4. 🏷️ 路由别名应用


### 4.1 别名概念理解


**别名(alias)**就是给路由**起小名**，**一个页面可以用多个网址访问**。

```
原始路径：/user/profile
设置别名：/me

效果：
- 访问 /user/profile → 显示个人资料页
- 访问 /me → 显示相同的个人资料页
- 浏览器地址栏显示用户实际访问的地址
```

### 4.2 alias基础语法


```javascript
const routes = [
  {
    path: '/user/profile',
    alias: '/me',              // 单个别名
    component: UserProfile
  },
  
  {
    path: '/products',
    alias: ['/goods', '/items'], // 多个别名
    component: ProductList  
  }
]
```

### 4.3 别名 vs 重定向对比


```
┌─────────────────┬─────────────────┬─────────────────┐
│     操作         │     重定向       │      别名       │
├─────────────────┼─────────────────┼─────────────────┤
│ 用户访问 /me     │   跳转到真实路径  │   直接显示内容   │
│ 浏览器地址栏     │   改变为真实路径  │   保持用户输入   │
│ 是否发生跳转     │     是          │       否        │
│ URL参数传递      │     保持        │      保持       │
└─────────────────┴─────────────────┴─────────────────┘
```

```javascript
// 重定向示例
{
  path: '/me',
  redirect: '/user/profile'  // 访问/me时跳转，地址栏变成/user/profile
}

// 别名示例  
{
  path: '/user/profile',
  alias: '/me',             // 访问/me时直接显示，地址栏还是/me
  component: UserProfile
}
```

### 4.4 实际应用场景


**🔸 简化常用路径**
```javascript
{
  path: '/user/personal/settings',
  alias: '/settings',        // 简化长路径
  component: UserSettings
}
```

**🔸 多语言路径支持**
```javascript
{
  path: '/about',
  alias: ['/about-us', '/关于我们'],
  component: AboutPage
}
```

**🔸 兼容旧版本路径**
```javascript
{
  path: '/products',
  alias: '/goods',           // 兼容旧版本的/goods路径
  component: ProductList
}
```

---

## 5. 🏢 多层嵌套路由实战


### 5.1 三层嵌套结构设计


实现一个**后台管理系统**的三层嵌套：`系统 > 模块 > 功能页面`

```
系统结构：
/admin                    ← 第一层：管理系统框架
  /admin/user            ← 第二层：用户管理模块
    /admin/user/list     ← 第三层：用户列表页面
    /admin/user/add      ← 第三层：添加用户页面
    /admin/user/edit/:id ← 第三层：编辑用户页面
  /admin/product         ← 第二层：商品管理模块  
    /admin/product/list  ← 第三层：商品列表页面
    /admin/product/add   ← 第三层：添加商品页面
```

### 5.2 路由配置实现


```javascript
const routes = [
  {
    path: '/admin',
    component: AdminLayout,        // 第一层：系统框架
    redirect: '/admin/dashboard',
    children: [
      // 仪表盘 (二级页面)
      {
        path: 'dashboard',
        component: Dashboard
      },
      
      // 用户管理模块 (二级框架)
      {
        path: 'user',
        component: UserModule,     // 第二层：模块框架
        redirect: '/admin/user/list',
        children: [
          {
            path: 'list',          // /admin/user/list
            component: UserList
          },
          {
            path: 'add',           // /admin/user/add
            component: UserAdd
          },
          {
            path: 'edit/:id',      // /admin/user/edit/123
            component: UserEdit
          }
        ]
      },
      
      // 商品管理模块
      {
        path: 'product',
        component: ProductModule,   // 第二层：模块框架
        children: [
          {
            path: '',              // /admin/product 默认显示列表
            component: ProductList
          },
          {
            path: 'add',
            component: ProductAdd
          },
          {
            path: 'detail/:id',
            component: ProductDetail
          }
        ]
      }
    ]
  }
]
```

### 5.3 组件结构设计


```vue
<!-- AdminLayout.vue - 第一层框架 -->
<template>
  <div class="admin-layout">
    <header>系统顶部导航</header>
    <aside>
      <router-link to="/admin/dashboard">仪表盘</router-link>
      <router-link to="/admin/user">用户管理</router-link>
      <router-link to="/admin/product">商品管理</router-link>
    </aside>
    <main>
      <router-view></router-view> ← 第二层内容显示区
    </main>
  </div>
</template>
```

```vue
<!-- UserModule.vue - 第二层用户管理框架 -->
<template>
  <div class="user-module">
    <nav class="sub-nav">
      <router-link to="/admin/user/list">用户列表</router-link>
      <router-link to="/admin/user/add">添加用户</router-link>
    </nav>
    <div class="content">
      <router-view></router-view> ← 第三层页面显示区
    </div>
  </div>
</template>
```

### 5.4 面包屑导航实现


```vue
<!-- 动态生成面包屑导航 -->
<template>
  <nav class="breadcrumb">
    <span v-for="(item, index) in breadcrumbs" :key="index">
      <router-link v-if="item.path" :to="item.path">
        {{ item.name }}
      </router-link>
      <span v-else>{{ item.name }}</span>
      <span v-if="index < breadcrumbs.length - 1"> > </span>
    </span>
  </nav>
</template>

<script>
export default {
  computed: {
    breadcrumbs() {
      const route = this.$route
      const breadcrumbs = []
      
      // 根据当前路由生成面包屑
      if (route.path.includes('/admin')) {
        breadcrumbs.push({ name: '管理后台', path: '/admin' })
        
        if (route.path.includes('/user')) {
          breadcrumbs.push({ name: '用户管理', path: '/admin/user' })
          
          if (route.path.includes('/list')) {
            breadcrumbs.push({ name: '用户列表' })
          } else if (route.path.includes('/add')) {
            breadcrumbs.push({ name: '添加用户' })
          }
        }
      }
      
      return breadcrumbs
    }
  }
}
</script>
```

### 5.5 最佳实践建议


**🔸 层级控制：**
- **不超过4层** - 太深的嵌套影响用户体验
- **每层职责明确** - 框架层、模块层、功能层

**🔸 命名规范：**
```javascript
// 推荐的路由命名
{
  path: 'list',
  name: 'UserList',      // 模块名 + 功能名
  component: UserList
}
```

**🔸 组件复用：**
```vue
<!-- 通用的二级框架组件 -->
<template>
  <div class="module-layout">
    <nav><slot name="nav"></slot></nav>
    <main><router-view></router-view></main>
  </div>
</template>
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的概念


```
🔸 嵌套路由 = 父组件 + children配置 + <router-view>
🔸 children数组：在路由里面再放路由，实现页面套页面
🔸 重定向redirect：自动跳转，访问A地址时跳到B地址  
🔸 别名alias：给路由起小名，一个页面多个网址访问
🔸 多层嵌套：通过children的children实现深层结构
```

### 6.2 关键理解要点


**🔹 路径拼接规则**
```
父路径：/admin
子路径：users
结果：/admin/users

特殊情况：
空字符串 '' → 默认子路由，路径等于父路径
以 / 开头 → 绝对路径，忽略父路径
```

**🔹 组件渲染层级**
```
第一层：<App> 中的 <router-view>     ← 显示顶级路由组件
第二层：顶级组件中的 <router-view>    ← 显示子路由组件  
第三层：子组件中的 <router-view>     ← 显示孙路由组件
```

**🔹 重定向 vs 别名选择**
```
需要跳转且改变URL → 使用 redirect
需要多个URL访问同一页面 → 使用 alias
嵌套路由的默认页面 → 使用空path子路由
```

### 6.3 实战应用指南


**🎯 适用场景**
- ✅ **后台管理系统** - 多层级的管理界面
- ✅ **电商网站** - 商城框架下的各种页面  
- ✅ **个人中心** - 用户相关的多个功能页面
- ✅ **文档网站** - 分类目录下的详细内容

**🎯 设计原则**
- **结构清晰** - URL层级与页面层级对应
- **复用框架** - 相同布局的页面共享父组件
- **用户友好** - 提供面包屑导航和清晰的路径

**🎯 常见问题避免**
```
❌ 忘记在父组件中写 <router-view>
❌ 子路径写成绝对路径 (以/开头)
❌ 嵌套层级过深 (超过4层)
❌ 路由重定向和别名混用造成混乱
```

### 6.4 记忆要点


> **核心记忆：** 嵌套路由就是**页面套页面**，通过`children`配置实现，父组件必须有`<router-view>`作为子页面的显示区域。重定向是**自动跳转**，别名是**一页多址**。

**实战口诀：**
```
嵌套路由页套页，children配置不能少
父组件必须有出口，router-view来显示
重定向跳转改地址，别名访问保原址
多层嵌套要适度，结构清晰用户爱
```