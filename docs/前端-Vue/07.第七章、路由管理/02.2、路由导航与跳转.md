---
title: 2、路由导航与跳转
---
## 📚 目录

1. [路由导航基本概念](#1-路由导航基本概念)
2. [声明式导航 vs 编程式导航](#2-声明式导航-vs-编程式导航)
3. [编程式导航的三大方法](#3-编程式导航的三大方法)
4. [路径跳转与命名路由](#4-路径跳转与命名路由)
5. [导航故障处理](#5-导航故障处理)
6. [实际应用场景](#6-实际应用场景)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🧭 路由导航基本概念


### 1.1 什么是路由导航


**🔸 简单理解**
```
就像在手机里切换不同的APP页面一样：
📱 微信首页 → 聊天列表 → 具体聊天
💻 网站首页 → 商品列表 → 商品详情

路由导航 = 在不同页面之间跳转的方式
```

**🎯 导航的本质**
- **URL改变**：地址栏的网址发生变化
- **页面切换**：显示的内容完全更新
- **历史记录**：浏览器记住你的访问路径

### 1.2 为什么需要路由导航


**传统多页面网站问题**：
```
❌ 每次跳转都要重新加载整个页面
❌ 加载慢，用户体验差
❌ 数据重复获取，浪费资源
```

**SPA单页面应用优势**：
```
✅ 只加载一次，后续只更新内容
✅ 快速响应，体验流畅
✅ 数据可以缓存，节省资源
```

### 1.3 导航方式总览


```
路由导航方式图解：

用户操作 ──────┐
              │
        ┌─────▼─────┐
        │   导航触发   │
        └─────┬─────┘
              │
    ┌─────────┴─────────┐
    │                   │
┌───▼───┐         ┌───▼───┐
│声明式  │         │编程式  │
│导航    │         │导航    │
│<router-│         │$router.│
│link>   │         │push()  │
└───────┘         └───────┘
    │                   │
    └─────────┬─────────┘
              │
        ┌─────▼─────┐
        │   页面跳转   │
        └───────────┘
```

---

## 2. 🔄 声明式导航 vs 编程式导航


### 2.1 声明式导航 - 模板中直接使用


**🔸 什么是声明式导航**
```
就像HTML的<a>标签一样，直接写在模板里
点击就能跳转，非常直观简单
```

**💡 基本用法**
```vue
<template>
  <!-- 最简单的跳转 -->
  <router-link to="/home">回到首页</router-link>
  
  <!-- 跳转到用户页面 -->
  <router-link to="/user">用户中心</router-link>
  
  <!-- 带参数的跳转 -->
  <router-link to="/product/123">查看商品</router-link>
</template>
```

**✨ 声明式导航特点**
- ✅ **写法简单**：直接写在模板里，一看就懂
- ✅ **自动处理**：Vue Router自动处理点击事件
- ✅ **样式支持**：自动添加活跃状态的CSS类
- ❌ **功能有限**：只能做简单跳转，复杂逻辑做不了

### 2.2 编程式导航 - JavaScript代码控制


**🔸 什么是编程式导航**
```
用JavaScript代码来控制跳转
就像用遥控器控制电视换台一样
想什么时候跳转，就什么时候跳转
```

**💡 基本用法**
```vue
<template>
  <button @click="goHome">回到首页</button>
  <button @click="goUser">用户中心</button>
</template>

<script>
export default {
  methods: {
    goHome() {
      // 编程式跳转到首页
      this.$router.push('/home')
    },
    
    goUser() {
      // 编程式跳转到用户页面
      this.$router.push('/user')
    }
  }
}
</script>
```

**✨ 编程式导航特点**
- ✅ **功能强大**：可以加各种判断和逻辑
- ✅ **灵活控制**：想什么时候跳转都可以
- ✅ **数据处理**：跳转前后可以处理数据
- ❌ **代码稍多**：需要写方法，比声明式复杂一点

### 2.3 两种方式对比


| 对比项目 | **声明式导航** | **编程式导航** |
|---------|---------------|---------------|
| 🎯 **适用场景** | `简单的页面跳转` | `需要逻辑判断的跳转` |
| 📝 **写法复杂度** | `简单，一行搞定` | `稍复杂，需要写方法` |
| 🔧 **功能强度** | `基础功能` | `强大灵活` |
| 💡 **典型使用** | `导航菜单、面包屑` | `表单提交后跳转、权限检查` |

**🎯 选择建议**
```
🔸 普通链接跳转 → 用声明式导航
🔸 按钮点击跳转 → 用编程式导航  
🔸 需要条件判断 → 用编程式导航
🔸 表单提交后跳转 → 用编程式导航
```

---

## 3. ⚡ 编程式导航的三大方法


### 3.1 $router.push() - 最常用的跳转


**🔸 作用说明**
```
push = 推入，就像往书堆里放一本新书
跳转到新页面，同时把这次跳转记录到历史记录里
用户可以点击浏览器的"返回"按钮回到上一页
```

**💡 基本用法**
```javascript
// 方式1：直接写路径
this.$router.push('/home')

// 方式2：写成对象形式
this.$router.push({ path: '/home' })

// 方式3：带参数的跳转
this.$router.push('/user/123')
this.$router.push({ path: '/user/123' })

// 方式4：查询参数
this.$router.push({ path: '/search', query: { keyword: 'vue' } })
// 结果：/search?keyword=vue
```

### 3.2 $router.replace() - 替换当前页面


**🔸 作用说明**
```
replace = 替换，就像把书堆最上面的书换成另一本
跳转到新页面，但不增加历史记录
用户点击"返回"会回到再前一页，跳过当前这次跳转
```

**💡 实际应用场景**
```javascript
// 场景：登录成功后跳转
login() {
  // 登录成功，替换到首页
  // 用户不应该能返回到登录页
  this.$router.replace('/home')
}

// 场景：重定向页面
redirect() {
  // 从中间页面跳转，不留历史记录
  this.$router.replace('/target-page')
}
```

### 3.3 $router.go() - 历史记录中前进后退


**🔸 作用说明**
```
go = 前进/后退，就像翻书一样
在浏览历史中前进或后退几页
正数=前进，负数=后退
```

**💡 常用写法**
```javascript
// 后退一页（相当于浏览器的后退按钮）
this.$router.go(-1)

// 前进一页（相当于浏览器的前进按钮）
this.$router.go(1)

// 后退两页
this.$router.go(-2)

// 如果历史记录不够，什么也不做
this.$router.go(-100) // 静默失败
```

**🎯 三种方法对比**

| 方法 | **历史记录** | **常用场景** | **记忆技巧** |
|-----|-------------|-------------|-------------|
| **push** | `增加记录` | `普通跳转` | `往历史里推入一条新记录` |
| **replace** | `替换当前记录` | `登录后跳转、重定向` | `替换掉当前记录` |
| **go** | `在记录中移动` | `返回上页、前进` | `在历史中来回走动` |

---

## 4. 🎯 路径跳转与命名路由


### 4.1 路径跳转 - 直接写URL路径


**🔸 什么是路径跳转**
```
就像告诉出租车司机：去"中山路123号"
直接说出具体的路径地址
```

**💡 路径跳转示例**
```javascript
// 基础路径
this.$router.push('/home')
this.$router.push('/about')

// 带参数的路径
this.$router.push('/user/123')
this.$router.push('/product/456')

// 带查询参数的路径
this.$router.push('/search?q=vue&type=tutorial')

// 对象形式的路径跳转
this.$router.push({
  path: '/user/123',
  query: { tab: 'profile' }
})
```

### 4.2 命名路由 - 给路由起个名字


**🔸 什么是命名路由**
```
就像给联系人起个昵称：
张三 → 昵称"小张"
/user/profile → 名字"userProfile"

以后说"小张"大家就知道是张三
说"userProfile"就知道是用户档案页
```

**📝 命名路由配置**
```javascript
// 路由配置文件
const routes = [
  {
    path: '/home',
    name: 'home',        // 给路由起名字
    component: Home
  },
  {
    path: '/user/:id',
    name: 'userDetail',  // 用户详情页的名字
    component: UserDetail
  },
  {
    path: '/product/:id',
    name: 'productDetail', // 商品详情页的名字
    component: ProductDetail
  }
]
```

**💡 使用命名路由跳转**
```javascript
// 使用名字跳转（推荐）
this.$router.push({ name: 'home' })

// 带参数的命名路由跳转
this.$router.push({
  name: 'userDetail',
  params: { id: 123 }
})

// 带查询参数的命名路由跳转
this.$router.push({
  name: 'userDetail',
  params: { id: 123 },
  query: { tab: 'settings' }
})
```

### 4.3 params参数 vs query参数


**🔸 params参数（路径参数）**
```javascript
// 路由配置：/user/:id
// 跳转方式：
this.$router.push({
  name: 'userDetail',
  params: { id: 123 }
})
// 结果URL：/user/123
// 特点：参数是URL路径的一部分
```

**🔸 query参数（查询参数）**
```javascript
// 跳转方式：
this.$router.push({
  path: '/search',
  query: { keyword: 'vue', page: 1 }
})
// 结果URL：/search?keyword=vue&page=1
// 特点：参数在?号后面，用&连接
```

**📊 两种参数对比**

| 参数类型 | **URL形式** | **使用场景** | **特点** |
|---------|------------|-------------|---------|
| **params** | `/user/123` | `必需的核心参数` | `简洁，SEO友好` |
| **query** | `/search?q=vue` | `可选的附加参数` | `灵活，可有可无` |

### 4.4 路径跳转 vs 命名路由对比


**🔸 路径跳转的问题**
```javascript
// 如果路径改了，所有跳转代码都要改
this.$router.push('/user/profile/settings')  // 路径很长
this.$router.push('/user/profile/settings')  // 到处都是这个路径
this.$router.push('/user/profile/settings')  // 维护困难
```

**🔸 命名路由的优势**
```javascript
// 路径改了，只需要改路由配置，跳转代码不用改
this.$router.push({ name: 'userSettings' })  // 简洁明了
this.$router.push({ name: 'userSettings' })  // 到处都用名字
this.$router.push({ name: 'userSettings' })  // 维护方便
```

**🎯 最佳实践建议**
```
✅ 推荐：优先使用命名路由
✅ 理由：代码更清晰，维护更容易
✅ 场景：除了最简单的跳转，都建议用名字

❌ 避免：复杂路径直接写URL
❌ 原因：容易出错，难以维护
```

---

## 5. 🚨 导航故障处理


### 5.1 什么是导航故障


**🔸 简单理解**
```
就像开车导航时遇到的问题：
🚫 路不通（路由不存在）
🔄 路被堵（导航被阻止）
⚠️ 走错路（重复导航）
```

**💡 常见导航故障场景**
```javascript
// 场景1：重复点击同一个链接
this.$router.push('/home')  // 第一次，正常跳转
this.$router.push('/home')  // 第二次，会报错！

// 场景2：路由不存在
this.$router.push('/not-exist-page')  // 路由表里没有这个路径

// 场景3：导航守卫阻止
// 比如没登录不能访问某些页面
```

### 5.2 导航故障的类型


**📋 Vue Router内置的故障类型**
```javascript
import { NavigationFailureType } from 'vue-router'

// 导航被导航守卫终止
NavigationFailureType.aborted

// 导航被另一个导航取代
NavigationFailureType.cancelled  

// 重复导航到相同位置
NavigationFailureType.duplicated
```

### 5.3 如何处理导航故障


**🔧 基本处理方式**
```javascript
// 方式1：简单的错误捕获
this.$router.push('/home').catch(err => {
  console.log('跳转失败了:', err)
})

// 方式2：详细的故障处理
import { isNavigationFailure, NavigationFailureType } from 'vue-router'

this.$router.push('/home').catch(failure => {
  if (isNavigationFailure(failure, NavigationFailureType.duplicated)) {
    console.log('重复跳转到相同页面')
    // 可以忽略这个错误
  } else {
    console.log('其他跳转错误:', failure)
  }
})
```

**🎯 实用的处理封装**
```javascript
// 在方法中封装一个安全的跳转函数
methods: {
  safeNavigate(to) {
    this.$router.push(to).catch(failure => {
      // 重复导航就忽略，其他错误才提示
      if (!isNavigationFailure(failure, NavigationFailureType.duplicated)) {
        this.$message.error('页面跳转失败')
      }
    })
  },
  
  // 使用封装的安全跳转
  goHome() {
    this.safeNavigate('/home')
  }
}
```

### 5.4 预防导航故障的最佳实践


**✅ 预防措施**
```javascript
// 1. 跳转前检查当前路由
goToPage(path) {
  if (this.$route.path !== path) {
    this.$router.push(path)
  }
}

// 2. 使用防抖避免重复点击
import { debounce } from 'lodash'

methods: {
  goHome: debounce(function() {
    this.$router.push('/home')
  }, 300)  // 300ms内只能点击一次
}

// 3. 按钮点击后禁用
data() {
  return {
    navigating: false
  }
},
methods: {
  async goHome() {
    if (this.navigating) return
    
    this.navigating = true
    try {
      await this.$router.push('/home')
    } finally {
      this.navigating = false
    }
  }
}
```

---

## 6. 🎪 实际应用场景


### 6.1 表单提交后跳转


**📝 典型场景：用户注册/登录**
```vue
<template>
  <form @submit.prevent="handleSubmit">
    <input v-model="username" placeholder="用户名">
    <input v-model="password" type="password" placeholder="密码">
    <button type="submit" :disabled="loading">
      {{ loading ? '登录中...' : '登录' }}
    </button>
  </form>
</template>

<script>
export default {
  data() {
    return {
      username: '',
      password: '',
      loading: false
    }
  },
  methods: {
    async handleSubmit() {
      this.loading = true
      
      try {
        // 调用登录接口
        await this.login()
        
        // 登录成功，跳转到首页（不保留登录页历史）
        this.$router.replace('/home')
        
        this.$message.success('登录成功')
      } catch (error) {
        this.$message.error('登录失败：' + error.message)
      } finally {
        this.loading = false
      }
    }
  }
}
</script>
```

### 6.2 权限检查后跳转


**🔒 需要登录才能访问的页面**
```javascript
methods: {
  goToUserCenter() {
    // 检查是否已登录
    if (!this.$store.getters.isLoggedIn) {
      // 未登录，先跳转到登录页
      this.$router.push({
        name: 'login',
        query: { 
          redirect: '/user-center'  // 登录后要跳转的页面
        }
      })
      return
    }
    
    // 已登录，直接跳转
    this.$router.push('/user-center')
  },
  
  goToVIPPage() {
    const user = this.$store.state.user
    
    if (!user.isVIP) {
      // 不是VIP用户，跳转到升级页面
      this.$router.push('/upgrade-vip')
      this.$message.warning('该功能仅限VIP用户使用')
      return
    }
    
    // 是VIP用户，可以访问
    this.$router.push('/vip-exclusive')
  }
}
```

### 6.3 动态路由跳转


**🔄 根据数据动态决定跳转目标**
```javascript
methods: {
  viewItem(item) {
    // 根据商品类型跳转到不同页面
    switch (item.type) {
      case 'product':
        this.$router.push({
          name: 'productDetail',
          params: { id: item.id }
        })
        break
        
      case 'article':
        this.$router.push({
          name: 'articleDetail', 
          params: { id: item.id }
        })
        break
        
      case 'video':
        this.$router.push({
          name: 'videoPlayer',
          params: { id: item.id },
          query: { autoplay: true }
        })
        break
        
      default:
        this.$message.error('未知的内容类型')
    }
  },
  
  // 搜索结果跳转
  searchAndJump(keyword) {
    if (!keyword.trim()) {
      this.$message.warning('请输入搜索关键词')
      return
    }
    
    this.$router.push({
      name: 'searchResult',
      query: { 
        q: keyword,
        timestamp: Date.now()  // 确保每次搜索都会刷新
      }
    })
  }
}
```

### 6.4 Tab切换导航


**📑 在单页面内切换不同内容**
```vue
<template>
  <div class="tabs">
    <!-- 使用查询参数控制tab -->
    <div class="tab-buttons">
      <button 
        v-for="tab in tabs" 
        :key="tab.name"
        :class="{ active: currentTab === tab.name }"
        @click="switchTab(tab.name)"
      >
        {{ tab.label }}
      </button>
    </div>
    
    <!-- 根据当前tab显示内容 -->
    <div class="tab-content">
      <ProfileInfo v-if="currentTab === 'profile'" />
      <OrderHistory v-if="currentTab === 'orders'" />
      <Settings v-if="currentTab === 'settings'" />
    </div>
  </div>
</template>

<script>
export default {
  data() {
    return {
      tabs: [
        { name: 'profile', label: '个人信息' },
        { name: 'orders', label: '订单历史' },
        { name: 'settings', label: '设置' }
      ]
    }
  },
  
  computed: {
    currentTab() {
      // 从URL查询参数获取当前tab
      return this.$route.query.tab || 'profile'
    }
  },
  
  methods: {
    switchTab(tabName) {
      // 切换tab时更新URL
      this.$router.replace({
        query: { ...this.$route.query, tab: tabName }
      })
    }
  }
}
</script>
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 声明式导航：<router-link>，模板中直接使用
🔸 编程式导航：$router方法，JavaScript代码控制
🔸 三大导航方法：push()添加历史、replace()替换历史、go()历史中移动
🔸 路径跳转：直接写URL路径
🔸 命名路由：给路由起名字，用名字跳转
🔸 导航故障：跳转失败的情况和处理方法
```

### 7.2 关键理解要点


**🔹 什么时候用哪种导航方式**
```
简单链接 → 声明式导航 <router-link>
按钮点击 → 编程式导航 $router.push()
需要逻辑判断 → 编程式导航
表单提交后 → 编程式导航
```

**🔹 三种跳转方法的选择**
```
普通跳转 → push()（最常用）
登录后跳转 → replace()（不能返回登录页）
返回上页 → go(-1)（相当于浏览器后退）
```

**🔹 路径 vs 命名路由的选择**
```
简单路径 → 直接写路径 '/home'
复杂路径 → 推荐用命名路由 { name: 'home' }
带参数跳转 → 推荐用命名路由，更清晰
```

### 7.3 实际应用价值


**🎯 常见应用场景**
- **导航菜单**：使用声明式导航快速实现
- **按钮跳转**：使用编程式导航，可加逻辑判断
- **表单提交**：成功后用replace()跳转
- **权限控制**：跳转前检查用户权限
- **搜索功能**：带参数的页面跳转

**🔧 最佳实践建议**
- **优先使用命名路由**：维护更容易
- **处理导航故障**：避免控制台报错
- **合理使用历史记录**：提升用户体验
- **参数传递要清晰**：params用于必需参数，query用于可选参数

**核心记忆口诀**：
```
声明编程两导航，模板代码各有长
push添加replace换，go方法前后翻
路径命名两跳转，名字维护更方便
故障处理要做好，用户体验才会高
```