---
title: 1、Vue Router基础配置
---
## 📚 目录

1. [什么是Vue Router](#1-什么是vue-router)
2. [安装与初始配置](#2-安装与初始配置)
3. [路由表配置详解](#3-路由表配置详解)
4. [核心组件使用](#4-核心组件使用)
5. [路由模式选择](#5-路由模式选择)
6. [基础路由跳转](#6-基础路由跳转)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🌐 什么是Vue Router


### 1.1 路由的本质概念


**🔸 简单理解**
```
想象你在一个大商场：
- 不同楼层 = 不同页面
- 楼层指示牌 = 路由配置
- 电梯/扶梯 = 路由跳转
- 你当前位置 = 当前路由

Vue Router就是这个"导航系统"
```

**🔸 技术定义**
```
Vue Router = Vue.js的官方路由管理器
作用：让单页面应用(SPA)实现多页面效果
原理：根据URL显示不同组件，无需刷新页面
```

### 1.2 为什么需要路由


**传统多页面应用**：
```
用户点击链接 → 浏览器请求新页面 → 服务器返回HTML → 页面刷新
问题：页面刷新慢，用户体验差
```

**SPA + 路由**：
```
用户点击链接 → 路由切换 → 显示新组件 → 页面无刷新
优势：切换快速，体验流畅
```

### 1.3 核心功能一览


**📋 Vue Router主要功能**
- ✅ **页面导航**：实现组件间切换
- ✅ **URL管理**：同步URL与页面状态  
- ✅ **路由守卫**：控制页面访问权限
- ✅ **参数传递**：页面间数据传递
- ✅ **嵌套路由**：复杂页面结构支持

---

## 2. 🛠️ 安装与初始配置


### 2.1 安装Vue Router


**📦 安装命令**
```bash
# npm 安装
npm install vue-router@4

# yarn 安装  
yarn add vue-router@4
```

> 💡 **版本说明**：Vue 3.x 使用 vue-router@4，Vue 2.x 使用 vue-router@3

### 2.2 基础项目结构


**📁 推荐的项目目录**
```
src/
├── components/          # 公共组件
├── views/              # 页面组件
│   ├── Home.vue        # 首页
│   ├── About.vue       # 关于页
│   └── Contact.vue     # 联系页
├── router/
│   └── index.js        # 路由配置
└── main.js            # 入口文件
```

### 2.3 创建基础页面组件


**🏠 Home.vue - 首页组件**
```vue
<template>
  <div class="home">
    <h1>欢迎来到首页</h1>
    <p>这是网站的首页内容</p>
  </div>
</template>

<script>
export default {
  name: 'Home'
}
</script>
```

**📄 About.vue - 关于页组件**
```vue
<template>
  <div class="about">
    <h1>关于我们</h1>
    <p>这里是关于页面的内容</p>
  </div>
</template>

<script>
export default {
  name: 'About'
}
</script>
```

---

## 3. ⚙️ 路由表配置详解


### 3.1 创建路由配置文件


**📋 router/index.js - 路由配置**
```javascript
import { createRouter, createWebHistory } from 'vue-router'
import Home from '../views/Home.vue'
import About from '../views/About.vue'

// 路由配置数组
const routes = [
  {
    path: '/',          // URL路径
    name: 'Home',       // 路由名称
    component: Home     // 对应组件
  },
  {
    path: '/about',
    name: 'About', 
    component: About
  }
]

// 创建路由实例
const router = createRouter({
  history: createWebHistory(),  // 路由模式
  routes                        // 路由配置
})

export default router
```

### 3.2 路由配置对象详解


**🔧 路由配置项说明**

| 配置项 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `path` | String | URL路径 | `'/'`, `'/about'` |
| `name` | String | 路由名称 | `'Home'`, `'About'` |
| `component` | Component | 页面组件 | `Home`, `About` |
| `redirect` | String | 重定向路径 | `'/home'` |
| `alias` | String/Array | 路径别名 | `'/index'` |

**💡 实用配置示例**
```javascript
const routes = [
  // 基础路由
  { path: '/', name: 'Home', component: Home },
  
  // 路由重定向
  { path: '/index', redirect: '/' },
  
  // 路由别名
  { path: '/about', alias: '/关于', component: About },
  
  // 懒加载路由
  { 
    path: '/contact', 
    component: () => import('../views/Contact.vue')
  }
]
```

### 3.3 在main.js中注册路由


**⚡ main.js - 应用入口**
```javascript
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

const app = createApp(App)

// 使用路由
app.use(router)

app.mount('#app')
```

---

## 4. 🧩 核心组件使用


### 4.1 router-view 路由出口


**🔸 概念解释**
```
router-view = 路由组件的"显示窗口"
作用：根据当前路由显示对应组件
位置：通常放在App.vue中
```

**📱 App.vue - 根组件配置**
```vue
<template>
  <div id="app">
    <!-- 导航菜单 -->
    <nav>
      <router-link to="/">首页</router-link>
      <router-link to="/about">关于</router-link>
    </nav>
    
    <!-- 路由组件显示区域 -->
    <router-view></router-view>
  </div>
</template>

<style>
nav {
  padding: 20px;
  background: #f0f0f0;
}

nav a {
  margin-right: 20px;
  text-decoration: none;
  color: #333;
}
</style>
```

### 4.2 router-link 导航链接


**🔸 基础用法**
```vue
<!-- 最简单的用法 -->
<router-link to="/">首页</router-link>
<router-link to="/about">关于我们</router-link>

<!-- 使用路由名称跳转 -->
<router-link :to="{ name: 'Home' }">首页</router-link>
<router-link :to="{ name: 'About' }">关于</router-link>
```

**🎨 样式相关属性**
```vue
<template>
  <!-- 自定义激活样式 -->
  <router-link 
    to="/about" 
    active-class="active-link"
    exact-active-class="exact-active">
    关于我们
  </router-link>
</template>

<style>
.active-link {
  color: #42b983;
  font-weight: bold;
}

.exact-active {
  background-color: #42b983;
  color: white;
}
</style>
```

**📋 常用属性对比**

| 属性 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `to` | 目标路由 | - | `'/about'` |
| `tag` | 渲染标签 | `'a'` | `'button'` |
| `active-class` | 激活时CSS类 | `'router-link-active'` | `'active'` |
| `exact` | 精确匹配 | `false` | `true` |

---

## 5. 🔄 路由模式选择


### 5.1 History模式 vs Hash模式


**📊 两种模式对比**

```
History模式：
URL样式：http://example.com/about
原理：使用HTML5 History API
优点：URL美观，SEO友好
缺点：需要服务器配置

Hash模式：
URL样式：http://example.com/#/about  
原理：使用URL的hash部分
优点：无需服务器配置
缺点：URL有#号，SEO不友好
```

### 5.2 History模式配置


**⚙️ createWebHistory() - 推荐模式**
```javascript
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  // History模式 - 现代化URL
  history: createWebHistory(),
  routes
})
```

**🔧 服务器配置说明**
```
使用History模式需要服务器支持：
- Apache：配置.htaccess文件
- Nginx：配置try_files指令
- 开发环境：Vue CLI自动处理
```

### 5.3 Hash模式配置


**⚙️ createWebHashHistory() - 兼容模式**
```javascript
import { createRouter, createWebHashHistory } from 'vue-router'

const router = createRouter({
  // Hash模式 - 兼容性好
  history: createWebHashHistory(),
  routes
})
```

**💡 选择建议**
- ✅ **History模式**：现代项目首选，URL美观
- ✅ **Hash模式**：老项目或服务器限制时使用

---

## 6. 🚀 基础路由跳转


### 6.1 声明式跳转


**🔗 使用router-link组件**
```vue
<template>
  <!-- 字符串路径 -->
  <router-link to="/about">关于我们</router-link>
  
  <!-- 对象形式 -->
  <router-link :to="{ path: '/about' }">关于我们</router-link>
  
  <!-- 使用路由名称 -->
  <router-link :to="{ name: 'About' }">关于我们</router-link>
</template>
```

### 6.2 编程式跳转


**💻 使用$router.push()方法**
```vue
<template>
  <button @click="goToAbout">去关于页</button>
  <button @click="goBack">返回上一页</button>
</template>

<script>
export default {
  methods: {
    // 跳转到指定页面
    goToAbout() {
      // 方式1：字符串路径
      this.$router.push('/about')
      
      // 方式2：对象形式
      // this.$router.push({ path: '/about' })
      
      // 方式3：路由名称
      // this.$router.push({ name: 'About' })
    },
    
    // 返回上一页
    goBack() {
      this.$router.go(-1)
      // 或者：this.$router.back()
    }
  }
}
</script>
```

### 6.3 路由跳转方法对比


**📋 常用跳转方法**

| 方法 | 说明 | 历史记录 | 示例 |
|------|------|----------|------|
| `push()` | 跳转到新页面 | 添加记录 | `$router.push('/about')` |
| `replace()` | 替换当前页面 | 替换记录 | `$router.replace('/about')` |
| `go()` | 前进/后退 | 不变 | `$router.go(-1)` |
| `back()` | 后退一页 | 不变 | `$router.back()` |
| `forward()` | 前进一页 | 不变 | `$router.forward()` |

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的基本概念


```
🔸 Vue Router：Vue.js官方路由管理器，实现SPA页面切换
🔸 路由表routes：配置URL与组件的对应关系
🔸 router-view：路由组件的显示出口
🔸 router-link：声明式导航链接组件
🔸 路由模式：History模式(美观)和Hash模式(兼容)
```

### 7.2 关键配置步骤


**🔹 完整配置流程**
```
1. 安装vue-router包
2. 创建页面组件(views目录)
3. 配置路由表(router/index.js)
4. 注册路由到Vue应用(main.js)
5. 使用router-view显示组件(App.vue)
6. 使用router-link实现导航
```

### 7.3 实用开发技巧


**💡 最佳实践建议**
- ✅ **组件分类**：页面组件放views，公共组件放components
- ✅ **懒加载**：大页面使用`() => import()`懒加载
- ✅ **路由命名**：给路由设置name便于跳转
- ✅ **模式选择**：优先使用History模式
- ✅ **样式管理**：合理使用active-class设置导航样式

**🔹 常见问题解决**
```
问题1：页面不显示
→ 检查router-view是否正确放置

问题2：路由跳转无效  
→ 检查路径是否正确配置

问题3：样式不生效
→ 检查active-class类名是否定义

问题4：History模式404
→ 检查服务器是否正确配置
```

### 7.4 学习路径建议


**🎯 学习顺序**
1. 理解SPA和路由概念
2. 掌握基础配置和组件使用
3. 学习路由跳转和参数传递
4. 深入路由守卫和权限控制
5. 实践复杂路由场景

**核心记忆**：
- Vue Router让单页应用实现多页效果
- routes配置URL与组件对应关系
- router-view显示组件，router-link实现导航
- History模式URL美观，Hash模式兼容性好
- 声明式跳转用router-link，编程式跳转用$router