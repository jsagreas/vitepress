---
title: 3、Vue实例方法与属性
---
## 📚 目录

1. [Vue实例的核心概念](#1-Vue实例的核心概念)
2. [$nextTick异步更新机制](#2-nextTick异步更新机制)
3. [$watch数据监听器](#3-watch数据监听器)
4. [$refs组件引用](#4-refs组件引用)
5. [$set和$delete响应式操作](#5-set和delete响应式操作)
6. [实例属性与配置](#6-实例属性与配置)
7. [挂载点与生命周期](#7-挂载点与生命周期)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 Vue实例的核心概念


### 1.1 什么是Vue实例


**简单理解**：Vue实例就像是一个"智能管家"，它管理着你页面上的数据、方法和各种操作。

```
现实生活类比：
智能管家 = Vue实例
├── 房间状态 = data数据
├── 遥控器 = methods方法  
├── 监控系统 = watch监听
└── 呼叫铃 = $refs引用
```

### 1.2 实例是如何工作的


```javascript
// 创建一个Vue实例，就像雇佣一个管家
const app = new Vue({
  el: '#app',        // 管家负责的区域
  data: {            // 管家要管理的物品
    message: 'Hello Vue!'
  },
  methods: {         // 管家会做的事情
    sayHello() {
      alert(this.message)
    }
  }
})

// 现在这个管家（app）就开始工作了
```

### 1.3 实例方法的特点


```
实例方法的命名规则：
- 以 $ 开头：$nextTick、$watch、$refs
- 为什么用 $？避免与用户定义的属性冲突
- 就像给"系统功能"贴个特殊标签
```

---

## 2. ⏰ $nextTick异步更新机制


### 2.1 为什么需要$nextTick


**核心问题**：Vue更新DOM不是立即的，而是"批量处理"的。

```
现实例子：
你在手机上连续发了3条消息
手机不会一条一条地发送
而是等一小会儿，把3条消息一起发出去
这样更高效，不会卡顿

Vue的DOM更新也是这个道理
```

### 2.2 什么时候用$nextTick


```javascript
// 问题场景：修改数据后立即获取DOM
data() {
  return {
    message: 'Hello'
  }
},
methods: {
  updateMessage() {
    this.message = 'Hello Vue!'
    
    // ❌ 错误：这时DOM还没更新
    console.log(this.$refs.msgDiv.textContent) // 还是 "Hello"
    
    // ✅ 正确：等DOM更新完再执行
    this.$nextTick(() => {
      console.log(this.$refs.msgDiv.textContent) // "Hello Vue!"
    })
  }
}
```

### 2.3 $nextTick的使用场景


```javascript
// 场景1：获取更新后的DOM尺寸
methods: {
  addItem() {
    this.list.push('新项目')
    
    this.$nextTick(() => {
      // 现在可以获取新的列表高度了
      const height = this.$refs.list.scrollHeight
      console.log('列表新高度:', height)
    })
  },
  
  // 场景2：聚焦到新创建的输入框
  showInput() {
    this.inputVisible = true
    
    this.$nextTick(() => {
      this.$refs.input.focus() // 输入框已经渲染出来了
    })
  }
}
```

### 2.4 Promise形式的$nextTick


```javascript
// 现代写法：async/await
async updateAndGet() {
  this.count++
  
  // 等待DOM更新
  await this.$nextTick()
  
  // 现在可以安全地操作DOM了
  const newValue = this.$refs.counter.textContent
  console.log('更新后的值:', newValue)
}
```

---

## 3. 👀 $watch数据监听器


### 3.1 $watch是做什么的


**简单理解**：就像给数据安装一个"监控摄像头"，数据一变化就立即通知你。

```
生活例子：
你在网上关注某个商品的价格
一旦降价，就给你发通知
$watch 就是这个"价格监控器"
```

### 3.2 基本用法


```javascript
data() {
  return {
    searchText: '',
    userInfo: {
      name: 'Tom',
      age: 20
    }
  }
},
mounted() {
  // 监听简单数据
  this.$watch('searchText', (newVal, oldVal) => {
    console.log(`搜索词从 "${oldVal}" 变成了 "${newVal}"`)
    // 执行搜索逻辑
    this.doSearch(newVal)
  })
  
  // 监听对象属性
  this.$watch('userInfo.name', (newName) => {
    console.log('用户名变了:', newName)
  })
}
```

### 3.3 深度监听


```javascript
mounted() {
  // 监听整个对象的变化
  this.$watch('userInfo', {
    handler(newVal, oldVal) {
      console.log('用户信息发生了变化')
      this.saveUserInfo(newVal)
    },
    deep: true,      // 深度监听：对象内部任何变化都会触发
    immediate: true  // 立即执行：组件创建时就执行一次
  })
}
```

### 3.4 停止监听


```javascript
mounted() {
  // $watch 返回一个"停止函数"
  const stopWatching = this.$watch('someData', (newVal) => {
    console.log('数据变了:', newVal)
  })
  
  // 某个时候不想监听了
  setTimeout(() => {
    stopWatching() // 调用这个函数就停止监听了
    console.log('不再监听 someData')
  }, 5000)
}
```

### 3.5 实用场景


```javascript
methods: {
  // 场景1：搜索防抖
  setupSearchWatch() {
    let timer = null
    
    this.$watch('searchKeyword', (keyword) => {
      // 清除上次的定时器
      clearTimeout(timer)
      
      // 500ms后执行搜索
      timer = setTimeout(() => {
        this.performSearch(keyword)
      }, 500)
    })
  },
  
  // 场景2：表单验证
  setupFormValidation() {
    this.$watch('form.email', (email) => {
      this.emailError = this.validateEmail(email) ? '' : '邮箱格式不正确'
    })
  }
}
```

---

## 4. 📍 $refs组件引用


### 4.1 $refs是什么


**简单理解**：给页面元素贴个"名字标签"，然后通过名字直接找到它。

```
现实例子：
你在停车场给你的车贴个 "我的车" 标签
下次要找车时，直接喊 "我的车在哪"
$refs 就是这个标签系统
```

### 4.2 基本使用


```vue
<template>
  <!-- 给元素贴标签 -->
  <input ref="usernameInput" v-model="username">
  <button ref="submitBtn" @click="submit">提交</button>
  
  <!-- 给组件贴标签 -->
  <UserCard ref="userCard" :user="user" />
</template>

<script>
export default {
  methods: {
    submit() {
      // 通过标签名找到元素
      if (!this.username) {
        this.$refs.usernameInput.focus() // 聚焦到输入框
        return
      }
      
      // 调用子组件的方法
      this.$refs.userCard.refresh()
    },
    
    resetForm() {
      // 清空输入框
      this.$refs.usernameInput.value = ''
      
      // 禁用按钮
      this.$refs.submitBtn.disabled = true
    }
  }
}
</script>
```

### 4.3 操作DOM元素


```javascript
methods: {
  // 场景1：获取元素信息
  getElementInfo() {
    const input = this.$refs.myInput
    console.log('输入框宽度:', input.offsetWidth)
    console.log('输入框的值:', input.value)
  },
  
  // 场景2：修改样式
  highlightError() {
    this.$refs.errorMessage.style.color = 'red'
    this.$refs.errorMessage.style.fontWeight = 'bold'
  },
  
  // 场景3：滚动操作
  scrollToBottom() {
    const chatBox = this.$refs.chatContainer
    chatBox.scrollTop = chatBox.scrollHeight
  }
}
```

### 4.4 访问子组件


```vue
<!-- 父组件 -->
<template>
  <ChildComponent ref="child" />
  <button @click="callChildMethod">调用子组件方法</button>
</template>

<script>
export default {
  methods: {
    callChildMethod() {
      // 调用子组件的方法
      this.$refs.child.childMethod()
      
      // 获取子组件的数据
      console.log('子组件的数据:', this.$refs.child.childData)
      
      // 修改子组件的数据
      this.$refs.child.childData = '新值'
    }
  }
}
</script>
```

### 4.5 使用注意事项


```javascript
// ⚠️ 注意：$refs 只能在组件挂载后使用
mounted() {
  // ✅ 正确：组件已经挂载
  this.$refs.myInput.focus()
},

created() {
  // ❌ 错误：组件还没挂载，DOM还不存在
  // this.$refs.myInput.focus() // 会报错
},

// ⚠️ 注意：v-if 的元素可能不存在
methods: {
  focusInput() {
    // ✅ 安全的写法
    if (this.$refs.myInput) {
      this.$refs.myInput.focus()
    }
  }
}
```

---

## 5. 🔧 $set和$delete响应式操作


> **📝 说明**：这两个方法主要用于Vue 2，Vue 3已经不需要了。

### 5.1 为什么需要$set和$delete


**Vue 2的限制**：直接给对象添加属性或删除属性，Vue检测不到变化。

```javascript
// Vue 2 中的问题
data() {
  return {
    userInfo: {
      name: 'Tom'
    }
  }
},
methods: {
  // ❌ Vue 2 检测不到这个变化
  addAge() {
    this.userInfo.age = 25  // 页面不会更新
  },
  
  // ❌ Vue 2 检测不到这个删除
  removeName() {
    delete this.userInfo.name  // 页面不会更新
  }
}
```

### 5.2 $set的使用


```javascript
methods: {
  // ✅ 正确的添加属性方式
  addAge() {
    // this.$set(对象, 属性名, 值)
    this.$set(this.userInfo, 'age', 25)
    // 现在页面会正确更新
  },
  
  // 数组操作
  updateArrayItem() {
    // ❌ 错误：直接修改数组索引
    // this.list[0] = '新值'  // Vue 2 检测不到
    
    // ✅ 正确：使用 $set
    this.$set(this.list, 0, '新值')
  }
}
```

### 5.3 $delete的使用


```javascript
methods: {
  // ✅ 正确的删除属性方式
  removeAge() {
    // this.$delete(对象, 属性名)
    this.$delete(this.userInfo, 'age')
    // 现在页面会正确更新
  },
  
  // 删除数组元素
  removeArrayItem(index) {
    this.$delete(this.list, index)
  }
}
```

### 5.4 Vue 3的改进


```javascript
// Vue 3 中已经不需要 $set 和 $delete 了
methods: {
  // ✅ Vue 3 中直接这样写就行
  addAge() {
    this.userInfo.age = 25  // 页面会自动更新
  },
  
  removeAge() {
    delete this.userInfo.age  // 页面会自动更新
  }
}
```

---

## 6. 🏠 实例属性与配置


### 6.1 常用实例属性


```javascript
// Vue 实例的重要属性
export default {
  mounted() {
    // $el: 实例挂载的根元素
    console.log('根元素:', this.$el)
    
    // $data: 实例的数据对象
    console.log('所有数据:', this.$data)
    
    // $options: 创建实例时的选项
    console.log('实例选项:', this.$options)
    
    // $parent: 父实例（如果有的话）
    console.log('父组件:', this.$parent)
    
    // $children: 子实例数组
    console.log('子组件:', this.$children)
    
    // $root: 根实例
    console.log('根实例:', this.$root)
  }
}
```

### 6.2 全局配置Vue.config


```javascript
// 在main.js中配置
import Vue from 'vue'

// 开发环境配置
if (process.env.NODE_ENV === 'development') {
  // 显示性能提示
  Vue.config.performance = true
  
  // 显示开发提示
  Vue.config.devtools = true
}

// 生产环境配置
if (process.env.NODE_ENV === 'production') {
  // 关闭生产提示
  Vue.config.productionTip = false
  
  // 关闭开发工具
  Vue.config.devtools = false
}

// 全局错误处理
Vue.config.errorHandler = (err, vm, info) => {
  console.error('Vue错误:', err)
  console.error('组件:', vm)
  console.error('错误信息:', info)
}
```

### 6.3 自定义全局属性


```javascript
// 添加全局方法
Vue.prototype.$utils = {
  formatDate(date) {
    return date.toLocaleDateString()
  },
  
  showMessage(msg) {
    alert(msg)
  }
}

// 在组件中使用
export default {
  methods: {
    handleClick() {
      // 使用全局工具方法
      const today = this.$utils.formatDate(new Date())
      this.$utils.showMessage(`今天是 ${today}`)
    }
  }
}
```

---

## 7. 🎪 挂载点与生命周期


### 7.1 什么是挂载点


**简单理解**：挂载点就是Vue实例"落户"的地方，指定Vue管理哪块页面区域。

```html
<!-- index.html -->
<body>
  <div id="app">
    <!-- Vue 实例管理这里面的内容 -->
    {{ message }}
  </div>
  
  <div id="other">
    <!-- Vue 管不到这里 -->
    这里Vue管不到
  </div>
</body>
```

### 7.2 挂载的几种方式


```javascript
// 方式1：创建时指定
const app = new Vue({
  el: '#app',  // 直接指定挂载点
  data: {
    message: 'Hello!'
  }
})

// 方式2：手动挂载
const app = new Vue({
  data: {
    message: 'Hello!'
  }
})
app.$mount('#app')  // 手动挂载到#app

// 方式3：挂载到新创建的元素
const app = new Vue({
  data: {
    message: 'Hello!'
  }
})
const el = document.createElement('div')
document.body.appendChild(el)
app.$mount(el)  // 挂载到新元素
```

### 7.3 挂载点与生命周期的关系


```
Vue实例的"人生历程"：

创建阶段：
beforeCreate → created
(准备好了数据和方法)

挂载阶段：
beforeMount → mounted
(找到挂载点，渲染到页面)

运行阶段：
beforeUpdate → updated
(数据变化时更新页面)

销毁阶段：
beforeDestroy → destroyed
(从页面移除，清理资源)
```

### 7.4 实际挂载示例


```javascript
// main.js - 项目入口
import Vue from 'vue'
import App from './App.vue'

new Vue({
  render: h => h(App),  // 渲染App组件
}).$mount('#app')       // 挂载到#app元素

// 等价于：
new Vue({
  el: '#app',
  render: h => h(App),
})
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 Vue实例：页面的"智能管家"，管理数据和操作
🔸 $nextTick：等待DOM更新完成后再执行
🔸 $watch：给数据安装"监控器"，变化时立即通知
🔸 $refs：给元素贴"标签"，通过标签直接操作
🔸 $set/$delete：Vue2中响应式地添加/删除属性
🔸 挂载点：Vue实例管理的页面区域
```

### 8.2 使用场景对应表


| **场景** | **使用方法** | **典型用途** |
|---------|-------------|-------------|
| 修改数据后操作DOM | `$nextTick` | 获取更新后的元素尺寸、聚焦输入框 |
| 监听数据变化 | `$watch` | 搜索防抖、表单验证、数据同步 |
| 直接操作DOM/组件 | `$refs` | 聚焦元素、调用子组件方法、获取DOM信息 |
| 动态添加对象属性 | `$set` | 用户信息补充、动态表单字段 |
| 删除对象属性 | `$delete` | 清除临时数据、权限控制 |

### 8.3 最佳实践建议


**✅ 推荐做法**：
```javascript
// 1. 使用$nextTick确保DOM更新
this.$nextTick(() => {
  this.$refs.input.focus()
})

// 2. 及时清理$watch监听器
const stop = this.$watch('data', handler)
// 组件销毁时
beforeDestroy() {
  stop()
}

// 3. $refs访问前检查存在性
if (this.$refs.myComponent) {
  this.$refs.myComponent.method()
}
```

**❌ 避免的做法**：
```javascript
// 1. 不要在created中使用$refs
created() {
  // this.$refs.input.focus() // 错误：DOM还没创建
}

// 2. 不要过度使用$refs操作DOM
// 应该优先使用数据驱动的方式

// 3. Vue3中不要使用$set/$delete
// 直接修改对象属性即可
```

### 8.4 记忆要点


**🎯 核心理解**：
- **$nextTick**：等一等，让Vue先更新完DOM
- **$watch**：当数据变化时，我要知道
- **$refs**：给我这个元素/组件的直接控制权
- **$set/$delete**：让Vue2知道对象属性的变化
- **挂载点**：Vue的"势力范围"

**🔧 使用原则**：
- 能用数据驱动就不直接操作DOM
- 需要DOM操作时才用$refs
- 异步DOM操作要用$nextTick
- Vue3优先，不需要$set/$delete