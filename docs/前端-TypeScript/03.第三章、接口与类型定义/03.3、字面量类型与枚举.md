---
title: 3、字面量类型与枚举
---
## 📚 目录

1. [字面量类型基础](#1-字面量类型基础)
2. [模板字面量类型](#2-模板字面量类型)
3. [枚举类型详解](#3-枚举类型详解)
4. [联合枚举与类型兼容](#4-联合枚举与类型兼容)
5. [实际应用场景](#5-实际应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 字面量类型基础


### 1.1 什么是字面量类型


想象一下，你在编程时经常遇到这样的情况：某个变量只能是特定的几个值，比如状态只能是"成功"、"失败"、"进行中"。字面量类型就是用来解决这个问题的。

**🔸 简单理解**：
- 普通类型：`string` 表示任意字符串
- 字面量类型：`"success"` 只能是这个具体字符串

```typescript
// 普通string类型，可以是任意字符串
let status: string = "success";
status = "failed";     // ✅ 可以
status = "anything";   // ✅ 也可以

// 字面量类型，只能是指定的值
let state: "success" | "failed" | "loading" = "success";
state = "failed";      // ✅ 可以
state = "anything";    // ❌ 错误！只能是指定的三个值
```

### 1.2 三种字面量类型


#### 📝 字符串字面量


最常用的字面量类型，用于限制字符串的具体值：

```typescript
// 主题类型，只能是这三种
type Theme = "light" | "dark" | "auto";

function setTheme(theme: Theme) {
    console.log(`切换到${theme}主题`);
}

setTheme("light");    // ✅ 正确
setTheme("blue");     // ❌ 错误，不是指定的主题
```

#### 🔢 数字字面量


用于限制数字的具体值，常用于状态码、配置项：

```typescript
// HTTP状态码
type HttpStatus = 200 | 404 | 500;

// 分页大小，只允许这几种
type PageSize = 10 | 20 | 50;

function setPageSize(size: PageSize) {
    console.log(`每页显示${size}条`);
}

setPageSize(20);     // ✅ 正确
setPageSize(15);     // ❌ 错误，不在允许的范围内
```

#### ✅ 布尔字面量


虽然用得少，但在某些特殊场景很有用：

```typescript
// 只能是true的类型
type OnlyTrue = true;

// 常用于配置项，表示某功能必须开启
interface Config {
    debug: false;           // 生产环境，debug必须是false
    autoSave: true;         // 自动保存必须开启
}
```

### 1.3 字面量类型的实际价值


```typescript
// 🎯 实际应用：API响应状态
interface ApiResponse {
    status: "success" | "error";
    code: 200 | 400 | 500;
    message: string;
}

// 使用时IDE会有智能提示，防止拼写错误
function handleResponse(response: ApiResponse) {
    if (response.status === "success") {
        // 只有输入"success"时才会进入这里
        console.log("操作成功");
    }
    // 如果写成"sucess"（少了c），编译器会报错
}
```

---

## 2. 🔧 模板字面量类型


### 2.1 什么是模板字面量类型


这是TypeScript 4.1引入的强大功能，可以像写模板字符串一样定义类型：

```typescript
// 基础用法：组合固定的字符串
type Greeting = `Hello ${string}`;

let msg1: Greeting = "Hello World";      // ✅ 正确
let msg2: Greeting = "Hello TypeScript"; // ✅ 正确
let msg3: Greeting = "Hi there";         // ❌ 错误，必须以"Hello "开头
```

### 2.2 实用的模板字面量


#### 🎨 CSS属性生成


```typescript
// 生成CSS属性名
type CSSProperty = "margin" | "padding";
type Direction = "top" | "right" | "bottom" | "left";

// 自动生成所有组合：margin-top, margin-right, padding-left 等
type CSSPropertyWithDirection = `${CSSProperty}-${Direction}`;

// 实际使用
interface Styles {
    "margin-top": string;
    "padding-left": string;
    // ... 其他组合
}
```

#### 🗂️ 事件名称约定


```typescript
// 事件类型约定
type EventName = "click" | "change" | "submit";
type EventHandler = `on${Capitalize<EventName>}`;

// 生成：onClick, onChange, onSubmit
interface Props {
    onClick?: () => void;
    onChange?: (value: string) => void;
    onSubmit?: () => void;
}
```

### 2.3 模板字面量的组合威力


```typescript
// 🚀 路由系统示例
type HttpMethod = "GET" | "POST" | "PUT" | "DELETE";
type ApiVersion = "v1" | "v2";
type Resource = "users" | "posts" | "comments";

// 自动生成API路径类型
type ApiEndpoint = `/${ApiVersion}/${Resource}`;
// 结果："/v1/users" | "/v1/posts" | "/v2/users" 等

type ApiRoute = `${HttpMethod} ${ApiEndpoint}`;
// 结果："GET /v1/users" | "POST /v2/posts" 等

function defineRoute(route: ApiRoute) {
    console.log(`定义路由：${route}`);
}

defineRoute("GET /v1/users");    // ✅ 正确
defineRoute("PATCH /v3/admin");  // ❌ 错误，不符合约定
```

---

## 3. 📋 枚举类型详解


### 3.1 为什么需要枚举


想象你在开发一个订单系统，订单有不同状态：待支付、已支付、已发货、已完成。如果用字符串，容易拼写错误：

```typescript
// ❌ 不好的做法：容易出错
let orderStatus = "pending";    // 可能写成"peding"
let orderStatus2 = "shipped";   // 可能写成"shiped"

// ✅ 好的做法：使用枚举
enum OrderStatus {
    Pending = "pending",
    Paid = "paid", 
    Shipped = "shipped",
    Completed = "completed"
}

let status = OrderStatus.Pending;  // 有智能提示，不会拼错
```

### 3.2 数字枚举 vs 字符串枚举


#### 🔢 数字枚举（默认）


```typescript
enum Direction {
    Up,      // 0
    Down,    // 1
    Left,    // 2
    Right    // 3
}

// 可以手动指定起始值
enum Size {
    Small = 1,   // 1
    Medium,      // 2 (自动递增)
    Large        // 3
}

console.log(Direction.Up);     // 0
console.log(Size.Medium);      // 2
```

#### 📝 字符串枚举（推荐）


```typescript
enum Theme {
    Light = "light",
    Dark = "dark",
    Auto = "auto"
}

// 🎯 优势：更清晰，调试时看到的是有意义的字符串
console.log(Theme.Light);  // "light"

// API响应也更友好
interface UserSettings {
    theme: Theme;
}

const settings: UserSettings = {
    theme: Theme.Dark  // 存储为"dark"字符串
};
```

### 3.3 const 枚举的性能优化


```typescript
// 普通枚举：编译后会生成对象
enum Color {
    Red = "red",
    Green = "green",
    Blue = "blue"
}

// const枚举：编译时直接替换，更高效
const enum Status {
    Loading = "loading",
    Success = "success", 
    Error = "error"
}

// 使用时会被直接替换成字符串
let currentStatus = Status.Loading;  // 编译后变成 "loading"
```

**📊 编译结果对比**：

```javascript
// 普通枚举编译后：
var Color;
(function (Color) {
    Color["Red"] = "red";
    Color["Green"] = "green";
    Color["Blue"] = "blue";
})(Color || (Color = {}));

// const枚举编译后：
let currentStatus = "loading";  // 直接是字符串，没有额外代码
```

### 3.4 枚举的实际应用模式


```typescript
// 🎯 API状态管理
enum ApiStatus {
    Idle = "idle",
    Loading = "loading", 
    Success = "success",
    Error = "error"
}

interface ApiState<T> {
    status: ApiStatus;
    data?: T;
    error?: string;
}

// 使用示例
const userState: ApiState<User> = {
    status: ApiStatus.Loading  // 清晰的状态表示
};

// 🎯 用户权限系统
enum UserRole {
    Guest = "guest",
    User = "user",
    Admin = "admin",
    SuperAdmin = "super_admin"
}

function checkPermission(userRole: UserRole, action: string) {
    switch (userRole) {
        case UserRole.Admin:
        case UserRole.SuperAdmin:
            return true;
        case UserRole.User:
            return action !== "delete";
        default:
            return false;
    }
}
```

---

## 4. 🔗 联合枚举与类型兼容


### 4.1 枚举与联合类型的结合


```typescript
// 枚举定义
enum Color {
    Red = "red",
    Green = "green", 
    Blue = "blue"
}

// 与字面量类型结合
type ExtendedColor = Color | "purple" | "orange";

function setColor(color: ExtendedColor) {
    if (color === Color.Red) {
        console.log("设置为红色");
    } else if (color === "purple") {
        console.log("设置为紫色");
    }
}

setColor(Color.Blue);    // ✅ 使用枚举值
setColor("purple");      // ✅ 使用字面量
setColor("yellow");      // ❌ 错误，不在允许范围内
```

### 4.2 类型兼容性问题


```typescript
enum Status {
    Active = "active",
    Inactive = "inactive"
}

// ⚠️ 注意：枚举类型比字符串类型更严格
let enumStatus: Status = Status.Active;           // ✅ 正确
let stringStatus: string = Status.Active;         // ✅ 正确，枚举可以赋值给string

// ❌ 但反过来不行：string不能直接赋值给枚举
enumStatus = "active";  // ❌ 错误！即使字符串内容相同

// ✅ 解决方案：类型断言或联合类型
enumStatus = "active" as Status;                   // 类型断言
// 或者
type FlexibleStatus = Status | "active" | "inactive";
```

### 4.3 枚举在函数参数中的应用


```typescript
enum HttpMethod {
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE"
}

// 🎯 实际应用：API请求函数
interface RequestConfig {
    method: HttpMethod;
    url: string;
    data?: any;
}

function apiRequest(config: RequestConfig) {
    console.log(`${config.method} ${config.url}`);
}

// 使用时有智能提示，不会出错
apiRequest({
    method: HttpMethod.POST,  // IDE会提示所有可用选项
    url: "/api/users",
    data: { name: "张三" }
});
```

---

## 5. 🚀 实际应用场景


### 5.1 状态机模式


```typescript
// 🎯 订单状态机
enum OrderStatus {
    Created = "created",
    Paid = "paid", 
    Shipped = "shipped",
    Delivered = "delivered",
    Cancelled = "cancelled"
}

// 状态转换规则
type StatusTransition = {
    [OrderStatus.Created]: OrderStatus.Paid | OrderStatus.Cancelled;
    [OrderStatus.Paid]: OrderStatus.Shipped | OrderStatus.Cancelled;
    [OrderStatus.Shipped]: OrderStatus.Delivered;
    [OrderStatus.Delivered]: never;  // 终止状态
    [OrderStatus.Cancelled]: never;  // 终止状态
};

class Order {
    constructor(private status: OrderStatus = OrderStatus.Created) {}
    
    transition<T extends keyof StatusTransition>(
        newStatus: StatusTransition[T]
    ) {
        // 状态转换逻辑
        this.status = newStatus as OrderStatus;
    }
}
```

### 5.2 配置系统


```typescript
// 🎯 应用主题配置
const enum Theme {
    Light = "light",
    Dark = "dark", 
    Auto = "auto"
}

const enum Language {
    Chinese = "zh-CN",
    English = "en-US",
    Japanese = "ja-JP"
}

interface AppConfig {
    theme: Theme;
    language: Language;
    debug: boolean;
}

// 配置工厂函数
function createConfig(
    theme: Theme = Theme.Auto,
    language: Language = Language.Chinese
): AppConfig {
    return {
        theme,
        language,
        debug: false
    };
}

// 使用
const config = createConfig(Theme.Dark, Language.English);
```

### 5.3 事件系统


```typescript
// 🎯 事件类型定义
enum EventType {
    UserLogin = "user:login",
    UserLogout = "user:logout",
    OrderCreated = "order:created",
    PaymentSuccess = "payment:success"
}

// 事件数据类型映射
interface EventDataMap {
    [EventType.UserLogin]: { userId: string; timestamp: number };
    [EventType.UserLogout]: { userId: string };
    [EventType.OrderCreated]: { orderId: string; amount: number };
    [EventType.PaymentSuccess]: { orderId: string; paymentId: string };
}

// 类型安全的事件处理器
function handleEvent<T extends EventType>(
    type: T,
    data: EventDataMap[T]
) {
    switch (type) {
        case EventType.UserLogin:
            // data的类型自动推断为登录事件数据
            console.log(`用户${data.userId}在${data.timestamp}登录`);
            break;
        case EventType.OrderCreated:
            // data的类型自动推断为订单事件数据  
            console.log(`创建订单${data.orderId}，金额${data.amount}`);
            break;
    }
}
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 字面量类型：用具体值限制类型范围，提供编译时检查
🔸 模板字面量：动态组合字符串类型，适合API路径、事件名等场景  
🔸 字符串枚举：推荐使用，调试友好，JSON序列化清晰
🔸 const枚举：性能优化，编译时替换，适合不需要反向映射的场景
🔸 类型兼容：枚举比字符串更严格，注意转换方式
```

### 6.2 最佳实践建议


**🎯 选择原则**：

```typescript
// ✅ 少量固定值：使用字面量联合类型
type Size = "small" | "medium" | "large";

// ✅ 多个相关常量：使用字符串枚举
enum UserRole {
    Guest = "guest",
    User = "user", 
    Admin = "admin"
}

// ✅ 性能敏感场景：使用const枚举
const enum HttpStatus {
    OK = 200,
    NotFound = 404,
    ServerError = 500
}
```

**🔧 实用技巧**：

| **场景** | **推荐方案** | **原因** |
|---------|-------------|---------|
| **API状态** | `字符串枚举` | 调试清晰，JSON友好 |
| **配置常量** | `const枚举` | 编译时优化，性能好 |
| **组件Props** | `字面量联合` | 简单直接，类型清晰 |
| **事件类型** | `模板字面量` | 动态组合，扩展性好 |

### 6.3 常见问题避坑


```typescript
// ❌ 避免：混用数字和字符串枚举
enum BadExample {
    First = 1,
    Second = "second"  // 不要这样混用
}

// ✅ 推荐：保持一致性
enum GoodExample {
    First = "first",
    Second = "second"
}

// ❌ 避免：过度使用any绕过枚举检查
function badHandle(status: any) {
    // 失去了类型安全
}

// ✅ 推荐：使用联合类型扩展
type ExtendedStatus = OrderStatus | "unknown";
function goodHandle(status: ExtendedStatus) {
    // 保持类型安全的同时提供灵活性
}
```

### 6.4 记忆要点


**🧠 核心记忆**：
- **字面量类型** = 具体值当类型，限制范围防出错
- **模板字面量** = 字符串模板做类型，动态组合很强大  
- **字符串枚举** = 常量集合有名字，调试清晰易维护
- **const枚举** = 编译替换性能好，不需对象用它巧

**💡 应用思路**：
1. **固定选择项** → 字面量联合类型
2. **业务常量集** → 字符串枚举  
3. **性能要求高** → const枚举
4. **动态字符串** → 模板字面量类型