---
title: 2、类型别名与联合交叉
---
## 📚 目录

1. [type 关键字与类型别名](#1-type-关键字与类型别名)
2. [联合类型的使用](#2-联合类型的使用)
3. [交叉类型的应用](#3-交叉类型的应用)
4. [type vs interface 对比](#4-type-vs-interface-对比)
5. [递归类型定义](#5-递归类型定义)
6. [类型保护技巧](#6-类型保护技巧)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🏷️ type 关键字与类型别名


### 1.1 什么是类型别名


**简单理解**：类型别名就是给复杂的类型起个简单好记的名字，就像给长长的网址起个短链接一样。

```typescript
// 没有别名时 - 代码冗长难读
let user1: { name: string; age: number; email: string };
let user2: { name: string; age: number; email: string };

// 使用类型别名 - 简洁清晰
type User = { name: string; age: number; email: string };
let user1: User;
let user2: User;
```

### 1.2 基础语法与使用


**🔸 基本语法**
```typescript
type TypeName = TypeDefinition;
```

**💡 常见使用场景**
```typescript
// 基础类型别名
type UserID = string;
type Count = number;

// 对象类型别名
type Product = {
  id: UserID;
  name: string;
  price: number;
  inStock: boolean;
};

// 函数类型别名
type EventHandler = (event: string) => void;
type Calculator = (a: number, b: number) => number;
```

### 1.3 类型别名的优势


```
✅ 代码复用：一次定义，多处使用
✅ 易于维护：修改类型只需改一处
✅ 语义清晰：名字表达业务含义
✅ 降低复杂度：简化复杂类型表达
```

**实际对比**：
```typescript
// ❌ 不使用别名 - 重复且难维护
function processOrder(order: { id: string; items: { name: string; price: number }[]; total: number }) {
  // 处理订单
}

function validateOrder(order: { id: string; items: { name: string; price: number }[]; total: number }) {
  // 验证订单
}

// ✅ 使用别名 - 清晰且易维护
type OrderItem = { name: string; price: number };
type Order = { id: string; items: OrderItem[]; total: number };

function processOrder(order: Order) {
  // 处理订单
}

function validateOrder(order: Order) {
  // 验证订单
}
```

---

## 2. ⚡ 联合类型的使用


### 2.1 联合类型概念


**通俗解释**：联合类型就像"要么A，要么B，要么C"的选择题，变量可以是其中任意一种类型。

```typescript
// 状态可以是这三种之一
type Status = 'loading' | 'success' | 'error';

// ID可以是数字或字符串
type ID = number | string;

// 用户输入可以是文本或数字
type UserInput = string | number;
```

### 2.2 联合类型的实际应用


**🔸 API响应处理**
```typescript
type ApiResponse = 
  | { status: 'success'; data: any }
  | { status: 'error'; message: string };

function handleResponse(response: ApiResponse) {
  if (response.status === 'success') {
    console.log('数据:', response.data);
  } else {
    console.log('错误:', response.message);
  }
}
```

**🔸 主题设置**
```typescript
type Theme = 'light' | 'dark' | 'auto';
type Size = 'small' | 'medium' | 'large';

function setButtonStyle(theme: Theme, size: Size) {
  // 根据主题和尺寸设置按钮样式
}
```

### 2.3 联合类型使用技巧


**💡 字符串字面量联合**
```typescript
// 用户角色权限
type UserRole = 'admin' | 'editor' | 'viewer';

// 支付方式
type PaymentMethod = 'wechat' | 'alipay' | 'credit_card';

function processPayment(method: PaymentMethod) {
  switch (method) {
    case 'wechat':
      // 微信支付逻辑
      break;
    case 'alipay':
      // 支付宝逻辑
      break;
    case 'credit_card':
      // 信用卡逻辑
      break;
  }
}
```

---

## 3. 🔗 交叉类型的应用


### 3.1 交叉类型概念


**通俗解释**：交叉类型就像"既要A，又要B，还要C"，把多个类型的属性合并到一起。

```typescript
// 基础用户信息
type BaseUser = {
  id: string;
  name: string;
};

// 联系方式
type ContactInfo = {
  email: string;
  phone: string;
};

// 权限信息  
type Permission = {
  role: string;
  permissions: string[];
};

// 完整用户 = 基础信息 + 联系方式 + 权限
type FullUser = BaseUser & ContactInfo & Permission;
```

### 3.2 交叉类型实际使用


**🔸 组件属性组合**
```typescript
// 基础按钮属性
type BaseButtonProps = {
  text: string;
  onClick: () => void;
};

// 样式属性
type StyleProps = {
  color: string;
  size: 'small' | 'large';
};

// 可选属性
type OptionalProps = {
  disabled?: boolean;
  loading?: boolean;
};

// 完整按钮组件属性
type ButtonProps = BaseButtonProps & StyleProps & OptionalProps;

function Button(props: ButtonProps) {
  // 按钮组件实现
}
```

**🔸 配置对象合并**
```typescript
// 默认配置
type DefaultConfig = {
  timeout: number;
  retries: number;
};

// 用户自定义配置
type UserConfig = {
  apiUrl: string;
  token: string;
};

// 最终配置
type FinalConfig = DefaultConfig & UserConfig;

const config: FinalConfig = {
  timeout: 5000,    // 来自 DefaultConfig
  retries: 3,       // 来自 DefaultConfig
  apiUrl: 'xxx',    // 来自 UserConfig
  token: 'yyy'      // 来自 UserConfig
};
```

### 3.3 交叉类型的注意事项


**⚠️ 属性冲突处理**
```typescript
type A = { name: string; age: number };
type B = { name: number; email: string };

// 交叉后 name 属性冲突：string & number = never
type C = A & B;  // { name: never; age: number; email: string }

// 实际使用中要避免这种冲突
```

---

## 4. 🆚 type vs interface 对比


### 4.1 基本差异对比


| 特性 | **type** | **interface** |
|------|----------|---------------|
| **定义方式** | `type Name = ...` | `interface Name { ... }` |
| **扩展方式** | `&` 交叉类型 | `extends` 继承 |
| **重复声明** | ❌ 不允许 | ✅ 自动合并 |
| **联合类型** | ✅ 支持 | ❌ 不支持 |
| **计算属性名** | ✅ 支持 | ❌ 不支持 |

### 4.2 使用场景选择


**🔸 使用 type 的场景**
```typescript
// 1. 联合类型
type Status = 'loading' | 'success' | 'error';

// 2. 基础类型别名
type ID = string | number;

// 3. 计算属性名
type EventMap = {
  [K in 'click' | 'hover']: (event: Event) => void;
};

// 4. 条件类型
type ApiResponse<T> = T extends string ? string : number;
```

**🔸 使用 interface 的场景**
```typescript
// 1. 对象结构定义
interface User {
  id: string;
  name: string;
  email: string;
}

// 2. 类的契约
interface Drawable {
  draw(): void;
}

class Circle implements Drawable {
  draw() {
    // 绘制圆形
  }
}

// 3. 需要扩展的类型
interface BaseProps {
  className?: string;
}

interface ButtonProps extends BaseProps {
  onClick: () => void;
}
```

### 4.3 实际选择建议


```
💡 选择建议：

对象类型定义：优先使用 interface
类型别名：使用 type
联合/交叉类型：使用 type
需要继承扩展：使用 interface
API 设计：使用 interface（便于扩展）
工具类型：使用 type
```

---

## 5. 🔄 递归类型定义


### 5.1 递归类型概念


**通俗解释**：递归类型就像俄罗斯套娃，类型定义中引用自己，用来描述树形或嵌套结构。

### 5.2 常见递归类型例子


**🔸 文件系统结构**
```typescript
type FileSystemItem = {
  name: string;
  type: 'file' | 'folder';
  children?: FileSystemItem[];  // 递归引用自己
};

const fileSystem: FileSystemItem = {
  name: 'project',
  type: 'folder',
  children: [
    { name: 'src', type: 'folder', children: [
      { name: 'index.ts', type: 'file' },
      { name: 'utils.ts', type: 'file' }
    ]},
    { name: 'package.json', type: 'file' }
  ]
};
```

**🔸 评论嵌套结构**
```typescript
type Comment = {
  id: string;
  content: string;
  author: string;
  replies?: Comment[];  // 递归：回复也是评论
};

const comments: Comment[] = [
  {
    id: '1',
    content: '这篇文章很好',
    author: '张三',
    replies: [
      {
        id: '2', 
        content: '我也觉得',
        author: '李四',
        replies: [
          { id: '3', content: '+1', author: '王五' }
        ]
      }
    ]
  }
];
```

### 5.3 递归类型使用技巧


**💡 JSON 数据类型**
```typescript
type JSONValue = 
  | string 
  | number 
  | boolean 
  | null
  | JSONObject 
  | JSONArray;

type JSONObject = { [key: string]: JSONValue };
type JSONArray = JSONValue[];

// 可以描述任意 JSON 数据
const data: JSONValue = {
  name: '张三',
  age: 25,
  hobbies: ['读书', '游戏'],
  address: {
    city: '北京',
    district: '朝阳区'
  }
};
```

---

## 6. 🛡️ 类型保护技巧


### 6.1 类型保护概念


**通俗解释**：类型保护就像安检，帮助 TypeScript 确定联合类型中具体是哪一种类型，从而安全地访问对应属性。

### 6.2 常用类型保护方法


**🔸 使用 typeof 保护**
```typescript
type StringOrNumber = string | number;

function processValue(value: StringOrNumber) {
  if (typeof value === 'string') {
    // 这里 TypeScript 知道 value 是 string
    console.log(value.toUpperCase());
  } else {
    // 这里 TypeScript 知道 value 是 number  
    console.log(value.toFixed(2));
  }
}
```

**🔸 使用 in 操作符保护**
```typescript
type Bird = { fly: () => void; feathers: number };
type Fish = { swim: () => void; scales: number };

function move(animal: Bird | Fish) {
  if ('fly' in animal) {
    // 这里 animal 是 Bird 类型
    animal.fly();
  } else {
    // 这里 animal 是 Fish 类型
    animal.swim();
  }
}
```

### 6.3 标识属性（判别联合）


**🔸 最实用的类型保护方式**
```typescript
type LoadingState = { status: 'loading' };
type SuccessState = { status: 'success'; data: any };
type ErrorState = { status: 'error'; message: string };

type AppState = LoadingState | SuccessState | ErrorState;

function handleState(state: AppState) {
  switch (state.status) {
    case 'loading':
      // state 是 LoadingState
      showSpinner();
      break;
    case 'success':
      // state 是 SuccessState，可以安全访问 data
      showData(state.data);
      break;
    case 'error':
      // state 是 ErrorState，可以安全访问 message
      showError(state.message);
      break;
  }
}
```

### 6.4 自定义类型保护函数


**💡 高级用法**
```typescript
// 自定义类型保护函数
function isString(value: unknown): value is string {
  return typeof value === 'string';
}

function isUser(obj: any): obj is { name: string; age: number } {
  return obj && typeof obj.name === 'string' && typeof obj.age === 'number';
}

// 使用自定义保护
function processInput(input: unknown) {
  if (isString(input)) {
    // input 现在被确认为 string 类型
    console.log(input.length);
  }
}
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 type 关键字：给复杂类型起别名，提高代码可读性和维护性
🔸 联合类型 |：表示"要么这个，要么那个"的类型选择
🔸 交叉类型 &：表示"既要这个，又要那个"的类型合并
🔸 type vs interface：根据使用场景选择合适的类型定义方式
🔸 递归类型：描述树形或嵌套结构的自引用类型
🔸 类型保护：帮助TypeScript确定联合类型的具体类型
```

### 7.2 实际使用建议


**💡 最佳实践**
```typescript
// ✅ 好的做法
type Status = 'idle' | 'loading' | 'success' | 'error';
type User = { id: string; name: string; email: string };
type ApiResponse<T> = { status: Status; data?: T; error?: string };

// ❌ 避免的做法  
type BadUnion = string | any;  // any 会破坏类型安全
type BadIntersection = { id: string } & { id: number };  // 属性冲突
```

**🎯 选择指南**
```
什么时候用 type：
- 联合类型：'a' | 'b' | 'c'
- 交叉类型：A & B & C  
- 基础类型别名：type ID = string
- 计算类型：映射类型、条件类型

什么时候用 interface：
- 对象结构定义
- 类的契约（implements）
- 需要继承扩展的类型
- 第三方库的类型声明
```

### 7.3 常见错误与解决


```
🚫 常见错误：
1. 联合类型中直接访问不存在的属性
2. 交叉类型产生属性冲突
3. 递归类型缺少终止条件
4. 类型保护判断不充分

✅ 解决方案：
1. 使用类型保护确定具体类型
2. 设计时避免属性名冲突  
3. 递归类型设置可选或终止条件
4. 使用标识属性进行类型判别
```

**核心记忆**：
- type 起别名，| 表选择，& 表合并
- interface 定对象，extends 来扩展
- 递归套娃式，保护要判断
- 实际应用中，场景选方案