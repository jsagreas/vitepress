---
title: 4、抽象类与接口实现
---
## 📚 目录

1. [抽象类基础概念](#1-抽象类基础概念)
2. [抽象方法的使用](#2-抽象方法的使用)
3. [接口实现机制](#3-接口实现机制)
4. [接口继承与扩展](#4-接口继承与扩展)
5. [类与接口的结合应用](#5-类与接口的结合应用)
6. [多接口实现模式](#6-多接口实现模式)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🏗️ 抽象类基础概念


### 1.1 什么是抽象类


**🔸 通俗理解**
```
抽象类就像建筑设计图纸：
- 定义了建筑的基本结构和要求
- 但不能直接住人（不能实例化）
- 需要建筑工人按图纸盖房子（子类继承实现）

抽象类 = 半成品 + 设计规范
```

**💡 核心特点**
```typescript
// 抽象类使用 abstract 关键字
abstract class Animal {
    name: string;
    
    constructor(name: string) {
        this.name = name;
    }
    
    // 普通方法：有具体实现
    eat(): void {
        console.log(`${this.name} is eating`);
    }
    
    // 抽象方法：只定义规范，不实现
    abstract makeSound(): void;
}

// ❌ 错误：不能直接创建抽象类实例
// const animal = new Animal("Tom"); 

// ✅ 正确：通过子类创建实例
class Dog extends Animal {
    makeSound(): void {
        console.log(`${this.name} barks: Woof!`);
    }
}

const dog = new Dog("旺财");
```

### 1.2 抽象类的设计意图


**🎯 解决的问题**
```
现实场景：动物管理系统

问题：
- 所有动物都有名字、会吃饭（共同特征）
- 但不同动物的叫声不同（个性化行为）
- 不应该直接创建"动物"对象（太抽象）

解决方案：
┌─────────────────┐
│   Animal(抽象)   │ ← 定义通用属性和抽象方法
├─────────────────┤
│ + name: string  │
│ + eat(): void   │
│ + makeSound()   │ ← 抽象方法，强制子类实现
└─────────────────┘
         ↑
    ┌────┴────┐
    │         │
┌───────┐ ┌───────┐
│  Dog  │ │  Cat  │ ← 具体实现
└───────┘ └───────┘
```

---

## 2. 🔧 抽象方法的使用


### 2.1 抽象方法的定义规则


**📋 基本语法**
```typescript
abstract class Shape {
    color: string;
    
    constructor(color: string) {
        this.color = color;
    }
    
    // 抽象方法：只声明，不实现
    abstract getArea(): number;
    abstract getPerimeter(): number;
    
    // 普通方法：有具体实现
    displayInfo(): void {
        console.log(`颜色: ${this.color}, 面积: ${this.getArea()}`);
    }
}
```

**⚠️ 重要约束**
```
抽象方法的规则：
✅ 只能在抽象类中定义
✅ 不能有方法体（只有声明）
✅ 子类必须实现所有抽象方法
✅ 可以有参数和返回值类型
❌ 不能是 private（子类无法访问）
```

### 2.2 子类实现抽象方法


**🔸 完整实现示例**
```typescript
// 圆形类：实现所有抽象方法
class Circle extends Shape {
    private radius: number;
    
    constructor(color: string, radius: number) {
        super(color);
        this.radius = radius;
    }
    
    // 必须实现：计算面积
    getArea(): number {
        return Math.PI * this.radius * this.radius;
    }
    
    // 必须实现：计算周长
    getPerimeter(): number {
        return 2 * Math.PI * this.radius;
    }
}

// 矩形类：实现所有抽象方法
class Rectangle extends Shape {
    private width: number;
    private height: number;
    
    constructor(color: string, width: number, height: number) {
        super(color);
        this.width = width;
        this.height = height;
    }
    
    getArea(): number {
        return this.width * this.height;
    }
    
    getPerimeter(): number {
        return 2 * (this.width + this.height);
    }
}

// 使用示例
const circle = new Circle("红色", 5);
const rectangle = new Rectangle("蓝色", 4, 6);

circle.displayInfo();    // 颜色: 红色, 面积: 78.54
rectangle.displayInfo(); // 颜色: 蓝色, 面积: 24
```

---

## 3. 🔗 接口实现机制


### 3.1 接口的作用与意义


**🔸 接口 vs 抽象类对比**
```
接口（Interface）：
- 纯粹的约定，不包含实现
- 类似合同条款，规定必须做什么
- 一个类可以实现多个接口

抽象类（Abstract Class）：
- 部分实现 + 抽象规范
- 类似半成品，有基础功能
- 一个类只能继承一个抽象类

现实比喻：
接口 = 职业要求（程序员要会编程、设计师要会设计）
抽象类 = 员工基础模板（都要打卡、都有工号）
```

### 3.2 implements 关键字使用


**📝 基础语法**
```typescript
// 定义接口：规定能力要求
interface Flyable {
    fly(): void;
    maxSpeed: number;
}

interface Swimmable {
    swim(): void;
    waterDepth: number;
}

// 类实现接口：承诺具备某种能力
class Duck implements Flyable, Swimmable {
    maxSpeed: number = 50;
    waterDepth: number = 10;
    
    fly(): void {
        console.log("鸭子在天上飞");
    }
    
    swim(): void {
        console.log("鸭子在水里游");
    }
    
    // 类还可以有自己的额外属性和方法
    quack(): void {
        console.log("嘎嘎嘎");
    }
}
```

### 3.3 接口实现的约束规则


**⚖️ 实现规则**
```typescript
interface Vehicle {
    speed: number;
    start(): void;
    stop(): void;
}

class Car implements Vehicle {
    speed: number = 0;
    
    // ✅ 必须实现所有接口属性
    start(): void {
        this.speed = 60;
        console.log("汽车启动");
    }
    
    // ✅ 必须实现所有接口方法
    stop(): void {
        this.speed = 0;
        console.log("汽车停止");
    }
    
    // ✅ 可以添加额外的属性和方法
    brand: string = "Toyota";
    
    refuel(): void {
        console.log("加油");
    }
}

// ❌ 错误示例：没有实现所有接口成员
class Bicycle implements Vehicle {
    // 缺少 speed 属性 - 编译错误
    start(): void {
        console.log("开始骑行");
    }
    // 缺少 stop 方法 - 编译错误
}
```

---

## 4. 🔄 接口继承与扩展


### 4.1 接口继承基础


**🔸 接口继承语法**
```typescript
// 基础接口
interface Animal {
    name: string;
    age: number;
    eat(): void;
}

// 继承接口：扩展更多能力
interface Pet extends Animal {
    owner: string;
    play(): void;
}

interface WorkingAnimal extends Animal {
    work(): void;
    workHours: number;
}

// 实现继承后的接口
class Dog implements Pet {
    name: string;
    age: number;
    owner: string;
    
    constructor(name: string, age: number, owner: string) {
        this.name = name;
        this.age = age;
        this.owner = owner;
    }
    
    eat(): void {
        console.log(`${this.name}在吃狗粮`);
    }
    
    play(): void {
        console.log(`${this.name}在和${this.owner}玩耍`);
    }
}
```

### 4.2 多重接口继承


**🔀 组合多个接口**
```typescript
interface Readable {
    read(): string;
}

interface Writable {
    write(data: string): void;
}

interface Closable {
    close(): void;
}

// 接口可以继承多个接口
interface File extends Readable, Writable, Closable {
    filename: string;
    size: number;
}

class TextFile implements File {
    filename: string;
    size: number = 0;
    private content: string = "";
    
    constructor(filename: string) {
        this.filename = filename;
    }
    
    read(): string {
        return this.content;
    }
    
    write(data: string): void {
        this.content += data;
        this.size = this.content.length;
    }
    
    close(): void {
        console.log(`文件 ${this.filename} 已关闭`);
    }
}
```

### 4.3 接口继承的实际应用


**💼 实际场景示例**
```typescript
// 基础用户接口
interface User {
    id: number;
    username: string;
    email: string;
}

// 管理员继承用户并添加权限
interface Admin extends User {
    permissions: string[];
    canDelete: boolean;
}

// VIP用户继承用户并添加特权
interface VIPUser extends User {
    membershipLevel: 'Gold' | 'Platinum' | 'Diamond';
    discount: number;
}

// 超级管理员：同时具备管理和VIP特权
interface SuperAdmin extends Admin, VIPUser {
    isSuperAdmin: true;
}
```

---

## 5. 🤝 类与接口的结合应用


### 5.1 混合使用模式


**🔸 继承抽象类 + 实现接口**
```typescript
// 抽象类：提供基础实现
abstract class Employee {
    name: string;
    salary: number;
    
    constructor(name: string, salary: number) {
        this.name = name;
        this.salary = salary;
    }
    
    // 通用方法
    work(): void {
        console.log(`${this.name}正在工作`);
    }
    
    // 抽象方法：子类必须实现
    abstract getJobDescription(): string;
}

// 接口：定义额外能力
interface Manageable {
    manageTeam(): void;
    teamSize: number;
}

interface Teachable {
    teach(subject: string): void;
    students: string[];
}

// 类：继承抽象类 + 实现接口
class Manager extends Employee implements Manageable {
    teamSize: number;
    
    constructor(name: string, salary: number, teamSize: number) {
        super(name, salary);
        this.teamSize = teamSize;
    }
    
    // 实现抽象方法
    getJobDescription(): string {
        return `管理${this.teamSize}人的团队`;
    }
    
    // 实现接口方法
    manageTeam(): void {
        console.log(`${this.name}在管理${this.teamSize}人团队`);
    }
}

class Teacher extends Employee implements Teachable {
    students: string[];
    
    constructor(name: string, salary: number, students: string[]) {
        super(name, salary);
        this.students = students;
    }
    
    getJobDescription(): string {
        return `教授${this.students.length}名学生`;
    }
    
    teach(subject: string): void {
        console.log(`${this.name}在教授${subject}`);
    }
}
```

### 5.2 类型约束与多态


**🎭 多态应用示例**
```typescript
// 使用接口作为类型约束
function processVehicles(vehicles: Vehicle[]): void {
    vehicles.forEach(vehicle => {
        vehicle.start(); // 多态：不同类调用各自的实现
        console.log(`速度：${vehicle.speed}km/h`);
        vehicle.stop();
    });
}

// 创建不同类型的交通工具
const car = new Car();
const truck = new Truck(); // 假设 Truck 也实现了 Vehicle 接口

// 统一处理：体现多态特性
processVehicles([car, truck]);
```

---

## 6. 🔀 多接口实现模式


### 6.1 基础多接口实现


**🔸 实现多个独立接口**
```typescript
interface Printable {
    print(): void;
}

interface Scannable {
    scan(): void;
}

interface Faxable {
    fax(number: string): void;
}

// 多功能打印机：实现多个接口
class MultiFunctionPrinter implements Printable, Scannable, Faxable {
    print(): void {
        console.log("正在打印文档");
    }
    
    scan(): void {
        console.log("正在扫描文档");
    }
    
    fax(number: string): void {
        console.log(`正在向${number}发送传真`);
    }
}

// 简单打印机：只实现部分接口
class SimplePrinter implements Printable {
    print(): void {
        console.log("简单打印");
    }
}
```

### 6.2 接口组合设计模式


**🏗️ 灵活的接口组合**
```typescript
// 定义细粒度的能力接口
interface Readable {
    read(): string;
}

interface Writable {
    write(content: string): void;
}

interface Compressible {
    compress(): void;
    decompress(): void;
}

interface Encryptable {
    encrypt(key: string): void;
    decrypt(key: string): void;
}

// 不同类型的文件处理器
class BasicFileHandler implements Readable, Writable {
    private content: string = "";
    
    read(): string {
        return this.content;
    }
    
    write(content: string): void {
        this.content = content;
    }
}

class SecureFileHandler implements Readable, Writable, Encryptable {
    private content: string = "";
    private isEncrypted: boolean = false;
    
    read(): string {
        if (this.isEncrypted) {
            throw new Error("文件已加密，请先解密");
        }
        return this.content;
    }
    
    write(content: string): void {
        this.content = content;
    }
    
    encrypt(key: string): void {
        // 简化的加密逻辑
        this.content = btoa(this.content);
        this.isEncrypted = true;
        console.log("文件已加密");
    }
    
    decrypt(key: string): void {
        // 简化的解密逻辑
        this.content = atob(this.content);
        this.isEncrypted = false;
        console.log("文件已解密");
    }
}
```

### 6.3 多接口实现的最佳实践


**📈 设计原则与技巧**

| 原则 | 说明 | 示例 |
|------|------|------|
| **接口分离** | 接口功能单一，避免臃肿 | `Printable`, `Scannable` 分开定义 |
| **组合优于继承** | 通过实现多接口组合功能 | 打印机实现多个独立接口 |
| **依赖接口不依赖实现** | 使用接口类型而非具体类 | `Vehicle[]` 而不是 `Car[]` |

**💡 实用技巧**
```typescript
// 使用类型联合创建灵活的约束
type FileProcessor = Readable & Writable;
type SecureProcessor = FileProcessor & Encryptable;

function processFile(processor: FileProcessor): void {
    const content = processor.read();
    processor.write(content.toUpperCase());
}

function processSecureFile(processor: SecureProcessor): void {
    processor.decrypt("secret-key");
    processFile(processor);
    processor.encrypt("secret-key");
}
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 抽象类：半成品类，不能实例化，可包含具体实现和抽象方法
🔸 抽象方法：只声明不实现，强制子类提供具体实现
🔸 接口实现：类通过 implements 承诺具备接口定义的能力
🔸 接口继承：extends 扩展接口功能，支持多重继承
🔸 混合模式：同时继承抽象类和实现接口
🔸 多接口实现：一个类可以实现多个接口获得多种能力
```

### 7.2 关键理解要点


**🔹 何时使用抽象类 vs 接口**
```
使用抽象类的场景：
✅ 有共同的实现逻辑需要复用
✅ 需要定义通用的属性和方法
✅ 强制子类实现特定方法
✅ 类似"是什么"的关系（IS-A）

使用接口的场景：
✅ 只关心能力约束，不关心实现
✅ 需要多重继承的效果
✅ 跨越不同类层次的通用能力
✅ 类似"能做什么"的关系（CAN-DO）
```

**🔹 设计思路**
```
现实建模思考过程：

1. 识别共同特征 → 抽象类
   所有动物都有名字、年龄 → Animal 抽象类

2. 识别能力要求 → 接口
   会飞、会游泳、会工作 → Flyable, Swimmable, Workable 接口

3. 组合设计 → 继承 + 实现
   class Duck extends Animal implements Flyable, Swimmable
```

### 7.3 实际应用指导


**🎯 代码设计最佳实践**
```typescript
// ✅ 好的设计：职责清晰
abstract class Shape {
    protected color: string;
    constructor(color: string) { this.color = color; }
    abstract getArea(): number;  // 抽象方法：强制实现
    display(): void {            // 具体方法：复用逻辑
        console.log(`${this.color}图形，面积：${this.getArea()}`);
    }
}

interface Drawable {
    draw(): void;               // 接口：定义能力
}

interface Movable {
    move(x: number, y: number): void;
}

class Circle extends Shape implements Drawable, Movable {
    constructor(color: string, private radius: number) {
        super(color);
    }
    
    getArea(): number {
        return Math.PI * this.radius ** 2;
    }
    
    draw(): void {
        console.log("绘制圆形");
    }
    
    move(x: number, y: number): void {
        console.log(`圆形移动到 (${x}, ${y})`);
    }
}
```

**🚫 常见误区避免**
```typescript
// ❌ 错误：接口过于复杂
interface SuperInterface {
    prop1: string;
    prop2: number;
    method1(): void;
    method2(): string;
    method3(x: number): boolean;
    // ... 太多方法
}

// ✅ 正确：接口功能单一
interface Identifiable {
    id: string;
}

interface Nameable {
    name: string;
}

interface Saveable {
    save(): void;
}
```

**核心记忆**：
- 抽象类像半成品，提供基础框架和强制规范
- 接口像合同条款，约定必须具备的能力
- 继承体现"是什么"关系，实现体现"能做什么"关系
- 灵活组合继承和实现，构建强大而清晰的类型系统