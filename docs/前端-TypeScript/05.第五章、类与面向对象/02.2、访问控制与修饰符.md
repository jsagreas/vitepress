---
title: 2、访问控制与修饰符
---
## 📚 目录

1. [访问控制基本概念](#1-访问控制基本概念)
2. [public 公共访问权限](#2-public-公共访问权限)
3. [private 私有访问权限](#3-private-私有访问权限)
4. [protected 受保护访问权限](#4-protected-受保护访问权限)
5. [readonly 只读属性](#5-readonly-只读属性)
6. [static 静态成员](#6-static-静态成员)
7. [访问控制总结对比](#7-访问控制总结对比)

---

## 1. 🔐 访问控制基本概念


### 1.1 什么是访问控制


**简单理解**：访问控制就是**控制谁能访问类里面的东西**

```
想象一个房子：
🏠 房子 = 类
🚪 房门 = public（任何人都能进）
🔒 私人房间 = private（只有主人能进）
👨‍👩‍👧‍👦 家庭房间 = protected（家人能进）
```

### 1.2 为什么需要访问控制


**核心作用**：
- **🛡️ 数据保护**：防止外部随意修改内部数据
- **📋 接口简化**：只暴露必要的功能，隐藏实现细节
- **🔧 维护性**：内部实现变化不影响外部使用

**实际例子**：
```typescript
// 银行账户类
class BankAccount {
  public accountNumber: string;     // 账号可以公开
  private balance: number;          // 余额必须保护
  protected bankCode: string;       // 银行代码子类可见
}
```

---

## 2. 🌍 public 公共访问权限


### 2.1 public 的含义


**通俗解释**：`public` 就是**完全开放**，任何地方都能访问

**关键特点**：
- ✅ **默认修饰符**：不写修饰符就是 public
- ✅ **完全开放**：类内部、外部、子类都能访问
- ✅ **最宽松**：没有任何限制

### 2.2 public 使用示例


```typescript
class User {
  // 以下三种写法完全等价
  public name: string;        // 明确写 public
  age: number;               // 不写修饰符，默认 public
  public email: string;      // 明确写 public
  
  public constructor(name: string, age: number, email: string) {
    this.name = name;
    this.age = age;
    this.email = email;
  }
  
  // public 方法
  public introduce(): string {
    return `我是 ${this.name}，今年 ${this.age} 岁`;
  }
}

// 使用示例
const user = new User("小明", 25, "xiaoming@email.com");

// 所有 public 成员都可以自由访问
console.log(user.name);           // ✅ 可以访问
console.log(user.age);            // ✅ 可以访问
console.log(user.introduce());    // ✅ 可以调用

// 可以自由修改
user.name = "小红";               // ✅ 可以修改
user.age = 26;                   // ✅ 可以修改
```

### 2.3 什么时候用 public


**适用场景**：
- 🔸 **对外接口**：需要让外部调用的方法
- 🔸 **基础属性**：姓名、年龄等可以公开的信息
- 🔸 **工具方法**：通用的辅助功能

---

## 3. 🔒 private 私有访问权限


### 3.1 private 的含义


**通俗解释**：`private` 就是**私人专用**，只有类自己内部能访问

**关键特点**：
- 🚫 **外部禁止**：类外部完全无法访问
- 🚫 **子类禁止**：连继承的子类都不能访问
- ✅ **内部专用**：只能在类的内部方法中使用

### 3.2 private 使用示例


```typescript
class BankAccount {
  public accountNumber: string;
  private balance: number;          // 私有属性：余额
  private pin: string;              // 私有属性：密码
  
  constructor(accountNumber: string, initialBalance: number, pin: string) {
    this.accountNumber = accountNumber;
    this.balance = initialBalance;
    this.pin = pin;
  }
  
  // 私有方法：验证密码
  private validatePin(inputPin: string): boolean {
    return this.pin === inputPin;
  }
  
  // 公共方法：取钱（内部使用私有成员）
  public withdraw(amount: number, inputPin: string): boolean {
    // 在类内部可以访问 private 成员
    if (!this.validatePin(inputPin)) {
      console.log("密码错误");
      return false;
    }
    
    if (this.balance >= amount) {
      this.balance -= amount;
      console.log(`取款成功，余额：${this.balance}`);
      return true;
    }
    
    console.log("余额不足");
    return false;
  }
  
  // 公共方法：查询余额
  public getBalance(inputPin: string): number | null {
    if (this.validatePin(inputPin)) {
      return this.balance;
    }
    return null;
  }
}

// 使用示例
const account = new BankAccount("123456", 1000, "0000");

// ✅ 可以访问 public 成员
console.log(account.accountNumber);

// ❌ 无法访问 private 成员（编译错误）
// console.log(account.balance);        // 错误！
// console.log(account.pin);            // 错误！
// account.validatePin("0000");         // 错误！

// ✅ 通过 public 方法间接访问
account.withdraw(100, "0000");          // 正确的使用方式
console.log(account.getBalance("0000"));
```

### 3.3 private 的实际价值


**数据保护示例**：
```typescript
class Counter {
  private count: number = 0;
  
  // 只允许通过特定方法修改 count
  public increment(): void {
    this.count++;
  }
  
  public decrement(): void {
    if (this.count > 0) {    // 防止负数
      this.count--;
    }
  }
  
  public getValue(): number {
    return this.count;
  }
}

const counter = new Counter();
// counter.count = -100;     // ❌ 无法直接修改，避免了错误数据
counter.increment();         // ✅ 通过安全的方法操作
```

---

## 4. 👨‍👩‍👧‍👦 protected 受保护访问权限


### 4.1 protected 的含义


**通俗解释**：`protected` 就是**家人专用**，自己和子类能用，外人不行

**关键特点**：
- ✅ **类内部可用**：当前类内部可以访问
- ✅ **子类可用**：继承的子类可以访问
- 🚫 **外部禁止**：类外部无法直接访问

### 4.2 protected 使用示例


```typescript
class Animal {
  public name: string;
  protected age: number;           // 受保护：子类可以访问
  private id: string;              // 私有：子类不能访问
  
  constructor(name: string, age: number, id: string) {
    this.name = name;
    this.age = age;
    this.id = id;
  }
  
  protected makeSound(): void {    // 受保护的方法
    console.log("动物发出声音");
  }
  
  public introduce(): void {
    console.log(`我是 ${this.name}，${this.age} 岁`);
  }
}

class Dog extends Animal {
  constructor(name: string, age: number, id: string) {
    super(name, age, id);
  }
  
  public bark(): void {
    // ✅ 子类可以访问 protected 成员
    console.log(`${this.name} 汪汪叫，它 ${this.age} 岁了`);
    this.makeSound();              // ✅ 可以调用 protected 方法
    
    // ❌ 子类无法访问 private 成员
    // console.log(this.id);       // 编译错误！
  }
}

// 使用示例
const dog = new Dog("旺财", 3, "D001");

// ✅ 可以访问 public 成员
console.log(dog.name);
dog.introduce();
dog.bark();

// ❌ 无法直接访问 protected 成员
// console.log(dog.age);           // 编译错误！
// dog.makeSound();                // 编译错误！
```

### 4.3 protected 的使用场景


**模板方法模式**：
```typescript
abstract class GameCharacter {
  protected health: number = 100;
  protected level: number = 1;
  
  // 受保护方法：子类可以重写
  protected calculateDamage(): number {
    return this.level * 10;
  }
  
  // 公共方法：使用受保护的内部逻辑
  public attack(target: GameCharacter): void {
    const damage = this.calculateDamage();
    target.health -= damage;
    console.log(`造成 ${damage} 点伤害`);
  }
}

class Warrior extends GameCharacter {
  // 子类可以访问和重写 protected 方法
  protected calculateDamage(): number {
    return this.level * 15;  // 战士伤害更高
  }
}
```

---

## 5. 📖 readonly 只读属性


### 5.1 readonly 的含义


**通俗解释**：`readonly` 就是**只能读不能改**，像身份证号一样

**关键特点**：
- ✅ **初始化时设置**：构造函数中可以赋值
- 🚫 **后续无法修改**：初始化后就不能改了
- ✅ **可以组合**：可以与 public/private/protected 组合

### 5.2 readonly 使用示例


```typescript
class Person {
  public readonly id: string;           // 只读公共属性
  private readonly ssn: string;         // 只读私有属性
  public name: string;                  // 普通可修改属性
  
  constructor(id: string, ssn: string, name: string) {
    // ✅ 构造函数中可以给 readonly 属性赋值
    this.id = id;
    this.ssn = ssn;
    this.name = name;
  }
  
  public updateName(newName: string): void {
    this.name = newName;              // ✅ 普通属性可以修改
    // this.id = "new-id";            // ❌ readonly 属性不能修改
  }
  
  public getId(): string {
    return this.id;                   // ✅ readonly 属性可以读取
  }
}

// 使用示例
const person = new Person("P001", "123456789", "张三");

console.log(person.id);               // ✅ 可以读取
console.log(person.name);             // ✅ 可以读取

person.name = "李四";                 // ✅ 可以修改普通属性
// person.id = "P002";                // ❌ 不能修改 readonly 属性
```

### 5.3 readonly 与 const 的区别


```typescript
class Config {
  // readonly：类的属性，运行时确定
  public readonly apiUrl: string;
  
  // static readonly：静态只读，编译时确定
  public static readonly VERSION = "1.0.0";
  
  constructor(environment: string) {
    // 可以根据参数动态设置 readonly 属性
    this.apiUrl = environment === "prod" 
      ? "https://api.prod.com" 
      : "https://api.dev.com";
  }
}

// const：编译时常量
const MAX_USERS = 100;
```

---

## 6. ⚡ static 静态成员


### 6.1 static 的含义


**通俗解释**：`static` 就是**属于类本身的**，不属于任何具体实例

**生活类比**：
```
学校 = 类
学校名称 = static 属性（所有学生共享）
校训 = static 属性（所有学生共享）
学生姓名 = 实例属性（每个学生不同）
```

### 6.2 static 基本使用


```typescript
class MathUtils {
  // 静态属性：属于类，不属于实例
  public static readonly PI = 3.14159;
  private static instanceCount = 0;
  
  // 实例属性
  public name: string;
  
  constructor(name: string) {
    this.name = name;
    MathUtils.instanceCount++;     // 访问静态属性
  }
  
  // 静态方法：属于类，不属于实例
  public static add(a: number, b: number): number {
    return a + b;
  }
  
  public static getInstanceCount(): number {
    // 静态方法中只能访问静态成员
    return MathUtils.instanceCount;
    // return this.name;           // ❌ 无法访问实例属性
  }
  
  // 实例方法
  public getName(): string {
    return this.name;
  }
}

// 使用示例
// ✅ 直接通过类名访问静态成员
console.log(MathUtils.PI);                 // 3.14159
console.log(MathUtils.add(5, 3));         // 8
console.log(MathUtils.getInstanceCount()); // 0

// 创建实例
const math1 = new MathUtils("计算器1");
const math2 = new MathUtils("计算器2");

// ✅ 实例可以访问实例成员
console.log(math1.getName());              // "计算器1"

// ✅ 静态成员通过类名访问
console.log(MathUtils.getInstanceCount()); // 2

// ❌ 实例无法访问静态成员（TypeScript 警告）
// console.log(math1.PI);                  // 不推荐
```

### 6.3 static 实际应用场景


**工具类**：
```typescript
class StringUtils {
  public static isEmpty(str: string): boolean {
    return str === null || str === undefined || str.trim() === "";
  }
  
  public static capitalize(str: string): string {
    return str.charAt(0).toUpperCase() + str.slice(1);
  }
}

// 使用：不需要创建实例
if (StringUtils.isEmpty(userInput)) {
  console.log("输入不能为空");
}
```

**单例模式**：
```typescript
class Database {
  private static instance: Database;
  private constructor() {} // 私有构造函数
  
  public static getInstance(): Database {
    if (!Database.instance) {
      Database.instance = new Database();
    }
    return Database.instance;
  }
}

// 使用：确保全局只有一个数据库连接
const db1 = Database.getInstance();
const db2 = Database.getInstance();
console.log(db1 === db2); // true，同一个实例
```

---

## 7. 📊 访问控制总结对比


### 7.1 访问权限对比表


| 修饰符 | **类内部** | **子类** | **外部** | **说明** |
|--------|-----------|---------|---------|----------|
| `public` | ✅ | ✅ | ✅ | `完全开放，默认修饰符` |
| `protected` | ✅ | ✅ | ❌ | `家人可见，继承友好` |
| `private` | ✅ | ❌ | ❌ | `私人专用，最严格` |

### 7.2 访问控制使用原则


**🎯 选择原则**：
```typescript
class UserService {
  // public：对外提供的接口
  public login(username: string, password: string): boolean { /*...*/ }
  
  // protected：子类可能需要的功能
  protected validateUser(username: string): boolean { /*...*/ }
  
  // private：内部实现细节
  private hashPassword(password: string): string { /*...*/ }
}
```

**💡 最佳实践**：
- **默认 private**：先设为私有，需要时再放开
- **接口 public**：对外提供的功能设为公共
- **继承 protected**：子类可能需要的设为受保护
- **数据保护**：重要数据用 private + readonly

### 7.3 综合示例


```typescript
class Vehicle {
  // 公共属性：基本信息
  public readonly brand: string;
  public model: string;
  
  // 受保护属性：子类可能需要
  protected engine: string;
  protected maxSpeed: number;
  
  // 私有属性：内部实现
  private vin: string;
  private isRunning: boolean = false;
  
  // 静态属性：共享数据
  public static totalVehicles: number = 0;
  
  constructor(brand: string, model: string, engine: string, vin: string) {
    this.brand = brand;      // readonly 只能在构造函数中设置
    this.model = model;
    this.engine = engine;
    this.vin = vin;
    this.maxSpeed = 120;
    
    Vehicle.totalVehicles++; // 更新静态计数
  }
  
  // 公共方法：对外接口
  public start(): void {
    if (this.validateVin()) {
      this.isRunning = true;
      console.log(`${this.brand} ${this.model} 启动成功`);
    }
  }
  
  // 受保护方法：子类可以重写
  protected getMaxSpeed(): number {
    return this.maxSpeed;
  }
  
  // 私有方法：内部逻辑
  private validateVin(): boolean {
    return this.vin.length === 17;
  }
  
  // 静态方法：工具功能
  public static getTotalVehicles(): number {
    return Vehicle.totalVehicles;
  }
}

class Car extends Vehicle {
  private doors: number;
  
  constructor(brand: string, model: string, doors: number, vin: string) {
    super(brand, model, "汽油", vin);
    this.doors = doors;
    this.maxSpeed = 180;  // ✅ 可以修改 protected 属性
  }
  
  public getCarInfo(): string {
    // ✅ 可以访问 protected 成员
    return `${this.brand} ${this.model}，最高时速 ${this.getMaxSpeed()}`;
    
    // ❌ 无法访问 private 成员
    // return this.vin;  // 编译错误
  }
}

// 使用示例
const car = new Car("丰田", "卡罗拉", 4, "12345678901234567");

// ✅ 访问 public 成员
console.log(car.brand);        // "丰田"
car.start();                   // 启动成功

// ✅ 访问静态成员
console.log(Vehicle.getTotalVehicles()); // 1

// ❌ 无法访问非 public 成员
// console.log(car.engine);     // 编译错误
// console.log(car.vin);        // 编译错误
```

### 7.4 核心要点总结


**🔑 关键记忆点**：
- **public**：大门敞开，人人可进
- **protected**：家人房间，子类能进
- **private**：私人空间，外人勿进
- **readonly**：只看不改，初始设定
- **static**：类的财产，共同拥有

**🎯 使用建议**：
- 优先使用 `private`，保护数据安全
- 需要继承时用 `protected`
- 对外接口用 `public`
- 不变数据用 `readonly`
- 共享功能用 `static`