---
title: 1、类的基础语法与成员定义
---
## 📚 目录

1. [什么是类？基础概念](#1-什么是类基础概念)
2. [类的定义和声明](#2-类的定义和声明)
3. [构造函数与初始化](#3-构造函数与初始化)
4. [属性类型注解](#4-属性类型注解)
5. [方法类型定义](#5-方法类型定义)
6. [this指向与类型约束](#6-this指向与类型约束)
7. [对象实例化](#7-对象实例化)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🏗️ 什么是类？基础概念


### 1.1 类的本质理解


**🔸 什么是类？**
```
简单理解：类就是制造对象的"模板"或"蓝图"

现实比喻：
汽车设计图纸 → 类 (Car)
具体的汽车   → 对象 (myCar, yourCar)

一张图纸可以造出很多台汽车
一个类可以创建很多个对象
```

**💡 类 vs 对象的关系**
```
类（Class）：                对象（Object）：
┌─────────────┐             ┌─────────────┐
│   Person    │    创建     │    小明     │
│ ─────────── │   ────→     │ ─────────── │
│ name: 姓名  │             │ name: "小明" │
│ age: 年龄   │             │ age: 25     │
│ sayHi()     │             │ sayHi()     │
└─────────────┘             └─────────────┘

类是抽象的模板                对象是具体的实例
```

### 1.2 为什么要用类？


**🎯 类的核心价值**
```
✅ 代码复用：写一次，创建多个相似对象
✅ 结构清晰：把相关的数据和方法组织在一起
✅ 类型安全：TypeScript 能检查对象的结构和类型
✅ 维护性好：修改模板，所有实例都受益
```

---

## 2. 📝 类的定义和声明


### 2.1 基础语法结构


**🔸 最简单的类**
```typescript
class Person {
  // 这里定义类的内容
}
```

**🔸 带基本成员的类**
```typescript
class Person {
  name: string;           // 属性：存储数据
  age: number;           // 属性：存储数据
  
  constructor(name: string, age: number) {  // 构造函数：初始化
    this.name = name;
    this.age = age;
  }
  
  sayHi(): string {      // 方法：定义行为
    return `Hello, I'm ${this.name}`;
  }
}
```

### 2.2 类的组成部分


**🏗️ 类的结构图解**
```
class Person {
  ┌─────────────────────────────────────┐
  │           属性声明区域               │
  │  name: string;                     │
  │  age: number;                      │
  └─────────────────────────────────────┘
  
  ┌─────────────────────────────────────┐
  │           构造函数区域               │
  │  constructor(name: string, age: number) {│
  │    this.name = name;               │
  │    this.age = age;                 │
  │  }                                 │
  └─────────────────────────────────────┘
  
  ┌─────────────────────────────────────┐
  │           方法定义区域               │
  │  sayHi(): string {                 │
  │    return `Hello, I'm ${this.name}`;│
  │  }                                 │
  └─────────────────────────────────────┘
}
```

---

## 3. 🔧 构造函数与初始化


### 3.1 构造函数的作用


**🔸 构造函数是什么？**
```
构造函数 = 对象的"出生证明"

作用：
1. 创建对象时自动调用
2. 设置对象的初始状态
3. 接收参数来定制对象
```

### 3.2 构造函数语法


**💡 基础构造函数**
```typescript
class Car {
  brand: string;
  price: number;
  
  // constructor 是固定关键字，不能改名
  constructor(brand: string, price: number) {
    this.brand = brand;    // this.属性名 = 参数值
    this.price = price;
  }
}

// 使用时：
const myCar = new Car("Toyota", 200000);
// 相当于调用了 constructor("Toyota", 200000)
```

**🔸 参数类型约束**
```typescript
class Student {
  name: string;
  grade: number;
  
  constructor(name: string, grade: number) {
    // TypeScript 会检查参数类型
    this.name = name;      // ✅ string 赋值给 string
    this.grade = grade;    // ✅ number 赋值给 number
    
    // this.grade = name;  // ❌ 类型错误！string 不能赋值给 number
  }
}
```

### 3.3 构造函数的参数处理


**🎯 默认参数**
```typescript
class Book {
  title: string;
  pages: number;
  
  constructor(title: string, pages: number = 100) {  // 默认100页
    this.title = title;
    this.pages = pages;
  }
}

const book1 = new Book("TypeScript入门");      // pages = 100
const book2 = new Book("JavaScript高级", 300); // pages = 300
```

**🎯 可选参数**
```typescript
class User {
  name: string;
  email?: string;  // 可选属性
  
  constructor(name: string, email?: string) {  // 可选参数
    this.name = name;
    if (email) {
      this.email = email;
    }
  }
}

const user1 = new User("张三");           // 只传name
const user2 = new User("李四", "li4@qq.com"); // 传两个参数
```

---

## 4. 🏷️ 属性类型注解


### 4.1 属性声明的重要性


**🔸 为什么要声明属性？**
```typescript
// ❌ 错误写法：没有声明属性
class Person {
  constructor(name: string) {
    this.name = name;  // TypeScript 报错：属性'name'不存在
  }
}

// ✅ 正确写法：先声明后使用
class Person {
  name: string;      // 必须先声明属性
  
  constructor(name: string) {
    this.name = name;  // 现在可以安全赋值了
  }
}
```

### 4.2 常用属性类型


**📋 基础类型属性**
```typescript
class Product {
  // 基础类型
  name: string;           // 字符串
  price: number;          // 数字
  isOnSale: boolean;      // 布尔值
  
  // 数组类型
  tags: string[];         // 字符串数组
  reviews: number[];      // 数字数组
  
  // 对象类型
  supplier: {
    name: string;
    phone: string;
  };
  
  constructor(name: string, price: number) {
    this.name = name;
    this.price = price;
    this.isOnSale = false;
    this.tags = [];
    this.reviews = [];
    this.supplier = { name: "", phone: "" };
  }
}
```

### 4.3 可选属性与默认值


**🔸 可选属性**
```typescript
class Employee {
  name: string;        // 必需属性
  age?: number;        // 可选属性（可能存在，也可能不存在）
  email?: string;      // 可选属性
  
  constructor(name: string) {
    this.name = name;
    // age 和 email 可以不设置
  }
}

const emp = new Employee("王五");
console.log(emp.age);  // undefined（可选属性可能为undefined）
```

**🔸 属性初始化**
```typescript
class Counter {
  count: number = 0;     // 直接初始化
  step: number = 1;      // 直接初始化
  
  constructor() {
    // 不需要在构造函数中设置count和step
  }
  
  increment() {
    this.count += this.step;
  }
}
```

---

## 5. ⚙️ 方法类型定义


### 5.1 方法的基本结构


**🔸 方法 = 类里面的函数**
```typescript
class Calculator {
  // 方法语法：方法名(参数): 返回类型 { 方法体 }
  add(a: number, b: number): number {
    return a + b;
  }
  
  // 无返回值的方法
  printResult(result: number): void {
    console.log(`结果是: ${result}`);
  }
  
  // 返回字符串的方法
  getInfo(): string {
    return "这是一个计算器";
  }
}
```

### 5.2 方法参数类型


**📝 参数类型约束**
```typescript
class StringHelper {
  // 单个参数
  reverse(text: string): string {
    return text.split('').reverse().join('');
  }
  
  // 多个参数
  repeat(text: string, times: number): string {
    return text.repeat(times);
  }
  
  // 可选参数
  format(text: string, upperCase?: boolean): string {
    return upperCase ? text.toUpperCase() : text;
  }
  
  // 默认参数
  padString(text: string, length: number = 10): string {
    return text.padStart(length, '0');
  }
}
```

### 5.3 方法返回类型


**🎯 返回类型的重要性**
```typescript
class DataProcessor {
  numbers: number[] = [];
  
  // 返回 number
  getSum(): number {
    return this.numbers.reduce((sum, num) => sum + num, 0);
  }
  
  // 返回 boolean
  isEmpty(): boolean {
    return this.numbers.length === 0;
  }
  
  // 返回 void（无返回值）
  clear(): void {
    this.numbers = [];
  }
  
  // 返回数组
  getEvenNumbers(): number[] {
    return this.numbers.filter(num => num % 2 === 0);
  }
}
```

**🔸 方法重载（同名不同参数）**
```typescript
class Formatter {
  // 重载声明：定义多种调用方式
  format(value: string): string;
  format(value: number): string;
  format(value: boolean): string;
  
  // 实际实现
  format(value: string | number | boolean): string {
    return String(value);
  }
}

const formatter = new Formatter();
formatter.format("hello");    // ✅ 调用string版本
formatter.format(123);        // ✅ 调用number版本
formatter.format(true);       // ✅ 调用boolean版本
```

---

## 6. 🎯 this指向与类型约束


### 6.1 this的含义


**🔸 this 是什么？**
```typescript
class Person {
  name: string;
  
  constructor(name: string) {
    this.name = name;  // this 指向当前创建的对象
  }
  
  introduce(): string {
    return `我是 ${this.name}`;  // this 指向调用方法的对象
  }
}

const person1 = new Person("张三");
const person2 = new Person("李四");

person1.introduce();  // this 指向 person1，输出"我是 张三"
person2.introduce();  // this 指向 person2，输出"我是 李四"
```

### 6.2 this的类型推断


**💡 TypeScript 自动推断 this 类型**
```typescript
class Counter {
  count: number = 0;
  
  increment(): void {
    this.count++;     // TypeScript 知道 this 是 Counter 类型
                      // 所以 this.count 是 number 类型
  }
  
  getCurrentCount(): number {
    return this.count;  // 自动提示和类型检查
  }
}
```

### 6.3 this绑定问题和解决


**⚠️ 常见的 this 丢失问题**
```typescript
class Button {
  text: string = "点击我";
  
  handleClick(): void {
    console.log(`按钮文字: ${this.text}`);
  }
}

const btn = new Button();

// ✅ 正常调用
btn.handleClick();  // 输出: "按钮文字: 点击我"

// ❌ this 丢失
const clickHandler = btn.handleClick;
clickHandler();     // 错误！this 不再指向 btn

// ✅ 解决方案1：箭头函数
class Button {
  text: string = "点击我";
  
  handleClick = (): void => {  // 箭头函数自动绑定 this
    console.log(`按钮文字: ${this.text}`);
  }
}

// ✅ 解决方案2：bind 绑定
const clickHandler = btn.handleClick.bind(btn);
clickHandler();  // 正常工作
```

---

## 7. 🚀 对象实例化


### 7.1 创建对象实例


**🔸 new 关键字的作用**
```typescript
class Dog {
  name: string;
  breed: string;
  
  constructor(name: string, breed: string) {
    this.name = name;
    this.breed = breed;
  }
  
  bark(): string {
    return `${this.name} says: 汪汪！`;
  }
}

// 创建对象实例
const dog1 = new Dog("旺财", "金毛");    // new 创建新对象
const dog2 = new Dog("小白", "泰迪");    // 又创建一个新对象

// 每个对象都是独立的
console.log(dog1.name);    // "旺财"
console.log(dog2.name);    // "小白"
```

### 7.2 类型推断与检查


**💡 TypeScript 的类型推断**
```typescript
class Car {
  brand: string;
  year: number;
  
  constructor(brand: string, year: number) {
    this.brand = brand;
    this.year = year;
  }
  
  getAge(): number {
    return new Date().getFullYear() - this.year;
  }
}

// TypeScript 自动推断类型
const myCar = new Car("Honda", 2020);  // myCar 的类型是 Car

// 享受类型提示和检查
myCar.brand;     // ✅ string 类型，有代码提示
myCar.year;      // ✅ number 类型，有代码提示
myCar.getAge();  // ✅ 方法调用，返回 number

// myCar.color;  // ❌ 编译错误：Car 类型没有 color 属性
```

### 7.3 对象实例的使用


**🎯 实例的常见操作**
```typescript
class BankAccount {
  balance: number;
  accountNumber: string;
  
  constructor(accountNumber: string, initialBalance: number = 0) {
    this.accountNumber = accountNumber;
    this.balance = initialBalance;
  }
  
  deposit(amount: number): void {
    this.balance += amount;
  }
  
  withdraw(amount: number): boolean {
    if (amount <= this.balance) {
      this.balance -= amount;
      return true;
    }
    return false;
  }
  
  getBalance(): number {
    return this.balance;
  }
}

// 创建和使用实例
const account = new BankAccount("123456", 1000);

// 调用方法
account.deposit(500);           // 存款
console.log(account.getBalance()); // 1500

account.withdraw(200);          // 取款
console.log(account.getBalance()); // 1300

// 访问属性
console.log(account.accountNumber); // "123456"
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 类的本质：制造对象的模板，定义对象的结构和行为
🔸 构造函数：创建对象时自动调用，负责初始化对象状态
🔸 属性声明：必须先声明属性类型，才能在类中使用
🔸 方法定义：类中的函数，包含参数类型和返回类型
🔸 this 指向：在类的方法中，this 指向调用方法的对象实例
🔸 对象实例化：使用 new 关键字创建类的具体实例
```

### 8.2 类的基础语法模板


**🔧 标准类定义模板**
```typescript
class ClassName {
  // 1. 属性声明（带类型）
  property1: type1;
  property2: type2;
  
  // 2. 构造函数（参数带类型）
  constructor(param1: type1, param2: type2) {
    this.property1 = param1;
    this.property2 = param2;
  }
  
  // 3. 方法定义（参数和返回值带类型）
  methodName(param: paramType): returnType {
    // 方法体
    return someValue;
  }
}

// 4. 创建实例
const instance = new ClassName(value1, value2);
```

### 8.3 常见错误和解决方案


| 错误类型 | **错误示例** | **正确做法** |
|---------|-------------|-------------|
| **未声明属性** | `直接在constructor中使用this.name` | `先声明 name: string;` |
| **类型不匹配** | `this.age = "18"` (string赋值给number) | `this.age = 18` |
| **this丢失** | `const fn = obj.method; fn()` | `使用箭头函数或bind绑定` |
| **忘记new** | `const obj = MyClass()` | `const obj = new MyClass()` |

### 8.4 实际应用建议


**🎯 什么时候使用类？**
```
✅ 需要创建多个相似对象时
✅ 对象有复杂的行为和状态时
✅ 需要面向对象编程模式时
✅ 需要继承和多态特性时

替代方案：
- 简单数据 → 使用接口 interface
- 函数集合 → 使用命名空间 namespace
- 单一对象 → 直接对象字面量
```

**💡 最佳实践**
```
1. 属性声明要清晰明确
2. 构造函数参数使用合理的默认值
3. 方法要有明确的返回类型
4. 避免过度复杂的类设计
5. 使用有意义的类名和方法名
```

**核心记忆**：
- 类是对象的模板，构造函数是对象的初始化器
- 属性要先声明类型，方法要标注参数和返回类型
- this 指向调用方法的对象，注意绑定问题
- 用 new 创建实例，TypeScript 提供完整的类型检查