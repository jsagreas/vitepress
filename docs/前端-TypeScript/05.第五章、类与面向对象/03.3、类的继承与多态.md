---
title: 3、类的继承与多态
---
## 📚 目录

1. [继承的基本概念](#1-继承的基本概念)
2. [extends继承语法](#2-extends继承语法)
3. [super关键字详解](#3-super关键字详解)
4. [方法重写机制](#4-方法重写机制)
5. [方法重载实现](#5-方法重载实现)
6. [多态的实现与应用](#6-多态的实现与应用)
7. [继承中的类型约束](#7-继承中的类型约束)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🧬 继承的基本概念


### 1.1 什么是继承


**🔸 简单理解**
```
继承就像现实中的"传承"：
- 孩子从父母那里继承特征和能力
- 子类从父类那里继承属性和方法
- 子类可以使用父类的所有公开功能
```

**💡 继承的本质**
```typescript
// 基础的"动物"类
class Animal {
    name: string;
    
    constructor(name: string) {
        this.name = name;
    }
    
    move() {
        console.log(`${this.name} 正在移动`);
    }
}

// "狗"类继承自"动物"类
class Dog extends Animal {
    // Dog 自动拥有了 name 属性和 move 方法
    bark() {
        console.log(`${this.name} 正在汪汪叫`);
    }
}

const myDog = new Dog("小白");
myDog.move();  // 使用继承来的方法 ✓
myDog.bark();  // 使用自己的方法 ✓
```

### 1.2 继承的核心优势


**🚀 代码复用**
```
优势说明：
✅ 避免重复编写相同代码
✅ 统一管理公共功能
✅ 便于维护和修改
✅ 建立类之间的层次关系
```

---

## 2. 📝 extends继承语法


### 2.1 基础继承语法


**🔸 语法格式**
```typescript
class 子类名 extends 父类名 {
    // 子类的新增内容
}
```

**💻 实际示例**
```typescript
// 父类：交通工具
class Vehicle {
    brand: string;
    speed: number;
    
    constructor(brand: string, speed: number) {
        this.brand = brand;
        this.speed = speed;
    }
    
    start() {
        console.log(`${this.brand} 开始启动`);
    }
    
    getInfo() {
        return `品牌：${this.brand}，速度：${this.speed}km/h`;
    }
}

// 子类：汽车
class Car extends Vehicle {
    doors: number;
    
    constructor(brand: string, speed: number, doors: number) {
        super(brand, speed);  // 调用父类构造函数
        this.doors = doors;
    }
    
    // 新增方法
    openDoor() {
        console.log(`打开${this.doors}个车门`);
    }
}
```

### 2.2 继承关系图解


```
继承关系示意：

    Vehicle (父类)
    ┌─────────────────┐
    │ brand: string   │
    │ speed: number   │
    │ start()         │
    │ getInfo()       │
    └─────────────────┘
            │
            │ extends
            ▼
     Car (子类)
    ┌─────────────────┐
    │ 继承父类所有属性  │
    │ doors: number   │ ← 新增属性
    │ openDoor()      │ ← 新增方法
    └─────────────────┘
```

### 2.3 继承的实际使用


```typescript
// 创建汽车实例
const myCar = new Car("丰田", 120, 4);

// 使用继承的方法
myCar.start();        // "丰田 开始启动"
console.log(myCar.getInfo()); // "品牌：丰田，速度：120km/h"

// 使用自己的方法
myCar.openDoor();     // "打开4个车门"

// 访问所有属性
console.log(myCar.brand);  // "丰田" (继承的)
console.log(myCar.speed);  // 120 (继承的)
console.log(myCar.doors);  // 4 (自己的)
```

---

## 3. 🔑 super关键字详解


### 3.1 super的作用


**🔸 核心功能**
```
super 关键字的两个主要用途：
1. 在构造函数中调用父类构造函数
2. 在方法中调用父类的方法
```

### 3.2 构造函数中的super


**💡 必须使用super的情况**
```typescript
class Person {
    name: string;
    age: number;
    
    constructor(name: string, age: number) {
        this.name = name;
        this.age = age;
    }
}

class Student extends Person {
    studentId: string;
    
    constructor(name: string, age: number, studentId: string) {
        // ⚠️ 必须先调用 super()，然后才能使用 this
        super(name, age);      // 调用父类构造函数
        this.studentId = studentId;
    }
}
```

**❌ 错误示例**
```typescript
class Student extends Person {
    studentId: string;
    
    constructor(name: string, age: number, studentId: string) {
        // ❌ 错误：必须先调用 super()
        this.studentId = studentId;  // TypeScript 会报错
        super(name, age);
    }
}
```

### 3.3 方法中的super


**🔸 调用父类方法**
```typescript
class Animal {
    name: string;
    
    constructor(name: string) {
        this.name = name;
    }
    
    makeSound() {
        console.log(`${this.name} 发出声音`);
    }
}

class Cat extends Animal {
    makeSound() {
        // 先调用父类的方法
        super.makeSound();
        // 再添加自己的逻辑
        console.log("喵喵喵~");
    }
}

const cat = new Cat("小花");
cat.makeSound();
// 输出：
// "小花 发出声音"
// "喵喵喵~"
```

### 3.4 super使用规则


| 使用场景 | **语法** | **说明** | **注意事项** |
|---------|---------|---------|-------------|
| **构造函数** | `super(参数)` | `调用父类构造函数` | `必须在使用this之前调用` |
| **普通方法** | `super.方法名()` | `调用父类的方法` | `可以在方法中任意位置调用` |
| **访问属性** | `super.属性名` | `访问父类的属性` | `通常不建议直接访问` |

---

## 4. 🔄 方法重写机制


### 4.1 什么是方法重写


**🔸 简单理解**
```
方法重写 = 子类重新定义父类已有的方法
- 方法名相同
- 参数相同
- 但实现逻辑不同
```

### 4.2 基础重写示例


```typescript
class Shape {
    color: string;
    
    constructor(color: string) {
        this.color = color;
    }
    
    // 父类的方法
    draw() {
        console.log(`绘制一个${this.color}的形状`);
    }
    
    getArea(): number {
        return 0;  // 基类返回默认值
    }
}

class Circle extends Shape {
    radius: number;
    
    constructor(color: string, radius: number) {
        super(color);
        this.radius = radius;
    }
    
    // 重写父类的 draw 方法
    draw() {
        console.log(`绘制一个${this.color}的圆形，半径为${this.radius}`);
    }
    
    // 重写父类的 getArea 方法
    getArea(): number {
        return Math.PI * this.radius * this.radius;
    }
}
```

### 4.3 方法重写的实际应用


```typescript
const shapes: Shape[] = [
    new Shape("红色"),
    new Circle("蓝色", 5)
];

// 调用相同的方法，但执行不同的逻辑
shapes.forEach(shape => {
    shape.draw();
    console.log(`面积：${shape.getArea()}`);
});

// 输出：
// "绘制一个红色的形状"
// "面积：0"
// "绘制一个蓝色的圆形，半径为5"
// "面积：78.54"
```

### 4.4 重写时的类型约束


```typescript
class Base {
    process(data: string): string {
        return `处理：${data}`;
    }
}

class Extended extends Base {
    // ✅ 正确：参数和返回值类型匹配
    process(data: string): string {
        return `高级处理：${data}`;
    }
    
    // ❌ 错误：不能改变参数类型
    // process(data: number): string {
    //     return `处理数字：${data}`;
    // }
}
```

---

## 5. 🔀 方法重载实现


### 5.1 TypeScript中的方法重载


**🔸 基本概念**
```
方法重载 = 同一个方法名，不同的参数组合
- 提供多种调用方式
- 根据参数自动选择对应的实现
```

### 5.2 重载语法示例


```typescript
class Calculator {
    // 重载签名声明
    add(a: number, b: number): number;
    add(a: string, b: string): string;
    add(a: number[], b: number[]): number[];
    
    // 实际实现
    add(a: any, b: any): any {
        if (typeof a === 'number' && typeof b === 'number') {
            return a + b;
        }
        if (typeof a === 'string' && typeof b === 'string') {
            return a + b;
        }
        if (Array.isArray(a) && Array.isArray(b)) {
            return [...a, ...b];
        }
        throw new Error('不支持的参数类型');
    }
}

const calc = new Calculator();

// 不同的调用方式
console.log(calc.add(1, 2));           // 3
console.log(calc.add("Hello", "World")); // "HelloWorld"
console.log(calc.add([1, 2], [3, 4]));   // [1, 2, 3, 4]
```

### 5.3 继承中的方法重载


```typescript
class BaseProcessor {
    // 基类的重载
    process(data: string): string;
    process(data: number): number;
    process(data: any): any {
        if (typeof data === 'string') {
            return `基础处理：${data}`;
        }
        if (typeof data === 'number') {
            return data * 2;
        }
    }
}

class AdvancedProcessor extends BaseProcessor {
    // 子类可以继承重载，也可以添加新的重载
    process(data: string): string;
    process(data: number): number;
    process(data: boolean): boolean;  // 新增的重载
    
    process(data: any): any {
        if (typeof data === 'boolean') {
            return !data;  // 新增的逻辑
        }
        // 对于其他类型，调用父类方法
        return super.process(data);
    }
}
```

---

## 6. 🎭 多态的实现与应用


### 6.1 多态的核心概念


**🔸 通俗理解**
```
多态 = "多种形态"
- 同一个操作，在不同对象上有不同的行为
- 父类引用可以指向任何子类对象
- 运行时决定调用哪个具体的方法
```

### 6.2 多态的经典示例


```typescript
// 基类：员工
class Employee {
    name: string;
    
    constructor(name: string) {
        this.name = name;
    }
    
    work() {
        console.log(`${this.name} 正在工作`);
    }
    
    calculateSalary(): number {
        return 5000;  // 基础工资
    }
}

// 子类：程序员
class Developer extends Employee {
    work() {
        console.log(`${this.name} 正在编写代码`);
    }
    
    calculateSalary(): number {
        return 8000;
    }
}

// 子类：设计师
class Designer extends Employee {
    work() {
        console.log(`${this.name} 正在设计界面`);
    }
    
    calculateSalary(): number {
        return 7000;
    }
}
```

### 6.3 多态的实际应用


```typescript
// 多态的威力：统一处理不同类型的员工
function manageEmployee(employee: Employee) {
    employee.work();  // 会调用各自重写的方法
    console.log(`工资：${employee.calculateSalary()}元`);
}

// 创建不同类型的员工
const employees: Employee[] = [
    new Employee("张三"),
    new Developer("李四"),
    new Designer("王五")
];

// 统一处理，但表现不同
employees.forEach(emp => {
    manageEmployee(emp);
});

// 输出：
// "张三 正在工作" "工资：5000元"
// "李四 正在编写代码" "工资：8000元"  
// "王五 正在设计界面" "工资：7000元"
```

### 6.4 多态的优势


**🚀 核心价值**
```
代码的可扩展性：
✅ 新增类型无需修改现有代码
✅ 统一的接口，不同的实现
✅ 降低代码耦合度
✅ 提高代码的灵活性
```

**📝 扩展示例**
```typescript
// 新增一个经理类，无需修改 manageEmployee 函数
class Manager extends Employee {
    work() {
        console.log(`${this.name} 正在管理团队`);
    }
    
    calculateSalary(): number {
        return 12000;
    }
}

// 直接可以使用现有的管理函数
const manager = new Manager("赵六");
manageEmployee(manager);  // "赵六 正在管理团队" "工资：12000元"
```

---

## 7. 🎯 继承中的类型约束


### 7.1 子类型兼容性


**🔸 基本规则**
```typescript
class Animal {
    name: string;
    constructor(name: string) {
        this.name = name;
    }
}

class Dog extends Animal {
    breed: string;
    constructor(name: string, breed: string) {
        super(name);
        this.breed = breed;
    }
}

// ✅ 子类可以赋值给父类类型
let animal: Animal = new Dog("旺财", "金毛");

// ❌ 父类不能直接赋值给子类类型
// let dog: Dog = new Animal("动物");  // 类型错误
```

### 7.2 方法重写的类型约束


```typescript
class Base {
    process(input: string): string {
        return input;
    }
}

class Derived extends Base {
    // ✅ 可以返回更具体的类型（协变）
    process(input: string): string {
        return input.toUpperCase();
    }
    
    // ❌ 不能改变参数类型
    // process(input: number): string {  // 错误
    //     return input.toString();
    // }
}
```

### 7.3 泛型继承的类型约束


```typescript
// 泛型基类
class Container<T> {
    private items: T[] = [];
    
    add(item: T): void {
        this.items.push(item);
    }
    
    get(index: number): T | undefined {
        return this.items[index];
    }
}

// 继承泛型类
class NumberContainer extends Container<number> {
    getSum(): number {
        let sum = 0;
        for (let i = 0; this.get(i) !== undefined; i++) {
            sum += this.get(i)!;
        }
        return sum;
    }
}

// 使用示例
const numbers = new NumberContainer();
numbers.add(1);
numbers.add(2);
numbers.add(3);
console.log(numbers.getSum()); // 6
```

### 7.4 访问修饰符的继承规则


| 修饰符 | **父类中** | **子类中** | **实例访问** |
|--------|-----------|-----------|-------------|
| **public** | `✅ 可访问` | `✅ 可访问和重写` | `✅ 可访问` |
| **protected** | `✅ 可访问` | `✅ 可访问和重写` | `❌ 不可访问` |
| **private** | `✅ 可访问` | `❌ 不可访问` | `❌ 不可访问` |

```typescript
class Parent {
    public publicProp = "公开属性";
    protected protectedProp = "受保护属性"; 
    private privateProp = "私有属性";
}

class Child extends Parent {
    showProps() {
        console.log(this.publicProp);     // ✅ 可以访问
        console.log(this.protectedProp); // ✅ 可以访问
        // console.log(this.privateProp);  // ❌ 无法访问
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 继承基础：extends 关键字建立父子类关系
🔸 super 关键字：构造函数调用和方法调用的桥梁
🔸 方法重写：子类重新实现父类方法，实现个性化行为
🔸 方法重载：同名方法的多种参数形式
🔸 多态机制：父类引用指向子类，运行时动态绑定
🔸 类型约束：继承关系中的类型安全和兼容性规则
```

### 8.2 关键理解要点


**🔹 继承的本质**
```
继承不是复制代码，而是建立关系：
- 子类"是一个"父类的特殊版本
- 共享父类的行为，扩展特定功能
- 通过类型系统保证使用安全
```

**🔹 super的使用时机**
```
构造函数中：
- 必须调用 super() 初始化父类
- 必须在使用 this 之前调用

方法中：
- 可选择性调用 super.方法名()
- 通常用于扩展而非完全替换父类逻辑
```

**🔹 多态的价值**
```
统一接口，多样实现：
- 写一套代码，处理多种类型
- 新增类型无需修改现有逻辑
- 降低系统复杂度和耦合度
```

### 8.3 实际应用指导


**🎯 什么时候使用继承**
- ✅ 存在明确的"是一个"关系（Dog 是一个 Animal）
- ✅ 需要代码复用和统一管理
- ✅ 有层次化的类型体系

**🎯 继承的注意事项**
- ⚠️ 不要为了复用代码而强行继承
- ⚠️ 保持继承层次简单，避免过深嵌套
- ⚠️ 优先考虑组合而非继承

**🎯 TypeScript特有优势**
- 🔒 编译时类型检查，避免运行时错误
- 🔍 更好的代码提示和重构支持
- 📝 接口与类的结合，更灵活的设计

**核心记忆要点**：
- 继承建立"是一个"的关系，不是简单的代码复用
- super是连接父子类的桥梁，构造函数中必须先调用
- 多态让同一接口有不同表现，是面向对象的精髓
- TypeScript的类型系统让继承更安全更可控