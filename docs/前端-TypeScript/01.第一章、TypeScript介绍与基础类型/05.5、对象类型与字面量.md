---
title: 5、对象类型与字面量
---
## 📚 目录

1. [对象类型定义基础](#1-对象类型定义基础)
2. [索引签名详解](#2-索引签名详解)
3. [只读属性控制](#3-只读属性控制)
4. [对象解构类型处理](#4-对象解构类型处理)
5. [字面量类型详解](#5-字面量类型详解)
6. [嵌套对象类型定义](#6-嵌套对象类型定义)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🏗️ 对象类型定义基础


### 1.1 对象类型的本质理解


**简单理解**：对象类型就是告诉 TypeScript "这个对象里面有什么属性，每个属性是什么类型"

```typescript
// 最基本的对象类型定义
let user: {
  name: string;
  age: number;
} = {
  name: "张三",
  age: 25
};
```

### 1.2 可选属性的实际应用


**核心概念**：有些属性可能有，也可能没有，用 `?` 标记

```typescript
// 用户信息，邮箱可能没填
type UserInfo = {
  name: string;        // 必须有姓名
  age: number;         // 必须有年龄
  email?: string;      // 邮箱可选
  phone?: string;      // 电话可选
};

// 这样使用都是合法的
const user1: UserInfo = { name: "李四", age: 30 };
const user2: UserInfo = { name: "王五", age: 28, email: "wang@qq.com" };
```

**实际场景**：
```
API 接口返回的数据 → 某些字段可能为空
表单数据 → 某些输入框可能没填写
配置对象 → 某些配置可能使用默认值
```

### 1.3 对象类型定义的常见写法


```typescript
// 方式1：直接定义（适合一次性使用）
let product: { name: string; price: number; };

// 方式2：类型别名（推荐，可复用）
type Product = {
  name: string;
  price: number;
};

// 方式3：接口定义（面向对象风格）
interface IProduct {
  name: string;
  price: number;
}
```

---

## 2. 🔑 索引签名详解


### 2.1 什么是索引签名


**通俗理解**：当你不知道对象会有哪些属性名，但知道属性值的类型时使用

```typescript
// 基础语法
type MyObject = {
  [key: string]: any;  // 允许任意字符串作为属性名
};

// 实际例子：存储用户配置
type UserSettings = {
  [setting: string]: boolean;
};

const settings: UserSettings = {
  darkMode: true,
  notifications: false,
  autoSave: true,
  // 可以添加任意新的布尔配置项
};
```

### 2.2 索引签名的实际应用场景


```typescript
// 场景1：API返回的动态数据
type ApiResponse = {
  status: number;                    // 固定属性
  message: string;                   // 固定属性
  [key: string]: any;               // 动态属性
};

// 场景2：多语言翻译对象
type Translations = {
  [languageCode: string]: string;
};

const messages: Translations = {
  "zh": "你好",
  "en": "Hello",
  "ja": "こんにちは"
};

// 场景3：CSS样式对象
type CSSProperties = {
  [property: string]: string | number;
};
```

### 2.3 索引签名的限制和注意事项


```typescript
// ⚠️ 注意：索引签名会"吃掉"所有属性
type BadExample = {
  name: string;           // ❌ 错误！string 不能赋值给 number
  [key: string]: number;  // 所有属性都必须是 number
};

// ✅ 正确写法：使用联合类型
type GoodExample = {
  name: string;
  [key: string]: string | number;  // 允许字符串或数字
};
```

---

## 3. 🔒 只读属性控制


### 3.1 readonly 的基本用法


**核心概念**：有些属性一旦设置就不能修改，像身份证号一样

```typescript
type Person = {
  readonly id: number;        // 只读：不能修改
  readonly birthDate: string; // 只读：不能修改
  name: string;               // 普通：可以修改
  age: number;                // 普通：可以修改
};

const person: Person = {
  id: 12345,
  birthDate: "1990-01-01",
  name: "张三",
  age: 25
};

// person.id = 67890;        // ❌ 编译错误：不能修改只读属性
person.name = "李四";         // ✅ 正确：可以修改普通属性
```

### 3.2 readonly 的实际应用场景


```typescript
// 场景1：配置对象
type AppConfig = {
  readonly apiUrl: string;      // API地址不能改
  readonly version: string;     // 版本号不能改
  theme: 'light' | 'dark';     // 主题可以改
};

// 场景2：数据库实体
type DatabaseRecord = {
  readonly id: number;          // 主键不能改
  readonly createdAt: Date;     // 创建时间不能改
  updatedAt: Date;              // 更新时间可以改
  content: string;              // 内容可以改
};
```

### 3.3 整个对象只读的处理


```typescript
// 方式1：使用 Readonly 工具类型
type User = {
  name: string;
  age: number;
};

type ReadonlyUser = Readonly<User>;
// 等价于：
// type ReadonlyUser = {
//   readonly name: string;
//   readonly age: number;
// }

// 方式2：对象字面量的 as const
const config = {
  host: "localhost",
  port: 3000
} as const;  // 所有属性都变成只读字面量类型
```

---

## 4. 📦 对象解构类型处理


### 4.1 解构赋值的类型推断


**基本理解**：解构就是把对象拆开，TypeScript 会自动推断每个变量的类型

```typescript
// 基础解构
type User = {
  name: string;
  age: number;
  email?: string;
};

const user: User = { name: "张三", age: 25 };

// 解构时 TypeScript 自动推断类型
const { name, age } = user;  // name: string, age: number
```

### 4.2 解构时的类型处理技巧


```typescript
// 技巧1：解构并重命名
const { name: userName, age: userAge } = user;
// userName: string, userAge: number

// 技巧2：解构可选属性
const { email = "未填写" } = user;  // email: string

// 技巧3：解构嵌套对象
type Address = {
  city: string;
  street: string;
};

type UserWithAddress = {
  name: string;
  address: Address;
};

const userWithAddr: UserWithAddress = {
  name: "李四",
  address: { city: "北京", street: "长安街" }
};

const { address: { city, street } } = userWithAddr;
// city: string, street: string
```

### 4.3 函数参数解构


```typescript
// 函数参数解构的类型定义
function updateUser({ name, age }: { name: string; age: number }) {
  console.log(`更新用户：${name}，年龄：${age}`);
}

// 更清晰的写法：先定义类型
type UpdateUserParams = {
  name: string;
  age: number;
};

function updateUserBetter({ name, age }: UpdateUserParams) {
  console.log(`更新用户：${name}，年龄：${age}`);
}
```

---

## 5. 🎯 字面量类型详解


### 5.1 字面量类型的基本概念


**简单理解**：不是"任意字符串"，而是"具体的这个字符串"

```typescript
// 普通类型 vs 字面量类型
let normalString: string = "任意字符串";      // 可以是任何字符串
let literalString: "hello" = "hello";        // 只能是 "hello"

// literalString = "world";  // ❌ 错误：只能是 "hello"
```

### 5.2 字符串字面量类型的实际应用


```typescript
// 场景1：定义有限的选项
type Theme = "light" | "dark" | "auto";
type Language = "zh" | "en" | "ja";

function setTheme(theme: Theme) {
  // theme 只能是三个值中的一个
  console.log(`设置主题为：${theme}`);
}

setTheme("light");    // ✅ 正确
// setTheme("blue");  // ❌ 错误：不在允许的值中

// 场景2：HTTP 方法
type HttpMethod = "GET" | "POST" | "PUT" | "DELETE";

function request(url: string, method: HttpMethod) {
  // method 只能是这四个 HTTP 方法
}
```

### 5.3 数字和布尔字面量类型


```typescript
// 数字字面量
type DiceNumber = 1 | 2 | 3 | 4 | 5 | 6;
type HttpStatus = 200 | 404 | 500;

function rollDice(): DiceNumber {
  return Math.ceil(Math.random() * 6) as DiceNumber;
}

// 布尔字面量（较少用，但有特殊场景）
type IsEnabled = true;  // 只能是 true，不能是 false

// 混合字面量类型
type MixedLiteral = "success" | "error" | 200 | 404 | true;
```

### 5.4 字面量类型的类型收窄


```typescript
// TypeScript 会根据条件自动收窄类型
type Status = "loading" | "success" | "error";

function handleStatus(status: Status) {
  if (status === "loading") {
    // 这里 status 的类型被收窄为 "loading"
    console.log("正在加载...");
  } else if (status === "success") {
    // 这里 status 的类型被收窄为 "success"
    console.log("加载成功");
  } else {
    // 这里 status 的类型被收窄为 "error"
    console.log("加载失败");
  }
}
```

---

## 6. 🏢 嵌套对象类型定义


### 6.1 嵌套对象的基本定义


**实际理解**：对象里面包含对象，就像公司里的部门结构

```typescript
// 基础嵌套结构
type Address = {
  province: string;
  city: string;
  street: string;
};

type User = {
  name: string;
  age: number;
  address: Address;  // 嵌套另一个对象类型
};

const user: User = {
  name: "张三",
  age: 30,
  address: {
    province: "北京市",
    city: "海淀区",
    street: "中关村大街"
  }
};
```

### 6.2 复杂嵌套结构的实际应用


```typescript
// 电商订单的复杂结构
type Product = {
  id: number;
  name: string;
  price: number;
};

type OrderItem = {
  product: Product;
  quantity: number;
  subtotal: number;
};

type Customer = {
  id: number;
  name: string;
  email: string;
  address: Address;
};

type Order = {
  orderId: string;
  customer: Customer;        // 嵌套客户信息
  items: OrderItem[];        // 嵌套商品数组
  total: number;
  status: "pending" | "shipped" | "delivered";
  createdAt: Date;
};
```

### 6.3 嵌套对象的可选属性处理


```typescript
// 实际场景：用户资料，某些信息可能不完整
type ContactInfo = {
  email?: string;
  phone?: string;
  qq?: string;
};

type PersonalInfo = {
  name: string;
  age: number;
  avatar?: string;
};

type UserProfile = {
  basic: PersonalInfo;          // 基本信息必须有
  contact?: ContactInfo;        // 联系方式可选
  preferences?: {               // 偏好设置可选
    theme: "light" | "dark";
    language: "zh" | "en";
    notifications: boolean;
  };
};

// 使用时的灵活性
const profile1: UserProfile = {
  basic: { name: "张三", age: 25 }
  // contact 和 preferences 都可以不提供
};

const profile2: UserProfile = {
  basic: { name: "李四", age: 30, avatar: "avatar.jpg" },
  contact: { email: "li@example.com" },
  preferences: {
    theme: "dark",
    language: "zh",
    notifications: true
  }
};
```

### 6.4 嵌套对象的访问和类型安全


```typescript
// 安全访问嵌套属性
function getUserCity(user: User): string | undefined {
  return user.address?.city;  // 可选链操作符
}

// 类型守卫确保安全访问
function hasCompleteAddress(user: User): boolean {
  return !!(user.address && 
           user.address.province && 
           user.address.city && 
           user.address.street);
}

// 解构嵌套对象
const { address: { city, street } } = user;  // city: string, street: string
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 对象类型定义：描述对象的"形状"，指定属性名和类型
🔸 可选属性(?)：某些属性可能存在，也可能不存在
🔸 索引签名：处理属性名不确定的动态对象
🔸 readonly：防止属性被意外修改
🔸 字面量类型：限制值只能是特定的几个选项
🔸 嵌套对象：对象内部包含其他对象的类型定义
```

### 7.2 实际应用的最佳实践


```typescript
// ✅ 推荐的类型定义模式
type ApiResponse<T> = {
  readonly status: number;
  readonly message: string;
  data?: T;
  [key: string]: any;  // 扩展字段
};

type UserData = {
  id: number;
  name: string;
  email?: string;
  profile: {
    avatar?: string;
    bio?: string;
    settings: {
      theme: "light" | "dark";
      language: "zh" | "en";
    };
  };
};
```

### 7.3 常见错误和避免方法


```typescript
// ❌ 常见错误1：忘记可选属性
type BadUser = {
  name: string;
  email: string;  // 如果email可能为空，应该用 email?: string
};

// ❌ 常见错误2：索引签名类型冲突
type BadConfig = {
  name: string;
  [key: string]: number;  // 冲突！name是string但索引签名要求number
};

// ✅ 正确写法
type GoodConfig = {
  name: string;
  [key: string]: string | number;
};
```

### 7.4 记忆要点


**核心理解**：
- **对象类型** = 描述对象的"身份证"
- **可选属性** = 可有可无的信息
- **只读属性** = 一旦确定不能改的信息  
- **字面量类型** = 限定具体值的类型
- **嵌套对象** = 对象套对象的结构描述

**实用技巧**：
- 优先使用 `type` 定义对象类型（简洁明了）
- 合理使用可选属性，避免强制要求不必要的字段
- 对不会变的属性使用 `readonly`
- 用字面量类型替代"魔法字符串"
- 嵌套不要太深，保持结构清晰