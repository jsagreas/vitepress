---
title: 4、数组与元组类型
---
## 📚 目录

1. [数组类型基础](#1-数组类型基础)
2. [元组类型详解](#2-元组类型详解)
3. [只读数组与不可变性](#3-只读数组与不可变性)
4. [数组解构与类型推断](#4-数组解构与类型推断)
5. [可选元组元素](#5-可选元组元素)
6. [实际应用与最佳实践](#6-实际应用与最佳实践)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 📊 数组类型基础


### 1.1 什么是数组类型


**简单理解**：数组类型就是告诉TypeScript"这个数组里装的都是什么类型的数据"

```typescript
// 就像给盒子贴标签 - 这个盒子里装的都是数字
let numbers: number[] = [1, 2, 3, 4, 5];

// 这个盒子里装的都是字符串
let names: string[] = ["张三", "李四", "王五"];
```

### 1.2 两种数组类型写法


**🔸 方括号写法 `T[]`**（推荐）
```typescript
let scores: number[] = [85, 92, 78];
let cities: string[] = ["北京", "上海", "深圳"];
let flags: boolean[] = [true, false, true];
```

**🔸 泛型写法 `Array<T>`**
```typescript
let scores: Array<number> = [85, 92, 78];
let cities: Array<string> = ["北京", "上海", "深圳"];
let flags: Array<boolean> = [true, false, true];
```

> **💡 选择建议**：日常开发推荐用 `T[]` 写法，更简洁直观

### 1.3 数组类型的好处


```typescript
let ages: number[] = [18, 25, 30];

// ✅ 正确：添加数字
ages.push(35);

// ❌ 错误：TypeScript会报错
ages.push("四十"); // 类型 "string" 的参数不能赋给类型 "number" 的参数
```

**实际意义**：
- 🛡️ **防止错误**：避免往数组里放错类型的数据
- 🔍 **代码提示**：编辑器会给出准确的方法提示
- 📖 **可读性强**：一眼就知道数组里应该放什么

### 1.4 常见数组类型示例


```typescript
// 基础类型数组
let userIds: number[] = [1001, 1002, 1003];
let userNames: string[] = ["admin", "user1", "guest"];
let isOnline: boolean[] = [true, false, true];

// 对象数组
let users: { id: number; name: string }[] = [
  { id: 1, name: "张三" },
  { id: 2, name: "李四" }
];

// 混合类型数组（联合类型）
let mixedData: (string | number)[] = ["hello", 42, "world", 100];
```

---

## 2. 🎯 元组类型详解


### 2.1 什么是元组


**通俗解释**：元组就像是"定制版数组"，不仅规定了每个位置放什么类型的数据，还规定了总共有几个位置

```
普通数组：[📱, 📱, 📱, 📱, ...] - 所有位置都是手机，数量不限
元组：    [📱, 💻, 🖥️]           - 第1个是手机，第2个是笔记本，第3个是台式机，就这3个
```

### 2.2 元组的基本语法


```typescript
// 定义一个元组：第一个是字符串，第二个是数字
let userInfo: [string, number] = ["张三", 25];

// 坐标点：x坐标和y坐标
let point: [number, number] = [100, 200];

// RGB颜色值
let color: [number, number, number] = [255, 128, 0];
```

### 2.3 元组 vs 数组的区别


```typescript
// 数组：类型相同，长度不固定
let scores: number[] = [85, 92, 78, 96]; // 可以有任意多个数字

// 元组：类型和位置都固定
let student: [string, number, boolean] = ["李四", 20, true]; // 必须是3个元素，类型必须对应
```

**📋 对比表格**

| 特性 | 数组 `number[]` | 元组 `[string, number]` |
|------|----------------|------------------------|
| **长度** | 可变 | 固定 |
| **类型** | 所有元素同类型 | 每个位置可以不同类型 |
| **用途** | 存储同类数据集合 | 存储结构化的异构数据 |

### 2.4 元组的实际应用场景


```typescript
// 函数返回多个值
function getUserInfo(): [string, number, string] {
  return ["张三", 25, "工程师"];
}

let [name, age, job] = getUserInfo();

// API 响应数据
type ApiResponse = [boolean, string, any]; // [成功标志, 消息, 数据]
let response: ApiResponse = [true, "获取成功", { id: 1, name: "用户1" }];

// 数据库查询结果
type QueryResult = [number, string, Date]; // [ID, 名称, 创建时间]
let userRecord: QueryResult = [1001, "张三", new Date()];
```

---

## 3. 🔒 只读数组与不可变性


### 3.1 为什么需要只读数组


**生活比喻**：有些东西只能看不能动，比如博物馆的展品

```typescript
// 普通数组：可以随意修改
let cities = ["北京", "上海", "深圳"];
cities.push("广州");    // ✅ 可以添加
cities[0] = "天津";     // ✅ 可以修改

// 只读数组：不能修改
let readonlyCities: readonly string[] = ["北京", "上海", "深圳"];
readonlyCities.push("广州");    // ❌ 报错：只读数组不能添加元素
readonlyCities[0] = "天津";     // ❌ 报错：不能修改只读数组的元素
```

### 3.2 三种只读数组写法


```typescript
// 方法1：readonly 修饰符（推荐）
let config: readonly string[] = ["dev", "test", "prod"];

// 方法2：ReadonlyArray 泛型
let settings: ReadonlyArray<string> = ["theme", "language", "timezone"];

// 方法3：const 断言（最严格）
let constants = ["API_URL", "APP_NAME"] as const;
```

### 3.3 只读数组的使用场景


```typescript
// 配置常量
const SUPPORTED_LANGUAGES: readonly string[] = ["zh-CN", "en-US", "ja-JP"];

// 枚举替代
const USER_ROLES = ["admin", "user", "guest"] as const;
type UserRole = typeof USER_ROLES[number]; // "admin" | "user" | "guest"

// 函数参数保护
function processData(data: readonly number[]) {
  // 这里不能修改传入的数组，确保数据安全
  return data.map(x => x * 2); // 只能读取，不能修改
}
```

---

## 4. 🎪 数组解构与类型推断


### 4.1 什么是数组解构


**简单理解**：解构就是"拆包"，把数组里的东西一个个取出来给变量

```typescript
// 传统方式：一个个取
let colors = ["红", "绿", "蓝"];
let first = colors[0];   // "红"
let second = colors[1];  // "绿"
let third = colors[2];   // "蓝"

// 解构方式：一次性取出
let [first, second, third] = ["红", "绿", "蓝"];
```

### 4.2 TypeScript 的类型推断


```typescript
// TypeScript 自动推断类型
let [name, age, isStudent] = ["张三", 20, true];
// name: string, age: number, isStudent: boolean

// 手动指定类型
let [x, y]: [number, number] = [100, 200];

// 混合解构
let userInfo: [string, number, boolean] = ["李四", 25, false];
let [userName, userAge, isVip] = userInfo;
```

### 4.3 解构的实用技巧


```typescript
// 跳过某些元素
let [first, , third] = [1, 2, 3]; // first = 1, third = 3

// 剩余元素
let [head, ...tail] = [1, 2, 3, 4, 5]; // head = 1, tail = [2, 3, 4, 5]

// 默认值
let [a = 0, b = 0] = [10]; // a = 10, b = 0

// 嵌套解构
let matrix = [[1, 2], [3, 4]];
let [[a, b], [c, d]] = matrix; // a=1, b=2, c=3, d=4
```

### 4.4 函数返回值解构


```typescript
// 函数返回元组
function getScreenSize(): [number, number] {
  return [1920, 1080];
}

let [width, height] = getScreenSize();
console.log(`屏幕尺寸：${width} × ${height}`);

// React useState 的经典用法
function useState<T>(initial: T): [T, (value: T) => void] {
  // 实现细节...
  return [initial, (value) => {}];
}

let [count, setCount] = useState(0);
```

---

## 5. ❓ 可选元组元素


### 5.1 什么是可选元组元素


**通俗理解**：有些位置的数据可有可无，就像点餐时的配菜

```typescript
// 基本信息：姓名必须有，年龄可选，爱好也可选
type UserInfo = [string, number?, string?];

let user1: UserInfo = ["张三"];                    // ✅ 只有姓名
let user2: UserInfo = ["李四", 25];               // ✅ 姓名 + 年龄  
let user3: UserInfo = ["王五", 30, "篮球"];       // ✅ 全部信息
```

### 5.2 可选元素的语法规则


```typescript
// ❌ 错误：可选元素必须在最后
type BadTuple = [string, number?, string]; // 报错

// ✅ 正确：可选元素在最后
type GoodTuple = [string, string, number?]; // 正确

// 多个可选元素
type FlexibleTuple = [string, number?, boolean?, string?];
```

### 5.3 实际应用场景


```typescript
// API 接口响应
type ApiResult = [boolean, string, any?]; // [成功标志, 消息, 数据(可选)]

let success: ApiResult = [true, "操作成功"];
let successWithData: ApiResult = [true, "获取成功", { id: 1, name: "用户" }];
let failure: ApiResult = [false, "操作失败"];

// 坐标系统
type Point2D = [number, number];          // 2D坐标
type Point3D = [number, number, number?]; // 3D坐标，z轴可选

let point2d: Point3D = [100, 200];        // 2D点
let point3d: Point3D = [100, 200, 50];    // 3D点
```

### 5.4 可选元组的类型检查


```typescript
type OptionalTuple = [string, number?];

function processData(data: OptionalTuple) {
  let [name, age] = data;
  
  console.log(name); // string - 确定存在
  
  if (age !== undefined) {
    console.log(age); // number - 需要检查后使用
  }
}

processData(["张三"]);       // ✅ 正确
processData(["李四", 25]);   // ✅ 正确
```

---

## 6. 🚀 实际应用与最佳实践


### 6.1 选择数组还是元组？


```
🤔 什么时候用数组？
- 存储同类型的数据集合
- 数量不确定或会变化
- 需要遍历处理

🤔 什么时候用元组？
- 存储固定结构的数据
- 每个位置含义不同
- 函数返回多个值
```

**🔸 数组使用场景**
```typescript
// 商品列表（数量不定）
let products: string[] = ["iPhone", "iPad", "MacBook"];

// 分数列表（需要计算平均值）
let scores: number[] = [85, 92, 78, 96];
let average = scores.reduce((a, b) => a + b) / scores.length;
```

**🔸 元组使用场景**
```typescript
// 数据库记录（结构固定）
type UserRecord = [number, string, string]; // [ID, 姓名, 邮箱]

// 函数返回多值
function divideWithRemainder(a: number, b: number): [number, number] {
  return [Math.floor(a / b), a % b]; // [商, 余数]
}
```

### 6.2 性能与内存考虑


```typescript
// ✅ 好习惯：明确类型可以帮助优化
let coordinates: [number, number] = [x, y]; // 明确只有2个数字

// ❌ 避免：过度嵌套的复杂元组
type BadTuple = [string, [number, [boolean, string]]]; // 太复杂，难维护

// ✅ 推荐：使用接口替代复杂元组
interface UserData {
  name: string;
  position: [number, number];
  isActive: boolean;
}
```

### 6.3 与其他类型的配合


```typescript
// 元组 + 联合类型
type Action = 
  | ["SET_NAME", string]
  | ["SET_AGE", number]
  | ["RESET"];

// 元组 + 泛型
type Result<T> = [boolean, T | null]; // [成功标志, 数据或null]

function fetchUser(id: number): Result<User> {
  // 实现逻辑...
  return [true, { id, name: "张三" }];
}

// 元组 + keyof
type ObjectEntry<T> = {
  [K in keyof T]: [K, T[K]];
}[keyof T];
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 数组类型：用 T[] 或 Array<T> 约束数组元素类型
🔸 元组类型：用 [T1, T2, T3] 定义固定长度和类型的数组
🔸 只读数组：用 readonly 或 ReadonlyArray 创建不可变数组
🔸 数组解构：从数组中提取元素并自动推断类型
🔸 可选元组：用 ? 标记可选的元组元素，必须在末尾
```

### 7.2 关键理解要点


**🔹 数组 vs 元组的选择**
```
数组：同类型数据的集合，长度可变
- 用户列表、商品列表、分数列表

元组：异构数据的组合，长度固定
- 坐标点、函数返回值、数据库记录
```

**🔹 只读的价值**
```
防止意外修改：保护重要数据不被改变
函数参数：确保函数不会修改传入的数组
配置数据：确保配置在运行时不被篡改
```

**🔹 类型推断的便利**
```
解构赋值：TypeScript 自动推断每个变量的类型
减少重复：不需要为每个变量单独声明类型
类型安全：即使是解构出的变量也有类型保护
```

### 7.3 实际应用价值


**🎯 业务场景应用**
- **前端开发**：组件 props 类型定义、状态管理
- **API 接口**：请求响应数据结构、错误处理
- **数据处理**：坐标计算、颜色值管理、配置项
- **工具函数**：返回多个值、参数校验

**🔧 开发实践**
- **类型安全**：编译时发现数组操作错误
- **代码提示**：IDE 提供准确的方法和属性提示  
- **重构支持**：类型约束让重构更安全
- **文档化**：类型即文档，清晰表达数据结构

### 7.4 常见误区避免


```typescript
// ❌ 误区1：滥用 any
let data: any[] = [1, "hello", true]; // 失去类型检查

// ✅ 正确：使用联合类型
let data: (number | string | boolean)[] = [1, "hello", true];

// ❌ 误区2：复杂元组嵌套
type BadDesign = [string, [number, [boolean, string]]];

// ✅ 正确：使用接口
interface GoodDesign {
  name: string;
  score: number;
  status: {
    isActive: boolean;
    message: string;
  };
}

// ❌ 误区3：忽略只读保护
function processArray(arr: number[]) {
  arr.push(0); // 可能意外修改原数组
}

// ✅ 正确：使用只读保护
function processArray(arr: readonly number[]) {
  return [...arr, 0]; // 创建新数组，不修改原数组
}
```

**核心记忆**：
- 数组装同类型，元组装结构化
- 只读保护很重要，解构推断很智能
- 选择合适的类型，让代码更安全更清晰