---
title: 2、基础数据类型与类型注解
---
## 📚 目录

1. [TypeScript类型系统概述](#1-TypeScript类型系统概述)
2. [三大原始数据类型](#2-三大原始数据类型)
3. [空值类型处理](#3-空值类型处理)
4. [特殊类型any与unknown](#4-特殊类型any与unknown)
5. [函数返回类型void与never](#5-函数返回类型void与never)
6. [类型注解与类型推断](#6-类型注解与类型推断)
7. [类型断言技巧](#7-类型断言技巧)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 TypeScript类型系统概述


### 1.1 为什么需要类型系统


**🤔 JavaScript的痛点**
```javascript
// JavaScript 中的常见问题
function add(a, b) {
    return a + b;
}

add(1, 2);        // 3 (正常)
add(1, "2");      // "12" (意外的字符串拼接)
add("hello");     // "helloundefined" (更意外的结果)
```

**✨ TypeScript 的解决方案**
```typescript
// TypeScript 明确类型，避免意外
function add(a: number, b: number): number {
    return a + b;
}

add(1, 2);        // ✅ 3
add(1, "2");      // ❌ 编译报错：类型不匹配
add("hello");     // ❌ 编译报错：参数不足且类型错误
```

> **💡 核心价值**：TypeScript 的类型系统让我们在 **编写代码时** 就能发现问题，而不是等到运行时才出错。

### 1.2 TypeScript 类型系统特点


```
TypeScript 类型系统架构：

┌─────────────────────────────────────────────────────────┐
│                  TypeScript 类型系统                    │
├─────────────────┬─────────────────┬─────────────────────┤
│   原始类型      │    对象类型      │     特殊类型        │
├─────────────────┼─────────────────┼─────────────────────┤
│• string         │• object         │• any               │
│• number         │• array          │• unknown           │
│• boolean        │• function       │• void              │
│• null           │• Date           │• never             │
│• undefined      │• 自定义类型      │• undefined         │
│• symbol         │                 │• null              │
│• bigint         │                 │                    │
└─────────────────┴─────────────────┴─────────────────────┘
```

**🔸 三个关键特征**：
- **静态类型检查**：编译时检查类型错误
- **类型推断**：自动推断变量类型
- **类型兼容性**：JavaScript 代码可以逐步迁移

---

## 2. 📝 三大原始数据类型


### 2.1 string 字符串类型


**🔤 基本使用**
```typescript
// 明确指定类型
let userName: string = "张三";
let email: string = 'user@example.com';
let message: string = `欢迎您，${userName}！`;

// 常见应用场景
function greet(name: string): string {
    return `你好，${name}！`;
}

// ✅ 正确使用
greet("李四");

// ❌ 错误使用
greet(123);  // 编译报错：不能将number赋给string
```

> **💡 记忆技巧**：string 就是文本内容，用引号包起来的都是字符串。

### 2.2 number 数字类型


**🔢 基本使用**
```typescript
// 整数和小数都是 number 类型
let age: number = 25;
let price: number = 99.9;
let score: number = 0;

// 特殊数值也是 number
let infinity: number = Infinity;
let notANumber: number = NaN;

// 实际应用
function calculateTotal(price: number, quantity: number): number {
    return price * quantity;
}

calculateTotal(10.5, 3);  // ✅ 正确
calculateTotal(10, 3);    // ✅ 也正确
```

> **💡 重要提醒**：TypeScript 中没有 int、float 的区别，所有数字都是 number 类型。

### 2.3 boolean 布尔类型


**✅ 基本使用**
```typescript
// 只有两个值：true 和 false
let isLoading: boolean = true;
let hasPermission: boolean = false;

// 条件判断的结果也是 boolean
let isAdult: boolean = age >= 18;
let isEmpty: boolean = userName.length === 0;

// 实际应用
function canAccess(isLoggedIn: boolean, hasPermission: boolean): boolean {
    return isLoggedIn && hasPermission;
}
```

**📊 三大原始类型对比表**

| **类型** | **用途** | **常见值** | **注意事项** |
|---------|---------|----------|-------------|
| **string** | `文本内容` | `"hello"`, `'world'`, `` `模板` `` | `单双引号都可以，推荐单引号` |
| **number** | `数值计算` | `42`, `3.14`, `NaN`, `Infinity` | `整数小数都是number` |
| **boolean** | `逻辑判断` | `true`, `false` | `只有这两个值，注意大小写` |

---

## 3. 🚫 空值类型处理


### 3.1 null 与 undefined 的区别


**🔍 概念理解**
```typescript
// null：明确表示"没有值"，是一个有意的空值
let data: null = null;          // 明确设置为空
let result: string | null = null; // 可能有值，也可能为空

// undefined：表示"未定义"，通常是意外的空值
let name: undefined = undefined;  // 未定义
let obj = { age: 20 };
console.log(obj.name);           // undefined (属性不存在)
```

**🎯 实际应用场景**
```typescript
// API 返回值处理
interface User {
    id: number;
    name: string;
    avatar: string | null;  // 头像可能为空
}

// 数组查找结果
function findUser(id: number): User | undefined {
    // 找到返回User，找不到返回undefined
    return users.find(user => user.id === id);
}

// 使用时的类型检查
const user = findUser(123);
if (user) {  // TypeScript 知道这里 user 不是 undefined
    console.log(user.name);  // ✅ 安全访问
}
```

### 3.2 严格空值检查


**⚙️ tsconfig.json 配置**
```json
{
  "compilerOptions": {
    "strictNullChecks": true  // 开启严格空值检查
  }
}
```

**🛡️ 安全处理空值**
```typescript
// 开启严格检查后，必须明确处理空值
function processUser(user: User | null) {
    // ❌ 直接访问会报错
    // console.log(user.name);  

    // ✅ 正确的处理方式
    if (user !== null) {
        console.log(user.name);  // 类型缩小，确定不是null
    }
    
    // ✅ 或者使用可选链（推荐）
    console.log(user?.name);
}
```

---

## 4. 🔀 特殊类型any与unknown


### 4.1 any：万能类型


**🚨 any 的作用与问题**
```typescript
// any 可以是任何类型，关闭类型检查
let value: any = 42;
value = "hello";     // ✅ 可以赋值字符串
value = true;        // ✅ 可以赋值布尔值
value = { x: 1 };    // ✅ 可以赋值对象

// any 可以访问任何属性和方法
value.foo.bar.baz;   // ✅ 编译通过，但运行时可能报错
value();             // ✅ 编译通过，但可能不是函数
value[0][1][2];      // ✅ 编译通过，但可能出错
```

**💡 any 的使用场景**
```typescript
// 1. 从 JavaScript 迁移时的临时方案
let legacyData: any = getLegacyData();

// 2. 第三方库没有类型定义时
declare const $: any;  // jQuery 没有类型时

// 3. 确实需要接受任意类型的工具函数
function logToConsole(data: any): void {
    console.log(JSON.stringify(data));
}
```

### 4.2 unknown：安全的任意类型


**🔒 unknown 的安全性**
```typescript
// unknown 可以接受任何值
let value: unknown = 42;
value = "hello";
value = true;

// 但使用前必须进行类型检查
// ❌ 直接使用会报错
// value.toUpperCase();
// value();
// value.x.y.z;

// ✅ 必须先检查类型
if (typeof value === "string") {
    console.log(value.toUpperCase());  // 现在知道是字符串了
}

if (typeof value === "function") {
    value();  // 现在知道是函数了
}
```

**📊 any vs unknown 对比**

| **特性** | **any** | **unknown** |
|---------|---------|------------|
| **接受任意值** | `✅ 可以` | `✅ 可以` |
| **直接使用** | `✅ 可以` | `❌ 需要类型检查` |
| **类型安全** | `❌ 不安全` | `✅ 安全` |
| **使用建议** | `尽量避免` | `优先选择` |

**🎯 最佳实践**
```typescript
// ❌ 避免使用 any
function processData(data: any) {
    return data.whatever.might.not.exist;
}

// ✅ 使用 unknown + 类型守卫
function processData(data: unknown) {
    if (typeof data === "object" && data !== null && "name" in data) {
        return (data as { name: string }).name;
    }
    return "未知数据";
}
```

---

## 5. 🚀 函数返回类型void与never


### 5.1 void：无返回值类型


**🔄 void 的含义**
```typescript
// void 表示函数不返回任何有用的值
function printMessage(msg: string): void {
    console.log(msg);
    // 没有 return 语句，或者 return; 或者 return undefined;
}

function clearData(): void {
    localStorage.clear();
    return;  // ✅ 可以只写 return
}

function resetForm(): void {
    // 隐式返回 undefined
}
```

**💡 void 使用场景**
```typescript
// 事件处理函数
button.addEventListener("click", function(): void {
    handleClick();
});

// 回调函数
setTimeout(function(): void {
    console.log("时间到了");
}, 1000);

// 注意：void 类型的变量只能赋值 undefined
let result: void = undefined;  // ✅ 正确
// let result: void = null;    // ❌ 严格模式下错误
```

### 5.2 never：永不返回类型


**🚫 never 的含义**
```typescript
// never 表示永远不会正常返回的函数
function throwError(message: string): never {
    throw new Error(message);
    // 函数到这里就结束了，不会正常返回
}

function infiniteLoop(): never {
    while (true) {
        console.log("永远循环");
    }
    // 永远执行不到这里
}
```

**🎯 never 的实际应用**
```typescript
// 穷尽性检查：确保所有情况都被处理
type Color = "red" | "green" | "blue";

function getColorName(color: Color): string {
    switch (color) {
        case "red":
            return "红色";
        case "green":
            return "绿色";
        case "blue":
            return "蓝色";
        default:
            // 如果所有情况都处理了，这里的 color 类型就是 never
            const exhaustiveCheck: never = color;
            throw new Error(`未处理的颜色: ${exhaustiveCheck}`);
    }
}
```

**📊 void vs never 对比**

| **类型** | **含义** | **返回值** | **使用场景** |
|---------|---------|----------|-------------|
| **void** | `无有用返回值` | `undefined` | `打印日志、修改状态、事件处理` |
| **never** | `永不返回` | `无返回` | `抛异常、死循环、穷尽检查` |

---

## 6. ✏️ 类型注解与类型推断


### 6.1 类型注解：明确指定类型


**📝 类型注解语法**
```typescript
// 变量类型注解
let name: string = "张三";
let age: number = 25;
let isStudent: boolean = true;

// 函数参数和返回值注解
function greet(name: string, age: number): string {
    return `你好，${name}，你今年${age}岁`;
}

// 对象类型注解
let user: { name: string; age: number } = {
    name: "李四",
    age: 30
};
```

**🎯 什么时候需要类型注解**
```typescript
// 1. 函数参数（必须）
function calculate(x: number, y: number) {
    return x + y;
}

// 2. 初始值无法推断类型时
let data: string | null = null;  // 后面可能赋值字符串

// 3. 想要更严格的类型约束
let status: "loading" | "success" | "error" = "loading";
```

### 6.2 类型推断：自动推断类型


**🤖 TypeScript 的智能推断**
```typescript
// TypeScript 可以自动推断这些类型
let name = "张三";        // 推断为 string
let age = 25;            // 推断为 number
let isActive = true;     // 推断为 boolean

// 函数返回值推断
function add(x: number, y: number) {
    return x + y;  // 推断返回类型为 number
}

// 数组类型推断
let numbers = [1, 2, 3];           // 推断为 number[]
let mixed = [1, "hello", true];    // 推断为 (string | number | boolean)[]
```

**🔍 类型推断的规则**
```typescript
// 最佳公共类型推断
let items = [1, 2, 3];           // number[]
let mixed = [1, "hello"];        // (number | string)[]
let empty = [];                  // any[] (需要后续推断)

// 上下文类型推断
window.onload = function(event) {
    // event 自动推断为 Event 类型
    console.log(event.target);
};

// 赋值时的类型推断
let x = 3;        // x 推断为 number
x = "hello";      // ❌ 错误：不能将 string 赋给 number
```

**📊 注解 vs 推断选择指南**

| **情况** | **建议** | **原因** |
|---------|---------|---------|
| **变量有初始值** | `使用推断` | `简洁，TypeScript 很聪明` |
| **函数参数** | `使用注解` | `必须明确，提高可读性` |
| **复杂类型** | `使用注解` | `确保类型正确` |
| **API 接口** | `使用注解` | `明确契约，便于维护` |

---

## 7. 🎭 类型断言技巧


### 7.1 as 语法：类型断言


**🔧 基本语法**
```typescript
// 语法：值 as 类型
let someValue: unknown = "hello world";
let strLength: number = (someValue as string).length;

// 另一种语法（不推荐，JSX 中会冲突）
let strLength2: number = (<string>someValue).length;
```

**⚠️ 类型断言不是类型转换**
```typescript
// ❌ 错误理解：以为会转换类型
let num: number = "123" as number;  // 运行时还是字符串！

// ✅ 正确理解：只是告诉编译器如何理解类型
let input = document.getElementById("input") as HTMLInputElement;
console.log(input.value);  // 现在可以访问 value 属性
```

### 7.2 类型断言的使用场景


**🎯 DOM 元素处理**
```typescript
// DOM 查询返回的是 Element | null，需要断言具体类型
let input = document.getElementById("username") as HTMLInputElement;
let button = document.querySelector(".btn") as HTMLButtonElement;

// 安全的写法
let element = document.getElementById("myDiv");
if (element) {
    let div = element as HTMLDivElement;
    div.style.color = "red";
}
```

**📡 API 响应处理**
```typescript
// 从服务器获取的数据，我们知道具体结构
async function getUser() {
    const response = await fetch("/api/user");
    const userData = await response.json() as {
        id: number;
        name: string;
        email: string;
    };
    
    return userData;
}
```

**🔄 类型缩小场景**
```typescript
function processValue(value: string | number) {
    if (typeof value === "string") {
        // 这里 TypeScript 已经知道是 string，不需要断言
        console.log(value.toUpperCase());
    } else {
        // 这里 TypeScript 知道是 number
        console.log(value.toFixed(2));
    }
}

// 但有时需要手动断言
function handleEvent(event: Event) {
    // 我们知道这是鼠标事件
    let mouseEvent = event as MouseEvent;
    console.log(mouseEvent.clientX, mouseEvent.clientY);
}
```

### 7.3 非空断言操作符


**❗ ! 操作符的使用**
```typescript
// ! 告诉 TypeScript：这个值肯定不是 null 或 undefined
function processUser(user: User | null) {
    // 我们确定 user 不是 null
    console.log(user!.name);  // 跳过空值检查
}

// DOM 操作中的使用
let button = document.getElementById("submitBtn")!;  // 确定元素存在
button.addEventListener("click", handleClick);
```

**⚠️ 非空断言的注意事项**
```typescript
// ❌ 危险用法：如果判断错误，运行时会报错
let user: User | null = getUser();
console.log(user!.name);  // 如果 user 是 null，运行时报错

// ✅ 安全用法：在确定不为空的情况下使用
function processValidUser(user: User | null) {
    if (user !== null) {
        console.log(user!.name);  // 这里可以安全使用
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 三大原始类型：string文本、number数字、boolean布尔
🔸 空值处理：null明确空值、undefined未定义，需要安全检查
🔸 特殊类型：any万能但不安全、unknown安全但需检查
🔸 函数类型：void无返回值、never永不返回
🔸 类型注解：明确指定类型，函数参数必须标注
🔸 类型推断：TypeScript自动推断，减少冗余代码
🔸 类型断言：as语法告诉编译器类型，不是类型转换
```

### 8.2 关键理解要点


**🔹 类型安全的价值**
```
编译时发现错误 > 运行时发现错误
明确的代码意图 > 隐含的代码逻辑
团队协作效率 > 个人开发便利
```

**🔹 类型选择的原则**
```
能推断就推断，保持代码简洁
必须注解的地方一定要注解
优先选择安全类型（unknown > any）
合理使用断言，但不要滥用
```

**🔹 渐进式学习路径**
```
第一步：掌握基础类型，能看懂类型错误
第二步：学会写类型注解，函数参数不报错
第三步：理解类型推断，减少冗余代码
第四步：合理使用断言，处理复杂场景
```

### 8.3 实际开发应用


**💼 日常开发实践**
```typescript
// ✅ 推荐的代码风格
function createUser(name: string, age: number) {  // 参数明确注解
    const user = { name, age, id: generateId() };  // 推断对象类型
    return user;  // 推断返回类型
}

// API 调用
async function fetchUser(id: number): Promise<User | null> {
    const response = await fetch(`/api/users/${id}`);
    if (response.ok) {
        return response.json() as User;  // 合理使用断言
    }
    return null;
}
```

**🛠️ 工具配置建议**
```json
// tsconfig.json 推荐配置
{
  "compilerOptions": {
    "strict": true,              // 开启严格模式
    "noImplicitAny": true,       // 禁止隐式 any
    "strictNullChecks": true     // 严格空值检查
  }
}
```

**🚫 常见错误避免**
```typescript
// ❌ 避免的写法
let data: any = getData();           // 失去类型安全
let name = data.user.name;          // 可能运行时报错

// ✅ 推荐的写法
let data: unknown = getData();       // 保持类型安全
if (isValidUser(data)) {            // 类型守卫
    let name = data.user.name;       // 安全访问
}
```

### 8.4 学习进阶方向


**📈 下一步学习重点**
- **复合类型**：数组、对象、函数类型的组合使用
- **接口定义**：interface 关键字定义复杂对象结构
- **泛型基础**：\<T> 参数化类型，提高代码复用性
- **类型工具**：Pick、Omit 等内置工具类型的使用

**🎯 实践建议**
- 在现有 JavaScript 项目中逐步添加类型注解
- 多看 TypeScript 官方文档和错误提示
- 参与开源项目，学习优秀的类型定义
- 配置 IDE 的 TypeScript 支持，提高开发效率

**核心记忆口诀**：
```
原始三兄弟：string number boolean
空值要小心：null undefined 需检查
any 虽万能：unknown 更安全
void 无返回：never 不返回
注解要明确：推断可依赖
断言需谨慎：as 不转换只告知
```