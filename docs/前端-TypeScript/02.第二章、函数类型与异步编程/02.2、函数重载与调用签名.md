---
title: 2、函数重载与调用签名
---
## 📚 目录

1. [函数重载基础概念](#1-函数重载基础概念)
2. [重载声明与实现](#2-重载声明与实现)
3. [调用签名详解](#3-调用签名详解)
4. [重载解析机制](#4-重载解析机制)
5. [函数表达式重载](#5-函数表达式重载)
6. [实际应用场景](#6-实际应用场景)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 函数重载基础概念


### 1.1 什么是函数重载


**简单理解**：函数重载就是让同一个函数名能够接受不同类型或数量的参数，并且根据传入的参数返回不同类型的结果。

```
现实生活类比：
打印机的"打印"功能
- print(文档) → 打印文档
- print(图片) → 打印图片  
- print(网页) → 打印网页

同一个"打印"动作，但处理不同类型的内容
```

### 1.2 为什么需要函数重载


**解决的问题**：一个函数可能需要处理多种不同的输入情况

```typescript
// ❌ 没有重载时的问题
function process(input: any): any {
    if (typeof input === 'string') {
        return input.toUpperCase();
    }
    if (typeof input === 'number') {
        return input * 2;
    }
    // 类型信息丢失，返回值类型不明确
}

// ✅ 使用重载后的清晰定义
function process(input: string): string;
function process(input: number): number;
function process(input: string | number): string | number {
    if (typeof input === 'string') {
        return input.toUpperCase();
    }
    return input * 2;
}
```

### 1.3 重载的核心价值


**🔸 类型安全**：确保输入输出类型的一致性
**🔸 代码提示**：IDE 能够提供准确的类型提示
**🔸 可读性**：函数的使用方式一目了然

---

## 2. ⚙️ 重载声明与实现


### 2.1 重载的基本语法


**语法结构**：
```
重载签名1  ← 告诉 TypeScript 这个函数可以这样调用
重载签名2  ← 告诉 TypeScript 这个函数也可以这样调用
...
实现签名   ← 真正的函数实现代码
```

### 2.2 简单重载示例


```typescript
// 重载签名：声明函数的不同调用方式
function greet(name: string): string;           // 方式1：传入姓名
function greet(age: number): string;            // 方式2：传入年龄
function greet(name: string, age: number): string; // 方式3：传入姓名和年龄

// 实现签名：具体的函数实现
function greet(nameOrAge: string | number, age?: number): string {
    if (typeof nameOrAge === 'string') {
        if (age !== undefined) {
            return `Hello ${nameOrAge}, you are ${age} years old`;
        }
        return `Hello ${nameOrAge}`;
    }
    return `Hello, you are ${nameOrAge} years old`;
}

// 使用时的效果
greet("张三");           // ✅ 匹配第1个重载
greet(25);              // ✅ 匹配第2个重载  
greet("李四", 30);      // ✅ 匹配第3个重载
```

### 2.3 重载的注意事项


**📋 重要规则**：

```typescript
// ✅ 正确：重载签名必须兼容实现签名
function add(a: number, b: number): number;
function add(a: string, b: string): string;
function add(a: number | string, b: number | string): number | string {
    // 实现必须能处理所有重载情况
    if (typeof a === 'number' && typeof b === 'number') {
        return a + b;
    }
    return a.toString() + b.toString();
}

// ❌ 错误：实现签名对外不可见
// add(1, "hello"); // 类型错误！实现签名不能直接调用
```

**🔸 核心理解**：
- **重载签名**：对外承诺的调用方式（类型检查用）
- **实现签名**：内部实际的代码逻辑（不对外暴露）

---

## 3. 📝 调用签名详解


### 3.1 什么是调用签名


**通俗解释**：调用签名就是描述一个函数如何被调用的"说明书"，包括参数类型、返回值类型等。

```typescript
// 调用签名的不同写法对比

// 方式1：直接函数声明（最常用）
function calculate(x: number, y: number): number;

// 方式2：类型别名中的调用签名
type CalculateFunc = {
    (x: number, y: number): number;  // 这就是调用签名
    description: string;             // 还可以有其他属性
};

// 方式3：接口中的调用签名
interface MathOperation {
    (x: number, y: number): number;  // 调用签名
    name: string;                    // 其他属性
}
```

### 3.2 调用签名的实际应用


```typescript
// 定义一个既是函数又有属性的对象
interface Logger {
    (message: string): void;         // 调用签名：可以像函数一样调用
    level: 'info' | 'warn' | 'error'; // 属性：日志级别
    enable: boolean;                 // 属性：是否启用
}

// 实现这个接口
const logger: Logger = Object.assign(
    function(message: string) {
        if (logger.enable) {
            console.log(`[${logger.level}] ${message}`);
        }
    },
    {
        level: 'info' as const,
        enable: true
    }
);

// 使用方式
logger("这是一条日志");              // ✅ 像函数一样调用
logger.level = 'warn';             // ✅ 修改属性
logger.enable = false;             // ✅ 修改属性
```

### 3.3 多重调用签名


```typescript
interface Converter {
    (input: string): number;        // 字符串转数字
    (input: number): string;        // 数字转字符串
    (input: boolean): string;       // 布尔值转字符串
}

const convert: Converter = function(input: any): any {
    if (typeof input === 'string') {
        return parseInt(input);
    }
    if (typeof input === 'number') {
        return input.toString();
    }
    return input ? 'true' : 'false';
} as Converter;

// 使用时会有精确的类型提示
const num = convert("123");        // num 的类型是 number
const str = convert(456);          // str 的类型是 string
const bool = convert(true);        // bool 的类型是 string
```

---

## 4. 🔍 重载解析机制


### 4.1 TypeScript 如何选择重载


**解析顺序**：TypeScript 从上到下逐个匹配重载签名，找到第一个匹配的就停止。

```typescript
function format(value: string): string;
function format(value: number): string;
function format(value: Date): string;
function format(value: any): string {
    if (value instanceof Date) {
        return value.toISOString();
    }
    return String(value);
}

// 匹配过程示例
format("hello");    // 匹配第1个：string → string
format(123);        // 匹配第2个：number → string  
format(new Date()); // 匹配第3个：Date → string
```

### 4.2 重载顺序的重要性


```typescript
// ❌ 错误的顺序：具体类型放在通用类型后面
function process(value: any): any;           // 太宽泛，会匹配所有情况
function process(value: string): string;     // 永远不会被匹配到
function process(value: number): number;     // 永远不会被匹配到

// ✅ 正确的顺序：从具体到通用
function process(value: string): string;     // 具体类型优先
function process(value: number): number;     // 具体类型优先
function process(value: any): any;           // 通用类型兜底
```

### 4.3 重载解析的最佳实践


**🎯 排序原则**：
```
1. 参数更具体的重载放前面
2. 参数更少的重载放前面（当参数类型相同时）
3. 通用的兜底重载放最后
```

```typescript
// 最佳实践示例
function request(url: string): Promise<string>;                    // 最具体
function request(url: string, method: 'GET'): Promise<string>;     // 较具体
function request(url: string, method: 'POST', data: any): Promise<string>; // 最完整
function request(url: string, method?: string, data?: any): Promise<string> {
    // 实现逻辑
    return fetch(url, { method: method || 'GET', body: data }).then(r => r.text());
}
```

---

## 5. 💡 函数表达式重载


### 5.1 函数表达式重载语法


**两种主要方式**：

```typescript
// 方式1：先定义类型，再赋值
type MathFunc = {
    (a: number, b: number): number;          // 两个数字相加
    (a: string, b: string): string;          // 两个字符串拼接
};

const mathOp: MathFunc = function(a: any, b: any): any {
    if (typeof a === 'number') {
        return a + b;
    }
    return a + b;
};

// 方式2：接口定义
interface Calculator {
    (x: number): number;                     // 一个参数：平方
    (x: number, y: number): number;          // 两个参数：相加
}

const calc: Calculator = function(x: number, y?: number): number {
    if (y === undefined) {
        return x * x;
    }
    return x + y;
};
```

### 5.2 箭头函数的重载


**注意**：箭头函数不能直接重载，需要通过类型定义：

```typescript
// ❌ 箭头函数不支持直接重载
// const add = (a: number, b: number): number => a + b;
// const add = (a: string, b: string): string => a + b;  // 错误！

// ✅ 正确方式：先定义类型
type AddFunc = {
    (a: number, b: number): number;
    (a: string, b: string): string;
};

const add: AddFunc = (a: any, b: any): any => {
    return a + b;
};

// 使用时有完整的类型支持
const numResult = add(1, 2);        // number 类型
const strResult = add("a", "b");    // string 类型
```

### 5.3 对象方法的重载


```typescript
class DataProcessor {
    // 方法重载
    process(data: string): string;
    process(data: number[]): number;
    process(data: object): object;
    process(data: any): any {
        if (typeof data === 'string') {
            return data.toUpperCase();
        }
        if (Array.isArray(data)) {
            return data.reduce((sum, num) => sum + num, 0);
        }
        return { ...data, processed: true };
    }
}

const processor = new DataProcessor();
const result1 = processor.process("hello");      // string 类型
const result2 = processor.process([1, 2, 3]);    // number 类型
const result3 = processor.process({name: "test"}); // object 类型
```

---

## 6. 🚀 实际应用场景


### 6.1 API 封装中的重载


```typescript
// HTTP 请求库的重载设计
interface ApiClient {
    get(url: string): Promise<any>;
    get<T>(url: string): Promise<T>;
    get<T>(url: string, config: RequestConfig): Promise<T>;
}

class HttpClient implements ApiClient {
    get<T = any>(url: string, config?: RequestConfig): Promise<T> {
        return fetch(url, config).then(res => res.json());
    }
}

// 使用时的便利性
const client = new HttpClient();
const users = await client.get<User[]>('/users');     // 有类型提示
const data = await client.get('/data');               // 通用类型
```

### 6.2 工具函数的重载


```typescript
// 深度克隆函数的重载
function clone<T extends object>(obj: T): T;
function clone<T>(obj: T): T;
function clone(obj: any): any {
    if (obj === null || typeof obj !== 'object') {
        return obj;
    }
    
    if (obj instanceof Date) {
        return new Date(obj);
    }
    
    if (Array.isArray(obj)) {
        return obj.map(item => clone(item));
    }
    
    const cloned: any = {};
    for (const key in obj) {
        cloned[key] = clone(obj[key]);
    }
    return cloned;
}

// 使用时保持类型安全
const originalArray = [1, 2, 3];
const clonedArray = clone(originalArray);    // number[] 类型

const originalObj = { name: "test", age: 20 };
const clonedObj = clone(originalObj);        // { name: string, age: number } 类型
```

### 6.3 事件处理的重载


```typescript
// 事件监听器的重载设计
interface EventEmitter {
    on(event: 'data', callback: (data: string) => void): void;
    on(event: 'error', callback: (error: Error) => void): void;
    on(event: 'close', callback: () => void): void;
    on(event: string, callback: Function): void;  // 兜底签名
}

class MyEmitter implements EventEmitter {
    on(event: string, callback: Function): void {
        // 实现事件监听逻辑
        console.log(`监听事件: ${event}`);
    }
}

const emitter = new MyEmitter();
// TypeScript 会根据事件名推断回调函数的类型
emitter.on('data', (data) => {        // data 自动推断为 string
    console.log(data.toUpperCase());
});

emitter.on('error', (error) => {      // error 自动推断为 Error
    console.log(error.message);
});
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 函数重载：同一函数名支持多种调用方式的类型定义机制
🔸 重载签名：对外声明的函数调用接口（类型检查用）
🔸 实现签名：内部实际的函数实现（必须兼容所有重载签名）
🔸 调用签名：描述函数如何被调用的类型定义
🔸 重载解析：TypeScript 从上到下匹配重载的机制
```

### 7.2 关键理解要点


**🔹 重载的本质作用**
```
类型安全：确保输入输出类型的精确匹配
代码提示：IDE 提供准确的参数和返回值提示
可读性：函数的多种用法一目了然
向后兼容：支持函数功能的渐进式扩展
```

**🔹 重载与联合类型的区别**
```
联合类型：function process(input: string | number): string | number
- 输入可以是 string 或 number
- 输出也是 string 或 number（但不知道具体对应关系）

函数重载：
function process(input: string): string;
function process(input: number): number;
- 输入 string 时，输出必定是 string
- 输入 number 时，输出必定是 number
- 类型关系更精确
```

**🔹 重载顺序的重要性**
```
匹配原则：从上到下，找到第一个匹配就停止
排序策略：具体类型在前，通用类型在后
避免陷阱：过于宽泛的重载会"吞掉"后面的重载
```

### 7.3 实际应用价值


**📊 适用场景判断**
```
✅ 适合使用重载：
- 同一功能处理不同类型参数
- 参数数量不同但功能相关
- 需要精确的类型推断
- API 设计需要向后兼容

❌ 不适合使用重载：
- 功能完全不同的函数
- 参数类型过于复杂
- 重载数量过多（>5个）
- 简单的联合类型已足够
```

**🔧 最佳实践总结**
```
设计原则：
- 重载签名从具体到通用排序
- 实现签名必须兼容所有重载
- 避免过度重载，保持简洁

代码质量：
- 重载数量控制在合理范围（2-5个）
- 每个重载都应该有明确的使用场景
- 实现逻辑要处理所有重载情况

团队协作：
- 重载的文档说明要清晰
- 重载变更要考虑向后兼容性
- 统一团队的重载使用规范
```

### 7.4 常见问题解决


**🔹 重载不生效**
```
检查点：
1. 重载签名顺序是否正确
2. 实现签名是否兼容所有重载
3. 是否误用了实现签名的类型
4. 函数表达式是否正确定义了类型
```

**🔹 类型推断不准确**
```
解决方案：
1. 调整重载签名的顺序
2. 使用更具体的类型约束
3. 考虑使用泛型替代部分重载
4. 检查参数的类型守卫逻辑
```

**核心记忆**：
- 重载让一个函数支持多种调用方式
- 重载签名对外，实现签名对内
- 解析顺序从上到下，具体到通用
- 类型安全和代码提示是核心价值