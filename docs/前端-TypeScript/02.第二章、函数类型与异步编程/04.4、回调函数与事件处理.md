---
title: 4、回调函数与事件处理
---
## 📚 目录

1. [回调函数类型基础](#1-回调函数类型基础)
2. [DOM事件处理类型](#2-DOM事件处理类型)
3. [高阶函数类型定义](#3-高阶函数类型定义)
4. [React事件类型处理](#4-React事件类型处理)
5. [实际应用与最佳实践](#5-实际应用与最佳实践)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔄 回调函数类型基础


### 1.1 什么是回调函数类型


**💡 通俗理解**
回调函数就是"你先干活，干完了叫我一声"的函数。在TypeScript中，我们需要明确告诉编译器这个"叫我一声"的函数长什么样。

```typescript
// 最简单的回调函数类型
function processData(callback: () => void) {
    // 处理一些数据...
    console.log("数据处理完成");
    callback(); // 叫你一声：处理完了！
}

// 使用回调
processData(() => {
    console.log("收到通知，可以继续后续操作了");
});
```

### 1.2 带参数的回调函数


**📝 实际场景示例**
```typescript
// 文件读取完成后的回调
type FileCallback = (error: string | null, data?: string) => void;

function readFile(filename: string, callback: FileCallback) {
    // 模拟文件读取
    setTimeout(() => {
        if (filename === "error.txt") {
            callback("文件不存在"); // 出错了，只传错误信息
        } else {
            callback(null, "文件内容数据"); // 成功了，传null和数据
        }
    }, 1000);
}

// 使用示例
readFile("test.txt", (error, data) => {
    if (error) {
        console.log("读取失败:", error);
    } else {
        console.log("读取成功:", data);
    }
});
```

### 1.3 回调函数的类型约束


**🎯 关键概念**
- **参数类型**：回调函数接收什么参数
- **返回值类型**：回调函数返回什么（通常是`void`）
- **可选参数**：某些参数可能不存在

```typescript
// 多种回调函数类型定义方式
interface CallbackTypes {
    // 无参无返回值
    simple: () => void;
    
    // 有参数无返回值
    withParams: (id: number, name: string) => void;
    
    // 有返回值的回调
    withReturn: (value: number) => boolean;
    
    // 可选参数的回调
    optional: (required: string, optional?: number) => void;
}

// 实际使用
function subscribe(callback: (data: string) => void) {
    // 模拟数据推送
    setInterval(() => {
        callback("新数据到达");
    }, 2000);
}
```

---

## 2. 🖱️ DOM事件处理类型


### 2.1 基础DOM事件类型


**⚡ 常用事件类型**
```typescript
// 按钮点击事件
const button = document.querySelector('button')!;

button.addEventListener('click', (event: MouseEvent) => {
    console.log('按钮被点击了');
    console.log('点击位置:', event.clientX, event.clientY);
});

// 输入框变化事件
const input = document.querySelector('input')!;

input.addEventListener('input', (event: Event) => {
    const target = event.target as HTMLInputElement;
    console.log('输入内容:', target.value);
});
```

### 2.2 不同类型的DOM事件


**📋 事件类型对照表**

| 事件类型 | TypeScript类型 | 常用场景 | 示例 |
|---------|---------------|---------|------|
| `click` | `MouseEvent` | 按钮点击、链接点击 | `button.onclick = (e: MouseEvent) => {}` |
| `input` | `Event` | 输入框内容变化 | `input.oninput = (e: Event) => {}` |
| `keydown` | `KeyboardEvent` | 键盘按键 | `document.onkeydown = (e: KeyboardEvent) => {}` |
| `submit` | `Event` | 表单提交 | `form.onsubmit = (e: Event) => {}` |

**🔧 实用示例**
```typescript
// 键盘事件处理
document.addEventListener('keydown', (event: KeyboardEvent) => {
    if (event.key === 'Enter') {
        console.log('用户按了回车键');
    }
    if (event.ctrlKey && event.key === 's') {
        event.preventDefault(); // 阻止默认保存行为
        console.log('用户按了Ctrl+S');
    }
});

// 表单提交事件
const form = document.querySelector('form')!;
form.addEventListener('submit', (event: Event) => {
    event.preventDefault(); // 阻止默认提交
    
    const formData = new FormData(event.target as HTMLFormElement);
    console.log('表单数据:', Object.fromEntries(formData));
});
```

### 2.3 事件对象的类型断言


**🎯 为什么需要类型断言**
```typescript
// 问题：event.target 的类型是 EventTarget | null
function handleInput(event: Event) {
    // ❌ 错误：TypeScript不知道target是input元素
    // console.log(event.target.value); // 报错！
    
    // ✅ 正确：告诉TypeScript这是一个input元素
    const target = event.target as HTMLInputElement;
    console.log(target.value);
}

// 更安全的写法
function handleInputSafe(event: Event) {
    const target = event.target;
    if (target instanceof HTMLInputElement) {
        console.log(target.value); // 类型安全
    }
}
```

---

## 3. 🔧 高阶函数类型定义


### 3.1 什么是高阶函数


**💡 简单理解**
高阶函数就是"处理函数的函数"，要么接收函数作为参数，要么返回一个函数。

```typescript
// 接收函数作为参数的高阶函数
function repeat(times: number, action: () => void) {
    for (let i = 0; i < times; i++) {
        action();
    }
}

// 使用示例
repeat(3, () => {
    console.log("Hello World!");
});
// 输出：Hello World! 被打印3次
```

### 3.2 常见的高阶函数模式


**🔄 数组处理函数**
```typescript
// map函数的类型定义（简化版）
function myMap<T, U>(
    array: T[], 
    callback: (item: T, index: number) => U
): U[] {
    const result: U[] = [];
    for (let i = 0; i < array.length; i++) {
        result.push(callback(array[i], i));
    }
    return result;
}

// 使用示例
const numbers = [1, 2, 3, 4];
const doubled = myMap(numbers, (num) => num * 2);
console.log(doubled); // [2, 4, 6, 8]

const names = myMap(numbers, (num) => `数字${num}`);
console.log(names); // ["数字1", "数字2", "数字3", "数字4"]
```

### 3.3 返回函数的高阶函数


**🏭 函数工厂模式**
```typescript
// 创建验证函数的工厂
function createValidator(minLength: number) {
    return (value: string): boolean => {
        return value.length >= minLength;
    };
}

// 使用示例
const validatePassword = createValidator(8);
const validateUsername = createValidator(3);

console.log(validatePassword("123")); // false
console.log(validatePassword("12345678")); // true
console.log(validateUsername("tom")); // true
```

### 3.4 复杂高阶函数类型


**⚙️ 实用工具函数**
```typescript
// 防抖函数的类型定义
function debounce<T extends (...args: any[]) => any>(
    func: T,
    delay: number
): (...args: Parameters<T>) => void {
    let timeoutId: NodeJS.Timeout;
    
    return (...args: Parameters<T>) => {
        clearTimeout(timeoutId);
        timeoutId = setTimeout(() => func(...args), delay);
    };
}

// 使用示例
const handleSearch = debounce((query: string) => {
    console.log("搜索:", query);
}, 300);

// 用户快速输入时，只有最后一次会真正执行搜索
handleSearch("a");
handleSearch("ab");
handleSearch("abc"); // 只有这个会在300ms后执行
```

---

## 4. ⚛️ React事件类型处理


### 4.1 React事件系统基础


**🎯 React合成事件**
React有自己的事件系统，叫"合成事件"(SyntheticEvent)，它包装了原生DOM事件。

```typescript
import React from 'react';

function MyComponent() {
    // React按钮点击事件
    const handleClick = (event: React.MouseEvent<HTMLButtonElement>) => {
        console.log('按钮被点击');
        console.log('点击位置:', event.clientX, event.clientY);
    };
    
    // React输入框事件
    const handleChange = (event: React.ChangeEvent<HTMLInputElement>) => {
        console.log('输入内容:', event.target.value);
    };
    
    return (
        <div>
            <button onClick={handleClick}>点击我</button>
            <input onChange={handleChange} placeholder="输入内容" />
        </div>
    );
}
```

### 4.2 常用React事件类型


**📋 React事件类型速查**

```typescript
// 鼠标事件
const handleMouseEvent = (e: React.MouseEvent<HTMLDivElement>) => {
    console.log('鼠标事件:', e.type);
};

// 键盘事件
const handleKeyEvent = (e: React.KeyboardEvent<HTMLInputElement>) => {
    if (e.key === 'Enter') {
        console.log('按下回车');
    }
};

// 表单事件
const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    console.log('表单提交');
};

// 焦点事件
const handleFocus = (e: React.FocusEvent<HTMLInputElement>) => {
    console.log('输入框获得焦点');
};
```

### 4.3 React事件处理最佳实践


**✅ 推荐写法**
```typescript
interface LoginFormProps {
    onLogin: (username: string, password: string) => void;
}

function LoginForm({ onLogin }: LoginFormProps) {
    const [username, setUsername] = useState('');
    const [password, setPassword] = useState('');
    
    // 处理表单提交
    const handleSubmit = (e: React.FormEvent) => {
        e.preventDefault();
        onLogin(username, password);
    };
    
    // 处理输入变化的通用函数
    const handleInputChange = (
        setter: (value: string) => void
    ) => (e: React.ChangeEvent<HTMLInputElement>) => {
        setter(e.target.value);
    };
    
    return (
        <form onSubmit={handleSubmit}>
            <input 
                type="text"
                placeholder="用户名"
                value={username}
                onChange={handleInputChange(setUsername)}
            />
            <input 
                type="password" 
                placeholder="密码"
                value={password}
                onChange={handleInputChange(setPassword)}
            />
            <button type="submit">登录</button>
        </form>
    );
}
```

### 4.4 自定义事件处理Hook


**🔧 实用自定义Hook**
```typescript
// 通用的输入框处理Hook
function useInput(initialValue: string = '') {
    const [value, setValue] = useState(initialValue);
    
    const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
        setValue(e.target.value);
    };
    
    const reset = () => setValue(initialValue);
    
    return {
        value,
        onChange: handleChange,
        reset
    };
}

// 使用示例
function ContactForm() {
    const nameInput = useInput('');
    const emailInput = useInput('');
    
    const handleSubmit = (e: React.FormEvent) => {
        e.preventDefault();
        console.log('姓名:', nameInput.value);
        console.log('邮箱:', emailInput.value);
        
        // 重置表单
        nameInput.reset();
        emailInput.reset();
    };
    
    return (
        <form onSubmit={handleSubmit}>
            <input {...nameInput} placeholder="姓名" />
            <input {...emailInput} placeholder="邮箱" />
            <button type="submit">提交</button>
        </form>
    );
}
```

---

## 5. 💼 实际应用与最佳实践


### 5.1 API请求的回调处理


**🌐 实际场景应用**
```typescript
// 定义API响应类型
interface User {
    id: number;
    name: string;
    email: string;
}

// 定义错误类型
interface ApiError {
    message: string;
    code: number;
}

// API回调函数类型
type ApiCallback<T> = (error: ApiError | null, data: T | null) => void;

// 通用API请求函数
function fetchUser(userId: number, callback: ApiCallback<User>) {
    fetch(`/api/users/${userId}`)
        .then(response => {
            if (!response.ok) {
                callback({ message: '用户不存在', code: 404 }, null);
                return;
            }
            return response.json();
        })
        .then(user => {
            callback(null, user);
        })
        .catch(error => {
            callback({ message: '网络错误', code: 500 }, null);
        });
}

// 使用示例
fetchUser(123, (error, user) => {
    if (error) {
        console.error('获取用户失败:', error.message);
        return;
    }
    
    console.log('用户信息:', user!.name);
});
```

### 5.2 事件总线模式


**📡 发布订阅模式**
```typescript
// 事件监听器类型
type EventListener<T = any> = (data: T) => void;

// 简单的事件总线
class EventBus {
    private listeners: { [key: string]: EventListener[] } = {};
    
    // 订阅事件
    on<T>(event: string, listener: EventListener<T>) {
        if (!this.listeners[event]) {
            this.listeners[event] = [];
        }
        this.listeners[event].push(listener);
    }
    
    // 发布事件
    emit<T>(event: string, data: T) {
        const eventListeners = this.listeners[event];
        if (eventListeners) {
            eventListeners.forEach(listener => listener(data));
        }
    }
    
    // 取消订阅
    off<T>(event: string, listenerToRemove: EventListener<T>) {
        const eventListeners = this.listeners[event];
        if (eventListeners) {
            this.listeners[event] = eventListeners.filter(
                listener => listener !== listenerToRemove
            );
        }
    }
}

// 使用示例
const eventBus = new EventBus();

// 订阅用户登录事件
eventBus.on('userLogin', (user: User) => {
    console.log('用户登录:', user.name);
});

// 订阅购物车更新事件
eventBus.on('cartUpdate', (count: number) => {
    console.log('购物车商品数量:', count);
});

// 发布事件
eventBus.emit('userLogin', { id: 1, name: '张三', email: 'zhang@example.com' });
eventBus.emit('cartUpdate', 5);
```

### 5.3 常见陷阱与解决方案


**⚠️ 常见问题**

```typescript
// ❌ 错误：回调函数中的this指向问题
class Counter {
    count = 0;
    
    // 错误的写法
    setupBadButton() {
        const button = document.querySelector('#btn')!;
        button.addEventListener('click', function() {
            this.count++; // 这里的this不是Counter实例！
            console.log(this.count); // undefined
        });
    }
    
    // ✅ 正确的写法
    setupGoodButton() {
        const button = document.querySelector('#btn')!;
        button.addEventListener('click', () => {
            this.count++; // 箭头函数保持this指向
            console.log(this.count); // 正确输出
        });
    }
    
    // ✅ 另一种正确写法
    setupAnotherButton() {
        const button = document.querySelector('#btn')!;
        button.addEventListener('click', this.handleClick.bind(this));
    }
    
    private handleClick() {
        this.count++;
        console.log(this.count);
    }
}
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 回调函数类型：明确参数类型和返回值类型
🔸 DOM事件类型：MouseEvent、KeyboardEvent、Event等
🔸 高阶函数：接收或返回函数的函数类型定义
🔸 React事件：React.MouseEvent、React.ChangeEvent等合成事件
🔸 类型断言：event.target as HTMLInputElement
```

### 6.2 关键理解要点


**🔹 回调函数的本质**
```
回调函数 = 延迟执行的函数 + 明确的类型约束
- 参数类型要准确：知道会传入什么
- 返回值类型要明确：通常是void
- 错误处理要考虑：成功和失败的情况
```

**🔹 事件处理的核心**
```
事件处理 = 事件对象 + 类型安全的访问
- 选择正确的事件类型：MouseEvent vs Event
- 安全的类型断言：event.target as HTMLInputElement
- 阻止默认行为：preventDefault()的使用时机
```

**🔹 高阶函数的价值**
```
高阶函数 = 代码复用 + 类型安全
- 泛型让函数更通用：<T, U>的使用
- 参数类型的传递：Parameters<T>工具类型
- 返回值类型的保持：ReturnType<T>工具类型
```

### 6.3 实际应用指南


**✅ 最佳实践**
- **简单优先**：先写出能工作的代码，再优化类型
- **类型明确**：回调函数的类型要写清楚
- **错误处理**：考虑回调中的错误情况
- **性能优化**：使用防抖、节流等技巧

**❌ 常见错误**
- 忘记绑定this指向
- 事件对象类型断言不安全
- 回调函数类型定义不完整
- 忽略错误处理逻辑

**🎯 记忆口诀**
```
回调类型要写清，参数返回不能省
事件对象要断言，类型安全最重要  
高阶函数用泛型，代码复用更灵活
React事件有前缀，合成事件要记住
```

### 6.4 学习路径建议


1. **先理解概念**：什么是回调函数、事件处理
2. **练习基础语法**：简单的回调函数类型定义
3. **掌握DOM事件**：常用事件类型和处理方式
4. **学习React事件**：合成事件的使用
5. **实践项目**：在实际项目中应用这些知识

**核心记忆**：
- 回调函数是"干完活叫我一声"的函数
- 事件处理要选对类型、安全断言
- 高阶函数让代码更优雅、更复用
- React事件有自己的类型系统