---
title: 1、泛型基础语法
---
## 📚 目录

1. [泛型是什么？为什么需要泛型？](#1-泛型是什么为什么需要泛型)
2. [泛型函数 - 让函数更灵活](#2-泛型函数-让函数更灵活)
3. [泛型接口 - 可复用的类型结构](#3-泛型接口-可复用的类型结构)
4. [泛型类 - 类型参数化的类](#4-泛型类-类型参数化的类)
5. [泛型约束 - 给泛型加点限制](#5-泛型约束-给泛型加点限制)
6. [多泛型参数 - 处理多种类型](#6-多泛型参数-处理多种类型)
7. [默认泛型参数 - 让泛型更友好](#7-默认泛型参数-让泛型更友好)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🤔 泛型是什么？为什么需要泛型？


### 1.1 先看一个问题


想象你要写一个简单的函数，返回传入的值：

```typescript
// 只能处理 string
function returnString(value: string): string {
  return value;
}

// 只能处理 number  
function returnNumber(value: number): number {
  return value;
}

// 只能处理 boolean
function returnBoolean(value: boolean): boolean {
  return value;
}
```

**问题**：每种类型都要写一个函数，太麻烦了！

### 1.2 用 any 试试？


```typescript
function returnAny(value: any): any {
  return value;
}

const result = returnAny("hello");
// 问题：result 的类型是 any，失去了类型安全
// result.toUpperCase(); // 编译器不知道 result 是 string
```

**问题**：用 `any` 虽然灵活，但丢失了类型信息！

### 1.3 泛型来救场！


```typescript
// 泛型函数：<T> 表示这是一个类型参数
function returnValue<T>(value: T): T {
  return value;
}

// 使用时，TypeScript 会自动推断类型
const stringResult = returnValue("hello");    // stringResult: string
const numberResult = returnValue(42);         // numberResult: number
const boolResult = returnValue(true);         // boolResult: boolean
```

**🎯 泛型的核心思想**：
- **T** 就像一个"类型变量"，可以代表任何类型
- 使用时，TypeScript 会把 **T** 替换成具体的类型
- 既保持了灵活性，又保持了类型安全

```
简单理解：
泛型 = 类型的参数化 = 让类型也能像变量一样传递
```

---

## 2. 🔧 泛型函数 - 让函数更灵活


### 2.1 基础泛型函数语法


```typescript
// 语法：function functionName<T>(参数: T): T
function identity<T>(arg: T): T {
  return arg;
}

// 两种调用方式
const result1 = identity<string>("hello");   // 明确指定类型
const result2 = identity("hello");           // 让 TS 自动推断（推荐）
```

### 2.2 实用的泛型函数例子


**📦 获取数组第一个元素**
```typescript
function getFirst<T>(array: T[]): T | undefined {
  return array[0];
}

const numbers = [1, 2, 3];
const first = getFirst(numbers);  // first: number | undefined

const names = ["Alice", "Bob"];
const firstName = getFirst(names); // firstName: string | undefined
```

**🔄 交换数组中两个元素**
```typescript
function swap<T>(array: T[], i: number, j: number): T[] {
  const newArray = [...array];
  [newArray[i], newArray[j]] = [newArray[j], newArray[i]];
  return newArray;
}

const nums = [1, 2, 3];
const swapped = swap(nums, 0, 2); // [3, 2, 1]
```

**🎯 关键理解**：
- `<T>` 在函数名后面，告诉 TypeScript 这是一个泛型函数
- `T` 可以是任何名字，但习惯上用 `T`（Type 的缩写）
- 函数内部可以把 `T` 当作一个真实的类型来使用

---

## 3. 🏗️ 泛型接口 - 可复用的类型结构


### 3.1 为什么需要泛型接口？


想象你要定义 API 响应的格式：

```typescript
// 不用泛型：每种数据都要定义一个接口
interface UserResponse {
  code: number;
  message: string;
  data: User;
}

interface ProductResponse {
  code: number;
  message: string;
  data: Product;
}
```

**问题**：重复代码太多！

### 3.2 用泛型接口解决


```typescript
// 泛型接口：<T> 让接口可以接受不同的数据类型
interface ApiResponse<T> {
  code: number;
  message: string;
  data: T;
}

// 使用时指定具体类型
interface User {
  id: number;
  name: string;
}

interface Product {
  id: number;
  title: string;
  price: number;
}

// 一个接口，多种用法
const userResponse: ApiResponse<User> = {
  code: 200,
  message: "success",
  data: { id: 1, name: "Alice" }
};

const productResponse: ApiResponse<Product> = {
  code: 200,
  message: "success", 
  data: { id: 1, title: "iPhone", price: 999 }
};
```

### 3.3 实用的泛型接口模式


**📋 分页数据结构**
```typescript
interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  pageSize: number;
}

// 用户分页
const userPage: PaginatedResponse<User> = {
  items: [{ id: 1, name: "Alice" }],
  total: 100,
  page: 1,
  pageSize: 10
};
```

**🔑 键值对映射**
```typescript
interface KeyValuePair<K, V> {
  key: K;
  value: V;
}

const stringToNumber: KeyValuePair<string, number> = {
  key: "age",
  value: 25
};

const numberToBoolean: KeyValuePair<number, boolean> = {
  key: 1,
  value: true
};
```

---

## 4. 🏛️ 泛型类 - 类型参数化的类


### 4.1 基础泛型类


```typescript
// 一个简单的容器类
class Container<T> {
  private item: T;

  constructor(item: T) {
    this.item = item;
  }

  getItem(): T {
    return this.item;
  }

  setItem(item: T): void {
    this.item = item;
  }
}

// 使用
const stringContainer = new Container<string>("hello");
const numberContainer = new Container<number>(42);

console.log(stringContainer.getItem()); // "hello"
console.log(numberContainer.getItem());  // 42
```

### 4.2 实用例子：栈（Stack）


```typescript
class Stack<T> {
  private items: T[] = [];

  push(item: T): void {
    this.items.push(item);
  }

  pop(): T | undefined {
    return this.items.pop();
  }

  peek(): T | undefined {
    return this.items[this.items.length - 1];
  }

  isEmpty(): boolean {
    return this.items.length === 0;
  }
}

// 字符串栈
const stringStack = new Stack<string>();
stringStack.push("first");
stringStack.push("second");
console.log(stringStack.pop()); // "second"

// 数字栈
const numberStack = new Stack<number>();
numberStack.push(1);
numberStack.push(2);
console.log(numberStack.peek()); // 2
```

**🎯 泛型类的好处**：
- 一个类定义，可以处理多种数据类型
- 保持类型安全，编译器会检查类型匹配
- 代码复用性高

---

## 5. 🚧 泛型约束 - 给泛型加点限制


### 5.1 为什么需要约束？


有时候我们希望泛型不是"任何类型"，而是"满足某些条件的类型"：

```typescript
function getLength<T>(arg: T): number {
  return arg.length; // ❌ 错误：T 不一定有 length 属性
}
```

### 5.2 用 extends 添加约束


```typescript
// 约束：T 必须有 length 属性
function getLength<T extends { length: number }>(arg: T): number {
  return arg.length; // ✅ 现在可以安全使用 length
}

// 这些都可以调用
getLength("hello");        // string 有 length
getLength([1, 2, 3]);      // array 有 length
getLength({ length: 5 });  // 对象有 length 属性

// getLength(123);         // ❌ number 没有 length 属性
```

### 5.3 常用的约束模式


**🔑 约束为对象类型**
```typescript
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}

const person = { name: "Alice", age: 25, city: "Beijing" };

const name = getProperty(person, "name");  // name: string
const age = getProperty(person, "age");    // age: number
// const invalid = getProperty(person, "salary"); // ❌ 属性不存在
```

**📝 约束为某个基类型**
```typescript
interface Printable {
  print(): void;
}

function processPrintable<T extends Printable>(item: T): void {
  item.print(); // ✅ 确保 T 有 print 方法
}

class Document implements Printable {
  print() {
    console.log("Printing document...");
  }
}

class Photo implements Printable {
  print() {
    console.log("Printing photo...");
  }
}

processPrintable(new Document()); // ✅ 
processPrintable(new Photo());    // ✅
```

**🎯 理解约束**：
```
extends 在泛型中的意思：
"T extends U" = "T 必须是 U 的子类型" = "T 必须包含 U 的所有特征"
```

---

## 6. 🎭 多泛型参数 - 处理多种类型


### 6.1 基础多泛型语法


```typescript
// 可以有多个泛型参数
function combine<T, U>(first: T, second: U): [T, U] {
  return [first, second];
}

const result = combine("hello", 42);     // result: [string, number]
const result2 = combine(true, "world");  // result2: [boolean, string]
```

### 6.2 实用例子：键值对处理


**🗂️ 创建映射**
```typescript
function createMap<K, V>(keys: K[], values: V[]): Map<K, V> {
  const map = new Map<K, V>();
  for (let i = 0; i < Math.min(keys.length, values.length); i++) {
    map.set(keys[i], values[i]);
  }
  return map;
}

const stringToNumberMap = createMap(
  ["one", "two", "three"], 
  [1, 2, 3]
);
// Map<string, number>
```

**🔄 对象属性转换**
```typescript
function mapObject<T, U>(
  obj: Record<string, T>, 
  transform: (value: T) => U
): Record<string, U> {
  const result: Record<string, U> = {};
  for (const key in obj) {
    result[key] = transform(obj[key]);
  }
  return result;
}

const numbers = { a: 1, b: 2, c: 3 };
const strings = mapObject(numbers, num => num.toString());
// { a: "1", b: "2", c: "3" }
```

### 6.3 多泛型约束


```typescript
// 多个泛型都可以有约束
function merge<T extends object, U extends object>(obj1: T, obj2: U): T & U {
  return { ...obj1, ...obj2 };
}

const person = { name: "Alice", age: 25 };
const job = { title: "Developer", salary: 8000 };

const employee = merge(person, job);
// employee: { name: string; age: number; title: string; salary: number; }
```

**🎯 多泛型的命名习惯**：
```
单个泛型：T (Type)
两个泛型：T, U 
三个泛型：T, U, V
键值对：K (Key), V (Value)
元素和结果：T (元素), R (Result)
```

---

## 7. 🎛️ 默认泛型参数 - 让泛型更友好


### 7.1 什么是默认泛型参数？


就像函数可以有默认参数一样，泛型也可以有默认值：

```typescript
// 给泛型设置默认值
interface ApiResponse<T = any> {
  code: number;
  message: string;
  data: T;
}

// 不指定类型时，T 默认为 any
const response1: ApiResponse = {
  code: 200,
  message: "success",
  data: "anything"  // data: any
};

// 指定类型时，使用指定的类型
const response2: ApiResponse<User> = {
  code: 200,
  message: "success",
  data: { id: 1, name: "Alice" }  // data: User
};
```

### 7.2 实用的默认参数模式


**📋 分页组件的默认配置**
```typescript
interface PaginationConfig<T = any> {
  data: T[];
  pageSize?: number;
  showQuickJumper?: boolean;
}

// 简单使用（不关心具体数据类型）
const config1: PaginationConfig = {
  data: [1, 2, 3, "hello", true], // 混合类型也可以
  pageSize: 10
};

// 严格类型控制
const config2: PaginationConfig<User> = {
  data: [{ id: 1, name: "Alice" }], // 必须是 User[]
  pageSize: 5
};
```

**🛠️ 工具函数的默认返回类型**
```typescript
function fetchData<T = unknown>(url: string): Promise<T> {
  return fetch(url).then(res => res.json());
}

// 不指定类型时，返回 unknown（安全）
const data1 = await fetchData("/api/data"); // data1: unknown

// 指定类型时，返回指定类型
const data2 = await fetchData<User[]>("/api/users"); // data2: User[]
```

### 7.3 多个默认参数


```typescript
// 可以给多个泛型设置默认值
class EventEmitter<T = any, U = string> {
  private events: Map<U, Array<(data: T) => void>> = new Map();

  on(event: U, callback: (data: T) => void): void {
    if (!this.events.has(event)) {
      this.events.set(event, []);
    }
    this.events.get(event)!.push(callback);
  }

  emit(event: U, data: T): void {
    const callbacks = this.events.get(event);
    if (callbacks) {
      callbacks.forEach(callback => callback(data));
    }
  }
}

// 使用默认类型
const emitter1 = new EventEmitter(); // T = any, U = string
emitter1.on("click", (data) => console.log(data)); // data: any

// 指定数据类型
const emitter2 = new EventEmitter<number>(); // T = number, U = string  
emitter2.on("count", (count) => console.log(count)); // count: number

// 指定所有类型
const emitter3 = new EventEmitter<User, "userEvent">(); // T = User, U = "userEvent"
emitter3.on("userEvent", (user) => console.log(user.name));
```

**🎯 默认泛型参数的好处**：
- 让泛型接口/类更容易使用
- 在不需要严格类型控制时提供便利
- 在需要类型安全时仍然可以指定具体类型

---

## 8. 📋 核心要点总结


### 8.1 泛型的本质理解


```
🎯 泛型是什么？
类型的参数化 = 让类型也能像变量一样传递和使用

🎯 为什么用泛型？
1. 代码复用：一套代码处理多种类型
2. 类型安全：保持编译时类型检查  
3. 灵活性：比 any 更安全，比具体类型更灵活

🎯 什么时候用泛型？
- 函数/类需要处理多种类型，但逻辑相同
- 想要保持类型安全，又不想重复写代码
- 构建可复用的组件或工具函数
```

### 8.2 语法速查表


| 用法 | 语法 | 说明 |
|------|------|------|
| **泛型函数** | `function fn<T>(param: T): T` | `函数名后加 <T>` |
| **泛型接口** | `interface API<T> { data: T }` | `接口名后加 <T>` |
| **泛型类** | `class Box<T> { item: T }` | `类名后加 <T>` |
| **泛型约束** | `<T extends string>` | `T 必须是 string 的子类型` |
| **多泛型** | `<T, U, V>` | `可以有多个类型参数` |
| **默认泛型** | `<T = string>` | `T 的默认类型是 string` |

### 8.3 实用记忆技巧


**🔤 泛型命名约定**
```typescript
T          // Type，最常用的泛型参数名
K, V       // Key, Value，用于键值对
U          // 第二个泛型参数
R          // Result，表示返回结果类型
E          // Element，表示数组/集合元素类型
P          // Props，React 组件的属性类型
```

**🚀 常见使用模式**
```typescript
// 1. 函数工具模式
function identity<T>(arg: T): T { return arg; }

// 2. 容器类模式  
class Container<T> { private item: T; }

// 3. API 响应模式
interface Response<T> { data: T; code: number; }

// 4. 属性获取模式
function get<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}

// 5. 数组处理模式
function filter<T>(arr: T[], predicate: (item: T) => boolean): T[] {
  return arr.filter(predicate);
}
```

### 8.4 新手常见问题


**❓ 什么时候用泛型，什么时候用联合类型？**
```typescript
// 联合类型：参数可能是几种类型中的一种
function process(value: string | number): void {
  // 需要类型检查才能使用特定方法
}

// 泛型：保持传入和返回的类型一致
function identity<T>(value: T): T {
  // 传入什么类型，返回什么类型
  return value;
}
```

**❓ 泛型约束什么时候用？**
```typescript
// 需要使用泛型的某些属性/方法时
function getLength<T extends { length: number }>(arg: T): number {
  return arg.length; // 没有约束就不能使用 length
}
```

**❓ 默认泛型参数的使用时机？**
```typescript
// 大多数情况下用一种类型，偶尔需要其他类型
interface Config<T = string> {
  value: T;
}

const config1: Config = { value: "hello" };        // 大多数情况
const config2: Config<number> = { value: 42 };     // 特殊情况
```

**🎯 核心记忆要点**：
- 泛型让代码更**灵活**，同时保持**类型安全**
- `<T>` 就是"类型变量"，使用时会被具体类型替换
- `extends` 在泛型中表示"必须满足某种条件"
- 能用类型推断的地方就不用手动指定类型
- 泛型是 TypeScript 最强大的特性之一，掌握它就掌握了类型系统的精髓