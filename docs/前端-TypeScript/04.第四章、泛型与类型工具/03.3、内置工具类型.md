---
title: 3、内置工具类型
---
## 📚 目录

1. [工具类型概述](#1-工具类型概述)
2. [属性变换工具](#2-属性变换工具)
3. [属性选择工具](#3-属性选择工具)
4. [键值映射工具](#4-键值映射工具)
5. [联合类型工具](#5-联合类型工具)
6. [函数类型工具](#6-函数类型工具)
7. [实战应用技巧](#7-实战应用技巧)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🛠️ 工具类型概述


### 1.1 什么是工具类型？


**💡 通俗理解**：
工具类型就像是 TypeScript 提供的"**类型加工厂**"，可以把现有的类型进行各种变换，生成新的类型。

```typescript
// 把这个想象成原材料
interface User {
  name: string;
  age: number;
  email: string;
}

// 工具类型就是加工机器，可以生产出各种变种
type OptionalUser = Partial<User>;     // 所有属性变可选
type NameOnly = Pick<User, 'name'>;    // 只要name属性
type NoEmail = Omit<User, 'email'>;    // 去掉email属性
```

### 1.2 为什么需要工具类型？


**🎯 解决的问题**：
- **避免重复定义**：不用重新写相似的接口
- **类型复用**：基于现有类型快速生成新类型
- **代码维护**：修改原类型，工具类型自动更新

```
现实场景：
用户注册 → 需要所有字段
用户更新 → 所有字段都可选
用户展示 → 只需要部分字段

有了工具类型，一个User接口就够了！
```

---

## 2. 🔧 属性变换工具


### 2.1 Partial\<T> - 全部变可选


**🔸 作用**：把类型的所有属性都变成可选的（加上 `?`）

```typescript
interface User {
  name: string;
  age: number;
  email: string;
}

// 原来是这样：所有属性都必填
const user1: User = {
  name: "张三",
  age: 25,
  email: "zhangsan@qq.com"
};

// 使用Partial后：所有属性都可选
type PartialUser = Partial<User>;
// 等价于：
// type PartialUser = {
//   name?: string;
//   age?: number;
//   email?: string;
// }

const user2: PartialUser = {
  name: "李四"  // 只填一个也行
};

const user3: PartialUser = {}; // 什么都不填也行
```

**💼 实际应用**：
```typescript
// 用户更新功能：不是所有字段都要更新
function updateUser(id: string, updates: Partial<User>) {
  // 只更新传入的字段
  return { ...getUser(id), ...updates };
}

updateUser("123", { age: 26 }); // 只更新年龄
```

### 2.2 Required\<T> - 全部变必填


**🔸 作用**：把类型的所有属性都变成必填的（去掉 `?`）

```typescript
interface Config {
  host?: string;
  port?: number;
  timeout?: number;
}

// 使用Required后：所有属性都必填
type RequiredConfig = Required<Config>;
// 等价于：
// type RequiredConfig = {
//   host: string;
//   port: number;
//   timeout: number;
// }

// 这样会报错，因为少了属性
const config: RequiredConfig = {
  host: "localhost"
  // ❌ 缺少 port 和 timeout
};
```

**💼 实际应用**：
```typescript
// 配置验证：确保所有配置项都有值
function validateConfig(config: Required<Config>) {
  // 这里可以安全使用所有属性，不用担心 undefined
  console.log(`连接到 ${config.host}:${config.port}`);
}
```

### 2.3 Readonly\<T> - 全部变只读


**🔸 作用**：把类型的所有属性都变成只读的（加上 `readonly`）

```typescript
interface Settings {
  theme: string;
  language: string;
}

type ReadonlySettings = Readonly<Settings>;
// 等价于：
// type ReadonlySettings = {
//   readonly theme: string;
//   readonly language: string;
// }

const settings: ReadonlySettings = {
  theme: "dark",
  language: "zh"
};

// settings.theme = "light"; // ❌ 错误！属性是只读的
```

**💼 实际应用**：
```typescript
// 常量配置：防止意外修改
const APP_CONFIG: Readonly<Settings> = {
  theme: "default",
  language: "en"
};

// 函数参数：保证不会被修改
function processSettings(settings: Readonly<Settings>) {
  // 只能读取，不能修改
  console.log(settings.theme);
}
```

---

## 3. ✂️ 属性选择工具


### 3.1 Pick\<T, K> - 选择指定属性


**🔸 作用**：从类型中"挑选"出指定的属性，组成新类型

```typescript
interface User {
  id: string;
  name: string;
  age: number;
  email: string;
  password: string;
}

// 只要 name 和 email
type UserDisplay = Pick<User, 'name' | 'email'>;
// 等价于：
// type UserDisplay = {
//   name: string;
//   email: string;
// }

const displayUser: UserDisplay = {
  name: "张三",
  email: "zhangsan@qq.com"
  // 不需要其他属性
};
```

**💼 实际应用**：
```typescript
// API响应：只返回安全的用户信息
type PublicUser = Pick<User, 'id' | 'name'>;

function getUserProfile(id: string): PublicUser {
  const user = getFullUser(id);
  return {
    id: user.id,
    name: user.name
    // 不暴露 password 等敏感信息
  };
}
```

### 3.2 Omit\<T, K> - 排除指定属性


**🔸 作用**：从类型中"去掉"指定的属性，保留其他的

```typescript
interface User {
  id: string;
  name: string;
  age: number;
  email: string;
  password: string;
}

// 去掉 password，保留其他的
type SafeUser = Omit<User, 'password'>;
// 等价于：
// type SafeUser = {
//   id: string;
//   name: string;
//   age: number;
//   email: string;
// }

// 可以去掉多个属性
type CreateUser = Omit<User, 'id' | 'password'>;
```

**💼 实际应用**：
```typescript
// 创建用户：去掉id（自动生成）和password（单独处理）
function createUser(userData: CreateUser, password: string) {
  return {
    id: generateId(),
    ...userData,
    password: hashPassword(password)
  };
}
```

### 3.3 Pick vs Omit 对比


```
📊 使用场景对比：

Pick - "我只要这几个"
✅ 需要的属性很少时使用
✅ 明确知道要哪些属性
例：从User中只要name和email

Omit - "我不要这几个，其他都要"  
✅ 不需要的属性很少时使用
✅ 明确知道不要哪些属性
例：从User中去掉password，其他都要
```

---

## 4. 🗂️ 键值映射工具


### 4.1 Record\<K, V> - 构造键值对类型


**🔸 作用**：创建一个"字典"类型，指定键的类型和值的类型

```typescript
// 语法：Record<键类型, 值类型>

// 例子1：用户角色权限
type UserRole = 'admin' | 'user' | 'guest';
type Permission = {
  read: boolean;
  write: boolean;
};

type RolePermissions = Record<UserRole, Permission>;
// 等价于：
// type RolePermissions = {
//   admin: Permission;
//   user: Permission;
//   guest: Permission;
// }

const permissions: RolePermissions = {
  admin: { read: true, write: true },
  user: { read: true, write: false },
  guest: { read: false, write: false }
};
```

**💼 实际应用**：
```typescript
// 配置映射
type Environment = 'dev' | 'test' | 'prod';
type Config = {
  apiUrl: string;
  debug: boolean;
};

const envConfigs: Record<Environment, Config> = {
  dev: { apiUrl: 'http://localhost:3000', debug: true },
  test: { apiUrl: 'http://test.example.com', debug: false },
  prod: { apiUrl: 'http://api.example.com', debug: false }
};

// 状态管理
type Status = 'loading' | 'success' | 'error';
const statusMessages: Record<Status, string> = {
  loading: '加载中...',
  success: '操作成功',
  error: '操作失败'
};
```

---

## 5. 🔄 联合类型工具


### 5.1 Exclude\<T, U> - 排除联合类型


**🔸 作用**：从联合类型中排除某些类型

```typescript
type AllColors = 'red' | 'green' | 'blue' | 'yellow' | 'purple';

// 排除暖色调，只保留冷色调
type CoolColors = Exclude<AllColors, 'red' | 'yellow'>;
// 结果：'green' | 'blue' | 'purple'

const coolColor: CoolColors = 'blue'; // ✅
// const warmColor: CoolColors = 'red'; // ❌ 错误
```

**💼 实际应用**：
```typescript
// 事件类型过滤
type AllEvents = 'click' | 'scroll' | 'keydown' | 'resize' | 'load';
type UserEvents = Exclude<AllEvents, 'resize' | 'load'>; // 排除系统事件

function handleUserEvent(event: UserEvents) {
  // 只处理用户交互事件
}
```

### 5.2 Extract\<T, U> - 提取联合类型


**🔸 作用**：从联合类型中提取出指定的类型

```typescript
type AllEvents = 'click' | 'scroll' | 'keydown' | 'resize' | 'load';

// 只提取鼠标事件
type MouseEvents = Extract<AllEvents, 'click' | 'scroll'>;
// 结果：'click' | 'scroll'

const mouseEvent: MouseEvents = 'click'; // ✅
// const keyEvent: MouseEvents = 'keydown'; // ❌ 错误
```

**💼 实际应用**：
```typescript
// API方法过滤
type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH' | 'HEAD';
type DataMethods = Extract<HttpMethod, 'POST' | 'PUT' | 'PATCH'>;

function sendData(method: DataMethods, data: any) {
  // 只处理带数据的请求方法
}
```

### 5.3 Exclude vs Extract 对比


```
🎯 记忆技巧：

Exclude = "排除" = "不要这些"
Extract = "提取" = "只要这些"

从 'A' | 'B' | 'C' | 'D' 中：
Exclude<..., 'A' | 'B'> → 'C' | 'D'  (不要A和B)
Extract<..., 'A' | 'B'> → 'A' | 'B'  (只要A和B)
```

---

## 6. 🔧 函数类型工具


### 6.1 ReturnType\<T> - 获取函数返回值类型


**🔸 作用**：提取函数的返回值类型

```typescript
// 例子1：普通函数
function getUser() {
  return {
    id: '123',
    name: '张三',
    age: 25
  };
}

type User = ReturnType<typeof getUser>;
// 结果：{ id: string; name: string; age: number; }

// 例子2：异步函数
async function fetchData() {
  return { data: [1, 2, 3], status: 'success' };
}

type FetchResult = ReturnType<typeof fetchData>;
// 结果：Promise<{ data: number[]; status: string; }>
```

**💼 实际应用**：
```typescript
// API响应类型推导
function createApiClient() {
  return {
    getUsers: () => [{ id: 1, name: 'user1' }],
    getOrders: () => [{ id: 1, total: 100 }]
  };
}

type ApiClient = ReturnType<typeof createApiClient>;
// 自动推导出客户端的类型结构

// 复杂函数返回值
function processData(input: string[]) {
  return {
    processed: input.map(s => s.toUpperCase()),
    count: input.length,
    summary: `处理了 ${input.length} 条数据`
  };
}

type ProcessResult = ReturnType<typeof processData>;
// 避免手动重复定义返回值类型
```

### 6.2 函数类型工具的实用技巧


```typescript
// 技巧1：结合泛型使用
function createStore<T>(initialState: T) {
  return {
    state: initialState,
    setState: (newState: T) => {},
    getState: () => initialState
  };
}

type Store<T> = ReturnType<typeof createStore<T>>;
// 可以用于任何类型的Store

// 技巧2：从第三方库提取类型
import { someComplexFunction } from 'third-party-lib';
type LibResult = ReturnType<typeof someComplexFunction>;
// 不用查文档，直接提取返回值类型
```

---

## 7. 💡 实战应用技巧


### 7.1 工具类型组合使用


```typescript
interface User {
  id: string;
  name: string;
  email: string;
  password: string;
  createdAt: Date;
  updatedAt: Date;
}

// 组合1：创建用户（去掉自动字段，密码变可选）
type CreateUserInput = Omit<User, 'id' | 'createdAt' | 'updatedAt'> & {
  password?: string; // 重写为可选
};

// 组合2：更新用户（去掉不可变字段，其他变可选）
type UpdateUserInput = Partial<Omit<User, 'id' | 'createdAt'>>;

// 组合3：公开用户信息（只要安全字段，设为只读）
type PublicUser = Readonly<Pick<User, 'id' | 'name'>>;
```

### 7.2 常见使用模式


```typescript
// 模式1：表单数据处理
interface FormData {
  name: string;
  email: string;
  age: number;
}

type FormErrors = Partial<Record<keyof FormData, string>>;
// 每个字段都可能有错误信息，也可能没有

const errors: FormErrors = {
  email: '邮箱格式不正确'
  // name 和 age 没有错误，可以不填
};

// 模式2：配置管理
type Environment = 'dev' | 'prod';
interface BaseConfig {
  appName: string;
  version: string;
}

type EnvConfig = Record<Environment, BaseConfig & {
  apiUrl: string;
  debug: boolean;
}>;
```

### 7.3 最佳实践建议


```
🎯 选择工具类型的建议：

1. 属性数量考虑：
   需要的少 → 用 Pick
   不要的少 → 用 Omit

2. 可选性处理：
   全部可选 → 用 Partial
   全部必填 → 用 Required

3. 数据安全：
   防止修改 → 用 Readonly
   隐藏敏感信息 → 用 Omit 或 Pick

4. 映射关系：
   枚举对应值 → 用 Record
```

---

## 8. 📋 核心要点总结


### 8.1 工具类型速查表


| 工具类型 | **作用** | **使用场景** | **记忆技巧** |
|---------|---------|-------------|-------------|
| `Partial<T>` | `所有属性变可选` | `更新操作、表单校验` | `部分填写` |
| `Required<T>` | `所有属性变必填` | `配置验证、完整性检查` | `必须填写` |
| `Readonly<T>` | `所有属性变只读` | `常量配置、防止修改` | `只能读取` |
| `Pick<T, K>` | `选择指定属性` | `API返回、数据展示` | `挑选出来` |
| `Omit<T, K>` | `排除指定属性` | `隐藏敏感信息、创建数据` | `忽略掉` |
| `Record<K, V>` | `构造键值映射` | `枚举映射、配置对象` | `记录本` |
| `Exclude<T, U>` | `排除联合类型` | `类型过滤、条件限制` | `排除出去` |
| `Extract<T, U>` | `提取联合类型` | `类型筛选、特定处理` | `提取出来` |
| `ReturnType<T>` | `获取返回值类型` | `函数结果类型、API推导` | `返回什么类型` |

### 8.2 关键理解要点


**🔹 工具类型的本质**：
```
工具类型 = 类型的"函数"
输入一个类型，输出一个新类型
就像数学函数：f(x) = y
```

**🔹 组合使用的威力**：
```
单个工具类型功能有限
组合使用可以解决复杂需求
Partial<Omit<User, 'id'>> = 去掉id后全部变可选
```

**🔹 实用性原则**：
```
不要为了用而用
根据实际需求选择
保持代码可读性
```

### 8.3 学习建议


**🎯 循序渐进**：
1. **先掌握基础4个**：Partial、Required、Pick、Omit
2. **再学习映射工具**：Record
3. **最后掌握高级工具**：联合类型操作、函数类型

**🎯 实践方法**：
- 在真实项目中使用
- 遇到重复类型定义时，想想能否用工具类型简化
- 多看开源项目的类型定义

**核心记忆**：
- 工具类型让类型操作变简单
- 避免重复定义，提高代码复用
- 选择合适的工具，组合解决复杂需求
- 实用为主，不过度复杂化