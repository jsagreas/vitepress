---
title: 4、实用泛型技巧
---
## 📚 目录

1. [typeof 与泛型结合](#1-typeof-与泛型结合)
2. [keyof 与泛型的键类型操作](#2-keyof-与泛型的键类型操作)
3. [泛型约束实践模式](#3-泛型约束实践模式)
4. [泛型类型别名技巧](#4-泛型类型别名技巧)
5. [泛型默认值策略](#5-泛型默认值策略)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔍 typeof 与泛型结合


### 1.1 什么是 typeof 类型操作


**简单理解**: `typeof` 就是让 TypeScript 直接"看"一个值，然后自动推出它的类型

```typescript
// 普通用法：从值推出类型
const user = {
  name: 'tom',
  age: 18,
  active: true
}

// typeof 获取 user 的类型
type UserType = typeof user
// 等价于: { name: string; age: number; active: boolean }
```

### 1.2 typeof 与泛型的实用组合


**场景1**: 根据配置对象创建泛型函数

```typescript
// 配置对象
const apiConfig = {
  userApi: '/api/users',
  postApi: '/api/posts',
  timeout: 5000
} as const  // 重要：as const 保持字面量类型

// 泛型函数：基于配置类型工作
function createApiClient<T extends typeof apiConfig>(config: T) {
  return {
    get: <K extends keyof T>(endpoint: K) => {
      // 这里 T[K] 的类型是精确的字面量类型
      console.log(`GET ${config[endpoint]}`)
      return config[endpoint]
    },
    config
  }
}

const client = createApiClient(apiConfig)
// 类型推断：client.get('userApi') 返回 '/api/users'
```

**场景2**: 从现有对象提取类型作为泛型约束

```typescript
// 已有的数据
const defaultSettings = {
  theme: 'light',
  language: 'en',
  notifications: true
}

// 泛型函数：只接受与默认设置结构相同的对象
function updateSettings<T extends typeof defaultSettings>(
  updates: Partial<T>
): T {
  return { ...defaultSettings, ...updates } as T
}

// 使用：类型安全，且有智能提示
const newSettings = updateSettings({
  theme: 'dark',  // ✅ 正确
  // typo: 'error'  // ❌ 类型错误，属性不存在
})
```

### 1.3 typeof 在工厂函数中的应用


```typescript
// 类的实例
class DatabaseConnection {
  host = 'localhost'
  port = 5432
  connect() { /* ... */ }
}

// 工厂函数：根据类创建实例
function createInstance<T extends new (...args: any[]) => any>(
  constructor: T,
  ...args: any[]
): InstanceType<T> {
  return new constructor(...args)
}

// 使用
const db = createInstance(DatabaseConnection)
// db 的类型自动推断为 DatabaseConnection
```

---

## 2. 🗝️ keyof 与泛型的键类型操作


### 2.1 keyof 的基本作用


**简单理解**: `keyof` 就是获取一个对象所有属性名，组成联合类型

```typescript
type Person = {
  name: string
  age: number
  email: string
}

// keyof Person = 'name' | 'age' | 'email'
type PersonKeys = keyof Person
```

### 2.2 keyof 与泛型的核心应用


**应用1**: 类型安全的属性获取器

```typescript
// 泛型函数：从对象中安全地取值
function getValue<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key]
}

const person = { name: 'Alice', age: 25, active: true }

const name = getValue(person, 'name')    // 类型: string
const age = getValue(person, 'age')      // 类型: number
// const invalid = getValue(person, 'xxx')  // ❌ 编译错误
```

**应用2**: 动态属性设置器

```typescript
// 类型安全的属性设置
function setValue<T, K extends keyof T>(
  obj: T, 
  key: K, 
  value: T[K]
): void {
  obj[key] = value
}

const settings = { theme: 'light', fontSize: 14 }

setValue(settings, 'theme', 'dark')     // ✅ 正确
setValue(settings, 'fontSize', 16)      // ✅ 正确
// setValue(settings, 'theme', 123)     // ❌ 类型不匹配
```

### 2.3 keyof 在对象映射中的妙用


```typescript
// 将对象的所有值转为字符串
type StringifyValues<T> = {
  [K in keyof T]: string
}

type OriginalData = {
  id: number
  active: boolean
  count: number
}

// 应用映射类型
type StringifiedData = StringifyValues<OriginalData>
// 结果: { id: string; active: string; count: string }

// 实际函数
function stringifyObject<T extends Record<string, any>>(
  obj: T
): StringifyValues<T> {
  const result = {} as StringifyValues<T>
  
  for (const key in obj) {
    result[key] = String(obj[key])
  }
  
  return result
}
```

### 2.4 keyof 与条件类型结合


```typescript
// 过滤出指定类型的属性
type FilterByType<T, U> = {
  [K in keyof T]: T[K] extends U ? K : never
}[keyof T]

type User = {
  id: number
  name: string
  age: number
  email: string
  active: boolean
}

// 获取所有 string 类型的属性名
type StringProps = FilterByType<User, string>  // 'name' | 'email'
type NumberProps = FilterByType<User, number>  // 'id' | 'age'
```

---

## 3. ⚡ 泛型约束实践模式


### 3.1 什么是泛型约束


**简单理解**: 泛型约束就是给泛型"设规矩"，限制它只能是某些特定类型

```typescript
// 无约束：T 可以是任何类型
function process<T>(data: T): T {
  // console.log(data.length)  // ❌ 错误：T 不一定有 length
  return data
}

// 有约束：T 必须有 length 属性
function processWithLength<T extends { length: number }>(data: T): T {
  console.log(data.length)  // ✅ 正确：确保有 length
  return data
}
```

### 3.2 常见约束模式


**模式1**: 对象类型约束

```typescript
// 约束：T 必须是对象类型
function updateObject<T extends Record<string, any>>(
  original: T,
  updates: Partial<T>
): T {
  return { ...original, ...updates }
}

// 约束：T 必须有 id 属性
function findById<T extends { id: string | number }>(
  items: T[],
  id: T['id']
): T | undefined {
  return items.find(item => item.id === id)
}
```

**模式2**: 函数类型约束

```typescript
// 约束：T 必须是函数类型
function retry<T extends (...args: any[]) => any>(
  fn: T,
  times: number = 3
): T {
  return ((...args: Parameters<T>) => {
    for (let i = 0; i < times; i++) {
      try {
        return fn(...args)
      } catch (error) {
        if (i === times - 1) throw error
      }
    }
  }) as T
}

// 使用
const apiCall = retry((url: string) => fetch(url))
```

**模式3**: 多重约束

```typescript
// 多个约束条件
interface Identifiable { id: string }
interface Timestamped { createdAt: Date }

function processEntity<T extends Identifiable & Timestamped>(
  entity: T
): T & { processed: boolean } {
  console.log(`Processing ${entity.id} created at ${entity.createdAt}`)
  return { ...entity, processed: true }
}
```

### 3.3 条件约束模式


```typescript
// 根据条件动态约束
type ApiResponse<T> = T extends 'success' 
  ? { data: any; status: 200 }
  : { error: string; status: 400 }

// 条件泛型函数
function handleResponse<T extends 'success' | 'error'>(
  type: T,
  payload: T extends 'success' ? any : string
): ApiResponse<T> {
  if (type === 'success') {
    return { data: payload, status: 200 } as ApiResponse<T>
  } else {
    return { error: payload as string, status: 400 } as ApiResponse<T>
  }
}
```

---

## 4. 🏷️ 泛型类型别名技巧


### 4.1 基础泛型别名


**简单理解**: 把复杂的泛型类型起个简单的名字，方便复用

```typescript
// 复杂的类型定义
type ComplexApiResponse<T> = {
  data: T
  meta: {
    total: number
    page: number
    hasMore: boolean
  }
  status: 'success' | 'error'
}

// 使用别名
type UserListResponse = ComplexApiResponse<User[]>
type ProductResponse = ComplexApiResponse<Product>
```

### 4.2 实用的泛型工具别名


**常用模式**: 创建自己的工具类型

```typescript
// 深度可选类型
type DeepPartial<T> = {
  [P in keyof T]?: T[P] extends object ? DeepPartial<T[P]> : T[P]
}

// 深度只读类型
type DeepReadonly<T> = {
  readonly [P in keyof T]: T[P] extends object ? DeepReadonly<T[P]> : T[P]
}

// 非空类型（去除 null 和 undefined）
type NonNullable<T> = T extends null | undefined ? never : T

// 使用示例
type Config = {
  database: {
    host: string
    port: number
  }
  cache: {
    enabled: boolean
    ttl: number
  }
}

type PartialConfig = DeepPartial<Config>
// 所有属性（包括嵌套）都变为可选
```

### 4.3 业务场景的泛型别名


```typescript
// API 响应格式
type ApiResult<T = any> = {
  success: boolean
  data: T
  message: string
  code: number
}

// 分页数据格式
type PagedData<T> = {
  items: T[]
  pagination: {
    current: number
    size: number
    total: number
  }
}

// 表单字段类型
type FormField<T> = {
  value: T
  error?: string
  touched: boolean
  required: boolean
}

// 实际使用
type UserForm = {
  name: FormField<string>
  age: FormField<number>
  email: FormField<string>
}

type UserListApi = ApiResult<PagedData<User>>
```

### 4.4 函数类型的泛型别名


```typescript
// 事件处理器类型
type EventHandler<T = Event> = (event: T) => void

// 异步操作类型
type AsyncOperation<T, E = Error> = () => Promise<T | E>

// 状态更新器类型
type StateUpdater<T> = (prev: T) => T

// 使用
const handleClick: EventHandler<MouseEvent> = (e) => {
  console.log('Clicked:', e.target)
}

const updateUser: StateUpdater<User> = (prevUser) => ({
  ...prevUser,
  lastUpdated: new Date()
})
```

---

## 5. 🎯 泛型默认值策略


### 5.1 泛型默认值的基本用法


**简单理解**: 给泛型设置默认类型，就像函数参数的默认值一样

```typescript
// 基础语法：T = DefaultType
interface ApiResponse<T = any> {
  data: T
  status: number
  message: string
}

// 使用时可以不指定类型参数
const response1: ApiResponse = { data: 'anything', status: 200, message: 'ok' }
const response2: ApiResponse<User> = { data: userObj, status: 200, message: 'ok' }
```

### 5.2 常见的默认值策略


**策略1**: 宽松默认值（开发阶段）

```typescript
// 开发时用 any，上线前再具体化
interface DataStore<T = any> {
  items: T[]
  add(item: T): void
  find(predicate: (item: T) => boolean): T | undefined
}

// 开发阶段
const tempStore: DataStore = {
  items: [],
  add(item) { this.items.push(item) },
  find(predicate) { return this.items.find(predicate) }
}
```

**策略2**: 合理的默认类型

```typescript
// HTTP 客户端：默认返回 unknown（更安全）
class ApiClient<TResponse = unknown> {
  async get(url: string): Promise<TResponse> {
    const response = await fetch(url)
    return response.json()
  }
}

// 数组工具：默认处理任意类型
function chunk<T = any>(array: T[], size: number = 10): T[][] {
  const chunks: T[][] = []
  for (let i = 0; i < array.length; i += size) {
    chunks.push(array.slice(i, i + size))
  }
  return chunks
}
```

**策略3**: 条件默认值

```typescript
// 根据其他泛型参数决定默认值
type EventCallback<
  TEventType extends string,
  TData = TEventType extends 'error' ? Error : any
> = (data: TData) => void

// 使用
const errorHandler: EventCallback<'error'> = (error: Error) => {
  console.error(error)  // error 自动推断为 Error 类型
}

const dataHandler: EventCallback<'data'> = (data: any) => {
  console.log(data)  // data 类型为 any
}
```

### 5.3 多个泛型参数的默认值


```typescript
// 多个泛型参数的默认值策略
interface Repository<
  TEntity = Record<string, any>,  // 实体类型
  TId = string,                   // ID 类型
  TQuery = Partial<TEntity>       // 查询条件类型
> {
  findById(id: TId): Promise<TEntity | null>
  findMany(query: TQuery): Promise<TEntity[]>
  create(entity: Omit<TEntity, 'id'>): Promise<TEntity>
  update(id: TId, updates: Partial<TEntity>): Promise<TEntity>
}

// 灵活使用
type UserRepository = Repository<User, string, UserSearchQuery>
type ProductRepository = Repository<Product, number>  // 只指定前两个
type SimpleRepository = Repository  // 全部使用默认值
```

### 5.4 默认值的最佳实践


```typescript
// ✅ 好的默认值策略
interface AsyncState<T = null, E = Error> {
  data: T
  loading: boolean
  error: E | null
}

// 渐进式类型化
function createAsyncState<T = null, E = Error>(
  initialData?: T
): AsyncState<T, E> {
  return {
    data: initialData ?? null as T,
    loading: false,
    error: null
  }
}

// 使用：从简单到复杂
const state1 = createAsyncState()                    // 默认类型
const state2 = createAsyncState<User>()              // 指定数据类型
const state3 = createAsyncState<User, ApiError>()   // 完全指定

// ❌ 避免的模式
interface BadExample<T = any, U = any, V = any> {
  // 太多 any 默认值，失去类型安全
}
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 typeof + 泛型：从值推出类型，用于类型约束和推断
🔸 keyof + 泛型：键类型操作，实现类型安全的属性访问
🔸 泛型约束：extends 限制泛型范围，确保类型安全
🔸 泛型别名：type 定义复用性强的泛型工具
🔸 泛型默认值：平衡灵活性和类型安全的策略
```

### 6.2 实践应用要点


**🔹 typeof 使用场景**
```typescript
// ✅ 适合：从配置对象推导类型
const config = { api: '/users', timeout: 5000 } as const
type ConfigType = typeof config

// ✅ 适合：工厂函数的类型推导
function factory<T extends typeof SomeClass>(ctor: T): InstanceType<T>
```

**🔹 keyof 使用原则**
```typescript
// ✅ 类型安全的属性操作
function get<T, K extends keyof T>(obj: T, key: K): T[K]

// ✅ 对象类型映射
type Partial<T> = { [K in keyof T]?: T[K] }
```

**🔹 约束设计原则**
```typescript
// ✅ 最小必要约束
function process<T extends { id: string }>(item: T): T

// ❌ 过度约束
function process<T extends { id: string; name: string; age: number }>(item: T): T
```

### 6.3 常见错误与解决


| 问题 | 错误示例 | 正确做法 |
|------|----------|----------|
| **typeof 忘记 as const** | `typeof config` 类型过宽 | `typeof config` + `as const` |
| **keyof 类型不安全** | 直接使用字符串作为键 | `K extends keyof T` 约束 |
| **过度使用 any 默认值** | `<T = any>` 失去类型检查 | 使用更具体的默认类型 |
| **约束过于复杂** | 多层嵌套的约束条件 | 拆分为多个简单约束 |

### 6.4 记忆技巧


**🎯 实用口诀**：
- `typeof` 看值推类型，配置对象最常用
- `keyof` 取键做约束，属性访问保安全  
- 约束要有必要性，过多过少都不行
- 别名复用提效率，默认值要选明智
- 渐进类型最实用，从简单到复杂

**🔧 开发建议**：
1. **先写具体，再抽象**：先写出具体类型，再用泛型抽象
2. **渐进式类型化**：从宽松默认值开始，逐步收紧类型
3. **业务场景优先**：根据实际需求设计泛型，避免过度设计
4. **文档和示例**：复杂泛型要有清晰的使用示例

**核心记忆**：
- 泛型技巧让代码更灵活，类型更安全
- `typeof` 和 `keyof` 是泛型编程的左膀右臂
- 合理的约束和默认值是泛型设计的关键
- 实用性永远比炫技更重要