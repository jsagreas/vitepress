---
title: 1、模块导入导出语法
---
## 📚 目录

1. [模块系统基本概念](#1-模块系统基本概念)
2. [export导出语法详解](#2-export导出语法详解)
3. [import导入语法详解](#3-import导入语法详解)
4. [export default默认导出](#4-export-default默认导出)
5. [重新导出转发语法](#5-重新导出转发语法)
6. [动态导入与异步加载](#6-动态导入与异步加载)
7. [type-only类型专用导入导出](#7-type-only类型专用导入导出)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🏗️ 模块系统基本概念


### 1.1 什么是模块系统


**🔸 模块的本质**
```
模块就像一个独立的代码包裹
• 每个文件就是一个模块
• 模块内的变量和函数默认是私有的
• 只有通过 export 导出的内容才能被其他模块使用
• 通过 import 可以使用其他模块导出的内容
```

**💡 为什么需要模块系统**
```
解决的问题：
• 避免全局变量污染
• 代码复用和组织
• 依赖关系管理
• 按需加载提升性能

举个例子：
就像房子有不同的房间，每个房间有特定功能
厨房的工具不会乱放到卧室，需要时才拿出来用
```

### 1.2 TypeScript中的模块


**🔗 模块类型系统**
```
TypeScript 的模块不仅管理代码，还管理类型
• 导出的不仅是值，还有类型信息
• 类型安全在模块边界得到保证
• 支持类型专用的导入导出
```

**📁 文件示例结构**
```
project/
├── utils/
│   ├── math.ts        ← 数学工具模块
│   ├── string.ts      ← 字符串工具模块
│   └── index.ts       ← 统一导出文件
├── types/
│   └── user.ts        ← 用户类型定义
└── main.ts            ← 主入口文件
```

---

## 2. 📤 export导出语法详解


### 2.1 命名导出 (Named Export)


**🔸 基础语法**
```typescript
// math.ts - 数学工具模块
export function add(a: number, b: number): number {
  return a + b;
}

export function multiply(a: number, b: number): number {
  return a * b;
}

export const PI = 3.14159;

// 也可以先定义再导出
function subtract(a: number, b: number): number {
  return a - b;
}

const E = 2.71828;

export { subtract, E };
```

**💡 命名导出的特点**
```
✅ 一个模块可以有多个命名导出
✅ 导出的名称必须和定义时的名称一致
✅ 导入时需要使用相同的名称
✅ 支持类型和值的混合导出
```

### 2.2 导出时重命名


**🔧 重命名语法**
```typescript
// string.ts
function processText(text: string): string {
  return text.trim().toLowerCase();
}

function checkLength(text: string): boolean {
  return text.length > 0;
}

// 导出时重命名
export { 
  processText as formatText,
  checkLength as isValidText 
};
```

### 2.3 类型导出


**📋 类型定义导出**
```typescript
// user.ts - 用户相关类型
export interface User {
  id: number;
  name: string;
  email: string;
}

export type UserRole = 'admin' | 'user' | 'guest';

export class UserManager {
  private users: User[] = [];
  
  addUser(user: User): void {
    this.users.push(user);
  }
}
```

---

## 3. 📥 import导入语法详解


### 3.1 命名导入


**🔸 基础导入语法**
```typescript
// main.ts
import { add, multiply, PI } from './utils/math';

const result = add(5, 3);        // 8
const area = multiply(PI, 4);     // 12.56636
```

**🔄 导入时重命名**
```typescript
// 导入时重命名避免冲突
import { add as mathAdd } from './utils/math';
import { add as stringAdd } from './utils/string';

const sum = mathAdd(1, 2);
const text = stringAdd("Hello", " World");
```

### 3.2 导入所有内容


**⭐ 命名空间导入**
```typescript
// 把所有导出内容放到一个对象中
import * as MathUtils from './utils/math';

const result = MathUtils.add(5, 3);
const circle = MathUtils.PI * 2;
```

**💡 什么时候使用命名空间导入**
```
✅ 适用场景：
• 模块导出内容较多时
• 避免命名冲突
• 明确表示来源模块

❌ 避免场景：
• 只需要少数几个导出时
• 会导致代码冗长时
```

### 3.3 类型导入


**📋 导入类型定义**
```typescript
// 导入类型和类
import { User, UserRole, UserManager } from './types/user';

// 创建用户对象
const newUser: User = {
  id: 1,
  name: "张三",
  email: "zhangsan@example.com"
};

const role: UserRole = 'admin';
const manager = new UserManager();
```

---

## 4. 🎯 export default默认导出


### 4.1 默认导出语法


**🔸 函数默认导出**
```typescript
// calculator.ts
function calculate(a: number, b: number, operation: string): number {
  switch (operation) {
    case '+': return a + b;
    case '-': return a - b;
    case '*': return a * b;
    case '/': return a / b;
    default: return 0;
  }
}

export default calculate;
```

**🔸 类默认导出**
```typescript
// database.ts
class Database {
  private data: any[] = [];
  
  save(item: any): void {
    this.data.push(item);
  }
  
  findAll(): any[] {
    return this.data;
  }
}

export default Database;
```

### 4.2 默认导入语法


**📥 导入默认导出**
```typescript
// 导入时可以使用任意名称
import calculator from './calculator';
import DB from './database';           // 可以重命名
import MyCalculator from './calculator'; // 也可以这样命名

const result = calculator(5, 3, '+');
const db = new DB();
```

### 4.3 混合导入导出


**🔀 同时使用命名和默认导出**
```typescript
// config.ts
export const API_URL = 'https://api.example.com';
export const TIMEOUT = 5000;

const defaultConfig = {
  url: API_URL,
  timeout: TIMEOUT,
  retries: 3
};

export default defaultConfig;
```

```typescript
// 混合导入
import config, { API_URL, TIMEOUT } from './config';

// 使用默认导出
console.log(config.retries); // 3

// 使用命名导出
console.log(API_URL);         // https://api.example.com
```

### 4.4 默认导出的类型处理


**📋 默认导出的类型定义**
```typescript
// userService.ts
interface ServiceConfig {
  baseUrl: string;
  timeout: number;
}

class UserService {
  constructor(private config: ServiceConfig) {}
  
  async getUser(id: number): Promise<User> {
    // 实现逻辑
    return {} as User;
  }
}

export default UserService;
export type { ServiceConfig }; // 同时导出类型
```

---

## 5. 🔄 重新导出转发语法


### 5.1 基础重新导出


**🔸 转发导出语法**
```typescript
// utils/index.ts - 统一导出文件
export { add, multiply, PI } from './math';
export { formatText, isValidText } from './string';
export { default as Calculator } from './calculator';
```

**💡 重新导出的用途**
```
统一入口：
• 提供一个统一的导入入口
• 隐藏内部模块结构
• 简化使用者的导入路径

使用对比：
// 没有统一导出时
import { add } from './utils/math';
import { formatText } from './utils/string';

// 有统一导出后
import { add, formatText } from './utils';
```

### 5.2 全部重新导出


**⭐ 导出所有内容**
```typescript
// utils/index.ts
export * from './math';      // 导出 math 模块的所有命名导出
export * from './string';    // 导出 string 模块的所有命名导出

// 注意：不能使用 export * 导出默认导出
export { default as Calculator } from './calculator';
```

### 5.3 选择性重新导出


**🎯 有选择地转发**
```typescript
// api/index.ts
// 只导出部分内容，隐藏内部实现
export { getUser, updateUser } from './userApi';
export { getProduct } from './productApi';
// 不导出 deleteUser，因为这是内部接口
```

### 5.4 重新导出与类型


**📋 类型转发**
```typescript
// types/index.ts
export type { User, UserRole } from './user';
export type { Product, Category } from './product';
export { UserManager } from './user';        // 类既是类型也是值
```

---

## 6. ⚡ 动态导入与异步加载


### 6.1 动态导入基础


**🔸 import() 函数语法**
```typescript
// 动态导入返回 Promise
async function loadMathUtils() {
  const mathModule = await import('./utils/math');
  
  const result = mathModule.add(5, 3);
  console.log(result); // 8
}

// 也可以使用 .then()
import('./utils/math').then(mathModule => {
  const result = mathModule.add(5, 3);
  console.log(result);
});
```

### 6.2 动态导入的类型处理


**📋 Promise<Module> 类型**
```typescript
// 明确指定动态导入的类型
type MathModule = typeof import('./utils/math');

async function calculate(): Promise<number> {
  const math: MathModule = await import('./utils/math');
  return math.add(10, 20);
}

// 或者直接使用
async function quickCalc(): Promise<number> {
  const { add, multiply } = await import('./utils/math');
  return add(multiply(2, 3), 4); // (2*3)+4 = 10
}
```

### 6.3 条件动态导入


**🔀 按需加载**
```typescript
// 根据条件加载不同模块
async function getProcessor(type: 'json' | 'xml') {
  if (type === 'json') {
    const { JsonProcessor } = await import('./processors/json');
    return new JsonProcessor();
  } else {
    const { XmlProcessor } = await import('./processors/xml');
    return new XmlProcessor();
  }
}

// 使用示例
const processor = await getProcessor('json');
```

### 6.4 动态导入的实际应用


**⚡ 性能优化场景**
```typescript
// 路由级别的代码分割
async function loadPage(pageName: string) {
  switch (pageName) {
    case 'dashboard':
      const { DashboardPage } = await import('./pages/Dashboard');
      return DashboardPage;
      
    case 'profile':
      const { ProfilePage } = await import('./pages/Profile');
      return ProfilePage;
      
    default:
      const { NotFoundPage } = await import('./pages/NotFound');
      return NotFoundPage;
  }
}

// 功能模块的懒加载
async function enableAdvancedFeatures() {
  const { AdvancedChart } = await import('./components/AdvancedChart');
  const { DataAnalyzer } = await import('./utils/DataAnalyzer');
  
  return { AdvancedChart, DataAnalyzer };
}
```

---

## 7. 🏷️ type-only类型专用导入导出


### 7.1 类型专用导出


**🔸 export type 语法**
```typescript
// types.ts
export interface User {
  id: number;
  name: string;
}

export type Status = 'active' | 'inactive';

// 只导出类型，不导出值
export type { User as UserType, Status };

// 如果同时有值和类型
export const DEFAULT_STATUS = 'active';
export type { Status }; // 明确只导出类型
```

### 7.2 类型专用导入


**📥 import type 语法**
```typescript
// main.ts
import type { User, Status } from './types';

// 编译后这些导入会被移除，不会出现在运行时代码中
function processUser(user: User): Status {
  return user.id > 0 ? 'active' : 'inactive';
}

// 错误示例：不能在运行时使用 type-only 导入
// const user = new User(); // ❌ 错误！User 只是类型
```

### 7.3 混合导入的类型处理


**🔀 值和类型混合导入**
```typescript
// userService.ts
export class UserService {
  // 实现...
}

export interface UserServiceConfig {
  apiUrl: string;
}

// main.ts - 分别导入值和类型
import { UserService } from './userService';           // 导入类（值）
import type { UserServiceConfig } from './userService'; // 导入接口（类型）

const config: UserServiceConfig = { apiUrl: 'http://api.com' };
const service = new UserService(); // 可以实例化类
```

### 7.4 type-only 的优势


**💡 为什么使用 type-only**
```
编译优化：
• 类型导入在编译后会被完全移除
• 减少运行时代码体积
• 避免循环依赖问题

代码清晰：
• 明确区分值导入和类型导入
• 提高代码可读性
• 防止误用类型作为值

示例对比：
// 普通导入 - 编译后仍然存在
import { User } from './types';

// 类型导入 - 编译后被移除
import type { User } from './types';
```

### 7.5 实际使用建议


**🎯 最佳实践**
```typescript
// 推荐的导入方式
import React from 'react';                    // 值导入
import type { FC, ReactNode } from 'react';   // 类型导入

interface Props {
  children: ReactNode;
  title: string;
}

const MyComponent: FC<Props> = ({ children, title }) => {
  return (
    <div>
      <h1>{title}</h1>
      {children}
    </div>
  );
};
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 模块系统：每个文件是一个模块，默认私有，通过导入导出共享
🔸 命名导出：export { name }，导入时使用相同名称
🔸 默认导出：export default，导入时可以使用任意名称
🔸 重新导出：统一入口，简化导入路径
🔸 动态导入：import() 异步加载，返回 Promise
🔸 类型导入：import type 仅导入类型，编译后移除
```

### 8.2 语法对比总结


| 导出方式 | **语法** | **导入语法** | **特点** |
|---------|---------|-------------|----------|
| 🏷️ **命名导出** | `export { name }` | `import { name }` | `多个导出，名称固定` |
| 🎯 **默认导出** | `export default value` | `import name` | `一个默认，名称自定` |
| ⭐ **全部导入** | - | `import * as ns` | `命名空间导入` |
| 🔄 **重新导出** | `export { } from './module'` | - | `转发其他模块` |
| ⚡ **动态导入** | - | `await import('./module')` | `异步加载` |
| 🏷️ **类型导入** | `export type { Type }` | `import type { Type }` | `仅类型，编译移除` |

### 8.3 实际使用建议


**🎯 选择导出方式的原则**
```
命名导出：
✅ 工具函数库（多个相关函数）
✅ 常量定义文件
✅ 类型定义文件

默认导出：
✅ React组件
✅ 主要的类或服务
✅ 配置对象

重新导出：
✅ 创建统一的API入口
✅ 简化复杂的目录结构
✅ 版本兼容性处理
```

**⚡ 性能优化建议**
```
动态导入场景：
• 路由级别的代码分割
• 可选功能的懒加载  
• 条件性的库加载

类型导入使用：
• 只需要类型定义时
• 避免循环依赖
• 减少编译后代码体积
```

### 8.4 常见错误与避免


**❌ 容易犯的错误**
```typescript
// 错误1：混淆命名导出和默认导出
import add from './math';        // ❌ 如果 add 是命名导出
import { default } from './math'; // ❌ 语法错误

// 正确写法
import { add } from './math';     // ✅ 命名导入
import calculator from './math';  // ✅ 默认导入

// 错误2：type-only 导入当作值使用
import type { User } from './types';
const user = new User();         // ❌ 不能实例化类型

// 正确写法
import { User } from './types';   // ✅ 导入类（值+类型）
const user = new User();
```

**核心记忆口诀**：
- 模块导入导出很重要，类型安全要记牢
- 命名导出用花括号，默认导出随意叫
- 重新导出做统一，动态导入性能好
- 类型导入编译消，按需使用是技巧