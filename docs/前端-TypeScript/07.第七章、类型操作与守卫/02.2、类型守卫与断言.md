---
title: 2、类型守卫与断言
---
## 📚 目录

1. [什么是类型守卫](#1-什么是类型守卫)
2. [typeof 守卫 - 基础类型检查](#2-typeof-守卫---基础类型检查)
3. [instanceof 守卫 - 实例类型检查](#3-instanceof-守卫---实例类型检查)
4. [in 操作符守卫 - 属性存在检查](#4-in-操作符守卫---属性存在检查)
5. [自定义类型守卫 - is 谓词](#5-自定义类型守卫---is-谓词)
6. [类型断言 - as 语法](#6-类型断言---as-语法)
7. [非空断言 - ! 操作符](#7-非空断言---!-操作符)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🛡️ 什么是类型守卫


### 1.1 概念理解

**类型守卫**就像是程序中的"安全检查员"，它帮助我们在运行时确认变量的具体类型，让 TypeScript 知道在某个代码块中变量是什么类型。

```
想象一个场景：
你有一个包裹，不知道里面是苹果还是橙子
类型守卫就是帮你"拆开包裹看一眼"的过程
确认后，你就知道该怎么处理这个水果了
```

### 1.2 为什么需要类型守卫


```typescript
// 没有类型守卫的困惑
function processValue(value: string | number) {
    // TypeScript 不知道 value 到底是 string 还是 number
    // value.toUpperCase(); // ❌ 错误：number 类型没有 toUpperCase 方法
    // value.toFixed(2);    // ❌ 错误：string 类型没有 toFixed 方法
}

// 使用类型守卫后
function processValue(value: string | number) {
    if (typeof value === 'string') {
        // 这里 TypeScript 知道 value 是 string 类型
        console.log(value.toUpperCase()); // ✅ 正确
    } else {
        // 这里 TypeScript 知道 value 是 number 类型
        console.log(value.toFixed(2)); // ✅ 正确
    }
}
```

---

## 2. 🔍 typeof 守卫 - 基础类型检查


### 2.1 基本用法

`typeof` 守卫用来检查**基础数据类型**，就像问"这个变量是什么类型的？"

```typescript
function handleInput(input: string | number | boolean) {
    if (typeof input === 'string') {
        // 这里 input 被确认为 string 类型
        console.log(`字符串长度: ${input.length}`);
        console.log(`转大写: ${input.toUpperCase()}`);
    } 
    else if (typeof input === 'number') {
        // 这里 input 被确认为 number 类型
        console.log(`数字值: ${input}`);
        console.log(`保留两位小数: ${input.toFixed(2)}`);
    } 
    else {
        // 这里 input 被确认为 boolean 类型
        console.log(`布尔值: ${input ? '真' : '假'}`);
    }
}

// 使用示例
handleInput("Hello");    // 输出字符串相关操作
handleInput(3.14159);    // 输出数字相关操作
handleInput(true);       // 输出布尔值相关操作
```

### 2.2 typeof 可以检查的类型


```typescript
// typeof 守卫支持的基础类型
function checkType(value: unknown) {
    if (typeof value === 'string') {
        console.log('这是字符串');
    }
    if (typeof value === 'number') {
        console.log('这是数字');
    }
    if (typeof value === 'boolean') {
        console.log('这是布尔值');
    }
    if (typeof value === 'undefined') {
        console.log('这是 undefined');
    }
    if (typeof value === 'function') {
        console.log('这是函数');
    }
    if (typeof value === 'object') {
        console.log('这是对象（包括 null、数组、对象）');
    }
}
```

### 2.3 实际应用场景


```typescript
// API 响应处理
function processApiResponse(response: string | { data: any }) {
    if (typeof response === 'string') {
        // 错误响应，返回错误信息
        console.error('API 错误:', response);
    } else {
        // 成功响应，处理数据
        console.log('收到数据:', response.data);
    }
}

// 配置参数处理
function setSize(size: number | string) {
    if (typeof size === 'number') {
        return `${size}px`;  // 数字直接加 px
    } else {
        return size;         // 字符串直接返回（可能包含单位）
    }
}
```

---

## 3. 🏗️ instanceof 守卫 - 实例类型检查


### 3.1 基本概念

`instanceof` 守卫用来检查**对象是否是某个类的实例**，就像问"这个对象是从哪个模具做出来的？"

```typescript
class Cat {
    meow() {
        console.log('喵喵~');
    }
}

class Dog {
    bark() {
        console.log('汪汪!');
    }
}

function petSound(pet: Cat | Dog) {
    if (pet instanceof Cat) {
        // 这里 TypeScript 知道 pet 是 Cat 类型
        pet.meow(); // ✅ 可以调用 Cat 的方法
    } else {
        // 这里 TypeScript 知道 pet 是 Dog 类型
        pet.bark(); // ✅ 可以调用 Dog 的方法
    }
}

// 使用示例
const myCat = new Cat();
const myDog = new Dog();

petSound(myCat); // 输出: 喵喵~
petSound(myDog); // 输出: 汪汪!
```

### 3.2 内置类型的 instanceof 检查


```typescript
function processData(data: Date | Array<any> | Error) {
    if (data instanceof Date) {
        // 处理日期
        console.log(`年份: ${data.getFullYear()}`);
    } 
    else if (data instanceof Array) {
        // 处理数组
        console.log(`数组长度: ${data.length}`);
    } 
    else if (data instanceof Error) {
        // 处理错误
        console.log(`错误信息: ${data.message}`);
    }
}

// 使用示例
processData(new Date());           // 处理日期
processData([1, 2, 3]);           // 处理数组
processData(new Error('出错了'));  // 处理错误
```

### 3.3 实际应用：事件处理


```typescript
function handleEvent(event: MouseEvent | KeyboardEvent) {
    if (event instanceof MouseEvent) {
        // 鼠标事件
        console.log(`鼠标点击位置: (${event.clientX}, ${event.clientY})`);
    } else {
        // 键盘事件
        console.log(`按下的键: ${event.key}`);
    }
}
```

---

## 4. 🔑 in 操作符守卫 - 属性存在检查


### 4.1 基本概念

`in` 操作符守卫用来检查**对象是否有某个属性**，就像问"这个对象里有没有这个功能？"

```typescript
interface Bird {
    fly(): void;
    feathers: number;
}

interface Fish {
    swim(): void;
    scales: number;
}

function moveAnimal(animal: Bird | Fish) {
    if ('fly' in animal) {
        // 这里 TypeScript 知道 animal 是 Bird 类型
        console.log('这是鸟类，可以飞行');
        animal.fly();
        console.log(`羽毛数量: ${animal.feathers}`);
    } else {
        // 这里 TypeScript 知道 animal 是 Fish 类型
        console.log('这是鱼类，可以游泳');
        animal.swim();
        console.log(`鳞片数量: ${animal.scales}`);
    }
}
```

### 4.2 实际应用：API 响应处理


```typescript
interface SuccessResponse {
    success: true;
    data: any;
}

interface ErrorResponse {
    success: false;
    error: string;
}

function handleResponse(response: SuccessResponse | ErrorResponse) {
    if ('data' in response) {
        // 成功响应
        console.log('请求成功:', response.data);
    } else {
        // 错误响应
        console.log('请求失败:', response.error);
    }
}
```

### 4.3 检查可选属性


```typescript
interface User {
    name: string;
    email?: string;  // 可选属性
    phone?: string;  // 可选属性
}

function contactUser(user: User) {
    console.log(`联系 ${user.name}:`);
    
    if ('email' in user && user.email) {
        console.log(`邮箱: ${user.email}`);
    }
    
    if ('phone' in user && user.phone) {
        console.log(`电话: ${user.phone}`);
    }
}
```

---

## 5. 🎯 自定义类型守卫 - is 谓词


### 5.1 为什么需要自定义类型守卫

有时候内置的类型守卫不够用，我们需要**自己定义判断规则**。

```typescript
interface Admin {
    name: string;
    privileges: string[];
}

interface User {
    name: string;
    email: string;
}

// 自定义类型守卫函数
function isAdmin(person: Admin | User): person is Admin {
    // 返回 true 表示 person 是 Admin 类型
    return (person as Admin).privileges !== undefined;
}

function handlePerson(person: Admin | User) {
    if (isAdmin(person)) {
        // 这里 TypeScript 知道 person 是 Admin 类型
        console.log(`管理员 ${person.name}`);
        console.log(`权限: ${person.privileges.join(', ')}`);
    } else {
        // 这里 TypeScript 知道 person 是 User 类型
        console.log(`用户 ${person.name}`);
        console.log(`邮箱: ${person.email}`);
    }
}
```

### 5.2 实用的自定义类型守卫


```typescript
// 检查是否为非空值
function isNotNull<T>(value: T | null | undefined): value is T {
    return value !== null && value !== undefined;
}

// 检查是否为字符串数组
function isStringArray(value: any): value is string[] {
    return Array.isArray(value) && value.every(item => typeof item === 'string');
}

// 使用示例
function processValue(value: string | null) {
    if (isNotNull(value)) {
        // 这里 value 被确认不是 null，可以安全使用
        console.log(value.toUpperCase());
    }
}

function processArray(data: unknown) {
    if (isStringArray(data)) {
        // 这里 data 被确认是 string[]
        data.forEach(str => console.log(str.length));
    }
}
```

---

## 6. 🎭 类型断言 - as 语法


### 6.1 基本概念

类型断言就是**告诉 TypeScript："相信我，我知道这个变量是什么类型"**。

```typescript
// 基本语法
let someValue: unknown = "这是一个字符串";
let strLength: number = (someValue as string).length;

// 或者使用 <> 语法（在 JSX 中不推荐）
let strLength2: number = (<string>someValue).length;
```

### 6.2 常见应用场景


**场景1：DOM 元素操作**
```typescript
// 获取 DOM 元素时的类型断言
const button = document.getElementById('myButton') as HTMLButtonElement;
const input = document.querySelector('.my-input') as HTMLInputElement;

// 现在可以安全地使用特定类型的属性
button.disabled = true;
input.value = 'Hello';
```

**场景2：API 响应处理**
```typescript
interface ApiResponse {
    id: number;
    name: string;
}

async function fetchData() {
    const response = await fetch('/api/data');
    const data = await response.json();
    
    // 断言 API 返回的数据符合我们的接口
    const typedData = data as ApiResponse;
    console.log(typedData.name); // 可以安全使用
}
```

### 6.3 双重断言（谨慎使用）


```typescript
// 当 TypeScript 认为断言不安全时，可能需要双重断言
interface Cat { meow(): void; }
interface Dog { bark(): void; }

let pet = {} as Dog;
// let cat = pet as Cat;  // ❌ 错误：Dog 和 Cat 没有重叠

// 双重断言（不推荐，但有时必要）
let cat = (pet as unknown) as Cat; // ✅ 可以，但要确保你知道在做什么
```

---

## 7. ❗ 非空断言 - ! 操作符


### 7.1 基本概念

非空断言告诉 TypeScript："这个值肯定不是 `null` 或 `undefined`，请忽略空值检查"。

```typescript
function processUser(user?: { name: string; email: string }) {
    // 不使用非空断言
    if (user) {
        console.log(user.name); // 需要先检查
    }
    
    // 使用非空断言（确定 user 存在时）
    console.log(user!.name); // 直接使用，跳过空值检查
}
```

### 7.2 实际应用场景


**场景1：数组操作**
```typescript
const numbers = [1, 2, 3, 4, 5];

// find 方法返回 number | undefined
const found = numbers.find(n => n > 3);

// 如果确定能找到，使用非空断言
console.log(found!.toString()); // 确定 found 不是 undefined
```

**场景2：DOM 操作**
```typescript
// 确定元素存在时使用非空断言
const button = document.getElementById('submit-btn')!;
button.addEventListener('click', () => {
    console.log('按钮被点击');
});
```

### 7.3 ⚠️ 使用注意事项


```typescript
// ❌ 危险用法：不确定值是否存在
function riskyFunction(value?: string) {
    console.log(value!.length); // 如果 value 是 undefined，会运行时报错
}

// ✅ 安全用法：确定值存在后使用
function safeFunction(value?: string) {
    if (value) {
        console.log(value.length); // 先检查再使用
    }
    // 或者在确定存在的情况下使用非空断言
}
```

---

## 8. 📋 核心要点总结


### 8.1 类型守卫对比表


| 类型守卫 | **使用场景** | **示例** | **优缺点** |
|---------|------------|---------|-----------|
| 🔍 **typeof** | `基础类型检查` | `typeof x === 'string'` | `简单，只能检查基础类型` |
| 🏗️ **instanceof** | `类实例检查` | `obj instanceof Date` | `适合类和内置对象` |
| 🔑 **in** | `属性存在检查` | `'name' in obj` | `灵活，适合接口区分` |
| 🎯 **自定义守卫** | `复杂类型判断` | `function isUser(x): x is User` | `最灵活，需要自己实现` |

### 8.2 类型断言使用指南


```typescript
// ✅ 好的使用场景
const element = document.getElementById('btn') as HTMLButtonElement;
const response = await api.getData() as ApiResponse;

// ⚠️ 谨慎使用场景
const value = userInput as number; // 用户输入不一定是数字

// ❌ 避免的用法
const dangerous = {} as ComplexInterface; // 空对象断言为复杂接口
```

### 8.3 最佳实践原则


**🎯 选择合适的守卫类型**
```
基础类型 → typeof
类实例 → instanceof  
对象属性 → in 操作符
复杂逻辑 → 自定义守卫
```

**⚠️ 安全使用断言**
```
确定性高的场景 → as 断言
DOM 操作 → 非空断言 !
不确定的数据 → 避免断言，使用守卫
```

**💡 记忆口诀**
```
typeof 问类型，instanceof 问出身
in 问属性，自定义问逻辑
断言要谨慎，守卫更安全
非空叹号用，确定才可行
```

### 8.4 实际开发建议


1. **优先使用类型守卫**而不是类型断言
2. **类型断言主要用于你比 TypeScript 更了解类型的场景**
3. **非空断言要确保值真的不为空**
4. **复杂的类型判断逻辑封装成自定义守卫函数**
5. **在团队开发中统一类型守卫的使用规范**