---
title: 3、映射类型基础
---
## 📚 目录

1. [映射类型是什么](#1-映射类型是什么)
2. [映射类型基础语法](#2-映射类型基础语法)
3. [keyof在映射类型中的应用](#3-keyof在映射类型中的应用)
4. [映射修饰符的使用](#4-映射修饰符的使用)
5. [常用映射模式](#5-常用映射模式)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔍 映射类型是什么


### 1.1 通俗理解映射类型


想象一下你有一个用户信息表，现在要创建一个新表，把原表的每个字段都变成可选的：

```
原用户表：                    新用户表（全部可选）：
name: string                 name?: string
age: number          →       age?: number  
email: string                email?: string
```

**映射类型就像一个"类型转换器"**，它能按照你设定的规则，把一个类型的所有属性"转换"成新的形式。

### 1.2 为什么需要映射类型


**🎯 解决的核心问题**：
- **避免重复定义**：不用手动写两套相似的类型
- **自动同步更新**：原类型变化时，映射类型自动跟着变
- **批量处理属性**：一次性处理所有属性，而不是一个个手写

```typescript
// ❌ 不用映射类型 - 重复且容易出错
interface User {
  name: string;
  age: number;
  email: string;
}

// 手动创建可选版本，容易漏掉字段
interface PartialUser {
  name?: string;
  age?: number;
  // 忘记写email了！
}

// ✅ 用映射类型 - 自动且准确
type PartialUser = {
  [K in keyof User]?: User[K];
}
```

### 1.3 映射类型的本质


**核心理念**：
```
映射类型 = 遍历原类型的每个属性 + 应用转换规则
```

就像数组的 `map` 方法一样，映射类型对原类型的每个属性都"执行一遍"你指定的操作。

---

## 2. ⚙️ 映射类型基础语法


### 2.1 基础语法结构


```typescript
type 新类型名 = {
  [K in keyof 原类型]: 新属性类型
}
```

**语法拆解**：
```
[K in keyof T]  ←  这部分是"遍历器"
    ↓
K              ←  当前属性名（键）
keyof T        ←  原类型的所有属性名
新属性类型      ←  每个属性要变成什么类型
```

### 2.2 最简单的例子


```typescript
// 原始类型
interface Person {
  name: string;
  age: number;
}

// 映射类型：把所有属性都变成字符串
type StringPerson = {
  [K in keyof Person]: string;
}

// 结果等价于：
// type StringPerson = {
//   name: string;
//   age: string;
// }
```

### 2.3 保持原类型不变


```typescript
// 最基础的映射：完全复制原类型
type CopyPerson = {
  [K in keyof Person]: Person[K];
}

// 等价于原始的Person类型
```

### 2.4 执行过程图解


```
原类型 Person:
┌─────────────┐
│ name: string│  ──┐
│ age: number │    │  遍历每个属性
└─────────────┘    │
                   ↓
               映射处理过程:
               ┌──────────────────┐
               │ K = "name"       │
               │ Person[K] = string│ → name: string
               ├──────────────────┤
               │ K = "age"        │
               │ Person[K] = number│ → age: string
               └──────────────────┘
                        ↓
               新类型 StringPerson:
               ┌─────────────┐
               │ name: string│
               │ age: string │
               └─────────────┘
```

---

## 3. 🔑 keyof在映射类型中的应用


### 3.1 keyof的作用回顾


```typescript
interface User {
  id: number;
  name: string;
  email: string;
}

// keyof User = "id" | "name" | "email"
type UserKeys = keyof User;
```

### 3.2 在映射类型中的核心作用


**`keyof T` 提供了"要遍历的属性名列表"**：

```typescript
type MyMapped<T> = {
  [K in keyof T]: T[K];  // K 会依次是 T 的每个属性名
}

// 使用时：
type Result = MyMapped<User>;
// K 依次为 "id", "name", "email"
```

### 3.3 实际应用场景


**场景1：创建只读版本**
```typescript
interface Settings {
  theme: string;
  language: string;
  autoSave: boolean;
}

// 创建只读版本
type ReadonlySettings = {
  readonly [K in keyof Settings]: Settings[K];
}

// 使用
const config: ReadonlySettings = {
  theme: "dark",
  language: "zh",
  autoSave: true
};
// config.theme = "light"; // ❌ 报错：只读属性
```

**场景2：类型转换**
```typescript
// 把所有属性都变成可选的字符串
type OptionalStrings<T> = {
  [K in keyof T]?: string;
}

type UserForm = OptionalStrings<User>;
// 结果：{ id?: string; name?: string; email?: string; }
```

### 3.4 keyof的限制和注意事项


```typescript
// ⚠️ keyof 只获取对象类型的属性
type StringKeys = keyof string; // number | "toString" | "charAt" | ...
type NumberKeys = keyof number; // "toString" | "toFixed" | ...

// ✅ 主要用于对象类型
interface Obj {
  a: string;
  b: number;
}
type ObjKeys = keyof Obj; // "a" | "b"
```

---

## 4. 🔧 映射修饰符的使用


### 4.1 什么是映射修饰符


映射修饰符就是在映射过程中，给属性"加上"或"去掉"某些特性：

```
readonly  ←  只读修饰符
?         ←  可选修饰符
```

### 4.2 添加修饰符


**添加只读修饰符**：
```typescript
interface User {
  name: string;
  age: number;
}

// 添加 readonly
type ReadonlyUser = {
  readonly [K in keyof User]: User[K];
}

// 结果：
// type ReadonlyUser = {
//   readonly name: string;
//   readonly age: number;
// }
```

**添加可选修饰符**：
```typescript
// 添加 ?
type PartialUser = {
  [K in keyof User]?: User[K];
}

// 结果：
// type PartialUser = {
//   name?: string;
//   age?: number;
// }
```

**同时添加两个修饰符**：
```typescript
type ReadonlyPartialUser = {
  readonly [K in keyof User]?: User[K];
}

// 结果：既只读又可选
// type ReadonlyPartialUser = {
//   readonly name?: string;
//   readonly age?: number;
// }
```

### 4.3 移除修饰符


如果原类型已经有修饰符，可以用 `-` 号移除：

```typescript
interface OptionalUser {
  name?: string;      // 可选
  age?: number;       // 可选
  readonly id: string; // 只读
}

// 移除可选修饰符，变成必需
type RequiredUser = {
  [K in keyof OptionalUser]-?: OptionalUser[K];
}

// 移除只读修饰符
type MutableUser = {
  -readonly [K in keyof OptionalUser]: OptionalUser[K];
}

// 同时移除两种修饰符
type PlainUser = {
  -readonly [K in keyof OptionalUser]-?: OptionalUser[K];
}
```

### 4.4 修饰符操作对比表


| 操作 | 语法 | 效果 | 示例 |
|------|------|------|------|
| **添加只读** | `readonly [K in keyof T]` | 所有属性变只读 | `name: string` → `readonly name: string` |
| **添加可选** | `[K in keyof T]?` | 所有属性变可选 | `name: string` → `name?: string` |
| **移除只读** | `-readonly [K in keyof T]` | 移除只读限制 | `readonly name: string` → `name: string` |
| **移除可选** | `[K in keyof T]-?` | 所有属性变必需 | `name?: string` → `name: string` |

---

## 5. 🛠️ 常用映射模式


### 5.1 内置工具类型的实现


TypeScript 提供的很多工具类型，底层就是用映射类型实现的：

```typescript
// Partial<T> - 所有属性变可选
type MyPartial<T> = {
  [K in keyof T]?: T[K];
}

// Required<T> - 所有属性变必需
type MyRequired<T> = {
  [K in keyof T]-?: T[K];
}

// Readonly<T> - 所有属性变只读
type MyReadonly<T> = {
  readonly [K in keyof T]: T[K];
}
```

### 5.2 实用的自定义映射模式


**模式1：类型转换器**
```typescript
// 把所有属性都变成字符串
type Stringify<T> = {
  [K in keyof T]: string;
}

// 把所有属性都变成数组
type Arrayify<T> = {
  [K in keyof T]: T[K][];
}

interface User {
  name: string;
  age: number;
}

type StringUser = Stringify<User>;
// { name: string; age: string; }

type ArrayUser = Arrayify<User>;
// { name: string[]; age: number[]; }
```

**模式2：条件映射**
```typescript
// 只保留字符串类型的属性
type StringKeysOnly<T> = {
  [K in keyof T]: T[K] extends string ? T[K] : never;
}

interface Mixed {
  name: string;    // ✅ 保留
  age: number;     // ❌ 变成never
  email: string;   // ✅ 保留
}

type OnlyStrings = StringKeysOnly<Mixed>;
// { name: string; age: never; email: string; }
```

**模式3：属性名转换**
```typescript
// 给所有属性名加前缀
type AddPrefix<T, P extends string> = {
  [K in keyof T as `${P}${string & K}`]: T[K];
}

type PrefixedUser = AddPrefix<User, "user_">;
// { user_name: string; user_age: number; }
```

### 5.3 实际开发中的应用场景


**场景1：API响应类型处理**
```typescript
// 服务器返回的数据（所有字段都可能为空）
interface ServerUser {
  name: string | null;
  age: number | null;
  email: string | null;
}

// 处理后的客户端数据（移除null）
type ClientUser = {
  [K in keyof ServerUser]: NonNullable<ServerUser[K]>;
}
// { name: string; age: number; email: string; }
```

**场景2：表单状态管理**
```typescript
interface FormData {
  username: string;
  password: string;
  email: string;
}

// 表单错误状态
type FormErrors = {
  [K in keyof FormData]?: string; // 每个字段的错误信息
}

// 表单验证状态
type FormValidation = {
  [K in keyof FormData]: boolean; // 每个字段是否验证通过
}
```

**场景3：配置对象处理**
```typescript
interface AppConfig {
  database: {
    host: string;
    port: number;
  };
  redis: {
    url: string;
  };
}

// 创建配置的环境变量版本（所有值都是字符串）
type EnvConfig = {
  [K in keyof AppConfig]: AppConfig[K] extends object 
    ? { [P in keyof AppConfig[K]]: string }
    : string;
}
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 映射类型本质：类型级别的"遍历+转换"操作
🔸 基础语法：{ [K in keyof T]: 新类型 }
🔸 keyof作用：提供要遍历的属性名列表
🔸 修饰符操作：readonly和?的添加/移除
🔸 实用模式：类型转换、属性过滤、名称变换
```

### 6.2 关键理解要点


**🔹 映射类型的思维方式**
```
不要把映射类型想得太复杂：
1. 拿到原类型的所有属性名
2. 遍历每个属性名
3. 对每个属性应用转换规则
4. 组成新的类型
```

**🔹 keyof的核心价值**
```
keyof让我们能够：
- 获取类型的所有属性名
- 在编译时进行类型级别的遍历
- 确保映射的完整性和类型安全
```

**🔹 修饰符的灵活运用**
```
记住规律：
+ 号或直接写 = 添加修饰符
- 号 = 移除修饰符
? = 可选
readonly = 只读
```

### 6.3 实际应用价值


**📊 开发效率提升**
- **减少重复代码**：一次定义，多种变形
- **自动同步更新**：原类型变化时映射类型自动跟进
- **类型安全保障**：编译时确保转换的正确性

**🎯 常见使用场景**
- **API数据处理**：服务端→客户端类型转换
- **表单状态管理**：数据→错误状态→验证状态
- **配置对象变换**：开发配置→生产配置
- **组件Props处理**：必需→可选、普通→只读

### 6.4 学习建议


**🔄 循序渐进的练习路径**
1. **基础练习**：从简单的Partial、Required开始
2. **修饰符练习**：熟练掌握readonly和?的添加移除
3. **条件映射**：结合extends进行条件类型转换
4. **实际项目**：在真实项目中找到应用场景

**💡 理解要点**
- 映射类型是"批量操作"，不是"单个处理"
- 重点理解语法中每个部分的作用
- 多练习，从模仿内置工具类型开始
- 在实际项目中寻找应用场景

**核心记忆口诀**：
```
映射类型像工厂，原类型进新类型出
keyof提供属性名，in关键字来遍历
修饰符加减很灵活，readonly问号要记清
实用模式多练习，开发效率大提升
```