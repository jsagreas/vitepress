---
title: 4、DOM遍历与查找
---
## 📚 目录

1. [DOM遍历概述](#1-DOM遍历概述)
2. [向下查找后代](#2-向下查找后代)
3. [向上查找祖先](#3-向上查找祖先)
4. [平级查找兄弟](#4-平级查找兄弟)
5. [元素筛选与判断](#5-元素筛选与判断)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔍 DOM遍历概述


### 1.1 什么是DOM遍历

**简单理解**：就是在页面元素之间"走来走去"，找到你想要的元素

```
DOM结构就像家族关系：
        爷爷
       /    \
     爸爸    叔叔
    /  \    /  \
  我   弟弟 堂哥 堂弟

遍历就是：从"我"出发，找到爷爷、爸爸、弟弟、堂哥等
```

### 1.2 为什么需要遍历

**实际场景**：
- 点击按钮，找到对应的输入框
- 选择列表项，高亮同级其他项
- 操作子元素，需要先找到父容器

**核心优势**：
- 🎯 **精确定位** - 基于关系查找，更准确
- 🚀 **动态查找** - 结构变化时依然有效
- 💡 **代码简洁** - 一行代码搞定复杂查找

---

## 2. ⬇️ 向下查找后代


### 2.1 `.find()` - 查找所有后代

**作用**：在当前元素内部查找所有匹配的子孙元素

```javascript
// HTML结构
<div class="container">
    <p>段落1</p>
    <div class="inner">
        <p>段落2</p>
        <span>文本</span>
    </div>
</div>

// 查找所有后代p元素
$('.container').find('p');  // 找到段落1和段落2
$('.container').find('.inner');  // 找到inner div
```

### 2.2 `.children()` - 查找直接子元素

**作用**：只查找直接子元素，不包括孙子、重孙等

```javascript
// 只找直接子元素
$('.container').children();     // 找到p和div.inner
$('.container').children('p');  // 只找到段落1

// 对比理解
$('.container').find('p');      // 找到段落1和段落2（所有后代）
$('.container').children('p');  // 只找到段落1（直接子元素）
```

**记忆技巧**：
- `find()` = 找**所有子孙**（家族所有人）
- `children()` = 找**亲儿子**（只找下一代）

---

## 3. ⬆️ 向上查找祖先


### 3.1 `.parent()` - 查找直接父元素

**作用**：查找当前元素的直接父元素

```javascript
// HTML结构
<div class="wrapper">
    <div class="container">
        <p id="target">目标元素</p>
    </div>
</div>

// 向上查找
$('#target').parent();        // 找到div.container
$('#target').parent('.container'); // 找到div.container（带条件）
```

### 3.2 `.parents()` - 查找所有祖先

**作用**：查找所有上级元素，直到文档根部

```javascript
// 查找所有祖先
$('#target').parents();          // 找到container、wrapper、body、html
$('#target').parents('.wrapper'); // 只找class为wrapper的祖先
$('#target').parents('div');     // 找到所有div类型的祖先
```

### 3.3 `.closest()` - 查找最近祖先 ⭐重要

**作用**：向上查找第一个匹配的祖先元素（包括自己）

```javascript
// 最实用的向上查找方法
$('#target').closest('.container'); // 找到最近的container
$('#target').closest('form');       // 找到最近的表单

// 实际应用：事件委托
$('.delete-btn').click(function() {
    $(this).closest('tr').remove(); // 删除最近的表格行
});
```

**三者区别对比**：
```
parent()   - 只找爸爸
parents()  - 找所有长辈
closest()  - 找最近的匹配长辈（最常用）
```

---

## 4. ↔️ 平级查找兄弟


### 4.1 `.siblings()` - 查找所有兄弟

**作用**：查找同级的其他元素

```javascript
// HTML结构
<ul>
    <li class="item1">项目1</li>
    <li class="item2 active">项目2</li>
    <li class="item3">项目3</li>
</ul>

// 查找兄弟元素
$('.active').siblings();           // 找到item1和item3
$('.active').siblings('.item1');   // 只找到item1
```

### 4.2 `.next()` 和 `.prev()` - 相邻兄弟

**作用**：查找紧挨着的下一个或上一个元素

```javascript
// 查找相邻元素
$('.active').next();     // 找到item3（下一个）
$('.active').prev();     // 找到item1（上一个）

// 带条件查找
$('.active').next('li'); // 下一个li元素
$('.active').prev('.item1'); // 上一个class为item1的元素
```

**实际应用场景**：
```javascript
// 选项卡切换
$('.tab').click(function() {
    $(this).addClass('active')
           .siblings().removeClass('active'); // 兄弟去掉active类
});

// 表单导航：上一步/下一步
$('.next-btn').click(function() {
    $(this).closest('.step').hide()
           .next('.step').show(); // 显示下一步
});
```

---

## 5. 🎯 元素筛选与判断


### 5.1 位置筛选方法

```javascript
// HTML：多个li元素
$('li').first();    // 第一个li
$('li').last();     // 最后一个li
$('li').eq(0);      // 第1个li（索引从0开始）
$('li').eq(2);      // 第3个li
$('li').eq(-1);     // 最后一个li（负数从后数）
```

### 5.2 `.filter()` - 条件筛选

**作用**：从已选择的元素中，筛选出符合条件的

```javascript
// 从所有li中筛选有active类的
$('li').filter('.active');

// 从所有input中筛选type为text的
$('input').filter('[type="text"]');

// 使用函数筛选
$('li').filter(function() {
    return $(this).text().length > 5; // 筛选文本长度大于5的
});
```

### 5.3 `.is()` - 判断是否匹配 ⭐实用

**作用**：判断元素是否匹配某个选择器，返回true/false

```javascript
// 判断元素状态
if ($('#checkbox').is(':checked')) {
    console.log('复选框被选中');
}

if ($('.btn').is(':disabled')) {
    console.log('按钮被禁用');
}

// 判断类名
if ($(this).is('.active')) {
    console.log('有active类');
}
```

**实际应用**：
```javascript
// 表单验证
$('input').blur(function() {
    if ($(this).is(':empty') && $(this).is('[required]')) {
        $(this).addClass('error');
    }
});

// 条件操作
$('.item').click(function() {
    if ($(this).is('.locked')) {
        alert('此项目已锁定');
        return;
    }
    // 执行正常操作
});
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的遍历方法


| 方向 | **方法** | **作用** | **使用频率** |
|------|---------|----------|-------------|
| ⬇️ **向下** | `.find()` | `查找所有后代` | ⭐⭐⭐⭐⭐ |
| ⬇️ **向下** | `.children()` | `查找直接子元素` | ⭐⭐⭐⭐ |
| ⬆️ **向上** | `.closest()` | `查找最近祖先` | ⭐⭐⭐⭐⭐ |
| ⬆️ **向上** | `.parent()` | `查找直接父元素` | ⭐⭐⭐ |
| ↔️ **平级** | `.siblings()` | `查找兄弟元素` | ⭐⭐⭐⭐ |
| ↔️ **平级** | `.next()/.prev()` | `查找相邻元素` | ⭐⭐⭐ |

### 6.2 关键理解要点


**🔸 选择合适的方法**
```javascript
// ✅ 推荐：精确查找
$(this).closest('form').find('input[type="text"]');

// ❌ 不推荐：全局查找
$('form input[type="text"]'); // 可能找到多个表单的输入框
```

**🔸 遍历与选择器的区别**
```
选择器：直接定位元素（静态）
遍历：  基于关系查找（动态）

使用场景：
- 固定结构 → 用选择器
- 动态结构 → 用遍历
```

**🔸 性能考虑**
```javascript
// ✅ 好的做法：缓存jQuery对象
var $container = $('.container');
$container.find('.item');
$container.children('.header');

// ❌ 避免：重复查找
$('.container').find('.item');
$('.container').children('.header');
```

### 6.3 实际应用场景


**📝 表单操作**
```javascript
// 找到表单中所有必填字段
$('form').find('[required]').each(function() {
    // 验证逻辑
});
```

**🎯 列表操作**
```javascript
// 点击列表项，高亮当前，取消其他
$('.list-item').click(function() {
    $(this).addClass('active')
           .siblings().removeClass('active');
});
```

**🎪 导航操作**
```javascript
// 面包屑导航
$('.breadcrumb').on('click', 'a', function() {
    $(this).nextAll().remove(); // 移除后面所有链接
});
```

### 6.4 记忆技巧


**🎯 方向记忆**
- `find/children` = 往**下**找（子孙）
- `parent/closest` = 往**上**找（祖先）  
- `siblings/next/prev` = **平级**找（兄弟）

**⚡ 使用频率**
- **最常用**：`find()` `closest()` `siblings()`
- **次常用**：`children()` `parent()` `next()/prev()`
- **按需用**：`parents()` `filter()` `is()`

**核心原则**：
- DOM遍历是jQuery的核心优势，必须熟练掌握
- 选择合适的方法，让代码更简洁高效
- 结合实际业务场景，灵活运用各种遍历方法