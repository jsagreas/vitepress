---
title: 3、属性与表单选择器
---
## 📚 目录

1. [属性选择器](#1-属性选择器)
2. [表单元素选择器](#2-表单元素选择器)
3. [表单状态选择器](#3-表单状态选择器)
4. [实战应用场景](#4-实战应用场景)
5. [核心要点总结](#5-核心要点总结)

---

## 1. 🎯 属性选择器


### 1.1 基础属性选择器


**🔸 有无属性判断**
```javascript
// 选择有title属性的元素
$("[title]")

// 选择有data-id属性的元素  
$("[data-id]")
```

**💡 实际应用**
```html
<!-- HTML结构 -->
<div title="提示信息">有提示的div</div>
<div>没有提示的div</div>
<img src="logo.jpg" alt="logo" title="网站logo">

<script>
// 给所有有title属性的元素添加样式
$("[title]").css("border", "2px solid red");
</script>
```

### 1.2 属性值精确匹配


**🔸 完全匹配**
```javascript
// 选择type='text'的元素
$("[type='text']")

// 选择class='btn'的元素
$("[class='btn']")
```

**💡 常见用法**
```html
<!-- HTML表单 -->
<input type="text" name="username">
<input type="password" name="pwd"> 
<input type="email" name="email">
<button class="btn">提交</button>

<script>
// 只给文本输入框添加样式
$("[type='text']").addClass("highlight");

// 给指定class的按钮绑定事件
$("[class='btn']").click(function(){
    alert("按钮被点击");
});
</script>
```

### 1.3 属性值包含匹配


**🔸 包含指定值**
```javascript
// 选择class包含'btn'的元素
$("[class*='btn']")

// 选择src包含'icon'的图片
$("img[src*='icon']")
```

**💡 实际场景**
```html
<!-- 多个CSS类名的情况 -->
<button class="btn btn-primary">主要按钮</button>
<button class="btn btn-secondary">次要按钮</button>
<img src="icon-home.png" alt="首页">
<img src="icon-user.png" alt="用户">

<script>
// 选择所有包含'btn'的按钮
$("[class*='btn']").hover(function(){
    $(this).addClass("hover-effect");
});

// 选择所有图标图片
$("img[src*='icon']").css("width", "32px");
</script>
```

---

## 2. 📝 表单元素选择器


### 2.1 通用表单选择器


**🔸 所有表单元素**
```javascript
// 选择页面中所有表单控件
$(":input")
```

**💡 包含范围**
```
:input 选择器包含：
• <input> 所有类型
• <textarea> 文本域
• <select> 下拉框
• <button> 按钮
```

**📋 对比差异**
```javascript
// 只选择input标签
$("input")           // <input type="text">, <input type="checkbox">

// 选择所有表单控件
$(":input")          // 上面的 + <textarea>, <select>, <button>
```

### 2.2 输入类型选择器


**🔸 常用输入类型**
```javascript
$(":text")           // 文本输入框
$(":password")       // 密码输入框  
$(":checkbox")       // 复选框
$(":radio")          // 单选框
$(":submit")         // 提交按钮
```

**💻 实用示例**
```html
<!-- 登录表单 -->
<form id="loginForm">
    <input type="text" name="username" placeholder="用户名">
    <input type="password" name="password" placeholder="密码">
    <input type="checkbox" name="remember"> 记住密码
    <input type="submit" value="登录">
</form>

<script>
// 文本框获得焦点时高亮
$(":text").focus(function(){
    $(this).css("background", "#e6f3ff");
});

// 密码框失去焦点时恢复
$(":password").blur(function(){
    $(this).css("background", "white");
});

// 提交按钮点击处理
$(":submit").click(function(){
    alert("准备提交表单");
});
</script>
```

---

## 3. ✅ 表单状态选择器


### 3.1 选中状态选择器


**🔸 复选框和单选框**
```javascript
$(":checked")        // 被选中的复选框/单选框
```

**💡 实际应用**
```html
<!-- 购物车选择 -->
<div class="cart-items">
    <label><input type="checkbox" value="1"> 商品1 - ¥100</label>
    <label><input type="checkbox" value="2" checked> 商品2 - ¥200</label>
    <label><input type="checkbox" value="3"> 商品3 - ¥150</label>
</div>
<div>总价：<span id="total">0</span>元</div>

<script>
// 计算选中商品总价
function calculateTotal() {
    let total = 0;
    $(":checked").each(function(){
        total += parseInt($(this).val()) * 100; // 假设value*100为价格
    });
    $("#total").text(total);
}

// 复选框状态改变时重新计算
$(":checkbox").change(calculateTotal);
</script>
```

### 3.2 下拉选择状态


**🔸 下拉框选中项**
```javascript
$(":selected")       // 下拉框中被选中的选项
```

**📋 使用场景**
```html
<!-- 城市选择器 -->
<select id="citySelect">
    <option value="">请选择城市</option>
    <option value="beijing">北京</option>
    <option value="shanghai" selected>上海</option>
    <option value="guangzhou">广州</option>
</select>

<script>
// 获取当前选中的城市
$("#citySelect").change(function(){
    let selectedCity = $(":selected", this).text();
    console.log("选择的城市：" + selectedCity);
});
</script>
```

### 3.3 可用状态选择器


**🔸 启用和禁用状态**
```javascript
$(":enabled")        // 可用的表单元素
$(":disabled")       // 禁用的表单元素
```

**⚡ 动态控制示例**
```html
<!-- 用户信息表单 -->
<form>
    <input type="text" name="username" placeholder="用户名">
    <input type="email" name="email" placeholder="邮箱" disabled>
    <button type="button" id="enableEmail">启用邮箱</button>
</form>

<script>
// 启用邮箱输入框
$("#enableEmail").click(function(){
    $(":disabled").prop("disabled", false);
    $(this).text("邮箱已启用").prop("disabled", true);
});

// 给所有可用输入框添加边框
$(":enabled").css("border", "1px solid #ccc");
</script>
```

---

## 4. 🚀 实战应用场景


### 4.1 表单验证场景


**📝 必填字段验证**
```html
<form id="userForm">
    <input type="text" name="name" required placeholder="姓名*">
    <input type="email" name="email" required placeholder="邮箱*">
    <input type="tel" name="phone" placeholder="电话(可选)">
    <button type="submit">提交</button>
</form>

<script>
$("#userForm").submit(function(e){
    // 检查所有必填字段
    let emptyFields = $("[required]").filter(function(){
        return $(this).val().trim() === "";
    });
    
    if(emptyFields.length > 0) {
        e.preventDefault();
        emptyFields.css("border", "2px solid red");
        alert("请填写所有必填字段");
    }
});
</script>
```

### 4.2 批量操作场景


**🔄 全选/反选功能**
```html
<div class="product-list">
    <label><input type="checkbox" id="selectAll"> 全选</label>
    <label><input type="checkbox" name="product" value="1"> 产品1</label>
    <label><input type="checkbox" name="product" value="2"> 产品2</label>
    <label><input type="checkbox" name="product" value="3"> 产品3</label>
    <button id="deleteSelected">删除选中</button>
</div>

<script>
// 全选/反选
$("#selectAll").change(function(){
    $("[name='product']").prop("checked", $(this).prop("checked"));
});

// 删除选中项
$("#deleteSelected").click(function(){
    let selected = $(":checked[name='product']");
    if(selected.length === 0) {
        alert("请先选择要删除的项目");
        return;
    }
    
    selected.each(function(){
        console.log("删除产品：" + $(this).val());
    });
});
</script>
```

### 4.3 动态表单场景


**📊 条件显示字段**
```html
<form>
    <label>
        <input type="radio" name="userType" value="individual"> 个人用户
    </label>
    <label>
        <input type="radio" name="userType" value="company"> 企业用户
    </label>
    
    <!-- 企业用户专属字段 -->
    <div id="companyFields" style="display:none;">
        <input type="text" name="companyName" placeholder="公司名称">
        <input type="text" name="taxId" placeholder="税号">
    </div>
</form>

<script>
$("[name='userType']").change(function(){
    if($(this).val() === "company" && $(":checked[name='userType']").val() === "company") {
        $("#companyFields").show();
        $("#companyFields :input").prop("required", true);
    } else {
        $("#companyFields").hide();
        $("#companyFields :input").prop("required", false);
    }
});
</script>
```

---

## 5. 📋 核心要点总结


### 5.1 必须掌握的选择器


```
🔸 属性选择器核心：
• $("[attribute]") - 有某属性的元素
• $("[attribute='value']") - 属性值完全匹配
• $("[attribute*='value']") - 属性值包含匹配

🔸 表单选择器核心：
• $(":input") - 所有表单控件
• $(":text") $(":password") - 常用输入类型
• $(":checked") $(":selected") - 选中状态
• $(":disabled") $(":enabled") - 可用状态
```

### 5.2 使用场景记忆


| 选择器类型 | **典型场景** | **代码示例** |
|-----------|-------------|-------------|
| **属性选择器** | `数据属性操作` | `$("[data-id]").hide()` |
| **表单类型** | `输入框样式` | `$(":text").addClass("form-control")` |
| **选中状态** | `计算总价` | `$(":checked").each(function(){...})` |
| **可用状态** | `动态启用` | `$(":disabled").prop("disabled", false)` |

### 5.3 实际工作要点


**🎯 性能优化建议**
```javascript
// ❌ 避免过于宽泛的选择
$(":input").css("border", "1px solid #ccc");

// ✅ 限定范围提高性能
$("#myForm :input").css("border", "1px solid #ccc");
```

**⚡ 常见组合用法**
```javascript
// 选中的复选框值
$(":checkbox:checked").map(function(){
    return $(this).val();
}).get();

// 表单中的必填字段
$("form [required]").each(function(){
    // 验证逻辑
});
```

**💡 核心记忆**
- **属性选择器**：找有特定属性或属性值的元素
- **表单选择器**：快速定位各种表单控件
- **状态选择器**：处理选中、禁用等动态状态
- **实战组合**：多个选择器组合使用解决复杂需求
