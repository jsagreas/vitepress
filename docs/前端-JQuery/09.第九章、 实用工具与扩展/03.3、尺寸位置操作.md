---
title: 3、尺寸位置操作
---
## 📚 目录

1. [基础尺寸操作](#1-基础尺寸操作)
2. [高级尺寸控制](#2-高级尺寸控制)
3. [位置获取与定位](#3-位置获取与定位)
4. [滚动位置控制](#4-滚动位置控制)
5. [实战应用场景](#5-实战应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 📏 基础尺寸操作


### 1.1 width() 和 height() - 最常用的尺寸方法


**🔸 基本用法**
```javascript
// 获取元素宽度和高度
var boxWidth = $('#box').width();    // 获取内容区域宽度
var boxHeight = $('#box').height();  // 获取内容区域高度

// 设置元素宽度和高度
$('#box').width(300);    // 设置宽度为300px
$('#box').height(200);   // 设置高度为200px
```

**💡 理解要点**
```
width() 和 height() 只包含内容区域的尺寸
不包含 padding、border、margin

例如：一个div实际显示200px宽，但如果有padding: 10px
那么 .width() 返回的是 180px（200 - 10*2）
```

### 1.2 实际使用场景


**📊 动态调整元素大小**
```javascript
// 根据内容动态调整容器高度
function adjustHeight() {
    var contentHeight = $('.content').height();
    $('.container').height(contentHeight + 50); // 多留50px间距
}

// 等比例缩放图片
$('.image').each(function() {
    var originalWidth = $(this).width();
    $(this).width(originalWidth * 0.8); // 缩小到80%
});
```

---

## 2. 📐 高级尺寸控制


### 2.1 innerWidth() 和 innerHeight() - 包含padding


**🔸 作用说明**
```
innerWidth() = 内容宽度 + 左右padding
innerHeight() = 内容高度 + 上下padding

实际开发中用来获取"实际可用空间"
```

**💻 代码示例**
```javascript
// 假设一个div样式为：width: 100px; padding: 10px;
$('#box').width();      // 返回 100px（纯内容区）
$('#box').innerWidth(); // 返回 120px（100 + 10*2）

// 常用场景：计算可用空间
var availableSpace = $('.container').innerWidth() - $('.sidebar').outerWidth();
```

### 2.2 outerWidth() 和 outerHeight() - 包含所有


**🔸 功能详解**
```
outerWidth() = 内容 + padding + border
outerWidth(true) = 内容 + padding + border + margin

这是元素真正占用的空间大小
```

**🎯 实用技巧**
```javascript
// 计算元素完整占用空间
var totalWidth = $('#element').outerWidth(true);

// 判断元素是否超出容器
if ($('.item').outerWidth() > $('.container').innerWidth()) {
    console.log('元素太宽了！');
}

// 居中定位计算
var leftPos = ($(window).width() - $('.modal').outerWidth()) / 2;
$('.modal').css('left', leftPos + 'px');
```

### 2.3 尺寸方法对比表格


| 方法 | **包含内容** | **使用场景** | **示例值** |
|------|-------------|-------------|-----------|
| `.width()` | `内容区域` | `设置元素大小` | `100px` |
| `.innerWidth()` | `内容 + padding` | `计算可用空间` | `120px` |
| `.outerWidth()` | `内容 + padding + border` | `布局计算` | `124px` |
| `.outerWidth(true)` | `内容 + padding + border + margin` | `完整占用空间` | `144px` |

---

## 3. 🎯 位置获取与定位


### 3.1 offset() - 相对文档的绝对位置


**🔸 基本概念**
```
offset() 返回元素相对于整个文档的位置
无论元素的定位方式是什么，都是相对于文档左上角计算
返回对象：{top: 值, left: 值}
```

**💻 实际应用**
```javascript
// 获取元素在页面中的绝对位置
var pos = $('#element').offset();
console.log('距离页面顶部：' + pos.top + 'px');
console.log('距离页面左侧：' + pos.left + 'px');

// 设置元素的绝对位置
$('#element').offset({
    top: 100,
    left: 200
});

// 常见用法：跟随鼠标的提示框
$(document).mousemove(function(e) {
    $('.tooltip').offset({
        top: e.pageY + 10,
        left: e.pageX + 10
    });
});
```

### 3.2 position() - 相对父元素的位置


**🔸 核心区别**
```
position() 返回元素相对于定位父元素的位置
只对设置了 position: relative/absolute 的元素有效
主要用于精确的布局控制
```

**🎯 使用场景**
```javascript
// 获取相对定位位置
var relativePos = $('.child').position();

// 在父容器内移动元素
$('.item').position({
    top: 50,
    left: 100
});

// 实用案例：下拉菜单定位
$('.dropdown-toggle').click(function() {
    var btnPos = $(this).position();
    $('.dropdown-menu').css({
        top: btnPos.top + $(this).outerHeight(),
        left: btnPos.left
    }).show();
});
```

### 3.3 offset() vs position() 对比


```
                    offset()              position()
相对于：           整个文档               定位父元素
返回值：           {top, left}          {top, left}  
设置位置：         可以                  可以
常用场景：         弹窗、提示框          下拉菜单、相对定位

选择原则：
- 需要在页面任意位置显示 → 用 offset()
- 需要在父容器内调整 → 用 position()
```

---

## 4. 📜 滚动位置控制


### 4.1 scrollTop() - 垂直滚动控制


**🔸 基础用法**
```javascript
// 获取滚动位置
var scrollPosition = $(window).scrollTop();   // 页面滚动位置
var divScroll = $('.container').scrollTop();  // 容器滚动位置

// 设置滚动位置
$(window).scrollTop(0);        // 回到页面顶部
$('.container').scrollTop(100); // 容器滚动到100px位置
```

**🚀 经典应用：返回顶部**
```javascript
// 返回顶部按钮
$('.back-to-top').click(function() {
    $('html, body').animate({scrollTop: 0}, 500);
});

// 滚动超过300px时显示返回顶部按钮
$(window).scroll(function() {
    if ($(this).scrollTop() > 300) {
        $('.back-to-top').fadeIn();
    } else {
        $('.back-to-top').fadeOut();
    }
});
```

### 4.2 scrollLeft() - 水平滚动控制


**🔸 使用场景**
```javascript
// 水平滚动容器控制
$('.horizontal-scroll').scrollLeft(200);

// 常用于图片轮播、选项卡滚动
$('.tab-next').click(function() {
    var currentScroll = $('.tab-container').scrollLeft();
    $('.tab-container').scrollLeft(currentScroll + 200);
});
```

### 4.3 滚动监听的实用技巧


**📊 滚动加载更多**
```javascript
$(window).scroll(function() {
    var scrollTop = $(this).scrollTop();
    var documentHeight = $(document).height();
    var windowHeight = $(this).height();
    
    // 滚动到底部附近时加载更多
    if (scrollTop + windowHeight >= documentHeight - 100) {
        loadMoreContent();
    }
});
```

**🎯 导航高亮跟随**
```javascript
$(window).scroll(function() {
    var scrollTop = $(this).scrollTop();
    
    $('.section').each(function() {
        var sectionTop = $(this).offset().top - 100;
        var sectionId = $(this).attr('id');
        
        if (scrollTop >= sectionTop) {
            $('.nav-link').removeClass('active');
            $('.nav-link[href="#' + sectionId + '"]').addClass('active');
        }
    });
});
```

---

## 5. 🛠️ 实战应用场景


### 5.1 响应式布局辅助


**📱 根据屏幕尺寸调整布局**
```javascript
function adjustLayout() {
    var windowWidth = $(window).width();
    
    if (windowWidth < 768) {
        // 移动端布局
        $('.sidebar').height('auto');
        $('.content').css('margin-left', 0);
    } else {
        // 桌面端布局
        var sidebarHeight = $('.sidebar').outerHeight();
        $('.content').css('min-height', sidebarHeight);
    }
}

// 页面加载和窗口大小改变时调整
$(document).ready(adjustLayout);
$(window).resize(adjustLayout);
```

### 5.2 模态框居中定位


**🎪 完美居中显示**
```javascript
function centerModal() {
    var modal = $('.modal');
    var windowWidth = $(window).width();
    var windowHeight = $(window).height();
    var modalWidth = modal.outerWidth();
    var modalHeight = modal.outerHeight();
    
    modal.css({
        'position': 'fixed',
        'left': (windowWidth - modalWidth) / 2,
        'top': (windowHeight - modalHeight) / 2
    });
}

$('.open-modal').click(function() {
    $('.modal').show();
    centerModal();
});
```

### 5.3 图片懒加载实现


**🖼️ 性能优化技巧**
```javascript
function lazyLoad() {
    $('.lazy-image').each(function() {
        var imageTop = $(this).offset().top;
        var windowBottom = $(window).scrollTop() + $(window).height();
        
        // 图片进入视窗时加载
        if (imageTop < windowBottom + 200) {
            var src = $(this).data('src');
            $(this).attr('src', src).removeClass('lazy-image');
        }
    });
}

$(window).scroll(lazyLoad);
$(document).ready(lazyLoad);
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心方法


```
🔸 基础尺寸：width() height() - 日常开发最常用
🔸 完整尺寸：outerWidth() outerHeight() - 布局计算必备
🔸 绝对定位：offset() - 弹窗、提示框定位
🔸 相对定位：position() - 下拉菜单、相对布局
🔸 滚动控制：scrollTop() - 返回顶部、滚动加载
```

### 6.2 记忆口诀


```
width height 测内容，
inner outer 包更多，
offset 文档来定位，
position 父级作参考，
scroll 滚动要记牢。
```

### 6.3 开发中的最佳实践


**✅ 推荐做法**
- 布局计算时优先使用 `outerWidth(true)` 获取完整空间
- 动态定位时结合 `offset()` 和窗口尺寸计算
- 滚动监听要做节流处理，避免性能问题
- 响应式开发时监听 `resize` 事件重新计算

**❌ 避免的误区**
- 不要混淆 `width()` 和 `outerWidth()` 的使用场景
- 不要在高频事件中重复获取尺寸，应该缓存值
- 设置位置时要考虑元素的定位属性（relative/absolute）

### 6.4 实际应用价值


**🎯 核心用途**
- **响应式布局**：根据屏幕尺寸动态调整
- **弹窗定位**：模态框、提示框精确定位  
- **滚动效果**：返回顶部、滚动加载、导航跟随
- **布局计算**：元素间距、对齐、适配计算

**💡 开发技巧**
- 尺寸和位置操作要考虑CSS盒模型
- 动态布局时要处理边界情况
- 性能敏感场景要合理缓存计算结果
- 移动端开发要考虑触摸滚动的特殊性

**核心记忆**：
- jQuery的尺寸位置方法是响应式开发和动态布局的基础工具
- 选择合适的方法比记住所有方法更重要
- 实际项目中这些方法常常组合使用，解决复杂的布局需求
