---
title: 2、元素遍历操作
---
## 📚 目录

1. [什么是.each()遍历](#1-什么是each遍历)
2. [基础语法与使用](#2-基础语法与使用)
3. [this和index参数详解](#3-this和index参数详解)
4. [遍历控制技巧](#4-遍历控制技巧)
5. [实际应用场景](#5-实际应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔄 什么是.each()遍历


### 1.1 基本概念

**`.each()`** 就像是给每个元素都执行一遍相同的操作，类似于循环但更简单。

```
想象一个场景：
你有3个按钮，要给每个按钮都设置不同的背景色
传统方法：写3遍代码
jQuery方法：用.each()一遍搞定！
```

### 1.2 为什么需要遍历

**实际工作中的需求**：
- 给多个相同元素设置不同内容
- 批量处理表单数据
- 动态生成列表内容
- 统计页面元素信息

**`.each()` vs 普通循环**：
```
普通for循环：需要手动获取每个元素
$('.item').each()：自动遍历所有匹配元素
```

---

## 2. 💡 基础语法与使用


### 2.1 核心语法

```javascript
$('选择器').each(function(index, element) {
    // index：当前元素的索引（从0开始）
    // element：当前DOM元素
    // this：也指向当前DOM元素
});
```

### 2.2 最简单的例子

**给所有段落添加序号**：
```javascript
// HTML: <p>内容1</p><p>内容2</p><p>内容3</p>

$('p').each(function(index) {
    $(this).text('第' + (index + 1) + '段：' + $(this).text());
});

// 结果：
// 第1段：内容1
// 第2段：内容2  
// 第3段：内容3
```

### 2.3 实用的批量操作

**批量设置不同的背景色**：
```javascript
var colors = ['red', 'blue', 'green'];

$('.box').each(function(index) {
    $(this).css('background-color', colors[index]);
});
```

---

## 3. 🎯 this和index参数详解


### 3.1 index索引的妙用

**`index` 就是当前元素的序号**，从0开始计数：

```javascript
// 给奇偶行设置不同样式
$('tr').each(function(index) {
    if (index % 2 === 0) {
        $(this).addClass('even-row');  // 偶数行
    } else {
        $(this).addClass('odd-row');   // 奇数行
    }
});
```

### 3.2 this的三种用法

```javascript
$('.item').each(function(index, element) {
    // 方法1：直接用this（最常用）
    $(this).addClass('processed');
    
    // 方法2：用element参数（和this一样）
    $(element).addClass('processed');
    
    // 方法3：用数组索引方式
    $('.item').eq(index).addClass('processed');
});
```

**💡 记住**：`this` 是原生DOM元素，需要用 `$(this)` 包装后才能使用jQuery方法！

### 3.3 获取元素信息

```javascript
// 统计所有输入框的值
var totalLength = 0;
$('input[type="text"]').each(function() {
    totalLength += $(this).val().length;
    console.log('第' + (index + 1) + '个输入框长度：' + $(this).val().length);
});
console.log('总字符数：' + totalLength);
```

---

## 4. 🛑 遍历控制技巧


### 4.1 跳过当前元素（continue）

**使用 `return true` 跳过**：
```javascript
$('.item').each(function(index) {
    // 跳过第2个元素
    if (index === 1) {
        return true;  // 相当于continue
    }
    
    $(this).css('color', 'red');
});
```

### 4.2 提前结束遍历（break）

**使用 `return false` 停止**：
```javascript
$('.item').each(function(index) {
    // 只处理前3个元素
    if (index >= 3) {
        return false;  // 相当于break，停止遍历
    }
    
    $(this).addClass('processed');
});
```

### 4.3 实际场景：查找特定元素

```javascript
// 查找第一个空的输入框并聚焦
$('input').each(function() {
    if ($(this).val() === '') {
        $(this).focus();
        return false;  // 找到就停止
    }
});
```

---

## 5. 🚀 实际应用场景


### 5.1 表单数据收集

```javascript
// 收集所有表单数据
var formData = {};
$('input, select, textarea').each(function() {
    var name = $(this).attr('name');
    var value = $(this).val();
    if (name) {
        formData[name] = value;
    }
});
```

### 5.2 动态生成内容

```javascript
// 根据数据生成列表
var students = ['张三', '李四', '王五'];
var html = '';

// 虽然这里用的是数组，但思路一样
$.each(students, function(index, name) {
    html += '<li>学号：' + (index + 1) + '，姓名：' + name + '</li>';
});
$('#student-list').html(html);
```

### 5.3 批量验证

```javascript
// 验证必填字段
var hasError = false;
$('.required').each(function() {
    if ($(this).val().trim() === '') {
        $(this).addClass('error');
        hasError = true;
    } else {
        $(this).removeClass('error');
    }
});

if (hasError) {
    alert('请填写所有必填项');
}
```

### 5.4 图片懒加载处理

```javascript
// 简单的图片懒加载
$('img[data-src]').each(function() {
    var $img = $(this);
    var realSrc = $img.attr('data-src');
    
    $img.attr('src', realSrc);
    $img.removeAttr('data-src');
});
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的要点

```
🔸 基础语法：$('选择器').each(function(index, element){})
🔸 参数理解：index是索引，element是DOM元素，this也是DOM元素
🔸 jQuery包装：$(this)才能使用jQuery方法
🔸 遍历控制：return true跳过，return false停止
🔸 实际应用：批量操作、数据收集、条件处理
```

### 6.2 常见使用场景

| 场景 | **代码模式** | **说明** |
|------|-------------|---------|
| **批量设置内容** | `$(this).text('内容' + index)` | `根据索引设置不同内容` |
| **条件处理** | `if (条件) return true` | `跳过不符合条件的元素` |
| **数据收集** | `data[name] = $(this).val()` | `收集表单或元素数据` |
| **查找元素** | `if (找到) return false` | `找到目标后停止遍历` |

### 6.3 性能提示

```
优化建议：
✅ 在遍历外部定义jQuery对象：var $items = $('.item')
✅ 避免在each内部重复查找相同元素
✅ 大量数据时考虑分批处理
❌ 不要在each内部写复杂的DOM操作
```

### 6.4 记忆口诀

```
each遍历真简单，
索引this要记清，
return true是跳过，
return false快停止。
```

**核心记忆**：
- `.each()` 是jQuery中最常用的遍历方法
- `index` 从0开始的索引，`this` 是当前DOM元素
- 记住用 `$(this)` 包装才能用jQuery方法
- 工作中主要用于批量操作和数据处理
