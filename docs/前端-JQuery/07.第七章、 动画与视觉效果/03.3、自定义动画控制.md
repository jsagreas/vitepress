---
title: 3、自定义动画控制
---
## 📚 目录

1. [自定义动画基础](#1-自定义动画基础)
2. [动画队列机制](#2-动画队列机制)
3. [动画控制方法](#3-动画控制方法)
4. [缓动效果详解](#4-缓动效果详解)
5. [实战应用技巧](#5-实战应用技巧)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎬 自定义动画基础


### 1.1 什么是自定义动画


**简单理解**：就是让元素从一个状态变到另一个状态，比如从左边移到右边，从小变大等等。

```html
<!-- 准备一个盒子 -->
<div id="box" style="width:100px; height:100px; background:red; position:relative;"></div>
<button id="btn">开始动画</button>
```

### 1.2 animate()基础用法


**最简单的动画**：
```javascript
// 让盒子向右移动200px
$('#btn').click(function() {
    $('#box').animate({
        left: '200px'  // 移动到距离左边200px的位置
    }, 1000);  // 1秒完成
});
```

**同时改变多个属性**：
```javascript
$('#box').animate({
    left: '200px',    // 向右移
    top: '100px',     // 向下移  
    width: '200px',   // 变宽
    height: '200px'   // 变高
}, 2000);  // 2秒完成
```

### 1.3 相对值动画


**让元素基于当前位置移动**：
```javascript
// 向右移动50px（不管现在在哪）
$('#box').animate({
    left: '+=50px'    // 当前位置 + 50px
}, 500);

// 向左移动30px
$('#box').animate({
    left: '-=30px'    // 当前位置 - 30px
}, 500);
```

**实际应用场景**：
```javascript
// 点击按钮，图片每次向右移动一点
$('#moveBtn').click(function() {
    $('.photo').animate({
        left: '+=100px'
    }, 300);
});
```

---

## 2. 🔄 动画队列机制


### 2.1 什么是动画队列


**通俗解释**：就像排队买票，前面的动画没完成，后面的动画就等着，一个接一个执行。

```javascript
// 这三个动画会依次执行，不会同时进行
$('#box')
    .animate({left: '200px'}, 1000)    // 第1个：向右移
    .animate({top: '100px'}, 1000)     // 第2个：向下移
    .animate({width: '200px'}, 1000);  // 第3个：变宽
```

**动画执行顺序图**：
```
时间轴：  0s -----> 1s -----> 2s -----> 3s
动画1：   [向右移动]
动画2：              [向下移动]  
动画3：                        [变宽]
```

### 2.2 队列操作 queue()


**查看当前队列有多少个动画**：
```javascript
// 添加几个动画
$('#box')
    .animate({left: '100px'}, 1000)
    .animate({top: '50px'}, 1000)
    .animate({width: '150px'}, 1000);

// 查看队列长度
console.log($('#box').queue().length);  // 输出：3
```

**手动添加动画到队列**：
```javascript
$('#box').queue(function() {
    $(this).css('background', 'blue');  // 改变颜色
    $(this).dequeue();  // 继续执行下一个动画
});
```

**清空动画队列**：
```javascript
// 清空所有等待的动画
$('#box').queue([]);
```

---

## 3. ⏯️ 动画控制方法


### 3.1 停止动画 stop()


**最常用的停止方法**：
```javascript
// 停止当前动画，继续执行队列中的下一个
$('#box').stop();

// 停止所有动画，清空队列
$('#box').stop(true);

// 停止动画并跳到最终状态
$('#box').stop(true, true);
```

**实际应用 - 鼠标悬停控制**：
```javascript
$('.menu-item').hover(
    function() {
        $(this).stop(true).animate({height: '60px'}, 200);
    },
    function() {
        $(this).stop(true).animate({height: '40px'}, 200);
    }
);
```

### 3.2 动画延迟 delay()


**在动画之间加等待时间**：
```javascript
$('#box')
    .animate({left: '100px'}, 500)   // 向右移
    .delay(1000)                     // 等待1秒
    .animate({top: '100px'}, 500);   // 向下移
```

**实际应用 - 按钮点击反馈**：
```javascript
$('#submitBtn').click(function() {
    $(this)
        .animate({scale: 0.9}, 100)  // 缩小
        .delay(100)                  // 停顿
        .animate({scale: 1}, 100);   // 恢复
});
```

### 3.3 链式动画调用


**连续动画的最佳写法**：
```javascript
// ✅ 推荐写法 - 链式调用
$('#box')
    .animate({left: '200px'}, 500)
    .animate({top: '100px'}, 500)
    .animate({opacity: 0.5}, 500);

// ❌ 不推荐 - 分开写
$('#box').animate({left: '200px'}, 500);
$('#box').animate({top: '100px'}, 500);    // 这样会同时执行！
$('#box').animate({opacity: 0.5}, 500);
```

---

## 4. 🎨 缓动效果详解


### 4.1 什么是缓动效果


**简单理解**：就是动画的"节奏感"，是匀速运动还是先快后慢，还是先慢后快。

```
匀速运动：    ————————————————>
先快后慢：    ————————————————>
先慢后快：    ————————————————>
```

### 4.2 jQuery内置缓动


**两种基本缓动**：
```javascript
// 匀速运动（默认）
$('#box').animate({left: '200px'}, 1000, 'linear');

// 先快后慢（更自然）
$('#box').animate({left: '200px'}, 1000, 'swing');
```

**实际对比效果**：
```javascript
// 创建两个盒子对比效果
$('#box1').animate({left: '300px'}, 2000, 'linear');  // 匀速
$('#box2').animate({left: '300px'}, 2000, 'swing');   // 先快后慢
```

### 4.3 常用缓动场景


**按钮悬停效果**：
```javascript
$('.btn').hover(
    function() {
        $(this).animate({
            paddingLeft: '20px',
            paddingRight: '20px'
        }, 200, 'swing');  // 自然的变化
    },
    function() {
        $(this).animate({
            paddingLeft: '10px', 
            paddingRight: '10px'
        }, 200, 'swing');
    }
);
```

**图片切换效果**：
```javascript
function showNextImage() {
    $('.current-img')
        .animate({opacity: 0}, 300, 'swing')  // 淡出
        .removeClass('current-img')
        .next()
        .addClass('current-img')
        .animate({opacity: 1}, 300, 'swing'); // 淡入
}
```

---

## 5. 💡 实战应用技巧


### 5.1 购物车抛物线动画


**商品飞入购物车效果**：
```javascript
$('.add-to-cart').click(function() {
    var $product = $(this).closest('.product');
    var $cart = $('#cart-icon');
    
    // 克隆商品图标
    var $fly = $product.find('img').clone()
        .css({
            position: 'fixed',
            'z-index': 9999,
            width: '30px',
            height: '30px'
        })
        .appendTo('body');
    
    // 抛物线动画
    $fly.animate({
        top: $cart.offset().top,
        left: $cart.offset().left,
        opacity: 0
    }, 800, function() {
        $(this).remove();  // 动画完成后删除
        updateCartCount(); // 更新购物车数量
    });
});
```

### 5.2 进度条动画


**模拟加载进度**：
```javascript
function showProgress() {
    $('.progress-bar')
        .css('width', '0%')
        .animate({width: '100%'}, 3000, 'linear', function() {
            // 完成后的回调
            $('.loading-text').text('加载完成！');
        });
}
```

### 5.3 数字递增动画


**让数字从0增加到目标值**：
```javascript
function animateNumber(target, duration) {
    $({count: 0}).animate({count: target}, {
        duration: duration,
        step: function() {
            $('#number-display').text(Math.floor(this.count));
        },
        complete: function() {
            $('#number-display').text(target);
        }
    });
}

// 使用
animateNumber(1234, 2000);  // 2秒内从0增加到1234
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🎯 自定义动画：animate()是核心，可以改变任何数值CSS属性
🔄 动画队列：链式调用的动画会排队执行，一个接一个
⏯️ 动画控制：stop()停止，delay()延迟，是常用的控制方法
🎨 缓动效果：swing比linear更自然，适合大部分场景
```

### 6.2 关键理解要点


**🔹 animate()能动画的属性**
```
✅ 可以：left, top, width, height, opacity, margin, padding
❌ 不行：color, background-color（需要插件）
```

**🔹 队列机制的关键**
```
链式调用 = 排队执行
同时写多行 = 同时执行
想要排队就用链式调用！
```

**🔹 stop()的三种用法**
```
stop()：停止当前，继续下一个
stop(true)：停止所有，清空队列  
stop(true, true)：停止并跳到结束状态
```

### 6.3 实际应用价值


**📱 移动端交互**：
- 滑动菜单、抽屉效果
- 按钮点击反馈动画
- 页面切换过渡效果

**💻 桌面端应用**：
- 鼠标悬停效果
- 模态框弹出动画  
- 进度条和加载动画

**🛍️ 电商项目**：
- 商品飞入购物车
- 图片轮播切换
- 价格数字递增

### 6.4 开发建议


**⚡ 性能优化**：
```javascript
// ✅ 好的做法
$('.item').hover(function() {
    $(this).stop(true).animate({scale: 1.1}, 200);
});

// ❌ 避免的问题
$('.item').hover(function() {
    $(this).animate({scale: 1.1}, 200);  // 没有stop()，会重复执行
});
```

**🎯 用户体验**：
- 动画时间一般控制在200-500ms
- 缓动效果优选swing，更自然
- 及时停止不需要的动画，避免卡顿

**核心记忆**：
- animate()让元素动起来，支持相对值
- 链式调用实现动画排队，stop()控制动画停止
- swing缓动比linear更自然，实际项目优选swing
- 合理使用delay()和回调函数，让动画更有节奏感