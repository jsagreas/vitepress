---
title: 1、表单数据操作
---
## 📚 目录

1. [表单值的获取与设置](#1-表单值的获取与设置)
2. [表单状态控制](#2-表单状态控制)
3. [表单数据序列化](#3-表单数据序列化)
4. [不同表单控件处理](#4-不同表单控件处理)
5. [实用表单验证技巧](#5-实用表单验证技巧)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 📝 表单值的获取与设置


### 1.1 `.val()` 方法核心用法


**🔸 基本概念**
```
.val() 是处理表单数据的万能方法
- 获取值：$('#input').val()
- 设置值：$('#input').val('新值')
- 适用于：input、textarea、select等所有表单元素
```

**💡 实际使用场景**
```javascript
// 获取输入框的值
var username = $('#username').val();

// 设置输入框的值
$('#username').val('张三');

// 清空输入框
$('#password').val('');

// 获取下拉框选中的值
var city = $('#city').val();
```

### 1.2 与其他方法的区别


**📊 方法对比**
```
方法对比：
.val()     → 专门处理表单值，最常用
.text()    → 获取纯文本内容
.html()    → 获取HTML内容
.attr()    → 获取HTML属性值

实际区别：
<input value="123" />
$('input').val()     // 获取用户输入的实际值
$('input').attr('value') // 获取HTML中写死的value属性
```

---

## 2. 🎛️ 表单状态控制


### 2.1 `.prop()` 方法详解


**🔸 核心作用**
```
.prop() 专门用来控制表单元素的状态
主要用于：选中、禁用、只读等布尔状态
```

**✅ 常用状态控制**
```javascript
// 复选框操作
$('#agree').prop('checked', true);   // 选中
$('#agree').prop('checked', false);  // 取消选中
var isChecked = $('#agree').prop('checked'); // 检查是否选中

// 禁用/启用控件
$('#submit').prop('disabled', true);  // 禁用按钮
$('#submit').prop('disabled', false); // 启用按钮

// 只读控制
$('#readonly-input').prop('readonly', true);
```

### 2.2 实用的状态操作


**🎯 批量状态控制**
```javascript
// 全选/全不选
$('#checkAll').click(function() {
    var checked = $(this).prop('checked');
    $('.item-checkbox').prop('checked', checked);
});

// 根据条件启用提交按钮
$('#agree').change(function() {
    $('#submit').prop('disabled', !$(this).prop('checked'));
});
```

---

## 3. 📦 表单数据序列化


### 3.1 `.serialize()` 快速提交


**🔸 什么是序列化**
```
序列化就是把表单数据变成可以提交的字符串格式
格式：name1=value1&name2=value2
```

**💻 基础用法**
```javascript
// HTML表单
<form id="myForm">
    <input name="username" value="张三" />
    <input name="age" value="25" />
    <select name="city">
        <option value="beijing" selected>北京</option>
    </select>
</form>

// jQuery序列化
var formData = $('#myForm').serialize();
console.log(formData); // username=张三&age=25&city=beijing

// 直接用于Ajax提交
$.post('/submit', $('#myForm').serialize());
```

### 3.2 `.serializeArray()` 数组格式


**🔸 返回数组格式**
```javascript
var formArray = $('#myForm').serializeArray();
console.log(formArray);
// 输出：
// [
//   {name: 'username', value: '张三'},
//   {name: 'age', value: '25'},
//   {name: 'city', value: 'beijing'}
// ]

// 转换为对象格式（实用技巧）
var formObj = {};
$.each(formArray, function(i, field) {
    formObj[field.name] = field.value;
});
```

### 3.3 序列化的注意事项


**⚠️ 重要提醒**
```
注意事项：
1. 表单元素必须有name属性
2. 禁用的元素不会被序列化
3. 未选中的复选框不会被序列化
4. 提交按钮的值不会被序列化

解决方案：
<input name="username" value="张三" />  ✅ 有name属性
<input value="李四" />                  ❌ 没有name属性
```

---

## 4. 🎮 不同表单控件处理


### 4.1 输入框类控件


**📝 文本输入框**
```javascript
// 普通输入框
$('#username').val();           // 获取值
$('#username').val('新用户名');  // 设置值

// 密码框（处理方式相同）
$('#password').val();

// 文本域
$('#message').val();
$('#message').val('新内容');
```

### 4.2 选择类控件


**🔘 单选框处理**
```javascript
// 获取选中的单选框值
var gender = $('input[name="gender"]:checked').val();

// 设置单选框选中
$('input[name="gender"][value="male"]').prop('checked', true);

// 清除单选框选择
$('input[name="gender"]').prop('checked', false);
```

**☑️ 复选框处理**
```javascript
// 单个复选框
$('#agree').prop('checked', true);   // 选中
var isAgreed = $('#agree').prop('checked'); // 检查状态

// 多个复选框获取选中值
var hobbies = [];
$('input[name="hobby"]:checked').each(function() {
    hobbies.push($(this).val());
});
```

**📋 下拉框处理**
```javascript
// 获取选中值
var city = $('#city').val();

// 设置选中项
$('#city').val('shanghai');

// 获取选中项的文本
var cityText = $('#city option:selected').text();

// 多选下拉框
var multiValues = $('#multi-select').val(); // 返回数组
```

### 4.3 控件处理对比表


| 控件类型 | **获取值** | **设置值** | **状态检查** |
|---------|-----------|-----------|-------------|
| **输入框** | `.val()` | `.val('值')` | - |
| **单选框** | `$(':checked').val()` | `.prop('checked', true)` | `.prop('checked')` |
| **复选框** | `$(':checked').val()` | `.prop('checked', true)` | `.prop('checked')` |
| **下拉框** | `.val()` | `.val('选项值')` | `.val()` |

---

## 5. ✅ 实用表单验证技巧


### 5.1 简单验证方法


**🔸 非空验证**
```javascript
function validateRequired(selector, message) {
    var value = $(selector).val().trim();
    if (value === '') {
        alert(message);
        $(selector).focus();
        return false;
    }
    return true;
}

// 使用
if (!validateRequired('#username', '请输入用户名')) {
    return false;
}
```

### 5.2 实时验证提示


**⚡ 失去焦点验证**
```javascript
$('#email').blur(function() {
    var email = $(this).val();
    var emailReg = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    
    if (!emailReg.test(email)) {
        $(this).addClass('error');
        $('#email-tip').text('邮箱格式不正确').show();
    } else {
        $(this).removeClass('error');
        $('#email-tip').hide();
    }
});
```

### 5.3 表单提交验证


**🚀 完整提交流程**
```javascript
$('#myForm').submit(function(e) {
    // 阻止默认提交
    e.preventDefault();
    
    // 验证必填项
    var isValid = true;
    $('.required').each(function() {
        if ($(this).val().trim() === '') {
            $(this).addClass('error');
            isValid = false;
        }
    });
    
    if (isValid) {
        // 序列化数据并提交
        $.post('/submit', $(this).serialize(), function(response) {
            alert('提交成功！');
        });
    } else {
        alert('请填写必填项');
    }
});
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心方法


```
🔸 .val() - 表单值操作的万能方法
🔸 .prop() - 表单状态控制的专用方法  
🔸 .serialize() - 表单数据快速序列化
🔸 .serializeArray() - 数组格式序列化
🔸 :checked 选择器 - 获取选中状态
```

### 6.2 关键理解要点


**🔹 方法选择原则**
```
获取表单值：优先使用 .val()
控制状态：优先使用 .prop()
提交数据：优先使用 .serialize()
```

**🔹 常见使用场景**
```
表单验证：.val() + 正则表达式
批量操作：.prop() + 循环
Ajax提交：.serialize() + $.post()
```

### 6.3 实际应用价值


**💼 工作中的实际用途**
- **登录表单**：用户名密码获取与验证
- **注册表单**：复杂表单数据处理与提交
- **设置页面**：表单状态的批量控制
- **搜索功能**：表单数据的快速序列化

**🎯 记忆口诀**
```
表单操作记住三个宝：
val取值prop状态，serialize来打包
不同控件区别小，掌握方法用到老
```

**核心记忆**：
- `.val()` 是表单操作的核心，90%的表单操作都用它
- `.prop()` 专门控制表单状态，checked、disabled必用
- `.serialize()` 让表单提交变得超简单
- 不同控件的处理方式要记住，实际项目中经常遇到
