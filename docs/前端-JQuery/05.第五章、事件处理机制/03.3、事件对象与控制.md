---
title: 3、事件对象与控制
---
## 📚 目录

1. [事件对象基础](#1-事件对象基础)
2. [阻止默认行为](#2-阻止默认行为)
3. [阻止事件冒泡](#3-阻止事件冒泡)
4. [event.target vs this](#4-eventtarget-vs-this)
5. [鼠标事件属性](#5-鼠标事件属性)
6. [键盘事件属性](#6-键盘事件属性)
7. [事件冒泡机制](#7-事件冒泡机制)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 事件对象基础


### 1.1 什么是事件对象

**简单理解**：当用户点击、输入、移动鼠标时，浏览器会创建一个**事件对象**，里面装着这次操作的所有信息。

```javascript
$('#btn').click(function(event) {
    console.log(event); // 这就是事件对象
    // 包含了点击位置、时间、目标元素等信息
});
```

### 1.2 获取事件对象的方式

```javascript
// 方式1：函数参数接收（推荐）
$('#btn').click(function(e) {
    console.log(e.type); // 'click'
});

// 方式2：jQuery包装
$('#btn').click(function(event) {
    console.log(event.target); // 点击的元素
});
```

**💡 实际应用**：
```javascript
// 表单提交时获取事件信息
$('#form').submit(function(e) {
    console.log('表单被提交了');
    console.log('提交时间：', new Date(e.timeStamp));
});
```

---

## 2. 🚫 阻止默认行为


### 2.1 什么是默认行为

**通俗解释**：浏览器对某些元素有"内置"的行为：
- **链接**：点击会跳转页面
- **表单**：提交会刷新页面  
- **右键**：会弹出菜单

### 2.2 preventDefault()的使用

```javascript
// 阻止链接跳转
$('a').click(function(e) {
    e.preventDefault(); // 不跳转了
    alert('链接被点击，但不跳转');
});

// 阻止表单提交
$('#form').submit(function(e) {
    e.preventDefault(); // 不提交了
    // 在这里用Ajax提交
    $.post('/api/submit', $(this).serialize());
});
```

### 2.3 常见应用场景

```javascript
// 1. 自定义按钮功能
$('#delete-btn').click(function(e) {
    e.preventDefault();
    if(confirm('确定删除吗？')) {
        // 执行删除操作
    }
});

// 2. 禁用右键菜单
$(document).contextmenu(function(e) {
    e.preventDefault();
    alert('右键已禁用');
});

// 3. 阻止回车提交表单
$('#input').keydown(function(e) {
    if(e.keyCode === 13) {
        e.preventDefault();
        // 执行其他操作
    }
});
```

---

## 3. 🛑 阻止事件冒泡


### 3.1 什么是事件冒泡

**形象比喻**：就像水中的气泡，事件从最深的元素一层层"冒"到最外层。

```
HTML结构冒泡示例：
┌─────────────────────┐  ← body (最外层)
│  ┌───────────────┐  │  ← div
│  │  ┌─────────┐  │  │  ← button (最内层)
│  │  │  点击   │  │  │
│  │  └─────────┘  │  │
│  └───────────────┘  │
└─────────────────────┘

点击button → 触发button事件 → 触发div事件 → 触发body事件
```

### 3.2 stopPropagation()的使用

```javascript
// HTML
// <div id="outer">
//   <div id="inner">
//     <button id="btn">点击我</button>
//   </div>
// </div>

$('#btn').click(function(e) {
    alert('按钮被点击');
    e.stopPropagation(); // 阻止冒泡
});

$('#inner').click(function() {
    alert('内层div被点击'); // 不会触发
});

$('#outer').click(function() {
    alert('外层div被点击'); // 不会触发
});
```

### 3.3 实际应用场景

```javascript
// 1. 弹窗点击内容不关闭
$('#modal').click(function(e) {
    e.stopPropagation(); // 点击弹窗内容不关闭
});

$('#overlay').click(function() {
    $('#modal').hide(); // 点击遮罩关闭弹窗
});

// 2. 下拉菜单控制
$('#dropdown').click(function(e) {
    e.stopPropagation();
    $(this).find('.menu').toggle();
});

$(document).click(function() {
    $('.menu').hide(); // 点击其他地方关闭菜单
});
```

---

## 4. 🎭 event.target vs this


### 4.1 两者的区别

**通俗解释**：
- **`event.target`**：真正被点击的那个元素（事件发生的地方）
- **`this`**：绑定事件监听器的那个元素（事件处理的地方）

```javascript
// HTML
// <div id="parent">
//   <span>点击我</span>
// </div>

$('#parent').click(function(e) {
    console.log('event.target:', e.target.tagName); // 'SPAN'
    console.log('this:', this.id);                  // 'parent'
});
```

### 4.2 图解说明

```
事件绑定在父元素上，但点击了子元素：

┌─────────────────────┐ ← this (绑定事件的元素)
│  #parent           │
│  ┌───────────────┐  │
│  │ <span> 点击  │  │ ← event.target (实际点击的元素)
│  └───────────────┘  │
└─────────────────────┘
```

### 4.3 实际应用

```javascript
// 1. 事件委托中的应用
$('#list').on('click', 'li', function(e) {
    console.log('点击的li:', e.target.textContent);
    console.log('事件绑定在:', this.id); // 'list'
});

// 2. 表格行点击
$('#table').on('click', 'tr', function(e) {
    if(e.target.tagName === 'TD') {
        // 点击的是单元格
        $(this).addClass('selected'); // this指向整行
    }
});

// 3. 按钮组处理
$('#btn-group').click(function(e) {
    if($(e.target).hasClass('btn')) {
        alert('点击了按钮：' + e.target.textContent);
    }
});
```

---

## 5. 🖱️ 鼠标事件属性


### 5.1 位置相关属性

```javascript
$('#box').click(function(e) {
    console.log('相对页面:', e.pageX, e.pageY);
    console.log('相对屏幕:', e.screenX, e.screenY);
    console.log('相对元素:', e.offsetX, e.offsetY);
});
```

### 5.2 按键状态属性

```javascript
$('#area').click(function(e) {
    if(e.ctrlKey) console.log('按住了Ctrl键');
    if(e.shiftKey) console.log('按住了Shift键');
    if(e.altKey) console.log('按住了Alt键');
    
    console.log('鼠标按键:', e.which); // 1左键 2中键 3右键
});
```

### 5.3 实际应用示例

```javascript
// 1. 鼠标拖拽
let isDragging = false;
$('#box').mousedown(function(e) {
    isDragging = true;
    console.log('开始拖拽:', e.pageX, e.pageY);
});

$(document).mousemove(function(e) {
    if(isDragging) {
        $('#box').css({
            left: e.pageX,
            top: e.pageY
        });
    }
});

// 2. 右键菜单定位
$(document).contextmenu(function(e) {
    e.preventDefault();
    $('#contextMenu').css({
        left: e.pageX,
        top: e.pageY,
        display: 'block'
    });
});
```

---

## 6. ⌨️ 键盘事件属性


### 6.1 按键识别

```javascript
$('#input').keydown(function(e) {
    console.log('按键代码:', e.keyCode);
    console.log('按键字符:', e.key);
    console.log('实际按键:', e.which);
});
```

### 6.2 常用按键代码

| 按键 | **keyCode** | **说明** |
|------|-------------|----------|
| `Enter` | `13` | `回车确认` |
| `Esc` | `27` | `取消退出` |
| `Space` | `32` | `空格键` |
| `Delete` | `46` | `删除键` |
| `↑` | `38` | `上箭头` |
| `↓` | `40` | `下箭头` |

### 6.3 实际应用

```javascript
// 1. 回车提交
$('#search').keydown(function(e) {
    if(e.keyCode === 13) {
        $('#searchBtn').click();
    }
});

// 2. Esc关闭弹窗
$(document).keydown(function(e) {
    if(e.keyCode === 27) {
        $('.modal').hide();
    }
});

// 3. 方向键控制
$('#game').keydown(function(e) {
    switch(e.keyCode) {
        case 37: moveLeft(); break;  // 左
        case 38: moveUp(); break;    // 上
        case 39: moveRight(); break; // 右
        case 40: moveDown(); break;  // 下
    }
});
```

---

## 7. 🔄 事件冒泡机制


### 7.1 冒泡流程图解

```
事件冒泡执行顺序：

用户点击 → button元素
                ↓
              div元素  
                ↓
             body元素
                ↓
             html元素
                ↓
           document对象

每一层都会触发对应的事件处理函数
```

### 7.2 冒泡示例演示

```javascript
// HTML结构
// <body>
//   <div id="outer">
//     <div id="inner">
//       <button id="btn">点击</button>
//     </div>
//   </div>
// </body>

$('#btn').click(function() {
    console.log('1. 按钮被点击');
});

$('#inner').click(function() {
    console.log('2. 内层div被点击');
});

$('#outer').click(function() {
    console.log('3. 外层div被点击');
});

$(document).click(function() {
    console.log('4. document被点击');
});

// 点击按钮，控制台输出：
// 1. 按钮被点击
// 2. 内层div被点击  
// 3. 外层div被点击
// 4. document被点击
```

### 7.3 利用冒泡实现事件委托

```javascript
// 动态添加的元素也能响应事件
$('#list').on('click', 'li', function() {
    alert('点击了：' + $(this).text());
});

// 后面添加的li也会有点击事件
$('#addBtn').click(function() {
    $('#list').append('<li>新项目</li>');
});
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念

```
🔸 事件对象：包含事件信息的对象，通过函数参数获取
🔸 preventDefault()：阻止默认行为，常用于表单、链接
🔸 stopPropagation()：阻止事件冒泡，避免触发父元素事件
🔸 target vs this：target是真正点击的元素，this是绑定事件的元素
🔸 事件冒泡：事件从内层元素向外层元素传播的机制
```

### 8.2 实际开发中的应用

```javascript
// 万能事件处理模板
$('#element').click(function(e) {
    e.preventDefault();     // 根据需要阻止默认行为
    e.stopPropagation();   // 根据需要阻止冒泡
    
    console.log('点击的元素:', e.target);
    console.log('绑定事件的元素:', this);
    
    // 业务逻辑处理
});
```

### 8.3 记忆要点

- **`event.preventDefault()`** - 我不要默认行为
- **`event.stopPropagation()`** - 我不要事件传播  
- **`event.target`** - 真正被点击的
- **`this`** - 监听事件的
- **事件冒泡** - 从里到外，层层触发

**核心记忆**：
- 事件对象装着操作信息，preventDefault阻止默认行为
- stopPropagation阻止冒泡传播，target指向真实点击
- this指向事件绑定元素，冒泡机制从内向外传播