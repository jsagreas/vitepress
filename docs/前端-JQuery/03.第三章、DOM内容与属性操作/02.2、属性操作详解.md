---
title: 2、属性操作详解
---
## 📚 目录

1. [attr()属性操作基础](#1-attr属性操作基础)
2. [prop()布尔属性操作](#2-prop布尔属性操作)
3. [attr()与prop()的核心区别](#3-attr与prop的核心区别)
4. [data()数据存储机制](#4-data数据存储机制)
5. [实际应用场景](#5-实际应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔧 attr()属性操作基础


### 1.1 基本用法


**🔸 获取属性值**
```javascript
// 获取单个属性
var imgSrc = $('img').attr('src');
var linkHref = $('#myLink').attr('href');

// 检查属性是否存在
if ($('#btn').attr('disabled') !== undefined) {
    console.log('按钮被禁用了');
}
```

**🔸 设置属性值**
```javascript
// 设置单个属性
$('img').attr('src', 'new-image.jpg');
$('#myLink').attr('href', 'https://www.example.com');

// 设置多个属性
$('img').attr({
    'src': 'avatar.jpg',
    'alt': '用户头像',
    'width': '100'
});
```

### 1.2 移除属性


**🔸 removeAttr()移除属性**
```javascript
// 移除单个属性
$('#btn').removeAttr('disabled');

// 移除多个属性
$('img').removeAttr('width height');

// 常见应用：表单重置
$('input').removeAttr('readonly disabled');
```

### 1.3 常见应用场景


```javascript
// 图片切换
$('#gallery img').click(function() {
    var newSrc = $(this).attr('data-large');
    $('#preview').attr('src', newSrc);
});

// 链接状态切换
$('.nav-link').attr('class', 'nav-link active');

// 动态设置表单属性
$('#userForm input').attr('placeholder', '请输入内容');
```

---

## 2. ⚡ prop()布尔属性操作


### 2.1 核心布尔属性


**🔸 表单控件状态**
```javascript
// 复选框操作
$('#checkbox').prop('checked', true);     // 选中
$('#checkbox').prop('checked', false);    // 取消选中

// 下拉框操作
$('#option1').prop('selected', true);     // 选中选项

// 按钮状态
$('#submitBtn').prop('disabled', true);   // 禁用按钮
$('#submitBtn').prop('disabled', false);  // 启用按钮
```

**🔸 获取布尔属性状态**
```javascript
// 检查复选框是否选中
if ($('#agree').prop('checked')) {
    console.log('用户已同意条款');
}

// 检查按钮是否禁用
if ($('#btn').prop('disabled')) {
    console.log('按钮当前被禁用');
}
```

### 2.2 实际应用示例


```javascript
// 全选/反选功能
$('#selectAll').change(function() {
    var isChecked = $(this).prop('checked');
    $('.item-checkbox').prop('checked', isChecked);
});

// 表单验证控制提交按钮
$('#userForm input').on('input', function() {
    var allFilled = true;
    $('#userForm input[required]').each(function() {
        if ($(this).val() === '') {
            allFilled = false;
            return false;
        }
    });
    $('#submitBtn').prop('disabled', !allFilled);
});
```

---

## 3. 🔀 attr()与prop()的核心区别


### 3.1 本质区别


```
HTML属性 vs DOM属性
┌─────────────────┐    ┌─────────────────┐
│   HTML源码      │    │   DOM对象       │
│ <input checked> │ → │ property: true  │
│   attribute     │    │   property      │
└─────────────────┘    └─────────────────┘

attr()  ← 操作HTML属性（字符串）
prop()  ← 操作DOM属性（布尔值/对象）
```

### 3.2 对比表格


| 属性 | **attr()操作** | **prop()操作** | **推荐使用** |
|------|---------------|---------------|-------------|
| 🔘 **checked** | `"checked"或undefined` | `true或false` | **prop()** |
| 🔘 **disabled** | `"disabled"或undefined` | `true或false` | **prop()** |
| 🔘 **selected** | `"selected"或undefined` | `true或false` | **prop()** |
| 🔗 **href** | `相对路径` | `完整URL` | **attr()** |
| 🖼️ **src** | `相对路径` | `完整URL` | **attr()** |
| 🏷️ **class** | `class属性值` | `class对象` | **attr()** |

### 3.3 实际对比示例


```html
<input type="checkbox" id="test" checked>
```

```javascript
// HTML刚加载时
console.log($('#test').attr('checked'));  // "checked"
console.log($('#test').prop('checked'));  // true

// 用户点击取消选中后
console.log($('#test').attr('checked'));  // 仍然是"checked"
console.log($('#test').prop('checked'));  // false

// 结论：prop()反映当前状态，attr()反映初始HTML
```

### 3.4 使用原则


**💡 简单记忆法则**
```javascript
// 布尔类型的属性用prop()
$('.checkbox').prop('checked', true);
$('.button').prop('disabled', false);

// 字符串类型的属性用attr()
$('.image').attr('src', 'new.jpg');
$('.link').attr('href', 'http://example.com');
```

---

## 4. 💾 data()数据存储机制


### 4.1 基本用法


**🔸 存储数据**
```javascript
// 存储简单数据
$('#user').data('id', 12345);
$('#user').data('name', '张三');

// 存储复杂数据
$('#product').data('info', {
    price: 99.9,
    category: '电子产品',
    inStock: true
});
```

**🔸 读取数据**
```javascript
// 读取数据
var userId = $('#user').data('id');        // 12345
var userName = $('#user').data('name');     // "张三"

// 读取复杂数据
var productInfo = $('#product').data('info');
console.log(productInfo.price);            // 99.9
```

### 4.2 HTML5 data-* 属性


**🔸 HTML中预设数据**
```html
<div id="article" data-id="123" data-author="李四" data-published="true">
    文章内容
</div>
```

```javascript
// jQuery自动读取data-*属性
var articleId = $('#article').data('id');          // 123（数字类型）
var author = $('#article').data('author');         // "李四"
var isPublished = $('#article').data('published'); // true（布尔类型）

// 注意：data()会自动转换数据类型
// "123" → 123, "true" → true, "false" → false
```

### 4.3 删除数据


**🔸 removeData()删除**
```javascript
// 删除单个数据
$('#user').removeData('id');

// 删除多个数据
$('#user').removeData('id name');

// 删除所有存储的数据
$('#user').removeData();
```

### 4.4 实际应用场景


```javascript
// 购物车功能
$('.add-to-cart').click(function() {
    var productId = $(this).data('product-id');
    var price = $(this).data('price');
    addToCart(productId, price);
});

// 选项卡切换
$('.tab-btn').click(function() {
    var targetTab = $(this).data('target');
    $('.tab-content').hide();
    $(targetTab).show();
});

// 表单验证规则存储
$('#email').data('validate', {
    required: true,
    pattern: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    message: '请输入有效邮箱'
});
```

---

## 5. 🎯 实际应用场景


### 5.1 表单处理综合示例


```javascript
// 用户注册表单处理
$(document).ready(function() {
    // 表单验证状态控制
    $('#registerForm input').on('blur', function() {
        var field = $(this);
        var value = field.val();
        
        if (value === '') {
            field.attr('class', 'form-control error');
            field.prop('disabled', false);
        } else {
            field.attr('class', 'form-control success');
        }
    });
    
    // 同意条款控制提交按钮
    $('#agreeTerms').change(function() {
        $('#submitBtn').prop('disabled', !$(this).prop('checked'));
    });
    
    // 存储用户选择的偏好
    $('.preference').change(function() {
        var preferences = [];
        $('.preference:checked').each(function() {
            preferences.push($(this).val());
        });
        $('#userForm').data('preferences', preferences);
    });
});
```

### 5.2 动态内容管理


```javascript
// 图片画廊管理
$('.gallery img').each(function(index) {
    $(this)
        .attr('data-index', index)
        .data('original-src', $(this).attr('src'))
        .click(function() {
            var currentIndex = $(this).data('index');
            var originalSrc = $(this).data('original-src');
            showLightbox(originalSrc, currentIndex);
        });
});

// 列表项状态管理
$('.todo-item').each(function() {
    var item = $(this);
    var isCompleted = item.data('completed') || false;
    
    item.find('.checkbox').prop('checked', isCompleted);
    
    if (isCompleted) {
        item.attr('class', 'todo-item completed');
    }
});
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心方法


```
🔸 属性操作三大核心：
• attr() - HTML属性操作，返回字符串
• prop() - DOM属性操作，返回实际类型
• data() - 数据存储操作，支持复杂数据

🔸 删除操作：
• removeAttr() - 移除HTML属性
• removeData() - 删除存储数据
```

### 6.2 关键使用原则


**🔹 何时用attr()**
```javascript
// 字符串类型的HTML属性
$('.img').attr('src', 'image.jpg');
$('.link').attr('href', 'http://example.com');
$('.element').attr('class', 'active');
```

**🔹 何时用prop()**
```javascript
// 布尔类型的DOM属性
$('.checkbox').prop('checked', true);
$('.button').prop('disabled', false);
$('.option').prop('selected', true);
```

**🔹 何时用data()**
```javascript
// 临时数据存储和HTML5 data-*属性
$('.element').data('user-id', 123);
var config = $('.element').data('config');
```

### 6.3 常见误区避免


| 误区 | **错误写法** | **正确写法** | **说明** |
|------|-------------|-------------|---------|
| 🚫 **复选框状态** | `attr('checked')` | `prop('checked')` | `用prop()获取真实状态` |
| 🚫 **按钮禁用** | `attr('disabled', true)` | `prop('disabled', true)` | `布尔属性用prop()` |
| 🚫 **数据存储** | `attr('data-info', obj)` | `data('info', obj)` | `复杂数据用data()` |

### 6.4 性能优化建议


```javascript
// ✅ 好的做法：缓存jQuery对象
var $element = $('#myElement');
$element.attr('src', 'new.jpg');
$element.data('loaded', true);

// ❌ 避免的做法：重复查询
$('#myElement').attr('src', 'new.jpg');
$('#myElement').data('loaded', true);

// ✅ 链式调用优化
$('#myElement')
    .attr('src', 'new.jpg')
    .data('loaded', true)
    .prop('disabled', false);
```

**核心记忆**：
- **attr()管HTML**，**prop()管状态**，**data()管数据**
- **布尔属性必用prop()**，**字符属性常用attr()**
- **复杂数据存储首选data()**，**简单标记可用attr()**
- **删除属性用removeAttr()**，**删除数据用removeData()**