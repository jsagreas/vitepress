---
title: 3、Ajax回调与错误处理
---
## 📚 目录

1. [Ajax回调基础概念](#1-Ajax回调基础概念)
2. [三大核心回调函数](#2-三大核心回调函数)
3. [Promise风格的现代写法](#3-Promise风格的现代写法)
4. [错误处理与状态码](#4-错误处理与状态码)
5. [Ajax全局事件监听](#5-Ajax全局事件监听)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔄 Ajax回调基础概念


### 1.1 什么是Ajax回调


**通俗理解**：Ajax是异步的，就像你点外卖一样
```
你点外卖(发送请求) → 等待配送(请求过程) → 收到外卖(处理响应)

Ajax请求 → 服务器处理 → 执行回调函数处理结果
```

**为什么需要回调**：
- Ajax请求不是立即完成的，需要时间
- 我们需要在**请求完成后**执行相应的操作
- 回调函数就是"请求完成后要做什么"的指令

### 1.2 回调的三种结果


```
Ajax请求结果：
✅ 成功 → success回调
❌ 失败 → error回调  
⭕ 无论成功失败 → complete回调
```

---

## 2. 📞 三大核心回调函数


### 2.1 success - 成功回调


**作用**：请求成功时执行，处理服务器返回的数据

```javascript
$.ajax({
    url: '/api/users',
    type: 'GET',
    success: function(data, status, xhr) {
        // data：服务器返回的数据
        // status：状态字符串 'success'
        // xhr：原生XMLHttpRequest对象
        
        console.log('请求成功！数据：', data);
        $('#userList').html(data); // 显示数据
    }
});
```

**常见使用场景**：
```javascript
// 获取用户列表
success: function(users) {
    users.forEach(user => {
        $('#userList').append(`<li>${user.name}</li>`);
    });
}

// 提交表单
success: function(result) {
    alert('保存成功！');
    $('#form')[0].reset(); // 清空表单
}
```

### 2.2 error - 错误回调


**作用**：请求失败时执行，处理错误情况

```javascript
$.ajax({
    url: '/api/users',
    type: 'GET',
    error: function(xhr, status, error) {
        // xhr：XMLHttpRequest对象
        // status：错误类型 'error'、'timeout'等
        // error：具体错误信息
        
        console.log('请求失败：', error);
        alert('加载失败，请重试');
    }
});
```

**常见错误处理**：
```javascript
error: function(xhr, status, error) {
    if (xhr.status === 404) {
        alert('数据不存在');
    } else if (xhr.status === 500) {
        alert('服务器错误');
    } else {
        alert('网络错误，请检查连接');
    }
}
```

### 2.3 complete - 完成回调


**作用**：无论成功失败都会执行，用于清理工作

```javascript
$.ajax({
    url: '/api/users',
    type: 'GET',
    beforeSend: function() {
        $('#loading').show(); // 显示加载动画
    },
    complete: function() {
        $('#loading').hide(); // 隐藏加载动画
    }
});
```

### 2.4 三个回调的执行顺序


```
Ajax请求流程：
开始请求 → beforeSend回调
     ↓
等待响应...
     ↓
成功 → success回调 → complete回调
失败 → error回调  → complete回调
```

**完整示例**：
```javascript
$.ajax({
    url: '/api/data',
    type: 'POST',
    data: {name: 'John'},
    beforeSend: function() {
        console.log('1. 开始发送请求');
        $('.btn').prop('disabled', true);
    },
    success: function(data) {
        console.log('2. 请求成功', data);
    },
    error: function(xhr, status, error) {
        console.log('2. 请求失败', error);
    },
    complete: function() {
        console.log('3. 请求完成');
        $('.btn').prop('disabled', false);
    }
});
```

---

## 3. ✨ Promise风格的现代写法


### 3.1 为什么要用Promise风格


**传统回调的问题**：
```javascript
// 传统写法：回调地狱
$.ajax({
    success: function(data1) {
        $.ajax({
            data: data1,
            success: function(data2) {
                $.ajax({
                    data: data2,
                    success: function(data3) {
                        // 嵌套太深，难以维护
                    }
                });
            }
        });
    }
});
```

**Promise风格的优势**：
- 代码更清晰，避免嵌套地狱
- 更符合现代JavaScript写法
- 易于错误处理和调试

### 3.2 三个核心Promise方法


#### 🔸 .done() - 成功处理


```javascript
$.ajax({
    url: '/api/users',
    type: 'GET'
}).done(function(data) {
    // 等同于 success 回调
    console.log('请求成功：', data);
    $('#result').html(data);
});
```

#### 🔸 .fail() - 失败处理


```javascript
$.ajax({
    url: '/api/users',
    type: 'GET'
}).fail(function(xhr, status, error) {
    // 等同于 error 回调
    console.log('请求失败：', error);
    alert('加载失败');
});
```

#### 🔸 .always() - 总是执行


```javascript
$.ajax({
    url: '/api/users',
    type: 'GET'
}).always(function() {
    // 等同于 complete 回调
    console.log('请求结束');
    $('#loading').hide();
});
```

### 3.3 链式调用的完整写法


```javascript
$.ajax({
    url: '/api/users',
    type: 'GET',
    dataType: 'json'
})
.done(function(users) {
    // 成功时处理数据
    console.log('获取到用户：', users.length);
    users.forEach(user => {
        $('#userList').append(`<li>${user.name}</li>`);
    });
})
.fail(function(xhr, status, error) {
    // 失败时处理错误
    console.error('请求失败：', error);
    $('#userList').html('<li>加载失败，请重试</li>');
})
.always(function() {
    // 无论成功失败都执行
    $('#loading').hide();
    console.log('请求处理完成');
});
```

### 3.4 传统回调 vs Promise对比


| **方面** | **传统回调** | **Promise风格** |
|---------|-------------|----------------|
| **写法** | `success: function(){}` | `.done(function(){})` |
| **可读性** | `容易嵌套，难读` | `链式调用，清晰` |
| **错误处理** | `分散在各处` | `统一用.fail()` |
| **推荐程度** | `基础学习` | `实际开发推荐` |

---

## 4. ❌ 错误处理与状态码


### 4.1 常见HTTP状态码处理


```javascript
$.ajax({
    url: '/api/data',
    type: 'GET'
})
.fail(function(xhr, status, error) {
    const statusCode = xhr.status;
    
    switch(statusCode) {
        case 400:
            alert('请求参数错误');
            break;
        case 401:
            alert('未登录，请先登录');
            window.location.href = '/login';
            break;
        case 403:
            alert('没有权限访问');
            break;
        case 404:
            alert('请求的资源不存在');
            break;
        case 500:
            alert('服务器内部错误');
            break;
        default:
            alert('网络错误：' + error);
    }
});
```

### 4.2 网络连接错误处理


```javascript
$.ajax({
    url: '/api/data',
    type: 'GET',
    timeout: 5000  // 5秒超时
})
.fail(function(xhr, status, error) {
    if (status === 'timeout') {
        alert('请求超时，请检查网络');
    } else if (status === 'abort') {
        alert('请求被取消');
    } else if (xhr.status === 0) {
        alert('网络连接失败');
    } else {
        alert('未知错误：' + error);
    }
});
```

### 4.3 实用的错误处理函数


```javascript
// 统一错误处理函数
function handleAjaxError(xhr, status, error) {
    let message = '';
    
    if (xhr.status === 0) {
        message = '网络连接失败，请检查网络';
    } else if (xhr.status >= 500) {
        message = '服务器错误，请稍后重试';
    } else if (xhr.status >= 400) {
        message = '请求错误：' + (xhr.responseText || error);
    } else {
        message = '未知错误：' + error;
    }
    
    console.error('Ajax错误：', message);
    alert(message);
}

// 使用统一错误处理
$.ajax({
    url: '/api/data',
    type: 'GET'
})
.done(function(data) {
    // 处理成功数据
})
.fail(handleAjaxError);
```

---

## 5. 🌐 Ajax全局事件监听


### 5.1 为什么需要全局事件


**使用场景**：
- 所有Ajax请求都显示统一的加载动画
- 统一处理所有请求的错误
- 监控页面所有的Ajax活动

### 5.2 核心全局事件


#### 🔸 ajaxStart - 开始请求


```javascript
// 当任何Ajax请求开始时触发
$(document).ajaxStart(function() {
    $('#globalLoading').show();
    console.log('有Ajax请求开始了');
});
```

#### 🔸 ajaxStop - 停止请求


```javascript
// 当所有Ajax请求完成时触发
$(document).ajaxStop(function() {
    $('#globalLoading').hide();
    console.log('所有Ajax请求完成了');
});
```

#### 🔸 ajaxError - 全局错误


```javascript
// 当任何Ajax请求出错时触发
$(document).ajaxError(function(event, xhr, settings, error) {
    console.error('Ajax请求出错：', settings.url, error);
    
    // 统一错误提示
    if (xhr.status === 401) {
        alert('登录已过期，请重新登录');
        window.location.href = '/login';
    }
});
```

### 5.3 实用的全局配置


```javascript
// 页面初始化时设置全局Ajax监听
$(document).ready(function() {
    
    // 全局加载动画
    $(document).ajaxStart(function() {
        $('body').addClass('loading');
        $('#pageLoading').fadeIn();
    });
    
    $(document).ajaxStop(function() {
        $('body').removeClass('loading');
        $('#pageLoading').fadeOut();
    });
    
    // 全局错误处理
    $(document).ajaxError(function(event, xhr, settings, error) {
        // 忽略404错误（有些接口可能故意返回404）
        if (xhr.status === 404) return;
        
        // 处理认证错误
        if (xhr.status === 401) {
            alert('登录已过期');
            window.location.href = '/login';
            return;
        }
        
        // 其他错误统一提示
        console.error('请求失败：', settings.url, error);
    });
});
```

### 5.4 全局事件的执行顺序


```
Ajax请求生命周期：

1. ajaxStart  → 第一个请求开始时
2. ajaxSend   → 每个请求发送前
3. ajaxSuccess → 每个请求成功时
4. ajaxError   → 每个请求失败时
5. ajaxComplete → 每个请求完成时
6. ajaxStop    → 最后一个请求完成时
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 三大回调：success处理成功，error处理失败，complete总是执行
🔸 Promise风格：.done() .fail() .always() 现代推荐写法
🔸 错误处理：根据状态码分类处理，提供友好的用户提示
🔸 全局事件：ajaxStart/ajaxStop 统一控制加载状态
```

### 6.2 实际开发中的最佳实践


**🔹 推荐的Ajax写法模板**：
```javascript
// 现代推荐写法
$.ajax({
    url: '/api/data',
    type: 'GET',
    dataType: 'json',
    timeout: 10000
})
.done(function(data) {
    // 处理成功数据
})
.fail(function(xhr, status, error) {
    // 处理错误
    handleError(xhr, status, error);
})
.always(function() {
    // 清理工作
});
```

**🔹 错误处理原则**：
```
用户友好：给出易懂的错误提示，不要显示技术术语
分类处理：根据不同错误类型给出不同的解决建议
日志记录：在控制台记录详细错误信息，便于调试
优雅降级：请求失败时，页面仍能正常使用
```

**🔹 性能优化建议**：
```
设置超时：避免请求卡死影响用户体验
全局监听：统一处理加载状态，避免重复代码
错误重试：网络错误时提供重试机制
请求缓存：相同请求避免重复发送
```

### 6.3 常见问题与解决方案


| **问题** | **原因** | **解决方案** |
|---------|---------|-------------|
| **请求一直loading** | `没有处理error回调` | `添加.fail()或error回调` |
| **错误信息不友好** | `直接显示技术错误` | `根据状态码自定义提示` |
| **页面卡死** | `没有设置超时` | `添加timeout参数` |
| **重复请求** | `用户多次点击` | `请求期间禁用按钮` |

**核心记忆口诀**：
- Ajax回调三兄弟，成功失败都要记
- Promise写法更现代，链式调用最清晰  
- 错误处理要友好，状态码里找原因
- 全局事件很实用，统一控制省心力

**实际应用价值**：
- **用户体验**：合理的加载提示和错误处理让用户体验更好
- **代码维护**：统一的错误处理减少重复代码
- **问题调试**：详细的错误信息帮助快速定位问题
- **系统稳定**：完善的异常处理提高系统健壮性
