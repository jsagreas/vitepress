---
title: 4、JSON数据处理实战
---
## 📚 目录

1. [快速获取JSON数据](#1-快速获取json数据)
2. [JSON数据解析和处理](#2-json数据解析和处理)  
3. [动态生成HTML内容](#3-动态生成html内容)
4. [列表数据渲染实战](#4-列表数据渲染实战)
5. [实际项目应用场景](#5-实际项目应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🚀 快速获取JSON数据


### 1.1 什么是$.getJSON()


**🔸 简单理解**
```
$.getJSON() = $.get() + JSON数据类型
专门用来获取JSON格式的数据，比$.get()更方便
```

**💡 基本语法**
```javascript
$.getJSON(url, data, success)
```

### 1.2 最简单的用法


**📝 获取用户信息**
```javascript
// 最基础的用法
$.getJSON('/api/user/123', function(data) {
    console.log(data.name);  // 直接使用JSON数据
});

// 等价于
$.get('/api/user/123', function(data) {
    // data已经是JavaScript对象，无需JSON.parse()
    console.log(data.name);
}, 'json');
```

### 1.3 携带参数请求


**🔧 传递查询参数**
```javascript
// 获取用户列表，带分页参数
$.getJSON('/api/users', {
    page: 1,
    size: 10,
    keyword: '张三'
}, function(data) {
    console.log('总数：' + data.total);
    console.log('用户列表：', data.list);
});

// 实际请求URL：/api/users?page=1&size=10&keyword=张三
```

---

## 2. 📊 JSON数据解析和处理


### 2.1 常见JSON数据格式


**🔸 单个对象数据**
```javascript
// 服务器返回的用户信息
{
    "id": 123,
    "name": "张三",
    "email": "zhangsan@email.com",
    "avatar": "/images/avatar.jpg"
}

// jQuery处理
$.getJSON('/api/user/123', function(user) {
    $('#username').text(user.name);
    $('#email').text(user.email);
    $('#avatar').attr('src', user.avatar);
});
```

**🔸 列表数据格式**
```javascript
// 服务器返回的商品列表
{
    "total": 156,
    "page": 1,
    "list": [
        {"id": 1, "name": "苹果手机", "price": 5999},
        {"id": 2, "name": "华为手机", "price": 4999},
        {"id": 3, "name": "小米手机", "price": 2999}
    ]
}
```

### 2.2 数据安全处理


**⚠️ 检查数据是否存在**
```javascript
$.getJSON('/api/products', function(data) {
    // 安全检查，避免程序报错
    if (data && data.list && data.list.length > 0) {
        // 有数据时的处理
        renderProductList(data.list);
    } else {
        // 无数据时的处理
        $('#product-list').html('<p>暂无商品数据</p>');
    }
});
```

**🛡️ 错误处理**
```javascript
$.getJSON('/api/products')
    .done(function(data) {
        // 成功获取数据
        console.log('数据获取成功');
    })
    .fail(function(xhr, status, error) {
        // 请求失败处理
        console.log('数据获取失败：' + error);
        alert('网络错误，请稍后重试');
    });
```

---

## 3. 🎨 动态生成HTML内容


### 3.1 基础HTML拼接


**📝 简单数据渲染**
```javascript
// 用户信息渲染
$.getJSON('/api/user/123', function(user) {
    var html = '<div class="user-card">' +
                 '<img src="' + user.avatar + '" alt="头像">' +
                 '<h3>' + user.name + '</h3>' +
                 '<p>' + user.email + '</p>' +
               '</div>';
    
    $('#user-info').html(html);
});
```

### 3.2 循环生成列表


**🔄 商品列表渲染**
```javascript
$.getJSON('/api/products', function(data) {
    var html = '';
    
    // 循环生成每个商品的HTML
    $.each(data.list, function(index, product) {
        html += '<div class="product-item">' +
                  '<h4>' + product.name + '</h4>' +
                  '<p class="price">￥' + product.price + '</p>' +
                  '<button data-id="' + product.id + '">加入购物车</button>' +
                '</div>';
    });
    
    $('#product-list').html(html);
});
```

### 3.3 使用模板字符串（现代写法）


**💎 ES6模板字符串**
```javascript
$.getJSON('/api/products', function(data) {
    let html = '';
    
    data.list.forEach(product => {
        html += `
            <div class="product-item">
                <h4>${product.name}</h4>
                <p class="price">￥${product.price}</p>
                <button data-id="${product.id}">加入购物车</button>
            </div>
        `;
    });
    
    $('#product-list').html(html);
});
```

---

## 4. 📋 列表数据渲染实战


### 4.1 完整的商品列表示例


**🛍️ HTML结构**
```html
<div id="product-container">
    <div id="product-list"></div>
    <div id="pagination"></div>
</div>
```

**💻 JavaScript实现**
```javascript
// 获取并渲染商品列表
function loadProducts(page = 1) {
    $.getJSON('/api/products', {
        page: page,
        size: 12
    }, function(data) {
        renderProducts(data.list);
        renderPagination(data.total, data.page);
    });
}

// 渲染商品列表
function renderProducts(products) {
    var html = '';
    
    if (products.length === 0) {
        html = '<p class="no-data">暂无商品</p>';
    } else {
        $.each(products, function(i, product) {
            html += `
                <div class="product-card">
                    <img src="${product.image}" alt="${product.name}">
                    <h3>${product.name}</h3>
                    <p class="price">￥${product.price}</p>
                    <button class="btn-buy" data-id="${product.id}">立即购买</button>
                </div>
            `;
        });
    }
    
    $('#product-list').html(html);
}

// 页面加载时获取数据
$(document).ready(function() {
    loadProducts();
});
```

### 4.2 搜索功能实现


**🔍 实时搜索**
```javascript
// 搜索框输入事件
$('#search-input').on('input', function() {
    var keyword = $(this).val();
    
    if (keyword.length >= 2) {  // 输入2个字符开始搜索
        searchProducts(keyword);
    }
});

// 搜索商品
function searchProducts(keyword) {
    $.getJSON('/api/products/search', {
        keyword: keyword
    }, function(data) {
        renderProducts(data.list);
        
        // 显示搜索结果数量
        $('#search-result').text(`找到 ${data.total} 个商品`);
    });
}
```

---

## 5. 🎯 实际项目应用场景


### 5.1 用户评论加载


**💬 评论列表**
```javascript
// 加载商品评论
function loadComments(productId) {
    $.getJSON(`/api/products/${productId}/comments`, function(data) {
        var html = '';
        
        $.each(data.comments, function(i, comment) {
            html += `
                <div class="comment-item">
                    <div class="user-info">
                        <span class="username">${comment.username}</span>
                        <span class="date">${comment.date}</span>
                    </div>
                    <div class="content">${comment.content}</div>
                    <div class="rating">评分：${comment.rating}分</div>
                </div>
            `;
        });
        
        $('#comments-list').html(html);
    });
}
```

### 5.2 购物车数量更新


**🛒 购物车实时更新**
```javascript
// 添加到购物车
$('.btn-add-cart').click(function() {
    var productId = $(this).data('id');
    
    $.getJSON('/api/cart/add', {
        product_id: productId,
        quantity: 1
    }, function(data) {
        // 更新购物车数量显示
        $('#cart-count').text(data.cart_count);
        
        // 显示成功提示
        showMessage('已添加到购物车');
    });
});
```

### 5.3 省市区级联选择


**🏙️ 地区选择器**
```javascript
// 省份改变时加载城市
$('#province').change(function() {
    var provinceId = $(this).val();
    
    if (provinceId) {
        $.getJSON('/api/cities', {
            province_id: provinceId
        }, function(cities) {
            var options = '<option value="">请选择城市</option>';
            
            $.each(cities, function(i, city) {
                options += `<option value="${city.id}">${city.name}</option>`;
            });
            
            $('#city').html(options);
            $('#district').html('<option value="">请选择区县</option>');
        });
    }
});
```

### 5.4 数据表格分页


**📊 表格数据加载**
```javascript
// 加载用户列表
function loadUserTable(page = 1) {
    $.getJSON('/api/admin/users', {
        page: page,
        size: 20
    }, function(data) {
        var tbody = '';
        
        $.each(data.users, function(i, user) {
            tbody += `
                <tr>
                    <td>${user.id}</td>
                    <td>${user.name}</td>
                    <td>${user.email}</td>
                    <td>${user.status}</td>
                    <td>
                        <button class="btn-edit" data-id="${user.id}">编辑</button>
                        <button class="btn-delete" data-id="${user.id}">删除</button>
                    </td>
                </tr>
            `;
        });
        
        $('#user-table tbody').html(tbody);
        updatePagination(data.total, data.page);
    });
}
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 $.getJSON()：专门获取JSON数据的简化方法
🔸 数据安全：始终检查数据是否存在，避免程序报错
🔸 HTML拼接：使用字符串拼接或模板字符串生成HTML
🔸 循环渲染：使用$.each()或forEach()处理列表数据
🔸 错误处理：使用.fail()处理请求失败的情况
```

### 6.2 实战开发要点


**🔹 性能优化**
```javascript
// ✅ 正确做法：一次性拼接HTML
var html = '';
$.each(data, function(i, item) {
    html += '<div>' + item.name + '</div>';
});
$('#container').html(html);

// ❌ 错误做法：多次操作DOM
$.each(data, function(i, item) {
    $('#container').append('<div>' + item.name + '</div>');
});
```

**🔹 用户体验**
```javascript
// 显示加载状态
$('#loading').show();

$.getJSON('/api/data', function(data) {
    renderData(data);
}).always(function() {
    // 无论成功失败都隐藏加载状态
    $('#loading').hide();
});
```

### 6.3 常见应用场景


| 场景 | **典型用法** | **关键点** |
|------|-------------|-----------|
| 🛍️ **商品列表** | `分页加载+搜索` | `数据渲染+用户交互` |
| 💬 **评论系统** | `动态加载评论` | `实时更新+用户反馈` |
| 🛒 **购物车** | `数量更新+状态同步` | `实时响应+数据同步` |
| 📊 **数据表格** | `分页+排序+筛选` | `复杂交互+性能优化` |
| 🏙️ **级联选择** | `省市区联动` | `依赖关系+动态更新` |

### 6.4 开发建议


**✅ 最佳实践**
- 始终进行数据安全检查
- 合理使用加载状态提示
- 统一错误处理机制
- 避免频繁操作DOM

**⚠️ 注意事项**
- JSON数据可能为空或格式异常
- 网络请求可能失败，要有备用方案
- 大量数据要考虑分页或虚拟滚动
- 用户操作要有及时反馈

**核心记忆**：
- `$.getJSON()`是获取JSON数据最简单的方法
- 数据安全检查是必须的开发习惯
- HTML字符串拼接要一次性完成，避免多次DOM操作
- 实际项目中要关注用户体验和性能优化