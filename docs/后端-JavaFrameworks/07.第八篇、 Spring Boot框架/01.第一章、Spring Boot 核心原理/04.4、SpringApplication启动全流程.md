---
title: 4、SpringApplication启动全流程
---
## 📚 目录

1. [SpringApplication核心概念](#1-springapplication核心概念)
2. [SpringApplication.run方法详解](#2-springapplicationrun方法详解)
3. [Bootstrap启动流程](#3-bootstrap启动流程)
4. [环境准备阶段](#4-环境准备阶段)
5. [ApplicationContext创建过程](#5-applicationcontext创建过程)
6. [上下文刷新机制](#6-上下文刷新机制)
7. [CommandLineRunner与ApplicationRunner执行](#7-commandlinerunner与applicationrunner执行)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🚀 SpringApplication核心概念


### 1.1 SpringApplication是什么


想象一下，你要开一家餐厅。传统方式需要自己找地方、装修、招聘、采购设备、制定菜单等等，非常繁琐。而SpringApplication就像一个**全能的餐厅管家**，你只需要告诉它"我要开餐厅"，它就会帮你把所有准备工作都安排好。

**SpringApplication的本质**：
- **应用启动器**：负责启动整个Spring应用程序
- **配置管理器**：自动读取和处理各种配置
- **容器初始化器**：创建和管理Spring容器
- **生命周期控制器**：管理应用的整个生命周期

### 1.2 为什么需要SpringApplication


**传统Spring应用的痛点**：
```
手动配置繁琐：需要写大量XML配置文件
依赖管理复杂：jar包版本冲突问题
启动代码冗长：需要写很多样板代码
环境配置困难：开发、测试、生产环境切换麻烦
```

**SpringApplication解决的问题**：
- ✅ **自动配置**：根据classpath自动配置Bean
- ✅ **内嵌服务器**：内置Tomcat，无需外部部署
- ✅ **依赖管理**：starter依赖自动管理版本
- ✅ **配置外部化**：统一的配置文件管理

### 1.3 SpringApplication的工作原理


```
就像开餐厅的完整流程：

第一步：选址装修 → 创建ApplicationContext容器
第二步：准备设备 → 加载配置和环境变量  
第三步：招聘员工 → 创建和注册Bean
第四步：制定流程 → 配置Bean之间的关系
第五步：开门营业 → 启动内嵌服务器
第六步：日常运营 → 处理请求和业务逻辑
```

---

## 2. 🔧 SpringApplication.run方法详解


### 2.1 run方法的作用


`SpringApplication.run()`是Spring Boot应用的**入口大门**，它就像汽车的点火器，一键启动整个应用程序。

```java
@SpringBootApplication
public class MyApplication {
    public static void main(String[] args) {
        // 这一行代码启动了整个Spring Boot应用
        SpringApplication.run(MyApplication.class, args);
    }
}
```

### 2.2 run方法的两种调用方式


**方式一：静态方法调用（推荐）**
```java
// 最简单的启动方式，适合大部分场景
SpringApplication.run(MyApplication.class, args);
```

**方式二：实例方法调用（可定制）**
```java
// 当你需要自定义启动行为时使用
SpringApplication app = new SpringApplication(MyApplication.class);
app.setBannerMode(Banner.Mode.OFF);  // 关闭启动横幅
app.setWebApplicationType(WebApplicationType.NONE);  // 设置应用类型
app.run(args);
```

### 2.3 run方法的核心流程


```
SpringApplication.run() 执行流程：

用户调用
    ↓
创建SpringApplication实例
    ↓
执行run()方法
    ↓
【步骤1】准备环境 Environment
    ↓
【步骤2】创建ApplicationContext
    ↓ 
【步骤3】准备Context
    ↓
【步骤4】刷新Context
    ↓
【步骤5】刷新后处理
    ↓
应用启动完成
```

### 2.4 run方法的返回值


```java
// run方法返回ConfigurableApplicationContext
ConfigurableApplicationContext context = SpringApplication.run(MyApplication.class, args);

// 可以通过返回的context获取Bean
MyService service = context.getBean(MyService.class);
```

**为什么返回ApplicationContext？**
- 🔸 **容器访问**：可以手动获取Spring容器中的Bean
- 🔸 **生命周期控制**：可以手动关闭应用程序
- 🔸 **运行时操作**：可以在运行时动态操作容器

---

## 3. 🏗️ Bootstrap启动流程


### 3.1 Bootstrap是什么


Bootstrap在Spring Boot中指的是**应用的引导启动过程**，就像建房子时的地基工程，为后续的所有工作打好基础。

**Bootstrap的核心任务**：
- 🔸 **环境检测**：检查运行环境是否满足要求
- 🔸 **配置加载**：加载application.properties等配置文件
- 🔸 **类路径扫描**：扫描classpath下的jar包和类
- 🔸 **自动配置准备**：准备自动配置所需的条件

### 3.2 Bootstrap启动的详细步骤


```
Bootstrap启动流程（像搭建舞台）：

阶段1：搭建舞台框架
├── 创建SpringApplication实例
├── 确定应用类型（Web/非Web）
└── 设置主要配置类

阶段2：准备舞台设备  
├── 加载ApplicationContextInitializer
├── 加载ApplicationListener
└── 确定main方法所在的类

阶段3：舞台最终检查
├── 验证配置类有效性
├── 检查必要依赖是否存在
└── 准备启动环境变量
```

### 3.3 应用类型的自动检测


Spring Boot会**自动检测**你的应用应该以什么方式运行：

| 应用类型 | 检测条件 | 说明 | 适用场景 |
|---------|----------|------|----------|
| **SERVLET** | 检测到`spring-webmvc`或`spring-webflux` | 传统的Web应用 | Web服务、REST API |
| **REACTIVE** | 检测到`WebFlux`但没有`WebMVC` | 响应式Web应用 | 高并发、异步处理 |
| **NONE** | 都没检测到 | 非Web应用 | 命令行工具、定时任务 |

```java
// 手动指定应用类型的例子
SpringApplication app = new SpringApplication(MyApplication.class);
app.setWebApplicationType(WebApplicationType.SERVLET);  // 强制指定为Web应用
```

### 3.4 Bootstrap配置的优先级


```
配置加载优先级（数字越小优先级越高）：

1. 命令行参数       java -jar app.jar --server.port=8080
2. 系统环境变量     export SERVER_PORT=8080  
3. application.properties  server.port=8080
4. @PropertySource注解
5. 默认配置         SpringBoot内置默认值
```

---

## 4. 🌍 环境准备阶段


### 4.1 Environment是什么


Environment就像是应用程序的**生存环境检测器**，它负责收集和管理应用运行所需的所有环境信息。

**Environment包含的信息**：
- **配置属性**：application.properties中的配置
- **系统属性**：JVM系统属性（如java.version）
- **环境变量**：操作系统环境变量（如PATH）
- **命令行参数**：启动时传入的参数

### 4.2 环境准备的核心步骤


```
环境准备流程（像准备旅行装备）：

步骤1：创建环境对象
└── 根据应用类型创建对应的Environment

步骤2：配置环境参数  
├── 解析命令行参数
├── 添加到Environment中
└── 设置默认的PropertySource

步骤3：发布环境准备事件
└── 通知监听器环境已准备就绪

步骤4：绑定环境到SpringApplication
└── 将环境信息绑定到应用配置中
```

### 4.3 配置文件的加载机制


Spring Boot按照特定的**搜索顺序**加载配置文件：

```
配置文件搜索路径（按优先级排序）：

1. ./config/          项目根目录下的config文件夹
2. ./                 项目根目录
3. classpath:/config/ classpath下的config包
4. classpath:/        classpath根目录

文件名搜索顺序：
- application.properties
- application.yml
- application.yaml
```

**实际示例**：
```
项目结构：
myapp/
├── config/
│   └── application.properties     ← 优先级最高
├── application.properties         ← 优先级第二
└── src/
    └── main/
        └── resources/
            ├── config/
            │   └── application.yml ← 优先级第三
            └── application.yml     ← 优先级最低
```

### 4.4 Profile环境配置


**Profile是什么？**
Profile就像是**不同场景的配置方案**，比如开发环境用一套配置，生产环境用另一套配置。

```
Profile配置示例：

application.properties          # 通用配置
application-dev.properties      # 开发环境配置  
application-test.properties     # 测试环境配置
application-prod.properties     # 生产环境配置
```

**激活Profile的方式**：
```bash
# 方式1：命令行参数
java -jar myapp.jar --spring.profiles.active=prod

# 方式2：环境变量
export SPRING_PROFILES_ACTIVE=prod

# 方式3：配置文件
spring.profiles.active=dev
```

---

## 5. 📦 ApplicationContext创建过程


### 5.1 ApplicationContext是什么


ApplicationContext是Spring的**核心容器**，就像一个超级工厂，负责创建、管理和组装所有的Bean对象。

**ApplicationContext的作用**：
- 🏭 **Bean工厂**：创建和管理Bean实例
- 🔗 **依赖注入器**：自动装配Bean之间的依赖关系
- 📋 **配置管理器**：管理应用的配置信息
- 📡 **事件发布器**：发布和监听应用事件

### 5.2 ApplicationContext的类型选择


Spring Boot会根据你的应用类型**自动选择**合适的ApplicationContext：

```
ApplicationContext类型选择逻辑：

if (应用类型 == WebApplicationType.SERVLET) {
    return new AnnotationConfigServletWebServerApplicationContext();
    // 支持Web功能，内嵌Servlet容器
}
else if (应用类型 == WebApplicationType.REACTIVE) {
    return new AnnotationConfigReactiveWebServerApplicationContext();  
    // 支持响应式Web功能
}
else {
    return new AnnotationConfigApplicationContext();
    // 普通的非Web应用
}
```

### 5.3 ApplicationContext创建流程


```
ApplicationContext创建过程（像建造工厂）：

阶段1：工厂选址
└── 根据应用类型确定Context实现类

阶段2：工厂建设
├── 创建ApplicationContext实例
├── 设置父Context（如果有）
└── 设置Environment环境

阶段3：工厂装备
├── 应用ApplicationContextInitializer
├── 注册配置类（@SpringBootApplication类）
└── 准备Bean定义注册器

阶段4：工厂验收  
└── 验证Context配置是否正确
```

### 5.4 ApplicationContextInitializer的作用


ApplicationContextInitializer是**Context的初始化定制器**，允许你在Context创建后、刷新前进行自定义配置。

```java
// 自定义ApplicationContextInitializer示例
public class MyContextInitializer 
    implements ApplicationContextInitializer<ConfigurableApplicationContext> {
    
    @Override
    public void initialize(ConfigurableApplicationContext context) {
        // 在这里可以对Context进行自定义配置
        System.out.println("正在初始化ApplicationContext...");
        
        // 添加自定义的PropertySource
        context.getEnvironment().getPropertySources()
            .addFirst(new MapPropertySource("my-props", 
                Map.of("my.custom.property", "custom-value")));
    }
}
```

---

## 6. 🔄 上下文刷新机制


### 6.1 Context刷新是什么


Context刷新是Spring Boot启动过程中的**核心环节**，就像工厂正式投产前的最后调试，确保所有设备都能正常工作。

**刷新过程的主要工作**：
- 🔧 **Bean定义加载**：扫描和注册所有Bean定义
- ⚙️ **Bean实例创建**：根据定义创建Bean实例
- 🔗 **依赖注入**：为Bean注入所需的依赖
- 🚀 **后置处理**：执行各种后置处理器
- 📡 **事件发布**：发布Context刷新完成事件

### 6.2 刷新机制的详细流程


```
Context刷新流程（像工厂生产线启动）：

第1步：准备刷新
├── 设置启动时间戳
├── 标记Context为激活状态
└── 初始化PropertySources

第2步：获取BeanFactory
└── 获取内部的DefaultListableBeanFactory

第3步：准备BeanFactory
├── 设置ClassLoader
├── 设置表达式解析器
└── 添加BeanPostProcessor

第4步：后置处理BeanFactory
└── 执行BeanFactoryPostProcessor

第5步：注册BeanPostProcessor
└── 注册Bean的后置处理器

第6步：初始化MessageSource
└── 国际化支持

第7步：初始化事件广播器
└── ApplicationEventMulticaster

第8步：刷新特定Context
└── 启动内嵌Web服务器（如Tomcat）

第9步：注册监听器
└── 注册ApplicationListener

第10步：完成BeanFactory初始化
├── 实例化所有非懒加载的单例Bean
└── 执行依赖注入

第11步：完成刷新
├── 清理缓存
├── 发布ContextRefreshedEvent事件
└── 启动LifecycleProcessor
```

### 6.3 Bean的创建和依赖注入


**Bean创建过程**像是**流水线生产**：

```
Bean创建流水线：

原材料进入 → Bean定义(BeanDefinition)
    ↓
检查工序 → 检查Bean是否已存在
    ↓  
生产工序 → 调用构造方法创建实例
    ↓
装配工序 → 注入属性依赖(@Autowired)
    ↓
质检工序 → BeanPostProcessor前置处理
    ↓
初始化工序 → 调用@PostConstruct方法
    ↓
再次质检 → BeanPostProcessor后置处理
    ↓
产品入库 → 放入Spring容器
```

**依赖注入的三种方式**：

```java
@Service
public class UserService {
    
    // 方式1：构造器注入（推荐）
    private final UserRepository userRepository;
    
    public UserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }
    
    // 方式2：字段注入（简单但不推荐）
    @Autowired
    private EmailService emailService;
    
    // 方式3：Setter注入（可选依赖时使用）
    private SmsService smsService;
    
    @Autowired
    public void setSmsService(SmsService smsService) {
        this.smsService = smsService;
    }
}
```

### 6.4 自动配置的执行


**自动配置**是Spring Boot的核心特性，它会根据你的classpath自动配置Bean：

```
自动配置执行过程：

1. 扫描classpath
   └── 查找META-INF/spring.factories文件

2. 加载自动配置类  
   └── 读取EnableAutoConfiguration指定的配置类

3. 条件判断
   ├── @ConditionalOnClass：类路径中是否有指定类
   ├── @ConditionalOnBean：容器中是否有指定Bean
   └── @ConditionalOnProperty：是否有指定配置属性

4. 创建Bean
   └── 满足条件就自动创建相应的Bean
```

**自动配置示例**：
```java
@Configuration
@ConditionalOnClass(DataSource.class)  // 只有classpath有DataSource才生效
@ConditionalOnProperty(name = "spring.datasource.url")  // 配置了数据库URL才生效
public class DataSourceAutoConfiguration {
    
    @Bean
    @ConditionalOnMissingBean  // 用户没有自定义DataSource才创建
    public DataSource dataSource() {
        // 自动创建数据源
        return new HikariDataSource();
    }
}
```

---

## 7. 🏃‍♂️ CommandLineRunner与ApplicationRunner执行


### 7.1 Runner接口的作用


CommandLineRunner和ApplicationRunner就像是应用启动后的**开业庆典主持人**，在所有准备工作完成后，执行一些开业必需的任务。

**Runner接口的使用场景**：
- 🔸 **数据初始化**：启动时插入初始数据
- 🔸 **缓存预热**：提前加载热点数据到缓存
- 🔸 **健康检查**：检查外部服务连接状态
- 🔸 **定时任务启动**：启动后台定时任务
- 🔸 **清理工作**：清理临时文件或过期数据

### 7.2 CommandLineRunner详解


**CommandLineRunner**接收的是**原始的命令行参数数组**：

```java
@Component
public class MyCommandLineRunner implements CommandLineRunner {
    
    private static final Logger logger = LoggerFactory.getLogger(MyCommandLineRunner.class);
    
    @Override
    public void run(String... args) throws Exception {
        logger.info("应用启动完成，开始执行初始化任务...");
        
        // 打印所有命令行参数
        for (int i = 0; i < args.length; i++) {
            logger.info("参数[{}]: {}", i, args[i]);
        }
        
        // 执行初始化逻辑
        initializeData();
    }
    
    private void initializeData() {
        // 初始化数据的逻辑
        logger.info("正在初始化基础数据...");
    }
}
```

### 7.3 ApplicationRunner详解


**ApplicationRunner**接收的是**解析后的应用参数**，使用起来更方便：

```java
@Component  
public class MyApplicationRunner implements ApplicationRunner {
    
    private static final Logger logger = LoggerFactory.getLogger(MyApplicationRunner.class);
    
    @Override
    public void run(ApplicationArguments args) throws Exception {
        logger.info("ApplicationRunner开始执行...");
        
        // 获取选项参数（--key=value格式）
        Set<String> optionNames = args.getOptionNames();
        for (String optionName : optionNames) {
            List<String> values = args.getOptionValues(optionName);
            logger.info("选项 --{} 的值: {}", optionName, values);
        }
        
        // 获取非选项参数
        List<String> nonOptionArgs = args.getNonOptionArgs();
        logger.info("非选项参数: {}", nonOptionArgs);
        
        // 检查特定参数
        if (args.containsOption("init-data")) {
            logger.info("检测到 --init-data 参数，开始初始化数据...");
            initializeDatabase();
        }
    }
    
    private void initializeDatabase() {
        // 数据库初始化逻辑
        logger.info("数据库初始化完成");
    }
}
```

### 7.4 两种Runner的区别对比


| 特性 | CommandLineRunner | ApplicationRunner |
|------|-------------------|-------------------|
| **参数类型** | `String... args` | `ApplicationArguments args` |
| **参数解析** | 原始字符串数组 | 解析后的参数对象 |
| **使用便利性** | 需要自己解析参数 | 提供便捷的参数访问方法 |
| **适用场景** | 简单的参数处理 | 复杂的参数处理 |

**命令行参数示例**：
```bash
# 启动命令
java -jar myapp.jar --server.port=8080 --init-data=true arg1 arg2

# CommandLineRunner接收到：
# args[0] = "--server.port=8080"  
# args[1] = "--init-data=true"
# args[2] = "arg1"
# args[3] = "arg2"

# ApplicationRunner可以：
# args.getOptionValues("server.port") → ["8080"]
# args.getOptionValues("init-data") → ["true"] 
# args.getNonOptionArgs() → ["arg1", "arg2"]
```

### 7.5 Runner的执行顺序控制


当有多个Runner时，可以使用`@Order`注解控制执行顺序：

```java
@Component
@Order(1)  // 数字越小越先执行
public class DatabaseInitRunner implements CommandLineRunner {
    @Override
    public void run(String... args) throws Exception {
        System.out.println("第1步：初始化数据库");
    }
}

@Component
@Order(2)
public class CacheWarmupRunner implements ApplicationRunner {
    @Override
    public void run(ApplicationArguments args) throws Exception {
        System.out.println("第2步：预热缓存");
    }
}

@Component  
@Order(3)
public class HealthCheckRunner implements CommandLineRunner {
    @Override
    public void run(String... args) throws Exception {
        System.out.println("第3步：健康检查");
    }
}
```

### 7.6 Runner中的异常处理


Runner中的异常需要妥善处理，否则可能导致应用启动失败：

```java
@Component
public class SafeRunner implements ApplicationRunner {
    
    private static final Logger logger = LoggerFactory.getLogger(SafeRunner.class);
    
    @Override
    public void run(ApplicationArguments args) throws Exception {
        try {
            // 执行可能失败的初始化任务
            riskyInitializationTask();
            
        } catch (Exception e) {
            // 记录异常但不抛出，避免影响应用启动
            logger.error("初始化任务执行失败，但应用仍可正常启动", e);
            
            // 或者根据业务需求决定是否抛出异常
            if (isCriticalTask()) {
                throw new RuntimeException("关键初始化任务失败，应用无法启动", e);
            }
        }
    }
    
    private void riskyInitializationTask() throws Exception {
        // 可能失败的任务...
    }
    
    private boolean isCriticalTask() {
        // 判断是否是关键任务
        return false;
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 Spring Boot启动流程全貌


```
完整启动流程时间线：

T0: main方法调用
    ↓
T1: 创建SpringApplication实例
    ├── 确定应用类型
    ├── 加载初始化器和监听器  
    └── 确定主配置类
    ↓
T2: 执行run方法
    ├── 准备Environment环境
    ├── 打印Banner横幅
    ├── 创建ApplicationContext
    └── 准备Context
    ↓
T3: 刷新ApplicationContext
    ├── 加载Bean定义
    ├── 创建Bean实例
    ├── 执行依赖注入
    ├── 启动内嵌服务器
    └── 执行自动配置
    ↓
T4: 刷新后处理
    ├── 执行CommandLineRunner
    ├── 执行ApplicationRunner
    └── 发布ApplicationReadyEvent
    ↓
T5: 应用启动完成，开始处理请求
```

### 8.2 关键概念速记


| 概念 | 作用 | 类比 |
|------|------|------|
| **SpringApplication** | 应用启动器 | 餐厅管家，全程协调 |
| **Environment** | 环境配置管理 | 生存环境检测器 |
| **ApplicationContext** | Spring容器 | 超级工厂，生产Bean |
| **Context刷新** | 容器初始化 | 工厂生产线启动 |
| **自动配置** | 智能配置Bean | 根据条件自动装配设备 |
| **Runner接口** | 启动后执行器 | 开业庆典主持人 |

### 8.3 实际开发建议


**启动优化技巧**：
- ✅ **合理使用@Lazy**：延迟初始化非关键Bean
- ✅ **Profile分环境**：不同环境加载不同配置
- ✅ **排除不需要的自动配置**：提高启动速度
- ✅ **异步初始化**：Runner中的耗时任务异步执行

**常见问题排查**：
- 🔍 **启动慢**：检查是否有耗时的Bean初始化
- 🔍 **端口占用**：检查server.port配置和进程占用
- 🔍 **Bean创建失败**：检查依赖注入和自动配置条件
- 🔍 **配置不生效**：检查Profile激活和配置文件优先级

**最佳实践总结**：
```java
// 推荐的应用入口写法
@SpringBootApplication
@EnableScheduling  // 需要定时任务时才加
@EnableAsync      // 需要异步处理时才加  
public class MyApplication {
    
    public static void main(String[] args) {
        // 可以根据需要自定义启动行为
        SpringApplication app = new SpringApplication(MyApplication.class);
        
        // 生产环境建议关闭Banner
        app.setBannerMode(Banner.Mode.OFF);
        
        app.run(args);
    }
}
```

**记忆口诀**：
- Spring Boot启动像开店，准备环境创建间
- 容器刷新装设备，自动配置很智能  
- Runner执行开门庆，应用启动就完成
- 理解流程排故障，开发部署都不难