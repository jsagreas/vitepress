---
title: 2、Jackson配置与序列化
---
## 📚 目录


1. [Jackson基础概念](#1-jackson基础概念)
2. [日期时间格式化配置](#2-日期时间格式化配置)
3. [时区处理策略](#3-时区处理策略)
4. [空值处理规则](#4-空值处理规则)
5. [反序列化安全配置](#5-反序列化安全配置)
6. [全局命名策略](#6-全局命名策略)
7. [自定义序列化器](#7-自定义序列化器)
8. [核心要点总结](#8-核心要点总结)

---

# 🎯 **学习路径导航**


**前置知识**：需要掌握Spring Boot基础、Java对象概念 → **当前内容**：Jackson配置与序列化 → **后续学习**：建议学习API设计与数据校验

⏱️ **预计学习时间**：本章预计45分钟 | 实践练习30分钟

🏷️ **知识标签**：`#Spring Boot核心` `#JSON处理` `#数据转换` `#必掌握技能`

---

## 1. 📖 Jackson基础概念



### 1.1 什么是Jackson



**🔸 核心定义**
```
Jackson：Java中最流行的JSON处理库
作用：把Java对象转成JSON，把JSON转成Java对象
在Spring Boot中：默认的JSON处理工具
```

**💡 生活化理解**
想象Jackson是一个"翻译员"：
- **序列化**：把中文（Java对象）翻译成英文（JSON）
- **反序列化**：把英文（JSON）翻译成中文（Java对象）
- Spring Boot内置了这个翻译员，我们只需要告诉它怎么翻译

### 1.2 为什么需要配置Jackson



**🤔 常见问题场景**
```
用户注册时间显示问题：
前端显示：2025-09-22T14:30:00.000Z
用户看到：这是什么鬼时间？

数据传输问题：
Java中：userName
前端需要：user_name
结果：字段名对不上，数据丢失

空值问题：
用户没填写的字段传null给前端
前端页面：显示一堆null，体验很差
```

### 1.3 Jackson在Spring Boot中的位置



**🏗️ 数据流转示意图**
```
客户端发送JSON
       ↓
   Controller接收
       ↓
Jackson反序列化 → Java对象
       ↓
   业务逻辑处理
       ↓
Jackson序列化 → JSON响应
       ↓
   返回给客户端
```

**🔑 关键理解**
- Jackson负责JSON和Java对象之间的转换
- 配置Jackson就是告诉它转换的具体规则
- 配置一次，全项目生效

---

## 2. 📅 日期时间格式化配置



### 2.1 默认时间格式问题



**❌ 未配置时的问题**
```java
@RestController
public class UserController {
    @GetMapping("/user")
    public User getUser() {
        User user = new User();
        user.setCreateTime(new Date());
        return user;
    }
}

// 前端收到的JSON
{
    "createTime": 1695377400000  // 这是时间戳，用户看不懂
}
```

**💭 用户困惑**
- 用户：这个1695377400000是什么意思？
- 开发者：这是时间戳...
- 用户：我想看到"2025-09-22 14:30:00"这样的时间

### 2.2 全局日期格式配置



**✅ 配置方法一：application.yml**
```yaml
spring:
  jackson:
#    # 日期格式化模式
    date-format: yyyy-MM-dd HH:mm:ss
#    # 序列化时间戳为格式化字符串
    serialization:
      write-dates-as-timestamps: false
```

**📝 配置解释**
- `date-format`：告诉Jackson用什么格式显示日期
- `write-dates-as-timestamps: false`：不要用时间戳，用字符串

**🎯 效果对比**
```json
// 配置前
{
    "createTime": 1695377400000
}

// 配置后  
{
    "createTime": "2025-09-22 14:30:00"
}
```

### 2.3 Java配置方式



**⚙️ 配置类实现**
```java
@Configuration
public class JacksonConfig {
    
    @Bean
    @Primary
    public ObjectMapper objectMapper() {
        ObjectMapper mapper = new ObjectMapper();
        
        // 设置日期格式
        SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        mapper.setDateFormat(dateFormat);
        
        // 不使用时间戳
        mapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);
        
        return mapper;
    }
}
```

**🤔 为什么用Java配置**
- 更灵活：可以写复杂的配置逻辑
- 更强大：可以配置YAML无法配置的功能
- 更直观：配置逻辑一目了然

### 2.4 不同时间类型的处理



**📊 时间类型对比**

| **时间类型** | **默认格式** | **推荐用法** | **配置复杂度** |
|-------------|-------------|-------------|----------------|
| `java.util.Date` | 时间戳 | 传统项目 | ⭐ |
| `LocalDateTime` | 数组格式 | 新项目推荐 | ⭐⭐ |
| `Instant` | 字符串 | 需要时区 | ⭐⭐⭐ |

**💡 LocalDateTime配置**
```java
@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
private LocalDateTime createTime;

// 或者全局配置
@Bean
public ObjectMapper objectMapper() {
    ObjectMapper mapper = new ObjectMapper();
    mapper.registerModule(new JavaTimeModule());
    return mapper;
}
```

---

## 3. 🌍 时区处理策略



### 3.1 时区问题的困扰



**🌏 真实场景**
```
北京用户：2025-09-22 14:30 注册
纽约用户看到：2025-09-22 02:30 
纽约用户：这用户凌晨2点半注册的？这么勤奋？

问题根源：服务器用UTC时间，前端显示没转换
```

### 3.2 时区配置策略



**⚙️ 服务端时区配置**
```yaml
spring:
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss
```

**🔧 Java配置方式**
```java
@Bean
public ObjectMapper objectMapper() {
    ObjectMapper mapper = new ObjectMapper();
    
    // 设置时区
    mapper.setTimeZone(TimeZone.getTimeZone("Asia/Shanghai"));
    
    // 设置日期格式
    SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
    dateFormat.setTimeZone(TimeZone.getTimeZone("Asia/Shanghai"));
    mapper.setDateFormat(dateFormat);
    
    return mapper;
}
```

### 3.3 时区处理最佳实践



**💡 推荐方案：UTC存储 + 前端转换**
```java
@Entity
public class User {
    // 数据库存储UTC时间
    private Instant createTime;
    
    // JSON输出时转换为指定时区
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "Asia/Shanghai")
    public String getCreateTimeFormatted() {
        return createTime.atZone(ZoneId.of("Asia/Shanghai"))
                        .format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
    }
}
```

**🎯 处理原则**
```
存储原则：数据库永远存UTC时间
传输原则：API可以传用户时区时间  
显示原则：前端根据用户时区显示
```

---

## 4. 🔄 空值处理规则



### 4.1 空值问题分析



**❌ 默认空值处理**
```java
public class UserProfile {
    private String name;
    private String nickname;     // 用户没设置，为null
    private String avatar;       // 用户没上传，为null
    private Integer age;         // 用户没填写，为null
}

// 默认JSON输出
{
    "name": "张三",
    "nickname": null,
    "avatar": null, 
    "age": null
}
```

**😤 前端开发者的抱怨**
- "这么多null，我还要一个个判断"
- "能不能不要的字段就别传了"
- "null值让页面显示很难看"

### 4.2 全局空值处理配置



**⚙️ YAML配置**
```yaml
spring:
  jackson:
#    # 忽略空值字段
    default-property-inclusion: non_null
    serialization:
#      # 忽略空的集合和数组
      fail-on-empty-beans: false
```

**🔧 Java配置方式**
```java
@Bean
public ObjectMapper objectMapper() {
    ObjectMapper mapper = new ObjectMapper();
    
    // 忽略null值
    mapper.setSerializationInclusion(JsonInclude.Include.NON_NULL);
    
    // 忽略空集合
    mapper.setSerializationInclusion(JsonInclude.Include.NON_EMPTY);
    
    return mapper;
}
```

**✅ 配置后的效果**
```json
// 配置后只显示有值的字段
{
    "name": "张三"
}
```

### 4.3 字段级别的空值控制



**🎯 精确控制**
```java
public class UserProfile {
    private String name;
    
    // 这个字段即使为null也要显示
    @JsonInclude(JsonInclude.Include.ALWAYS)
    private String avatar;
    
    // 这个字段为空时不显示
    @JsonInclude(JsonInclude.Include.NON_NULL)
    private String nickname;
    
    // 这个字段为空或空字符串都不显示
    @JsonInclude(JsonInclude.Include.NON_EMPTY)
    private String description;
}
```

### 4.4 空值处理策略选择



**📋 不同策略对比**

| **策略** | **效果** | **适用场景** |
|---------|---------|-------------|
| `ALWAYS` | 所有字段都显示 | 严格的API规范 |
| `NON_NULL` | 不显示null字段 | 一般Web应用 |
| `NON_EMPTY` | 不显示null和空值 | 移动端API |
| `NON_DEFAULT` | 不显示默认值 | 配置类序列化 |

**💡 选择建议**
- **移动端API**：用NON_EMPTY，节省流量
- **Web端API**：用NON_NULL，前端处理简单  
- **系统对接**：用ALWAYS，字段明确

---

## 5. 🔒 反序列化安全配置



### 5.1 安全问题解析



**⚠️ 潜在安全风险**
```java
// 恶意JSON数据
{
    "name": "张三",
    "password": "secret123",
    "adminFlag": true,        // 恶意添加管理员标识
    "deleteFlag": false       // 恶意修改删除标识
}

// 如果没有安全配置，这些字段可能被恶意设置
```

**🤔 问题思考**
- 用户能通过JSON设置不该设置的字段吗？
- 如何防止未知字段的注入？
- 怎样保证数据的安全性？

### 5.2 安全配置实现



**🛡️ 基础安全配置**
```yaml
spring:
  jackson:
    deserialization:
#      # 遇到未知字段时失败（安全但严格）
      fail-on-unknown-properties: true
#      # 遇到空对象时不失败
      fail-on-empty-beans: false
```

**⚙️ Java安全配置**
```java
@Bean
public ObjectMapper objectMapper() {
    ObjectMapper mapper = new ObjectMapper();
    
    // 遇到未知属性时失败
    mapper.configure(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, true);
    
    // 遇到null值时不失败
    mapper.configure(DeserializationFeature.FAIL_ON_NULL_FOR_PRIMITIVES, false);
    
    // 遇到数字溢出时失败
    mapper.configure(DeserializationFeature.FAIL_ON_NUMBERS_FOR_ENUMS, true);
    
    return mapper;
}
```

### 5.3 字段级别安全控制



**🔐 精确安全控制**
```java
public class User {
    // 允许前端设置的字段
    private String name;
    private String email;
    
    // 禁止前端设置的字段
    @JsonIgnore  // 完全忽略，JSON中不出现
    private String password;
    
    @JsonProperty(access = JsonProperty.Access.READ_ONLY)  // 只读，不能设置
    private Boolean adminFlag;
    
    @JsonProperty(access = JsonProperty.Access.WRITE_ONLY)  // 只写，不显示
    private String oldPassword;
}
```

**📊 访问控制对比**

| **注解** | **序列化** | **反序列化** | **适用场景** |
|---------|----------|------------|-------------|
| `@JsonIgnore` | ❌不包含 | ❌不处理 | 敏感字段完全隐藏 |
| `READ_ONLY` | ✅包含 | ❌不处理 | 系统字段只展示 |
| `WRITE_ONLY` | ❌不包含 | ✅处理 | 密码等只接收不返回 |

### 5.4 自定义安全验证



**🔍 复杂安全逻辑**
```java
@Component
public class SecurityDeserializer extends JsonDeserializer<User> {
    
    @Override
    public User deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
        JsonNode node = p.getCodec().readTree(p);
        
        // 安全检查：禁止设置管理员字段
        if (node.has("adminFlag")) {
            throw new SecurityException("不允许设置管理员标识");
        }
        
        // 安全检查：用户名长度限制
        if (node.has("name")) {
            String name = node.get("name").asText();
            if (name.length() > 50) {
                throw new ValidationException("用户名过长");
            }
        }
        
        // 正常反序列化
        User user = new User();
        user.setName(node.get("name").asText());
        return user;
    }
}
```

---

## 6. 🏷️ 全局命名策略



### 6.1 命名冲突问题



**🤝 前后端命名习惯**
```
Java开发习惯：驼峰命名 (camelCase)  
- userName, createTime, isActive

前端开发习惯：下划线命名 (snake_case)
- user_name, create_time, is_active

数据库习惯：下划线命名
- user_name, create_time, is_active
```

**😕 不配置的后果**
```java
// Java对象
public class User {
    private String userName;
    private LocalDateTime createTime;
}

// 前端收到的JSON
{
    "userName": "张三",        // 前端期望：user_name
    "createTime": "2025-09-22" // 前端期望：create_time
}

// 结果：前端拿不到数据，页面显示空白
```

### 6.2 全局命名策略配置



**⚙️ YAML配置**
```yaml
spring:
  jackson:
#    # 下划线命名策略
    property-naming-strategy: SNAKE_CASE
#    # 其他可选策略：
#    # KEBAB_CASE: user-name
#    # UPPER_CAMEL_CASE: UserName  
#    # LOWER_CAMEL_CASE: userName (默认)
```

**🔧 Java配置**
```java
@Bean
public ObjectMapper objectMapper() {
    ObjectMapper mapper = new ObjectMapper();
    
    // 设置命名策略为下划线
    mapper.setPropertyNamingStrategy(PropertyNamingStrategies.SNAKE_CASE);
    
    return mapper;
}
```

**✅ 配置后效果**
```java
// Java代码不变
public class User {
    private String userName;
    private LocalDateTime createTime;
}

// JSON自动转换
{
    "user_name": "张三",
    "create_time": "2025-09-22 14:30:00"
}
```

### 6.3 命名策略选择指南



**📋 各种命名策略对比**

| **策略** | **示例** | **适用场景** | **兼容性** |
|---------|---------|-------------|-----------|
| `SNAKE_CASE` | user_name | 前端为主的项目 | ⭐⭐⭐⭐⭐ |
| `KEBAB_CASE` | user-name | RESTful风格API | ⭐⭐⭐ |
| `LOWER_CAMEL_CASE` | userName | Java为主的项目 | ⭐⭐⭐⭐ |
| `UPPER_CAMEL_CASE` | UserName | .NET风格项目 | ⭐⭐ |

**💡 选择建议**
- **前端团队强势**：选择SNAKE_CASE
- **Java团队主导**：保持LOWER_CAMEL_CASE  
- **多语言团队**：根据主要客户端决定

### 6.4 字段级别命名控制



**🎯 精确命名控制**
```java
public class User {
    // 使用全局策略：user_name
    private String userName;
    
    // 自定义字段名：uid
    @JsonProperty("uid")
    private Long userId;
    
    // 保持原始名称：createTime
    @JsonProperty("createTime")
    private LocalDateTime createTime;
}
```

---

## 7. 🎨 自定义序列化器



### 7.1 为什么需要自定义序列化器



**🤔 复杂场景需求**
```java
// 普通的Money类
public class Money {
    private BigDecimal amount;  
    private String currency;
}

// 默认JSON输出
{
    "amount": 12.50,
    "currency": "CNY"
}

// 业务需求：显示为 "¥12.50"
// 默认配置做不到，需要自定义
```

**💡 常见自定义场景**
- 金额格式化：12.50 → "¥12.50"
- 枚举友好显示：1 → "启用"
- 敏感信息脱敏：13812345678 → "138****5678"
- 复杂对象简化：User对象 → "张三(ID:123)"

### 7.2 自定义序列化器实现



**🎨 金额格式化序列化器**
```java
public class MoneySerializer extends JsonSerializer<Money> {
    
    @Override
    public void serialize(Money money, JsonGenerator gen, SerializerProvider serializers) 
            throws IOException {
        
        if (money == null) {
            gen.writeNull();
            return;
        }
        
        // 根据币种选择符号
        String symbol = getCurrencySymbol(money.getCurrency());
        
        // 格式化金额
        DecimalFormat df = new DecimalFormat("#,##0.00");
        String formattedAmount = df.format(money.getAmount());
        
        // 输出格式化后的字符串
        gen.writeString(symbol + formattedAmount);
    }
    
    private String getCurrencySymbol(String currency) {
        switch (currency) {
            case "CNY": return "¥";
            case "USD": return "$";
            case "EUR": return "€";
            default: return currency + " ";
        }
    }
}
```

**📝 使用自定义序列化器**
```java
public class Product {
    private String name;
    
    // 应用自定义序列化器
    @JsonSerialize(using = MoneySerializer.class)
    private Money price;
}

// 输出结果
{
    "name": "iPhone 15",
    "price": "¥8,999.00"
}
```

### 7.3 常用自定义序列化器示例



**🔐 手机号脱敏序列化器**
```java
public class PhoneSerializer extends JsonSerializer<String> {
    
    @Override
    public void serialize(String phone, JsonGenerator gen, SerializerProvider serializers) 
            throws IOException {
        
        if (phone == null || phone.length() < 7) {
            gen.writeString(phone);
            return;
        }
        
        // 脱敏处理：保留前3位和后4位
        String masked = phone.substring(0, 3) + "****" + phone.substring(phone.length() - 4);
        gen.writeString(masked);
    }
}

// 使用示例
public class User {
    @JsonSerialize(using = PhoneSerializer.class)
    private String phone;  // 13812345678 → 138****5678
}
```

**🏷️ 枚举友好显示序列化器**
```java
public enum UserStatus {
    ACTIVE(1, "启用"),
    INACTIVE(0, "禁用"),
    LOCKED(-1, "锁定");
    
    private int code;
    private String description;
    
    // 构造函数和getter省略...
}

public class UserStatusSerializer extends JsonSerializer<UserStatus> {
    
    @Override
    public void serialize(UserStatus status, JsonGenerator gen, SerializerProvider serializers) 
            throws IOException {
        
        // 输出包含代码和描述的对象
        gen.writeStartObject();
        gen.writeNumberField("code", status.getCode());
        gen.writeStringField("name", status.name());
        gen.writeStringField("description", status.getDescription());
        gen.writeEndObject();
    }
}

// 输出结果
{
    "status": {
        "code": 1,
        "name": "ACTIVE", 
        "description": "启用"
    }
}
```

### 7.4 全局注册自定义序列化器



**🌍 全局配置**
```java
@Configuration
public class JacksonConfig {
    
    @Bean
    public ObjectMapper objectMapper() {
        ObjectMapper mapper = new ObjectMapper();
        
        // 创建自定义序列化器模块
        SimpleModule module = new SimpleModule();
        
        // 注册自定义序列化器
        module.addSerializer(Money.class, new MoneySerializer());
        module.addSerializer(String.class, new PhoneSerializer());
        module.addSerializer(UserStatus.class, new UserStatusSerializer());
        
        // 注册模块到ObjectMapper
        mapper.registerModule(module);
        
        return mapper;
    }
}
```

**💡 注册方式对比**

| **方式** | **作用范围** | **灵活性** | **适用场景** |
|---------|-------------|-----------|-------------|
| `@JsonSerialize` | 单个字段 | ⭐⭐⭐ | 特殊字段处理 |
| `全局注册` | 所有相同类型 | ⭐⭐⭐⭐⭐ | 统一格式规范 |
| `配置类注册` | 指定类型 | ⭐⭐⭐⭐ | 业务逻辑定制 |

---

## 8. 📋 核心要点总结



### 8.1 必须掌握的核心概念



```
🔸 Jackson本质：Java对象和JSON之间的翻译器
🔸 配置目的：告诉Jackson怎么翻译，翻译成什么样
🔸 全局配置：一次配置，整个项目生效
🔸 字段配置：针对特定字段的精确控制
🔸 序列化：Java对象 → JSON（给前端看）
🔸 反序列化：JSON → Java对象（处理前端数据）
```

### 8.2 关键配置理解要点



**🔹 日期时间配置重点**
```
时间戳问题：用户看不懂数字时间戳
解决方案：配置为用户友好的时间格式
时区问题：全球化应用必须考虑
最佳实践：服务器存UTC，显示用本地时区
```

**🔹 空值处理策略**
```
NON_NULL：不显示null字段，减少前端判断
NON_EMPTY：不显示空值，节省网络传输
选择原则：根据前端团队习惯和业务需求
```

**🔹 命名策略选择**
```
问题根源：Java用驼峰，前端/数据库用下划线
解决思路：全局统一命名风格
选择依据：团队主导语言和现有规范
```

### 8.3 安全配置要点



**🛡️ 安全原则**
```
最小权限：只允许设置该设置的字段
输入验证：拒绝未知字段，防止恶意注入
敏感保护：密码等敏感信息特殊处理
异常处理：安全失败时给出明确提示
```

### 8.4 实际应用价值



**🎯 开发场景应用**
- **团队协作**：统一的数据格式规范
- **前端联调**：减少字段名不匹配问题
- **国际化项目**：正确的时区和时间显示
- **安全防护**：防止恶意数据注入

**💡 最佳实践建议**
```
配置原则：
- 优先使用全局配置，减少重复代码
- 特殊需求才用字段级配置
- 安全配置不能省，防患于未然

性能考虑：
- 自定义序列化器要考虑性能影响
- 复杂逻辑可以缓存计算结果
- 大量数据序列化时要测试性能
```

### 8.5 学习检查清单



- [ ] 理解Jackson在Spring Boot中的作用
- [ ] 会配置日期时间格式化
- [ ] 掌握空值处理策略选择
- [ ] 了解命名策略的影响
- [ ] 知道基本的安全配置
- [ ] 能编写简单的自定义序列化器

**🔑 核心记忆要点**
> Jackson是JSON翻译器，配置它就是定规则
> 时间格式要友好，空值策略要合理
> 命名统一很重要，安全配置不能少
> 自定义序列化器，复杂需求都能搞

**💡 延伸学习建议**
- 学习Jackson高级特性：@JsonView、@JsonFilter
- 了解其他JSON库：Gson、Fastjson对比
- 研究JSON Schema验证和API文档生成