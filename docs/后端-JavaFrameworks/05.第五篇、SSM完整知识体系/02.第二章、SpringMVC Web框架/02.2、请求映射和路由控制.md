---
title: 2、请求映射和路由控制
---
## 📚 目录

1. [请求映射基础概念](#1-请求映射基础概念)
2. [核心映射注解详解](#2-核心映射注解详解)
3. [路径映射规则](#3-路径映射规则)
4. [请求限定条件](#4-请求限定条件)
5. [RESTful风格设计](#5-RESTful风格设计)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 请求映射基础概念


### 1.1 什么是请求映射


**通俗理解**：就像快递员送包裹，需要知道送到哪个地址、哪个门牌号。请求映射就是告诉SpringMVC：当用户访问某个网址时，应该调用哪个方法来处理。

```
用户访问：http://localhost:8080/user/list
         ↓
SpringMVC根据映射规则找到对应的方法
         ↓
执行方法，返回结果给用户
```

**专业定义**：请求映射（Request Mapping）是将HTTP请求URL映射到具体Controller方法的机制，它决定了哪个请求由哪个方法处理。

### 1.2 为什么需要请求映射


**实际问题**：
```
网站有很多功能：
- 查看用户列表 → 需要一个方法处理
- 添加新用户   → 需要另一个方法处理
- 删除用户     → 又需要一个方法处理

怎么让SpringMVC知道调用哪个方法？
→ 答案：通过请求映射注解！
```

### 1.3 映射的本质


**核心理解**：
- 🔹 **URL** = 钥匙（用户访问的地址）
- 🔹 **Controller方法** = 锁（处理请求的代码）
- 🔹 **映射注解** = 钥匙孔（告诉哪把钥匙配哪把锁）

---

## 2. 🔧 核心映射注解详解


### 2.1 @RequestMapping - 万能映射注解


**作用说明**：这是SpringMVC中最基础、最通用的映射注解，其他所有映射注解都是它的简化版本。

**基本使用**：
```java
@Controller
public class UserController {
    
    // 基础映射：访问 /user 就会调用这个方法
    @RequestMapping("/user")
    public String getUser() {
        return "user";
    }
}
```

**常用属性**：

| 属性 | 说明 | 示例 |
|------|------|------|
| `value` | 映射的URL路径 | `value = "/user"` |
| `method` | 限定请求方式 | `method = RequestMethod.GET` |
| `params` | 限定请求参数 | `params = "id"` |
| `headers` | 限定请求头 | `headers = "content-type=application/json"` |

**类级别映射**：
```java
@Controller
@RequestMapping("/user")  // 基础路径
public class UserController {
    
    @RequestMapping("/list")  // 实际路径：/user/list
    public String list() {
        return "userList";
    }
    
    @RequestMapping("/add")   // 实际路径：/user/add
    public String add() {
        return "userAdd";
    }
}
```

> 💡 **理解要点**：类上的 `@RequestMapping` 相当于给所有方法加了一个"统一前缀"，避免路径重复。

### 2.2 @GetMapping - GET请求专用


**通俗解释**：专门处理"查询、获取数据"的请求，就像你在浏览器地址栏输入网址访问页面。

**等价关系**：
```java
// 这两种写法完全一样
@RequestMapping(value = "/user", method = RequestMethod.GET)
@GetMapping("/user")
```

**典型场景**：
```java
@Controller
@RequestMapping("/user")
public class UserController {
    
    // 查看用户列表
    @GetMapping("/list")
    public String list() {
        return "userList";
    }
    
    // 查看用户详情
    @GetMapping("/detail")
    public String detail() {
        return "userDetail";
    }
}
```

> 📌 **记忆技巧**：GET = 获取（Get），用于查询数据，不会修改数据。

### 2.3 @PostMapping - POST请求专用


**通俗解释**：专门处理"提交表单、添加数据"的请求，就像你填写注册表单点击"提交"按钮。

**典型场景**：
```java
@Controller
@RequestMapping("/user")
public class UserController {
    
    // 添加新用户（接收表单数据）
    @PostMapping("/add")
    public String add(User user) {
        // 保存用户到数据库
        return "redirect:/user/list";
    }
    
    // 用户登录
    @PostMapping("/login")
    public String login(String username, String password) {
        // 验证用户名密码
        return "home";
    }
}
```

> 📌 **记忆技巧**：POST = 投递（Post），用于提交数据，会修改服务器数据。

### 2.4 @PutMapping - PUT请求专用


**通俗解释**：专门处理"修改、更新数据"的请求，就像你修改个人资料。

**RESTful场景**：
```java
@RestController
@RequestMapping("/user")
public class UserRestController {
    
    // 更新用户信息
    @PutMapping("/{id}")
    public String update(@PathVariable Long id, @RequestBody User user) {
        // 根据id更新用户信息
        return "更新成功";
    }
}
```

> 💡 **理解要点**：PUT通常用于完整更新整个资源。

### 2.5 @DeleteMapping - DELETE请求专用


**通俗解释**：专门处理"删除数据"的请求。

**RESTful场景**：
```java
@RestController
@RequestMapping("/user")
public class UserRestController {
    
    // 删除用户
    @DeleteMapping("/{id}")
    public String delete(@PathVariable Long id) {
        // 根据id删除用户
        return "删除成功";
    }
}
```

> 📌 **记忆技巧**：DELETE = 删除，用于删除资源。

### 2.6 @PatchMapping - PATCH请求专用


**通俗解释**：专门处理"部分更新数据"的请求，就像你只修改用户的手机号，不动其他信息。

**典型场景**：
```java
@RestController
@RequestMapping("/user")
public class UserRestController {
    
    // 部分更新：只更新手机号
    @PatchMapping("/{id}/phone")
    public String updatePhone(@PathVariable Long id, @RequestParam String phone) {
        // 只更新手机号字段
        return "手机号更新成功";
    }
}
```

**PUT vs PATCH 对比**：

| 注解 | 更新方式 | 举例说明 |
|------|---------|---------|
| `@PutMapping` | **完整更新** | 修改用户时必须提供所有字段 |
| `@PatchMapping` | **部分更新** | 修改用户时只提供要改的字段 |

---

## 3. 🛤️ 路径映射规则


### 3.1 精确路径映射


**说明**：最简单直接的映射方式，一个路径对应一个方法。

```java
@Controller
public class ProductController {
    
    @GetMapping("/product/list")  // 精确匹配 /product/list
    public String list() {
        return "productList";
    }
}
```

### 3.2 路径变量映射


**通俗理解**：URL路径中有些部分是"变化的"，比如用户ID、商品ID，我们可以用花括号 `{}` 把它们标记出来。

**基本用法**：
```java
@Controller
@RequestMapping("/user")
public class UserController {
    
    // {id} 是路径变量，会动态变化
    @GetMapping("/{id}")
    public String detail(@PathVariable Long id) {
        // 访问 /user/1  → id = 1
        // 访问 /user/99 → id = 99
        return "userDetail";
    }
}
```

**多个路径变量**：
```java
@GetMapping("/{userId}/order/{orderId}")
public String orderDetail(
    @PathVariable Long userId,
    @PathVariable Long orderId
) {
    // 访问 /user/10/order/888
    // userId = 10, orderId = 888
    return "orderDetail";
}
```

**指定变量名**：
```java
@GetMapping("/user/{uid}")
public String getUser(@PathVariable("uid") Long userId) {
    // 路径变量名是uid，但方法参数名是userId
    return "user";
}
```

> 💡 **理解要点**：路径变量让URL更灵活，可以在一个方法中处理多个不同ID的请求。

### 3.3 通配符路径映射


**? 通配符** - 匹配单个字符：
```java
@GetMapping("/user/test?")
// 能匹配：/user/test1, /user/testA
// 不匹配：/user/test, /user/test12
```

**\* 通配符** - 匹配任意字符（同一层级）：
```java
@GetMapping("/user/*")
// 能匹配：/user/list, /user/add, /user/anything
// 不匹配：/user/order/1（跨层级了）
```

**\*\* 通配符** - 匹配任意层级路径：
```java
@GetMapping("/user/**")
// 能匹配：/user/list
//        /user/order/1
//        /user/order/detail/999
```

**实际应用场景**：
```java
@Controller
public class FileController {
    
    // 下载任意层级的文件
    @GetMapping("/download/**")
    public void download(HttpServletRequest request) {
        String path = request.getRequestURI();
        // 处理文件下载
    }
}
```

### 3.4 多路径映射


**说明**：一个方法可以处理多个不同的URL路径。

```java
@Controller
public class HomeController {
    
    // 这三个路径都会调用同一个方法
    @RequestMapping({"/", "/index", "/home"})
    public String home() {
        return "home";
    }
}
```

**实际应用**：
```java
@GetMapping({"/user/list", "/user/all", "/users"})
public String listUsers() {
    // 访问 /user/list 或 /user/all 或 /users
    // 都会执行这个方法
    return "userList";
}
```

---

## 4. 🔒 请求限定条件


### 4.1 请求方法限定


**说明**：限制只能用特定的HTTP方法访问。

```java
@Controller
@RequestMapping("/user")
public class UserController {
    
    // 只接受GET请求
    @RequestMapping(value = "/list", method = RequestMethod.GET)
    public String list() {
        return "userList";
    }
    
    // 可以接受GET和POST两种请求
    @RequestMapping(value = "/search", 
                   method = {RequestMethod.GET, RequestMethod.POST})
    public String search() {
        return "searchResult";
    }
}
```

**简化写法**（推荐）：
```java
@GetMapping("/list")     // 只接受GET
@PostMapping("/add")     // 只接受POST
@PutMapping("/update")   // 只接受PUT
@DeleteMapping("/delete") // 只接受DELETE
```

### 4.2 请求参数限定


**通俗理解**：要求URL中必须带有某些参数，或者参数必须是某个值。

**必须包含参数**：
```java
@GetMapping(value = "/search", params = "keyword")
public String search(@RequestParam String keyword) {
    // 必须访问 /search?keyword=xxx
    // 访问 /search 会报错（缺少参数）
    return "searchResult";
}
```

**参数必须等于某个值**：
```java
@GetMapping(value = "/user", params = "type=vip")
public String vipUsers() {
    // 必须访问 /user?type=vip
    // 访问 /user?type=normal 不会匹配
    return "vipUserList";
}
```

**多个参数条件**：
```java
@GetMapping(value = "/user", params = {"type=vip", "status=active"})
public String activeVipUsers() {
    // 必须同时满足：type=vip 且 status=active
    // /user?type=vip&status=active ✅
    return "activeVipList";
}
```

**参数不等于某个值**：
```java
@GetMapping(value = "/user", params = "type!=guest")
public String nonGuestUsers() {
    // type参数不能是guest
    // /user?type=vip ✅
    // /user?type=guest ❌
    return "userList";
}
```

### 4.3 请求头限定


**通俗理解**：根据浏览器发送的请求头信息来决定是否处理请求。

**限定Content-Type**：
```java
@PostMapping(value = "/user", 
            headers = "Content-Type=application/json")
public String addUserJson(@RequestBody User user) {
    // 只处理JSON格式的请求
    return "success";
}
```

**限定Accept**：
```java
@GetMapping(value = "/user", 
           headers = "Accept=application/json")
@ResponseBody
public List<User> getUsersJson() {
    // 只返回JSON格式给要求JSON的客户端
    return userList;
}
```

**实际应用场景**：
```java
@RestController
@RequestMapping("/api/user")
public class UserApiController {
    
    // 移动端API（通过请求头区分）
    @GetMapping(value = "/list", headers = "X-Client-Type=mobile")
    public List<User> mobileUserList() {
        // 返回适合移动端的数据
        return mobileUserList;
    }
    
    // Web端API
    @GetMapping(value = "/list", headers = "X-Client-Type=web")
    public List<User> webUserList() {
        // 返回适合Web端的数据
        return webUserList;
    }
}
```

---

## 5. 🌐 RESTful风格设计


### 5.1 什么是RESTful


**通俗解释**：
- **传统方式**：用不同的URL表示不同的操作
  ```
  查询用户：/getUserById?id=1
  添加用户：/addUser
  修改用户：/updateUser
  删除用户：/deleteUser?id=1
  ```

- **RESTful方式**：用相同的URL + 不同的HTTP方法表示不同操作
  ```
  查询用户：GET    /user/1
  添加用户：POST   /user
  修改用户：PUT    /user/1
  删除用户：DELETE /user/1
  ```

**核心思想**：
- 🔹 URL表示资源（名词）
- 🔹 HTTP方法表示操作（动词）
- 🔹 统一、规范、语义化

### 5.2 RESTful标准实现


**完整示例**：
```java
@RestController
@RequestMapping("/user")
public class UserRestController {
    
    // 查询所有用户 - GET /user
    @GetMapping
    public List<User> list() {
        return userService.findAll();
    }
    
    // 查询单个用户 - GET /user/{id}
    @GetMapping("/{id}")
    public User getById(@PathVariable Long id) {
        return userService.findById(id);
    }
    
    // 添加用户 - POST /user
    @PostMapping
    public User add(@RequestBody User user) {
        return userService.save(user);
    }
    
    // 修改用户 - PUT /user/{id}
    @PutMapping("/{id}")
    public User update(@PathVariable Long id, @RequestBody User user) {
        user.setId(id);
        return userService.update(user);
    }
    
    // 删除用户 - DELETE /user/{id}
    @DeleteMapping("/{id}")
    public String delete(@PathVariable Long id) {
        userService.deleteById(id);
        return "删除成功";
    }
}
```

### 5.3 HTTP方法语义对照


| HTTP方法 | 操作含义 | 是否幂等 | 典型场景 |
|----------|---------|---------|---------|
| **GET** | 查询资源 | ✅ 是 | 查看用户列表、查看用户详情 |
| **POST** | 创建资源 | ❌ 否 | 添加新用户、提交订单 |
| **PUT** | 完整更新资源 | ✅ 是 | 修改用户全部信息 |
| **PATCH** | 部分更新资源 | ✅ 是 | 只修改用户手机号 |
| **DELETE** | 删除资源 | ✅ 是 | 删除用户、取消订单 |

> 💡 **幂等性理解**：多次执行同一操作，结果是一样的。比如删除用户ID=1，执行一次删除和执行十次删除，结果都是用户1被删除了。

### 5.4 RESTful最佳实践


**✅ 推荐做法**：
```java
// 1. 使用名词复数形式
@RequestMapping("/users")    // ✅ 推荐
@RequestMapping("/user")     // 可接受

// 2. 资源嵌套表示关系
@GetMapping("/users/{userId}/orders")  // 查询某用户的订单
@GetMapping("/users/{userId}/orders/{orderId}")  // 查询某用户的某个订单

// 3. 使用查询参数过滤
@GetMapping("/users?status=active")  // 查询活跃用户
@GetMapping("/users?age=18&city=北京")  // 组合条件查询

// 4. 返回合适的HTTP状态码
@PostMapping
@ResponseStatus(HttpStatus.CREATED)  // 201 创建成功
public User create(@RequestBody User user) {
    return userService.save(user);
}
```

**❌ 不推荐做法**：
```java
// ❌ URL中包含动词
@GetMapping("/getUser")
@PostMapping("/createUser")

// ❌ 用GET方法修改数据
@GetMapping("/user/delete")

// ❌ 资源嵌套过深
@GetMapping("/company/{id}/dept/{did}/user/{uid}/order/{oid}")
```

### 5.5 RESTful vs 传统对比


```
需求：用户管理的增删改查功能

【传统风格】
查询列表：GET  /user/list
查询详情：GET  /user/detail?id=1
添加用户：POST /user/add
修改用户：POST /user/update
删除用户：GET  /user/delete?id=1

【RESTful风格】
查询列表：GET    /user
查询详情：GET    /user/1
添加用户：POST   /user
修改用户：PUT    /user/1
删除用户：DELETE /user/1
```

**RESTful优势**：
- 🎯 **统一规范**：所有资源操作遵循统一模式
- 📱 **前后端分离**：适合移动端、Web端等多端访问
- 🔍 **语义清晰**：看URL和方法就知道要做什么
- 🌐 **国际通用**：业界标准，易于理解和维护

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 请求映射：URL路径与Controller方法的对应关系
🔸 映射注解：@RequestMapping、@GetMapping、@PostMapping等
🔸 路径变量：用{}捕获URL中的动态部分
🔸 请求限定：通过方法、参数、请求头限定请求
🔸 RESTful：用HTTP方法表示操作，URL表示资源
```

### 6.2 映射注解速记表


| 注解 | HTTP方法 | 典型用途 | 记忆口诀 |
|------|---------|---------|---------|
| `@GetMapping` | GET | 查询数据 | **Get = 获取** |
| `@PostMapping` | POST | 提交数据 | **Post = 投递** |
| `@PutMapping` | PUT | 完整更新 | **Put = 放置（整个替换）** |
| `@PatchMapping` | PATCH | 部分更新 | **Patch = 补丁（部分修复）** |
| `@DeleteMapping` | DELETE | 删除数据 | **Delete = 删除** |

### 6.3 路径映射规则总结


```
精确匹配：
/user/list  → 完全匹配路径

路径变量：
/user/{id}  → {id}可以是任意值

通配符：
/user/test?   → ?匹配单个字符
/user/*       → *匹配同一层级
/user/**      → **匹配任意层级

多路径：
{"/", "/home"}  → 多个路径对应一个方法
```

### 6.4 实际开发建议


**✅ 推荐实践**：
- 优先使用具体的映射注解（@GetMapping等）而不是@RequestMapping
- RESTful风格适合前后端分离项目
- 路径变量命名要有意义（userId而不是id）
- 合理使用类级别@RequestMapping统一管理路径前缀

**⚠️ 注意事项**：
- 避免路径冲突（两个方法映射到同一路径）
- GET请求不要修改数据，只用于查询
- 路径变量要做参数校验，防止非法输入
- 注意HTTP方法的语义，不要混用

**核心记忆**：
```
映射就是路由规则，告诉Spring哪个请求找哪个方法
注解选择看操作：查询GET，提交POST，更新PUT，删除DELETE
路径设计要规范：名词表示资源，动词用HTTP方法
RESTful是趋势：统一、清晰、易维护
```

### 6.5 学习检验清单


✅ 我能说出@GetMapping和@PostMapping的区别
✅ 我能写出路径变量的映射方法
✅ 我理解RESTful风格的核心思想
✅ 我知道如何限定请求方法和参数
✅ 我能设计出规范的URL路径

**下一步学习**：掌握了请求映射后，可以继续学习参数接收、数据绑定等知识，进一步完善Controller的功能！