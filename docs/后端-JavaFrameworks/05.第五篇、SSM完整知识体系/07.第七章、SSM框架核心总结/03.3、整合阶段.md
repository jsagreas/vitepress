---
title: 3、整合阶段
---
## 📚 目录


1. [SSM框架整合概述](#1-SSM框架整合概述)
2. [整合配置详解](#2-整合配置详解)
3. [分层架构设计](#3-分层架构设计)
4. [事务统一管理](#4-事务统一管理)
5. [性能优化策略](#5-性能优化策略)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 SSM框架整合概述



### 1.1 什么是SSM框架整合



**通俗理解**：想象你在组装一台电脑

```
CPU (Spring)      → 整个系统的大脑，负责调度管理
显卡 (SpringMVC)  → 负责图像显示，就像Web层展示页面
硬盘 (MyBatis)    → 负责数据存储，就像数据库操作

SSM整合 = 让这三个部件协同工作，各司其职又互相配合
```

**专业定义**：
- **Spring** - 核心容器，负责对象管理和依赖注入
- **SpringMVC** - Web层框架，处理HTTP请求响应
- **MyBatis** - 持久层框架，负责数据库操作

### 1.2 为什么需要整合



**实际问题场景**：

```
场景1: 用户注册功能
┌─────────┐      ┌─────────┐      ┌─────────┐
│ 前端页面 │ -->  │ 业务逻辑 │ -->  │ 数据库   │
│(SpringMVC)│     │(Spring) │      │(MyBatis)│
└─────────┘      └─────────┘      └─────────┘

问题：这三层怎么协同工作？
答案：通过SSM整合！
```

**整合的核心价值**：

> 💡 **形象比喻**
> 
> 如果把开发比作盖房子：
> - **Spring** 是地基和框架结构
> - **SpringMVC** 是门窗和外观装修
> - **MyBatis** 是地下室和仓储系统
> 
> 整合就是让这三部分完美配合，建成一座实用又美观的大楼

### 1.3 整合后的项目结构



```
my-project/
├── 📁 src/main/java/
│   ├── 📁 com.example.controller/    ← SpringMVC层
│   │   └── 📄 UserController.java
│   ├── 📁 com.example.service/       ← Spring业务层
│   │   ├── 📄 UserService.java
│   │   └── 📄 UserServiceImpl.java
│   ├── 📁 com.example.mapper/        ← MyBatis数据层
│   │   └── 📄 UserMapper.java
│   └── 📁 com.example.entity/        ← 实体类
│       └── 📄 User.java
├── 📁 src/main/resources/
│   ├── 📄 spring.xml                 ← Spring配置
│   ├── 📄 spring-mvc.xml             ← SpringMVC配置
│   ├── 📄 mybatis-config.xml         ← MyBatis配置
│   └── 📁 mapper/                    ← SQL映射文件
│       └── 📄 UserMapper.xml
└── 📁 src/main/webapp/
    ├── 📁 WEB-INF/
    │   └── 📄 web.xml                ← Web应用配置
    └── 📁 pages/                     ← 页面文件
```

---

## 2. ⚙️ 整合配置详解



### 2.1 配置文件的作用对比



| 配置文件 | 🎯 核心作用 | 📝 通俗理解 | ⚡ 关键配置项 |
|---------|-----------|------------|-------------|
| **web.xml** | `Web应用入口` | 像房子的大门，决定谁能进来 | DispatcherServlet、编码过滤器 |
| **spring.xml** | `Spring容器配置` | 像管家，管理所有对象 | 组件扫描、数据源、事务 |
| **spring-mvc.xml** | `MVC层配置` | 像前台接待，处理访客请求 | 处理器映射、视图解析器 |
| **mybatis-config.xml** | `MyBatis配置` | 像仓库管理员，管理数据存取 | 类型别名、插件、设置 |

### 2.2 web.xml配置 - Web应用的大门



**🔸 核心配置思路**：

```
启动流程:
浏览器请求 → Tomcat启动 → 读取web.xml → 
加载Spring容器 → 初始化SpringMVC → 准备就绪
```

**关键配置示例**：

```xml
<!-- 第一步：配置编码过滤器 - 防止中文乱码 -->
<filter>
    <filter-name>encodingFilter</filter-name>
    <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
    <init-param>
        <param-name>encoding</param-name>
        <param-value>UTF-8</param-value>
    </init-param>
</filter>
<filter-mapping>
    <filter-name>encodingFilter</filter-name>
    <url-pattern>/*</url-pattern>
</filter-mapping>

<!-- 第二步：配置Spring监听器 - 启动Spring容器 -->
<listener>
    <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
</listener>
<context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>classpath:spring.xml</param-value>
</context-param>

<!-- 第三步：配置SpringMVC前端控制器 - 拦截所有请求 -->
<servlet>
    <servlet-name>dispatcherServlet</servlet-name>
    <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
    <init-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>classpath:spring-mvc.xml</param-value>
    </init-param>
    <load-on-startup>1</load-on-startup>
</servlet>
<servlet-mapping>
    <servlet-name>dispatcherServlet</servlet-name>
    <url-pattern>/</url-pattern>
</servlet-mapping>
```

> ⚠️ **新手易错点**
> 
> - `url-pattern`配置为`/`而不是`/*`，否则会拦截JSP页面
> - `load-on-startup`必须配置，让DispatcherServlet随应用启动

### 2.3 spring.xml配置 - Spring容器的管家



**🔸 配置分区规划**：

```
spring.xml配置结构:
├── 📦 组件扫描 (告诉Spring去哪找Bean)
├── 🗄️ 数据源配置 (连接数据库)
├── 🔧 SqlSessionFactory (MyBatis核心工厂)
├── 📝 Mapper扫描 (批量注册Mapper接口)
└── 💰 事务管理器 (管理数据库事务)
```

**配置示例与说明**：

```xml
<!-- 1. 加载properties文件 - 把数据库配置写在外面，方便修改 -->
<context:property-placeholder location="classpath:db.properties"/>

<!-- 2. 组件扫描 - Spring去com.example.service包找所有类并管理 -->
<context:component-scan base-package="com.example.service"/>

<!-- 3. 数据源配置 - 建立数据库连接池 -->
<bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
    <property name="driverClassName" value="${jdbc.driver}"/>
    <property name="url" value="${jdbc.url}"/>
    <property name="username" value="${jdbc.username}"/>
    <property name="password" value="${jdbc.password}"/>
    <!-- 连接池配置 -->
    <property name="initialSize" value="5"/>
    <property name="maxActive" value="20"/>
</bean>

<!-- 4. SqlSessionFactory - MyBatis的核心工厂 -->
<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
    <property name="dataSource" ref="dataSource"/>
    <property name="configLocation" value="classpath:mybatis-config.xml"/>
    <property name="mapperLocations" value="classpath:mapper/*.xml"/>
</bean>

<!-- 5. Mapper接口扫描 - 自动生成Mapper实现类 -->
<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
    <property name="basePackage" value="com.example.mapper"/>
</bean>

<!-- 6. 事务管理器 - 统一管理事务 -->
<bean id="transactionManager" 
      class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
    <property name="dataSource" ref="dataSource"/>
</bean>

<!-- 7. 开启注解事务 - 可以用@Transactional注解 -->
<tx:annotation-driven transaction-manager="transactionManager"/>
```

**📊 各组件关系图**：

```
                 Spring容器
                     │
        ┌────────────┼────────────┐
        ↓            ↓            ↓
   Service层    DataSource   TransactionManager
        │            │            │
        └─────→ SqlSessionFactory ←┘
                     │
                     ↓
              Mapper接口实现
                     │
                     ↓
                  数据库
```

### 2.4 spring-mvc.xml配置 - MVC层的指挥官



**配置要点**：

```xml
<!-- 1. 组件扫描 - 只扫描Controller层 -->
<context:component-scan base-package="com.example.controller"/>

<!-- 2. 注解驱动 - 开启SpringMVC注解功能 -->
<mvc:annotation-driven/>

<!-- 3. 视图解析器 - 把逻辑视图名转换为真实页面路径 -->
<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
    <property name="prefix" value="/WEB-INF/pages/"/>
    <property name="suffix" value=".jsp"/>
</bean>

<!-- 4. 静态资源放行 - 让CSS、JS、图片能正常访问 -->
<mvc:resources mapping="/static/**" location="/static/"/>
<mvc:default-servlet-handler/>
```

> 💡 **视图解析器原理**
> 
> 当你在Controller返回`"user/list"`时：
> - 前缀：`/WEB-INF/pages/`
> - 逻辑名：`user/list`
> - 后缀：`.jsp`
> - 最终路径：`/WEB-INF/pages/user/list.jsp`

---

## 3. 🏗️ 分层架构设计



### 3.1 三层架构思想



**🔸 生活化理解**：

```
餐厅运作流程                    三层架构对应
┌─────────────┐               ┌──────────────┐
│  顾客点餐    │  ---------->  │ Controller层  │ (接收请求)
└─────────────┘               └──────────────┘
       ↓                              ↓
┌─────────────┐               ┌──────────────┐
│  厨师做菜    │  ---------->  │  Service层   │ (处理业务)
└─────────────┘               └──────────────┘
       ↓                              ↓
┌─────────────┐               ┌──────────────┐
│  仓库取料    │  ---------->  │  Mapper层    │ (数据访问)
└─────────────┘               └──────────────┘
```

### 3.2 Controller层设计



**职责定位**：
- ✅ 接收前端请求参数
- ✅ 调用Service处理业务
- ✅ 返回响应结果
- ❌ 不写业务逻辑
- ❌ 不直接操作数据库

**标准写法示例**：

```java
@RestController
@RequestMapping("/user")
public class UserController {
    
    @Autowired
    private UserService userService;
    
    // 查询用户列表
    @GetMapping("/list")
    public Result<List<User>> getUserList() {
        List<User> users = userService.findAll();
        return Result.success(users);
    }
    
    // 添加用户
    @PostMapping("/add")
    public Result<Void> addUser(@RequestBody User user) {
        userService.save(user);
        return Result.success();
    }
    
    // 根据ID查询
    @GetMapping("/{id}")
    public Result<User> getUserById(@PathVariable Long id) {
        User user = userService.findById(id);
        return Result.success(user);
    }
}
```

**🎯 Controller层最佳实践**：

| ✅ 推荐做法 | ❌ 不推荐做法 |
|-----------|------------|
| `统一返回结果封装` | 每个方法返回不同格式 |
| `参数校验使用@Validated` | 在方法内手动校验 |
| `异常统一处理` | 每个方法写try-catch |
| `RESTful风格URL` | 参数全部放在URL里 |

### 3.3 Service层设计



**职责定位**：
- ✅ 编写业务逻辑
- ✅ 调用多个Mapper完成复杂操作
- ✅ 处理事务
- ❌ 不处理HTTP请求
- ❌ 不返回特定视图

**接口与实现分离**：

```java
// 1. 定义接口
public interface UserService {
    List<User> findAll();
    User findById(Long id);
    void save(User user);
    void update(User user);
    void delete(Long id);
}

// 2. 实现类
@Service
@Transactional  // 类级别事务
public class UserServiceImpl implements UserService {
    
    @Autowired
    private UserMapper userMapper;
    
    @Autowired
    private RoleMapper roleMapper;
    
    @Override
    public List<User> findAll() {
        return userMapper.selectAll();
    }
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void save(User user) {
        // 业务逻辑：保存用户并分配默认角色
        userMapper.insert(user);
        roleMapper.insertUserRole(user.getId(), 1L); // 默认角色
    }
}
```

**💡 Service层设计原则**：

> **单一职责原则**
> 一个Service只负责一个业务领域，比如UserService只管用户相关

> **依赖抽象原则**  
> Controller依赖Service接口，而不是具体实现类

> **事务一致性**
> 一个业务方法要么全成功，要么全失败

### 3.4 Mapper层设计



**职责定位**：
- ✅ 定义数据访问接口
- ✅ 编写SQL映射
- ✅ 完成数据库CRUD
- ❌ 不包含业务逻辑

**接口定义**：

```java
@Repository
public interface UserMapper {
    // 查询所有
    List<User> selectAll();
    
    // 根据ID查询
    User selectById(Long id);
    
    // 插入
    void insert(User user);
    
    // 更新
    void update(User user);
    
    // 删除
    void deleteById(Long id);
    
    // 复杂查询：根据条件分页查询
    List<User> selectByCondition(@Param("name") String name, 
                                  @Param("status") Integer status);
}
```

**XML映射文件**：

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    
    <!-- 结果映射 -->
    <resultMap id="UserMap" type="User">
        <id column="user_id" property="id"/>
        <result column="user_name" property="name"/>
        <result column="user_age" property="age"/>
    </resultMap>
    
    <!-- 查询所有 -->
    <select id="selectAll" resultMap="UserMap">
        SELECT user_id, user_name, user_age 
        FROM t_user
    </select>
    
    <!-- 插入 -->
    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (user_name, user_age)
        VALUES (#{name}, #{age})
    </insert>
    
    <!-- 动态SQL查询 -->
    <select id="selectByCondition" resultMap="UserMap">
        SELECT * FROM t_user
        <where>
            <if test="name != null and name != ''">
                AND user_name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>
</mapper>
```

---

## 4. 💰 事务统一管理



### 4.1 为什么需要事务



**实际场景问题**：

```
转账业务：A给B转账100元

步骤1: A账户扣款100  ✅
步骤2: 系统崩溃了！   💥
步骤3: B账户加款100  ❌ (没执行)

结果：A的钱没了，B也没收到 → 数据不一致！
```

**事务的作用**：

> 🛡️ **事务四大特性 (ACID)**
> 
> - **A**tomicity (原子性): 要么全做，要么全不做
> - **C**onsistency (一致性): 数据从一个正确状态到另一个正确状态  
> - **I**solation (隔离性): 多个事务互不干扰
> - **D**urability (持久性): 提交后永久保存

### 4.2 声明式事务配置



**🔸 注解方式（推荐）**：

```java
@Service
public class AccountServiceImpl implements AccountService {
    
    @Autowired
    private AccountMapper accountMapper;
    
    // 方法级事务
    @Transactional(
        propagation = Propagation.REQUIRED,      // 传播行为
        isolation = Isolation.DEFAULT,           // 隔离级别  
        rollbackFor = Exception.class,           // 遇到什么异常回滚
        timeout = 30                             // 超时时间(秒)
    )
    public void transfer(Long fromId, Long toId, BigDecimal amount) {
        // 1. 扣款
        accountMapper.deduct(fromId, amount);
        
        // 2. 模拟异常
        // int i = 1 / 0;  // 如果这里异常，上面的扣款会回滚
        
        // 3. 加款
        accountMapper.add(toId, amount);
    }
}
```

**🔸 事务传播行为详解**：

| 传播行为 | 说明 | 使用场景 |
|---------|------|---------|
| **REQUIRED** (默认) | `有事务就加入，没有就新建` | 最常用，适合大部分业务 |
| **REQUIRES_NEW** | `总是开启新事务，挂起当前事务` | 日志记录，独立提交 |
| **SUPPORTS** | `有事务就加入，没有就不用` | 查询操作 |
| **NOT_SUPPORTED** | `以非事务方式运行，挂起当前事务` | 耗时操作，不需要事务 |
| **NEVER** | `不能在事务中运行，否则抛异常` | 严格禁止事务的场景 |

**传播行为示例**：

```java
@Service
public class OrderServiceImpl {
    
    @Autowired
    private LogService logService;
    
    // 外层事务
    @Transactional
    public void createOrder(Order order) {
        // 1. 保存订单
        orderMapper.insert(order);
        
        // 2. 记录日志（使用新事务，即使订单失败日志也要保存）
        logService.saveLog("创建订单:" + order.getId());
        
        // 3. 如果这里异常，订单回滚，但日志已提交
    }
}

@Service
public class LogServiceImpl {
    
    // 新事务，独立提交
    @Transactional(propagation = Propagation.REQUIRES_NEW)
    public void saveLog(String message) {
        logMapper.insert(new Log(message));
    }
}
```

### 4.3 事务失效场景



**⚠️ 常见失效原因**：

```
失效场景1: 方法不是public
❌ @Transactional
   private void save() { }  // 事务不生效

✅ @Transactional  
   public void save() { }   // 正确

失效场景2: 同类方法调用
❌ public void methodA() {
       methodB();  // 内部调用，事务失效
   }
   @Transactional
   public void methodB() { }

✅ 通过Spring容器获取代理对象调用

失效场景3: 异常被捕获
❌ @Transactional
   public void save() {
       try {
           // 操作
       } catch(Exception e) {
           // 吞掉异常，事务不回滚
       }
   }

✅ 捕获后重新抛出或手动回滚
```

---

## 5. ⚡ 性能优化策略



### 5.1 数据库层面优化



**🔸 连接池优化配置**：

```xml
<bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
    <!-- 初始化连接数 -->
    <property name="initialSize" value="5"/>
    <!-- 最大活跃连接数 -->
    <property name="maxActive" value="20"/>
    <!-- 最小空闲连接数 -->
    <property name="minIdle" value="5"/>
    <!-- 最大等待时间(毫秒) -->
    <property name="maxWait" value="60000"/>
    <!-- 连接有效性检测 -->
    <property name="validationQuery" value="SELECT 1"/>
    <property name="testWhileIdle" value="true"/>
</bean>
```

**连接池工作原理**：

```
请求处理流程:
┌──────┐    借用连接    ┌──────────┐
│ 应用 │ ────────────> │ 连接池    │
│ 线程 │               │ [5-20个] │
└──────┘ <──────────── └──────────┘
          归还连接             ↓
                          ┌──────┐
                          │数据库│
                          └──────┘
好处：复用连接，避免频繁创建销毁
```

**🔸 SQL优化策略**：

| 优化手段 | ❌ 慢查询写法 | ✅ 优化后写法 |
|---------|-------------|-------------|
| **索引使用** | `WHERE name LIKE '%张%'` | `WHERE name LIKE '张%'` |
| **避免全表扫描** | `SELECT *` | `SELECT id, name, age` |
| **批量操作** | 循环单条insert | `<foreach>`批量insert |
| **分页查询** | 查全部再截取 | `LIMIT #{offset}, #{size}` |

**批量插入示例**：

```xml
<!-- 批量插入 - 一次性插入多条 -->
<insert id="batchInsert" parameterType="list">
    INSERT INTO t_user (name, age) VALUES
    <foreach collection="list" item="user" separator=",">
        (#{user.name}, #{user.age})
    </foreach>
</insert>
```

### 5.2 缓存优化



**🔸 一级缓存与二级缓存**：

```
MyBatis缓存机制:

一级缓存 (SqlSession级别)
┌─────────────────┐
│  SqlSession 1   │ → 同一个SqlSession内，相同查询走缓存
│  [缓存: user_1] │
└─────────────────┘

二级缓存 (Mapper级别)  
┌─────────────────────────┐
│    UserMapper缓存区      │ → 不同SqlSession，共享缓存
│  [user_1, user_2, ...]  │
└─────────────────────────┘
```

**二级缓存配置**：

```xml
<!-- mybatis-config.xml -->
<settings>
    <setting name="cacheEnabled" value="true"/>
</settings>

<!-- UserMapper.xml -->
<cache eviction="LRU" flushInterval="60000" size="512" readOnly="true"/>
```

**🔸 Spring缓存注解**：

```java
@Service
public class UserServiceImpl {
    
    // 查询结果缓存
    @Cacheable(value = "userCache", key = "#id")
    public User findById(Long id) {
        return userMapper.selectById(id);
    }
    
    // 更新时清除缓存
    @CacheEvict(value = "userCache", key = "#user.id")
    public void update(User user) {
        userMapper.update(user);
    }
    
    // 更新缓存
    @CachePut(value = "userCache", key = "#user.id")
    public User save(User user) {
        userMapper.insert(user);
        return user;
    }
}
```

### 5.3 异步处理优化



**🔸 @Async异步方法**：

```java
@Service
public class NotificationService {
    
    // 异步发送邮件
    @Async
    public void sendEmail(String to, String content) {
        // 发送邮件逻辑（耗时操作）
        emailClient.send(to, content);
    }
}

// 主业务方法
@Service
public class OrderServiceImpl {
    
    @Autowired
    private NotificationService notificationService;
    
    public void createOrder(Order order) {
        // 1. 保存订单（同步）
        orderMapper.insert(order);
        
        // 2. 发送通知（异步，不阻塞主流程）
        notificationService.sendEmail(order.getUserEmail(), "订单创建成功");
        
        // 3. 立即返回，不等邮件发送完成
    }
}
```

**配置线程池**：

```xml
<!-- 异步任务线程池 -->
<task:executor id="asyncExecutor" 
               pool-size="5-10" 
               queue-capacity="100"/>
<task:annotation-driven executor="asyncExecutor"/>
```

### 5.4 性能监控



**🔸 Druid监控配置**：

```xml
<bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
    <!-- ... 其他配置 ... -->
    
    <!-- 开启监控统计 -->
    <property name="filters" value="stat,wall"/>
</bean>

<!-- 配置Druid监控页面 -->
<servlet>
    <servlet-name>DruidStatView</servlet-name>
    <servlet-class>com.alibaba.druid.support.http.StatViewServlet</servlet-class>
</servlet>
<servlet-mapping>
    <servlet-name>DruidStatView</servlet-name>
    <url-pattern>/druid/*</url-pattern>
</servlet-mapping>
```

访问：`http://localhost:8080/druid/` 查看监控面板

**监控指标**：
- ✅ SQL执行耗时统计
- ✅ 慢SQL记录
- ✅ 连接池使用情况
- ✅ Web应用访问统计

---

## 6. 📋 核心要点总结



### 6.1 整合配置关键点



```
🎯 配置文件记忆口诀:

web.xml    → 大门守卫，配置过滤器和Servlet
spring.xml → 管家总管，扫描Bean、配数据源、管事务
spring-mvc.xml → 前台接待，处理请求、解析视图
mybatis-config.xml → 仓库管理，别名设置、插件配置
```

**配置顺序**：
1️⃣ web.xml（入口） 
2️⃣ spring.xml（核心容器）
3️⃣ spring-mvc.xml（Web层）
4️⃣ mybatis-config.xml（持久层）

### 6.2 分层架构核心原则



| 层次 | 核心职责 | 不该做的事 |
|------|---------|-----------|
| **Controller** | `接收请求，调用Service，返回结果` | ❌ 写业务逻辑、直接访问数据库 |
| **Service** | `编写业务逻辑，调用Mapper，处理事务` | ❌ 处理HTTP、返回特定视图 |
| **Mapper** | `定义SQL操作，完成数据访问` | ❌ 包含业务逻辑 |

**依赖关系**：
```
Controller → Service接口 → ServiceImpl → Mapper接口
   (调用)      (依赖)       (实现)        (调用)
```

### 6.3 事务管理要点



> ⚡ **事务最佳实践**
> 
> ✅ 方法必须是`public`  
> ✅ 通过Spring容器调用才有效
> ✅ 异常要抛出，不要吞掉  
> ✅ 指定`rollbackFor = Exception.class`
> ✅ 耗时操作考虑异步或新事务

**传播行为选择**：
- 普通业务 → `REQUIRED`
- 日志记录 → `REQUIRES_NEW`  
- 查询操作 → `SUPPORTS`

### 6.4 性能优化检查清单



**数据库优化** ☑️
- [ ] 连接池参数合理配置
- [ ] SQL添加必要索引
- [ ] 避免SELECT *
- [ ] 使用批量操作

**缓存优化** ☑️  
- [ ] 开启MyBatis二级缓存
- [ ] 使用@Cacheable注解
- [ ] 热点数据Redis缓存

**异步优化** ☑️
- [ ] 邮件/短信发送异步化
- [ ] 配置合理的线程池
- [ ] 非关键流程异步处理

**监控优化** ☑️
- [ ] Druid监控开启
- [ ] 慢SQL日志记录  
- [ ] 定期性能分析

---

# 🎓 学习路线建议



```
第1周: 理解整合原理
      └─ 搭建SSM基础项目
      
第2周: 实践三层架构  
      └─ 开发用户CRUD功能
      
第3周: 掌握事务管理
      └─ 实现转账等复杂业务
      
第4周: 性能优化实战
      └─ 缓存、异步、监控
      
第5-6周: 综合项目实战
        └─ 电商/博客系统开发
```

> 💡 **新手建议**
> 
> 不要一开始就追求完美架构，先把功能实现，再逐步优化。
> 多写代码，多踩坑，多总结，SSM整合熟练需要实践积累！