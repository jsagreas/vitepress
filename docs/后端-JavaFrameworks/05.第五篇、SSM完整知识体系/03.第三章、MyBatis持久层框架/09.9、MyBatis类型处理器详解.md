---
title: 9、MyBatis类型处理器详解
---
## 📚 目录

1. [类型处理器概述](#1-类型处理器概述)
2. [TypeHandler接口详解](#2-TypeHandler接口详解)
3. [BaseTypeHandler基础处理器](#3-BaseTypeHandler基础处理器)
4. [自定义类型处理器开发](#4-自定义类型处理器开发)
5. [常用类型处理器详解](#5-常用类型处理器详解)
6. [高级类型处理器开发](#6-高级类型处理器开发)
7. [类型处理器注册与配置](#7-类型处理器注册与配置)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 类型处理器概述


### 1.1 什么是类型处理器


> 💡 **通俗理解**
> 
> 类型处理器就像是"翻译官"，负责在Java对象和数据库之间做数据类型转换：
> - **存数据时**：把Java类型翻译成数据库能理解的类型
> - **取数据时**：把数据库类型翻译成Java能理解的类型

**🔸 现实场景类比**

```
就像出国旅游需要货币兑换：

Java世界（人民币）    ←→  翻译官  ←→    数据库世界（美元）
  String "男"         ←→  翻译官  ←→    TINYINT 1
  LocalDateTime       ←→  翻译官  ←→    TIMESTAMP
  List<String>        ←→  翻译官  ←→    VARCHAR "a,b,c"
```

### 1.2 为什么需要类型处理器


**核心问题**：Java类型 ≠ 数据库类型

| 😕 常见问题场景 | Java类型 | 数据库类型 | 😤 困难 |
|----------------|----------|-----------|--------|
| 性别字段 | `String "男/女"` | `TINYINT 1/0` | ❌ 类型不匹配 |
| 时间处理 | `LocalDateTime` | `TIMESTAMP` | ❌ 格式转换复杂 |
| JSON数据 | `List<String>` | `VARCHAR` | ❌ 需要序列化 |
| 地理位置 | `Point对象` | `VARCHAR` | ❌ 对象转字符串 |

**✅ 类型处理器的作用**

```
没有类型处理器：
你需要手动在每个Mapper中写转换逻辑 😓

有了类型处理器：
MyBatis自动帮你转换，写一次到处用 😊
```

### 1.3 类型处理器的工作流程


```
【保存数据流程】
Java对象 → Mapper → TypeHandler → PreparedStatement → 数据库
   ↓                      ↓                ↓
 User对象            setParameter      转换成SQL参数
 gender="男"           调用             设置值为 1

【查询数据流程】  
数据库 → ResultSet → TypeHandler → Mapper → Java对象
   ↓          ↓            ↓              ↓
TINYINT 1   获取值      getResult      gender="男"
                        转换回来
```

---

## 2. 📋 TypeHandler接口详解


### 2.1 TypeHandler核心接口


**接口结构图**

```
                TypeHandler<T>
                      |
        +-------------+-------------+
        |                           |
   setParameter()              getResult()
    设置参数到数据库            从结果集获取值
```

**🔸 四个核心方法**

```java
public interface TypeHandler<T> {
  
  // 方法①：设置参数到PreparedStatement
  void setParameter(PreparedStatement ps, int i, T parameter, JdbcType jdbcType);
  
  // 方法②：从ResultSet根据列名获取结果
  T getResult(ResultSet rs, String columnName);
  
  // 方法③：从ResultSet根据列索引获取结果
  T getResult(ResultSet rs, int columnIndex);
  
  // 方法④：从CallableStatement获取结果（存储过程用）
  T getResult(CallableStatement cs, int columnIndex);
}
```

**🎯 方法职责说明**

| 方法 | 🔧 作用场景 | 📝 说明 |
|------|-----------|--------|
| `setParameter()` | **插入/更新时** | 把Java对象转成数据库值 |
| `getResult(列名)` | **查询时（常用）** | 根据列名从结果集取值 |
| `getResult(索引)` | **查询时** | 根据索引从结果集取值 |
| `getResult(存储过程)` | **调用存储过程时** | 从存储过程获取输出值 |

### 2.2 方法参数详解


**setParameter 参数说明**

```java
void setParameter(PreparedStatement ps, int i, T parameter, JdbcType jdbcType)
                    ↓                  ↓      ↓            ↓
                  预处理语句          位置   Java值      数据库类型
```

**参数解释**：
- `PreparedStatement ps` → SQL预处理对象，用来设置参数
- `int i` → 参数在SQL中的位置（第几个`?`）
- `T parameter` → Java对象的值
- `JdbcType jdbcType` → 对应的JDBC数据库类型

**getResult 参数说明**

```java
T getResult(ResultSet rs, String columnName)
              ↓              ↓
           结果集对象        列名
```

**参数解释**：
- `ResultSet rs` → 查询结果集
- `String columnName` → 数据库列名
- 返回值 `T` → 转换后的Java对象

---

## 3. 🛠️ BaseTypeHandler基础处理器


### 3.1 为什么使用BaseTypeHandler


> ⚠️ **新手注意**
> 
> 直接实现TypeHandler接口有4个方法要实现，太麻烦！
> 
> MyBatis提供了**BaseTypeHandler抽象类**，只需实现2个核心方法即可！

**对比一下**：

```
【直接实现TypeHandler】
public class MyHandler implements TypeHandler<String> {
  // 要实现4个方法 😰
  void setParameter(...) { }
  String getResult(ResultSet, String) { }
  String getResult(ResultSet, int) { }
  String getResult(CallableStatement, int) { }
}

【继承BaseTypeHandler】  
public class MyHandler extends BaseTypeHandler<String> {
  // 只需实现2个方法 😊
  void setNonNullParameter(...) { }
  String getNullableResult(...) { }
}
```

### 3.2 BaseTypeHandler核心方法


**🔸 两大核心方法**

| 方法 | 职责 | 💡 特点 |
|------|------|--------|
| `setNonNullParameter()` | **设置非空参数** | 不用处理null，框架已处理 |
| `getNullableResult()` | **获取可能为空的结果** | 需要处理null情况 |

**方法签名**

```java
public abstract class BaseTypeHandler<T> extends TypeReference<T> 
                                          implements TypeHandler<T> {
  
  // ① 设置非空参数（保存时调用）
  public abstract void setNonNullParameter(
    PreparedStatement ps,    // SQL语句对象
    int i,                   // 参数位置
    T parameter,             // Java值（不会是null）
    JdbcType jdbcType        // 数据库类型
  ) throws SQLException;
  
  // ② 获取结果（查询时调用）
  public abstract T getNullableResult(
    ResultSet rs,            // 结果集
    String columnName        // 列名
  ) throws SQLException;
  
  // ③ 获取结果（通过索引）
  public abstract T getNullableResult(
    ResultSet rs,            // 结果集
    int columnIndex          // 列索引
  ) throws SQLException;
  
  // ④ 获取结果（存储过程）
  public abstract T getNullableResult(
    CallableStatement cs,    // 存储过程对象
    int columnIndex          // 参数索引
  ) throws SQLException;
}
```

### 3.3 BaseTypeHandler的优势


**✅ 自动处理null值**

```
框架自动帮你做了：

setParameter() {
  if (parameter == null) {
    ps.setNull(i, jdbcType);  ← 框架处理null
  } else {
    setNonNullParameter(...);  ← 你只需实现这个
  }
}
```

**✅ 统一异常处理**

```
框架自动包装SQLException：

try {
  你的处理逻辑
} catch (Exception e) {
  throw new TypeException("错误信息", e);  ← 框架统一处理
}
```

---

## 4. 🔨 自定义类型处理器开发


### 4.1 开发步骤概览


```
步骤 1️⃣  确定要转换的类型（Java类型 ↔ 数据库类型）
         ↓
步骤 2️⃣  继承BaseTypeHandler<T>泛型类
         ↓
步骤 3️⃣  实现setNonNullParameter方法（保存逻辑）
         ↓
步骤 4️⃣  实现getNullableResult方法（查询逻辑）
         ↓
步骤 5️⃣  注册类型处理器到MyBatis配置
```

### 4.2 实战案例：性别类型处理器


**🎯 需求场景**

```
数据库设计：
gender TINYINT  → 0代表女，1代表男

Java实体类：
private String gender;  → "男" 或 "女"

问题：
如何自动在 "男/女" 和 0/1 之间转换？
```

**💻 完整代码实现**

```java
/**
 * 性别类型处理器
 * 数据库存储：0=女，1=男
 * Java使用：  "女"=女，"男"=男
 */
@MappedJdbcTypes(JdbcType.TINYINT)  // ← 处理TINYINT类型
@MappedTypes(String.class)           // ← 对应Java的String类型
public class GenderTypeHandler extends BaseTypeHandler<String> {
    
    // ① 保存到数据库：将"男/女"转成0/1
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                     String parameter, JdbcType jdbcType) 
                                     throws SQLException {
        // 字符串转数字
        if ("男".equals(parameter)) {
            ps.setInt(i, 1);      // 男 → 1
        } else if ("女".equals(parameter)) {
            ps.setInt(i, 0);      // 女 → 0
        } else {
            throw new TypeException("性别值只能是'男'或'女'");
        }
    }
    
    // ② 从数据库查询：将0/1转成"男/女"
    @Override
    public String getNullableResult(ResultSet rs, String columnName) 
                                     throws SQLException {
        int gender = rs.getInt(columnName);  // 获取数字
        if (rs.wasNull()) {                  // 判断是否为null
            return null;
        }
        return gender == 1 ? "男" : "女";    // 数字转字符串
    }
    
    // ③ 通过列索引获取
    @Override
    public String getNullableResult(ResultSet rs, int columnIndex) 
                                     throws SQLException {
        int gender = rs.getInt(columnIndex);
        return rs.wasNull() ? null : (gender == 1 ? "男" : "女");
    }
    
    // ④ 存储过程获取
    @Override
    public String getNullableResult(CallableStatement cs, int columnIndex) 
                                     throws SQLException {
        int gender = cs.getInt(columnIndex);
        return cs.wasNull() ? null : (gender == 1 ? "男" : "女");
    }
}
```

**📝 代码关键点解析**

| 注解/方法 | 🔧 作用 |
|----------|--------|
| `@MappedJdbcTypes` | 声明处理的数据库类型 |
| `@MappedTypes` | 声明处理的Java类型 |
| `setNonNullParameter` | 保存时转换：String → int |
| `getNullableResult` | 查询时转换：int → String |
| `rs.wasNull()` | 判断数据库值是否为NULL |

### 4.3 使用类型处理器


**在Mapper XML中使用**

```xml
<!-- 插入时自动转换 -->
<insert id="addUser">
    INSERT INTO user(name, gender) 
    VALUES(#{name}, #{gender, typeHandler=com.example.GenderTypeHandler})
</insert>

<!-- 查询时自动转换 -->
<resultMap id="userMap" type="User">
    <id property="id" column="id"/>
    <result property="name" column="name"/>
    <result property="gender" column="gender" 
            typeHandler="com.example.GenderTypeHandler"/>
</resultMap>
```

**🎉 效果展示**

```
保存数据：
Java: user.setGender("男")
  ↓ 经过TypeHandler转换
SQL: INSERT INTO user VALUES(?, 1)

查询数据：
SQL: SELECT * FROM user  → gender=1
  ↓ 经过TypeHandler转换
Java: user.getGender()  → "男"
```

---

## 5. 🌟 常用类型处理器详解


### 5.1 枚举类型处理器


> 💡 **为什么需要枚举处理器**
> 
> Java中用枚举表示状态很优雅，但数据库不认识枚举，需要转换！

**🔸 两种内置枚举处理器对比**

| 处理器类型 | 存储方式 | 优点 | 缺点 |
|-----------|---------|------|------|
| `EnumTypeHandler` | 存储枚举名称 | 可读性好 | 占空间大 |
| `EnumOrdinalTypeHandler` | 存储枚举序号 | 节省空间 | 可读性差 |

**💻 枚举定义**

```java
// 订单状态枚举
public enum OrderStatus {
    PENDING("待支付", 0),
    PAID("已支付", 1),
    SHIPPED("已发货", 2),
    COMPLETED("已完成", 3),
    CANCELLED("已取消", 4);
    
    private String desc;
    private int code;
    
    OrderStatus(String desc, int code) {
        this.desc = desc;
        this.code = code;
    }
    
    // getter方法
    public int getCode() { return code; }
    public String getDesc() { return desc; }
}
```

**📊 内置处理器效果对比**

```
【EnumTypeHandler - 存储名称】
Java:  OrderStatus.PAID
  ↓
数据库: VARCHAR "PAID"  ← 存的是枚举名

【EnumOrdinalTypeHandler - 存储序号】
Java:  OrderStatus.PAID
  ↓
数据库: INT 1  ← 存的是枚举位置（从0开始）
```

**🔧 自定义枚举处理器（推荐）**

```java
/**
 * 通用枚举处理器 - 存储code值
 */
public class UniversalEnumTypeHandler<E extends Enum<E>> 
                                      extends BaseTypeHandler<E> {
    
    private Class<E> type;
    
    public UniversalEnumTypeHandler(Class<E> type) {
        this.type = type;
    }
    
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                     E parameter, JdbcType jdbcType) 
                                     throws SQLException {
        // 假设枚举都有getCode()方法
        try {
            Method method = type.getMethod("getCode");
            int code = (int) method.invoke(parameter);
            ps.setInt(i, code);  // 存储code值
        } catch (Exception e) {
            throw new TypeException("枚举必须有getCode()方法", e);
        }
    }
    
    @Override
    public E getNullableResult(ResultSet rs, String columnName) 
                                throws SQLException {
        int code = rs.getInt(columnName);
        if (rs.wasNull()) return null;
        
        // 根据code找到对应枚举
        return getEnumByCode(code);
    }
    
    // 其他方法类似...
    
    private E getEnumByCode(int code) {
        for (E enumConst : type.getEnumConstants()) {
            try {
                Method method = type.getMethod("getCode");
                if ((int)method.invoke(enumConst) == code) {
                    return enumConst;
                }
            } catch (Exception e) {
                throw new TypeException("获取枚举失败", e);
            }
        }
        throw new TypeException("未找到code=" + code + "的枚举值");
    }
}
```

### 5.2 日期时间类型处理器


**🔸 Java 8日期处理器对照表**

| Java类型 | 数据库类型 | MyBatis处理器 | 🎯 用途 |
|----------|-----------|---------------|--------|
| `LocalDate` | DATE | `LocalDateTypeHandler` | 只存日期 2024-09-23 |
| `LocalTime` | TIME | `LocalTimeTypeHandler` | 只存时间 14:30:00 |
| `LocalDateTime` | TIMESTAMP | `LocalDateTimeTypeHandler` | 日期+时间 |
| `Instant` | TIMESTAMP | `InstantTypeHandler` | UTC时间戳 |

**💻 自定义日期格式处理器**

```java
/**
 * 自定义日期格式处理器
 * 数据库：VARCHAR格式 "2024-09-23 14:30:00"
 * Java：  LocalDateTime对象
 */
public class CustomDateTimeHandler extends BaseTypeHandler<LocalDateTime> {
    
    private static final DateTimeFormatter FORMATTER = 
        DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
    
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                     LocalDateTime parameter, 
                                     JdbcType jdbcType) throws SQLException {
        // LocalDateTime → String
        String dateStr = parameter.format(FORMATTER);
        ps.setString(i, dateStr);
    }
    
    @Override
    public LocalDateTime getNullableResult(ResultSet rs, String columnName) 
                                           throws SQLException {
        String dateStr = rs.getString(columnName);
        if (dateStr == null) return null;
        
        // String → LocalDateTime
        return LocalDateTime.parse(dateStr, FORMATTER);
    }
    
    // 其他方法类似...
}
```

**🎯 使用场景**

```xml
<!-- 配置日期处理器 -->
<resultMap id="orderMap" type="Order">
    <result property="createTime" column="create_time" 
            typeHandler="com.example.CustomDateTimeHandler"/>
</resultMap>
```

---

## 6. 🚀 高级类型处理器开发


### 6.1 JSON类型处理器


> 💡 **应用场景**
> 
> 数据库存储JSON字符串，Java中使用List、Map等集合对象

**💻 JSON列表处理器**

```java
/**
 * JSON数组类型处理器
 * 数据库：VARCHAR "[\"java\",\"python\",\"go\"]"
 * Java：  List<String> ["java","python","go"]
 */
public class JsonListTypeHandler extends BaseTypeHandler<List<String>> {
    
    private static final ObjectMapper MAPPER = new ObjectMapper();
    
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                     List<String> parameter, 
                                     JdbcType jdbcType) throws SQLException {
        try {
            // List → JSON字符串
            String json = MAPPER.writeValueAsString(parameter);
            ps.setString(i, json);
        } catch (JsonProcessingException e) {
            throw new TypeException("JSON序列化失败", e);
        }
    }
    
    @Override
    public List<String> getNullableResult(ResultSet rs, String columnName) 
                                          throws SQLException {
        String json = rs.getString(columnName);
        if (json == null || json.isEmpty()) {
            return new ArrayList<>();
        }
        
        try {
            // JSON字符串 → List
            return MAPPER.readValue(json, 
                new TypeReference<List<String>>() {});
        } catch (JsonProcessingException e) {
            throw new TypeException("JSON反序列化失败", e);
        }
    }
    
    // 其他方法类似...
}
```

**🎯 实际使用**

```java
// 实体类
public class User {
    private Long id;
    private String name;
    private List<String> hobbies;  // 兴趣爱好列表
}
```

```xml
<!-- Mapper配置 -->
<resultMap id="userMap" type="User">
    <result property="hobbies" column="hobbies" 
            typeHandler="com.example.JsonListTypeHandler"/>
</resultMap>
```

```
效果：
数据库: hobbies = '["游泳","跑步","篮球"]'
  ↓
Java:  user.getHobbies() = ["游泳", "跑步", "篮球"]
```

### 6.2 数组类型处理器


**💻 字符串数组处理器**

```java
/**
 * 数组类型处理器
 * 数据库：VARCHAR "tag1,tag2,tag3"（逗号分隔）
 * Java：  String[] ["tag1","tag2","tag3"]
 */
public class StringArrayTypeHandler extends BaseTypeHandler<String[]> {
    
    private static final String SEPARATOR = ",";
    
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                     String[] parameter, 
                                     JdbcType jdbcType) throws SQLException {
        // 数组 → 逗号分隔字符串
        String value = String.join(SEPARATOR, parameter);
        ps.setString(i, value);
    }
    
    @Override
    public String[] getNullableResult(ResultSet rs, String columnName) 
                                      throws SQLException {
        String value = rs.getString(columnName);
        if (value == null || value.isEmpty()) {
            return new String[0];
        }
        
        // 逗号分隔字符串 → 数组
        return value.split(SEPARATOR);
    }
    
    // 其他方法类似...
}
```

### 6.3 地理位置类型处理器


**💻 坐标点处理器**

```java
/**
 * 地理位置处理器
 * 数据库：VARCHAR "116.404,39.915"（经度,纬度）
 * Java：  Point对象
 */
@Data
@AllArgsConstructor
public class Point {
    private double longitude;  // 经度
    private double latitude;   // 纬度
}

public class PointTypeHandler extends BaseTypeHandler<Point> {
    
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                     Point parameter, 
                                     JdbcType jdbcType) throws SQLException {
        // Point → "经度,纬度"
        String value = parameter.getLongitude() + "," + 
                      parameter.getLatitude();
        ps.setString(i, value);
    }
    
    @Override
    public Point getNullableResult(ResultSet rs, String columnName) 
                                   throws SQLException {
        String value = rs.getString(columnName);
        if (value == null) return null;
        
        // "经度,纬度" → Point
        String[] parts = value.split(",");
        double lng = Double.parseDouble(parts[0]);
        double lat = Double.parseDouble(parts[1]);
        return new Point(lng, lat);
    }
    
    // 其他方法类似...
}
```

**🎯 使用示例**

```java
// 实体类
public class Store {
    private Long id;
    private String name;
    private Point location;  // 店铺位置
}
```

```
效果：
数据库: location = '116.404,39.915'
  ↓
Java:  Point(116.404, 39.915)
```

---

## 7. ⚙️ 类型处理器注册与配置


### 7.1 全局配置注册


**方式一：MyBatis配置文件注册**

```xml
<!-- mybatis-config.xml -->
<configuration>
    <typeHandlers>
        <!-- 注册单个处理器 -->
        <typeHandler handler="com.example.GenderTypeHandler"/>
        <typeHandler handler="com.example.JsonListTypeHandler"/>
        
        <!-- 扫描包下所有处理器 -->
        <package name="com.example.typehandler"/>
    </typeHandlers>
</configuration>
```

**方式二：Spring Boot配置**

```yaml
# application.yml
mybatis:
  type-handlers-package: com.example.typehandler  # 扫描包
  configuration:
    default-enum-type-handler: org.apache.ibatis.type.EnumTypeHandler
```

```java
// 或者用配置类
@Configuration
public class MyBatisConfig {
    
    @Bean
    public ConfigurationCustomizer configurationCustomizer() {
        return configuration -> {
            // 注册自定义处理器
            configuration.getTypeHandlerRegistry()
                .register(GenderTypeHandler.class);
        };
    }
}
```

### 7.2 局部指定处理器


**在Mapper XML中指定**

```xml
<!-- 插入时指定 -->
<insert id="addUser">
    INSERT INTO user(gender, hobbies) 
    VALUES(
        #{gender, typeHandler=com.example.GenderTypeHandler},
        #{hobbies, typeHandler=com.example.JsonListTypeHandler}
    )
</insert>

<!-- ResultMap中指定 -->
<resultMap id="userMap" type="User">
    <result property="gender" column="gender" 
            typeHandler="com.example.GenderTypeHandler"/>
    <result property="hobbies" column="hobbies" 
            typeHandler="com.example.JsonListTypeHandler"/>
</resultMap>
```

**在注解中指定**

```java
@Results({
    @Result(property = "gender", column = "gender",
            typeHandler = GenderTypeHandler.class),
    @Result(property = "hobbies", column = "hobbies",
            typeHandler = JsonListTypeHandler.class)
})
@Select("SELECT * FROM user WHERE id = #{id}")
User selectById(Long id);
```

### 7.3 类型处理器优先级


```
类型处理器生效优先级（从高到低）：

优先级 1️⃣  SQL语句中显式指定
          ↓
优先级 2️⃣  ResultMap中配置的处理器
          ↓
优先级 3️⃣  @MappedTypes注解指定的类型
          ↓
优先级 4️⃣  全局配置的默认处理器
          ↓
优先级 5️⃣  MyBatis内置处理器
```

**🔸 优先级示例**

```xml
<!-- 全局配置了GenderTypeHandler -->
<typeHandlers>
    <typeHandler handler="com.example.GenderTypeHandler"/>
</typeHandlers>

<!-- 但SQL中显式指定了另一个处理器，优先使用SQL中的 -->
<insert id="addUser">
    INSERT INTO user(gender) 
    VALUES(#{gender, typeHandler=com.example.CustomGenderHandler})
</insert>
```

### 7.4 处理器注册最佳实践


**✅ 推荐做法**

```
1️⃣  通用处理器 → 全局注册（扫描包）
   例：GenderTypeHandler、JsonListTypeHandler

2️⃣  特殊场景处理器 → 局部指定
   例：某个字段需要特殊格式的日期处理器

3️⃣  使用@MappedTypes和@MappedJdbcTypes注解
   让MyBatis自动识别处理类型
```

**⚠️ 注意事项**

> **避免类型冲突**
> 
> 同一个Java类型不要注册多个全局处理器，会导致MyBatis不知道用哪个！
> 
> ❌ 错误示例：
> ```xml
> <typeHandlers>
>     <typeHandler handler="GenderHandler1"/>  <!-- 都处理String -->
>     <typeHandler handler="GenderHandler2"/>  <!-- 都处理String -->
> </typeHandlers>
> ```
> 
> ✅ 正确做法：
> - 用@MappedJdbcTypes区分不同数据库类型
> - 或在使用时显式指定typeHandler

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 类型处理器本质：Java类型与数据库类型的"翻译官"
🔸 核心接口：TypeHandler（4个方法）和BaseTypeHandler（2个方法）
🔸 工作时机：setParameter保存时，getResult查询时
🔸 注册方式：全局配置、局部指定、注解标记
🔸 优先级：SQL指定 > ResultMap > 注解 > 全局 > 内置
```

### 8.2 开发自定义处理器的步骤


| 步骤 | 🔧 操作 | 💡 要点 |
|------|--------|--------|
| **1️⃣ 继承** | `extends BaseTypeHandler<T>` | 泛型T是Java类型 |
| **2️⃣ 注解** | `@MappedTypes`、`@MappedJdbcTypes` | 声明处理类型 |
| **3️⃣ 实现** | `setNonNullParameter` | Java→数据库转换 |
| **4️⃣ 实现** | `getNullableResult`（3个） | 数据库→Java转换 |
| **5️⃣ 注册** | 全局/局部配置 | 让MyBatis识别 |

### 8.3 常用处理器速查表


| 处理器 | Java类型 | 数据库类型 | 🎯 场景 |
|--------|----------|-----------|--------|
| `GenderTypeHandler` | String | TINYINT | 性别转换 |
| `EnumTypeHandler` | Enum | VARCHAR | 枚举名称存储 |
| `EnumOrdinalTypeHandler` | Enum | INT | 枚举序号存储 |
| `LocalDateTimeTypeHandler` | LocalDateTime | TIMESTAMP | 日期时间 |
| `JsonListTypeHandler` | List | VARCHAR | JSON数组 |
| `StringArrayTypeHandler` | String[] | VARCHAR | 逗号分隔数组 |
| `PointTypeHandler` | Point对象 | VARCHAR | 地理坐标 |

### 8.4 最佳实践建议


**✅ 推荐做法**

```
1️⃣  优先使用BaseTypeHandler，代码更简洁
2️⃣  处理器类加@MappedTypes和@MappedJdbcTypes注解
3️⃣  通用处理器全局注册，特殊处理器局部指定
4️⃣  getNullableResult方法记得判断null
5️⃣  处理器要考虑异常情况，抛出TypeException
```

**⚠️ 避免踩坑**

```
❌  忘记判断null值导致NullPointerException
❌  同一类型注册多个全局处理器导致冲突
❌  序列化/反序列化没有异常处理
❌  枚举处理器没有考虑新增枚举值的情况
❌  JSON处理器没有处理空字符串""的情况
```

### 8.5 学习路线建议


```
新手入门：
├─ 1️⃣  理解TypeHandler的作用和工作原理
├─ 2️⃣  学会使用MyBatis内置处理器
└─ 3️⃣  开发简单的GenderTypeHandler练手

进阶提升：
├─ 4️⃣  掌握BaseTypeHandler的使用
├─ 5️⃣  开发JSON、数组等复杂类型处理器
└─ 6️⃣  理解处理器注册和优先级机制

高级应用：
├─ 7️⃣  开发通用枚举处理器
├─ 8️⃣  处理地理位置、加密字段等特殊类型
└─ 9️⃣  在实际项目中合理使用类型处理器
```

### 8.6 记忆口诀


> 🧠 **类型处理器记忆法**
> 
> *"类型转换找Handler，Base继承更简单；*
> *setParameter存数据，getResult取出来；*  
> *注解标记类型匹配，全局局部两种配；*
> *null值判断要仔细，异常处理别遗漏"*

---

**🎉 恭喜！你已经掌握了MyBatis类型处理器的核心知识！**

类型处理器是MyBatis中非常实用的扩展机制，合理使用可以让代码更优雅、更易维护。记住：
- ✅ 简单场景用内置处理器
- ✅ 复杂场景自定义处理器
- ✅ 先理解原理，再灵活应用
