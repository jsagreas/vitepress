---
title: 4、参数传递与处理机制
---
## 📚 目录

1. [参数传递基础概念](#1-参数传递基础概念)
2. [单个参数传递](#2-单个参数传递)
3. [多个参数传递](#3-多个参数传递)
4. [对象与集合参数](#4-对象与集合参数)
5. [类型处理器机制](#5-类型处理器机制)
6. [特殊类型处理](#6-特殊类型处理)
7. [最佳实践与总结](#7-最佳实践与总结)

---

## 1. 🎯 参数传递基础概念


### 1.1 什么是参数传递


**生活化理解**：
想象你去餐厅点餐，服务员问你"要什么？"，你可以：
- 简单回答："一碗面"（单个参数）
- 详细说明："一碗牛肉面，不要香菜，多加辣椒"（多个参数）
- 拿出点餐单："按这个单子做"（对象参数）

MyBatis的参数传递就是这样的过程，你告诉它查询条件，它帮你去数据库"取数据"。

```
参数传递的本质：
Java方法参数 → MyBatis处理 → SQL语句占位符

就像这样：
方法调用: findUserById(10)
         ↓
MyBatis处理: 把10传给SQL
         ↓
SQL执行: SELECT * FROM user WHERE id = 10
```

### 1.2 为什么需要理解参数传递


**🔍 核心原因**：

```
1️⃣ 避免常见错误
   错误示例：多个参数不加@Param注解
   结果：参数绑定失败，SQL执行报错

2️⃣ 提升开发效率  
   知道何时用对象，何时用Map
   选择最简洁的传参方式

3️⃣ 解决复杂场景
   动态查询、批量操作、类型转换
   这些都依赖正确的参数传递
```

### 1.3 参数传递全景图


```
📊 MyBatis参数传递方式概览：

基础传递：
├─ 单个参数 → 直接使用，无需注解
├─ 多个参数 → 必须用@Param或位置参数
└─ 无参数   → 直接写SQL，不用占位符

对象传递：
├─ POJO对象 → 通过属性名获取值
├─ Map对象  → 通过key获取值
└─ 集合对象 → 用于批量操作

特殊传递：
├─ 数组参数 → 常用于in查询
├─ List参数 → 批量插入/更新
└─ 枚举参数 → 需要类型处理器
```

---

## 2. 🔢 单个参数传递


### 2.1 基本用法


**最简单的情况**：方法只有一个参数

```java
// Mapper接口
public interface UserMapper {
    // 单个参数：根据ID查询用户
    User findById(Integer id);
    
    // 单个参数：根据用户名查询
    User findByUsername(String username);
}
```

**📝 对应的XML写法**：

```xml
<!-- 方式1: 使用#{任意名称}，MyBatis会自动匹配 -->
<select id="findById" resultType="User">
    SELECT * FROM user WHERE id = #{id}
</select>

<!-- 方式2: 名称随意，都能正确获取参数值 -->
<select id="findById" resultType="User">
    SELECT * FROM user WHERE id = #{value}
</select>

<!-- 方式3: 用其他名字也可以 -->
<select id="findById" resultType="User">
    SELECT * FROM user WHERE id = #{userId}
</select>
```

**💡 理解要点**：
```
单个参数的特殊性：
✅ 不需要任何注解
✅ #{} 里的名称可以随便写
✅ MyBatis会自动把参数值填进去

为什么这么灵活？
因为只有一个参数，MyBatis知道你要的就是它
不会产生歧义，所以名称不重要
```

### 2.2 基本类型参数


**📌 常见基本类型处理**：

```java
public interface UserMapper {
    // 整数类型
    List<User> findByAge(int age);
    
    // 字符串类型
    User findByEmail(String email);
    
    // 布尔类型
    List<User> findByStatus(boolean active);
    
    // 长整型
    User findByPhone(Long phoneNumber);
}
```

```xml
<!-- 整数参数 -->
<select id="findByAge" resultType="User">
    SELECT * FROM user WHERE age = #{age}
</select>

<!-- 字符串参数：模糊查询 -->
<select id="findByEmail" resultType="User">
    SELECT * FROM user 
    WHERE email LIKE CONCAT('%', #{email}, '%')
</select>

<!-- 布尔参数 -->
<select id="findByStatus" resultType="User">
    SELECT * FROM user WHERE is_active = #{active}
</select>
```

**🔍 注意事项**：
```
基本类型的小陷阱：
⚠️ int类型不能为null
   如果可能为空，用Integer

⚠️ 字符串注意SQL注入
   不要用${} 拼接用户输入
   必须用#{} 来防止注入

⚠️ 布尔值的数据库表示
   MySQL中通常是0/1或TINYINT类型
```

---

## 3. 🔀 多个参数传递


### 3.1 问题的产生


**❌ 错误示范**：不加注解直接传多个参数

```java
public interface UserMapper {
    // 这样写会报错！
    User findByUsernameAndPassword(String username, String password);
}
```

```xml
<!-- 这个SQL会执行失败 -->
<select id="findByUsernameAndPassword" resultType="User">
    SELECT * FROM user 
    WHERE username = #{username} AND password = #{password}
</select>
```

**🔴 为什么会报错？**
```
错误信息：
Parameter 'username' not found. 
Available parameters are [arg0, arg1, param1, param2]

原因解释：
MyBatis不知道你的参数叫什么名字
它只知道第一个参数、第二个参数...
所以只能用arg0、arg1或param1、param2来表示
```

### 3.2 解决方案一：使用@Param注解


**✅ 正确写法**：给每个参数加上@Param注解

```java
public interface UserMapper {
    // 用@Param给参数命名
    User findByUsernameAndPassword(
        @Param("username") String username,
        @Param("password") String password
    );
    
    // 分页查询
    List<User> findByPage(
        @Param("offset") int offset,
        @Param("limit") int limit
    );
    
    // 多条件查询
    List<User> searchUsers(
        @Param("keyword") String keyword,
        @Param("minAge") Integer minAge,
        @Param("maxAge") Integer maxAge
    );
}
```

```xml
<!-- 现在可以用注解指定的名称了 -->
<select id="findByUsernameAndPassword" resultType="User">
    SELECT * FROM user 
    WHERE username = #{username} AND password = #{password}
</select>

<!-- 分页查询 -->
<select id="findByPage" resultType="User">
    SELECT * FROM user 
    LIMIT #{offset}, #{limit}
</select>

<!-- 多条件查询 -->
<select id="searchUsers" resultType="User">
    SELECT * FROM user 
    WHERE username LIKE CONCAT('%', #{keyword}, '%')
    <if test="minAge != null">
        AND age &gt;= #{minAge}
    </if>
    <if test="maxAge != null">
        AND age &lt;= #{maxAge}
    </if>
</select>
```

**💡 @Param注解的作用**：
```
作用说明：
1. 给参数取个"代号"
   就像给变量起名字一样

2. 让XML中的#{名称}能找到对应参数
   名称对应了，值就能正确传递

3. 提高代码可读性
   一眼就知道这个参数是做什么的

最佳实践：
✅ 只要有多个参数，建议都加@Param
✅ 名称要见名知意
✅ 保持Java接口和XML的名称一致
```

### 3.3 解决方案二：使用位置参数


**🔢 使用arg或param**：

```xml
<!-- 方式1: 使用arg0, arg1... -->
<select id="findByUsernameAndPassword" resultType="User">
    SELECT * FROM user 
    WHERE username = #{arg0} AND password = #{arg1}
</select>

<!-- 方式2: 使用param1, param2... -->
<select id="findByUsernameAndPassword" resultType="User">
    SELECT * FROM user 
    WHERE username = #{param1} AND password = #{param2}
</select>
```

**⚖️ 位置参数的优缺点**：
```
优点：
✅ 不需要写@Param注解
✅ 代码稍微简洁一点

缺点：
❌ 可读性差：arg0是什么意思？
❌ 维护困难：参数顺序变了就错了
❌ 容易出错：数字序号容易搞混

建议：
🎯 不推荐使用位置参数
🎯 除非参数特别少（只有2个）且不会变
🎯 大多数情况还是用@Param更好
```

---

## 4. 📦 对象与集合参数


### 4.1 对象参数传递


**使用场景**：参数很多时，用对象更清晰

```java
// 用户实体类
public class User {
    private Integer id;
    private String username;
    private String password;
    private String email;
    private Integer age;
    // getter和setter省略...
}

// Mapper接口
public interface UserMapper {
    // 插入用户：传入整个对象
    int insertUser(User user);
    
    // 更新用户：传入对象
    int updateUser(User user);
}
```

```xml
<!-- 插入操作：通过对象属性名获取值 -->
<insert id="insertUser">
    INSERT INTO user (username, password, email, age)
    VALUES (#{username}, #{password}, #{email}, #{age})
</insert>

<!-- 更新操作 -->
<update id="updateUser">
    UPDATE user SET
        username = #{username},
        password = #{password},
        email = #{email},
        age = #{age}
    WHERE id = #{id}
</update>
```

**🔍 对象参数的原理**：
```
参数传递过程：
Java对象 → MyBatis → 通过getter获取属性值

示例：
user.getUsername() → #{username}
user.getAge()      → #{age}

注意事项：
✅ #{属性名} 必须对应类中的属性
✅ 实际调用的是get方法
✅ 属性名大小写敏感
✅ 必须有对应的getter方法
```

### 4.2 Map参数传递


**灵活的传参方式**：不想创建类时用Map

```java
public interface UserMapper {
    // 使用Map传递参数
    List<User> findByConditions(Map<String, Object> params);
    
    // 复杂查询条件
    List<User> complexSearch(Map<String, Object> conditions);
}
```

```xml
<!-- Map参数：通过key获取value -->
<select id="findByConditions" resultType="User">
    SELECT * FROM user 
    WHERE 1=1
    <if test="username != null">
        AND username = #{username}
    </if>
    <if test="minAge != null">
        AND age &gt;= #{minAge}
    </if>
    <if test="maxAge != null">
        AND age &lt;= #{maxAge}
    </if>
</select>
```

**💻 调用示例**：

```java
// 构建查询条件
Map<String, Object> params = new HashMap<>();
params.put("username", "张三");
params.put("minAge", 18);
params.put("maxAge", 30);

// 执行查询
List<User> users = userMapper.findByConditions(params);
```

**⚖️ Map vs 对象的选择**：

| 场景 | **推荐方式** | **原因** |
|------|-------------|---------|
| **固定业务实体** | `POJO对象` | `类型安全，代码清晰` |
| **动态查询条件** | `Map` | `灵活，不用创建很多类` |
| **临时操作** | `Map` | `快速实现，不想建类` |
| **团队协作** | `POJO对象` | `便于理解和维护` |

### 4.3 List集合参数


**批量操作的利器**：

```java
public interface UserMapper {
    // 批量插入用户
    int insertBatch(List<User> users);
    
    // 批量删除
    int deleteByIds(List<Integer> ids);
    
    // 根据用户名列表查询
    List<User> findByUsernames(List<String> usernames);
}
```

```xml
<!-- 批量插入 -->
<insert id="insertBatch">
    INSERT INTO user (username, password, email)
    VALUES
    <foreach collection="list" item="user" separator=",">
        (#{user.username}, #{user.password}, #{user.email})
    </foreach>
</insert>

<!-- 批量删除 -->
<delete id="deleteByIds">
    DELETE FROM user WHERE id IN
    <foreach collection="list" item="id" open="(" separator="," close=")">
        #{id}
    </foreach>
</delete>

<!-- IN查询 -->
<select id="findByUsernames" resultType="User">
    SELECT * FROM user WHERE username IN
    <foreach collection="list" item="name" open="(" separator="," close=")">
        #{name}
    </foreach>
</select>
```

**🔑 foreach关键属性**：
```
collection: 集合参数名
• list: 方法参数是List时
• array: 方法参数是数组时  
• 自定义: 用@Param指定的名称

item: 循环变量名
• 代表集合中的每一个元素
• 可以自己命名

separator: 分隔符
• 每个元素之间的符号
• 常用逗号,

open/close: 开始和结束符号
• 常用括号()
```

### 4.4 数组参数传递


```java
public interface UserMapper {
    // 数组参数
    List<User> findByIds(Integer[] ids);
    
    // 字符串数组
    List<User> findByNames(String[] names);
}
```

```xml
<!-- 数组参数：collection固定写array -->
<select id="findByIds" resultType="User">
    SELECT * FROM user WHERE id IN
    <foreach collection="array" item="id" open="(" separator="," close=")">
        #{id}
    </foreach>
</select>

<!-- 或者用@Param自定义名称 -->
<select id="findByIds" resultType="User">
    SELECT * FROM user WHERE id IN
    <foreach collection="idArray" item="id" open="(" separator="," close=")">
        #{id}
    </foreach>
</select>
```

**💡 数组 vs List的选择**：
```
使用建议：
🎯 一般推荐用List
   • Java集合框架更常用
   • 功能更丰富

🎯 数组的适用场景
   • 性能要求极高
   • 处理基本类型大量数据
   • 与旧代码兼容

实践经验：
现代Java开发更偏向使用List
除非有特殊性能需求或历史遗留代码
```

---

## 5. 🔧 类型处理器机制


### 5.1 什么是类型处理器


**简单理解**：类型处理器就是"翻译官"

```
Java类型 ←→ 数据库类型

例如：
Java的String    ←→  MySQL的VARCHAR
Java的Integer   ←→  MySQL的INT
Java的Date      ←→  MySQL的DATETIME

类型处理器的工作：
1. 存数据时：Java类型 → 数据库类型
2. 取数据时：数据库类型 → Java类型
```

### 5.2 MyBatis内置类型处理器


**📋 常用内置处理器**：

| Java类型 | **数据库类型** | **说明** |
|---------|--------------|---------|
| `String` | `VARCHAR/CHAR` | `字符串处理` |
| `Integer` | `INT` | `整数处理` |
| `Long` | `BIGINT` | `长整数处理` |
| `Double` | `DOUBLE` | `浮点数处理` |
| `Boolean` | `TINYINT/BIT` | `布尔值处理` |
| `Date` | `TIMESTAMP` | `日期时间处理` |
| `LocalDateTime` | `DATETIME` | `JDK8日期时间` |

**🔍 内置处理器的使用**：
```
无需配置，自动生效：
Integer age = 25;
MyBatis自动知道要转成数据库的INT类型

String name = "张三";  
MyBatis自动知道要转成VARCHAR类型

这些都是内置的，开箱即用
```

### 5.3 自定义类型处理器


**使用场景**：MyBatis不知道怎么处理的类型

```java
// 1. 创建自定义类型处理器
@MappedTypes(Phone.class)  // 指定处理的Java类型
@MappedJdbcTypes(JdbcType.VARCHAR)  // 指定对应的JDBC类型
public class PhoneTypeHandler extends BaseTypeHandler<Phone> {
    
    // 设置参数：Java对象 → 数据库
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                    Phone parameter, JdbcType jdbcType) 
                                    throws SQLException {
        // 把Phone对象转成字符串存入数据库
        ps.setString(i, parameter.getFullNumber());
    }
    
    // 获取结果：数据库 → Java对象（按列名）
    @Override
    public Phone getNullableResult(ResultSet rs, String columnName) 
                                   throws SQLException {
        String phoneStr = rs.getString(columnName);
        return phoneStr == null ? null : new Phone(phoneStr);
    }
    
    // 获取结果：数据库 → Java对象（按列索引）
    @Override
    public Phone getNullableResult(ResultSet rs, int columnIndex) 
                                   throws SQLException {
        String phoneStr = rs.getString(columnIndex);
        return phoneStr == null ? null : new Phone(phoneStr);
    }
    
    // 获取结果：数据库 → Java对象（存储过程）
    @Override
    public Phone getNullableResult(CallableStatement cs, int columnIndex) 
                                   throws SQLException {
        String phoneStr = cs.getString(columnIndex);
        return phoneStr == null ? null : new Phone(phoneStr);
    }
}
```

**📝 注册自定义类型处理器**：

```xml
<!-- mybatis-config.xml -->
<typeHandlers>
    <!-- 方式1: 指定具体的处理器 -->
    <typeHandler handler="com.example.handler.PhoneTypeHandler"/>
    
    <!-- 方式2: 扫描包下所有处理器 -->
    <package name="com.example.handler"/>
</typeHandlers>
```

**💡 理解要点**：
```
类型处理器的四个核心方法：

1. setNonNullParameter
   作用：存数据时调用
   转换：Java对象 → 数据库值

2. getNullableResult (三个重载)
   作用：取数据时调用
   转换：数据库值 → Java对象

为什么需要三个get方法？
• 按列名获取
• 按列索引获取  
• 存储过程调用

一般前两个用得最多
```

---

## 6. 🌟 特殊类型处理


### 6.1 枚举类型处理


**场景**：用户状态、订单状态等固定值

```java
// 定义枚举
public enum UserStatus {
    ACTIVE(1, "激活"),
    INACTIVE(0, "未激活"),
    BANNED(-1, "已封禁");
    
    private final int code;
    private final String desc;
    
    UserStatus(int code, String desc) {
        this.code = code;
        this.desc = desc;
    }
    
    public int getCode() { return code; }
    public String getDesc() { return desc; }
}

// 用户实体类
public class User {
    private Integer id;
    private String username;
    private UserStatus status;  // 枚举属性
}
```

**🔸 方式一：使用EnumTypeHandler（默认）**

```xml
<!-- 默认按枚举名称存储：ACTIVE、INACTIVE、BANNED -->
<insert id="insertUser">
    INSERT INTO user (username, status)
    VALUES (#{username}, #{status})
</insert>
<!-- 实际存入数据库的是：'ACTIVE' 这样的字符串 -->
```

**🔸 方式二：使用EnumOrdinalTypeHandler**

```xml
<!-- mybatis-config.xml -->
<typeHandlers>
    <typeHandler 
        handler="org.apache.ibatis.type.EnumOrdinalTypeHandler" 
        javaType="com.example.enums.UserStatus"/>
</typeHandlers>

<!-- 按枚举序号存储：0、1、2 -->
```

**🔸 方式三：自定义枚举处理器（推荐）**

```java
// 按code值存储的处理器
public class UserStatusTypeHandler extends BaseTypeHandler<UserStatus> {
    
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                    UserStatus parameter, JdbcType jdbcType) 
                                    throws SQLException {
        // 存储code值：1、0、-1
        ps.setInt(i, parameter.getCode());
    }
    
    @Override
    public UserStatus getNullableResult(ResultSet rs, String columnName) 
                                        throws SQLException {
        int code = rs.getInt(columnName);
        return code == 0 ? null : getStatusByCode(code);
    }
    
    // 其他方法省略...
    
    private UserStatus getStatusByCode(int code) {
        for (UserStatus status : UserStatus.values()) {
            if (status.getCode() == code) {
                return status;
            }
        }
        return null;
    }
}
```

**📊 三种方式对比**：

| 方式 | **数据库存储** | **优点** | **缺点** |
|------|--------------|---------|---------|
| **默认方式** | `ACTIVE字符串` | `可读性好` | `占空间，不支持国际化` |
| **序号方式** | `0,1,2数字` | `节省空间` | `可读性差，顺序不能变` |
| **自定义code** | `自定义数值` | `灵活可控` | `需要自己实现` |

### 6.2 日期类型处理


**🕐 JDK8之前：Date类型**

```java
public class User {
    private Date birthday;
    private Date createTime;
}
```

```xml
<!-- Date类型自动处理 -->
<insert id="insertUser">
    INSERT INTO user (username, birthday, create_time)
    VALUES (#{username}, #{birthday}, #{createTime})
</insert>

<!-- 如果需要指定格式，可以用jdbcType -->
<insert id="insertUser">
    INSERT INTO user (username, birthday, create_time)
    VALUES (
        #{username}, 
        #{birthday, jdbcType=DATE},
        #{createTime, jdbcType=TIMESTAMP}
    )
</insert>
```

**🕐 JDK8：LocalDateTime类型（推荐）**

```java
public class User {
    private LocalDateTime birthday;
    private LocalDateTime createTime;
    private LocalDate birthDate;
    private LocalTime wakeupTime;
}
```

```xml
<!-- MyBatis 3.4.5+ 原生支持JDK8日期 -->
<insert id="insertUser">
    INSERT INTO user (username, birthday, create_time, birth_date)
    VALUES (
        #{username}, 
        #{birthday},
        #{createTime},
        #{birthDate}
    )
</insert>
```

**💡 日期处理最佳实践**：
```
推荐使用：
✅ LocalDateTime：日期+时间
✅ LocalDate：只要日期
✅ LocalTime：只要时间

不推荐：
❌ Date：老旧，线程不安全
❌ Timestamp：属于JDBC类型
❌ Calendar：太复杂

时区处理：
• 统一使用UTC时间
• 数据库用TIMESTAMP类型
• 显示时再转换为用户时区
```

### 6.3 JSON类型处理


**现代Web开发常见需求**：存储JSON数据

```java
// 用户兴趣标签（JSON数组）
public class User {
    private Integer id;
    private String username;
    private List<String> tags;  // 存储为JSON
    private Map<String, Object> profile;  // 存储为JSON
}

// 自定义JSON类型处理器
public class JsonTypeHandler extends BaseTypeHandler<Object> {
    
    private static final ObjectMapper mapper = new ObjectMapper();
    
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                    Object parameter, JdbcType jdbcType) 
                                    throws SQLException {
        // 把Java对象转成JSON字符串
        try {
            ps.setString(i, mapper.writeValueAsString(parameter));
        } catch (JsonProcessingException e) {
            throw new SQLException(e);
        }
    }
    
    @Override
    public Object getNullableResult(ResultSet rs, String columnName) 
                                    throws SQLException {
        String json = rs.getString(columnName);
        if (json == null) return null;
        
        // JSON字符串转回Java对象
        try {
            return mapper.readValue(json, Object.class);
        } catch (JsonProcessingException e) {
            throw new SQLException(e);
        }
    }
    
    // 其他方法省略...
}
```

**📝 使用JSON处理器**：

```xml
<!-- 指定某个字段使用JSON处理器 -->
<resultMap id="userMap" type="User">
    <id property="id" column="id"/>
    <result property="username" column="username"/>
    <result property="tags" column="tags" 
            typeHandler="com.example.handler.JsonTypeHandler"/>
    <result property="profile" column="profile" 
            typeHandler="com.example.handler.JsonTypeHandler"/>
</resultMap>

<select id="findById" resultMap="userMap">
    SELECT * FROM user WHERE id = #{id}
</select>
```

**💼 实际应用场景**：
```
适合用JSON存储的数据：
✅ 用户标签、兴趣爱好
✅ 扩展属性、自定义字段
✅ 配置信息、选项设置
✅ 非关键业务数据

不适合用JSON的场景：
❌ 需要查询的字段
❌ 需要建索引的数据
❌ 关系型数据
❌ 核心业务字段
```

---

## 7. 📚 最佳实践与总结


### 7.1 参数传递最佳实践


**🎯 参数选择决策树**：

```
开始选择参数传递方式
    ↓
只有一个参数？ ──是──> 直接传递，无需注解
    ↓ 否
参数是业务实体？ ──是──> 使用POJO对象
    ↓ 否
参数超过3个？ ──是──> 考虑对象或Map
    ↓ 否
条件经常变化？ ──是──> 使用Map
    ↓ 否
使用@Param多参数传递
```

**✅ 推荐做法**：

```java
// 1. 单个参数：直接传递
User findById(Integer id);

// 2. 两个参数：使用@Param
List<User> findByPage(
    @Param("offset") int offset, 
    @Param("limit") int limit
);

// 3. 多个参数：使用对象
int insertUser(User user);

// 4. 动态条件：使用Map
List<User> search(Map<String, Object> conditions);

// 5. 批量操作：使用List
int insertBatch(List<User> users);
```

**❌ 应该避免的做法**：

```java
// 避免：超过3个参数不用@Param
List<User> badMethod(String a, String b, int c, int d);

// 避免：滥用Map导致类型不安全
User badMap(Map map);  // 应该指定泛型

// 避免：使用位置参数（arg0、param1）
// XML中写 #{arg0} #{param1} 可读性太差
```

### 7.2 类型处理器使用建议


**🔧 何时需要自定义类型处理器**：

```
必须自定义的场景：
1️⃣ 枚举按code值存储
2️⃣ 自定义业务类型（如电话号码、身份证）
3️⃣ 复杂对象与JSON互转
4️⃣ 加密解密处理

可以不自定义的场景：
✅ 基本类型：String、Integer等
✅ 标准日期：Date、LocalDateTime
✅ 枚举按名称存储
```

**📝 自定义处理器模板**：

```java
public class XxxTypeHandler extends BaseTypeHandler<Xxx> {
    
    // 1. 设置参数（存入数据库）
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, 
                                    Xxx parameter, JdbcType jdbcType) 
                                    throws SQLException {
        // TODO: Xxx类型 → 数据库类型
        ps.setString(i, parameter.toString());
    }
    
    // 2. 获取结果（从数据库取出）
    @Override
    public Xxx getNullableResult(ResultSet rs, String columnName) 
                                 throws SQLException {
        String value = rs.getString(columnName);
        // TODO: 数据库类型 → Xxx类型
        return value == null ? null : new Xxx(value);
    }
    
    // 3. 按索引获取（复制上面的逻辑）
    @Override
    public Xxx getNullableResult(ResultSet rs, int columnIndex) 
                                 throws SQLException {
        String value = rs.getString(columnIndex);
        return value == null ? null : new Xxx(value);
    }
    
    // 4. 存储过程获取（一般复制上面的逻辑）
    @Override
    public Xxx getNullableResult(CallableStatement cs, int columnIndex) 
                                 throws SQLException {
        String value = cs.getString(columnIndex);
        return value == null ? null : new Xxx(value);
    }
}
```

### 7.3 性能优化建议


**⚡ 参数传递性能考虑**：

```
批量操作优化：
❌ 循环调用单条插入（慢）
for (User user : users) {
    userMapper.insert(user);  // N次数据库操作
}

✅ 批量插入（快）
userMapper.insertBatch(users);  // 1次数据库操作

集合参数优化：
✅ 使用List而不是Set（List效率更高）
✅ IN查询时注意集合大小（建议<1000）
✅ 超大集合分批处理
```

**💾 类型转换优化**：

```
避免重复转换：
❌ 每次查询都转换JSON
✅ 使用缓存或懒加载

枚举处理：
❌ 每次都遍历values()
✅ 使用Map缓存code→枚举映射

日期处理：
✅ 优先使用数据库时间函数
✅ 批量查询时避免逐个转换
```

### 7.4 核心知识总结


**📋 必须掌握的要点**：

```
🔸 参数传递原则：
• 单个参数：直接用，#{任意名}
• 多个参数：必须@Param，或用对象
• 对象参数：#{属性名}调用getter
• 集合参数：配合foreach使用

🔸 类型处理器：
• 内置处理器：开箱即用
• 自定义处理器：继承BaseTypeHandler
• 四个方法：1个set + 3个get
• 注册方式：XML配置或注解

🔸 特殊类型：
• 枚举：建议自定义按code存储
• 日期：推荐LocalDateTime
• JSON：适合非查询字段
• 数组/List：批量操作必备
```

**🎯 学习检查清单**：

```
□ 理解单个参数的灵活性
□ 掌握@Param注解的使用
□ 会用对象和Map传参
□ 理解集合参数和foreach
□ 知道类型处理器的作用
□ 能自定义简单的类型处理器
□ 会处理枚举和日期类型
□ 了解JSON类型存储方案
```

**💡 实践建议**：

```
学习路径：
1. 从简单参数开始练习
2. 逐步尝试对象和集合参数
3. 遇到特殊类型再学处理器
4. 项目中积累最佳实践

避免误区：
❌ 不是所有类型都需要处理器
❌ 不要过度设计参数传递
❌ 注意SQL注入风险（用#不用$）
❌ 批量操作注意性能和事务

记住核心：
参数传递是手段，目标是写出
清晰、安全、高效的数据访问代码
```

**🧠 记忆口诀**：
```
单参简单直接用，
多参注解不能忘，
对象属性点语法，
集合批量配foreach，
类型转换有处理器，
枚举日期要注意，
安全性能两手抓，
清晰易懂是王道！
```