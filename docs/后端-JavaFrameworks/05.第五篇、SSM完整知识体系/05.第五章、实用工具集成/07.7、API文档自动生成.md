---
title: 7、API文档自动生成
---
## 📚 目录

1. [为什么需要API文档](#1-为什么需要API文档)
2. [Swagger3快速入门](#2-Swagger3快速入门)
3. [核心注解详解](#3-核心注解详解)
4. [Swagger UI界面定制](#4-Swagger-UI界面定制)
5. [高级功能应用](#5-高级功能应用)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 📖 为什么需要API文档


### 1.1 传统文档的痛点


> **🤔 想象这个场景**  
> 你辛辛苦苦写完了接口，然后要在Word里写文档：
> - 接口地址、请求方式、参数说明...全手敲
> - 改了代码，文档忘记更新，前端骂娘
> - 新人来了，看着过时的文档一脸懵

**传统方式的问题**：
```
手写文档的困境：
📝 写文档：费时费力，容易出错
🔄 不同步：代码改了文档忘更新
😫 难维护：项目大了根本维护不过来
❌ 难测试：想试接口还要用Postman单独配置
```

### 1.2 自动化文档的优势


**Swagger能帮你做什么？**

```
智能生成文档：
┌─────────────────────────────────────┐
│  你的代码                            │
│  @GetMapping("/user/{id}")          │
│  @ApiOperation("获取用户信息")       │
│  public User getUser(@PathVariable) │
└──────────────┬──────────────────────┘
               ↓ 自动扫描
┌─────────────────────────────────────┐
│  漂亮的在线文档                      │
│  • 接口列表自动生成                  │
│  • 参数说明自动展示                  │
│  • 在线测试一键调用                  │
│  • 代码同步文档同步                  │
└─────────────────────────────────────┘
```

**核心优势**：
- 🚀 **自动生成**：代码写完，文档就有了
- ✅ **永远同步**：代码改了，文档自动更新
- 🎯 **在线测试**：文档里直接测接口，不用Postman
- 📱 **美观易读**：界面友好，前后端都爱用

---

## 2. 🚀 Swagger3快速入门


### 2.1 什么是Swagger和OpenAPI


> **💡 核心理解**  
> - **OpenAPI**：一套接口描述的标准规范（就像普通话标准）
> - **Swagger**：实现这套规范的工具（就像说普通话的工具）
> - **Swagger3** = Swagger + OpenAPI3规范

**版本演变**：
```
Swagger发展史：
Swagger2 (旧版)  →  Swagger3 (新版)
   ↓                    ↓
使用Swagger2规范    使用OpenAPI3规范
注解：@Api          注解：@Tag
配置复杂            配置简化
```

### 2.2 Maven依赖配置


**引入Swagger3依赖**：

```xml
<!-- Swagger3 核心依赖 -->
<dependency>
    <groupId>io.springfox</groupId>
    <artifactId>springfox-boot-starter</artifactId>
    <version>3.0.0</version>
</dependency>
```

> **⚠️ 注意事项**  
> Swagger3只需要一个依赖就够了！不像Swagger2需要导入多个包

### 2.3 基础配置类


**创建Swagger配置**：

```java
@Configuration
@EnableOpenApi  // 开启Swagger3
public class SwaggerConfig {
    
    @Bean
    public Docket createApi() {
        return new Docket(DocumentationType.OAS_30) // 使用OpenAPI3规范
                .apiInfo(apiInfo())
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.example.controller"))
                .paths(PathSelectors.any())
                .build();
    }
    
    private ApiInfo apiInfo() {
        return new ApiInfoBuilder()
                .title("我的API文档")
                .description("这是一个示例项目的接口文档")
                .version("1.0")
                .contact(new Contact("张三", "http://example.com", "zhang@example.com"))
                .build();
    }
}
```

**配置说明**：

| 配置项 | 作用 | 说明 |
|--------|------|------|
| `DocumentationType.OAS_30` | 指定OpenAPI版本 | 使用OpenAPI 3.0规范 |
| `basePackage` | 扫描包路径 | 只扫描这个包下的Controller |
| `paths` | 接口路径规则 | `any()`表示所有路径都扫描 |
| `apiInfo` | 文档基本信息 | 标题、描述、版本等 |

### 2.4 访问文档


**启动项目后访问**：

```
Swagger UI 地址：
http://localhost:8080/swagger-ui/index.html

API文档 JSON：
http://localhost:8080/v3/api-docs
```

> **🎉 成功标志**  
> 能看到漂亮的接口文档页面，里面自动列出了你的所有接口

---

## 3. 🏷️ 核心注解详解


### 3.1 @Tag - 控制器分组


**作用**：给Controller分类，方便文档分组

```java
@RestController
@RequestMapping("/user")
@Tag(name = "用户管理", description = "用户相关的所有接口")
public class UserController {
    // 接口方法...
}
```

**效果展示**：
```
文档页面效果：
📁 用户管理 (User相关接口都在这里)
   ├─ GET  /user/list      获取用户列表
   ├─ GET  /user/{id}      获取用户详情
   └─ POST /user/save      保存用户信息
   
📁 订单管理 (Order相关接口都在这里)
   ├─ GET  /order/list     获取订单列表
   └─ POST /order/create   创建订单
```

> **💡 理解要点**  
> `@Tag` 就像给接口分文件夹，让文档更有条理

### 3.2 @Operation - 接口说明


**作用**：描述单个接口的功能

```java
@GetMapping("/{id}")
@Operation(
    summary = "根据ID获取用户",
    description = "传入用户ID，返回用户的详细信息，包括姓名、年龄、邮箱等"
)
public User getUserById(@PathVariable Long id) {
    return userService.getById(id);
}
```

**参数详解**：

| 参数 | 必填 | 说明 | 示例 |
|------|------|------|------|
| `summary` | ✅ | 接口简短描述 | "获取用户信息" |
| `description` | ❌ | 接口详细说明 | "根据用户ID查询..." |
| `tags` | ❌ | 额外分组标签 | `{"用户", "查询"}` |

### 3.3 @Parameter - 参数注解


**作用**：说明接口参数的含义

```java
@GetMapping("/list")
@Operation(summary = "获取用户列表")
public List<User> getUserList(
    @Parameter(description = "页码，从1开始", required = true, example = "1")
    @RequestParam Integer pageNum,
    
    @Parameter(description = "每页数量", required = false, example = "10")
    @RequestParam(defaultValue = "10") Integer pageSize,
    
    @Parameter(description = "用户名关键词", required = false)
    @RequestParam(required = false) String keyword
) {
    return userService.list(pageNum, pageSize, keyword);
}
```

**文档效果**：
```
参数列表展示：
┌─────────┬──────┬────────────────┬─────────┐
│ 参数名   │ 类型 │ 说明           │ 是否必填 │
├─────────┼──────┼────────────────┼─────────┤
│ pageNum │ int  │ 页码，从1开始   │ 是 ✅   │
│ pageSize│ int  │ 每页数量       │ 否 ❌   │
│ keyword │ str  │ 用户名关键词    │ 否 ❌   │
└─────────┴──────┴────────────────┴─────────┘
```

> **🎯 实用技巧**  
> `example`属性很重要！它会在测试时自动填充示例值

### 3.4 @Schema - 模型注解


**作用**：描述实体类的字段信息

```java
@Data
@Schema(description = "用户实体")
public class User {
    
    @Schema(description = "用户ID", example = "1001")
    private Long id;
    
    @Schema(description = "用户名", required = true, example = "张三")
    private String username;
    
    @Schema(description = "年龄", minimum = "0", maximum = "150", example = "25")
    private Integer age;
    
    @Schema(description = "邮箱", format = "email", example = "zhangsan@example.com")
    private String email;
}
```

**常用属性**：

| 属性 | 作用 | 示例 |
|------|------|------|
| `description` | 字段说明 | "用户的唯一标识" |
| `required` | 是否必填 | `true` / `false` |
| `example` | 示例值 | "张三" |
| `format` | 数据格式 | `"email"`, `"date"` |
| `minimum/maximum` | 数值范围 | `"0"`, `"100"` |

**文档展示**：
```
User模型结构：
{
  "id": 1001,           ← 用户ID
  "username": "张三",   ← 用户名 (必填)
  "age": 25,            ← 年龄 (0-150)
  "email": "..."        ← 邮箱 (email格式)
}
```

### 3.5 @ApiResponse - 响应说明


**作用**：描述接口的返回结果

```java
@PostMapping("/login")
@Operation(summary = "用户登录")
@ApiResponse(responseCode = "200", description = "登录成功")
@ApiResponse(responseCode = "401", description = "用户名或密码错误")
@ApiResponse(responseCode = "500", description = "服务器内部错误")
public Result login(@RequestBody LoginDTO loginDTO) {
    return userService.login(loginDTO);
}
```

**多状态码说明**：
```
响应状态说明：
✅ 200 - 登录成功 → 返回用户token
❌ 401 - 认证失败 → 用户名或密码错误
❌ 500 - 服务错误 → 系统异常，请联系管理员
```

---

## 4. 🎨 Swagger UI界面定制


### 4.1 自定义主题样式


**修改UI配置**：

```java
@Configuration
public class SwaggerConfig {
    
    @Bean
    public Docket docket() {
        return new Docket(DocumentationType.OAS_30)
                .apiInfo(apiInfo())
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.example"))
                .build()
                .globalRequestParameters(globalParams())  // 全局参数
                .globalResponses(HttpMethod.GET, responseMessages()); // 全局响应
    }
    
    private ApiInfo apiInfo() {
        return new ApiInfoBuilder()
                .title("📚 项目API文档")
                .description("## 接口说明\n" +
                           "- 所有接口都需要token认证\n" +
                           "- 请求头需携带：Authorization: Bearer {token}")
                .version("v1.0.0")
                .contact(new Contact("技术团队", "http://example.com", "dev@example.com"))
                .license("Apache 2.0")
                .licenseUrl("http://www.apache.org/licenses/LICENSE-2.0")
                .build();
    }
}
```

**页面效果**：
```
文档顶部展示：
┌──────────────────────────────────┐
│  📚 项目API文档                   │
│  版本：v1.0.0                     │
│  ────────────────────────         │
│  接口说明                          │
│  • 所有接口都需要token认证         │
│  • 请求头需携带：Authorization...  │
│  ────────────────────────         │
│  联系人：技术团队 (dev@...)        │
└──────────────────────────────────┘
```

### 4.2 全局参数配置


**添加通用Header**：

```java
private List<RequestParameter> globalParams() {
    return Collections.singletonList(
        RequestParameterBuilder()
            .name("Authorization")
            .description("认证令牌")
            .required(true)
            .in(ParameterType.HEADER)
            .query(q -> q.model(m -> m.scalarModel(ScalarType.STRING)))
            .build()
    );
}
```

> **💡 作用说明**  
> 配置后，所有接口测试时都会自动带上这个Header，不用每次手动填写

### 4.3 接口分组管理


**创建多个Docket实现分组**：

```java
@Configuration
public class SwaggerConfig {
    
    // 用户模块文档
    @Bean
    public Docket userApi() {
        return new Docket(DocumentationType.OAS_30)
                .groupName("用户模块")
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.example.user"))
                .build();
    }
    
    // 订单模块文档
    @Bean
    public Docket orderApi() {
        return new Docket(DocumentationType.OAS_30)
                .groupName("订单模块")
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.example.order"))
                .build();
    }
    
    // 系统管理文档
    @Bean
    public Docket systemApi() {
        return new Docket(DocumentationType.OAS_30)
                .groupName("系统管理")
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.example.system"))
                .build();
    }
}
```

**切换效果**：
```
右上角分组选择：
┌─────────────────┐
│ Select a spec   │
│ ┌─────────────┐ │
│ │ 用户模块  ▼ │ │  ← 下拉菜单
│ └─────────────┘ │
│                 │
│ • 用户模块      │
│ • 订单模块      │
│ • 系统管理      │
└─────────────────┘
```

---

## 5. ⚡ 高级功能应用


### 5.1 在线调试功能


**使用步骤**：

```
在线测试流程：
第1步：点击接口 → 展开详情
第2步：点击 "Try it out" 按钮
第3步：填写参数（或使用示例值）
第4步：点击 "Execute" 执行
第5步：查看响应结果

实际演示：
┌─────────────────────────────────────┐
│ POST /user/login                     │
│ [Try it out]  ← 点这里               │
├─────────────────────────────────────┤
│ Request body                         │
│ {                                    │
│   "username": "admin",  ← 自动填充   │
│   "password": "123456"               │
│ }                                    │
│ [Execute]  ← 点这里执行              │
├─────────────────────────────────────┤
│ Response                             │
│ Code: 200 ✅                         │
│ {                                    │
│   "code": 200,                       │
│   "msg": "登录成功",                 │
│   "data": { "token": "..." }         │
│ }                                    │
└─────────────────────────────────────┘
```

> **🎯 实用技巧**  
> - 接口调试不需要Postman了
> - 参数有`example`值会自动填充
> - 可以直接看到响应码和返回数据

### 5.2 文档导出功能


**导出API文档**：

```java
// 方法1：直接访问JSON格式
http://localhost:8080/v3/api-docs

// 方法2：导出为YAML格式  
http://localhost:8080/v3/api-docs.yaml
```

**用途场景**：
- 📤 **分享文档**：导出JSON给前端团队
- 📋 **版本存档**：每次发版保存一份文档
- 🔄 **导入工具**：导入到Postman、Apifox等工具

### 5.3 文档版本管理


**多版本文档配置**：

```java
@Configuration
public class SwaggerConfig {
    
    // V1版本API
    @Bean
    public Docket v1Api() {
        return new Docket(DocumentationType.OAS_30)
                .groupName("V1版本")
                .apiInfo(apiInfo("V1.0", "第一版API"))
                .select()
                .apis(RequestHandlerSelectors.withMethodAnnotation(ApiV1.class))
                .build();
    }
    
    // V2版本API
    @Bean
    public Docket v2Api() {
        return new Docket(DocumentationType.OAS_30)
                .groupName("V2版本")
                .apiInfo(apiInfo("V2.0", "第二版API，新增XXX功能"))
                .select()
                .apis(RequestHandlerSelectors.withMethodAnnotation(ApiV2.class))
                .build();
    }
}

// 自定义版本注解
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface ApiV1 {}

@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface ApiV2 {}
```

**使用版本注解**：

```java
@RestController
@RequestMapping("/user")
@Tag(name = "用户管理")
public class UserController {
    
    @ApiV1  // 标记为V1版本
    @GetMapping("/info")
    @Operation(summary = "获取用户信息(V1)")
    public User getUserV1() {
        return userService.getUser();
    }
    
    @ApiV2  // 标记为V2版本
    @GetMapping("/detail")
    @Operation(summary = "获取用户详情(V2)")
    public UserDetail getUserV2() {
        return userService.getUserDetail();
    }
}
```

### 5.4 生产环境安全配置


**关闭生产环境文档**：

```java
@Configuration
public class SwaggerConfig {
    
    @Value("${swagger.enabled:true}")
    private boolean swaggerEnabled;
    
    @Bean
    public Docket docket() {
        return new Docket(DocumentationType.OAS_30)
                .enable(swaggerEnabled)  // 通过配置控制开关
                .apiInfo(apiInfo())
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.example"))
                .build();
    }
}
```

**配置文件设置**：

```yaml
# application-dev.yml (开发环境)
swagger:
  enabled: true

# application-prod.yml (生产环境)
swagger:
  enabled: false
```

> **⚠️ 安全提醒**  
> 生产环境务必关闭Swagger！避免接口信息泄露

---

## 6. 📋 核心要点总结


### 6.1 知识点回顾


**🔸 核心概念**：
- **OpenAPI3**：接口描述标准规范
- **Swagger3**：实现OpenAPI3规范的工具
- **自动生成**：代码和文档同步，永不过时

**🔸 核心注解**：

| 注解 | 作用对象 | 核心功能 |
|------|----------|----------|
| `@Tag` | Controller | 接口分组分类 |
| `@Operation` | Method | 描述接口功能 |
| `@Parameter` | 参数 | 说明参数含义 |
| `@Schema` | 实体类/字段 | 描述模型结构 |
| `@ApiResponse` | Method | 说明响应状态 |

**🔸 核心功能**：
```
Swagger能做什么：
✅ 自动生成API文档
✅ 在线测试接口
✅ 分组管理接口
✅ 导出文档数据
✅ 版本控制管理
✅ UI界面定制
```

### 6.2 实践建议


> **💡 最佳实践**

**文档编写规范**：
```
✅ 推荐做法：
• 每个接口都要加@Operation说明
• 复杂参数必须用@Parameter解释
• 实体类字段都要用@Schema标注
• 提供合理的example示例值
• 多状态码用@ApiResponse说明

❌ 避免的错误：
• 不写任何注解，只靠方法名
• 注解信息太简单，没有实际说明
• 示例值随便写，不符合实际
• 生产环境忘记关闭Swagger
```

**学习路径建议**：
```
🎯 新手学习步骤：
第1步：快速入门 → 配置基础Swagger，能看到文档
第2步：掌握注解 → 学会使用@Operation等核心注解
第3步：界面定制 → 美化文档，添加全局配置
第4步：高级功能 → 分组管理、版本控制、在线测试
第5步：生产应用 → 环境隔离、安全配置
```

**常见问题解决**：

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 访问404 | 路径错误 | 检查是否`/swagger-ui/index.html` |
| 扫描不到接口 | 包路径配置错 | 检查`basePackage`配置 |
| 注解不生效 | 版本冲突 | 确保使用Swagger3注解 |
| 生产环境暴露 | 未关闭文档 | 配置`enabled=false` |

### 6.3 扩展学习


**🔗 关联知识**：
- **前置知识**：SpringBoot基础、RestController使用
- **配合使用**：统一结果封装、全局异常处理
- **替代方案**：Knife4j（Swagger增强版）、Apifox

**📚 进阶方向**：
- Knife4j集成（更美观的UI）
- 自定义文档模板
- 接口权限控制
- Mock数据生成

---

**🎯 核心记忆**：
```
Swagger = 自动API文档神器
• 写代码 = 写文档（注解即文档）
• 代码改 = 文档改（永远同步）
• 看文档 = 能测试（在线调试）
• 一次配置，终身受益！
```

> **💪 实践建议**  
> 看完笔记不算学会，动手配置一次，写几个带注解的接口，
> 看到自己的文档自动生成的那一刻，你就真正掌握了！