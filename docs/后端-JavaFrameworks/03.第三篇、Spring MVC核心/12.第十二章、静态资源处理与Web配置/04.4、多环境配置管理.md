---
title: 4、多环境配置管理
---
## 📚 目录

1. [多环境配置概述](#1-多环境配置概述)
2. [@Profile环境配置](#2-profile环境配置)
3. [@Value外部化配置](#3-value外部化配置)
4. [@ConfigurationProperties配置绑定](#4-configurationproperties配置绑定)
5. [环境切换策略](#5-环境切换策略)
6. [配置文件管理](#6-配置文件管理)
7. [属性覆盖机制](#7-属性覆盖机制)
8. [敏感信息加密](#8-敏感信息加密)
9. [配置热更新](#9-配置热更新)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🌍 多环境配置概述


### 1.1 什么是多环境配置


在实际项目开发中，我们通常会面对多个不同的运行环境：

```
项目生命周期的不同环境：
开发环境(dev)  → 测试环境(test)  → 预发布环境(pre)  → 生产环境(prod)
    ↓               ↓                ↓                  ↓
  开发调试         功能测试          上线验证           正式运行
  本地数据库       测试数据库        预发数据库         生产数据库
  调试日志         详细日志          关键日志           错误日志
```

**核心问题**：不同环境需要不同的配置，如何优雅地管理？

### 1.2 为什么需要多环境配置


**实际场景举例**：

```
数据库配置差异：
开发环境：localhost:3306/dev_db     (本地数据库)
测试环境：test-db.com:3306/test_db  (测试服务器)
生产环境：prod-db.com:3306/prod_db  (生产服务器)

日志级别差异：
开发环境：DEBUG 级别 (查看详细信息)
测试环境：INFO 级别  (记录重要信息)
生产环境：ERROR 级别 (只记录错误)

功能开关差异：
开发环境：所有新功能开启
测试环境：部分新功能开启
生产环境：稳定功能开启
```

> 💡 **核心理念**：一套代码，多个环境，配置分离，灵活切换

### 1.3 配置管理的演进


**传统方式的问题**：
```
❌ 硬编码配置：直接写在代码里，修改需要重新编译
❌ 手动替换文件：每次部署前手动替换配置文件
❌ 多套代码：不同环境维护不同代码分支
❌ 配置泄露：敏感信息直接暴露在代码中
```

**现代化方案**：
```
✅ 配置外部化：配置与代码分离
✅ 环境自动识别：根据环境自动加载对应配置
✅ 统一管理：集中管理所有环境配置
✅ 安全加密：敏感信息加密存储
```

---

## 2. 🏷️ @Profile环境配置


### 2.1 @Profile基本概念


**@Profile 是什么？**
- 它是 Spring 提供的环境标识注解
- 用来标记某个配置只在特定环境下生效
- 可以理解为"环境标签"或"环境开关"

### 2.2 @Profile 的使用方式


**方式一：标注在配置类上**

```java
// 开发环境的数据库配置
@Configuration
@Profile("dev")  // 只在dev环境生效
public class DevDatabaseConfig {
    
    @Bean
    public DataSource dataSource() {
        // 开发环境使用内嵌H2数据库
        return new EmbeddedDatabaseBuilder()
            .setType(EmbeddedDatabaseType.H2)
            .build();
    }
}

// 生产环境的数据库配置
@Configuration
@Profile("prod")  // 只在prod环境生效
public class ProdDatabaseConfig {
    
    @Bean
    public DataSource dataSource() {
        // 生产环境使用MySQL
        HikariDataSource dataSource = new HikariDataSource();
        dataSource.setJdbcUrl("jdbc:mysql://prod-db:3306/app");
        dataSource.setUsername("prod_user");
        dataSource.setPassword("prod_pass");
        return dataSource;
    }
}
```

**方式二：标注在Bean方法上**

```java
@Configuration
public class CacheConfig {
    
    @Bean
    @Profile("dev")
    public CacheManager devCacheManager() {
        // 开发环境使用简单缓存
        return new ConcurrentMapCacheManager("users", "products");
    }
    
    @Bean
    @Profile("prod")
    public CacheManager prodCacheManager() {
        // 生产环境使用Redis缓存
        RedisCacheManager cacheManager = RedisCacheManager
            .builder(redisConnectionFactory)
            .cacheDefaults(RedisCacheConfiguration.defaultCacheConfig())
            .build();
        return cacheManager;
    }
}
```

### 2.3 多环境组合与默认值


**组合多个环境**：
```java
@Configuration
@Profile({"dev", "test"})  // 在dev或test环境下都生效
public class NonProdConfig {
    // 开发和测试共用的配置
}
```

**排除特定环境**：
```java
@Configuration
@Profile("!prod")  // 除了prod环境外都生效
public class DebugConfig {
    // 非生产环境的调试配置
}
```

**设置默认环境**：
```java
@Configuration
@Profile("default")  // 当没有指定环境时生效
public class DefaultConfig {
    // 默认配置
}
```

### 2.4 实战案例：日志配置


```java
@Configuration
public class LogConfig {
    
    @Bean
    @Profile("dev")
    public LogLevel devLogLevel() {
        // 开发环境：详细日志
        return LogLevel.DEBUG;
    }
    
    @Bean
    @Profile("test")
    public LogLevel testLogLevel() {
        // 测试环境：普通日志
        return LogLevel.INFO;
    }
    
    @Bean
    @Profile("prod")
    public LogLevel prodLogLevel() {
        // 生产环境：只记录错误
        return LogLevel.ERROR;
    }
}
```

---

## 3. 💰 @Value外部化配置


### 3.1 @Value 基本用法


**@Value 是做什么的？**
- 从配置文件中读取属性值
- 注入到 Spring Bean 的字段中
- 支持默认值和表达式

**基础示例**：

```java
@Component
public class AppConfig {
    
    // 读取配置文件中的值
    @Value("${app.name}")
    private String appName;
    
    // 带默认值（如果配置不存在，使用默认值）
    @Value("${app.version:1.0.0}")
    private String appVersion;
    
    // 读取数字类型
    @Value("${server.port:8080}")
    private int serverPort;
    
    // 读取布尔值
    @Value("${feature.enabled:false}")
    private boolean featureEnabled;
}
```

**对应的配置文件**：
```properties
# application.properties
app.name=我的应用
app.version=2.0.0
server.port=9090
feature.enabled=true
```

### 3.2 SpEL 表达式支持


**Spring Expression Language（SpEL）** 让配置更灵活：

```java
@Component
public class ConfigExample {
    
    // 使用系统属性
    @Value("#{systemProperties['user.home']}")
    private String userHome;
    
    // 调用静态方法
    @Value("#{T(java.lang.Math).random()}")
    private double randomValue;
    
    // 三元表达式
    @Value("#{${app.debug:false} ? 'DEBUG' : 'RELEASE'}")
    private String mode;
    
    // 读取其他Bean的属性
    @Value("#{databaseConfig.url}")
    private String dbUrl;
}
```

### 3.3 集合类型注入


```java
@Component
public class ListConfig {
    
    // 注入数组
    @Value("${app.servers}")
    private String[] servers;
    
    // 注入List（用逗号分隔）
    @Value("#{'${app.emails}'.split(',')}")
    private List<String> emails;
    
    // 注入Map
    @Value("#{${app.settings}}")
    private Map<String, String> settings;
}
```

**配置文件**：
```properties
app.servers=server1,server2,server3
app.emails=admin@example.com,support@example.com
app.settings={'theme':'dark','lang':'zh-CN'}
```

### 3.4 多环境配置文件


**配置文件命名规范**：
```
application.properties         (公共配置)
application-dev.properties     (开发环境)
application-test.properties    (测试环境)
application-prod.properties    (生产环境)
```

**配置文件加载顺序**：
```
优先级从高到低：
1️⃣ 命令行参数
2️⃣ application-{profile}.properties (当前激活环境)
3️⃣ application.properties (公共配置)
```

**实际应用**：

```properties
# application.properties (公共配置)
app.name=电商平台
app.feature.search=true

# application-dev.properties (开发环境)
spring.datasource.url=jdbc:mysql://localhost:3306/dev_db
logging.level.root=DEBUG

# application-prod.properties (生产环境)
spring.datasource.url=jdbc:mysql://prod-db:3306/prod_db
logging.level.root=ERROR
```

---

## 4. 📦 @ConfigurationProperties配置绑定


### 4.1 为什么需要 @ConfigurationProperties


**@Value 的局限性**：
```java
// 使用@Value需要一个一个写
@Value("${db.host}")
private String host;
@Value("${db.port}")
private int port;
@Value("${db.username}")
private String username;
@Value("${db.password}")
private String password;
// ... 如果有20个配置项，要写20次！
```

**@ConfigurationProperties 的优势**：
```java
// 一次性绑定所有配置
@ConfigurationProperties(prefix = "db")
public class DatabaseProperties {
    private String host;
    private int port;
    private String username;
    private String password;
    // getter/setter
}
```

### 4.2 基本使用方式


**步骤1：创建配置属性类**

```java
@Component
@ConfigurationProperties(prefix = "app.mail")
public class MailProperties {
    
    private String host;           // 邮件服务器地址
    private int port;              // 端口号
    private String username;       // 用户名
    private String password;       // 密码
    private boolean enableSsl;     // 是否启用SSL
    
    // 必须提供 getter 和 setter
    public String getHost() { return host; }
    public void setHost(String host) { this.host = host; }
    // ... 其他getter/setter
}
```

**步骤2：配置文件**

```properties
# application.properties
app.mail.host=smtp.gmail.com
app.mail.port=587
app.mail.username=myapp@gmail.com
app.mail.password=secret123
app.mail.enable-ssl=true
```

**步骤3：使用配置**

```java
@Service
public class MailService {
    
    @Autowired
    private MailProperties mailProperties;
    
    public void sendEmail(String to, String subject, String content) {
        System.out.println("使用邮件服务器: " + mailProperties.getHost());
        System.out.println("端口: " + mailProperties.getPort());
        // 发送邮件逻辑...
    }
}
```

### 4.3 嵌套对象配置


**复杂配置结构**：

```java
@Component
@ConfigurationProperties(prefix = "app")
public class AppProperties {
    
    private String name;
    private String version;
    private Security security = new Security();  // 嵌套对象
    private List<Server> servers = new ArrayList<>();  // 列表
    
    // 嵌套配置类
    public static class Security {
        private boolean enabled;
        private String algorithm;
        // getter/setter
    }
    
    public static class Server {
        private String name;
        private String url;
        // getter/setter
    }
    
    // getter/setter
}
```

**配置文件（YAML格式更清晰）**：

```yaml
app:
  name: 电商系统
  version: 2.0.0
  security:
    enabled: true
    algorithm: SHA256
  servers:
    - name: server1
      url: http://192.168.1.100:8080
    - name: server2
      url: http://192.168.1.101:8080
```

### 4.4 配置验证


**添加校验注解**：

```java
@Component
@ConfigurationProperties(prefix = "app.user")
@Validated  // 启用验证
public class UserProperties {
    
    @NotBlank(message = "用户名不能为空")
    private String username;
    
    @Min(value = 18, message = "年龄不能小于18岁")
    @Max(value = 100, message = "年龄不能大于100岁")
    private int age;
    
    @Email(message = "邮箱格式不正确")
    private String email;
    
    @Pattern(regexp = "^1[3-9]\\d{9}$", message = "手机号格式不正确")
    private String phone;
    
    // getter/setter
}
```

> ⚠️ **注意**：需要添加 `spring-boot-starter-validation` 依赖才能使用校验功能

---

## 5. 🔄 环境切换策略


### 5.1 激活环境的方式


**方式一：配置文件激活**

```properties
# application.properties
spring.profiles.active=dev  # 激活开发环境
```

**方式二：命令行参数**

```bash
# 启动时指定环境
java -jar myapp.jar --spring.profiles.active=prod
```

**方式三：JVM系统属性**

```bash
java -Dspring.profiles.active=test -jar myapp.jar
```

**方式四：环境变量**

```bash
export SPRING_PROFILES_ACTIVE=prod
java -jar myapp.jar
```

### 5.2 多环境同时激活


```properties
# 同时激活多个环境
spring.profiles.active=prod,monitoring,logging
```

```
激活效果：
prod配置      ✅ 生效
monitoring配置 ✅ 生效
logging配置    ✅ 生效
dev配置        ❌ 不生效
```

### 5.3 环境切换最佳实践


**推荐方案**：

```
开发阶段：
  ↓
application.properties 设置 spring.profiles.active=dev
(开发人员本地调试)

打包部署：
  ↓
打包时不包含 spring.profiles.active 配置
(保持灵活性)

运行时指定：
  ↓
java -jar app.jar --spring.profiles.active=prod
(运维人员根据环境指定)
```

**实战示例 - Docker部署**：

```dockerfile
# Dockerfile
FROM openjdk:11-jre
COPY target/myapp.jar /app/app.jar
ENTRYPOINT ["java", "-jar", "/app/app.jar"]

# docker-compose.yml
services:
  app:
    image: myapp:latest
    environment:
      - SPRING_PROFILES_ACTIVE=prod  # 通过环境变量指定
```

---

## 6. 📁 配置文件管理


### 6.1 配置文件的组织结构


**推荐目录结构**：

```
src/main/resources/
├── application.properties          # 公共基础配置
├── application-dev.properties      # 开发环境
├── application-test.properties     # 测试环境
├── application-pre.properties      # 预发布环境
├── application-prod.properties     # 生产环境
└── config/
    ├── database.properties         # 数据库专项配置
    ├── redis.properties            # Redis专项配置
    └── mq.properties               # 消息队列专项配置
```

### 6.2 配置文件格式对比


| 格式 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **Properties** | `简单直观`<br>`兼容性好` | `不支持层级结构`<br>`重复前缀多` | 简单配置 |
| **YAML** | `层级清晰`<br>`易读易写` | `格式严格`<br>`空格敏感` | 复杂配置 |
| **JSON** | `结构化强`<br>`通用性好` | `不支持注释`<br>`冗余符号多` | API配置 |

**同一配置的不同格式对比**：

```properties
# application.properties
app.database.host=localhost
app.database.port=3306
app.database.username=root
app.database.password=123456
app.database.pool.min-size=5
app.database.pool.max-size=20
```

```yaml
# application.yml (推荐)
app:
  database:
    host: localhost
    port: 3306
    username: root
    password: 123456
    pool:
      min-size: 5
      max-size: 20
```

### 6.3 外部配置文件加载


**配置文件加载优先级（从高到低）**：

```
1️⃣ 命令行参数
   java -jar app.jar --server.port=9090

2️⃣ 操作系统环境变量
   export SERVER_PORT=9090

3️⃣ Java系统属性
   -Dserver.port=9090

4️⃣ 外部配置文件
   /config/application.properties (jar包同级config目录)

5️⃣ 内部配置文件
   classpath:/application.properties (resources目录)
```

**加载外部配置**：

```bash
# 方式1：指定配置文件位置
java -jar app.jar --spring.config.location=/opt/config/

# 方式2：指定配置文件名
java -jar app.jar --spring.config.name=myapp

# 方式3：额外添加配置文件
java -jar app.jar --spring.config.additional-location=/etc/config/
```

---

## 7. 🔝 属性覆盖机制


### 7.1 覆盖规则详解


**配置覆盖顺序**：

```
最终生效的配置 = 公共配置 + 环境配置 + 外部配置

示例说明：
application.properties (公共)
  ↓ 被覆盖
application-dev.properties (环境)
  ↓ 被覆盖
外部配置文件
  ↓ 被覆盖
命令行参数 (最高优先级)
```

### 7.2 实战案例演示


**场景**：配置数据库连接

```properties
# application.properties (公共配置)
spring.datasource.url=jdbc:mysql://localhost:3306/app_db
spring.datasource.username=root
spring.datasource.password=default_pass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
```

```properties
# application-prod.properties (生产环境配置)
spring.datasource.url=jdbc:mysql://prod-db:3306/app_db
spring.datasource.password=prod_password
# username 和 driver-class-name 会使用公共配置的值
```

**最终生效（prod环境）**：

```
✅ spring.datasource.url=jdbc:mysql://prod-db:3306/app_db  (prod覆盖)
✅ spring.datasource.username=root                         (公共配置)
✅ spring.datasource.password=prod_password                (prod覆盖)
✅ spring.datasource.driver-class-name=com.mysql...        (公共配置)
```

### 7.3 环境特定Bean覆盖


```java
@Configuration
public class CommonConfig {
    
    @Bean
    @ConditionalOnMissingBean  // 如果没有其他Bean，才创建这个
    public CacheManager defaultCacheManager() {
        return new ConcurrentMapCacheManager();
    }
}

@Configuration
@Profile("prod")
public class ProdConfig {
    
    @Bean
    public CacheManager redisCacheManager() {
        // 生产环境会优先使用这个Bean
        return new RedisCacheManager(...);
    }
}
```

---

## 8. 🔐 敏感信息加密


### 8.1 为什么要加密配置


**敏感信息泄露风险**：

```
❌ 明文存储的风险：
配置文件：spring.datasource.password=MyP@ssw0rd123
   ↓
代码提交到Git → 泄露
   ↓
日志打印配置 → 泄露
   ↓
配置文件外传 → 泄露
```

### 8.2 使用 Jasypt 加密（推荐方案）


**步骤1：添加依赖**

```xml
<dependency>
    <groupId>com.github.ulisesbocchio</groupId>
    <artifactId>jasypt-spring-boot-starter</artifactId>
    <version>3.0.5</version>
</dependency>
```

**步骤2：加密敏感信息**

```java
// 工具类：用于生成加密文本
public class PasswordEncryptor {
    public static void main(String[] args) {
        StandardPBEStringEncryptor encryptor = new StandardPBEStringEncryptor();
        encryptor.setPassword("my-secret-key");  // 加密密钥
        
        String encrypted = encryptor.encrypt("MyP@ssw0rd123");
        System.out.println("加密后: " + encrypted);
        // 输出: jL9X8kP3nF2sQ7...
    }
}
```

**步骤3：配置文件使用加密值**

```properties
# application.properties
jasypt.encryptor.password=my-secret-key  # 解密密钥

# 使用 ENC() 包裹加密后的值
spring.datasource.password=ENC(jL9X8kP3nF2sQ7...)
redis.password=ENC(aB3dF7gH9...)
```

**步骤4：安全存储密钥**

```bash
# 方式1：环境变量（推荐）
export JASYPT_ENCRYPTOR_PASSWORD=my-secret-key
java -jar app.jar

# 方式2：启动参数
java -jar app.jar --jasypt.encryptor.password=my-secret-key

# 方式3：系统属性
java -Djasypt.encryptor.password=my-secret-key -jar app.jar
```

### 8.3 自定义加密方案


```java
@Component
public class ConfigDecryptor implements EnvironmentPostProcessor {
    
    @Override
    public void postProcessEnvironment(ConfigurableEnvironment environment, 
                                       SpringApplication application) {
        // 自定义解密逻辑
        String encryptedValue = environment.getProperty("my.secret");
        if (encryptedValue != null && encryptedValue.startsWith("ENC(")) {
            String decrypted = decrypt(encryptedValue);
            // 替换为解密后的值
        }
    }
    
    private String decrypt(String encrypted) {
        // 实现自己的解密算法
        return "解密后的值";
    }
}
```

---

## 9. 🔥 配置热更新


### 9.1 什么是配置热更新


**传统方式的问题**：

```
配置修改 → 重启应用 → 服务中断 → 用户受影响

例如：修改限流阈值
1. 修改配置文件
2. 停止应用
3. 重新部署
4. 启动应用
总耗时：5-10分钟，期间服务不可用 ❌
```

**热更新的优势**：

```
配置修改 → 自动刷新 → 无需重启 → 用户无感知

修改限流阈值：
1. 修改配置中心的值
2. 应用自动感知并更新
总耗时：<1秒，服务持续可用 ✅
```

### 9.2 使用 @RefreshScope 实现热更新


**步骤1：添加依赖**

```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-bootstrap</artifactId>
</dependency>
```

**步骤2：标记需要刷新的Bean**

```java
@Component
@RefreshScope  // 关键注解：支持配置热更新
public class RateLimitConfig {
    
    @Value("${app.ratelimit.max-requests:100}")
    private int maxRequests;
    
    @Value("${app.ratelimit.time-window:60}")
    private int timeWindow;
    
    public boolean allowRequest() {
        System.out.println("当前限流配置: " + maxRequests + "次/" + timeWindow + "秒");
        // 限流逻辑
        return true;
    }
}
```

**步骤3：触发刷新**

```java
@RestController
public class RefreshController {
    
    @Autowired
    private ContextRefresher contextRefresher;
    
    @PostMapping("/refresh")
    public String refresh() {
        contextRefresher.refresh();  // 手动触发刷新
        return "配置已刷新";
    }
}
```

**步骤4：使用方式**

```bash
# 1. 修改配置文件
vim application.properties
app.ratelimit.max-requests=200  # 从100改为200

# 2. 调用刷新接口
curl -X POST http://localhost:8080/refresh

# 3. 配置立即生效，无需重启 ✅
```

### 9.3 配置中心方案（生产级）


**集成 Nacos 配置中心**：

```yaml
# bootstrap.yml
spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: dev
        group: DEFAULT_GROUP
        file-extension: yaml
        refresh-enabled: true  # 启用自动刷新
```

```java
@Component
@RefreshScope
public class BusinessConfig {
    
    @NacosValue(value = "${app.discount.rate:0.8}", autoRefreshed = true)
    private double discountRate;
    
    public double calculatePrice(double originalPrice) {
        return originalPrice * discountRate;  // 使用最新的折扣率
    }
}
```

**配置更新流程**：

```
Nacos控制台修改配置
         ↓
配置变更事件推送
         ↓
Spring应用接收事件
         ↓
@RefreshScope Bean重新加载
         ↓
新配置立即生效 ✅
(全程<1秒，应用无感知)
```

### 9.4 热更新注意事项


> ⚠️ **重要提示**：

**不适合热更新的配置**：
```
❌ 数据库连接池配置 (可能导致连接异常)
❌ 服务器端口号 (需要重启才能生效)
❌ 线程池核心参数 (可能导致任务丢失)
❌ Spring Bean的创建参数 (生命周期管理复杂)
```

**适合热更新的配置**：
```
✅ 业务规则参数 (折扣率、限流阈值)
✅ 功能开关 (新功能启用/禁用)
✅ 超时时间 (接口调用超时)
✅ 日志级别 (动态调整日志详细程度)
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


```
🔸 多环境配置：一套代码适配多个运行环境
🔸 @Profile：环境标识，控制Bean在特定环境生效
🔸 @Value：从配置文件读取单个属性值
🔸 @ConfigurationProperties：批量绑定配置到对象
🔸 环境切换：灵活指定激活的环境
🔸 配置覆盖：高优先级配置覆盖低优先级
🔸 敏感信息加密：保护密码等敏感数据
🔸 配置热更新：无需重启即可更新配置
```

### 10.2 关键理解要点


**🔹 配置管理的核心思想**
```
配置与代码分离：
  代码 → 处理业务逻辑，稳定不变
  配置 → 适配不同环境，灵活变化

环境自动适配：
  根据环境标识自动加载对应配置
  无需手动修改代码或配置文件
```

**🔹 选择合适的配置方式**

| 场景 | 推荐方式 | 原因 |
|------|---------|------|
| 单个简单值 | `@Value` | 简单直接 |
| 一组相关配置 | `@ConfigurationProperties` | 类型安全、易维护 |
| 环境差异配置 | `@Profile` | 清晰的环境隔离 |
| 敏感信息 | `加密存储` | 安全性 |
| 频繁变化的值 | `配置中心+热更新` | 灵活性 |

**🔹 配置文件命名规范**
```
application.properties              # 所有环境公共配置
application-{profile}.properties   # 特定环境配置

示例：
application-dev.properties   → 开发环境
application-test.properties  → 测试环境
application-prod.properties  → 生产环境
```

### 10.3 实际应用指导


**🎯 开发阶段**
```
本地开发：
1. 在application.properties设置 spring.profiles.active=dev
2. 使用内嵌数据库(H2)或本地MySQL
3. 开启DEBUG日志查看详细信息
4. 不加密配置，方便调试
```

**🚀 测试阶段**
```
测试环境：
1. 打包时不指定profile
2. 部署时通过环境变量激活test环境
3. 连接测试数据库
4. 使用INFO级别日志
```

**🏭 生产阶段**
```
生产环境：
1. 启动时指定: --spring.profiles.active=prod
2. 敏感信息全部加密
3. 只记录ERROR级别日志
4. 连接配置中心实现动态配置
5. 关键配置支持热更新
```

### 10.4 最佳实践清单


**✅ 配置管理规范**
```
1. 公共配置放在 application.properties
2. 环境差异配置放在 application-{profile}.properties
3. 敏感信息必须加密，密钥不能提交代码库
4. 配置文件使用YAML格式提高可读性
5. 为配置添加注释说明用途
```

**✅ 安全建议**
```
1. 生产环境密钥使用环境变量传递
2. 定期更换加密密钥
3. 不同环境使用不同的数据库账号
4. 日志中屏蔽敏感信息
```

**✅ 部署建议**
```
1. 使用配置中心统一管理配置
2. 配置变更记录审计日志
3. 重要配置修改需要审批流程
4. 配置回滚机制
```

---

## 📚 扩展学习


**🔗 相关主题**
- Spring Boot 自动配置原理
- Spring Cloud Config 配置中心
- Nacos 配置管理实战
- Apollo 配置中心对比

**🎓 进阶方向**
- 配置加密算法深入
- 分布式配置同步机制
- 配置版本管理
- 灰度发布配置策略

**💡 实战项目建议**
- 搭建完整的多环境配置体系
- 集成配置中心实现动态配置
- 实现配置变更的全链路追踪
- 构建配置管理的可视化平台