---
title: 2、请求体数据处理机制
---
## 📚 目录

1. [请求体数据基础概念](#1-请求体数据基础概念)
2. [@RequestBody核心用法](#2-RequestBody核心用法)
3. [JSON数据绑定详解](#3-JSON数据绑定详解)
4. [XML数据绑定处理](#4-XML数据绑定处理)
5. [HttpMessageConverter机制](#5-HttpMessageConverter机制)
6. [Content-Type处理规则](#6-Content-Type处理规则)
7. [序列化与反序列化](#7-序列化与反序列化)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 请求体数据基础概念


### 1.1 什么是请求体数据


**通俗理解**：想象你在网上购物填写订单信息，这些详细的商品、地址、支付信息不能简单地放在网址里（太长、不安全），而是"打包"放在请求的"包裹"里发送给服务器，这个"包裹"就是**请求体（Request Body）**。

```
🔸 URL参数方式（GET请求）：
http://shop.com/order?name=手机&price=3999&count=1
问题：信息暴露在地址栏，长度有限制，不适合复杂数据

🔸 请求体方式（POST请求）：
POST http://shop.com/order
请求体内容：
{
  "name": "手机",
  "price": 3999,
  "count": 1,
  "address": {
    "province": "北京",
    "city": "朝阳区"
  }
}
优势：数据隐藏、支持复杂结构、无长度限制
```

### 1.2 请求体 vs URL参数对比


| 对比项 | **URL参数（Query String）** | **请求体（Request Body）** |
|--------|---------------------------|--------------------------|
| 📍 **位置** | 在URL地址后面 `?key=value` | 在HTTP请求的消息体中 |
| 📊 **数据量** | 有长度限制（通常2KB） | 几乎无限制 |
| 🔐 **安全性** | 地址栏可见，不安全 | 不在地址栏显示，相对安全 |
| 🏗️ **结构** | 简单键值对 | 支持复杂嵌套结构 |
| 📝 **格式** | `key1=value1&key2=value2` | JSON、XML等多种格式 |
| 🎯 **适用场景** | 简单查询、搜索条件 | 创建、更新复杂数据 |

### 1.3 常见的请求体格式


**实际应用场景分析**：

```
场景1：用户注册（JSON格式）
POST /api/register
Content-Type: application/json

{
  "username": "zhangsan",
  "password": "123456",
  "email": "zhangsan@example.com"
}

场景2：表单提交（Form格式）
POST /api/login
Content-Type: application/x-www-form-urlencoded

username=zhangsan&password=123456

场景3：文件上传（Multipart格式）
POST /api/upload
Content-Type: multipart/form-data

------WebKitFormBoundary
Content-Disposition: form-data; name="file"; filename="photo.jpg"
...文件二进制数据...
```

---

## 2. 📮 @RequestBody核心用法


### 2.1 @RequestBody是什么


**核心作用**：`@RequestBody` 就像一个"智能快递员"，它会自动把请求体中的数据"拆包"，转换成Java对象交给你使用。

```
请求流程图解：

客户端                        Spring MVC                    你的代码
  |                              |                            |
  |--[发送JSON数据]------------->|                            |
  |  {"name":"张三","age":25}    |                            |
  |                              |--[@RequestBody解析]------->|
  |                              |  转换成User对象            |
  |                              |                            |接收User对象
  |                              |                            |user.getName() → "张三"
```

### 2.2 基本使用方法


**步骤详解**：

```java
// 1️⃣ 首先定义一个接收数据的Java类（普通的JavaBean）
public class User {
    private String name;
    private Integer age;
    private String email;
    
    // getter和setter方法（必须有，用于数据填充）
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
    // ... 其他getter/setter
}

// 2️⃣ 在Controller中使用@RequestBody接收
@RestController
@RequestMapping("/api/user")
public class UserController {
    
    @PostMapping("/register")
    public String register(@RequestBody User user) {
        // 此时user对象已经自动填充好数据了
        System.out.println("姓名：" + user.getName());
        System.out.println("年龄：" + user.getAge());
        return "注册成功";
    }
}
```

**前端发送请求示例**：

```javascript
// 前端Ajax发送JSON数据
fetch('/api/user/register', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'  // 告诉服务器：我发的是JSON
    },
    body: JSON.stringify({
        name: '张三',
        age: 25,
        email: 'zhangsan@example.com'
    })
})
```

### 2.3 处理复杂嵌套对象


**实际场景**：用户下单，订单包含商品列表和收货地址

```java
// 地址类
public class Address {
    private String province;
    private String city;
    private String detail;
    // getter/setter...
}

// 商品类
public class Product {
    private String name;
    private Integer quantity;
    private Double price;
    // getter/setter...
}

// 订单类（包含复杂嵌套）
public class Order {
    private String orderNo;
    private List<Product> products;  // 商品列表
    private Address address;         // 收货地址
    // getter/setter...
}

// Controller处理
@PostMapping("/order/create")
public String createOrder(@RequestBody Order order) {
    // Spring会自动处理嵌套结构
    System.out.println("订单号：" + order.getOrderNo());
    System.out.println("商品数量：" + order.getProducts().size());
    System.out.println("收货城市：" + order.getAddress().getCity());
    return "订单创建成功";
}
```

**对应的JSON请求体**：

```json
{
  "orderNo": "20250924001",
  "products": [
    {
      "name": "手机",
      "quantity": 1,
      "price": 3999.0
    },
    {
      "name": "耳机",
      "quantity": 2,
      "price": 299.0
    }
  ],
  "address": {
    "province": "北京市",
    "city": "朝阳区",
    "detail": "某某街道123号"
  }
}
```

### 2.4 常见问题与解决


**问题1：接收到的对象属性全是null**

```java
// ❌ 错误原因：User类没有提供setter方法
public class User {
    private String name;
    // 缺少 setName() 方法
}

// ✅ 正确做法：提供完整的getter/setter
public class User {
    private String name;
    
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
}
```

**问题2：日期格式转换失败**

```java
public class User {
    private String name;
    
    // 🔸 指定日期格式
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private Date birthday;
    
    // getter/setter...
}

// 前端发送：
// {"name": "张三", "birthday": "2000-01-01 10:30:00"}
```

---

## 3. 🔄 JSON数据绑定详解


### 3.1 JSON绑定的工作原理


**通俗比喻**：JSON绑定就像翻译工作，把外国语言（JSON格式）翻译成我们能理解的本地语言（Java对象）。

```
JSON → Java 转换过程：

1️⃣ 客户端发送JSON字符串：
   '{"name":"张三","age":25}'

2️⃣ Spring接收到字符串

3️⃣ Jackson（默认翻译官）开始工作：
   - 读取JSON字符串
   - 解析出 name="张三", age=25
   - 创建User对象
   - 调用 user.setName("张三")
   - 调用 user.setAge(25)

4️⃣ 返回填充好的User对象给你的方法
```

### 3.2 JSON属性映射规则


**核心规则**：JSON的key名要和Java类的属性名对应

```java
// Java类定义
public class User {
    private String username;  // 属性名
    private Integer userAge;
    // getter/setter...
}

// ✅ 正确的JSON（key名匹配）
{
  "username": "张三",
  "userAge": 25
}

// ❌ 错误的JSON（key名不匹配）
{
  "name": "张三",      // 没有name属性，数据丢失
  "age": 25           // 没有age属性，数据丢失
}

// 🔧 解决方案：使用@JsonProperty指定映射
public class User {
    @JsonProperty("name")        // JSON中的key是name
    private String username;     // Java中的属性是username
    
    @JsonProperty("age")
    private Integer userAge;
}
```

### 3.3 处理特殊数据类型


**常见场景处理**：

```java
public class Product {
    private String name;
    
    // 🔸 数字类型（自动转换）
    private Integer stock;        // JSON: 100
    private Double price;         // JSON: 99.99
    
    // 🔸 布尔类型
    private Boolean onSale;       // JSON: true 或 false
    
    // 🔸 数组/集合类型
    private List<String> tags;    // JSON: ["热卖", "新品"]
    
    // 🔸 枚举类型
    private Status status;        // JSON: "ACTIVE"
    
    // 🔸 Map类型
    private Map<String, Object> extra;  // JSON: {"color":"红色"}
    
    // getter/setter...
}

// 对应的JSON示例
{
  "name": "手机",
  "stock": 100,
  "price": 3999.99,
  "onSale": true,
  "tags": ["热卖", "新品"],
  "status": "ACTIVE",
  "extra": {
    "color": "红色",
    "brand": "华为"
  }
}
```

### 3.4 忽略多余字段


**实际需求**：前端发来的JSON字段比Java类多，怎么办？

```java
// 🔸 默认情况：多余字段会被忽略（不报错）
public class User {
    private String name;
    private Integer age;
    // 只有这两个属性
}

// 前端发送的JSON（有额外字段）
{
  "name": "张三",
  "age": 25,
  "address": "北京",    // 多余字段，自动忽略
  "phone": "13800138000"  // 多余字段，自动忽略
}
// 结果：只有name和age被填充，其他字段被忽略

// 🔸 如果需要严格模式（有多余字段就报错）
@JsonIgnoreProperties(ignoreUnknown = false)  // 不忽略未知字段
public class User {
    private String name;
    private Integer age;
}
```

---

## 4. 📄 XML数据绑定处理


### 4.1 XML数据绑定概述


**使用场景**：虽然现在JSON更流行，但一些老系统、企业应用还在用XML格式传输数据。

```
对比理解：

JSON格式（现代、简洁）：
{
  "name": "张三",
  "age": 25
}

XML格式（传统、详细）：
<user>
  <name>张三</name>
  <age>25</age>
</user>

两者本质相同，只是表现形式不同
```

### 4.2 XML绑定配置


**步骤1：添加依赖**

```xml
<!-- pom.xml中添加XML处理依赖 -->
<dependency>
    <groupId>com.fasterxml.jackson.dataformat</groupId>
    <artifactId>jackson-dataformat-xml</artifactId>
</dependency>
```

**步骤2：Java类配置**

```java
// 使用JAXB注解标记XML映射
@XmlRootElement(name = "user")  // 根元素名称
public class User {
    
    @XmlElement(name = "name")  // XML元素名称
    private String name;
    
    @XmlElement(name = "age")
    private Integer age;
    
    // getter/setter...
}
```

**步骤3：Controller接收**

```java
@PostMapping(value = "/user/create", 
             consumes = "application/xml")  // 声明接收XML
public String createUser(@RequestBody User user) {
    System.out.println("姓名：" + user.getName());
    return "创建成功";
}
```

### 4.3 XML vs JSON选择建议


| 场景 | **推荐格式** | **原因** |
|------|------------|---------|
| 🌐 **现代Web应用** | JSON | 简洁、前端原生支持 |
| 🏢 **企业系统对接** | XML | 严格规范、自描述能力强 |
| 📱 **移动应用** | JSON | 数据量小、解析快 |
| 🔧 **配置文件** | XML | 层次清晰、支持Schema验证 |

---

## 5. 🔧 HttpMessageConverter机制


### 5.1 消息转换器是什么


**通俗比喻**：`HttpMessageConverter` 就像一个"万能翻译机"，它能把各种格式的数据（JSON、XML、文本等）翻译成Java对象，也能把Java对象翻译回各种格式。

```
工作流程示意：

请求阶段（Read）：
客户端 → [JSON数据] → HttpMessageConverter → [Java对象] → Controller

响应阶段（Write）：
Controller → [Java对象] → HttpMessageConverter → [JSON数据] → 客户端
```

### 5.2 Spring内置的转换器


**常用转换器清单**：

```
📋 Spring MVC默认提供的转换器：

1️⃣ MappingJackson2HttpMessageConverter
   作用：处理JSON格式
   支持类型：application/json
   
2️⃣ Jaxb2RootElementHttpMessageConverter
   作用：处理XML格式
   支持类型：application/xml
   
3️⃣ StringHttpMessageConverter
   作用：处理纯文本
   支持类型：text/plain
   
4️⃣ FormHttpMessageConverter
   作用：处理表单数据
   支持类型：application/x-www-form-urlencoded
```

### 5.3 转换器工作原理


**核心流程解析**：

```
请求处理流程：

1️⃣ Spring接收到HTTP请求

2️⃣ 检查Content-Type头
   例如：Content-Type: application/json

3️⃣ 遍历所有MessageConverter，找到支持该类型的转换器
   发现：MappingJackson2HttpMessageConverter支持

4️⃣ 调用转换器的read方法
   converter.read(User.class, request)

5️⃣ 转换器读取请求体，转换成User对象

6️⃣ 把User对象传递给Controller方法
```

**响应处理流程**：

```
1️⃣ Controller方法返回User对象

2️⃣ Spring检查请求的Accept头（客户端期望的格式）
   例如：Accept: application/json

3️⃣ 选择支持该格式的转换器

4️⃣ 调用转换器的write方法
   converter.write(user, MediaType.JSON, response)

5️⃣ 转换器把User对象转成JSON字符串

6️⃣ 写入响应体返回给客户端
```

### 5.4 自定义消息转换器


**实际需求**：假设你想支持YAML格式数据

```java
// 1️⃣ 创建自定义转换器
public class YamlHttpMessageConverter 
    extends AbstractHttpMessageConverter<Object> {
    
    // 声明支持的媒体类型
    public YamlHttpMessageConverter() {
        super(new MediaType("application", "yaml"));
    }
    
    // 判断是否支持该类型
    @Override
    protected boolean supports(Class<?> clazz) {
        return true;  // 支持所有类型
    }
    
    // 读取请求体（YAML → Java对象）
    @Override
    protected Object readInternal(Class<?> clazz, 
                                   HttpInputMessage message) {
        // YAML解析逻辑...
        return yamlObject;
    }
    
    // 写入响应体（Java对象 → YAML）
    @Override
    protected void writeInternal(Object object, 
                                  HttpOutputMessage message) {
        // YAML生成逻辑...
    }
}

// 2️⃣ 注册自定义转换器
@Configuration
public class WebConfig implements WebMvcConfigurer {
    
    @Override
    public void configureMessageConverters(
        List<HttpMessageConverter<?>> converters) {
        
        // 添加自定义YAML转换器
        converters.add(new YamlHttpMessageConverter());
    }
}
```

---

## 6. 📡 Content-Type处理规则


### 6.1 Content-Type是什么


**本质理解**：`Content-Type` 就像快递包裹上的标签，告诉接收方"这个包裹里装的是什么类型的东西"。

```
HTTP请求示例：

POST /api/user/create HTTP/1.1
Host: example.com
Content-Type: application/json    ← 这行告诉服务器：我发的是JSON

{"name":"张三","age":25}           ← 实际数据
```

### 6.2 常见Content-Type类型


| Content-Type | **说明** | **数据格式示例** | **使用场景** |
|--------------|---------|----------------|-------------|
| `application/json` | JSON格式 | `{"key":"value"}` | 现代API通信 |
| `application/xml` | XML格式 | `<user><name>张三</name></user>` | 企业系统 |
| `text/plain` | 纯文本 | `Hello World` | 简单文本 |
| `application/x-www-form-urlencoded` | 表单格式 | `name=张三&age=25` | 传统表单 |
| `multipart/form-data` | 多部分数据 | 二进制+文本混合 | 文件上传 |

### 6.3 Content-Type匹配机制


**工作原理**：Spring根据Content-Type自动选择合适的转换器

```java
// 🔸 明确指定接收的Content-Type
@PostMapping(value = "/user/create", 
             consumes = "application/json")  // 只接受JSON
public String createUser(@RequestBody User user) {
    return "成功";
}

// 🔸 支持多种Content-Type
@PostMapping(value = "/user/create",
             consumes = {"application/json", 
                        "application/xml"})  // 同时支持JSON和XML
public String createUser(@RequestBody User user) {
    return "成功";
}

// 🔸 不指定（接受所有类型）
@PostMapping("/user/create")  // 接受任何Content-Type
public String createUser(@RequestBody User user) {
    return "成功";
}
```

### 6.4 Content-Type不匹配问题


**常见错误场景**：

```
❌ 错误1：前端发的是JSON，但没设置Content-Type
请求：
POST /api/user/create
（缺少Content-Type头）
{"name":"张三"}

错误信息：
415 Unsupported Media Type

✅ 解决：前端必须设置正确的Content-Type
fetch('/api/user/create', {
    headers: {
        'Content-Type': 'application/json'  // 添加这行
    },
    body: JSON.stringify({name: '张三'})
})
```

```
❌ 错误2：Controller限制了类型，但前端发错了
Controller：
@PostMapping(consumes = "application/json")  // 只接受JSON

前端发送：
Content-Type: application/xml  // 发的是XML

错误信息：
415 Unsupported Media Type

✅ 解决：保持一致
- 要么前端改成JSON
- 要么Controller增加XML支持
```

---

## 7. 🔄 序列化与反序列化


### 7.1 核心概念理解


**通俗解释**：

```
📦 序列化（Serialization）
= 把Java对象"打包"成字节流或文本（如JSON）的过程
= 对象 → 文本

📭 反序列化（Deserialization）
= 把字节流或文本"拆包"还原成Java对象的过程
= 文本 → 对象
```

**生活比喻**：

```
序列化就像把家具打包：
Java对象（沙发） → 序列化 → JSON字符串（打包后的沙发）

反序列化就像拆包组装：
JSON字符串（包裹） → 反序列化 → Java对象（组装好的沙发）
```

### 7.2 序列化过程详解


**实际执行流程**：

```java
// Java对象
User user = new User();
user.setName("张三");
user.setAge(25);

// ⬇️ 序列化过程（Jackson库执行）

// 1️⃣ Jackson扫描User对象的所有getter方法
user.getName() → "张三"
user.getAge()  → 25

// 2️⃣ 构建JSON结构
{
  "name": "张三",    ← 来自getName()
  "age": 25          ← 来自getAge()
}

// 3️⃣ 转换成字符串
String json = "{\"name\":\"张三\",\"age\":25}"

// 4️⃣ 写入HTTP响应体
```

**注意事项**：

```java
public class User {
    private String name;
    private String password;  // 敏感信息
    
    // 🔸 控制哪些字段参与序列化
    @JsonIgnore  // 序列化时忽略此字段
    public String getPassword() {
        return password;
    }
    
    // name会正常序列化
    public String getName() {
        return name;
    }
}

// 序列化结果：
// {"name":"张三"}  ← password字段被忽略了
```

### 7.3 反序列化过程详解


**执行流程**：

```java
// 1️⃣ Spring接收到JSON字符串
String json = "{\"name\":\"张三\",\"age\":25}";

// 2️⃣ Jackson解析JSON
Map<String, Object> map = parse(json);
// 结果：{"name"="张三", "age"=25}

// 3️⃣ 创建目标对象
User user = new User();

// 4️⃣ 调用setter方法填充数据
user.setName("张三");   ← 对应JSON中的name
user.setAge(25);        ← 对应JSON中的age

// 5️⃣ 返回填充好的对象
return user;
```

**常见问题处理**：

```java
// ❌ 问题：JSON字段类型和Java属性类型不匹配
JSON: {"age": "25"}        // 字符串类型
Java: private Integer age; // 整数类型

// ✅ Jackson会自动尝试类型转换
"25" → 25 (成功)
"abc" → 转换失败，抛出异常

// 🔸 自定义转换失败处理
public class User {
    private Integer age;
    
    @JsonSetter("age")
    public void setAge(String ageStr) {
        try {
            this.age = Integer.parseInt(ageStr);
        } catch (Exception e) {
            this.age = 0;  // 转换失败给默认值
        }
    }
}
```

### 7.4 序列化配置优化


**实用配置示例**：

```java
@Configuration
public class JacksonConfig {
    
    @Bean
    public ObjectMapper objectMapper() {
        ObjectMapper mapper = new ObjectMapper();
        
        // 🔸 日期格式化
        mapper.setDateFormat(
            new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
        );
        
        // 🔸 null值处理
        mapper.setSerializationInclusion(
            JsonInclude.Include.NON_NULL  // null值不序列化
        );
        
        // 🔸 忽略未知字段（反序列化时）
        mapper.configure(
            DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, 
            false
        );
        
        // 🔸 美化输出（开发环境）
        mapper.enable(SerializationFeature.INDENT_OUTPUT);
        
        return mapper;
    }
}
```

**配置效果对比**：

```java
// 未配置前：
{"name":"张三","age":25,"address":null,"createTime":1695542400000}

// 配置后：
{
  "name": "张三",
  "age": 25,
  "createTime": "2024-09-24 10:30:00"
}
// address为null被过滤，日期格式化，缩进美化
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 请求体数据：POST请求中携带的复杂数据，支持JSON/XML等格式
🔸 @RequestBody：自动将请求体数据转换为Java对象的注解
🔸 HttpMessageConverter：负责数据格式转换的核心组件
🔸 Content-Type：声明数据格式类型的HTTP头
🔸 序列化：Java对象 → JSON/XML文本
🔸 反序列化：JSON/XML文本 → Java对象
```

### 8.2 关键理解要点


**🔹 数据流转全过程**

```
完整请求流程：

客户端                                               服务端
  |                                                   |
  |--[1]构造JSON数据--------------------------------->|
  |   {"name":"张三","age":25}                        |
  |                                                   |
  |--[2]设置Content-Type:application/json----------->|
  |                                                   |
  |                                    [3]Spring检查Content-Type
  |                                    [4]选择JSON转换器
  |                                    [5]读取请求体
  |                                    [6]反序列化成User对象
  |                                                   |
  |                        [7]Controller方法接收User对象
  |                        [8]业务处理
  |                        [9]返回结果对象
  |                                                   |
  |                                    [10]序列化成JSON
  |                                    [11]设置响应头
  |<--[12]返回JSON响应-------------------------------|
  |   {"code":200,"msg":"成功"}                       |
```

**🔹 核心注解使用对比**

| 注解 | **作用对象** | **数据来源** | **使用场景** |
|------|------------|-------------|-------------|
| `@RequestParam` | 方法参数 | URL参数 | 简单查询条件 |
| `@PathVariable` | 方法参数 | URL路径 | RESTful风格 |
| `@RequestBody` | 方法参数 | 请求体 | 复杂对象提交 |
| `@ResponseBody` | 方法/类 | 返回值 | 返回JSON数据 |

**🔹 常见问题快速诊断**

```
问题症状                    可能原因                     解决方案
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
接收对象属性全null          缺少setter方法               添加setter
                          JSON字段名不匹配              使用@JsonProperty
                          
415错误                    Content-Type不匹配           检查请求头设置
                          未添加JSON依赖                添加jackson依赖
                          
400错误                    JSON格式错误                 验证JSON语法
                          数据类型不匹配                检查字段类型
                          
日期格式错误                默认格式不符                 配置日期格式化
```

### 8.3 实战最佳实践


**✅ 推荐做法**：

```java
// 1️⃣ 统一响应格式
public class Result<T> {
    private Integer code;
    private String message;
    private T data;
    // getter/setter...
}

@PostMapping("/user/create")
public Result<User> createUser(@RequestBody User user) {
    // 业务处理...
    return Result.success(user);
}

// 2️⃣ 参数校验
public class User {
    @NotBlank(message = "姓名不能为空")
    private String name;
    
    @Min(value = 1, message = "年龄必须大于0")
    private Integer age;
    // ...
}

@PostMapping("/user/create")
public Result createUser(
    @RequestBody @Validated User user) {  // 添加@Validated
    // ...
}

// 3️⃣ 异常统一处理
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(HttpMessageNotReadableException.class)
    public Result handleJsonError(HttpMessageNotReadableException e) {
        return Result.error("JSON格式错误：" + e.getMessage());
    }
}
```

**📌 记忆口诀**：

```
请求体数据不简单，@RequestBody来帮忙
JSON格式最常用，Content-Type要对上
转换器在背后，序列化双向忙
getter和setter，一个都不能少
异常要处理，响应格式要统一
```

**🎯 学习检验清单**：

- [ ] 能说清楚请求体和URL参数的区别
- [ ] 理解@RequestBody的作用和使用方法
- [ ] 知道HttpMessageConverter的工作机制
- [ ] 掌握JSON数据绑定的基本规则
- [ ] 了解序列化和反序列化的概念
- [ ] 能处理常见的数据绑定问题
- [ ] 会配置Jackson的常用选项