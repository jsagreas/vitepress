---
title: 1、视图解析器原理与配置
---
## 📚 目录

1. [视图解析器是什么](#1-视图解析器是什么)
2. [ViewResolver接口核心机制](#2-ViewResolver接口核心机制)
3. [InternalResourceViewResolver详解](#3-InternalResourceViewResolver详解)
4. [视图名称解析原理](#4-视图名称解析原理)
5. [视图链式解析机制](#5-视图链式解析机制)
6. [视图缓存优化](#6-视图缓存优化)
7. [前缀后缀配置技巧](#7-前缀后缀配置技巧)
8. [多视图解析器配置](#8-多视图解析器配置)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🎯 视图解析器是什么


### 1.1 通俗理解视图解析器

💡 **生活化类比**：视图解析器就像"邮递员"

```
想象你寄信的场景：
你写信 → 写上收件人地址 → 邮递员根据地址送信

Spring MVC处理请求：
Controller返回视图名 → 视图解析器查找具体页面 → 返回页面给用户

核心作用：
- 把逻辑视图名称转换成实际的页面文件
- 就像把"home"变成"/WEB-INF/views/home.jsp"
```

### 1.2 为什么需要视图解析器

🤔 **解决的核心问题**

```
没有视图解析器的困扰：
Controller每次都要写完整路径
return "/WEB-INF/views/user/list.jsp";  ← 太繁琐了

有了视图解析器的便利：
return "user/list";  ← 简洁清晰

好处总结：
✅ 代码更简洁：只写逻辑名称
✅ 易于维护：统一管理视图路径
✅ 灵活切换：改配置就能换视图技术
```

### 1.3 视图解析的工作流程

📋 **完整处理流程图**

```
用户请求流程：
浏览器发送请求
      ↓
DispatcherServlet接收
      ↓
找到对应的Controller
      ↓
Controller处理业务逻辑
      ↓
返回逻辑视图名（如"home"）
      ↓
【视图解析器登场】
      ↓
解析成具体页面路径
      ↓
渲染页面返回给用户

举个例子：
Controller: return "user/profile"
解析器处理: /WEB-INF/views/ + user/profile + .jsp
最终结果: /WEB-INF/views/user/profile.jsp
```

---

## 2. 🔍 ViewResolver接口核心机制


### 2.1 ViewResolver接口的本质

🎯 **接口设计思想**

ViewResolver接口定义了一个核心方法：
- **作用**：把视图名称转换成View对象
- **输入**：逻辑视图名称（String）
- **输出**：View对象（真正的视图）

```
接口方法解读：
View resolveViewName(String viewName, Locale locale)

参数说明：
viewName - Controller返回的视图名称
locale   - 国际化区域信息（支持多语言）

返回值：
View对象 - 包含了如何渲染页面的逻辑
```

### 2.2 View对象的作用

💡 **View接口的职责**

```
View对象做什么？
1. 确定响应的内容类型（HTML、JSON、PDF等）
2. 准备数据模型用于渲染
3. 真正执行页面渲染工作

常见View实现类：
- JstlView        → JSP页面渲染
- FreeMarkerView  → FreeMarker模板
- ThymeleafView   → Thymeleaf模板
- MappingJackson2JsonView → JSON数据
```

### 2.3 Spring提供的内置视图解析器

📊 **常用视图解析器对比**

| 视图解析器类型 | **作用说明** | **适用场景** |
|--------------|------------|-------------|
| **InternalResourceViewResolver** | `JSP页面解析` | `传统Web项目` |
| **FreeMarkerViewResolver** | `FreeMarker模板` | `模板引擎项目` |
| **ThymeleafViewResolver** | `Thymeleaf模板` | `现代Spring Boot` |
| **BeanNameViewResolver** | `根据Bean名称查找` | `自定义View` |
| **XmlViewResolver** | `XML配置视图` | `复杂视图场景` |
| **ContentNegotiatingViewResolver** | `内容协商视图` | `多格式响应` |

---

## 3. 📄 InternalResourceViewResolver详解


### 3.1 什么是InternalResourceViewResolver

🎯 **最常用的JSP视图解析器**

```
简单理解：
这是专门处理JSP页面的视图解析器
"InternalResource" = 内部资源（服务器内的JSP文件）

核心特点：
1. 专为JSP设计
2. 支持JSTL标签库
3. 配置简单易用
4. Spring MVC默认推荐
```

### 3.2 基础配置方式

🔧 **Java配置方式（推荐）**

```java
@Configuration
@EnableWebMvc
public class WebConfig implements WebMvcConfigurer {
    
    @Bean
    public InternalResourceViewResolver viewResolver() {
        InternalResourceViewResolver resolver = new InternalResourceViewResolver();
        
        // 设置前缀：视图文件存放的目录
        resolver.setPrefix("/WEB-INF/views/");
        
        // 设置后缀：视图文件的扩展名
        resolver.setSuffix(".jsp");
        
        // 设置视图类（支持JSTL）
        resolver.setViewClass(JstlView.class);
        
        return resolver;
    }
}
```

**XML配置方式（传统）**

```xml
<!-- 配置视图解析器 -->
<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
    <!-- 前缀：JSP文件所在目录 -->
    <property name="prefix" value="/WEB-INF/views/" />
    
    <!-- 后缀：JSP文件扩展名 -->
    <property name="suffix" value=".jsp" />
    
    <!-- 视图类：使用JSTL视图 -->
    <property name="viewClass" 
              value="org.springframework.web.servlet.view.JstlView" />
</bean>
```

### 3.3 配置参数详解

📋 **重要配置属性说明**

```
核心配置项：

1. prefix（前缀）
   作用：指定视图文件所在的目录
   示例：/WEB-INF/views/
   说明：放在WEB-INF下更安全，外部无法直接访问

2. suffix（后缀）  
   作用：指定视图文件的扩展名
   示例：.jsp 或 .html
   说明：Controller返回时不用写扩展名

3. viewClass（视图类）
   作用：指定使用的View实现类
   常用值：
   - JstlView        → 支持JSTL标签
   - InternalResourceView → 普通JSP

4. order（优先级）
   作用：多个视图解析器时的执行顺序
   说明：数字越小优先级越高
```

### 3.4 实际使用示例

💡 **从Controller到页面的完整流程**

```java
// Controller代码
@Controller
public class UserController {
    
    @GetMapping("/user/profile")
    public String showProfile(Model model) {
        // 准备数据
        model.addAttribute("username", "张三");
        
        // 返回逻辑视图名
        return "user/profile";
    }
}

视图解析过程：
1. Controller返回 "user/profile"
2. 视图解析器拼接路径：
   前缀 + 视图名 + 后缀
   /WEB-INF/views/ + user/profile + .jsp
3. 最终路径：/WEB-INF/views/user/profile.jsp
4. 渲染这个JSP页面并返回
```

**对应的JSP页面**

```jsp
<%-- /WEB-INF/views/user/profile.jsp --%>
<%@ page contentType="text/html;charset=UTF-8" %>
<html>
<head>
    <title>用户信息</title>
</head>
<body>
    <h1>欢迎，${username}！</h1>
</body>
</html>
```

---

## 4. 🔤 视图名称解析原理


### 4.1 逻辑视图名的概念

💡 **什么是逻辑视图名**

```
逻辑视图名 vs 物理视图路径

逻辑视图名（抽象的）：
- "home"
- "user/list"  
- "admin/dashboard"

物理视图路径（具体的）：
- /WEB-INF/views/home.jsp
- /WEB-INF/views/user/list.jsp
- /WEB-INF/views/admin/dashboard.jsp

为什么要分离？
✅ 代码更简洁
✅ 修改路径不用改代码
✅ 支持多种视图技术切换
```

### 4.2 视图名称的解析规则

📋 **解析规则详解**

```
基本解析规则：
最终路径 = prefix + 视图名 + suffix

示例1：简单视图名
Controller: return "home"
配置：prefix="/WEB-INF/views/", suffix=".jsp"
结果：/WEB-INF/views/home.jsp

示例2：带目录的视图名  
Controller: return "user/list"
配置：prefix="/WEB-INF/views/", suffix=".jsp"
结果：/WEB-INF/views/user/list.jsp

示例3：多级目录
Controller: return "admin/user/edit"
配置：prefix="/WEB-INF/views/", suffix=".jsp"
结果：/WEB-INF/views/admin/user/edit.jsp
```

### 4.3 特殊视图名处理

⚡ **重定向和转发**

```
1. 重定向（Redirect）
语法：return "redirect:目标路径"
示例：return "redirect:/user/list"
说明：
- 浏览器地址栏会变
- 发起新的请求
- 数据不会保留（除非用RedirectAttributes）

2. 转发（Forward）
语法：return "forward:目标路径"  
示例：return "forward:/user/detail"
说明：
- 浏览器地址栏不变
- 服务器内部跳转
- 数据会保留

区别理解：
redirect → 让浏览器重新请求
forward  → 服务器内部传递请求
```

**实际应用场景**

```java
@Controller
public class UserController {
    
    // 场景1：提交后重定向（防止重复提交）
    @PostMapping("/user/save")
    public String saveUser(User user) {
        userService.save(user);
        // 保存后重定向到列表页
        return "redirect:/user/list";
    }
    
    // 场景2：转发到另一个请求
    @GetMapping("/user/search")
    public String searchUser(String keyword, Model model) {
        if (StringUtils.isEmpty(keyword)) {
            // 参数不对，转发到列表页
            return "forward:/user/list";
        }
        List<User> users = userService.search(keyword);
        model.addAttribute("users", users);
        return "user/list";
    }
}
```

---

## 5. 🔗 视图链式解析机制


### 5.1 什么是链式解析

🎯 **链式解析的原理**

```
链式解析 = 多个视图解析器按顺序尝试

工作流程：
请求视图名 "user/profile"
      ↓
解析器1尝试 → 找不到
      ↓
解析器2尝试 → 找不到  
      ↓
解析器3尝试 → 找到了！
      ↓
返回View对象

类比理解：
就像打电话找人
- 先打家里电话 → 没人接
- 再打手机 → 没人接
- 最后打办公室 → 接通了
```

### 5.2 配置多个视图解析器

🔧 **多解析器配置示例**

```java
@Configuration
public class ViewResolverConfig {
    
    // 解析器1：处理FreeMarker模板（优先级高）
    @Bean
    @Order(1)  // 数字越小优先级越高
    public FreeMarkerViewResolver freeMarkerResolver() {
        FreeMarkerViewResolver resolver = new FreeMarkerViewResolver();
        resolver.setPrefix("");
        resolver.setSuffix(".ftl");
        resolver.setContentType("text/html;charset=UTF-8");
        return resolver;
    }
    
    // 解析器2：处理JSP页面（优先级低）
    @Bean
    @Order(2)
    public InternalResourceViewResolver jspResolver() {
        InternalResourceViewResolver resolver = 
            new InternalResourceViewResolver();
        resolver.setPrefix("/WEB-INF/views/");
        resolver.setSuffix(".jsp");
        return resolver;
    }
}
```

### 5.3 解析器的执行顺序

📊 **优先级控制**

```
优先级设置方式：

方式1：使用@Order注解
@Order(1)  → 最先执行
@Order(2)  → 其次执行

方式2：实现Ordered接口
public class MyResolver implements ViewResolver, Ordered {
    @Override
    public int getOrder() {
        return 1;  // 返回优先级数值
    }
}

方式3：直接设置order属性
resolver.setOrder(1);

执行逻辑：
1. 按order从小到大排序
2. 依次调用resolveViewName方法
3. 第一个返回非null的View就使用
4. 都返回null则报错
```

### 5.4 链式解析的应用场景

💡 **实际使用案例**

```
场景1：兼容多种模板
- FreeMarker模板优先
- 找不到就用JSP
- 灵活支持旧项目迁移

场景2：API和页面混合
- JSON视图解析器处理API
- JSP解析器处理页面
- 根据请求类型自动选择

场景3：主题切换
- 主题A的视图解析器
- 主题B的视图解析器
- 根据用户偏好动态切换
```

---

## 6. ⚡ 视图缓存优化


### 6.1 为什么需要视图缓存

🎯 **缓存的性能价值**

```
没有缓存的问题：
每次请求都要：
1. 解析视图名称
2. 查找视图文件
3. 创建View对象
→ 重复工作，浪费资源

有了缓存的好处：
第一次：解析 → 缓存
后续：直接从缓存取
→ 响应速度提升明显

性能对比：
无缓存：每次 5-10ms
有缓存：每次 < 1ms
```

### 6.2 缓存的开启与配置

🔧 **缓存配置方法**

```java
@Bean
public InternalResourceViewResolver viewResolver() {
    InternalResourceViewResolver resolver = 
        new InternalResourceViewResolver();
    
    // 基础配置
    resolver.setPrefix("/WEB-INF/views/");
    resolver.setSuffix(".jsp");
    
    // 开启视图缓存（生产环境推荐）
    resolver.setCache(true);
    
    // 设置缓存大小（默认1024）
    resolver.setCacheLimit(2048);
    
    return resolver;
}
```

**环境区分配置**

```java
@Configuration
public class ViewConfig {
    
    @Value("${spring.profiles.active}")
    private String activeProfile;
    
    @Bean
    public ViewResolver viewResolver() {
        InternalResourceViewResolver resolver = 
            new InternalResourceViewResolver();
        resolver.setPrefix("/WEB-INF/views/");
        resolver.setSuffix(".jsp");
        
        // 开发环境关闭缓存，方便调试
        if ("dev".equals(activeProfile)) {
            resolver.setCache(false);
        } else {
            // 生产环境开启缓存
            resolver.setCache(true);
            resolver.setCacheLimit(2048);
        }
        
        return resolver;
    }
}
```

### 6.3 缓存的工作原理

📋 **缓存机制详解**

```
缓存工作流程：

第一次请求 "user/list"：
1. 检查缓存 → 没有
2. 解析视图名称
3. 创建View对象
4. 存入缓存（key="user/list"）
5. 返回View对象

第二次请求 "user/list"：
1. 检查缓存 → 找到了
2. 直接返回缓存的View对象
3. 跳过解析步骤

缓存淘汰策略：
- 达到cacheLimit时使用LRU算法
- LRU = 最近最少使用的先淘汰
```

### 6.4 缓存的注意事项

⚠️ **使用缓存的最佳实践**

```
开发环境建议：
❌ 不建议开启缓存
原因：
- 修改JSP后需重启才能看到效果
- 影响开发调试效率

生产环境建议：
✅ 强烈建议开启缓存
原因：
- 大幅提升响应速度
- 减少服务器资源消耗
- 支持高并发访问

特殊场景处理：
- 动态视图：不要缓存
- 国际化视图：注意locale参数
- 个性化视图：考虑用户上下文
```

---

## 7. 🔧 前缀后缀配置技巧


### 7.1 前缀配置的最佳实践

📁 **目录结构规划**

```
推荐的目录结构：
webapp/
└── WEB-INF/
    └── views/           ← 统一存放视图
        ├── common/      ← 公共页面
        │   ├── header.jsp
        │   └── footer.jsp
        ├── user/        ← 用户模块
        │   ├── list.jsp
        │   └── edit.jsp
        └── admin/       ← 管理模块
            └── dashboard.jsp

前缀配置：
prefix="/WEB-INF/views/"

为什么放在WEB-INF下？
✅ 安全：外部无法直接访问
✅ 强制：必须通过Controller访问
✅ 规范：符合Java Web标准
```

### 7.2 后缀配置的灵活运用

📝 **不同视图技术的后缀**

```
常见后缀配置：

JSP页面：
suffix=".jsp"

HTML模板：  
suffix=".html"

FreeMarker模板：
suffix=".ftl"

Thymeleaf模板：
suffix=".html"

混合使用技巧：
// 可以不设置后缀，在视图名中指定
return "user/list.jsp"   → JSP
return "user/list.html"  → HTML
```

### 7.3 多模块项目的配置

🏗️ **大型项目的目录规划**

```
多模块目录结构：
webapp/WEB-INF/views/
├── front/              ← 前台页面
│   ├── home.jsp
│   └── product/
│       └── detail.jsp
├── admin/              ← 后台页面
│   ├── dashboard.jsp
│   └── user/
│       └── manage.jsp
└── mobile/             ← 移动端页面
    └── index.jsp

配置方式1：统一前缀
prefix="/WEB-INF/views/"
视图名：return "front/home"
      return "admin/dashboard"

配置方式2：多个视图解析器
// 前台解析器
resolver1.setPrefix("/WEB-INF/views/front/");

// 后台解析器  
resolver2.setPrefix("/WEB-INF/views/admin/");
```

### 7.4 动态前缀配置

🔄 **运行时动态调整**

```java
// 根据请求路径动态选择前缀
@Component
public class DynamicViewResolver extends InternalResourceViewResolver {
    
    @Override
    protected View loadView(String viewName, Locale locale) {
        // 获取当前请求
        HttpServletRequest request = 
            ((ServletRequestAttributes) RequestContextHolder
                .getRequestAttributes())
                .getRequest();
        
        // 根据请求路径动态设置前缀
        String requestURI = request.getRequestURI();
        if (requestURI.startsWith("/admin")) {
            setPrefix("/WEB-INF/views/admin/");
        } else if (requestURI.startsWith("/mobile")) {
            setPrefix("/WEB-INF/views/mobile/");
        } else {
            setPrefix("/WEB-INF/views/front/");
        }
        
        return super.loadView(viewName, locale);
    }
}
```

---

## 8. 🔀 多视图解析器配置


### 8.1 多解析器的应用场景

🎯 **什么时候需要多个解析器**

```
场景1：支持多种模板引擎
项目需求：
- 老页面用JSP
- 新页面用Thymeleaf
- 逐步迁移

场景2：API和页面分离
系统需求：
- Web界面返回HTML
- API接口返回JSON
- 移动端返回XML

场景3：国际化多语言
业务需求：
- 中文用一套模板
- 英文用另一套模板
- 根据locale自动选择
```

### 8.2 内容协商视图解析器

🔄 **ContentNegotiatingViewResolver详解**

```java
@Configuration
public class ViewResolverConfig {
    
    @Bean
    public ContentNegotiatingViewResolver contentNegotiatingResolver() {
        ContentNegotiatingViewResolver resolver = 
            new ContentNegotiatingViewResolver();
        
        // 配置默认视图
        List<View> defaultViews = new ArrayList<>();
        defaultViews.add(new MappingJackson2JsonView());
        resolver.setDefaultViews(defaultViews);
        
        // 配置视图解析器列表
        List<ViewResolver> resolvers = new ArrayList<>();
        resolvers.add(jspViewResolver());
        resolvers.add(freeMarkerViewResolver());
        resolver.setViewResolvers(resolvers);
        
        return resolver;
    }
    
    @Bean
    public ViewResolver jspViewResolver() {
        InternalResourceViewResolver resolver = 
            new InternalResourceViewResolver();
        resolver.setPrefix("/WEB-INF/views/");
        resolver.setSuffix(".jsp");
        return resolver;
    }
    
    @Bean
    public ViewResolver freeMarkerViewResolver() {
        FreeMarkerViewResolver resolver = new FreeMarkerViewResolver();
        resolver.setSuffix(".ftl");
        return resolver;
    }
}
```

**内容协商的工作原理**

```
请求头决定返回格式：

请求1：Accept: text/html
→ 返回HTML页面（JSP/Thymeleaf）

请求2：Accept: application/json
→ 返回JSON数据

请求3：Accept: application/xml  
→ 返回XML数据

URL扩展名方式：
/user/list.html  → HTML页面
/user/list.json  → JSON数据
/user/list.xml   → XML数据
```

### 8.3 Bean名称视图解析器

📦 **BeanNameViewResolver应用**

```java
@Configuration
public class ViewConfig {
    
    // 配置Bean名称视图解析器
    @Bean
    public BeanNameViewResolver beanNameViewResolver() {
        BeanNameViewResolver resolver = new BeanNameViewResolver();
        resolver.setOrder(1); // 最高优先级
        return resolver;
    }
    
    // 自定义视图Bean
    @Bean("excelView")
    public View excelView() {
        return new AbstractExcelView() {
            @Override
            protected void buildExcelDocument(
                Map<String, Object> model,
                Workbook workbook,
                HttpServletRequest request,
                HttpServletResponse response) {
                
                // 创建Excel文件逻辑
                Sheet sheet = workbook.createSheet("User List");
                // ... 填充数据
            }
        };
    }
    
    // 自定义PDF视图
    @Bean("pdfView")
    public View pdfView() {
        return new AbstractPdfView() {
            @Override
            protected void buildPdfDocument(
                Map<String, Object> model,
                Document document,
                PdfWriter writer,
                HttpServletRequest request,
                HttpServletResponse response) {
                
                // 创建PDF文档逻辑
                document.add(new Paragraph("User Report"));
                // ... 填充数据
            }
        };
    }
}

// Controller中使用
@Controller
public class ReportController {
    
    @GetMapping("/report/excel")
    public String exportExcel(Model model) {
        List<User> users = userService.findAll();
        model.addAttribute("users", users);
        return "excelView"; // 直接返回Bean名称
    }
    
    @GetMapping("/report/pdf")
    public String exportPdf(Model model) {
        List<User> users = userService.findAll();
        model.addAttribute("users", users);
        return "pdfView";
    }
}
```

### 8.4 完整的多解析器配置示例

🏗️ **企业级配置方案**

```java
@Configuration
@EnableWebMvc
public class WebMvcConfig implements WebMvcConfigurer {
    
    @Override
    public void configureViewResolvers(ViewResolverRegistry registry) {
        
        // 1. Bean名称视图解析器（最高优先级）
        BeanNameViewResolver beanNameResolver = new BeanNameViewResolver();
        beanNameResolver.setOrder(1);
        registry.viewResolver(beanNameResolver);
        
        // 2. Thymeleaf视图解析器
        ThymeleafViewResolver thymeleafResolver = new ThymeleafViewResolver();
        thymeleafResolver.setTemplateEngine(templateEngine());
        thymeleafResolver.setOrder(2);
        registry.viewResolver(thymeleafResolver);
        
        // 3. JSP视图解析器（兜底）
        InternalResourceViewResolver jspResolver = 
            new InternalResourceViewResolver();
        jspResolver.setPrefix("/WEB-INF/views/");
        jspResolver.setSuffix(".jsp");
        jspResolver.setOrder(3);
        registry.viewResolver(jspResolver);
    }
    
    // 解析顺序说明
    /*
     * 1. 先检查是否有同名的View Bean
     * 2. 再尝试Thymeleaf模板
     * 3. 最后尝试JSP页面
     * 
     * 使用示例：
     * return "excelView"     → Bean视图
     * return "user/list"     → Thymeleaf模板
     * return "legacy/page"   → JSP页面
     */
}
```

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的核心概念


```
🔸 视图解析器作用：把逻辑视图名转换成具体页面路径
🔸 ViewResolver接口：定义视图解析的标准规范
🔸 InternalResourceViewResolver：最常用的JSP解析器
🔸 视图名称解析：前缀 + 视图名 + 后缀 = 物理路径
🔸 链式解析：多个解析器按优先级依次尝试
🔸 视图缓存：提升性能的重要优化手段
🔸 前缀后缀：统一管理视图文件位置和类型
🔸 多解析器：支持多种视图技术和响应格式
```

### 9.2 关键理解要点


**🔹 视图解析的本质**
```
核心思想：
- 分离逻辑和物理：代码只关心逻辑名称
- 配置灵活：改配置不改代码
- 技术无关：轻松切换视图技术

实际价值：
✅ 代码更简洁优雅
✅ 维护更方便快捷
✅ 扩展更灵活自如
```

**🔹 多解析器的协作机制**
```
工作原理：
1. 按order优先级排序
2. 依次尝试解析视图名
3. 第一个成功的就返回
4. 都失败则抛出异常

最佳实践：
- 特殊视图放前面（Bean名称）
- 常用视图放中间（Thymeleaf）
- 兜底视图放最后（JSP）
```

**🔹 生产环境优化策略**
```
性能优化：
1. 开启视图缓存
2. 合理设置缓存大小
3. 静态资源CDN加速

开发调试：
1. 关闭视图缓存
2. 使用热部署工具
3. 配置详细日志

环境隔离：
- 开发环境：缓存关，日志详细
- 测试环境：缓存开，日志适中
- 生产环境：缓存开，日志精简
```

### 9.3 实际应用价值


**🎯 项目实战场景**
- **传统Web项目**：JSP视图解析器满足基本需求
- **现代化改造**：多解析器支持渐进式迁移
- **前后端分离**：内容协商返回JSON/HTML
- **报表导出**：自定义视图生成Excel/PDF

**🔧 配置选择建议**
- **小型项目**：单一JSP解析器即可
- **中型项目**：JSP + Thymeleaf双解析器
- **大型项目**：完整的多解析器 + 内容协商
- **微服务架构**：主要返回JSON，少量页面用模板

**📈 学习路径建议**
```
初学阶段：
1. 掌握InternalResourceViewResolver
2. 理解前缀后缀配置
3. 学会基本的视图返回

进阶阶段：
1. 了解多种视图解析器
2. 掌握链式解析机制
3. 学习视图缓存优化

高级阶段：
1. 内容协商视图解析
2. 自定义视图实现
3. 性能调优实践
```

**核心记忆口诀**：
- 视图解析器是邮递员，逻辑名称变真实路径
- 前缀后缀来拼接，JSP模板都支持
- 多个解析按顺序，缓存优化提性能
- 内容协商很强大，一个接口多格式