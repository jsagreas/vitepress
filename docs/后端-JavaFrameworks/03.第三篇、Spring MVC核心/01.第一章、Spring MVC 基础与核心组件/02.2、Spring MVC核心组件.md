---
title: 2、Spring MVC核心组件
---
## 📚 目录

1. [核心组件概览](#1-核心组件概览)
2. [DispatcherServlet前端控制器](#2-DispatcherServlet前端控制器)
3. [HandlerMapping处理器映射](#3-HandlerMapping处理器映射)
4. [HandlerAdapter处理器适配器](#4-HandlerAdapter处理器适配器)
5. [ViewResolver视图解析器](#5-ViewResolver视图解析器)
6. [ModelAndView模型视图](#6-ModelAndView模型视图)
7. [HandlerInterceptor拦截器](#7-HandlerInterceptor拦截器)
8. [HandlerExceptionResolver异常解析器](#8-HandlerExceptionResolver异常解析器)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🎯 核心组件概览


### 1.1 什么是Spring MVC核心组件


**通俗理解**：Spring MVC就像一个餐厅的运作系统，各个组件就像餐厅里的不同角色

```
客户点餐流程                    Spring MVC处理流程
-------------                   ------------------
顾客 → 服务员                    浏览器 → DispatcherServlet(前台总调度)
服务员查看菜单                   HandlerMapping(查找处理器)
厨房准备菜品                     Handler/Controller(处理业务)
服务员端菜上桌                   ViewResolver(选择展示方式)
顾客享用                        浏览器显示结果
```

### 1.2 七大核心组件关系图


```
请求处理完整流程：

浏览器                                                     服务器
  |                                                         |
  |--① HTTP请求------------------------------------------>|
  |                                                         |
  |                                        DispatcherServlet(前端控制器)
  |                                                ↓
  |                                        HandlerMapping(处理器映射)
  |                                        "这个请求谁来处理?"
  |                                                ↓
  |                                        HandlerAdapter(处理器适配器)
  |                                        "用合适的方式调用"
  |                                                ↓
  |                                        Controller(控制器)
  |                                        "执行业务逻辑"
  |                                                ↓
  |                                        ModelAndView(模型和视图)
  |                                        "数据+页面名称"
  |                                                ↓
  |                                        ViewResolver(视图解析器)
  |                                        "找到真正的页面"
  |                                                ↓
  |                                        View(视图)
  |                                        "渲染页面"
  |                                                |
  |<--⑧ HTTP响应(HTML页面)-------------------------------|
```

### 1.3 组件协作的核心理念


**设计思想** - `[核心概念]`
- **职责分离**：每个组件只负责一件事，互不干扰
- **松耦合**：组件之间通过接口交互，可以随时替换
- **可扩展**：可以自定义任何一个组件的实现

---

## 2. 🚪 DispatcherServlet前端控制器


### 2.1 是什么？


**通俗解释**：DispatcherServlet就是**前台总调度员**，所有请求都要先经过它

```
类比理解：
医院的挂号台  →  DispatcherServlet
所有患者都要先在挂号台登记
挂号台根据病情分配到不同科室
最后把结果返回给患者
```

**专业定义**：
- DispatcherServlet是Spring MVC的**中央调度器**
- 它是一个Servlet，负责接收所有HTTP请求
- 把请求分发给合适的处理器，协调各个组件工作

### 2.2 工作原理


**核心流程** ⭐⭐⭐

```java
// 简化的工作流程
public class DispatcherServlet extends HttpServlet {
    
    protected void doDispatch(HttpServletRequest request) {
        // 1. 找到能处理这个请求的Handler
        HandlerExecutionChain handler = getHandler(request);
        
        // 2. 找到合适的适配器来调用Handler
        HandlerAdapter adapter = getHandlerAdapter(handler);
        
        // 3. 执行拦截器的前置处理
        handler.applyPreHandle(request);
        
        // 4. 真正执行Handler
        ModelAndView mv = adapter.handle(request, handler);
        
        // 5. 执行拦截器的后置处理
        handler.applyPostHandle(request);
        
        // 6. 渲染视图
        render(mv, request);
    }
}
```

**关键职责**：
1. **接收请求** - 拦截所有符合规则的HTTP请求
2. **查找处理器** - 委托HandlerMapping查找Handler
3. **调用处理器** - 通过HandlerAdapter执行Handler
4. **处理异常** - 捕获并处理执行过程中的异常
5. **渲染视图** - 将结果渲染成页面返回

### 2.3 配置方式


**Web.xml配置** `[传统方式]`

```xml
<!-- 配置前端控制器 -->
<servlet>
    <servlet-name>springmvc</servlet-name>
    <servlet-class>
        org.springframework.web.servlet.DispatcherServlet
    </servlet-class>
    <!-- 指定Spring MVC配置文件位置 -->
    <init-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>classpath:spring-mvc.xml</param-value>
    </init-param>
    <!-- 启动时加载 -->
    <load-on-startup>1</load-on-startup>
</servlet>

<!-- 拦截所有请求 -->
<servlet-mapping>
    <servlet-name>springmvc</servlet-name>
    <url-pattern>/</url-pattern>
</servlet-mapping>
```

**URL拦截规则对比** 📊

| 配置方式 | 说明 | 示例 | 适用场景 |
|---------|------|------|---------|
| `/` | 拦截所有请求(推荐) | `/user`, `/api/data` | 现代Web应用 |
| `/*` | 拦截所有包括jsp | 会拦截jsp页面 | ❌不建议使用 |
| `*.do` | 拦截特定后缀 | `/user.do` | 老项目 |
| `/api/*` | 拦截指定路径 | `/api/user` | RESTful API |

---

## 3. 🗺️ HandlerMapping处理器映射


### 3.1 是什么？


**通俗理解**：HandlerMapping就像**导航地图**，帮DispatcherServlet找到该谁来处理请求

```
生活场景类比：
你去商场想买衣服
→ 查看商场导航图(HandlerMapping)
→ 发现3楼是服装区(找到Handler)
→ 坐电梯上去(由Adapter调用)
```

**专业解释**：
- HandlerMapping负责**URL与处理器的映射关系**
- 根据请求URL，找到对应的Controller和方法
- 返回一个HandlerExecutionChain(处理器执行链)

### 3.2 工作机制


**映射查找过程** 🔍

```
请求URL: /user/list
               ↓
       HandlerMapping查找
               ↓
    扫描所有@RequestMapping注解
               ↓
       找到匹配的Controller方法
               ↓
    返回 HandlerExecutionChain
    (包含: Handler + 拦截器链)
```

**代码示例**：

```java
@Controller
@RequestMapping("/user")
public class UserController {
    
    // URL: /user/list → 映射到这个方法
    @RequestMapping("/list")
    public String getUserList(Model model) {
        // 处理逻辑
        return "userList";
    }
    
    // URL: /user/detail?id=1 → 映射到这个方法
    @GetMapping("/detail")
    public String getUserDetail(@RequestParam Long id) {
        return "userDetail";
    }
}
```

### 3.3 常用映射器类型


**三种主要映射器** `[重要程度★★★]`

1. **RequestMappingHandlerMapping** - 最常用 ⭐⭐⭐
   - 处理`@RequestMapping`注解
   - 支持REST风格URL
   - Spring 3.1+默认使用

2. **BeanNameUrlHandlerMapping** - 传统方式
   - 根据Bean名称映射URL
   - 较老的方式，现在较少使用

3. **SimpleUrlHandlerMapping** - 静态资源
   - 配置静态URL映射
   - 处理静态资源访问

**映射配置示例**：

```java
@Configuration
public class WebConfig {
    
    // 配置静态资源映射
    @Bean
    public SimpleUrlHandlerMapping simpleUrlMapping() {
        SimpleUrlHandlerMapping mapping = new SimpleUrlHandlerMapping();
        Map<String, Object> urlMap = new HashMap<>();
        urlMap.put("/static/**", "staticHandler");
        mapping.setUrlMap(urlMap);
        return mapping;
    }
}
```

---

## 4. 🔌 HandlerAdapter处理器适配器


### 4.1 是什么？


**通俗理解**：HandlerAdapter就像**万能转接头**，让不同类型的Handler都能正常工作

```
适配器模式类比：
你有个美规插头(Handler)
中国插座无法直接使用(DispatcherServlet)
需要一个转换器(HandlerAdapter)
转换后就能正常使用了
```

**为什么需要适配器？**
- Spring MVC支持**多种Handler类型**
- Controller、HttpRequestHandler、Servlet等
- 需要统一的调用方式 → 适配器模式

### 4.2 工作原理


**适配器选择流程**：

```
DispatcherServlet拿到Handler
         ↓
遍历所有HandlerAdapter
         ↓
判断: adapter.supports(handler)?
         ↓
    找到匹配的Adapter
         ↓
    adapter.handle(request, handler)
         ↓
    返回ModelAndView
```

**核心方法**：

```java
public interface HandlerAdapter {
    
    // 判断是否支持这个Handler
    boolean supports(Object handler);
    
    // 执行Handler，返回ModelAndView
    ModelAndView handle(HttpServletRequest request, 
                       Object handler) throws Exception;
}
```

### 4.3 常用适配器类型


**三种主要适配器** 📋

| 适配器类型 | 支持的Handler | 使用场景 | 常用程度 |
|-----------|--------------|----------|---------|
| `RequestMappingHandlerAdapter` | `@RequestMapping`注解的方法 | 现代Controller | ⭐⭐⭐ |
| `HttpRequestHandlerAdapter` | `HttpRequestHandler`接口 | 处理静态资源 | ⭐⭐ |
| `SimpleControllerHandlerAdapter` | `Controller`接口 | 传统Controller | ⭐ |

**适配不同Handler示例**：

```java
// 类型1: 注解式Controller (最常用)
@Controller
public class UserController {
    @RequestMapping("/user")
    public ModelAndView getUser() {
        // RequestMappingHandlerAdapter处理
        return new ModelAndView("user");
    }
}

// 类型2: 实现Controller接口 (传统方式)
public class OldController implements Controller {
    public ModelAndView handleRequest(HttpServletRequest req, 
                                     HttpServletResponse resp) {
        // SimpleControllerHandlerAdapter处理
        return new ModelAndView("oldStyle");
    }
}
```

---

## 5. 👁️ ViewResolver视图解析器


### 5.1 是什么？


**通俗理解**：ViewResolver是**页面管家**，负责找到真正要显示的页面

```
现实场景：
Controller说: "给我显示user页面"
ViewResolver: "user是哪个页面？"
→ 查找: /WEB-INF/views/user.jsp
→ 找到并返回真实页面
```

**专业解释**：
- 根据**逻辑视图名**找到**物理视图**
- 支持多种视图技术：JSP、Thymeleaf、FreeMarker
- 可以配置多个解析器，按优先级查找

### 5.2 工作流程


**视图解析过程**：

```
Controller返回: "userList"
            ↓
    ViewResolver解析
            ↓
    前缀 + 视图名 + 后缀
            ↓
/WEB-INF/views/ + userList + .jsp
            ↓
找到: /WEB-INF/views/userList.jsp
            ↓
    返回View对象
```

### 5.3 常用视图解析器


**InternalResourceViewResolver配置** `[最常用]`

```java
@Configuration
public class WebConfig {
    
    @Bean
    public InternalResourceViewResolver viewResolver() {
        InternalResourceViewResolver resolver = 
            new InternalResourceViewResolver();
        
        // 设置前缀：页面存放路径
        resolver.setPrefix("/WEB-INF/views/");
        
        // 设置后缀：页面类型
        resolver.setSuffix(".jsp");
        
        // 设置优先级(数字越小优先级越高)
        resolver.setOrder(1);
        
        return resolver;
    }
}
```

**多视图解析器配置**：

```java
@Configuration
public class ViewResolverConfig {
    
    // JSON视图解析器 - 优先级最高
    @Bean
    @Order(0)
    public ViewResolver jsonViewResolver() {
        return new JsonViewResolver();
    }
    
    // JSP视图解析器 - 兜底使用
    @Bean
    @Order(1)
    public InternalResourceViewResolver jspResolver() {
        InternalResourceViewResolver resolver = 
            new InternalResourceViewResolver();
        resolver.setPrefix("/WEB-INF/views/");
        resolver.setSuffix(".jsp");
        return resolver;
    }
}
```

**视图名称解析示例** 📝

```
逻辑视图名          →  实际物理路径
-----------           ---------------
"user"              →  /WEB-INF/views/user.jsp
"user/list"         →  /WEB-INF/views/user/list.jsp
"redirect:/home"    →  重定向到/home
"forward:/user"     →  转发到/user
```

---

## 6. 📦 ModelAndView模型视图


### 6.1 是什么？


**通俗理解**：ModelAndView就像**快递包裹**，既包含数据(Model)又包含送货地址(View)

```
快递比喻：
Model(数据) = 包裹里的商品
View(视图) = 收货地址
ModelAndView = 完整的快递包裹
```

**专业解释**：
- ModelAndView是一个**容器对象**
- Model：存放要传递给页面的数据
- View：指定要显示的页面名称
- 作为Controller和View之间的**数据传输对象**

### 6.2 核心用法


**三种创建方式**：

```java
@Controller
public class UserController {
    
    // 方式1: 构造时指定视图名
    @RequestMapping("/user1")
    public ModelAndView method1() {
        ModelAndView mv = new ModelAndView("userList");
        mv.addObject("message", "欢迎使用");
        return mv;
    }
    
    // 方式2: 先创建再设置
    @RequestMapping("/user2")
    public ModelAndView method2() {
        ModelAndView mv = new ModelAndView();
        mv.setViewName("userList");
        mv.addObject("users", userService.findAll());
        return mv;
    }
    
    // 方式3: 分离Model和View (推荐)
    @RequestMapping("/user3")
    public String method3(Model model) {
        model.addAttribute("message", "分离方式");
        return "userList";  // 只返回视图名
    }
}
```

### 6.3 Model数据传递


**数据存储方式对比** 📊

| 方式 | 作用域 | 适用场景 | 示例 |
|-----|--------|---------|------|
| `addObject()` | Request | 单次请求数据传递 | `mv.addObject("user", user)` |
| `@SessionAttributes` | Session | 多次请求共享数据 | `@SessionAttributes("user")` |
| `@ModelAttribute` | Model | 方法间共享数据 | `@ModelAttribute("type")` |

**实际应用示例**：

```java
@Controller
public class ProductController {
    
    // 传递单个对象
    @RequestMapping("/product/detail")
    public ModelAndView getProductDetail(@RequestParam Long id) {
        ModelAndView mv = new ModelAndView("productDetail");
        
        Product product = productService.findById(id);
        mv.addObject("product", product);
        
        return mv;
    }
    
    // 传递集合数据
    @RequestMapping("/product/list")
    public ModelAndView getProductList() {
        ModelAndView mv = new ModelAndView("productList");
        
        List<Product> products = productService.findAll();
        mv.addObject("products", products);
        mv.addObject("totalCount", products.size());
        
        return mv;
    }
}
```

**页面获取数据** (JSP示例)：

```jsp
<!-- 获取单个对象 -->
<h2>${product.name}</h2>
<p>价格：${product.price}</p>

<!-- 遍历集合 -->
<c:forEach items="${products}" var="item">
    <div>${item.name} - ${item.price}</div>
</c:forEach>

<!-- 显示总数 -->
<p>共 ${totalCount} 个商品</p>
```

---

## 7. 🛡️ HandlerInterceptor拦截器


### 7.1 是什么？


**通俗理解**：HandlerInterceptor就像**安检门**，请求处理前后都可以进行检查和处理

```
机场安检类比：
登机前 → preHandle() 检查身份证
登机中 → (Handler执行)
登机后 → postHandle() 检查行李
离开  → afterCompletion() 清理现场
```

**专业解释**：
- 拦截器在Handler执行前后进行**额外处理**
- 可以实现：权限校验、日志记录、性能监控
- 多个拦截器形成**拦截器链**

### 7.2 三个核心方法


**执行时机图解**：

```
请求到达
   ↓
preHandle()      ← 执行前：返回true继续，false中断
   ↓
Handler执行      ← 业务逻辑处理
   ↓
postHandle()     ← 执行后：可修改ModelAndView
   ↓
视图渲染
   ↓
afterCompletion() ← 完成后：清理资源
   ↓
响应返回
```

**接口定义**：

```java
public interface HandlerInterceptor {
    
    // 1. 前置处理：Handler执行前调用
    // 返回true继续执行，false中断请求
    default boolean preHandle(HttpServletRequest request, 
                            HttpServletResponse response, 
                            Object handler) {
        return true;
    }
    
    // 2. 后置处理：Handler执行后，视图渲染前调用
    // 可以修改ModelAndView
    default void postHandle(HttpServletRequest request, 
                          HttpServletResponse response,
                          Object handler, 
                          ModelAndView modelAndView) {
    }
    
    // 3. 完成处理：视图渲染后调用
    // 一般用于资源清理
    default void afterCompletion(HttpServletRequest request,
                                HttpServletResponse response,
                                Object handler, 
                                Exception ex) {
    }
}
```

### 7.3 实际应用场景


**场景1: 登录验证拦截器** `[高频使用★★★]`

```java
public class LoginInterceptor implements HandlerInterceptor {
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                            HttpServletResponse response, 
                            Object handler) {
        // 检查session中是否有用户信息
        HttpSession session = request.getSession();
        Object user = session.getAttribute("user");
        
        if (user == null) {
            // 未登录，重定向到登录页
            response.sendRedirect("/login");
            return false;  // 中断请求
        }
        
        return true;  // 已登录，继续执行
    }
}
```

**场景2: 日志记录拦截器**：

```java
public class LogInterceptor implements HandlerInterceptor {
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                            HttpServletResponse response, 
                            Object handler) {
        // 记录请求开始时间
        long startTime = System.currentTimeMillis();
        request.setAttribute("startTime", startTime);
        
        // 记录请求信息
        String url = request.getRequestURI();
        String method = request.getMethod();
        System.out.println("请求开始: " + method + " " + url);
        
        return true;
    }
    
    @Override
    public void afterCompletion(HttpServletRequest request,
                               HttpServletResponse response,
                               Object handler, Exception ex) {
        // 计算执行时间
        long startTime = (Long) request.getAttribute("startTime");
        long endTime = System.currentTimeMillis();
        long executeTime = endTime - startTime;
        
        System.out.println("请求完成，耗时: " + executeTime + "ms");
    }
}
```

**场景3: 编码设置拦截器**：

```java
public class EncodingInterceptor implements HandlerInterceptor {
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                            HttpServletResponse response, 
                            Object handler) {
        // 统一设置请求响应编码
        request.setCharacterEncoding("UTF-8");
        response.setContentType("text/html;charset=UTF-8");
        return true;
    }
}
```

### 7.4 拦截器配置


**注册拦截器** `[配置方式]`

```java
@Configuration
public class WebMvcConfig implements WebMvcConfigurer {
    
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        
        // 注册登录拦截器
        registry.addInterceptor(new LoginInterceptor())
                .addPathPatterns("/**")           // 拦截所有路径
                .excludePathPatterns(             // 排除不需要拦截的路径
                    "/login",                     // 登录页
                    "/register",                  // 注册页
                    "/static/**"                  // 静态资源
                )
                .order(1);  // 设置优先级
        
        // 注册日志拦截器
        registry.addInterceptor(new LogInterceptor())
                .addPathPatterns("/**")
                .order(2);
    }
}
```

**多拦截器执行顺序** 📋

```
假设有3个拦截器: A(order=1), B(order=2), C(order=3)

正常流程:
A.preHandle → B.preHandle → C.preHandle 
→ Handler执行 
→ C.postHandle → B.postHandle → A.postHandle
→ 视图渲染
→ C.afterCompletion → B.afterCompletion → A.afterCompletion

中断流程(B返回false):
A.preHandle → B.preHandle(返回false)
→ A.afterCompletion (只执行已成功的拦截器的完成方法)
```

---

## 8. ⚠️ HandlerExceptionResolver异常解析器


### 8.1 是什么？


**通俗理解**：HandlerExceptionResolver是**应急处理员**，当系统出错时负责友好地处理错误

```
生活场景：
程序出错 → 系统崩溃显示500错误 ❌
使用异常解析器 → 显示友好错误页面 ✅
```

**专业解释**：
- 统一处理Controller层抛出的异常
- 将异常转换为友好的错误页面或错误信息
- 避免将系统错误直接暴露给用户

### 8.2 异常处理流程


**异常捕获机制**：

```
Controller执行
      ↓
   抛出异常
      ↓
HandlerExceptionResolver捕获
      ↓
   解析异常类型
      ↓
   返回错误视图/错误信息
      ↓
   展示给用户
```

### 8.3 三种异常处理方式


**方式1: @ExceptionHandler注解** `[推荐★★★]`

```java
@Controller
public class UserController {
    
    @RequestMapping("/user/delete")
    public String deleteUser(@RequestParam Long id) {
        if (id == null) {
            throw new IllegalArgumentException("用户ID不能为空");
        }
        userService.delete(id);
        return "success";
    }
    
    // 处理本Controller的异常
    @ExceptionHandler(IllegalArgumentException.class)
    public ModelAndView handleIllegalArgument(Exception ex) {
        ModelAndView mv = new ModelAndView("error");
        mv.addObject("message", ex.getMessage());
        return mv;
    }
}
```

**方式2: 全局异常处理器** `[企业常用★★★]`

```java
@ControllerAdvice  // 全局异常处理
public class GlobalExceptionHandler {
    
    // 处理业务异常
    @ExceptionHandler(BusinessException.class)
    public ModelAndView handleBusinessException(BusinessException ex) {
        ModelAndView mv = new ModelAndView("error/business");
        mv.addObject("errorCode", ex.getCode());
        mv.addObject("errorMsg", ex.getMessage());
        return mv;
    }
    
    // 处理参数校验异常
    @ExceptionHandler(MethodArgumentNotValidException.class)
    @ResponseBody
    public Map<String, Object> handleValidException(
        MethodArgumentNotValidException ex) {
        
        Map<String, Object> result = new HashMap<>();
        result.put("success", false);
        result.put("message", "参数校验失败");
        
        // 获取具体错误信息
        List<String> errors = ex.getBindingResult()
            .getAllErrors()
            .stream()
            .map(error -> error.getDefaultMessage())
            .collect(Collectors.toList());
        
        result.put("errors", errors);
        return result;
    }
    
    // 兜底处理所有未知异常
    @ExceptionHandler(Exception.class)
    public ModelAndView handleException(Exception ex) {
        ModelAndView mv = new ModelAndView("error/500");
        mv.addObject("message", "系统繁忙，请稍后再试");
        
        // 记录详细错误日志
        log.error("系统异常", ex);
        return mv;
    }
}
```

**方式3: 实现HandlerExceptionResolver接口**

```java
@Component
public class CustomExceptionResolver 
    implements HandlerExceptionResolver {
    
    @Override
    public ModelAndView resolveException(
        HttpServletRequest request,
        HttpServletResponse response,
        Object handler,
        Exception ex) {
        
        ModelAndView mv = new ModelAndView();
        
        // 根据异常类型返回不同视图
        if (ex instanceof NullPointerException) {
            mv.setViewName("error/null");
        } else if (ex instanceof IOException) {
            mv.setViewName("error/io");
        } else {
            mv.setViewName("error/unknown");
        }
        
        mv.addObject("exception", ex);
        return mv;
    }
}
```

### 8.4 异常处理最佳实践


**分层异常处理策略** 📋

```
异常类型                    处理方式                  返回内容
---------                  --------                  --------
参数校验异常                → JSON格式                → 错误提示信息
业务逻辑异常                → 跳转错误页面             → 友好错误提示
系统运行时异常              → 跳转500页面             → 通用错误提示
404/405等                  → 跳转对应错误页           → 引导用户操作
```

**自定义业务异常**：

```java
// 自定义业务异常类
public class BusinessException extends RuntimeException {
    private String code;
    private String message;
    
    public BusinessException(String code, String message) {
        super(message);
        this.code = code;
        this.message = message;
    }
    
    // getters...
}

// 使用示例
@Service
public class UserService {
    
    public User getUserById(Long id) {
        User user = userDao.findById(id);
        if (user == null) {
            throw new BusinessException(
                "USER_NOT_FOUND", 
                "用户不存在，ID: " + id
            );
        }
        return user;
    }
}
```

---

## 9. 📋 核心要点总结


### 9.1 七大组件速记


```
🔸 DispatcherServlet  → 前台总调度，统一入口
🔸 HandlerMapping     → 导航地图，找到处理器
🔸 HandlerAdapter     → 万能转接头，调用处理器
🔸 ViewResolver       → 页面管家，解析视图
🔸 ModelAndView       → 快递包裹，数据+视图
🔸 HandlerInterceptor → 安检门，前后拦截
🔸 ExceptionResolver  → 应急处理，异常处理
```

### 9.2 完整请求处理流程


**8步完整流程** `[面试高频★★★]`

```
① 浏览器发送请求 → DispatcherServlet接收
                        ↓
② DispatcherServlet → HandlerMapping查找Handler
                        ↓
③ 返回HandlerExecutionChain(Handler+拦截器)
                        ↓
④ 拦截器preHandle() → 前置处理
                        ↓
⑤ HandlerAdapter → 调用Handler执行业务
                        ↓
⑥ 拦截器postHandle() → 后置处理
                        ↓
⑦ ViewResolver → 解析视图，渲染页面
                        ↓
⑧ 拦截器afterCompletion() → 返回响应
```

### 9.3 核心组件对比


| 组件 | 作用 | 数量 | 可自定义 | 使用频率 |
|-----|------|------|---------|---------|
| `DispatcherServlet` | 总调度 | 1个 | ❌ | 必须配置 |
| `HandlerMapping` | URL映射 | 多个 | ✅ | 默认够用 |
| `HandlerAdapter` | 适配调用 | 多个 | ✅ | 默认够用 |
| `ViewResolver` | 视图解析 | 多个 | ✅ | 必须配置 |
| `Interceptor` | 拦截处理 | 多个 | ✅ | 常用 ⭐⭐⭐ |
| `ExceptionResolver` | 异常处理 | 多个 | ✅ | 推荐配置 ⭐⭐⭐ |

### 9.4 学习建议


**掌握重点** `[学习路径]`

```
初级阶段：
✅ 理解DispatcherServlet作用
✅ 掌握@RequestMapping映射
✅ 会配置ViewResolver
✅ 能使用ModelAndView传数据

进阶阶段：
✅ 自定义HandlerInterceptor
✅ 全局异常处理
✅ 理解HandlerAdapter适配机制
✅ 多视图解析器配置

高级阶段：
✅ 自定义HandlerMapping
✅ 扩展HandlerAdapter
✅ 深入理解执行流程
✅ 性能优化调优
```

### 9.5 常见问题自查


**问题自检清单** ☑️

- [ ] 请求404：检查HandlerMapping配置和URL映射
- [ ] 视图找不到：检查ViewResolver前缀后缀配置
- [ ] 拦截器不生效：检查拦截路径和执行顺序
- [ ] 异常未捕获：检查ExceptionHandler范围
- [ ] 参数绑定失败：检查HandlerAdapter配置

**核心记忆口诀**：
```
前端控制是调度，映射适配找处理
视图解析选页面，模型数据一起传
拦截器里加检查，异常处理保安全
七大组件环环扣，MVC流程要记牢
```