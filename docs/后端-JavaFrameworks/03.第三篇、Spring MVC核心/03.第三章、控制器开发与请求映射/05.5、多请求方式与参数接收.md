---
title: 5、多请求方式与参数接收
---
## 📚 目录

1. [请求参数接收概述](#1-请求参数接收概述)
2. [QueryString参数处理](#2-QueryString参数处理)
3. [路径变量PathVariable](#3-路径变量PathVariable)
4. [请求体RequestBody处理](#4-请求体RequestBody处理)
5. [JSON数据绑定](#5-JSON数据绑定)
6. [表单数据处理](#6-表单数据处理)
7. [文件上传处理](#7-文件上传处理)
8. [复合请求处理](#8-复合请求处理)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🌐 请求参数接收概述


### 1.1 什么是请求参数


**通俗理解**：就像你在网上购物填写的收货地址、商品数量这些信息，都需要从网页传递到服务器，这个传递的数据就是"请求参数"。

```
前端页面发送数据 → 网络传输 → 后端控制器接收处理

比如：
- 搜索框输入"手机" → 传递关键词参数
- 点击"第2页" → 传递页码参数  
- 提交注册表单 → 传递用户信息参数
```

### 1.2 常见的请求参数传递方式


Spring MVC支持多种参数传递方式，每种方式适用不同场景：

| 传递方式 | **典型场景** | **数据位置** | **适用情况** |
|---------|------------|------------|------------|
| **QueryString** | `搜索、筛选、分页` | `URL后面?key=value` | 简单参数，可见参数 |
| **PathVariable** | `RESTful API资源定位` | `URL路径中/user/123` | 资源标识，语义化URL |
| **RequestBody** | `提交复杂数据` | `HTTP请求体中` | JSON/XML数据 |
| **FormData** | `表单提交` | `请求体中` | 传统表单、文件上传 |

### 1.3 请求参数的生命周期


```
客户端发起请求
    ↓
数据封装到HTTP请求中（URL、请求头、请求体）
    ↓
Spring MVC接收请求
    ↓
参数解析器识别参数类型
    ↓
自动转换并绑定到Controller方法参数
    ↓
业务逻辑处理
    ↓
返回响应数据
```

---

## 2. 🔍 QueryString参数处理


### 2.1 什么是QueryString


**QueryString**：就是URL问号后面的参数部分，用`&`连接多个参数。

```
完整URL示例：
https://www.example.com/search?keyword=手机&page=2&size=20
                                 ↑
                          这部分就是QueryString

拆解：
- keyword=手机   （搜索关键词）
- page=2         （第几页）
- size=20        （每页显示数量）
```

**特点**：
- ✅ 参数直接显示在浏览器地址栏
- ✅ 可以被收藏、分享
- ✅ 适合传递简单、非敏感数据
- ❌ 不适合传递密码等敏感信息
- ❌ 长度有限制（浏览器限制一般2KB-8KB）

### 2.2 接收单个参数


**方式一：使用`@RequestParam`注解**

```java
@GetMapping("/search")
public String search(@RequestParam String keyword) {
    // 接收 /search?keyword=手机
    System.out.println("搜索关键词：" + keyword);
    return "搜索结果页面";
}
```

> 💡 **新手提示**：`@RequestParam`告诉Spring："这个参数从URL的QueryString中获取"

**方式二：参数名匹配（简化写法）**

```java
@GetMapping("/search")
public String search(String keyword) {
    // 方法参数名与URL参数名一致时，可以省略@RequestParam
    return "搜索结果";
}
```

### 2.3 设置参数属性


```java
@GetMapping("/list")
public String list(
    @RequestParam(value = "page", required = false, defaultValue = "1") int page,
    @RequestParam(value = "size", defaultValue = "10") int size
) {
    // 参数详解：
    // value = "page"     → URL中的参数名
    // required = false   → 参数可选，不传也不报错
    // defaultValue = "1" → 参数未传时的默认值
    
    System.out.println("第" + page + "页，每页" + size + "条");
    return "列表页";
}
```

**参数说明**：
- `value`：URL中的参数名（可以与方法参数名不同）
- `required`：是否必须传递（默认true，必须传）
- `defaultValue`：默认值（设置后required自动变为false）

### 2.4 接收多个参数


```java
@GetMapping("/filter")
public String filter(
    String category,    // 商品分类
    Integer minPrice,   // 最低价格
    Integer maxPrice,   // 最高价格
    String brand        // 品牌
) {
    // 接收：/filter?category=手机&minPrice=1000&maxPrice=5000&brand=华为
    
    System.out.println("分类：" + category);
    System.out.println("价格区间：" + minPrice + " - " + maxPrice);
    System.out.println("品牌：" + brand);
    
    return "筛选结果";
}
```

### 2.5 接收数组和集合参数


**场景**：用户勾选多个选项，如商品颜色、尺寸等

```java
// URL: /product?colors=红色&colors=蓝色&colors=黑色

// 方式一：数组接收
@GetMapping("/product")
public String getProduct(String[] colors) {
    // colors = ["红色", "蓝色", "黑色"]
    System.out.println("选择的颜色：" + Arrays.toString(colors));
    return "商品详情";
}

// 方式二：集合接收
@GetMapping("/product")
public String getProduct(@RequestParam List<String> colors) {
    // colors = List.of("红色", "蓝色", "黑色")
    for (String color : colors) {
        System.out.println(color);
    }
    return "商品详情";
}
```

---

## 3. 🛤️ 路径变量PathVariable


### 3.1 什么是路径变量


**PathVariable**：把参数值直接嵌入到URL路径中，而不是放在问号后面。

```
传统QueryString方式：
/user/detail?id=100

RESTful PathVariable方式：
/user/100
       ↑
   参数值直接在路径中
```

**为什么要用路径变量？**
- ✅ URL更简洁、更语义化
- ✅ 符合RESTful API设计风格
- ✅ 搜索引擎更友好（SEO优化）

### 3.2 基本用法


```java
// URL: /user/100
@GetMapping("/user/{id}")
public String getUserById(@PathVariable Long id) {
    // {id} 是路径中的占位符
    // @PathVariable 把路径中的值绑定到参数id
    
    System.out.println("查询用户ID：" + id);
    return "用户详情";
}
```

**执行流程**：
```
请求：GET /user/100
       ↓
Spring识别到{id}占位符
       ↓
提取路径中的"100"
       ↓
转换为Long类型
       ↓
赋值给参数id
```

### 3.3 多个路径变量


```java
// URL: /order/2024/12/25
@GetMapping("/order/{year}/{month}/{day}")
public String getOrderByDate(
    @PathVariable int year,
    @PathVariable int month,
    @PathVariable int day
) {
    System.out.println("查询日期：" + year + "-" + month + "-" + day);
    return "订单列表";
}
```

### 3.4 指定路径变量名称


```java
// 当路径占位符名称与方法参数名不一致时
@GetMapping("/article/{articleId}")
public String getArticle(@PathVariable("articleId") Long id) {
    // articleId（路径中）映射到 id（方法参数）
    System.out.println("文章ID：" + id);
    return "文章详情";
}
```

### 3.5 路径变量与QueryString混用


```java
// URL: /product/123?color=红色&size=L
@GetMapping("/product/{id}")
public String getProduct(
    @PathVariable Long id,        // 从路径获取
    @RequestParam String color,   // 从QueryString获取
    @RequestParam String size     // 从QueryString获取
) {
    System.out.println("商品ID：" + id);
    System.out.println("颜色：" + color + "，尺寸：" + size);
    return "商品详情";
}
```

### 3.6 RESTful风格示例


```java
@RestController
@RequestMapping("/api/users")
public class UserController {
    
    // GET /api/users/100 - 查询用户
    @GetMapping("/{id}")
    public User getUser(@PathVariable Long id) {
        return userService.findById(id);
    }
    
    // PUT /api/users/100 - 更新用户
    @PutMapping("/{id}")
    public User updateUser(@PathVariable Long id, @RequestBody User user) {
        return userService.update(id, user);
    }
    
    // DELETE /api/users/100 - 删除用户
    @DeleteMapping("/{id}")
    public void deleteUser(@PathVariable Long id) {
        userService.delete(id);
    }
}
```

---

## 4. 📦 请求体RequestBody处理


### 4.1 什么是请求体


**请求体（Request Body）**：HTTP请求除了URL和请求头外，还有一个"主体部分"用来承载数据，这就是请求体。

```
HTTP请求结构示意图：

请求行     → POST /api/user HTTP/1.1
请求头     → Content-Type: application/json
           → Authorization: Bearer token123
           
空行（分隔符）
           
请求体     → {"username":"张三","age":25}
           ↑
        这部分就是Request Body
```

**什么时候用请求体？**
- ✅ 提交复杂数据结构（对象、数组）
- ✅ 数据量较大
- ✅ 需要加密传输
- ✅ POST、PUT、PATCH等方法

### 4.2 接收请求体数据


**前提**：前端发送JSON格式数据

```javascript
// 前端代码（示例）
fetch('/api/user', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({
        username: '张三',
        age: 25,
        email: 'zhangsan@example.com'
    })
});
```

**后端接收**：

```java
@PostMapping("/api/user")
public String createUser(@RequestBody User user) {
    // @RequestBody 告诉Spring：从请求体中读取JSON数据
    // 自动转换为User对象
    
    System.out.println("用户名：" + user.getUsername());
    System.out.println("年龄：" + user.getAge());
    System.out.println("邮箱：" + user.getEmail());
    
    return "创建成功";
}

// User类定义
public class User {
    private String username;
    private Integer age;
    private String email;
    // getter和setter方法...
}
```

> ⚠️ **重要**：使用`@RequestBody`需要添加JSON处理依赖（如Jackson），Spring Boot已自动包含

### 4.3 请求体与QueryString的区别


| 对比项 | **QueryString** | **RequestBody** |
|-------|----------------|----------------|
| **数据位置** | `URL后面` | `HTTP请求体中` |
| **可见性** | `浏览器地址栏可见` | `不可见，需工具查看` |
| **数据量** | `有长度限制(≈2KB)` | `无明显限制` |
| **数据类型** | `简单键值对` | `复杂JSON/XML对象` |
| **适用方法** | `GET等` | `POST、PUT、PATCH等` |
| **安全性** | `参数暴露，不安全` | `相对安全` |

---

## 5. 📄 JSON数据绑定


### 5.1 什么是JSON数据绑定


**JSON数据绑定**：就是把JSON格式的文本自动转换成Java对象的过程，Spring MVC帮我们自动完成。

```
客户端JSON字符串：
{
  "username": "张三",
  "age": 25
}
        ↓
   Spring自动转换
        ↓
Java对象：
User user = new User();
user.setUsername("张三");
user.setAge(25);
```

### 5.2 简单对象绑定


```java
// 接收单个用户对象
@PostMapping("/register")
public String register(@RequestBody User user) {
    // JSON自动绑定到User对象
    System.out.println(user.getUsername());
    return "注册成功";
}
```

**请求JSON**：
```json
{
  "username": "李四",
  "age": 30,
  "email": "lisi@example.com"
}
```

### 5.3 嵌套对象绑定


```java
// 订单对象包含用户信息
public class Order {
    private Long orderId;
    private User user;        // 嵌套用户对象
    private List<Product> products;  // 商品列表
    // getter/setter...
}

@PostMapping("/order")
public String createOrder(@RequestBody Order order) {
    System.out.println("订单号：" + order.getOrderId());
    System.out.println("用户：" + order.getUser().getUsername());
    System.out.println("商品数量：" + order.getProducts().size());
    return "订单创建成功";
}
```

**请求JSON**：
```json
{
  "orderId": 20240101,
  "user": {
    "username": "王五",
    "age": 28
  },
  "products": [
    {"name": "手机", "price": 3999},
    {"name": "耳机", "price": 299}
  ]
}
```

### 5.4 集合对象绑定


```java
// 批量创建用户
@PostMapping("/users/batch")
public String batchCreate(@RequestBody List<User> users) {
    System.out.println("批量创建：" + users.size() + " 个用户");
    for (User user : users) {
        System.out.println("- " + user.getUsername());
    }
    return "批量创建成功";
}
```

**请求JSON**：
```json
[
  {"username": "用户1", "age": 20},
  {"username": "用户2", "age": 22},
  {"username": "用户3", "age": 24}
]
```

### 5.5 Map接收动态数据


当数据结构不固定时，可以用Map接收：

```java
@PostMapping("/dynamic")
public String handleDynamic(@RequestBody Map<String, Object> data) {
    // 灵活处理不确定结构的数据
    System.out.println("接收到的数据：" + data);
    
    String name = (String) data.get("name");
    Integer age = (Integer) data.get("age");
    
    return "处理完成";
}
```

### 5.6 JSON字段名映射


**场景**：JSON字段名与Java属性名不一致

```java
import com.fasterxml.jackson.annotation.JsonProperty;

public class User {
    @JsonProperty("user_name")  // JSON中是user_name
    private String username;    // Java中是username
    
    @JsonProperty("user_age")
    private Integer age;
    
    // getter/setter...
}
```

**请求JSON**：
```json
{
  "user_name": "赵六",
  "user_age": 35
}
```

---

## 6. 📝 表单数据处理


### 6.1 什么是表单数据


**表单数据（Form Data）**：传统HTML表单提交的数据，格式类似QueryString，但放在请求体中。

```
表单提交流程：

HTML表单                     HTTP请求
<form>                 →   POST /register
  <input name="username">   Content-Type: application/x-www-form-urlencoded
  <input name="password">   
  <button>提交</button>      请求体：username=张三&password=123456
</form>
```

### 6.2 接收简单表单


```java
@PostMapping("/login")
public String login(String username, String password) {
    // 直接用方法参数接收，不需要@RequestParam
    // Spring自动从表单数据中匹配
    
    System.out.println("用户名：" + username);
    System.out.println("密码：" + password);
    return "登录成功";
}
```

### 6.3 表单绑定到对象


```java
// 用户注册表单
@PostMapping("/register")
public String register(User user) {
    // 表单字段名与User属性名匹配时，自动绑定
    // 不需要@RequestBody注解！
    
    System.out.println("注册用户：" + user.getUsername());
    return "注册成功";
}
```

**HTML表单示例**：
```html
<form action="/register" method="post">
  <input type="text" name="username" placeholder="用户名">
  <input type="number" name="age" placeholder="年龄">
  <input type="email" name="email" placeholder="邮箱">
  <button type="submit">注册</button>
</form>
```

### 6.4 复选框绑定


```java
public class UserPreference {
    private String username;
    private String[] hobbies;  // 复选框多选值
    // getter/setter...
}

@PostMapping("/preference")
public String savePreference(UserPreference pref) {
    System.out.println("用户：" + pref.getUsername());
    System.out.println("爱好：" + Arrays.toString(pref.getHobbies()));
    return "保存成功";
}
```

**HTML表单**：
```html
<form action="/preference" method="post">
  <input type="text" name="username">
  <label><input type="checkbox" name="hobbies" value="读书">读书</label>
  <label><input type="checkbox" name="hobbies" value="运动">运动</label>
  <label><input type="checkbox" name="hobbies" value="旅游">旅游</label>
  <button type="submit">提交</button>
</form>
```

### 6.5 表单与JSON的区别


| 对比项 | **表单数据** | **JSON数据** |
|-------|------------|------------|
| **Content-Type** | `application/x-www-form-urlencoded` | `application/json` |
| **数据格式** | `key=value&key2=value2` | `{"key":"value","key2":"value2"}` |
| **接收方式** | `直接参数或对象，无需注解` | `必须用@RequestBody` |
| **嵌套对象** | `不支持复杂嵌套` | `支持任意嵌套` |
| **典型场景** | `传统表单提交` | `前后端分离API` |

---

## 7. 📤 文件上传处理


### 7.1 什么是文件上传


**文件上传**：用户从本地选择文件（图片、文档等）提交到服务器的过程。

```
文件上传流程：

用户选择文件 → 浏览器读取文件 → HTTP请求携带文件数据 → 服务器接收保存

关键点：
- Content-Type: multipart/form-data（支持二进制数据）
- 文件数据在请求体中，以特殊格式分隔
```

### 7.2 单文件上传


**第一步：配置文件上传大小**（application.yml）

```yaml
spring:
  servlet:
    multipart:
      max-file-size: 10MB      # 单个文件最大10MB
      max-request-size: 50MB   # 整个请求最大50MB
```

**第二步：编写上传接口**

```java
@PostMapping("/upload")
public String upload(@RequestParam("file") MultipartFile file) {
    // MultipartFile：Spring提供的文件封装类
    
    if (file.isEmpty()) {
        return "请选择文件";
    }
    
    try {
        // 获取文件信息
        String fileName = file.getOriginalFilename();  // 原始文件名
        long fileSize = file.getSize();                // 文件大小（字节）
        String contentType = file.getContentType();    // 文件类型
        
        System.out.println("文件名：" + fileName);
        System.out.println("大小：" + fileSize + " 字节");
        System.out.println("类型：" + contentType);
        
        // 保存文件到服务器
        String savePath = "D:/uploads/" + fileName;
        file.transferTo(new File(savePath));
        
        return "上传成功：" + fileName;
        
    } catch (IOException e) {
        e.printStackTrace();
        return "上传失败";
    }
}
```

**前端表单**：
```html
<form action="/upload" method="post" enctype="multipart/form-data">
  <input type="file" name="file">
  <button type="submit">上传</button>
</form>
```

> ⚠️ **注意**：表单必须设置 `enctype="multipart/form-data"`

### 7.3 多文件上传


```java
@PostMapping("/upload/multiple")
public String uploadMultiple(@RequestParam("files") MultipartFile[] files) {
    // 数组接收多个文件
    
    if (files == null || files.length == 0) {
        return "请选择文件";
    }
    
    for (MultipartFile file : files) {
        if (!file.isEmpty()) {
            String fileName = file.getOriginalFilename();
            System.out.println("上传文件：" + fileName);
            
            try {
                file.transferTo(new File("D:/uploads/" + fileName));
            } catch (IOException e) {
                e.printStackTrace();
            }
        }
    }
    
    return "上传成功 " + files.length + " 个文件";
}
```

**前端表单**：
```html
<form action="/upload/multiple" method="post" enctype="multipart/form-data">
  <input type="file" name="files" multiple>
  <button type="submit">批量上传</button>
</form>
```

### 7.4 文件上传优化技巧


**技巧1：生成唯一文件名（避免覆盖）**

```java
@PostMapping("/upload")
public String upload(@RequestParam("file") MultipartFile file) {
    String originalName = file.getOriginalFilename();
    String extension = originalName.substring(originalName.lastIndexOf("."));
    
    // 使用UUID生成唯一文件名
    String uniqueName = UUID.randomUUID().toString() + extension;
    
    file.transferTo(new File("D:/uploads/" + uniqueName));
    return "上传成功：" + uniqueName;
}
```

**技巧2：限制文件类型**

```java
@PostMapping("/upload/image")
public String uploadImage(@RequestParam("file") MultipartFile file) {
    String contentType = file.getContentType();
    
    // 只允许图片类型
    if (!contentType.startsWith("image/")) {
        return "只能上传图片文件";
    }
    
    // 保存逻辑...
    return "图片上传成功";
}
```

**技巧3：按日期分类存储**

```java
@PostMapping("/upload")
public String upload(@RequestParam("file") MultipartFile file) {
    // 按日期创建文件夹：uploads/2024/01/25/
    String datePath = new SimpleDateFormat("yyyy/MM/dd").format(new Date());
    String dirPath = "D:/uploads/" + datePath;
    
    File dir = new File(dirPath);
    if (!dir.exists()) {
        dir.mkdirs();  // 创建目录
    }
    
    String fileName = UUID.randomUUID() + "_" + file.getOriginalFilename();
    file.transferTo(new File(dirPath + "/" + fileName));
    
    return "文件保存到：" + datePath + "/" + fileName;
}
```

---

## 8. 🔗 复合请求处理


### 8.1 什么是复合请求


**复合请求**：一个请求中同时包含多种类型的数据，比如：
- 文件 + 普通参数
- JSON + 路径变量
- 表单 + QueryString

```
典型场景示例：

上传用户头像 + 用户信息
/upload/avatar?userId=100     ← QueryString参数
[文件数据] + {name: "张三"}    ← 文件 + JSON
```

### 8.2 文件 + 表单参数


```java
@PostMapping("/upload/profile")
public String uploadProfile(
    @RequestParam("avatar") MultipartFile avatar,  // 文件
    @RequestParam String username,                 // 普通参数
    @RequestParam Integer age
) {
    System.out.println("用户名：" + username);
    System.out.println("年龄：" + age);
    System.out.println("头像文件：" + avatar.getOriginalFilename());
    
    // 保存文件和用户信息...
    return "上传成功";
}
```

**前端表单**：
```html
<form action="/upload/profile" method="post" enctype="multipart/form-data">
  <input type="file" name="avatar">
  <input type="text" name="username" placeholder="用户名">
  <input type="number" name="age" placeholder="年龄">
  <button type="submit">提交</button>
</form>
```

### 8.3 路径变量 + 请求体


```java
// PUT /api/user/100（更新用户）
@PutMapping("/api/user/{id}")
public String updateUser(
    @PathVariable Long id,              // 从路径获取ID
    @RequestBody User user              // 从请求体获取用户数据
) {
    System.out.println("更新用户ID：" + id);
    System.out.println("新数据：" + user.getUsername());
    
    // 更新逻辑...
    return "更新成功";
}
```

**请求示例**：
```
PUT /api/user/100
Content-Type: application/json

{
  "username": "新名字",
  "age": 30
}
```

### 8.4 QueryString + 请求体


```java
@PostMapping("/search")
public String search(
    @RequestParam(defaultValue = "1") int page,    // QueryString
    @RequestParam(defaultValue = "10") int size,   // QueryString
    @RequestBody SearchCondition condition         // 请求体
) {
    System.out.println("第" + page + "页，每页" + size + "条");
    System.out.println("搜索条件：" + condition.getKeyword());
    
    // 搜索逻辑...
    return "搜索结果";
}

public class SearchCondition {
    private String keyword;
    private String category;
    private Integer minPrice;
    private Integer maxPrice;
    // getter/setter...
}
```

**请求示例**：
```
POST /search?page=2&size=20
Content-Type: application/json

{
  "keyword": "手机",
  "category": "电子产品",
  "minPrice": 1000,
  "maxPrice": 5000
}
```

### 8.5 文件 + JSON数据


> 💡 **特殊处理**：同时上传文件和JSON，需要用`@RequestPart`

```java
@PostMapping("/upload/product")
public String uploadProduct(
    @RequestPart("image") MultipartFile image,       // 文件部分
    @RequestPart("product") Product product          // JSON部分
) {
    System.out.println("商品名称：" + product.getName());
    System.out.println("商品图片：" + image.getOriginalFilename());
    
    // 保存商品和图片...
    return "上传成功";
}
```

**前端代码（使用FormData）**：
```javascript
const formData = new FormData();

// 添加文件
formData.append('image', fileInput.files[0]);

// 添加JSON数据（需要转为Blob）
const product = {name: '新商品', price: 999};
formData.append('product', new Blob(
    [JSON.stringify(product)], 
    {type: 'application/json'}
));

fetch('/upload/product', {
    method: 'POST',
    body: formData
});
```

### 8.6 复合请求最佳实践


**场景分析表**：

| 场景 | **推荐方案** | **示例** |
|------|------------|---------|
| 文件+简单参数 | `@RequestParam` | 头像上传+用户名 |
| 文件+复杂对象 | `@RequestPart` | 商品图片+商品JSON |
| RESTful资源操作 | `PathVariable + RequestBody` | PUT /user/100 + JSON |
| 分页查询+条件 | `QueryString + RequestBody` | page=1 + 搜索条件JSON |
| 简单表单提交 | `直接参数绑定` | 登录表单 |

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的知识点


**🔸 参数接收方式对比**

```
📍 QueryString (@RequestParam)
   - 位置：URL?key=value
   - 用途：简单参数、分页、筛选
   - 特点：可见、可分享、有长度限制

📍 路径变量 (@PathVariable)  
   - 位置：URL路径中 /user/{id}
   - 用途：资源标识、RESTful API
   - 特点：语义化、SEO友好

📍 请求体 (@RequestBody)
   - 位置：HTTP请求体
   - 用途：复杂对象、JSON数据
   - 特点：不可见、无长度限制、POST/PUT使用

📍 表单数据 (直接参数)
   - 位置：请求体（表单格式）
   - 用途：传统表单提交
   - 特点：不需要@RequestBody

📍 文件上传 (MultipartFile)
   - 位置：请求体（multipart格式）
   - 用途：上传文件
   - 特点：需要特殊Content-Type
```

### 9.2 关键注解使用场景


| 注解 | **使用场景** | **必要性** |
|-----|------------|-----------|
| `@RequestParam` | `QueryString参数` | 可选（参数名匹配时） |
| `@PathVariable` | `URL路径变量` | 必须 |
| `@RequestBody` | `JSON/XML请求体` | 必须 |
| `@RequestPart` | `文件+JSON复合请求` | 必须 |
| 无注解 | `表单数据` | 自动绑定 |

### 9.3 常见问题与解决


**❓ 问题1：为什么接收不到参数？**

```
检查清单：
✅ 参数名是否匹配（大小写敏感）
✅ 是否缺少必要注解（@RequestBody、@PathVariable）
✅ Content-Type是否正确（JSON需要application/json）
✅ 是否有参数转换错误（类型不匹配）
```

**❓ 问题2：文件上传失败？**

```
检查清单：
✅ 表单是否设置 enctype="multipart/form-data"
✅ 文件大小是否超过配置限制
✅ 参数名是否与 @RequestParam("file") 匹配
✅ 保存路径是否有写入权限
```

**❓ 问题3：JSON绑定失败？**

```
检查清单：
✅ 是否添加 @RequestBody 注解
✅ 是否引入JSON处理库（Jackson）
✅ Java类是否有getter/setter方法
✅ JSON字段名是否与属性名匹配
```

### 9.4 最佳实践建议


**✅ 推荐做法**：
- 简单参数用QueryString，复杂数据用JSON
- RESTful API优先使用PathVariable
- 敏感数据（密码）必须用POST + 请求体
- 文件名用UUID避免冲突
- 设置合理的文件大小限制

**❌ 避免的做法**：
- 密码等敏感信息放在URL中
- 大量数据用QueryString传递
- 忘记校验文件类型和大小
- 直接使用用户上传的文件名

### 9.5 技术选型决策树


```
需要传递参数？
    ↓
简单参数（1-3个）？
    ↓ 是
    使用QueryString
        ↓
        GET请求 → @RequestParam
        
    ↓ 否
复杂对象/数组？
    ↓ 是
    使用请求体
        ↓
        JSON格式 → @RequestBody
        表单格式 → 直接参数绑定
        
    ↓
需要上传文件？
    ↓ 是
    MultipartFile
        ↓
        单文件 → @RequestParam
        文件+JSON → @RequestPart
```

**核心记忆口诀**：
- 简单参数问号传，复杂对象请求体
- 路径变量语义化，文件上传需特配
- JSON必用RequestBody，表单直接能绑定
- 注解选择看场景，类型匹配很关键