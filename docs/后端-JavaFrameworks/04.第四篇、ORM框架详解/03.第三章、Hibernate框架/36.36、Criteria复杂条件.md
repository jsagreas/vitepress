---
title: 36、Criteria复杂条件
---
## 📚 目录

1. [复杂条件查询概述](#1-复杂条件查询概述)
2. [AND条件组合](#2-AND条件组合)
3. [OR条件组合](#3-OR条件组合)
4. [逻辑运算符详解](#4-逻辑运算符详解)
5. [比较运算符应用](#5-比较运算符应用)
6. [范围查询](#6-范围查询)
7. [模糊查询](#7-模糊查询)
8. [空值判断](#8-空值判断)
9. [集合查询](#9-集合查询)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🎯 复杂条件查询概述


### 1.1 什么是复杂条件


**💡 生活中的类比**
```
想象你在购物网站筛选商品：

简单条件：
"找价格是100元的商品" → 一个条件

复杂条件：
"找价格在100-500元之间 并且 品牌是Apple 或者 Samsung的商品"
→ 多个条件组合，有AND、OR的逻辑关系
```

**🔸 Criteria复杂条件的本质**
- **组合多个查询条件**：就像购物筛选，可以同时设置多个条件
- **使用逻辑关系**：用AND（并且）、OR（或者）连接条件
- **构建灵活查询**：根据实际需求自由组合

### 1.2 为什么需要复杂条件


**📊 实际业务场景**
```
场景1：用户查询
"查找年龄在20-30岁之间，并且来自北京或上海的用户"

场景2：订单筛选  
"查找金额大于1000元，并且状态是已支付或已发货的订单"

场景3：商品搜索
"查找价格小于500元，并且名称包含'手机'，并且库存不为空的商品"

这些都需要复杂条件来实现！
```

### 1.3 复杂条件的核心工具


**🔧 Criteria提供的工具**

| 工具类 | **作用** | **通俗理解** |
|--------|---------|-------------|
| `Conjunction` | AND条件容器 | "并且"的关系，所有条件都要满足 |
| `Disjunction` | OR条件容器 | "或者"的关系，满足任一条件即可 |
| `Restrictions` | 条件工具类 | 提供各种比较、范围、模糊查询方法 |

---

## 2. ⚙️ AND条件组合


### 2.1 Conjunction的作用


**🔸 什么是Conjunction**
```
Conjunction = 连接词（并且）的意思
在Criteria中，它就是一个"条件容器"，装的都是AND关系的条件

理解要点：
- 所有条件必须同时满足
- 就像"又要...又要..."的关系
- 条件越多，结果越精确
```

**💡 生活类比**
```
招聘要求：
Conjunction and = new Conjunction();
and.add(学历 >= 本科);         // 条件1
and.add(年龄 < 35);           // 条件2  
and.add(工作经验 >= 3年);      // 条件3

必须同时满足这3个条件才能应聘！
```

### 2.2 AND条件的使用


**📝 基础用法**
```java
// 需求：查找年龄大于20并且性别为男的用户
Criteria criteria = session.createCriteria(User.class);

// 方式1：直接用add（默认就是AND关系）
criteria.add(Restrictions.gt("age", 20));      // 年龄>20
criteria.add(Restrictions.eq("gender", "男")); // 性别=男

// 方式2：使用Conjunction明确表示AND
Conjunction and = Restrictions.conjunction();  // 创建AND容器
and.add(Restrictions.gt("age", 20));
and.add(Restrictions.eq("gender", "男"));
criteria.add(and);

List<User> users = criteria.list();
```

**🔍 执行的SQL理解**
```sql
-- Criteria最终生成的SQL
SELECT * FROM user 
WHERE age > 20 AND gender = '男';

-- 逻辑：两个条件用AND连接，必须同时满足
```

### 2.3 多条件AND组合


**📌 复杂AND场景**
```java
// 需求：查找符合多个条件的订单
// 条件：金额>1000 AND 状态=已支付 AND 创建时间>某个日期
Criteria criteria = session.createCriteria(Order.class);

Conjunction and = Restrictions.conjunction();
and.add(Restrictions.gt("amount", 1000));              // 金额>1000
and.add(Restrictions.eq("status", "已支付"));          // 状态=已支付
and.add(Restrictions.gt("createTime", someDate));      // 时间>某日期

criteria.add(and);
List<Order> orders = criteria.list();
```

**💭 思考过程**
```
1. 创建一个AND条件容器
2. 把所有必须满足的条件都加进去
3. 把这个容器整体加到Criteria中
4. 执行查询，返回同时满足所有条件的结果
```

---

## 3. 🔀 OR条件组合


### 3.1 Disjunction的作用


**🔸 什么是Disjunction**
```
Disjunction = 析取（或者）的意思
在Criteria中，它是"OR条件容器"，装的都是OR关系的条件

理解要点：
- 满足任意一个条件即可
- 就像"要么...要么..."的关系  
- 条件越多，结果范围越大
```

**💡 生活类比**
```
出行方式选择：
Disjunction or = new Disjunction();
or.add(坐地铁);      // 选项1
or.add(坐公交);      // 选项2
or.add(打车);        // 选项3

三个选一个就能出行！
```

### 3.2 OR条件的使用


**📝 基础用法**
```java
// 需求：查找来自北京或上海的用户
Criteria criteria = session.createCriteria(User.class);

// 使用Disjunction表示OR关系
Disjunction or = Restrictions.disjunction();  // 创建OR容器
or.add(Restrictions.eq("city", "北京"));      // 城市=北京
or.add(Restrictions.eq("city", "上海"));      // 城市=上海

criteria.add(or);
List<User> users = criteria.list();
```

**🔍 执行的SQL理解**
```sql
-- Criteria生成的SQL
SELECT * FROM user 
WHERE city = '北京' OR city = '上海';

-- 逻辑：满足任一条件即可返回
```

### 3.3 多条件OR组合


**📌 复杂OR场景**
```java
// 需求：查找多种状态的订单
// 条件：状态=待支付 OR 状态=已支付 OR 状态=已发货
Criteria criteria = session.createCriteria(Order.class);

Disjunction or = Restrictions.disjunction();
or.add(Restrictions.eq("status", "待支付"));
or.add(Restrictions.eq("status", "已支付"));
or.add(Restrictions.eq("status", "已发货"));

criteria.add(or);
List<Order> orders = criteria.list();
```

---

## 4. 🔗 逻辑运算符详解


### 4.1 AND与OR的嵌套


**🔸 嵌套逻辑的含义**
```
实际业务常常需要"既有AND又有OR"的复杂组合

例如：(条件A AND 条件B) OR (条件C AND 条件D)
```

**💡 实际案例**
```
需求：查找用户
(年龄>20 AND 城市=北京) OR (年龄>30 AND 城市=上海)

翻译：
要么是"20岁以上的北京用户"
要么是"30岁以上的上海用户"
```

**📝 代码实现**
```java
Criteria criteria = session.createCriteria(User.class);

// 构建第一组AND条件：年龄>20 AND 城市=北京
Conjunction and1 = Restrictions.conjunction();
and1.add(Restrictions.gt("age", 20));
and1.add(Restrictions.eq("city", "北京"));

// 构建第二组AND条件：年龄>30 AND 城市=上海
Conjunction and2 = Restrictions.conjunction();
and2.add(Restrictions.gt("age", 30));
and2.add(Restrictions.eq("city", "上海"));

// 用OR连接两组AND条件
Disjunction or = Restrictions.disjunction();
or.add(and1);  // 加入第一组
or.add(and2);  // 加入第二组

criteria.add(or);
List<User> users = criteria.list();
```

**🔍 SQL理解**
```sql
SELECT * FROM user 
WHERE (age > 20 AND city = '北京') 
   OR (age > 30 AND city = '上海');
```

### 4.2 逻辑运算符组合原则


**📌 组合策略**
```
1️⃣ 先分析需求的逻辑结构
   - 找出哪些是AND关系
   - 找出哪些是OR关系
   - 确定嵌套层次

2️⃣ 从内到外构建
   - 先构建最内层的条件组
   - 再用逻辑运算符连接
   - 最后加到Criteria中

3️⃣ 使用括号思维
   - AND条件 = 一个括号
   - OR条件 = 另一个括号
   - 用容器体现层次关系
```

---

## 5. 📊 比较运算符应用


### 5.1 常用比较运算符


**🔸 Restrictions提供的比较方法**

| 方法 | **含义** | **SQL对应** | **示例** |
|------|---------|------------|----------|
| `eq()` | 等于 | `=` | `eq("age", 20)` → age=20 |
| `ne()` | 不等于 | `!=` | `ne("status", "删除")` → status!='删除' |
| `gt()` | 大于 | `>` | `gt("price", 100)` → price>100 |
| `ge()` | 大于等于 | `>=` | `ge("score", 60)` → score>=60 |
| `lt()` | 小于 | `<` | `lt("age", 18)` → age<18 |
| `le()` | 小于等于 | `<=` | `le("stock", 10)` → stock<=10 |

### 5.2 比较运算符实战


**📝 等值查询**
```java
// 查找年龄等于25的用户
criteria.add(Restrictions.eq("age", 25));
// SQL: WHERE age = 25
```

**📝 不等值查询**
```java
// 查找状态不是"已删除"的记录
criteria.add(Restrictions.ne("status", "已删除"));
// SQL: WHERE status != '已删除'
```

**📝 范围比较**
```java
// 查找价格大于100的商品
criteria.add(Restrictions.gt("price", 100));
// SQL: WHERE price > 100

// 查找库存小于等于10的商品（快售罄）
criteria.add(Restrictions.le("stock", 10));
// SQL: WHERE stock <= 10
```

---

## 6. 📏 范围查询


### 6.1 between范围查询


**🔸 什么是between**
```
between = "在...之间"
用于查询某个字段在指定范围内的数据

包含边界值：between 10 and 20 → 包括10和20
```

**💡 生活场景**
```
查找价格在100-500元之间的商品
查找年龄在18-35岁之间的用户
查找某个时间段内的订单
```

**📝 基本用法**
```java
// 查找价格在100-500之间的商品
criteria.add(Restrictions.between("price", 100, 500));
// SQL: WHERE price BETWEEN 100 AND 500
```

### 6.2 范围查询组合


**📌 常见组合场景**
```java
// 场景：查找某个价格区间且库存充足的商品
criteria.add(Restrictions.between("price", 100, 500));  // 价格范围
criteria.add(Restrictions.gt("stock", 0));              // 有库存
// SQL: WHERE price BETWEEN 100 AND 500 AND stock > 0
```

**📊 时间范围查询**
```java
// 查找某个时间段内的订单
Date startDate = ...; // 开始时间
Date endDate = ...;   // 结束时间

criteria.add(Restrictions.between("createTime", startDate, endDate));
// SQL: WHERE create_time BETWEEN '开始时间' AND '结束时间'
```

---

## 7. 🔍 模糊查询


### 7.1 like模糊匹配


**🔸 什么是模糊查询**
```
模糊查询 = 不需要完全匹配，只要包含关键词即可

就像搜索引擎：
输入"手机" → 能找到"苹果手机"、"华为手机"、"手机壳"等

SQL中用%表示任意字符：
%手机   → 以"手机"结尾
手机%   → 以"手机"开头  
%手机%  → 包含"手机"
```

**💡 模糊查询的通配符**
```
%  → 匹配任意多个字符（包括0个）
_  → 匹配单个字符

示例：
"张%"   → 张三、张四、张三丰
"_三"   → 张三、李三（只匹配两个字）
"%手机%" → 苹果手机、手机壳、买手机
```

### 7.2 like查询的使用


**📝 基本模糊查询**
```java
// 查找姓"张"的用户
criteria.add(Restrictions.like("name", "张%"));
// SQL: WHERE name LIKE '张%'

// 查找名字包含"三"的用户  
criteria.add(Restrictions.like("name", "%三%"));
// SQL: WHERE name LIKE '%三%'
```

**📌 实际应用场景**
```java
// 商品搜索：查找商品名包含"手机"的记录
criteria.add(Restrictions.like("productName", "%手机%"));

// 邮箱搜索：查找QQ邮箱用户
criteria.add(Restrictions.like("email", "%@qq.com"));

// 地址筛选：查找地址在"朝阳区"的用户
criteria.add(Restrictions.like("address", "%朝阳区%"));
```

### 7.3 大小写敏感问题


**⚠️ 注意事项**
```java
// 默认like是区分大小写的（取决于数据库配置）

// 不区分大小写的模糊查询（推荐）
criteria.add(Restrictions.ilike("name", "%zhang%"));
// ilike = insensitive like（不敏感的like）

对比：
like("name", "Zhang")  → 只能匹配"Zhang"
ilike("name", "Zhang") → 能匹配"zhang"、"ZHANG"、"Zhang"
```

---

## 8. ❓ 空值判断


### 8.1 isNull和isNotNull


**🔸 空值的含义**
```
数据库中的NULL = 没有值、未知、不存在

与空字符串的区别：
NULL    → 没有值
""      → 空字符串，是一个值（长度为0的字符串）

判断：
IS NULL     → 字段是空值
IS NOT NULL → 字段不是空值
```

**💡 实际场景**
```
检查必填字段：
- 查找邮箱为空的用户（需要补充信息）
- 查找有头像的用户（头像不为空）
- 查找已分配的订单（分配人不为空）
```

### 8.2 空值查询使用


**📝 基本用法**
```java
// 查找邮箱为空的用户（未填写邮箱）
criteria.add(Restrictions.isNull("email"));
// SQL: WHERE email IS NULL

// 查找邮箱不为空的用户（已填写邮箱）
criteria.add(Restrictions.isNotNull("email"));
// SQL: WHERE email IS NOT NULL
```

**📌 实际应用**
```java
// 场景1：查找未删除的记录（删除时间为空）
criteria.add(Restrictions.isNull("deleteTime"));

// 场景2：查找有备注的订单（备注不为空）
criteria.add(Restrictions.isNotNull("remark"));

// 场景3：查找未分配的任务（负责人为空）
criteria.add(Restrictions.isNull("assignUser"));
```

### 8.3 空值处理组合


**🔗 与其他条件组合**
```java
// 查找价格>100且描述不为空的商品
criteria.add(Restrictions.gt("price", 100));
criteria.add(Restrictions.isNotNull("description"));
// SQL: WHERE price > 100 AND description IS NOT NULL
```

---

## 9. 📦 集合查询


### 9.1 in集合包含


**🔸 什么是in查询**
```
in = 在...之中
用于判断字段值是否在指定的集合中

类比：
判断"你的年龄是否在[18, 20, 25, 30]中"
判断"订单状态是否在[已支付, 已发货]中"
```

**💡 in查询的价值**
```
不用in的写法（繁琐）：
status='已支付' OR status='已发货' OR status='已完成'

用in的写法（简洁）：
status IN ('已支付', '已发货', '已完成')
```

### 9.2 in查询的使用


**📝 基本用法**
```java
// 查找指定城市的用户
String[] cities = {"北京", "上海", "广州", "深圳"};
criteria.add(Restrictions.in("city", cities));
// SQL: WHERE city IN ('北京', '上海', '广州', '深圳')
```

**📌 实际应用场景**
```java
// 场景1：查找多个状态的订单
List<String> statusList = Arrays.asList("已支付", "已发货", "已完成");
criteria.add(Restrictions.in("status", statusList));

// 场景2：查找指定ID的记录
Integer[] ids = {1, 3, 5, 7, 9};
criteria.add(Restrictions.in("id", ids));

// 场景3：查找VIP等级的用户
criteria.add(Restrictions.in("level", new String[]{"VIP1", "VIP2", "VIP3"}));
```

### 9.3 集合查询优化


**⚡ 使用建议**
```
✅ 适合场景：
- 集合元素较少（< 1000个）
- 经常需要查询多个固定值
- 替代多个OR条件

❌ 不适合场景：
- 集合元素过多（会导致SQL过长）
- 可以用范围查询替代的情况

优化方案：
如果集合很大 → 考虑用临时表或分批查询
如果是连续值 → 考虑用between代替
```

---

## 10. 📋 核心要点总结


### 10.1 复杂条件构建思路


**🎯 构建流程**
```
步骤1：分析需求
├─ 明确查询目标
├─ 识别查询条件
└─ 确定逻辑关系

步骤2：拆解条件
├─ 哪些是AND关系（必须同时满足）
├─ 哪些是OR关系（满足一个即可）
└─ 是否有嵌套逻辑

步骤3：编写代码
├─ 创建对应的容器（Conjunction/Disjunction）
├─ 添加具体条件（Restrictions.xxx）
└─ 组合到Criteria中

步骤4：测试验证
└─ 查看生成的SQL是否符合预期
```

### 10.2 条件工具速查


**🔧 核心方法总结**

| 类别 | **方法** | **作用** | **记忆要点** |
|------|---------|---------|-------------|
| **逻辑组合** | `conjunction()` | AND容器 | 所有条件都要满足 |
| | `disjunction()` | OR容器 | 满足任一即可 |
| **比较运算** | `eq()` | 等于 | equal的缩写 |
| | `ne()` | 不等于 | not equal |
| | `gt()` | 大于 | greater than |
| | `lt()` | 小于 | less than |
| **范围查询** | `between()` | 区间 | 包含边界值 |
| | `in()` | 集合包含 | 替代多个OR |
| **模糊查询** | `like()` | 模糊匹配 | 配合%使用 |
| | `ilike()` | 忽略大小写 | insensitive |
| **空值判断** | `isNull()` | 为空 | IS NULL |
| | `isNotNull()` | 不为空 | IS NOT NULL |

### 10.3 实战经验总结


**💡 使用建议**
```
1️⃣ 条件构建原则
   - 先简后繁：从简单条件开始，逐步组合
   - 层次清晰：用好AND/OR容器，避免混乱
   - 可读性优先：条件复杂时添加注释

2️⃣ 性能优化建议
   - 优先使用精确查询（eq、in）
   - 模糊查询避免前缀%（影响索引）
   - in集合不要过大（< 1000个元素）
   - 合理使用索引字段作为条件

3️⃣ 常见错误避免
   - 注意null的判断（用isNull，不是eq(null)）
   - 字符串大小写敏感性（考虑用ilike）
   - between是包含边界的（闭区间）
   - 多条件组合要加括号思维
```

**🎓 学习路线**
```
第1阶段：掌握基础条件
└─ eq、ne、gt、lt等比较运算

第2阶段：理解逻辑组合
└─ Conjunction(AND)、Disjunction(OR)

第3阶段：灵活运用高级查询
└─ 范围、模糊、空值、集合查询

第4阶段：构建复杂业务查询
└─ 多条件嵌套、动态条件组合
```

### 10.4 记忆口诀


**📝 核心记忆**
```
复杂条件不慌张，AND和OR来帮忙
Conjunction并且关系，Disjunction或者思想

等于不等eq和ne，大于小于gt和lt
范围查询用between，模糊搜索找like帮

空值判断isNull用，集合包含in最行
嵌套逻辑要清晰，括号容器分层装

条件工具Restrictions，方法丰富任你选
SQL生成要验证，性能优化记心间
```

---

**🎯 下一步学习**
- 掌握了复杂条件构建后，可以学习：
  - Criteria的排序和分页
  - 投影查询（只查询部分字段）
  - 关联查询（多表关联）
  - 动态查询（根据参数动态构建条件）