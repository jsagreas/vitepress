---
title: 6、实体类设计规范
---
## 📚 目录

1. [什么是实体映射](#1-什么是实体映射)
2. [POJO设计规范](#2-pojo设计规范)
3. [实体注解详解](#3-实体注解详解)
4. [主键配置策略](#4-主键配置策略)
5. [字段映射配置](#5-字段映射配置)
6. [类与表映射原则](#6-类与表映射原则)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 什么是实体映射


### 1.1 通俗理解


🌰 **生活类比**: 想象你要把现实生活中的"学生信息"存到电脑里：
```
现实世界的学生               数据库里的表
姓名：张三        ←→        name字段
年龄：20岁        ←→        age字段  
学号：2024001     ←→        student_id字段
```

**实体映射就是建立这座桥梁**，让Java对象和数据库表能互相转换。

### 1.2 核心概念


**实体映射**：把Java类（实体类）和数据库表对应起来的过程

```
Java世界                数据库世界
  
Student类        ←映射→     student表
├─ id属性        ←→         id列
├─ name属性      ←→         name列  
└─ age属性       ←→         age列
```

> 💡 **简单理解**  
> Hibernate帮你做"翻译官"的工作：你操作Java对象，它自动帮你操作数据库表

### 1.3 为什么需要实体映射


**传统JDBC方式的痛点**：
```java
// 每次查询都要手动转换
ResultSet rs = stmt.executeQuery("SELECT * FROM student");
while(rs.next()) {
    Student s = new Student();
    s.setId(rs.getInt("id"));        // 手动一个个取值
    s.setName(rs.getString("name"));  // 容易出错
    s.setAge(rs.getInt("age"));       // 重复劳动
}
```

**Hibernate方式**：
```java
// 自动映射，一行搞定
Student student = session.get(Student.class, 1);
```

⚡ **好处显而易见**：
- 不用写SQL语句
- 自动类型转换
- 减少重复代码
- 降低出错概率

---

## 2. 📝 POJO设计规范


### 2.1 什么是POJO


**POJO** = Plain Old Java Object（普通的老式Java对象）

🧠 **记忆方式**: 就是最普通的Java类，没有继承特殊的类，没有实现特殊的接口

**POJO的特点**：
```
✅ 有私有属性
✅ 有getter/setter方法
✅ 有无参构造器
✅ 可序列化（建议）
❌ 不继承框架类
❌ 不实现框架接口
```

### 2.2 标准实体类示例


```java
// 一个符合规范的实体类
public class Student {
    
    // 1. 私有属性
    private Integer id;
    private String name;
    private Integer age;
    
    // 2. 无参构造器（必须有）
    public Student() {}
    
    // 3. 有参构造器（可选）
    public Student(String name, Integer age) {
        this.name = name;
        this.age = age;
    }
    
    // 4. getter和setter方法
    public Integer getId() {
        return id;
    }
    
    public void setId(Integer id) {
        this.id = id;
    }
    
    // ... 其他getter/setter
}
```

### 2.3 设计规范要点


| 规范项 | **要求** | **原因说明** |
|--------|---------|-------------|
| 🔸 **无参构造器** | `必须提供` | `Hibernate用反射创建对象需要` |
| 🔸 **属性私有化** | `private修饰` | `封装性，通过方法访问` |
| 🔸 **提供getter/setter** | `所有属性都要有` | `Hibernate通过它们读写数据` |
| 🔸 **实现Serializable** | `建议实现` | `支持序列化，分布式环境需要` |
| 🔸 **避免final类** | `不用final修饰类` | `Hibernate可能需要创建代理` |

> ⚠️ **新手易错点**  
> 如果忘记写无参构造器，Hibernate会报错：`No default constructor for entity`

---

## 3. 🏷️ 实体注解详解


### 3.1 @Entity注解


**作用**: 告诉Hibernate这是一个实体类

```java
@Entity  // 标记为实体类
public class Student {
    // ...
}
```

🔍 **深入理解**：
- 加了`@Entity`，Hibernate才会管理这个类
- 没加的话，就是普通Java类，Hibernate不认识

**属性说明**：
```java
@Entity(name = "StudentEntity")  // 指定实体名称，默认用类名
```

> 💡 **什么时候需要指定name？**  
> 当你的类名和你想要的实体名不一样时，比如类名是`StudentInfo`，但你想实体名叫`Student`

### 3.2 @Table注解


**作用**: 指定对应的数据库表

```java
@Entity
@Table(name = "t_student")  // 对应数据库的t_student表
public class Student {
    // ...
}
```

**完整属性配置**：
```java
@Table(
    name = "t_student",           // 表名
    schema = "school_db",         // 数据库schema
    catalog = "my_catalog",       // 数据库catalog
    uniqueConstraints = {         // 唯一约束
        @UniqueConstraint(columnNames = {"name", "age"})
    }
)
```

**常用场景对比**：

```
场景1：类名和表名一致
@Entity
public class Student { }        → 映射到student表（自动转换）

场景2：类名和表名不一致  
@Entity
@Table(name = "t_student")
public class Student { }        → 映射到t_student表（手动指定）

场景3：表有前缀
@Entity
@Table(name = "sys_user")
public class User { }           → 映射到sys_user表
```

> 🎯 **最佳实践**  
> 建议总是明确指定`@Table`，这样代码更清晰，不依赖默认规则

---

## 4. 🔑 主键配置策略


### 4.1 @Id注解基础


**作用**: 标记哪个属性是主键

```java
@Entity
@Table(name = "t_student")
public class Student {
    
    @Id  // 标记为主键
    private Integer id;
    
    private String name;
}
```

🔍 **为什么需要主键？**
- 数据库用主键唯一标识每条记录
- Hibernate用主键查询、更新、删除数据
- 没有主键的表Hibernate无法正常工作

### 4.2 主键生成策略


**@GeneratedValue注解**: 指定主键如何生成

#### 策略1：AUTO（自动选择）


```java
@Id
@GeneratedValue(strategy = GenerationType.AUTO)
private Integer id;
```

🧠 **通俗理解**: 让Hibernate自己决定用什么方式生成主键

#### 策略2：IDENTITY（自增）


```java
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Integer id;
```

**工作原理**：
```
插入数据时：
你的代码：student.setName("张三")  → 不设置id
数据库：  INSERT INTO student(name) VALUES('张三')
数据库自动生成：id = 1
Hibernate回填：student.setId(1)
```

💡 **适用场景**: MySQL的AUTO_INCREMENT，SQL Server的IDENTITY

#### 策略3：SEQUENCE（序列）


```java
@Id
@GeneratedValue(strategy = GenerationType.SEQUENCE, 
                generator = "student_seq")
@SequenceGenerator(name = "student_seq", 
                   sequenceName = "student_id_seq",
                   allocationSize = 1)
private Integer id;
```

**工作原理**：
```
数据库序列对象
1. 创建序列：CREATE SEQUENCE student_id_seq START WITH 1
2. 获取下一个值：SELECT student_id_seq.NEXTVAL
3. 插入数据：INSERT INTO student(id, name) VALUES(1, '张三')
```

💡 **适用场景**: Oracle、PostgreSQL数据库

#### 策略4：TABLE（表生成器）


```java
@Id
@GeneratedValue(strategy = GenerationType.TABLE, 
                generator = "student_gen")
@TableGenerator(name = "student_gen",
                table = "id_generator",
                pkColumnName = "gen_name",
                valueColumnName = "gen_value",
                allocationSize = 1)
private Integer id;
```

**工作原理**：
```
专门的ID生成表：
id_generator表
+----------+----------+
| gen_name | gen_value|
+----------+----------+
| student  |    5     |  ← 当前student表的最大id是5
+----------+----------+

生成新ID时：
1. 查询：SELECT gen_value FROM id_generator WHERE gen_name='student'
2. 更新：UPDATE id_generator SET gen_value=6 WHERE gen_name='student'
3. 返回：新的id=6
```

### 4.3 策略选择指南


| 策略 | **数据库支持** | **性能** | **推荐场景** |
|------|--------------|---------|-------------|
| `IDENTITY` | `MySQL、SQL Server` | `⭐⭐⭐⭐` | `单数据库，要求简单` |
| `SEQUENCE` | `Oracle、PostgreSQL` | `⭐⭐⭐⭐⭐` | `高并发，Oracle数据库` |
| `TABLE` | `所有数据库` | `⭐⭐⭐` | `多数据库兼容需求` |
| `AUTO` | `自动选择` | `取决于选择` | `快速开发，不关心细节` |

> 🎯 **新手建议**  
> MySQL用IDENTITY，Oracle用SEQUENCE，最简单直接

---

## 5. 📋 字段映射配置


### 5.1 @Column注解详解


**作用**: 精确控制属性如何映射到数据库字段

```java
@Column(
    name = "student_name",     // 数据库列名
    length = 50,               // 字段长度
    nullable = false,          // 是否允许为空
    unique = true,             // 是否唯一
    columnDefinition = "VARCHAR(50) DEFAULT 'Unknown'"  // 自定义SQL
)
private String name;
```

### 5.2 常用属性说明


#### name属性


```java
// 属性名和列名不一致时使用
@Column(name = "user_name")
private String userName;      // Java属性是userName
                             // 数据库列是user_name
```

**命名转换对比**：
```
不加@Column：
Java: userName     → 数据库: user_name（驼峰转下划线）

加@Column：  
Java: userName     → 数据库: user_name（明确指定）
```

#### nullable属性


```java
@Column(nullable = false)
private String name;    // 对应：name VARCHAR(255) NOT NULL

@Column(nullable = true)
private String email;   // 对应：email VARCHAR(255) NULL（默认）
```

> ⚠️ **注意区别**  
> `nullable=false`只在自动建表时生效，手动建的表需要自己加NOT NULL约束

#### length属性


```java
@Column(length = 50)
private String name;      // VARCHAR(50)

@Column(length = 500)
private String description;  // VARCHAR(500)
```

#### unique属性


```java
@Column(unique = true)
private String email;    // 添加唯一约束：UNIQUE(email)
```

### 5.3 特殊类型映射


#### 日期时间类型


```java
import java.time.LocalDateTime;
import java.time.LocalDate;

@Column(name = "create_time")
private LocalDateTime createTime;  // 映射到DATETIME

@Column(name = "birth_date")
private LocalDate birthDate;       // 映射到DATE
```

**旧版日期类型**：
```java
import java.util.Date;

@Temporal(TemporalType.TIMESTAMP)  // 精确到时分秒
private Date createTime;

@Temporal(TemporalType.DATE)       // 只保留日期
private Date birthDate;
```

#### 大文本类型


```java
@Lob  // Large Object
@Column(columnDefinition = "TEXT")
private String content;    // 映射到TEXT类型，存储大量文本
```

#### 枚举类型


```java
public enum Gender {
    MALE, FEMALE
}

// 方式1：存储枚举名称
@Enumerated(EnumType.STRING)  // 数据库存"MALE"、"FEMALE"
private Gender gender;

// 方式2：存储枚举序号
@Enumerated(EnumType.ORDINAL) // 数据库存0、1
private Gender gender;
```

> 💡 **推荐使用STRING**  
> 序号方式添加新枚举值会导致数据错乱

---

## 6. 🔄 类与表映射原则


### 6.1 命名映射规则


**默认映射规则**：

```
Java类名(驼峰)        →    数据库表名(下划线)
StudentInfo          →    student_info
UserAccount          →    user_account  
OrderDetail          →    order_detail
```

**属性与列的映射**：

```
Java属性(驼峰)        →    数据库列(下划线)
userName             →    user_name
createTime           →    create_time
isActive             →    is_active
```

### 6.2 映射配置示例


#### 完整实体类示例


```java
@Entity
@Table(name = "t_student")
public class Student {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id")
    private Integer id;
    
    @Column(name = "student_name", length = 50, nullable = false)
    private String name;
    
    @Column(name = "age")
    private Integer age;
    
    @Column(name = "email", unique = true)
    private String email;
    
    @Column(name = "create_time")
    private LocalDateTime createTime;
    
    // 构造器、getter/setter省略
}
```

**对应的数据库表结构**：
```sql
CREATE TABLE t_student (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_name VARCHAR(50) NOT NULL,
    age INT,
    email VARCHAR(255) UNIQUE,
    create_time DATETIME
);
```

### 6.3 映射关系图解


```
Student类                          t_student表
┌─────────────────┐               ┌─────────────────┐
│ @Entity         │               │                 │
│ @Table          │   ←映射→      │  数据库表       │
├─────────────────┤               ├─────────────────┤
│ @Id             │               │ PRIMARY KEY     │
│ id: Integer     │   ←→          │ id INT          │
├─────────────────┤               ├─────────────────┤
│ @Column         │               │                 │
│ name: String    │   ←→          │ student_name    │
│                 │               │ VARCHAR(50)     │
├─────────────────┤               ├─────────────────┤
│ age: Integer    │   ←→          │ age INT         │
├─────────────────┤               ├─────────────────┤
│ email: String   │   ←→          │ email VARCHAR   │
│ @Column(unique) │               │ UNIQUE          │
└─────────────────┘               └─────────────────┘
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的概念


```
🔸 实体映射：Java类和数据库表的对应关系
🔸 POJO规范：无参构造器、私有属性、getter/setter
🔸 @Entity：标记实体类
🔸 @Table：指定表名
🔸 @Id：标记主键
🔸 @Column：字段映射配置
```

### 7.2 关键理解要点


**🔹 为什么需要无参构造器？**
```
Hibernate用反射创建对象：
Class<?> clazz = Student.class;
Student student = (Student) clazz.newInstance();  // 需要无参构造器
```

**🔹 主键策略怎么选？**
```
MySQL数据库    → IDENTITY（自增）
Oracle数据库   → SEQUENCE（序列）
通用方案       → TABLE（表生成器）
不确定         → AUTO（自动选择）
```

**🔹 什么时候用@Column？**
```
必须用：属性名和列名不一致
建议用：需要指定长度、约束
可选用：使用默认规则即可
```

### 7.3 实践要点


**✅ 推荐做法**：
- 总是明确指定`@Table(name="...")`
- 主键用包装类型（Integer、Long）而不是基本类型
- 字符串字段指定合理的length
- 重要字段设置nullable=false

**❌ 避免的错误**：
- 忘记写无参构造器
- 主键用基本类型int（无法表示null）
- 不指定@Table导致表名不确定
- 所有字段都用@Column（过度配置）

### 7.4 学习检查清单


**📈 掌握程度自测**
- [ ] 能说出POJO的设计规范
- [ ] 理解@Entity和@Table的作用
- [ ] 会配置不同的主键生成策略
- [ ] 掌握@Column的常用属性
- [ ] 能独立设计一个实体类

**🔗 扩展学习**
- 下一节：[实体关系映射](#实体关系映射)
- 相关主题：[级联操作](#级联操作) | [继承映射](#继承映射)

---

**🧠 记忆口诀**：
```
实体映射搭桥梁，Java对象变数据行
POJO规范要记牢，无参构造不能少
Entity标记实体类，Table指定表名称
Id注解标主键，策略选择看数据库
Column配置需谨慎，长度约束要分清
```