---
title: 21、Configuration配置详解
---
## 📚 目录

1. [Configuration类是什么](#1-Configuration类是什么)
2. [配置文件加载方式](#2-配置文件加载方式)
3. [Properties属性设置](#3-Properties属性设置)
4. [数据库方言配置](#4-数据库方言配置)
5. [映射资源添加](#5-映射资源添加)
6. [配置验证机制](#6-配置验证机制)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🔧 Configuration类是什么


### 1.1 Configuration的核心作用

💡 **简单理解**：Configuration就像是"装配工厂的图纸"

```
想象开一家餐厅：
原材料：各种食材和配料
图纸：菜谱、厨房设备说明书、营业时间表
开业：按图纸装配好就能营业

Hibernate也一样：
数据库：原材料（数据）
Configuration：图纸（配置信息）
SessionFactory：正式营业的工厂
```

**🎯 Configuration的三大职责**

| 职责 | **作用说明** | **类比理解** |
|-----|------------|------------|
| 📋 **读取配置** | 加载数据库连接、ORM映射等信息 | 读取餐厅的装修图纸 |
| 🔗 **建立连接** | 配置数据库连接参数 | 设置水电煤气管道 |
| 🏭 **创建工厂** | 生成SessionFactory对象 | 开业准备完毕 |

### 1.2 Configuration在Hibernate中的位置


```
Hibernate启动流程：

第1步：创建Configuration对象
    ↓
第2步：加载配置文件（hibernate.cfg.xml）
    ↓
第3步：添加实体映射信息
    ↓
第4步：验证配置正确性
    ↓
第5步：构建SessionFactory（核心工厂）
    ↓
第6步：开始使用Session操作数据库
```

### 1.3 最简单的Configuration使用示例


```java
// 第一步：创建Configuration对象
Configuration config = new Configuration();

// 第二步：加载配置文件
config.configure();  // 默认加载hibernate.cfg.xml

// 第三步：构建SessionFactory
SessionFactory factory = config.buildSessionFactory();

// 第四步：打开Session开始工作
Session session = factory.openSession();
```

**🔸 代码解读**
```
Configuration config = new Configuration();
→ 创建一个空白的配置对象，就像拿到一张空白图纸

config.configure();
→ 读取hibernate.cfg.xml文件，填充配置信息
→ 就像把装修方案写到图纸上

config.buildSessionFactory();
→ 根据配置信息建立数据库连接池
→ 就像按图纸装修好餐厅，准备开业

factory.openSession();
→ 打开一个会话，开始操作数据库
→ 就像餐厅开始接待第一位客人
```

---

## 2. 📂 配置文件加载方式


### 2.1 XML配置文件加载（最常用）


**📋 默认配置文件方式**
```java
// 方式1：加载默认配置文件（推荐新手使用）
Configuration config = new Configuration();
config.configure();  // 自动找hibernate.cfg.xml文件

// Hibernate会在这些位置查找文件：
// 1. 类路径根目录（src/main/resources/）
// 2. 当前工作目录
```

**🔸 配置文件示例（hibernate.cfg.xml）**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- 数据库连接信息 -->
        <property name="hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver
        </property>
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/mydb?useSSL=false
        </property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">123456</property>
        
        <!-- 其他配置 -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.MySQL8Dialect
        </property>
        <property name="show_sql">true</property>
    </session-factory>
</hibernate-configuration>
```

**📍 自定义配置文件路径**
```java
// 方式2：指定配置文件名称
Configuration config = new Configuration();
config.configure("my-hibernate.cfg.xml");

// 方式3：指定完整路径
File configFile = new File("config/hibernate-prod.cfg.xml");
config.configure(configFile);
```

### 2.2 Properties配置文件加载


**🔧 使用properties文件配置**
```java
// 加载properties格式的配置
Configuration config = new Configuration();
config.configure("hibernate.properties");

// 或者手动设置properties
Properties props = new Properties();
props.load(new FileInputStream("db.properties"));
config.setProperties(props);
```

**📝 hibernate.properties文件示例**
```properties
# 数据库连接配置
hibernate.connection.driver_class=com.mysql.cj.jdbc.Driver
hibernate.connection.url=jdbc:mysql://localhost:3306/mydb
hibernate.connection.username=root
hibernate.connection.password=123456

# Hibernate行为配置
hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
hibernate.show_sql=true
hibernate.format_sql=true
hibernate.hbm2ddl.auto=update
```

### 2.3 编程式配置（代码中直接配置）


**💻 纯代码配置方式**
```java
Configuration config = new Configuration();

// 数据库连接配置
config.setProperty("hibernate.connection.driver_class", 
                   "com.mysql.cj.jdbc.Driver");
config.setProperty("hibernate.connection.url", 
                   "jdbc:mysql://localhost:3306/mydb");
config.setProperty("hibernate.connection.username", "root");
config.setProperty("hibernate.connection.password", "123456");

// 方言配置
config.setProperty("hibernate.dialect", 
                   "org.hibernate.dialect.MySQL8Dialect");

// 其他配置
config.setProperty("show_sql", "true");
config.setProperty("format_sql", "true");
```

**⚖️ 配置方式对比**

| 方式 | **优点** | **缺点** | **适用场景** |
|-----|---------|---------|------------|
| 🔸 **XML文件** | 结构清晰，易于维护 | 需要额外文件 | 生产环境，配置复杂 |
| 🔸 **Properties** | 格式简单，易于理解 | 功能有限 | 简单项目，配置较少 |
| 🔸 **编程式** | 灵活性最高 | 代码耦合度高 | 动态配置，测试环境 |

---

## 3. ⚙️ Properties属性设置


### 3.1 核心属性分类


**🔸 数据库连接属性**
```java
Configuration config = new Configuration();

// 必需的四大连接属性
config.setProperty("hibernate.connection.driver_class", 
    "com.mysql.cj.jdbc.Driver");        // 数据库驱动
config.setProperty("hibernate.connection.url", 
    "jdbc:mysql://localhost:3306/mydb"); // 数据库地址
config.setProperty("hibernate.connection.username", "root"); // 用户名
config.setProperty("hibernate.connection.password", "pass"); // 密码
```

**🔸 连接池属性**
```java
// C3P0连接池配置（推荐新手使用）
config.setProperty("hibernate.c3p0.min_size", "5");      // 最小连接数
config.setProperty("hibernate.c3p0.max_size", "20");     // 最大连接数
config.setProperty("hibernate.c3p0.timeout", "300");     // 超时时间（秒）
config.setProperty("hibernate.c3p0.max_statements", "50"); // 缓存语句数

// 解释：
// min_size=5：池中至少保持5个连接待命，就像餐厅至少备5张桌子
// max_size=20：最多20个连接，超过就排队等待
// timeout=300：连接闲置5分钟自动关闭，节约资源
```

### 3.2 SQL调试属性


**🔍 开发阶段必备配置**
```java
// SQL语句显示
config.setProperty("show_sql", "true");        // 控制台显示SQL
config.setProperty("format_sql", "true");      // 格式化SQL，便于阅读
config.setProperty("use_sql_comments", "true"); // 添加注释说明

// 效果对比：
// show_sql=false：看不到执行的SQL，像黑盒操作
// show_sql=true：每条SQL都在控制台打印，清楚知道干了什么
```

**💡 实际输出效果**
```
未格式化的SQL（format_sql=false）：
Hibernate: select user0_.id as id1_0_, user0_.name as name2_0_ from user user0_

格式化后的SQL（format_sql=true）：
Hibernate: 
    select
        user0_.id as id1_0_,
        user0_.name as name2_0_ 
    from
        user user0_
```

### 3.3 自动建表属性


**🏗️ DDL自动生成配置**
```java
// hbm2ddl.auto属性（开发神器）
config.setProperty("hibernate.hbm2ddl.auto", "update");

// 可选值详解：
// "none"     → 什么都不做（生产环境用）
// "validate" → 只验证表结构，不修改（安全模式）
// "update"   → 更新表结构，保留数据（开发常用）
// "create"   → 每次启动删除并重建表（慎用！数据会丢）
// "create-drop" → 启动建表，关闭删表（测试用）
```

**⚠️ 不同值的实际效果**

```
假设你的实体类：
class User {
    Long id;
    String name;
    String email;  // 新增字段
}

hbm2ddl.auto = "validate"
→ 检查表结构，发现缺少email字段，抛出异常
→ 适合：生产环境前的最后检查

hbm2ddl.auto = "update"  
→ 自动给user表添加email列
→ 适合：开发阶段，快速迭代

hbm2ddl.auto = "create"
→ 删除user表，重新创建（数据丢失！）
→ 适合：全新开始的测试

hbm2ddl.auto = "create-drop"
→ 启动时创建表，关闭时删除表
→ 适合：单元测试，不污染数据库
```

### 3.4 性能优化属性


**⚡ 批量操作配置**
```java
// 批量插入/更新优化
config.setProperty("hibernate.jdbc.batch_size", "20");
// 解释：每20条记录批量提交一次，比一条条提交快很多
// 就像送快递，攒够20个包裹一起送，比一个个送效率高

// 批量查询优化
config.setProperty("hibernate.jdbc.fetch_size", "50");
// 解释：每次从数据库取50条记录到内存
// 就像去超市购物，一次性多拿点，减少来回跑
```

**🔸 二级缓存配置**
```java
// 启用二级缓存
config.setProperty("hibernate.cache.use_second_level_cache", "true");
config.setProperty("hibernate.cache.region.factory_class", 
    "org.hibernate.cache.ehcache.EhCacheRegionFactory");

// 解释：
// 一级缓存：Session级别，关闭Session就失效
// 二级缓存：SessionFactory级别，全局共享
// 就像：一级缓存是你的口袋，二级缓存是家里的冰箱
```

---

## 4. 🗣️ 数据库方言配置


### 4.1 什么是数据库方言


💬 **通俗理解**：方言就是"同一句话的不同说法"

```
普通话：请问现在几点了？
广东话：而家几点啊？
上海话：现在啥辰光了？

意思一样，表达不同 → 这就是"方言"

数据库也一样：
标准SQL：SELECT * FROM user
MySQL：SELECT * FROM user LIMIT 10
Oracle：SELECT * FROM user WHERE ROWNUM <= 10
SQL Server：SELECT TOP 10 * FROM user

功能一样，语法不同 → 这就是"数据库方言"
```

### 4.2 为什么需要配置方言


**🎯 方言配置的核心价值**

```
问题：不同数据库的SQL语法差异

分页查询：
MySQL：   LIMIT 10 OFFSET 0
Oracle：  ROWNUM <= 10
PostgreSQL：LIMIT 10 OFFSET 0
SQL Server：OFFSET 0 ROWS FETCH NEXT 10 ROWS ONLY

自增主键：
MySQL：    AUTO_INCREMENT
Oracle：   SEQUENCE
PostgreSQL：SERIAL
SQL Server：IDENTITY

Hibernate的解决方案：
→ 配置好方言后，你只写HQL（Hibernate查询语言）
→ Hibernate自动翻译成对应数据库的SQL
→ 就像有个翻译官，你说普通话，他自动翻译成粤语、上海话
```

### 4.3 常用数据库方言配置


**📋 主流数据库方言列表**

| 数据库 | **方言类全名** | **使用场景** |
|-------|--------------|------------|
| 🔸 **MySQL 8.x** | `org.hibernate.dialect.MySQL8Dialect` | MySQL 8.0及以上版本 |
| 🔸 **MySQL 5.x** | `org.hibernate.dialect.MySQL5Dialect` | MySQL 5.x版本 |
| 🔸 **Oracle 12c** | `org.hibernate.dialect.Oracle12cDialect` | Oracle 12c数据库 |
| 🔸 **PostgreSQL** | `org.hibernate.dialect.PostgreSQLDialect` | PostgreSQL数据库 |
| 🔸 **SQL Server** | `org.hibernate.dialect.SQLServerDialect` | Microsoft SQL Server |
| 🔸 **H2数据库** | `org.hibernate.dialect.H2Dialect` | 内存数据库（测试用） |

**🔧 实际配置示例**

```xml
<!-- MySQL 8配置 -->
<property name="hibernate.dialect">
    org.hibernate.dialect.MySQL8Dialect
</property>

<!-- Oracle配置 -->
<property name="hibernate.dialect">
    org.hibernate.dialect.Oracle12cDialect
</property>

<!-- PostgreSQL配置 -->
<property name="hibernate.dialect">
    org.hibernate.dialect.PostgreSQLDialect
</property>
```

### 4.4 方言配置错误的后果


**❌ 常见错误示例**

```
错误1：使用MySQL数据库，但配置了Oracle方言
结果：
→ 生成的SQL是Oracle语法，MySQL无法识别
→ 报错：You have an error in your SQL syntax

错误2：MySQL 8数据库，配置了MySQL5Dialect
结果：
→ 无法使用MySQL 8的新特性（如窗口函数）
→ 性能可能不是最优

错误3：没有配置方言
结果：
→ Hibernate使用通用SQL，功能受限
→ 无法使用数据库特有的优化功能
```

**✅ 正确配置检查**
```java
// 方式1：配置文件检查
// 查看hibernate.cfg.xml中的dialect配置是否匹配数据库版本

// 方式2：代码检查
Configuration config = new Configuration();
config.configure();

String dialect = config.getProperty("hibernate.dialect");
System.out.println("当前方言配置：" + dialect);
// 确认输出的方言类名与数据库版本匹配
```

---

## 5. 📦 映射资源添加


### 5.1 什么是映射资源


🗺️ **通俗理解**：映射就是"翻译手册"

```
场景：中国人去美国旅游
需要：中英翻译手册
作用：看懂英文标识，正确表达

Hibernate也一样：
Java对象 ←→ 数据库表
需要映射文件作为"翻译手册"

例如：
User对象     ←→  user表
- id字段     ←→  id列
- name字段   ←→  name列
- email字段  ←→  email列
```

### 5.2 添加映射资源的方式


**📋 方式1：在配置文件中添加（XML映射）**

```xml
<!-- hibernate.cfg.xml -->
<hibernate-configuration>
    <session-factory>
        <!-- 数据库配置... -->
        
        <!-- 添加实体映射文件 -->
        <mapping resource="com/example/entity/User.hbm.xml"/>
        <mapping resource="com/example/entity/Order.hbm.xml"/>
        <mapping resource="com/example/entity/Product.hbm.xml"/>
    </session-factory>
</hibernate-configuration>
```

**🔸 User.hbm.xml映射文件示例**
```xml
<?xml version="1.0"?>
<!DOCTYPE hibernate-mapping PUBLIC 
    "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">

<hibernate-mapping package="com.example.entity">
    <class name="User" table="user">
        <!-- id映射 -->
        <id name="id" column="id">
            <generator class="native"/>
        </id>
        
        <!-- 普通属性映射 -->
        <property name="name" column="name"/>
        <property name="email" column="email"/>
    </class>
</hibernate-mapping>
```

**💻 方式2：代码中添加映射**

```java
Configuration config = new Configuration();
config.configure();

// 添加XML映射文件
config.addResource("com/example/entity/User.hbm.xml");
config.addResource("com/example/entity/Order.hbm.xml");

// 或者添加整个包的映射
config.addPackage("com.example.entity");

// 直接添加映射的类（使用注解时）
config.addAnnotatedClass(User.class);
config.addAnnotatedClass(Order.class);
```

**🎯 方式3：使用注解映射（现代推荐）**

```java
// User实体类（注解映射）
@Entity
@Table(name = "user")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(name = "name")
    private String name;
    
    @Column(name = "email")
    private String email;
    
    // getter/setter...
}

// Configuration中添加
Configuration config = new Configuration();
config.configure();
config.addAnnotatedClass(User.class);  // 直接添加带注解的类
```

### 5.3 映射方式对比


**⚖️ XML映射 vs 注解映射**

| 特性 | **XML映射** | **注解映射** |
|-----|-----------|-----------|
| 🔸 **代码侵入性** | 无侵入，映射与代码分离 | 注解写在代码中 |
| 🔸 **配置灵活性** | 可随时修改不重新编译 | 需要重新编译代码 |
| 🔸 **开发便捷性** | 需要额外维护XML文件 | 代码即文档，更直观 |
| 🔸 **适用场景** | 映射复杂，需要灵活调整 | 简单映射，快速开发 |

**💡 实际选择建议**
```
新项目推荐：注解映射
→ 开发效率高，代码可读性好
→ 适合90%的常规业务场景

老项目或复杂映射：XML映射  
→ 可以不修改Java代码调整映射
→ 适合复杂的继承、组合关系映射

混合使用：
→ 简单实体用注解
→ 复杂实体用XML
→ 灵活性最高
```

---

## 6. ✅ 配置验证机制


### 6.1 为什么需要配置验证


🛡️ **配置验证的价值**

```
问题场景：
开发环境：配置正确，一切正常
测试环境：配置错误，启动失败
生产环境：配置错误，直接宕机

配置验证的作用：
→ 在启动前检查配置
→ 提前发现问题
→ 避免运行时错误

就像：
坐飞机前的安全检查
→ 发现问题可以修正
→ 起飞后发现问题就晚了
```

### 6.2 Hibernate的自动验证机制


**🔍 启动时自动验证**

```java
try {
    Configuration config = new Configuration();
    config.configure();  // 加载配置
    
    // buildSessionFactory时会自动验证：
    // 1. 数据库连接是否正确
    // 2. 方言配置是否匹配
    // 3. 映射文件是否存在
    // 4. 实体类与表结构是否对应
    SessionFactory factory = config.buildSessionFactory();
    
    System.out.println("✅ 配置验证通过，系统启动成功");
    
} catch (HibernateException e) {
    System.err.println("❌ 配置验证失败：" + e.getMessage());
    // 根据错误信息修正配置
}
```

**📋 常见验证错误及解决方案**

```
错误1：数据库连接失败
异常：Could not open JDBC Connection
原因：
→ 数据库服务未启动
→ 连接URL错误
→ 用户名密码错误
解决：检查数据库配置四要素

错误2：找不到映射文件
异常：MappingNotFoundException
原因：
→ 映射文件路径错误
→ 文件名拼写错误
解决：确认映射资源路径正确

错误3：表结构不匹配
异常：SchemaManagementException
原因：
→ 实体类字段与表列不对应
→ 数据类型不匹配
解决：检查映射配置或使用hbm2ddl.auto=update
```

### 6.3 手动配置验证工具


**🔧 配置验证工具类**

```java
public class ConfigurationValidator {
    
    /**
     * 验证数据库连接配置
     */
    public static boolean validateConnection(Configuration config) {
        try {
            String driver = config.getProperty("hibernate.connection.driver_class");
            String url = config.getProperty("hibernate.connection.url");
            String username = config.getProperty("hibernate.connection.username");
            String password = config.getProperty("hibernate.connection.password");
            
            // 检查必需配置项
            if (driver == null || url == null || username == null) {
                System.err.println("❌ 缺少必需的数据库连接配置");
                return false;
            }
            
            // 尝试建立连接
            Class.forName(driver);
            Connection conn = DriverManager.getConnection(url, username, password);
            conn.close();
            
            System.out.println("✅ 数据库连接验证通过");
            return true;
            
        } catch (Exception e) {
            System.err.println("❌ 数据库连接验证失败: " + e.getMessage());
            return false;
        }
    }
    
    /**
     * 验证方言配置
     */
    public static boolean validateDialect(Configuration config) {
        String dialect = config.getProperty("hibernate.dialect");
        
        if (dialect == null) {
            System.err.println("❌ 未配置数据库方言");
            return false;
        }
        
        try {
            // 检查方言类是否存在
            Class.forName(dialect);
            System.out.println("✅ 方言配置验证通过: " + dialect);
            return true;
        } catch (ClassNotFoundException e) {
            System.err.println("❌ 找不到方言类: " + dialect);
            return false;
        }
    }
    
    /**
     * 验证映射配置
     */
    public static boolean validateMappings(Configuration config) {
        try {
            // buildMappings会验证所有映射配置
            config.buildMappings();
            System.out.println("✅ 映射配置验证通过");
            return true;
        } catch (Exception e) {
            System.err.println("❌ 映射配置验证失败: " + e.getMessage());
            return false;
        }
    }
    
    /**
     * 完整配置验证
     */
    public static boolean validateAll(Configuration config) {
        System.out.println("开始配置验证...");
        
        boolean result = true;
        result &= validateConnection(config);
        result &= validateDialect(config);
        result &= validateMappings(config);
        
        if (result) {
            System.out.println("✅ 所有配置验证通过");
        } else {
            System.out.println("❌ 配置验证失败，请检查错误信息");
        }
        
        return result;
    }
}

// 使用示例
Configuration config = new Configuration();
config.configure();

if (ConfigurationValidator.validateAll(config)) {
    SessionFactory factory = config.buildSessionFactory();
    // 继续业务逻辑
}
```

### 6.4 配置最佳实践检查清单


**📝 启动前检查清单**

```
数据库配置：
□ 驱动类是否正确
□ 连接URL是否可达
□ 用户名密码是否正确
□ 数据库服务是否启动

方言配置：
□ 方言类是否存在
□ 方言是否匹配数据库版本
□ 是否支持所需特性

映射配置：
□ 映射文件是否存在
□ 映射路径是否正确
□ 实体类是否存在
□ 表结构是否对应

性能配置：
□ 连接池参数是否合理
□ 批量操作是否配置
□ 缓存策略是否启用

开发配置：
□ show_sql是否开启（开发环境）
□ format_sql是否开启（便于调试）
□ hbm2ddl.auto设置是否合理
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 Configuration作用：加载配置、管理映射、创建SessionFactory
🔸 配置加载方式：XML文件、Properties文件、编程式配置
🔸 核心属性分类：连接属性、性能属性、调试属性、DDL属性
🔸 数据库方言：不同数据库的SQL翻译器，必须正确配置
🔸 映射资源：实体类与数据库表的对应关系
🔸 配置验证：启动前检查配置正确性，避免运行时错误
```

### 7.2 关键理解要点


**🔹 Configuration的核心价值**
```
集中管理：
→ 所有配置集中在一处
→ 便于维护和修改
→ 支持多种配置方式

启动准备：
→ 验证配置正确性
→ 建立数据库连接
→ 加载映射信息

工厂构建：
→ 创建SessionFactory
→ 初始化连接池
→ 准备缓存机制
```

**🔹 配置文件选择策略**
```
开发阶段：
→ 使用XML配置，便于调整
→ 启用show_sql，便于调试
→ hbm2ddl.auto=update，自动建表

测试阶段：
→ 使用properties文件，便于切换环境
→ validate模式验证表结构
→ 配置连接池参数

生产阶段：
→ 最小化配置，提高性能
→ 关闭SQL打印
→ hbm2ddl.auto=none，手动管理表结构
```

**🔹 方言配置的重要性**
```
为什么重要：
→ 不同数据库SQL语法差异大
→ 方言错误导致SQL执行失败
→ 影响数据库特性的使用

如何选择：
→ 根据数据库类型选择
→ 注意版本匹配
→ 测试环境验证后再上线

常见问题：
→ MySQL 8用MySQL5Dialect（功能受限）
→ Oracle数据库用MySQL方言（直接报错）
→ 没配置方言（无法使用高级特性）
```

### 7.3 实际应用价值


**🎯 企业开发场景**
- **多环境部署**：开发、测试、生产环境配置分离
- **数据库迁移**：通过修改方言快速切换数据库
- **性能调优**：通过配置优化批量操作和缓存
- **故障排查**：通过show_sql快速定位问题

**🔧 最佳实践建议**
- **配置分离**：数据库配置独立成properties文件
- **环境区分**：不同环境使用不同配置文件
- **验证机制**：启动前自动验证配置正确性
- **文档化**：配置项添加注释说明用途

**📈 学习进阶路径**
- **初级**：掌握基本配置，能让Hibernate跑起来
- **中级**：理解各配置项含义，能优化性能
- **高级**：根据业务需求灵活配置，解决复杂问题

**核心记忆口诀**：
- Configuration是配置管家，读取设置创建工厂
- 三种加载方式灵活选，XML常用properties简单
- 方言配置千万别错，数据库版本要匹配
- 映射资源指明道路，实体表格正确对应
- 启动验证提前检查，发现问题早点解决