---
title: 26、对象三种状态详解
---
## 📚 目录

1. [持久化对象状态概述](#1-持久化对象状态概述)
2. [瞬时态详解](#2-瞬时态详解)
3. [持久态详解](#3-持久态详解)
4. [游离态详解](#4-游离态详解)
5. [状态转换机制](#5-状态转换机制)
6. [对象生命周期管理](#6-对象生命周期管理)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 持久化对象状态概述


### 1.1 什么是对象状态


> 💡 **通俗理解**：想象你在网上购物，一个订单从创建到完成经历不同的阶段——刚下单、支付中、已完成。Hibernate中的对象也是这样，在不同时期有不同的"身份状态"。

**核心概念**：
```
Hibernate中的对象状态 = 对象与数据库的关系状态

就像人的身份状态：
- 还没登记的人（瞬时态）
- 已经登记在册的人（持久态）  
- 曾经登记过但现在注销的人（游离态）
```

### 1.2 为什么要理解对象状态


**实际意义**：
```
理解对象状态能帮你：

✅ 知道什么时候数据会自动保存到数据库
✅ 避免重复保存同一个对象
✅ 理解Session关闭后对象的行为
✅ 正确处理对象的增删改查操作
```

### 1.3 三种状态速览


```
┌─────────────────────────────────────────────────┐
│                  对象状态图谱                    │
├─────────────────────────────────────────────────┤
│                                                 │
│  瞬时态(Transient)     持久态(Persistent)       │
│  ┌──────────┐         ┌──────────┐            │
│  │  new出来  │ save()  │  在Session│            │
│  │  的对象   │───────→│  管理中   │            │
│  └──────────┘         └──────────┘            │
│       ↑                     │                   │
│       │                     │ Session.close()   │
│       │ new               ↓                     │
│  ┌──────────┐         ┌──────────┐            │
│  │  普通对象 │         │ 游离态对象│            │
│  └──────────┘         └──────────┘            │
│                      (Detached)                 │
└─────────────────────────────────────────────────┘
```

---

## 2. 🆕 瞬时态详解


### 2.1 什么是瞬时态


> 📌 **简单理解**：瞬时态就像是你刚用Word写了一篇文章，但还没保存到硬盘。这个文章内容只在内存里，电脑一关就没了。

**特征定义**：
```
瞬时态对象（Transient）：

✓ 刚用new创建出来的对象
✓ Hibernate不知道它的存在
✓ 数据库里没有对应的记录
✓ 没有持久化标识（ID为null或默认值）
```

### 2.2 瞬时态对象示例


```java
// 示例：创建一个User对象
User user = new User();
user.setName("张三");
user.setAge(25);

// 此时user是瞬时态：
// ✓ 只在Java内存中存在
// ✓ Hibernate不管理它
// ✓ 数据库里没有这条记录
// ✓ user.getId() 返回 null
```

**状态特点**：
```
┌─────────────────────────────────────┐
│ 瞬时态对象的特点                     │
├─────────────────────────────────────┤
│ 1️⃣ 存在位置：只在JVM内存中          │
│ 2️⃣ Hibernate关系：不受管理          │
│ 3️⃣ 数据库状态：无对应记录           │
│ 4️⃣ 持久化标识：ID为null             │
│ 5️⃣ 生命周期：跟普通Java对象一样     │
└─────────────────────────────────────┘
```

### 2.3 瞬时态的实际应用


**使用场景**：
- 📝 准备新增的数据（还未保存）
- 🔧 临时计算或处理的对象
- ⚙️ 需要验证后再决定是否保存

```java
// 实际例子：用户注册
public void register(String username, String password) {
    // 1. 创建瞬时态对象
    User user = new User();
    user.setUsername(username);
    user.setPassword(password);
    
    // 2. 此时还是瞬时态，可以进行验证
    if (usernameExists(username)) {
        throw new Exception("用户名已存在");
    }
    
    // 3. 验证通过后再转为持久态
    session.save(user);  // 变成持久态
}
```

---

## 3. 💾 持久态详解


### 3.1 什么是持久态


> 💡 **形象比喻**：持久态就像你的Word文档已经保存到硬盘，并且还开着编辑窗口。你在窗口里的任何修改，都会自动保存（Hibernate的自动更新机制）。

**核心定义**：
```
持久态对象（Persistent）：

✓ 与Session关联的对象
✓ 在数据库中有对应的记录
✓ 有持久化标识（ID有值）
✓ 对象的修改会自动同步到数据库
```

### 3.2 如何变成持久态


**转换方式**：
```java
// 方式1：保存新对象
User user = new User("张三", 25);  // 瞬时态
session.save(user);                // 变成持久态

// 方式2：从数据库查询
User user = session.get(User.class, 1L);  // 直接就是持久态

// 方式3：更新游离态对象
User user = ...; // 游离态对象
session.update(user);  // 变成持久态
```

### 3.3 持久态的核心特性


**⭐ 自动更新机制**：

> 🚀 **神奇之处**：持久态对象的属性修改，不需要手动调用update()，Hibernate会自动检测并更新数据库！

```java
// 持久态对象的自动更新
Session session = sessionFactory.openSession();
Transaction tx = session.beginTransaction();

// 1. 查询得到持久态对象
User user = session.get(User.class, 1L);

// 2. 直接修改属性（不需要调用update）
user.setAge(26);        // 只是修改内存中的对象
user.setName("李四");   // Hibernate会自动检测到变化

// 3. 提交事务时自动执行UPDATE
tx.commit();  // Hibernate自动生成并执行：
              // UPDATE user SET age=26, name='李四' WHERE id=1

session.close();
```

**工作原理**：
```
Hibernate的自动更新原理：

1️⃣ 快照机制：
   Session加载对象时，会保存一份快照
   
2️⃣ 脏检查：
   提交事务时，对比当前对象和快照
   
3️⃣ 自动生成SQL：
   发现有变化，自动生成UPDATE语句
   
4️⃣ 执行更新：
   将变化同步到数据库
```

### 3.4 持久态对象的特点


```
┌────────────────────────────────────────┐
│ 持久态对象的关键特征                    │
├────────────────────────────────────────┤
│ ✅ 特征1：与Session关联                │
│    - Session管理着这个对象              │
│    - Session关闭，对象变成游离态        │
│                                        │
│ ✅ 特征2：有持久化标识                  │
│    - ID属性有值（对应数据库主键）       │
│    - 可通过ID从数据库重新加载           │
│                                        │
│ ✅ 特征3：自动脏检查                    │
│    - Hibernate监控属性变化              │
│    - 事务提交时自动更新                 │
│                                        │
│ ✅ 特征4：数据库同步                    │
│    - 内存对象 ←→ 数据库记录             │
│    - 保持一致性                         │
└────────────────────────────────────────┘
```

### 3.5 持久态的实际应用


**典型场景**：
```java
// 场景1：修改用户信息
public void updateUserInfo(Long userId, String newName) {
    Session session = sessionFactory.openSession();
    Transaction tx = session.beginTransaction();
    
    User user = session.get(User.class, userId);  // 持久态
    user.setName(newName);  // 直接修改，自动更新
    
    tx.commit();  // 提交时自动UPDATE
    session.close();
}

// 场景2：批量操作
public void batchUpdate() {
    Session session = sessionFactory.openSession();
    Transaction tx = session.beginTransaction();
    
    List<User> users = session.createQuery("from User").list();
    for (User user : users) {  // 所有user都是持久态
        if (user.getAge() < 18) {
            user.setStatus("未成年");  // 自动更新
        }
    }
    
    tx.commit();  // 一次性更新所有修改
    session.close();
}
```

---

## 4. 🔄 游离态详解


### 4.1 什么是游离态


> 📌 **生活类比**：游离态就像你关闭了Word文档，但文件还保存在硬盘上。你手里还有文档的副本（打印件），但它不再和硬盘上的文件实时同步了。

**核心定义**：
```
游离态对象（Detached）：

✓ 曾经是持久态对象
✓ Session已经关闭，不再管理它
✓ 有持久化标识（ID有值）
✓ 数据库中有对应记录
✓ 修改不会自动同步到数据库
```

### 4.2 如何产生游离态


```java
// 产生游离态的典型过程
Session session = sessionFactory.openSession();
Transaction tx = session.beginTransaction();

// 1. 查询得到持久态对象
User user = session.get(User.class, 1L);  // 持久态

tx.commit();
session.close();  // Session关闭

// 2. 此时user变成游离态
// - user对象还在内存中
// - user.getId()还有值
// - 但已经不受Hibernate管理
// - 修改user不会自动更新数据库

user.setAge(30);  // 修改无效，不会同步到数据库
```

### 4.3 游离态的特点


**状态特征**：
```
┌───────────────────────────────────────┐
│ 游离态对象的特点                       │
├───────────────────────────────────────┤
│ 🔸 曾经的身份：原来是持久态            │
│ 🔸 当前状态：不在Session管理中         │
│ 🔸 持久化标识：ID有值（有数据库记录）   │
│ 🔸 修改行为：改了也不会自动保存        │
│ 🔸 可重新关联：可以重新变成持久态      │
└───────────────────────────────────────┘
```

### 4.4 游离态的使用场景


**实际应用**：

```java
// 场景1：跨Session操作（常见于Web应用）
public class UserService {
    
    // 第一次请求：查询用户
    public User getUser(Long id) {
        Session session = sessionFactory.openSession();
        User user = session.get(User.class, id);  // 持久态
        session.close();  // 关闭后变游离态
        return user;  // 返回给前端的是游离态对象
    }
    
    // 第二次请求：更新用户
    public void updateUser(User user) {  // 接收的是游离态对象
        Session session = sessionFactory.openSession();
        Transaction tx = session.beginTransaction();
        
        session.update(user);  // 重新关联，变持久态
        
        tx.commit();
        session.close();
    }
}
```

**⚠️ 常见问题**：
```java
// ❌ 错误做法：游离态对象修改无效
Session session = sessionFactory.openSession();
User user = session.get(User.class, 1L);
session.close();  // user变成游离态

user.setAge(30);  // 修改无效！

// ✅ 正确做法：重新关联到Session
Session newSession = sessionFactory.openSession();
Transaction tx = newSession.beginTransaction();
newSession.update(user);  // 重新变持久态
tx.commit();
newSession.close();
```

---

## 5. 🔀 状态转换机制


### 5.1 状态转换全景图


```
                    状态转换流程图
                    
瞬时态 ──────save()────────→ 持久态
(Transient)                (Persistent)
    ↑                           │
    │                           │
    │                           │
    │                      Session.close()
    │                      Session.evict()
    │                           │
    │                           ↓
    │                       游离态
    │                    (Detached)
    │                           │
    │                           │
    └────────delete()───────────┘
         update()/merge()
              saveOrUpdate()
```

### 5.2 详细转换方式


**从瞬时态到持久态**：
```java
User user = new User("张三", 25);  // 瞬时态

// 方式1：save() - 立即生成ID并插入
session.save(user);  // INSERT立即执行，变持久态

// 方式2：persist() - 延迟到事务提交
session.persist(user);  // 标记为持久态，提交时INSERT

// 方式3：saveOrUpdate() - 智能判断
session.saveOrUpdate(user);  // 根据ID判断save还是update
```

**从持久态到游离态**：
```java
User user = session.get(User.class, 1L);  // 持久态

// 方式1：关闭Session
session.close();  // 所有持久态对象变游离态

// 方式2：从Session中移除
session.evict(user);  // 只把user变成游离态

// 方式3：清空Session
session.clear();  // 所有对象变游离态
```

**从游离态到持久态**：
```java
User user = ...;  // 游离态对象（有ID值）

// 方式1：update() - 强制更新
session.update(user);  // 执行UPDATE，变持久态

// 方式2：merge() - 合并更新
User mergedUser = session.merge(user);  // 返回持久态对象

// 方式3：lock() - 不更新，只关联
session.lock(user, LockMode.NONE);  // 变持久态但不UPDATE
```

### 5.3 方法对比与选择


| 转换方法 | **作用** | **适用场景** | **执行时机** |
|---------|---------|-------------|-------------|
| `save()` | `新增对象` | `确定是新数据` | `立即INSERT` |
| `persist()` | `新增对象` | `确定是新数据` | `事务提交时INSERT` |
| `update()` | `更新对象` | `确定是已存在数据` | `立即UPDATE` |
| `merge()` | `合并对象` | `不确定是新是旧` | `根据情况INSERT/UPDATE` |
| `saveOrUpdate()` | `智能保存` | `不确定是新是旧` | `根据ID判断` |

### 5.4 状态转换的实际应用


**完整示例**：
```java
public class ObjectStateDemo {
    
    public void demonstrateStates() {
        Session session = sessionFactory.openSession();
        Transaction tx = session.beginTransaction();
        
        // 1. 瞬时态 → 持久态
        User user = new User("张三", 25);  // 瞬时态
        System.out.println("ID: " + user.getId());  // null
        
        session.save(user);  // 变持久态
        System.out.println("ID: " + user.getId());  // 有值了，如：1
        
        // 2. 持久态修改（自动更新）
        user.setAge(26);  // 会自动UPDATE
        
        tx.commit();
        session.close();  // user变游离态
        
        // 3. 游离态 → 持久态
        Session newSession = sessionFactory.openSession();
        Transaction newTx = newSession.beginTransaction();
        
        user.setAge(27);  // 游离态修改无效
        newSession.update(user);  // 重新变持久态
        
        newTx.commit();
        newSession.close();
    }
}
```

---

## 6. 🔄 对象生命周期管理


### 6.1 完整生命周期


```
对象从创建到销毁的完整过程：

1️⃣ 创建阶段（瞬时态）
   new User() → 在JVM内存中
   
2️⃣ 持久化阶段（持久态）
   session.save() → 进入Hibernate管理
   ↓
   自动脏检查 → 同步到数据库
   
3️⃣ 游离阶段（游离态）
   session.close() → 脱离管理
   ↓
   可以序列化传输、展示等
   
4️⃣ 重新持久化
   session.update() → 再次进入管理
   
5️⃣ 删除
   session.delete() → 从数据库删除
```

### 6.2 生命周期最佳实践


> 💡 **核心原则**：在Session范围内完成完整的业务操作

**推荐模式**：
```java
// ✅ 推荐：完整的Session生命周期
public void updateUserInfo(Long userId, String newName) {
    Session session = null;
    Transaction tx = null;
    
    try {
        session = sessionFactory.openSession();
        tx = session.beginTransaction();
        
        // 1. 查询（持久态）
        User user = session.get(User.class, userId);
        
        // 2. 修改（自动更新）
        user.setName(newName);
        
        // 3. 提交
        tx.commit();
        
    } catch (Exception e) {
        if (tx != null) tx.rollback();
        throw e;
    } finally {
        if (session != null) session.close();
    }
}
```

**避免的模式**：
```java
// ❌ 不推荐：跨Session操作
public void badPractice(Long userId) {
    // Session 1：查询
    Session session1 = sessionFactory.openSession();
    User user = session1.get(User.class, userId);  // 持久态
    session1.close();  // 变游离态
    
    // 中间可能有其他操作...
    user.setName("新名字");  // 游离态修改
    
    // Session 2：更新
    Session session2 = sessionFactory.openSession();
    session2.update(user);  // 需要重新关联
    session2.close();
    
    // 问题：两次Session可能导致数据不一致
}
```

### 6.3 Web应用中的对象状态


> 📌 **实际场景**：在Web应用中，对象经常需要跨请求传递

**典型流程**：
```
HTTP请求1（查询）             HTTP请求2（更新）
     ↓                            ↓
┌─────────┐                  ┌─────────┐
│ Session │                  │ Session │
│  打开   │                  │  打开   │
└─────────┘                  └─────────┘
     ↓                            ↓
  查询User                     接收User
  (持久态)                      (游离态)
     ↓                            ↓
 Session关闭                  update()重新
  (游离态)                     变持久态
     ↓                            ↓
 返回给前端                    更新数据库
     ↓                            ↓
 (序列化传输)                  Session关闭
```

**代码实现**：
```java
@Controller
public class UserController {
    
    // 请求1：查询用户
    @GetMapping("/user/{id}")
    public String getUser(@PathVariable Long id, Model model) {
        Session session = sessionFactory.openSession();
        User user = session.get(User.class, id);  // 持久态
        session.close();  // 变游离态
        
        model.addAttribute("user", user);  // 游离态对象传给视图
        return "user-edit";
    }
    
    // 请求2：更新用户
    @PostMapping("/user/update")
    public String updateUser(@ModelAttribute User user) {  // 游离态对象
        Session session = sessionFactory.openSession();
        Transaction tx = session.beginTransaction();
        
        session.update(user);  // 游离态→持久态
        
        tx.commit();
        session.close();
        
        return "redirect:/user/" + user.getId();
    }
}
```

### 6.4 对象状态的注意事项


**⚠️ 常见陷阱**：

```
陷阱1：游离态对象修改无效
┌────────────────────────────────┐
│ Session session = ...;         │
│ User user = session.get(...);  │
│ session.close();  // 变游离态  │
│                                │
│ user.setAge(30);  // ❌ 无效！ │
└────────────────────────────────┘

陷阱2：同一对象在不同Session中
┌────────────────────────────────┐
│ Session s1 = ...;              │
│ User u1 = s1.get(User, 1L);    │
│                                │
│ Session s2 = ...;              │
│ User u2 = s2.get(User, 1L);    │
│                                │
│ // u1和u2是不同的对象实例！    │
└────────────────────────────────┘

陷阱3：忘记提交事务
┌────────────────────────────────┐
│ Session s = ...;               │
│ Transaction tx = s.beginTx();  │
│ User user = s.get(User, 1L);   │
│ user.setAge(30);               │
│ s.close();  // ❌ 忘记commit！ │
│                                │
│ // 修改会丢失！                 │
└────────────────────────────────┘
```

---

## 7. 📋 核心要点总结


### 7.1 三种状态速记


```
┌─────────────────────────────────────────┐
│          🎯 状态记忆口诀                 │
├─────────────────────────────────────────┤
│                                         │
│  瞬时态：new出对象在内存                │
│         没ID没Session不入库             │
│                                         │
│  持久态：Session管理有ID                │
│         改了自动存数据库                │
│                                         │
│  游离态：曾经持久现脱管                 │
│         有ID在库但不同步                │
│                                         │
└─────────────────────────────────────────┘
```

### 7.2 关键对比表格


| 状态 | **在Session中** | **有持久化ID** | **数据库记录** | **自动更新** |
|------|---------------|--------------|--------------|------------|
| **瞬时态** | `❌ 否` | `❌ 无` | `❌ 无` | `❌ 否` |
| **持久态** | `✅ 是` | `✅ 有` | `✅ 有` | `✅ 是` |
| **游离态** | `❌ 否` | `✅ 有` | `✅ 有` | `❌ 否` |

### 7.3 转换方法速查


```
🔸 变成持久态的方法：
   save() / persist()     - 新对象
   get() / load()         - 查询
   update() / merge()     - 游离对象

🔸 变成游离态的方法：
   close()                - 关闭Session
   evict()                - 移除指定对象
   clear()                - 清空所有对象

🔸 删除对象：
   delete()               - 从数据库删除
```

### 7.4 实际应用建议


> 💡 **最佳实践**

```
1️⃣ 单一Session原则
   一次完整的业务操作在一个Session中完成
   
2️⃣ 及时关闭Session
   使用try-finally确保Session关闭
   
3️⃣ 理解状态转换
   知道何时需要重新关联对象
   
4️⃣ 利用自动更新
   持久态对象修改自动同步，无需手动update
   
5️⃣ 注意跨层传递
   Service层返回的对象通常是游离态
```

**记忆要点**：
- ⭐ **瞬时态**：new出来还没save，Hibernate不管它
- ⭐ **持久态**：Session管着，改了自动存
- ⭐ **游离态**：Session关了，有ID但不自动存
- ⭐ **核心机制**：持久态的自动脏检查和更新是Hibernate的核心优势