---
title: 15、条件构造器基础
---
## 📚 目录

1. [什么是条件构造器](#1-什么是条件构造器)
2. [QueryWrapper查询构造器](#2-querywrapper查询构造器)
3. [UpdateWrapper更新构造器](#3-updatewrapper更新构造器)
4. [LambdaQueryWrapper安全构造器](#4-lambdaquerywrapper安全构造器)
5. [条件链式调用技巧](#5-条件链式调用技巧)
6. [动态条件拼装](#6-动态条件拼装)
7. [SQL注入防护](#7-sql注入防护)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🤔 什么是条件构造器


### 1.1 为什么需要条件构造器


**传统方式的痛点**：
```
以前写SQL查询：
SELECT * FROM user WHERE age > 18 AND name LIKE '%张%'

问题：
- 需要手写SQL语句，容易出错
- 条件多了，SQL拼接很繁琐
- 动态条件处理麻烦（有时传参，有时不传）
- 字段名写错了，只能运行时才发现
```

**条件构造器的作用**：
把查询条件用Java代码表示，不用写SQL！

```
类比生活场景：
传统方式 = 自己组装家具（容易装错）
条件构造器 = 用乐高积木搭建（模块化，不易出错）

核心理念：
用面向对象的方式描述查询条件
让代码更清晰、更安全、更易维护
```

### 1.2 条件构造器家族成员


**MyBatis-Plus提供的构造器**：

```
构造器类型：
├── QueryWrapper        → 用于查询条件
├── UpdateWrapper       → 用于更新条件
├── LambdaQueryWrapper  → 查询+类型安全（推荐）
└── LambdaUpdateWrapper → 更新+类型安全（推荐）

选择建议：
🔸 新手学习：从 QueryWrapper 开始
🔸 实际项目：优先用 Lambda 版本（防止字段写错）
```

### 1.3 基本使用流程


**三步走战略**：

```
步骤1：创建构造器对象
QueryWrapper<User> wrapper = new QueryWrapper<>();

步骤2：添加查询条件
wrapper.eq("age", 18)           // 年龄等于18
       .like("name", "张");     // 姓名包含"张"

步骤3：执行查询
List<User> users = userMapper.selectList(wrapper);

完整流程图：
创建构造器 → 添加条件 → 传给Mapper → 执行查询 → 返回结果
```

---

## 2. 🔍 QueryWrapper查询构造器


### 2.1 基本相等条件


**eq() - 等于条件**：

```java
// 场景：查询年龄等于18岁的用户
QueryWrapper<User> wrapper = new QueryWrapper<>();
wrapper.eq("age", 18);
// 生成SQL: WHERE age = 18

// 场景：查询指定姓名和年龄
wrapper.eq("name", "张三")
       .eq("age", 20);
// 生成SQL: WHERE name = '张三' AND age = 20
```

**ne() - 不等于条件**：

```java
// 场景：查询不是VIP的用户
wrapper.ne("vip_level", 0);
// 生成SQL: WHERE vip_level != 0
```

**💡 理解要点**：
- `eq` 就是 equals（等于）的缩写
- 多个条件默认用 **AND** 连接
- 字段名要和数据库表字段对应（不是Java属性名）

### 2.2 模糊查询条件


**like系列方法**：

```java
// like - 两边模糊：%值%
wrapper.like("name", "张");
// 生成SQL: WHERE name LIKE '%张%'
// 匹配：张三、小张、张小明 ✓

// likeLeft - 左边模糊：%值
wrapper.likeLeft("name", "三");
// 生成SQL: WHERE name LIKE '%三'
// 匹配：张三、李三 ✓  三张 ✗

// likeRight - 右边模糊：值%
wrapper.likeRight("name", "张");
// 生成SQL: WHERE name LIKE '张%'
// 匹配：张三、张小明 ✓  小张 ✗
```

**实际应用示例**：

```java
// 场景：搜索框输入"手机"，查找商品
QueryWrapper<Product> wrapper = new QueryWrapper<>();
wrapper.like("product_name", "手机");
// 能匹配：华为手机、小米手机、手机壳、手机配件

// 场景：查询以"1380"开头的手机号
wrapper.likeRight("phone", "1380");
// 能匹配：13800138000、13801234567
```

### 2.3 范围查询条件


**数值范围**：

```java
// gt - 大于 (greater than)
wrapper.gt("age", 18);
// SQL: WHERE age > 18

// ge - 大于等于 (greater or equal)
wrapper.ge("age", 18);
// SQL: WHERE age >= 18

// lt - 小于 (less than)
wrapper.lt("price", 100);
// SQL: WHERE price < 100

// le - 小于等于 (less or equal)
wrapper.le("price", 100);
// SQL: WHERE price <= 100

// between - 区间查询
wrapper.between("age", 18, 30);
// SQL: WHERE age BETWEEN 18 AND 30
// 包含边界值：18和30都会被查出来
```

**日期范围查询**：

```java
// 场景：查询最近7天注册的用户
Date sevenDaysAgo = // 7天前日期
Date now = new Date();

wrapper.between("create_time", sevenDaysAgo, now);
// SQL: WHERE create_time BETWEEN '2025-09-16' AND '2025-09-23'
```

### 2.4 NULL值判断


**isNull / isNotNull**：

```java
// 查询没有填写邮箱的用户
wrapper.isNull("email");
// SQL: WHERE email IS NULL

// 查询已填写邮箱的用户
wrapper.isNotNull("email");
// SQL: WHERE email IS NOT NULL

// 实际场景：查询未删除的数据
wrapper.isNull("delete_time");  // 删除时间为空 = 未删除
```

### 2.5 集合范围查询


**in / notIn**：

```java
// 场景：查询指定ID的用户
List<Long> ids = Arrays.asList(1L, 2L, 3L, 4L);
wrapper.in("id", ids);
// SQL: WHERE id IN (1, 2, 3, 4)

// 场景：排除某些状态的订单
wrapper.notIn("status", Arrays.asList("已取消", "已退款"));
// SQL: WHERE status NOT IN ('已取消', '已退款')

// 单个值也可以用in（虽然用eq更好）
wrapper.in("id", 1);  // 等同于 eq("id", 1)
```

### 2.6 组合条件查询


**OR条件连接**：

```java
// 默认是AND连接
wrapper.eq("age", 18).eq("city", "北京");
// SQL: WHERE age = 18 AND city = '北京'

// 使用or()改为OR连接
wrapper.eq("age", 18)
       .or()
       .eq("city", "北京");
// SQL: WHERE age = 18 OR city = '北京'

// 复杂OR条件（使用lambda）
wrapper.eq("age", 18)
       .or(w -> w.eq("vip_level", 5).eq("city", "上海"));
// SQL: WHERE age = 18 OR (vip_level = 5 AND city = '上海')
```

**嵌套条件**：

```java
// 场景：查询 (年龄18-30岁 或 VIP用户) 且 城市是北京
wrapper.nested(w -> 
    w.between("age", 18, 30)
     .or()
     .eq("vip_level", 1)
).eq("city", "北京");

// SQL: WHERE (age BETWEEN 18 AND 30 OR vip_level = 1) AND city = '北京'
```

---

## 3. 🔧 UpdateWrapper更新构造器


### 3.1 为什么需要UpdateWrapper


**对比传统更新方式**：

```java
// ❌ 方式1：先查询再更新（两次数据库操作）
User user = userMapper.selectById(1);
user.setVipLevel(5);
userMapper.updateById(user);

// ❌ 方式2：手写SQL更新
userMapper.updateVipLevel(5, 1);  // 需要写XML

// ✅ 方式3：UpdateWrapper（直接更新，一次操作）
UpdateWrapper<User> wrapper = new UpdateWrapper<>();
wrapper.eq("id", 1)
       .set("vip_level", 5);
userMapper.update(null, wrapper);
```

### 3.2 基本更新操作


**set() - 设置字段值**：

```java
UpdateWrapper<User> wrapper = new UpdateWrapper<>();

// 场景：给ID为1的用户升级VIP
wrapper.eq("id", 1)
       .set("vip_level", 5);
userMapper.update(null, wrapper);
// SQL: UPDATE user SET vip_level = 5 WHERE id = 1

// 场景：批量更新多个字段
wrapper.eq("city", "北京")
       .set("area_code", "010")
       .set("province", "北京市");
userMapper.update(null, wrapper);
// SQL: UPDATE user SET area_code = '010', province = '北京市' 
//      WHERE city = '北京'
```

### 3.3 条件更新实战


**结合查询条件更新**：

```java
// 场景：给所有北京的成年用户发放优惠券
UpdateWrapper<User> wrapper = new UpdateWrapper<>();
wrapper.eq("city", "北京")
       .ge("age", 18)
       .set("coupon_count", "coupon_count + 1");  // 数量+1
userMapper.update(null, wrapper);

// 场景：清空所有过期商品的库存
wrapper.lt("expire_date", new Date())
       .set("stock", 0)
       .set("status", "已下架");
productMapper.update(null, wrapper);
```

### 3.4 UpdateWrapper vs 实体更新


**两种更新方式对比**：

| 更新方式 | **使用场景** | **优点** | **缺点** |
|---------|------------|---------|---------|
| **UpdateWrapper** | `批量更新、条件更新、字段计算` | `灵活，一次SQL完成` | `需要手写字段名` |
| **实体对象更新** | `单条更新、更新大部分字段` | `类型安全，代码清晰` | `需要先查询` |

```java
// 场景选择示例：

// ✅ 用UpdateWrapper：批量更新库存
wrapper.in("product_id", productIds)
       .setSql("stock = stock - 1");  // 库存减1

// ✅ 用实体更新：修改用户详细信息
User user = userMapper.selectById(1);
user.setName("新名字");
user.setEmail("new@email.com");
user.setPhone("13800138000");
userMapper.updateById(user);
```

---

## 4. 🛡️ LambdaQueryWrapper安全构造器


### 4.1 为什么推荐Lambda版本


**字符串字段名的问题**：

```java
// ❌ 普通QueryWrapper：字段名是字符串
QueryWrapper<User> wrapper = new QueryWrapper<>();
wrapper.eq("nmae", "张三");  // 写错了！但编译不报错
// 运行时才发现错误：Unknown column 'nmae'

// ✅ LambdaQueryWrapper：类型安全
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
wrapper.eq(User::getName, "张三");  // 方法引用，写错立即报错
// IDE会自动提示可用方法
```

**核心优势**：

```
优势对比：
┌─────────────────┬──────────────┬──────────────┐
│    特性         │ QueryWrapper │ Lambda版本   │
├─────────────────┼──────────────┼──────────────┤
│ 字段名写法      │ "name"字符串 │ User::getName│
│ 编译期检查      │ ❌ 不检查    │ ✅ 检查      │
│ IDE自动提示     │ ❌ 无        │ ✅ 有        │
│ 重构友好        │ ❌ 手动改    │ ✅ 自动改    │
│ 防止写错        │ ❌ 运行报错  │ ✅ 编译报错  │
└─────────────────┴──────────────┴──────────────┘
```

### 4.2 Lambda语法快速入门


**方法引用语法解释**：

```java
// User类的getter方法
public class User {
    private String name;
    
    public String getName() {  // getter方法
        return name;
    }
}

// Lambda方法引用写法
User::getName  // 表示"User类的getName方法"

// 完整对比：
wrapper.eq("name", "张三")          // 字符串版本
wrapper.eq(User::getName, "张三")   // Lambda版本（推荐）

// 理解技巧：
User::getName 就是告诉MyBatis-Plus去找User类的name字段
```

### 4.3 LambdaQueryWrapper基本使用


**常用查询示例**：

```java
// 创建Lambda构造器
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();

// 等值查询
wrapper.eq(User::getAge, 18);
// SQL: WHERE age = 18

// 模糊查询
wrapper.like(User::getName, "张");
// SQL: WHERE name LIKE '%张%'

// 范围查询
wrapper.between(User::getAge, 18, 30);
// SQL: WHERE age BETWEEN 18 AND 30

// 组合条件
wrapper.eq(User::getCity, "北京")
       .ge(User::getAge, 18)
       .isNotNull(User::getEmail);
// SQL: WHERE city = '北京' AND age >= 18 AND email IS NOT NULL
```

### 4.4 选择查询字段


**select() - 指定返回字段**：

```java
// 只查询部分字段（优化性能）
wrapper.select(User::getId, User::getName, User::getAge)
       .eq(User::getCity, "北京");
// SQL: SELECT id, name, age FROM user WHERE city = '北京'

// 场景：查询用户列表，只需要ID和姓名
List<User> users = userMapper.selectList(
    new LambdaQueryWrapper<User>()
        .select(User::getId, User::getName)
        .eq(User::getStatus, 1)
);

// 好处：
// ✅ 减少网络传输数据量
// ✅ 提高查询性能
// ✅ 避免查询敏感字段（如密码）
```

### 4.5 排序和分组


**orderBy系列方法**：

```java
// 按年龄升序
wrapper.orderByAsc(User::getAge);
// SQL: ORDER BY age ASC

// 按创建时间降序
wrapper.orderByDesc(User::getCreateTime);
// SQL: ORDER BY create_time DESC

// 多字段排序
wrapper.orderByAsc(User::getAge)
       .orderByDesc(User::getCreateTime);
// SQL: ORDER BY age ASC, create_time DESC
```

**groupBy分组查询**：

```java
// 按城市分组统计人数
wrapper.select("city, COUNT(*) as count")
       .groupBy(User::getCity);
// SQL: SELECT city, COUNT(*) as count FROM user GROUP BY city

// 实际场景：统计各省份用户数
QueryWrapper<User> wrapper = new QueryWrapper<>();
wrapper.select("province, COUNT(*) as user_count")
       .groupBy("province")
       .having("COUNT(*) > 100");  // 只要用户数>100的省份
```

---

## 5. 🔗 条件链式调用技巧


### 5.1 链式调用基本原理


**什么是链式调用**：

```java
// 非链式写法（繁琐）
QueryWrapper<User> wrapper = new QueryWrapper<>();
wrapper.eq("age", 18);
wrapper.like("name", "张");
wrapper.isNotNull("email");

// ✅ 链式写法（优雅）
wrapper.eq("age", 18)
       .like("name", "张")
       .isNotNull("email");

// 原理：每个方法都返回自己(this)
public QueryWrapper<T> eq(String column, Object val) {
    // ... 添加条件
    return this;  // 返回自己，可以继续调用
}
```

### 5.2 复杂条件组合


**AND和OR混合使用**：

```java
// 场景：查询 (北京18岁) 或 (上海VIP)
wrapper.and(w -> 
    w.eq(User::getCity, "北京")
     .eq(User::getAge, 18)
).or(w -> 
    w.eq(User::getCity, "上海")
     .eq(User::getVipLevel, 1)
);
// SQL: WHERE (city = '北京' AND age = 18) 
//         OR (city = '上海' AND vip_level = 1)

// 场景：查询年龄18-30岁的北京或上海用户
wrapper.between(User::getAge, 18, 30)
       .and(w -> 
           w.eq(User::getCity, "北京")
            .or()
            .eq(User::getCity, "上海")
       );
```

### 5.3 条件函数封装


**提取可复用条件**：

```java
// 定义通用条件函数
private void addAgeCondition(LambdaQueryWrapper<User> wrapper, 
                            Integer minAge, Integer maxAge) {
    if (minAge != null) {
        wrapper.ge(User::getAge, minAge);
    }
    if (maxAge != null) {
        wrapper.le(User::getAge, maxAge);
    }
}

// 使用
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
addAgeCondition(wrapper, 18, 30);  // 复用年龄条件
wrapper.eq(User::getCity, "北京");
```

---

## 6. ⚙️ 动态条件拼装


### 6.1 什么是动态条件


**实际业务场景**：

```
用户搜索页面：
□ 姓名：[      ]  （可能填，可能不填）
□ 年龄：[  ] - [  ]  （可能只填一个，可能都不填）
□ 城市：[选择▼]  （可能选，可能不选）

问题：用户不一定填所有条件，SQL条件要动态组合！
```

### 6.2 条件判断方法


**方式1：if判断添加条件**：

```java
// ❌ 繁琐写法
QueryWrapper<User> wrapper = new QueryWrapper<>();
if (name != null && !name.isEmpty()) {
    wrapper.like("name", name);
}
if (minAge != null) {
    wrapper.ge("age", minAge);
}
if (maxAge != null) {
    wrapper.le("age", maxAge);
}

// ✅ 优雅写法：条件方法自带判断
wrapper.like(name != null, "name", name)
       .ge(minAge != null, "age", minAge)
       .le(maxAge != null, "age", maxAge);

// 第一个参数是boolean：true才添加条件，false跳过
```

**方式2：Lambda版本动态条件**：

```java
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();

// 只有条件不为空才添加
wrapper.like(StringUtils.isNotBlank(name), User::getName, name)
       .ge(minAge != null, User::getAge, minAge)
       .le(maxAge != null, User::getAge, maxAge)
       .eq(StringUtils.isNotBlank(city), User::getCity, city);

// 实际效果：
// name="张三", minAge=null, maxAge=30, city=null
// SQL: WHERE name LIKE '%张三%' AND age <= 30
```

### 6.3 实战案例：用户搜索


**完整动态搜索示例**：

```java
public List<User> searchUsers(UserSearchDTO searchDTO) {
    LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
    
    // 姓名模糊查询
    wrapper.like(
        StringUtils.isNotBlank(searchDTO.getName()),
        User::getName, 
        searchDTO.getName()
    );
    
    // 年龄范围
    wrapper.ge(
        searchDTO.getMinAge() != null,
        User::getAge,
        searchDTO.getMinAge()
    ).le(
        searchDTO.getMaxAge() != null,
        User::getAge,
        searchDTO.getMaxAge()
    );
    
    // 城市精确匹配
    wrapper.eq(
        StringUtils.isNotBlank(searchDTO.getCity()),
        User::getCity,
        searchDTO.getCity()
    );
    
    // VIP等级（只查VIP）
    wrapper.gt(
        searchDTO.getOnlyVip() != null && searchDTO.getOnlyVip(),
        User::getVipLevel,
        0
    );
    
    return userMapper.selectList(wrapper);
}
```

### 6.4 高级技巧：条件工具方法


**自定义条件工具类**：

```java
public class WrapperUtil {
    
    // 非空字符串条件
    public static <T> boolean notBlank(String str) {
        return str != null && !str.trim().isEmpty();
    }
    
    // 非空对象条件
    public static boolean notNull(Object obj) {
        return obj != null;
    }
    
    // 集合非空条件
    public static boolean notEmpty(Collection<?> coll) {
        return coll != null && !coll.isEmpty();
    }
}

// 使用工具方法
wrapper.like(WrapperUtil.notBlank(name), User::getName, name)
       .in(WrapperUtil.notEmpty(ids), User::getId, ids);
```

---

## 7. 🔒 SQL注入防护


### 7.1 什么是SQL注入


**SQL注入攻击原理**：

```
正常查询：
用户输入：张三
生成SQL: SELECT * FROM user WHERE name = '张三'

SQL注入攻击：
用户输入：张三' OR '1'='1
生成SQL: SELECT * FROM user WHERE name = '张三' OR '1'='1'
结果：查出所有用户！（绕过了条件限制）

危害：
- 泄露所有数据
- 删除数据库
- 获取管理员权限
```

### 7.2 MyBatis-Plus如何防护


**预编译机制**：

```java
// ✅ 安全：使用构造器（自动预编译）
wrapper.eq(User::getName, userInput);
// 生成: SELECT * FROM user WHERE name = ?
// 参数单独传递：? 被替换为 '张三'（自动转义）

// ❌ 危险：字符串拼接
String sql = "SELECT * FROM user WHERE name = '" + userInput + "'";
// 如果userInput是恶意输入，直接拼接会被攻击

// MyBatis-Plus优势：
wrapper的所有方法都使用预编译，天然防SQL注入！
```

### 7.3 特殊场景注意事项


**apply()方法的风险**：

```java
// ⚠️ apply()直接拼接SQL，有风险
wrapper.apply("date_format(create_time,'%Y-%m-%d') = {0}", 
              userInput);  // 如果userInput是恶意的？

// ✅ 安全做法1：不用apply，用其他方法
wrapper.eq(User::getCreateDate, LocalDate.parse(userInput));

// ✅ 安全做法2：严格校验输入
if (userInput.matches("\\d{4}-\\d{2}-\\d{2}")) {  // 验证日期格式
    wrapper.apply("date_format(create_time,'%Y-%m-%d') = {0}", 
                  userInput);
}
```

**last()方法的风险**：

```java
// ⚠️ last()直接拼接SQL片段，极度危险
wrapper.last("LIMIT " + userInput);
// 如果userInput是 "1; DROP TABLE user;--"  ← 删库跑路！

// ✅ 安全做法：限制输入范围
int pageSize = Math.min(Integer.parseInt(userInput), 100);  // 最大100
wrapper.last("LIMIT " + pageSize);
```

### 7.4 安全使用原则


**防护最佳实践**：

```
✅ 推荐做法：
1. 优先使用构造器方法（eq、like等）
2. 使用Lambda版本（类型安全）
3. 输入严格校验（正则、范围检查）
4. 避免apply()和last()直接拼接用户输入

❌ 危险做法：
1. 直接拼接SQL字符串
2. 不验证用户输入
3. 信任前端传来的数据
4. 使用字符串字段名（容易拼错）

核心原则：
永远不要相信用户输入！
能用构造器方法就不要用字符串拼接！
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 条件构造器本质：用Java对象表示SQL条件，避免手写SQL
🔸 三大核心构造器：QueryWrapper、UpdateWrapper、Lambda版本
🔸 链式调用：方法返回自己，可以连续调用组合条件
🔸 动态条件：第一个参数是boolean，控制条件是否添加
🔸 SQL注入防护：构造器自动预编译，天然安全
```

### 8.2 使用建议速查表


| 使用场景 | **推荐方案** | **理由** |
|---------|------------|---------|
| 简单查询 | `LambdaQueryWrapper` | `类型安全，防止字段写错` |
| 复杂查询 | `Lambda + 嵌套条件` | `灵活组合，代码清晰` |
| 批量更新 | `UpdateWrapper` | `一次SQL完成，高效` |
| 动态条件 | `条件判断参数` | `简洁优雅，自动跳过null` |
| 字段选择 | `select()方法` | `减少传输，提升性能` |

### 8.3 常见方法速查


**查询条件**：
```
eq(字段, 值)          → 等于
ne(字段, 值)          → 不等于
gt(字段, 值)          → 大于
ge(字段, 值)          → 大于等于
lt(字段, 值)          → 小于
le(字段, 值)          → 小于等于
between(字段, 值1, 值2) → 范围
like(字段, 值)        → 模糊查询
in(字段, 集合)        → 在集合中
isNull(字段)         → 为空
isNotNull(字段)      → 不为空
```

**逻辑连接**：
```
and()               → AND条件
or()                → OR条件
nested(条件)        → 嵌套条件 ()
```

**更新操作**：
```
set(字段, 值)        → 设置值
setSql(SQL片段)     → 自定义SQL
```

### 8.4 学习检验清单


**检查你是否真正掌握**：

```
□ 能独立写出基本查询条件（eq、like、between）
□ 理解AND和OR的区别和组合使用
□ 会使用Lambda版本避免字段写错
□ 能根据业务需求写动态条件
□ 知道SQL注入的危害和防护方法
□ 能区分QueryWrapper和UpdateWrapper的使用场景
□ 会用select()优化查询性能

全部打勾 = 已掌握基础！可以进入实战项目 ✓
```

### 8.5 记忆口诀


```
条件构造不写SQL，
链式调用真优雅。
Lambda版本最安全，
字段引用不怕错。

动态条件第一参，
布尔判断自动跳。
预编译防注入，
apply和last要慎用。

查询用Query构造器，
更新用Update更高效。
掌握这些基础功，
CRUD操作样样通！
```

---

## 💡 实战小贴士


**新手常犯错误**：

```java
// ❌ 错误1：字段名写错（用字符串版本）
wrapper.eq("nmae", "张三");  // name拼错了

// ✅ 正确：用Lambda版本
wrapper.eq(User::getName, "张三");

// ❌ 错误2：忘记OR连接
wrapper.eq("age", 18).eq("city", "北京");  // 这是AND
// SQL: age = 18 AND city = '北京'

// ✅ 正确：使用or()
wrapper.eq("age", 18).or().eq("city", "北京");
// SQL: age = 18 OR city = '北京'

// ❌ 错误3：动态条件判断写在外面
if (name != null) {
    wrapper.like("name", name);  // 代码冗余
}

// ✅ 正确：判断写在方法里
wrapper.like(name != null, "name", name);
```

**性能优化提示**：

```java
// ✅ 只查需要的字段
wrapper.select(User::getId, User::getName)  // 不查无用字段

// ✅ 使用索引字段作为条件
wrapper.eq(User::getId, 1)  // ID有索引，快

// ✅ 避免全表扫描
wrapper.isNotNull(User::getCreateTime)  // 加个条件限制范围

// ✅ 大数据量用分页
Page<User> page = new Page<>(1, 10);
userMapper.selectPage(page, wrapper);
```