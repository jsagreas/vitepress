---
title: 32、 前端分页集成
---
## 📚 目录

1. [分页的本质理解](#1-分页的本质理解)
2. [分页参数接收](#2-分页参数接收)
3. [PageHelper工具类](#3-pagehelper工具类)
4. [分页结果封装](#4-分页结果封装)
5. [前后端交互协议](#5-前后端交互协议)
6. [分页组件适配](#6-分页组件适配)
7. [响应数据格式](#7-响应数据格式)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 分页的本质理解


### 1.1 为什么需要分页


**生活场景类比**：
```
就像翻书看小说：
- 不分页：一次性看完1000页 → 累死人，手机也卡死
- 分页：每次看10页 → 轻松愉快,想看哪页翻哪页

数据库也是一样：
- 不分页：一次查10万条数据 → 服务器压力大,网络传输慢
- 分页：每次查20条 → 快速响应,用户体验好
```

**核心作用**：
- 📉 **减轻服务器压力**：不用一次加载所有数据
- ⚡ **提升响应速度**：数据量小,传输快
- 👍 **改善用户体验**：分批展示,操作流畅

### 1.2 分页的核心要素


**必须知道的4个参数**：
```
用户请求分页时需要告诉后端：

1️⃣ 当前页码（currentPage）
   - 用户想看第几页
   - 通常从1开始计数
   - 例如：第3页

2️⃣ 每页条数（pageSize）  
   - 每页显示多少条数据
   - 通常是10、20、50这种整数
   - 例如：每页20条

后端返回给前端的信息：

3️⃣ 总记录数（total）
   - 数据库里一共有多少条数据
   - 例如：总共158条记录

4️⃣ 总页数（pages）
   - 根据总记录数和每页条数计算出来
   - 例如：158÷20=8页（向上取整）
```

**分页计算示例**：
```
📊 实际场景：
数据库有158条用户记录
用户要求每页显示20条

计算过程：
第1页：显示第1-20条
第2页：显示第21-40条  
第3页：显示第41-60条
...
第8页：显示第141-158条（最后一页只有18条）

总页数 = ⌈158 ÷ 20⌉ = ⌈7.9⌉ = 8页
```

---

## 2. 📥 分页参数接收


### 2.1 前端如何传递分页参数


**常见的3种传参方式**：

**方式1：URL参数传递（最常用）**
```
前端请求：
GET /api/users?currentPage=2&pageSize=20

优点：简单直观,RESTful风格
适用：查询列表、搜索等场景
```

**方式2：请求体传递（复杂查询）**
```
前端请求：
POST /api/users/search
Content-Type: application/json

{
  "currentPage": 2,
  "pageSize": 20,
  "name": "张三",
  "status": 1
}

优点：参数多时更清晰
适用：带条件查询的分页
```

**方式3：路径参数传递（少见）**
```
前端请求：
GET /api/users/page/2/size/20

优点：路径清晰
缺点：不够灵活
```

### 2.2 后端接收参数的正确姿势


**方式1：单独接收（简单场景）**
```java
@RestController
@RequestMapping("/api/users")
public class UserController {
    
    @GetMapping
    public Result<Page<User>> getUsers(
        @RequestParam(defaultValue = "1") Integer currentPage,
        @RequestParam(defaultValue = "10") Integer pageSize
    ) {
        // defaultValue：前端不传时的默认值
        // 第1页,每页10条
        
        Page<User> page = userService.page(
            new Page<>(currentPage, pageSize)
        );
        return Result.success(page);
    }
}
```

**方式2：封装成对象（推荐）**
```java
// 分页参数对象
public class PageParam {
    @Min(value = 1, message = "页码最小为1")
    private Integer currentPage = 1;
    
    @Min(value = 1, message = "每页条数最小为1")
    @Max(value = 100, message = "每页条数最大为100")
    private Integer pageSize = 10;
    
    // getter/setter省略
}

// 控制器使用
@GetMapping
public Result<Page<User>> getUsers(PageParam pageParam) {
    // 自动封装,参数验证
    Page<User> page = userService.page(
        new Page<>(pageParam.getCurrentPage(), pageParam.getPageSize())
    );
    return Result.success(page);
}
```

> **💡 参数校验重要性**
> 
> 必须校验的原因：
> - 防止页码为0或负数
> - 防止每页条数过大（如10000条）
> - 避免恶意请求拖垮数据库

---

## 3. 🛠️ PageHelper工具类


### 3.1 什么是PageHelper


**简单理解**：
```
PageHelper就像一个"分页管家"：
- 帮你把分页参数传给MyBatis-Plus
- 自动计算SQL的LIMIT和OFFSET
- 统一处理分页逻辑,让代码更简洁
```

**有无PageHelper的对比**：
```java
// ❌ 不用工具类,代码重复
public Page<User> getUsers(int current, int size) {
    Page<User> page = new Page<>(current, size);
    return this.page(page);
}

public Page<Order> getOrders(int current, int size) {
    Page<Order> page = new Page<>(current, size);
    return this.page(page);
}

// ✅ 使用工具类,一行搞定
public Page<User> getUsers(PageParam param) {
    return this.page(PageHelper.buildPage(param));
}

public Page<Order> getOrders(PageParam param) {
    return this.page(PageHelper.buildPage(param));
}
```

### 3.2 PageHelper工具类实现


**核心功能实现**：
```java
public class PageHelper {
    
    /**
     * 构建分页对象
     */
    public static <T> Page<T> buildPage(PageParam pageParam) {
        return new Page<>(
            pageParam.getCurrentPage(),
            pageParam.getPageSize()
        );
    }
    
    /**
     * 快速创建分页（带默认值）
     */
    public static <T> Page<T> buildPage(Integer current, Integer size) {
        current = current == null || current < 1 ? 1 : current;
        size = size == null || size < 1 ? 10 : size;
        return new Page<>(current, size);
    }
}
```

---

## 4. 📦 分页结果封装


### 4.1 为什么要封装结果


**原因分析**：
```
MyBatis-Plus返回的Page对象包含很多字段：
- records（数据列表）✅ 需要
- total（总记录数）✅ 需要
- size（每页条数）✅ 需要
- current（当前页）✅ 需要
- pages（总页数）✅ 需要
- orders（排序信息）❌ 前端不需要
- optimizeCountSql（优化标志）❌ 前端不需要

问题：
1. 字段太多,传输浪费流量
2. 字段名不符合前端习惯
3. 不利于统一接口规范
```

### 4.2 统一分页结果对象


**封装后的结果对象**：
```java
public class PageResult<T> {
    
    private List<T> list;        // 数据列表
    private Long total;          // 总记录数
    private Long currentPage;    // 当前页码
    private Long pageSize;       // 每页条数
    private Long totalPages;     // 总页数
    
    /**
     * 从MyBatis-Plus的Page转换
     */
    public static <T> PageResult<T> from(Page<T> page) {
        PageResult<T> result = new PageResult<>();
        result.setList(page.getRecords());
        result.setTotal(page.getTotal());
        result.setCurrentPage(page.getCurrent());
        result.setPageSize(page.getSize());
        result.setTotalPages(page.getPages());
        return result;
    }
}
```

**使用示例**：
```java
@GetMapping("/list")
public Result<PageResult<User>> list(PageParam pageParam) {
    // 1. 执行分页查询
    Page<User> page = userService.page(PageHelper.buildPage(pageParam));
    
    // 2. 转换为统一格式
    PageResult<User> result = PageResult.from(page);
    
    // 3. 返回给前端
    return Result.success(result);
}
```

---

## 5. 🤝 前后端交互协议


### 5.1 请求参数规范


**统一的分页请求格式**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `currentPage` | Integer | 否 | 1 | 当前页码,从1开始 |
| `pageSize` | Integer | 否 | 10 | 每页条数 |
| `sortField` | String | 否 | - | 排序字段 |
| `sortOrder` | String | 否 | asc | 排序方式：asc升序,desc降序 |

**请求示例**：
```
# 基础分页
GET /api/users?currentPage=2&pageSize=20

# 带排序
GET /api/users?currentPage=1&pageSize=10&sortField=createTime&sortOrder=desc
```

### 5.2 响应数据规范


**统一的分页响应格式**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `list` | Array | 当前页的数据列表 |
| `total` | Long | 总记录数 |
| `currentPage` | Long | 当前页码 |
| `pageSize` | Long | 每页条数 |
| `totalPages` | Long | 总页数 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
      {"id": 21, "name": "用户21", "age": 25},
      {"id": 22, "name": "用户22", "age": 28}
    ],
    "total": 158,
    "currentPage": 2,
    "pageSize": 20,
    "totalPages": 8
  }
}
```

### 5.3 前后端协议示意图


```
前端发起请求                      后端处理流程
     |                                |
     |  1. 发送分页参数                |
     |----------------------------->  |
     |  currentPage=2                 |
     |  pageSize=20                   | 2. 接收参数
     |                                | 3. 构建Page对象
     |                                | 4. 查询数据库
     |                                | 5. 封装结果
     |                                |
     |  6. 返回分页数据                |
     | <----------------------------- |
     |  {list, total, pages...}       |
     |                                |
     ↓                                ↓
  渲染表格                         完成响应
  更新分页器
```

---

## 6. 🎨 分页组件适配


### 6.1 常见前端分页组件


**主流框架的分页组件**：

| 前端框架 | 分页组件 | 特点 |
|---------|---------|------|
| **Vue + Element UI** | `<el-pagination>` | 功能完善,样式美观 |
| **React + Ant Design** | `<Pagination>` | 企业级UI,配置灵活 |
| **Vue + Vant** | `<van-list>` | 移动端友好,下拉加载 |
| **原生JS** | 自定义组件 | 轻量级,可控性强 |

### 6.2 Element UI适配示例


```vue
<template>
  <div>
    <!-- 数据表格 -->
    <el-table :data="tableData" v-loading="loading">
      <el-table-column prop="id" label="ID" width="80"/>
      <el-table-column prop="name" label="姓名"/>
      <el-table-column prop="age" label="年龄"/>
    </el-table>
    
    <!-- 分页器 -->
    <el-pagination
      @size-change="handleSizeChange"
      @current-change="handleCurrentChange"
      :current-page="pageParam.currentPage"
      :page-sizes="[10, 20, 50, 100]"
      :page-size="pageParam.pageSize"
      :total="total"
      layout="total, sizes, prev, pager, next, jumper"
    />
  </div>
</template>

<script>
export default {
  data() {
    return {
      tableData: [],
      loading: false,
      total: 0,
      pageParam: {
        currentPage: 1,
        pageSize: 10
      }
    }
  },
  
  methods: {
    async loadData() {
      this.loading = true
      const res = await this.$http.get('/api/users', {
        params: this.pageParam
      })
      this.tableData = res.data.list
      this.total = res.data.total
      this.loading = false
    },
    
    handleSizeChange(size) {
      this.pageParam.pageSize = size
      this.pageParam.currentPage = 1  // 重置到第1页
      this.loadData()
    },
    
    handleCurrentChange(page) {
      this.pageParam.currentPage = page
      this.loadData()
    }
  }
}
</script>
```

---

## 7. 📋 响应数据格式


### 7.1 标准响应结构


```java
public class Result<T> {
    private Integer code;     // 响应码：200成功,其他失败
    private String message;   // 响应消息
    private T data;          // 响应数据
    
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("操作成功");
        result.setData(data);
        return result;
    }
}
```

### 7.2 不同场景的响应格式


**成功返回分页数据**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [...],
    "total": 158,
    "currentPage": 2,
    "pageSize": 20,
    "totalPages": 8
  }
}
```

**查询结果为空**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [],
    "total": 0,
    "currentPage": 1,
    "pageSize": 10,
    "totalPages": 0
  }
}
```

---

## 8. 📌 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 分页本质：分批次查询大量数据,提升性能和体验
🔸 四大参数：currentPage、pageSize、total、totalPages
🔸 参数接收：URL参数、请求体、分页对象封装
🔸 PageHelper：统一分页逻辑,简化代码
🔸 结果封装：统一响应格式,前后端规范
🔸 组件适配：对接Element UI、Ant Design等主流组件
```

### 8.2 分页完整流程图


```
用户操作           前端处理           后端处理           数据库
   |                 |                  |                  |
   | 点击第2页        |                  |                  |
   |-------------->  |                  |                  |
   |                 | 发送请求          |                  |
   |                 | currentPage=2     |                  |
   |                 | pageSize=20       |                  |
   |                 |----------------> |                  |
   |                 |                  | 接收参数          |
   |                 |                  | 构建Page对象      |
   |                 |                  |----------------> |
   |                 |                  |                  | 执行SQL
   |                 |                  | <----------------| LIMIT 20
   |                 | <----------------| 封装结果          | OFFSET 20
   |                 | 解析数据          |                  |
   | <--------------| 渲染表格          |                  |
   | 更新分页器       |                  |                  |
```

### 8.3 实际开发检查清单


**后端开发检查**：
- [ ] 分页参数校验（页码≥1,每页条数1-100）
- [ ] 使用PageHelper统一分页逻辑
- [ ] 结果封装为统一格式（PageResult）
- [ ] 敏感信息脱敏处理
- [ ] 异常情况处理（空数据、参数错误）

**前端开发检查**：
- [ ] 正确传递分页参数
- [ ] 分页器配置正确（总数、当前页、每页条数）
- [ ] 分页改变时重新请求数据
- [ ] 加载状态提示（loading）
- [ ] 空数据提示

### 8.4 常见问题解决


| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 分页数据重复 | 前端没有清空旧数据 | 每次请求前清空或直接覆盖 |
| 总页数计算错误 | 除法取整方式不对 | 使用向上取整 Math.ceil() |
| 改变每页条数后停留在原页码 | 没有重置页码 | 改变pageSize时,将currentPage重置为1 |

### 8.5 最佳实践建议


**性能优化**：
- 合理设置默认每页条数（10-20条）
- 限制每页最大条数（≤100）
- 大数据量时使用游标分页
- 避免频繁查询总数

**用户体验**：
- 提供加载中提示
- 空数据友好提示
- 记住用户的分页偏好
- 支持快速跳转到指定页

**开发规范**：
- 统一分页参数命名
- 统一响应数据格式
- 封装分页工具类
- 编写接口文档

---

**核心记忆**：
- 分页=分批查询,提升性能和体验
- 前端传currentPage和pageSize,后端返回list和total
- PageHelper统一分页逻辑,PageResult统一响应格式
- 前后端协议规范,组件适配灵活
- 参数校验、异常处理、性能优化缺一不可