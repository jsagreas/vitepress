---
title: 3、项目搭建与依赖管理
---
## 📚 目录

1. [什么是依赖管理](#1-什么是依赖管理)
2. [Maven依赖配置](#2-Maven依赖配置)
3. [Spring Boot集成MyBatis-Plus](#3-Spring-Boot集成MyBatis-Plus)
4. [版本兼容性说明](#4-版本兼容性说明)
5. [多模块项目结构](#5-多模块项目结构)
6. [包结构规划最佳实践](#6-包结构规划最佳实践)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 什么是依赖管理


### 1.1 通俗理解依赖


**🏗️ 生活中的类比**
```
盖房子需要：
├── 🧱 砖块（基础材料）
├── 🪵 木材（框架支撑）
├── 🔩 螺丝（连接件）
└── 🎨 油漆（美化）

写Java项目需要：
├── 📦 Spring Boot（框架基础）
├── 📦 MyBatis-Plus（数据库操作）
├── 📦 MySQL驱动（连接数据库）
└── 📦 Lombok（简化代码）

这些"材料包"就是依赖！
```

> 💡 **新手提示**
> 
> 依赖就像是别人写好的"工具包"，我们不需要自己造轮子，直接拿来用就行了。Maven帮我们自动下载和管理这些工具包。

### 1.2 为什么需要依赖管理


**❌ 没有依赖管理的痛苦**
- 手动下载jar包 → 费时费力
- 版本冲突 → 项目跑不起来
- 缺少依赖 → 各种报错
- 升级困难 → 一个一个改

**✅ 有依赖管理的好处**
- 自动下载 → 一行配置搞定
- 版本统一 → 不会冲突
- 依赖传递 → 自动引入需要的包
- 轻松升级 → 改个版本号即可

---

## 2. 📦 Maven依赖配置


### 2.1 pom.xml是什么


**🗂️ pom.xml文件理解**

pom.xml就像是项目的"购物清单"，告诉Maven需要下载哪些依赖包。

```
┌─────────────────────────────┐
│      pom.xml 文件            │
│  (Project Object Model)      │
├─────────────────────────────┤
│  项目基本信息                │
│  ├─ 项目名称                 │
│  ├─ 版本号                   │
│  └─ 打包方式                 │
├─────────────────────────────┤
│  依赖列表                    │
│  ├─ Spring Boot              │
│  ├─ MyBatis-Plus            │
│  └─ MySQL驱动               │
├─────────────────────────────┤
│  插件配置                    │
│  └─ 打包插件                 │
└─────────────────────────────┘
```

### 2.2 基础依赖配置


**🔧 核心依赖清单**

| 依赖名称 | 作用说明 | 必需程度 |
|---------|---------|---------|
| `mybatis-plus-boot-starter` | MP核心启动器 | 🔴 必需 |
| `mysql-connector-java` | MySQL数据库驱动 | 🔴 必需 |
| `spring-boot-starter-web` | Web开发基础 | 🟡 常用 |
| `lombok` | 简化实体类代码 | 🟢 推荐 |

**完整pom.xml配置**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <!-- 继承Spring Boot父项目 -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.7.14</version>
    </parent>
    
    <!-- 项目基本信息 -->
    <groupId>com.example</groupId>
    <artifactId>mybatis-plus-demo</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    
    <!-- 版本号统一管理 -->
    <properties>
        <java.version>1.8</java.version>
        <mybatis-plus.version>3.5.3.1</mybatis-plus.version>
    </properties>
    
    <dependencies>
        <!-- MyBatis-Plus 启动器 -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
        
        <!-- MySQL 驱动 -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <scope>runtime</scope>
        </dependency>
        
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        
        <!-- Lombok 代码简化工具 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        
        <!-- 测试依赖 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    
    <build>
        <plugins>
            <!-- Spring Boot 打包插件 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

> ⚠️ **新手常见错误**
> 
> - 忘记添加`<parent>`标签，导致Spring Boot相关配置失效
> - 版本号写错，导致依赖下载失败
> - `scope`配置不当，导致运行时找不到类

### 2.3 依赖配置详解


**📌 关键配置项说明**

**Parent父项目**
```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>2.7.14</version>
</parent>
```
- 作用：继承Spring Boot的默认配置
- 好处：自动管理常用依赖的版本号

**版本管理**
```xml
<properties>
    <mybatis-plus.version>3.5.3.1</mybatis-plus.version>
</properties>
```
- 作用：统一管理版本号，方便升级
- 使用：通过`${mybatis-plus.version}`引用

**依赖作用域 scope**

| scope值 | 含义 | 示例场景 |
|---------|------|---------|
| `compile` | 编译、测试、运行都需要 | Spring框架 |
| `runtime` | 运行时需要 | MySQL驱动 |
| `test` | 仅测试时需要 | JUnit |
| `provided` | 编译需要，运行由容器提供 | Servlet API |

---

## 3. 🚀 Spring Boot集成MyBatis-Plus


### 3.1 Spring Boot Starter原理


**🎁 Starter启动器理解**

Spring Boot Starter就像"全家桶套餐"：
```
普通点餐：
├─ 汉堡 ← 需要单点
├─ 薯条 ← 需要单点  
├─ 可乐 ← 需要单点
└─ 需要点3次

Starter套餐：
└─ 全家桶 ← 一次搞定
   ├─ 汉堡 ✓
   ├─ 薯条 ✓
   └─ 可乐 ✓
```

`mybatis-plus-boot-starter`就是这样的"全家桶"，包含了：
- MyBatis-Plus核心包
- MyBatis核心包
- 自动配置类
- 默认配置

### 3.2 核心配置文件


**📝 application.yml配置**

```yaml
# 服务器配置
server:
  port: 8080

# Spring配置
spring:
  # 数据源配置
  datasource:
    # 驱动类名（MySQL 8.x）
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 数据库连接地址
    url: jdbc:mysql://localhost:3306/mybatis_plus_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    # 用户名
    username: root
    # 密码
    password: 123456

# MyBatis-Plus配置
mybatis-plus:
  # 配置文件位置
  configuration:
    # 日志输出（开发时开启，方便查看SQL）
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # 驼峰命名转换
    map-underscore-to-camel-case: true
  # 全局配置
  global-config:
    db-config:
      # 表名前缀
      table-prefix: t_
      # 主键策略（自增）
      id-type: auto
```

> 💡 **配置项通俗解释**
> 
> - `driver-class-name`：告诉程序用什么"车"去连接数据库（MySQL的驱动）
> - `url`：数据库的"地址"（IP + 端口 + 数据库名）
> - `log-impl`：打开"日志监控"，能看到执行了什么SQL
> - `map-underscore-to-camel-case`：数据库字段`user_name`自动对应Java的`userName`

**🔄 配置文件格式对比**

**application.properties格式**
```properties
# 数据源配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/mybatis_plus_db
spring.datasource.username=root
spring.datasource.password=123456

# MyBatis-Plus配置
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus.global-config.db-config.id-type=auto
```

**application.yml格式**（推荐✨）
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis_plus_db
    username: root
    password: 123456
```

> 🎯 **为什么推荐yml？**
> 
> - 层次清晰，一眼看出配置结构
> - 避免重复前缀
> - 更适合复杂配置

### 3.3 启动类配置


**🎬 Application主启动类**

```java
package com.example.mybatisplus;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * MyBatis-Plus 主启动类
 */
@SpringBootApplication
@MapperScan("com.example.mybatisplus.mapper")  // ← 扫描Mapper接口
public class MybatisPlusApplication {
    
    public static void main(String[] args) {
        SpringApplication.run(MybatisPlusApplication.class, args);
        System.out.println("MyBatis-Plus 启动成功！");
    }
}
```

**📍 注解说明**

| 注解 | 作用 | 必需性 |
|------|------|--------|
| `@SpringBootApplication` | 标识Spring Boot主启动类 | 🔴 必需 |
| `@MapperScan` | 扫描Mapper接口，自动创建代理对象 | 🔴 必需 |

> ⚠️ **@MapperScan注意事项**
> 
> - 包路径要写对，不然找不到Mapper
> - 可以扫描多个包：`@MapperScan({"com.example.mapper1", "com.example.mapper2"})`

---

## 4. 🔄 版本兼容性说明


### 4.1 版本对应关系


**📊 主流版本兼容表**

| MyBatis-Plus | Spring Boot | MyBatis | JDK | 数据库 |
|--------------|-------------|---------|-----|--------|
| `3.5.3+` | `2.6.x ~ 3.x` | `3.5.x` | `8+` | MySQL 5.7+ |
| `3.4.x` | `2.3.x ~ 2.7.x` | `3.5.x` | `8+` | MySQL 5.7+ |
| `3.3.x` | `2.1.x ~ 2.5.x` | `3.5.x` | `8` | MySQL 5.6+ |

> 💡 **版本选择建议**
> 
> **新项目推荐：** MyBatis-Plus `3.5.3.1` + Spring Boot `2.7.14`
> 
> - 稳定性好
> - 社区活跃
> - 文档完善

### 4.2 MySQL驱动版本


**🚗 驱动版本对应**

```
MySQL 5.x 数据库：
└─ 使用驱动：mysql-connector-java 5.1.x
   └─ 驱动类：com.mysql.jdbc.Driver

MySQL 8.x 数据库：
└─ 使用驱动：mysql-connector-java 8.0.x
   └─ 驱动类：com.mysql.cj.jdbc.Driver
```

**配置示例对比**

<kbd>MySQL 5.x 配置</kbd>
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_name?useUnicode=true&characterEncoding=utf8
```

<kbd>MySQL 8.x 配置</kbd>
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_name?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
```

> ⚠️ **MySQL 8.x 额外注意**
> 
> - 必须添加`serverTimezone=Asia/Shanghai`参数
> - 驱动类名变了，注意是`cj`不是`jdbc`

### 4.3 依赖冲突解决


**🔧 常见冲突场景**

**场景1：MyBatis版本冲突**
```
项目依赖树：
├─ mybatis-plus-boot-starter 3.5.3 
│  └─ mybatis 3.5.10
└─ mybatis-spring-boot-starter 2.2.0
   └─ mybatis 3.5.6  ← 冲突！
```

**解决方案：排除低版本**
```xml
<dependency>
    <groupId>org.mybatis.spring.boot</groupId>
    <artifactId>mybatis-spring-boot-starter</artifactId>
    <exclusions>
        <exclusion>
            <groupId>org.mybatis</groupId>
            <artifactId>mybatis</artifactId>
        </exclusion>
    </exclusions>
</dependency>
```

**场景2：日志框架冲突**

不同依赖引入了不同的日志框架，解决思路：
1. 统一使用`slf4j`接口
2. 排除冲突的日志实现
3. 引入统一的日志桥接器

---

## 5. 🏢 多模块项目结构


### 5.1 单模块 vs 多模块


**📦 结构对比**

**单模块项目（适合小项目）**
```
mybatis-plus-demo/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com.example/
│   │   │       ├── controller/
│   │   │       ├── service/
│   │   │       ├── mapper/
│   │   │       └── entity/
│   │   └── resources/
│   └── test/
└── pom.xml
```

**多模块项目（适合大项目）**
```
mybatis-plus-parent/              ← 父模块
├── common/                        ← 公共模块
│   └── pom.xml
├── entity/                        ← 实体类模块
│   └── pom.xml
├── service/                       ← 服务模块
│   └── pom.xml
├── web/                          ← Web模块
│   └── pom.xml
└── pom.xml                       ← 父pom
```

> 💡 **选择建议**
> 
> | 项目规模 | 推荐结构 | 理由 |
> |---------|---------|------|
> | 个人学习/小项目 | 单模块 | 简单直观 |
> | 中小企业项目 | 单模块 | 够用，维护简单 |
> | 大型项目/微服务 | 多模块 | 分工明确，复用性强 |

### 5.2 多模块配置示例


**🏗️ 父模块pom.xml**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project>
    <modelVersion>4.0.0</modelVersion>
    
    <groupId>com.example</groupId>
    <artifactId>mybatis-plus-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>  <!-- 注意是pom类型 -->
    
    <!-- 子模块列表 -->
    <modules>
        <module>common</module>
        <module>entity</module>
        <module>service</module>
        <module>web</module>
    </modules>
    
    <!-- 版本统一管理 -->
    <properties>
        <java.version>1.8</java.version>
        <mybatis-plus.version>3.5.3.1</mybatis-plus.version>
    </properties>
    
    <!-- 依赖版本管理（不会实际引入） -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
```

**🧩 子模块pom.xml（service模块示例）**

```xml
<project>
    <modelVersion>4.0.0</modelVersion>
    
    <!-- 继承父模块 -->
    <parent>
        <groupId>com.example</groupId>
        <artifactId>mybatis-plus-parent</artifactId>
        <version>1.0.0</version>
    </parent>
    
    <artifactId>service</artifactId>
    
    <dependencies>
        <!-- 依赖entity模块 -->
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>entity</artifactId>
            <version>${project.version}</version>
        </dependency>
        
        <!-- MyBatis-Plus（版本继承父模块） -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
        </dependency>
    </dependencies>
</project>
```

**📋 模块依赖关系**
```
web 模块
  ↓ 依赖
service 模块
  ↓ 依赖
entity 模块
  ↓ 依赖
common 模块

依赖方向：上层依赖下层，下层不能依赖上层
```

---

## 6. 📁 包结构规划最佳实践


### 6.1 标准包结构


**🗂️ 推荐的包结构**

```
com.example.mybatisplus/
├── 📁 controller/              # 控制器层
│   ├── UserController.java
│   └── OrderController.java
├── 📁 service/                 # 服务层
│   ├── IUserService.java      # 接口
│   └── impl/                  # 实现类
│       └── UserServiceImpl.java
├── 📁 mapper/                  # 数据访问层
│   ├── UserMapper.java
│   └── OrderMapper.java
├── 📁 entity/                  # 实体类
│   ├── User.java
│   └── Order.java
├── 📁 dto/                     # 数据传输对象
│   ├── UserDTO.java
│   └── UserQueryDTO.java
├── 📁 vo/                      # 视图对象
│   └── UserVO.java
├── 📁 config/                  # 配置类
│   └── MybatisPlusConfig.java
├── 📁 common/                  # 公共类
│   ├── result/                # 统一返回
│   ├── exception/             # 异常处理
│   └── constant/              # 常量
└── 📄 MybatisPlusApplication.java  # 启动类
```

### 6.2 各层职责说明


**📌 分层架构理解**

```
用户请求流程：

浏览器/APP
    ↓ HTTP请求
┌─────────────────┐
│  Controller层   │  ← 接收请求，参数校验
│  (控制器)       │
└─────────────────┘
    ↓ 调用Service
┌─────────────────┐
│   Service层     │  ← 业务逻辑处理
│  (业务层)       │
└─────────────────┘
    ↓ 调用Mapper
┌─────────────────┐
│   Mapper层      │  ← 数据库操作
│  (持久层)       │
└─────────────────┘
    ↓ 操作
┌─────────────────┐
│   Database      │  ← MySQL数据库
└─────────────────┘
```

**各层详细说明**

| 层次 | 职责 | 示例代码 |
|------|------|---------|
| **Controller** | 接收请求、返回响应 | `@RestController` |
| **Service** | 业务逻辑、事务控制 | `@Service` |
| **Mapper** | 数据库CRUD操作 | `extends BaseMapper<T>` |
| **Entity** | 数据库表映射 | `@TableName("t_user")` |

### 6.3 命名规范


**📝 统一命名约定**

**实体类命名**
```java
// ✅ 推荐
User.java          // 对应表 t_user
UserInfo.java      // 对应表 t_user_info
OrderDetail.java   // 对应表 t_order_detail

// ❌ 不推荐
TUser.java         // 不要带表前缀
user.java          // 首字母要大写
```

**Mapper接口命名**
```java
// ✅ 推荐
UserMapper.java
OrderMapper.java

// ❌ 不推荐
UserDao.java       // 不要用Dao后缀
IUserMapper.java   // 接口不加I前缀
```

**Service命名**
```java
// ✅ 推荐（接口）
IUserService.java
IOrderService.java

// ✅ 推荐（实现类）
UserServiceImpl.java
OrderServiceImpl.java

// ❌ 不推荐
UserService.java   // 实现类要加Impl
UserServiceImp.java // 拼写错误
```

### 6.4 完整示例


**👤 User用户模块完整结构**

**Entity实体类**
```java
package com.example.mybatisplus.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@TableName("t_user")
public class User {
    
    @TableId(type = IdType.AUTO)
    private Long id;
    
    private String username;
    
    private String password;
    
    private String email;
    
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
    
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```

**Mapper接口**
```java
package com.example.mybatisplus.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.example.mybatisplus.entity.User;

public interface UserMapper extends BaseMapper<User> {
    // 继承BaseMapper，自动拥有CRUD方法
}
```

**Service接口**
```java
package com.example.mybatisplus.service;

import com.baomidou.mybatisplus.extension.service.IService;
import com.example.mybatisplus.entity.User;

public interface IUserService extends IService<User> {
    // 继承IService，自动拥有CRUD方法
}
```

**Service实现类**
```java
package com.example.mybatisplus.service.impl;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.example.mybatisplus.entity.User;
import com.example.mybatisplus.mapper.UserMapper;
import com.example.mybatisplus.service.IUserService;
import org.springframework.stereotype.Service;

@Service
public class UserServiceImpl extends ServiceImpl<UserMapper, User> 
    implements IUserService {
    // 继承ServiceImpl，实现IUserService
}
```

**Controller控制器**
```java
package com.example.mybatisplus.controller;

import com.example.mybatisplus.entity.User;
import com.example.mybatisplus.service.IUserService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/user")
public class UserController {
    
    @Autowired
    private IUserService userService;
    
    @GetMapping("/list")
    public List<User> list() {
        return userService.list();
    }
    
    @GetMapping("/{id}")
    public User getById(@PathVariable Long id) {
        return userService.getById(id);
    }
    
    @PostMapping
    public boolean save(@RequestBody User user) {
        return userService.save(user);
    }
}
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 依赖管理：Maven帮我们自动下载和管理jar包
🔸 pom.xml：项目的"购物清单"，配置需要的依赖
🔸 Starter：Spring Boot的"全家桶"，一次引入多个依赖
🔸 版本兼容：注意MyBatis-Plus、Spring Boot、MySQL版本对应
🔸 包结构：Controller → Service → Mapper → Database 分层清晰
🔸 命名规范：统一的命名让代码更易读易维护
```

### 7.2 项目搭建检查清单


**✅ 搭建步骤核对表**

- [x] **步骤1：** 创建Spring Boot项目
- [x] **步骤2：** 配置pom.xml，添加MP依赖
- [x] **步骤3：** 配置application.yml数据源
- [x] **步骤4：** 添加@MapperScan扫描Mapper
- [x] **步骤5：** 规划包结构，创建基础目录
- [x] **步骤6：** 编写实体类、Mapper、Service
- [x] **步骤7：** 测试运行，验证配置

### 7.3 常见问题快速排查


**🐛 问题诊断表**

| 问题现象 | 可能原因 | 解决方案 |
|---------|---------|---------|
| 启动报错找不到Driver | 缺少MySQL驱动 | 检查pom.xml是否添加mysql-connector |
| 找不到Mapper | @MapperScan路径错误 | 检查包路径是否正确 |
| SQL日志不显示 | 未配置日志 | 添加`log-impl`配置 |
| 版本冲突 | 依赖版本不兼容 | 使用`<exclusions>`排除冲突 |

### 7.4 学习建议


> 💡 **给新手的建议**
> 
> **学习顺序：**
> 1. 先搭建最简单的单模块项目
> 2. 理解每个配置项的作用
> 3. 动手写一遍完整流程
> 4. 遇到问题查看控制台日志
> 5. 熟练后再考虑多模块
>
> **记忆技巧：**
> - pom.xml = 购物清单
> - Starter = 全家桶套餐
> - Controller → Service → Mapper = 请求处理流水线
> - Entity = 数据库表的Java版本

**🎯 下一步学习**
- 掌握了项目搭建，接下来学习：
  - MyBatis-Plus基础CRUD操作
  - 条件构造器查询
  - 分页插件使用
  - 代码生成器

**核心记忆口诀：**
```
项目搭建三步走，
依赖配置数据源，
包结构分层要清晰，
版本兼容别忽略！
```