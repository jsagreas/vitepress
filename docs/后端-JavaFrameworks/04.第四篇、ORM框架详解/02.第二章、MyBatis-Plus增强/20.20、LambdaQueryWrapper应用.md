---
title: 20、LambdaQueryWrapper应用
---
## 📚 目录

1. [什么是LambdaQueryWrapper](#1-什么是LambdaQueryWrapper)
2. [Lambda条件构造详解](#2-Lambda条件构造详解)
3. [方法引用语法深入](#3-方法引用语法深入)
4. [类型推断机制](#4-类型推断机制)
5. [链式调用风格](#5-链式调用风格)
6. [字段安全访问](#6-字段安全访问)
7. [条件动态组装](#7-条件动态组装)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 什么是LambdaQueryWrapper


### 1.1 从一个实际问题说起


假设你要查询数据库中所有年龄大于18岁的用户，传统方式你可能要写：

```sql
SELECT * FROM user WHERE age > 18
```

但在Java代码里，如果用字符串写字段名，很容易出错：

```java
// ❌ 容易写错字段名
queryWrapper.gt("agee", 18);  // 不小心多打了个e，编译器不会报错！
```

**LambdaQueryWrapper就是来解决这个问题的**！它让你用Java的方式来写查询条件，编译器会帮你检查错误。

### 1.2 核心概念通俗解释


> **什么是Lambda？**
> 
> Lambda就像是一个"临时工"，你需要它干什么活，就临时写个小函数给它。不用专门定义一个正式的方法。

> **什么是Wrapper？**
>
> Wrapper就是"包装器"，把复杂的SQL查询条件包装成简单易用的Java代码。

**LambdaQueryWrapper = Lambda + QueryWrapper**
- **Lambda部分**：用方法引用代替字符串字段名
- **QueryWrapper部分**：构建查询条件的工具

### 1.3 对比理解


```
传统QueryWrapper写法：
┌─────────────────────────────┐
│ queryWrapper.eq("name", "张三")│  ← 字段名是字符串
│ queryWrapper.gt("age", 18)   │  ← 容易写错，编译不检查
└─────────────────────────────┘

LambdaQueryWrapper写法：
┌─────────────────────────────┐
│ wrapper.eq(User::getName, "张三")│  ← 用方法引用
│ wrapper.gt(User::getAge, 18)  │  ← 写错会编译报错
└─────────────────────────────┘
```

**核心优势**：
- ✅ **类型安全**：编译时检查，写错字段名直接报错
- ✅ **重构友好**：字段改名，IDE自动更新
- ✅ **代码提示**：IDE能自动提示可用字段
- ✅ **可读性强**：代码即文档，一看就懂

---

## 2. 🔧 Lambda条件构造详解


### 2.1 基本使用方式


**第一步：创建LambdaQueryWrapper对象**

```java
// 方式1：直接new创建
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();

// 方式2：通过Wrappers工具类创建（推荐）
LambdaQueryWrapper<User> wrapper = Wrappers.lambdaQuery();
```

**第二步：添加查询条件**

```java
// 等值查询：查询姓名为"张三"的用户
wrapper.eq(User::getName, "张三");

// 大于查询：查询年龄大于18的用户
wrapper.gt(User::getAge, 18);

// 模糊查询：查询姓名包含"张"的用户
wrapper.like(User::getName, "张");
```

### 2.2 常用条件方法对照表


| 方法名 | **SQL含义** | **使用示例** | **通俗解释** |
|-------|-----------|------------|------------|
| `eq` | `=` 等于 | `wrapper.eq(User::getAge, 18)` | 年龄**等于**18 |
| `ne` | `!=` 不等于 | `wrapper.ne(User::getStatus, 0)` | 状态**不是**0 |
| `gt` | `>` 大于 | `wrapper.gt(User::getAge, 18)` | 年龄**大于**18 |
| `ge` | `>=` 大于等于 | `wrapper.ge(User::getAge, 18)` | 年龄**至少**18 |
| `lt` | `<` 小于 | `wrapper.lt(User::getAge, 60)` | 年龄**小于**60 |
| `le` | `<=` 小于等于 | `wrapper.le(User::getAge, 60)` | 年龄**最多**60 |
| `like` | `LIKE '%值%'` | `wrapper.like(User::getName, "张")` | 名字**包含**"张" |
| `likeLeft` | `LIKE '%值'` | `wrapper.likeLeft(User::getName, "三")` | 名字**以"三"结尾** |
| `likeRight` | `LIKE '值%'` | `wrapper.likeRight(User::getName, "张")` | 名字**以"张"开头** |
| `isNull` | `IS NULL` | `wrapper.isNull(User::getEmail)` | 邮箱**为空** |
| `isNotNull` | `IS NOT NULL` | `wrapper.isNotNull(User::getEmail)` | 邮箱**不为空** |
| `in` | `IN (...)` | `wrapper.in(User::getAge, 18, 20, 25)` | 年龄**是这几个之一** |
| `between` | `BETWEEN ... AND ...` | `wrapper.between(User::getAge, 18, 60)` | 年龄**在18到60之间** |

### 2.3 实战案例：组合条件查询


**需求**：查询年龄在18-60岁之间，并且姓张的用户

```java
LambdaQueryWrapper<User> wrapper = Wrappers.lambdaQuery();

wrapper.between(User::getAge, 18, 60)      // 年龄在18-60之间
       .likeRight(User::getName, "张");     // 名字以"张"开头

List<User> users = userMapper.selectList(wrapper);
```

**生成的SQL**：
```sql
SELECT * FROM user 
WHERE age BETWEEN 18 AND 60 
  AND name LIKE '张%'
```

---

## 3. 📖 方法引用语法深入


### 3.1 什么是方法引用


> **通俗理解**：方法引用就像是给方法起个"外号"，用`::`这个符号来表示。

**传统Lambda写法**：
```java
// 完整的Lambda表达式
wrapper.eq(user -> user.getName(), "张三");
```

**方法引用写法**：
```java
// 简化成方法引用
wrapper.eq(User::getName, "张三");
```

### 3.2 方法引用的格式解析


```
User::getName
 ↓    ↓
类名  方法名

完整理解：
┌──────────────────────────────────┐
│ User::getName                    │
│ ↓                                │
│ 意思是：调用User类的getName方法    │
│ 等同于：user -> user.getName()   │
└──────────────────────────────────┘
```

### 3.3 方法引用的类型


**①类型1：对象的实例方法引用**
```java
// 引用getter方法（最常用）
wrapper.eq(User::getName, "张三");
wrapper.gt(User::getAge, 18);
```

**②类型2：类的静态方法引用**
```java
// 不常用，了解即可
wrapper.apply(Math::max);
```

**③类型3：构造方法引用**
```java
// 不常用，了解即可
User::new
```

### 3.4 为什么要用方法引用


**场景对比**：假设字段名从`userName`改成了`name`

```
❌ 传统字符串方式：
wrapper.eq("userName", "张三");  
↓ 字段改名后
需要手动查找所有"userName"字符串，逐个修改，容易遗漏

✅ 方法引用方式：
wrapper.eq(User::getUserName, "张三");
↓ 字段改名后
IDE重构工具自动把getUserName改成getName，一键搞定
```

---

## 4. 🧠 类型推断机制


### 4.1 什么是类型推断


> **通俗解释**：类型推断就像一个聪明的助手，你不用明说它也能猜出你要的是什么类型。

**不用类型推断**（代码啰嗦）：
```java
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<User>();
//                  ↑这里写了                              ↑这里又写了，重复！
```

**使用类型推断**（简洁优雅）：
```java
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
//                  ↑只写一次                          ↑用<>让编译器自己推断
```

### 4.2 类型推断的工作原理


```
代码执行流程：

LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
        ↓
编译器看到左边是 LambdaQueryWrapper<User>
        ↓
推断右边的<>里也应该是User
        ↓
自动补全为：new LambdaQueryWrapper<User>()
```

### 4.3 类型推断的实际应用


**示例1：方法参数推断**
```java
// 方法定义
public List<User> findUsers(LambdaQueryWrapper<User> wrapper) {
    return userMapper.selectList(wrapper);
}

// 调用时可以省略类型
findUsers(new LambdaQueryWrapper<>());  // 自动推断为User
```

**示例2：链式调用中的推断**
```java
List<User> users = userMapper.selectList(
    Wrappers.<User>lambdaQuery()           // 明确指定泛型
            .eq(User::getName, "张三")
            .gt(User::getAge, 18)
);

// 或者让IDE推断
LambdaQueryWrapper<User> wrapper = Wrappers.lambdaQuery();  // 自动推断
```

---

## 5. 🔗 链式调用风格


### 5.1 什么是链式调用


> **通俗比喻**：链式调用就像搭积木，一块接一块，最后搭成一个完整的作品。

**非链式写法**（传统方式）：
```java
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
wrapper.eq(User::getName, "张三");
wrapper.gt(User::getAge, 18);
wrapper.isNotNull(User::getEmail);
// 每次都要重复写wrapper，代码冗余
```

**链式写法**（优雅方式）：
```java
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
wrapper.eq(User::getName, "张三")
       .gt(User::getAge, 18)
       .isNotNull(User::getEmail);
// 一气呵成，代码简洁
```

### 5.2 链式调用的原理


**核心秘密**：每个方法返回的都是`this`（自己）

```java
// MyBatis-Plus源码简化示例
public LambdaQueryWrapper<T> eq(SFunction<T, ?> column, Object val) {
    // 添加条件的逻辑
    addCondition("=", column, val);
    
    return this;  // ← 关键：返回自己，才能继续调用
}
```

**执行流程图解**：
```
wrapper.eq(...)      返回wrapper
   ↓
wrapper.gt(...)      返回wrapper  
   ↓
wrapper.isNotNull(...) 返回wrapper
   ↓
最终的wrapper
```

### 5.3 链式调用的最佳实践


**①合理换行，提高可读性**
```java
// ✅ 推荐：条件较多时分行写
LambdaQueryWrapper<User> wrapper = Wrappers.lambdaQuery();
wrapper.eq(User::getName, "张三")
       .between(User::getAge, 18, 60)
       .like(User::getEmail, "@qq.com")
       .orderByDesc(User::getCreateTime);

// ❌ 不推荐：全写在一行，难以阅读
wrapper.eq(User::getName, "张三").between(User::getAge, 18, 60).like(User::getEmail, "@qq.com");
```

**②相关条件分组**
```java
wrapper
    // 基本条件
    .eq(User::getStatus, 1)
    .isNotNull(User::getEmail)
    
    // 时间范围
    .ge(User::getCreateTime, startTime)
    .le(User::getCreateTime, endTime)
    
    // 排序
    .orderByDesc(User::getCreateTime);
```

---

## 6. 🛡️ 字段安全访问


### 6.1 什么是字段安全


> **核心问题**：怎么保证你写的字段名是真实存在的，不会在运行时才发现写错了？

**传统方式的问题**：
```java
// ❌ 字符串方式：字段名写错了，编译器不知道
queryWrapper.eq("namee", "张三");  // name写成了namee
// 编译通过 ✓
// 运行报错 ✗  找不到namee这个字段！
```

**Lambda方式的优势**：
```java
// ✅ 方法引用：字段名写错了，编译器立即报错
wrapper.eq(User::getNamee, "张三");  // getName写成了getNamee
// 编译就报错 ✗  没有getNamee这个方法！
```

### 6.2 字段安全的三重保障


**保障1️⃣：编译时检查**
```
你写代码时 → IDE实时检查 → 发现错误立即标红 → 编译不通过
```

**保障2️⃣：重构支持**
```
实体类字段改名：
User类：userName → name
        ↓
IDE全局查找：User::getUserName
        ↓
一键重构为：User::getName
        ↓
所有地方自动更新 ✓
```

**保障3️⃣：代码提示**
```
输入：wrapper.eq(User::get
        ↓
IDE自动列出所有getter方法：
  - getName()
  - getAge()
  - getEmail()
  - ...
```

### 6.3 安全访问对比表


| 特性 | **字符串方式** | **Lambda方式** | **优势方** |
|-----|--------------|---------------|-----------|
| **编译检查** | ❌ 不检查 | ✅ 严格检查 | Lambda |
| **重构支持** | ❌ 需手动查找 | ✅ 自动更新 | Lambda |
| **代码提示** | ❌ 无提示 | ✅ 智能提示 | Lambda |
| **类型安全** | ❌ 运行时才知道 | ✅ 编译时确定 | Lambda |
| **学习成本** | ✅ 简单直接 | ⚠️ 需要理解Lambda | 字符串 |

### 6.4 实际案例对比


**场景**：需求变更，User表的`phone`字段改名为`mobile`

```java
// ❌ 字符串方式
queryWrapper.eq("phone", "13800138000");     // 改前
queryWrapper.eq("mobile", "13800138000");    // 改后：需要手动找到所有"phone"改成"mobile"

// ✅ Lambda方式
wrapper.eq(User::getPhone, "13800138000");   // 改前
wrapper.eq(User::getMobile, "13800138000");  // 改后：IDE重构工具一键完成
```

---

## 7. 🎨 条件动态组装


### 7.1 什么是动态条件


> **生活场景类比**：
> 你去餐厅点菜，有人说"不要香菜"，有人说"多放辣"，有人什么都不说。
> 厨师要根据每个人的要求，动态调整做菜方式。

**编程中的动态条件**：根据实际情况，决定要不要添加某个查询条件。

### 7.2 传统方式的问题


**场景**：根据用户输入的条件查询，但用户可能不填某些条件

```java
// ❌ 不好的写法：需要大量if判断
LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();

if (name != null && !name.isEmpty()) {
    wrapper.like(User::getName, name);
}

if (minAge != null) {
    wrapper.ge(User::getAge, minAge);
}

if (maxAge != null) {
    wrapper.le(User::getAge, maxAge);
}

if (email != null && !email.isEmpty()) {
    wrapper.like(User::getEmail, email);
}
// 代码冗长，每个条件都要判断
```

### 7.3 动态条件的优雅写法


**核心方法**：`condition`参数，控制条件是否生效

```java
// ✅ 优雅写法：用condition参数控制
LambdaQueryWrapper<User> wrapper = Wrappers.lambdaQuery();

wrapper.like(StringUtils.hasText(name), User::getName, name)
       .ge(minAge != null, User::getAge, minAge)
       .le(maxAge != null, User::getAge, maxAge)
       .like(StringUtils.hasText(email), User::getEmail, email);
```

**condition参数工作原理**：
```
wrapper.like(condition, column, value)
              ↓
        判断condition
              ↓
    true: 添加这个条件
    false: 忽略这个条件
```

### 7.4 动态条件的实战案例


**案例1：用户列表搜索功能**

```java
/**
 * 用户搜索：支持按姓名、年龄范围、邮箱模糊查询
 * 用户可以只填部分条件，系统自动组装
 */
public List<User> searchUsers(String name, Integer minAge, Integer maxAge, String email) {
    LambdaQueryWrapper<User> wrapper = Wrappers.lambdaQuery();
    
    wrapper
        // 姓名：用户填了就模糊查询，没填就不查
        .like(StringUtils.hasText(name), User::getName, name)
        
        // 最小年龄：填了就添加>=条件
        .ge(minAge != null, User::getAge, minAge)
        
        // 最大年龄：填了就添加<=条件
        .le(maxAge != null, User::getAge, maxAge)
        
        // 邮箱：用户填了就模糊查询
        .like(StringUtils.hasText(email), User::getEmail, email)
        
        // 默认按创建时间倒序
        .orderByDesc(User::getCreateTime);
    
    return userMapper.selectList(wrapper);
}
```

**案例2：复杂条件组合**

```java
/**
 * 高级搜索：支持多种条件组合
 */
public List<User> advancedSearch(UserSearchDTO dto) {
    LambdaQueryWrapper<User> wrapper = Wrappers.lambdaQuery();
    
    wrapper
        // 基础条件
        .eq(dto.getStatus() != null, User::getStatus, dto.getStatus())
        .like(StringUtils.hasText(dto.getKeyword()), User::getName, dto.getKeyword())
        
        // 时间范围（两个都填才生效）
        .between(
            dto.getStartTime() != null && dto.getEndTime() != null,
            User::getCreateTime,
            dto.getStartTime(),
            dto.getEndTime()
        )
        
        // VIP条件（只有VIP用户才查询）
        .eq(Boolean.TRUE.equals(dto.getIsVip()), User::getVipLevel, 1)
        
        // 排序条件（用户选择了排序字段才排序）
        .orderByDesc(
            "age".equals(dto.getSortField()),
            User::getAge
        )
        .orderByDesc(
            "createTime".equals(dto.getSortField()),
            User::getCreateTime
        );
    
    return userMapper.selectList(wrapper);
}
```

### 7.5 动态条件的高级技巧


**技巧1：使用Optional简化判断**

```java
// 传统写法
wrapper.eq(name != null && !name.isEmpty(), User::getName, name);

// Optional写法（Java 8+）
Optional.ofNullable(name)
        .filter(StringUtils::hasText)
        .ifPresent(val -> wrapper.eq(User::getName, val));
```

**技巧2：条件分组**

```java
wrapper
    .and(w -> w  // 分组1：年龄或VIP条件满足其一
        .gt(User::getAge, 18)
        .or()
        .eq(User::getVipLevel, 1)
    )
    .and(w -> w  // 分组2：姓名或邮箱包含关键词
        .like(User::getName, keyword)
        .or()
        .like(User::getEmail, keyword)
    );
```

**生成的SQL**：
```sql
SELECT * FROM user
WHERE (age > 18 OR vip_level = 1)
  AND (name LIKE '%keyword%' OR email LIKE '%keyword%')
```

### 7.6 动态条件最佳实践


```
✅ DO - 推荐做法：
├─ 用condition参数控制条件是否生效
├─ 对空值进行判断，避免无意义的条件
├─ 合理分组，使SQL逻辑清晰
└─ 添加注释说明复杂条件的业务含义

❌ DON'T - 不推荐：
├─ 不判断就直接用null值查询
├─ 过多的if-else嵌套
├─ 条件逻辑混乱，难以维护
└─ 不考虑SQL性能，盲目添加条件
```

---

## 8. 📋 核心要点总结


### 8.1 知识体系回顾


```
LambdaQueryWrapper 核心知识树：

LambdaQueryWrapper
├─ 📌 基本概念
│  ├─ Lambda + QueryWrapper的组合
│  ├─ 用方法引用代替字符串字段名
│  └─ 类型安全的查询条件构造器
│
├─ 🔧 条件构造
│  ├─ eq、ne、gt、lt等比较条件
│  ├─ like、in、between等范围条件
│  └─ isNull、isNotNull等状态判断
│
├─ 📖 方法引用
│  ├─ User::getName格式
│  ├─ 编译时类型检查
│  └─ 重构友好
│
├─ 🧠 类型推断
│  ├─ <> 菱形语法
│  ├─ 编译器自动推断泛型
│  └─ 简化代码书写
│
├─ 🔗 链式调用
│  ├─ 每个方法返回this
│  ├─ 一气呵成的流畅写法
│  └─ 提高代码可读性
│
├─ 🛡️ 字段安全
│  ├─ 编译时检查字段名
│  ├─ IDE智能提示
│  └─ 重构自动更新
│
└─ 🎨 动态组装
   ├─ condition参数控制条件
   ├─ 根据实际情况动态添加
   └─ 避免大量if-else判断
```

### 8.2 必须掌握的核心要点


> **🔸 核心1：为什么用LambdaQueryWrapper？**
> - 传统字符串方式容易写错字段名，编译器不检查
> - Lambda方式用方法引用，编译时就能发现错误
> - 重构时IDE自动更新，省时省力

> **🔸 核心2：方法引用的本质**
> - `User::getName` 等同于 `user -> user.getName()`
> - `::`符号表示方法引用
> - 左边是类名，右边是方法名

> **🔸 核心3：链式调用的秘密**
> - 每个方法都返回`this`（自己）
> - 所以可以一直`.`下去
> - 代码简洁，逻辑清晰

> **🔸 核心4：动态条件的精髓**
> - 用`condition`参数控制条件是否生效
> - `wrapper.eq(condition, column, value)`
> - condition为true添加条件，false忽略

### 8.3 常见方法速查表


| 需求 | **Lambda写法** | **生成SQL** |
|-----|---------------|------------|
| 等于 | `wrapper.eq(User::getName, "张三")` | `name = '张三'` |
| 不等于 | `wrapper.ne(User::getAge, 18)` | `age != 18` |
| 大于 | `wrapper.gt(User::getAge, 18)` | `age > 18` |
| 大于等于 | `wrapper.ge(User::getAge, 18)` | `age >= 18` |
| 小于 | `wrapper.lt(User::getAge, 60)` | `age < 60` |
| 小于等于 | `wrapper.le(User::getAge, 60)` | `age <= 60` |
| 模糊查询 | `wrapper.like(User::getName, "张")` | `name LIKE '%张%'` |
| 左模糊 | `wrapper.likeLeft(User::getName, "三")` | `name LIKE '%三'` |
| 右模糊 | `wrapper.likeRight(User::getName, "张")` | `name LIKE '张%'` |
| 为空 | `wrapper.isNull(User::getEmail)` | `email IS NULL` |
| 不为空 | `wrapper.isNotNull(User::getEmail)` | `email IS NOT NULL` |
| 在...之中 | `wrapper.in(User::getAge, 18, 20, 25)` | `age IN (18, 20, 25)` |
| 在范围内 | `wrapper.between(User::getAge, 18, 60)` | `age BETWEEN 18 AND 60` |

### 8.4 学习建议与最佳实践


**🎯 学习路径建议**：
```
第1步：理解基本概念 → 知道什么是Lambda、Wrapper
   ↓
第2步：掌握常用方法 → eq、gt、like等条件方法
   ↓
第3步：熟悉方法引用 → User::getName的写法
   ↓
第4步：应用动态条件 → 根据实际场景灵活组装
   ↓
第5步：链式调用优化 → 写出优雅的查询代码
```

**💡 代码风格建议**：
```java
// ✅ 推荐：清晰的条件分组
LambdaQueryWrapper<User> wrapper = Wrappers.lambdaQuery();
wrapper
    // 基本条件
    .eq(User::getStatus, 1)
    .isNotNull(User::getEmail)
    
    // 动态条件
    .like(StringUtils.hasText(name), User::getName, name)
    .between(minAge != null && maxAge != null, User::getAge, minAge, maxAge)
    
    // 排序
    .orderByDesc(User::getCreateTime);

// ❌ 不推荐：全部挤在一起
wrapper.eq(User::getStatus,1).isNotNull(User::getEmail).like(StringUtils.hasText(name),User::getName,name);
```

**🚨 常见错误与解决**：

| 错误现象 | **原因** | **解决方案** |
|---------|---------|------------|
| `编译报错：找不到方法` | 方法引用写错了 | 检查getter方法名是否正确 |
| `SQL条件不生效` | condition判断有问题 | 检查condition表达式的逻辑 |
| `空指针异常` | 值为null未判断 | 添加null检查或使用condition |
| `SQL性能差` | 条件过多或索引缺失 | 优化条件，检查数据库索引 |

### 8.5 记忆口诀


```
Lambda构造条件妙，
方法引用最可靠。
编译检查保安全，
重构代码无烦恼。

链式调用一气成，
条件动态巧组装。
空值判断别忘记，
性能优化要思量。
```

**核心记忆**：
- LambdaQueryWrapper = 类型安全 + 方法引用 + 链式调用
- 方法引用格式：`类名::方法名`，如`User::getName`
- 动态条件核心：`wrapper.method(condition, column, value)`
- 链式调用原理：每个方法返回`this`，所以能一直调用下去