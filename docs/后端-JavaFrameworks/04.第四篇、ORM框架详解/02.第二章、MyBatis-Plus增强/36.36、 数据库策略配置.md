---
title: 36、 数据库策略配置
---
## 📚 目录


1. [StrategyConfig策略配置概述](#1-StrategyConfig策略配置概述)
2. [表名包含过滤](#2-表名包含过滤)
3. [表名排除过滤](#3-表名排除过滤)
4. [字段填充策略](#4-字段填充策略)
5. [命名策略设置](#5-命名策略设置)
6. [表前缀处理](#6-表前缀处理)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 StrategyConfig策略配置概述



### 1.1 什么是策略配置



**通俗理解**：策略配置就像是给代码生成器制定"游戏规则"

```
类比生活场景：
你去餐厅点餐 → 告诉服务员你的需求
- "我要这几道菜"（包含过滤）
- "我不吃辣"（排除过滤）  
- "麻烦少放盐"（自定义策略）

代码生成器也一样 → 通过策略配置告诉它
- "我要生成这些表"（表名过滤）
- "字段这样命名"（命名策略）
- "自动填充这些字段"（填充策略）
```

**核心作用**：
- 🎯 **精准控制**：决定生成哪些表、哪些字段
- 🔧 **自动处理**：自动转换命名、处理前缀
- ⚡ **提升效率**：一次配置，批量生成

### 1.2 策略配置的位置



```
代码生成器工作流程：

第1步：全局配置 (GlobalConfig)
   ↓  
第2步：数据源配置 (DataSourceConfig)  
   ↓
第3步：策略配置 (StrategyConfig) ← 我们在这里！
   ↓
第4步：包配置 (PackageConfig)
   ↓
第5步：模板配置 (TemplateConfig)
   ↓
第6步：执行生成
```

**配置示例框架**：
```java
// 创建代码生成器
AutoGenerator generator = new AutoGenerator();

// 1. 全局配置
generator.setGlobalConfig(globalConfig);

// 2. 数据源配置  
generator.setDataSource(dataSourceConfig);

// 3. 策略配置 ← 本章重点
StrategyConfig strategy = new StrategyConfig();
// ...各种策略设置...
generator.setStrategy(strategy);

// 4. 其他配置...
```

---

## 2. 📋 表名包含过滤



### 2.1 包含过滤的含义



**通俗解释**：告诉生成器"只生成我指定的这些表"

```
数据库中的表：          你的需求：
├─ user               只需要这3个表 →  user ✓
├─ order                              order ✓  
├─ product                            product ✓
├─ log                 不需要 →       log ✗
├─ temp_data          不需要 →       temp_data ✗
└─ system_config      不需要 →       system_config ✗
```

### 2.2 包含过滤的配置方式



**方式一：指定具体表名（推荐）**
```java
StrategyConfig strategy = new StrategyConfig();

// 只生成user、order、product这3个表
strategy.setInclude("user", "order", "product");
```

**方式二：使用通配符**
```java
// 生成所有以t_开头的表
strategy.setInclude("t_.*");

// 生成所有以_info结尾的表  
strategy.setInclude(".*_info");

// 组合使用（生成t_user或t_order）
strategy.setInclude("t_user", "t_order");
```

### 2.3 实际应用场景



**场景1：新项目开发**
```java
// 第一期只做用户和订单模块
strategy.setInclude(
    "sys_user",      // 用户表
    "sys_role",      // 角色表  
    "order_main",    // 订单主表
    "order_item"     // 订单明细表
);

结果：只生成这4个表的代码，其他表不管
```

**场景2：按业务模块生成**
```java
// 只生成商品相关的表
strategy.setInclude(
    "product_.*"     // 所有product_开头的表
);

可能包括：
- product_info      → 生成 ✓
- product_category  → 生成 ✓  
- product_stock     → 生成 ✓
- order_main        → 不生成 ✗（不符合规则）
```

**场景3：测试验证**
```java
// 先用一个表测试配置是否正确
strategy.setInclude("user");

// 确认没问题后，再生成全部
strategy.setInclude("user", "order", "product", "..."); 
```

### 2.4 包含过滤的注意事项



⚠️ **重要提示**：

| 情况 | 说明 | 示例 |
|------|------|------|
| **表名大小写** | `区分大小写` | `User` ≠ `user` |
| **表名必须存在** | `数据库中必须有这个表` | 写错名字会报错 |
| **支持正则** | `可以使用.*等通配符` | `"t_.*"` 匹配所有t_开头 |
| **多个表名** | `用逗号分隔` | `"user", "order"` |

---

## 3. 🚫 表名排除过滤



### 3.1 排除过滤的含义



**通俗解释**：告诉生成器"除了这些表，其他都生成"

```
对比理解：

包含过滤（白名单）：     排除过滤（黑名单）：
只要这些 →              除了这些都要 →
  user ✓                  log ✗
  order ✓                 temp ✗  
  其他 ✗                  其他 ✓
```

### 3.2 排除过滤的配置方式



**基本配置**：
```java
StrategyConfig strategy = new StrategyConfig();

// 排除这些表，其他都生成
strategy.setExclude("log", "temp_data", "system_config");
```

**使用通配符**：
```java
// 排除所有临时表（temp_开头）
strategy.setExclude("temp_.*");

// 排除所有日志表（_log结尾）
strategy.setExclude(".*_log");

// 排除多种类型的表
strategy.setExclude(
    "temp_.*",        // 所有临时表
    ".*_backup",      // 所有备份表
    ".*_history"      // 所有历史表  
);
```

### 3.3 包含和排除的配合使用



**重要原则**：`include` 优先级高于 `exclude`

```java
// 配置1：只用include
strategy.setInclude("user", "order");
// 结果：只生成user和order

// 配置2：只用exclude  
strategy.setExclude("log", "temp");
// 结果：除了log和temp，其他都生成

// 配置3：同时使用（不推荐，容易混乱）
strategy.setInclude("user", "order", "log");
strategy.setExclude("log");
// 结果：生成user、order、log（include优先）
```

💡 **最佳实践**：
```
✅ 推荐：表少时用include（明确要什么）
✅ 推荐：表多时用exclude（明确不要什么）
❌ 避免：同时使用include和exclude
```

### 3.4 实际应用场景



**场景1：排除系统表**
```java
// 业务表很多，但不要生成系统维护的表
strategy.setExclude(
    "qrtz_.*",           // Quartz定时任务表
    "act_.*",            // Activiti工作流表  
    ".*_log",            // 所有日志表
    ".*_temp",           // 所有临时表
    "flyway_.*"          // 数据库版本管理表
);
```

**场景2：排除测试数据**
```java
// 生产环境表都要，但测试表不要
strategy.setExclude(
    "test_.*",           // 测试表
    "demo_.*",           // 演示表
    ".*_backup"          // 备份表
);
```

---

## 4. 🔄 字段填充策略



### 4.1 什么是字段填充



**通俗理解**：某些字段在保存/更新时，自动给它赋值，不用你手动写

```
生活类比：
你去医院看病
- 挂号时间 → 系统自动记录（不用你说）
- 医生姓名 → 系统自动填写（当前登录医生）
- 病历编号 → 系统自动生成（有规则）

数据库也一样：
- create_time → 插入时自动填充当前时间
- update_time → 更新时自动填充当前时间  
- create_user → 插入时自动填充当前用户
- update_user → 更新时自动填充当前用户
```

### 4.2 字段填充的类型



MyBatis-Plus提供4种填充时机：

| 填充类型 | **说明** | **典型字段** |
|---------|---------|-------------|
| `INSERT` | `插入时填充` | `create_time, create_user` |
| `UPDATE` | `更新时填充` | `update_time, update_user` |
| `INSERT_UPDATE` | `插入和更新都填充` | `version（乐观锁）` |
| `DEFAULT` | `不填充（默认）` | `普通业务字段` |

### 4.3 配置字段填充策略



**第一步：在策略配置中声明字段**
```java
StrategyConfig strategy = new StrategyConfig();

// 方式1：单个字段配置
strategy.setTableFillList(Arrays.asList(
    // 插入时填充
    new TableFill("create_time", FieldFill.INSERT),
    new TableFill("create_user", FieldFill.INSERT),
    
    // 更新时填充  
    new TableFill("update_time", FieldFill.UPDATE),
    new TableFill("update_user", FieldFill.UPDATE),
    
    // 插入和更新都填充
    new TableFill("version", FieldFill.INSERT_UPDATE)
));
```

**生成的实体类效果**：
```java
public class User {
    private Long id;
    private String username;
    
    // 插入时自动填充
    @TableField(fill = FieldFill.INSERT)
    private Date createTime;
    
    @TableField(fill = FieldFill.INSERT)
    private String createUser;
    
    // 更新时自动填充
    @TableField(fill = FieldFill.UPDATE)  
    private Date updateTime;
    
    @TableField(fill = FieldFill.UPDATE)
    private String updateUser;
    
    // 插入和更新都填充
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private Integer version;
}
```

### 4.4 实现填充处理器



**光配置还不够，要告诉它"填充什么值"**：

```java
@Component
public class MyMetaObjectHandler implements MetaObjectHandler {
    
    // 插入时的填充规则
    @Override
    public void insertFill(MetaObject metaObject) {
        // 填充创建时间
        this.strictInsertFill(metaObject, "createTime", 
            Date.class, new Date());
        
        // 填充创建人（从当前登录用户获取）
        this.strictInsertFill(metaObject, "createUser", 
            String.class, getCurrentUser());
        
        // 初始化版本号
        this.strictInsertFill(metaObject, "version", 
            Integer.class, 1);
    }
    
    // 更新时的填充规则  
    @Override
    public void updateFill(MetaObject metaObject) {
        // 填充更新时间
        this.strictUpdateFill(metaObject, "updateTime", 
            Date.class, new Date());
        
        // 填充更新人
        this.strictUpdateFill(metaObject, "updateUser", 
            String.class, getCurrentUser());
    }
    
    // 获取当前登录用户（示意）
    private String getCurrentUser() {
        // 实际从Session或Token中获取
        return "admin";
    }
}
```

### 4.5 字段填充的使用效果



**不需要写代码，自动完成**：
```java
// 插入数据
User user = new User();
user.setUsername("张三");
user.setAge(25);
// 不用写create_time，自动填充！
// 不用写create_user，自动填充！

userMapper.insert(user);

// 数据库实际保存的数据：
// username: 张三
// age: 25
// create_time: 2025-09-23 10:30:00  ← 自动填充
// create_user: admin                ← 自动填充
// version: 1                        ← 自动填充
```

---

## 5. 📝 命名策略设置



### 5.1 什么是命名策略



**通俗理解**：数据库字段和Java属性的命名规则不一样，需要自动转换

```
数据库命名风格：          Java命名风格：
user_name               userName
order_id                orderId  
create_time             createTime
product_price           productPrice

命名策略就是：告诉生成器如何做这个转换
```

### 5.2 MyBatis-Plus的命名策略类型



**NamingStrategy 枚举说明**：

| 策略类型 | **说明** | **示例** |
|---------|---------|---------|
| `no_change` | `不做任何改变` | `user_name → user_name` |
| `underline_to_camel` | `下划线转驼峰（常用）` | `user_name → userName` |

### 5.3 命名策略配置



**完整配置示例**：
```java
StrategyConfig strategy = new StrategyConfig();

// 数据库表映射到实体的命名策略
strategy.setNaming(NamingStrategy.underline_to_camel);

// 数据库字段映射到实体的命名策略  
strategy.setColumnNaming(NamingStrategy.underline_to_camel);
```

**配置效果对比**：

```
数据库表：sys_user

配置1：不转换
strategy.setNaming(NamingStrategy.no_change);
→ 生成实体类：SysUser ✗（看起来怪怪的）

配置2：下划线转驼峰（推荐）  
strategy.setNaming(NamingStrategy.underline_to_camel);
→ 生成实体类：SysUser ✓（符合Java规范）
```

### 5.4 表名和字段名的转换规则



**表名转换**：
```
数据库表名             Java实体类名
-----------------    ------------------
user                → User
sys_user            → SysUser
t_order_info        → TOrderInfo
product_category    → ProductCategory
```

**字段名转换**：
```
数据库字段             Java属性
-----------------    ------------------
id                  → id
user_name           → userName
create_time         → createTime
is_deleted          → isDeleted（注意：Boolean类型）
order_amount        → orderAmount
```

### 5.5 特殊情况处理



**情况1：Boolean类型的is前缀**
```java
// 数据库字段：is_deleted
// 错误生成：isIsDeleted ✗
// 正确生成：isDeleted ✓

// MyBatis-Plus会智能处理
strategy.setEntityBooleanColumnRemoveIsPrefix(true); // 移除is前缀
```

**情况2：全大写表名**
```java
// 数据库表名：USER
strategy.setCapitalMode(true);  // 开启大写命名
// 生成：USER.java

// 建议还是用驼峰
strategy.setNaming(NamingStrategy.underline_to_camel);
// 生成：User.java ✓
```

**情况3：保持数据库原样**
```java
// 某些特殊场景需要保持原名
strategy.setNaming(NamingStrategy.no_change);
strategy.setColumnNaming(NamingStrategy.no_change);

// sys_user → sys_user（不推荐，不符合Java规范）
```

---

## 6. ✂️ 表前缀处理



### 6.1 为什么需要去除表前缀



**通俗理解**：很多公司的表都有统一前缀，但Java实体类不需要

```
数据库设计规范：
系统模块    → 表名前缀
用户模块    → sys_user, sys_role, sys_menu
订单模块    → order_main, order_item, order_log
商品模块    → product_info, product_category

如果不去前缀：
sys_user  → SysUser     ✗（类名太长）
sys_role  → SysRole     ✗（前缀重复）

去除前缀后：
sys_user  → User        ✓（简洁清晰）
sys_role  → Role        ✓（符合规范）
```

### 6.2 表前缀配置方式



**单个前缀配置**：
```java
StrategyConfig strategy = new StrategyConfig();

// 去除sys_前缀
strategy.setTablePrefix("sys_");

// 转换效果：
// sys_user     → User
// sys_role     → Role  
// sys_menu     → Menu
// order_main   → OrderMain（不影响其他前缀）
```

**多个前缀配置**：
```java
// 同时去除多个前缀
strategy.setTablePrefix("sys_", "t_", "tb_");

// 转换效果：
// sys_user     → User
// t_order      → Order
// tb_product   → Product
```

### 6.3 字段前缀处理



**字段前缀场景**：
```
数据库字段            问题
------------------  ---------------
user_id             → userId（正常）
user_name           → userName（正常）
user_create_time    → userCreateTime（前缀冗余）
user_update_time    → userUpdateTime（前缀冗余）

理想效果：
user_create_time    → createTime（去掉user_前缀）
```

**配置字段前缀**：
```java
// 去除字段前缀
strategy.setFieldPrefix("user_");

// 转换效果：
// user_id          → id  
// user_name        → name
// user_create_time → createTime
```

### 6.4 前缀处理的实际应用



**场景1：多模块系统**
```java
// 系统有多个模块，每个模块有前缀
strategy.setTablePrefix(
    "sys_",      // 系统模块
    "order_",    // 订单模块
    "product_",  // 商品模块
    "customer_"  // 客户模块
);

// 生成的实体类：
// sys_user      → User
// order_main    → Main
// product_info  → Info
// customer_base → Base
```

**场景2：临时表前缀**
```java
// 开发环境用tmp_前缀，生产环境要去掉
strategy.setTablePrefix("tmp_");

// tmp_test_user    → TestUser
// tmp_demo_order   → DemoOrder
```

### 6.5 前缀处理的注意事项



⚠️ **重要提醒**：

```
情况1：前缀要精确匹配
配置：strategy.setTablePrefix("sys_");
✓ sys_user    → User
✗ system_log  → SystemLog（不匹配，不处理）

情况2：不要配置太短的前缀  
配置：strategy.setTablePrefix("s_");
问题：可能误伤其他表
- sys_user   → ysUser ✗（错误去除）
- student    → tudent ✗（错误去除）

情况3：优先级问题
同时配置：strategy.setTablePrefix("sys_", "s_");
表名：sys_user
结果：User（sys_优先匹配）
```

💡 **最佳实践**：
```java
// 1. 明确前缀规范
strategy.setTablePrefix("sys_", "order_", "product_");

// 2. 配合命名策略
strategy.setNaming(NamingStrategy.underline_to_camel);

// 3. 验证生成结果
// 先生成一个表，检查类名是否正确
```

---

## 7. 📋 核心要点总结



### 7.1 策略配置的核心概念



```
策略配置 = 告诉代码生成器的"工作规则"
├─ 表过滤：要生成哪些表
├─ 字段填充：哪些字段自动赋值  
├─ 命名转换：如何转换成Java命名
└─ 前缀处理：去掉表名/字段的前缀
```

### 7.2 关键配置方法速查



| 配置项 | **方法** | **作用** | **示例** |
|-------|---------|---------|---------|
| **包含表** | `setInclude()` | `指定生成哪些表` | `"user", "order"` |
| **排除表** | `setExclude()` | `指定不生成哪些表` | `".*_log", "temp_.*"` |
| **字段填充** | `setTableFillList()` | `设置自动填充字段` | `create_time, update_time` |
| **表名策略** | `setNaming()` | `表名转换规则` | `underline_to_camel` |
| **字段策略** | `setColumnNaming()` | `字段名转换规则` | `underline_to_camel` |
| **表前缀** | `setTablePrefix()` | `去除表名前缀` | `"sys_", "t_"` |
| **字段前缀** | `setFieldPrefix()` | `去除字段前缀` | `"user_"` |

### 7.3 完整配置示例



**实际项目的完整策略配置**：
```java
StrategyConfig strategy = new StrategyConfig();

// 1. 表过滤策略
strategy.setInclude("sys_user", "sys_role", "sys_menu");
// 或者用排除
// strategy.setExclude(".*_log", "temp_.*", ".*_backup");

// 2. 字段填充策略
strategy.setTableFillList(Arrays.asList(
    new TableFill("create_time", FieldFill.INSERT),
    new TableFill("create_user", FieldFill.INSERT),
    new TableFill("update_time", FieldFill.UPDATE),
    new TableFill("update_user", FieldFill.UPDATE)
));

// 3. 命名策略（下划线转驼峰）
strategy.setNaming(NamingStrategy.underline_to_camel);
strategy.setColumnNaming(NamingStrategy.underline_to_camel);

// 4. 去除表前缀
strategy.setTablePrefix("sys_", "t_");

// 5. 其他策略（可选）
strategy.setEntityLombokModel(true);           // 使用Lombok
strategy.setRestControllerStyle(true);         // Controller用@RestController
strategy.setControllerMappingHyphenStyle(true);// URL用连字符风格

// 应用策略
generator.setStrategy(strategy);
```

### 7.4 使用技巧总结



**🎯 选择过滤策略**：
```
① 表少（<10个）   → 用include明确指定
② 表多（>10个）   → 用exclude排除不要的
③ 有规律的表      → 用正则表达式匹配
④ 首次使用        → 先生成1个表测试
```

**🔄 字段填充最佳实践**：
```
① 公共字段：create_time、update_time等
② 审计字段：create_user、update_user等  
③ 版本控制：version（乐观锁）
④ 逻辑删除：is_deleted
```

**📝 命名策略建议**：
```
✅ 推荐：underline_to_camel（符合Java规范）
❌ 避免：no_change（不符合规范）
💡 特殊：Boolean字段记得移除is前缀
```

**✂️ 前缀处理原则**：
```
① 统一前缀必须去掉（sys_、t_等）
② 前缀要精确匹配，不要太短
③ 配置多个前缀时注意优先级
④ 验证生成的类名是否正确
```

### 7.5 常见问题排查



**问题1：生成了不想要的表**
```
原因：没配置include或exclude
解决：strategy.setInclude("需要的表");
```

**问题2：字段没有自动填充**
```
原因：只配置了策略，没写填充处理器
解决：实现MetaObjectHandler接口
```

**问题3：类名/属性名不符合规范**
```
原因：命名策略不对或前缀没去掉  
解决：检查setNaming()和setTablePrefix()
```

**问题4：Boolean字段有双重is**
```
原因：is_deleted → isIsDeleted
解决：strategy.setEntityBooleanColumnRemoveIsPrefix(true);
```

---

# 💡 核心记忆口诀



```
策略配置定规则，生成代码有章法
包含排除筛选表，通配正则好帮手
填充策略省代码，时间用户自动填  
命名转换要规范，下划线变驼峰峰
表名前缀要去掉，实体类名更简洁
```

**关键理解**：
- 策略配置 = 告诉生成器"怎么做"
- 表过滤 = 决定"做什么"  
- 字段填充 = 实现"自动化"
- 命名转换 = 保证"规范化"
- 前缀处理 = 保持"简洁化"