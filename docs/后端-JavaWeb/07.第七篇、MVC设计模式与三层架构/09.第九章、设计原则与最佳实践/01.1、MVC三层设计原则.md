---
title: 1、MVC三层设计原则
---
## 📚 目录

1. [设计原则概述](#1-设计原则概述)
2. [单一职责原则](#2-单一职责原则)
3. [开闭原则](#3-开闭原则)
4. [依赖倒置原则](#4-依赖倒置原则)
5. [接口隔离原则](#5-接口隔离原则)
6. [里氏替换原则](#6-里氏替换原则)
7. [分层解耦实践](#7-分层解耦实践)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 设计原则概述


### 1.1 什么是设计原则


**💡 生活类比**：
```
盖房子的规则：         写代码的规则：
- 地基要稳固          - 代码结构要清晰
- 各房间功能明确      - 每个类职责单一  
- 水电分离布线        - 不同功能要解耦
- 预留扩展空间        - 方便后期维护
```

> 📖 **核心概念**  
> 设计原则就是写代码时遵循的"建筑规范"，让你的程序像精心设计的房子一样，结构清晰、功能明确、易于维护和扩展

### 1.2 为什么需要设计原则


**🔍 问题分析**：没有设计原则的代码就像：
```
❌ 糟糕的代码：
- 一个类做很多事情（像卧室既当厨房又当厕所）
- 修改一处影响多处（牵一发动全身）
- 代码重复到处都是（复制粘贴编程）
- 难以测试和维护（改个小功能要动很多地方）
```

**✅ 好的设计带来的好处**：
- 🎯 **代码清晰**：每个部分职责明确
- 🔧 **易于维护**：改动影响范围小
- 🚀 **便于扩展**：添加新功能不影响原有功能
- 🧪 **容易测试**：各个模块可以独立测试

### 1.3 设计原则在MVC中的作用


```
传统混乱架构：              MVC设计良好架构：
┌─────────────────┐        ┌─────────┐   ┌─────────┐   ┌─────────┐
│   一个大类包含   │        │  View   │   │ Controller │  │  Model  │
│  界面+业务+数据 │   VS   │  视图层  │ ← │   控制层   │ → │  数据层  │
│     全部逻辑    │        │         │   │           │   │         │
└─────────────────┘        └─────────┘   └─────────┘   └─────────┘
      难以维护                          职责清晰，便于维护
```

---

## 2. 🎯 单一职责原则


### 2.1 原则理解


> 📖 **原则定义**  
> Single Responsibility Principle (SRP)：一个类应该只有一个引起它变化的原因

**🔍 通俗解释**：
- 就像专业分工：厨师专门做菜，服务员专门服务客人
- 每个类只做一件事，做好一件事
- 如果一个类有多个变化原因，说明它承担了太多职责

### 2.2 违反单一职责的例子


**❌ 错误示例**：一个类做太多事情
```java
// 这个用户类违反了单一职责原则
class User {
    private String username;
    private String password;
    
    // 用户数据相关 ✓
    public void setUsername(String username) { ... }
    public String getUsername() { ... }
    
    // 数据库操作相关 ❌ 不应该在这里
    public void saveToDatabase() { ... }
    public void deleteFromDatabase() { ... }
    
    // 邮件发送相关 ❌ 也不应该在这里
    public void sendEmail() { ... }
    
    // 密码加密相关 ❌ 同样不应该在这里
    public void encryptPassword() { ... }
}
```

**🚫 问题分析**：
- 修改数据库操作时，用户类要改
- 修改邮件功能时，用户类也要改  
- 修改加密算法时，用户类还要改
- 一个类有太多变化的理由

### 2.3 正确的单一职责实现


**✅ 正确示例**：职责分离
```java
// 用户实体类：只负责用户数据
class User {
    private String username;
    private String password;
    
    // 只包含用户基本属性和方法
    public void setUsername(String username) { this.username = username; }
    public String getUsername() { return username; }
}

// 用户数据库操作类：只负责数据库相关
class UserDAO {
    public void save(User user) { /* 保存到数据库 */ }
    public void delete(String username) { /* 从数据库删除 */ }
    public User findByUsername(String username) { /* 查询用户 */ }
}

// 邮件服务类：只负责邮件发送
class EmailService {
    public void sendWelcomeEmail(String email) { /* 发送欢迎邮件 */ }
    public void sendPasswordResetEmail(String email) { /* 发送重置密码邮件 */ }
}

// 密码服务类：只负责密码处理
class PasswordService {
    public String encrypt(String password) { /* 加密密码 */ }
    public boolean verify(String password, String hash) { /* 验证密码 */ }
}
```

### 2.4 MVC中的单一职责


**🏗️ 三层架构中的职责划分**：
```
Controller层 (控制器)：
🎯 只负责：接收请求、调用业务逻辑、返回响应
❌ 不负责：具体业务处理、数据库操作

Service层 (业务逻辑)：  
🎯 只负责：业务规则、流程控制、事务处理
❌ 不负责：HTTP请求处理、具体数据存储

DAO层 (数据访问)：
🎯 只负责：数据库增删改查操作
❌ 不负责：业务逻辑判断、请求响应处理
```

---

## 3. 🔓 开闭原则


### 3.1 原则理解


> 📖 **原则定义**  
> Open/Closed Principle (OCP)：软件实体应该对扩展开放，对修改封闭

**🔍 通俗解释**：
- 就像手机充电器：可以给不同手机充电（扩展），但充电器本身不用改（封闭）
- 添加新功能时，不修改原有代码，而是增加新代码
- 通过接口和继承来实现

### 3.2 违反开闭原则的例子


**❌ 错误示例**：每次加新功能都要修改原有代码
```java
// 计算器类 - 违反开闭原则
class Calculator {
    public double calculate(String operation, double a, double b) {
        if ("add".equals(operation)) {
            return a + b;
        } else if ("subtract".equals(operation)) {
            return a - b;
        } else if ("multiply".equals(operation)) {
            return a * b;
        }
        // 每次要添加新运算，都要修改这个方法 ❌
        // 比如要加除法，就要在这里加 else if
        return 0;
    }
}
```

**🚫 问题分析**：
- 添加新运算类型，必须修改 `calculate` 方法
- 可能引入新的bug到已有功能
- 违反了"对修改封闭"的原则

### 3.3 正确的开闭原则实现


**✅ 正确示例**：使用接口实现扩展
```java
// 运算接口
interface Operation {
    double execute(double a, double b);
}

// 具体运算实现
class AddOperation implements Operation {
    public double execute(double a, double b) { return a + b; }
}

class SubtractOperation implements Operation {
    public double execute(double a, double b) { return a - b; }
}

class MultiplyOperation implements Operation {
    public double execute(double a, double b) { return a * b; }
}

// 计算器类 - 符合开闭原则
class Calculator {
    public double calculate(Operation operation, double a, double b) {
        return operation.execute(a, b);  // 不需要修改这个方法
    }
}

// 要添加除法运算，只需要新增类，不修改原有代码 ✅
class DivideOperation implements Operation {
    public double execute(double a, double b) { 
        return b != 0 ? a / b : 0; 
    }
}
```

### 3.4 MVC中的开闭原则


**🏗️ 控制器扩展示例**：
```java
// 基础控制器接口
interface Controller {
    String handleRequest(HttpServletRequest request);
}

// 用户控制器
class UserController implements Controller {
    public String handleRequest(HttpServletRequest request) {
        // 处理用户相关请求
        return "user.jsp";
    }
}

// 要添加商品控制器，不需要修改现有代码 ✅
class ProductController implements Controller {
    public String handleRequest(HttpServletRequest request) {
        // 处理商品相关请求  
        return "product.jsp";
    }
}
```

---

## 4. 🔄 依赖倒置原则


### 4.1 原则理解


> 📖 **原则定义**  
> Dependency Inversion Principle (DIP)：高层模块不应该依赖低层模块，两者都应该依赖抽象

**🔍 通俗解释**：
- 就像开车：你依赖的是"方向盘"这个抽象概念，而不是具体某个品牌的方向盘
- 老板（高层）不直接管员工（低层），而是通过部门经理（抽象层）
- 依赖接口，而不依赖具体实现

### 4.2 传统的依赖关系


**❌ 错误示例**：高层直接依赖低层
```java
// 低层模块：具体的数据库操作
class MySQLUserDAO {
    public void save(User user) {
        // MySQL特定的保存逻辑
        System.out.println("保存用户到MySQL数据库");
    }
}

// 高层模块：业务逻辑层
class UserService {
    private MySQLUserDAO userDAO = new MySQLUserDAO(); // ❌ 直接依赖具体实现
    
    public void registerUser(User user) {
        // 业务逻辑
        userDAO.save(user);
    }
}
```

**🚫 问题分析**：
- 如果要换成Oracle数据库，UserService必须修改
- 难以进行单元测试（无法模拟数据库）
- 高层模块被低层模块绑死了

### 4.3 正确的依赖倒置实现


**✅ 正确示例**：依赖抽象接口
```java
// 抽象层：定义接口
interface UserDAO {
    void save(User user);
    User findByUsername(String username);
}

// 低层模块：具体实现
class MySQLUserDAO implements UserDAO {
    public void save(User user) {
        System.out.println("保存用户到MySQL数据库");
    }
    public User findByUsername(String username) { /* MySQL查询 */ return null; }
}

class OracleUserDAO implements UserDAO {
    public void save(User user) {
        System.out.println("保存用户到Oracle数据库");
    }
    public User findByUsername(String username) { /* Oracle查询 */ return null; }
}

// 高层模块：依赖抽象
class UserService {
    private UserDAO userDAO; // ✅ 依赖接口，不依赖具体实现
    
    // 通过构造器注入依赖
    public UserService(UserDAO userDAO) {
        this.userDAO = userDAO;
    }
    
    public void registerUser(User user) {
        // 业务逻辑不变，可以使用任何UserDAO实现
        userDAO.save(user);
    }
}

// 使用时的配置
UserDAO dao = new MySQLUserDAO();  // 可以随时换成OracleUserDAO
UserService service = new UserService(dao);
```

### 4.4 依赖注入的三种方式


**📋 依赖注入方式对比**：

| 注入方式 | **实现方法** | **优点** | **缺点** |
|---------|-------------|----------|----------|
| 🏗️ **构造器注入** | `通过构造函数传入依赖` | `强制依赖，对象创建即完整` | `依赖多时构造函数复杂` |
| ⚙️ **Setter注入** | `通过setter方法设置依赖` | `灵活，可选依赖` | `可能忘记设置必需依赖` |
| 🔧 **接口注入** | `通过特定接口方法注入` | `明确的依赖关系` | `需要额外的接口定义` |

---

## 5. 🔌 接口隔离原则


### 5.1 原则理解


> 📖 **原则定义**  
> Interface Segregation Principle (ISP)：客户端不应该依赖它不需要的接口

**🔍 通俗解释**：
- 就像遥控器：电视遥控器只有电视需要的按钮，不会包含空调的按钮
- 接口要小而专，不要大而全
- 宁可多个专用接口，也不要一个万能接口

### 5.2 违反接口隔离的例子


**❌ 错误示例**：万能接口
```java
// 违反接口隔离原则的大接口
interface Worker {
    void work();      // 工作
    void eat();       // 吃饭  
    void sleep();     // 睡觉
    void program();   // 编程
    void design();    // 设计
    void test();      // 测试
}

// 程序员实现所有方法
class Programmer implements Worker {
    public void work() { System.out.println("写代码"); }
    public void eat() { System.out.println("吃饭"); }
    public void sleep() { System.out.println("睡觉"); }
    public void program() { System.out.println("编程"); } // ✓ 需要
    public void design() { /* 空实现，程序员不一定会设计 */ } // ❌ 不需要但被迫实现
    public void test() { /* 空实现，程序员不一定做测试 */ }    // ❌ 不需要但被迫实现
}
```

**🚫 问题分析**：
- 程序员被迫实现不需要的方法
- 接口变化时影响所有实现类
- 职责不清，接口职责过重

### 5.3 正确的接口隔离实现


**✅ 正确示例**：专用小接口
```java
// 基础工作接口
interface Workable {
    void work();
}

// 生活需求接口
interface Liveable {
    void eat();
    void sleep();
}

// 编程技能接口
interface Programmable {
    void program();
}

// 设计技能接口  
interface Designable {
    void design();
}

// 测试技能接口
interface Testable {
    void test();
}

// 程序员只实现需要的接口 ✅
class Programmer implements Workable, Liveable, Programmable {
    public void work() { System.out.println("上班写代码"); }
    public void eat() { System.out.println("吃工作餐"); }
    public void sleep() { System.out.println("加班后睡觉"); }
    public void program() { System.out.println("编写程序"); }
    // 不需要实现 design() 和 test()
}

// 全栈开发者可以实现更多接口
class FullStackDeveloper implements Workable, Liveable, Programmable, Designable, Testable {
    public void work() { System.out.println("全栈工作"); }
    public void eat() { System.out.println("快餐解决"); }
    public void sleep() { System.out.println("熬夜后补觉"); }
    public void program() { System.out.println("前后端都写"); }
    public void design() { System.out.println("UI设计"); }
    public void test() { System.out.println("自动化测试"); }
}
```

### 5.4 MVC中的接口隔离


**🏗️ 分层接口设计**：
```java
// 用户相关的不同接口，各司其职
interface UserQueryService {
    User findByUsername(String username);
    List<User> findAll();
}

interface UserCommandService {
    void save(User user);
    void update(User user);  
    void delete(String username);
}

// 只需要查询功能的类，只依赖查询接口
class UserReportController {
    private UserQueryService queryService; // ✅ 只依赖需要的接口
    
    public String generateReport() {
        List<User> users = queryService.findAll();
        // 生成报表逻辑
        return "report.jsp";
    }
}
```

---

## 6. 🔄 里氏替换原则


### 6.1 原则理解


> 📖 **原则定义**  
> Liskov Substitution Principle (LSP)：子类对象应该能够替换父类对象，而不破坏程序的正确性

**🔍 通俗解释**：
- 就像不同品牌的USB设备：都能插到USB接口上正常工作
- 子类要能完全替代父类，而不影响程序运行  
- "子类可以扩展父类功能，但不能改变父类原有功能"

### 6.2 违反里氏替换的例子


**❌ 错误示例**：子类改变了父类行为
```java
// 父类：矩形
class Rectangle {
    protected int width;
    protected int height;
    
    public void setWidth(int width) { this.width = width; }
    public void setHeight(int height) { this.height = height; }
    public int getArea() { return width * height; }
}

// 子类：正方形 - 违反里氏替换原则
class Square extends Rectangle {
    @Override
    public void setWidth(int width) {
        this.width = width;
        this.height = width; // ❌ 改变了父类的行为
    }
    
    @Override
    public void setHeight(int height) {
        this.width = height;  // ❌ 改变了父类的行为
        this.height = height;
    }
}

// 测试代码
public void testRectangle(Rectangle rect) {
    rect.setWidth(5);
    rect.setHeight(4);
    // 期望面积是 5*4=20，但如果传入Square，面积是 4*4=16 ❌
    System.out.println("面积: " + rect.getArea());
}
```

**🚫 问题分析**：
- 用Square替换Rectangle时，行为发生了变化
- 违背了调用者的期望
- 子类限制了父类的功能

### 6.3 正确的里氏替换实现


**✅ 正确示例**：重新设计继承关系
```java
// 抽象形状类
abstract class Shape {
    public abstract int getArea();
}

// 矩形类
class Rectangle extends Shape {
    protected int width;
    protected int height;
    
    public Rectangle(int width, int height) {
        this.width = width;
        this.height = height;
    }
    
    public void setWidth(int width) { this.width = width; }
    public void setHeight(int height) { this.height = height; }
    public int getArea() { return width * height; }
}

// 正方形类 - 符合里氏替换原则
class Square extends Shape {
    private int side;
    
    public Square(int side) {
        this.side = side;
    }
    
    public void setSide(int side) { this.side = side; }
    public int getArea() { return side * side; }
}

// 测试代码 - 所有Shape子类都能正确工作
public void testShape(Shape shape) {
    System.out.println("面积: " + shape.getArea()); // ✅ 行为一致
}
```

### 6.4 MVC中的里氏替换


**🏗️ 服务层的替换示例**：
```java
// 用户服务接口
interface UserService {
    boolean login(String username, String password);
    void register(User user);
}

// 基础实现
class SimpleUserService implements UserService {
    public boolean login(String username, String password) {
        // 简单的用户名密码验证
        return "admin".equals(username) && "123456".equals(password);
    }
    
    public void register(User user) {
        // 简单注册逻辑
        System.out.println("用户注册成功: " + user.getUsername());
    }
}

// 增强实现 - 完全可以替换基础实现 ✅
class EnhancedUserService implements UserService {
    public boolean login(String username, String password) {
        // 增强的验证逻辑：加密、验证码等
        // 但依然遵循接口契约，返回boolean
        boolean result = verifyPassword(username, password);
        logLoginAttempt(username, result);  // 额外功能
        return result;
    }
    
    public void register(User user) {
        // 增强的注册逻辑：邮件验证、数据校验等
        validateUser(user);     // 额外校验
        sendWelcomeEmail(user); // 额外功能  
        System.out.println("用户注册成功: " + user.getUsername());
    }
    
    private boolean verifyPassword(String username, String password) { return true; }
    private void logLoginAttempt(String username, boolean success) { }
    private void validateUser(User user) { }
    private void sendWelcomeEmail(User user) { }
}
```

---

## 7. 🏗️ 分层解耦实践


### 7.1 什么是分层解耦


**💡 生活类比**：
```
传统小作坊：                   现代工厂流水线：
老板既管理又生产又销售           管理层 → 生产层 → 销售层
一个人做所有事情               专业分工，各司其职
```

> 📖 **核心概念**  
> 分层解耦就是把复杂的系统按照功能分成不同的层次，每层只关心自己的职责，层与层之间通过接口通信

### 7.2 MVC三层架构解耦


**🏗️ 传统三层架构**：
```
┌─────────────────┐
│   Presentation  │ ← 表示层 (Controller + JSP)
│      Layer      │   处理HTTP请求和响应
├─────────────────┤
│    Business     │ ← 业务逻辑层 (Service)  
│     Layer       │   处理业务规则和流程
├─────────────────┤
│   Data Access   │ ← 数据访问层 (DAO)
│     Layer       │   处理数据库操作
└─────────────────┘
```

**🔗 层间依赖关系**：
```
Controller ──depends on──> Service ──depends on──> DAO
    ↑                         ↑                     ↑
只知道Service接口         只知道DAO接口         只知道数据库
不关心具体实现           不关心具体实现        不关心业务逻辑
```

### 7.3 解耦实践示例


**✅ 完整的解耦示例**：用户管理系统

```java
// 1. 数据访问层接口
interface UserDAO {
    void save(User user);
    User findByUsername(String username);
    List<User> findAll();
}

// 2. 业务逻辑层接口  
interface UserService {
    boolean register(User user);
    boolean login(String username, String password);
    List<User> getAllUsers();
}

// 3. 具体实现 - 数据访问层
class UserDAOImpl implements UserDAO {
    public void save(User user) {
        // 具体的数据库保存逻辑
        System.out.println("保存用户到数据库: " + user.getUsername());
    }
    
    public User findByUsername(String username) {
        // 从数据库查询用户
        return new User(username, "encrypted_password");
    }
    
    public List<User> findAll() {
        // 查询所有用户
        return Arrays.asList(new User("user1", "pass1"), new User("user2", "pass2"));
    }
}

// 4. 具体实现 - 业务逻辑层
class UserServiceImpl implements UserService {
    private UserDAO userDAO; // 依赖DAO接口，不依赖具体实现
    
    public UserServiceImpl(UserDAO userDAO) {
        this.userDAO = userDAO;
    }
    
    public boolean register(User user) {
        // 业务逻辑：检查用户是否已存在
        User existingUser = userDAO.findByUsername(user.getUsername());
        if (existingUser != null) {
            return false; // 用户已存在
        }
        
        // 保存新用户
        userDAO.save(user);
        return true;
    }
    
    public boolean login(String username, String password) {
        User user = userDAO.findByUsername(username);
        return user != null && user.getPassword().equals(password);
    }
    
    public List<User> getAllUsers() {
        return userDAO.findAll();
    }
}

// 5. 表示层 - 控制器
class UserController {
    private UserService userService; // 依赖Service接口，不依赖具体实现
    
    public UserController(UserService userService) {
        this.userService = userService;
    }
    
    public String handleRegister(String username, String password) {
        User user = new User(username, password);
        boolean success = userService.register(user);
        
        if (success) {
            return "register_success.jsp";
        } else {
            return "register_error.jsp";
        }
    }
    
    public String handleLogin(String username, String password) {
        boolean success = userService.login(username, password);
        return success ? "dashboard.jsp" : "login_error.jsp";
    }
}
```

### 7.4 解耦的好处体现


**📊 解耦前后对比**：

| 方面 | **紧耦合** | **松耦合** |
|------|----------|----------|
| 🔧 **可维护性** | `修改一处影响多处` | `修改影响局限在一层` |
| 🧪 **可测试性** | `难以单独测试某个功能` | `每层可以独立测试` |
| 🔄 **可替换性** | `换个数据库要改很多代码` | `只需要换DAO实现类` |
| 🚀 **可扩展性** | `加功能要修改多个类` | `按层次扩展，影响最小` |
| 👥 **团队协作** | `多人修改同一个文件冲突多` | `不同人负责不同层次` |

### 7.5 依赖注入框架


**🔧 Spring框架的依赖注入**：
```java
// 使用Spring注解进行依赖注入
@Service
public class UserServiceImpl implements UserService {
    
    @Autowired  // 自动注入DAO实现
    private UserDAO userDAO;
    
    // 业务方法...
}

@Repository
public class UserDAOImpl implements UserDAO {
    // 数据访问方法...
}

@Controller
public class UserController {
    
    @Autowired  // 自动注入Service实现
    private UserService userService;
    
    // 控制器方法...
}
```

**💡 Spring解耦的好处**：
- 🤖 **自动装配**：框架自动管理依赖关系
- 📝 **配置分离**：依赖关系通过配置文件或注解管理
- 🔄 **运行时替换**：可以在不修改代码的情况下换实现
- 🧪 **测试友好**：容易注入模拟对象进行测试

---

## 8. 📋 核心要点总结


### 8.1 五大设计原则速记


```
🎯 单一职责原则 (SRP)：一个类只做一件事
   记忆口诀："专业的人做专业的事"
   
🔓 开闭原则 (OCP)：对扩展开放，对修改封闭  
   记忆口诀："添加功能不改旧代码"
   
🔄 依赖倒置原则 (DIP)：依赖抽象，不依赖具体
   记忆口诀："面向接口编程"
   
🔌 接口隔离原则 (ISP)：接口小而专，不要大而全
   记忆口诀："需要什么接口就实现什么"
   
🔄 里氏替换原则 (LSP)：子类能完全替代父类
   记忆口诀："子类要守父类的规矩"
```

### 8.2 MVC中的设计原则应用


**🏗️ 架构设计检查清单**：
```
✅ Controller层是否只处理请求和响应？
✅ Service层是否只包含业务逻辑？  
✅ DAO层是否只处理数据操作？
✅ 各层之间是否通过接口通信？
✅ 添加新功能是否不需要修改原有代码？
✅ 每个类的职责是否单一明确？
```

### 8.3 实际开发指导


**🎯 设计原则应用策略**：

1. **🔥 高优先级**：必须遵循
   - 单一职责原则：每个类职责明确
   - 依赖倒置原则：面向接口编程
   - 分层解耦：MVC三层架构

2. **⭐ 中优先级**：重要但可以逐步完善
   - 开闭原则：预留扩展点
   - 接口隔离原则：接口设计要合理

3. **💫 低优先级**：深入学习后应用
   - 里氏替换原则：继承关系设计

### 8.4 常见误区避免


**❌ 设计过度**：
```
新手常见问题：
- 为每个类都定义接口（没必要）
- 把简单功能复杂化（过度设计）
- 一开始就考虑所有可能的扩展点（YAGNI原则：你不需要它）
```

**✅ 渐进式设计**：
```
推荐做法：
- 先写能工作的代码
- 发现重复或难维护时再重构  
- 根据实际需要应用设计原则
- 保持简单，逐步改进
```

### 8.5 学习建议


**📚 学习路径**：
```
初学阶段：
1. 理解MVC三层架构的基本概念
2. 学会写基本的增删改查功能
3. 练习单一职责原则

进阶阶段：  
1. 学习接口的使用和依赖注入
2. 实践依赖倒置原则
3. 掌握Spring框架的基本使用

高级阶段：
1. 深入理解所有设计原则
2. 学习设计模式  
3. 实践微服务架构
```

**🧠 记忆技巧**：
- **SOLID原则**：Single、Open/Closed、Liskov、Interface、Dependency
- **核心思想**：高内聚、低耦合、面向接口、职责分离
- **实践原则**：从简单开始，遇到问题再重构，不要过度设计

**💡 最终目标**：写出像搭积木一样的代码，每个积木（类/模块）功能明确，可以自由组合，易于维护和扩展。