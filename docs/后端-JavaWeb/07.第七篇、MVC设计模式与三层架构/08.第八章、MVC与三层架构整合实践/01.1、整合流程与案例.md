---
title: 1、整合流程与案例
---
## 📚 目录

1. [MVC与三层架构整合概述](#1-MVC与三层架构整合概述)
2. [完整数据流程详解](#2-完整数据流程详解)
3. [请求入口Servlet设计](#3-请求入口Servlet设计)
4. [Controller层实现](#4-Controller层实现)
5. [Service业务层设计](#5-Service业务层设计)
6. [DAO数据访问层](#6-DAO数据访问层)
7. [JSP视图层展示](#7-JSP视图层展示)
8. [完整案例实战](#8-完整案例实战)
9. [整合最佳实践](#9-整合最佳实践)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🎯 MVC与三层架构整合概述


### 1.1 什么是整合架构


> 💡 **简单理解**：就像搭积木一样，把MVC设计模式和三层架构两套"积木"组合在一起，构建出一个更强大、更清晰的Web应用

**整合的本质**：
```
MVC设计模式 + 三层架构 = 完整的JavaWeb应用架构

MVC关注的是：用户界面怎么和业务逻辑分离
三层架构关注的是：业务逻辑内部怎么分层组织
```

### 1.2 为什么要整合？


**🔹 单独使用MVC的问题**：
- Model层可能会变得很臃肿
- 业务逻辑和数据访问混在一起
- 代码难以维护和测试

**🔹 单独使用三层架构的问题**：
- 缺少明确的用户界面处理规范
- 请求响应流程不够清晰

**🔹 整合后的优势**：
- ✅ **职责更清晰**：每一层都有明确的责任
- ✅ **代码更整洁**：相关功能聚集在一起
- ✅ **维护更容易**：修改某个功能只需要改对应层
- ✅ **测试更方便**：每层都可以独立测试

### 1.3 整合架构图解


```
用户浏览器
     ↓ HTTP请求
┌─────────────────┐
│   表示层(Web)    │ ← MVC中的View + Controller
├─────────────────┤
│   业务逻辑层     │ ← MVC中的Model + 三层架构的Service
├─────────────────┤
│   数据访问层     │ ← 三层架构的DAO
└─────────────────┘
     ↓
   数据库
```

---

## 2. 🔄 完整数据流程详解


### 2.1 数据流向总览


**完整的请求-响应流程**：

```
① 用户操作 → ② Servlet接收 → ③ Controller处理 → ④ Service业务 → ⑤ DAO数据库 
                                                                        ↓
⑩ 用户看到 ← ⑨ JSP渲染 ← ⑧ View展示 ← ⑦ Model数据 ← ⑥ 返回结果
```

### 2.2 详细流程分解


**🔸 第1步：用户发起请求**
```
用户在浏览器中：
- 点击链接：<a href="/user/list">查看用户列表</a>
- 提交表单：<form action="/user/add" method="post">
- 或者直接在地址栏输入URL
```

**🔸 第2步：Servlet接收请求**
```java
// 前端控制器模式 - 统一入口
public class FrontController extends HttpServlet {
    protected void doGet(HttpServletRequest request, HttpServletResponse response) {
        String uri = request.getRequestURI(); // 获取请求路径
        // 根据路径分发到不同的Controller
    }
}
```

**🔸 第3步：Controller处理请求**
- 解析请求参数
- 调用对应的Service方法
- 准备响应数据

**🔸 第4步：Service执行业务逻辑**
- 参数验证和转换
- 业务规则处理
- 调用DAO进行数据操作

**🔸 第5步：DAO操作数据库**
- 执行SQL语句
- 处理数据库连接
- 返回查询结果

**🔸 第6-10步：数据返回和视图渲染**
- 数据层层返回
- Controller整理数据
- 转发到JSP页面
- JSP渲染HTML
- 返回给用户浏览器

### 2.3 数据流实例


**以"查看用户列表"为例**：

```
用户点击"用户管理" 
    ↓
UserServlet.doGet()接收请求
    ↓  
UserController.listUsers()处理
    ↓
UserService.getAllUsers()业务处理
    ↓
UserDAO.findAll()查询数据库
    ↓
返回List<User>集合
    ↓
Controller将数据放入request
    ↓
转发到userList.jsp
    ↓
JSP遍历显示用户信息
    ↓
HTML页面返回给浏览器
```

---

## 3. 🚪 请求入口Servlet设计


### 3.1 Servlet的作用理解


> 💡 **形象比喻**：Servlet就像是一个"接待员"，负责接待来访的客户(HTTP请求)，然后把他们引导到对应的办事窗口(Controller)

**Servlet的核心职责**：
- 接收HTTP请求
- 解析请求参数
- 调用相应的Controller
- 处理响应结果

### 3.2 前端控制器模式


**🔹 什么是前端控制器？**

就是用一个统一的Servlet来处理所有请求，而不是为每个功能都写一个Servlet。

```java
// 传统方式：需要很多个Servlet
UserServlet     → 处理用户相关请求
ProductServlet  → 处理商品相关请求
OrderServlet    → 处理订单相关请求

// 前端控制器方式：只需要一个Servlet
FrontController → 处理所有请求，然后分发
```

### 3.3 前端控制器实现


```java
/**
 * 前端控制器 - 所有请求的统一入口
 */
@WebServlet("*.do") // 拦截所有以.do结尾的请求
public class FrontController extends HttpServlet {
    
    @Override
    protected void service(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        // 1. 获取请求路径
        String uri = request.getRequestURI();
        String action = uri.substring(uri.lastIndexOf("/") + 1, uri.lastIndexOf("."));
        
        // 2. 根据action分发请求
        try {
            if ("userList".equals(action)) {
                new UserController().listUsers(request, response);
            } else if ("userAdd".equals(action)) {
                new UserController().addUser(request, response);
            } else if ("productList".equals(action)) {
                new ProductController().listProducts(request, response);
            }
            // ... 其他action处理
        } catch (Exception e) {
            // 统一异常处理
            request.setAttribute("errorMsg", "系统出现错误：" + e.getMessage());
            request.getRequestDispatcher("/error.jsp").forward(request, response);
        }
    }
}
```

### 3.4 配置文件方式的改进


**使用配置文件管理URL映射**：

```properties
# action-mapping.properties
userList=com.example.controller.UserController.listUsers
userAdd=com.example.controller.UserController.addUser  
productList=com.example.controller.ProductController.listProducts
```

```java
public class FrontController extends HttpServlet {
    private Properties actionMappings;
    
    @Override
    public void init() throws ServletException {
        // 加载配置文件
        actionMappings = new Properties();
        try {
            actionMappings.load(getServletContext()
                .getResourceAsStream("/WEB-INF/action-mapping.properties"));
        } catch (IOException e) {
            throw new ServletException("无法加载action映射配置", e);
        }
    }
    
    @Override
    protected void service(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        String action = getActionFromURI(request.getRequestURI());
        String handlerInfo = actionMappings.getProperty(action);
        
        if (handlerInfo != null) {
            // 通过反射调用对应的Controller方法
            invokeController(handlerInfo, request, response);
        } else {
            response.sendError(404, "未找到对应的处理器");
        }
    }
}
```

---

## 4. 🎮 Controller层实现


### 4.1 Controller层的职责


> 💡 **通俗理解**：Controller就像是"业务经理"，接收客户需求，组织各个部门配合完成工作，最后把结果交给客户

**Controller的主要工作**：
- 接收和验证参数
- 调用Service层处理业务
- 整理数据准备展示
- 选择合适的视图

### 4.2 Controller设计原则


**🔹 薄Controller原则**
```java
// ❌ 错误示例：Controller里写业务逻辑
public class UserController {
    public void addUser(HttpServletRequest request, HttpServletResponse response) {
        String name = request.getParameter("name");
        String email = request.getParameter("email");
        
        // 这些业务逻辑不应该在Controller里
        if (name.length() < 2) {
            // 验证逻辑
        }
        if (isEmailExists(email)) {
            // 重复检查逻辑  
        }
        // 数据库操作逻辑
        Connection conn = ...;
        PreparedStatement ps = ...;
    }
}

// ✅ 正确示例：Controller只做协调工作
public class UserController {
    private UserService userService = new UserService();
    
    public void addUser(HttpServletRequest request, HttpServletResponse response) {
        // 1. 获取参数
        String name = request.getParameter("name");
        String email = request.getParameter("email");
        
        // 2. 封装对象
        User user = new User(name, email);
        
        // 3. 调用Service处理
        boolean success = userService.addUser(user);
        
        // 4. 准备响应
        if (success) {
            request.setAttribute("message", "用户添加成功");
            request.getRequestDispatcher("/userList.do").forward(request, response);
        } else {
            request.setAttribute("error", "用户添加失败");
            request.getRequestDispatcher("/addUser.jsp").forward(request, response);
        }
    }
}
```

### 4.3 完整的Controller示例


```java
/**
 * 用户管理Controller
 */
public class UserController {
    private UserService userService = new UserService();
    
    /**
     * 显示用户列表
     */
    public void listUsers(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        try {
            // 1. 获取分页参数
            int pageNum = getIntParameter(request, "pageNum", 1);
            int pageSize = getIntParameter(request, "pageSize", 10);
            
            // 2. 调用Service获取数据
            List<User> userList = userService.getUserList(pageNum, pageSize);
            int totalCount = userService.getUserCount();
            
            // 3. 准备页面数据
            request.setAttribute("userList", userList);
            request.setAttribute("totalCount", totalCount);
            request.setAttribute("currentPage", pageNum);
            
            // 4. 转发到JSP页面
            request.getRequestDispatcher("/WEB-INF/views/userList.jsp")
                   .forward(request, response);
                   
        } catch (Exception e) {
            handleException(e, request, response);
        }
    }
    
    /**
     * 添加用户
     */
    public void addUser(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        try {
            // 1. 获取表单参数
            String name = request.getParameter("name");
            String email = request.getParameter("email");
            String phone = request.getParameter("phone");
            
            // 2. 简单参数验证
            if (isEmpty(name) || isEmpty(email)) {
                request.setAttribute("error", "姓名和邮箱不能为空");
                request.getRequestDispatcher("/WEB-INF/views/addUser.jsp")
                       .forward(request, response);
                return;
            }
            
            // 3. 创建用户对象
            User user = new User();
            user.setName(name);
            user.setEmail(email);
            user.setPhone(phone);
            
            // 4. 调用Service保存
            boolean success = userService.saveUser(user);
            
            // 5. 处理结果
            if (success) {
                response.sendRedirect(request.getContextPath() + "/userList.do");
            } else {
                request.setAttribute("error", "用户保存失败，请重试");
                request.getRequestDispatcher("/WEB-INF/views/addUser.jsp")
                       .forward(request, response);
            }
            
        } catch (Exception e) {
            handleException(e, request, response);
        }
    }
    
    // 工具方法
    private int getIntParameter(HttpServletRequest request, String name, int defaultValue) {
        String value = request.getParameter(name);
        if (isEmpty(value)) {
            return defaultValue;
        }
        try {
            return Integer.parseInt(value);
        } catch (NumberFormatException e) {
            return defaultValue;
        }
    }
    
    private boolean isEmpty(String str) {
        return str == null || str.trim().isEmpty();
    }
    
    private void handleException(Exception e, HttpServletRequest request, 
                               HttpServletResponse response) throws ServletException, IOException {
        request.setAttribute("errorMsg", "系统异常：" + e.getMessage());
        request.getRequestDispatcher("/WEB-INF/views/error.jsp").forward(request, response);
    }
}
```

---

## 5. 🏢 Service业务层设计


### 5.1 Service层的作用


> 💡 **生活化理解**：Service就像银行的业务处理部门，负责处理各种业务规则。比如转账时要检查余额够不够、账户是否正常等等

**Service层核心职责**：
- **业务逻辑处理**：实现具体的业务规则
- **事务管理**：确保数据操作的完整性  
- **数据验证**：检查数据是否符合业务要求
- **异常处理**：处理业务相关的异常情况

### 5.2 Service层设计模式


**🔹 接口 + 实现类模式**

```java
// 1. 定义Service接口
public interface UserService {
    boolean saveUser(User user);
    List<User> getUserList(int pageNum, int pageSize);
    User getUserById(int userId);
    boolean updateUser(User user);
    boolean deleteUser(int userId);
    int getUserCount();
}

// 2. 实现Service接口
public class UserServiceImpl implements UserService {
    private UserDAO userDAO = new UserDAOImpl();
    
    @Override
    public boolean saveUser(User user) {
        // 具体的业务逻辑实现
    }
    // ... 其他方法实现
}
```

**为什么要用接口？**
- ✅ **便于测试**：可以创建测试用的假实现
- ✅ **便于切换**：可以轻松更换不同的实现
- ✅ **代码解耦**：Controller不直接依赖具体实现

### 5.3 完整的Service实现


```java
/**
 * 用户业务服务实现类
 */
public class UserServiceImpl implements UserService {
    private UserDAO userDAO = new UserDAOImpl();
    
    @Override
    public boolean saveUser(User user) {
        try {
            // 1. 业务验证
            if (!isValidUser(user)) {
                throw new BusinessException("用户信息不完整或格式错误");
            }
            
            // 2. 检查邮箱是否已存在
            if (userDAO.existsByEmail(user.getEmail())) {
                throw new BusinessException("该邮箱已被注册");
            }
            
            // 3. 数据预处理
            user.setCreateTime(new Date());
            user.setStatus("ACTIVE"); // 设置默认状态
            
            // 4. 保存到数据库
            return userDAO.save(user) > 0;
            
        } catch (BusinessException e) {
            // 重新抛出业务异常
            throw e;
        } catch (Exception e) {
            // 包装其他异常
            throw new BusinessException("保存用户失败：" + e.getMessage(), e);
        }
    }
    
    @Override
    public List<User> getUserList(int pageNum, int pageSize) {
        try {
            // 1. 参数验证
            if (pageNum < 1) pageNum = 1;
            if (pageSize < 1 || pageSize > 100) pageSize = 10;
            
            // 2. 计算分页参数
            int offset = (pageNum - 1) * pageSize;
            
            // 3. 查询数据
            return userDAO.findByPage(offset, pageSize);
            
        } catch (Exception e) {
            throw new BusinessException("获取用户列表失败：" + e.getMessage(), e);
        }
    }
    
    @Override
    public User getUserById(int userId) {
        try {
            if (userId <= 0) {
                throw new BusinessException("用户ID无效");
            }
            
            User user = userDAO.findById(userId);
            if (user == null) {
                throw new BusinessException("用户不存在");
            }
            
            return user;
            
        } catch (Exception e) {
            throw new BusinessException("获取用户信息失败：" + e.getMessage(), e);
        }
    }
    
    @Override
    public boolean updateUser(User user) {
        try {
            // 1. 验证用户是否存在
            User existingUser = userDAO.findById(user.getId());
            if (existingUser == null) {
                throw new BusinessException("要更新的用户不存在");
            }
            
            // 2. 如果修改了邮箱，检查新邮箱是否重复
            if (!existingUser.getEmail().equals(user.getEmail())) {
                if (userDAO.existsByEmail(user.getEmail())) {
                    throw new BusinessException("该邮箱已被其他用户使用");
                }
            }
            
            // 3. 保留原有的创建时间，更新修改时间
            user.setCreateTime(existingUser.getCreateTime());
            user.setUpdateTime(new Date());
            
            // 4. 更新数据库
            return userDAO.update(user) > 0;
            
        } catch (Exception e) {
            throw new BusinessException("更新用户失败：" + e.getMessage(), e);
        }
    }
    
    @Override
    public boolean deleteUser(int userId) {
        try {
            // 1. 检查用户是否存在
            User user = userDAO.findById(userId);
            if (user == null) {
                throw new BusinessException("要删除的用户不存在");
            }
            
            // 2. 业务规则检查（比如不能删除管理员）
            if ("ADMIN".equals(user.getRole())) {
                throw new BusinessException("不能删除管理员用户");
            }
            
            // 3. 软删除（修改状态而不是真正删除）
            user.setStatus("DELETED");
            user.setUpdateTime(new Date());
            
            return userDAO.update(user) > 0;
            
        } catch (Exception e) {
            throw new BusinessException("删除用户失败：" + e.getMessage(), e);
        }
    }
    
    @Override
    public int getUserCount() {
        try {
            return userDAO.count();
        } catch (Exception e) {
            throw new BusinessException("获取用户总数失败：" + e.getMessage(), e);
        }
    }
    
    /**
     * 验证用户信息是否有效
     */
    private boolean isValidUser(User user) {
        if (user == null) return false;
        if (isEmpty(user.getName()) || user.getName().length() < 2) return false;
        if (isEmpty(user.getEmail()) || !isValidEmail(user.getEmail())) return false;
        return true;
    }
    
    private boolean isEmpty(String str) {
        return str == null || str.trim().isEmpty();
    }
    
    private boolean isValidEmail(String email) {
        return email.matches("^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$");
    }
}

/**
 * 业务异常类
 */
public class BusinessException extends RuntimeException {
    public BusinessException(String message) {
        super(message);
    }
    
    public BusinessException(String message, Throwable cause) {
        super(message, cause);
    }
}
```

---

## 6. 💾 DAO数据访问层


### 6.1 DAO层的职责理解


> 💡 **形象比喻**：DAO就像是"仓库管理员"，专门负责把东西存入仓库、从仓库取出东西、整理仓库等工作。Service是"业务经理"，告诉仓库管理员要存什么、取什么

**DAO层的核心工作**：
- 封装数据库操作
- 提供CRUD基本方法
- 处理SQL语句和参数
- 管理数据库连接

### 6.2 DAO接口设计


```java
/**
 * 用户数据访问接口
 */
public interface UserDAO {
    // 基础的CRUD操作
    int save(User user);                    // 新增用户
    User findById(int id);                  // 根据ID查询
    List<User> findAll();                   // 查询所有用户
    int update(User user);                  // 更新用户
    int deleteById(int id);                 // 删除用户
    
    // 业务相关的查询方法
    List<User> findByPage(int offset, int pageSize);  // 分页查询
    boolean existsByEmail(String email);              // 检查邮箱是否存在
    List<User> findByStatus(String status);           // 根据状态查询
    int count();                                       // 统计用户总数
    User findByEmail(String email);                   // 根据邮箱查询
}
```

### 6.3 DAO实现类


```java
/**
 * 用户数据访问实现类
 */
public class UserDAOImpl implements UserDAO {
    
    @Override
    public int save(User user) {
        String sql = "INSERT INTO users(name, email, phone, status, create_time) VALUES(?, ?, ?, ?, ?)";
        
        try (Connection conn = DBUtil.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql, Statement.RETURN_GENERATED_KEYS)) {
            
            ps.setString(1, user.getName());
            ps.setString(2, user.getEmail());
            ps.setString(3, user.getPhone());
            ps.setString(4, user.getStatus());
            ps.setTimestamp(5, new Timestamp(user.getCreateTime().getTime()));
            
            int result = ps.executeUpdate();
            
            // 获取自动生成的主键
            if (result > 0) {
                try (ResultSet rs = ps.getGeneratedKeys()) {
                    if (rs.next()) {
                        user.setId(rs.getInt(1));
                    }
                }
            }
            
            return result;
            
        } catch (SQLException e) {
            throw new RuntimeException("保存用户失败", e);
        }
    }
    
    @Override
    public User findById(int id) {
        String sql = "SELECT * FROM users WHERE id = ?";
        
        try (Connection conn = DBUtil.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            
            ps.setInt(1, id);
            
            try (ResultSet rs = ps.executeQuery()) {
                if (rs.next()) {
                    return mapRowToUser(rs);
                }
                return null;
            }
            
        } catch (SQLException e) {
            throw new RuntimeException("根据ID查询用户失败", e);
        }
    }
    
    @Override
    public List<User> findByPage(int offset, int pageSize) {
        String sql = "SELECT * FROM users WHERE status != 'DELETED' ORDER BY create_time DESC LIMIT ?, ?";
        List<User> userList = new ArrayList<>();
        
        try (Connection conn = DBUtil.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            
            ps.setInt(1, offset);
            ps.setInt(2, pageSize);
            
            try (ResultSet rs = ps.executeQuery()) {
                while (rs.next()) {
                    userList.add(mapRowToUser(rs));
                }
            }
            
        } catch (SQLException e) {
            throw new RuntimeException("分页查询用户失败", e);
        }
        
        return userList;
    }
    
    @Override
    public boolean existsByEmail(String email) {
        String sql = "SELECT COUNT(*) FROM users WHERE email = ? AND status != 'DELETED'";
        
        try (Connection conn = DBUtil.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            
            ps.setString(1, email);
            
            try (ResultSet rs = ps.executeQuery()) {
                if (rs.next()) {
                    return rs.getInt(1) > 0;
                }
            }
            
        } catch (SQLException e) {
            throw new RuntimeException("检查邮箱是否存在失败", e);
        }
        
        return false;
    }
    
    @Override
    public int update(User user) {
        String sql = "UPDATE users SET name=?, email=?, phone=?, status=?, update_time=? WHERE id=?";
        
        try (Connection conn = DBUtil.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            
            ps.setString(1, user.getName());
            ps.setString(2, user.getEmail());
            ps.setString(3, user.getPhone());
            ps.setString(4, user.getStatus());
            ps.setTimestamp(5, user.getUpdateTime() != null ? 
                new Timestamp(user.getUpdateTime().getTime()) : new Timestamp(System.currentTimeMillis()));
            ps.setInt(6, user.getId());
            
            return ps.executeUpdate();
            
        } catch (SQLException e) {
            throw new RuntimeException("更新用户失败", e);
        }
    }
    
    @Override
    public int count() {
        String sql = "SELECT COUNT(*) FROM users WHERE status != 'DELETED'";
        
        try (Connection conn = DBUtil.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            
            try (ResultSet rs = ps.executeQuery()) {
                if (rs.next()) {
                    return rs.getInt(1);
                }
            }
            
        } catch (SQLException e) {
            throw new RuntimeException("统计用户总数失败", e);
        }
        
        return 0;
    }
    
    /**
     * 将ResultSet行数据映射为User对象
     */
    private User mapRowToUser(ResultSet rs) throws SQLException {
        User user = new User();
        user.setId(rs.getInt("id"));
        user.setName(rs.getString("name"));
        user.setEmail(rs.getString("email"));
        user.setPhone(rs.getString("phone"));
        user.setStatus(rs.getString("status"));
        
        Timestamp createTime = rs.getTimestamp("create_time");
        if (createTime != null) {
            user.setCreateTime(new Date(createTime.getTime()));
        }
        
        Timestamp updateTime = rs.getTimestamp("update_time");
        if (updateTime != null) {
            user.setUpdateTime(new Date(updateTime.getTime()));
        }
        
        return user;
    }
}
```

### 6.4 数据库连接工具类


```java
/**
 * 数据库连接工具类
 */
public class DBUtil {
    private static final String URL = "jdbc:mysql://localhost:3306/javaweb_db?useSSL=false&serverTimezone=UTC";
    private static final String USERNAME = "root";
    private static final String PASSWORD = "123456";
    
    static {
        try {
            Class.forName("com.mysql.cj.jdbc.Driver");
        } catch (ClassNotFoundException e) {
            throw new RuntimeException("MySQL驱动加载失败", e);
        }
    }
    
    /**
     * 获取数据库连接
     */
    public static Connection getConnection() throws SQLException {
        return DriverManager.getConnection(URL, USERNAME, PASSWORD);
    }
    
    /**
     * 关闭数据库资源
     */
    public static void close(Connection conn, PreparedStatement ps, ResultSet rs) {
        try {
            if (rs != null) rs.close();
            if (ps != null) ps.close();
            if (conn != null) conn.close();
        } catch (SQLException e) {
            e.printStackTrace();
        }
    }
}
```

---

## 7. 🎨 JSP视图层展示


### 7.1 JSP在整合架构中的作用


> 💡 **理解JSP的定位**：JSP就像是"前台展示员"，负责把后台处理好的数据以美观、易懂的形式展示给用户看

**JSP的核心职责**：
- 展示Controller传递的数据
- 提供用户交互界面
- 处理页面的展示逻辑
- 收集用户输入

### 7.2 用户列表页面


```jsp
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/fmt" prefix="fmt" %>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>用户管理系统</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .header { background-color: #f4f4f4; padding: 15px; margin-bottom: 20px; }
        .btn { padding: 8px 15px; background-color: #007bff; color: white; 
               text-decoration: none; border-radius: 3px; }
        .btn:hover { background-color: #0056b3; }
        .table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        .table th, .table td { border: 1px solid #ddd; padding: 10px; text-align: left; }
        .table th { background-color: #f8f9fa; }
        .pagination { margin-top: 20px; text-align: center; }
        .pagination a { padding: 8px 12px; margin: 0 2px; text-decoration: none; 
                       border: 1px solid #ddd; }
        .pagination .current { background-color: #007bff; color: white; }
        .success { color: green; font-weight: bold; }
        .error { color: red; font-weight: bold; }
    </style>
</head>
<body>
    <div class="header">
        <h2>用户管理系统</h2>
        <a href="${pageContext.request.contextPath}/addUser.jsp" class="btn">添加新用户</a>
    </div>
    
    <!-- 显示操作结果消息 -->
    <c:if test="${not empty message}">
        <div class="success">${message}</div>
    </c:if>
    <c:if test="${not empty error}">
        <div class="error">${error}</div>
    </c:if>
    
    <!-- 用户列表表格 -->
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>姓名</th>
                <th>邮箱</th>
                <th>电话</th>
                <th>状态</th>
                <th>创建时间</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            <c:choose>
                <c:when test="${not empty userList}">
                    <c:forEach var="user" items="${userList}">
                        <tr>
                            <td>${user.id}</td>
                            <td>${user.name}</td>
                            <td>${user.email}</td>
                            <td>${user.phone}</td>
                            <td>
                                <c:choose>
                                    <c:when test="${user.status == 'ACTIVE'}">
                                        <span style="color: green;">正常</span>
                                    </c:when>
                                    <c:otherwise>
                                        <span style="color: orange;">停用</span>
                                    </c:otherwise>
                                </c:choose>
                            </td>
                            <td>
                                <fmt:formatDate value="${user.createTime}" pattern="yyyy-MM-dd HH:mm"/>
                            </td>
                            <td>
                                <a href="${pageContext.request.contextPath}/userDetail.do?id=${user.id}">查看</a>
                                |
                                <a href="${pageContext.request.contextPath}/editUser.do?id=${user.id}">编辑</a>
                                |
                                <a href="javascript:deleteUser(${user.id}, '${user.name}')" 
                                   style="color: red;">删除</a>
                            </td>
                        </tr>
                    </c:forEach>
                </c:when>
                <c:otherwise>
                    <tr>
                        <td colspan="7" style="text-align: center; color: #999;">
                            暂无用户数据
                        </td>
                    </tr>
                </c:otherwise>
            </c:choose>
        </tbody>
    </table>
    
    <!-- 分页导航 -->
    <div class="pagination">
        <c:set var="totalPages" value="${(totalCount + 9) / 10}" />
        <c:if test="${currentPage > 1}">
            <a href="${pageContext.request.contextPath}/userList.do?pageNum=${currentPage - 1}">上一页</a>
        </c:if>
        
        <c:forEach begin="1" end="${totalPages}" var="pageNum">
            <c:choose>
                <c:when test="${pageNum == currentPage}">
                    <span class="current">${pageNum}</span>
                </c:when>
                <c:otherwise>
                    <a href="${pageContext.request.contextPath}/userList.do?pageNum=${pageNum}">${pageNum}</a>
                </c:otherwise>
            </c:choose>
        </c:forEach>
        
        <c:if test="${currentPage < totalPages}">
            <a href="${pageContext.request.contextPath}/userList.do?pageNum=${currentPage + 1}">下一页</a>
        </c:if>
    </div>
    
    <script>
        function deleteUser(userId, userName) {
            if (confirm('确定要删除用户 "' + userName + '" 吗？')) {
                window.location.href = '${pageContext.request.contextPath}/deleteUser.do?id=' + userId;
            }
        }
    </script>
</body>
</html>
```

### 7.3 用户添加页面


```jsp
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>添加用户</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .form-container { max-width: 500px; margin: 0 auto; }
        .form-group { margin-bottom: 15px; }
        .form-group label { display: block; margin-bottom: 5px; font-weight: bold; }
        .form-group input { width: 100%; padding: 8px; border: 1px solid #ddd; 
                           border-radius: 3px; box-sizing: border-box; }
        .btn { padding: 10px 20px; background-color: #007bff; color: white; 
               border: none; border-radius: 3px; cursor: pointer; }
        .btn:hover { background-color: #0056b3; }
        .btn-cancel { background-color: #6c757d; margin-left: 10px; }
        .btn-cancel:hover { background-color: #545b62; }
        .error { color: red; font-weight: bold; margin-bottom: 15px; }
        .required { color: red; }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>添加新用户</h2>
        
        <!-- 显示错误信息 -->
        <c:if test="${not empty error}">
            <div class="error">${error}</div>
        </c:if>
        
        <form action="${pageContext.request.contextPath}/userAdd.do" method="post" onsubmit="return validateForm()">
            <div class="form-group">
                <label for="name">姓名 <span class="required">*</span></label>
                <input type="text" id="name" name="name" value="${param.name}" 
                       placeholder="请输入用户姓名" maxlength="50" required>
            </div>
            
            <div class="form-group">
                <label for="email">邮箱 <span class="required">*</span></label>
                <input type="email" id="email" name="email" value="${param.email}" 
                       placeholder="请输入邮箱地址" maxlength="100" required>
            </div>
            
            <div class="form-group">
                <label for="phone">电话</label>
                <input type="text" id="phone" name="phone" value="${param.phone}" 
                       placeholder="请输入电话号码" maxlength="20">
            </div>
            
            <div class="form-group">
                <button type="submit" class="btn">保存用户</button>
                <button type="button" class="btn btn-cancel" onclick="history.back()">取消</button>
            </div>
        </form>
    </div>
    
    <script>
        function validateForm() {
            var name = document.getElementById('name').value.trim();
            var email = document.getElementById('email').value.trim();
            
            if (name === '') {
                alert('请输入用户姓名');
                return false;
            }
            
            if (name.length < 2) {
                alert('用户姓名至少2个字符');
                return false;
            }
            
            if (email === '') {
                alert('请输入邮箱地址');
                return false;
            }
            
            // 简单的邮箱格式验证
            var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email)) {
                alert('请输入正确的邮箱格式');
                return false;
            }
            
            return true;
        }
    </script>
</body>
</html>
```

---

## 8. 🛠️ 完整案例实战


### 8.1 案例场景说明


**📋 案例背景**：开发一个简单的用户管理系统，实现用户的增删改查功能

**🎯 功能需求**：
- 查看用户列表（支持分页）
- 添加新用户
- 编辑用户信息  
- 删除用户（软删除）
- 查看用户详情

### 8.2 项目结构


```
webapp/
├── WEB-INF/
│   ├── lib/              ← jar包目录
│   ├── views/            ← JSP页面目录
│   │   ├── userList.jsp
│   │   ├── addUser.jsp
│   │   ├── editUser.jsp
│   │   └── error.jsp
│   └── web.xml
├── index.jsp             ← 首页
└── assets/
    ├── css/
    └── js/

src/
├── com/example/
│   ├── controller/       ← Controller层
│   │   └── UserController.java
│   ├── service/          ← Service层
│   │   ├── UserService.java
│   │   └── UserServiceImpl.java
│   ├── dao/              ← DAO层
│   │   ├── UserDAO.java
│   │   └── UserDAOImpl.java
│   ├── model/            ← 实体类
│   │   └── User.java
│   ├── util/             ← 工具类
│   │   └── DBUtil.java
│   └── servlet/          ← Servlet
│       └── FrontController.java
```

### 8.3 用户实体类


```java
/**
 * 用户实体类
 */
public class User {
    private Integer id;         // 用户ID
    private String name;        // 用户姓名
    private String email;       // 邮箱地址
    private String phone;       // 电话号码
    private String status;      // 状态：ACTIVE-正常, INACTIVE-停用, DELETED-删除
    private Date createTime;    // 创建时间
    private Date updateTime;    // 更新时间
    private String role;        // 角色：USER-普通用户, ADMIN-管理员
    
    // 构造方法
    public User() {}
    
    public User(String name, String email) {
        this.name = name;
        this.email = email;
        this.status = "ACTIVE"; // 默认状态
        this.role = "USER";     // 默认角色
        this.createTime = new Date();
    }
    
    // getter和setter方法
    public Integer getId() { return id; }
    public void setId(Integer id) { this.id = id; }
    
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
    
    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
    
    public String getPhone() { return phone; }
    public void setPhone(String phone) { this.phone = phone; }
    
    public String getStatus() { return status; }
    public void setStatus(String status) { this.status = status; }
    
    public Date getCreateTime() { return createTime; }
    public void setCreateTime(Date createTime) { this.createTime = createTime; }
    
    public Date getUpdateTime() { return updateTime; }
    public void setUpdateTime(Date updateTime) { this.updateTime = updateTime; }
    
    public String getRole() { return role; }
    public void setRole(String role) { this.role = role; }
    
    @Override
    public String toString() {
        return "User{id=" + id + ", name='" + name + "', email='" + email + "'}";
    }
}
```

### 8.4 数据库表结构


```sql
-- 创建用户表
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL COMMENT '用户姓名',
    email VARCHAR(100) NOT NULL UNIQUE COMMENT '邮箱地址',
    phone VARCHAR(20) COMMENT '电话号码',
    status VARCHAR(20) DEFAULT 'ACTIVE' COMMENT '状态：ACTIVE/INACTIVE/DELETED',
    role VARCHAR(20) DEFAULT 'USER' COMMENT '角色：USER/ADMIN',
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
);

-- 插入测试数据
INSERT INTO users (name, email, phone) VALUES 
('张三', 'zhangsan@example.com', '13800138001'),
('李四', 'lisi@example.com', '13800138002'),
('王五', 'wangwu@example.com', '13800138003');
```

### 8.5 完整的请求处理流程示例


**🔸 用户访问用户列表页面的完整流程**：

```
① 用户在浏览器输入：http://localhost:8080/webapp/userList.do

② FrontController接收请求：
   - 解析URL得到action="userList"
   - 调用UserController.listUsers()

③ UserController.listUsers()处理：
   - 获取分页参数 pageNum=1, pageSize=10
   - 调用userService.getUserList(1, 10)

④ UserServiceImpl.getUserList()业务处理：
   - 验证参数合法性
   - 计算offset = (1-1) * 10 = 0
   - 调用userDAO.findByPage(0, 10)

⑤ UserDAOImpl.findByPage()数据访问：
   - 获取数据库连接
   - 执行SQL：SELECT * FROM users WHERE status != 'DELETED' 
               ORDER BY create_time DESC LIMIT 0, 10
   - 将ResultSet转换为List<User>

⑥ 数据返回到Controller：
   - 接收List<User>数据
   - 设置request属性：userList, totalCount, currentPage
   - 转发到userList.jsp

⑦ userList.jsp渲染页面：
   - 使用JSTL遍历userList
   - 显示表格数据
   - 生成分页导航

⑧ HTML返回给浏览器：
   - 浏览器接收HTML
   - 解析并显示页面
   - 用户看到用户列表

整个过程每一层都只做自己职责范围内的事情：
- FrontController：请求分发
- Controller：参数处理和视图选择  
- Service：业务逻辑和事务管理
- DAO：数据库操作
- JSP：数据展示
```

---

## 9. 🏆 整合最佳实践


### 9.1 代码组织原则


**🔹 单一职责原则**
```java
// ❌ 错误：一个类承担太多职责
public class UserServlet extends HttpServlet {
    protected void doPost(HttpServletRequest request, HttpServletResponse response) {
        // 获取参数
        String name = request.getParameter("name");
        // 验证业务规则
        if (name.length() < 2) {...}
        // 操作数据库
        Connection conn = DriverManager.getConnection(...);
        // 处理响应
        response.getWriter().write("success");
    }
}

// ✅ 正确：每个类只负责一件事
public class UserController {
    private UserService userService = new UserService();
    
    public void addUser(HttpServletRequest request, HttpServletResponse response) {
        // 只负责协调工作
        String name = request.getParameter("name");
        User user = new User(name, email);
        boolean success = userService.addUser(user);
        // 选择视图
    }
}
```

**🔹 依赖注入原则**
```java
// ❌ 紧耦合：硬编码依赖
public class UserController {
    private UserService userService = new UserServiceImpl(); // 硬依赖
}

// ✅ 松耦合：通过构造器注入
public class UserController {
    private UserService userService;
    
    public UserController(UserService userService) {
        this.userService = userService;
    }
}
```

### 9.2 异常处理策略


**🔹 分层异常处理**

```java
// 1. DAO层：抛出数据访问异常
public class UserDAOImpl implements UserDAO {
    public User findById(int id) {
        try {
            // 数据库操作
        } catch (SQLException e) {
            throw new DataAccessException("查询用户失败", e);
        }
    }
}

// 2. Service层：抛出业务异常
public class UserServiceImpl implements UserService {
    public User getUserById(int id) {
        try {
            return userDAO.findById(id);
        } catch (DataAccessException e) {
            throw new BusinessException("获取用户信息失败：" + e.getMessage(), e);
        }
    }
}

// 3. Controller层：处理异常并选择错误页面
public class UserController {
    public void getUserDetail(HttpServletRequest request, HttpServletResponse response) {
        try {
            int id = Integer.parseInt(request.getParameter("id"));
            User user = userService.getUserById(id);
            request.setAttribute("user", user);
            request.getRequestDispatcher("/userDetail.jsp").forward(request, response);
        } catch (BusinessException e) {
            request.setAttribute("errorMsg", e.getMessage());
            request.getRequestDispatcher("/error.jsp").forward(request, response);
        } catch (Exception e) {
            request.setAttribute("errorMsg", "系统异常，请联系管理员");
            request.getRequestDispatcher("/error.jsp").forward(request, response);
        }
    }
}
```

### 9.3 配置管理最佳实践


**🔹 外部化配置**

```properties
# database.properties
db.url=jdbc:mysql://localhost:3306/javaweb_db
db.username=root
db.password=123456
db.maxPoolSize=10

# application.properties  
app.pageSize=10
app.uploadPath=/uploads
app.logLevel=INFO
```

**🔹 配置工具类**

```java
public class ConfigManager {
    private static Properties dbConfig;
    private static Properties appConfig;
    
    static {
        loadConfigs();
    }
    
    private static void loadConfigs() {
        try {
            dbConfig = new Properties();
            dbConfig.load(ConfigManager.class.getResourceAsStream("/database.properties"));
            
            appConfig = new Properties();
            appConfig.load(ConfigManager.class.getResourceAsStream("/application.properties"));
        } catch (IOException e) {
            throw new RuntimeException("加载配置文件失败", e);
        }
    }
    
    public static String getDbUrl() {
        return dbConfig.getProperty("db.url");
    }
    
    public static int getPageSize() {
        return Integer.parseInt(appConfig.getProperty("app.pageSize", "10"));
    }
}
```

### 9.4 性能优化建议


**🔹 数据库连接池**
```java
// 使用连接池而不是每次创建连接
public class DBUtil {
    private static DataSource dataSource;
    
    static {
        // 初始化连接池（如HikariCP）
        HikariConfig config = new HikariConfig();
        config.setJdbcUrl(ConfigManager.getDbUrl());
        config.setUsername(ConfigManager.getDbUsername());
        config.setPassword(ConfigManager.getDbPassword());
        config.setMaximumPoolSize(10);
        dataSource = new HikariDataSource(config);
    }
    
    public static Connection getConnection() throws SQLException {
        return dataSource.getConnection();
    }
}
```

**🔹 缓存策略**
```java
public class UserServiceImpl implements UserService {
    private Map<Integer, User> userCache = new ConcurrentHashMap<>();
    
    @Override
    public User getUserById(int id) {
        // 先查缓存
        User cachedUser = userCache.get(id);
        if (cachedUser != null) {
            return cachedUser;
        }
        
        // 缓存没有，查数据库
        User user = userDAO.findById(id);
        if (user != null) {
            userCache.put(id, user); // 放入缓存
        }
        
        return user;
    }
}
```

---

## 10. 📋 核心要点总结


### 10.1 整合架构的核心价值


**🎯 架构整合的本质理解**
```
MVC + 三层架构 = 完整的Web应用解决方案

MVC解决的问题：用户界面与业务逻辑分离
三层架构解决的问题：业务逻辑内部的分层组织

整合后的效果：
✅ 职责清晰：每层都有明确的职责边界
✅ 维护性好：修改某个功能只影响对应的层
✅ 可测试性强：每层都可以独立进行单元测试  
✅ 可扩展性好：可以轻松替换或升级某一层的实现
```

### 10.2 数据流转核心流程


> 💡 **记忆口诀**：用户点击→Servlet分发→Controller协调→Service业务→DAO数据→JSP展示

**🔸 完整请求处理链**
```
HTTP请求 → FrontController → 具体Controller → Service → DAO → 数据库
                                                              ↓
HTTP响应 ← JSP页面 ← 数据模型 ← Controller ← Service ← DAO ← 查询结果
```

**🔸 各层职责要点**
- **Servlet层**：请求分发，统一入口
- **Controller层**：薄控制器，只做协调工作  
- **Service层**：业务逻辑，事务管理
- **DAO层**：数据访问，SQL操作
- **JSP层**：数据展示，用户交互

### 10.3 关键设计原则


**🔹 单一职责原则**
- 每个类只负责一种类型的功能
- 避免"万能类"，功能过于复杂

**🔹 依赖倒置原则**  
- 依赖接口而不是具体实现
- 便于测试和替换组件

**🔹 开闭原则**
- 对扩展开放，对修改关闭
- 通过接口和配置实现功能扩展

### 10.4 实践要点


**✅ 开发建议**
- 从数据库表结构开始设计
- 先写实体类和DAO接口
- 再实现Service业务逻辑
- 最后完成Controller和JSP
- 每完成一层都要进行测试

**⚠️ 常见陷阱**
- Controller里写业务逻辑
- Service直接操作HttpServlet相关对象
- DAO里处理业务规则
- JSP里写复杂的Java代码

**🔧 调试技巧**
- 在每层的关键方法加日志
- 使用断点调试跟踪数据流转
- 检查每层的输入输出数据
- 验证异常处理是否正确

### 10.5 学习建议


**📚 进阶学习路径**
1. **深入理解各层职责**：熟练掌握每层应该做什么，不应该做什么
2. **掌握设计模式**：工厂模式、单例模式、观察者模式等
3. **学习框架原理**：理解Spring MVC、MyBatis等框架的设计思想
4. **关注性能优化**：连接池、缓存、异步处理等技术
5. **实践项目开发**：通过完整项目加深理解

**🎯 能力检验标准**
- 能够独立设计整合架构
- 能够快速定位和解决分层问题
- 理解每层的测试方法
- 掌握常见的重构技巧
- 能够进行性能调优

**核心记忆**：
- 整合架构让Web开发更规范、更清晰
- 数据流转有固定路径，严格按层级处理
- 每层职责单一，边界清晰，便于维护
- 良好的架构是可扩展、可测试、可维护的基础