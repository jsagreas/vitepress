---
title: 3、监听器实际应用
---
## 📚 目录

1. [监听器概念回顾](#1-监听器概念回顾)
2. [系统启动初始化](#2-系统启动初始化)
3. [在线用户统计](#3-在线用户统计)
4. [定时任务启动](#4-定时任务启动)
5. [资源清理释放](#5-资源清理释放)
6. [数据库连接池初始化](#6-数据库连接池初始化)
7. [缓存预热处理](#7-缓存预热处理)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔍 监听器概念回顾


### 1.1 什么是监听器


**简单理解**：监听器就像是一个"小助手"，专门负责监控Web应用中发生的各种事件

```
现实类比：
门卫大爷 = 监听器
公司大楼 = Web应用
员工进出 = 各种事件（session创建/销毁等）

当有员工进出时，门卫大爷会：
- 记录进出时间
- 统计在楼人数  
- 做一些必要的处理
```

**监听器的本质**：
- **自动触发**：不需要手动调用，事件发生时自动执行
- **后台工作**：在幕后默默工作，用户感知不到
- **系统级功能**：处理全局性的任务

### 1.2 监听器的工作原理


```
Web应用运行流程：

应用启动 ─┐
         ├─→ ServletContextListener.contextInitialized() 被调用
         └─→ 进行系统初始化工作

用户访问 ─┐
         ├─→ HttpSessionListener.sessionCreated() 被调用  
         └─→ 统计在线用户数量

用户离开 ─┐
         ├─→ HttpSessionListener.sessionDestroyed() 被调用
         └─→ 清理用户相关资源

应用关闭 ─┐
         ├─→ ServletContextListener.contextDestroyed() 被调用
         └─→ 释放系统资源
```

---

## 2. 🚀 系统启动初始化


### 2.1 为什么需要系统启动初始化


**生活中的例子**：
开餐厅前需要做的准备工作：
- 🔥 点火热锅（启动服务）
- 📋 准备菜单（加载配置）
- 🥬 洗菜备料（初始化数据）
- 👨‍🍳 叫齐厨师（启动组件）

**Web应用启动时需要做的事**：
- 📖 读取配置文件
- 🗄️ 初始化数据库连接
- 🧠 加载系统参数
- ⚙️ 启动后台服务

### 2.2 系统启动监听器实现


> 💡 **核心思想**：在应用启动时自动执行初始化逻辑

```java
@WebListener
public class SystemInitListener implements ServletContextListener {
    
    @Override
    public void contextInitialized(ServletContextEvent sce) {
        System.out.println("=== 系统正在启动，开始初始化 ===");
        
        // 1. 加载系统配置
        loadSystemConfig(sce.getServletContext());
        
        // 2. 初始化全局变量
        initGlobalVariables(sce.getServletContext());
        
        // 3. 预加载必要数据
        preloadData(sce.getServletContext());
        
        System.out.println("=== 系统初始化完成 ===");
    }
    
    // 加载系统配置的方法
    private void loadSystemConfig(ServletContext context) {
        // 从配置文件读取系统参数
        Properties props = new Properties();
        try {
            props.load(context.getResourceAsStream("/WEB-INF/system.properties"));
            context.setAttribute("systemConfig", props);
            System.out.println("✅ 系统配置加载成功");
        } catch (Exception e) {
            System.out.println("❌ 系统配置加载失败：" + e.getMessage());
        }
    }
}
```

### 2.3 实际应用场景


**常见的系统初始化任务**：

| 初始化任务 | **说明** | **重要性** |
|----------|----------|-----------|
| 🔧 **配置文件加载** | `读取数据库配置、业务参数等` | `⭐⭐⭐ 基础必需` |
| 📊 **全局计数器** | `初始化访问统计、在线用户计数` | `⭐⭐ 功能增强` |
| 🗂️ **缓存字典** | `加载省市区数据、分类信息等` | `⭐⭐ 性能提升` |
| 📝 **日志系统** | `初始化日志记录组件` | `⭐⭐⭐ 运维必需` |

---

## 3. 👥 在线用户统计


### 3.1 在线用户统计的实现原理


**生活类比**：
```
网吧管理系统：
客户上机 → 开卡计时 → 在线人数+1
客户下机 → 结账离开 → 在线人数-1
随时可以看到：当前在线XX人
```

**Web应用中的实现**：
```
用户访问网站 → 创建Session → 在线用户+1
用户关闭浏览器/超时 → Session销毁 → 在线用户-1
管理员随时查看：当前在线用户数
```

### 3.2 在线用户统计监听器


```java
@WebListener
public class OnlineUserListener implements HttpSessionListener {
    
    @Override
    public void sessionCreated(HttpSessionEvent se) {
        // 用户上线，计数器+1
        ServletContext context = se.getSession().getServletContext();
        
        // 从应用中获取当前在线人数
        Integer onlineCount = (Integer) context.getAttribute("onlineCount");
        if (onlineCount == null) {
            onlineCount = 0;
        }
        
        // 人数+1
        onlineCount++;
        context.setAttribute("onlineCount", onlineCount);
        
        System.out.println("👋 新用户上线，当前在线人数：" + onlineCount);
    }
    
    @Override
    public void sessionDestroyed(HttpSessionEvent se) {
        // 用户下线，计数器-1
        ServletContext context = se.getSession().getServletContext();
        Integer onlineCount = (Integer) context.getAttribute("onlineCount");
        
        if (onlineCount != null && onlineCount > 0) {
            onlineCount--;
            context.setAttribute("onlineCount", onlineCount);
            System.out.println("👋 用户下线，当前在线人数：" + onlineCount);
        }
    }
}
```

### 3.3 在线用户信息展示


**创建显示在线用户的Servlet**：

```java
@WebServlet("/online-users")
public class OnlineUsersServlet extends HttpServlet {
    
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        // 获取在线用户数
        Integer onlineCount = (Integer) getServletContext().getAttribute("onlineCount");
        if (onlineCount == null) {
            onlineCount = 0;
        }
        
        // 设置响应
        response.setContentType("text/html;charset=UTF-8");
        response.getWriter().write(
            "<h1>📊 网站在线统计</h1>" +
            "<p>🟢 当前在线用户：<strong>" + onlineCount + "</strong> 人</p>" +
            "<p>📅 统计时间：" + new Date() + "</p>"
        );
    }
}
```

> 🎯 **实用技巧**：可以结合Ajax定时刷新，实现实时的在线用户数显示

---

## 4. ⏰ 定时任务启动


### 4.1 为什么需要定时任务


**日常生活中的定时任务**：
- ⏰ 每天7点的闹钟（定时提醒）
- 🗑️ 每周一次的垃圾回收（定期清理）
- 💊 每日三次的吃药提醒（定时执行）

**Web应用中的定时任务**：
- 📊 每小时统计网站访问量
- 🧹 每天清理过期的临时文件
- 📧 每周发送系统报告邮件

### 4.2 使用监听器启动定时任务


```java
@WebListener
public class TimerTaskListener implements ServletContextListener {
    
    private Timer timer;
    
    @Override
    public void contextInitialized(ServletContextEvent sce) {
        System.out.println("🕐 启动定时任务...");
        
        timer = new Timer("系统定时任务");
        
        // 每5分钟执行一次数据清理
        TimerTask cleanTask = new TimerTask() {
            @Override
            public void run() {
                System.out.println("🧹 执行数据清理任务：" + new Date());
                // 这里写具体的清理逻辑
                cleanExpiredData();
            }
        };
        
        // 延迟1分钟后开始，每5分钟执行一次
        timer.schedule(cleanTask, 60 * 1000, 5 * 60 * 1000);
        
        System.out.println("✅ 定时任务启动成功");
    }
    
    @Override
    public void contextDestroyed(ServletContextEvent sce) {
        // 应用关闭时，停止定时任务
        if (timer != null) {
            timer.cancel();
            System.out.println("⏹️ 定时任务已停止");
        }
    }
    
    private void cleanExpiredData() {
        // 具体的清理逻辑
        System.out.println("  - 清理过期session数据");
        System.out.println("  - 清理临时文件");
        System.out.println("  - 清理过期缓存");
    }
}
```

### 4.3 定时任务的应用场景


**常见定时任务类型**：

```
┌─ 📊 数据统计类
├─ 🧹 清理维护类  
├─ 📧 通知提醒类
├─ 🔄 数据同步类
└─ 📋 报表生成类

具体示例：
📈 每小时统计PV/UV
🗑️ 每天清理日志文件
📮 每周发送运营报告
🔗 定时同步外部数据
📊 每月生成财务报表
```

---

## 5. 🧹 资源清理释放


### 5.1 为什么需要资源清理


**生活中的资源管理**：
```
使用完电脑后需要：
✅ 保存文件
✅ 关闭程序
✅ 断开网络
✅ 关闭电源

不这样做的后果：
❌ 数据丢失
❌ 程序崩溃  
❌ 资源浪费
❌ 系统不稳定
```

**Web应用中的资源清理**：
- 🔌 关闭数据库连接
- 📁 删除临时文件
- 🧠 清空缓存数据
- ⏹️ 停止后台线程

### 5.2 资源清理监听器实现


```java
@WebListener  
public class ResourceCleanupListener implements ServletContextListener {
    
    @Override
    public void contextInitialized(ServletContextEvent sce) {
        System.out.println("🚀 系统启动，资源管理器就绪");
    }
    
    @Override
    public void contextDestroyed(ServletContextEvent sce) {
        System.out.println("🛑 系统即将关闭，开始清理资源...");
        
        ServletContext context = sce.getServletContext();
        
        // 1. 清理数据库连接池
        cleanupDatabaseConnections(context);
        
        // 2. 清理缓存
        cleanupCache(context);
        
        // 3. 清理临时文件
        cleanupTempFiles(context);
        
        // 4. 停止后台线程
        shutdownBackgroundTasks(context);
        
        System.out.println("✅ 资源清理完成，系统安全关闭");
    }
    
    private void cleanupDatabaseConnections(ServletContext context) {
        try {
            // 关闭数据库连接池
            Object dataSource = context.getAttribute("dataSource");
            if (dataSource != null) {
                // 这里写关闭数据源的代码
                System.out.println("  📊 数据库连接池已关闭");
            }
        } catch (Exception e) {
            System.out.println("  ❌ 关闭数据库连接失败：" + e.getMessage());
        }
    }
    
    private void cleanupCache(ServletContext context) {
        // 清空所有缓存
        context.removeAttribute("userCache");
        context.removeAttribute("configCache");
        System.out.println("  🧠 缓存数据已清空");
    }
    
    private void cleanupTempFiles(ServletContext context) {
        // 删除临时文件
        String tempDir = context.getRealPath("/temp");
        // 这里写删除临时文件的代码
        System.out.println("  📁 临时文件已清理");
    }
}
```

### 5.3 Session级别的资源清理


```java
@WebListener
public class SessionResourceListener implements HttpSessionListener {
    
    @Override
    public void sessionDestroyed(HttpSessionEvent se) {
        HttpSession session = se.getSession();
        System.out.println("🧹 清理Session资源：" + session.getId());
        
        // 清理用户上传的临时文件
        cleanupUserTempFiles(session);
        
        // 清理用户缓存数据
        cleanupUserCache(session);
    }
    
    private void cleanupUserTempFiles(HttpSession session) {
        String userId = (String) session.getAttribute("userId");
        if (userId != null) {
            // 删除该用户的临时文件
            System.out.println("  📄 清理用户 " + userId + " 的临时文件");
        }
    }
}
```

---

## 6. 🗄️ 数据库连接池初始化


### 6.1 什么是数据库连接池


**生活类比**：
```
🏊‍♂️ 游泳池 vs 🚿 自来水龙头

游泳池模式（连接池）：
- 池子里准备好了水（预创建连接）
- 想游泳直接跳进去（直接使用连接）
- 游完了水还在池子里（连接复用）
- 多人可以同时游泳（并发访问）

水龙头模式（每次新建连接）：
- 每次使用都要接水（创建连接）
- 用完就倒掉（关闭连接）  
- 浪费水且效率低（资源浪费）
```

**数据库连接池的好处**：
- ⚡ **提高性能**：避免频繁创建/关闭连接
- 🔒 **控制并发**：限制同时连接数量
- 💰 **节省资源**：连接复用，减少开销

### 6.2 使用监听器初始化连接池


```java
@WebListener
public class DatabaseInitListener implements ServletContextListener {
    
    @Override
    public void contextInitialized(ServletContextEvent sce) {
        System.out.println("🗄️ 初始化数据库连接池...");
        
        try {
            // 创建数据源配置
            BasicDataSource dataSource = createDataSource();
            
            // 将数据源保存到ServletContext中
            ServletContext context = sce.getServletContext();
            context.setAttribute("dataSource", dataSource);
            
            // 测试连接
            testConnection(dataSource);
            
            System.out.println("✅ 数据库连接池初始化成功");
            
        } catch (Exception e) {
            System.out.println("❌ 数据库连接池初始化失败：" + e.getMessage());
        }
    }
    
    private BasicDataSource createDataSource() {
        BasicDataSource dataSource = new BasicDataSource();
        
        // 数据库连接信息
        dataSource.setDriverClassName("com.mysql.jdbc.Driver");
        dataSource.setUrl("jdbc:mysql://localhost:3306/mydb");
        dataSource.setUsername("root");
        dataSource.setPassword("password");
        
        // 连接池参数
        dataSource.setInitialSize(5);      // 初始连接数
        dataSource.setMaxActive(20);       // 最大连接数
        dataSource.setMaxIdle(10);         // 最大空闲连接数
        dataSource.setMinIdle(2);          // 最小空闲连接数
        
        return dataSource;
    }
    
    private void testConnection(BasicDataSource dataSource) throws SQLException {
        try (Connection conn = dataSource.getConnection()) {
            System.out.println("  🔍 数据库连接测试成功");
            System.out.println("  📊 当前活跃连接数：" + dataSource.getNumActive());
        }
    }
    
    @Override
    public void contextDestroyed(ServletContextEvent sce) {
        // 关闭数据库连接池
        ServletContext context = sce.getServletContext();
        BasicDataSource dataSource = (BasicDataSource) context.getAttribute("dataSource");
        
        if (dataSource != null) {
            try {
                dataSource.close();
                System.out.println("🔒 数据库连接池已关闭");
            } catch (SQLException e) {
                System.out.println("❌ 关闭连接池失败：" + e.getMessage());
            }
        }
    }
}
```

### 6.3 如何使用连接池


**在其他地方使用数据库连接**：

```java
@WebServlet("/users")
public class UserServlet extends HttpServlet {
    
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        // 从ServletContext获取数据源
        DataSource dataSource = (DataSource) getServletContext().getAttribute("dataSource");
        
        try (Connection conn = dataSource.getConnection()) {
            // 使用连接执行SQL
            String sql = "SELECT * FROM users";
            try (PreparedStatement pstmt = conn.prepareStatement(sql);
                 ResultSet rs = pstmt.executeQuery()) {
                
                // 处理查询结果
                while (rs.next()) {
                    System.out.println("用户：" + rs.getString("username"));
                }
            }
        } catch (SQLException e) {
            System.out.println("数据库操作失败：" + e.getMessage());
        }
    }
}
```

> 💡 **重要提醒**：使用`try-with-resources`语句可以自动关闭连接，连接会自动返回连接池

---

## 7. 🔥 缓存预热处理


### 7.1 什么是缓存预热


**生活类比**：
```
🏠 冬天的暖气系统：

不预热的情况：
- 客人来了再开暖气 → 需要等待很久才暖和
- 第一批客人体验很差 → 又冷又慢

预热的情况：  
- 客人来之前就开始暖房 → 客人一来就很暖和
- 所有客人体验都很好 → 温暖舒适
```

**缓存预热的作用**：
- 🚀 **提升响应速度**：常用数据提前加载到内存
- 😊 **改善用户体验**：避免首次访问缓慢
- ⚡ **减轻数据库压力**：减少实时查询次数

### 7.2 缓存预热监听器实现


```java
@WebListener
public class CacheWarmupListener implements ServletContextListener {
    
    @Override
    public void contextInitialized(ServletContextEvent sce) {
        System.out.println("🔥 开始缓存预热...");
        
        ServletContext context = sce.getServletContext();
        
        // 1. 预热用户字典数据
        warmupDictionaryData(context);
        
        // 2. 预热热门商品数据
        warmupPopularProducts(context);
        
        // 3. 预热系统配置数据
        warmupSystemConfig(context);
        
        System.out.println("✅ 缓存预热完成");
    }
    
    private void warmupDictionaryData(ServletContext context) {
        System.out.println("  📖 预热字典数据...");
        
        // 模拟加载省市区数据
        Map<String, String> provinces = new HashMap<>();
        provinces.put("110000", "北京市");
        provinces.put("120000", "天津市");
        provinces.put("310000", "上海市");
        // ... 更多省份数据
        
        context.setAttribute("provinceCache", provinces);
        System.out.println("    ✅ 省份数据已缓存，共 " + provinces.size() + " 条");
        
        // 加载其他字典数据...
        Map<String, String> categories = loadCategoryData();
        context.setAttribute("categoryCache", categories);
        System.out.println("    ✅ 商品分类已缓存，共 " + categories.size() + " 条");
    }
    
    private void warmupPopularProducts(ServletContext context) {
        System.out.println("  🔥 预热热门商品数据...");
        
        // 模拟从数据库加载热门商品
        List<Product> hotProducts = new ArrayList<>();
        hotProducts.add(new Product(1, "iPhone 15", 5999.0));
        hotProducts.add(new Product(2, "MacBook Pro", 12999.0));
        // ... 更多热门商品
        
        context.setAttribute("hotProductsCache", hotProducts);
        System.out.println("    ✅ 热门商品已缓存，共 " + hotProducts.size() + " 个");
    }
    
    private void warmupSystemConfig(ServletContext context) {
        System.out.println("  ⚙️ 预热系统配置...");
        
        // 加载系统配置参数
        Map<String, String> config = new HashMap<>();
        config.put("site_name", "我的电商网站");
        config.put("max_upload_size", "10MB");
        config.put("session_timeout", "30");
        
        context.setAttribute("systemConfigCache", config);
        System.out.println("    ✅ 系统配置已缓存");
    }
    
    private Map<String, String> loadCategoryData() {
        // 模拟从数据库加载分类数据
        Map<String, String> categories = new HashMap<>();
        categories.put("1", "电子产品");
        categories.put("2", "服装鞋帽");  
        categories.put("3", "家居用品");
        return categories;
    }
}

// 商品实体类
class Product {
    private int id;
    private String name;
    private double price;
    
    public Product(int id, String name, double price) {
        this.id = id;
        this.name = name;
        this.price = price;
    }
    
    // getter和setter方法...
}
```

### 7.3 如何使用缓存数据


**在Servlet中使用缓存数据**：

```java
@WebServlet("/products")
public class ProductServlet extends HttpServlet {
    
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        // 直接从缓存获取热门商品数据
        List<Product> hotProducts = 
            (List<Product>) getServletContext().getAttribute("hotProductsCache");
        
        if (hotProducts != null) {
            // 使用缓存数据，响应速度很快
            response.setContentType("text/html;charset=UTF-8");
            PrintWriter out = response.getWriter();
            
            out.println("<h1>🔥 热门商品</h1>");
            for (Product product : hotProducts) {
                out.println("<p>" + product.getName() + " - ¥" + product.getPrice() + "</p>");
            }
        } else {
            // 缓存不存在，从数据库查询（较慢）
            response.getWriter().write("缓存数据不可用，请联系管理员");
        }
    }
}
```

### 7.4 缓存更新策略


**如何保持缓存数据的新鲜度**：

```
🔄 定时更新策略：
├─ 每小时更新热门商品缓存
├─ 每天更新字典数据缓存  
└─ 实时更新关键配置缓存

🎯 按需更新策略：
├─ 商品信息变化时 → 更新商品缓存
├─ 用户反馈数据错误 → 立即更新对应缓存
└─ 管理员修改配置 → 同步更新配置缓存

⚠️ 注意事项：
- 不要缓存变化频繁的数据
- 设置合适的缓存过期时间
- 监控缓存命中率和内存使用
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的基本概念


```
🔸 监听器本质：Web应用事件的"小助手"，自动响应系统事件
🔸 工作原理：基于事件驱动，在特定时机自动执行预定义的任务
🔸 主要类型：ServletContextListener（应用级）、HttpSessionListener（会话级）
🔸 核心价值：系统初始化、资源管理、数据预处理、状态监控
```

### 8.2 六大实际应用场景


| 应用场景 | **核心作用** | **技术要点** | **使用频率** |
|---------|-------------|-------------|-------------|
| 🚀 **系统启动初始化** | `加载配置、初始化组件` | `ServletContextListener.contextInitialized()` | `⭐⭐⭐ 必用` |
| 👥 **在线用户统计** | `实时统计在线人数` | `HttpSessionListener监听session创建/销毁` | `⭐⭐ 常用` |
| ⏰ **定时任务启动** | `启动后台定时服务` | `Timer + TimerTask组合使用` | `⭐⭐ 常用` |
| 🧹 **资源清理释放** | `应用关闭时清理资源` | `ServletContextListener.contextDestroyed()` | `⭐⭐⭐ 必用` |
| 🗄️ **数据库连接池** | `初始化和管理连接池` | `BasicDataSource配置和管理` | `⭐⭐⭐ 必用` |
| 🔥 **缓存预热处理** | `预加载热点数据` | `ServletContext作为缓存容器` | `⭐⭐ 常用` |

### 8.3 关键理解要点


**🔹 监听器的自动性**
```
理解要点：
- 监听器不需要手动调用，事件发生时自动触发
- 一次配置，终身受益，减少重复代码
- 在应用生命周期的关键节点执行重要任务
```

**🔹 应用级 vs 会话级**
```
应用级监听器（ServletContextListener）：
- 整个应用只执行一次
- 适合做全局性的初始化和清理工作
- 例：加载配置文件、初始化连接池

会话级监听器（HttpSessionListener）：
- 每个用户会话都会触发
- 适合做用户相关的统计和管理
- 例：在线用户统计、用户资源清理
```

**🔹 资源管理的重要性**
```
为什么要做资源管理：
- 防止内存泄漏：及时释放不再使用的资源
- 保证系统稳定：避免资源耗尽导致系统崩溃
- 提升系统性能：合理的资源分配和回收

最佳实践：
- 有借有还：创建资源后一定要清理
- 异常安全：在finally块或try-with-resources中清理
- 监控告警：定期检查资源使用情况
```

### 8.4 实际应用指导


**🎯 选择合适的监听器类型**
- **全局初始化任务** → 使用`ServletContextListener`
- **用户会话管理** → 使用`HttpSessionListener`
- **请求级别监控** → 使用`ServletRequestListener`

**⚠️ 常见注意事项**
- **异常处理**：监听器中的异常可能影响应用启动，必须做好异常捕获
- **执行顺序**：多个监听器的执行顺序不确定，不要依赖顺序
- **性能考虑**：避免在监听器中执行耗时操作，影响应用启动速度

**🚀 最佳实践建议**
- **日志记录**：在关键节点记录日志，便于问题排查
- **配置外置**：将配置参数放在外部文件中，便于修改
- **优雅关闭**：确保应用关闭时能够完整清理所有资源
- **监控指标**：定期监控内存使用、连接池状态等关键指标

### 8.5 学习路径建议


**🎓 新手学习顺序**
1. **第一步**：理解监听器的基本概念和工作原理
2. **第二步**：实践系统启动初始化监听器
3. **第三步**：实现在线用户统计功能
4. **第四步**：学习资源清理和连接池管理
5. **第五步**：尝试缓存预热和定时任务

**💪 进阶提升方向**
- **监听器链**：多个监听器协同工作
- **监听器与Spring集成**：在Spring框架中使用监听器
- **监听器性能优化**：提升监听器执行效率
- **分布式环境**：集群环境下的监听器应用

**🔧 实战项目建议**
- **个人博客系统**：实现访问统计、资源管理
- **在线商城**：商品缓存预热、用户会话管理
- **企业管理系统**：系统初始化、定时任务处理

**核心记忆口诀**：
```
🎵 监听器来帮大忙，系统启动它先上
🎵 用户上线自动数，资源清理不能忘  
🎵 缓存预热提速度，定时任务后台忙
🎵 连接池子要管好，Web应用更健壮
```