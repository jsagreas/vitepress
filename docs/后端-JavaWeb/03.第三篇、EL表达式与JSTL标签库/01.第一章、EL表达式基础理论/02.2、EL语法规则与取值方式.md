---
title: 2、EL语法规则与取值方式
---
## 📚 目录

1. [EL表达式基础认知](#1-EL表达式基础认知)
2. [EL语法规则详解](#2-EL语法规则详解)
3. [点号访问与中括号访问](#3-点号访问与中括号访问)
4. [属性取值规则深入](#4-属性取值规则深入)
5. [空安全访问机制](#5-空安全访问机制)
6. [嵌套取值实战应用](#6-嵌套取值实战应用)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 EL表达式基础认知


### 1.1 什么是EL表达式


> **📋 简单理解**：EL表达式就像是JSP页面上的"取值器"，专门用来从后台取数据显示在网页上

**EL表达式的本质**：
- **全称**：Expression Language（表达式语言）
- **作用**：简化JSP页面中数据的获取和显示
- **特点**：语法简单，自动处理空值，减少Java代码在JSP中的使用

```
传统方式 vs EL表达式对比：

传统JSP scriptlet取值：
<%= request.getAttribute("userName") %>  // 繁琐，需要判断空值

EL表达式取值：
${userName}  // 简洁，自动处理空值
```

### 1.2 EL表达式解决了什么问题


**🔸 核心解决的问题**：
- **代码混乱**：避免JSP页面中大量的Java代码
- **空指针异常**：自动处理null值，不会报错
- **取值复杂**：简化从各个域对象中取值的过程
- **代码可读性**：让前端页面更容易理解和维护

**💡 生活化理解**：
```
把EL表达式想象成"自动取款机"：
- 你只需要告诉它你要什么（变量名）
- 它会自动去各个"账户"（域对象）里找
- 找不到也不会出错，只是显示空白
- 比手工一个个账户去找要方便得多
```

### 1.3 EL表达式的工作环境


**🏗️ 运行环境要求**：
- **Servlet 2.4** 及以上版本
- **JSP 2.0** 及以上版本
- 在JSP页面中使用
- 需要Web容器支持（如Tomcat）

---

## 2. ⚙️ EL语法规则详解


### 2.1 基本语法格式


**🔸 标准语法**：`${表达式内容}`

```jsp
<!-- 基本取值语法 -->
${变量名}           // 取变量值
${对象.属性}        // 取对象属性
${对象["属性"]}     // 中括号方式取属性
${对象.方法()}      // 调用方法（受限）
```

### 2.2 语法规则详细说明


**📋 语法构成要素**：

| 组成部分 | **说明** | **示例** |
|---------|----------|---------|
| `${` | **起始标记** | `固定开头，不能省略` |
| `表达式内容` | **具体的取值逻辑** | `userName、user.name等` |
| `}` | **结束标记** | `固定结尾，不能省略` |

**⚠️ 语法注意事项**：
```jsp
正确写法：
${userName}        ✅ 标准格式
${user.name}       ✅ 对象属性访问
${user['name']}    ✅ 中括号访问

错误写法：
${ userName }      ❌ 不建议有空格
{userName}         ❌ 缺少$符号
$(userName)        ❌ 错误的括号
```

### 2.3 EL表达式的求值过程


**🔄 自动搜索机制**：

```
EL表达式 ${userName} 的查找顺序：

1️⃣ pageScope     (页面域)
    ↓ 没找到继续
2️⃣ requestScope  (请求域)  
    ↓ 没找到继续
3️⃣ sessionScope  (会话域)
    ↓ 没找到继续  
4️⃣ applicationScope (应用域)
    ↓ 还没找到
5️⃣ 返回空字符串 ""
```

**💡 搜索规则理解**：
- EL表达式会**自动**按照域的**优先级**依次查找
- 找到第一个匹配的就停止搜索
- 完全找不到时返回**空字符串**而不是null
- 这个过程**完全自动**，无需程序员干预

---

## 3. 🔍 点号访问与中括号访问


### 3.1 点号访问方式详解


**🔸 点号访问语法**：`${对象.属性名}`

```jsp
<!-- 点号访问示例 -->
${user.name}        // 获取user对象的name属性
${student.age}      // 获取student对象的age属性
${product.price}    // 获取product对象的price属性
```

**📋 点号访问的工作原理**：
```
当写 ${user.name} 时，EL表达式实际上是：

1. 找到user对象
2. 调用user.getName()方法  // 自动调用getter方法
3. 返回方法的返回值
4. 如果没有getName()方法，则查找name字段
```

**✅ 点号访问适用场景**：
- 属性名符合Java标识符规则
- 属性名不包含特殊字符
- 属性名已知且固定
- 最常用的访问方式

### 3.2 中括号访问方式详解


**🔸 中括号访问语法**：`${对象["属性名"]}` 或 `${对象['属性名']}`

```jsp
<!-- 中括号访问示例 -->
${user["name"]}           // 等同于 ${user.name}
${user['age']}           // 单引号双引号都可以
${map["user-info"]}      // 属性名包含特殊字符
${list[0]}               // 访问数组或List的元素
${array[index]}          // 动态索引访问
```

**🔧 中括号访问的特殊用法**：

```jsp
<!-- 动态属性名访问 -->
<%
    String propName = "userName";
    request.setAttribute("propName", propName);
    request.setAttribute("user", new User("张三"));
%>

${user[propName]}        // 动态获取属性，相当于 user.getUserName()
```

### 3.3 两种访问方式对比


| 访问方式 | **语法** | **适用场景** | **优缺点** |
|---------|----------|-------------|-----------|
| **点号访问** | `${obj.prop}` | `属性名固定，无特殊字符` | `简洁直观，最常用` |
| **中括号访问** | `${obj["prop"]}` | `属性名动态，有特殊字符` | `灵活强大，但稍复杂` |

**🎯 选择建议**：
- **优先使用点号访问** - 代码更简洁易读
- **特殊情况用中括号** - 属性名包含减号、空格等特殊字符时
- **动态访问必须用中括号** - 属性名来自变量时

---

## 4. ⚖️ 属性取值规则深入


### 4.1 JavaBean属性访问规则


**📋 JavaBean标准访问规则**：

```java
// JavaBean示例
public class User {
    private String name;
    private int age;
    
    // 标准getter方法
    public String getName() { return name; }
    public int getAge() { return age; }
    
    // 标准setter方法
    public void setName(String name) { this.name = name; }
    public void setAge(int age) { this.age = age; }
}
```

```jsp
<!-- EL表达式访问 -->
${user.name}    // 调用 user.getName()
${user.age}     // 调用 user.getAge()
```

**🔸 属性访问的查找顺序**：
```
${user.name} 的查找过程：

1️⃣ 查找 getName() 方法
   ↓ 存在则调用
2️⃣ 查找 name 字段（如果是public）
   ↓ 存在则直接访问
3️⃣ 返回 null
```

### 4.2 Map集合的访问规则


**🗺️ Map对象的EL访问**：

```java
// 后台Java代码
Map<String, Object> userMap = new HashMap<>();
userMap.put("userName", "张三");
userMap.put("user-age", 25);
userMap.put("user info", "用户信息");
request.setAttribute("userMap", userMap);
```

```jsp
<!-- JSP页面EL访问 -->
${userMap.userName}      // 正常属性名，用点号
${userMap["user-age"]}   // 含减号，必须用中括号
${userMap["user info"]}  // 含空格，必须用中括号
```

**💡 Map访问规则理解**：
- EL表达式把Map当作**特殊的对象**来处理
- `${map.key}` 等同于 `map.get("key")`
- 属性名有特殊字符时必须使用中括号访问

### 4.3 List和数组的访问规则


**📚 List集合访问**：

```java
// 后台Java代码
List<String> nameList = Arrays.asList("张三", "李四", "王五");
request.setAttribute("nameList", nameList);

List<User> userList = new ArrayList<>();
userList.add(new User("张三", 20));
userList.add(new User("李四", 25));
request.setAttribute("userList", userList);
```

```jsp
<!-- JSP页面EL访问 -->
${nameList[0]}           // 获取第一个元素："张三"
${nameList[1]}           // 获取第二个元素："李四"
${userList[0].name}      // 获取第一个用户的姓名
${userList[1].age}       // 获取第二个用户的年龄
```

**🔢 数组访问规则**：
```java
// 后台Java代码
String[] colors = {"红色", "绿色", "蓝色"};
request.setAttribute("colors", colors);
```

```jsp
<!-- JSP页面EL访问 -->
${colors[0]}             // 获取第一个颜色："红色"
${colors[2]}             // 获取第三个颜色："蓝色"
```

---

## 5. 🛡️ 空安全访问机制


### 5.1 什么是空安全访问


**🔸 空安全的含义**：当访问的对象或属性为null时，EL表达式不会抛出异常，而是返回空字符串

```
传统Java代码的问题：
User user = null;
String name = user.getName();  // 抛出 NullPointerException

EL表达式的优势：
${user.name}  // 返回空字符串 ""，不会出错
```

### 5.2 空安全机制详解


**🛡️ 空值处理规则**：

```jsp
<%-- 设置测试数据 --%>
<%
    User user = null;
    request.setAttribute("user", user);
    
    User validUser = new User();
    // 注意：validUser.name 为 null
    request.setAttribute("validUser", validUser);
%>

<!-- EL表达式空安全测试 -->
${user}              <!-- 显示：空白 -->
${user.name}         <!-- 显示：空白，不报错 -->
${validUser.name}    <!-- 显示：空白，属性为null -->
${nonExistent}       <!-- 显示：空白，变量不存在 -->
```

**📋 空安全规则总结**：

| 情况 | **EL表达式** | **显示结果** | **说明** |
|------|-------------|-------------|----------|
| 对象为null | `${null对象.属性}` | 空白 | 不抛异常 |
| 属性为null | `${对象.null属性}` | 空白 | 返回空字符串 |
| 变量不存在 | `${不存在的变量}` | 空白 | 自动处理 |
| 索引越界 | `${数组[99]}` | 空白 | 不报错 |

### 5.3 空值判断与处理


**🔍 empty操作符**：用于判断值是否为空

```jsp
<!-- empty操作符使用 -->
${empty user}              <!-- user为null时返回true -->
${empty user.name}         <!-- user.name为空时返回true -->
${empty nameList}          <!-- 集合为空时返回true -->
${not empty user}          <!-- 非空判断 -->
```

**💡 empty判断规则**：
```
以下情况 empty 返回 true：
- null 值
- 空字符串 ""
- 空数组 []
- 空集合 Collection
- 空Map
```

**🎯 实际应用示例**：
```jsp
<!-- 条件显示 -->
${not empty user.name ? user.name : "匿名用户"}

<!-- 或者使用三元表达式的变体 -->
<c:if test="${not empty user.name}">
    欢迎，${user.name}！
</c:if>
<c:if test="${empty user.name}">
    欢迎，游客！
</c:if>
```

---

## 6. 🔗 嵌套取值实战应用


### 6.1 简单嵌套访问


**🏢 对象嵌套结构示例**：

```java
// Java实体类
public class Company {
    private String name;
    private Department department;
    // getter/setter略
}

public class Department {
    private String name;
    private Employee manager;
    // getter/setter略
}

public class Employee {
    private String name;
    private int age;
    // getter/setter略
}
```

```jsp
<!-- 嵌套访问 -->
${company.name}                    <!-- 公司名称 -->
${company.department.name}         <!-- 部门名称 -->
${company.department.manager.name} <!-- 部门经理姓名 -->
${company.department.manager.age}  <!-- 部门经理年龄 -->
```

### 6.2 复杂嵌套访问


**🗂️ 集合中的对象嵌套**：

```java
// 后台数据准备
List<Department> deptList = new ArrayList<>();
Department dept1 = new Department("技术部");
dept1.setEmployees(Arrays.asList(
    new Employee("张三", 28),
    new Employee("李四", 32)
));
deptList.add(dept1);
request.setAttribute("deptList", deptList);
```

```jsp
<!-- 复杂嵌套访问 -->
${deptList[0].name}                    <!-- 第一个部门名称 -->
${deptList[0].employees[0].name}       <!-- 第一个部门第一个员工姓名 -->
${deptList[0].employees[1].age}        <!-- 第一个部门第二个员工年龄 -->
```

### 6.3 Map嵌套访问


**🗝️ Map中嵌套Map**：

```java
// 后台数据准备
Map<String, Object> companyInfo = new HashMap<>();
Map<String, String> address = new HashMap<>();
address.put("province", "广东省");
address.put("city", "深圳市");
address.put("district", "南山区");

companyInfo.put("name", "科技公司");
companyInfo.put("address", address);
request.setAttribute("companyInfo", companyInfo);
```

```jsp
<!-- Map嵌套访问 -->
${companyInfo.name}                   <!-- 公司名称 -->
${companyInfo.address.province}       <!-- 省份 -->
${companyInfo.address.city}           <!-- 城市 -->
${companyInfo.address["district"]}    <!-- 区域（两种写法都可以） -->
```

### 6.4 嵌套访问最佳实践


**📚 访问路径规划**：

```
嵌套访问的层级建议：
✅ 1-3层嵌套：${obj.prop.subProp}          // 推荐
⚠️ 4-5层嵌套：${obj.a.b.c.d}             // 谨慎使用  
❌ 6层以上：${obj.a.b.c.d.e.f}           // 不推荐，难维护
```

**🔧 嵌套访问优化技巧**：
```jsp
<!-- 避免重复的长路径 -->
<!-- 不好的写法 -->
<p>员工：${company.department.manager.name}</p>
<p>年龄：${company.department.manager.age}</p>
<p>职位：${company.department.manager.position}</p>

<!-- 好的写法：使用中间变量 -->
<c:set var="manager" value="${company.department.manager}" />
<p>员工：${manager.name}</p>
<p>年龄：${manager.age}</p>
<p>职位：${manager.position}</p>
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的基础概念


```
🔸 EL表达式本质：JSP页面的"自动取值器"，语法简单，空值安全
🔸 基本语法：${变量名}，自动从四个域按优先级查找
🔸 两种访问方式：点号访问（常用）、中括号访问（灵活）
🔸 空安全机制：任何null值都返回空字符串，不抛异常
🔸 嵌套访问：支持多层级的对象属性访问
```

### 7.2 关键理解要点


**🔹 EL表达式的自动查找机制**
```
记住查找顺序：page → request → session → application
理解原理：EL帮你自动遍历这四个域，找到就停止
实际意义：简化了程序员手动查找的复杂过程
```

**🔹 点号vs中括号访问的选择**
```
点号访问：${obj.prop} - 属性名确定，无特殊字符时使用
中括号访问：${obj["prop"]} - 属性名动态，有特殊字符时使用
选择原则：能用点号就用点号，特殊情况才用中括号
```

**🔹 空安全的实际价值**
```
传统问题：null.getXXX() 抛出 NullPointerException
EL解决方案：${null对象.属性} 返回空字符串
实际价值：页面不会因为数据问题而报错，用户体验更好
```

### 7.3 实际开发应用指导


**💼 常见应用场景**
- **用户信息显示**：`${user.name}` 显示当前登录用户
- **商品信息展示**：`${product.price}` 显示商品价格
- **列表数据遍历**：`${userList[0].name}` 获取列表中的数据
- **配置信息读取**：`${config["app.name"]}` 读取应用配置

**⚠️ 使用注意事项**
- EL表达式只能**读取数据**，不能**修改数据**
- 优先从**小范围域**（page）开始查找
- 嵌套层级不宜**太深**（建议不超过3层）
- 属性名包含**特殊字符**时必须使用中括号访问

**🎯 性能优化建议**
- 避免过深的嵌套访问
- 合理使用中间变量减少重复计算
- 在合适的域中存储数据（减少查找范围）

### 7.4 学习进阶路径


```
📚 当前学习阶段：EL表达式语法基础 ⭐⭐⭐
📖 下一步学习：EL表达式运算符与内置对象 ⭐⭐
🚀 进阶学习：JSTL标签库配合EL表达式使用 ⭐⭐⭐
🎯 实战应用：构建完整的JSP动态网页 ⭐⭐⭐
```

**核心记忆口诀**：
```
EL表达式真简单，花括号加美元完整版
点号中括号两访问，特殊字符中括号见
四个域里自动找，空值安全不用慌
嵌套访问层层取，JavaWeb开发必须会
```

**🔗 相关知识点**
- **JSP内置对象** - 理解EL表达式的数据来源
- **JavaBean规范** - 掌握getter/setter方法的命名规则  
- **JSTL核心标签** - EL表达式的最佳搭档
- **MVC模式** - 理解EL在View层的作用定位