---
title: 1、用户管理系统实战
---
## 📚 目录

1. [项目概述与架构设计](#1-项目概述与架构设计)
2. [用户列表展示实现](#2-用户列表展示实现)
3. [分页数据处理](#3-分页数据处理)
4. [条件查询功能](#4-条件查询功能)
5. [表格数据展示优化](#5-表格数据展示优化)
6. [状态判断与动态显示](#6-状态判断与动态显示)
7. [操作按钮权限控制](#7-操作按钮权限控制)
8. [完整功能集成](#8-完整功能集成)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🏗️ 项目概述与架构设计


### 1.1 什么是用户管理系统


> 📌 **核心概念**  
> 用户管理系统就像公司的人事管理系统，用来管理网站或应用中的所有用户信息，包括查看、添加、修改、删除用户等功能。

**系统功能结构图**：
```
用户管理系统
├── 用户列表展示
│   ├── 分页显示
│   ├── 条件查询
│   └── 状态筛选
├── 用户操作功能
│   ├── 新增用户
│   ├── 编辑用户
│   └── 删除用户
└── 权限控制
    ├── 角色判断
    ├── 操作权限
    └── 数据权限
```

### 1.2 技术架构选择


| **技术层次** | **选用技术** | **主要作用** | **为什么选择** |
|-------------|-------------|-------------|----------------|
| **前端展示** | `EL表达式` + `JSTL` | 数据显示和逻辑控制 | 简单易学，适合新手 |
| **后端逻辑** | `Servlet` + `JSP` | 业务处理和页面渲染 | JavaWeb基础技术 |
| **数据存储** | `MySQL` | 用户数据持久化 | 关系型数据库，数据结构清晰 |
| **样式美化** | `Bootstrap` | 页面美观和响应式 | 快速开发，样式统一 |

### 1.3 核心实现思路


🌱 **入门理解**：把用户管理想象成学校的学生名册管理
- **查看名册**：展示所有学生信息
- **按班级查找**：根据条件筛选学生
- **分页翻看**：一页显示部分学生，可以翻页
- **权限控制**：只有老师能修改学生信息

🌿 **技术实现**：
- **EL表达式**：从后台取数据显示到页面
- **JSTL标签**：循环显示用户列表，判断用户状态
- **分页处理**：计算页数，控制显示范围
- **条件查询**：根据用户输入筛选数据

---

## 2. 👥 用户列表展示实现


### 2.1 基础数据结构设计


**用户信息包含哪些内容**：
```
用户基本信息：
├── id（用户编号）
├── username（用户名）
├── email（邮箱）
├── phone（手机号）
├── status（状态：1-正常，0-禁用）
├── role（角色：admin-管理员，user-普通用户）
├── createTime（注册时间）
└── lastLoginTime（最后登录时间）
```

### 2.2 后台数据准备


**Servlet中准备用户列表数据**：
```java
// UserListServlet.java - 获取用户列表
@WebServlet("/userList")
public class UserListServlet extends HttpServlet {
    protected void doGet(HttpServletRequest request, HttpServletResponse response) {
        // 1. 获取所有用户数据（实际项目中从数据库查询）
        List<User> userList = getUserListFromDB();
        
        // 2. 将数据放入request域，供页面使用
        request.setAttribute("userList", userList);
        request.setAttribute("totalCount", userList.size());
        
        // 3. 转发到用户列表页面
        request.getRequestDispatcher("/userList.jsp").forward(request, response);
    }
}
```

### 2.3 前端页面基础展示


**用户列表页面框架**：
```html
<%-- userList.jsp --%>
<%@ page contentType="text/html;charset=UTF-8" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>

<!DOCTYPE html>
<html>
<head>
    <title>用户管理系统</title>
    <link href="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/4.6.0/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h2>📋 用户管理列表</h2>
        
        <!-- 这里放用户列表表格 -->
        <!-- 后面章节详细实现 -->
        
    </div>
</body>
</html>
```

> 💡 **理解要点**  
> - `request.setAttribute()` 就像把数据放在一个篮子里
> - `EL表达式 ${userList}` 就是从篮子里取数据
> - `JSTL标签` 帮我们处理数据的展示逻辑

---

## 3. 📄 分页数据处理


### 3.1 分页的基本概念


> 📌 **什么是分页**  
> 就像看书一样，一本厚书不能一页显示所有内容，需要分成很多页来看。网页也是如此，用户太多时需要分页显示。

**分页核心参数**：
```
分页参数说明：
├── currentPage（当前页码）
├── pageSize（每页显示数量）
├── totalCount（总记录数）
├── totalPages（总页数）
├── startIndex（查询起始位置）
└── endIndex（查询结束位置）

计算关系：
totalPages = Math.ceil(totalCount / pageSize)
startIndex = (currentPage - 1) * pageSize
```

### 3.2 后台分页逻辑实现


```java
// 分页查询用户列表
@WebServlet("/userList")
public class UserListServlet extends HttpServlet {
    protected void doGet(HttpServletRequest request, HttpServletResponse response) {
        // 1. 获取分页参数
        int currentPage = getIntParam(request, "page", 1);  // 默认第1页
        int pageSize = 10;  // 每页10条数据
        
        // 2. 计算查询范围
        int startIndex = (currentPage - 1) * pageSize;
        
        // 3. 查询分页数据
        List<User> userList = getUserListByPage(startIndex, pageSize);
        int totalCount = getTotalUserCount();
        int totalPages = (int) Math.ceil((double) totalCount / pageSize);
        
        // 4. 封装分页信息
        request.setAttribute("userList", userList);
        request.setAttribute("currentPage", currentPage);
        request.setAttribute("totalPages", totalPages);
        request.setAttribute("totalCount", totalCount);
        
        request.getRequestDispatcher("/userList.jsp").forward(request, response);
    }
}
```

### 3.3 前端分页控件实现


**Bootstrap分页组件**：
```html
<!-- 分页导航栏 -->
<nav aria-label="用户列表分页">
    <ul class="pagination justify-content-center">
        <!-- 上一页按钮 -->
        <c:if test="${currentPage > 1}">
            <li class="page-item">
                <a class="page-link" href="userList?page=${currentPage - 1}">上一页</a>
            </li>
        </c:if>
        
        <!-- 页码按钮 -->
        <c:forEach begin="1" end="${totalPages}" var="pageNum">
            <li class="page-item ${pageNum == currentPage ? 'active' : ''}">
                <a class="page-link" href="userList?page=${pageNum}">${pageNum}</a>
            </li>
        </c:forEach>
        
        <!-- 下一页按钮 -->
        <c:if test="${currentPage < totalPages}">
            <li class="page-item">
                <a class="page-link" href="userList?page=${currentPage + 1}">下一页</a>
            </li>
        </c:if>
    </ul>
</nav>

<!-- 分页信息显示 -->
<div class="text-center mt-3">
    <small class="text-muted">
        共 ${totalCount} 条记录，当前第 ${currentPage} / ${totalPages} 页
    </small>
</div>
```

> ⚠️ **注意事项**  
> - 页码从1开始，不是从0开始
> - 要处理边界情况：第一页没有"上一页"，最后一页没有"下一页"
> - `${pageNum == currentPage ? 'active' : ''}` 是三目运算符，当前页高亮显示

---

## 4. 🔍 条件查询功能


### 4.1 查询条件设计


**用户常用的查询需求**：
```
查询维度分析：
├── 按用户名查询（模糊搜索）
├── 按邮箱查询（精确或模糊）
├── 按状态筛选（正常/禁用）
├── 按角色筛选（管理员/普通用户）
└── 按注册时间范围查询
```

### 4.2 查询表单实现


```html
<!-- 查询条件表单 -->
<div class="card mb-4">
    <div class="card-body">
        <form method="get" action="userList" class="form-inline">
            <!-- 用户名搜索 -->
            <div class="form-group mr-3">
                <label class="mr-2">用户名：</label>
                <input type="text" name="username" class="form-control" 
                       value="${param.username}" placeholder="请输入用户名">
            </div>
            
            <!-- 状态筛选 -->
            <div class="form-group mr-3">
                <label class="mr-2">状态：</label>
                <select name="status" class="form-control">
                    <option value="">全部状态</option>
                    <option value="1" ${param.status == '1' ? 'selected' : ''}>正常</option>
                    <option value="0" ${param.status == '0' ? 'selected' : ''}>禁用</option>
                </select>
            </div>
            
            <!-- 角色筛选 -->
            <div class="form-group mr-3">
                <label class="mr-2">角色：</label>
                <select name="role" class="form-control">
                    <option value="">全部角色</option>
                    <option value="admin" ${param.role == 'admin' ? 'selected' : ''}>管理员</option>
                    <option value="user" ${param.role == 'user' ? 'selected' : ''}>普通用户</option>
                </select>
            </div>
            
            <!-- 查询和重置按钮 -->
            <button type="submit" class="btn btn-primary mr-2">🔍 查询</button>
            <a href="userList" class="btn btn-secondary">🔄 重置</a>
        </form>
    </div>
</div>
```

### 4.3 后台查询逻辑处理


```java
// 处理条件查询的Servlet逻辑
protected void doGet(HttpServletRequest request, HttpServletResponse response) {
    // 1. 获取查询条件
    String username = request.getParameter("username");
    String status = request.getParameter("status");
    String role = request.getParameter("role");
    
    // 2. 构建查询条件对象
    UserQuery query = new UserQuery();
    if (isNotEmpty(username)) query.setUsername(username);
    if (isNotEmpty(status)) query.setStatus(Integer.valueOf(status));
    if (isNotEmpty(role)) query.setRole(role);
    
    // 3. 分页参数
    int currentPage = getIntParam(request, "page", 1);
    int pageSize = 10;
    
    // 4. 执行查询
    List<User> userList = getUserListByCondition(query, currentPage, pageSize);
    int totalCount = getUserCountByCondition(query);
    
    // 5. 返回结果
    request.setAttribute("userList", userList);
    request.setAttribute("currentPage", currentPage);
    request.setAttribute("totalPages", (int) Math.ceil((double) totalCount / pageSize));
    request.setAttribute("totalCount", totalCount);
    
    request.getRequestDispatcher("/userList.jsp").forward(request, response);
}
```

> 💡 **实用技巧**  
> - `${param.username}` 可以直接获取URL参数，保持搜索条件
> - `isNotEmpty()` 判断参数是否为空，避免空值查询
> - 查询后要保持分页功能正常工作

---

## 5. 📊 表格数据展示优化


### 5.1 用户列表表格结构


```html
<!-- 用户数据表格 -->
<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="thead-dark">
            <tr>
                <th>序号</th>
                <th>用户名</th>
                <th>邮箱</th>
                <th>手机号</th>
                <th>角色</th>
                <th>状态</th>
                <th>注册时间</th>
                <th>最后登录</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            <!-- 用户数据循环显示 -->
            <c:forEach items="${userList}" var="user" varStatus="status">
                <tr>
                    <!-- 序号计算：(当前页-1) × 每页条数 + 当前行号 -->
                    <td>${(currentPage - 1) * 10 + status.index + 1}</td>
                    
                    <!-- 基本信息显示 -->
                    <td>
                        <strong>${user.username}</strong>
                        <c:if test="${user.isVip}">
                            <span class="badge badge-warning ml-1">VIP</span>
                        </c:if>
                    </td>
                    
                    <td>
                        <a href="mailto:${user.email}" class="text-decoration-none">
                            ${user.email}
                        </a>
                    </td>
                    
                    <td>${user.phone}</td>
                    
                    <!-- 角色显示（后面详细说明） -->
                    <td>角色标签</td>
                    
                    <!-- 状态显示（后面详细说明） -->
                    <td>状态标签</td>
                    
                    <!-- 时间格式化显示 -->
                    <td>
                        <fmt:formatDate value="${user.createTime}" pattern="yyyy-MM-dd HH:mm"/>
                    </td>
                    
                    <td>
                        <c:choose>
                            <c:when test="${not empty user.lastLoginTime}">
                                <fmt:formatDate value="${user.lastLoginTime}" pattern="MM-dd HH:mm"/>
                            </c:when>
                            <c:otherwise>
                                <span class="text-muted">从未登录</span>
                            </c:otherwise>
                        </c:choose>
                    </td>
                    
                    <!-- 操作按钮（后面详细说明） -->
                    <td>操作按钮组</td>
                </tr>
            </c:forEach>
        </tbody>
    </table>
</div>

<!-- 无数据提示 -->
<c:if test="${empty userList}">
    <div class="text-center py-5">
        <div class="text-muted">
            <i class="fas fa-inbox fa-3x mb-3"></i>
            <p>暂无用户数据</p>
        </div>
    </div>
</c:if>
```

### 5.2 表格样式优化技巧


| **样式类** | **作用** | **使用场景** |
|-----------|---------|-------------|
| `table-striped` | 斑马纹效果 | 提高表格可读性 |
| `table-hover` | 鼠标悬停高亮 | 增强交互体验 |
| `table-responsive` | 响应式滚动 | 移动端适配 |
| `thead-dark` | 深色表头 | 突出标题行 |

> 📊 **数据展示最佳实践**  
> - **序号连续性**：分页时序号要连续，不能每页都从1开始
> - **时间格式化**：使用 `<fmt:formatDate>` 格式化日期显示
> - **空值处理**：用 `<c:choose>` 处理可能为空的数据
> - **响应式设计**：使用 `table-responsive` 适配移动端

---

## 6. 🎯 状态判断与动态显示


### 6.1 用户状态的可视化展示


**状态标签实现**：
```html
<!-- 用户状态列 -->
<td>
    <c:choose>
        <c:when test="${user.status == 1}">
            <span class="badge badge-success">✅ 正常</span>
        </c:when>
        <c:when test="${user.status == 0}">
            <span class="badge badge-danger">❌ 禁用</span>
        </c:when>
        <c:otherwise>
            <span class="badge badge-secondary">❓ 未知</span>
        </c:otherwise>
    </c:choose>
</td>
```

### 6.2 用户角色的标签显示


```html
<!-- 用户角色列 -->
<td>
    <c:choose>
        <c:when test="${user.role == 'admin'}">
            <span class="badge badge-primary">👑 管理员</span>
        </c:when>
        <c:when test="${user.role == 'user'}">
            <span class="badge badge-info">👤 普通用户</span>
        </c:when>
        <c:when test="${user.role == 'vip'}">
            <span class="badge badge-warning">💎 VIP用户</span>
        </c:when>
        <c:otherwise>
            <span class="badge badge-light">❓ 未知角色</span>
        </c:otherwise>
    </c:choose>
</td>
```

### 6.3 动态样式控制


**根据用户状态改变行样式**：
```html
<c:forEach items="${userList}" var="user" varStatus="status">
    <!-- 动态添加CSS类 -->
    <tr class="${user.status == 0 ? 'table-secondary' : ''} 
               ${user.isOnline ? 'border-left-success' : ''}">
        
        <!-- 用户名显示，禁用用户显示为灰色 -->
        <td>
            <span class="${user.status == 0 ? 'text-muted' : 'font-weight-bold'}">
                ${user.username}
            </span>
            
            <!-- 在线状态指示器 -->
            <c:if test="${user.isOnline}">
                <span class="badge badge-success badge-pill ml-1">在线</span>
            </c:if>
        </td>
        
        <!-- 其他列... -->
    </tr>
</c:forEach>
```

### 6.4 状态统计信息


```html
<!-- 页面顶部显示统计信息 -->
<div class="row mb-4">
    <div class="col-md-3">
        <div class="card text-white bg-primary">
            <div class="card-body">
                <h5>总用户数</h5>
                <h2>${totalCount}</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-success">
            <div class="card-body">
                <h5>正常用户</h5>
                <h2>${normalUserCount}</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-danger">
            <div class="card-body">
                <h5>禁用用户</h5>
                <h2>${disabledUserCount}</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-info">
            <div class="card-body">
                <h5>在线用户</h5>
                <h2>${onlineUserCount}</h2>
            </div>
        </div>
    </div>
</div>
```

> 🎨 **视觉设计要点**  
> - **颜色含义**：绿色表示正常/成功，红色表示禁用/危险，蓝色表示信息
> - **图标使用**：适当使用emoji或图标增强识别度
> - **状态一致性**：整个系统的状态颜色要保持一致

---

## 7. 🔐 操作按钮权限控制


### 7.1 权限判断的基本原理


> 📌 **权限控制概念**  
> 就像现实中的门禁卡，不同级别的员工能进入不同的房间。在系统中，不同角色的用户能看到和操作不同的功能。

**权限控制层次**：
```
权限控制架构：
├── 页面级权限（能否访问页面）
├── 功能级权限（能否看到按钮）
├── 数据级权限（能否操作某条数据）
└── 字段级权限（能否查看某个字段）
```

### 7.2 操作按钮的权限控制


```html
<!-- 操作按钮列 -->
<td>
    <div class="btn-group btn-group-sm" role="group">
        <!-- 查看按钮 - 所有用户都能看 -->
        <a href="userDetail?id=${user.id}" class="btn btn-outline-info" title="查看详情">
            👁️ 查看
        </a>
        
        <!-- 编辑按钮 - 需要编辑权限 -->
        <c:if test="${sessionScope.currentUser.hasEditPermission}">
            <a href="userEdit?id=${user.id}" class="btn btn-outline-primary" title="编辑用户">
                ✏️ 编辑
            </a>
        </c:if>
        
        <!-- 删除按钮 - 需要删除权限且不能删除自己 -->
        <c:if test="${sessionScope.currentUser.hasDeletePermission && user.id != sessionScope.currentUser.id}">
            <button type="button" class="btn btn-outline-danger" 
                    onclick="deleteUser(${user.id}, '${user.username}')" title="删除用户">
                🗑️ 删除
            </button>
        </c:if>
        
        <!-- 重置密码 - 管理员权限 -->
        <c:if test="${sessionScope.currentUser.role == 'admin'}">
            <button type="button" class="btn btn-outline-warning" 
                    onclick="resetPassword(${user.id})" title="重置密码">
                🔑 重置密码
            </button>
        </c:if>
        
        <!-- 启用/禁用按钮 - 管理员权限且不能操作自己 -->
        <c:if test="${sessionScope.currentUser.role == 'admin' && user.id != sessionScope.currentUser.id}">
            <c:choose>
                <c:when test="${user.status == 1}">
                    <button type="button" class="btn btn-outline-secondary" 
                            onclick="changeUserStatus(${user.id}, 0)" title="禁用用户">
                        🚫 禁用
                    </button>
                </c:when>
                <c:otherwise>
                    <button type="button" class="btn btn-outline-success" 
                            onclick="changeUserStatus(${user.id}, 1)" title="启用用户">
                        ✅ 启用
                    </button>
                </c:otherwise>
            </c:choose>
        </c:if>
    </div>
</td>
```

### 7.3 权限控制的JavaScript支持


```html
<script>
// 删除用户确认
function deleteUser(userId, username) {
    if (confirm('确定要删除用户"' + username + '"吗？删除后不可恢复！')) {
        // 发送删除请求
        window.location.href = 'userDelete?id=' + userId;
    }
}

// 重置密码确认
function resetPassword(userId) {
    if (confirm('确定要重置此用户的密码吗？用户需要重新设置密码。')) {
        // 发送重置密码请求
        window.location.href = 'userResetPassword?id=' + userId;
    }
}

// 改变用户状态
function changeUserStatus(userId, newStatus) {
    const action = newStatus === 1 ? '启用' : '禁用';
    if (confirm('确定要' + action + '此用户吗？')) {
        // 发送状态变更请求
        window.location.href = 'userChangeStatus?id=' + userId + '&status=' + newStatus;
    }
}
</script>
```

### 7.4 批量操作功能


```html
<!-- 批量操作区域 -->
<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <!-- 全选复选框 -->
        <input type="checkbox" id="selectAll" class="mr-2">
        <label for="selectAll">全选</label>
        
        <!-- 批量操作按钮 -->
        <c:if test="${sessionScope.currentUser.role == 'admin'}">
            <button type="button" class="btn btn-sm btn-outline-danger ml-3" 
                    onclick="batchDelete()" id="batchDeleteBtn" style="display:none;">
                🗑️ 批量删除
            </button>
            <button type="button" class="btn btn-sm btn-outline-secondary ml-2" 
                    onclick="batchDisable()" id="batchDisableBtn" style="display:none;">
                🚫 批量禁用
            </button>
        </c:if>
    </div>
    
    <!-- 新增用户按钮 -->
    <c:if test="${sessionScope.currentUser.hasAddPermission}">
        <a href="userAdd" class="btn btn-success">
            ➕ 新增用户
        </a>
    </c:if>
</div>

<!-- 表格中的复选框列 -->
<thead class="thead-dark">
    <tr>
        <th>
            <input type="checkbox" class="select-all">
        </th>
        <th>序号</th>
        <!-- 其他表头... -->
    </tr>
</thead>

<tbody>
    <c:forEach items="${userList}" var="user" varStatus="status">
        <tr>
            <td>
                <!-- 不能选择自己 -->
                <c:if test="${user.id != sessionScope.currentUser.id}">
                    <input type="checkbox" name="selectedUsers" value="${user.id}" class="user-checkbox">
                </c:if>
            </td>
            <!-- 其他列... -->
        </tr>
    </c:forEach>
</tbody>
```

> 🔒 **权限控制要点**  
> - **最小权限原则**：用户只能看到和操作自己权限范围内的内容
> - **自我保护**：用户不能删除或禁用自己
> - **危险操作确认**：删除、重置密码等操作要用户二次确认
> - **前后端双重验证**：前端控制显示，后端验证权限

---

## 8. 🔧 完整功能集成


### 8.1 完整的JSP页面结构


```html
<%@ page contentType="text/html;charset=UTF-8" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>用户管理系统</title>
    <link href="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/4.6.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.bootcdn.net/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container-fluid">
        <!-- 页面标题 -->
        <div class="d-flex justify-content-between align-items-center py-3">
            <h2>📋 用户管理系统</h2>
            <small class="text-muted">
                欢迎，${sessionScope.currentUser.username} 
                <span class="badge badge-info">${sessionScope.currentUser.role}</span>
            </small>
        </div>
        
        <!-- 统计卡片区域 -->
        <!-- (前面已实现) -->
        
        <!-- 查询条件区域 -->
        <!-- (前面已实现) -->
        
        <!-- 操作按钮区域 -->
        <!-- (前面已实现) -->
        
        <!-- 用户列表表格 -->
        <!-- (前面已实现) -->
        
        <!-- 分页导航 -->
        <!-- (前面已实现) -->
    </div>
    
    <!-- JavaScript脚本 -->
    <script src="https://cdn.bootcdn.net/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/4.6.0/js/bootstrap.bundle.min.js"></script>
    <!-- 自定义脚本 -->
</body>
</html>
```

### 8.2 完整的Servlet控制器


```java
@WebServlet("/userList")
public class UserListServlet extends HttpServlet {
    private UserService userService = new UserService();
    
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        try {
            // 1. 参数获取与验证
            UserQueryParam queryParam = buildQueryParam(request);
            
            // 2. 权限检查
            User currentUser = (User) request.getSession().getAttribute("currentUser");
            if (currentUser == null) {
                response.sendRedirect("login.jsp");
                return;
            }
            
            // 3. 执行查询
            PageResult<User> pageResult = userService.getUserListByPage(queryParam);
            Map<String, Integer> statistics = userService.getUserStatistics();
            
            // 4. 数据封装
            request.setAttribute("userList", pageResult.getList());
            request.setAttribute("currentPage", queryParam.getCurrentPage());
            request.setAttribute("totalPages", pageResult.getTotalPages());
            request.setAttribute("totalCount", pageResult.getTotalCount());
            request.setAttribute("normalUserCount", statistics.get("normal"));
            request.setAttribute("disabledUserCount", statistics.get("disabled"));
            request.setAttribute("onlineUserCount", statistics.get("online"));
            
            // 5. 页面转发
            request.getRequestDispatcher("/WEB-INF/views/userList.jsp").forward(request, response);
            
        } catch (Exception e) {
            logger.error("用户列表查询失败", e);
            request.setAttribute("errorMessage", "系统繁忙，请稍后再试");
            request.getRequestDispatcher("/error.jsp").forward(request, response);
        }
    }
    
    // 构建查询参数对象
    private UserQueryParam buildQueryParam(HttpServletRequest request) {
        UserQueryParam param = new UserQueryParam();
        param.setCurrentPage(getIntParam(request, "page", 1));
        param.setPageSize(10);
        param.setUsername(request.getParameter("username"));
        param.setStatus(getIntParam(request, "status", null));
        param.setRole(request.getParameter("role"));
        return param;
    }
}
```

### 8.3 关键功能的完整流程


**用户查询完整流程图**：
```
用户操作流程：
用户访问页面 → Servlet接收请求 → 参数解析验证 → 权限检查
     ↓              ↓              ↓          ↓
JSP页面显示 ← 数据封装返回 ← 业务逻辑处理 ← 数据库查询
     ↓
用户交互操作（分页、查询、操作按钮）
     ↓
重新发起请求...
```

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的核心技术


```markdown
🔸 EL表达式应用：${userList}、${param.username}、${sessionScope.currentUser}
🔸 JSTL标签使用：<c:forEach>、<c:if>、<c:choose>、<fmt:formatDate>
🔸 分页逻辑实现：页码计算、数据分页、分页导航
🔸 条件查询功能：参数获取、SQL构建、结果展示
🔸 权限控制机制：会话管理、角色判断、按钮控制
🔸 状态动态显示：条件判断、样式控制、标签展示
```

### 9.2 关键理解要点


**🔹 分页的核心思想**
```
理解要点：
- 分页 = 总数据切片显示
- 核心参数：当前页、每页条数、总条数、总页数
- 用户体验：页码连续、边界处理、加载提示
```

**🔹 权限控制的实现策略**
```
控制层次：
- 会话级别：登录状态检查
- 角色级别：管理员/普通用户区分
- 数据级别：只能操作自己的数据
- 操作级别：增删改查权限细分
```

**🔹 前后端数据交互**
```
数据流向：
数据库 → Servlet → Request域 → JSP → EL表达式 → 页面显示
用户操作 → 表单提交 → Servlet处理 → 业务逻辑 → 数据库操作
```

### 9.3 实际应用价值


- **企业级开发**：掌握基础的用户管理功能开发
- **权限系统设计**：理解RBAC（基于角色的访问控制）基础
- **前端界面优化**：Bootstrap + EL + JSTL的组合使用
- **数据展示技巧**：分页、查询、状态显示的标准实现
- **用户体验设计**：操作确认、状态提示、错误处理

### 9.4 学习检验清单


✅ **自检清单**：
- [ ] 能独立实现用户列表的分页显示
- [ ] 能实现基于条件的用户查询功能
- [ ] 理解并能应用基本的权限控制
- [ ] 能使用JSTL标签进行状态判断和循环显示
- [ ] 掌握EL表达式在数据展示中的应用
- [ ] 能处理用户操作的确认和反馈机制

### 9.5 扩展学习方向


```markdown
📚 **进阶学习路径**：
基础扩展：
- Ajax无刷新分页和查询
- 数据导出功能（Excel/PDF）
- 高级权限控制（字段级权限）

技术升级：
- Spring Boot + MyBatis改造
- Vue.js前后端分离版本
- Redis缓存优化

企业应用：
- 日志审计系统
- 多租户用户管理
- 单点登录（SSO）集成
```

**核心记忆**：
- 用户管理系统是JavaWeb综合应用的典型案例
- EL表达式和JSTL标签是数据展示的核心技术
- 分页和查询是数据处理的基础功能
- 权限控制是企业级应用的必备功能
- 用户体验细节决定系统的专业程度