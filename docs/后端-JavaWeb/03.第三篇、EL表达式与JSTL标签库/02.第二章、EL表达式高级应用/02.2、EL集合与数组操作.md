---
title: 2、EL集合与数组操作
---
## 📚 目录

1. [EL处理集合数据的基本概念](#1-EL处理集合数据的基本概念)
2. [List集合的EL操作](#2-List集合的EL操作)
3. [Set集合的EL处理](#3-Set集合的EL处理)
4. [Map集合的EL操作](#4-Map集合的EL操作)
5. [数组的EL访问操作](#5-数组的EL访问操作)
6. [集合判断与大小获取](#6-集合判断与大小获取)
7. [集合遍历的EL准备工作](#7-集合遍历的EL准备工作)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 EL处理集合数据的基本概念


### 1.1 什么是集合数据操作


**🔸 通俗理解**  
想象你有一个装苹果的篮子，EL表达式就是帮你从篮子里取苹果的工具。不管篮子是什么类型（List、Set、Map），EL都能帮你轻松取出里面的数据。

**🔸 为什么需要EL处理集合**
```
传统JSP方式：需要写Java代码循环取数据，复杂繁琐
EL表达式方式：直接用简单语法访问，简洁高效

现实场景：
- 显示用户列表
- 展示商品信息
- 显示菜单选项
- 查看订单详情
```

### 1.2 EL支持的集合类型


**🔸 四大集合家族**
```
📋 List集合：有序的数据列表，像排队的人群
📦 Set集合：不重复的数据集合，像身份证号码库
🗂️ Map集合：键值对数据，像电话簿（姓名->电话）
📊 数组Array：固定大小的数据容器，像停车位
```

> 💡 **新手提示**  
> 在JavaWeb中，我们经常需要把这些集合数据从后端传递到前端页面显示，EL表达式就是这座桥梁。

---

## 2. 📋 List集合的EL操作


### 2.1 List集合基本概念


**🌰 生活类比**  
List就像一个有编号的置物架，每个位置都有一个索引号（0、1、2...），你可以根据编号准确找到想要的东西。

### 2.2 List集合的创建与访问


**🔸 后端准备List数据**
```java
// Servlet中准备数据
List<String> fruits = new ArrayList<>();
fruits.add("苹果");
fruits.add("香蕉");
fruits.add("橙子");
fruits.add("葡萄");

// 存储到请求域
request.setAttribute("fruitList", fruits);
```

**🔸 前端EL访问**
```jsp
<!-- 访问第一个水果 -->
第一个水果：${fruitList[0]}   <!-- 输出：苹果 -->

<!-- 访问第二个水果 -->
第二个水果：${fruitList[1]}   <!-- 输出：香蕉 -->

<!-- 获取List大小 -->
水果总数：${fruitList.size()}  <!-- 输出：4 -->
```

### 2.3 复杂对象List的处理


**🔸 学生信息列表示例**
```java
// 学生类
public class Student {
    private String name;
    private int age;
    // 构造方法和getter/setter...
}

// Servlet中准备数据
List<Student> students = new ArrayList<>();
students.add(new Student("张三", 20));
students.add(new Student("李四", 21));
request.setAttribute("studentList", students);
```

**🔸 EL访问对象属性**
```jsp
<!-- 访问第一个学生的姓名 -->
第一个学生：${studentList[0].name}  <!-- 输出：张三 -->

<!-- 访问第二个学生的年龄 -->
第二个学生年龄：${studentList[1].age}  <!-- 输出：21 -->
```

> ⚠️ **重要提醒**  
> List的索引从0开始，不是从1开始！这是新手最容易犯的错误。

---

## 3. 📦 Set集合的EL处理


### 3.1 Set集合特点理解


**🌰 生活类比**  
Set就像一个会自动去重的盒子，你放进去重复的东西，它只会保留一个。而且里面的东西没有固定顺序。

**🔸 Set与List的区别**
```
List特点：有序、可重复、可通过索引访问
Set特点：无序、不重复、不能通过索引访问
```

### 3.2 Set集合的EL操作限制


**🔸 后端准备Set数据**
```java
Set<String> colors = new HashSet<>();
colors.add("红色");
colors.add("蓝色");
colors.add("绿色");
colors.add("红色"); // 重复，只会保留一个

request.setAttribute("colorSet", colors);
```

**🔸 EL访问方式**
```jsp
<!-- ❌ 错误方式：Set不支持索引访问 -->
${colorSet[0]}  <!-- 这样写会报错！ -->

<!-- ✅ 正确方式：获取Set大小 -->
颜色种类数：${colorSet.size()}  <!-- 输出：3 -->

<!-- ✅ 正确方式：判断是否包含某元素 -->
${colorSet.contains('红色')}  <!-- 输出：true -->
```

> 💡 **实用技巧**  
> Set主要用于判断元素是否存在，如果需要遍历显示，通常配合JSTL的forEach标签使用。

---

## 4. 🗂️ Map集合的EL操作


### 4.1 Map集合基本理解


**🌰 生活类比**  
Map就像一个电话簿，每个人的姓名（键）对应一个电话号码（值）。你知道姓名就能快速找到对应的电话号码。

### 4.2 Map集合的创建与访问


**🔸 后端准备Map数据**
```java
Map<String, String> userInfo = new HashMap<>();
userInfo.put("name", "张三");
userInfo.put("age", "25");
userInfo.put("city", "北京");
userInfo.put("job", "程序员");

request.setAttribute("user", userInfo);
```

**🔸 EL访问Map数据**
```jsp
<!-- 方式1：点语法访问 -->
用户姓名：${user.name}     <!-- 输出：张三 -->
用户年龄：${user.age}      <!-- 输出：25 -->

<!-- 方式2：中括号语法访问 -->
用户城市：${user['city']}  <!-- 输出：北京 -->
用户职业：${user["job"]}   <!-- 输出：程序员 -->
```

### 4.3 复杂Map操作


**🔸 嵌套Map处理**
```java
Map<String, Object> data = new HashMap<>();
data.put("total", 100);

Map<String, String> detail = new HashMap<>();
detail.put("type", "订单");
detail.put("status", "已完成");
data.put("info", detail);

request.setAttribute("orderData", data);
```

**🔸 EL访问嵌套数据**
```jsp
总数：${orderData.total}              <!-- 输出：100 -->
类型：${orderData.info.type}          <!-- 输出：订单 -->
状态：${orderData['info']['status']}  <!-- 输出：已完成 -->
```

> 🤔 **思考题**  
> 什么时候用点语法，什么时候用中括号语法？
> - 点语法：键名是固定的标识符时
> - 中括号：键名包含特殊字符或是动态的时

---

## 5. 📊 数组的EL访问操作


### 5.1 数组基本理解


**🌰 生活类比**  
数组就像一排固定的储物柜，每个柜子都有一个编号，柜子的数量在创建时就确定了，不能随意增减。

### 5.2 一维数组操作


**🔸 后端准备数组数据**
```java
// 字符串数组
String[] subjects = {"语文", "数学", "英语", "物理"};
request.setAttribute("subjectArray", subjects);

// 整数数组
int[] scores = {85, 92, 78, 88};
request.setAttribute("scoreArray", scores);
```

**🔸 EL访问数组元素**
```jsp
<!-- 访问具体元素 -->
第一门课：${subjectArray[0]}    <!-- 输出：语文 -->
第一门成绩：${scoreArray[0]}    <!-- 输出：85 -->

<!-- 获取数组长度 -->
科目总数：${subjectArray.length}  <!-- 输出：4 -->
```

### 5.3 二维数组操作


**🔸 后端准备二维数组**
```java
String[][] schedule = {
    {"语文", "数学", "英语"},
    {"物理", "化学", "生物"},
    {"历史", "地理", "政治"}
};
request.setAttribute("classSchedule", schedule);
```

**🔸 EL访问二维数组**
```jsp
<!-- 访问第一天第二节课 -->
${classSchedule[0][1]}  <!-- 输出：数学 -->

<!-- 访问第三天第一节课 -->
${classSchedule[2][0]}  <!-- 输出：历史 -->
```

---

## 6. 🔍 集合判断与大小获取


### 6.1 集合大小获取


**🔸 各种集合的大小获取**
```jsp
<!-- List大小 -->
列表大小：${myList.size()}

<!-- Set大小 -->
集合大小：${mySet.size()}  

<!-- Map大小 -->
映射大小：${myMap.size()}

<!-- 数组长度 -->
数组长度：${myArray.length}
```

### 6.2 集合为空判断


**🔸 empty运算符的使用**
```jsp
<!-- 判断List是否为空 -->
${empty fruitList}    <!-- true表示为空，false表示非空 -->

<!-- 判断Map是否为空 -->
${empty userMap}      <!-- true表示为空，false表示非空 -->

<!-- 判断数组是否为空 -->
${empty scoreArray}   <!-- true表示为空，false表示非空 -->
```

**🔸 实际应用场景**
```jsp
<!-- 根据集合是否为空显示不同内容 -->
<c:choose>
    <c:when test="${empty productList}">
        <p>暂无商品信息</p>
    </c:when>
    <c:otherwise>
        <p>共有 ${productList.size()} 件商品</p>
    </c:otherwise>
</c:choose>
```

### 6.3 集合包含判断


**🔸 判断集合是否包含特定元素**
```java
// 后端代码
List<String> permissions = Arrays.asList("read", "write", "delete");
request.setAttribute("userPermissions", permissions);
```

```jsp
<!-- 判断用户是否有删除权限 -->
${userPermissions.contains('delete')}  <!-- 输出：true -->

<!-- 实际应用 -->
<c:if test="${userPermissions.contains('delete')}">
    <button>删除按钮</button>
</c:if>
```

---

## 7. 🔄 集合遍历的EL准备工作


### 7.1 为什么需要遍历准备


**🌰 现实场景**  
当你需要显示一个商品列表时，不可能一个一个手写`${products[0]}`、`${products[1]}`...这样太繁琐了。我们需要用循环的方式遍历显示。

### 7.2 遍历前的数据准备


**🔸 后端数据准备示例**
```java
// 准备商品列表
List<Product> products = new ArrayList<>();
products.add(new Product("手机", 2999.0));
products.add(new Product("电脑", 5999.0));
products.add(new Product("平板", 1999.0));

request.setAttribute("productList", products);

// 准备Map数据用于遍历
Map<String, Integer> inventory = new HashMap<>();
inventory.put("手机", 50);
inventory.put("电脑", 30);
inventory.put("平板", 25);

request.setAttribute("inventoryMap", inventory);
```

### 7.3 EL在遍历中的作用


**🔸 配合JSTL forEach使用**
```jsp
<!-- 遍历List -->
<c:forEach var="product" items="${productList}">
    商品名：${product.name}，价格：${product.price}
</c:forEach>

<!-- 遍历Map -->
<c:forEach var="item" items="${inventoryMap}">
    ${item.key}：库存${item.value}件
</c:forEach>
```

### 7.4 遍历中的索引处理


**🔸 获取遍历索引**
```jsp
<c:forEach var="product" items="${productList}" varStatus="status">
    第${status.index + 1}个商品：${product.name}
    <!-- status.index从0开始，所以+1显示自然序号 -->
</c:forEach>
```

> 💡 **实用技巧**  
> varStatus提供了很多有用的信息：
> - `index`：当前索引（从0开始）
> - `count`：当前计数（从1开始）  
> - `first`：是否第一个元素
> - `last`：是否最后一个元素

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的基本概念


**🔸 集合访问语法**
```
List访问：${listName[index]} 或 ${listName.get(index)}
Map访问：${mapName.key} 或 ${mapName['key']}
数组访问：${arrayName[index]}
Set访问：不支持索引，只能判断包含
```

**🔸 集合属性获取**
```
大小获取：${collection.size()} 或 ${array.length}
为空判断：${empty collection}
包含判断：${collection.contains(element)}
```

### 8.2 关键理解要点


**🔹 索引从0开始**
```
记住：计算机世界的计数从0开始
${list[0]}：第一个元素
${list[1]}：第二个元素
```

**🔹 Map的两种访问方式**
```
点语法：${map.key} - 键名简单时使用
括号语法：${map['key']} - 键名有特殊字符时使用
```

**🔹 empty运算符的含义**
```
true：集合为null或大小为0
false：集合不为空且有元素
```

### 8.3 实际应用指导


**✅ 适用场景**
- 显示数据列表（商品、用户、订单等）
- 根据权限显示不同内容
- 数据统计信息显示
- 动态生成页面选项

**❌ 使用注意事项**
- Set不能用索引访问
- 访问不存在的索引会报错
- Map的键名要注意大小写
- 空集合访问要先判断

### 8.4 学习进度检查


**🎯 掌握程度自测**
- [ ] 能区分List、Set、Map、Array的特点
- [ ] 掌握各种集合的EL访问语法  
- [ ] 理解empty运算符的使用场景
- [ ] 能为集合遍历做好数据准备

**🔗 知识关联**
- **前置知识**: [EL基本语法](#EL基本语法) | [EL运算符](#EL运算符)
- **后续学习**: [JSTL核心标签](#JSTL核心标签) | [页面数据遍历](#页面数据遍历)

**🧠 记忆口诀**
- List有序可索引，Set去重无下标
- Map键值成对现，Array固定有长度
- Empty判断很重要，Size获取常用到

**🚀 实战建议**
在实际项目中，集合数据的EL操作是非常常见的，建议多练习不同类型集合的访问方法，熟练掌握后能大大提高页面开发效率。