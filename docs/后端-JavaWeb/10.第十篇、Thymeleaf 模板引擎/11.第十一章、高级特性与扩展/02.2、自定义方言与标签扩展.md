---
title: 2、自定义方言与标签扩展
---
## 📚 目录

1. [方言扩展基础概念](#1-方言扩展基础概念)
2. [自定义属性处理器](#2-自定义属性处理器)
3. [自定义元素处理器](#3-自定义元素处理器)
4. [方言注册与配置](#4-方言注册与配置)
5. [优先级与执行顺序](#5-优先级与执行顺序)
6. [标签库开发实战](#6-标签库开发实战)
7. [扩展语法设计](#7-扩展语法设计)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 方言扩展基础概念


### 1.1 什么是Thymeleaf方言


**通俗理解：方言就是Thymeleaf的"插件系统"**

想象一下学习语言：
```
标准普通话 = Thymeleaf标准方言（th:text、th:if等）
地方方言   = 自定义方言（自己创造的标签）

就像你可以在普通话基础上创造自己的词汇，
Thymeleaf也允许你创造自己的标签！
```

**📌 核心概念解释**

**Dialect（方言）**是什么：
- 一套自定义的标签和属性规则
- 可以扩展Thymeleaf的功能
- 就像给HTML加上自己的"超能力"

**为什么需要自定义方言：**
```
场景举例：
❌ 重复代码：每个页面都写一长串格式化日期的代码
✅ 自定义标签：<my:formatDate value="${date}" pattern="yyyy-MM-dd"/>

❌ 复杂逻辑：模板里写很多判断和处理
✅ 自定义属性：<div my:permission="admin">管理员内容</div>
```

### 1.2 方言的组成部分


**🔸 方言包含的核心组件**

| 组件类型 | **作用** | **类比理解** |
|---------|---------|-------------|
| **属性处理器** | `处理自定义属性` | `像th:text一样的功能` |
| **元素处理器** | `处理自定义标签` | `创造全新的HTML标签` |
| **表达式对象** | `提供工具方法` | `像#strings一样的工具` |
| **前后置处理器** | `模板预处理` | `在渲染前后做额外操作` |

**简单示意图：**
```
Thymeleaf引擎
    ↓
标准方言（内置）
    ├── th:text
    ├── th:if  
    └── th:each
    
自定义方言（你创造的）
    ├── my:format
    ├── my:permission
    └── my:component
```

### 1.3 方言的工作流程


**🔄 从HTML到最终页面的过程**

```
用户请求页面
    ↓
Thymeleaf读取模板
    ↓
扫描所有方言（标准+自定义）
    ↓
找到匹配的处理器
    ↓
执行处理器逻辑
    ↓
生成最终HTML
    ↓
返回给用户
```

**💡 实际例子理解：**

模板代码：
```html
<div my:highlight="important">这是重要内容</div>
```

处理过程：
1. Thymeleaf看到`my:highlight`属性
2. 查找名为"my"的方言
3. 找到对应的属性处理器
4. 执行处理逻辑（比如加红色背景）
5. 输出最终HTML：`<div class="highlight-red">这是重要内容</div>`

---

## 2. 🔧 自定义属性处理器


### 2.1 属性处理器基本概念


**什么是属性处理器：**
- 用来处理HTML标签上的自定义属性
- 类似于Thymeleaf自带的`th:text`、`th:if`
- 可以读取属性值，修改标签内容或结构

**🎯 应用场景举例：**
```
场景1：权限控制
<button my:role="admin">删除</button>
→ 只有管理员能看到这个按钮

场景2：数据格式化  
<span my:money="${price}">100</span>
→ 自动格式化为：¥100.00

场景3：样式增强
<div my:theme="dark">内容</div>
→ 自动添加深色主题样式
```

### 2.2 创建属性处理器


**📝 开发步骤详解：**

**步骤1：创建处理器类**

```java
// 继承AbstractAttributeTagProcessor是关键
public class HighlightAttributeProcessor 
    extends AbstractAttributeTagProcessor {
    
    // 设置方言前缀（my）和属性名（highlight）
    public HighlightAttributeProcessor(String dialectPrefix) {
        super(
            TemplateMode.HTML,      // 处理HTML模式
            dialectPrefix,          // 方言前缀：my
            null,                   // 不限制标签名
            false,                  // 不是标签前缀
            "highlight",            // 属性名
            true,                   // 移除原属性
            1000,                   // 优先级
            true                    // 移除元素前缀
        );
    }
}
```

**📌 参数详解：**
- `TemplateMode.HTML`：处理HTML模板（还有XML、TEXT等）
- `dialectPrefix`：方言前缀，比如"my"
- `"highlight"`：属性名，组合起来就是`my:highlight`
- `优先级1000`：数字越小越先执行

**步骤2：实现处理逻辑**

```java
@Override
protected void doProcess(
    ITemplateContext context,           // 模板上下文
    IProcessableElementTag tag,         // 当前标签
    AttributeName attributeName,        // 属性名
    String attributeValue,              // 属性值
    IElementTagStructureHandler handler // 处理器
) {
    
    // 1. 获取属性值（用户写的值）
    String level = attributeValue;
    
    // 2. 根据不同值做不同处理
    String cssClass = "";
    if ("important".equals(level)) {
        cssClass = "bg-red";
    } else if ("warning".equals(level)) {
        cssClass = "bg-yellow";
    }
    
    // 3. 修改标签：添加class属性
    handler.setAttribute("class", cssClass);
}
```

**🔍 代码理解：**
```
模板输入：<div my:highlight="important">文本</div>
          ↓
处理器读取：attributeValue = "important"
          ↓  
判断逻辑：cssClass = "bg-red"
          ↓
输出结果：<div class="bg-red">文本</div>
```

### 2.3 属性处理器实战案例


**案例1：权限控制属性**

```java
public class PermissionAttributeProcessor 
    extends AbstractAttributeTagProcessor {
    
    @Autowired
    private UserService userService;
    
    public PermissionAttributeProcessor(String prefix) {
        super(TemplateMode.HTML, prefix, null, false, 
              "permission", true, 1000, true);
    }
    
    @Override
    protected void doProcess(...) {
        // 获取需要的权限
        String requiredRole = attributeValue;
        
        // 获取当前用户权限
        String userRole = userService.getCurrentUserRole();
        
        // 判断权限
        if (!userRole.equals(requiredRole)) {
            // 没权限：删除整个元素
            handler.removeElement();
        }
        // 有权限：保留元素，移除my:permission属性
    }
}
```

使用示例：
```html
<!-- 只有管理员能看到 -->
<button my:permission="admin">删除用户</button>

<!-- 所有人都能看到 -->
<button>查看详情</button>
```

**案例2：金额格式化属性**

```java
public class MoneyAttributeProcessor 
    extends AbstractAttributeTagProcessor {
    
    public MoneyAttributeProcessor(String prefix) {
        super(TemplateMode.HTML, prefix, null, false, 
              "money", true, 1000, true);
    }
    
    @Override
    protected void doProcess(...) {
        // 解析表达式获取金额值
        Object value = context.getExpressionObjects()
            .evaluate(attributeValue, context);
        
        // 格式化为货币格式
        String formatted = String.format("¥%.2f", 
            Double.parseDouble(value.toString()));
        
        // 设置为元素内容
        handler.setBody(formatted, false);
    }
}
```

使用示例：
```html
<!-- 输入 -->
<span my:money="${product.price}"></span>

<!-- 如果price=99.5，输出 -->
<span>¥99.50</span>
```

---

## 3. 🏷️ 自定义元素处理器


### 3.1 元素处理器基本概念


**什么是元素处理器：**
- 处理完整的自定义标签（不只是属性）
- 可以创造全新的HTML元素
- 像创造自己的"Vue组件"或"React组件"

**🎯 元素处理器 vs 属性处理器对比：**

| 对比项 | **属性处理器** | **元素处理器** |
|-------|--------------|--------------|
| **处理对象** | `标签上的属性` | `整个自定义标签` |
| **使用方式** | `<div my:attr="value">` | `<my:tag>内容</my:tag>` |
| **典型场景** | `修改现有标签` | `创造新标签` |

**示例对比：**
```html
<!-- 属性处理器：修改现有div -->
<div my:highlight="red">内容</div>

<!-- 元素处理器：创造新标签 -->
<my:alert type="warning">警告信息</my:alert>
```

### 3.2 创建元素处理器


**📝 开发步骤：**

**步骤1：创建处理器类**

```java
// 继承AbstractElementTagProcessor
public class AlertElementProcessor 
    extends AbstractElementTagProcessor {
    
    public AlertElementProcessor(String dialectPrefix) {
        super(
            TemplateMode.HTML,    // HTML模式
            dialectPrefix,        // 前缀：my
            "alert",              // 元素名：alert
            true,                 // 有前缀
            null,                 // 不限制属性
            false,                // 不是属性前缀
            1000                  // 优先级
        );
    }
}
```

**步骤2：实现处理逻辑**

```java
@Override
protected void doProcess(
    ITemplateContext context,
    IProcessableElementTag tag,
    IElementTagStructureHandler handler
) {
    
    // 1. 获取标签属性
    String type = tag.getAttributeValue("type"); // warning/error/info
    
    // 2. 获取标签内容
    String body = tag.getElementContent();
    
    // 3. 构建新的HTML结构
    String iconClass = getIconClass(type);
    String colorClass = getColorClass(type);
    
    String html = String.format(
        "<div class='alert %s'>" +
        "  <i class='%s'></i>" +
        "  <span>%s</span>" +
        "</div>",
        colorClass, iconClass, body
    );
    
    // 4. 替换整个元素
    handler.replaceWith(html, false);
}

private String getIconClass(String type) {
    switch(type) {
        case "warning": return "icon-warning";
        case "error": return "icon-error";
        default: return "icon-info";
    }
}
```

**🔍 处理流程图示：**
```
输入模板：
<my:alert type="warning">操作失败</my:alert>
    ↓
元素处理器识别
    ↓
读取type="warning"
    ↓
读取内容"操作失败"
    ↓
生成新HTML
    ↓
输出结果：
<div class='alert alert-warning'>
  <i class='icon-warning'></i>
  <span>操作失败</span>
</div>
```

### 3.3 元素处理器实战案例


**案例1：面包屑导航组件**

```java
public class BreadcrumbElementProcessor 
    extends AbstractElementTagProcessor {
    
    public BreadcrumbElementProcessor(String prefix) {
        super(TemplateMode.HTML, prefix, "breadcrumb", 
              true, null, false, 1000);
    }
    
    @Override
    protected void doProcess(...) {
        // 从标签获取路径数据
        String pathsAttr = tag.getAttributeValue("paths");
        
        // 解析为路径列表
        List<String> paths = Arrays.asList(
            pathsAttr.split(",")
        );
        
        // 构建面包屑HTML
        StringBuilder html = new StringBuilder(
            "<nav class='breadcrumb'><ul>"
        );
        
        for (int i = 0; i < paths.size(); i++) {
            String path = paths.get(i);
            if (i == paths.size() - 1) {
                // 最后一项：当前页，不加链接
                html.append("<li class='active'>")
                    .append(path)
                    .append("</li>");
            } else {
                // 其他项：加链接
                html.append("<li>")
                    .append("<a href='#'>")
                    .append(path)
                    .append("</a></li>");
            }
        }
        
        html.append("</ul></nav>");
        handler.replaceWith(html.toString(), false);
    }
}
```

使用示例：
```html
<!-- 模板中使用 -->
<my:breadcrumb paths="首页,产品列表,产品详情"/>

<!-- 渲染结果 -->
<nav class='breadcrumb'>
  <ul>
    <li><a href='#'>首页</a></li>
    <li><a href='#'>产品列表</a></li>
    <li class='active'>产品详情</li>
  </ul>
</nav>
```

**案例2：卡片组件**

```java
public class CardElementProcessor 
    extends AbstractElementTagProcessor {
    
    public CardElementProcessor(String prefix) {
        super(TemplateMode.HTML, prefix, "card", 
              true, null, false, 1000);
    }
    
    @Override
    protected void doProcess(...) {
        String title = tag.getAttributeValue("title");
        String footer = tag.getAttributeValue("footer");
        String content = tag.getElementContent();
        
        StringBuilder html = new StringBuilder();
        html.append("<div class='card'>");
        
        // 卡片头部
        if (title != null) {
            html.append("<div class='card-header'>")
                .append(title)
                .append("</div>");
        }
        
        // 卡片内容
        html.append("<div class='card-body'>")
            .append(content)
            .append("</div>");
        
        // 卡片底部
        if (footer != null) {
            html.append("<div class='card-footer'>")
                .append(footer)
                .append("</div>");
        }
        
        html.append("</div>");
        handler.replaceWith(html.toString(), false);
    }
}
```

使用示例：
```html
<my:card title="用户信息" footer="更新时间：2024-01-01">
    <p>姓名：张三</p>
    <p>邮箱：zhangsan@example.com</p>
</my:card>
```

---

## 4. ⚙️ 方言注册与配置


### 4.1 创建方言类


**方言类的作用：把处理器组装在一起**

```java
// 实现IDialect接口
public class MyCustomDialect implements IDialect {
    
    private static final String PREFIX = "my";
    
    // 1. 返回方言名称（唯一标识）
    @Override
    public String getName() {
        return "My Custom Dialect";
    }
    
    // 2. 返回处理器集合
    @Override
    public Set<IProcessor> getProcessors(String dialectPrefix) {
        Set<IProcessor> processors = new HashSet<>();
        
        // 添加属性处理器
        processors.add(new HighlightAttributeProcessor(PREFIX));
        processors.add(new PermissionAttributeProcessor(PREFIX));
        processors.add(new MoneyAttributeProcessor(PREFIX));
        
        // 添加元素处理器
        processors.add(new AlertElementProcessor(PREFIX));
        processors.add(new BreadcrumbElementProcessor(PREFIX));
        processors.add(new CardElementProcessor(PREFIX));
        
        return processors;
    }
}
```

**🔍 理解方言类：**
```
方言类 = 一个容器
    ├── 属性处理器1（my:highlight）
    ├── 属性处理器2（my:permission）
    ├── 属性处理器3（my:money）
    ├── 元素处理器1（my:alert）
    ├── 元素处理器2（my:breadcrumb）
    └── 元素处理器3（my:card）
```

### 4.2 注册方言到Spring


**方式1：配置类注册（推荐）**

```java
@Configuration
public class ThymeleafConfig {
    
    @Bean
    public MyCustomDialect myCustomDialect() {
        return new MyCustomDialect();
    }
    
    // Spring Boot会自动将这个Bean注册到Thymeleaf
}
```

**方式2：手动配置模板引擎**

```java
@Configuration
public class ThymeleafConfig {
    
    @Bean
    public SpringTemplateEngine templateEngine() {
        SpringTemplateEngine engine = new SpringTemplateEngine();
        
        // 添加标准方言
        engine.addDialect(new StandardDialect());
        
        // 添加自定义方言
        engine.addDialect(new MyCustomDialect());
        
        return engine;
    }
}
```

**📌 注册流程图：**
```
创建方言类
    ↓
注册为Spring Bean
    ↓
Spring Boot自动扫描
    ↓
添加到Thymeleaf引擎
    ↓
在模板中可用
```

### 4.3 配置方言前缀


**默认前缀配置：**

```java
public class MyCustomDialect implements IDialect {
    
    private String prefix = "my";  // 默认前缀
    
    // 允许自定义前缀
    public MyCustomDialect(String prefix) {
        this.prefix = prefix;
    }
    
    @Override
    public String getPrefix() {
        return prefix;
    }
}
```

**application.yml配置：**

```yaml
# 也可以通过配置文件指定
spring:
  thymeleaf:
    dialect:
      prefix: custom  # 改为custom前缀
```

使用效果：
```html
<!-- 默认前缀my -->
<div my:highlight="red">内容</div>

<!-- 自定义前缀custom -->
<div custom:highlight="red">内容</div>
```

---

## 5. 🎚️ 优先级与执行顺序


### 5.1 优先级概念理解


**什么是优先级：**
- 决定多个处理器的执行顺序
- 数字越小，优先级越高，越先执行
- 类似于CSS的z-index

**🔍 为什么需要优先级：**

```html
<div th:if="${show}" my:highlight="red">内容</div>
```

这个div上有两个属性：
- `th:if`：决定是否显示
- `my:highlight`：添加样式

问题来了：**先判断显示，还是先加样式？**

答案：**应该先判断th:if**
- 如果不显示，就不需要加样式了
- 这就是为什么th:if的优先级要高

### 5.2 Thymeleaf标准优先级


**📊 标准方言优先级表：**

| 优先级 | **属性** | **作用** | **说明** |
|-------|---------|---------|---------|
| 100 | `th:include/th:insert` | `片段引入` | `最先执行` |
| 200 | `th:replace` | `片段替换` | `` |
| 300 | `th:each` | `循环` | `` |
| 400 | `th:if/th:unless` | `条件判断` | `` |
| 500 | `th:switch/th:case` | `分支` | `` |
| 600 | `th:object/th:with` | `变量定义` | `` |
| 700 | `th:attr/th:attrappend` | `属性修改` | `` |
| 800 | `th:value/th:href` | `属性值设置` | `` |
| 900 | `th:text/th:utext` | `文本内容` | `` |
| 1000 | `其他` | `默认优先级` | `最后执行` |

**💡 执行顺序示例：**

```html
<li th:each="item : ${items}"           <!-- 优先级300 -->
    th:if="${item.active}"              <!-- 优先级400 -->
    th:text="${item.name}"              <!-- 优先级900 -->
    my:highlight="red">                 <!-- 优先级1000 -->
</li>
```

执行顺序：
```
1. th:each     → 循环每个item
2. th:if       → 判断item是否active
3. th:text     → 设置文本为item.name
4. my:highlight → 添加红色样式
```

### 5.3 自定义优先级设置


**在处理器中设置优先级：**

```java
public class MyProcessor extends AbstractAttributeTagProcessor {
    
    public MyProcessor(String prefix) {
        super(
            TemplateMode.HTML,
            prefix,
            null,
            false,
            "myattr",
            true,
            500,        // ← 这里设置优先级：500
            true
        );
    }
}
```

**🎯 优先级设置策略：**

| 场景 | **建议优先级** | **原因** |
|-----|--------------|---------|
| **条件控制** | `300-500` | `需要早执行，决定元素显示` |
| **数据处理** | `600-800` | `在显示前处理数据` |
| **样式修改** | `900-1100` | `最后修改外观` |
| **内容替换** | `800-900` | `在样式前设置内容` |

**实战案例：权限控制优先级**

```java
// 权限检查应该在内容渲染之前
public class PermissionProcessor 
    extends AbstractAttributeTagProcessor {
    
    public PermissionProcessor(String prefix) {
        super(
            TemplateMode.HTML, prefix, null, false,
            "permission", true,
            300,  // 优先级设为300，比th:text更早
            true
        );
    }
}
```

原因：
```
如果没权限，直接删除元素
就不需要执行后面的th:text了
节省性能，逻辑也更清晰
```

---

## 6. 📦 标签库开发实战


### 6.1 标签库设计思路


**什么是标签库：**
- 一组相关的自定义标签集合
- 解决特定领域的问题
- 可以复用到多个项目

**🎯 常见标签库类型：**

```
UI组件库：
├── <ui:button>      按钮
├── <ui:modal>       模态框
├── <ui:table>       表格
└── <ui:form>        表单

工具标签库：
├── <util:format>    格式化
├── <util:permission> 权限
├── <util:cache>     缓存
└── <util:log>       日志

业务标签库：
├── <biz:user-card>  用户卡片
├── <biz:product>    商品展示
└── <biz:order>      订单信息
```

### 6.2 开发表单标签库


**需求分析：**
- 简化表单开发
- 统一表单样式
- 自动处理验证

**标签设计：**

| 标签 | **用途** | **示例** |
|-----|---------|---------|
| `<form:input>` | `输入框` | `<form:input name="username" label="用户名"/>` |
| `<form:select>` | `下拉框` | `<form:select name="city" options="${cities}"/>` |
| `<form:checkbox>` | `复选框` | `<form:checkbox name="agree" label="同意协议"/>` |
| `<form:submit>` | `提交按钮` | `<form:submit text="提交" theme="primary"/>` |

**实现表单输入框：**

```java
public class FormInputProcessor 
    extends AbstractElementTagProcessor {
    
    public FormInputProcessor(String prefix) {
        super(TemplateMode.HTML, prefix, "input", 
              true, null, false, 1000);
    }
    
    @Override
    protected void doProcess(...) {
        // 读取属性
        String name = tag.getAttributeValue("name");
        String label = tag.getAttributeValue("label");
        String type = tag.getAttributeValue("type", "text");
        String placeholder = tag.getAttributeValue("placeholder", "");
        String required = tag.getAttributeValue("required", "false");
        
        // 构建HTML
        StringBuilder html = new StringBuilder();
        html.append("<div class='form-group'>");
        
        // 标签
        if (label != null) {
            html.append("<label>").append(label);
            if ("true".equals(required)) {
                html.append("<span class='required'>*</span>");
            }
            html.append("</label>");
        }
        
        // 输入框
        html.append("<input type='").append(type).append("'")
            .append(" name='").append(name).append("'")
            .append(" class='form-control'")
            .append(" placeholder='").append(placeholder).append("'");
        
        if ("true".equals(required)) {
            html.append(" required");
        }
        
        html.append(">");
        html.append("</div>");
        
        handler.replaceWith(html.toString(), false);
    }
}
```

使用示例：
```html
<form:input 
    name="username" 
    label="用户名" 
    placeholder="请输入用户名"
    required="true"/>
```

生成结果：
```html
<div class='form-group'>
    <label>用户名<span class='required'>*</span></label>
    <input type='text' name='username' class='form-control' 
           placeholder='请输入用户名' required>
</div>
```

### 6.3 开发UI组件库


**案例：模态框组件**

```java
public class ModalElementProcessor 
    extends AbstractElementTagProcessor {
    
    public ModalElementProcessor(String prefix) {
        super(TemplateMode.HTML, prefix, "modal", 
              true, null, false, 1000);
    }
    
    @Override
    protected void doProcess(...) {
        String id = tag.getAttributeValue("id");
        String title = tag.getAttributeValue("title");
        String size = tag.getAttributeValue("size", "medium");
        String content = tag.getElementContent();
        
        String html = String.format(
            "<div id='%s' class='modal modal-%s'>" +
            "  <div class='modal-dialog'>" +
            "    <div class='modal-header'>" +
            "      <h3>%s</h3>" +
            "      <button class='close'>×</button>" +
            "    </div>" +
            "    <div class='modal-body'>%s</div>" +
            "  </div>" +
            "</div>",
            id, size, title, content
        );
        
        handler.replaceWith(html, false);
    }
}
```

使用：
```html
<ui:modal id="confirmModal" title="确认操作" size="small">
    <p>确定要删除这条记录吗？</p>
    <button onclick="deleteRecord()">确定</button>
</ui:modal>
```

---

## 7. 🎨 扩展语法设计


### 7.1 表达式对象扩展


**什么是表达式对象：**
- 类似`${#strings}`、`${#dates}`的工具类
- 在模板中直接调用
- 提供常用功能方法

**创建自定义表达式对象：**

```java
// 1. 创建工具类
public class MyUtils {
    
    public String formatMoney(Double amount) {
        return String.format("¥%.2f", amount);
    }
    
    public String hidePhone(String phone) {
        if (phone == null || phone.length() < 11) {
            return phone;
        }
        return phone.substring(0, 3) + "****" + 
               phone.substring(7);
    }
    
    public String avatar(String name) {
        // 根据名字生成头像URL
        return "/avatar/" + name.hashCode() + ".png";
    }
}
```

```java
// 2. 创建表达式对象工厂
public class MyUtilsDialect extends AbstractDialect 
    implements IExpressionObjectDialect {
    
    public MyUtilsDialect() {
        super("My Utils");
    }
    
    @Override
    public IExpressionObjectFactory getExpressionObjectFactory() {
        return new IExpressionObjectFactory() {
            @Override
            public Set<String> getAllExpressionObjectNames() {
                return Collections.singleton("myutil");
            }
            
            @Override
            public Object buildObject(IExpressionContext context, 
                                     String name) {
                if ("myutil".equals(name)) {
                    return new MyUtils();
                }
                return null;
            }
        };
    }
}
```

**模板中使用：**

```html
<!-- 格式化金额 -->
<span>${#myutil.formatMoney(product.price)}</span>

<!-- 隐藏手机号 -->
<span>${#myutil.hidePhone(user.phone)}</span>

<!-- 生成头像 -->
<img th:src="${#myutil.avatar(user.name)}"/>
```

### 7.2 前后置处理器


**什么时候用前后置处理器：**
- 在模板渲染前做统一处理
- 在渲染后做统一清理
- 类似于Spring的拦截器

**创建预处理器：**

```java
public class SecurityPreProcessor 
    implements IPreProcessor {
    
    @Override
    public void preProcess(ITemplateContext context, 
                          IModel model) {
        // 在渲染前添加安全Token
        String token = generateSecurityToken();
        
        // 在model中添加隐藏字段
        IModelFactory factory = context.getModelFactory();
        IStandaloneElementTag input = factory.createStandaloneElementTag(
            "input",
            "type", "hidden",
            "name", "securityToken",
            "value", token
        );
        
        model.insert(0, input);
    }
}
```

结果：每个表单都会自动添加安全Token

---

## 8. 📋 核心要点总结


### 8.1 关键概念回顾


```
🎯 方言（Dialect）= Thymeleaf的插件系统
   ├── 属性处理器：处理自定义属性（my:xxx）
   ├── 元素处理器：处理自定义标签（<my:xxx>）
   ├── 表达式对象：提供工具方法（${#myutil}）
   └── 前后置处理器：全局预处理
```

### 8.2 开发步骤总结


**📝 完整开发流程：**

```
步骤1：创建处理器
    ↓
步骤2：实现处理逻辑
    ↓
步骤3：创建方言类
    ↓
步骤4：注册到Spring
    ↓
步骤5：模板中使用
```

### 8.3 优先级记忆要点


**🎚️ 优先级原则：**
```
数字越小 → 越先执行
100-200  → 片段操作
300-500  → 条件循环
600-800  → 数据处理
900-1000 → 内容样式
```

### 8.4 实战建议


**✅ 最佳实践：**

1. **命名规范**
   - 方言前缀简短：`ui`、`my`、`util`
   - 属性名语义化：`permission`、`format`
   
2. **优先级设置**
   - 条件判断：300-500
   - 数据处理：600-800
   - 样式修改：900-1100

3. **代码组织**
   - 相关处理器放在一个方言
   - 不同领域用不同方言
   - 保持处理器职责单一

4. **性能优化**
   - 避免复杂逻辑
   - 合理使用缓存
   - 优先级设置合理

**🔧 常见应用场景：**

| 场景 | **推荐方案** | **示例** |
|-----|------------|---------|
| **权限控制** | `属性处理器` | `<div my:role="admin">` |
| **数据格式化** | `表达式对象` | `${#myutil.format()}` |
| **UI组件** | `元素处理器` | `<ui:modal>` |
| **表单生成** | `标签库` | `<form:input>` |

### 8.5 学习检查清单


**📋 掌握程度自测：**

- [ ] 理解方言的概念和作用
- [ ] 能创建属性处理器
- [ ] 能创建元素处理器  
- [ ] 会注册方言到Spring
- [ ] 理解优先级机制
- [ ] 能设计标签库
- [ ] 会创建表达式对象

**🎓 进阶学习方向：**
- 研究Spring Security集成
- 开发完整UI组件库
- 学习模板缓存优化
- 探索国际化扩展