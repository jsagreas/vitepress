---
title: 3、多语言网站实战开发
---
## 📚 目录

1. [多语言菜单实战](#1-多语言菜单实战)
2. [多语言表单开发](#2-多语言表单开发)
3. [多语言错误信息处理](#3-多语言错误信息处理)
4. [日期本地化显示](#4-日期本地化显示)
5. [数字本地化处理](#5-数字本地化处理)
6. [货币本地化展示](#6-货币本地化展示)
7. [文化适配处理](#7-文化适配处理)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🍔 多语言菜单实战


### 1.1 什么是多语言菜单


**🎯 通俗理解**
```
就像麦当劳的菜单：
- 中国：汉堡、薯条、可乐
- 美国：Burger、Fries、Coke  
- 日本：ハンバーガー、フライドポテト、コーラ

同样的内容，不同语言显示
网站菜单也是一样的道理！
```

**💡 核心作用**：让不同国家的用户看到熟悉的语言导航

### 1.2 导航菜单国际化配置


**📋 准备资源文件**

首先在 `src/main/resources` 创建配置文件：

```
resources/
  └── i18n/
      ├── messages_zh_CN.properties  # 中文菜单
      ├── messages_en_US.properties  # 英文菜单
      └── messages_ja_JP.properties  # 日文菜单
```

**🔧 配置文件内容**

`messages_zh_CN.properties`（中文）：
```properties
# 导航菜单
menu.home=首页
menu.products=产品中心
menu.about=关于我们
menu.contact=联系我们
menu.login=登录
menu.register=注册
```

`messages_en_US.properties`（英文）：
```properties
# Navigation Menu
menu.home=Home
menu.products=Products
menu.about=About Us
menu.contact=Contact
menu.login=Login
menu.register=Register
```

`messages_ja_JP.properties`（日文）：
```properties
# ナビゲーションメニュー
menu.home=ホーム
menu.products=製品
menu.about=会社概要
menu.contact=お問い合わせ
menu.login=ログイン
menu.register=登録
```

### 1.3 页面菜单实现


**📝 Thymeleaf模板代码**

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title th:text="#{menu.home}">首页</title>
</head>
<body>
    <!-- 导航菜单 -->
    <nav class="navbar">
        <ul>
            <li><a href="/" th:text="#{menu.home}">首页</a></li>
            <li><a href="/products" th:text="#{menu.products}">产品</a></li>
            <li><a href="/about" th:text="#{menu.about}">关于</a></li>
            <li><a href="/contact" th:text="#{menu.contact}">联系</a></li>
        </ul>
        
        <!-- 用户区域 -->
        <div class="user-area">
            <a href="/login" th:text="#{menu.login}">登录</a>
            <a href="/register" th:text="#{menu.register}">注册</a>
        </div>
        
        <!-- 语言切换 -->
        <div class="language-switch">
            <a href="?lang=zh_CN">中文</a>
            <a href="?lang=en_US">English</a>
            <a href="?lang=ja_JP">日本語</a>
        </div>
    </nav>
</body>
</html>
```

**🔑 关键理解**：
- `#{menu.home}` - 这个语法会自动根据用户语言找对应的文本
- 用户点击语言切换时，整个菜单会立即变成对应语言
- 不需要写if-else判断，Thymeleaf自动处理

### 1.4 下拉菜单国际化


**📂 二级菜单配置**

`messages_zh_CN.properties`：
```properties
# 产品分类
menu.products=产品中心
menu.products.phone=手机
menu.products.laptop=笔记本
menu.products.tablet=平板电脑
menu.products.accessories=配件
```

**📝 实现代码**

```html
<nav>
    <ul>
        <li class="dropdown">
            <a href="#" th:text="#{menu.products}">产品</a>
            <!-- 下拉菜单 -->
            <ul class="dropdown-menu">
                <li><a href="/phone" th:text="#{menu.products.phone}">手机</a></li>
                <li><a href="/laptop" th:text="#{menu.products.laptop}">笔记本</a></li>
                <li><a href="/tablet" th:text="#{menu.products.tablet}">平板</a></li>
                <li><a href="/accessories" th:text="#{menu.products.accessories}">配件</a></li>
            </ul>
        </li>
    </ul>
</nav>
```

**💡 实际效果**：
```
中文用户看到：产品中心 → 手机、笔记本、平板电脑
英文用户看到：Products → Phone, Laptop, Tablet
日文用户看到：製品 → スマホ、ノートPC、タブレット
```

---

## 2. 📝 多语言表单开发


### 2.1 表单国际化的意义


**🎯 为什么要做表单国际化**
```
想象你要填写一个日文网站的注册表单：
❌ 看不懂"氏名"、"メールアドレス"是什么意思
✅ 如果显示"姓名"、"邮箱地址"就清楚多了

这就是表单国际化的价值！
```

### 2.2 注册表单实战


**📋 配置文件准备**

`messages_zh_CN.properties`：
```properties
# 注册表单
form.register.title=用户注册
form.register.username=用户名
form.register.email=邮箱地址
form.register.password=密码
form.register.confirmPassword=确认密码
form.register.phone=手机号码
form.register.agree=我已阅读并同意
form.register.terms=服务条款
form.register.submit=立即注册
form.register.hasAccount=已有账号？
form.register.login=去登录

# 表单占位符
form.placeholder.username=请输入用户名(6-20个字符)
form.placeholder.email=请输入邮箱地址
form.placeholder.password=请输入密码(至少8位)
form.placeholder.phone=请输入手机号码
```

`messages_en_US.properties`：
```properties
# Register Form
form.register.title=User Registration
form.register.username=Username
form.register.email=Email Address
form.register.password=Password
form.register.confirmPassword=Confirm Password
form.register.phone=Phone Number
form.register.agree=I agree to the
form.register.terms=Terms of Service
form.register.submit=Register Now
form.register.hasAccount=Already have an account?
form.register.login=Sign In

# Form Placeholders
form.placeholder.username=Enter username (6-20 characters)
form.placeholder.email=Enter email address
form.placeholder.password=Enter password (min 8 characters)
form.placeholder.phone=Enter phone number
```

### 2.3 表单页面实现


```html
<div class="register-form">
    <!-- 表单标题 -->
    <h2 th:text="#{form.register.title}">用户注册</h2>
    
    <form th:action="@{/register}" method="post">
        <!-- 用户名输入 -->
        <div class="form-group">
            <label th:text="#{form.register.username}">用户名</label>
            <input type="text" 
                   name="username"
                   th:placeholder="#{form.placeholder.username}"
                   required>
        </div>
        
        <!-- 邮箱输入 -->
        <div class="form-group">
            <label th:text="#{form.register.email}">邮箱</label>
            <input type="email" 
                   name="email"
                   th:placeholder="#{form.placeholder.email}"
                   required>
        </div>
        
        <!-- 密码输入 -->
        <div class="form-group">
            <label th:text="#{form.register.password}">密码</label>
            <input type="password" 
                   name="password"
                   th:placeholder="#{form.placeholder.password}"
                   required>
        </div>
        
        <!-- 确认密码 -->
        <div class="form-group">
            <label th:text="#{form.register.confirmPassword}">确认密码</label>
            <input type="password" name="confirmPassword" required>
        </div>
        
        <!-- 手机号 -->
        <div class="form-group">
            <label th:text="#{form.register.phone}">手机号</label>
            <input type="tel" 
                   name="phone"
                   th:placeholder="#{form.placeholder.phone}">
        </div>
        
        <!-- 协议勾选 -->
        <div class="form-group">
            <label>
                <input type="checkbox" required>
                <span th:text="#{form.register.agree}">我已阅读并同意</span>
                <a href="/terms" th:text="#{form.register.terms}">服务条款</a>
            </label>
        </div>
        
        <!-- 提交按钮 -->
        <button type="submit" th:text="#{form.register.submit}">立即注册</button>
        
        <!-- 登录链接 -->
        <p>
            <span th:text="#{form.register.hasAccount}">已有账号？</span>
            <a href="/login" th:text="#{form.register.login}">去登录</a>
        </p>
    </form>
</div>
```

**🔑 关键技巧**：
- `th:text` - 显示标签文本
- `th:placeholder` - 显示输入提示
- 两个都用 `#{}` 语法读取配置

### 2.4 动态表单选项


**📋 下拉选择框国际化**

配置文件：
```properties
# 中文
form.country=国家/地区
form.country.china=中国
form.country.usa=美国
form.country.japan=日本
form.country.uk=英国

# English
form.country=Country/Region
form.country.china=China
form.country.usa=United States
form.country.japan=Japan
form.country.uk=United Kingdom
```

实现代码：
```html
<div class="form-group">
    <label th:text="#{form.country}">国家</label>
    <select name="country">
        <option value="CN" th:text="#{form.country.china}">中国</option>
        <option value="US" th:text="#{form.country.usa}">美国</option>
        <option value="JP" th:text="#{form.country.japan}">日本</option>
        <option value="UK" th:text="#{form.country.uk}">英国</option>
    </select>
</div>
```

---

## 3. ⚠️ 多语言错误信息处理


### 3.1 为什么需要多语言错误提示


**💭 场景理解**
```
用户填写表单出错时：
❌ 英文用户看到："用户名格式不正确" → 看不懂！
✅ 英文用户看到："Invalid username format" → 明白了！

友好的错误提示 = 更好的用户体验
```

### 3.2 错误消息配置


**📋 错误信息资源文件**

`messages_zh_CN.properties`：
```properties
# 表单验证错误
error.username.required=用户名不能为空
error.username.length=用户名长度必须在6-20个字符之间
error.username.format=用户名只能包含字母、数字和下划线
error.email.required=邮箱地址不能为空
error.email.invalid=邮箱地址格式不正确
error.password.required=密码不能为空
error.password.length=密码长度至少8位
error.password.weak=密码强度太弱，请包含大小写字母和数字
error.password.mismatch=两次输入的密码不一致
error.phone.invalid=手机号码格式不正确

# 业务错误
error.username.exists=该用户名已被注册
error.email.exists=该邮箱已被使用
error.login.failed=用户名或密码错误
error.permission.denied=您没有访问权限
error.network.error=网络连接失败，请稍后重试
```

`messages_en_US.properties`：
```properties
# Form Validation Errors
error.username.required=Username is required
error.username.length=Username must be 6-20 characters
error.username.format=Username can only contain letters, numbers and underscores
error.email.required=Email is required
error.email.invalid=Invalid email format
error.password.required=Password is required
error.password.length=Password must be at least 8 characters
error.password.weak=Password is too weak, include uppercase, lowercase and numbers
error.password.mismatch=Passwords do not match
error.phone.invalid=Invalid phone number format

# Business Errors
error.username.exists=Username already taken
error.email.exists=Email already in use
error.login.failed=Invalid username or password
error.permission.denied=Access denied
error.network.error=Network error, please try again later
```

### 3.3 后端验证与错误返回


**🔧 Controller层处理**

```java
@Controller
public class RegisterController {
    
    @PostMapping("/register")
    public String register(@Valid UserRegisterDTO dto, 
                          BindingResult result,
                          Model model) {
        
        // 1. 检查表单验证错误
        if (result.hasErrors()) {
            // 错误会自动根据用户语言显示
            return "register";
        }
        
        // 2. 检查用户名是否已存在
        if (userService.usernameExists(dto.getUsername())) {
            model.addAttribute("usernameError", "error.username.exists");
            return "register";
        }
        
        // 3. 检查邮箱是否已使用
        if (userService.emailExists(dto.getEmail())) {
            model.addAttribute("emailError", "error.email.exists");
            return "register";
        }
        
        // 4. 注册成功
        userService.register(dto);
        return "redirect:/login?registerSuccess";
    }
}
```

### 3.4 页面错误显示


```html
<form th:action="@{/register}" method="post">
    <!-- 用户名输入框 -->
    <div class="form-group">
        <label th:text="#{form.register.username}">用户名</label>
        <input type="text" 
               name="username" 
               th:value="${dto?.username}"
               th:class="${#fields.hasErrors('username')} ? 'error' : ''">
        
        <!-- 显示验证错误 -->
        <span class="error-message" 
              th:if="${#fields.hasErrors('username')}"
              th:errors="*{username}"
              th:text="#{${#fields.errors('username')[0]}}">
        </span>
        
        <!-- 显示业务错误 -->
        <span class="error-message"
              th:if="${usernameError}"
              th:text="#{${usernameError}}">
        </span>
    </div>
    
    <!-- 邮箱输入框 -->
    <div class="form-group">
        <label th:text="#{form.register.email}">邮箱</label>
        <input type="email" 
               name="email"
               th:value="${dto?.email}"
               th:class="${#fields.hasErrors('email')} ? 'error' : ''">
        
        <span class="error-message"
              th:if="${#fields.hasErrors('email')}"
              th:text="#{${#fields.errors('email')[0]}}">
        </span>
        
        <span class="error-message"
              th:if="${emailError}"
              th:text="#{${emailError}}">
        </span>
    </div>
</form>
```

**🔑 工作原理**：
```
1. 用户提交表单
2. 后端验证发现错误
3. 返回错误key：error.username.required
4. Thymeleaf根据用户语言找对应翻译
5. 显示：中文用户看"用户名不能为空"
          英文用户看"Username is required"
```

### 3.5 全局错误页面


**📝 404错误页面**

`messages_zh_CN.properties`：
```properties
error.404.title=页面未找到
error.404.message=抱歉，您访问的页面不存在
error.404.suggestion=您可以尝试：
error.404.home=返回首页
error.404.back=返回上一页
```

`404.html`：
```html
<div class="error-page">
    <h1>404</h1>
    <h2 th:text="#{error.404.title}">页面未找到</h2>
    <p th:text="#{error.404.message}">抱歉，您访问的页面不存在</p>
    
    <div class="suggestions">
        <p th:text="#{error.404.suggestion}">您可以尝试：</p>
        <a th:href="@{/}" th:text="#{error.404.home}">返回首页</a>
        <a href="javascript:history.back()" th:text="#{error.404.back}">返回上一页</a>
    </div>
</div>
```

---

## 4. 📅 日期本地化显示


### 4.1 日期本地化的重要性


**🌍 不同国家的日期格式**
```
同一个日期：2024年3月15日

中国格式：2024-03-15  或  2024年3月15日
美国格式：03/15/2024  或  March 15, 2024
英国格式：15/03/2024  或  15 March 2024
日本格式：2024/03/15  或  2024年3月15日

用户习惯的格式 = 更好的阅读体验
```

### 4.2 配置日期格式


**📋 资源文件配置**

`messages_zh_CN.properties`：
```properties
# 日期格式
date.format.short=yyyy-MM-dd
date.format.medium=yyyy年MM月dd日
date.format.long=yyyy年MM月dd日 HH:mm
date.format.full=yyyy年MM月dd日 HH:mm:ss

# 日期相关文本
date.today=今天
date.yesterday=昨天
date.tomorrow=明天
date.published=发布于
date.updated=更新于
date.created=创建于
```

`messages_en_US.properties`：
```properties
# Date Formats
date.format.short=MM/dd/yyyy
date.format.medium=MMM dd, yyyy
date.format.long=MMM dd, yyyy HH:mm
date.format.full=MMMM dd, yyyy HH:mm:ss

# Date Related Text
date.today=Today
date.yesterday=Yesterday
date.tomorrow=Tomorrow
date.published=Published on
date.updated=Updated on
date.created=Created on
```

### 4.3 使用Thymeleaf格式化日期


**📝 基础日期显示**

```html
<!-- Java对象 -->
<!-- Article对象有 publishDate 属性(Date类型) -->

<!-- 方式1：使用#dates工具 -->
<p>
    <span th:text="#{date.published}">发布于</span>
    <span th:text="${#dates.format(article.publishDate, 'yyyy-MM-dd')}"></span>
</p>

<!-- 方式2：使用配置的格式 -->
<p>
    <span th:text="#{date.published}">发布于</span>
    <span th:text="${#dates.format(article.publishDate, #{date.format.medium})}"></span>
</p>

<!-- 方式3：完整日期时间 -->
<p>
    <span th:text="#{date.created}">创建于</span>
    <span th:text="${#dates.format(article.createTime, #{date.format.full})}"></span>
</p>
```

**💡 实际显示效果**：
```
中文用户看到：
发布于 2024年03月15日
创建于 2024年03月15日 14:30:25

英文用户看到：
Published on Mar 15, 2024
Created on March 15, 2024 14:30:25
```

### 4.4 相对时间显示


**🕐 人性化的时间显示**

后端处理：
```java
@Service
public class DateService {
    
    public String getRelativeTime(Date date, Locale locale) {
        long diff = System.currentTimeMillis() - date.getTime();
        long minutes = diff / (1000 * 60);
        long hours = diff / (1000 * 60 * 60);
        long days = diff / (1000 * 60 * 60 * 24);
        
        MessageSource messageSource = getMessageSource();
        
        if (minutes < 1) {
            return messageSource.getMessage("time.justnow", null, locale);
        } else if (minutes < 60) {
            return messageSource.getMessage("time.minutes.ago", 
                new Object[]{minutes}, locale);
        } else if (hours < 24) {
            return messageSource.getMessage("time.hours.ago", 
                new Object[]{hours}, locale);
        } else if (days < 7) {
            return messageSource.getMessage("time.days.ago", 
                new Object[]{days}, locale);
        } else {
            return new SimpleDateFormat("yyyy-MM-dd").format(date);
        }
    }
}
```

配置文件：
```properties
# 中文
time.justnow=刚刚
time.minutes.ago={0}分钟前
time.hours.ago={0}小时前
time.days.ago={0}天前

# English
time.justnow=Just now
time.minutes.ago={0} minutes ago
time.hours.ago={0} hours ago
time.days.ago={0} days ago
```

页面显示：
```html
<div class="article-meta">
    <span th:text="${dateService.getRelativeTime(article.publishDate, #locale)}">
        5分钟前
    </span>
</div>
```

---

## 5. 🔢 数字本地化处理


### 5.1 数字格式的地区差异


**🌏 不同国家的数字习惯**
```
数字：1234567.89

中国：1,234,567.89  (逗号分隔千位，点是小数点)
德国：1.234.567,89  (点分隔千位，逗号是小数点)
印度：12,34,567.89  (每两位一个逗号)

百分比：
中国：85%
法国：85 %  (有空格)
```

### 5.2 数字格式化配置


**📋 配置文件**

`messages_zh_CN.properties`：
```properties
# 数字格式
number.format.integer=#,##0
number.format.decimal=#,##0.00
number.format.percent=#0.00%
number.format.currency=¥#,##0.00

# 数字相关文本
number.total=总计
number.subtotal=小计
number.discount=折扣
number.quantity=数量
```

`messages_en_US.properties`：
```properties
# Number Formats
number.format.integer=#,##0
number.format.decimal=#,##0.00
number.format.percent=#0.00%
number.format.currency=$#,##0.00

# Number Related Text
number.total=Total
number.subtotal=Subtotal
number.discount=Discount
number.quantity=Quantity
```

### 5.3 页面数字格式化


**📝 使用#numbers工具**

```html
<!-- 商品列表 -->
<div class="product-card">
    <!-- 价格显示 -->
    <div class="price">
        <span th:text="${#numbers.formatDecimal(product.price, 1, 2)}">99.99</span>
    </div>
    
    <!-- 库存数量 -->
    <div class="stock">
        <span th:text="#{number.quantity}">数量</span>:
        <span th:text="${#numbers.formatInteger(product.stock, 1)}">1,000</span>
    </div>
    
    <!-- 折扣显示 -->
    <div class="discount" th:if="${product.discount > 0}">
        <span th:text="#{number.discount}">折扣</span>:
        <span th:text="${#numbers.formatPercent(product.discount, 1, 2)}">15%</span>
    </div>
</div>

<!-- 订单总计 -->
<div class="order-summary">
    <div class="row">
        <span th:text="#{number.subtotal}">小计</span>
        <span th:text="${#numbers.formatCurrency(order.subtotal)}">¥299.00</span>
    </div>
    <div class="row">
        <span th:text="#{number.total}">总计</span>
        <span th:text="${#numbers.formatCurrency(order.total)}">¥299.00</span>
    </div>
</div>
```

**💡 显示效果**：
```
中文用户看到：
价格：¥1,299.99
数量：1,000
折扣：15.00%

英文用户看到：
Price: $1,299.99
Quantity: 1,000
Discount: 15.00%
```

### 5.4 统计数据展示


**📊 大数字的友好显示**

```html
<!-- 网站统计 -->
<div class="statistics">
    <div class="stat-item">
        <h3 th:text="${#numbers.formatInteger(stats.totalUsers, 1)}">1,234,567</h3>
        <p th:text="#{stats.users}">注册用户</p>
    </div>
    
    <div class="stat-item">
        <h3 th:text="${#numbers.formatInteger(stats.totalOrders, 1)}">98,765</h3>
        <p th:text="#{stats.orders}">订单数</p>
    </div>
    
    <div class="stat-item">
        <h3 th:text="${#numbers.formatDecimal(stats.revenue, 1, 2)}">12,345,678.90</h3>
        <p th:text="#{stats.revenue}">总营收</p>
    </div>
</div>
```

---

## 6. 💰 货币本地化展示


### 6.1 货币符号与格式


**🌍 不同国家的货币习惯**
```
价格：100元

中国：¥100.00  或  100.00元
美国：$100.00
欧元区：€100,00  (注意逗号是小数点)
日本：¥100  (没有小数)
英国：£100.00
```

### 6.2 货币配置


**📋 资源文件**

`messages_zh_CN.properties`：
```properties
# 货币设置
currency.symbol=¥
currency.code=CNY
currency.name=人民币
currency.format={0}{1}

# 价格相关
price.original=原价
price.current=现价
price.save=节省
price.free=免费
price.from=起
```

`messages_en_US.properties`：
```properties
# Currency Settings
currency.symbol=$
currency.code=USD
currency.name=US Dollar
currency.format={0}{1}

# Price Related
price.original=Original Price
price.current=Current Price
price.save=Save
price.free=Free
price.from=From
```

`messages_ja_JP.properties`：
```properties
# 通貨設定
currency.symbol=¥
currency.code=JPY
currency.name=日本円
currency.format={0}{1}

# 価格関連
price.original=定価
price.current=現在価格
price.save=節約
price.free=無料
price.from=から
```

### 6.3 商品价格显示


**📝 电商页面实现**

```html
<!-- 商品价格卡片 -->
<div class="product-price">
    <!-- 原价 -->
    <div class="original-price" th:if="${product.originalPrice != null}">
        <span th:text="#{price.original}">原价</span>:
        <del>
            <span th:text="#{currency.symbol}">¥</span>
            <span th:text="${#numbers.formatDecimal(product.originalPrice, 1, 2)}">299.00</span>
        </del>
    </div>
    
    <!-- 现价 -->
    <div class="current-price">
        <span th:text="#{price.current}">现价</span>:
        <strong>
            <span th:text="#{currency.symbol}">¥</span>
            <span th:text="${#numbers.formatDecimal(product.currentPrice, 1, 2)}">199.00</span>
        </strong>
    </div>
    
    <!-- 节省金额 -->
    <div class="save-amount" th:if="${product.originalPrice != null}">
        <span th:text="#{price.save}">节省</span>
        <span th:text="#{currency.symbol}">¥</span>
        <span th:text="${#numbers.formatDecimal(product.originalPrice - product.currentPrice, 1, 2)}">100.00</span>
    </div>
</div>

<!-- 价格区间显示 -->
<div class="price-range">
    <span th:text="#{currency.symbol}">¥</span>
    <span th:text="${#numbers.formatDecimal(product.minPrice, 1, 2)}">99.00</span>
    -
    <span th:text="#{currency.symbol}">¥</span>
    <span th:text="${#numbers.formatDecimal(product.maxPrice, 1, 2)}">299.00</span>
</div>

<!-- 免费标记 -->
<span class="free-tag" th:if="${product.price == 0}" 
      th:text="#{price.free}">免费</span>
```

**💡 显示效果**：
```
中文：
原价：¥299.00
现价：¥199.00
节省 ¥100.00

English:
Original Price: $299.00
Current Price: $199.00
Save $100.00

日本語:
定価：¥299
現在価格：¥199
節約 ¥100
```

### 6.4 购物车总价计算


```html
<div class="cart-summary">
    <!-- 商品小计 -->
    <div class="summary-row">
        <span th:text="#{cart.subtotal}">商品小计</span>
        <span>
            <span th:text="#{currency.symbol}">¥</span>
            <span th:text="${#numbers.formatDecimal(cart.subtotal, 1, 2)}">500.00</span>
        </span>
    </div>
    
    <!-- 运费 -->
    <div class="summary-row">
        <span th:text="#{cart.shipping}">运费</span>
        <span th:if="${cart.shipping > 0}">
            <span th:text="#{currency.symbol}">¥</span>
            <span th:text="${#numbers.formatDecimal(cart.shipping, 1, 2)}">10.00</span>
        </span>
        <span th:if="${cart.shipping == 0}" th:text="#{price.free}">免费</span>
    </div>
    
    <!-- 优惠券 -->
    <div class="summary-row" th:if="${cart.couponDiscount > 0}">
        <span th:text="#{cart.coupon}">优惠券</span>
        <span class="discount">
            -<span th:text="#{currency.symbol}">¥</span>
            <span th:text="${#numbers.formatDecimal(cart.couponDiscount, 1, 2)}">50.00</span>
        </span>
    </div>
    
    <!-- 总计 -->
    <div class="summary-total">
        <span th:text="#{cart.total}">应付总额</span>
        <strong>
            <span th:text="#{currency.symbol}">¥</span>
            <span th:text="${#numbers.formatDecimal(cart.total, 1, 2)}">460.00</span>
        </strong>
    </div>
</div>
```

---

## 7. 🌏 文化适配处理


### 7.1 什么是文化适配


**🎯 通俗理解**
```
不只是翻译文字，还要考虑文化习惯：

时间习惯：
- 中国：24小时制(14:00)
- 美国：12小时制(2:00 PM)

颜色含义：
- 中国：红色代表喜庆
- 西方：红色代表危险

排版习惯：
- 中文：从左到右
- 阿拉伯语：从右到左

这就是文化适配！
```

### 7.2 时间制式适配


**📋 配置不同时间格式**

`messages_zh_CN.properties`：
```properties
# 时间格式(24小时制)
time.format.12hour=false
time.format.pattern=HH:mm:ss
time.am=上午
time.pm=下午
```

`messages_en_US.properties`：
```properties
# Time Format (12-hour)
time.format.12hour=true
time.format.pattern=hh:mm:ss a
time.am=AM
time.pm=PM
```

**📝 页面实现**

```html
<!-- 营业时间显示 -->
<div class="business-hours">
    <h3 th:text="#{business.hours}">营业时间</h3>
    
    <!-- 如果是12小时制 -->
    <p th:if="${@environment.getProperty('time.format.12hour') == 'true'}">
        <span>9:00 AM - 10:00 PM</span>
    </p>
    
    <!-- 如果是24小时制 -->
    <p th:unless="${@environment.getProperty('time.format.12hour') == 'true'}">
        <span>09:00 - 22:00</span>
    </p>
</div>
```

### 7.3 地址格式适配


**🏠 不同国家的地址习惯**

配置文件：
```properties
# 中国地址格式
address.format.china={province} {city} {district} {street} {detail}
address.province=省份
address.city=城市
address.district=区/县
address.street=街道
address.detail=详细地址

# 美国地址格式
address.format.usa={street} {detail}, {city}, {state} {zipcode}
address.street=Street Address
address.city=City
address.state=State
address.zipcode=ZIP Code
```

页面显示：
```html
<div class="address-display">
    <!-- 中国地址 -->
    <div th:if="${#locale.language == 'zh'}">
        <p>
            <span th:text="${address.province}">广东省</span>
            <span th:text="${address.city}">深圳市</span>
            <span th:text="${address.district}">南山区</span>
        </p>
        <p th:text="${address.detail}">科技园XXX大厦</p>
    </div>
    
    <!-- 美国地址 -->
    <div th:if="${#locale.language == 'en'}">
        <p th:text="${address.street}">123 Main Street</p>
        <p>
            <span th:text="${address.city}">New York</span>,
            <span th:text="${address.state}">NY</span>
            <span th:text="${address.zipcode}">10001</span>
        </p>
    </div>
</div>
```

### 7.4 节日与活动适配


**🎉 本地化节日促销**

配置文件：
```properties
# 中国节日
holiday.spring.festival=春节
holiday.mid.autumn=中秋节
holiday.dragon.boat=端午节
promotion.spring.festival=春节特惠，全场8折！

# 美国节日
holiday.christmas=Christmas
holiday.thanksgiving=Thanksgiving
holiday.black.friday=Black Friday
promotion.black.friday=Black Friday Sale - Up to 50% OFF!
```

动态展示：
```html
<div class="promotion-banner">
    <!-- 根据当前日期和地区显示对应促销 -->
    <div th:if="${#locale.language == 'zh' && #dates.day(#dates.createNow()) == 1 && #dates.month(#dates.createNow()) == 0}">
        <h2 th:text="#{holiday.spring.festival}">春节</h2>
        <p th:text="#{promotion.spring.festival}">春节特惠</p>
    </div>
    
    <div th:if="${#locale.language == 'en' && #dates.month(#dates.createNow()) == 10}">
        <h2 th:text="#{holiday.black.friday}">Black Friday</h2>
        <p th:text="#{promotion.black.friday}">Black Friday Sale</p>
    </div>
</div>
```

### 7.5 图标与颜色适配


**🎨 视觉元素本地化**

```html
<!-- 成功提示 -->
<div class="alert-success">
    <!-- 中国用户：红色背景，√图标 -->
    <div th:if="${#locale.language == 'zh'}" 
         style="background-color: #ff4444;">
        <i class="icon-check">✓</i>
        <span th:text="#{message.success}">操作成功</span>
    </div>
    
    <!-- 西方用户：绿色背景，✓图标 -->
    <div th:unless="${#locale.language == 'zh'}"
         style="background-color: #44ff44;">
        <i class="icon-check">✓</i>
        <span th:text="#{message.success}">Success</span>
    </div>
</div>
```

### 7.6 支付方式适配


**💳 本地化支付选项**

```html
<div class="payment-methods">
    <h3 th:text="#{payment.method}">支付方式</h3>
    
    <!-- 中国用户看到的支付方式 -->
    <div th:if="${#locale.language == 'zh'}">
        <label>
            <input type="radio" name="payment" value="alipay">
            <span th:text="#{payment.alipay}">支付宝</span>
        </label>
        <label>
            <input type="radio" name="payment" value="wechat">
            <span th:text="#{payment.wechat}">微信支付</span>
        </label>
        <label>
            <input type="radio" name="payment" value="unionpay">
            <span th:text="#{payment.unionpay}">银联支付</span>
        </label>
    </div>
    
    <!-- 国际用户看到的支付方式 -->
    <div th:unless="${#locale.language == 'zh'}">
        <label>
            <input type="radio" name="payment" value="visa">
            <span th:text="#{payment.visa}">Visa</span>
        </label>
        <label>
            <input type="radio" name="payment" value="mastercard">
            <span th:text="#{payment.mastercard}">Mastercard</span>
        </label>
        <label>
            <input type="radio" name="payment" value="paypal">
            <span th:text="#{payment.paypal}">PayPal</span>
        </label>
    </div>
</div>
```

---

## 8. 📋 核心要点总结


### 8.1 多语言开发关键点


**🎯 核心理解**
```
多语言网站 ≠ 只是翻译文字
多语言网站 = 文字翻译 + 格式适配 + 文化习惯

记住三个层次：
Level 1: 文本国际化 (菜单、按钮、提示)
Level 2: 格式本地化 (日期、数字、货币)  
Level 3: 文化适配 (节日、颜色、习惯)
```

### 8.2 实战开发检查清单


**✅ 开发前准备**
- [ ] 确定支持的语言列表
- [ ] 设计资源文件结构
- [ ] 规划命名规范(如：menu.home、form.username)

**✅ 功能开发**
- [ ] 导航菜单国际化
- [ ] 表单标签和占位符国际化
- [ ] 错误消息多语言支持
- [ ] 日期格式本地化
- [ ] 数字和货币格式化
- [ ] 支付方式适配

**✅ 测试验证**
- [ ] 测试所有语言的显示效果
- [ ] 检查特殊字符是否正常
- [ ] 验证日期时间格式
- [ ] 确认货币符号位置

### 8.3 常见问题与解决


**❓ Q1: 新增语言需要改动很多代码吗？**
```
A: 不需要！只需要：
1. 添加新的资源文件 (如 messages_fr_FR.properties)
2. 翻译所有key对应的文本
3. 配置支持的语言列表
页面代码完全不用改！
```

**❓ Q2: 如何处理语言切换后的页面跳转？**
```java
// Controller添加语言参数处理
@GetMapping("/switch-language")
public String switchLanguage(@RequestParam String lang, 
                            HttpServletRequest request) {
    // 保存用户语言偏好
    request.getSession().setAttribute("user.locale", lang);
    
    // 跳转回原页面
    String referer = request.getHeader("Referer");
    return "redirect:" + referer;
}
```

**❓ Q3: 如何处理动态内容的翻译？**
```java
// 数据库存储多语言字段
@Entity
public class Product {
    private Long id;
    
    @Column(name = "name_zh")
    private String nameZh;  // 中文名称
    
    @Column(name = "name_en")
    private String nameEn;  // 英文名称
    
    // 根据当前语言返回对应字段
    public String getLocalizedName(Locale locale) {
        if ("zh".equals(locale.getLanguage())) {
            return nameZh;
        }
        return nameEn;
    }
}
```

### 8.4 性能优化建议


**⚡ 优化策略**
```
1. 资源文件缓存
   - Spring Boot默认会缓存资源文件
   - 开发环境可以禁用：spring.messages.cache-duration=-1

2. 减少重复查询
   - 在页面开始获取常用文本
   - 使用变量存储，避免重复 #{} 查询

3. CDN加速
   - 静态资源使用CDN
   - 不同语言的资源分开部署

4. 懒加载翻译
   - 只加载当前页面需要的翻译
   - 大型应用可以按模块拆分资源文件
```

### 8.5 最佳实践建议


**💡 开发建议**
```
🔸 统一命名规范
   menu.*     - 菜单相关
   form.*     - 表单相关
   error.*    - 错误信息
   message.*  - 提示消息

🔸 合理组织文件
   messages.properties           - 默认(英文)
   messages_zh_CN.properties    - 简体中文
   messages_zh_TW.properties    - 繁体中文
   messages_en_US.properties    - 美式英语
   messages_ja_JP.properties    - 日语

🔸 注释和文档
   # 在资源文件中添加注释
   # menu.home - 首页导航按钮文本
   menu.home=Home

🔸 版本控制
   - 资源文件纳入版本管理
   - 记录翻译变更历史
   - 建立翻译审核流程
```

**🎯 核心记忆口诀**
```
多语言开发三步走：
1. 资源文件配置好 (properties文件)
2. 页面#{key}来引用 (Thymeleaf语法)
3. 格式本地化处理 (日期数字货币)

记住：
- 文本不写死，全部用资源文件
- 格式要适配，考虑用户习惯
- 文化要尊重，避免敏感问题
```