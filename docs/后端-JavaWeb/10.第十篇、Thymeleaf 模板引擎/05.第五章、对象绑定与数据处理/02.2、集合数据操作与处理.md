---
title: 2、集合数据操作与处理
---
## 📚 目录

1. [集合数据处理概述](#1-集合数据处理概述)
2. [List集合处理](#2-List集合处理)
3. [Set集合处理](#3-Set集合处理)
4. [Map集合处理](#4-Map集合处理)
5. [数组操作](#5-数组操作)
6. [集合过滤](#6-集合过滤)
7. [集合排序](#7-集合排序)
8. [集合转换](#8-集合转换)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 📦 集合数据处理概述


### 1.1 什么是集合数据处理


**简单理解**：在实际项目中，我们经常需要在网页上展示一批数据，比如：
- 商品列表（多个商品）
- 用户列表（多个用户）
- 订单记录（多条订单）

这些"一批数据"在Java中就是**集合**，Thymeleaf需要把这些集合数据渲染到HTML页面上。

### 1.2 为什么需要处理集合


```
实际场景举例：
电商网站商品展示
┌──────────────┐
│ 商品1: ¥99  │  ← 这是List中的第1个对象
├──────────────┤
│ 商品2: ¥199 │  ← 这是List中的第2个对象
├──────────────┤
│ 商品3: ¥299 │  ← 这是List中的第3个对象
└──────────────┘

后端传来的数据：List<Product> products
前端需要：把每个商品都显示出来
```

### 1.3 Thymeleaf支持的集合类型


| 集合类型 | 用途说明 | 常见场景 |
|---------|---------|---------|
| **List** | 有序、可重复的列表 | 商品列表、用户列表、新闻列表 |
| **Set** | 无序、不重复的集合 | 标签集合、分类集合 |
| **Map** | 键值对映射 | 配置信息、字典数据 |
| **Array** | 数组 | 图片数组、ID数组 |

---

## 2. 📋 List集合处理


### 2.1 List是什么


> 💡 **核心概念**：List就像一个"购物清单"，按顺序记录了多个商品，可以重复。
> 
> 特点：①有顺序 ②可以重复 ③可以通过索引访问

### 2.2 基本遍历操作


**场景**：后端传来一个用户列表，前端要把所有用户显示出来

```java
// 后端Controller代码
@GetMapping("/users")
public String showUsers(Model model) {
    List<User> userList = new ArrayList<>();
    userList.add(new User(1, "张三", 25));
    userList.add(new User(2, "李四", 30));
    userList.add(new User(3, "王五", 28));
    
    model.addAttribute("users", userList);
    return "user-list";
}
```

**前端模板**：
```html
<!-- 最基础的遍历：th:each -->
<table>
    <tr>
        <th>ID</th>
        <th>姓名</th>
        <th>年龄</th>
    </tr>
    <!-- user是当前循环的对象，users是后端传来的集合 -->
    <tr th:each="user : ${users}">
        <td th:text="${user.id}"></td>
        <td th:text="${user.name}"></td>
        <td th:text="${user.age}"></td>
    </tr>
</table>
```

### 2.3 获取循环状态信息


**什么是状态信息**：就是循环过程中的"辅助信息"，比如当前是第几个、是不是第一个等

```html
<!-- stat是状态变量名，可以自定义 -->
<div th:each="user, stat : ${users}">
    <p>
        <!-- 显示序号（从1开始） -->
        第 <span th:text="${stat.count}"></span> 个用户：
        <span th:text="${user.name}"></span>
        
        <!-- 显示索引（从0开始） -->
        （索引：<span th:text="${stat.index}"></span>）
    </p>
</div>
```

**状态变量常用属性**：

| 属性名 | 含义 | 示例值 |
|-------|------|--------|
| `index` | 当前索引（从0开始） | 0, 1, 2... |
| `count` | 当前计数（从1开始） | 1, 2, 3... |
| `size` | 集合总大小 | 10 |
| `first` | 是否第一个 | true/false |
| `last` | 是否最后一个 | true/false |
| `even` | 是否偶数项 | true/false |
| `odd` | 是否奇数项 | true/false |

### 2.4 实用案例：隔行变色


```html
<!-- 利用状态变量实现斑马纹效果 -->
<table>
    <tr th:each="item, stat : ${items}" 
        th:class="${stat.odd} ? 'odd-row' : 'even-row'">
        <td th:text="${item.name}"></td>
    </tr>
</table>

<style>
    .odd-row { background-color: #f0f0f0; }
    .even-row { background-color: #ffffff; }
</style>
```

### 2.5 空集合处理


> ⚠️ **注意事项**：如果List为空，页面应该有友好提示

```html
<!-- 方式1：使用th:if判断 -->
<div th:if="${#lists.isEmpty(users)}">
    <p>暂无用户数据</p>
</div>

<div th:unless="${#lists.isEmpty(users)}">
    <table>
        <tr th:each="user : ${users}">
            <td th:text="${user.name}"></td>
        </tr>
    </table>
</div>

<!-- 方式2：使用Elvis运算符 -->
<p th:text="${#lists.isEmpty(users)} ? '暂无数据' : '共' + ${#lists.size(users)} + '条数据'"></p>
```

---

## 3. 🔷 Set集合处理


### 3.1 Set是什么


> 💡 **核心概念**：Set就像一个"去重后的标签集合"，不允许重复元素。
> 
> 特点：①无序（不保证顺序） ②不重复 ③没有索引

### 3.2 Set的典型应用场景


```
场景：文章标签系统
文章对象：
{
    title: "Java学习指南",
    tags: ["Java", "编程", "后端", "Java"]  ← 后端传来可能有重复
}

使用Set存储：
Set<String> tags = new HashSet<>();
tags.add("Java");
tags.add("编程");
tags.add("后端");
tags.add("Java");  ← 重复的不会添加

最终Set中只有：["Java", "编程", "后端"]
```

### 3.3 Set的遍历操作


```java
// 后端代码
@GetMapping("/article")
public String showArticle(Model model) {
    Set<String> tags = new HashSet<>();
    tags.add("Java");
    tags.add("Spring");
    tags.add("数据库");
    
    model.addAttribute("tags", tags);
    return "article";
}
```

```html
<!-- 前端模板：和List遍历方式相同 -->
<div class="tags">
    <span th:each="tag : ${tags}" 
          th:text="${tag}"
          class="tag-item"></span>
</div>

<!-- 输出效果：Java Spring 数据库 -->
```

### 3.4 Set常用工具方法


| 方法 | 作用 | 示例 |
|------|------|------|
| `#sets.isEmpty(set)` | 判断Set是否为空 | `${#sets.isEmpty(tags)}` |
| `#sets.size(set)` | 获取Set大小 | `${#sets.size(tags)}` |
| `#sets.contains(set, element)` | 判断是否包含元素 | `${#sets.contains(tags, 'Java')}` |

```html
<!-- 实际应用示例 -->
<div th:if="${#sets.contains(tags, 'Java')}">
    <p>✅ 这是一篇Java相关文章</p>
</div>

<p>
    共有 <span th:text="${#sets.size(tags)}"></span> 个标签
</p>
```

---

## 4. 🗂️ Map集合处理


### 4.1 Map是什么


> 💡 **核心概念**：Map就像一个"字典"，通过"key"（关键字）查找对应的"value"（值）。
> 
> 特点：①键值对存储 ②key不能重复 ③通过key快速查找value

```
实际理解：
英汉字典就是Map结构
┌──────────────┐
│ apple → 苹果 │  ← key: "apple",  value: "苹果"
│ banana → 香蕉│  ← key: "banana", value: "香蕉"
│ orange → 橙子│  ← key: "orange", value: "橙子"
└──────────────┘
```

### 4.2 Map的典型应用场景


**场景1：配置信息**
```java
Map<String, String> config = new HashMap<>();
config.put("websiteName", "我的博客");
config.put("author", "张三");
config.put("email", "zhangsan@example.com");
```

**场景2：统计数据**
```java
Map<String, Integer> statistics = new HashMap<>();
statistics.put("totalUsers", 1000);
statistics.put("activeUsers", 800);
statistics.put("newUsers", 50);
```

### 4.3 Map的遍历操作


```java
// 后端代码
@GetMapping("/config")
public String showConfig(Model model) {
    Map<String, String> siteConfig = new HashMap<>();
    siteConfig.put("title", "技术博客");
    siteConfig.put("author", "李四");
    siteConfig.put("description", "分享技术知识");
    
    model.addAttribute("config", siteConfig);
    return "config";
}
```

**方式1：遍历键值对**
```html
<table>
    <tr>
        <th>配置项</th>
        <th>配置值</th>
    </tr>
    <!-- entry代表每一个键值对，entry.key是键，entry.value是值 -->
    <tr th:each="entry : ${config}">
        <td th:text="${entry.key}"></td>
        <td th:text="${entry.value}"></td>
    </tr>
</table>
```

**方式2：直接访问指定key**
```html
<!-- 通过key直接获取value -->
<h1 th:text="${config.get('title')}"></h1>
<!-- 或者使用简写 -->
<h1 th:text="${config['title']}"></h1>

<p>作者：<span th:text="${config.author}"></span></p>
```

### 4.4 Map常用工具方法


| 方法 | 作用 | 示例 |
|------|------|------|
| `#maps.isEmpty(map)` | 判断Map是否为空 | `${#maps.isEmpty(config)}` |
| `#maps.size(map)` | 获取Map大小 | `${#maps.size(config)}` |
| `#maps.containsKey(map, key)` | 判断是否包含某个key | `${#maps.containsKey(config, 'title')}` |

```html
<!-- 安全访问：先判断key是否存在 -->
<div th:if="${#maps.containsKey(config, 'logo')}">
    <img th:src="${config.logo}" alt="Logo">
</div>
```

---

## 5. 📊 数组操作


### 5.1 数组是什么


> 💡 **核心概念**：数组就是固定长度的"格子"，每个格子存一个数据，通过位置（索引）访问。
> 
> 特点：①固定长度 ②有序 ③通过索引访问

### 5.2 数组的基本使用


```java
// 后端代码
@GetMapping("/images")
public String showImages(Model model) {
    String[] imageUrls = {
        "/img/photo1.jpg",
        "/img/photo2.jpg",
        "/img/photo3.jpg"
    };
    
    model.addAttribute("images", imageUrls);
    return "gallery";
}
```

```html
<!-- 前端模板：遍历数组 -->
<div class="gallery">
    <div th:each="img : ${images}">
        <img th:src="${img}" alt="照片">
    </div>
</div>
```

### 5.3 数组工具方法


| 方法 | 作用 | 示例 |
|------|------|------|
| `#arrays.isEmpty(array)` | 判断数组是否为空 | `${#arrays.isEmpty(images)}` |
| `#arrays.length(array)` | 获取数组长度 | `${#arrays.length(images)}` |
| `#arrays.contains(array, element)` | 判断是否包含元素 | `${#arrays.contains(images, 'photo1.jpg')}` |

```html
<!-- 显示数组长度 -->
<p>共有 <span th:text="${#arrays.length(images)}"></span> 张图片</p>

<!-- 访问指定索引的元素 -->
<img th:src="${images[0]}" alt="第一张图片">
```

---

## 6. 🔍 集合过滤


### 6.1 什么是集合过滤


**简单理解**：从一堆数据中，按照条件筛选出需要的数据。

```
原始数据：[商品A-¥50, 商品B-¥150, 商品C-¥200, 商品D-¥80]
过滤条件：价格 < ¥100
过滤结果：[商品A-¥50, 商品D-¥80]
```

### 6.2 使用th:if进行过滤


```html
<!-- 只显示价格低于100的商品 -->
<div th:each="product : ${products}">
    <div th:if="${product.price < 100}">
        <p th:text="${product.name} + ': ¥' + ${product.price}"></p>
    </div>
</div>
```

### 6.3 复杂过滤条件


```html
<!-- 多条件过滤：价格在50-200之间，且有库存 -->
<div th:each="product : ${products}">
    <div th:if="${product.price >= 50 && product.price <= 200 && product.stock > 0}">
        <p>
            <span th:text="${product.name}"></span> - 
            <span th:text="'¥' + ${product.price}"></span>
            （库存：<span th:text="${product.stock}"></span>）
        </p>
    </div>
</div>
```

### 6.4 后端过滤 vs 前端过滤


| 方式 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **后端过滤** | 数据量小，性能好 | 需要修改后端代码 | 数据量大，复杂过滤 |
| **前端过滤** | 灵活，无需改后端 | 传输数据量大 | 简单过滤，数据量小 |

> 🔥 **最佳实践**：复杂过滤和大数据量过滤应该在后端完成，前端只做简单的展示过滤。

---

## 7. 📈 集合排序


### 7.1 什么是集合排序


**简单理解**：把数据按照某种规则重新排列，比如从小到大、从新到旧。

```
原始数据：[30, 10, 50, 20, 40]
升序排序：[10, 20, 30, 40, 50]
降序排序：[50, 40, 30, 20, 10]
```

### 7.2 后端排序（推荐）


```java
// 后端排序示例
@GetMapping("/products")
public String showProducts(Model model) {
    List<Product> products = getProducts();
    
    // 按价格升序排序
    products.sort(Comparator.comparing(Product::getPrice));
    
    // 按价格降序排序
    // products.sort(Comparator.comparing(Product::getPrice).reversed());
    
    model.addAttribute("products", products);
    return "products";
}
```

### 7.3 前端排序展示


> ⚠️ **重要提示**：Thymeleaf本身不支持直接在模板中排序，排序应该在后端完成。

**如果必须在前端排序**（不推荐）：
```html
<!-- 方案：使用JavaScript辅助 -->
<script th:inline="javascript">
    let products = /*[[${products}]]*/ [];
    // 排序逻辑
    products.sort((a, b) => a.price - b.price);
    // 重新渲染
</script>
```

### 7.4 多字段排序


```java
// 先按类别，再按价格排序
products.sort(
    Comparator.comparing(Product::getCategory)
              .thenComparing(Product::getPrice)
);
```

---

## 8. 🔄 集合转换


### 8.1 什么是集合转换


**简单理解**：把一种集合类型变成另一种，或者提取集合中的某些字段。

```
原始数据：List<User> → 用户对象列表
转换目标：List<String> → 只要用户名列表

例如：
[User(id=1, name="张三"), User(id=2, name="李四")]
    ↓ 转换
["张三", "李四"]
```

### 8.2 提取单个字段（后端实现）


```java
@GetMapping("/user-names")
public String showUserNames(Model model) {
    List<User> users = getUsers();
    
    // 使用Stream API提取所有用户名
    List<String> names = users.stream()
                              .map(User::getName)
                              .collect(Collectors.toList());
    
    model.addAttribute("names", names);
    return "names";
}
```

```html
<!-- 前端展示 -->
<ul>
    <li th:each="name : ${names}" th:text="${name}"></li>
</ul>
```

### 8.3 List转Set（去重）


```java
// 后端代码
List<String> tags = Arrays.asList("Java", "Spring", "Java", "数据库", "Spring");
Set<String> uniqueTags = new HashSet<>(tags);

model.addAttribute("tags", uniqueTags);
```

```html
<!-- 前端显示唯一标签 -->
<div th:each="tag : ${tags}">
    <span th:text="${tag}" class="tag"></span>
</div>
```

### 8.4 集合分组（Map转换）


```java
// 按类别分组商品
Map<String, List<Product>> groupedProducts = products.stream()
    .collect(Collectors.groupingBy(Product::getCategory));

model.addAttribute("grouped", groupedProducts);
```

```html
<!-- 分类显示 -->
<div th:each="entry : ${grouped}">
    <h3 th:text="${entry.key}"></h3>
    <ul>
        <li th:each="product : ${entry.value}" th:text="${product.name}"></li>
    </ul>
</div>
```

---

## 9. 📋 核心要点总结


### 9.1 集合类型选择指南


```
选择List：需要有序、可重复的数据
    ✅ 商品列表、订单列表、评论列表
    
选择Set：需要去重的数据
    ✅ 标签集合、分类集合、唯一ID集合
    
选择Map：需要键值对映射的数据
    ✅ 配置信息、字典数据、统计结果
    
选择Array：固定长度的数据
    ✅ 图片数组、固定选项
```

### 9.2 遍历语法记忆


```html
<!-- 基本格式 -->
th:each="临时变量 : ${集合}"

<!-- 带状态变量 -->
th:each="临时变量, 状态变量 : ${集合}"

<!-- 实例 -->
th:each="user : ${users}"           ← 只要数据
th:each="user, stat : ${users}"     ← 还要循环信息
```

### 9.3 常用工具方法速查


| 工具类 | 常用方法 | 作用 |
|--------|---------|------|
| `#lists` | `isEmpty()` `size()` `contains()` | List操作 |
| `#sets` | `isEmpty()` `size()` `contains()` | Set操作 |
| `#maps` | `isEmpty()` `size()` `containsKey()` | Map操作 |
| `#arrays` | `isEmpty()` `length()` `contains()` | 数组操作 |

### 9.4 最佳实践建议


> 🔥 **性能优化原则**
> 
> ① 复杂过滤和排序在后端完成
> ② 数据量大时避免前端遍历
> ③ 合理使用状态变量减少计算

> 💡 **代码可读性原则**
> 
> ① 变量命名要有意义（user而不是u）
> ② 复杂逻辑拆分成多个小步骤
> ③ 添加注释说明业务含义

> ⚠️ **安全性原则**
> 
> ① 处理空集合要有判断
> ② Map访问key前先检查是否存在
> ③ 数组访问要注意索引越界

### 9.5 学习路径建议


```
新手学习顺序：
① 先掌握List遍历 → 最常用，必须熟练
    ↓
② 理解状态变量 → 实现复杂展示效果
    ↓
③ 学习Map操作 → 处理配置和字典数据
    ↓
④ 了解Set和数组 → 特定场景使用
    ↓
⑤ 掌握过滤排序 → 提升数据处理能力
```

**核心记忆口诀**：
- 集合遍历用th:each，临时变量接数据
- 状态信息stat记录，索引计数全知晓
- List有序可重复，Set去重无顺序
- Map键值对查找，Array固定长度用
- 过滤排序后端做，前端展示要简洁