---
title: 1、表单对象绑定与回显
---
## 📚 目录

1. [表单对象绑定th:object](#1-表单对象绑定thobject)
2. [字段绑定th:field](#2-字段绑定thfield)
3. [表单数据回显](#3-表单数据回显)
4. [对象属性映射](#4-对象属性映射)
5. [数据类型转换](#5-数据类型转换)
6. [表单提交处理](#6-表单提交处理)
7. [重定向后数据保持](#7-重定向后数据保持)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 📝 表单对象绑定th:object


### 1.1 什么是表单对象绑定


**通俗理解**：就像给表单找一个"数据管家"，这个管家（对象）负责管理表单里所有的数据。

```
现实场景类比：
填写用户注册表单 = 往一个"用户信息表"里填数据
┌─────────────────┐
│  用户信息表      │ ← 这就是th:object绑定的对象
├─────────────────┤
│ 姓名：[ ]       │
│ 年龄：[ ]       │
│ 邮箱：[ ]       │
└─────────────────┘
```

**核心作用**：
- 🔸 **统一管理**：把表单里的多个输入框都关联到同一个Java对象
- 🔸 **简化操作**：不用一个个手动取值，自动帮你对应好
- 🔸 **双向绑定**：既能把对象数据显示到表单，也能把表单数据收集到对象

### 1.2 基本使用方式


**Java后端代码**：
```java
@Controller
public class UserController {
    
    // 显示表单页面
    @GetMapping("/user/add")
    public String showForm(Model model) {
        // 创建一个空的用户对象，作为表单的"数据管家"
        User user = new User();
        model.addAttribute("userForm", user);
        return "user-form";
    }
}
```

**Thymeleaf模板代码**：
```html
<!-- th:object绑定表单对象 -->
<form th:action="@{/user/save}" th:object="${userForm}" method="post">
    <!-- 表单里的字段都会和userForm对象关联 -->
    <input type="text" th:field="*{username}">
    <input type="email" th:field="*{email}">
    <button type="submit">提交</button>
</form>
```

### 1.3 th:object的工作原理


**绑定流程图示**：
```
Controller准备数据          Thymeleaf模板          浏览器显示
       |                         |                    |
   创建User对象              th:object绑定          生成HTML表单
       |                         |                    |
   放入Model中     ─────────────>  ${userForm}        <form>元素
       |                         |                    |
   返回模板名称              找到对象引用            准备接收数据
```

**重点理解**：
- `th:object="${userForm}"` → 告诉Thymeleaf："这个表单的数据都来自userForm对象"
- `*{username}` → 星号表达式，意思是"从上面绑定的对象里取username属性"
- **选择变量表达式** `*{...}` 只能在 `th:object` 范围内使用

### 1.4 为什么要用th:object


| 🆚 对比项 | **不使用th:object** | **使用th:object** |
|---------|-------------------|------------------|
| **代码写法** | `th:value="${user.username}"` | `th:field="*{username}"` |
| **重复程度** | 每个字段都要写完整路径 | 简洁，只写属性名 |
| **修改成本** | 对象名改了，到处都要改 | 只改一处th:object |
| **双向绑定** | 需要手动处理 | 自动处理 |
| **适用场景** | 简单展示数据 | 表单数据交互 |

---

## 2. ⚙️ 字段绑定th:field


### 2.1 th:field是什么


**简单理解**：th:field是Thymeleaf提供的"懒人神器"，一个属性搞定三件事。

**th:field自动做的三件事**：
```
普通写法需要写三个属性：
<input type="text" 
       id="username" 
       name="username" 
       value="${user.username}">

使用th:field只需一个：
<input type="text" th:field="*{username}">

自动生成结果：
<input type="text" 
       id="username"     ← 自动生成id
       name="username"   ← 自动生成name
       value="张三">     ← 自动填充value
```

### 2.2 th:field的三大功能


**功能 1️⃣：自动生成id属性**
```html
<input th:field="*{email}">
<!-- 生成：id="email" -->

<input th:field="*{user.address}">  
<!-- 生成：id="user.address" -->
```

**功能 2️⃣：自动生成name属性**
```html
<input th:field="*{username}">
<!-- 生成：name="username" -->
<!-- 提交时参数名就是：username=输入的值 -->
```

**功能 3️⃣：自动填充value值**
```html
<!-- 后端传来的user对象：username="张三" -->
<input th:field="*{username}">
<!-- 生成：value="张三" -->
```

### 2.3 不同表单元素的使用


**文本输入框**：
```html
<input type="text" th:field="*{username}">
<input type="password" th:field="*{password}">
<input type="email" th:field="*{email}">
```

**单选框**：
```html
性别：
<input type="radio" th:field="*{gender}" value="male"> 男
<input type="radio" th:field="*{gender}" value="female"> 女

<!-- 如果user.gender="male"，第一个会自动选中 -->
```

**复选框**：
```html
<!-- 单个复选框 -->
<input type="checkbox" th:field="*{agreement}"> 同意协议

<!-- 多个复选框（绑定到数组或集合） -->
<input type="checkbox" th:field="*{hobbies}" value="reading"> 阅读
<input type="checkbox" th:field="*{hobbies}" value="sports"> 运动
<input type="checkbox" th:field="*{hobbies}" value="music"> 音乐
```

**下拉选择框**：
```html
<select th:field="*{city}">
    <option value="">请选择城市</option>
    <option value="beijing">北京</option>
    <option value="shanghai">上海</option>
</select>
```

**文本域**：
```html
<textarea th:field="*{description}" rows="5"></textarea>
```

### 2.4 th:field使用注意事项


**⚠️ 常见错误**：
```html
<!-- ❌ 错误：在没有th:object的地方用*{} -->
<form action="/save" method="post">
    <input th:field="*{username}">  <!-- 报错！找不到对象 -->
</form>

<!-- ✅ 正确：先绑定对象 -->
<form th:action="@{/save}" th:object="${user}" method="post">
    <input th:field="*{username}">  <!-- 正确 -->
</form>
```

**💡 最佳实践**：
- 🟢 表单数据交互 → 用 `th:field`
- 🟢 只是显示数据 → 用 `th:text` 或 `th:value`
- 🟢 复杂逻辑 → 用 `th:value` 配合表达式

---

## 3. 🔄 表单数据回显


### 3.1 什么是表单数据回显


**通俗理解**：当你填错表单提交后，页面刷新了，但你之前填的数据还在，不用重新填，这就是"数据回显"。

**应用场景**：
```
用户注册失败的流程：
1. 用户填写：姓名"张三"、年龄"abc"（格式错误）
2. 点击提交
3. 后端验证失败
4. 返回表单页面
5. 姓名还显示"张三"，只需要改年龄 ← 这就是数据回显
```

### 3.2 数据回显的实现方式


**方式一：使用th:field自动回显** 🔥最推荐
```java
@PostMapping("/user/save")
public String saveUser(@ModelAttribute("userForm") User user, 
                      BindingResult result, Model model) {
    // 验证失败
    if (result.hasErrors()) {
        // user对象会自动回显到表单
        return "user-form";
    }
    // 保存成功
    return "redirect:/success";
}
```

```html
<form th:action="@{/user/save}" th:object="${userForm}" method="post">
    <!-- th:field自动回显user对象的值 -->
    <input type="text" th:field="*{username}">
    <input type="email" th:field="*{email}">
    <button type="submit">提交</button>
</form>
```

**方式二：手动设置回显值**
```java
@PostMapping("/user/save")
public String saveUser(User user, Model model) {
    if (有错误) {
        model.addAttribute("userForm", user); // 手动放回Model
        model.addAttribute("errorMsg", "数据格式错误");
        return "user-form";
    }
    return "redirect:/success";
}
```

### 3.3 不同字段类型的回显


**文本框回显**：
```html
<!-- 自动回显文本 -->
<input type="text" th:field="*{username}">
```

**单选框回显**：
```html
<!-- 自动匹配选中状态 -->
<input type="radio" th:field="*{gender}" value="male"> 男
<input type="radio" th:field="*{gender}" value="female"> 女
<!-- 如果gender="male"，第一个自动checked -->
```

**复选框回显**：
```html
<!-- 自动匹配多个选中状态 -->
<input type="checkbox" th:field="*{hobbies}" value="reading"> 阅读
<input type="checkbox" th:field="*{hobbies}" value="sports"> 运动
<!-- 如果hobbies=["reading", "sports"]，两个都选中 -->
```

**下拉框回显**：
```html
<select th:field="*{city}">
    <option value="">请选择</option>
    <option value="beijing">北京</option>
    <option value="shanghai">上海</option>
</select>
<!-- 自动匹配selected -->
```

### 3.4 回显配合错误提示


```html
<form th:action="@{/user/save}" th:object="${userForm}" method="post">
    <div>
        <label>用户名：</label>
        <input type="text" th:field="*{username}">
        <!-- 回显错误信息 -->
        <span th:if="${#fields.hasErrors('username')}" 
              th:errors="*{username}" style="color:red;">
        </span>
    </div>
    
    <div>
        <label>邮箱：</label>
        <input type="email" th:field="*{email}">
        <span th:if="${#fields.hasErrors('email')}" 
              th:errors="*{email}" style="color:red;">
        </span>
    </div>
    
    <button type="submit">提交</button>
</form>
```

---

## 4. 🔗 对象属性映射


### 4.1 什么是对象属性映射


**简单理解**：就是把表单里的输入框数据，自动"对号入座"到Java对象的属性里。

**映射过程图示**：
```
表单数据                  Spring处理              Java对象
  |                          |                      |
username=张三      ─────>  自动映射  ─────>    user.username = "张三"
email=test@qq.com          |                user.email = "test@qq.com"
age=25                     |                user.age = 25
```

### 4.2 简单属性映射


**Java实体类**：
```java
public class User {
    private String username;  // 对应表单name="username"
    private String email;     // 对应表单name="email"
    private Integer age;      // 对应表单name="age"
    
    // getter和setter方法（必须要有！）
}
```

**表单映射**：
```html
<form th:action="@{/user/save}" th:object="${userForm}" method="post">
    <!-- name会自动映射到对象属性 -->
    <input type="text" th:field="*{username}">  <!-- → user.username -->
    <input type="email" th:field="*{email}">    <!-- → user.email -->
    <input type="number" th:field="*{age}">     <!-- → user.age -->
    <button type="submit">提交</button>
</form>
```

**后端接收**：
```java
@PostMapping("/user/save")
public String saveUser(@ModelAttribute User user) {
    // Spring自动完成映射
    System.out.println(user.getUsername()); // 张三
    System.out.println(user.getEmail());    // test@qq.com
    System.out.println(user.getAge());      // 25
    return "success";
}
```

### 4.3 嵌套对象映射


**场景**：用户有地址信息（嵌套对象）

**Java实体类**：
```java
public class User {
    private String username;
    private Address address;  // 嵌套对象
    // getter/setter
}

public class Address {
    private String city;
    private String street;
    // getter/setter
}
```

**表单映射方式**：
```html
<form th:action="@{/user/save}" th:object="${userForm}" method="post">
    <input type="text" th:field="*{username}">
    
    <!-- 嵌套对象用"."访问 -->
    <input type="text" th:field="*{address.city}">
    <input type="text" th:field="*{address.street}">
    
    <button type="submit">提交</button>
</form>

<!-- 生成的HTML：
<input name="username" value="">
<input name="address.city" value="">      ← 注意name格式
<input name="address.street" value="">
-->
```

### 4.4 集合属性映射


**场景**：用户有多个手机号

**Java实体类**：
```java
public class User {
    private String username;
    private List<String> phones;  // 集合属性
    // getter/setter
}
```

**表单映射**：
```html
<form th:action="@{/user/save}" th:object="${userForm}" method="post">
    <input type="text" th:field="*{username}">
    
    <!-- 集合用索引访问 -->
    <input type="text" th:field="*{phones[0]}">  <!-- 第1个手机号 -->
    <input type="text" th:field="*{phones[1]}">  <!-- 第2个手机号 -->
    <input type="text" th:field="*{phones[2]}">  <!-- 第3个手机号 -->
    
    <button type="submit">提交</button>
</form>
```

### 4.5 映射规则总结


| 🎯 属性类型 | **th:field写法** | **生成的name** | **映射到对象** |
|-----------|----------------|--------------|-------------|
| 简单属性 | `*{username}` | `name="username"` | `user.username` |
| 嵌套对象 | `*{address.city}` | `name="address.city"` | `user.address.city` |
| 集合索引 | `*{phones[0]}` | `name="phones[0]"` | `user.phones.get(0)` |
| 数组 | `*{hobbies}` | `name="hobbies"` | `user.hobbies[]` |

**⚠️ 注意事项**：
- 🟡 对象必须有**无参构造方法**
- 🟡 属性必须有**getter和setter方法**
- 🟡 表单的name必须和属性名**完全一致**（大小写敏感）

---

## 5. 🔄 数据类型转换


### 5.1 为什么需要类型转换


**根本原因**：表单提交的数据都是**字符串类型**，但Java对象的属性可能是各种类型。

```
表单提交的数据         需要转换成          Java对象属性
     |                     |                    |
age="25"      ─────>   类型转换  ─────>    Integer age = 25
price="99.9"           |               Double price = 99.9
birthday="2024-01-01"  |               Date birthday = ...
```

### 5.2 Spring自动转换的类型


**基本数据类型** 🟢自动转换
```java
public class Product {
    private Integer id;        // "123" → 123
    private Double price;      // "99.9" → 99.9
    private Boolean onSale;    // "true" → true
    private Long stock;        // "1000" → 1000L
}
```

```html
<form th:object="${product}" method="post">
    <input type="number" th:field="*{id}">       <!-- 自动转Integer -->
    <input type="text" th:field="*{price}">      <!-- 自动转Double -->
    <input type="checkbox" th:field="*{onSale}"> <!-- 自动转Boolean -->
    <input type="number" th:field="*{stock}">    <!-- 自动转Long -->
</form>
```

### 5.3 日期类型转换


**方式一：使用@DateTimeFormat注解** 🔥推荐
```java
public class User {
    private String username;
    
    @DateTimeFormat(pattern = "yyyy-MM-dd")  // 指定日期格式
    private Date birthday;
    
    // getter/setter
}
```

```html
<form th:object="${user}" method="post">
    <input type="date" th:field="*{birthday}">
    <!-- 输入：2024-01-01 → 自动转成Date对象 -->
</form>
```

**方式二：全局配置日期格式**
```properties
# application.properties
spring.mvc.format.date=yyyy-MM-dd
spring.mvc.format.time=HH:mm:ss
spring.mvc.format.date-time=yyyy-MM-dd HH:mm:ss
```

### 5.4 自定义类型转换器


**场景**：表单提交的字符串需要转成自定义对象

**示例：标签字符串转List**
```java
// 自定义转换器
@Component
public class StringToTagListConverter implements Converter<String, List<Tag>> {
    
    @Override
    public List<Tag> convert(String source) {
        // "java,spring,mysql" → List<Tag>
        if (source == null || source.isEmpty()) {
            return new ArrayList<>();
        }
        
        String[] tagNames = source.split(",");
        List<Tag> tags = new ArrayList<>();
        
        for (String name : tagNames) {
            Tag tag = new Tag();
            tag.setName(name.trim());
            tags.add(tag);
        }
        
        return tags;
    }
}

// 注册转换器
@Configuration
public class WebConfig implements WebMvcConfigurer {
    
    @Autowired
    private StringToTagListConverter tagConverter;
    
    @Override
    public void addFormatters(FormatterRegistry registry) {
        registry.addConverter(tagConverter);
    }
}
```

```html
<form th:object="${article}" method="post">
    <input type="text" th:field="*{tags}" placeholder="java,spring,mysql">
    <!-- 输入"java,spring,mysql" → 自动转成List<Tag> -->
</form>
```

### 5.5 类型转换错误处理


**捕获转换错误**：
```java
@PostMapping("/user/save")
public String saveUser(@Valid User user, BindingResult result) {
    
    // 检查是否有转换错误
    if (result.hasErrors()) {
        // 获取错误信息
        List<ObjectError> errors = result.getAllErrors();
        for (ObjectError error : errors) {
            System.out.println(error.getDefaultMessage());
        }
        return "user-form";  // 返回表单页面
    }
    
    // 保存成功
    return "redirect:/success";
}
```

**显示转换错误**：
```html
<form th:object="${user}" method="post">
    <div>
        <label>年龄：</label>
        <input type="text" th:field="*{age}">
        <!-- 显示类型转换错误 -->
        <span th:if="${#fields.hasErrors('age')}" 
              th:errors="*{age}" 
              style="color:red;">
        </span>
    </div>
</form>
```

---

## 6. 📤 表单提交处理


### 6.1 表单提交的完整流程


```
用户操作            浏览器              Controller           数据库
   |                  |                    |                   |
填写表单    ──>    点击提交     ──>    接收数据      ──>    保存数据
   |               发送POST请求        验证数据             返回结果
   |                  |               处理业务                  |
看到结果   <──    渲染页面     <──    返回视图      <──    操作完成
```

### 6.2 接收表单数据的方式


**方式一：使用@ModelAttribute** 🔥推荐
```java
@PostMapping("/user/save")
public String saveUser(@ModelAttribute("userForm") User user, Model model) {
    // user对象已经封装好所有表单数据
    System.out.println("用户名：" + user.getUsername());
    System.out.println("邮箱：" + user.getEmail());
    
    // 保存到数据库
    userService.save(user);
    
    model.addAttribute("message", "保存成功");
    return "success";
}
```

**方式二：直接用对象接收**
```java
@PostMapping("/user/save")
public String saveUser(User user) {
    // Spring自动封装
    userService.save(user);
    return "redirect:/user/list";
}
```

**方式三：使用@RequestParam单独接收**
```java
@PostMapping("/user/save")
public String saveUser(@RequestParam String username,
                      @RequestParam String email,
                      @RequestParam Integer age) {
    // 逐个参数接收（不推荐，太繁琐）
    User user = new User();
    user.setUsername(username);
    user.setEmail(email);
    user.setAge(age);
    
    userService.save(user);
    return "redirect:/user/list";
}
```

### 6.3 表单验证


**使用校验注解**：
```java
public class User {
    
    @NotBlank(message = "用户名不能为空")
    @Size(min = 2, max = 20, message = "用户名长度2-20个字符")
    private String username;
    
    @NotBlank(message = "邮箱不能为空")
    @Email(message = "邮箱格式不正确")
    private String email;
    
    @NotNull(message = "年龄不能为空")
    @Min(value = 1, message = "年龄至少1岁")
    @Max(value = 150, message = "年龄最大150岁")
    private Integer age;
    
    // getter/setter
}
```

**Controller处理验证**：
```java
@PostMapping("/user/save")
public String saveUser(@Valid @ModelAttribute("userForm") User user, 
                      BindingResult result, Model model) {
    
    // 检查验证结果
    if (result.hasErrors()) {
        // 有错误，返回表单页面（数据自动回显）
        return "user-form";
    }
    
    // 验证通过，保存数据
    userService.save(user);
    return "redirect:/success";
}
```

**模板显示错误**：
```html
<form th:action="@{/user/save}" th:object="${userForm}" method="post">
    <div>
        <label>用户名：</label>
        <input type="text" th:field="*{username}">
        <span th:if="${#fields.hasErrors('username')}" 
              th:errors="*{username}" 
              style="color:red;">
        </span>
    </div>
    
    <div>
        <label>邮箱：</label>
        <input type="email" th:field="*{email}">
        <span th:if="${#fields.hasErrors('email')}" 
              th:errors="*{email}" 
              style="color:red;">
        </span>
    </div>
    
    <button type="submit">提交</button>
</form>
```

### 6.4 提交后的跳转方式


**方式一：返回视图名称**
```java
@PostMapping("/user/save")
public String saveUser(User user, Model model) {
    userService.save(user);
    model.addAttribute("message", "保存成功");
    return "success";  // 跳转到success.html
}
```

**方式二：重定向** 🔥推荐（避免重复提交）
```java
@PostMapping("/user/save")
public String saveUser(User user) {
    userService.save(user);
    return "redirect:/user/list";  // 重定向到列表页
}
```

**方式三：转发**
```java
@PostMapping("/user/save")
public String saveUser(User user) {
    userService.save(user);
    return "forward:/user/list";  // 转发到列表页
}
```

---

## 7. 🔁 重定向后数据保持


### 7.1 重定向的问题


**问题场景**：
```
表单提交流程：
1. POST /user/save  → 保存成功
2. redirect:/success  → 重定向
3. GET /success  → 显示成功页面

问题：重定向是新的请求，Model数据丢失！
```

**为什么数据会丢失**：
```
第一次请求（POST）       重定向（新请求）
     |                        |
  Model有数据      ──×──>   Model是空的
     |                        |
 请求结束，数据清空         无法获取之前的数据
```

### 7.2 解决方案一：使用RedirectAttributes


**基本用法**：
```java
@PostMapping("/user/save")
public String saveUser(User user, RedirectAttributes redirectAttributes) {
    
    userService.save(user);
    
    // 添加Flash属性（重定向后只能用一次）
    redirectAttributes.addFlashAttribute("message", "保存成功");
    redirectAttributes.addFlashAttribute("user", user);
    
    return "redirect:/success";
}

@GetMapping("/success")
public String success(Model model) {
    // 可以获取Flash属性
    // model中已经有message和user了
    return "success";
}
```

**模板使用**：
```html
<!-- success.html -->
<div th:if="${message}">
    <p th:text="${message}"></p>  <!-- 保存成功 -->
</div>

<div th:if="${user}">
    <p>用户名：<span th:text="${user.username}"></span></p>
</div>
```

### 7.3 解决方案二：URL参数传递


**传递简单参数**：
```java
@PostMapping("/user/save")
public String saveUser(User user, RedirectAttributes redirectAttributes) {
    
    userService.save(user);
    
    // 参数会拼接到URL上
    redirectAttributes.addAttribute("userId", user.getId());
    redirectAttributes.addAttribute("message", "保存成功");
    
    return "redirect:/user/detail";
    // 实际URL：/user/detail?userId=1&message=保存成功
}

@GetMapping("/user/detail")
public String detail(@RequestParam Long userId, 
                    @RequestParam String message,
                    Model model) {
    User user = userService.findById(userId);
    model.addAttribute("user", user);
    model.addAttribute("message", message);
    return "user-detail";
}
```

### 7.4 解决方案三：Session存储


**使用Session暂存数据**：
```java
@PostMapping("/user/save")
public String saveUser(User user, HttpSession session) {
    
    userService.save(user);
    
    // 存入Session
    session.setAttribute("message", "保存成功");
    session.setAttribute("user", user);
    
    return "redirect:/success";
}

@GetMapping("/success")
public String success(HttpSession session, Model model) {
    // 从Session取出数据
    String message = (String) session.getAttribute("message");
    User user = (User) session.getAttribute("user");
    
    model.addAttribute("message", message);
    model.addAttribute("user", user);
    
    // 用完立即清除（重要！）
    session.removeAttribute("message");
    session.removeAttribute("user");
    
    return "success";
}
```

### 7.5 三种方案对比


| 🆚 方案 | **优点** | **缺点** | **适用场景** |
|--------|---------|---------|------------|
| **RedirectAttributes** | 🟢简单方便<br>🟢自动清理<br>🟢支持对象 | 🔴只能用一次<br>🔴刷新页面数据丢失 | 提示消息、简单对象传递 |
| **URL参数** | 🟢可以书签<br>🟢可以分享链接 | 🔴只能传简单数据<br>🔴URL会很长 | ID、状态码等简单参数 |
| **Session** | 🟢可多次使用<br>🟢支持复杂对象 | 🔴需手动清理<br>🔴占用服务器内存 | 用户登录信息、购物车 |

### 7.6 完整示例：表单提交→重定向→显示结果


**Controller完整代码**：
```java
@Controller
public class UserController {
    
    @Autowired
    private UserService userService;
    
    // 显示表单
    @GetMapping("/user/add")
    public String showForm(Model model) {
        model.addAttribute("userForm", new User());
        return "user-form";
    }
    
    // 提交表单
    @PostMapping("/user/save")
    public String saveUser(@Valid @ModelAttribute("userForm") User user,
                          BindingResult result,
                          RedirectAttributes redirectAttributes) {
        
        // 验证失败，返回表单（数据自动回显）
        if (result.hasErrors()) {
            return "user-form";
        }
        
        // 保存成功
        userService.save(user);
        
        // 重定向时传递数据
        redirectAttributes.addFlashAttribute("message", "用户保存成功");
        redirectAttributes.addFlashAttribute("user", user);
        
        return "redirect:/user/success";
    }
    
    // 成功页面
    @GetMapping("/user/success")
    public String success() {
        // message和user已经在model中了
        return "success";
    }
}
```

**模板代码**：
```html
<!-- user-form.html 表单页面 -->
<form th:action="@{/user/save}" th:object="${userForm}" method="post">
    <div>
        <input type="text" th:field="*{username}">
        <span th:if="${#fields.hasErrors('username')}" 
              th:errors="*{username}" style="color:red;"></span>
    </div>
    <div>
        <input type="email" th:field="*{email}">
        <span th:if="${#fields.hasErrors('email')}" 
              th:errors="*{email}" style="color:red;"></span>
    </div>
    <button type="submit">提交</button>
</form>

<!-- success.html 成功页面 -->
<div>
    <h2 th:text="${message}">保存成功</h2>
    <p>用户名：<span th:text="${user.username}"></span></p>
    <p>邮箱：<span th:text="${user.email}"></span></p>
    <a th:href="@{/user/list}">返回列表</a>
</div>
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心知识


**🔸 th:object表单对象绑定**
- 作用：把表单和Java对象关联起来，统一管理表单数据
- 用法：`<form th:object="${对象名}">`
- 好处：简化代码，支持双向绑定

**🔸 th:field字段绑定**
- 作用：一个属性搞定id、name、value三件事
- 用法：`th:field="*{属性名}"`
- 注意：必须在th:object范围内使用

**🔸 表单数据回显**
- 原理：表单验证失败后，数据还在表单里
- 实现：使用th:field自动回显
- 场景：表单提交错误时不用重新填写

**🔸 对象属性映射**
- 简单属性：`*{username}` → `user.username`
- 嵌套对象：`*{address.city}` → `user.address.city`
- 集合属性：`*{phones[0]}` → `user.phones.get(0)`

**🔸 数据类型转换**
- 自动转换：基本类型、包装类型
- 日期转换：使用@DateTimeFormat注解
- 自定义转换：实现Converter接口

**🔸 表单提交处理**
- 接收数据：使用@ModelAttribute
- 数据验证：使用@Valid + BindingResult
- 错误处理：验证失败返回表单页面

**🔸 重定向数据保持**
- RedirectAttributes：一次性数据传递
- URL参数：简单数据传递
- Session：复杂对象暂存

### 8.2 实战开发建议


**💯 最佳实践**：
1. 表单交互统一使用 `th:object` + `th:field`
2. 实体类添加验证注解，Controller使用@Valid
3. 提交成功用redirect，避免重复提交
4. 使用RedirectAttributes传递提示消息
5. 嵌套对象和集合要初始化，避免空指针

**⚠️ 常见错误避免**：
- ❌ 没有绑定th:object就使用*{}表达式
- ❌ 实体类缺少getter/setter方法
- ❌ 表单name和属性名不一致
- ❌ 重定向后直接用Model传数据（会丢失）
- ❌ 忘记在Controller参数加@Valid

**🎯 记忆口诀**：
```
表单对象先绑定，th:object来指定
字段绑定用th:field，三合一真省心
数据回显很简单，验证失败自动填
对象映射看层次，点号索引要分清
类型转换Spring管，注解配置不用愁
提交处理验证先，错误返回成功转
重定向数据保持，Flash属性最合适
```