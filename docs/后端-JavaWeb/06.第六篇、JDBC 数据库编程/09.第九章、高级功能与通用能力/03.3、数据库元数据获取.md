---
title: 3、数据库元数据获取
---
## 📚 目录

1. [什么是数据库元数据](#1-什么是数据库元数据)
2. [DatabaseMetaData基础使用](#2-DatabaseMetaData基础使用)
3. [获取数据库信息](#3-获取数据库信息)
4. [获取表结构信息](#4-获取表结构信息)
5. [获取索引信息](#5-获取索引信息)
6. [动态SQL生成应用](#6-动态SQL生成应用)
7. [实际应用场景](#7-实际应用场景)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔍 什么是数据库元数据


### 1.1 元数据概念解释


**🔸 通俗理解**
```
元数据 = 描述数据的数据

生活例子：
图书馆书籍 = 数据
书籍目录信息(书名、作者、页数、类别) = 元数据

数据库中：
表中的具体记录 = 数据  
表的结构信息(列名、数据类型、约束) = 元数据
```

**💡 元数据包含什么**
```
数据库层面：
• 数据库名称、版本信息
• 数据库驱动信息
• 支持的SQL标准

表层面：
• 表名、表类型(TABLE/VIEW)
• 列名、数据类型、长度
• 主键、外键关系
• 索引信息

权限层面：
• 用户权限信息
• 可访问的表和视图
```

### 1.2 为什么需要获取元数据


**🎯 实际应用场景**
```
场景1：数据库管理工具
• 显示数据库结构树
• 自动生成表结构文档
• 数据库备份和迁移

场景2：ORM框架
• 根据表结构生成实体类
• 自动创建增删改查代码
• 验证实体类与表结构一致性

场景3：动态查询系统
• 根据表结构生成查询界面
• 动态构建SQL语句
• 数据导入导出工具
```

---

## 2. 📋 DatabaseMetaData基础使用


### 2.1 获取DatabaseMetaData对象


**🔸 基本获取方式**
```java
// 通过Connection对象获取
Connection conn = DriverManager.getConnection(url, user, password);
DatabaseMetaData metaData = conn.getMetaData();
```

**💻 完整示例**
```java
public class MetaDataDemo {
    public static void main(String[] args) {
        String url = "jdbc:mysql://localhost:3306/testdb";
        String user = "root";
        String password = "123456";
        
        try (Connection conn = DriverManager.getConnection(url, user, password)) {
            // 获取元数据对象
            DatabaseMetaData metaData = conn.getMetaData();
            System.out.println("成功获取数据库元数据对象");
            
        } catch (SQLException e) {
            e.printStackTrace();
        }
    }
}
```

### 2.2 DatabaseMetaData常用方法


**📊 方法分类表**

| 方法类型 | **核心方法** | **作用说明** | **返回类型** |
|---------|------------|-------------|-------------|
| 🏢 **数据库信息** | `getDatabaseProductName()` | `获取数据库产品名称` | `String` |
| 🏢 **数据库信息** | `getDatabaseProductVersion()` | `获取数据库版本` | `String` |
| 🏢 **数据库信息** | `getDriverName()` | `获取驱动名称` | `String` |
| 📋 **表信息** | `getTables()` | `获取表列表` | `ResultSet` |
| 🔧 **列信息** | `getColumns()` | `获取列信息` | `ResultSet` |
| 🔑 **主键信息** | `getPrimaryKeys()` | `获取主键信息` | `ResultSet` |
| 📊 **索引信息** | `getIndexInfo()` | `获取索引信息` | `ResultSet` |

---

## 3. 🏢 获取数据库信息


### 3.1 基本数据库信息


**🔸 获取数据库基础信息**
```java
public void getDatabaseInfo(Connection conn) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    
    // 数据库产品信息
    System.out.println("数据库名称: " + metaData.getDatabaseProductName());
    System.out.println("数据库版本: " + metaData.getDatabaseProductVersion());
    
    // 驱动信息
    System.out.println("JDBC驱动名称: " + metaData.getDriverName());
    System.out.println("JDBC驱动版本: " + metaData.getDriverVersion());
    
    // 连接信息
    System.out.println("数据库URL: " + metaData.getURL());
    System.out.println("当前用户: " + metaData.getUserName());
}
```

**📝 输出示例**
```
数据库名称: MySQL
数据库版本: 8.0.25
JDBC驱动名称: MySQL Connector/J
JDBC驱动版本: mysql-connector-java-8.0.25
数据库URL: jdbc:mysql://localhost:3306/testdb
当前用户: root@localhost
```

### 3.2 数据库能力信息


**🔸 查询数据库支持的功能**
```java
public void getDatabaseCapabilities(Connection conn) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    
    // 事务支持
    System.out.println("支持事务: " + metaData.supportsTransactions());
    
    // SQL标准支持
    System.out.println("支持SQL92入门级: " + metaData.supportsANSI92EntryLevelSQL());
    System.out.println("支持外连接: " + metaData.supportsOuterJoins());
    
    // 其他功能支持
    System.out.println("支持批量更新: " + metaData.supportsBatchUpdates());
    System.out.println("支持存储过程: " + metaData.supportsStoredProcedures());
}
```

---

## 4. 📋 获取表结构信息


### 4.1 获取所有表信息


**🔸 查询数据库中的所有表**
```java
public void getAllTables(Connection conn) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    
    // 获取所有表，参数：catalog, schema, tableName, types
    ResultSet tables = metaData.getTables(null, null, null, new String[]{"TABLE"});
    
    System.out.println("数据库中的所有表：");
    while (tables.next()) {
        String tableName = tables.getString("TABLE_NAME");
        String tableType = tables.getString("TABLE_TYPE");
        String remarks = tables.getString("REMARKS");
        
        System.out.printf("表名: %s, 类型: %s, 备注: %s%n", 
                         tableName, tableType, remarks);
    }
    tables.close();
}
```

### 4.2 获取表的列信息


**🔸 查询指定表的所有列**
```java
public void getTableColumns(Connection conn, String tableName) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    
    // 获取指定表的列信息
    ResultSet columns = metaData.getColumns(null, null, tableName, null);
    
    System.out.println("表 " + tableName + " 的列信息：");
    System.out.println("列名\t\t数据类型\t\t大小\t\t是否非空\t\t默认值");
    System.out.println("-----------------------------------------------------------------------");
    
    while (columns.next()) {
        String columnName = columns.getString("COLUMN_NAME");
        String dataType = columns.getString("TYPE_NAME");
        int columnSize = columns.getInt("COLUMN_SIZE");
        String nullable = columns.getString("IS_NULLABLE");
        String defaultValue = columns.getString("COLUMN_DEF");
        
        System.out.printf("%-15s %-15s %-10d %-10s %-15s%n", 
                         columnName, dataType, columnSize, nullable, 
                         defaultValue == null ? "NULL" : defaultValue);
    }
    columns.close();
}
```

**📊 列信息ResultSet重要字段**

| 字段名 | **含义** | **示例值** |
|-------|---------|-----------|
| `COLUMN_NAME` | `列名` | `user_id` |
| `TYPE_NAME` | `数据类型名称` | `INT` |
| `COLUMN_SIZE` | `列大小` | `11` |
| `DECIMAL_DIGITS` | `小数位数` | `0` |
| `IS_NULLABLE` | `是否允许NULL` | `NO` |
| `COLUMN_DEF` | `默认值` | `NULL` |

### 4.3 获取主键信息


**🔸 查询表的主键**
```java
public void getPrimaryKeys(Connection conn, String tableName) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    
    ResultSet primaryKeys = metaData.getPrimaryKeys(null, null, tableName);
    
    System.out.println("表 " + tableName + " 的主键信息：");
    while (primaryKeys.next()) {
        String columnName = primaryKeys.getString("COLUMN_NAME");
        String pkName = primaryKeys.getString("PK_NAME");
        short keySeq = primaryKeys.getShort("KEY_SEQ");
        
        System.out.printf("主键名: %s, 列名: %s, 序号: %d%n", 
                         pkName, columnName, keySeq);
    }
    primaryKeys.close();
}
```

---

## 5. 📊 获取索引信息


### 5.1 查询表的索引


**🔸 获取指定表的所有索引**
```java
public void getTableIndexes(Connection conn, String tableName) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    
    // 获取索引信息，参数：catalog, schema, table, unique, approximate
    ResultSet indexes = metaData.getIndexInfo(null, null, tableName, false, true);
    
    System.out.println("表 " + tableName + " 的索引信息：");
    System.out.println("索引名\t\t\t列名\t\t\t是否唯一\t\t索引类型");
    System.out.println("-----------------------------------------------------------------------");
    
    while (indexes.next()) {
        String indexName = indexes.getString("INDEX_NAME");
        String columnName = indexes.getString("COLUMN_NAME");
        boolean nonUnique = indexes.getBoolean("NON_UNIQUE");
        short type = indexes.getShort("TYPE");
        
        String uniqueStr = nonUnique ? "非唯一" : "唯一";
        String typeStr = getIndexTypeName(type);
        
        System.out.printf("%-20s %-20s %-10s %-10s%n", 
                         indexName, columnName, uniqueStr, typeStr);
    }
    indexes.close();
}

// 索引类型转换方法
private String getIndexTypeName(short type) {
    switch (type) {
        case DatabaseMetaData.tableIndexStatistic: return "统计信息";
        case DatabaseMetaData.tableIndexClustered: return "聚集索引";
        case DatabaseMetaData.tableIndexHashed: return "哈希索引";
        case DatabaseMetaData.tableIndexOther: return "其他索引";
        default: return "未知类型";
    }
}
```

### 5.2 外键关系信息


**🔸 查询表的外键关系**
```java
public void getForeignKeys(Connection conn, String tableName) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    
    // 获取导入的外键（当前表引用其他表）
    ResultSet importedKeys = metaData.getImportedKeys(null, null, tableName);
    
    System.out.println("表 " + tableName + " 引用的外键：");
    while (importedKeys.next()) {
        String pkTable = importedKeys.getString("PKTABLE_NAME");
        String pkColumn = importedKeys.getString("PKCOLUMN_NAME");
        String fkColumn = importedKeys.getString("FKCOLUMN_NAME");
        String fkName = importedKeys.getString("FK_NAME");
        
        System.out.printf("外键名: %s, 外键列: %s -> 主表: %s.%s%n", 
                         fkName, fkColumn, pkTable, pkColumn);
    }
    importedKeys.close();
}
```

---

## 6. ⚡ 动态SQL生成应用


### 6.1 根据表结构生成查询SQL


**🔸 自动生成SELECT语句**
```java
public String generateSelectSQL(Connection conn, String tableName) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    ResultSet columns = metaData.getColumns(null, null, tableName, null);
    
    StringBuilder sql = new StringBuilder("SELECT ");
    boolean first = true;
    
    while (columns.next()) {
        if (!first) {
            sql.append(", ");
        }
        sql.append(columns.getString("COLUMN_NAME"));
        first = false;
    }
    
    sql.append(" FROM ").append(tableName);
    columns.close();
    
    return sql.toString();
}
```

### 6.2 动态生成INSERT语句


**🔸 根据表结构生成INSERT模板**
```java
public String generateInsertSQL(Connection conn, String tableName) throws SQLException {
    DatabaseMetaData metaData = conn.getMetaData();
    ResultSet columns = metaData.getColumns(null, null, tableName, null);
    
    StringBuilder columnPart = new StringBuilder();
    StringBuilder valuePart = new StringBuilder();
    boolean first = true;
    
    while (columns.next()) {
        String columnName = columns.getString("COLUMN_NAME");
        
        if (!first) {
            columnPart.append(", ");
            valuePart.append(", ");
        }
        
        columnPart.append(columnName);
        valuePart.append("?"); // 预编译参数占位符
        first = false;
    }
    columns.close();
    
    return String.format("INSERT INTO %s (%s) VALUES (%s)", 
                        tableName, columnPart.toString(), valuePart.toString());
}
```

### 6.3 实体类生成器


**🔸 根据表结构生成Java实体类**
```java
public class EntityGenerator {
    
    public void generateEntityClass(Connection conn, String tableName) throws SQLException {
        DatabaseMetaData metaData = conn.getMetaData();
        ResultSet columns = metaData.getColumns(null, null, tableName, null);
        
        // 类名首字母大写
        String className = capitalize(tableName);
        
        System.out.println("public class " + className + " {");
        System.out.println();
        
        // 生成属性
        while (columns.next()) {
            String columnName = columns.getString("COLUMN_NAME");
            String dataType = columns.getString("TYPE_NAME");
            
            String javaType = sqlTypeToJavaType(dataType);
            String fieldName = toCamelCase(columnName);
            
            System.out.println("    private " + javaType + " " + fieldName + ";");
        }
        
        System.out.println();
        System.out.println("    // getter和setter方法...");
        System.out.println("}");
        
        columns.close();
    }
    
    // SQL类型转Java类型
    private String sqlTypeToJavaType(String sqlType) {
        switch (sqlType.toUpperCase()) {
            case "INT":
            case "INTEGER": return "Integer";
            case "VARCHAR":
            case "CHAR": return "String";
            case "DATETIME":
            case "TIMESTAMP": return "Date";
            case "DECIMAL": return "BigDecimal";
            default: return "Object";
        }
    }
    
    // 下划线转驼峰命名
    private String toCamelCase(String str) {
        String[] parts = str.split("_");
        StringBuilder camelCase = new StringBuilder(parts[0].toLowerCase());
        
        for (int i = 1; i < parts.length; i++) {
            camelCase.append(capitalize(parts[i]));
        }
        return camelCase.toString();
    }
    
    private String capitalize(String str) {
        return str.substring(0, 1).toUpperCase() + str.substring(1).toLowerCase();
    }
}
```

---

## 7. 🚀 实际应用场景


### 7.1 数据库文档生成器


**💡 应用描述**
```
用途：自动生成数据库结构文档
特点：
• 遍历所有表和列
• 生成HTML或Markdown格式文档
• 包含表关系图和字段说明
```

**🔧 简化实现思路**
```java
public class DatabaseDocGenerator {
    
    public void generateDoc(Connection conn) throws SQLException {
        DatabaseMetaData metaData = conn.getMetaData();
        
        System.out.println("# 数据库结构文档");
        System.out.println();
        
        // 获取所有表
        ResultSet tables = metaData.getTables(null, null, null, new String[]{"TABLE"});
        
        while (tables.next()) {
            String tableName = tables.getString("TABLE_NAME");
            generateTableDoc(conn, tableName);
        }
        tables.close();
    }
    
    private void generateTableDoc(Connection conn, String tableName) throws SQLException {
        System.out.println("## 表: " + tableName);
        System.out.println();
        System.out.println("| 列名 | 数据类型 | 长度 | 是否非空 | 默认值 | 备注 |");
        System.out.println("|------|----------|------|----------|--------|------|");
        
        DatabaseMetaData metaData = conn.getMetaData();
        ResultSet columns = metaData.getColumns(null, null, tableName, null);
        
        while (columns.next()) {
            String columnName = columns.getString("COLUMN_NAME");
            String dataType = columns.getString("TYPE_NAME");
            int size = columns.getInt("COLUMN_SIZE");
            String nullable = columns.getString("IS_NULLABLE");
            String defaultValue = columns.getString("COLUMN_DEF");
            String remarks = columns.getString("REMARKS");
            
            System.out.printf("| %s | %s | %d | %s | %s | %s |%n",
                    columnName, dataType, size, 
                    "NO".equals(nullable) ? "是" : "否",
                    defaultValue == null ? "-" : defaultValue,
                    remarks == null ? "-" : remarks);
        }
        columns.close();
        System.out.println();
    }
}
```

### 7.2 数据库比较工具


**💡 应用场景**
```
用途：比较两个数据库结构的差异
应用：
• 开发环境和生产环境对比
• 数据库版本升级检查
• 自动生成ALTER SQL脚本
```

### 7.3 通用查询界面生成


**💡 动态查询系统**
```
功能：
• 根据表结构生成查询表单
• 自动创建数据展示界面
• 支持动态添加查询条件

优势：
• 无需手工编写每个表的查询页面
• 表结构变化时界面自动适应
• 大大提高开发效率
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 元数据概念：描述数据结构的信息，包括表、列、索引等
🔸 DatabaseMetaData：JDBC提供的元数据获取接口
🔸 常用方法：getTables()、getColumns()、getPrimaryKeys()等
🔸 应用场景：动态SQL生成、实体类生成、文档生成
```

### 8.2 关键理解要点


**🔹 元数据的作用**
```
不是为了获取表中的数据（记录）
而是为了了解表的结构（字段、类型、约束等）
相当于获取数据库的"设计图纸"
```

**🔹 常见应用模式**
```
工具类应用：
• 数据库管理工具（如Navicat、phpMyAdmin）
• 代码生成器（如MyBatis Generator）
• 数据迁移工具

框架应用：
• ORM框架的表结构映射
• 动态查询系统
• 数据验证和校验
```

**🔹 性能注意事项**
```
获取元数据是IO密集操作：
• 尽量缓存获取的元数据信息
• 避免频繁调用元数据方法
• 可以在应用启动时一次性获取并存储
```

### 8.3 实际应用价值


**🎯 提高开发效率**
- **代码生成**：自动生成实体类、DAO类
- **动态查询**：根据表结构构建查询界面
- **文档生成**：自动生成数据库设计文档

**🔧 系统维护**
- **结构对比**：比较不同环境的数据库差异
- **版本管理**：跟踪数据库结构变化
- **自动化部署**：生成数据库升级脚本

### 8.4 学习建议


**📚 循序渐进**
```
第1步：理解元数据概念，练习基本获取方法
第2步：掌握表结构、索引信息的获取
第3步：尝试编写简单的动态SQL生成器
第4步：开发实际应用，如实体类生成器
```

**💡 实践项目**
- 开发一个简单的数据库文档生成器
- 编写表结构比较工具
- 创建动态查询界面
- 实现代码生成器功能

**核心记忆**：
- 元数据是描述数据库结构的信息
- DatabaseMetaData提供了丰富的方法获取各种结构信息  
- 元数据主要用于工具开发和框架实现
- 合理使用可以大大提高开发和维护效率