---
title: 1、Java与SQL类型映射
---
## 📚 目录

1. [类型映射基础概念](#1-类型映射基础概念)
2. [数值类型映射](#2-数值类型映射)
3. [字符串类型映射](#3-字符串类型映射)
4. [日期时间类型映射](#4-日期时间类型映射)
5. [布尔类型映射](#5-布尔类型映射)
6. [大数据类型映射](#6-大数据类型映射)
7. [类型转换实战技巧](#7-类型转换实战技巧)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔗 类型映射基础概念


### 1.1 什么是类型映射


**简单理解**：就像翻译一样，把Java语言的数据类型"翻译"成数据库能理解的类型。

```
比如说：
Java中的 int → 数据库中的 INT
Java中的 String → 数据库中的 VARCHAR
就像中文"你好" → 英文"Hello"一样的道理
```

**为什么需要类型映射？**
- Java是面向对象的编程语言，有自己的数据类型
- 数据库（如MySQL）也有自己的数据类型
- 两者需要"对话"，就必须找到对应关系

### 1.2 映射的工作原理


```
Java程序 ←→ JDBC驱动 ←→ 数据库

具体流程：
1. Java: int age = 25
2. JDBC: 自动转换成数据库认识的格式
3. 数据库: 存储为INT类型
4. 取出时反向转换
```

**📊 核心映射对应表**

| Java类型 | SQL类型 | 说明 | 使用场景 |
|---------|---------|------|----------|
| `int/Integer` | `INT` | 整数 | 年龄、数量 |
| `long/Long` | `BIGINT` | 长整数 | ID、时间戳 |
| `String` | `VARCHAR` | 字符串 | 姓名、描述 |
| `double/Double` | `DOUBLE` | 小数 | 价格、百分比 |
| `boolean/Boolean` | `TINYINT` | 布尔值 | 是否标记 |

---

## 2. 🔢 数值类型映射


### 2.1 整数类型详解


**int/Integer 映射**

```java
// Java代码示例
int age = 25;
Integer score = 98;

// 对应的SQL
// CREATE TABLE student (
//     age INT,
//     score INT
// );
```

**使用场景对比**：
```
🟢 适合用int的场景：
- 年龄: 0-120
- 成绩: 0-100  
- 数量: 商品库存

❌ 不适合用int的场景：
- 手机号: 会超出int范围
- 身份证号: 太长了
```

**long/Long 映射**

```java
// 什么时候用long？
long phoneNumber = 13800138000L;  // 手机号
long timestamp = System.currentTimeMillis();  // 时间戳
Long userId = 1000000001L;  // 用户ID

// 对应SQL: BIGINT类型
```

**💡 记忆技巧**：
- **int** = 普通整数（-21亿 到 +21亿）
- **long** = 超大整数（够用到天荒地老）

### 2.2 小数类型详解


**double/Double 处理**

```java
// 商品价格
double price = 99.99;
Double discount = 0.85;  // 8.5折

// 对应SQL: DOUBLE类型
```

**BigDecimal 精确小数**

```java
// 什么时候必须用BigDecimal？
import java.math.BigDecimal;

// 金融计算 - 必须精确
BigDecimal money = new BigDecimal("99.99");
BigDecimal tax = new BigDecimal("0.06");

// 对应SQL: DECIMAL(10,2) 类型
```

**⚠️ 重要提醒**：
```
💰 涉及钱的计算，一定要用BigDecimal！
原因：double有精度误差
错误例子：0.1 + 0.2 = 0.30000000000000004
正确做法：用BigDecimal保证精确计算
```

---

## 3. 📝 字符串类型映射


### 3.1 String与VARCHAR映射


**基础映射关系**：

```java
// Java字符串
String name = "张三";
String email = "zhangsan@example.com";

// 对应SQL定义
// name VARCHAR(50)    -- 姓名最多50字符
// email VARCHAR(100)  -- 邮箱最多100字符
```

**长度规划建议**：

| 字段类型 | 推荐长度 | 原因说明 |
|---------|---------|----------|
| **姓名** | `VARCHAR(50)` | 中文姓名一般2-4字，外国名字可能更长 |
| **手机号** | `VARCHAR(20)` | 考虑国际号码格式 |
| **邮箱** | `VARCHAR(100)` | 邮箱地址可能很长 |
| **密码** | `VARCHAR(255)` | 加密后的密码很长 |
| **地址** | `VARCHAR(500)` | 详细地址信息较长 |

### 3.2 字符编码处理


**中文字符问题**：

```java
// 设置数据库连接编码
String url = "jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf8";

// 这样确保中文正常显示
String chineseName = "李小明";  // 不会出现乱码
```

**📋 编码规范**：
```
✅ 推荐做法：
- 数据库: utf8mb4 编码
- 连接字符串: characterEncoding=utf8
- 表设计: VARCHAR(50) CHARACTER SET utf8mb4

❌ 避免问题：
- 不设置编码 → 中文乱码
- 长度设置太小 → 数据截断
```

---

## 4. 📅 日期时间类型映射


### 4.1 Date类型处理


**java.util.Date 映射**：

```java
import java.util.Date;
import java.sql.PreparedStatement;

// Java日期对象
Date birthday = new Date();  // 当前时间
Date createTime = new Date(System.currentTimeMillis());

// 插入数据库
PreparedStatement ps = conn.prepareStatement(
    "INSERT INTO user (name, birthday, create_time) VALUES (?, ?, ?)"
);
ps.setString(1, "张三");
ps.setDate(2, new java.sql.Date(birthday.getTime()));      // 只有日期
ps.setTimestamp(3, new java.sql.Timestamp(createTime.getTime())); // 日期+时间
```

**类型对应关系**：

```
Java类型                    SQL类型        存储内容
─────────────────────────────────────────────────
java.util.Date         →   DATE          2024-01-15 (只有日期)
java.util.Date         →   DATETIME      2024-01-15 14:30:25 (日期+时间)
java.util.Date         →   TIMESTAMP     2024-01-15 14:30:25 (带时区)
java.sql.Date          →   DATE          专门存日期
java.sql.Time          →   TIME          专门存时间
java.sql.Timestamp     →   TIMESTAMP     完整时间戳
```

### 4.2 实用日期处理


**格式化日期显示**：

```java
import java.text.SimpleDateFormat;

// 从数据库读取日期并格式化显示
Date dbDate = rs.getTimestamp("create_time");
SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
String formatDate = sdf.format(dbDate);

System.out.println("创建时间：" + formatDate);  // 输出：2024-01-15 14:30:25
```

**💡 实用技巧**：
```
🕐 什么时候用哪种类型？

DATE: 生日、入学日期 (只关心哪一天)
DATETIME: 创建时间、更新时间 (需要具体时刻)  
TIMESTAMP: 系统日志、操作记录 (需要时区信息)
```

---

## 5. ✅ 布尔类型映射


### 5.1 boolean处理方式


**Java布尔值的数据库存储**：

```java
// Java中的布尔值
boolean isActive = true;   // 用户是否激活
boolean isDeleted = false; // 是否删除
Boolean isPremium = null;  // VIP用户(可为空)
```

**数据库存储方案**：

| 存储方式 | SQL类型 | 存储值 | 优缺点 |
|---------|---------|--------|--------|
| **数字方式** | `TINYINT(1)` | `0/1` | 节省空间，查询快 |
| **字符方式** | `CHAR(1)` | `'Y'/'N'` | 直观易懂 |
| **字符方式** | `VARCHAR(10)` | `'true'/'false'` | 最直观，占空间大 |

**推荐做法**：

```java
// 数据库表设计 (推荐)
// CREATE TABLE user (
//     id INT PRIMARY KEY,
//     name VARCHAR(50),
//     is_active TINYINT(1) DEFAULT 1,    -- 1=激活, 0=未激活
//     is_deleted TINYINT(1) DEFAULT 0    -- 1=已删除, 0=正常
// );

// Java代码处理
PreparedStatement ps = conn.prepareStatement(
    "INSERT INTO user (name, is_active, is_deleted) VALUES (?, ?, ?)"
);
ps.setString(1, "张三");
ps.setInt(2, isActive ? 1 : 0);    // boolean转int
ps.setInt(3, isDeleted ? 1 : 0);
```

### 5.2 布尔值查询技巧


```java
// 查询激活用户
ResultSet rs = stmt.executeQuery("SELECT * FROM user WHERE is_active = 1");

while(rs.next()) {
    String name = rs.getString("name");
    boolean active = rs.getInt("is_active") == 1;  // int转boolean
    
    System.out.println(name + (active ? " (激活)" : " (未激活)"));
}
```

**🎯 实用规范**：
```
📝 布尔字段命名规范：
✅ is_active, is_deleted, is_premium
✅ has_permission, can_edit, can_view  
✅ enable_notification, allow_comment

❌ active, deleted, premium (不够清晰)
```

---

## 6. 📦 大数据类型映射


### 6.1 大文本处理


**TEXT类型应用**：

```java
// 存储长文本内容
String articleContent = "这是一篇很长很长的文章内容...";
String userDescription = "用户的个人简介，可能有很多文字...";

// 对应SQL类型选择
// description TEXT           -- 最多65535字符
// article_content LONGTEXT   -- 超大文本，最多4GB
```

**文本长度选择指南**：

```
📏 文本类型选择：

VARCHAR(255)     →  短文本：姓名、标题、摘要
TEXT             →  中等文本：评论、简介 (64KB)
MEDIUMTEXT       →  长文本：文章内容 (16MB)  
LONGTEXT         →  超长文本：小说、日志 (4GB)
```

### 6.2 二进制数据处理


**BLOB类型说明**：

```java
// 存储图片、文档等二进制数据
byte[] imageData = Files.readAllBytes(Paths.get("photo.jpg"));

PreparedStatement ps = conn.prepareStatement(
    "INSERT INTO user_photo (user_id, photo_data) VALUES (?, ?)"
);
ps.setInt(1, userId);
ps.setBytes(2, imageData);  // 存储二进制数据
```

**⚠️ 重要建议**：
```
🚫 不推荐在数据库存储大文件！

原因：
- 数据库文件会变得巨大
- 备份和恢复变慢
- 查询性能下降

💡 推荐做法：
- 文件存在服务器目录
- 数据库只存文件路径
- 例如：photo_path VARCHAR(255) 存 "/images/user123.jpg"
```

---

## 7. 🛠️ 类型转换实战技巧


### 7.1 获取数据时的类型处理


**安全的类型获取**：

```java
// 从ResultSet安全获取各种类型数据
ResultSet rs = stmt.executeQuery("SELECT * FROM user");

while(rs.next()) {
    // 字符串类型 - 最安全
    String name = rs.getString("name");
    
    // 数值类型 - 检查是否为null
    Integer age = rs.getObject("age", Integer.class);  // 新方法，推荐
    int ageOld = rs.getInt("age");  // 传统方法，null会变0
    
    // 布尔类型
    boolean active = rs.getBoolean("is_active");  // 自动转换
    
    // 日期类型
    Date createTime = rs.getTimestamp("create_time");
}
```

### 7.2 类型转换常见问题


**数字转换问题**：

```java
// ❌ 错误做法
int salary = rs.getInt("salary");  // 如果数据库中salary为null，这里会得到0

// ✅ 正确做法  
Integer salary = rs.getObject("salary", Integer.class);
if (salary != null) {
    System.out.println("工资：" + salary);
} else {
    System.out.println("工资信息未填写");
}
```

**字符串转换问题**：

```java
// 处理可能为null的字符串
String email = rs.getString("email");
if (email != null && !email.trim().isEmpty()) {
    System.out.println("邮箱：" + email);
} else {
    System.out.println("未设置邮箱");
}
```

### 7.3 PreparedStatement类型设置


**设置参数的最佳实践**：

```java
PreparedStatement ps = conn.prepareStatement(
    "INSERT INTO user (name, age, salary, is_active, create_time) VALUES (?, ?, ?, ?, ?)"
);

// 字符串参数
ps.setString(1, userName);

// 整数参数 (处理null值)
if (userAge != null) {
    ps.setInt(2, userAge);
} else {
    ps.setNull(2, java.sql.Types.INTEGER);
}

// 小数参数
ps.setBigDecimal(3, salary);

// 布尔参数
ps.setBoolean(4, isActive);

// 日期参数
ps.setTimestamp(5, new Timestamp(System.currentTimeMillis()));
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的映射关系


```
🔸 基础数值类型：
   int/Integer → INT (普通整数)
   long/Long → BIGINT (超大整数，如手机号、ID)
   double/Double → DOUBLE (小数，如价格)
   BigDecimal → DECIMAL (精确小数，金融计算)

🔸 文本类型：
   String → VARCHAR(长度) (普通文本)
   String → TEXT (长篇文本)

🔸 布尔类型：
   boolean/Boolean → TINYINT(1) (推荐用0/1表示)

🔸 日期类型：
   Date → DATE/DATETIME/TIMESTAMP
```

### 8.2 实用开发规范


**📝 字段长度规划**：
- 姓名：`VARCHAR(50)`
- 手机号：`VARCHAR(20)`  
- 邮箱：`VARCHAR(100)`
- 密码：`VARCHAR(255)` (加密后)
- 地址：`VARCHAR(500)`

**🎯 类型选择原则**：
```
选择原则：
1. 够用就行：不要过度设计
2. 考虑扩展：预留一定空间
3. 性能优先：数值比字符串查询快
4. 精确为先：金钱用BigDecimal
```

**💡 常见错误避免**：
```
❌ 避免这些错误：
- 手机号用int (会溢出)
- 金额用double (精度问题) 
- 中文不设编码 (乱码)
- 忘记处理null值 (空指针异常)

✅ 正确做法：
- 手机号用String/VARCHAR
- 金额用BigDecimal/DECIMAL  
- 连接设置utf8编码
- 从数据库取值时检查null
```

### 8.3 实战记忆口诀


```
类型映射记忆法：

📱 号码用字符串，int装不下
💰 钱用BigDecimal，精确不出差  
📅 日期有三种，按需求来选择
✅ 布尔存数字，0假1真清晰
📝 文本看长度，VARCHAR够用就行
```

**核心思想**：
- 理解Java和数据库类型的对应关系
- 根据实际业务需求选择合适类型
- 处理数据时要考虑null值情况  
- 涉及金钱计算必须用精确类型
- 中文数据要正确设置编码