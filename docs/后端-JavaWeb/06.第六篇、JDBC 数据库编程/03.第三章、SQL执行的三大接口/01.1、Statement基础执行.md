---
title: 1、Statement基础执行
---
## 📚 目录

1. [Statement接口概述](#1-Statement接口概述)
2. [Statement接口的创建](#2-Statement接口的创建)
3. [executeQuery()查询方法](#3-executeQuery查询方法)
4. [executeUpdate()更新方法](#4-executeUpdate更新方法)
5. [execute()通用执行方法](#5-execute通用执行方法)
6. [DDL与DML语句执行](#6-DDL与DML语句执行)
7. [实战应用案例](#7-实战应用案例)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 Statement接口概述


### 1.1 什么是Statement接口


**💡 简单理解**：Statement就像是你和数据库之间的"传话筒"，你想对数据库说什么（执行什么SQL），都要通过它来传达。

```
你的Java程序 ──SQL语句──> Statement接口 ──传递──> 数据库
              <──结果──                <──执行──
```

**🔸 Statement的本质作用**
- **SQL语句载体**：把你写的SQL语句包装起来
- **执行器**：负责把SQL发送给数据库执行
- **结果接收器**：接收数据库返回的结果

### 1.2 Statement在JDBC中的位置


```
JDBC编程流程：
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ 加载驱动     │ -> │ 获取连接     │ -> │ 创建Statement│ -> │ 执行SQL     │
│ DriverManager│    │ Connection  │    │ Statement   │    │ 得到结果     │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

**🔹 为什么需要Statement**
- **安全传输**：确保SQL语句正确传递到数据库
- **结果处理**：统一处理各种SQL的执行结果
- **资源管理**：管理数据库连接资源

---

## 2. 🔧 Statement接口的创建


### 2.1 创建Statement对象


**💡 基本创建方式**

```java
// 前提：已经获得数据库连接Connection对象
Connection conn = DriverManager.getConnection(url, username, password);

// 创建Statement对象
Statement stmt = conn.createStatement();
```

**🔸 创建过程详解**
1. **必须有连接**：Statement必须从Connection对象创建
2. **一次性使用**：通常用完就关闭，需要时重新创建
3. **轻量级对象**：创建开销很小，不用担心性能问题

### 2.2 完整的创建示例


```java
public class StatementDemo {
    public static void main(String[] args) {
        Connection conn = null;
        Statement stmt = null;
        
        try {
            // 1. 获取数据库连接
            conn = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/testdb", 
                "root", 
                "password"
            );
            
            // 2. 创建Statement对象
            stmt = conn.createStatement();
            
            // 3. 使用Statement执行SQL（后面详细讲解）
            // ...
            
        } catch (SQLException e) {
            e.printStackTrace();
        } finally {
            // 4. 关闭资源
            try {
                if (stmt != null) stmt.close();
                if (conn != null) conn.close();
            } catch (SQLException e) {
                e.printStackTrace();
            }
        }
    }
}
```

---

## 3. 🔍 executeQuery()查询方法


### 3.1 executeQuery方法的作用


**💡 专门用来查询数据**
- **作用**：执行SELECT语句，从数据库中查询数据
- **返回值**：ResultSet结果集对象
- **使用场景**：只要是查询数据，就用这个方法

### 3.2 executeQuery的工作流程


```
Java程序                    数据库
   |                         |
   |--SELECT语句------------->|
   |                         |执行查询
   |<--ResultSet结果集--------|
   |                         |
   |遍历ResultSet获取数据      |
```

### 3.3 实际使用示例


**假设有学生表**：
```sql
CREATE TABLE student (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    grade VARCHAR(10)
);
```

**Java查询代码**：
```java
public void queryStudents() {
    try {
        Statement stmt = conn.createStatement();
        
        // 执行查询SQL
        String sql = "SELECT id, name, age, grade FROM student";
        ResultSet rs = stmt.executeQuery(sql);
        
        // 处理查询结果
        while (rs.next()) {
            int id = rs.getInt("id");
            String name = rs.getString("name");
            int age = rs.getInt("age");
            String grade = rs.getString("grade");
            
            System.out.println("学生ID：" + id + 
                             "，姓名：" + name + 
                             "，年龄：" + age + 
                             "，年级：" + grade);
        }
        
        rs.close();
        stmt.close();
    } catch (SQLException e) {
        e.printStackTrace();
    }
}
```

### 3.4 executeQuery使用注意点


| 注意事项 | 说明 | 示例 |
|---------|------|------|
| **只能执行SELECT** | 不能执行增删改语句 | ❌ `INSERT INTO...` |
| **必须处理ResultSet** | 查询结果需要遍历获取 | `while(rs.next())` |
| **及时关闭资源** | ResultSet用完要关闭 | `rs.close()` |
| **SQL语法正确** | 确保SELECT语句无误 | 表名、字段名正确 |

---

## 4. ⚡ executeUpdate()更新方法


### 4.1 executeUpdate方法的作用


**💡 专门用来修改数据**
- **作用**：执行INSERT、UPDATE、DELETE语句
- **返回值**：int类型，表示受影响的行数
- **使用场景**：任何需要改变数据库内容的操作

### 4.2 executeUpdate支持的SQL类型


```
executeUpdate可以执行：
┌─────────────┐
│ INSERT      │ ← 插入新数据
├─────────────┤
│ UPDATE      │ ← 修改现有数据  
├─────────────┤
│ DELETE      │ ← 删除数据
├─────────────┤
│ CREATE TABLE│ ← 创建表
├─────────────┤
│ DROP TABLE  │ ← 删除表
└─────────────┘
```

### 4.3 实际使用示例


**插入数据示例**：
```java
public void insertStudent(String name, int age, String grade) {
    try {
        Statement stmt = conn.createStatement();
        
        String sql = "INSERT INTO student(name, age, grade) VALUES('" 
                    + name + "', " + age + ", '" + grade + "')";
        
        int rows = stmt.executeUpdate(sql);
        
        if (rows > 0) {
            System.out.println("插入成功，影响了 " + rows + " 行数据");
        } else {
            System.out.println("插入失败");
        }
        
        stmt.close();
    } catch (SQLException e) {
        e.printStackTrace();
    }
}
```

**更新数据示例**：
```java
public void updateStudentAge(int id, int newAge) {
    try {
        Statement stmt = conn.createStatement();
        
        String sql = "UPDATE student SET age = " + newAge + " WHERE id = " + id;
        int rows = stmt.executeUpdate(sql);
        
        System.out.println("更新了 " + rows + " 行数据");
        stmt.close();
    } catch (SQLException e) {
        e.printStackTrace();
    }
}
```

**删除数据示例**：
```java
public void deleteStudent(int id) {
    try {
        Statement stmt = conn.createStatement();
        
        String sql = "DELETE FROM student WHERE id = " + id;
        int rows = stmt.executeUpdate(sql);
        
        if (rows > 0) {
            System.out.println("删除成功，删除了 " + rows + " 行数据");
        } else {
            System.out.println("没有找到对应的学生记录");
        }
        
        stmt.close();
    } catch (SQLException e) {
        e.printStackTrace();
    }
}
```

### 4.4 返回值的含义


| 返回值 | 含义 | 示例场景 |
|-------|------|---------|
| **正数** | 实际影响的行数 | 更新了3行数据，返回3 |
| **0** | 没有行受到影响 | 删除一个不存在的记录 |
| **-1** | 某些特殊情况 | DDL语句（如CREATE TABLE） |

---

## 5. 🔄 execute()通用执行方法


### 5.1 execute方法的特点


**💡 万能执行器**
- **作用**：可以执行任何类型的SQL语句
- **返回值**：boolean类型，true表示有结果集，false表示没有结果集
- **灵活性**：当你不确定SQL类型时使用

### 5.2 execute的工作原理


```
execute方法执行流程：
SQL语句 ──> execute() ──> boolean返回值
   │                        │
   │                        ├─ true：有ResultSet（SELECT语句）
   │                        └─ false：返回更新行数（INSERT/UPDATE/DELETE）
   │
   └─ 根据返回值决定如何获取结果：
      ├─ getResultSet() 获取查询结果
      └─ getUpdateCount() 获取影响行数
```

### 5.3 execute使用示例


```java
public void executeAnySQL(String sql) {
    try {
        Statement stmt = conn.createStatement();
        
        // 执行SQL语句
        boolean hasResultSet = stmt.execute(sql);
        
        if (hasResultSet) {
            // 有结果集，说明是查询语句
            ResultSet rs = stmt.getResultSet();
            while (rs.next()) {
                // 处理查询结果
                System.out.println("查询结果：" + rs.getString(1));
            }
            rs.close();
        } else {
            // 没有结果集，说明是更新语句
            int updateCount = stmt.getUpdateCount();
            System.out.println("影响行数：" + updateCount);
        }
        
        stmt.close();
    } catch (SQLException e) {
        e.printStackTrace();
    }
}
```

### 5.4 三种执行方法对比


| 方法 | 适用SQL类型 | 返回值类型 | 使用场景 |
|------|-----------|----------|---------|
| **executeQuery** | `SELECT` | `ResultSet` | 🔍 确定是查询时用 |
| **executeUpdate** | `INSERT/UPDATE/DELETE/DDL` | `int` | ⚡ 确定是修改时用 |
| **execute** | `任何SQL` | `boolean` | 🔄 SQL类型不确定时用 |

---

## 6. 📋 DDL与DML语句执行


### 6.1 DDL和DML的区别


**💡 通俗理解**
- **DDL（数据定义语言）**：负责"搭建房子"——创建表、修改表结构
- **DML（数据操作语言）**：负责"管理房子里的东西"——增删改查数据

```
数据库操作分类：
┌─────────────────┐    ┌─────────────────┐
│ DDL - 结构操作   │    │ DML - 数据操作   │
├─────────────────┤    ├─────────────────┤
│ CREATE TABLE    │    │ INSERT 插入     │
│ ALTER TABLE     │    │ UPDATE 修改     │
│ DROP TABLE      │    │ DELETE 删除     │
│ CREATE INDEX    │    │ SELECT 查询     │
└─────────────────┘    └─────────────────┘
```

### 6.2 DDL语句执行示例


**创建表**：
```java
public void createStudentTable() {
    try {
        Statement stmt = conn.createStatement();
        
        String sql = "CREATE TABLE student (" +
                    "id INT PRIMARY KEY AUTO_INCREMENT, " +
                    "name VARCHAR(50) NOT NULL, " +
                    "age INT, " +
                    "grade VARCHAR(10)" +
                    ")";
        
        int result = stmt.executeUpdate(sql);
        System.out.println("创建表成功！");
        
        stmt.close();
    } catch (SQLException e) {
        System.out.println("创建表失败：" + e.getMessage());
    }
}
```

**修改表结构**：
```java
public void addColumnToTable() {
    try {
        Statement stmt = conn.createStatement();
        
        // 给student表添加一个email字段
        String sql = "ALTER TABLE student ADD COLUMN email VARCHAR(100)";
        
        stmt.executeUpdate(sql);
        System.out.println("添加字段成功！");
        
        stmt.close();
    } catch (SQLException e) {
        System.out.println("添加字段失败：" + e.getMessage());
    }
}
```

### 6.3 DML语句执行特点


**🔸 DML操作的共同点**
- 都是对表中的数据进行操作
- 可能影响多行数据
- 返回值表示实际影响的行数
- 支持WHERE条件筛选

```java
// DML操作示例汇总
public class DMLOperations {
    
    // 插入数据
    public void insertData() {
        String sql = "INSERT INTO student(name, age, grade) VALUES('张三', 20, '大二')";
        int rows = stmt.executeUpdate(sql);
    }
    
    // 更新数据
    public void updateData() {
        String sql = "UPDATE student SET age = 21 WHERE name = '张三'";
        int rows = stmt.executeUpdate(sql);
    }
    
    // 删除数据
    public void deleteData() {
        String sql = "DELETE FROM student WHERE age < 18";
        int rows = stmt.executeUpdate(sql);
    }
    
    // 查询数据
    public void selectData() {
        String sql = "SELECT * FROM student WHERE grade = '大二'";
        ResultSet rs = stmt.executeQuery(sql);
    }
}
```

---

## 7. 🚀 实战应用案例


### 7.1 学生管理系统案例


**完整的学生CRUD操作**：

```java
public class StudentManager {
    private Connection conn;
    
    public StudentManager(Connection conn) {
        this.conn = conn;
    }
    
    // 添加学生
    public boolean addStudent(String name, int age, String grade) {
        String sql = "INSERT INTO student(name, age, grade) VALUES('" 
                    + name + "', " + age + ", '" + grade + "')";
        try {
            Statement stmt = conn.createStatement();
            int rows = stmt.executeUpdate(sql);
            stmt.close();
            return rows > 0;
        } catch (SQLException e) {
            e.printStackTrace();
            return false;
        }
    }
    
    // 查询所有学生
    public void showAllStudents() {
        String sql = "SELECT * FROM student ORDER BY id";
        try {
            Statement stmt = conn.createStatement();
            ResultSet rs = stmt.executeQuery(sql);
            
            System.out.println("=== 学生列表 ===");
            while (rs.next()) {
                System.out.printf("ID:%d 姓名:%s 年龄:%d 年级:%s%n",
                    rs.getInt("id"),
                    rs.getString("name"),
                    rs.getInt("age"),
                    rs.getString("grade")
                );
            }
            
            rs.close();
            stmt.close();
        } catch (SQLException e) {
            e.printStackTrace();
        }
    }
    
    // 按年级查询学生
    public void findStudentsByGrade(String grade) {
        String sql = "SELECT * FROM student WHERE grade = '" + grade + "'";
        try {
            Statement stmt = conn.createStatement();
            ResultSet rs = stmt.executeQuery(sql);
            
            System.out.println("=== " + grade + " 学生列表 ===");
            boolean found = false;
            while (rs.next()) {
                found = true;
                System.out.printf("ID:%d 姓名:%s 年龄:%d%n",
                    rs.getInt("id"),
                    rs.getString("name"),
                    rs.getInt("age")
                );
            }
            
            if (!found) {
                System.out.println("没有找到 " + grade + " 的学生");
            }
            
            rs.close();
            stmt.close();
        } catch (SQLException e) {
            e.printStackTrace();
        }
    }
}
```

### 7.2 使用建议和最佳实践


**⚠️ 重要提醒**
- **SQL注入风险**：直接拼接字符串容易被注入攻击
- **资源管理**：Statement和ResultSet用完必须关闭
- **异常处理**：数据库操作要做好异常处理

**🔸 更安全的写法**（后续会学到PreparedStatement）：
```java
// 现在的写法（有风险）
String sql = "SELECT * FROM student WHERE name = '" + name + "'";

// 更安全的写法（使用PreparedStatement）
String sql = "SELECT * FROM student WHERE name = ?";
PreparedStatement pstmt = conn.prepareStatement(sql);
pstmt.setString(1, name);
```

---

## 8. 📚 核心要点总结


### 8.1 Statement三大核心方法


```
Statement执行方法选择指南：
┌─────────────────┐
│ 确定是查询？     │
│ SELECT语句      │ ──YES─> executeQuery()
└─────────────────┘
         │ NO
         ▼
┌─────────────────┐
│ 确定是增删改？   │
│ INSERT/UPDATE/  │ ──YES─> executeUpdate()  
│ DELETE/DDL      │
└─────────────────┘
         │ NO
         ▼
┌─────────────────┐
│ SQL类型不确定？  │ ──YES─> execute()
│ 动态SQL语句     │
└─────────────────┘
```

### 8.2 必须掌握的核心概念


**🔸 Statement的本质**
- 数据库执行器，负责传递SQL语句
- 从Connection对象创建
- 用完必须关闭释放资源

**🔸 三大执行方法**
- `executeQuery()`：专门查询，返回ResultSet
- `executeUpdate()`：专门修改，返回影响行数
- `execute()`：通用执行，返回boolean判断结果类型

**🔸 SQL语句分类**
- DDL：操作数据库结构（CREATE、ALTER、DROP）
- DML：操作数据内容（SELECT、INSERT、UPDATE、DELETE）

### 8.3 实际应用要点


| 使用场景 | 推荐方法 | 关键注意点 |
|---------|---------|-----------|
| **查询数据** | `executeQuery` | 处理ResultSet结果集 |
| **插入数据** | `executeUpdate` | 检查返回的影响行数 |
| **更新数据** | `executeUpdate` | 使用WHERE条件避免全表更新 |
| **删除数据** | `executeUpdate` | 使用WHERE条件避免全表删除 |
| **创建表** | `executeUpdate` | DDL语句，返回值通常是0 |
| **动态SQL** | `execute` | 根据返回值判断如何处理结果 |

**💡 记忆口诀**
```
Statement三兄弟，各有专长要记清：
executeQuery专查询，ResultSet结果要处理；
executeUpdate改数据，返回行数看效果；
execute最灵活，什么SQL都能行！
```

**🚨 安全提醒**：
- Statement直接拼接SQL存在注入风险
- 实际项目中更推荐使用PreparedStatement
- 本章重点是理解基础执行原理和方法使用