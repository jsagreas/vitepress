---
title: 2、留言板系统
---
## 📚 目录

1. [留言板系统概述](#1-留言板系统概述)
2. [留言发布功能实现](#2-留言发布功能实现)
3. [留言列表展示](#3-留言列表展示)
4. [分页显示实现](#4-分页显示实现)
5. [时间格式化处理](#5-时间格式化处理)
6. [留言管理操作](#6-留言管理操作)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 💬 留言板系统概述


### 1.1 什么是留言板系统


**简单理解**：留言板就像学校公告栏或者论坛，用户可以发表留言，其他人可以看到这些留言。

```
现实生活中的留言板：
┌─────────────────────────┐
│  学校公告栏              │
│                         │
│  张三：明天期末考试加油！  │
│  李四：图书馆新书到了     │  
│  王五：周末一起踢球       │
└─────────────────────────┘
```

**网页留言板的功能**：
- **🖊️ 发布留言**：用户可以写下自己想说的话
- **👁️ 查看留言**：显示所有人的留言内容
- **📄 分页浏览**：留言太多时分页显示
- **🗑️ 管理留言**：可以删除不当留言

### 1.2 系统架构设计


**三层架构模式**：
```
用户界面层 (JSP页面)
     ↓
业务逻辑层 (Java Bean)
     ↓
数据存储层 (数据库/文件)
```

**文件结构**：
```
留言板项目/
├── jsp/
│   ├── messageForm.jsp    ← 发布留言的表单页面
│   ├── messageList.jsp    ← 显示留言列表页面
│   └── messageAdmin.jsp   ← 管理留言页面
├── bean/
│   ├── Message.java       ← 留言信息类
│   └── MessageDAO.java    ← 数据访问类
└── data/
    └── messages.txt       ← 存储留言数据
```

---

## 2. ✍️ 留言发布功能实现


### 2.1 留言信息类设计


**Message.java - 留言的基本信息**：

```java
// 留言类 - 就像一张留言纸条
public class Message {
    private String author;    // 留言人姓名
    private String content;   // 留言内容
    private String postTime;  // 发布时间
    
    // 构造方法 - 创建留言对象
    public Message(String author, String content) {
        this.author = author;
        this.content = content;
        this.postTime = new Date().toString(); // 自动记录当前时间
    }
    
    // getter和setter方法
    // ... (省略具体实现)
}
```

> **💡 理解要点**：Message类就像定义留言纸条的格式，规定每张纸条必须包含：作者、内容、时间。

### 2.2 留言发布表单


**messageForm.jsp - 用户填写留言的页面**：

```jsp
<!-- 留言发布表单 -->
<div class="message-form-container">
    <h2>📝 发布留言</h2>
    
    <form action="submitMessage.jsp" method="post">
        <div class="form-group">
            <label>👤 您的姓名：</label>
            <input type="text" name="author" required 
                   placeholder="请输入您的姓名">
        </div>
        
        <div class="form-group">
            <label>💬 留言内容：</label>
            <textarea name="content" rows="4" required 
                      placeholder="请输入留言内容..."></textarea>
        </div>
        
        <button type="submit" class="submit-btn">🚀 发布留言</button>
    </form>
</div>
```

### 2.3 留言处理逻辑


**submitMessage.jsp - 处理提交的留言**：

```jsp
<%
// 获取表单提交的数据
String author = request.getParameter("author");
String content = request.getParameter("content");

// 简单验证 - 确保必要信息不为空
if(author != null && content != null && 
   !author.trim().isEmpty() && !content.trim().isEmpty()) {
    
    // 创建留言对象
    Message msg = new Message(author.trim(), content.trim());
    
    // 保存留言到文件
    MessageDAO dao = new MessageDAO();
    boolean success = dao.saveMessage(msg);
    
    if(success) {
        out.println("<script>alert('留言发布成功！'); window.location='messageList.jsp';</script>");
    }
} else {
    out.println("<script>alert('请填写完整信息！'); history.back();</script>");
}
%>
```

> **⚠️ 注意事项**：
> - `trim()`方法去除首尾空格，防止用户提交空内容
> - 使用JavaScript跳转页面，提升用户体验
> - 简单的表单验证确保数据完整性

---

## 3. 📋 留言列表展示


### 3.1 数据访问类实现


**MessageDAO.java - 负责文件操作的工具类**：

```java
public class MessageDAO {
    private String dataFile = "messages.txt"; // 数据文件路径
    
    // 获取所有留言
    public List<Message> getAllMessages() {
        List<Message> messages = new ArrayList<>();
        
        try(BufferedReader reader = new BufferedReader(new FileReader(dataFile))) {
            String line;
            while((line = reader.readLine()) != null) {
                // 解析每行数据：作者|内容|时间
                String[] parts = line.split("\\|");
                if(parts.length == 3) {
                    Message msg = new Message(parts[0], parts[1]);
                    msg.setPostTime(parts[2]);
                    messages.add(msg);
                }
            }
        } catch(IOException e) {
            System.out.println("读取留言文件出错：" + e.getMessage());
        }
        
        return messages;
    }
}
```

> **🔍 工作原理**：就像从文件柜里取出所有留言纸条，然后整理成一个列表。

### 3.2 留言列表页面


**messageList.jsp - 显示所有留言**：

```jsp
<%
// 获取留言数据
MessageDAO dao = new MessageDAO();
List<Message> messages = dao.getAllMessages();
%>

<div class="message-list-container">
    <h2>💬 留言列表 (共 <%= messages.size() %> 条)</h2>
    
    <% if(messages.isEmpty()) { %>
        <div class="no-message">
            <p>😊 还没有留言，来发布第一条吧！</p>
            <a href="messageForm.jsp" class="btn">📝 发布留言</a>
        </div>
    <% } else { %>
        <div class="message-grid">
            <% for(Message msg : messages) { %>
                <div class="message-card">
                    <div class="message-header">
                        <span class="author">👤 <%= msg.getAuthor() %></span>
                        <span class="time">🕒 <%= msg.getPostTime() %></span>
                    </div>
                    <div class="message-content">
                        <%= msg.getContent() %>
                    </div>
                </div>
            <% } %>
        </div>
    <% } %>
</div>
```

> **💡 设计思路**：
> - 先检查是否有留言，没有留言时显示提示信息
> - 用卡片式布局让每条留言更清晰
> - 显示留言总数让用户了解规模

---

## 4. 📄 分页显示实现


### 4.1 分页原理解释


**分页的作用**：就像把一本厚书分成多页，方便阅读。

```
留言总数：100条
每页显示：10条
总页数：10页

第1页：显示第1-10条留言
第2页：显示第11-20条留言
第3页：显示第21-30条留言
...以此类推
```

**分页关键参数**：
- **`currentPage`**：当前是第几页
- **`pageSize`**：每页显示多少条
- **`totalCount`**：留言总数
- **`totalPages`**：总共多少页

### 4.2 分页工具类


**PageHelper.java - 分页计算工具**：

```java
public class PageHelper {
    private int currentPage = 1;    // 当前页码
    private int pageSize = 5;       // 每页显示条数
    private int totalCount = 0;     // 总记录数
    
    public PageHelper(int currentPage, int pageSize, int totalCount) {
        this.currentPage = Math.max(1, currentPage); // 确保页码不小于1
        this.pageSize = pageSize;
        this.totalCount = totalCount;
    }
    
    // 计算总页数
    public int getTotalPages() {
        return (int) Math.ceil((double) totalCount / pageSize);
    }
    
    // 计算当前页的起始位置
    public int getStartIndex() {
        return (currentPage - 1) * pageSize;
    }
}
```

### 4.3 分页显示实现


**messageList.jsp - 带分页的留言列表**：

```jsp
<%
// 获取分页参数
String pageParam = request.getParameter("page");
int currentPage = (pageParam == null) ? 1 : Integer.parseInt(pageParam);
int pageSize = 5; // 每页5条留言

// 获取留言数据
MessageDAO dao = new MessageDAO();
List<Message> allMessages = dao.getAllMessages();
int totalCount = allMessages.size();

// 创建分页助手
PageHelper pageHelper = new PageHelper(currentPage, pageSize, totalCount);

// 获取当前页的留言
int start = pageHelper.getStartIndex();
int end = Math.min(start + pageSize, totalCount);
List<Message> currentMessages = allMessages.subList(start, end);
%>

<!-- 分页导航 -->
<div class="pagination">
    <% if(currentPage > 1) { %>
        <a href="?page=<%= currentPage - 1 %>" class="page-btn">⬅️ 上一页</a>
    <% } %>
    
    <span class="page-info">
        第 <%= currentPage %> 页 / 共 <%= pageHelper.getTotalPages() %> 页
    </span>
    
    <% if(currentPage < pageHelper.getTotalPages()) { %>
        <a href="?page=<%= currentPage + 1 %>" class="page-btn">下一页 ➡️</a>
    <% } %>
</div>
```

> **🎯 实现要点**：
> - 使用`subList()`方法获取指定范围的留言
> - 分页导航只在需要时显示上一页/下一页按钮
> - 通过URL参数传递页码信息

---

## 5. ⏰ 时间格式化处理


### 5.1 时间显示的重要性


**为什么要格式化时间**：
```
原始时间：2025-01-25T10:30:45.123
用户友好：2025年1月25日 10:30
相对时间：2分钟前
```

### 5.2 时间工具类


**TimeFormatter.java - 时间格式化工具**：

```java
public class TimeFormatter {
    
    // 格式化为友好显示
    public static String formatFriendly(Date date) {
        SimpleDateFormat sdf = new SimpleDateFormat("yyyy年MM月dd日 HH:mm");
        return sdf.format(date);
    }
    
    // 计算相对时间 (多少分钟前)
    public static String getRelativeTime(Date date) {
        long diff = System.currentTimeMillis() - date.getTime();
        long minutes = diff / (1000 * 60);
        
        if(minutes < 1) {
            return "刚刚";
        } else if(minutes < 60) {
            return minutes + "分钟前";
        } else if(minutes < 1440) { // 24小时内
            return (minutes / 60) + "小时前";
        } else {
            return formatFriendly(date);
        }
    }
}
```

### 5.3 页面中使用时间格式化


**在JSP中应用时间格式化**：

```jsp
<div class="message-time">
    <!-- 友好时间显示 -->
    <span class="formatted-time">
        🕒 <%= TimeFormatter.formatFriendly(msg.getPostTime()) %>
    </span>
    
    <!-- 相对时间显示 -->
    <span class="relative-time">
        (<%= TimeFormatter.getRelativeTime(msg.getPostTime()) %>)
    </span>
</div>
```

---

## 6. 🔧 留言管理操作


### 6.1 管理功能概述


**管理员需要的功能**：
- **📊 查看统计**：留言总数、今日留言数
- **🗑️ 删除留言**：移除不当内容
- **🔍 搜索留言**：按关键词查找
- **📤 导出数据**：备份留言数据

### 6.2 删除留言功能


**deleteMessage.jsp - 删除指定留言**：

```jsp
<%
// 获取要删除的留言ID (这里用留言内容的哈希值作为ID)
String messageId = request.getParameter("id");

if(messageId != null) {
    MessageDAO dao = new MessageDAO();
    boolean success = dao.deleteMessage(messageId);
    
    if(success) {
        out.println("<script>alert('留言删除成功！'); window.location='messageAdmin.jsp';</script>");
    } else {
        out.println("<script>alert('删除失败，请重试！'); history.back();</script>");
    }
}
%>
```

### 6.3 管理面板页面


**messageAdmin.jsp - 留言管理界面**：

```jsp
<div class="admin-panel">
    <h2>🔧 留言管理</h2>
    
    <!-- 统计信息 -->
    <div class="stats-grid">
        <div class="stat-card">
            <h3>📊 总留言数</h3>
            <span class="stat-number"><%= totalMessages %></span>
        </div>
        
        <div class="stat-card">
            <h3>📅 今日留言</h3>
            <span class="stat-number"><%= todayMessages %></span>
        </div>
    </div>
    
    <!-- 留言列表（管理版本） -->
    <div class="admin-message-list">
        <% for(Message msg : messages) { %>
            <div class="admin-message-item">
                <div class="message-info">
                    <strong><%= msg.getAuthor() %></strong>
                    <span class="time"><%= msg.getPostTime() %></span>
                </div>
                <div class="message-content"><%= msg.getContent() %></div>
                <div class="message-actions">
                    <a href="deleteMessage.jsp?id=<%= msg.getId() %>" 
                       class="delete-btn"
                       onclick="return confirm('确定要删除这条留言吗？')">
                       🗑️ 删除
                    </a>
                </div>
            </div>
        <% } %>
    </div>
</div>
```

> **🛡️ 安全提示**：
> - 删除操作要有确认提示，防止误删
> - 可以考虑软删除（标记为删除但不真正删除）
> - 管理功能需要身份验证保护

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 MVC架构：Model(JavaBean) + View(JSP) + Controller(处理逻辑)
🔸 数据流向：表单提交 → 后端处理 → 数据存储 → 页面展示
🔸 分页原理：总数÷每页数量=总页数，当前页×每页数量=起始位置
🔸 文件操作：读取留言列表，追加新留言，删除指定留言
🔸 时间处理：格式化显示时间，计算相对时间差
```

### 7.2 关键实现要点


**🔹 表单处理流程**：
```
用户填表 → 表单验证 → 数据封装 → 文件保存 → 结果反馈
```

**🔹 分页显示逻辑**：
```
计算总页数 → 确定当前页范围 → 截取对应数据 → 生成导航链接
```

**🔹 时间格式化策略**：
```
存储原始时间 → 显示时格式化 → 提供多种显示格式
```

### 7.3 实际应用价值


**📱 项目实战能力**：
- 完整的CRUD操作实现
- 文件I/O操作熟练运用  
- 分页技术掌握
- 时间处理技巧

**💼 职场应用场景**：
- **企业内网系统**：员工留言、通知发布
- **客户服务系统**：用户反馈、意见收集
- **社区论坛**：用户互动、内容管理
- **教育平台**：学生作业提交、师生交流

### 7.4 扩展学习方向


**🚀 功能增强建议**：
```
基础功能 → 高级功能
简单留言 → 富文本编辑
文件存储 → 数据库存储  
基础分页 → 高级搜索
单一管理 → 权限分级
```

**🔧 技术升级路径**：
```
纯JSP → JSP + Servlet → Spring MVC → Spring Boot
文件存储 → MySQL数据库 → Redis缓存 → 分布式存储
```

**核心记忆要点**：
- 留言板是Web应用的经典入门项目
- 涵盖了增删改查的完整操作流程  
- 分页和时间处理是常用技术点
- 从简单功能开始，逐步扩展复杂特性
- 注重用户体验和数据安全