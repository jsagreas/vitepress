---
title: 1、标准目录结构
---
## 📚 目录

1. [Maven标准目录结构](#1-Maven标准目录结构)
2. [约定优于配置原则](#2-约定优于配置原则)
3. [POM文件详解](#3-POM文件详解)
4. [项目结构实战解析](#4-项目结构实战解析)
5. [常见问题与最佳实践](#5-常见问题与最佳实践)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 📁 Maven标准目录结构


### 1.1 什么是标准目录结构


**🔸 简单理解**
Maven就像一个"房子设计师"，它为所有Java项目设计了一套标准的"房间布局"。就像我们家里有客厅、卧室、厨房一样，Maven项目也有固定的"房间"来放不同的东西。

**💡 为什么要有标准结构？**
```
想象一下：
- 如果每个人的房子布局都不同，客人来了找不到厕所
- 如果每个项目结构都不同，开发者打开项目就懵了

Maven的解决方案：
- 制定统一的"房间布局"标准
- 所有Maven项目都按这个标准来组织文件
- 任何开发者打开项目都能快速找到需要的文件
```

### 1.2 标准目录树状图


```
my-web-project/                    ← 项目根目录（整个房子）
├── pom.xml                        ← 项目配置文件（房子说明书）
├── src/                           ← 源代码目录（主要活动区域）
│   ├── main/                      ← 主程序代码（正式居住区）
│   │   ├── java/                  ← Java源码（书房）
│   │   │   └── com/
│   │   │       └── example/
│   │   │           └── MyApp.java
│   │   ├── resources/             ← 资源文件（储藏室）
│   │   │   ├── application.properties
│   │   │   └── static/
│   │   └── webapp/                ← Web资源（客厅展示区）
│   │       ├── WEB-INF/
│   │       │   └── web.xml
│   │       ├── index.html
│   │       ├── css/
│   │       └── js/
│   └── test/                      ← 测试代码（实验室）
│       ├── java/                  ← 测试Java代码
│       │   └── com/
│       │       └── example/
│       │           └── MyAppTest.java
│       └── resources/             ← 测试资源文件
└── target/                        ← 构建输出目录（垃圾回收站）
    ├── classes/                   ← 编译后的.class文件
    ├── test-classes/              ← 测试编译文件
    └── my-web-project.war         ← 最终打包文件
```

### 1.3 各目录详细说明


#### 📖 src/main/java - Java源码目录


**🔹 作用说明**
这里放你写的所有Java代码，就像书房放书一样。

```
实际例子：
src/main/java/com/example/controller/UserController.java
src/main/java/com/example/service/UserService.java  
src/main/java/com/example/dao/UserDao.java
```

**💡 包结构建议**
```
com.公司名.项目名.模块名

例如：
com.baidu.search.controller    ← 百度搜索项目的控制层
com.alibaba.mall.service       ← 阿里商城项目的服务层
```

#### 📦 src/main/resources - 资源文件目录


**🔹 作用说明**
放配置文件、图片、文本等非Java文件，就像储藏室放杂物。

```
常见文件类型：
├── application.properties     ← 项目配置文件
├── log4j.properties          ← 日志配置
├── database.sql              ← 数据库脚本
├── templates/                ← 模板文件
│   └── email-template.html
└── static/                   ← 静态资源
    ├── images/
    ├── css/
    └── js/
```

**⚡ 重要特点**
- 这里的文件会**直接复制**到最终的jar/war包中
- Java代码可以通过**类路径(classpath)**直接访问这些文件

#### 🌐 src/main/webapp - Web应用目录


**🔹 作用说明**
专门为Web项目准备的目录，放网页、CSS、JavaScript等前端文件。

```
webapp目录结构：
webapp/
├── index.html                 ← 首页
├── WEB-INF/                   ← Web配置目录（重要！）
│   ├── web.xml               ← Web应用配置文件
│   └── lib/                  ← Web应用专用库
├── css/                      ← 样式文件
│   └── style.css
├── js/                       ← JavaScript文件
│   └── app.js
└── images/                   ← 图片文件
    └── logo.png
```

**🚨 WEB-INF目录特点**
- 这个目录对外部用户**不可直接访问**
- 只能通过服务器内部访问
- 是放敏感配置的安全地带

#### 🧪 src/test/java - 测试代码目录


**🔹 作用说明**
放单元测试代码，就像实验室做实验验证产品质量。

```java
// 示例：测试用户服务类
public class UserServiceTest {
    @Test
    public void testCreateUser() {
        // 测试创建用户功能
        UserService service = new UserService();
        User user = service.createUser("张三", "zhangsan@email.com");
        assertEquals("张三", user.getName());
    }
}
```

#### 🎯 target目录 - 构建输出


**🔹 作用说明**
Maven构建过程中生成的所有文件都放在这里，就像垃圾回收站。

```
target目录内容：
target/
├── classes/                   ← 编译后的Java字节码文件
├── test-classes/              ← 测试代码编译结果
├── maven-archiver/            ← Maven归档信息
├── my-project-1.0.jar         ← 打包后的jar文件
└── my-project-1.0.war         ← 打包后的war文件（Web项目）
```

**⚠️ 注意事项**
- target目录可以随时删除，Maven会重新生成
- 不要把自己的代码放在target目录中
- 版本控制工具（如Git）通常会忽略这个目录

---

## 2. 🎯 约定优于配置原则


### 2.1 什么是约定优于配置


**🔸 通俗解释**
```
传统方式（配置繁琐）：
每次装修房子都要详细说明：
- 门在哪里开
- 窗户装在哪面墙
- 厨房放在几楼
- 卧室有几个...

Maven方式（约定简单）：
大家都按标准来：
- 门统一开在南面
- 厨房统一在一楼
- 卧室统一在二楼
- 只有特殊情况才需要说明
```

### 2.2 约定带来的好处


| **传统项目管理** | **Maven约定管理** | **提升效果** |
|-----------------|------------------|-------------|
| `每个项目结构不同` | `统一标准结构` | `🚀 学习成本降低90%` |
| `手动配置编译路径` | `自动识别src/main/java` | `⚡ 配置时间节省80%` |
| `手动管理依赖jar包` | `自动下载管理依赖` | `🎯 错误率降低95%` |
| `手动编写构建脚本` | `标准生命周期命令` | `💪 开发效率提升3倍` |

### 2.3 约定的具体体现


**📁 目录约定**
```
约定内容                           实际效果
src/main/java        →        Maven自动编译这里的Java文件
src/main/resources   →        Maven自动将资源复制到classpath
src/test/java        →        Maven自动运行这里的测试
target              →        Maven自动输出构建结果到这里
```

**🔧 构建约定**
```
约定命令                           执行效果
mvn compile          →        自动编译src/main/java下的代码
mvn test            →        自动运行src/test/java下的测试
mvn package         →        自动打包成jar或war文件
mvn install         →        自动安装到本地仓库
```

---

## 3. 📋 POM文件详解


### 3.1 什么是POM文件


**🔸 POM全称**：Project Object Model（项目对象模型）

**🔸 通俗理解**
POM文件就像项目的**身份证+说明书**，它告诉Maven：
- 这个项目叫什么名字
- 项目版本是多少
- 需要哪些第三方库
- 怎么构建和打包

### 3.2 POM文件基本结构


```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    
    <!-- POM模型版本，固定4.0.0 -->
    <modelVersion>4.0.0</modelVersion>
    
    <!-- 项目基本信息（身份证信息） -->
    <groupId>com.example</groupId>
    <artifactId>my-web-app</artifactId>
    <version>1.0.0</version>
    <packaging>war</packaging>
    
    <!-- 项目描述信息 -->
    <name>我的第一个Web应用</name>
    <description>学习JavaWeb开发的练习项目</description>
    
    <!-- 属性配置 -->
    <properties>
        <java.version>8</java.version>
        <maven.compiler.source>8</maven.compiler.source>
        <maven.compiler.target>8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>
    
    <!-- 依赖管理 -->
    <dependencies>
        <!-- 这里配置项目需要的第三方库 -->
    </dependencies>
    
    <!-- 构建配置 -->
    <build>
        <!-- 这里配置构建相关设置 -->
    </build>
    
</project>
```

### 3.3 POM核心元素详解


#### 🏷️ 项目坐标（GAV）


```xml
<groupId>com.example</groupId>        <!-- 组织/公司标识 -->
<artifactId>my-web-app</artifactId>   <!-- 项目名称 -->
<version>1.0.0</version>              <!-- 项目版本 -->
```

**🔹 坐标解释**
```
类比理解：
groupId    = 省份（com.baidu, com.alibaba）
artifactId = 城市（search, mall, pay）  
version    = 街道号（1.0.0, 2.1.5）

完整地址：com.baidu.search-1.0.0
就能唯一定位到百度搜索项目的1.0.0版本
```

**📦 packaging类型**
```xml
<packaging>jar</packaging>    <!-- 普通Java项目，打包成jar -->
<packaging>war</packaging>    <!-- Web项目，打包成war -->
<packaging>pom</packaging>    <!-- 父项目，不打包代码 -->
```

#### ⚙️ 属性配置


```xml
<properties>
    <!-- Java版本 -->
    <java.version>8</java.version>
    <maven.compiler.source>8</maven.compiler.source>
    <maven.compiler.target>8</maven.compiler.target>
    
    <!-- 编码设置 -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    
    <!-- 依赖版本统一管理 -->
    <spring.version>5.3.21</spring.version>
    <junit.version>4.13.2</junit.version>
</properties>
```

**💡 使用属性的好处**
```xml
<!-- 定义一次，多处使用 -->
<properties>
    <spring.version>5.3.21</spring.version>
</properties>

<!-- 在依赖中引用 -->
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-core</artifactId>
    <version>${spring.version}</version>  <!-- 引用属性 -->
</dependency>
```

#### 📚 依赖管理


```xml
<dependencies>
    <!-- Web开发必需：Servlet API -->
    <dependency>
        <groupId>javax.servlet</groupId>
        <artifactId>javax.servlet-api</artifactId>
        <version>4.0.1</version>
        <scope>provided</scope>  <!-- 服务器已提供，不打包 -->
    </dependency>
    
    <!-- 单元测试：JUnit -->
    <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>4.13.2</version>
        <scope>test</scope>      <!-- 只在测试时使用 -->
    </dependency>
    
    <!-- 数据库连接：MySQL驱动 -->
    <dependency>
        <groupId>mysql</groupId>
        <artifactId>mysql-connector-java</artifactId>
        <version>8.0.33</version>
        <scope>runtime</scope>   <!-- 运行时需要 -->
    </dependency>
</dependencies>
```

**🎯 依赖作用域(scope)说明**

| **作用域** | **含义** | **使用场景** |
|-----------|---------|-------------|
| `compile` | `编译、测试、运行都需要` | `核心业务代码依赖` |
| `provided` | `编译、测试需要，运行时由容器提供` | `Servlet API、JSP API` |
| `runtime` | `测试、运行需要，编译不需要` | `数据库驱动` |
| `test` | `只在测试时需要` | `JUnit、Mockito` |

---

## 4. 🛠️ 项目结构实战解析


### 4.1 创建标准Web项目


**🔹 使用Maven命令创建**
```bash
mvn archetype:generate \
    -DgroupId=com.example \
    -DartifactId=my-first-webapp \
    -DarchetypeArtifactId=maven-archetype-webapp \
    -DinteractiveMode=false
```

**🔹 生成的项目结构**
```
my-first-webapp/
├── pom.xml
└── src/
    └── main/
        ├── java/
        └── webapp/
            ├── index.jsp
            └── WEB-INF/
                └── web.xml
```

### 4.2 完善项目结构


**📝 手动补充缺失目录**
```bash
# 进入项目根目录
cd my-first-webapp

# 创建缺失的标准目录
mkdir -p src/main/java/com/example/controller
mkdir -p src/main/java/com/example/service  
mkdir -p src/main/java/com/example/dao
mkdir -p src/main/resources
mkdir -p src/test/java
mkdir -p src/test/resources
```

### 4.3 实际项目结构示例


```
完整的Web项目结构：
my-webapp/
├── pom.xml                                    ← Maven配置
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/example/
│   │   │       ├── controller/                ← 控制器层
│   │   │       │   └── UserController.java
│   │   │       ├── service/                   ← 业务逻辑层
│   │   │       │   └── UserService.java
│   │   │       ├── dao/                       ← 数据访问层
│   │   │       │   └── UserDao.java
│   │   │       └── model/                     ← 实体类
│   │   │           └── User.java
│   │   ├── resources/
│   │   │   ├── application.properties         ← 配置文件
│   │   │   ├── log4j.properties              ← 日志配置
│   │   │   └── sql/                          ← SQL脚本
│   │   │       └── schema.sql
│   │   └── webapp/
│   │       ├── index.html                     ← 首页
│   │       ├── WEB-INF/
│   │       │   ├── web.xml                   ← Web配置
│   │       │   └── views/                    ← JSP页面
│   │       │       └── user/
│   │       │           └── list.jsp
│   │       ├── static/                       ← 静态资源
│   │       │   ├── css/
│   │       │   │   └── style.css
│   │       │   ├── js/
│   │       │   │   └── app.js
│   │       │   └── images/
│   │       │       └── logo.png
│   │       └── WEB-INF/
│   └── test/
│       ├── java/
│       │   └── com/example/
│       │       └── service/
│       │           └── UserServiceTest.java  ← 单元测试
│       └── resources/
│           └── test-data.sql                 ← 测试数据
└── target/                                   ← 构建输出
```

### 4.4 理解目录间的协作关系


```
Web请求处理流程：
浏览器访问 → webapp/index.html → UserController.java → UserService.java → UserDao.java → 数据库
     ↓              ↓                    ↓                ↓               ↓
  静态页面     →  控制器层(controller)  →  业务层(service)  →  数据层(dao)  →  存储层

资源加载流程：
Java代码 → resources/application.properties → 读取配置
        → resources/sql/schema.sql → 加载SQL脚本
        → webapp/static/css/style.css → 加载样式
```

---

## 5. ❓ 常见问题与最佳实践


### 5.1 新手常见问题


**🚨 问题1：Java文件放错位置**
```
❌ 错误放置：
src/java/com/example/MyClass.java

✅ 正确位置：  
src/main/java/com/example/MyClass.java

💡 记忆方法：main是"主要的"意思，主要的Java代码放在main下
```

**🚨 问题2：资源文件路径错误**
```java
// ❌ 错误写法：找不到配置文件
InputStream is = new FileInputStream("config.properties");

// ✅ 正确写法：从classpath加载
InputStream is = getClass().getClassLoader()
    .getResourceAsStream("config.properties");
```

**🚨 问题3：Web资源访问问题**
```
❌ 错误理解：
以为webapp下的所有文件都能直接访问

✅ 正确理解：
- webapp/index.html → 可直接访问：http://localhost:8080/index.html
- webapp/WEB-INF/web.xml → 不能直接访问（安全保护）
```

### 5.2 最佳实践建议


**📋 目录命名规范**
```
推荐包结构：
com.公司.项目.controller  → 控制器
com.公司.项目.service    → 业务逻辑  
com.公司.项目.dao        → 数据访问
com.公司.项目.model      → 实体类
com.公司.项目.util       → 工具类
com.公司.项目.config     → 配置类
```

**🎯 文件组织原则**
```
按功能模块组织：
├── user/                    ← 用户模块
│   ├── UserController.java
│   ├── UserService.java
│   └── UserDao.java
├── product/                 ← 产品模块  
│   ├── ProductController.java
│   ├── ProductService.java
│   └── ProductDao.java
└── order/                   ← 订单模块
    ├── OrderController.java
    ├── OrderService.java
    └── OrderDao.java
```

**⚡ 构建优化建议**
```xml
<!-- POM文件优化配置 -->
<properties>
    <!-- 统一编码 -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- 统一Java版本 -->
    <maven.compiler.source>8</maven.compiler.source>
    <maven.compiler.target>8</maven.compiler.target>
    <!-- 跳过测试（开发阶段） -->
    <maven.test.skip>false</maven.test.skip>
</properties>
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 标准目录结构：Maven为Java项目设计的统一"房间布局"
🔸 约定优于配置：遵循标准约定，减少繁琐配置
🔸 POM文件：项目的"身份证+说明书"，管理依赖和构建
🔸 目录职责分工：每个目录都有明确的用途和职责
🔸 构建生命周期：compile → test → package → install
```

### 6.2 关键理解要点


**🔹 为什么要遵循标准结构？**
```
个人项目：可以随意组织，但团队协作困难
企业项目：必须统一标准，提高开发效率
Maven标准：业界公认，学会一次，处处适用
```

**🔹 各目录的核心作用**
```
src/main/java        →  写业务代码的地方
src/main/resources   →  放配置文件的地方  
src/main/webapp      →  放网页文件的地方
src/test            →  写测试代码的地方
target              →  Maven生成文件的地方
```

**🔹 POM文件的重要性**
```
没有POM = 没有身份证：
- Maven不知道这是什么项目
- 无法自动下载依赖库
- 无法执行标准构建命令
```

### 6.3 实际应用价值


**📈 学习收益**
- **🎯 快速上手**：任何Maven项目都能快速理解结构
- **🚀 提升效率**：不用花时间配置复杂的构建脚本  
- **🤝 团队协作**：统一标准，减少沟通成本
- **📚 持续学习**：掌握基础，为学习Spring等框架打基础

**🛠️ 实战技能**
- 创建标准Maven Web项目
- 正确放置Java代码和资源文件
- 编写基本POM配置
- 使用Maven命令构建项目

**核心记忆口诀**：
```
Maven项目有标准，目录结构不能乱
main放代码resources配置，webapp专门做网站
test目录写测试，target输出别管他
POM文件是核心，依赖构建全靠它
```