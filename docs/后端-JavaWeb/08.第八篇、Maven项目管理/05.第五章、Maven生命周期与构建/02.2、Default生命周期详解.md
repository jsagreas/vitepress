---
title: 2、Default生命周期详解
---
## 📚 目录

1. [Default生命周期概述](#1-Default生命周期概述)
2. [核心阶段详解](#2-核心阶段详解)
3. [阶段依赖关系](#3-阶段依赖关系)
4. [常用阶段实践](#4-常用阶段实践)
5. [生命周期执行示例](#5-生命周期执行示例)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔄 Default生命周期概述


### 1.1 什么是Default生命周期


**💡 通俗理解**
```
想象一下盖房子的过程：
规划设计 → 打地基 → 建框架 → 装修 → 验收 → 入住

Maven的Default生命周期就像盖房子的标准流程：
验证项目 → 编译代码 → 测试功能 → 打包项目 → 安装到本地 → 发布到远程
```

**🎯 核心定义**
- **Default生命周期**：Maven中最重要的生命周期，负责项目的**构建和部署**
- **标准化流程**：无论什么项目，都按照这个固定顺序执行
- **自动化管理**：一个命令就能完成从编译到部署的全部工作

### 1.2 为什么需要生命周期


**🔸 解决的问题**
```
传统开发痛点：
❌ 每次手动编译Java文件
❌ 手动复制jar包和资源文件  
❌ 手动运行测试
❌ 手动打包和部署

Maven生命周期的优势：
✅ 一个命令搞定所有步骤
✅ 标准化的构建流程
✅ 自动处理依赖关系
✅ 可重复、可靠的构建过程
```

---

## 2. 🔧 核心阶段详解


### 2.1 validate - 验证阶段


**🔍 阶段含义**
> 💡 **validate**：检查项目是否正确，所有必要的信息是否可用
> 
> 就像盖房子前检查建筑图纸是否齐全、合法

**⚡ 主要工作**
```
🔸 检查项目结构是否正确
🔸 验证pom.xml文件是否有效
🔸 确认必要的依赖信息是否齐全
🔸 检查项目配置是否合理
```

**💻 实际执行**
```bash
# 单独执行验证阶段
mvn validate

# 输出示例
[INFO] --- maven-enforcer-plugin:3.0.0:enforce (enforce-maven)
[INFO] --- maven-site-plugin:3.8.2:attach-descriptor (attach-descriptor)
[INFO] BUILD SUCCESS
```

### 2.2 initialize - 初始化阶段


**🚀 阶段含义**
> 💡 **initialize**：初始化构建状态，设置属性或创建目录
> 
> 相当于施工前的准备工作，搭脚手架、准备工具

**⚡ 主要工作**
```
🔸 创建必要的目录结构
🔸 初始化项目属性
🔸 设置构建环境变量
🔸 准备构建所需的资源
```

### 2.3 compile - 编译阶段 ⭐⭐⭐


**💻 阶段含义**
> 💡 **compile**：编译项目的源代码
> 
> 把Java源代码(.java)变成字节码文件(.class)，就像把设计图变成实际的建筑材料

**⚡ 主要工作**
```
🔸 编译src/main/java目录下的Java源代码
🔸 将.java文件编译成.class文件
🔸 复制src/main/resources目录下的资源文件
🔸 将编译结果放到target/classes目录
```

**📁 目录变化**
```
项目结构：
myproject/
├── src/main/java/
│   └── Hello.java          ← 源代码
├── src/main/resources/
│   └── config.properties   ← 资源文件
└── target/classes/         ← 编译结果
    ├── Hello.class         ← 编译后的字节码
    └── config.properties   ← 复制的资源文件
```

**💻 执行命令**
```bash
mvn compile

# 输出关键信息
[INFO] --- maven-compiler-plugin:3.8.1:compile (default-compile)
[INFO] Compiling 5 source files to /path/to/target/classes
[INFO] BUILD SUCCESS
```

### 2.4 test - 测试阶段 ⭐⭐⭐


**🧪 阶段含义**
> 💡 **test**：使用合适的单元测试框架运行测试
> 
> 就像建筑完工后的质量检测，确保功能正常

**⚡ 主要工作**
```
🔸 编译src/test/java目录下的测试代码
🔸 运行所有的单元测试
🔸 生成测试报告
🔸 如果测试失败，构建会停止
```

**📁 测试结构**
```
测试相关目录：
myproject/
├── src/test/java/
│   └── HelloTest.java      ← 测试源代码
├── target/test-classes/    ← 编译后的测试代码
│   └── HelloTest.class
└── target/surefire-reports/ ← 测试报告
    └── TEST-HelloTest.xml
```

**💻 执行效果**
```bash
mvn test

# 输出示例
[INFO] --- maven-surefire-plugin:2.22.2:test (default-test)
[INFO] Running com.example.HelloTest
[INFO] Tests run: 3, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

### 2.5 package - 打包阶段 ⭐⭐⭐


**📦 阶段含义**
> 💡 **package**：将编译后的代码打包成可分发的格式
> 
> 把所有建筑材料组装成完整的房子，方便整体搬运

**⚡ 主要工作**
```
🔸 将编译后的.class文件打包
🔸 包含资源文件和配置文件
🔸 生成JAR、WAR或其他格式的包
🔸 包文件放在target目录下
```

**📦 打包类型**
| 打包类型 | **文件格式** | **适用场景** | **包含内容** |
|---------|-------------|-------------|-------------|
| **JAR** | `.jar` | `Java应用程序` | `字节码文件 + 资源文件` |
| **WAR** | `.war` | `Web应用` | `JSP + Servlet + 静态文件` |
| **EAR** | `.ear` | `企业级应用` | `多个WAR + JAR包` |

**💻 执行结果**
```bash
mvn package

# 输出示例  
[INFO] --- maven-jar-plugin:3.2.0:jar (default-jar)
[INFO] Building jar: /path/to/target/myproject-1.0.jar
[INFO] BUILD SUCCESS
```

### 2.6 verify - 验证阶段


**✅ 阶段含义**
> 💡 **verify**：检查包是否有效且符合质量标准
> 
> 相当于房屋的最终验收，确保一切符合标准

**⚡ 主要工作**
```
🔸 运行集成测试
🔸 检查代码质量
🔸 验证包的完整性
🔸 执行各种检查插件
```

### 2.7 install - 安装阶段 ⭐⭐⭐


**🏠 阶段含义**
> 💡 **install**：将包安装到本地Maven仓库
> 
> 把建好的房子登记到本地的房产证系统，其他项目可以引用

**⚡ 主要工作**
```
🔸 将打包好的文件复制到本地仓库
🔸 安装pom.xml文件
🔸 其他项目可以作为依赖引用
🔸 更新本地仓库的元数据
```

**📁 安装路径**
```
本地仓库结构：
~/.m2/repository/
└── com/example/
    └── myproject/
        └── 1.0/
            ├── myproject-1.0.jar     ← 安装的JAR包
            ├── myproject-1.0.pom     ← 项目信息
            └── _remote.repositories  ← 仓库信息
```

**💻 执行命令**
```bash
mvn install

# 输出示例
[INFO] --- maven-install-plugin:2.5.2:install (default-install)
[INFO] Installing /path/to/target/myproject-1.0.jar to ~/.m2/repository/com/example/myproject/1.0/myproject-1.0.jar
[INFO] BUILD SUCCESS
```

### 2.8 deploy - 部署阶段 ⭐⭐


**🚀 阶段含义**
> 💡 **deploy**：将包发布到远程仓库供其他开发者使用
> 
> 把房子信息发布到公共的房产平台，全世界的人都可以看到和使用

**⚡ 主要工作**
```
🔸 将包上传到远程Maven仓库
🔸 发布到公司内部仓库或中央仓库
🔸 供其他开发团队使用
🔸 更新远程仓库的元数据
```

---

## 3. 🔗 阶段依赖关系


### 3.1 执行顺序图


```
Maven Default生命周期执行流程：

validate → initialize → compile → test → package → verify → install → deploy
   ↓           ↓          ↓        ↓        ↓        ↓        ↓        ↓
  检查      →  初始化   →  编译   → 测试  →  打包   → 验证  → 安装  → 部署
  项目        环境        代码      功能     文件     质量    本地    远程
```

### 3.2 依赖关系特点


**🔸 顺序执行原则**
```
📌 重要规则：执行任何阶段时，会自动执行它之前的所有阶段

实例说明：
执行 mvn package 时，实际执行顺序：
validate → initialize → compile → test → package

执行 mvn test 时，实际执行顺序：  
validate → initialize → compile → test
```

**⚡ 实际验证**
```bash
# 执行package命令
mvn package

# 控制台输出会显示所有阶段：
[INFO] --- maven-resources-plugin:3.1.0:resources (default-resources)
[INFO] --- maven-compiler-plugin:3.8.1:compile (default-compile)  
[INFO] --- maven-resources-plugin:3.1.0:testResources (default-testResources)
[INFO] --- maven-compiler-plugin:3.8.1:testCompile (default-testCompile)
[INFO] --- maven-surefire-plugin:2.22.2:test (default-test)
[INFO] --- maven-jar-plugin:3.2.0:jar (default-jar)
```

### 3.3 跳过阶段的方法


**🚫 跳过测试**
```bash
# 跳过测试阶段
mvn package -DskipTests=true

# 或者使用简写
mvn package -DskipTests
```

**🚫 跳过编译测试代码**
```bash
# 完全跳过测试编译和执行
mvn package -Dmaven.test.skip=true
```

---

## 4. 💼 常用阶段实践


### 4.1 日常开发常用命令


**📊 使用频率排行**
| 命令 | **使用频率** | **适用场景** | **执行阶段** |
|------|-------------|-------------|-------------|
| `mvn compile` | ⭐⭐⭐ | `开发调试` | `validate → compile` |
| `mvn test` | ⭐⭐⭐⭐ | `功能测试` | `validate → test` |
| `mvn package` | ⭐⭐⭐⭐⭐ | `本地构建` | `validate → package` |
| `mvn install` | ⭐⭐⭐⭐ | `本地安装` | `validate → install` |
| `mvn deploy` | ⭐⭐ | `发布版本` | `validate → deploy` |

### 4.2 实际开发场景


**🔧 开发阶段**
```bash
# 快速编译检查语法错误
mvn compile

# 运行测试验证功能
mvn test

# 打包准备部署
mvn package
```

**🚀 发布阶段**
```bash
# 安装到本地，供其他模块使用
mvn install

# 发布到远程仓库，供团队使用
mvn deploy
```

### 4.3 清理与重建


**🧹 清理项目**
```bash
# 删除target目录，清理所有构建结果
mvn clean

# 清理后重新编译
mvn clean compile

# 清理后重新打包
mvn clean package
```

---

## 5. 📋 生命周期执行示例


### 5.1 完整执行过程


**💻 实际命令执行**
```bash
# 执行完整的构建过程
mvn clean install
```

**📊 详细执行日志**
```
[INFO] --- maven-clean-plugin:3.1.0:clean (default-clean) @ myproject ---
[INFO] Deleting /path/to/myproject/target

[INFO] --- maven-resources-plugin:3.1.0:resources (default-resources) @ myproject ---
[INFO] Copying 1 resource

[INFO] --- maven-compiler-plugin:3.8.1:compile (default-compile) @ myproject ---
[INFO] Compiling 3 source files to /path/to/myproject/target/classes

[INFO] --- maven-resources-plugin:3.1.0:testResources (default-testResources) @ myproject ---
[INFO] Copying 1 resource

[INFO] --- maven-compiler-plugin:3.8.1:testCompile (default-testCompile) @ myproject ---
[INFO] Compiling 2 test source files to /path/to/myproject/target/test-classes

[INFO] --- maven-surefire-plugin:2.22.2:test (default-test) @ myproject ---
[INFO] Tests run: 5, Failures: 0, Errors: 0, Skipped: 0

[INFO] --- maven-jar-plugin:3.2.0:jar (default-jar) @ myproject ---
[INFO] Building jar: /path/to/myproject/target/myproject-1.0.jar

[INFO] --- maven-install-plugin:2.5.2:install (default-install) @ myproject ---
[INFO] Installing /path/to/myproject/target/myproject-1.0.jar to ~/.m2/repository/com/example/myproject/1.0/myproject-1.0.jar

[INFO] BUILD SUCCESS
[INFO] Total time:  8.234 s
```

### 5.2 目录结构变化


**📁 执行前后对比**
```
执行前：
myproject/
├── src/
│   ├── main/java/
│   └── test/java/
└── pom.xml

执行后：
myproject/
├── src/
│   ├── main/java/
│   └── test/java/
├── target/                    ← 新生成
│   ├── classes/              ← 编译结果
│   ├── test-classes/         ← 测试编译结果
│   ├── surefire-reports/     ← 测试报告
│   └── myproject-1.0.jar     ← 最终包文件
└── pom.xml
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的概念


```
🔸 Default生命周期：Maven最重要的构建生命周期
🔸 阶段顺序：validate → compile → test → package → install → deploy
🔸 依赖执行：执行某阶段会自动执行前面所有阶段
🔸 常用命令：compile、test、package、install是日常最常用的
```

### 6.2 关键理解要点


**🔹 生命周期的本质**
```
理解要点：
- 生命周期是一套标准化的构建流程
- 每个阶段都有明确的职责和作用
- 自动化完成从源码到部署的全过程
- 保证构建过程的一致性和可重复性
```

**🔹 阶段间的关系**
```
核心原则：
- 后面的阶段依赖前面的阶段
- 不能跳过中间阶段直接执行后面的阶段
- 每个阶段都有特定的插件来执行具体任务
- 失败的阶段会导致整个构建失败
```

**🔹 实际开发中的应用**
```
日常使用：
- 开发时：主要用compile和test
- 构建时：主要用package和install  
- 发布时：使用clean install deploy
- 调试时：可以单独执行某个阶段
```

### 6.3 实践建议


**💡 开发习惯**
```
建议做法：
✅ 提交代码前先执行mvn test确保测试通过
✅ 打包部署前执行mvn clean package确保干净构建
✅ 发布版本时使用mvn clean deploy完整流程
✅ 遇到奇怪问题时用mvn clean清理后重建
```

**⚠️ 注意事项**
```
常见误区：
❌ 以为package不会执行test阶段
❌ 不理解为什么install需要这么长时间
❌ 不知道可以跳过测试加速构建
❌ 不清楚clean的作用和必要性
```

**🎯 学习要点**
- Maven生命周期是自动化构建的核心机制
- 理解每个阶段的作用有助于排查构建问题  
- 熟练使用常用命令能大大提高开发效率
- 生命周期的概念在其他构建工具中也普遍存在