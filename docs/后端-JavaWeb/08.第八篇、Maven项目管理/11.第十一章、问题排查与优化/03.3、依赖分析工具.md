---
title: 3、依赖分析工具
---
## 📚 目录

1. [依赖分析概述](#1-依赖分析概述)
2. [dependency:tree - 依赖树分析](#2-dependency-tree-依赖树分析)
3. [dependency:analyze - 依赖使用分析](#3-dependency-analyze-依赖使用分析)
4. [dependency:resolve - 依赖解析分析](#4-dependency-resolve-依赖解析分析)
5. [versions插件 - 版本管理](#5-versions插件-版本管理)
6. [依赖冲突检测与解决](#6-依赖冲突检测与解决)
7. [依赖图可视化工具](#7-依赖图可视化工具)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔍 依赖分析概述


### 1.1 为什么需要依赖分析


在实际的Java Web项目开发中，你可能会遇到这些问题：

```
常见依赖问题：
❌ 项目启动报错：ClassNotFoundException
❌ 运行时异常：NoSuchMethodError  
❌ 项目打包后体积过大
❌ 不知道引入了哪些传递性依赖
❌ 版本冲突导致功能异常
```

**依赖分析的作用**就像给你的项目做"体检"，帮你发现这些问题：

```
依赖分析能解决什么：
✅ 查看完整的依赖关系树
✅ 找出未使用的依赖（减少项目体积）
✅ 发现版本冲突问题
✅ 检查依赖是否正确声明
✅ 优化项目依赖结构
```

### 1.2 Maven依赖分析工具概览


Maven提供了多种内置工具来帮助我们分析依赖：

```
核心分析工具：
🔸 dependency:tree     → 查看依赖树结构
🔸 dependency:analyze  → 分析依赖使用情况  
🔸 dependency:resolve  → 解析依赖详情
🔸 versions插件        → 管理版本更新
🔸 第三方可视化工具     → 图形化展示依赖
```

---

## 2. 🌳 dependency:tree - 依赖树分析


### 2.1 基本使用方法


**dependency:tree**是最常用的依赖分析命令，它能显示项目的完整依赖树结构。

**基本命令**：
```bash
mvn dependency:tree
```

**执行效果示例**：
```
[INFO] com.example:web-demo:jar:1.0-SNAPSHOT
[INFO] +- org.springframework:spring-core:jar:5.3.21:compile
[INFO] |  \- org.springframework:spring-jcl:jar:5.3.21:compile
[INFO] +- org.springframework:spring-web:jar:5.3.21:compile
[INFO] |  +- org.springframework:spring-beans:jar:5.3.21:compile
[INFO] |  \- org.springframework:spring-context:jar:5.3.21:compile
[INFO] \- junit:junit:jar:4.13.2:test
[INFO]    \- org.hamcrest:hamcrest-core:jar:1.3:test
```

### 2.2 读懂依赖树结构


让我们逐步解读这个树形结构：

```
依赖树解读：
📁 com.example:web-demo:jar:1.0-SNAPSHOT  ← 你的项目根节点
├── 📦 spring-core:jar:5.3.21:compile     ← 直接依赖
│   └── 📦 spring-jcl:jar:5.3.21:compile ← 传递性依赖
├── 📦 spring-web:jar:5.3.21:compile      ← 直接依赖  
│   ├── 📦 spring-beans:jar:5.3.21:compile ← 传递性依赖
│   └── 📦 spring-context:jar:5.3.21:compile ← 传递性依赖
└── 📦 junit:jar:4.13.2:test              ← 测试依赖
    └── 📦 hamcrest-core:jar:1.3:test     ← 测试传递依赖
```

**符号含义**：
- `+- ` 表示有后续兄弟节点的依赖
- `\- ` 表示最后一个兄弟节点  
- `|  ` 表示垂直连接线
- `:compile` `:test` 表示依赖作用域

### 2.3 常用参数选项


**查看特定作用域的依赖**：
```bash
# 只看编译期依赖
mvn dependency:tree -Dscope=compile

# 只看测试依赖  
mvn dependency:tree -Dscope=test
```

**输出到文件**：
```bash
# 保存依赖树到文件
mvn dependency:tree -DoutputFile=dependency-tree.txt
```

**查看特定依赖的路径**：
```bash
# 查看spring-core这个依赖是怎么来的
mvn dependency:tree -Dincludes=org.springframework:spring-core
```

### 2.4 实际应用场景


**场景1：排查类冲突问题**

假设你的项目运行时报错`NoSuchMethodError`，很可能是版本冲突导致的。

```bash
# 查看所有spring相关依赖
mvn dependency:tree -Dincludes=*spring*
```

**场景2：了解项目依赖规模**  

```bash
# 查看依赖总数
mvn dependency:tree | grep -c "\- "
```

---

## 3. 🔬 dependency:analyze - 依赖使用分析


### 3.1 analyze命令的作用


**dependency:analyze**命令帮你分析哪些依赖实际被使用了，哪些是多余的。这就像给你的项目"减肥"。

**基本用法**：
```bash
mvn dependency:analyze
```

### 3.2 分析结果解读


**输出示例**：
```
[WARNING] Used undeclared dependencies found:
[WARNING]    org.springframework:spring-context:jar:5.3.21:compile
[WARNING] Unused declared dependencies found:  
[WARNING]    org.springframework:spring-web:jar:5.3.21:compile
```

**结果含义解释**：

```
📊 分析结果说明：

Used undeclared dependencies（使用了但未声明）:
含义：你的代码中用到了这个依赖，但pom.xml中没有直接声明
影响：依赖别的jar包传递进来的，存在版本变化风险
建议：应该在pom.xml中明确声明

Unused declared dependencies（声明了但未使用）:
含义：pom.xml中声明了这个依赖，但代码中没有直接使用
影响：增加了项目体积，可能存在安全风险
建议：考虑移除（需要谨慎，可能是运行时需要的）
```

### 3.3 实际优化示例


**优化前的pom.xml**：
```xml
<dependencies>
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-core</artifactId>
        <version>5.3.21</version>
    </dependency>
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-web</artifactId>
        <version>5.3.21</version>
    </dependency>
    <!-- 假设这个依赖没有实际使用 -->
    <dependency>
        <groupId>commons-lang</groupId>
        <artifactId>commons-lang</artifactId>
        <version>2.6</version>
    </dependency>
</dependencies>
```

**执行分析**：
```bash
mvn dependency:analyze
```

**根据分析结果优化**：
```xml
<dependencies>
    <!-- 保留使用的依赖 -->
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-core</artifactId>
        <version>5.3.21</version>
    </dependency>
    <!-- 明确声明使用了但未声明的依赖 -->
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-context</artifactId>
        <version>5.3.21</version>
    </dependency>
    <!-- 移除未使用的依赖 commons-lang -->
</dependencies>
```

### 3.4 analyze命令的局限性


> ⚠️ **注意事项**：analyze命令只能检测编译时的直接引用，对于以下情况可能不准确：

```
analyze命令检测不到的情况：
🔸 通过反射使用的类
🔸 配置文件中引用的类  
🔸 运行时动态加载的类
🔸 SPI（Service Provider Interface）机制
🔸 测试代码中的依赖
```

因此，移除"未使用"的依赖时要格外小心，最好先在测试环境验证。

---

## 4. 🔧 dependency:resolve - 依赖解析分析


### 4.1 resolve命令的用途


**dependency:resolve**命令帮你了解Maven是如何解析和下载依赖的，特别适合排查依赖下载问题。

**基本用法**：
```bash
mvn dependency:resolve
```

### 4.2 解析过程分析


**输出示例**：
```
[INFO] The following files have been resolved:
[INFO]    org.springframework:spring-core:jar:5.3.21:compile
[INFO]    org.springframework:spring-jcl:jar:5.3.21:compile  
[INFO]    org.springframework:spring-beans:jar:5.3.21:compile
[INFO]    junit:junit:jar:4.13.2:test
[INFO]    org.hamcrest:hamcrest-core:jar:1.3:test
```

这个命令告诉你：
- 哪些依赖被成功解析了
- 依赖的具体版本
- 依赖的作用域

### 4.3 常用参数


**只解析特定作用域**：
```bash
# 只解析编译期依赖
mvn dependency:resolve -Dscope=compile
```

**查看解析源信息**：
```bash
# 显示从哪个仓库下载的
mvn dependency:resolve -Dverbose=true
```

### 4.4 排查下载问题


当遇到依赖下载失败时，可以这样排查：

```bash
# 强制重新下载所有依赖
mvn dependency:resolve -U

# 查看详细的下载过程
mvn dependency:resolve -X
```

---

## 5. 📊 versions插件 - 版本管理


### 5.1 versions插件介绍


**versions插件**是Maven生态中用于版本管理的强大工具，它能帮你：
- 检查依赖是否有新版本
- 批量更新依赖版本
- 管理项目版本号

### 5.2 安装和配置


在pom.xml中添加versions插件：

```xml
<build>
    <plugins>
        <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>versions-maven-plugin</artifactId>
            <version>2.8.1</version>
        </plugin>
    </plugins>
</build>
```

### 5.3 检查依赖更新


**查看所有依赖的可用更新**：
```bash
mvn versions:display-dependency-updates
```

**输出示例**：
```
[INFO] The following dependencies in Dependencies have newer versions:
[INFO]   org.springframework:spring-core ........ 5.3.21 -> 5.3.23
[INFO]   junit:junit ......................... 4.13.2 -> 4.13.4
```

**查看插件更新**：
```bash
mvn versions:display-plugin-updates
```

### 5.4 批量更新版本


**更新所有依赖到最新版本**：
```bash
# 谨慎使用！建议先在测试环境验证
mvn versions:use-latest-versions
```

**更新特定依赖**：
```bash
# 只更新Spring相关依赖
mvn versions:use-latest-versions -Dincludes=org.springframework:*
```

### 5.5 管理项目版本


```bash
# 设置新的项目版本
mvn versions:set -DnewVersion=2.0.0-SNAPSHOT

# 如果确认无误，提交版本更改
mvn versions:commit

# 如果有问题，回滚版本更改  
mvn versions:revert
```

---

## 6. ⚠️ 依赖冲突检测与解决


### 6.1 什么是依赖冲突


**依赖冲突**就像两个人同时要用同一个座位，Maven不知道该选哪个。

```
冲突产生原因：
项目依赖 A → 引入了 common-utils:1.0
项目依赖 B → 引入了 common-utils:2.0

Maven只能选择一个版本的common-utils，
选择错了可能导致运行时错误。
```

### 6.2 检测冲突的方法


**方法1：使用dependency:tree查看冲突**
```bash
mvn dependency:tree -Dverbose
```

这会显示被排除的依赖：
```
[INFO] +- com.example:module-a:jar:1.0:compile
[INFO] |  \- commons-lang:commons-lang:jar:2.6:compile
[INFO] \- com.example:module-b:jar:1.0:compile  
[INFO]    \- (commons-lang:commons-lang:jar:2.4:compile - omitted for conflict with 2.6)
```

`omitted for conflict`表示版本2.4被排除了，使用2.6版本。

### 6.3 解决冲突的策略


**策略1：显式声明依赖版本**

在pom.xml中明确指定要使用的版本：
```xml
<dependencies>
    <!-- 明确指定使用哪个版本 -->
    <dependency>
        <groupId>commons-lang</groupId>
        <artifactId>commons-lang</artifactId>
        <version>2.6</version>
    </dependency>
    
    <dependency>
        <groupId>com.example</groupId>
        <artifactId>module-a</artifactId>
        <version>1.0</version>
    </dependency>
    
    <dependency>
        <groupId>com.example</groupId>
        <artifactId>module-b</artifactId>
        <version>1.0</version>
    </dependency>
</dependencies>
```

**策略2：使用dependencyManagement统一管理**

```xml
<dependencyManagement>
    <dependencies>
        <!-- 在这里统一定义版本 -->
        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
            <version>2.6</version>
        </dependency>
    </dependencies>
</dependencyManagement>

<dependencies>
    <!-- 这里不需要指定版本，会使用上面定义的版本 -->
    <dependency>
        <groupId>commons-lang</groupId>
        <artifactId>commons-lang</artifactId>
    </dependency>
</dependencies>
```

**策略3：排除传递性依赖**

```xml
<dependency>
    <groupId>com.example</groupId>
    <artifactId>module-b</artifactId>
    <version>1.0</version>
    <exclusions>
        <!-- 排除它带来的commons-lang -->
        <exclusion>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
        </exclusion>
    </exclusions>
</dependency>
```

---

## 7. 🎨 依赖图可视化工具


### 7.1 IDEA插件 - Maven Helper


**安装方式**：
1. 打开IDEA → File → Settings → Plugins
2. 搜索"Maven Helper"
3. 安装并重启IDEA

**使用方法**：
1. 打开pom.xml文件
2. 底部会出现"Dependency Analyzer"标签
3. 点击可以看到图形化的依赖关系

**功能特点**：
```
Maven Helper功能：
✅ 图形化显示依赖关系
✅ 高亮显示冲突的依赖  
✅ 一键排除依赖
✅ 搜索特定依赖
✅ 显示依赖大小
```

### 7.2 在线工具 - Maven Dependency Analyzer


**使用方法**：
1. 复制你的pom.xml内容
2. 访问在线分析工具网站
3. 粘贴内容进行分析

### 7.3 命令行可视化


**使用dependency插件生成图表**：

```xml
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-dependency-plugin</artifactId>
    <version>3.2.0</version>
    <executions>
        <execution>
            <goals>
                <goal>tree</goal>
            </goals>
            <configuration>
                <outputFile>dependency-tree.txt</outputFile>
            </configuration>
        </execution>
    </executions>
</plugin>
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的命令


```
🔸 基础分析命令
mvn dependency:tree          # 查看完整依赖树
mvn dependency:analyze       # 分析依赖使用情况
mvn dependency:resolve       # 解析依赖详情

🔸 版本管理命令  
mvn versions:display-dependency-updates  # 检查依赖更新
mvn versions:use-latest-versions         # 更新依赖版本

🔸 冲突排查命令
mvn dependency:tree -Dverbose            # 显示冲突详情
```

### 8.2 依赖分析的最佳实践


**定期检查原则**：
```
📅 建议的检查频率：
- 每次发布前：执行完整依赖分析
- 每月一次：检查依赖更新
- 出现问题时：立即排查依赖冲突
```

**分析优先级**：
```
🎯 按重要性排序：
1. 解决依赖冲突（影响功能）
2. 移除未使用依赖（减少体积） 
3. 更新安全漏洞版本（提高安全性）
4. 升级到最新版本（获得新特性）
```

### 8.3 常见问题解决思路


```
问题诊断流程：
❓ 项目启动失败
  └── mvn dependency:tree 查看依赖树
      └── 检查是否有版本冲突
          └── 使用exclusion或显式版本解决

❓ 打包体积过大  
  └── mvn dependency:analyze 分析未使用依赖
      └── 谨慎移除未使用的依赖
          └── 在测试环境验证功能

❓ 运行时类找不到
  └── mvn dependency:tree -Dincludes=*目标类名*
      └── 确认依赖是否正确引入
          └── 检查作用域是否正确
```

### 8.4 学习建议


**渐进学习路径**：
```
第一阶段：掌握基本命令
- 会用dependency:tree查看依赖
- 理解依赖树的结构含义

第二阶段：学会问题排查  
- 能够发现并解决版本冲突
- 会使用analyze优化依赖

第三阶段：精通版本管理
- 熟练使用versions插件
- 建立项目依赖管理规范
```

**实践建议**：
> 💡 **新手提示**：刚开始不要急于移除所有"未使用"的依赖，先理解项目的运行逻辑，确保移除后不会影响功能。Maven的依赖分析工具是辅助工具，最终的决策还是要结合具体的业务需求。

**记忆口诀**：
```
依赖分析三步走：
tree看结构，analyze找多余，resolve查下载
冲突解决有妙招：  
显式声明、统一管理、排除传递样样好
版本更新要谨慎：
先看更新、测试验证、逐步升级最稳妥
```