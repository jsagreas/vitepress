---
title: 5、Maven目录结构与约定
---
## 📚 目录

1. [Maven目录结构概述](#1-Maven目录结构概述)
2. [标准目录结构详解](#2-标准目录结构详解)
3. [约定优于配置原则](#3-约定优于配置原则)
4. [target目录详解](#4-target目录详解)
5. [实际项目示例](#5-实际项目示例)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 📁 Maven目录结构概述


### 1.1 什么是Maven目录结构


Maven目录结构就像是**房子的标准户型**，每个房间都有固定的位置和用途。想象一下：

```
普通房子                        Maven项目
客厅 → 接待客人                src/main/java → 放主要代码
卧室 → 休息睡觉                src/main/resources → 放配置文件  
书房 → 学习办公                src/test/java → 放测试代码
厨房 → 做饭用餐                target → 放编译结果
```

**🔸 核心理念**：所有Maven项目都采用**相同的目录布局**，这样：
- 任何开发者看到Maven项目都知道代码在哪里
- 工具和IDE能自动识别项目结构
- 团队协作更加顺畅

### 1.2 为什么需要标准目录结构


**传统项目的问题**：
```
项目A：代码放在 code/ 目录
项目B：代码放在 source/ 目录  
项目C：代码放在 java/ 目录
```
👆 每个项目都不一样，新人加入需要重新学习

**Maven的解决方案**：
```
所有项目都是：代码放在 src/main/java/
```
👆 **一次学会，处处适用**

---

## 2. 🏗️ 标准目录结构详解


### 2.1 完整目录结构树形图


```
my-project/                    ← 项目根目录
├── pom.xml                   ← 项目配置文件（相当于项目的身份证）
├── src/                      ← 源码目录（所有代码的家）
│   ├── main/                 ← 主程序目录（正式代码住这里）
│   │   ├── java/             ← Java源代码（.java文件）
│   │   │   └── com/          ← 包结构开始
│   │   │       └── example/  
│   │   │           └── App.java
│   │   ├── resources/        ← 配置和资源文件
│   │   │   ├── application.properties
│   │   │   └── static/       ← 静态资源（图片、CSS等）
│   │   └── webapp/           ← Web应用资源（JSP、HTML等）
│   └── test/                 ← 测试代码目录（测试代码住这里）
│       ├── java/             ← 测试Java代码
│       │   └── com/
│       │       └── example/
│       │           └── AppTest.java
│       └── resources/        ← 测试用的配置文件
└── target/                   ← 编译输出目录（Maven自动创建）
    ├── classes/              ← 编译后的class文件
    ├── test-classes/         ← 编译后的测试class文件
    └── my-project-1.0.jar    ← 最终打包文件
```

### 2.2 核心目录详细解释


#### 📂 src/main/java 目录

**🔸 作用**：存放项目的**主要Java源代码**

通俗理解：这里放的是你**真正要交付给用户的代码**

```java
// src/main/java/com/example/service/UserService.java
package com.example.service;

public class UserService {
    public String getUserName() {
        return "张三";
    }
}
```

**📍 重要特点**：
- 📝 **包结构**：必须按照Java包命名规范组织
- 🎯 **业务代码**：实现具体功能的代码都放这里
- 🚀 **最终打包**：这里的代码会被编译打包到最终的jar文件中

#### 📂 src/main/resources 目录

**🔸 作用**：存放**配置文件和静态资源**

通俗理解：这里放的是代码运行时需要的**"配菜"**

```
src/main/resources/
├── application.properties     ← 应用配置（数据库连接等）
├── logback.xml               ← 日志配置  
├── static/                   ← 静态资源
│   ├── css/
│   ├── js/
│   └── images/
└── templates/                ← 模板文件
    └── index.html
```

**📋 常见文件类型**：
- `🔧 .properties` - 配置文件
- `📋 .xml` - XML配置文件  
- `🎨 .css/.js` - 前端资源
- `📄 .html/.jsp` - 页面模板

#### 📂 src/test/java 目录

**🔸 作用**：存放**测试代码**

通俗理解：这里放的是**检查主代码是否正确的代码**

```java
// src/test/java/com/example/service/UserServiceTest.java
package com.example.service;

import org.junit.Test;
import static org.junit.Assert.*;

public class UserServiceTest {
    @Test
    public void testGetUserName() {
        UserService service = new UserService();
        assertEquals("张三", service.getUserName());
    }
}
```

**🎯 测试代码特点**：
- ✅ **验证功能**：测试主代码是否按预期工作
- 🚫 **不打包**：测试代码不会包含在最终的jar包中
- 📊 **质量保证**：帮助发现和预防bug

### 2.3 目录对应关系表


| 目录类型 | **主目录** | **测试目录** | **用途说明** |
|---------|-----------|-------------|-------------|
| Java源码 | `src/main/java` | `src/test/java` | Java类文件 |
| 资源文件 | `src/main/resources` | `src/test/resources` | 配置和静态文件 |
| Web资源 | `src/main/webapp` | - | JSP、HTML等Web文件 |
| 编译输出 | `target/classes` | `target/test-classes` | 编译后的class文件 |

---

## 3. ⚖️ 约定优于配置原则


### 3.1 什么是约定优于配置


**🔸 简单理解**：Maven说"我们约定好规则，你按规则来，我就能自动帮你处理很多事情"

就像**标准化快餐店**：
- 顾客知道在哪里点餐、取餐、就座
- 员工知道工作流程和职责分工  
- 不需要每次都说明规则

### 3.2 约定带来的好处


**✅ 开发效率提升**
```
传统方式：
1. 告诉工具源码在哪个目录 ← 需要手动配置
2. 告诉工具测试代码在哪里 ← 需要手动配置  
3. 告诉工具输出到哪里 ← 需要手动配置

Maven方式：
什么都不用配置，Maven自动知道！
```

**✅ 团队协作无障碍**
```
新同事加入：
传统项目：需要问"代码在哪？配置文件在哪？"
Maven项目：直接开始工作，目录结构都一样
```

**✅ 工具支持完善**
```
IDE识别：
传统项目：需要手动设置源码目录
Maven项目：IDE自动识别所有目录用途
```

### 3.3 约定的具体体现


**📋 源码约定**
```
约定：Java源码放在 src/main/java
结果：Maven自动编译这个目录下的所有.java文件
```

**📋 测试约定**  
```
约定：测试代码放在 src/test/java
结果：Maven自动运行测试，并生成测试报告
```

**📋 资源约定**
```
约定：配置文件放在 src/main/resources  
结果：Maven自动将这些文件复制到最终的jar包中
```

### 3.4 打破约定的情况


虽然推荐遵循约定，但Maven也允许自定义：

```xml
<!-- 如果你的源码不在标准位置，可以这样配置 -->
<build>
    <sourceDirectory>src/main/myJava</sourceDirectory>
    <testSourceDirectory>src/test/myTest</testSourceDirectory>
</build>
```

**⚠️ 注意**：不建议随意打破约定，会失去标准化的好处

---

## 4. 🎯 target目录详解


### 4.1 target目录是什么


**🔸 简单理解**：target目录就是Maven的**"工作台"**

就像厨房做菜：
- 原料（源码）放在冰箱（src目录）
- 工作台（target目录）用来处理原料
- 最终的菜品（jar包）从工作台输出

### 4.2 target目录结构详解


```
target/                        ← Maven编译输出总目录
├── classes/                   ← 主代码编译结果
│   └── com/
│       └── example/
│           └── App.class      ← 编译后的Java类
├── test-classes/              ← 测试代码编译结果  
│   └── com/
│       └── example/
│           └── AppTest.class
├── maven-status/              ← 编译状态信息
├── surefire-reports/          ← 测试报告
│   ├── TEST-AppTest.xml
│   └── AppTest.txt
├── generated-sources/         ← 自动生成的源码
├── my-project-1.0.jar        ← 最终打包文件
└── maven-archiver/           ← 打包过程信息
    └── pom.properties
```

### 4.3 target目录的生命周期


```
Maven编译过程：
源码(.java) → 编译 → class文件(target/classes) → 打包 → jar文件(target/)

清理过程：
mvn clean → 删除整个target目录 → 下次编译重新生成
```

### 4.4 target目录使用说明


**🔸 自动管理**
- 开发者**不需要**手动创建target目录
- Maven会根据需要自动创建和管理

**🔸 可以删除**
- target目录可以随时删除，不会影响源码
- 下次编译时Maven会重新生成

**🔸 版本控制**
- 通常**不提交**target目录到Git
- `.gitignore`文件中应该包含`target/`

---

## 5. 💼 实际项目示例


### 5.1 创建标准Maven项目


**步骤演示**：使用Maven命令创建项目

```bash
# 创建Maven项目（一行命令生成标准结构）
mvn archetype:generate \
  -DgroupId=com.example \
  -DartifactId=my-web-app \
  -DarchetypeArtifactId=maven-archetype-webapp \
  -DinteractiveMode=false
```

**生成的项目结构**：
```
my-web-app/
├── pom.xml
└── src/
    └── main/
        ├── java/
        │   └── com/
        │       └── example/
        │           └── App.java
        ├── resources/
        └── webapp/
            ├── index.jsp
            └── WEB-INF/
                └── web.xml
```

### 5.2 实际开发中的项目结构


**🌟 Spring Boot项目示例**：
```
my-springboot-app/
├── pom.xml                           ← Maven配置
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/
│   │   │       └── example/
│   │   │           ├── MyApplication.java     ← 主启动类
│   │   │           ├── controller/            ← 控制器
│   │   │           │   └── UserController.java
│   │   │           ├── service/               ← 业务逻辑
│   │   │           │   └── UserService.java
│   │   │           └── model/                 ← 数据模型
│   │   │               └── User.java
│   │   └── resources/
│   │       ├── application.yml                ← 应用配置
│   │       ├── static/                        ← 静态资源
│   │       │   ├── css/
│   │       │   └── js/
│   │       └── templates/                     ← 模板文件
│   │           └── index.html
│   └── test/
│       └── java/
│           └── com/
│               └── example/
│                   └── MyApplicationTest.java ← 测试类
└── target/                                   ← 编译输出
```

### 5.3 常见目录使用场景


| 开发场景 | **使用目录** | **说明** |
|---------|-------------|----------|
| 编写业务代码 | `src/main/java` | 实现具体功能 |
| 配置数据库 | `src/main/resources` | 放置配置文件 |
| 添加图片资源 | `src/main/resources/static` | 静态资源 |
| 编写单元测试 | `src/test/java` | 测试代码 |
| 查看编译结果 | `target/classes` | 编译后的文件 |
| 获取最终程序 | `target/*.jar` | 可执行文件 |

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 标准目录结构：Maven项目有固定的目录布局
🔸 约定优于配置：遵循约定可以减少配置工作
🔸 src/main/java：存放主要的Java源代码
🔸 src/main/resources：存放配置文件和静态资源
🔸 src/test/java：存放测试代码
🔸 target目录：Maven编译和打包的输出目录
```

### 6.2 关键理解要点


**🔹 为什么要有标准结构**
```
统一性：所有Maven项目结构一致，降低学习成本
自动化：工具能自动识别和处理，提高开发效率
协作性：团队成员都知道文件放在哪里，便于协作
```

**🔹 main和test的区别**
```
main目录：正式代码，会被打包到最终产品中
test目录：测试代码，只在开发和测试阶段使用
就像演员和替身：main是正式演出，test是排练彩排
```

**🔹 target目录的特殊性**
```
临时性：可以随时删除，Maven会重新生成
自动性：开发者不需要手动管理
输出性：包含最终的可执行文件
```

### 6.3 实际应用指导


**✅ 开发建议**
- 严格按照Maven标准目录结构组织代码
- 不要随意修改标准目录名称
- 合理利用子目录进行代码分类

**✅ 团队协作**
- 新项目统一使用Maven标准结构
- 在`.gitignore`中排除target目录
- 定期清理target目录释放空间

**✅ 学习路径**
- 先熟悉标准目录结构
- 理解每个目录的用途
- 在实际项目中练习使用

### 6.4 常见问题解答


**🤔 Q: 为什么要分main和test目录？**
A: 就像正式演出和彩排要分开一样，业务代码和测试代码职责不同，分开管理更清晰

**🤔 Q: target目录可以删除吗？**  
A: 完全可以！就像删除垃圾一样，Maven随时可以重新生成

**🤔 Q: 可以不遵循Maven目录约定吗？**
A: 技术上可以，但就像逆行开车一样，虽然能到达目的地，但会增加很多麻烦

**核心记忆口诀**：
- Maven目录有约定，标准结构要遵循
- 主代码main里放，测试代码test中藏
- 配置资源resources，编译结果target装
- 约定优于配置好，团队协作效率高