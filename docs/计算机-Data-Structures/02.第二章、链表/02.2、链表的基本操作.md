---
title: 2、链表的基本操作
---
## 📚 目录

1. [链表操作概述](#1-链表操作概述)
2. [创建与初始化](#2-创建与初始化)
3. [插入操作](#3-插入操作)
4. [删除操作](#4-删除操作)
5. [查找与访问](#5-查找与访问)
6. [遍历操作](#6-遍历操作)

---

## 1. 🎯 链表操作概述


### 1.1 基本操作分类

```
链表的核心操作：

🔸 创建操作：创建空链表、初始化节点
🔸 插入操作：头部插入、尾部插入、中间插入
🔸 删除操作：头部删除、尾部删除、指定值删除
🔸 查找操作：按值查找、按位置访问
🔸 遍历操作：正向遍历、获取信息
🔸 辅助操作：判空、获取长度、清空

操作复杂度：
- 插入/删除（已知位置）：O(1)
- 查找/访问：O(n)
- 遍历：O(n)
```

### 1.2 操作的基本原理

```
链表操作的核心思想：

指针操作：
- 所有操作都基于指针的修改
- 维护节点之间的连接关系
- 注意空指针的处理

内存管理：
- 插入时分配新节点内存
- 删除时释放节点内存（Java由GC处理）
- 避免内存泄漏和悬挂指针

边界处理：
- 空链表的特殊情况
- 单节点链表的处理
- 头尾节点的特殊操作
```

### 1.3 操作的注意事项

```
链表操作常见陷阱：

🔸 空指针异常：
- 操作前检查指针是否为null
- 特别注意链表为空的情况

🔸 指针丢失：
- 修改指针前保存必要的引用
- 按正确顺序修改指针

🔸 边界条件：
- 头节点和尾节点的特殊处理
- 单节点链表的操作

🔸 内存泄漏：
- 删除节点后将引用置为null
- 帮助垃圾回收器回收内存
```

---

## 2. 🛠️ 创建与初始化


### 2.1 节点类定义

```java
// 链表节点定义
public class ListNode {
    int val;
    ListNode next;
    
    // 构造函数
    public ListNode() {}
    
    public ListNode(int val) {
        this.val = val;
    }
    
    public ListNode(int val, ListNode next) {
        this.val = val;
        this.next = next;
    }
}
```

### 2.2 链表类的基本结构

```java
public class LinkedList {
    private ListNode head;  // 头指针
    private int size;       // 链表长度
    
    // 构造函数 - 创建空链表
    public LinkedList() {
        this.head = null;
        this.size = 0;
    }
    
    // 基本信息方法
    public int size() {
        return size;
    }
    
    public boolean isEmpty() {
        return head == null;
    }
    
    public void clear() {
        head = null;
        size = 0;
    }
}
```

### 2.3 链表的创建方式

```java
public class ListCreation {
    
    // 方式1：逐个添加创建
    public ListNode createByAdding() {
        LinkedList list = new LinkedList();
        list.addFirst(3);
        list.addFirst(2);
        list.addFirst(1);
        return list.head; // 结果：1 -> 2 -> 3
    }
    
    // 方式2：直接构造
    public ListNode createDirectly() {
        ListNode node3 = new ListNode(3);
        ListNode node2 = new ListNode(2, node3);
        ListNode node1 = new ListNode(1, node2);
        return node1; // 结果：1 -> 2 -> 3
    }
    
    // 方式3：从数组创建
    public ListNode createFromArray(int[] arr) {
        if (arr.length == 0) return null;
        
        ListNode head = new ListNode(arr[0]);
        ListNode current = head;
        
        for (int i = 1; i < arr.length; i++) {
            current.next = new ListNode(arr[i]);
            current = current.next;
        }
        
        return head;
    }
}
```

---

## 3. ➕ 插入操作


### 3.1 头部插入

```java
// 在链表头部插入节点
public void addFirst(int val) {
    ListNode newNode = new ListNode(val);
    newNode.next = head;  // 新节点指向原头节点
    head = newNode;       // 更新头指针
    size++;
}

/*
插入过程示意：
原链表：head -> [1] -> [2] -> [3] -> null
插入0：  [0] -> [1] -> [2] -> [3] -> null
         ↑
        head
*/
```

### 3.2 尾部插入

```java
// 在链表尾部插入节点
public void addLast(int val) {
    ListNode newNode = new ListNode(val);
    
    if (head == null) {
        head = newNode;  // 空链表情况
    } else {
        ListNode current = head;
        // 找到最后一个节点
        while (current.next != null) {
            current = current.next;
        }
        current.next = newNode;  // 连接新节点
    }
    size++;
}

/*
插入过程示意：
原链表：[1] -> [2] -> [3] -> null
插入4：  [1] -> [2] -> [3] -> [4] -> null
*/
```

### 3.3 指定位置插入

```java
// 在指定位置插入节点
public void add(int index, int val) {
    if (index < 0 || index > size) {
        throw new IndexOutOfBoundsException("Index: " + index);
    }
    
    if (index == 0) {
        addFirst(val);
        return;
    }
    
    ListNode newNode = new ListNode(val);
    ListNode current = head;
    
    // 找到插入位置的前一个节点
    for (int i = 0; i < index - 1; i++) {
        current = current.next;
    }
    
    newNode.next = current.next;  // 新节点指向后续节点
    current.next = newNode;       // 前节点指向新节点
    size++;
}

/*
在位置1插入过程：
原链表：[1] -> [2] -> [3] -> null
步骤1：找到位置0的节点（值为1）
步骤2：newNode.next = current.next  // 新节点指向[2]
步骤3：current.next = newNode       // [1]指向新节点
结果：  [1] -> [新] -> [2] -> [3] -> null
*/
```

### 3.4 插入操作优化

```java
// 带尾指针的优化插入
public class OptimizedLinkedList {
    private ListNode head;
    private ListNode tail;  // 尾指针
    private int size;
    
    // 优化的尾部插入 O(1)
    public void addLast(int val) {
        ListNode newNode = new ListNode(val);
        
        if (head == null) {
            head = tail = newNode;
        } else {
            tail.next = newNode;
            tail = newNode;
        }
        size++;
    }
}
```

---

## 4. ➖ 删除操作


### 4.1 头部删除

```java
// 删除头节点
public int removeFirst() {
    if (head == null) {
        throw new NoSuchElementException("List is empty");
    }
    
    int val = head.val;
    head = head.next;  // 头指针后移
    size--;
    return val;
}

/*
删除过程示意：
原链表：head -> [1] -> [2] -> [3] -> null
删除后：        head -> [2] -> [3] -> null
*/
```

### 4.2 尾部删除

```java
// 删除尾节点
public int removeLast() {
    if (head == null) {
        throw new NoSuchElementException("List is empty");
    }
    
    if (head.next == null) {
        // 只有一个节点
        int val = head.val;
        head = null;
        size--;
        return val;
    }
    
    // 找到倒数第二个节点
    ListNode current = head;
    while (current.next.next != null) {
        current = current.next;
    }
    
    int val = current.next.val;
    current.next = null;  // 断开最后一个节点
    size--;
    return val;
}
```

### 4.3 指定位置删除

```java
// 删除指定位置的节点
public int remove(int index) {
    if (index < 0 || index >= size) {
        throw new IndexOutOfBoundsException("Index: " + index);
    }
    
    if (index == 0) {
        return removeFirst();
    }
    
    ListNode current = head;
    // 找到删除位置的前一个节点
    for (int i = 0; i < index - 1; i++) {
        current = current.next;
    }
    
    int val = current.next.val;
    current.next = current.next.next;  // 跳过要删除的节点
    size--;
    return val;
}

/*
删除位置1的过程：
原链表：[1] -> [2] -> [3] -> [4] -> null
步骤1：找到位置0的节点（值为1）
步骤2：current.next = current.next.next
结果：  [1] -> [3] -> [4] -> null
*/
```

### 4.4 按值删除

```java
// 删除第一个匹配的值
public boolean removeValue(int val) {
    if (head == null) {
        return false;
    }
    
    // 如果头节点就是要删除的
    if (head.val == val) {
        head = head.next;
        size--;
        return true;
    }
    
    ListNode current = head;
    while (current.next != null) {
        if (current.next.val == val) {
            current.next = current.next.next;
            size--;
            return true;
        }
        current = current.next;
    }
    
    return false;  // 未找到
}

// 删除所有匹配的值
public void removeAllValues(int val) {
    // 删除头部所有匹配的节点
    while (head != null && head.val == val) {
        head = head.next;
        size--;
    }
    
    if (head == null) return;
    
    ListNode current = head;
    while (current.next != null) {
        if (current.next.val == val) {
            current.next = current.next.next;
            size--;
        } else {
            current = current.next;
        }
    }
}
```

---

## 5. 🔍 查找与访问


### 5.1 按位置访问

```java
// 获取指定位置的值
public int get(int index) {
    if (index < 0 || index >= size) {
        throw new IndexOutOfBoundsException("Index: " + index);
    }
    
    ListNode current = head;
    for (int i = 0; i < index; i++) {
        current = current.next;
    }
    
    return current.val;
}

// 设置指定位置的值
public void set(int index, int val) {
    if (index < 0 || index >= size) {
        throw new IndexOutOfBoundsException("Index: " + index);
    }
    
    ListNode current = head;
    for (int i = 0; i < index; i++) {
        current = current.next;
    }
    
    current.val = val;
}
```

### 5.2 按值查找

```java
// 查找值的位置
public int indexOf(int val) {
    ListNode current = head;
    int index = 0;
    
    while (current != null) {
        if (current.val == val) {
            return index;
        }
        current = current.next;
        index++;
    }
    
    return -1;  // 未找到
}

// 检查是否包含某值
public boolean contains(int val) {
    return indexOf(val) != -1;
}

// 查找最后一次出现的位置
public int lastIndexOf(int val) {
    int lastIndex = -1;
    int index = 0;
    ListNode current = head;
    
    while (current != null) {
        if (current.val == val) {
            lastIndex = index;
        }
        current = current.next;
        index++;
    }
    
    return lastIndex;
}
```

### 5.3 特殊位置访问

```java
// 获取第一个元素
public int getFirst() {
    if (head == null) {
        throw new NoSuchElementException("List is empty");
    }
    return head.val;
}

// 获取最后一个元素
public int getLast() {
    if (head == null) {
        throw new NoSuchElementException("List is empty");
    }
    
    ListNode current = head;
    while (current.next != null) {
        current = current.next;
    }
    
    return current.val;
}

// 获取中间元素（快慢指针）
public int getMiddle() {
    if (head == null) {
        throw new NoSuchElementException("List is empty");
    }
    
    ListNode slow = head;
    ListNode fast = head;
    
    while (fast.next != null && fast.next.next != null) {
        slow = slow.next;
        fast = fast.next.next;
    }
    
    return slow.val;
}
```

---

## 6. 🔄 遍历操作


### 6.1 基本遍历

```java
// 正向遍历打印
public void printList() {
    ListNode current = head;
    System.out.print("[");
    
    while (current != null) {
        System.out.print(current.val);
        if (current.next != null) {
            System.out.print(", ");
        }
        current = current.next;
    }
    
    System.out.println("]");
}

// 转换为数组
public int[] toArray() {
    int[] arr = new int[size];
    ListNode current = head;
    int index = 0;
    
    while (current != null) {
        arr[index++] = current.val;
        current = current.next;
    }
    
    return arr;
}
```

### 6.2 条件遍历

```java
// 统计满足条件的元素个数
public int countIf(Predicate<Integer> condition) {
    int count = 0;
    ListNode current = head;
    
    while (current != null) {
        if (condition.test(current.val)) {
            count++;
        }
        current = current.next;
    }
    
    return count;
}

// 查找满足条件的第一个元素
public Integer findFirst(Predicate<Integer> condition) {
    ListNode current = head;
    
    while (current != null) {
        if (condition.test(current.val)) {
            return current.val;
        }
        current = current.next;
    }
    
    return null;
}
```

### 6.3 递归遍历

```java
// 递归打印链表
public void printRecursive(ListNode node) {
    if (node == null) {
        System.out.println("null");
        return;
    }
    
    System.out.print(node.val + " -> ");
    printRecursive(node.next);
}

// 递归计算长度
public int lengthRecursive(ListNode node) {
    if (node == null) {
        return 0;
    }
    return 1 + lengthRecursive(node.next);
}

// 递归查找
public boolean containsRecursive(ListNode node, int val) {
    if (node == null) {
        return false;
    }
    
    if (node.val == val) {
        return true;
    }
    
    return containsRecursive(node.next, val);
}
```

### 6.4 操作复杂度总结

```java
/*
链表基本操作的时间复杂度：

插入操作：
- addFirst(): O(1)
- addLast(): O(n) 无尾指针，O(1) 有尾指针
- add(index, val): O(n)

删除操作：
- removeFirst(): O(1)
- removeLast(): O(n)
- remove(index): O(n)
- removeValue(val): O(n)

访问操作：
- get(index): O(n)
- set(index, val): O(n)
- indexOf(val): O(n)
- contains(val): O(n)

遍历操作：
- printList(): O(n)
- toArray(): O(n)

空间复杂度：
- 所有操作的额外空间：O(1)
- 存储n个元素：O(n)

性能要点：
1. 链表的优势在于插入删除的常数时间（已知位置）
2. 访问和查找需要线性时间
3. 尾指针可以优化尾部插入操作
4. 递归实现简洁但有栈空间开销
*/
```