---
title: 3、链表的类型
---
## 📚 目录

1. [链表类型概述](#1-链表类型概述)
2. [单向链表](#2-单向链表)
3. [双向链表](#3-双向链表)
4. [循环链表](#4-循环链表)
5. [特殊链表类型](#5-特殊链表类型)
6. [类型选择指南](#6-类型选择指南)

---

## 1. 🎯 链表类型概述


### 1.1 链表的分类维度

```
按连接方向分类：
🔸 单向链表：节点只有一个指向下一节点的指针
🔸 双向链表：节点有指向前后节点的两个指针

按结构形态分类：
🔸 线性链表：有明确的头尾，呈线性结构
🔸 循环链表：尾节点指向头节点，形成环状

按功能特性分类：
🔸 普通链表：基础的链表结构
🔸 哨兵链表：带有哨兵节点的链表
🔸 跳跃链表：带有多级索引的链表
```

### 1.2 各类型特点对比

```
类型对比表：

类型         | 空间开销 | 操作复杂度 | 适用场景
------------|---------|-----------|----------
单向链表     | 低      | 中等      | 基础场景
双向链表     | 高      | 简单      | 频繁双向操作
循环链表     | 低      | 中等      | 循环处理
哨兵链表     | 稍高    | 简单      | 简化边界处理

选择原则：
- 简单场景：单向链表
- 需要反向遍历：双向链表
- 循环处理：循环链表
- 代码简洁：哨兵链表
```

---

## 2. 📝 单向链表


### 2.1 单向链表结构

```java
// 单向链表节点
public class ListNode {
    int val;
    ListNode next;
    
    public ListNode(int val) {
        this.val = val;
        this.next = null;
    }
}

/*
单向链表示意图：
head → [data|next] → [data|next] → [data|null]
       Node1        Node2        Node3

特点：
- 每个节点只有一个next指针
- 只能单向遍历
- 空间开销最小
- 实现相对简单
*/
```

### 2.2 单向链表实现

```java
public class SinglyLinkedList {
    private ListNode head;
    private int size;
    
    public SinglyLinkedList() {
        this.head = null;
        this.size = 0;
    }
    
    // 头部插入
    public void addFirst(int val) {
        ListNode newNode = new ListNode(val);
        newNode.next = head;
        head = newNode;
        size++;
    }
    
    // 尾部插入
    public void addLast(int val) {
        ListNode newNode = new ListNode(val);
        
        if (head == null) {
            head = newNode;
        } else {
            ListNode current = head;
            while (current.next != null) {
                current = current.next;
            }
            current.next = newNode;
        }
        size++;
    }
    
    // 删除头节点
    public int removeFirst() {
        if (head == null) {
            throw new RuntimeException("List is empty");
        }
        
        int val = head.val;
        head = head.next;
        size--;
        return val;
    }
}
```

### 2.3 单向链表的优缺点

```
优点：
✅ 内存开销最小
✅ 实现简单
✅ 适合大多数基础场景
✅ 插入删除效率高（已知位置）

缺点：
❌ 只能单向遍历
❌ 删除节点需要找到前驱节点
❌ 无法高效地反向操作
❌ 某些操作实现复杂
```

---

## 3. 🔄 双向链表


### 3.1 双向链表结构

```java
// 双向链表节点
public class DoublyListNode {
    int val;
    DoublyListNode prev;  // 前驱指针
    DoublyListNode next;  // 后继指针
    
    public DoublyListNode(int val) {
        this.val = val;
        this.prev = null;
        this.next = null;
    }
}

/*
双向链表示意图：
null ← [prev|data|next] ↔ [prev|data|next] ↔ [prev|data|next] → null
       Node1              Node2              Node3

特点：
- 每个节点有两个指针
- 可以双向遍历
- 操作更加灵活
- 空间开销较大
*/
```

### 3.2 双向链表实现

```java
public class DoublyLinkedList {
    private DoublyListNode head;
    private DoublyListNode tail;
    private int size;
    
    public DoublyLinkedList() {
        this.head = null;
        this.tail = null;
        this.size = 0;
    }
    
    // 头部插入
    public void addFirst(int val) {
        DoublyListNode newNode = new DoublyListNode(val);
        
        if (head == null) {
            head = tail = newNode;
        } else {
            newNode.next = head;
            head.prev = newNode;
            head = newNode;
        }
        size++;
    }
    
    // 尾部插入
    public void addLast(int val) {
        DoublyListNode newNode = new DoublyListNode(val);
        
        if (tail == null) {
            head = tail = newNode;
        } else {
            tail.next = newNode;
            newNode.prev = tail;
            tail = newNode;
        }
        size++;
    }
    
    // 删除指定节点（已知节点引用）
    public void removeNode(DoublyListNode node) {
        if (node == null) return;
        
        // 处理前驱连接
        if (node.prev != null) {
            node.prev.next = node.next;
        } else {
            head = node.next;  // 删除的是头节点
        }
        
        // 处理后继连接
        if (node.next != null) {
            node.next.prev = node.prev;
        } else {
            tail = node.prev;  // 删除的是尾节点
        }
        
        size--;
    }
    
    // 反向遍历
    public void printReverse() {
        DoublyListNode current = tail;
        while (current != null) {
            System.out.print(current.val + " ");
            current = current.prev;
        }
        System.out.println();
    }
}
```

### 3.3 双向链表的优缺点

```
优点：
✅ 支持双向遍历
✅ 删除节点更简单（不需要找前驱）
✅ 某些算法实现更高效
✅ 支持从尾部高效操作

缺点：
❌ 内存开销大（多一个指针）
❌ 插入删除时需要维护两个指针
❌ 实现稍微复杂
❌ 缓存性能可能较差
```

---

## 4. 🔁 循环链表


### 4.1 单向循环链表

```java
public class CircularLinkedList {
    private ListNode tail;  // 指向尾节点，方便操作
    private int size;
    
    /*
    循环链表结构：
    tail → [data|next] → [data|next] → [data|next] ↰
           Node3        Node1        Node2       ↱
             ↑                                  ↓
             ←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←
    
    特点：
    - 尾节点的next指向头节点
    - 没有null指针（非空链表）
    - 可以从任意节点访问所有节点
    */
    
    public CircularLinkedList() {
        this.tail = null;
        this.size = 0;
    }
    
    // 插入节点
    public void add(int val) {
        ListNode newNode = new ListNode(val);
        
        if (tail == null) {
            // 空链表
            tail = newNode;
            newNode.next = newNode;  // 指向自己
        } else {
            // 插入到头部
            newNode.next = tail.next;
            tail.next = newNode;
        }
        size++;
    }
    
    // 遍历循环链表
    public void printList() {
        if (tail == null) {
            System.out.println("Empty list");
            return;
        }
        
        ListNode current = tail.next;  // 从头节点开始
        do {
            System.out.print(current.val + " ");
            current = current.next;
        } while (current != tail.next);  // 回到头节点停止
        System.out.println();
    }
    
    // 删除节点
    public boolean remove(int val) {
        if (tail == null) return false;
        
        ListNode current = tail.next;  // 头节点
        ListNode prev = tail;
        
        do {
            if (current.val == val) {
                if (size == 1) {
                    // 只有一个节点
                    tail = null;
                } else {
                    prev.next = current.next;
                    if (current == tail) {
                        tail = prev;  // 删除的是尾节点
                    }
                }
                size--;
                return true;
            }
            prev = current;
            current = current.next;
        } while (current != tail.next);
        
        return false;
    }
}
```

### 4.2 双向循环链表

```java
public class DoublyCircularLinkedList {
    private DoublyListNode head;
    private int size;
    
    /*
    双向循环链表结构：
    ↗→ [prev|data|next] ↔ [prev|data|next] ↔ [prev|data|next] ←↖
    ↑   Node1              Node2              Node3          ↓
    ↖←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←→↗
    
    特点：
    - 结合了双向链表和循环链表的优点
    - 可以双向遍历且形成环状
    - 实现最复杂但功能最强
    */
    
    public void addLast(int val) {
        DoublyListNode newNode = new DoublyListNode(val);
        
        if (head == null) {
            head = newNode;
            newNode.next = newNode.prev = newNode;
        } else {
            DoublyListNode tail = head.prev;
            
            // 插入到尾部
            tail.next = newNode;
            newNode.prev = tail;
            newNode.next = head;
            head.prev = newNode;
        }
        size++;
    }
}
```

---

## 5. 🎯 特殊链表类型


### 5.1 哨兵链表

```java
// 带哨兵节点的链表
public class SentinelLinkedList {
    private ListNode sentinel;  // 哨兵节点
    private int size;
    
    /*
    哨兵链表结构：
    sentinel → [data|next] → [data|next] → [data|null]
    (虚拟)     Node1        Node2        Node3
    
    优点：
    - 简化边界处理
    - 避免空指针检查
    - 代码更简洁
    */
    
    public SentinelLinkedList() {
        sentinel = new ListNode(0);  // 哨兵值不重要
        size = 0;
    }
    
    // 插入操作简化
    public void add(int index, int val) {
        ListNode prev = sentinel;
        for (int i = 0; i < index; i++) {
            prev = prev.next;
        }
        
        ListNode newNode = new ListNode(val);
        newNode.next = prev.next;
        prev.next = newNode;
        size++;
    }
    
    // 删除操作简化
    public void remove(int index) {
        ListNode prev = sentinel;
        for (int i = 0; i < index; i++) {
            prev = prev.next;
        }
        
        prev.next = prev.next.next;
        size--;
    }
}
```

### 5.2 跳跃链表

```java
// 跳跃链表（简化版）
public class SkipList {
    private static final int MAX_LEVEL = 16;
    private SkipListNode head;
    private int level;
    
    /*
    跳跃链表结构：
    Level 3: head ————————————————————→ [30] → null
    Level 2: head ————————→ [20] ———————→ [30] → null  
    Level 1: head ——→ [10] → [20] ———————→ [30] → null
    Level 0: head → [5] → [10] → [15] → [20] → [25] → [30] → null
    
    特点：
    - 多层索引结构
    - 查找平均 O(log n)
    - 插入删除平均 O(log n)
    - 空间换时间的典型应用
    */
    
    class SkipListNode {
        int val;
        SkipListNode[] forward;
        
        SkipListNode(int val, int level) {
            this.val = val;
            this.forward = new SkipListNode[level + 1];
        }
    }
    
    public SkipList() {
        head = new SkipListNode(Integer.MIN_VALUE, MAX_LEVEL);
        level = 0;
    }
    
    // 查找操作
    public boolean search(int target) {
        SkipListNode current = head;
        
        // 从最高层开始向下查找
        for (int i = level; i >= 0; i--) {
            while (current.forward[i] != null && 
                   current.forward[i].val < target) {
                current = current.forward[i];
            }
        }
        
        current = current.forward[0];
        return current != null && current.val == target;
    }
}
```

### 5.3 链表栈和链表队列

```java
// 链表实现栈
public class LinkedStack<T> {
    private Node<T> top;
    
    private class Node<T> {
        T data;
        Node<T> next;
        
        Node(T data) {
            this.data = data;
        }
    }
    
    public void push(T item) {
        Node<T> newNode = new Node<>(item);
        newNode.next = top;
        top = newNode;
    }
    
    public T pop() {
        if (top == null) {
            throw new EmptyStackException();
        }
        T data = top.data;
        top = top.next;
        return data;
    }
}

// 链表实现队列
public class LinkedQueue<T> {
    private Node<T> front;
    private Node<T> rear;
    
    public void enqueue(T item) {
        Node<T> newNode = new Node<>(item);
        
        if (rear == null) {
            front = rear = newNode;
        } else {
            rear.next = newNode;
            rear = newNode;
        }
    }
    
    public T dequeue() {
        if (front == null) {
            throw new NoSuchElementException();
        }
        
        T data = front.data;
        front = front.next;
        
        if (front == null) {
            rear = null;
        }
        
        return data;
    }
}
```

---

## 6. 📋 类型选择指南


### 6.1 选择决策树

```
链表类型选择流程：

需要双向遍历？
├─ 是 → 双向链表
└─ 否 → 继续判断

需要循环处理？
├─ 是 → 循环链表
└─ 否 → 继续判断

需要简化边界处理？
├─ 是 → 哨兵链表
└─ 否 → 单向链表

需要高效查找？
├─ 是 → 跳跃链表
└─ 否 → 根据上述选择
```

### 6.2 应用场景对比

```
各类型适用场景：

单向链表：
✅ 基础的链表操作
✅ 实现栈结构
✅ 简单的顺序处理
✅ 内存受限的环境

双向链表：
✅ 需要反向遍历
✅ 实现双端队列
✅ LRU缓存实现
✅ 浏览器前进后退

循环链表：
✅ 轮询调度算法
✅ 约瑟夫环问题
✅ 循环播放列表
✅ 环形缓冲区

哨兵链表：
✅ 简化代码逻辑
✅ 减少边界判断
✅ 提高代码可读性
✅ 库的内部实现

跳跃链表：
✅ 有序数据快速查找
✅ 数据库索引
✅ Redis的有序集合
✅ 替代平衡树的场景
```

### 6.3 性能对比总结

```
性能特征对比：

操作      | 单向   | 双向   | 循环   | 哨兵   | 跳跃
---------|-------|-------|-------|-------|-------
插入头部  | O(1)  | O(1)  | O(1)  | O(1)  | O(log n)
插入尾部  | O(n)  | O(1)  | O(1)  | O(n)  | O(log n)
删除头部  | O(1)  | O(1)  | O(1)  | O(1)  | O(log n)
删除尾部  | O(n)  | O(1)  | O(n)  | O(n)  | O(log n)
查找     | O(n)  | O(n)  | O(n)  | O(n)  | O(log n)
遍历     | O(n)  | O(n)  | O(n)  | O(n)  | O(n)

空间开销排序：
单向 < 循环 ≈ 哨兵 < 双向 < 跳跃

实现复杂度排序：
单向 < 哨兵 < 循环 < 双向 < 跳跃

选择原则：
1. 优先考虑简单类型（单向链表）
2. 根据具体需求选择特殊类型
3. 性能敏感场景考虑跳跃链表
4. 代码简洁性要求高选择哨兵链表
```