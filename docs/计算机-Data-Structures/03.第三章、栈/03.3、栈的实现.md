---
title: 3、栈的实现
---
## 📚 目录

1. [栈实现概述](#1-栈实现概述)
2. [数组实现栈](#2-数组实现栈)
3. [链表实现栈](#3-链表实现栈)
4. [两种实现对比](#4-两种实现对比)
5. [实现选择指南](#5-实现选择指南)

---

## 1. 🎯 栈实现概述


### 1.1 实现方式分类

```
栈的主要实现方式：

🔸 数组实现（Array-based Stack）：
- 使用数组作为底层存储
- 用top指针指向栈顶
- 固定或动态容量

🔸 链表实现（Linked Stack）：
- 使用链表作为底层存储
- 链表头作为栈顶
- 动态容量

🔸 其他实现：
- 双栈共享数组
- 使用现有数据结构（如ArrayList）
```

### 1.2 实现要素

```
栈实现的核心要素：

数据存储：
- 选择合适的数据结构存储元素
- 维护栈顶位置信息
- 跟踪栈的大小

操作实现：
- push：在栈顶添加元素
- pop：从栈顶移除元素
- peek：查看栈顶元素

状态管理：
- 空栈检测
- 栈满检测（固定大小）
- 大小统计
```

### 1.3 设计考虑

```
栈实现的设计原则：

性能优先：
- 所有基本操作O(1)时间复杂度
- 最小化额外开销
- 良好的空间局部性

安全性：
- 边界检查
- 异常处理
- 内存安全

可扩展性：
- 泛型支持
- 容量管理
- 线程安全考虑
```

---

## 2. 📊 数组实现栈


### 2.1 基本结构

```java
// 数组实现栈的基本结构
public class ArrayStack<T> {
    private T[] elements;
    private int top;          // 栈顶指针
    private int capacity;     // 栈容量
    private static final int DEFAULT_CAPACITY = 10;
    
    @SuppressWarnings("unchecked")
    public ArrayStack() {
        this.capacity = DEFAULT_CAPACITY;
        this.elements = (T[]) new Object[capacity];
        this.top = -1;  // 空栈时top为-1
    }
    
    @SuppressWarnings("unchecked")
    public ArrayStack(int capacity) {
        this.capacity = capacity;
        this.elements = (T[]) new Object[capacity];
        this.top = -1;
    }
}
```

### 2.2 核心操作实现

```java
// 数组栈的核心操作
public class ArrayStackOperations {
    
    // 入栈操作
    public void push(T item) {
        if (isFull()) {
            throw new StackOverflowError("Stack is full");
        }
        elements[++top] = item;
    }
    
    // 出栈操作
    public T pop() {
        if (isEmpty()) {
            throw new EmptyStackException("Stack is empty");
        }
        T item = elements[top];
        elements[top--] = null;  // 清除引用，帮助GC
        return item;
    }
    
    // 查看栈顶
    public T peek() {
        if (isEmpty()) {
            throw new EmptyStackException("Stack is empty");
        }
        return elements[top];
    }
    
    // 栈状态检查
    public boolean isEmpty() {
        return top == -1;
    }
    
    public boolean isFull() {
        return top == capacity - 1;
    }
    
    public int size() {
        return top + 1;
    }
}
```

### 2.3 动态扩容实现

```java
// 动态扩容的数组栈
public class DynamicArrayStack<T> {
    private T[] elements;
    private int top;
    private int capacity;
    private static final int DEFAULT_CAPACITY = 10;
    
    public void push(T item) {
        // 检查是否需要扩容
        if (top == capacity - 1) {
            resize();
        }
        elements[++top] = item;
    }
    
    // 扩容操作
    @SuppressWarnings("unchecked")
    private void resize() {
        int newCapacity = capacity * 2;
        T[] newElements = (T[]) new Object[newCapacity];
        
        // 复制现有元素
        System.arraycopy(elements, 0, newElements, 0, capacity);
        
        elements = newElements;
        capacity = newCapacity;
    }
    
    // 缩容操作（可选）
    public T pop() {
        if (isEmpty()) {
            throw new EmptyStackException("Stack is empty");
        }
        
        T item = elements[top];
        elements[top--] = null;
        
        // 缩容检查
        if (top > 0 && top == capacity / 4) {
            shrink();
        }
        
        return item;
    }
    
    @SuppressWarnings("unchecked")
    private void shrink() {
        int newCapacity = capacity / 2;
        T[] newElements = (T[]) new Object[newCapacity];
        System.arraycopy(elements, 0, newElements, 0, top + 1);
        
        elements = newElements;
        capacity = newCapacity;
    }
}
```

### 2.4 数组实现的完整代码

```java
// 完整的数组栈实现
public class ArrayStack<T> {
    private T[] elements;
    private int top;
    private int capacity;
    private static final int DEFAULT_CAPACITY = 10;
    
    @SuppressWarnings("unchecked")
    public ArrayStack() {
        this.capacity = DEFAULT_CAPACITY;
        this.elements = (T[]) new Object[capacity];
        this.top = -1;
    }
    
    public void push(T item) {
        if (isFull()) {
            throw new StackOverflowError("Stack is full");
        }
        elements[++top] = item;
    }
    
    public T pop() {
        if (isEmpty()) {
            throw new EmptyStackException();
        }
        T item = elements[top];
        elements[top--] = null;
        return item;
    }
    
    public T peek() {
        if (isEmpty()) {
            throw new EmptyStackException();
        }
        return elements[top];
    }
    
    public boolean isEmpty() { return top == -1; }
    public boolean isFull() { return top == capacity - 1; }
    public int size() { return top + 1; }
    
    public void clear() {
        while (!isEmpty()) {
            elements[top--] = null;
        }
    }
}
```

---

## 3. 🔗 链表实现栈


### 3.1 基本结构

```java
// 链表实现栈的基本结构
public class LinkedStack<T> {
    private Node<T> top;  // 栈顶指针
    private int size;     // 栈大小
    
    // 内部节点类
    private static class Node<T> {
        T data;
        Node<T> next;
        
        Node(T data) {
            this.data = data;
            this.next = null;
        }
        
        Node(T data, Node<T> next) {
            this.data = data;
            this.next = next;
        }
    }
    
    public LinkedStack() {
        this.top = null;
        this.size = 0;
    }
}
```

### 3.2 核心操作实现

```java
// 链表栈的核心操作
public class LinkedStackOperations {
    
    // 入栈操作
    public void push(T item) {
        Node<T> newNode = new Node<>(item, top);
        top = newNode;
        size++;
    }
    
    // 出栈操作
    public T pop() {
        if (isEmpty()) {
            throw new EmptyStackException("Stack is empty");
        }
        T item = top.data;
        top = top.next;
        size--;
        return item;
    }
    
    // 查看栈顶
    public T peek() {
        if (isEmpty()) {
            throw new EmptyStackException("Stack is empty");
        }
        return top.data;
    }
    
    // 栈状态检查
    public boolean isEmpty() {
        return top == null;
    }
    
    public int size() {
        return size;
    }
}
```

### 3.3 链表实现的完整代码

```java
// 完整的链表栈实现
public class LinkedStack<T> {
    private Node<T> top;
    private int size;
    
    private static class Node<T> {
        T data;
        Node<T> next;
        
        Node(T data) {
            this.data = data;
        }
    }
    
    public LinkedStack() {
        this.top = null;
        this.size = 0;
    }
    
    public void push(T item) {
        Node<T> newNode = new Node<>(item);
        newNode.next = top;
        top = newNode;
        size++;
    }
    
    public T pop() {
        if (isEmpty()) {
            throw new EmptyStackException();
        }
        T item = top.data;
        top = top.next;
        size--;
        return item;
    }
    
    public T peek() {
        if (isEmpty()) {
            throw new EmptyStackException();
        }
        return top.data;
    }
    
    public boolean isEmpty() {
        return top == null;
    }
    
    public int size() {
        return size;
    }
    
    public void clear() {
        top = null;
        size = 0;
    }
}
```

### 3.4 链表栈的优化

```java
// 优化的链表栈实现
public class OptimizedLinkedStack<T> {
    private Node<T> top;
    private int size;
    
    // 无需维护size的版本
    public int sizeByTraversal() {
        int count = 0;
        Node<T> current = top;
        while (current != null) {
            count++;
            current = current.next;
        }
        return count;
    }
    
    // 批量操作优化
    public void pushAll(T[] items) {
        for (T item : items) {
            push(item);
        }
    }
    
    // 转换为数组
    @SuppressWarnings("unchecked")
    public T[] toArray(Class<T> clazz) {
        T[] array = (T[]) Array.newInstance(clazz, size);
        Node<T> current = top;
        int index = 0;
        
        while (current != null) {
            array[index++] = current.data;
            current = current.next;
        }
        
        return array;
    }
}
```

---

## 4. ⚖️ 两种实现对比


### 4.1 性能对比

```
性能特征对比：

时间复杂度：
操作      | 数组实现 | 链表实现
---------|---------|--------
push     | O(1)*   | O(1)
pop      | O(1)    | O(1)
peek     | O(1)    | O(1)
isEmpty  | O(1)    | O(1)
size     | O(1)    | O(1)**

注：
* 数组实现可能需要扩容O(n)
** 链表实现可以选择不维护size来节省空间
```

### 4.2 空间对比

```java
// 空间使用对比分析
public class SpaceComparison {
    
    /*
    数组实现空间分析：
    - 数据存储：n × elementSize
    - 额外空间：capacity × elementSize（可能有浪费）
    - 指针开销：1个top指针
    - 总空间：O(capacity)
    
    链表实现空间分析：
    - 数据存储：n × elementSize
    - 指针开销：n × pointerSize（每个节点一个next指针）
    - 对象头开销：n × objectHeaderSize
    - 总空间：O(n)但常数因子大
    
    结论：
    - 数组实现：空间紧凑，但可能浪费
    - 链表实现：无浪费，但指针开销大
    */
    
    public void spaceAnalysis() {
        System.out.println("空间使用对比：");
        System.out.println("数组实现：紧凑但可能浪费");
        System.out.println("链表实现：按需分配但开销大");
    }
}
```

### 4.3 缓存性能对比

```java
// 缓存性能对比
public class CachePerformance {
    
    /*
    数组实现的缓存优势：
    - 连续内存分配
    - 良好的空间局部性
    - CPU缓存友好
    - 预取机制有效
    
    链表实现的缓存劣势：
    - 节点分散在内存中
    - 差的空间局部性
    - 缓存命中率低
    - 频繁的内存跳转
    */
    
    public void cachePerformanceTest() {
        int iterations = 1000000;
        
        // 数组栈测试
        ArrayStack<Integer> arrayStack = new ArrayStack<>();
        long startTime = System.nanoTime();
        
        for (int i = 0; i < iterations; i++) {
            arrayStack.push(i);
        }
        for (int i = 0; i < iterations; i++) {
            arrayStack.pop();
        }
        
        long arrayTime = System.nanoTime() - startTime;
        
        // 链表栈测试
        LinkedStack<Integer> linkedStack = new LinkedStack<>();
        startTime = System.nanoTime();
        
        for (int i = 0; i < iterations; i++) {
            linkedStack.push(i);
        }
        for (int i = 0; i < iterations; i++) {
            linkedStack.pop();
        }
        
        long linkedTime = System.nanoTime() - startTime;
        
        System.out.println("数组栈时间: " + arrayTime / 1_000_000 + "ms");
        System.out.println("链表栈时间: " + linkedTime / 1_000_000 + "ms");
    }
}
```

### 4.4 详细对比表

```
特性详细对比：

特性          | 数组实现        | 链表实现
-------------|---------------|-------------
内存使用      | 连续，可能浪费   | 分散，按需分配
缓存性能      | 优秀           | 较差
实现复杂度    | 中等           | 简单
容量限制      | 有（可扩容）     | 无
内存开销      | 低             | 高（指针+对象头）
线程安全      | 易于实现        | 易于实现
调试友好      | 较好           | 一般
```

---

## 5. 📋 实现选择指南


### 5.1 选择决策树

```
栈实现选择流程：

是否有明确的容量上限？
├─ 有 → 数组实现
└─ 无 → 继续判断

对内存使用是否敏感？
├─ 是 → 数组实现
└─ 否 → 继续判断

是否需要频繁的push/pop操作？
├─ 是 → 数组实现（缓存友好）
└─ 否 → 链表实现

是否需要简单的实现？
├─ 是 → 链表实现
└─ 否 → 数组实现
```

### 5.2 应用场景推荐

```java
// 不同场景的实现选择
public class ImplementationChoice {
    
    // 场景1：函数调用栈 - 推荐数组实现
    /*
    特点：
    - 深度相对固定
    - 频繁的push/pop
    - 对性能要求高
    - 空间局部性重要
    */
    
    // 场景2：表达式求值 - 推荐数组实现
    /*
    特点：
    - 栈深度有限
    - 操作频繁
    - 需要高性能
    */
    
    // 场景3：撤销操作 - 推荐链表实现
    /*
    特点：
    - 栈深度不确定
    - 操作相对不频繁
    - 灵活性重要
    */
    
    // 场景4：内存受限环境 - 推荐数组实现
    /*
    特点：
    - 内存使用关键
    - 需要紧凑存储
    - 性能重要
    */
    
    public void choiceRecommendation() {
        System.out.println("选择建议：");
        System.out.println("高性能场景 → 数组实现");
        System.out.println("灵活性场景 → 链表实现");
        System.out.println("内存敏感 → 数组实现");
        System.out.println("简单实现 → 链表实现");
    }
}
```

### 5.3 混合实现策略

```java
// 混合实现策略
public class HybridStackImplementation<T> {
    private ArrayStack<T> arrayStack;
    private LinkedStack<T> linkedStack;
    private boolean useArray;
    private static final int ARRAY_THRESHOLD = 1000;
    
    public HybridStackImplementation() {
        this.arrayStack = new ArrayStack<>();
        this.linkedStack = new LinkedStack<>();
        this.useArray = true;
    }
    
    public void push(T item) {
        if (useArray) {
            if (arrayStack.size() < ARRAY_THRESHOLD) {
                arrayStack.push(item);
            } else {
                // 切换到链表实现
                switchToLinked();
                linkedStack.push(item);
            }
        } else {
            linkedStack.push(item);
        }
    }
    
    public T pop() {
        if (useArray) {
            return arrayStack.pop();
        } else {
            T item = linkedStack.pop();
            if (linkedStack.size() < ARRAY_THRESHOLD / 2) {
                switchToArray();
            }
            return item;
        }
    }
    
    private void switchToLinked() {
        // 将数组栈的内容转移到链表栈
        while (!arrayStack.isEmpty()) {
            linkedStack.push(arrayStack.pop());
        }
        useArray = false;
    }
    
    private void switchToArray() {
        // 将链表栈的内容转移到数组栈
        while (!linkedStack.isEmpty()) {
            arrayStack.push(linkedStack.pop());
        }
        useArray = true;
    }
}
```

### 5.4 实现总结

```java
/*
栈实现总结：

数组实现优势：
✅ 缓存友好，性能高
✅ 内存紧凑，空间效率高
✅ 实现相对简单
✅ 支持随机访问（虽然栈不需要）

数组实现劣势：
❌ 固定容量限制（除非动态扩容）
❌ 扩容时性能损失
❌ 可能浪费内存空间

链表实现优势：
✅ 动态容量，无大小限制
✅ 内存使用精确，无浪费
✅ 实现简单直观
✅ 插入删除操作简单

链表实现劣势：
❌ 指针开销大
❌ 缓存性能差
❌ 内存分散，局部性差

选择建议：
1. 默认选择数组实现（性能更好）
2. 需要无限容量时选择链表实现
3. 内存敏感场景选择数组实现
4. 实现简单性优先选择链表实现
5. 可以考虑混合实现策略

实际应用：
- Java的Stack类：基于Vector（动态数组）
- C++的std::stack：默认基于deque
- 大多数情况下推荐数组实现
*/
```