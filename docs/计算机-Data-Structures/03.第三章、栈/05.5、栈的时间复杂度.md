---
title: 5、栈的时间复杂度
---
## 📚 目录

1. [栈时间复杂度概述](#1-栈时间复杂度概述)
2. [基本操作复杂度](#2-基本操作复杂度)
3. [不同实现的复杂度对比](#3-不同实现的复杂度对比)
4. [复杂应用的复杂度分析](#4-复杂应用的复杂度分析)
5. [性能优化策略](#5-性能优化策略)

---

## 1. 🎯 栈时间复杂度概述


### 1.1 栈操作的复杂度特征

```
栈时间复杂度的核心特点：

🔸 基本操作高效：
- 所有核心操作都是O(1)
- 不依赖于栈的大小
- 操作时间可预测

🔸 访问限制：
- 只能访问栈顶元素
- 无法进行随机访问
- 查找操作需要O(n)时间

🔸 内存访问模式：
- 顺序访问栈顶附近元素
- 良好的空间局部性
- 缓存友好的操作
```

### 1.2 复杂度分析的关键因素

```
影响栈操作复杂度的因素：

实现方式：
- 数组实现：固定容量vs动态扩容
- 链表实现：节点分配开销
- 混合实现：切换策略开销

操作类型：
- 基本操作：push、pop、peek
- 状态查询：isEmpty、size
- 扩展操作：search、clear

数据规模：
- 栈中元素数量n
- 操作的频率
- 内存使用情况
```

### 1.3 分析方法

```
栈复杂度分析的方法：

最坏情况分析：
- 考虑最不利的操作序列
- 评估操作的上界
- 保证性能的下限

平均情况分析：
- 考虑典型的使用模式
- 评估期望性能
- 实际应用的参考

摊还分析：
- 考虑操作序列的整体代价
- 分摊昂贵操作的成本
- 动态数据结构的分析方法
```

---

## 2. 📊 基本操作复杂度


### 2.1 核心操作分析

```java
// 栈基本操作的时间复杂度分析
public class BasicOperationComplexity {
    
    // push操作 - O(1)
    public void push(T item) {
        /*
        数组实现：
        1. 检查容量：O(1)
        2. 在top位置赋值：O(1)
        3. 更新top指针：O(1)
        总复杂度：O(1)
        
        链表实现：
        1. 创建新节点：O(1)
        2. 设置指针：O(1)
        3. 更新头指针：O(1)
        总复杂度：O(1)
        */
        
        if (isFull()) {
            throw new StackOverflowError();
        }
        elements[++top] = item;
    }
    
    // pop操作 - O(1)
    public T pop() {
        /*
        数组实现：
        1. 检查是否为空：O(1)
        2. 获取top位置元素：O(1)
        3. 清除引用：O(1)
        4. 更新top指针：O(1)
        总复杂度：O(1)
        
        链表实现：
        1. 检查是否为空：O(1)
        2. 获取头节点数据：O(1)
        3. 更新头指针：O(1)
        总复杂度：O(1)
        */
        
        if (isEmpty()) {
            throw new EmptyStackException();
        }
        T item = elements[top];
        elements[top--] = null;
        return item;
    }
    
    // peek操作 - O(1)
    public T peek() {
        /*
        操作分析：
        1. 检查是否为空：O(1)
        2. 返回栈顶元素：O(1)
        总复杂度：O(1)
        
        特点：
        - 只读操作，不修改栈状态
        - 无需内存分配或释放
        - 最快的栈操作
        */
        
        if (isEmpty()) {
            throw new EmptyStackException();
        }
        return elements[top];
    }
}
```

### 2.2 状态查询操作

```java
// 状态查询操作的复杂度
public class StateQueryComplexity {
    
    // isEmpty操作 - O(1)
    public boolean isEmpty() {
        /*
        数组实现：return top == -1;
        链表实现：return head == null;
        
        复杂度分析：
        - 只需检查一个条件
        - 不依赖于栈大小
        - 时间复杂度：O(1)
        */
        return top == -1;
    }
    
    // size操作 - O(1)
    public int size() {
        /*
        实现方式1：维护size变量
        return size;  // O(1)
        
        实现方式2：通过指针计算
        return top + 1;  // O(1)
        
        实现方式3：遍历计算（不推荐）
        int count = 0;
        Node current = head;
        while (current != null) {  // O(n)
            count++;
            current = current.next;
        }
        return count;
        */
        return top + 1;
    }
    
    // isFull操作 - O(1)（仅适用于固定大小栈）
    public boolean isFull() {
        /*
        固定大小栈：
        return top == capacity - 1;  // O(1)
        
        动态栈：
        return false;  // 理论上不会满
        
        复杂度分析：
        - 简单的数值比较
        - 时间复杂度：O(1)
        */
        return top == capacity - 1;
    }
}
```

### 2.3 扩展操作复杂度

```java
// 扩展操作的复杂度分析
public class ExtendedOperationComplexity {
    
    // search操作 - O(n)
    public int search(T item) {
        /*
        操作分析：
        1. 从栈顶开始遍历：O(n)最坏情况
        2. 逐个比较元素：O(1)每次比较
        3. 返回相对位置：O(1)
        
        复杂度分析：
        - 最好情况：O(1) - 栈顶就是目标
        - 平均情况：O(n/2) = O(n)
        - 最坏情况：O(n) - 栈底或不存在
        
        注意：违背了栈的设计原则
        */
        
        for (int i = top; i >= 0; i--) {
            if (elements[i].equals(item)) {
                return top - i + 1;
            }
        }
        return -1;
    }
    
    // clear操作 - O(n)
    public void clear() {
        /*
        方法1：逐个清除引用
        while (!isEmpty()) {
            elements[top--] = null;  // O(n)
        }
        
        方法2：批量清除
        Arrays.fill(elements, 0, top + 1, null);  // O(n)
        
        复杂度分析：
        - 需要清除所有元素的引用
        - 时间复杂度：O(n)
        - 空间复杂度：O(1)
        */
        
        for (int i = 0; i <= top; i++) {
            elements[i] = null;
        }
        top = -1;
    }
    
    // toArray操作 - O(n)
    public T[] toArray() {
        /*
        操作分析：
        1. 创建新数组：O(1)
        2. 复制所有元素：O(n)
        3. 返回数组：O(1)
        
        复杂度分析：
        - 时间复杂度：O(n)
        - 空间复杂度：O(n)
        */
        
        T[] array = (T[]) new Object[size()];
        System.arraycopy(elements, 0, array, 0, size());
        return array;
    }
}
```

---

## 3. ⚖️ 不同实现的复杂度对比


### 3.1 数组实现复杂度

```java
// 数组实现的复杂度特征
public class ArrayStackComplexity {
    
    /*
    固定大小数组栈：
    操作      | 时间复杂度 | 空间复杂度
    ---------|-----------|----------
    push     | O(1)      | O(1)
    pop      | O(1)      | O(1)
    peek     | O(1)      | O(1)
    isEmpty  | O(1)      | O(1)
    size     | O(1)      | O(1)
    search   | O(n)      | O(1)
    clear    | O(n)      | O(1)
    
    优点：
    - 所有基本操作都是O(1)
    - 内存使用紧凑
    - 缓存友好
    
    缺点：
    - 容量固定，可能溢出
    - 可能浪费内存空间
    */
    
    // 动态扩容的复杂度分析
    public void pushWithResize(T item) {
        if (top == capacity - 1) {
            resize();  // 偶尔O(n)
        }
        elements[++top] = item;  // 通常O(1)
    }
    
    /*
    动态扩容分析：
    - 单次push：通常O(1)，偶尔O(n)
    - 摊还分析：O(1)
    - n次push的总成本：O(n)
    - 平均每次push：O(1)
    */
}
```

### 3.2 链表实现复杂度

```java
// 链表实现的复杂度特征
public class LinkedStackComplexity {
    
    /*
    链表栈复杂度：
    操作      | 时间复杂度 | 空间复杂度
    ---------|-----------|----------
    push     | O(1)      | O(1)
    pop      | O(1)      | O(1)
    peek     | O(1)      | O(1)
    isEmpty  | O(1)      | O(1)
    size     | O(1)*     | O(1)
    search   | O(n)      | O(1)
    clear    | O(1)**    | O(1)
    
    注释：
    * 如果维护size变量，否则O(n)
    ** 只需将头指针置null，GC自动回收
    
    优点：
    - 无容量限制
    - 内存使用精确
    - 实现简单
    
    缺点：
    - 指针开销大
    - 内存不连续，缓存性能差
    - 每次push都需要内存分配
    */
    
    public void push(T item) {
        /*
        链表push分析：
        1. 创建新节点：O(1) - 内存分配
        2. 设置节点数据：O(1)
        3. 更新指针：O(1)
        
        实际考虑：
        - 内存分配可能涉及系统调用
        - 垃圾回收器的影响
        - 内存碎片化问题
        */
        Node<T> newNode = new Node<>(item);
        newNode.next = head;
        head = newNode;
    }
}
```

### 3.3 性能对比测试

```java
// 不同实现的性能对比
public class PerformanceComparison {
    
    public void compareImplementations() {
        int iterations = 1000000;
        
        // 数组栈性能测试
        long arrayTime = testArrayStack(iterations);
        
        // 链表栈性能测试
        long linkedTime = testLinkedStack(iterations);
        
        System.out.println("数组栈时间: " + arrayTime + "ms");
        System.out.println("链表栈时间: " + linkedTime + "ms");
        System.out.println("性能差异: " + (double)linkedTime / arrayTime);
    }
    
    private long testArrayStack(int iterations) {
        ArrayStack<Integer> stack = new ArrayStack<>();
        
        long start = System.currentTimeMillis();
        
        // 入栈测试
        for (int i = 0; i < iterations; i++) {
            stack.push(i);
        }
        
        // 出栈测试
        for (int i = 0; i < iterations; i++) {
            stack.pop();
        }
        
        return System.currentTimeMillis() - start;
    }
    
    private long testLinkedStack(int iterations) {
        LinkedStack<Integer> stack = new LinkedStack<>();
        
        long start = System.currentTimeMillis();
        
        // 入栈测试
        for (int i = 0; i < iterations; i++) {
            stack.push(i);
        }
        
        // 出栈测试
        for (int i = 0; i < iterations; i++) {
            stack.pop();
        }
        
        return System.currentTimeMillis() - start;
    }
    
    /*
    典型测试结果：
    - 数组栈通常比链表栈快2-3倍
    - 主要原因：缓存局部性和内存分配开销
    - 在大量操作时差异更明显
    */
}
```

---

## 4. 🔧 复杂应用的复杂度分析


### 4.1 表达式求值复杂度

```java
// 表达式求值的复杂度分析
public class ExpressionEvaluationComplexity {
    
    // 中缀转后缀 - O(n)
    public String infixToPostfix(String infix) {
        /*
        复杂度分析：
        1. 遍历输入字符串：O(n)
        2. 每个字符最多入栈出栈一次：O(1)均摊
        3. 字符串构建：O(n)
        
        总时间复杂度：O(n)
        空间复杂度：O(n) - 栈的最大深度
        */
        
        Stack<Character> stack = new Stack<>();
        StringBuilder result = new StringBuilder();
        
        for (char c : infix.toCharArray()) {  // O(n)
            if (isOperand(c)) {
                result.append(c);  // O(1)
            } else if (isOperator(c)) {
                while (!stack.isEmpty() && 
                       precedence(stack.peek()) >= precedence(c)) {
                    result.append(stack.pop());  // O(1)
                }
                stack.push(c);  // O(1)
            }
        }
        
        while (!stack.isEmpty()) {
            result.append(stack.pop());  // O(1)
        }
        
        return result.toString();
    }
    
    // 后缀表达式求值 - O(n)
    public int evaluatePostfix(String postfix) {
        /*
        复杂度分析：
        1. 遍历后缀表达式：O(n)
        2. 每个操作数入栈：O(1)
        3. 每个操作符处理：O(1)
        
        总时间复杂度：O(n)
        空间复杂度：O(n) - 栈的最大深度
        */
        
        Stack<Integer> stack = new Stack<>();
        
        for (char c : postfix.toCharArray()) {  // O(n)
            if (Character.isDigit(c)) {
                stack.push(c - '0');  // O(1)
            } else {
                int b = stack.pop();  // O(1)
                int a = stack.pop();  // O(1)
                stack.push(calculate(a, b, c));  // O(1)
            }
        }
        
        return stack.pop();
    }
}
```

### 4.2 括号匹配复杂度

```java
// 括号匹配的复杂度分析
public class BracketMatchingComplexity {
    
    // 括号匹配检查 - O(n)
    public boolean isValid(String s) {
        /*
        复杂度分析：
        1. 遍历字符串：O(n)
        2. 每个字符最多入栈出栈一次：O(1)均摊
        3. 字符匹配检查：O(1)
        
        总时间复杂度：O(n)
        空间复杂度：O(n) - 最坏情况全是左括号
        */
        
        Stack<Character> stack = new Stack<>();
        
        for (char c : s.toCharArray()) {  // O(n)
            if (isLeftBracket(c)) {
                stack.push(c);  // O(1)
            } else if (isRightBracket(c)) {
                if (stack.isEmpty()) {
                    return false;
                }
                if (!matches(stack.pop(), c)) {  // O(1)
                    return false;
                }
            }
        }
        
        return stack.isEmpty();
    }
    
    /*
    最坏情况分析：
    - 输入："(((((((()))))))))"
    - 栈最大深度：n/2
    - 空间复杂度：O(n)
    
    最好情况分析：
    - 输入："()()()()"
    - 栈最大深度：1
    - 空间复杂度：O(1)
    
    平均情况：O(n)
    */
}
```

### 4.3 深度优先搜索复杂度

```java
// DFS的复杂度分析
public class DFSComplexity {
    
    // 图的DFS - O(V + E)
    public void dfs(int[][] graph, int start) {
        /*
        复杂度分析：
        - V：顶点数
        - E：边数
        
        时间复杂度：
        1. 每个顶点最多访问一次：O(V)
        2. 每条边最多检查一次：O(E)
        总时间复杂度：O(V + E)
        
        空间复杂度：
        1. 访问数组：O(V)
        2. 栈空间：O(V) - 最坏情况线性图
        总空间复杂度：O(V)
        */
        
        boolean[] visited = new boolean[graph.length];
        Stack<Integer> stack = new Stack<>();
        
        stack.push(start);
        
        while (!stack.isEmpty()) {
            int vertex = stack.pop();  // O(1)
            
            if (!visited[vertex]) {
                visited[vertex] = true;  // O(1)
                
                for (int neighbor : getNeighbors(graph, vertex)) {  // O(度数)
                    if (!visited[neighbor]) {
                        stack.push(neighbor);  // O(1)
                    }
                }
            }
        }
    }
    
    // 二叉树DFS - O(n)
    public void dfsTree(TreeNode root) {
        /*
        复杂度分析：
        - n：树的节点数
        
        时间复杂度：
        1. 每个节点访问一次：O(n)
        
        空间复杂度：
        1. 栈空间：O(h) - h是树的高度
        2. 最坏情况：O(n) - 退化为链表
        3. 平均情况：O(log n) - 平衡二叉树
        */
        
        if (root == null) return;
        
        Stack<TreeNode> stack = new Stack<>();
        stack.push(root);
        
        while (!stack.isEmpty()) {
            TreeNode node = stack.pop();  // O(1)
            visit(node);  // O(1)
            
            if (node.right != null) {
                stack.push(node.right);  // O(1)
            }
            if (node.left != null) {
                stack.push(node.left);  // O(1)
            }
        }
    }
}
```

---

## 5. 🚀 性能优化策略


### 5.1 实现选择优化

```java
// 根据使用场景选择最优实现
public class OptimizationStrategies {
    
    /*
    优化策略1：根据使用模式选择实现
    
    频繁操作场景：
    - 推荐：数组实现
    - 原因：缓存友好，操作开销小
    
    大小不确定场景：
    - 推荐：链表实现
    - 原因：无容量限制，内存使用精确
    
    内存受限场景：
    - 推荐：数组实现
    - 原因：指针开销小，内存紧凑
    */
    
    // 优化策略2：预分配容量
    public void optimizeArrayStack() {
        /*
        如果预知栈的大概大小，预分配容量：
        - 避免动态扩容的开销
        - 减少内存分配次数
        - 提高性能的稳定性
        */
        
        // 预估栈大小为1000
        ArrayStack<Integer> stack = new ArrayStack<>(1000);
        
        // 避免了多次扩容操作
        for (int i = 0; i < 1000; i++) {
            stack.push(i);  // 始终O(1)
        }
    }
    
    // 优化策略3：批量操作
    public void batchOperations() {
        /*
        批量操作可以减少：
        - 方法调用开销
        - 边界检查开销
        - 提高缓存局部性
        */
        
        ArrayStack<Integer> stack = new ArrayStack<>();
        
        // 批量入栈
        int[] values = {1, 2, 3, 4, 5};
        for (int value : values) {
            stack.push(value);
        }
        
        // 批量出栈
        int[] results = new int[values.length];
        for (int i = 0; i < results.length; i++) {
            results[i] = stack.pop();
        }
    }
}
```

### 5.2 缓存优化

```java
// 缓存友好的栈操作
public class CacheOptimization {
    
    // 优化：减少缓存缺失
    public void cacheOptimizedOperations() {
        /*
        缓存优化原则：
        1. 尽量使用数组实现
        2. 避免频繁的内存分配
        3. 保持数据的空间局部性
        4. 减少指针跳转
        */
        
        // 数组实现的栈操作缓存友好
        ArrayStack<Integer> stack = new ArrayStack<>();
        
        // 连续的push操作，内存访问模式规律
        for (int i = 0; i < 1000; i++) {
            stack.push(i);
        }
        
        // 连续的pop操作，访问相邻内存
        for (int i = 0; i < 1000; i++) {
            stack.pop();
        }
    }
    
    // 链表栈的缓存优化
    public void optimizeLinkedStack() {
        /*
        链表栈的缓存优化技巧：
        1. 节点池：复用节点对象
        2. 内存预分配：减少分配次数
        3. 本地化操作：减少指针跳转
        */
        
        // 使用对象池减少分配开销
        NodePool<Integer> nodePool = new NodePool<>();
        OptimizedLinkedStack<Integer> stack = 
            new OptimizedLinkedStack<>(nodePool);
    }
}
```

### 5.3 复杂度总结

```java
/*
栈时间复杂度总结：

基本操作复杂度：
- push(): O(1)
- pop(): O(1)
- peek(): O(1)
- isEmpty(): O(1)
- size(): O(1)

扩展操作复杂度：
- search(): O(n)
- clear(): O(n)
- toArray(): O(n)

实现对比：
特性        | 数组实现 | 链表实现
-----------|---------|--------
基本操作    | O(1)    | O(1)
内存使用    | 紧凑    | 分散
缓存性能    | 优秀    | 较差
扩容开销    | 偶尔O(n) | 无
空间开销    | 低      | 高

应用场景复杂度：
- 表达式求值：O(n)
- 括号匹配：O(n)
- DFS遍历：O(V+E)
- 函数调用：O(1)每次调用

优化策略：
1. 选择合适的实现方式
2. 预分配容量避免扩容
3. 批量操作减少开销
4. 利用缓存局部性

关键要点：
- 栈的核心优势在于O(1)的基本操作
- 实现方式影响实际性能
- 应用场景决定复杂度表现
- 缓存友好性很重要
*/
```