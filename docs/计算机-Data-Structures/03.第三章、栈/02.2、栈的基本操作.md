---
title: 2、栈的基本操作
---
## 📚 目录

1. [栈操作概述](#1-栈操作概述)
2. [核心操作详解](#2-核心操作详解)
3. [辅助操作](#3-辅助操作)
4. [操作的组合使用](#4-操作的组合使用)
5. [异常处理](#5-异常处理)

---

## 1. 🎯 栈操作概述


### 1.1 基本操作分类

```
栈的操作分为三类：

🔸 核心操作：
- push(item)：入栈
- pop()：出栈
- peek()/top()：查看栈顶

🔸 状态查询：
- isEmpty()：判断栈是否为空
- size()：获取栈中元素数量

🔸 辅助操作：
- clear()：清空栈
- search(item)：查找元素位置
```

### 1.2 操作的特点

```
栈操作的核心特征：

时间复杂度：
- 所有基本操作都是O(1)
- 不需要遍历或搜索
- 操作效率高且可预测

操作位置：
- 所有操作都在栈顶进行
- 无法直接操作栈中间或底部元素
- 保持LIFO特性

原子性：
- 每个操作都是原子的
- 要么完全成功，要么完全失败
- 不会产生不一致状态
```

### 1.3 操作的前置条件

```
栈操作的限制条件：

push操作：
- 栈未满（对于固定大小的栈）
- 要压入的元素不为null（某些实现）

pop操作：
- 栈非空
- 否则抛出异常或返回特殊值

peek操作：
- 栈非空
- 只读操作，不改变栈状态
```

---

## 2. 🔧 核心操作详解


### 2.1 push操作（入栈）

```java
// push操作的核心实现
public class StackPushOperation {
    
    // 基本push操作
    public void push(T item) {
        // 1. 检查栈是否已满（数组实现）
        if (size >= capacity) {
            throw new StackOverflowError("Stack is full");
        }
        
        // 2. 将元素放入栈顶位置
        elements[top + 1] = item;
        
        // 3. 更新栈顶指针和大小
        top++;
        size++;
    }
    
    /*
    push操作的步骤：
    1. 检查容量限制
    2. 在栈顶位置存放新元素
    3. 更新栈顶指针
    4. 更新元素计数
    
    时间复杂度：O(1)
    空间复杂度：O(1)
    */
}
```

### 2.2 pop操作（出栈）

```java
// pop操作的核心实现
public class StackPopOperation {
    
    // 基本pop操作
    public T pop() {
        // 1. 检查栈是否为空
        if (isEmpty()) {
            throw new EmptyStackException("Stack is empty");
        }
        
        // 2. 获取栈顶元素
        T item = elements[top];
        
        // 3. 清除引用（帮助GC）
        elements[top] = null;
        
        // 4. 更新栈顶指针和大小
        top--;
        size--;
        
        // 5. 返回弹出的元素
        return item;
    }
    
    /*
    pop操作的步骤：
    1. 检查栈是否为空
    2. 保存栈顶元素
    3. 清除栈顶位置的引用
    4. 更新栈顶指针
    5. 返回保存的元素
    
    时间复杂度：O(1)
    空间复杂度：O(1)
    */
}
```

### 2.3 peek操作（查看栈顶）

```java
// peek操作的核心实现
public class StackPeekOperation {
    
    // 基本peek操作
    public T peek() {
        // 1. 检查栈是否为空
        if (isEmpty()) {
            throw new EmptyStackException("Stack is empty");
        }
        
        // 2. 返回栈顶元素（不删除）
        return elements[top];
    }
    
    // 安全的peek操作（返回null而不是抛异常）
    public T peekOrNull() {
        return isEmpty() ? null : elements[top];
    }
    
    /*
    peek操作的特点：
    - 只读操作，不修改栈状态
    - 不更新栈顶指针或大小
    - 用于检查栈顶元素而不移除
    
    时间复杂度：O(1)
    空间复杂度：O(1)
    */
}
```

### 2.4 操作示例演示

```java
// 栈操作的完整演示
public class StackOperationDemo {
    
    public static void demonstrateOperations() {
        Stack<Integer> stack = new ArrayStack<>();
        
        // 连续入栈
        System.out.println("=== 入栈操作 ===");
        stack.push(10);
        System.out.println("push(10), 栈顶: " + stack.peek());
        
        stack.push(20);
        System.out.println("push(20), 栈顶: " + stack.peek());
        
        stack.push(30);
        System.out.println("push(30), 栈顶: " + stack.peek());
        
        // 查看栈顶
        System.out.println("\n=== 查看栈顶 ===");
        System.out.println("peek(): " + stack.peek());
        System.out.println("栈大小: " + stack.size());
        
        // 连续出栈
        System.out.println("\n=== 出栈操作 ===");
        System.out.println("pop(): " + stack.pop());
        System.out.println("pop(): " + stack.pop());
        System.out.println("剩余栈顶: " + stack.peek());
        
        // 最后出栈
        System.out.println("pop(): " + stack.pop());
        System.out.println("栈是否为空: " + stack.isEmpty());
    }
}
```

---

## 3. 🔍 辅助操作


### 3.1 状态查询操作

```java
// 栈状态查询操作
public class StackStateOperations {
    
    // 检查栈是否为空
    public boolean isEmpty() {
        return size == 0;
        // 或者: return top == -1;
    }
    
    // 获取栈大小
    public int size() {
        return size;
    }
    
    // 检查栈是否满（固定大小栈）
    public boolean isFull() {
        return size == capacity;
    }
    
    // 获取栈容量
    public int capacity() {
        return capacity;
    }
    
    /*
    状态查询的特点：
    - 不改变栈的状态
    - 返回栈的当前信息
    - 常用于操作前的条件检查
    
    时间复杂度：O(1)
    */
}
```

### 3.2 清空操作

```java
// 栈清空操作
public class StackClearOperation {
    
    // 清空栈
    public void clear() {
        // 方法1：逐个清除引用
        while (!isEmpty()) {
            elements[top] = null;
            top--;
        }
        size = 0;
        
        // 方法2：重置指针（更高效）
        // for (int i = 0; i <= top; i++) {
        //     elements[i] = null;
        // }
        // top = -1;
        // size = 0;
    }
    
    /*
    clear操作的作用：
    - 移除所有元素
    - 重置栈到初始状态
    - 帮助垃圾回收
    
    时间复杂度：O(n)
    */
}
```

### 3.3 搜索操作

```java
// 栈搜索操作
public class StackSearchOperation {
    
    // 搜索元素（返回距离栈顶的位置）
    public int search(T item) {
        for (int i = top; i >= 0; i--) {
            if (Objects.equals(elements[i], item)) {
                return top - i + 1;  // 返回从栈顶开始的位置
            }
        }
        return -1;  // 未找到
    }
    
    // 检查是否包含某元素
    public boolean contains(T item) {
        return search(item) != -1;
    }
    
    /*
    搜索操作的特点：
    - 从栈顶开始向下搜索
    - 返回相对栈顶的位置
    - 不改变栈的状态
    
    时间复杂度：O(n)
    注意：搜索操作违背了栈的设计原则，慎用
    */
}
```

---

## 4. 🔄 操作的组合使用


### 4.1 常见操作模式

```java
// 栈操作的常见组合模式
public class StackPatterns {
    
    // 模式1：安全的peek和pop
    public T safePopIfNotEmpty(Stack<T> stack) {
        if (!stack.isEmpty()) {
            return stack.pop();
        }
        return null;  // 或抛出自定义异常
    }
    
    // 模式2：条件性push
    public void pushIfNotNull(Stack<T> stack, T item) {
        if (item != null) {
            stack.push(item);
        }
    }
    
    // 模式3：栈顶元素替换
    public void replaceTop(Stack<T> stack, T newItem) {
        if (!stack.isEmpty()) {
            stack.pop();
            stack.push(newItem);
        } else {
            stack.push(newItem);
        }
    }
    
    // 模式4：获取栈顶N个元素
    public List<T> peekTopN(Stack<T> stack, int n) {
        List<T> result = new ArrayList<>();
        List<T> temp = new ArrayList<>();
        
        // 弹出N个元素
        for (int i = 0; i < n && !stack.isEmpty(); i++) {
            T item = stack.pop();
            result.add(item);
            temp.add(item);
        }
        
        // 恢复栈状态
        Collections.reverse(temp);
        for (T item : temp) {
            stack.push(item);
        }
        
        return result;
    }
}
```

### 4.2 栈的复制和比较

```java
// 栈的复制和比较操作
public class StackUtilityOperations {
    
    // 复制栈
    public Stack<T> copy(Stack<T> source) {
        Stack<T> temp = new ArrayStack<>();
        Stack<T> result = new ArrayStack<>();
        
        // 先倒序存储到临时栈
        while (!source.isEmpty()) {
            temp.push(source.pop());
        }
        
        // 再正序恢复到结果栈和源栈
        while (!temp.isEmpty()) {
            T item = temp.pop();
            source.push(item);
            result.push(item);
        }
        
        return result;
    }
    
    // 比较两个栈是否相等
    public boolean equals(Stack<T> stack1, Stack<T> stack2) {
        if (stack1.size() != stack2.size()) {
            return false;
        }
        
        Stack<T> temp1 = new ArrayStack<>();
        Stack<T> temp2 = new ArrayStack<>();
        boolean isEqual = true;
        
        // 逐一比较元素
        while (!stack1.isEmpty() && !stack2.isEmpty()) {
            T item1 = stack1.pop();
            T item2 = stack2.pop();
            
            temp1.push(item1);
            temp2.push(item2);
            
            if (!Objects.equals(item1, item2)) {
                isEqual = false;
                break;
            }
        }
        
        // 恢复栈状态
        restoreStack(stack1, temp1);
        restoreStack(stack2, temp2);
        
        return isEqual;
    }
    
    private void restoreStack(Stack<T> original, Stack<T> temp) {
        while (!temp.isEmpty()) {
            original.push(temp.pop());
        }
    }
}
```

### 4.3 栈的遍历

```java
// 栈的遍历操作
public class StackTraversal {
    
    // 非破坏性遍历（保持栈状态）
    public void traverse(Stack<T> stack, Consumer<T> action) {
        if (stack.isEmpty()) return;
        
        T item = stack.pop();
        action.accept(item);
        
        // 递归遍历剩余元素
        traverse(stack, action);
        
        // 恢复栈状态
        stack.push(item);
    }
    
    // 破坏性遍历（清空栈）
    public void traverseAndClear(Stack<T> stack, Consumer<T> action) {
        while (!stack.isEmpty()) {
            action.accept(stack.pop());
        }
    }
    
    // 转换为数组（保持栈状态）
    public T[] toArray(Stack<T> stack, T[] array) {
        List<T> elements = new ArrayList<>();
        
        // 收集所有元素
        while (!stack.isEmpty()) {
            elements.add(stack.pop());
        }
        
        // 转换为数组
        T[] result = elements.toArray(array);
        
        // 恢复栈状态
        Collections.reverse(elements);
        for (T item : elements) {
            stack.push(item);
        }
        
        return result;
    }
}
```

---

## 5. ⚠️ 异常处理


### 5.1 标准异常类型

```java
// 栈操作的异常处理
public class StackExceptionHandling {
    
    // 空栈异常
    public T pop() {
        if (isEmpty()) {
            throw new EmptyStackException("Cannot pop from empty stack");
        }
        return elements[top--];
    }
    
    public T peek() {
        if (isEmpty()) {
            throw new EmptyStackException("Cannot peek empty stack");
        }
        return elements[top];
    }
    
    // 栈满异常（固定大小栈）
    public void push(T item) {
        if (size >= capacity) {
            throw new StackOverflowError("Stack capacity exceeded");
        }
        elements[++top] = item;
        size++;
    }
    
    // 参数异常
    public void pushNonNull(T item) {
        if (item == null) {
            throw new IllegalArgumentException("Cannot push null element");
        }
        push(item);
    }
}
```

### 5.2 安全操作实现

```java
// 提供安全的栈操作
public class SafeStackOperations {
    
    // 安全的pop操作
    public Optional<T> safePop() {
        return isEmpty() ? Optional.empty() : Optional.of(pop());
    }
    
    // 安全的peek操作
    public Optional<T> safePeek() {
        return isEmpty() ? Optional.empty() : Optional.of(peek());
    }
    
    // 尝试push操作
    public boolean tryPush(T item) {
        try {
            push(item);
            return true;
        } catch (StackOverflowError e) {
            return false;
        }
    }
    
    // 条件性操作
    public void pushIfSpace(T item) {
        if (!isFull()) {
            push(item);
        }
    }
    
    public T popIfNotEmpty(T defaultValue) {
        return isEmpty() ? defaultValue : pop();
    }
}
```

### 5.3 异常处理最佳实践

```java
// 栈操作的最佳实践
public class StackBestPractices {
    
    // 最佳实践1：操作前检查状态
    public void safeOperations(Stack<Integer> stack) {
        // 入栈前检查容量
        if (!stack.isFull()) {
            stack.push(42);
        }
        
        // 出栈前检查是否为空
        if (!stack.isEmpty()) {
            Integer value = stack.pop();
            System.out.println("弹出值: " + value);
        }
    }
    
    // 最佳实践2：使用try-with-resources模式
    public void withErrorHandling(Stack<String> stack) {
        try {
            String value = stack.pop();
            processValue(value);
        } catch (EmptyStackException e) {
            System.out.println("栈为空，无法执行操作");
        }
    }
    
    // 最佳实践3：批量操作的事务性
    public boolean batchPush(Stack<T> stack, List<T> items) {
        // 检查是否有足够空间
        if (stack.size() + items.size() > stack.capacity()) {
            return false;
        }
        
        // 批量插入
        for (T item : items) {
            stack.push(item);
        }
        
        return true;
    }
    
    private void processValue(String value) {
        // 处理逻辑
    }
}
```

### 5.4 操作复杂度总结

```java
/*
栈基本操作的时间复杂度总结：

核心操作：
- push(item): O(1)
- pop(): O(1)
- peek(): O(1)

状态查询：
- isEmpty(): O(1)
- size(): O(1)
- isFull(): O(1)

辅助操作：
- clear(): O(n)
- search(item): O(n)
- contains(item): O(n)

组合操作：
- copy(): O(n)
- equals(): O(n)
- traverse(): O(n)

空间复杂度：
- 所有操作额外空间: O(1)
- 存储n个元素: O(n)

关键要点：
1. 栈的核心优势在于O(1)的基本操作
2. 搜索类操作违背栈的设计原则
3. 异常处理是栈实现的重要组成部分
4. 状态检查是安全操作的基础
*/
```