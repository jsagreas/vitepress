---
title: 2、数组的基本操作
---
## 📚 目录

1. [数组操作概述](#1-数组操作概述)
2. [访问操作](#2-访问操作)
3. [插入操作](#3-插入操作)
4. [删除操作](#4-删除操作)
5. [查找操作](#5-查找操作)
6. [更新操作](#6-更新操作)
7. [遍历操作](#7-遍历操作)
8. [操作复杂度总结](#8-操作复杂度总结)

---

## 1. 🎯 数组操作概述


### 1.1 基本操作分类

```
数组的基本操作可以分为以下几类：

🔸 访问操作（Access）：根据索引获取元素
🔸 插入操作（Insert）：在指定位置添加新元素
🔸 删除操作（Delete）：移除指定位置的元素
🔸 查找操作（Search）：寻找特定元素的位置
🔸 更新操作（Update）：修改指定位置的元素值
🔸 遍历操作（Traverse）：访问所有元素
```

### 1.2 操作的核心原理

```
数组操作基于以下核心原理：
- 地址计算：element_address = base + index × size
- 索引边界：0 ≤ index < length
- 内存连续性：元素在内存中连续存储
- 顺序维护：插入删除需要保持元素的相对顺序
```

### 1.3 操作安全性考虑

```
进行数组操作时需要注意：
✅ 索引边界检查：防止越界访问
✅ 空数组处理：避免对空数组进行不当操作
✅ 容量限制：静态数组不能超出预分配大小
✅ 数据类型匹配：确保操作数据类型正确
```

---

## 2. 🔍 访问操作


### 2.1 基本访问原理

```
数组访问是最基础也是最高效的操作：
- 通过索引直接计算内存地址
- 硬件级别的支持，执行速度极快
- 时间复杂度：O(1)
```

### 2.2 访问操作实现

```java
public class ArrayAccess {
    
    // 基本访问操作
    public int get(int[] arr, int index) {
        // 边界检查
        if (index < 0 || index >= arr.length) {
            throw new ArrayIndexOutOfBoundsException("Index: " + index);
        }
        
        return arr[index];  // O(1) 时间复杂度
    }
    
    // 安全访问（返回默认值）
    public int safeGet(int[] arr, int index, int defaultValue) {
        if (index < 0 || index >= arr.length) {
            return defaultValue;
        }
        return arr[index];
    }
    
    // 获取第一个元素
    public int getFirst(int[] arr) {
        if (arr.length == 0) {
            throw new IllegalStateException("Array is empty");
        }
        return arr[0];
    }
    
    // 获取最后一个元素
    public int getLast(int[] arr) {
        if (arr.length == 0) {
            throw new IllegalStateException("Array is empty");
        }
        return arr[arr.length - 1];
    }
}
```

### 2.3 访问模式分析

```
顺序访问：
for (int i = 0; i < arr.length; i++) {
    process(arr[i]);  // 缓存友好，性能最佳
}

随机访问：
int value = arr[randomIndex];  // 单次访问O(1)，但缓存不友好

反向访问：
for (int i = arr.length - 1; i >= 0; i--) {
    process(arr[i]);  // 仍然是O(1)，但不如顺序访问效率高
}
```

---

## 3. ➕ 插入操作


### 3.1 插入操作原理

```
数组插入操作需要：
1. 为新元素腾出空间
2. 将插入位置后的元素依次后移
3. 在指定位置放入新元素
4. 更新数组长度（动态数组）

时间复杂度分析：
- 尾部插入：O(1)
- 头部插入：O(n)
- 中间插入：O(n)
- 平均情况：O(n)
```

### 3.2 插入操作实现

```java
public class ArrayInsert {
    
    // 在指定位置插入元素（需要扩容的动态数组）
    public int[] insert(int[] arr, int index, int value) {
        // 边界检查
        if (index < 0 || index > arr.length) {
            throw new ArrayIndexOutOfBoundsException("Index: " + index);
        }
        
        // 创建新数组（长度+1）
        int[] newArr = new int[arr.length + 1];
        
        // 复制插入位置之前的元素
        for (int i = 0; i < index; i++) {
            newArr[i] = arr[i];
        }
        
        // 插入新元素
        newArr[index] = value;
        
        // 复制插入位置之后的元素
        for (int i = index; i < arr.length; i++) {
            newArr[i + 1] = arr[i];
        }
        
        return newArr;
    }
    
    // 尾部插入（最高效）
    public int[] append(int[] arr, int value) {
        return insert(arr, arr.length, value);
    }
    
    // 头部插入（最低效）
    public int[] prepend(int[] arr, int value) {
        return insert(arr, 0, value);
    }
    
    // 固定大小数组的插入（需要预留空间）
    public void insertInFixedArray(int[] arr, int size, int index, int value) {
        if (size >= arr.length) {
            throw new IllegalStateException("Array is full");
        }
        
        // 将元素向后移动
        for (int i = size; i > index; i--) {
            arr[i] = arr[i - 1];
        }
        
        // 插入新元素
        arr[index] = value;
    }
}
```

### 3.3 插入操作的性能分析

```
不同位置插入的元素移动次数：

插入位置 | 需要移动的元素数量 | 时间复杂度
--------|-----------------|----------
位置 0   | n 个元素        | O(n)
位置 1   | n-1 个元素      | O(n)
位置 i   | n-i 个元素      | O(n-i)
位置 n   | 0 个元素        | O(1)

平均移动次数：(0 + 1 + 2 + ... + n) / (n+1) = n/2
因此平均时间复杂度为 O(n)
```

---

## 4. ➖ 删除操作


### 4.1 删除操作原理

```
数组删除操作需要：
1. 找到要删除的元素位置
2. 将删除位置后的元素依次前移
3. 更新数组长度（动态数组）
4. 可选：清理最后一个位置（避免内存泄漏）

时间复杂度分析：
- 尾部删除：O(1)
- 头部删除：O(n)
- 中间删除：O(n)
- 平均情况：O(n)
```

### 4.2 删除操作实现

```java
public class ArrayDelete {
    
    // 删除指定位置的元素
    public int[] delete(int[] arr, int index) {
        // 边界检查
        if (index < 0 || index >= arr.length) {
            throw new ArrayIndexOutOfBoundsException("Index: " + index);
        }
        
        // 创建新数组（长度-1）
        int[] newArr = new int[arr.length - 1];
        
        // 复制删除位置之前的元素
        for (int i = 0; i < index; i++) {
            newArr[i] = arr[i];
        }
        
        // 复制删除位置之后的元素
        for (int i = index + 1; i < arr.length; i++) {
            newArr[i - 1] = arr[i];
        }
        
        return newArr;
    }
    
    // 删除第一个元素
    public int[] deleteFirst(int[] arr) {
        if (arr.length == 0) {
            throw new IllegalStateException("Array is empty");
        }
        return delete(arr, 0);
    }
    
    // 删除最后一个元素
    public int[] deleteLast(int[] arr) {
        if (arr.length == 0) {
            throw new IllegalStateException("Array is empty");
        }
        return delete(arr, arr.length - 1);
    }
    
    // 固定大小数组的删除（逻辑删除）
    public int deleteFromFixedArray(int[] arr, int size, int index) {
        if (index < 0 || index >= size) {
            throw new ArrayIndexOutOfBoundsException("Index: " + index);
        }
        
        int deletedValue = arr[index];
        
        // 将元素向前移动
        for (int i = index; i < size - 1; i++) {
            arr[i] = arr[i + 1];
        }
        
        // 清理最后一个位置
        arr[size - 1] = 0;
        
        return deletedValue;
    }
    
    // 删除指定值的第一个匹配元素
    public int[] deleteByValue(int[] arr, int value) {
        // 先查找值的位置
        int index = -1;
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == value) {
                index = i;
                break;
            }
        }
        
        if (index == -1) {
            return arr;  // 未找到，返回原数组
        }
        
        return delete(arr, index);
    }
}
```

### 4.3 删除操作的性能分析

```
不同位置删除的元素移动次数：

删除位置 | 需要移动的元素数量 | 时间复杂度
--------|-----------------|----------
位置 0   | n-1 个元素      | O(n)
位置 1   | n-2 个元素      | O(n)
位置 i   | n-i-1 个元素    | O(n-i)
位置 n-1 | 0 个元素        | O(1)

平均移动次数：(0 + 1 + 2 + ... + n-1) / n = (n-1)/2
因此平均时间复杂度为 O(n)
```

---

## 5. 🔎 查找操作


### 5.1 查找操作分类

```
数组查找操作主要分为：
🔸 线性查找：逐个比较，适用于无序数组
🔸 二分查找：分治思想，适用于有序数组
🔸 哨兵查找：线性查找的优化版本
🔸 插值查找：二分查找的改进版本
```

### 5.2 线性查找实现

```java
public class ArraySearch {
    
    // 基本线性查找
    public int linearSearch(int[] arr, int target) {
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == target) {
                return i;  // 返回第一个匹配的索引
            }
        }
        return -1;  // 未找到
    }
    
    // 查找所有匹配的位置
    public List<Integer> findAllIndices(int[] arr, int target) {
        List<Integer> indices = new ArrayList<>();
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == target) {
                indices.add(i);
            }
        }
        return indices;
    }
    
    // 从后向前查找
    public int linearSearchReverse(int[] arr, int target) {
        for (int i = arr.length - 1; i >= 0; i--) {
            if (arr[i] == target) {
                return i;  // 返回最后一个匹配的索引
            }
        }
        return -1;
    }
    
    // 哨兵查找（减少边界检查）
    public int sentinelSearch(int[] arr, int target) {
        if (arr.length == 0) return -1;
        
        int last = arr[arr.length - 1];
        arr[arr.length - 1] = target;  // 设置哨兵
        
        int i = 0;
        while (arr[i] != target) {
            i++;
        }
        
        arr[arr.length - 1] = last;  // 恢复原值
        
        if (i < arr.length - 1 || last == target) {
            return i;
        }
        return -1;
    }
}
```

### 5.3 二分查找实现

```java
public class BinarySearch {
    
    // 标准二分查找（递归版本）
    public int binarySearchRecursive(int[] arr, int target, int left, int right) {
        if (left > right) {
            return -1;  // 未找到
        }
        
        int mid = left + (right - left) / 2;  // 防止溢出
        
        if (arr[mid] == target) {
            return mid;
        } else if (arr[mid] < target) {
            return binarySearchRecursive(arr, target, mid + 1, right);
        } else {
            return binarySearchRecursive(arr, target, left, mid - 1);
        }
    }
    
    // 标准二分查找（迭代版本）
    public int binarySearchIterative(int[] arr, int target) {
        int left = 0, right = arr.length - 1;
        
        while (left <= right) {
            int mid = left + (right - left) / 2;
            
            if (arr[mid] == target) {
                return mid;
            } else if (arr[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        
        return -1;
    }
    
    // 查找第一个大于等于target的位置
    public int lowerBound(int[] arr, int target) {
        int left = 0, right = arr.length;
        
        while (left < right) {
            int mid = left + (right - left) / 2;
            if (arr[mid] < target) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }
        
        return left;
    }
    
    // 查找第一个大于target的位置
    public int upperBound(int[] arr, int target) {
        int left = 0, right = arr.length;
        
        while (left < right) {
            int mid = left + (right - left) / 2;
            if (arr[mid] <= target) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }
        
        return left;
    }
}
```

### 5.4 查找操作的复杂度分析

```
查找算法的时间复杂度比较：

算法类型   | 最好情况 | 平均情况 | 最坏情况 | 前提条件
----------|---------|---------|---------|----------
线性查找   | O(1)    | O(n)    | O(n)    | 无
二分查找   | O(1)    | O(log n)| O(log n)| 有序数组
哨兵查找   | O(1)    | O(n)    | O(n)    | 无
插值查找   | O(1)    | O(log log n)| O(n)| 均匀分布的有序数组
```

---

## 6. 🔄 更新操作


### 6.1 更新操作原理

```
数组更新操作是最简单的操作：
- 直接通过索引修改元素值
- 不改变数组结构和其他元素
- 时间复杂度：O(1)
- 空间复杂度：O(1)
```

### 6.2 更新操作实现

```java
public class ArrayUpdate {
    
    // 基本更新操作
    public void set(int[] arr, int index, int value) {
        // 边界检查
        if (index < 0 || index >= arr.length) {
            throw new ArrayIndexOutOfBoundsException("Index: " + index);
        }
        
        arr[index] = value;  // O(1) 时间复杂度
    }
    
    // 安全更新（返回是否成功）
    public boolean safeSet(int[] arr, int index, int value) {
        if (index < 0 || index >= arr.length) {
            return false;
        }
        arr[index] = value;
        return true;
    }
    
    // 批量更新
    public void batchUpdate(int[] arr, int[] indices, int[] values) {
        if (indices.length != values.length) {
            throw new IllegalArgumentException("Indices and values length mismatch");
        }
        
        for (int i = 0; i < indices.length; i++) {
            set(arr, indices[i], values[i]);
        }
    }
    
    // 条件更新
    public int conditionalUpdate(int[] arr, int oldValue, int newValue) {
        int updateCount = 0;
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == oldValue) {
                arr[i] = newValue;
                updateCount++;
            }
        }
        return updateCount;
    }
    
    // 范围更新
    public void rangeUpdate(int[] arr, int start, int end, int value) {
        if (start < 0 || end >= arr.length || start > end) {
            throw new IllegalArgumentException("Invalid range");
        }
        
        for (int i = start; i <= end; i++) {
            arr[i] = value;
        }
    }
    
    // 交换两个元素
    public void swap(int[] arr, int i, int j) {
        if (i < 0 || i >= arr.length || j < 0 || j >= arr.length) {
            throw new ArrayIndexOutOfBoundsException("Invalid indices");
        }
        
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}
```

### 6.3 更新操作的变种

```java
// 原子更新操作示例
public class AtomicArrayUpdate {
    
    // 增加值
    public void increment(int[] arr, int index, int delta) {
        if (index >= 0 && index < arr.length) {
            arr[index] += delta;
        }
    }
    
    // 乘以系数
    public void multiply(int[] arr, int index, int factor) {
        if (index >= 0 && index < arr.length) {
            arr[index] *= factor;
        }
    }
    
    // 位运算更新
    public void bitwiseOr(int[] arr, int index, int mask) {
        if (index >= 0 && index < arr.length) {
            arr[index] |= mask;
        }
    }
    
    // 最大值更新
    public void updateMax(int[] arr, int index, int value) {
        if (index >= 0 && index < arr.length) {
            arr[index] = Math.max(arr[index], value);
        }
    }
}
```

---

## 7. 🔄 遍历操作


### 7.1 遍历操作分类

```
数组遍历的常见方式：
🔸 索引遍历：使用索引逐一访问
🔸 增强for循环：直接访问元素值
🔸 迭代器遍历：使用Iterator接口
🔸 流式遍历：使用Stream API
🔸 递归遍历：递归方式访问元素
```

### 7.2 基本遍历实现

```java
public class ArrayTraversal {
    
    // 正向索引遍历
    public void forwardTraversal(int[] arr) {
        for (int i = 0; i < arr.length; i++) {
            System.out.print(arr[i] + " ");
        }
        System.out.println();
    }
    
    // 反向索引遍历
    public void backwardTraversal(int[] arr) {
        for (int i = arr.length - 1; i >= 0; i--) {
            System.out.print(arr[i] + " ");
        }
        System.out.println();
    }
    
    // 增强for循环遍历
    public void enhancedForTraversal(int[] arr) {
        for (int element : arr) {
            System.out.print(element + " ");
        }
        System.out.println();
    }
    
    // 步长遍历
    public void stepTraversal(int[] arr, int step) {
        for (int i = 0; i < arr.length; i += step) {
            System.out.print(arr[i] + " ");
        }
        System.out.println();
    }
    
    // 条件遍历
    public void conditionalTraversal(int[] arr, Predicate<Integer> condition) {
        for (int element : arr) {
            if (condition.test(element)) {
                System.out.print(element + " ");
            }
        }
        System.out.println();
    }
    
    // 递归遍历
    public void recursiveTraversal(int[] arr, int index) {
        if (index >= arr.length) {
            return;
        }
        
        System.out.print(arr[index] + " ");
        recursiveTraversal(arr, index + 1);
    }
}
```

### 7.3 遍历中的常见操作

```java
public class TraversalOperations {
    
    // 计算数组和
    public int calculateSum(int[] arr) {
        int sum = 0;
        for (int element : arr) {
            sum += element;
        }
        return sum;
    }
    
    // 查找最大值
    public int findMax(int[] arr) {
        if (arr.length == 0) {
            throw new IllegalArgumentException("Array is empty");
        }
        
        int max = arr[0];
        for (int i = 1; i < arr.length; i++) {
            if (arr[i] > max) {
                max = arr[i];
            }
        }
        return max;
    }
    
    // 统计满足条件的元素个数
    public int countElements(int[] arr, Predicate<Integer> condition) {
        int count = 0;
        for (int element : arr) {
            if (condition.test(element)) {
                count++;
            }
        }
        return count;
    }
    
    // 复制数组
    public int[] copyArray(int[] arr) {
        int[] copy = new int[arr.length];
        for (int i = 0; i < arr.length; i++) {
            copy[i] = arr[i];
        }
        return copy;
    }
    
    // 数组转换
    public int[] transformArray(int[] arr, Function<Integer, Integer> transformer) {
        int[] result = new int[arr.length];
        for (int i = 0; i < arr.length; i++) {
            result[i] = transformer.apply(arr[i]);
        }
        return result;
    }
}
```

### 7.4 遍历性能优化

```java
public class TraversalOptimization {
    
    // 缓存数组长度（避免重复计算）
    public void optimizedTraversal(int[] arr) {
        int length = arr.length;  // 缓存长度
        for (int i = 0; i < length; i++) {
            // 处理 arr[i]
        }
    }
    
    // 循环展开优化
    public int sumWithUnrolling(int[] arr) {
        int sum = 0;
        int i = 0;
        
        // 4路循环展开
        for (; i < arr.length - 3; i += 4) {
            sum += arr[i] + arr[i + 1] + arr[i + 2] + arr[i + 3];
        }
        
        // 处理剩余元素
        for (; i < arr.length; i++) {
            sum += arr[i];
        }
        
        return sum;
    }
    
    // 内存访问优化（顺序访问）
    public void cacheOptimizedTraversal(int[][] matrix) {
        int rows = matrix.length;
        int cols = matrix[0].length;
        
        // 按行遍历（缓存友好）
        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                // 处理 matrix[i][j]
            }
        }
    }
}
```

---

## 8. 📊 操作复杂度总结


### 8.1 时间复杂度汇总

```
操作类型     | 最好情况 | 平均情况 | 最坏情况 | 说明
------------|---------|---------|---------|-----
访问 Access  | O(1)    | O(1)    | O(1)    | 随机访问
查找 Search  | O(1)    | O(n)    | O(n)    | 线性查找
           | O(1)    | O(log n)| O(log n)| 二分查找（有序）
插入 Insert  | O(1)    | O(n)    | O(n)    | 平均需要移动n/2个元素
删除 Delete  | O(1)    | O(n)    | O(n)    | 平均需要移动n/2个元素
更新 Update  | O(1)    | O(1)    | O(1)    | 直接赋值
遍历 Traverse| O(n)    | O(n)    | O(n)    | 访问所有元素
```

### 8.2 空间复杂度汇总

```
操作类型     | 额外空间复杂度 | 说明
------------|--------------|-----
访问 Access  | O(1)        | 不需要额外空间
查找 Search  | O(1)        | 迭代查找
           | O(log n)    | 递归查找（调用栈）
插入 Insert  | O(n)        | 需要创建新数组（动态数组）
           | O(1)        | 原地操作（固定数组）
删除 Delete  | O(n)        |