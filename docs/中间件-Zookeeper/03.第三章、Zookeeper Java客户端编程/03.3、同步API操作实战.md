---
title: 3、同步API操作实战
---
## 📚 目录

1. [同步API概述](#1-同步API概述)
2. [create同步创建节点](#2-create同步创建节点)
3. [getData同步获取数据](#3-getData同步获取数据)
4. [setData同步设置数据](#4-setData同步设置数据)
5. [getChildren同步列出子节点](#5-getChildren同步列出子节点)
6. [delete同步删除节点](#6-delete同步删除节点)
7. [exists同步检查节点](#7-exists同步检查节点)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🌐 同步API概述


### 1.1 什么是同步API


**📋 核心定义**
```
同步API：调用方法后会阻塞等待，直到操作完成才返回结果
特点：代码执行顺序与书写顺序一致，容易理解和调试
适用场景：对操作顺序有严格要求的业务逻辑
```

**💡 同步 vs 异步**
```
同步操作流程：
客户端 → 发送请求 → 等待响应 → 获得结果 → 继续执行

异步操作流程：
客户端 → 发送请求 → 立即返回 → 继续执行 → 回调处理结果

简单理解：
同步 = 打电话（必须等对方接听才能说话）
异步 = 发短信（发完就能做其他事，对方回复时收到通知）
```

### 1.2 Zookeeper客户端连接


**🔗 建立连接的基本步骤**
```java
// 1. 创建连接配置
String connectString = "localhost:2181"; // Zookeeper服务器地址
int sessionTimeout = 5000; // 会话超时时间（毫秒）

// 2. 创建客户端实例
ZooKeeper zk = new ZooKeeper(connectString, sessionTimeout, null);

// 3. 等待连接建立（重要！）
Thread.sleep(2000); // 简单等待，实际项目中建议用CountDownLatch
```

> 💡 **重要提示**  
> Zookeeper连接是异步建立的，即使创建了ZooKeeper对象，也要等待连接真正建立后才能进行操作

### 1.3 节点路径规则


**📁 节点路径格式**
```
路径规则：
- 必须以 / 开头
- 不能以 / 结尾（根节点除外）
- 路径分隔符只能是 /
- 节点名称不能包含特殊字符

示例：
✅ 正确：/app, /app/config, /app/config/database
❌ 错误：app, /app/, /app//config
```

---

## 2. 🔨 create同步创建节点


### 2.1 create方法详解


**📝 方法签名**
```java
public String create(String path, byte[] data, List<ACL> acl, CreateMode createMode)
```

**参数含义解释**：
- **path**：要创建的节点路径，就像文件系统中的文件路径
- **data**：节点存储的数据，以字节数组形式存储
- **acl**：访问控制列表，控制谁能访问这个节点
- **createMode**：节点类型，决定节点的行为特性

### 2.2 节点类型详解


```
┌─────────────────────────────────────────────┐
│                节点类型                      │
├─────────────────────────────────────────────┤
│  持久节点(PERSISTENT)                       │
│  ├─ 创建后一直存在                          │
│  └─ 需要手动删除                            │
├─────────────────────────────────────────────┤
│  持久顺序节点(PERSISTENT_SEQUENTIAL)        │
│  ├─ 持久节点 + 自动编号                     │
│  └─ 如：/app/node0000000001                │
├─────────────────────────────────────────────┤
│  临时节点(EPHEMERAL)                        │
│  ├─ 客户端断开连接时自动删除                 │
│  └─ 不能创建子节点                          │
├─────────────────────────────────────────────┤
│  临时顺序节点(EPHEMERAL_SEQUENTIAL)         │
│  ├─ 临时节点 + 自动编号                     │
│  └─ 常用于分布式锁                          │
└─────────────────────────────────────────────┘
```

### 2.3 创建节点实战


**🔧 基础创建示例**
```java
public class ZkCreateExample {
    public static void main(String[] args) throws Exception {
        // 建立连接
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, null);
        Thread.sleep(2000); // 等待连接建立
        
        // 创建持久节点
        String path1 = zk.create("/myapp", 
                                "应用根目录".getBytes(), 
                                ZooDefs.Ids.OPEN_ACL_UNSAFE, 
                                CreateMode.PERSISTENT);
        System.out.println("创建成功：" + path1); // 输出：/myapp
        
        // 创建持久顺序节点
        String path2 = zk.create("/myapp/config", 
                                "配置信息".getBytes(), 
                                ZooDefs.Ids.OPEN_ACL_UNSAFE, 
                                CreateMode.PERSISTENT_SEQUENTIAL);
        System.out.println("创建成功：" + path2); // 输出：/myapp/config0000000000
        
        zk.close();
    }
}
```

### 2.4 访问控制列表(ACL)


**🔐 常用ACL设置**
```java
// 完全开放（任何人都可以操作）
ZooDefs.Ids.OPEN_ACL_UNSAFE

// 只有创建者可以操作
ZooDefs.Ids.CREATOR_ALL_ACL

// 只读权限（任何人都可以读，但不能写）
ZooDefs.Ids.READ_ACL_UNSAFE

// 自定义ACL示例
List<ACL> customAcl = Arrays.asList(
    new ACL(ZooDefs.Perms.ALL, new Id("digest", "user:password"))
);
```

### 2.5 异常处理


**⚠️ 常见异常及处理**
```java
try {
    String path = zk.create("/test", "data".getBytes(), 
                           ZooDefs.Ids.OPEN_ACL_UNSAFE, 
                           CreateMode.PERSISTENT);
} catch (KeeperException.NodeExistsException e) {
    System.out.println("节点已存在");
} catch (KeeperException.NoNodeException e) {
    System.out.println("父节点不存在");
} catch (KeeperException e) {
    System.out.println("其他Zookeeper异常：" + e.getMessage());
}
```

---

## 3. 📖 getData同步获取数据


### 3.1 getData方法详解


**📝 方法签名**
```java
public byte[] getData(String path, boolean watch, Stat stat)
```

**参数含义**：
- **path**：要获取数据的节点路径
- **watch**：是否设置监听器，true表示监听该节点的数据变化
- **stat**：用于接收节点的统计信息（可以为null）

### 3.2 节点统计信息(Stat)


**📊 Stat包含的信息**
```
Stat对象包含节点的详细信息：
├─ ctime：创建时间
├─ mtime：最后修改时间  
├─ version：数据版本号
├─ cversion：子节点版本号
├─ dataLength：数据长度
├─ numChildren：子节点数量
└─ ephemeralOwner：临时节点的会话ID
```

### 3.3 获取数据实战


**🔍 基础获取示例**
```java
public class ZkGetDataExample {
    public static void main(String[] args) throws Exception {
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, null);
        Thread.sleep(2000);
        
        // 先创建一个节点用于测试
        zk.create("/testdata", "Hello Zookeeper".getBytes(), 
                 ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
        
        // 获取节点数据（不设置监听）
        byte[] data = zk.getData("/testdata", false, null);
        System.out.println("节点数据：" + new String(data));
        
        // 获取数据同时获取统计信息
        Stat stat = new Stat();
        data = zk.getData("/testdata", false, stat);
        System.out.println("数据：" + new String(data));
        System.out.println("数据版本：" + stat.getVersion());
        System.out.println("创建时间：" + new Date(stat.getCtime()));
        System.out.println("数据长度：" + stat.getDataLength());
        
        zk.close();
    }
}
```

### 3.4 监听数据变化


**👁️ 设置监听器**
```java
// 监听器的作用：当节点数据发生变化时，会收到通知
public class ZkWatchExample implements Watcher {
    
    public static void main(String[] args) throws Exception {
        ZkWatchExample example = new ZkWatchExample();
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, example);
        Thread.sleep(2000);
        
        // 获取数据并设置监听
        byte[] data = zk.getData("/testdata", true, null);
        System.out.println("当前数据：" + new String(data));
        
        // 保持程序运行，等待监听器触发
        Thread.sleep(30000);
        zk.close();
    }
    
    @Override
    public void process(WatchedEvent event) {
        if (event.getType() == Event.EventType.NodeDataChanged) {
            System.out.println("检测到节点数据变化：" + event.getPath());
        }
    }
}
```

---

## 4. ✏️ setData同步设置数据


### 4.1 setData方法详解


**📝 方法签名**
```java
public Stat setData(String path, byte[] data, int version)
```

**参数含义**：
- **path**：要修改的节点路径
- **data**：新的数据内容
- **version**：期望的数据版本号，-1表示不检查版本

### 4.2 版本控制机制


**🔢 版本控制的作用**
```
版本控制解决并发修改问题：

场景：两个客户端同时修改同一个节点
客户端A：读取数据（version=5）→ 修改 → 写入（期望version=5）
客户端B：读取数据（version=5）→ 修改 → 写入（期望version=5）

结果：
- 先执行的客户端成功，版本变为6
- 后执行的客户端失败（version不匹配）
- 避免了数据覆盖问题
```

### 4.3 设置数据实战


**📝 基础修改示例**
```java
public class ZkSetDataExample {
    public static void main(String[] args) throws Exception {
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, null);
        Thread.sleep(2000);
        
        // 创建测试节点
        zk.create("/config", "初始配置".getBytes(), 
                 ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
        
        // 方式1：不检查版本直接修改
        Stat stat1 = zk.setData("/config", "新配置数据".getBytes(), -1);
        System.out.println("修改成功，新版本号：" + stat1.getVersion());
        
        // 方式2：检查版本后修改
        Stat currentStat = new Stat();
        zk.getData("/config", false, currentStat); // 先获取当前版本
        
        Stat stat2 = zk.setData("/config", "更安全的修改".getBytes(), 
                               currentStat.getVersion());
        System.out.println("安全修改成功，新版本号：" + stat2.getVersion());
        
        zk.close();
    }
}
```

### 4.4 版本冲突处理


**⚠️ 处理版本冲突**
```java
try {
    // 假设期望版本号是10，但实际已经是12
    zk.setData("/config", "数据".getBytes(), 10);
} catch (KeeperException.BadVersionException e) {
    System.out.println("版本冲突，需要重新获取最新数据");
    
    // 重新获取最新数据和版本
    Stat stat = new Stat();
    byte[] latestData = zk.getData("/config", false, stat);
    System.out.println("最新版本：" + stat.getVersion());
    
    // 基于最新数据进行修改
    // 这里可以加入业务逻辑来决定如何处理冲突
}
```

---

## 5. 📋 getChildren同步列出子节点


### 5.1 getChildren方法详解


**📝 方法签名**
```java
public List<String> getChildren(String path, boolean watch)
public List<String> getChildren(String path, boolean watch, Stat stat)
```

**参数含义**：
- **path**：父节点路径
- **watch**：是否监听子节点列表的变化
- **stat**：用于接收父节点的统计信息

### 5.2 子节点列表操作


**📁 获取子节点示例**
```java
public class ZkGetChildrenExample {
    public static void main(String[] args) throws Exception {
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, null);
        Thread.sleep(2000);
        
        // 创建父节点和几个子节点用于测试
        zk.create("/services", "服务注册中心".getBytes(), 
                 ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
        
        zk.create("/services/userService", "用户服务".getBytes(), 
                 ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
        
        zk.create("/services/orderService", "订单服务".getBytes(), 
                 ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
        
        // 获取子节点列表
        List<String> children = zk.getChildren("/services", false);
        System.out.println("子节点列表：");
        for (String child : children) {
            System.out.println("  - " + child);
        }
        
        // 获取子节点同时获取父节点统计信息
        Stat parentStat = new Stat();
        children = zk.getChildren("/services", false, parentStat);
        System.out.println("父节点子节点数量：" + parentStat.getNumChildren());
        
        zk.close();
    }
}
```

### 5.3 监听子节点变化


**👁️ 子节点监听器**
```java
public class ZkChildrenWatchExample implements Watcher {
    
    public static void main(String[] args) throws Exception {
        ZkChildrenWatchExample example = new ZkChildrenWatchExample();
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, example);
        Thread.sleep(2000);
        
        // 获取子节点并设置监听
        List<String> children = zk.getChildren("/services", true);
        System.out.println("当前子节点：" + children);
        
        System.out.println("请在另一个客户端添加或删除/services的子节点...");
        Thread.sleep(30000); // 等待监听器触发
        
        zk.close();
    }
    
    @Override
    public void process(WatchedEvent event) {
        if (event.getType() == Event.EventType.NodeChildrenChanged) {
            System.out.println("检测到子节点列表变化：" + event.getPath());
            // 实际应用中，这里会重新获取子节点列表
        }
    }
}
```

### 5.4 遍历整个节点树


**🌳 递归遍历**
```java
public static void printTree(ZooKeeper zk, String path, int depth) throws Exception {
    // 打印当前节点
    String indent = "  ".repeat(depth);
    System.out.println(indent + path.substring(path.lastIndexOf('/') + 1));
    
    // 获取子节点
    try {
        List<String> children = zk.getChildren(path, false);
        for (String child : children) {
            String childPath = path.equals("/") ? "/" + child : path + "/" + child;
            printTree(zk, childPath, depth + 1); // 递归遍历
        }
    } catch (KeeperException.NoNodeException e) {
        // 节点不存在，跳过
    }
}

// 使用示例
printTree(zk, "/", 0); // 从根节点开始遍历
```

---

## 6. 🗑️ delete同步删除节点


### 6.1 delete方法详解


**📝 方法签名**
```java
public void delete(String path, int version)
```

**参数含义**：
- **path**：要删除的节点路径
- **version**：期望的节点版本号，-1表示不检查版本

### 6.2 删除规则和限制


**📜 删除规则**
```
Zookeeper删除节点的规则：
├─ 只能删除没有子节点的节点
├─ 要删除有子节点的节点，必须先删除所有子节点
├─ 临时节点会在客户端断连时自动删除
└─ 版本号必须匹配（除非使用-1）

删除顺序：
父节点
├─ 子节点1 ← 必须先删除
├─ 子节点2 ← 必须先删除
└─ 父节点   ← 最后删除
```

### 6.3 删除节点实战


**🗑️ 基础删除示例**
```java
public class ZkDeleteExample {
    public static void main(String[] args) throws Exception {
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, null);
        Thread.sleep(2000);
        
        // 创建测试节点
        zk.create("/temp", "临时数据".getBytes(), 
                 ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
        
        // 删除节点（不检查版本）
        zk.delete("/temp", -1);
        System.out.println("节点删除成功");
        
        // 创建节点用于版本控制删除测试
        zk.create("/versioned", "版本控制".getBytes(), 
                 ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
        
        // 获取当前版本
        Stat stat = new Stat();
        zk.getData("/versioned", false, stat);
        
        // 使用版本控制删除
        zk.delete("/versioned", stat.getVersion());
        System.out.println("版本控制删除成功");
        
        zk.close();
    }
}
```

### 6.4 递归删除实现


**🔄 递归删除有子节点的父节点**
```java
public static void deleteRecursively(ZooKeeper zk, String path) throws Exception {
    try {
        // 获取子节点列表
        List<String> children = zk.getChildren(path, false);
        
        // 先递归删除所有子节点
        for (String child : children) {
            String childPath = path.equals("/") ? "/" + child : path + "/" + child;
            deleteRecursively(zk, childPath);
        }
        
        // 删除当前节点
        zk.delete(path, -1);
        System.out.println("已删除：" + path);
        
    } catch (KeeperException.NoNodeException e) {
        // 节点不存在，跳过
        System.out.println("节点不存在：" + path);
    }
}

// 使用示例
deleteRecursively(zk, "/services"); // 递归删除整个services树
```

### 6.5 删除异常处理


**⚠️ 常见删除异常**
```java
try {
    zk.delete("/somenode", 5);
} catch (KeeperException.NoNodeException e) {
    System.out.println("要删除的节点不存在");
} catch (KeeperException.NotEmptyException e) {
    System.out.println("节点有子节点，不能直接删除");
    // 需要先删除子节点，或使用递归删除
} catch (KeeperException.BadVersionException e) {
    System.out.println("版本号不匹配，删除失败");
    // 需要重新获取最新版本号
}
```

---

## 7. 🔍 exists同步检查节点


### 7.1 exists方法详解


**📝 方法签名**
```java
public Stat exists(String path, boolean watch)
public Stat exists(String path, Watcher watcher)
```

**返回值含义**：
- **不为null**：节点存在，返回节点的Stat信息
- **为null**：节点不存在

### 7.2 exists的作用


**🎯 exists方法的用途**
```
exists方法的主要作用：
├─ 检查节点是否存在
├─ 获取节点统计信息（如果存在）
├─ 设置节点创建/删除监听器
└─ 避免不必要的异常处理

实际应用场景：
- 创建前检查：避免NodeExistsException
- 操作前验证：确保节点存在再进行操作
- 监听节点创建：等待某个节点被创建
```

### 7.3 检查节点实战


**🔍 基础检查示例**
```java
public class ZkExistsExample {
    public static void main(String[] args) throws Exception {
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, null);
        Thread.sleep(2000);
        
        String nodePath = "/checkme";
        
        // 检查节点是否存在
        Stat stat = zk.exists(nodePath, false);
        if (stat != null) {
            System.out.println("节点存在");
            System.out.println("创建时间：" + new Date(stat.getCtime()));
            System.out.println("版本号：" + stat.getVersion());
            System.out.println("数据长度：" + stat.getDataLength());
        } else {
            System.out.println("节点不存在");
            
            // 节点不存在时创建它
            zk.create(nodePath, "新创建的节点".getBytes(), 
                     ZooDefs.Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
            System.out.println("节点已创建");
        }
        
        zk.close();
    }
}
```

### 7.4 监听节点创建和删除


**👁️ 监听节点生命周期**
```java
public class ZkExistsWatchExample implements Watcher {
    
    public static void main(String[] args) throws Exception {
        ZkExistsWatchExample example = new ZkExistsWatchExample();
        ZooKeeper zk = new ZooKeeper("localhost:2181", 5000, example);
        Thread.sleep(2000);
        
        String watchPath = "/watchme";
        
        // 监听节点存在性变化
        Stat stat = zk.exists(watchPath, true);
        if (stat == null) {
            System.out.println("节点不存在，开始监听节点创建...");
        } else {
            System.out.println("节点存在，开始监听节点删除...");
        }
        
        System.out.println("请在另一个客户端创建或删除节点：" + watchPath);
        Thread.sleep(30000);
        
        zk.close();
    }
    
    @Override
    public void process(WatchedEvent event) {
        String path = event.getPath();
        Event.EventType type = event.getType();
        
        switch (type) {
            case NodeCreated:
                System.out.println("节点被创建：" + path);
                break;
            case NodeDeleted:
                System.out.println("节点被删除：" + path);
                break;
            case NodeDataChanged:
                System.out.println("节点数据变化：" + path);
                break;
            default:
                System.out.println("其他事件：" + type + " " + path);
        }
    }
}
```

### 7.5 安全的节点操作模式


**🛡️ 使用exists避免异常**
```java
public class SafeZkOperations {
    
    // 安全创建：先检查再创建
    public static String safeCreate(ZooKeeper zk, String path, byte[] data) 
            throws Exception {
        Stat stat = zk.exists(path, false);
        if (stat == null) {
            return zk.create(path, data, ZooDefs.Ids.OPEN_ACL_UNSAFE, 
                           CreateMode.PERSISTENT);
        } else {
            System.out.println("节点已存在：" + path);
            return path;
        }
    }
    
    // 安全获取数据：先检查再获取
    public static String safeGetData(ZooKeeper zk, String path) throws Exception {
        Stat stat = zk.exists(path, false);
        if (stat != null) {
            byte[] data = zk.getData(path, false, null);
            return new String(data);
        } else {
            System.out.println("节点不存在：" + path);
            return null;
        }
    }
    
    // 安全删除：先检查再删除
    public static boolean safeDelete(ZooKeeper zk, String path) throws Exception {
        Stat stat = zk.exists(path, false);
        if (stat != null) {
            zk.delete(path, stat.getVersion());
            return true;
        } else {
            System.out.println("节点不存在，无需删除：" + path);
            return false;
        }
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 六大同步API操作对比


| 操作 | **方法** | **主要作用** | **返回值** | **常见用途** |
|------|---------|-------------|-----------|-------------|
| 🔨 **创建** | `create()` | `创建新节点` | `节点路径` | `初始化配置、服务注册` |
| 📖 **获取** | `getData()` | `读取节点数据` | `字节数组` | `读取配置、获取服务信息` |
| ✏️ **设置** | `setData()` | `修改节点数据` | `Stat对象` | `更新配置、修改状态` |
| 📋 **列出** | `getChildren()` | `获取子节点列表` | `字符串列表` | `服务发现、树形遍历` |
| 🗑️ **删除** | `delete()` | `删除节点` | `无` | `清理资源、服务下线` |
| 🔍 **检查** | `exists()` | `检查节点存在性` | `Stat对象或null` | `操作前验证、监听生命周期` |

### 8.2 必须掌握的核心概念


**🔸 同步API特点**
```
同步操作的核心特征：
- 阻塞等待：调用后等待结果返回
- 顺序执行：代码按书写顺序执行
- 异常处理：通过try-catch处理各种异常
- 简单直观：容易理解和调试
```

**🔸 节点路径规则**
```
路径命名规范：
- 必须以/开头
- 使用/作为分隔符
- 不能以/结尾（根节点除外）
- 避免特殊字符
```

**🔸 版本控制机制**
```
版本控制的重要性：
- 防止并发修改冲突
- 确保数据一致性
- 实现乐观锁机制
- 提高系统可靠性
```

### 8.3 实际应用指导


**🎯 使用场景选择**
```
创建节点：
✅ 服务注册、配置初始化
✅ 分布式锁实现
✅ 临时节点做心跳检测

获取数据：
✅ 读取配置信息
✅ 获取服务地址
✅ 监听配置变化

设置数据：
✅ 动态修改配置
✅ 更新服务状态
✅ 实现简单的消息传递

列出子节点：
✅ 服务发现
✅ 树形结构遍历
✅ 监控子服务状态

删除节点：
✅ 服务下线
✅ 清理临时数据
✅ 释放资源

检查存在：
✅ 操作前验证
✅ 监听节点生命周期
✅ 避免异常处理
```

### 8.4 最佳实践建议


**💡 编程建议**
- **连接管理**：合理设置会话超时，及时关闭连接
- **异常处理**：针对不同异常类型进行相应处理
- **版本控制**：重要数据修改时使用版本控制
- **监听器使用**：避免监听器中执行耗时操作
- **路径管理**：使用常量定义重要节点路径

**⚠️ 注意事项**
- 同步API会阻塞当前线程，不适合高并发场景
- 监听器是一次性的，触发后需要重新设置
- 临时节点不能有子节点
- 删除节点前必须先删除所有子节点
- 合理使用ACL控制访问权限

**核心记忆**：
- 同步API简单直观，适合学习和简单应用
- 六大操作覆盖节点的完整生命周期
- 版本控制是保证数据一致性的重要机制
- exists方法是其他操作的安全保障
- 实际项目中要结合业务场景选择合适的操作