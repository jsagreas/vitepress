---
title: 2、镜像基本命令
---
## 📚 目录

1. [Docker镜像基础概念](#1-Docker镜像基础概念)
2. [拉取镜像命令详解](#2-拉取镜像命令详解)
3. [查看镜像信息](#3-查看镜像信息)
4. [镜像标签管理](#4-镜像标签管理)
5. [推送镜像到仓库](#5-推送镜像到仓库)
6. [删除镜像操作](#6-删除镜像操作)
7. [镜像历史与详情](#7-镜像历史与详情)
8. [实用操作技巧](#8-实用操作技巧)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🐳 Docker镜像基础概念


### 1.1 什么是Docker镜像


**🔸 通俗理解**
Docker镜像就像是一个**应用程序的模板**，类似于手机APP的安装包。当你下载一个APP安装包时，这个安装包包含了运行该应用所需的所有文件和配置。Docker镜像也是如此，它包含了运行某个应用程序所需的**完整环境**。

> 💡 **生活类比**  
> 如果把Docker容器比作一栋房子，那么Docker镜像就是建房子的**设计图纸**。你可以用同一份图纸建造多栋相同的房子，每栋房子（容器）都有相同的结构和配置。

### 1.2 镜像的核心特点


**🔹 只读性质**
- 镜像是**只读**的，一旦创建就不能修改
- 就像光盘一样，内容固定不变
- 要修改只能创建新的镜像

**🔹 分层结构**
```
应用程序层 (最上层)
   ↓
运行时环境层
   ↓  
操作系统基础层 (最底层)
```

**🔹 共享机制**
- 相同的层可以在不同镜像间共享
- 节省存储空间，提高效率

### 1.3 镜像与容器的关系


```
镜像 (Image)          容器 (Container)
     |                      |
  静态模板    ──创建──▶    运行实例
  只读文件                可读写环境
  存储在磁盘              运行在内存
     |                      |
   一个镜像可以创建多个容器
```

---

## 2. 📥 拉取镜像命令详解


### 2.1 `docker pull` 基本用法


**🔸 命令格式**
```bash
docker pull [选项] [仓库地址/]镜像名[:标签]
```

**🔸 最简单的用法**
```bash
# 拉取最新版本的nginx镜像

docker pull nginx

# 完整写法等同于

docker pull docker.io/library/nginx:latest
```

> 🧠 **记忆要点**  
> 不指定标签时，Docker默认拉取`latest`标签的镜像

### 2.2 指定标签拉取


**🔸 拉取特定版本**
```bash
# 拉取nginx 1.20版本

docker pull nginx:1.20

# 拉取Ubuntu 20.04版本  

docker pull ubuntu:20.04

# 拉取Node.js 16版本

docker pull node:16
```

**🔸 标签命名规律**

| 标签类型 | **示例** | **含义说明** |
|---------|---------|-------------|
| 🔸 **版本号** | `nginx:1.20` | 指定具体版本 |
| 🔸 **系统标识** | `node:16-alpine` | 基于Alpine Linux |
| 🔸 **latest** | `redis:latest` | 最新稳定版 |
| 🔸 **开发版** | `mysql:8.0-rc` | 发布候选版本 |

### 2.3 从不同仓库拉取


**🔸 官方Docker Hub**
```bash
docker pull nginx
# 等同于 docker pull docker.io/library/nginx

```

**🔸 用户仓库**
```bash
# 从个人账户拉取

docker pull username/myapp:v1.0
```

**🔸 第三方仓库**
```bash
# 从阿里云仓库拉取

docker pull registry.cn-hangzhou.aliyuncs.com/namespace/nginx:latest
```

### 2.4 拉取过程详解


**🔸 拉取输出解读**
```bash
$ docker pull nginx:1.20
1.20: Pulling from library/nginx
a2abf6c4d29d: Pull complete    # ← 下载基础层
a9edb18cadd1: Pull complete    # ← 下载应用层
589b7251471a: Pull complete    # ← 下载配置层
...
Status: Downloaded newer image for nginx:1.20
```

**🔸 拉取状态说明**

🟢 `Pull complete` - 该层下载完成  
🟡 `Already exists` - 该层本地已存在  
🔵 `Downloading` - 正在下载中

---

## 3. 🔍 查看镜像信息


### 3.1 `docker images` 基本查看


**🔸 列出所有镜像**
```bash
docker images
```

**🔸 输出内容解读**
```
REPOSITORY    TAG       IMAGE ID      CREATED       SIZE
nginx         1.20      605c77e624dd  2 weeks ago   141MB
ubuntu        20.04     ba6acccedd29  3 weeks ago   72.8MB
node          16        f77a1a58cc1e  1 month ago   993MB
```

**🔹 字段含义说明**

| 字段 | **含义** | **作用** |
|-----|---------|---------|
| `REPOSITORY` | 镜像仓库名 | 标识镜像来源 |
| `TAG` | 镜像标签 | 区分版本 |
| `IMAGE ID` | 镜像唯一标识 | 系统内部识别 |
| `CREATED` | 创建时间 | 了解镜像新旧 |
| `SIZE` | 镜像大小 | 评估存储占用 |

### 3.2 过滤和格式化查看


**🔸 查看特定镜像**
```bash
# 只看nginx相关镜像

docker images nginx

# 使用通配符

docker images "nginx*"
```

**🔸 格式化输出**
```bash
# 只显示镜像ID

docker images -q

# 自定义格式输出

docker images --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}"
```

**🔸 显示中间层镜像**
```bash
# 显示所有镜像（包括中间层）

docker images -a
```

### 3.3 镜像占用空间统计


**🔸 查看镜像总占用**
```bash
docker system df
```

**🔸 输出示例**
```
TYPE      TOTAL   ACTIVE  SIZE      RECLAIMABLE
Images    10      5       2.5GB     1.2GB (48%)
Containers 8      3       245MB     180MB (73%)
Local Volumes 4   2       1.8GB     900MB (50%)
```

---

## 4. 🏷️ 镜像标签管理


### 4.1 `docker tag` 打标签原理


**🔸 标签的本质**
标签就像是给同一个人起**不同的名字**，本质上指向同一个镜像文件。就像一个人可以叫"小明"，也可以叫"明明"，但还是同一个人。

```
          nginx:1.20
             ↓
    同一个镜像文件 ← myapp:v1.0
             ↑
       custom-nginx:prod
```

### 4.2 创建标签的常见用法


**🔸 基本打标签**
```bash
# 给现有镜像创建新标签

docker tag nginx:1.20 myapp:v1.0

# 为推送到私有仓库准备

docker tag nginx:1.20 myregistry.com/nginx:1.20
```

**🔸 标签命名最佳实践**

| 标签用途 | **命名示例** | **使用场景** |
|---------|-------------|-------------|
| 🔸 **版本标识** | `myapp:v1.2.3` | 正式发布版本 |
| 🔸 **环境标识** | `myapp:prod` | 生产环境专用 |
| 🔸 **功能标识** | `myapp:with-ssl` | 特定功能版本 |
| 🔸 **时间标识** | `myapp:20250120` | 按日期标记 |

### 4.3 标签管理策略


**🔸 多环境标签**
```bash
# 开发环境

docker tag myapp:latest myapp:dev

# 测试环境  

docker tag myapp:latest myapp:test

# 生产环境

docker tag myapp:latest myapp:prod
```

> ⚠️ **注意事项**  
> 创建标签不会复制镜像文件，只是创建新的引用。删除一个标签不会删除镜像，除非这是镜像的最后一个标签。

---

## 5. 📤 推送镜像到仓库


### 5.1 `docker push` 推送原理


**🔸 推送过程理解**
推送镜像就像**上传文件**到云盘，但Docker会智能地只上传**本地仓库没有的层**，已存在的层会跳过，大大提高效率。

```
本地镜像层     远程仓库      推送结果
    层A    ──▶   已存在   ──▶  跳过
    层B    ──▶   不存在   ──▶  上传
    层C    ──▶   已存在   ──▶  跳过
```

### 5.2 推送到Docker Hub


**🔸 准备工作**
```bash
# 1. 登录Docker Hub

docker login

# 2. 为镜像打上用户名标签

docker tag myapp:latest username/myapp:latest

# 3. 推送镜像

docker push username/myapp:latest
```

**🔸 推送过程输出**
```bash
$ docker push username/myapp:latest
The push refers to repository [docker.io/username/myapp]
5f70bf18a086: Pushed     # ← 成功推送的层
a2abf6c4d29d: Layer already exists  # ← 已存在的层
```

### 5.3 推送到私有仓库


**🔸 推送流程**
```bash
# 1. 打标签指向私有仓库

docker tag nginx:1.20 myregistry.com:5000/nginx:1.20

# 2. 登录私有仓库

docker login myregistry.com:5000

# 3. 推送

docker push myregistry.com:5000/nginx:1.20
```

**🔸 推送状态说明**

🟢 `Pushed` - 层推送成功  
🟡 `Layer already exists` - 层已存在，跳过  
🔵 `Preparing` - 准备推送  
🔴 `Retrying` - 推送重试

---

## 6. 🗑️ 删除镜像操作


### 6.1 `docker rmi` 删除原理


**🔸 删除机制理解**
删除镜像时，Docker会检查是否有**容器正在使用**该镜像。如果有，会拒绝删除，确保系统安全。

```
删除检查流程：
镜像要删除 ──▶ 检查使用情况 ──▶ 有容器使用？
                                  ├─ 是 ──▶ 拒绝删除
                                  └─ 否 ──▶ 执行删除
```

### 6.2 删除镜像的几种方式


**🔸 按镜像名删除**
```bash
# 删除指定标签的镜像

docker rmi nginx:1.20

# 删除最新版本

docker rmi nginx:latest
```

**🔸 按镜像ID删除**
```bash
# 使用完整ID

docker rmi 605c77e624dd

# 使用短ID（前几位）

docker rmi 605c77e
```

**🔸 批量删除**
```bash
# 删除所有未使用的镜像

docker image prune

# 强制删除所有镜像（危险操作！）

docker rmi $(docker images -q)
```

### 6.3 删除时的常见情况


**🔸 删除成功示例**
```bash
$ docker rmi nginx:1.20
Untagged: nginx:1.20
Untagged: nginx@sha256:abc123...
Deleted: sha256:605c77e624dd...
```

**🔸 删除失败情况**

🔴 **容器正在使用**
```bash
Error: cannot remove image, container is using it
```
**解决方案**: 先停止并删除相关容器

🟡 **多标签情况**  
当镜像有多个标签时，删除一个标签不会删除镜像文件，只会移除标签引用。

---

## 7. 📋 镜像历史与详情


### 7.1 `docker history` 查看构建历史


**🔸 基本用法**
```bash
docker history nginx:1.20
```

**🔸 输出内容解读**
```
IMAGE         CREATED       CREATED BY                    SIZE      COMMENT
605c77e624dd  2 weeks ago   /bin/sh -c #(nop) CMD ["ng…   0B        
a2abf6c4d29d  2 weeks ago   /bin/sh -c #(nop) EXPOSE 80   0B        
589b7251471a  2 weeks ago   /bin/sh -c apt-get update …   25.6MB    
```

**🔹 字段含义**

| 字段 | **含义** | **作用** |
|-----|---------|---------|
| `IMAGE` | 层ID | 标识每一层 |
| `CREATED` | 创建时间 | 了解构建时间 |
| `CREATED BY` | 构建命令 | 查看具体操作 |
| `SIZE` | 层大小 | 分析空间占用 |

### 7.2 `docker inspect` 查看详细信息


**🔸 获取完整信息**
```bash
docker inspect nginx:1.20
```

**🔸 输出关键信息**
```json
{
    "Id": "sha256:605c77e624dd...",
    "RepoTags": ["nginx:1.20"],
    "Created": "2025-01-05T10:30:00Z",
    "Config": {
        "ExposedPorts": {"80/tcp": {}},
        "Env": ["PATH=/usr/local/sbin:/usr/local/bin..."],
        "Cmd": ["nginx", "-g", "daemon off;"]
    },
    "RootFS": {
        "Type": "layers",
        "Layers": ["sha256:abc123...", "sha256:def456..."]
    }
}
```

**🔸 提取特定信息**
```bash
# 只查看配置信息

docker inspect --format='{{.Config.Cmd}}' nginx:1.20

# 查看端口配置

docker inspect --format='{{.Config.ExposedPorts}}' nginx:1.20

# 查看环境变量

docker inspect --format='{{.Config.Env}}' nginx:1.20
```

### 7.3 信息的实用价值


> 💡 **实用技巧**  
> - `docker history` 帮助理解镜像构建过程，排查问题
> - `docker inspect` 提供运行容器所需的所有配置信息
> - 这些信息对于调试和优化都很有价值

---

## 8. 🛠️ 实用操作技巧


### 8.1 镜像清理与维护


**🔸 清理未使用镜像**
```bash
# 清理悬空镜像（dangling images）

docker image prune

# 清理所有未使用镜像

docker image prune -a

# 清理指定时间前的镜像

docker image prune -a --filter "until=24h"
```

**🔸 批量操作**
```bash
# 批量删除包含特定名称的镜像

docker images | grep "test" | awk '{print $3}' | xargs docker rmi

# 删除所有未标记的镜像

docker rmi $(docker images -f "dangling=true" -q)
```

### 8.2 镜像搜索与选择


**🔸 搜索镜像**
```bash
# 搜索nginx相关镜像

docker search nginx

# 限制搜索结果数量

docker search --limit 5 nginx
```

**🔸 选择镜像的建议**

| 考虑因素 | **判断标准** | **推荐做法** |
|---------|-------------|-------------|
| 🔸 **官方性** | 是否为官方镜像 | 优先选择官方镜像 |
| 🔸 **活跃度** | 下载量和更新频率 | 选择维护活跃的 |
| 🔸 **大小** | 镜像文件大小 | 权衡功能与大小 |
| 🔸 **安全性** | 是否有安全漏洞 | 选择最新稳定版 |

### 8.3 镜像导入导出


**🔸 导出镜像**
```bash
# 导出镜像为tar文件

docker save -o nginx.tar nginx:1.20

# 导出多个镜像

docker save -o images.tar nginx:1.20 ubuntu:20.04
```

**🔸 导入镜像**
```bash
# 从tar文件导入镜像

docker load -i nginx.tar

# 导入并查看结果

docker load < nginx.tar
```

> 🚀 **应用场景**  
> 导入导出功能适用于**离线环境**部署，或在不同Docker主机间**迁移镜像**。

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的基本命令


```
🔸 docker pull    - 拉取镜像，下载应用模板
🔸 docker images  - 查看镜像，了解本地资源
🔸 docker tag     - 打标签，管理镜像版本
🔸 docker push    - 推送镜像，分享到仓库
🔸 docker rmi     - 删除镜像，清理存储空间
🔸 docker history - 查看历史，了解构建过程
🔸 docker inspect - 查看详情，获取配置信息
```

### 9.2 关键理解要点


**🔹 镜像的本质**
- 镜像是**只读模板**，包含运行应用的完整环境
- 一个镜像可以创建多个容器实例
- 镜像采用分层存储，提高复用效率

**🔹 标签的作用**
- 标签是镜像的**别名系统**，便于版本管理
- 同一个镜像可以有多个标签
- 删除标签不等于删除镜像

**🔹 仓库的概念**
- **本地仓库**：存储在本机的镜像
- **远程仓库**：如Docker Hub、私有仓库
- 镜像可以在不同仓库间推拉

### 9.3 最佳实践建议


**🔸 镜像选择原则**
- 优先使用**官方镜像**，安全可靠
- 根据需要选择**合适版本**，避免总是用latest
- 关注镜像**大小**，选择精简版本

**🔸 标签管理规范**
- 使用**语义化版本**标签（如v1.2.3）
- 为不同**环境**创建专门标签
- 保持标签命名的**一致性**

**🔸 存储清理维护**
- 定期清理**未使用镜像**
- 避免积累过多**历史版本**
- 监控**磁盘空间**使用情况

### 9.4 常见问题解决


**🤔 拉取镜像很慢怎么办？**
- 配置国内镜像源加速器
- 选择网络条件好的时间段
- 考虑使用CDN加速服务

**🤔 推送失败怎么处理？**
- 检查是否正确登录
- 确认镜像标签格式正确
- 验证仓库访问权限

**🤔 删除镜像被拒绝？**
- 先停止相关容器
- 检查是否有其他依赖
- 使用强制删除参数（谨慎）

---

**🧠 记忆口诀**：
- **Pull拉Images看，Tag标Push传**
- **Rmi删History查，Inspect详情全**
- **镜像管理七把斧，Docker操作不用愁**

> 💪 **实战建议**  
> 熟练掌握这些基本命令后，你就能自如地管理Docker镜像了。建议多动手练习，在实际使用中加深理解！