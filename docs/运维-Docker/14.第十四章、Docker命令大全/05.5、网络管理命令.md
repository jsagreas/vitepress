---
title: 5、网络管理命令
---
## 📚 目录

1. [Docker网络基础概念](#1-Docker网络基础概念)
2. [查看网络列表 - docker network ls](#2-查看网络列表)
3. [创建网络 - docker network create](#3-创建网络)
4. [删除网络 - docker network rm](#4-删除网络)  
5. [查看网络详情 - docker network inspect](#5-查看网络详情)
6. [连接网络 - docker network connect](#6-连接网络)
7. [断开网络 - docker network disconnect](#7-断开网络)
8. [清理无用网络 - docker network prune](#8-清理无用网络)
9. [实战应用场景](#9-实战应用场景)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🌐 Docker网络基础概念


### 1.1 什么是Docker网络


**简单理解**：Docker网络就像是一个**虚拟的局域网**，让容器之间能够相互通信。

想象一个现实场景：
```
家里的WiFi网络：
手机 ←→ 路由器 ←→ 笔记本电脑
     ↑
   电视盒子

Docker网络：
容器A ←→ Docker网络 ←→ 容器B
      ↑
    容器C
```

### 1.2 Docker默认网络类型


**🔸 bridge网络（桥接）**
- **作用**：默认网络，容器间可以通过IP通信
- **特点**：每个容器都有独立的IP地址
- **适用**：单机环境下的容器通信

**🔸 host网络（主机）**  
- **作用**：容器直接使用主机的网络
- **特点**：容器和主机共享网络接口
- **适用**：需要最佳网络性能的场景

**🔸 none网络（无网络）**
- **作用**：容器没有网络连接
- **特点**：完全隔离的容器
- **适用**：只需要本地计算的任务

### 1.3 网络架构图解


```
┌─────────────────────────────────────┐
│           Docker主机                 │
├─────────────────────────────────────┤
│  ┌───────┐  ┌───────┐  ┌───────┐   │
│  │容器A  │  │容器B  │  │容器C  │   │
│  │192.168│  │192.168│  │192.168│   │
│  │.1.10  │  │.1.11  │  │.1.12  │   │
│  └───┬───┘  └───┬───┘  └───┬───┘   │
│      │          │          │       │
│  ┌───┴──────────┴──────────┴───┐   │
│  │      Docker网络(bridge)     │   │
│  │        192.168.1.0/24      │   │
│  └─────────────┬───────────────┘   │
│                │                   │
│    ┌───────────┴────────────┐     │
│    │     主机网络接口        │     │
│    │      eth0             │     │
│    └────────────────────────┘     │
└─────────────────────────────────────┘
```

---

## 2. 📋 查看网络列表


### 2.1 基本命令语法


```bash
docker network ls
```

**作用**：显示Docker主机上所有的网络

### 2.2 命令输出解析


```bash
NETWORK ID     NAME      DRIVER    SCOPE
b1234567890a   bridge    bridge    local
c2345678901b   host      host      local  
d3456789012c   none      null      local
```

**字段含义**：
- **NETWORK ID**：网络的唯一标识符（简写形式）
- **NAME**：网络名称，可以用来引用网络
- **DRIVER**：网络驱动类型（bridge、host、overlay等）
- **SCOPE**：网络作用域（local表示单机，global表示集群）

### 2.3 常用参数选项


**🔸 过滤显示**
```bash
# 只显示bridge类型的网络
docker network ls --filter driver=bridge

# 只显示自定义网络（非默认）
docker network ls --filter type=custom
```

**🔸 格式化输出**
```bash
# 只显示网络名称
docker network ls --format "table {{.Name}}\t{{.Driver}}"

# 输出为JSON格式
docker network ls --format json
```

> 💡 **新手提示**：刚安装Docker时，你会看到三个默认网络：bridge、host、none。这是正常现象！

---

## 3. 🔨 创建网络


### 3.1 基本创建命令


```bash
docker network create 网络名称
```

**示例**：创建一个名为`my-network`的网络
```bash
docker network create my-network
```

### 3.2 指定网络类型


**🔸 创建bridge网络**
```bash
docker network create --driver bridge my-bridge-network
```

**🔸 创建带子网的网络**
```bash
docker network create \
  --driver bridge \
  --subnet=192.168.100.0/24 \
  --gateway=192.168.100.1 \
  my-custom-network
```

**参数解释**：
- `--subnet`：指定网络的IP地址段
- `--gateway`：指定网关地址
- `192.168.100.0/24`：表示IP范围从192.168.100.1到192.168.100.254

### 3.3 高级网络配置


**🔸 限制IP分配范围**
```bash
docker network create \
  --driver bridge \
  --subnet=172.20.0.0/16 \
  --ip-range=172.20.240.0/20 \
  my-limited-network
```

**🔸 配置DNS**
```bash
docker network create \
  --driver bridge \
  --opt com.docker.network.bridge.name=my-bridge \
  my-dns-network
```

### 3.4 创建网络的实际意义


想象你要搭建一个**Web应用**：
```
前端应用 ←→ 后端API ←→ 数据库

使用自定义网络的好处：
✅ 容器可以通过名称互相访问
✅ 网络隔离，更安全
✅ 方便管理和维护
```

---

## 4. 🗑️ 删除网络


### 4.1 基本删除命令


```bash
docker network rm 网络名称
```

**示例**：删除名为`my-network`的网络
```bash
docker network rm my-network
```

### 4.2 批量删除网络


```bash
# 删除多个网络
docker network rm network1 network2 network3

# 删除所有未使用的网络
docker network prune
```

### 4.3 删除注意事项


> ⚠️ **重要提醒**：
> - 不能删除正在被容器使用的网络
> - 不能删除系统默认网络（bridge、host、none）
> - 删除前需要先断开所有连接的容器

**删除前的检查步骤**：
1. 查看网络是否有容器连接：`docker network inspect 网络名称`
2. 断开所有容器连接：`docker network disconnect 网络名称 容器名称`
3. 执行删除操作：`docker network rm 网络名称`

---

## 5. 🔍 查看网络详情


### 5.1 基本查看命令


```bash
docker network inspect 网络名称
```

**示例**：查看bridge网络的详细信息
```bash
docker network inspect bridge
```

### 5.2 输出信息解读


```json
[
    {
        "Name": "bridge",
        "Id": "b1234567890abcdef...",
        "Driver": "bridge",
        "Scope": "local",
        "IPAM": {
            "Config": [
                {
                    "Subnet": "172.17.0.0/16",
                    "Gateway": "172.17.0.1"
                }
            ]
        },
        "Containers": {
            "container_id": {
                "Name": "my-container",
                "IPv4Address": "172.17.0.2/16"
            }
        }
    }
]
```

**关键信息说明**：
- **IPAM**：IP地址管理配置
- **Containers**：连接到此网络的容器列表
- **Subnet**：子网地址范围
- **Gateway**：网关地址

### 5.3 格式化查看特定信息


```bash
# 只查看IP配置
docker network inspect bridge --format '{{json .IPAM.Config}}'

# 只查看连接的容器
docker network inspect bridge --format '{{json .Containers}}'

# 查看网络驱动
docker network inspect bridge --format '{{.Driver}}'
```

---

## 6. 🔗 连接网络


### 6.1 基本连接命令


```bash
docker network connect 网络名称 容器名称
```

**示例**：将运行中的容器连接到自定义网络
```bash
# 先创建网络
docker network create my-app-network

# 将容器连接到网络
docker network connect my-app-network my-container
```

### 6.2 指定IP地址连接


```bash
docker network connect --ip 192.168.1.100 my-network my-container
```

**应用场景**：当你需要容器使用固定IP地址时

### 6.3 实际应用示例


**场景**：让已运行的Web应用容器连接到数据库网络

```bash
# 1. 创建专用数据库网络  
docker network create db-network

# 2. 启动数据库容器并连接到网络
docker run -d --name mysql-db --network db-network mysql:8.0

# 3. 让现有Web应用也连接到数据库网络
docker network connect db-network my-web-app

# 现在Web应用可以通过容器名称访问数据库：
# mysql://mysql-db:3306/database
```

### 6.4 连接后的网络状态


```
连接前：
Web应用容器 ─── bridge网络

连接后：  
Web应用容器 ┬─── bridge网络
          └─── db-network ─── 数据库容器
```

---

## 7. ❌ 断开网络


### 7.1 基本断开命令


```bash
docker network disconnect 网络名称 容器名称
```

**示例**：断开容器与网络的连接
```bash
docker network disconnect my-network my-container
```

### 7.2 强制断开连接


```bash
docker network disconnect --force my-network my-container
```

> ⚠️ **使用场景**：当容器无法正常断开网络连接时使用强制断开

### 7.3 断开连接的影响


**断开前状态**：
```
容器A ─┬─ 网络1 ─── 容器B
       └─ 网络2 ─── 容器C
```

**断开网络1后**：
```
容器A ─── 网络2 ─── 容器C
       
       网络1 ─── 容器B (容器A无法访问)
```

**实际意义**：
- 容器失去对该网络中其他容器的访问权限
- 容器仍然保持在其他网络的连接
- 网络配置会从容器中移除

---

## 8. 🧹 清理无用网络


### 8.1 基本清理命令


```bash
docker network prune
```

**作用**：删除所有未被容器使用的网络

### 8.2 清理过程演示


执行命令前的网络状态：
```bash
docker network ls
```
```
NETWORK ID     NAME           DRIVER    SCOPE
abc123         bridge         bridge    local
def456         old-network    bridge    local  ← 无容器使用
ghi789         test-network   bridge    local  ← 无容器使用
jkl012         active-net     bridge    local  ← 有容器使用
```

执行清理：
```bash
docker network prune
```

清理后的状态：
```
NETWORK ID     NAME           DRIVER    SCOPE
abc123         bridge         bridge    local
jkl012         active-net     bridge    local
```

### 8.3 清理选项参数


**🔸 指定清理条件**
```bash
# 清理指定时间前创建的网络
docker network prune --filter until=24h

# 清理带有特定标签的网络
docker network prune --filter label=environment=test
```

**🔸 静默清理**
```bash
# 不询问确认，直接清理
docker network prune -f
```

### 8.4 清理的最佳实践


> 💡 **建议清理时机**：
> - 开发环境：每天清理一次
> - 测试环境：部署前清理
> - 生产环境：谨慎清理，先确认影响范围

---

## 9. 🚀 实战应用场景


### 9.1 多服务应用部署


**场景**：部署一个包含前端、后端、数据库的完整应用

```bash
# 1. 创建应用专用网络
docker network create --driver bridge webapp-network

# 2. 启动数据库
docker run -d \
  --name db \
  --network webapp-network \
  -e MYSQL_ROOT_PASSWORD=password123 \
  mysql:8.0

# 3. 启动后端API
docker run -d \
  --name api-server \
  --network webapp-network \
  -p 8080:8080 \
  my-api:latest

# 4. 启动前端应用
docker run -d \
  --name frontend \
  --network webapp-network \
  -p 80:80 \
  my-frontend:latest
```

**网络拓扑图**：
```
外部访问 :80 ────→ frontend容器
                    │
                    ▼
            webapp-network
                    │
frontend ←──────────┼──────────→ api-server
                    │               │
                    │               ▼
                    └──────────→ database
```

### 9.2 开发环境网络隔离


**需求**：将开发、测试、预发布环境的容器网络隔离

```bash
# 创建不同环境的网络
docker network create dev-network
docker network create test-network  
docker network create staging-network

# 开发环境容器
docker run -d --name dev-app --network dev-network my-app:dev
docker run -d --name dev-db --network dev-network mysql:latest

# 测试环境容器
docker run -d --name test-app --network test-network my-app:test
docker run -d --name test-db --network test-network mysql:latest
```

### 9.3 网络故障排查


**常见问题及解决方案**：

| 问题现象 | 可能原因 | 排查命令 | 解决方案 |
|---------|---------|---------|---------|
| 容器无法互相访问 | 不在同一网络 | `docker network inspect` | `docker network connect` |
| 网络删除失败 | 有容器正在使用 | `docker network inspect` | 先断开容器连接 |
| IP地址冲突 | 网络配置重叠 | `docker network ls` | 重新规划子网地址 |
| 容器启动失败 | 网络不存在 | `docker network ls` | 先创建所需网络 |

### 9.4 网络性能优化技巧


**🔸 选择合适的网络类型**
- **高性能要求**：使用`host`网络
- **安全隔离要求**：使用自定义`bridge`网络
- **集群部署**：使用`overlay`网络

**🔸 网络配置优化**
```bash
# 创建高性能网络
docker network create \
  --driver bridge \
  --opt com.docker.network.driver.mtu=9000 \
  high-performance-net
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的基础命令


```bash
# 🔍 查看网络
docker network ls                    # 列出所有网络
docker network inspect 网络名        # 查看网络详情

# 🔨 管理网络  
docker network create 网络名         # 创建网络
docker network rm 网络名            # 删除网络

# 🔗 连接管理
docker network connect 网络名 容器名  # 连接容器到网络
docker network disconnect 网络名 容器名 # 断开连接

# 🧹 清理维护
docker network prune               # 清理无用网络
```

### 10.2 关键理解要点


**🔹 网络的本质作用**
- **通信桥梁**：让容器之间能够相互访问
- **隔离屏障**：不同网络的容器默认无法通信
- **服务发现**：容器可以通过名称访问同网络的其他容器

**🔹 网络选择原则**
- **单机应用**：使用`bridge`网络
- **需要主机网络性能**：使用`host`网络  
- **需要完全隔离**：使用`none`网络
- **复杂应用**：创建自定义网络

**🔹 网络管理最佳实践**
- 为不同用途的服务创建专用网络
- 定期清理无用网络避免资源浪费
- 使用有意义的网络命名
- 在生产环境中谨慎使用网络删除命令

### 10.3 实际应用价值


**🎯 开发场景应用**
- **微服务架构**：每个服务组创建独立网络
- **环境隔离**：开发、测试、生产环境网络分离
- **数据库隔离**：敏感数据库使用专用网络

**🔧 运维实践**
- **容器编排**：配合docker-compose使用网络
- **负载均衡**：多实例容器共享网络
- **故障排查**：通过网络命令诊断连接问题

**核心记忆口诀**：
- **ls看全局，inspect看详情**
- **create建网络，rm删干净**  
- **connect来连接，disconnect断开**
- **prune清垃圾，网络更干净**

**新手避坑指南**：
- ✅ 删除网络前先断开容器连接
- ✅ 自定义网络比默认bridge网络功能更强
- ✅ 容器名称可以作为网络内的域名使用
- ❌ 不要删除系统默认的三个网络
- ❌ 不要在生产环境随意使用prune命令