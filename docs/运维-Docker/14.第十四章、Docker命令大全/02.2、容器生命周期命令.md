---
title: 2、容器生命周期命令
---
## 📚 目录

1. [容器生命周期概述](#1-容器生命周期概述)
2. [容器创建命令](#2-容器创建命令)
3. [容器启动与运行](#3-容器启动与运行)
4. [容器停止与重启](#4-容器停止与重启)
5. [容器暂停与恢复](#5-容器暂停与恢复)
6. [容器终止与删除](#6-容器终止与删除)
7. [容器管理操作](#7-容器管理操作)
8. [生命周期状态转换](#8-生命周期状态转换)
9. [实践应用场景](#9-实践应用场景)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🔄 容器生命周期概述


### 1.1 什么是容器生命周期


**🔸 生命周期定义**
容器生命周期就是容器从**被创建**到**被删除**的整个过程，就像人的生老病死一样，容器也有自己的"生命历程"。

**🔸 为什么要理解生命周期**
想象你在管理一个工厂的生产线：
- 需要知道什么时候**开工**（启动容器）
- 什么时候**暂停**（临时停工）
- 什么时候**停工**（正常关闭）
- 什么时候**报废设备**（删除容器）

### 1.2 容器状态图示


```
容器生命周期状态转换：

     [不存在]
         |
    docker create ←─┐
         |         │
      [已创建]      │
         |         │
    docker start   │
         |         │
      [运行中] ←────┤
     ↗   ↓   ↘     │
pause  stop  kill  │
     ↘   ↓   ↗     │
      [已停止]      │
         |         │
    docker start ──┘
         |
    docker rm
         |
      [已删除]
```

### 1.3 核心状态说明


| 状态 | 说明 | 比喻理解 |
|------|------|----------|
| **Created** | `容器已创建但未启动` | `汽车停在车库里，还没发动` |
| **Running** | `容器正在运行` | `汽车正在路上行驶` |
| **Paused** | `容器暂停但进程还在内存` | `汽车在红绿灯前临时停车` |
| **Stopped** | `容器已停止运行` | `汽车熄火停在停车场` |
| **Exited** | `容器运行完成后退出` | `汽车到达目的地后熄火` |

---

## 2. 🏗️ 容器创建命令


### 2.1 docker create - 只创建不运行


**🔸 基本概念**
`docker create` 就像**买了一台电脑但还没开机**，容器被创建出来了，但是还没有启动运行。

**💡 基础语法**
```bash
docker create [选项] 镜像名 [命令]
```

**🎯 实际应用示例**
```bash
# 创建一个nginx容器（但不启动）
docker create --name my-nginx -p 8080:80 nginx

# 创建并配置环境变量
docker create --name my-app -e NODE_ENV=production node:16
```

**🔧 常用选项对比**

| 选项 | 作用 | 举例理解 |
|------|------|----------|
| `--name` | `给容器起个名字` | `给你的宠物起名叫"小白"` |
| `-p` | `端口映射` | `在你家门口开个小窗口对外营业` |
| `-e` | `设置环境变量` | `告诉容器一些重要的配置信息` |
| `-v` | `挂载数据卷` | `给容器准备一个储物柜` |

### 2.2 创建容器的最佳实践


**✅ 推荐做法**
```bash
# 总是给容器起个有意义的名字
docker create --name web-server nginx

# 提前规划好端口映射
docker create --name my-db -p 3306:3306 mysql:8.0
```

**❌ 避免的做法**
```bash
# 不推荐：没有指定名字，后期管理困难
docker create nginx

# 不推荐：端口冲突
docker create -p 80:80 nginx  # 如果80端口已被占用
```

---

## 3. 🚀 容器启动与运行


### 3.1 docker run - 创建并启动的一步到位


**🔸 核心概念**
`docker run` = `docker create` + `docker start`，就像**买车后直接开走**，一个命令完成创建和启动两个动作。

**💡 基础用法对比**

```
分步骤操作：
① docker create --name test nginx  # 先创建
② docker start test                # 再启动

一步到位：
docker run --name test nginx       # 创建+启动
```

**🎯 常见运行模式**

| 模式 | 命令 | 什么时候用 | 比喻理解 |
|------|------|-----------|----------|
| **后台运行** | `docker run -d nginx` | `长期运行的服务` | `让程序在后台默默工作` |
| **交互运行** | `docker run -it ubuntu bash` | `需要操作容器内部` | `坐进车里亲自驾驶` |
| **临时运行** | `docker run --rm nginx` | `用完就丢的任务` | `一次性筷子，用完就扔` |

**🔧 实际应用示例**
```bash
# 启动一个Web服务器（后台运行）
docker run -d --name web-server -p 8080:80 nginx

# 进入Ubuntu容器进行操作（交互模式）
docker run -it --name my-ubuntu ubuntu:20.04 bash

# 临时运行一个测试（用完自动删除）
docker run --rm node:16 node --version
```

### 3.2 docker start - 启动已存在的容器


**🔸 使用场景**
当你有一个**已经创建**但**没在运行**的容器时，用 `docker start` 来启动它，就像**重新启动一台已经配置好的电脑**。

```bash
# 启动单个容器
docker start my-nginx

# 启动多个容器
docker start web-server db-server app-server

# 启动并查看输出
docker start -a my-app  # -a 表示附加到容器的输出
```

---

## 4. ⏹️ 容器停止与重启


### 4.1 docker stop - 优雅停止


**🔸 工作原理**
`docker stop` 像**礼貌地敲门说"该下班了"**：
1. 先发送 `SIGTERM` 信号（礼貌通知）
2. 等待10秒让程序自己结束
3. 如果还不停，就强制结束

```bash
# 停止单个容器
docker stop web-server

# 停止多个容器
docker stop app1 app2 db1

# 自定义等待时间（默认10秒）
docker stop -t 30 slow-app  # 等待30秒再强制停止
```

### 4.2 docker restart - 重启容器


**🔸 什么时候需要重启**
- **配置文件更新后**需要重新加载
- **容器运行异常**需要重置
- **定期维护**保持系统稳定

```bash
# 重启容器
docker restart web-server

# 重启时设置等待时间
docker restart -t 5 quick-app
```

### 4.3 停止策略对比


| 场景 | 使用命令 | 解释说明 |
|------|----------|----------|
| **正常关闭服务** | `docker stop` | `给程序时间保存数据，优雅退出` |
| **紧急情况** | `docker kill` | `立即强制终止，可能丢失数据` |
| **重新加载配置** | `docker restart` | `停止后重新启动，应用新配置` |

---

## 5. ⏸️ 容器暂停与恢复


### 5.1 docker pause - 暂停容器


**🔸 暂停的含义**
`docker pause` 就像给视频**按了暂停键**，容器里的所有进程都被冻结，但是：
- **内存中的数据还在**
- **网络连接保持**
- **可以随时恢复**

```bash
# 暂停一个容器
docker pause web-server

# 暂停多个容器
docker pause app1 app2 db1
```

### 5.2 docker unpause - 恢复容器


**🔸 恢复操作**
`docker unpause` 就像**按下播放键**，所有暂停的进程立即恢复运行。

```bash
# 恢复单个容器
docker unpause web-server

# 恢复多个容器
docker unpause app1 app2 db1
```

### 5.3 暂停 vs 停止的区别


```
暂停（Pause）：              停止（Stop）：
     ┌─────────┐                  ┌─────────┐
     │ 运行中  │                  │ 运行中  │
     └─────────┘                  └─────────┘
          │                           │
    docker pause                docker stop
          │                           │
          ▼                           ▼
     ┌─────────┐                  ┌─────────┐
     │ 已暂停  │                  │ 已停止  │
     │(进程冻结)│                  │(进程结束)│
     └─────────┘                  └─────────┘
          │                           │
    docker unpause              docker start
          │                           │
          ▼                           ▼
     ┌─────────┐                  ┌─────────┐
     │ 运行中  │                  │ 运行中  │
     │(立即恢复)│                  │(重新启动)│
     └─────────┘                  └─────────┘
```

| 操作类型 | 暂停/恢复 | 停止/启动 |
|----------|-----------|-----------|
| **速度** | `瞬间完成` | `需要启动时间` |
| **内存** | `保持在内存中` | `释放内存` |
| **数据** | `所有状态保持` | `可能需要重新初始化` |
| **适用场景** | `临时暂停服务` | `长期关闭服务` |

---

## 6. ❌ 容器终止与删除


### 6.1 docker kill - 强制终止


**🔸 什么时候用kill**
`docker kill` 就像**直接拔电源**，在这些情况下使用：
- 容器**卡死**无法正常停止
- 需要**立即终止**不等待
- **紧急情况**下的快速处理

```bash
# 强制终止容器
docker kill stuck-container

# 发送自定义信号
docker kill --signal=SIGTERM my-app
```

**⚠️ 使用警告**
> 使用 `docker kill` 可能导致数据丢失，除非紧急情况，建议先尝试 `docker stop`

### 6.2 docker rm - 删除容器


**🔸 删除的含义**
`docker rm` 就像**把废品扔进垃圾桶**，容器被彻底删除，无法恢复。

```bash
# 删除已停止的容器
docker rm my-old-app

# 强制删除运行中的容器
docker rm -f running-app

# 删除多个容器
docker rm app1 app2 db1

# 删除所有已停止的容器
docker rm $(docker ps -aq --filter status=exited)
```

### 6.3 删除注意事项


**✅ 删除前的检查**
```bash
# 1. 确认容器状态
docker ps -a

# 2. 备份重要数据（如果有挂载卷）
docker cp my-app:/data ./backup/

# 3. 停止容器再删除
docker stop my-app
docker rm my-app
```

**❌ 危险操作**
```bash
# 不推荐：直接强制删除运行中的容器
docker rm -f $(docker ps -aq)  # 这会删除所有容器！
```

---

## 7. 🔧 容器管理操作


### 7.1 docker rename - 重命名容器


**🔸 为什么要重命名**
就像给文件夹**重新起个更好记的名字**，让容器管理更方便：

```bash
# 重命名容器
docker rename old-name new-name

# 实际示例
docker rename boring_watson my-web-server
docker rename db_temp production-database
```

### 7.2 批量操作技巧


**🎯 批量启动相关容器**
```bash
# 启动所有Web相关容器
docker start $(docker ps -aq --filter name=web*)

# 停止所有测试容器
docker stop $(docker ps -q --filter name=test*)
```

**🎯 按标签管理容器**
```bash
# 创建时添加标签
docker run -d --name web1 --label env=prod nginx
docker run -d --name web2 --label env=dev nginx

# 批量操作带标签的容器
docker stop $(docker ps -q --filter label=env=dev)
```

---

## 8. 🔄 生命周期状态转换


### 8.1 完整状态转换图


```
详细的容器状态转换流程：

[不存在] 
    │
    └─→ docker create ──→ [Created已创建]
                              │
                         docker start
                              │
                              ▼
    ┌─────────────────→ [Running运行中] ←─────────┐
    │                      │ │ │ │               │
    │                      │ │ │ └─→ docker stop │
    │        docker pause ─┘ │ │                 │
    │                        │ │                 │
    │                        ▼ │                 │
    │         [Paused暂停] ────┘                 │
    │              │                            │
    │    docker unpause                         │
    │              │                            │
    │              └────────────────────────────┘
    │                        │
    │              docker kill/stop
    │                        │
    │                        ▼
    └── docker start ← [Stopped已停止] ──→ docker rm ──→ [删除]
                           │ │
                           │ └─→ docker restart
                           │         │
                           └←────────┘
```

### 8.2 状态查看命令


**📊 查看容器当前状态**
```bash
# 查看运行中的容器
docker ps

# 查看所有容器（包括停止的）
docker ps -a

# 查看特定容器详情
docker inspect container-name
```

**📊 状态过滤查看**
```bash
# 只看运行中的
docker ps --filter status=running

# 只看已停止的
docker ps -a --filter status=exited

# 只看暂停的
docker ps -a --filter status=paused
```

---

## 9. 💼 实践应用场景


### 9.1 Web服务部署场景


**🎯 场景描述**
部署一个Web应用，需要数据库、缓存、应用服务器三个容器：

```bash
# 1. 创建数据库容器
docker run -d --name my-mysql \
  -e MYSQL_ROOT_PASSWORD=secret \
  -v mysql-data:/var/lib/mysql \
  mysql:8.0

# 2. 创建Redis缓存
docker run -d --name my-redis redis:alpine

# 3. 创建Web应用
docker run -d --name my-app \
  -p 8080:3000 \
  --link my-mysql:mysql \
  --link my-redis:redis \
  my-web-app:latest
```

**🔄 日常维护操作**
```bash
# 重启应用（配置更新后）
docker restart my-app

# 临时暂停服务（维护期间）
docker pause my-app my-redis

# 维护完成后恢复
docker unpause my-app my-redis
```

### 9.2 开发测试场景


**🎯 快速环境搭建**
```bash
# 启动开发环境
docker run -it --name dev-env \
  -v $(pwd):/workspace \
  -w /workspace \
  node:16 bash

# 测试完成后清理
docker stop dev-env
docker rm dev-env
```

**🎯 一次性测试**
```bash
# 运行测试（用完自动删除）
docker run --rm -v $(pwd):/app -w /app node:16 npm test
```

### 9.3 故障处理场景


**⚠️ 容器无响应处理**
```bash
# 1. 检查容器状态
docker ps -a

# 2. 查看日志
docker logs problematic-container

# 3. 尝试优雅停止
docker stop problematic-container

# 4. 如果无效，强制终止
docker kill problematic-container

# 5. 重新启动
docker start problematic-container
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心命令


```
🔸 创建管理：docker create、docker run
🔸 启停控制：docker start、docker stop、docker restart  
🔸 暂停恢复：docker pause、docker unpause
🔸 强制终止：docker kill
🔸 删除清理：docker rm
🔸 重命名：docker rename
```

### 10.2 关键理解要点


**🔹 生命周期的本质**
```
容器生命周期 = 状态管理
每个状态都有特定的用途和转换规则
理解状态转换，就能灵活管理容器
```

**🔹 命令选择原则**
```
创建容器：
- 需要预配置 → docker create + docker start  
- 立即运行 → docker run

停止容器：
- 正常关闭 → docker stop（推荐）
- 紧急情况 → docker kill

临时暂停：
- 短期维护 → docker pause/unpause
- 长期停止 → docker stop/start
```

**🔹 数据安全考虑**
```
重要提醒：
- 删除容器前备份重要数据
- 优先使用优雅停止（docker stop）
- 合理使用数据卷持久化数据
```

### 10.3 实践应用指导


**✅ 最佳实践**
- 总是给容器起有意义的名字
- 使用 `docker stop` 而不是 `docker kill`
- 定期清理不用的容器
- 重要服务使用数据卷持久化

**❌ 常见错误**
- 直接删除运行中的容器
- 不备份数据就删除容器  
- 滥用 `docker kill` 强制终止
- 容器命名随意，管理混乱

**核心记忆口诀**：
- **创建运行一步到位，run命令最常用**
- **优雅停止用stop好，紧急才用kill命令**  
- **暂停恢复保状态，临时维护最合适**
- **删除之前先备份，数据安全记心中**