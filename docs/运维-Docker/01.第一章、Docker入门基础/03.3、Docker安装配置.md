---
title: 3、Docker安装配置
---
## 📚 目录

1. [系统要求与准备](#1-系统要求与准备)
2. [Linux系统安装Docker](#2-Linux系统安装Docker)
3. [Windows系统安装Docker Desktop](#3-Windows系统安装Docker-Desktop)
4. [macOS系统安装Docker Desktop](#4-macOS系统安装Docker-Desktop)
5. [国内镜像加速器配置](#5-国内镜像加速器配置)
6. [开机自启动设置](#6-开机自启动设置)
7. [安装验证测试](#7-安装验证测试)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔍 系统要求与准备


### 1.1 系统兼容性检查


**💻 Linux系统要求**
```
最低要求：
• 内核版本：3.10+
• 架构：x86_64 / amd64 / arm64
• 存储驱动：支持overlay2、aufs等

推荐配置：
• 内核版本：4.0+
• 内存：2GB+
• 存储空间：20GB+
```

**🪟 Windows系统要求**
```
Docker Desktop要求：
• Windows 10 Pro/Enterprise/Education (64位)
• 版本：1903或更高
• WSL 2支持
• Hyper-V功能

硬件要求：
• CPU：支持虚拟化技术
• 内存：4GB+
• BIOS：启用虚拟化
```

**🍎 macOS系统要求**
```
支持版本：
• macOS 10.14+ (Mojave)
• Intel芯片或Apple Silicon (M1/M2)

硬件要求：
• 内存：4GB+
• 存储空间：500MB+
```

### 1.2 预检查工具


**🔧 Linux内核版本检查**
```bash
# 查看内核版本
uname -r

# 查看系统架构
uname -m

# 查看Linux发行版信息
cat /etc/os-release
```

**⚡ Windows功能检查**
```powershell
# PowerShell中检查Windows版本
Get-ComputerInfo | Select WindowsProductName, WindowsVersion

# 检查WSL 2状态
wsl --status

# 检查Hyper-V状态
Get-WindowsOptionalFeature -Online -FeatureName Microsoft-Hyper-V-All
```

---

## 2. 🐧 Linux系统安装Docker


### 2.1 Ubuntu/Debian系统安装


**🔸 自动安装脚本（推荐）**
```bash
# 官方一键安装脚本
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 国内用户使用阿里云镜像脚本
curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyun
```

**🔸 手动安装步骤**
```bash
# 1. 更新系统包索引
sudo apt-get update

# 2. 安装必要的依赖包
sudo apt-get install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# 3. 添加Docker官方GPG密钥
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
    sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# 4. 设置稳定版仓库
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
  https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 5. 更新包索引并安装Docker
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

### 2.2 CentOS/RHEL系统安装


**🔸 使用yum安装**
```bash
# 1. 卸载旧版本
sudo yum remove docker \
                docker-client \
                docker-client-latest \
                docker-common \
                docker-latest \
                docker-latest-logrotate \
                docker-logrotate \
                docker-engine

# 2. 安装yum-utils
sudo yum install -y yum-utils

# 3. 设置仓库
sudo yum-config-manager \
    --add-repo \
    https://download.docker.com/linux/centos/docker-ce.repo

# 4. 安装Docker引擎
sudo yum install docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

### 2.3 用户权限配置


**👥 将用户添加到docker组**
```bash
# 创建docker组（通常已存在）
sudo groupadd docker

# 将当前用户添加到docker组
sudo usermod -aG docker $USER

# 重新加载组权限（或重新登录）
newgrp docker

# 测试无sudo运行docker
docker run hello-world
```

> ⚠️ **安全提示**
> 
> 将用户添加到docker组相当于给予root权限，请谨慎操作

---

## 3. 🪟 Windows系统安装Docker Desktop


### 3.1 系统准备工作


**🔧 启用必要功能**
```powershell
# 以管理员身份运行PowerShell
# 启用WSL 2功能
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart

# 启用虚拟机平台
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart

# 重启系统
Restart-Computer
```

**📋 WSL 2内核更新**
```
下载并安装WSL 2内核更新包：
https://wslstorestorage.blob.core.windows.net/wslblob/wsl_update_x64.msi

设置WSL 2为默认版本：
wsl --set-default-version 2
```

### 3.2 Docker Desktop安装


**💾 下载与安装**
```
安装步骤：
1️⃣ 访问官网下载：https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe
2️⃣ 双击运行安装程序
3️⃣ 勾选"Use WSL 2 instead of Hyper-V"选项
4️⃣ 完成安装并重启系统
```

**⚙️ 首次配置**
```
Docker Desktop配置界面：
┌─────────────────────────────────┐
│ 🐳 Docker Desktop Settings      │
├─────────────────────────────────┤
│ General:                        │
│ ☑️ Use WSL 2 based engine        │
│ ☑️ Start Docker Desktop at login │
│                                 │
│ Resources:                      │
│ Memory: [====    ] 2GB          │
│ CPUs: [==      ] 2 cores        │
└─────────────────────────────────┘
```

### 3.3 常见问题处理


**🔄 WSL 2集成问题**
| 问题现象 | 解决方案 | 验证命令 |
|---------|----------|----------|
| WSL 2未启用 | `wsl --install` | `wsl --status` |
| 虚拟化未开启 | BIOS中启用VT-x/AMD-V | 任务管理器查看 |
| 内存不足 | Docker Desktop调整内存分配 | 设置中调整 |

---

## 4. 🍎 macOS系统安装Docker Desktop


### 4.1 Intel Mac安装


**💻 下载安装包**
```
Intel芯片Mac下载地址：
https://desktop.docker.com/mac/main/amd64/Docker.dmg

安装步骤：
1️⃣ 下载Docker.dmg文件
2️⃣ 双击挂载磁盘镜像
3️⃣ 拖拽Docker图标到Applications文件夹
4️⃣ 启动Docker Desktop应用
```

### 4.2 Apple Silicon Mac安装


**🚀 M1/M2芯片专用版本**
```
Apple Silicon下载地址：
https://desktop.docker.com/mac/main/arm64/Docker.dmg

特殊配置：
• 支持ARM64原生容器
• 兼容x86_64容器（通过Rosetta 2）
• 性能优化更好
```

### 4.3 macOS权限设置


**🔐 系统权限授权**
```
首次启动需要授权：
┌─────────────────────────────────┐
│ "Docker Desktop" would like to  │
│ access files in your Downloads  │
│ folder.                         │
│                                 │
│ [Don't Allow]  [OK]            │
└─────────────────────────────────┘

需要输入系统管理员密码完成安装
```

---

## 5. 🚀 国内镜像加速器配置


### 5.1 为什么需要镜像加速


**⏰ 问题分析**
```
访问Docker Hub遇到的问题：
• 网络延迟：平均延迟500ms+
• 下载速度慢：通常<100KB/s
• 连接不稳定：经常超时断开
• 限流限制：匿名用户限制100次/6小时

使用国内镜像源的好处：
✅ 下载速度提升：5-10MB/s
✅ 连接稳定性：99.9%可用性
✅ 节省带宽成本
✅ 提高开发效率
```

### 5.2 主要镜像加速器服务商


**🏢 国内主要镜像源**
| 服务商 | 镜像地址 | 特点 | 可用性 |
|--------|----------|------|--------|
| **阿里云** | `https://registry.cn-hangzhou.aliyuncs.com` | 🟢 速度快，稳定性高 | ⭐⭐⭐⭐⭐ |
| **腾讯云** | `https://mirror.ccs.tencentyun.com` | 🟢 覆盖全国节点 | ⭐⭐⭐⭐ |
| **华为云** | `https://05f073ad3c0010ea0f4bc00b7105ec20.mirror.swr.myhuaweicloud.com` | 🟢 企业级稳定 | ⭐⭐⭐⭐ |
| **网易云** | `http://hub-mirror.c.163.com` | 🟡 免费使用 | ⭐⭐⭐ |
| **DaoCloud** | `http://f1361db2.m.daocloud.io` | 🟡 社区维护 | ⭐⭐⭐ |

### 5.3 Linux系统配置镜像加速


**⚙️ 配置daemon.json**
```bash
# 创建或编辑Docker配置文件
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<-'EOF'
{
  "registry-mirrors": [
    "https://registry.cn-hangzhou.aliyuncs.com",
    "https://mirror.ccs.tencentyun.com",
    "http://hub-mirror.c.163.com"
  ],
  "exec-opts": ["native.cgroupdriver=systemd"],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "100m"
  },
  "storage-driver": "overlay2"
}
EOF

# 重新加载配置并重启Docker
sudo systemctl daemon-reload
sudo systemctl restart docker
```

**🔍 验证配置是否生效**
```bash
# 查看Docker配置信息
docker info | grep -i registry

# 测试下载速度
time docker pull nginx:alpine
```

### 5.4 Docker Desktop配置镜像加速


**🖥️ 图形界面配置**
```
Docker Desktop -> Settings -> Docker Engine

修改配置JSON：
{
  "builder": {
    "gc": {
      "defaultKeepStorage": "20GB",
      "enabled": true
    }
  },
  "experimental": false,
  "features": {
    "buildkit": true
  },
  "registry-mirrors": [
    "https://registry.cn-hangzhou.aliyuncs.com",
    "https://mirror.ccs.tencentyun.com"
  ]
}

点击 [Apply & Restart] 应用配置
```

---

## 6. ⚡ 开机自启动设置


### 6.1 Linux系统自启动


**🔧 Systemd系统**
```bash
# 启用Docker服务开机自启
sudo systemctl enable docker.service
sudo systemctl enable containerd.service

# 查看服务状态
sudo systemctl status docker

# 手动启动/停止/重启服务
sudo systemctl start docker
sudo systemctl stop docker
sudo systemctl restart docker
```

**📊 服务状态检查**
```bash
# 检查Docker服务是否正在运行
systemctl is-active docker

# 检查Docker是否设置为开机启动
systemctl is-enabled docker

# 查看Docker服务详细信息
systemctl show docker --property=ActiveState,SubState,LoadState
```

### 6.2 Windows系统自启动


**⚙️ Docker Desktop配置**
```
自启动设置路径：
Docker Desktop -> Settings -> General
┌─────────────────────────────────┐
│ ☑️ Start Docker Desktop at login │
│ ☑️ Open Docker Dashboard at      │
│    startup                      │
│ ☑️ Use Docker Compose V2         │
└─────────────────────────────────┘
```

**📋 Windows服务管理**
```powershell
# 查看Docker相关服务状态
Get-Service | Where-Object {$_.Name -like "*docker*"}

# 设置服务自动启动
Set-Service -Name "Docker Desktop Service" -StartupType Automatic
```

### 6.3 macOS系统自启动


**🍎 系统偏好设置**
```
设置路径：
Docker Desktop -> Preferences -> General
☑️ Start Docker Desktop when you log in

或通过系统偏好设置：
系统偏好设置 -> 用户与群组 -> 登录项
添加Docker Desktop到登录项列表
```

---

## 7. ✅ 安装验证测试


### 7.1 基本功能测试


**🔍 版本信息验证**
```bash
# 查看Docker版本信息
docker --version
# 输出示例：Docker version 24.0.6, build ed223bc

# 查看详细版本信息
docker version

# 查看Docker系统信息
docker info
```

**📊 系统信息输出示例**
```
Client: Docker Engine - Community
 Version:           24.0.6
 API version:       1.43
 Go version:        go1.20.7
 Git commit:        ed223bc
 Built:             Mon Sep  4 12:31:44 2023
 OS/Arch:           linux/amd64

Server: Docker Engine - Community
 Version:          24.0.6
 API version:      1.43 (minimum version 1.12)
 Go version:       go1.20.7
```

### 7.2 容器运行测试


**🐳 Hello World测试**
```bash
# 运行官方测试容器
docker run hello-world

# 预期输出：
Hello from Docker!
This message shows that your installation appears to be working correctly.
```

**🌐 Web服务测试**
```bash
# 运行nginx容器测试
docker run -d -p 8080:80 nginx:alpine

# 查看运行中的容器
docker ps

# 测试访问（浏览器打开 http://localhost:8080）
curl http://localhost:8080

# 停止并删除容器
docker stop $(docker ps -q)
docker rm $(docker ps -aq)
```

### 7.3 镜像加速测试


**⚡ 下载速度测试**
```bash
# 清除本地镜像缓存
docker system prune -a

# 测试下载热门镜像的速度
echo "测试开始时间: $(date)"
time docker pull redis:alpine
echo "测试结束时间: $(date)"

# 验证镜像是否下载成功
docker images | grep redis
```

**📈 性能对比测试**
```bash
# 创建测试脚本
cat > speed_test.sh << 'EOF'
#!/bin/bash
echo "=== Docker镜像下载速度测试 ==="
echo "开始时间: $(date)"

images=("alpine:latest" "ubuntu:20.04" "node:16-alpine" "python:3.9-slim")

for img in "${images[@]}"; do
    echo "正在测试: $img"
    docker rmi $img 2>/dev/null
    start_time=$(date +%s)
    docker pull $img > /dev/null 2>&1
    end_time=$(date +%s)
    duration=$((end_time - start_time))
    echo "$img 下载耗时: ${duration}秒"
done

echo "结束时间: $(date)"
EOF

chmod +x speed_test.sh
./speed_test.sh
```

### 7.4 全面功能验证


**🔧 Docker Compose测试**
```yaml
# 创建测试用的docker-compose.yml
cat > docker-compose-test.yml << 'EOF'
version: '3.8'
services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
  
  redis:
    image: redis:alpine
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data

volumes:
  redis-data:
EOF

# 创建测试页面
mkdir -p html
echo "<h1>Docker安装测试成功！</h1>" > html/index.html

# 启动测试环境
docker-compose -f docker-compose-test.yml up -d

# 验证服务是否正常
curl http://localhost:8080
docker-compose -f docker-compose-test.yml ps

# 清理测试环境
docker-compose -f docker-compose-test.yml down -v
```

**📋 健康检查清单**

```
🔍 Docker安装验证清单：

基础功能：
☑️ docker --version 显示版本信息
☑️ docker info 显示系统信息  
☑️ docker run hello-world 运行成功
☑️ 无需sudo即可运行docker命令（Linux）

网络功能：
☑️ 能够从Docker Hub拉取镜像
☑️ 容器端口映射正常工作
☑️ 容器间网络通信正常

存储功能：
☑️ 卷挂载功能正常
☑️ 绑定挂载功能正常
☑️ 数据持久化正常

性能表现：
☑️ 镜像下载速度 > 1MB/s
☑️ 容器启动时间 < 5秒
☑️ 系统资源占用合理
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的安装要点


```
🔸 系统选择：根据操作系统选择合适的安装方式
🔸 权限配置：Linux系统需正确配置用户权限
🔸 镜像加速：国内用户必须配置镜像加速器
🔸 自启动设置：生产环境需要配置开机自启动
🔸 验证测试：安装后必须进行功能验证
```

### 8.2 关键配置理解


**🔹 镜像加速器的重要性**
```
性能提升：
• 下载速度：从KB/s提升到MB/s
• 连接稳定：减少网络超时问题
• 开发效率：大幅缩短环境搭建时间

配置原理：
• Docker首先访问配置的镜像源
• 镜像源从Docker Hub同步镜像
• 提供国内高速CDN加速服务
```

**🔹 用户权限配置的安全考量**
```
权限说明：
• docker组用户等同于root权限
• 可以访问整个文件系统
• 能够启动特权容器

安全建议：
• 仅将可信用户添加到docker组
• 生产环境考虑使用rootless模式
• 定期审查docker组成员
```

**🔹 系统资源配置优化**
```
内存分配：
• 开发环境：2-4GB足够
• 生产环境：根据容器数量调整
• Windows/macOS：通过Docker Desktop调整

存储配置：
• 选择合适的存储驱动（overlay2推荐）
• 配置日志轮转防止磁盘占满
• 定期清理未使用的镜像和容器
```

### 8.3 常见问题与解决方案


**🐛 安装常见问题**

| 问题类型 | 症状描述 | 解决方案 | 预防措施 |
|---------|----------|----------|----------|
| 📱 **权限问题** | `permission denied` | `sudo usermod -aG docker $USER` | 安装时正确配置用户组 |
| 🌐 **网络问题** | 镜像拉取超时 | 配置国内镜像加速器 | 预先配置加速器 |
| 💾 **存储问题** | 磁盘空间不足 | 清理无用镜像容器 | 定期执行清理任务 |
| ⚡ **服务问题** | Docker daemon启动失败 | 检查系统兼容性 | 安装前检查系统要求 |

### 8.4 最佳实践建议


**🎯 安装最佳实践**
- **系统选择**：生产环境推荐Linux，开发环境可用Docker Desktop
- **版本管理**：使用稳定版本，避免使用edge版本  
- **资源配置**：合理分配内存和CPU资源
- **监控设置**：配置日志收集和性能监控

**🔧 维护最佳实践**
- **定期更新**：跟随官方更新补丁和安全修复
- **清理策略**：建立自动化清理机制
- **备份恢复**：重要数据做好备份策略
- **安全加固**：启用必要的安全选项

**🚀 性能优化建议**
- **镜像优化**：使用官方精简镜像
- **网络优化**：配置合适的网络驱动
- **存储优化**：选择合适的存储驱动和卷类型
- **资源限制**：为容器设置合理的资源限制

**核心记忆要点**：
- Docker安装需要根据操作系统选择对应方法
- 国内用户必须配置镜像加速器提高效率  
- Linux用户权限配置和自启动设置很关键
- 安装完成后的验证测试确保功能正常
- 合理的资源配置和安全设置保障运行稳定