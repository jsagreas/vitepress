---
title: 1、企业级服务通信实践
---
## 📚 目录

1. [大型电商平台通信实践](#1-大型电商平台通信实践)
2. [金融系统通信架构](#2-金融系统通信架构)
3. [物联网平台通信](#3-物联网平台通信)
4. [社交媒体平台架构](#4-社交媒体平台架构)
5. [在线教育平台设计](#5-在线教育平台设计)
6. [医疗系统通信实践](#6-医疗系统通信实践)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🛒 大型电商平台通信实践


### 1.1 业务场景与挑战


**🏢 典型电商平台特点**
```
业务规模：
- 日订单量：百万级别
- 并发用户：十万级别
- 商品SKU：千万级别
- 促销活动：高峰流量暴增10-100倍

核心挑战：
- 高并发：秒杀、大促等场景
- 高可用：99.99%以上可用性要求
- 数据一致性：订单、库存、支付等强一致性
- 快速响应：用户体验要求毫秒级响应
```

💡 **换句话说**：电商平台就像一个超大型商场，需要同时服务成千上万的顾客，还要保证不出错、不卡顿。

### 1.2 服务拆分架构


**🏗️ 核心服务划分**
```
用户中心 ← 管理用户信息、登录注册
    ↓
订单服务 ← 处理下单、订单查询
    ↓ ↓ ↓
商品服务  库存服务  支付服务
    ↓       ↓        ↓
营销服务  物流服务  消息服务
```

**📋 服务职责清单**

| 服务名称 | **核心职责** | **调用关系** | **通信方式** |
|---------|------------|------------|------------|
| 🛍️ **商品服务** | `商品信息管理、搜索` | `被订单服务调用` | `REST + Redis缓存` |
| 📦 **库存服务** | `库存扣减、预占` | `被订单服务调用` | `RPC + MQ削峰` |
| 💳 **支付服务** | `支付处理、退款` | `被订单服务调用` | `同步RPC + 异步回调` |
| 🚚 **物流服务** | `物流信息查询` | `被订单服务调用` | `REST + 事件驱动` |
| 🎁 **营销服务** | `优惠券、促销` | `被订单服务调用` | `同步RPC + 缓存` |

### 1.3 通信方案设计


**🔸 同步调用场景**

```java
// 用户下单流程 - 同步调用示例
@Service
public class OrderService {
    
    @Autowired
    private ProductFeignClient productClient;  // 商品服务
    @Autowired
    private InventoryFeignClient inventoryClient;  // 库存服务
    
    // 创建订单（同步调用）
    public OrderDTO createOrder(OrderRequest request) {
        // 1. 查询商品信息（同步）
        ProductDTO product = productClient.getProduct(request.getProductId());
        
        // 2. 检查并锁定库存（同步）
        boolean locked = inventoryClient.lockInventory(
            request.getProductId(), 
            request.getQuantity()
        );
        
        if (!locked) {
            throw new BusinessException("库存不足");
        }
        
        // 3. 创建订单
        return saveOrder(request, product);
    }
}
```

💭 **思考一下**：为什么查商品、锁库存要用同步调用？
- 因为必须**立即知道结果**才能继续下单
- 如果商品不存在或库存不足，要马上告诉用户

**🔸 异步通信场景**

```java
// 订单支付成功后的异步处理
@Service
public class PaymentEventHandler {
    
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    // 支付成功后发送消息
    public void onPaymentSuccess(PaymentDTO payment) {
        // 构建订单支付成功事件
        OrderPaidEvent event = new OrderPaidEvent(
            payment.getOrderId(),
            payment.getAmount(),
            LocalDateTime.now()
        );
        
        // 发送到消息队列（异步）
        rabbitTemplate.convertAndSend(
            "order.exchange",
            "order.paid",
            event
        );
    }
}

// 库存服务监听支付成功消息
@Service
public class InventoryEventListener {
    
    @RabbitListener(queues = "inventory.queue")
    public void handleOrderPaid(OrderPaidEvent event) {
        // 确认扣减库存
        inventoryService.confirmDeduction(event.getOrderId());
    }
}
```

🌰 **举个例子**：支付成功就像你交了钱拿到小票，后面的发货、通知等可以慢慢来，不需要你等着。

### 1.4 高并发秒杀方案


**⚡ 秒杀流程架构**
```
用户请求 → CDN静态化 → 网关限流 → Redis预减库存 → 消息队列 → 异步下单
   ↓         ↓           ↓          ↓            ↓          ↓
百万QPS    缓存99%    只放行1%   内存操作快   削峰填谷   DB慢慢写
```

**🔑 关键技术点**

```java
// 秒杀核心代码
@Service
public class SecKillService {
    
    @Autowired
    private RedisTemplate<String, Integer> redisTemplate;
    
    // Redis预减库存（Lua脚本保证原子性）
    public boolean trySecKill(Long productId, Long userId) {
        String key = "seckill:stock:" + productId;
        
        // Lua脚本：检查库存并减1
        String script = 
            "if redis.call('get', KEYS[1]) > 0 then " +
            "   redis.call('decr', KEYS[1]) " +
            "   return 1 " +
            "else " +
            "   return 0 " +
            "end";
        
        Long result = redisTemplate.execute(
            new DefaultRedisScript<>(script, Long.class),
            Collections.singletonList(key)
        );
        
        if (result == 1) {
            // 发送到消息队列异步创建订单
            sendToMQ(productId, userId);
            return true;
        }
        return false;
    }
}
```

📊 **性能对比数据**

| 方案 | **吞吐量** | **响应时间** | **成功率** |
|------|----------|------------|----------|
| 直接查DB | `500 QPS` | `200ms` | `60%` |
| Redis缓存 | `5万 QPS` | `10ms` | `90%` |
| **Redis+MQ** | `**10万 QPS**` | `**5ms**` | `**99%**` |

### 1.5 服务降级与容错


**🛡️ 多级降级策略**

```
流量过载时的降级顺序：
1. 关闭推荐服务 → 用户看不到个性化推荐（影响小）
2. 关闭评论服务 → 暂时不能评论（可接受）
3. 只读商品详情 → 不能下单但能看（保护核心）
4. 限制登录注册 → 老用户优先（最后手段）
```

```java
// Hystrix降级示例
@Service
public class ProductService {
    
    @HystrixCommand(
        fallbackMethod = "getProductFromCache",
        commandProperties = {
            @HystrixProperty(name = "execution.isolation.thread.timeoutInMilliseconds", value = "3000")
        }
    )
    public ProductDTO getProduct(Long productId) {
        // 正常调用远程服务
        return productClient.getById(productId);
    }
    
    // 降级方法：从缓存获取
    public ProductDTO getProductFromCache(Long productId) {
        ProductDTO cached = redisTemplate.opsForValue().get("product:" + productId);
        if (cached != null) {
            return cached;
        }
        // 返回兜底数据
        return ProductDTO.builder()
            .id(productId)
            .name("商品详情加载中...")
            .build();
    }
}
```

🚨 **重要提醒**：降级不是关掉服务，而是**用更简单的方式完成任务**，就像电梯坏了改走楼梯。

---

## 2. 🏦 金融系统通信架构


### 2.1 金融系统特殊要求


**💰 核心业务特点**
```
安全性：
- 资金安全：分分钟不能出错
- 数据加密：敏感信息必须加密传输
- 审计追踪：所有操作可追溯

一致性：
- 强一致性：账户余额必须准确
- 事务保证：转账要么成功要么失败
- 数据不丢：所有交易记录永久保存

监管要求：
- 实名认证：用户身份验证
- 反洗钱：交易监控
- 数据留存：交易数据保存多年
```

💡 **换句话说**：金融系统就像银行金库，安全第一，钱的事情马虎不得。

### 2.2 分布式事务方案


**🔄 转账场景示例**
```
A账户 ----转账100元----> B账户

步骤：
1. A账户扣款 -100
2. B账户入账 +100
3. 记录交易流水

要求：要么全成功，要么全失败（不能A扣了钱，B没收到）
```

**📋 Seata AT模式实现**

```java
// 转账服务（分布式事务）
@Service
public class TransferService {
    
    @Autowired
    private AccountService accountService;
    @Autowired
    private RecordService recordService;
    
    @GlobalTransactional(name = "transfer", rollbackFor = Exception.class)
    public void transfer(Long fromId, Long toId, BigDecimal amount) {
        
        // 1. 扣款（远程调用账户服务）
        accountService.deduct(fromId, amount);
        
        // 2. 入账（远程调用账户服务）
        accountService.credit(toId, amount);
        
        // 3. 记录流水（本地数据库）
        recordService.saveRecord(fromId, toId, amount);
        
        // 如果任何一步失败，全部回滚
    }
}
```

🔍 **深入理解**：Seata就像一个"总指挥"，它会记住每一步操作，如果中间出错，就让所有操作都撤销。

**🔸 消息最终一致性方案**

```java
// 转账服务（消息保证最终一致）
@Service
public class TransferServiceV2 {
    
    @Autowired
    private RocketMQTemplate rocketMQTemplate;
    
    @Transactional
    public void transfer(Long fromId, Long toId, BigDecimal amount) {
        
        // 1. 本地扣款 + 发送事务消息
        TransactionSendResult result = rocketMQTemplate.sendMessageInTransaction(
            "transfer-topic",
            MessageBuilder.withPayload(
                new TransferMessage(fromId, toId, amount)
            ).build(),
            null
        );
        
        // 2. 如果消息发送成功，扣款才真正提交
    }
    
    // 消费者：处理入账
    @RocketMQMessageListener(topic = "transfer-topic", consumerGroup = "account-group")
    public void handleTransfer(TransferMessage msg) {
        // 入账操作（可能重试多次，直到成功）
        accountService.credit(msg.getToId(), msg.getAmount());
    }
}
```

**⚖️ 两种方案对比**

| 特性 | **Seata强一致** | **消息最终一致** |
|------|---------------|----------------|
| 一致性 | `立即一致` | `最终一致（可能延迟几秒）` |
| 性能 | `较慢（需要协调）` | `较快（异步处理）` |
| 适用场景 | `转账、支付` | `积分、通知` |

### 2.3 安全通信机制


**🔐 加密传输方案**

```java
// API网关：请求加密拦截器
@Component
public class EncryptionInterceptor implements HandlerInterceptor {
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                            HttpServletResponse response, 
                            Object handler) {
        
        // 1. 验证数字签名
        String signature = request.getHeader("X-Signature");
        String timestamp = request.getHeader("X-Timestamp");
        
        if (!verifySignature(signature, timestamp)) {
            response.setStatus(401);
            return false;
        }
        
        // 2. 解密请求体（AES）
        String encryptedBody = getRequestBody(request);
        String decryptedBody = AESUtil.decrypt(encryptedBody);
        
        // 3. 替换为解密后的内容
        request.setAttribute("decryptedBody", decryptedBody);
        
        return true;
    }
}
```

**🔑 关键安全措施**
- `AES加密`：请求和响应体加密
- `RSA签名`：防止数据篡改
- `Token认证`：JWT双token机制
- `IP白名单`：限制访问来源
- `操作日志`：记录所有敏感操作

---

## 3. 📡 物联网平台通信


### 3.1 物联网场景特点


**🌐 IoT平台架构**
```
设备层 → 网关层 → 平台层 → 应用层
  ↓        ↓        ↓        ↓
传感器   协议转换   数据处理   业务系统

设备数量：百万到千万级
通信协议：MQTT、CoAP、HTTP
数据特点：海量、高频、小包
```

💭 **思考一下**：物联网就像给每个设备装了一个"嘴巴"，它们不停地向平台"汇报"自己的状态。

### 3.2 MQTT通信实现


**📨 设备上报数据**

```java
// 设备管理服务
@Service
public class DeviceService {
    
    @Autowired
    private IMqttClient mqttClient;
    
    // 订阅设备上报主题
    @PostConstruct
    public void subscribe() throws MqttException {
        mqttClient.subscribe("device/+/data", (topic, message) -> {
            // 解析设备ID
            String deviceId = topic.split("/")[1];
            
            // 解析数据
            DeviceData data = JSON.parseObject(
                new String(message.getPayload()), 
                DeviceData.class
            );
            
            // 处理数据（异步）
            processDeviceData(deviceId, data);
        });
    }
    
    // 向设备下发指令
    public void sendCommand(String deviceId, String command) throws MqttException {
        String topic = "device/" + deviceId + "/command";
        mqttClient.publish(topic, command.getBytes(), 1, false);
    }
}
```

**🔸 海量数据处理**
```
设备上报 → MQTT Broker → Kafka → Flink实时计算 → 时序数据库
   ↓          ↓            ↓         ↓              ↓
 10万/秒    削峰缓冲      持久化    聚合分析        存储查询
```

### 3.3 设备状态同步


**📊 设备影子模式**

```java
// 设备影子服务
@Service
public class DeviceShadowService {
    
    @Autowired
    private RedisTemplate<String, DeviceShadow> redisTemplate;
    
    // 更新设备影子（设备上报时）
    public void updateShadow(String deviceId, DeviceData data) {
        String key = "shadow:" + deviceId;
        
        DeviceShadow shadow = DeviceShadow.builder()
            .deviceId(deviceId)
            .reported(data)  // 设备上报的状态
            .timestamp(System.currentTimeMillis())
            .build();
        
        redisTemplate.opsForValue().set(key, shadow);
    }
    
    // 应用查询设备状态（读影子）
    public DeviceShadow getShadow(String deviceId) {
        return redisTemplate.opsForValue().get("shadow:" + deviceId);
    }
}
```

🌰 **举个例子**：设备影子就像设备的"虚拟分身"，设备不在线时，也能知道它最后的状态。

---

## 4. 📱 社交媒体平台架构


### 4.1 社交场景特点


**👥 核心功能**
```
关系链：
- 关注/粉丝：双向/单向关注
- 好友推荐：基于关系链推荐
- 群组聊天：多人实时通信

内容分发：
- Feed流：关注人的动态
- 推荐流：智能推荐内容
- 热门榜：实时热点

实时通信：
- 即时消息：点对点、群聊
- 消息通知：点赞、评论提醒
- 在线状态：显示在线/离线
```

### 4.2 Feed流架构设计


**📰 推拉结合模式**

```
发布动态流程：
用户A发动态 → 推给活跃粉丝（推模式）→ 写入自己的发件箱（拉模式）
                    ↓                          ↓
              粉丝B收件箱立即可见        非活跃粉丝C访问时才拉取
```

```java
// Feed流服务
@Service
public class FeedService {
    
    @Autowired
    private RedisTemplate<String, List<FeedItem>> redisTemplate;
    
    // 发布动态（推拉结合）
    public void publishFeed(Long userId, FeedItem feed) {
        
        // 1. 写入发件箱（拉模式）
        String outboxKey = "outbox:" + userId;
        redisTemplate.opsForList().leftPush(outboxKey, feed);
        
        // 2. 推送给活跃粉丝（推模式）
        List<Long> activeFans = getActiveFans(userId);
        for (Long fanId : activeFans) {
            String inboxKey = "inbox:" + fanId;
            redisTemplate.opsForList().leftPush(inboxKey, feed);
        }
        
        // 3. 异步通知所有粉丝
        notifyFans(userId, feed);
    }
    
    // 获取Feed流（混合拉取）
    public List<FeedItem> getFeeds(Long userId) {
        String inboxKey = "inbox:" + userId;
        
        // 先从收件箱拉取（推送的内容）
        List<FeedItem> inbox = redisTemplate.opsForList()
            .range(inboxKey, 0, 19);
        
        // 再拉取关注人的发件箱（拉取的内容）
        List<Long> followings = getFollowings(userId);
        for (Long followingId : followings) {
            String outboxKey = "outbox:" + followingId;
            List<FeedItem> outbox = redisTemplate.opsForList()
                .range(outboxKey, 0, 4);
            inbox.addAll(outbox);
        }
        
        // 按时间排序返回
        return inbox.stream()
            .sorted(Comparator.comparing(FeedItem::getCreateTime).reversed())
            .limit(20)
            .collect(Collectors.toList());
    }
}
```

**⚡ 性能优化要点**
- Redis存储最近动态（热数据）
- 只推送给活跃粉丝（减少写入）
- 分页加载（避免一次拉太多）
- 异步推送（不阻塞发布）

### 4.3 即时通讯实现


**💬 WebSocket长连接**

```java
// WebSocket处理器
@Component
public class ChatWebSocketHandler extends TextWebSocketHandler {
    
    // 用户连接管理
    private static final Map<Long, WebSocketSession> sessions = new ConcurrentHashMap<>();
    
    @Override
    public void afterConnectionEstablished(WebSocketSession session) {
        Long userId = getUserId(session);
        sessions.put(userId, session);
        
        // 用户上线，广播在线状态
        broadcastOnlineStatus(userId, true);
    }
    
    @Override
    protected void handleTextMessage(WebSocketSession session, TextMessage message) {
        Long senderId = getUserId(session);
        ChatMessage chatMsg = JSON.parseObject(message.getPayload(), ChatMessage.class);
        
        // 发送消息
        Long receiverId = chatMsg.getReceiverId();
        WebSocketSession receiverSession = sessions.get(receiverId);
        
        if (receiverSession != null && receiverSession.isOpen()) {
            // 对方在线，直接推送
            receiverSession.sendMessage(new TextMessage(
                JSON.toJSONString(chatMsg)
            ));
        } else {
            // 对方离线，存储到消息队列
            saveOfflineMessage(receiverId, chatMsg);
        }
    }
}
```

🔍 **深入理解**：WebSocket就像一条"电话线"，连接后可以随时双向通话，不用每次都"拨号"（建连）。

---

## 5. 🎓 在线教育平台设计


### 5.1 教育场景特点


**📚 核心业务**
```
直播教学：
- 音视频流：低延迟推流
- 互动白板：实时同步
- 聊天互动：弹幕、问答

点播学习：
- 视频加密：防盗链
- 进度记录：断点续看
- 倍速播放：学习效率

作业系统：
- 作业提交：文件上传
- 自动批改：AI判题
- 成绩统计：数据分析
```

### 5.2 直播系统架构


**🎥 直播推拉流程**
```
老师端 → 推流服务器 → CDN分发 → 学生端
  ↓          ↓            ↓         ↓
RTMP推流   转码处理    边缘节点    HLS/FLV拉流
```

```java
// 直播服务
@Service
public class LiveService {
    
    @Autowired
    private LiveStreamClient streamClient;
    
    // 创建直播间
    public LiveRoomDTO createLiveRoom(Long teacherId, String courseName) {
        
        // 1. 生成推流地址
        String pushUrl = streamClient.generatePushUrl(
            "live",
            teacherId + "_" + System.currentTimeMillis()
        );
        
        // 2. 生成拉流地址
        String pullUrl = streamClient.generatePullUrl(
            "live",
            teacherId + "_" + System.currentTimeMillis()
        );
        
        // 3. 创建房间信息
        LiveRoomDTO room = LiveRoomDTO.builder()
            .teacherId(teacherId)
            .courseName(courseName)
            .pushUrl(pushUrl)
            .pullUrl(pullUrl)
            .status(LiveStatus.CREATED)
            .build();
        
        // 4. 存储房间信息
        saveRoom(room);
        
        return room;
    }
}
```

### 5.3 互动白板同步


**✍️ 白板数据同步**

```java
// 白板同步服务
@Service
public class WhiteboardService {
    
    @Autowired
    private RedisTemplate<String, String> redisTemplate;
    @Autowired
    private SimpMessagingTemplate messagingTemplate;
    
    // 老师操作白板（绘制、擦除等）
    public void handleTeacherAction(String roomId, WhiteboardAction action) {
        
        // 1. 保存到Redis（白板状态）
        String key = "whiteboard:" + roomId;
        redisTemplate.opsForList().rightPush(key, JSON.toJSONString(action));
        
        // 2. 广播给所有学生（WebSocket）
        messagingTemplate.convertAndSend(
            "/topic/whiteboard/" + roomId,
            action
        );
    }
    
    // 学生加入时获取白板内容
    public List<WhiteboardAction> getWhiteboardContent(String roomId) {
        String key = "whiteboard:" + roomId;
        List<String> actions = redisTemplate.opsForList().range(key, 0, -1);
        
        return actions.stream()
            .map(json -> JSON.parseObject(json, WhiteboardAction.class))
            .collect(Collectors.toList());
    }
}
```

🌰 **举个例子**：白板同步就像老师在黑板上写字，学生的"黑板"实时显示同样的内容。

---

## 6. 🏥 医疗系统通信实践


### 6.1 医疗系统特殊性


**⚕️ 核心要求**
```
数据安全：
- 患者隐私：严格加密和权限控制
- 数据留存：医疗数据长期保存
- 审计追踪：所有操作可追溯

系统可靠：
- 7x24运行：不能停机
- 数据准确：医疗数据容错率为0
- 快速响应：急诊场景秒级响应

互联互通：
- 院内系统：HIS、LIS、PACS集成
- 区域平台：医疗数据共享
- 医保对接：实时结算
```

### 6.2 HL7/FHIR标准集成


**📋 医疗数据交换**

```java
// FHIR资源转换服务
@Service
public class FhirService {
    
    // 将内部患者数据转为FHIR格式
    public Patient toFhirPatient(PatientDTO patient) {
        Patient fhirPatient = new Patient();
        
        // 患者ID
        fhirPatient.setId(patient.getId().toString());
        
        // 姓名
        HumanName name = new HumanName();
        name.setFamily(patient.getLastName());
        name.addGiven(patient.getFirstName());
        fhirPatient.addName(name);
        
        // 性别
        fhirPatient.setGender(
            patient.getGender().equals("M") ? 
            Enumerations.AdministrativeGender.MALE : 
            Enumerations.AdministrativeGender.FEMALE
        );
        
        // 出生日期
        fhirPatient.setBirthDate(patient.getBirthDate());
        
        return fhirPatient;
    }
    
    // 与其他医院交换患者信息
    public void shareFhirData(String hospitalUrl, Patient patient) {
        // 通过FHIR REST API发送
        restTemplate.postForObject(
            hospitalUrl + "/Patient",
            patient,
            OperationOutcome.class
        );
    }
}
```

💡 **换句话说**：FHIR就像医疗数据的"普通话"，不同医院、系统都能听懂。

### 6.3 实时监护数据处理


**📊 ICU监护系统**

```java
// 监护数据采集服务
@Service
public class MonitoringService {
    
    @Autowired
    private KafkaTemplate<String, MonitorData> kafkaTemplate;
    
    // 接收监护设备数据
    @Scheduled(fixedRate = 1000)  // 每秒采集
    public void collectMonitorData() {
        List<MonitorDevice> devices = getActiveDevices();
        
        for (MonitorDevice device : devices) {
            MonitorData data = MonitorData.builder()
                .deviceId(device.getId())
                .patientId(device.getPatientId())
                .heartRate(device.getHeartRate())
                .bloodPressure(device.getBloodPressure())
                .oxygenLevel(device.getOxygenLevel())
                .timestamp(System.currentTimeMillis())
                .build();
            
            // 发送到Kafka
            kafkaTemplate.send("monitor-data", data);
            
            // 异常报警
            checkAndAlert(data);
        }
    }
    
    // 异常检测和报警
    private void checkAndAlert(MonitorData data) {
        if (data.getHeartRate() > 120 || data.getHeartRate() < 60) {
            // 心率异常，发送报警
            alertService.sendAlert(
                data.getPatientId(),
                "心率异常：" + data.getHeartRate()
            );
        }
        
        if (data.getOxygenLevel() < 90) {
            // 血氧异常，紧急报警
            alertService.sendUrgentAlert(
                data.getPatientId(),
                "血氧过低：" + data.getOxygenLevel()
            );
        }
    }
}
```

**🚨 报警机制**
```
数据采集 → 实时分析 → 异常检测 → 多渠道报警
   ↓          ↓          ↓            ↓
 1秒/次    规则引擎    阈值判断    短信+APP+护士站
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的设计原则


**🎯 通用架构原则**
```
服务拆分：
✅ 按业务领域划分，职责单一
✅ 高内聚低耦合，独立部署
✅ 数据库分离，避免共享

通信选择：
✅ 同步：强依赖、需立即响应
✅ 异步：弱依赖、允许延迟
✅ 事件：解耦、最终一致

性能优化：
✅ 缓存：减少远程调用
✅ 限流：保护核心服务
✅ 降级：牺牲非核心保核心
```

### 7.2 不同场景的技术选型


| 场景 | **首选方案** | **原因** | **注意事项** |
|------|------------|---------|------------|
| 🛒 **电商秒杀** | `Redis+MQ` | `高并发、削峰` | `库存超卖问题` |
| 🏦 **金融转账** | `Seata分布式事务` | `强一致性` | `性能损耗` |
| 📡 **IoT设备** | `MQTT+Kafka` | `海量、高频` | `数据乱序` |
| 📱 **社交Feed** | `推拉结合+Redis` | `实时性好` | `存储成本` |
| 🎓 **在线直播** | `WebSocket+CDN` | `低延迟` | `带宽成本` |
| 🏥 **医疗互通** | `FHIR+REST` | `标准化` | `数据安全` |

### 7.3 关键实践经验


**✅ 做对的事**
- 先同步后异步：核心链路同步，非核心异步
- 先简单后复杂：从单体开始，逐步拆分
- 先保证后优化：先保证功能，再优化性能
- 先监控后诊断：完善监控，快速定位问题

**❌ 避免的坑**
- 过度拆分：服务太多难管理
- 盲目异步：增加复杂度
- 忽视监控：出问题无法定位
- 没有降级：故障雪崩

### 7.4 学习路径建议


**📚 新手进阶路线**
```
第1阶段：基础通信
→ 学习REST、RPC基本原理
→ 掌握Feign、Dubbo使用

第2阶段：异步通信
→ 学习MQ基本概念
→ 掌握RabbitMQ、Kafka

第3阶段：服务治理
→ 学习注册中心、负载均衡
→ 掌握Nacos、Ribbon

第4阶段：高级特性
→ 学习分布式事务、限流降级
→ 掌握Seata、Sentinel

第5阶段：实战项目
→ 参考企业级案例
→ 动手实践完整项目
```

**核心记忆**：
- 电商重并发，金融重一致，IoT重海量
- 同步快但脆弱，异步慢但稳定
- 缓存加速度，降级保稳定
- 监控是眼睛，日志是记忆
- 架构无完美，只有最合适