---
title: 3、API安全设计
---
## 📚 目录

1. [API安全基础概念](#1-API安全基础概念)
2. [HTTPS安全通信](#2-HTTPS安全通信)
3. [API密钥管理策略](#3-API密钥管理策略)
4. [请求签名与防重放](#4-请求签名与防重放)
5. [数据验证与编码](#5-数据验证与编码)
6. [常见攻击防护](#6-常见攻击防护)
7. [安全最佳实践](#7-安全最佳实践)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🛡️ API安全基础概念


### 1.1 为什么API安全如此重要


**🏠 生活中的比喻**
想象一下您家的门锁系统：
```
传统单体应用 = 一栋房子，只有一个大门
微服务架构 = 一个小区，每栋楼都有自己的门

问题：小区里楼与楼之间需要互相访问
挑战：如何确保只有授权的"住户"能进入？
```

### 1.2 微服务环境下的安全挑战


**🌐 网络通信复杂化**
```
服务A → 服务B → 服务C → 数据库
  ↓       ↓       ↓
外部API  缓存   消息队列

每个箭头都是潜在的安全风险点！
```

**📊 安全威胁统计**
| 威胁类型 | 发生频率 | 危害等级 | 典型后果 |
|---------|---------|---------|---------|
| **数据泄露** | `⭐⭐⭐⭐⭐` | `🔴 极高` | 用户信息被盗 |
| **API滥用** | `⭐⭐⭐⭐` | `🟡 中等` | 服务器过载 |
| **身份伪造** | `⭐⭐⭐` | `🔴 极高` | 非法操作 |
| **数据篡改** | `⭐⭐` | `🟠 高` | 业务逻辑错乱 |

### 1.3 API安全的核心原则


**🔐 CIA三元组**
```
🔒 机密性（Confidentiality）
   ↳ 确保数据只被授权用户访问

🔍 完整性（Integrity）  
   ↳ 确保数据在传输过程中不被篡改

⚡ 可用性（Availability）
   ↳ 确保服务在需要时能正常提供
```

> 💡 **新手友好提示**
> 
> 就像寄快递一样：机密性=包裹不被别人看到，完整性=包裹内容不被调包，可用性=快递能准时送达

---

## 2. 🔐 HTTPS安全通信


### 2.1 HTTP vs HTTPS 的本质区别


**📡 通信过程对比**
```
HTTP通信（明文传输）：
客户端 ----[用户名:admin,密码:123456]----> 服务器
       👀 任何人都能看到！

HTTPS通信（加密传输）：
客户端 ----[&@#$%^&*()加密数据]----> 服务器
       🔒 只有服务器能解密！
```

### 2.2 HTTPS工作原理简化理解


**🤝 SSL/TLS握手过程（像交朋友一样）**
```
步骤 1️⃣: 客户端："你好，我想安全聊天"
步骤 2️⃣: 服务器："好的，这是我的身份证（证书）"
步骤 3️⃣: 客户端："身份验证通过，我们用这个密钥聊天"
步骤 4️⃣: 服务器："同意，开始加密通信"
```

### 2.3 Spring Boot中配置HTTPS


**⚙️ 生成SSL证书**
```bash
# 生成自签名证书（开发环境）
keytool -genkeypair -alias myapp -keyalg RSA -keysize 2048 \
        -storetype PKCS12 -keystore keystore.p12 \
        -validity 365
```

**📋 application.yml配置**
```yaml
server:
  port: 8443
  ssl:
    key-store: classpath:keystore.p12
    key-store-password: mypassword
    key-store-type: PKCS12
    key-alias: myapp
    
# 强制HTTPS重定向    
security:
  require-ssl: true
```

**🛠️ Java代码配置示例**
```java
@Configuration
public class HttpsConfig {
    
    @Bean
    public ServletWebServerFactory servletContainer() {
        TomcatServletWebServerFactory tomcat = new TomcatServletWebServerFactory() {
            @Override
            protected void postProcessContext(Context context) {
                // 强制使用安全Cookie
                SecurityConstraint securityConstraint = new SecurityConstraint();
                securityConstraint.setUserConstraint("CONFIDENTIAL");
                SecurityCollection collection = new SecurityCollection();
                collection.addPattern("/*");
                securityConstraint.addCollection(collection);
                context.addConstraint(securityConstraint);
            }
        };
        
        // HTTP重定向到HTTPS
        tomcat.addAdditionalTomcatConnectors(redirectConnector());
        return tomcat;
    }
}
```

> ⚠️ **生产环境提醒**
> 
> 生产环境请使用正规CA机构签发的证书，自签名证书会显示"不安全"警告

### 2.4 HTTPS性能优化技巧


**⚡ 性能优化策略**
```
🚀 HTTP/2启用
└─ 多路复用，减少连接数

🔄 SSL会话复用  
└─ 避免重复握手过程

📦 GZIP压缩
└─ 减少传输数据量

🏃‍♂️ CDN加速
└─ 就近访问，降低延迟
```

---

## 3. 🔑 API密钥管理策略


### 3.1 API密钥的作用与类型


**🎫 API密钥就像门禁卡**
```
普通门禁卡 = API Key（识别身份）
VIP金卡   = API Key + Secret（身份+权限）
临时卡    = Token（有时效性）
万能卡    = Root Key（最高权限，谨慎使用）
```

### 3.2 API密钥设计原则


**📏 密钥强度要求**
| 类型 | 长度要求 | 字符集 | 示例格式 |
|------|---------|--------|---------|
| **API Key** | `≥32位` | 字母数字 | `ak_1a2b3c4d5e6f7g8h` |
| **Secret** | `≥64位` | 字母数字+符号 | `sk_A1b2C3d4...` |
| **Token** | `≥128位` | Base64编码 | `JWT格式` |

**🔄 密钥生命周期管理**
```
创建阶段 → 分发阶段 → 使用阶段 → 轮换阶段 → 销毁阶段
    ↓         ↓         ↓         ↓         ↓
  安全生成   安全传输   权限控制   定期更新   安全删除
```

### 3.3 密钥存储最佳实践


**🏦 多层存储策略**
```java
@Component
public class ApiKeyManager {
    
    // ❌ 错误做法：硬编码
    // private static final String API_KEY = "your-secret-key";
    
    // ✅ 正确做法：环境变量
    @Value("${app.api.key:}")
    private String apiKey;
    
    // ✅ 更好做法：配置中心
    @Autowired
    private ConfigClient configClient;
    
    public String getApiKey() {
        // 优先级：环境变量 > 配置中心 > 默认值
        return StringUtils.hasText(apiKey) ? 
               apiKey : configClient.getConfig("api.key");
    }
}
```

**🔐 敏感信息加密存储**
```java
@Service
public class EncryptionService {
    
    private final AESUtil aesUtil;
    
    // 加密存储API密钥
    public String encryptApiKey(String plainKey) {
        return aesUtil.encrypt(plainKey);
    }
    
    // 使用时解密
    public String decryptApiKey(String encryptedKey) {
        return aesUtil.decrypt(encryptedKey);
    }
}
```

### 3.4 API密钥验证机制


**🔍 三级验证体系**
```java
@RestController
public class SecureApiController {
    
    @PostMapping("/api/secure-data")
    public ResponseEntity<?> getSecureData(
            @RequestHeader("X-API-Key") String apiKey,
            @RequestHeader("X-Timestamp") String timestamp,
            @RequestHeader("X-Signature") String signature) {
        
        // 第一级：API Key验证
        if (!isValidApiKey(apiKey)) {
            return ResponseEntity.status(401).body("Invalid API Key");
        }
        
        // 第二级：时间戳验证（防重放攻击）
        if (!isValidTimestamp(timestamp)) {
            return ResponseEntity.status(401).body("Request expired");
        }
        
        // 第三级：签名验证
        if (!isValidSignature(apiKey, timestamp, signature)) {
            return ResponseEntity.status(401).body("Invalid signature");
        }
        
        return ResponseEntity.ok("Secure data");
    }
}
```

> 💡 **实用技巧**
> 
> 生产环境中，建议为不同的客户端分配不同的API密钥，方便追踪和管理

---

## 4. ✍️ 请求签名与防重放


### 4.1 请求签名的工作原理


**📝 签名就像手写签名**
```
现实中签合同：
1. 看合同内容
2. 确认无误后签名  
3. 签名证明是本人同意

API请求签名：
1. 看请求参数
2. 用密钥生成签名
3. 签名证明请求未被篡改
```

### 4.2 签名算法实现


**🔐 HMAC-SHA256签名实现**
```java
@Component
public class RequestSigner {
    
    /**
     * 生成请求签名
     * @param method HTTP方法（GET/POST等）
     * @param uri 请求路径
     * @param params 请求参数
     * @param timestamp 时间戳
     * @param secretKey 密钥
     * @return 签名字符串
     */
    public String generateSignature(String method, String uri, 
            Map<String, String> params, String timestamp, String secretKey) {
        
        // 1. 构建待签名字符串
        String signString = buildSignString(method, uri, params, timestamp);
        
        // 2. 使用HMAC-SHA256算法生成签名
        try {
            Mac mac = Mac.getInstance("HmacSHA256");
            SecretKeySpec keySpec = new SecretKeySpec(
                secretKey.getBytes(StandardCharsets.UTF_8), "HmacSHA256");
            mac.init(keySpec);
            
            byte[] signData = mac.doFinal(signString.getBytes(StandardCharsets.UTF_8));
            return Base64.getEncoder().encodeToString(signData);
            
        } catch (Exception e) {
            throw new RuntimeException("签名生成失败", e);
        }
    }
    
    private String buildSignString(String method, String uri, 
            Map<String, String> params, String timestamp) {
        
        StringBuilder sb = new StringBuilder();
        sb.append(method.toUpperCase()).append("\n");
        sb.append(uri).append("\n");
        
        // 参数按字典序排列
        params.entrySet().stream()
              .sorted(Map.Entry.comparingByKey())
              .forEach(entry -> sb.append(entry.getKey())
                                 .append("=")
                                 .append(entry.getValue())
                                 .append("&"));
                                 
        sb.append("timestamp=").append(timestamp);
        
        return sb.toString();
    }
}
```

### 4.3 防重放攻击机制


**⏰ 时间窗口验证**
```java
@Service
public class ReplayAttackProtector {
    
    private final RedisTemplate<String, String> redisTemplate;
    private static final long VALID_TIME_WINDOW = 5 * 60 * 1000; // 5分钟
    
    public boolean isValidRequest(String apiKey, String timestamp, 
            String signature, String requestId) {
        
        // 1. 时间戳验证
        long currentTime = System.currentTimeMillis();
        long requestTime = Long.parseLong(timestamp);
        
        if (Math.abs(currentTime - requestTime) > VALID_TIME_WINDOW) {
            log.warn("请求超时，当前时间：{}，请求时间：{}", currentTime, requestTime);
            return false;
        }
        
        // 2. 请求唯一性验证（防重放）
        String cacheKey = "request:" + apiKey + ":" + requestId;
        if (redisTemplate.hasKey(cacheKey)) {
            log.warn("重复请求检测到：{}", requestId);
            return false;
        }
        
        // 3. 记录请求ID（设置过期时间）
        redisTemplate.opsForValue().set(cacheKey, "1", 
            VALID_TIME_WINDOW, TimeUnit.MILLISECONDS);
            
        return true;
    }
}
```

### 4.4 客户端签名示例


**📱 客户端如何生成签名**
```java
public class ApiClient {
    
    private final String apiKey;
    private final String secretKey;
    private final RequestSigner signer;
    
    public String callSecureApi(String endpoint, Map<String, String> params) {
        
        // 1. 生成时间戳和请求ID
        String timestamp = String.valueOf(System.currentTimeMillis());
        String requestId = UUID.randomUUID().toString();
        
        // 2. 添加必要参数
        params.put("api_key", apiKey);
        params.put("timestamp", timestamp);
        params.put("request_id", requestId);
        
        // 3. 生成签名
        String signature = signer.generateSignature("POST", endpoint, 
                                                   params, timestamp, secretKey);
        
        // 4. 构建请求头
        HttpHeaders headers = new HttpHeaders();
        headers.set("X-API-Key", apiKey);
        headers.set("X-Timestamp", timestamp);
        headers.set("X-Request-Id", requestId);
        headers.set("X-Signature", signature);
        
        // 5. 发送请求
        return restTemplate.postForObject(endpoint, 
                new HttpEntity<>(params, headers), String.class);
    }
}
```

> 🔥 **安全提醒**
> 
> 签名算法的安全性依赖于密钥的保密性，请妥善保管API密钥，避免在日志中输出

---

## 5. 🔍 数据验证与编码


### 5.1 输入验证的重要性


**🚪 输入验证就像门卫检查**
```
门卫检查访客：
✅ 身份证件是否有效
✅ 访问目的是否合理
✅ 携带物品是否安全
❌ 可疑人员拒绝入内

API输入验证：
✅ 数据格式是否正确
✅ 数据长度是否合理  
✅ 数据内容是否安全
❌ 恶意数据拒绝处理
```

### 5.2 Spring Boot数据验证实践


**📋 实体类验证注解**
```java
@Entity
@Table(name = "users")
public class User {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 50, message = "用户名长度必须在3-50之间")
    @Pattern(regexp = "^[a-zA-Z0-9_]+$", message = "用户名只能包含字母、数字和下划线")
    private String username;
    
    @Email(message = "邮箱格式不正确")
    @NotBlank(message = "邮箱不能为空")
    private String email;
    
    @NotBlank(message = "密码不能为空")
    @Size(min = 8, max = 100, message = "密码长度必须在8-100之间")
    @Pattern(regexp = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]",
             message = "密码必须包含大小写字母、数字和特殊字符")
    private String password;
    
    @Min(value = 0, message = "年龄不能为负数")
    @Max(value = 150, message = "年龄不能超过150")
    private Integer age;
    
    // getter/setter...
}
```

**🔧 控制器验证处理**
```java
@RestController
@RequestMapping("/api/users")
@Validated
public class UserController {
    
    @PostMapping
    public ResponseEntity<?> createUser(@Valid @RequestBody User user, 
                                       BindingResult bindingResult) {
        
        // 手动检查验证结果
        if (bindingResult.hasErrors()) {
            Map<String, String> errors = new HashMap<>();
            bindingResult.getFieldErrors().forEach(error -> 
                errors.put(error.getField(), error.getDefaultMessage())
            );
            return ResponseEntity.badRequest().body(errors);
        }
        
        // 业务逻辑处理
        User savedUser = userService.createUser(user);
        return ResponseEntity.ok(savedUser);
    }
    
    // 路径参数验证
    @GetMapping("/{id}")
    public ResponseEntity<?> getUserById(
            @PathVariable @Min(value = 1, message = "用户ID必须大于0") Long id) {
        
        User user = userService.findById(id);
        return ResponseEntity.ok(user);
    }
}
```

### 5.3 自定义验证器


**🛠️ 业务逻辑验证**
```java
// 自定义注解
@Target({ElementType.FIELD})
@Retention(RetentionPolicy.RUNTIME)
@Constraint(validatedBy = UniqueUsernameValidator.class)
public @interface UniqueUsername {
    String message() default "用户名已存在";
    Class<?>[] groups() default {};
    Class<? extends Payload>[] payload() default {};
}

// 验证器实现
@Component
public class UniqueUsernameValidator implements ConstraintValidator<UniqueUsername, String> {
    
    @Autowired
    private UserRepository userRepository;
    
    @Override
    public boolean isValid(String username, ConstraintValidatorContext context) {
        if (username == null) {
            return true; // 空值由@NotBlank处理
        }
        
        return !userRepository.existsByUsername(username);
    }
}
```

### 5.4 输出编码防护


**🔒 XSS防护编码**
```java
@Component
public class OutputEncoder {
    
    // HTML编码
    public String encodeHtml(String input) {
        if (input == null) return null;
        
        return input.replace("&", "&amp;")
                   .replace("<", "&lt;")
                   .replace(">", "&gt;")
                   .replace("\"", "&quot;")
                   .replace("'", "&#x27;");
    }
    
    // JavaScript编码
    public String encodeJavaScript(String input) {
        if (input == null) return null;
        
        StringBuilder sb = new StringBuilder();
        for (char c : input.toCharArray()) {
            if (c < 256) {
                sb.append("\\x").append(String.format("%02x", (int) c));
            } else {
                sb.append("\\u").append(String.format("%04x", (int) c));
            }
        }
        return sb.toString();
    }
    
    // URL编码
    public String encodeUrl(String input) {
        try {
            return URLEncoder.encode(input, StandardCharsets.UTF_8.toString());
        } catch (UnsupportedEncodingException e) {
            throw new RuntimeException("URL编码失败", e);
        }
    }
}
```

> 💡 **安全提示**
> 
> 永远不要信任来自客户端的任何数据，即使是看起来无害的数据也要进行验证和编码

---

## 6. 🛡️ 常见攻击防护


### 6.1 SQL注入防护


**💉 SQL注入攻击原理**
```sql
-- 正常查询
SELECT * FROM users WHERE username = 'john' AND password = '123456';

-- SQL注入攻击
SELECT * FROM users WHERE username = 'admin' OR '1'='1' -- ' AND password = '';
-- 结果：返回所有用户数据！
```

**✅ 防护措施实现**
```java
@Repository
public class UserRepository {
    
    @Autowired
    private JdbcTemplate jdbcTemplate;
    
    // ❌ 错误做法：字符串拼接
    public User findByUsernameUnsafe(String username) {
        String sql = "SELECT * FROM users WHERE username = '" + username + "'";
        // 这样写容易被SQL注入！
        return jdbcTemplate.queryForObject(sql, User.class);
    }
    
    // ✅ 正确做法：参数化查询
    public User findByUsernameSafe(String username) {
        String sql = "SELECT * FROM users WHERE username = ?";
        return jdbcTemplate.queryForObject(sql, 
                new Object[]{username}, User.class);
    }
    
    // ✅ 更好做法：使用JPA
    @Query("SELECT u FROM User u WHERE u.username = :username")
    User findByUsername(@Param("username") String username);
}
```

### 6.2 XSS攻击防护


**🕸️ XSS攻击场景**
```html
<!-- 用户输入恶意脚本 -->
用户评论：<script>alert('XSS攻击!');</script>

<!-- 如果直接输出到页面 -->
<div class="comment">
    <script>alert('XSS攻击!');</script>  <!-- 脚本会执行！ -->
</div>
```

**🔐 XSS防护实现**
```java
@Service
public class ContentSecurityService {
    
    // HTML内容过滤
    private final PolicyFactory policy = Sanitizers.FORMATTING
            .and(Sanitizers.LINKS)
            .and(Sanitizers.BLOCKS)
            .and(Sanitizers.IMAGES);
    
    public String sanitizeHtml(String input) {
        return policy.sanitize(input);
    }
    
    // 内容安全策略头设置
    @Component
    public class SecurityHeadersFilter implements Filter {
        
        @Override
        public void doFilter(ServletRequest request, ServletResponse response,
                           FilterChain chain) throws IOException, ServletException {
            
            HttpServletResponse httpResponse = (HttpServletResponse) response;
            
            // Content Security Policy
            httpResponse.setHeader("Content-Security-Policy", 
                "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'");
            
            // XSS Protection
            httpResponse.setHeader("X-XSS-Protection", "1; mode=block");
            
            // Content Type Options
            httpResponse.setHeader("X-Content-Type-Options", "nosniff");
            
            chain.doFilter(request, response);
        }
    }
}
```

### 6.3 CSRF攻击防护


**🎭 CSRF攻击过程**
```
1. 用户登录银行网站A，获得认证Cookie
2. 用户访问恶意网站B（未退出网站A）
3. 恶意网站B包含转账请求：
   <img src="http://bank.com/transfer?to=hacker&amount=1000">
4. 浏览器自动携带Cookie发送请求
5. 银行网站A执行转账操作（以为是用户主动操作）
```

**🛡️ CSRF防护配置**
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig extends WebSecurityConfigurerAdapter {
    
    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http
            // 启用CSRF保护
            .csrf()
                .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
                .and()
            
            // 配置访问权限
            .authorizeRequests()
                .antMatchers("/api/public/**").permitAll()
                .anyRequest().authenticated()
                .and()
            
            // 配置登录
            .formLogin()
                .loginPage("/login")
                .permitAll();
    }
}

// 前端获取CSRF Token
@RestController
public class CsrfController {
    
    @GetMapping("/api/csrf-token")
    public CsrfToken csrf(CsrfToken token) {
        return token;
    }
}
```

### 6.4 限流与防DDoS


**⛽ 请求限流实现**
```java
@Component
public class RateLimiter {
    
    private final RedisTemplate<String, String> redisTemplate;
    
    /**
     * 基于滑动窗口的限流算法
     * @param key 限流键（通常是IP或用户ID）
     * @param limit 限制次数
     * @param window 时间窗口（秒）
     * @return 是否允许请求
     */
    public boolean isAllowed(String key, int limit, int window) {
        String redisKey = "rate_limit:" + key;
        long currentTime = System.currentTimeMillis();
        long windowStart = currentTime - window * 1000;
        
        // 使用Redis的ZSET实现滑动窗口
        redisTemplate.opsForZSet().removeRangeByScore(redisKey, 0, windowStart);
        
        Long currentCount = redisTemplate.opsForZSet().count(redisKey, windowStart, currentTime);
        
        if (currentCount < limit) {
            redisTemplate.opsForZSet().add(redisKey, UUID.randomUUID().toString(), currentTime);
            redisTemplate.expire(redisKey, window, TimeUnit.SECONDS);
            return true;
        }
        
        return false;
    }
}

// 限流拦截器
@Component
public class RateLimitInterceptor implements HandlerInterceptor {
    
    @Autowired
    private RateLimiter rateLimiter;
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                           HttpServletResponse response, 
                           Object handler) throws Exception {
        
        String clientIp = getClientIp(request);
        
        // 每个IP每分钟最多100次请求
        if (!rateLimiter.isAllowed(clientIp, 100, 60)) {
            response.setStatus(429); // Too Many Requests
            response.getWriter().write("请求过于频繁，请稍后再试");
            return false;
        }
        
        return true;
    }
}
```

> ⚠️ **重要提醒**
> 
> 安全防护是一个系统工程，单一的防护措施往往不够，需要多层防护策略相结合

---

## 7. 🏆 安全最佳实践


### 7.1 安全开发生命周期


**🔄 安全融入开发全流程**
```
需求分析阶段 → 设计阶段 → 编码阶段 → 测试阶段 → 部署阶段 → 运维阶段
     ↓           ↓         ↓         ↓         ↓         ↓
  威胁建模    安全设计   安全编码   安全测试   安全部署   安全监控
```

### 7.2 代码审查安全检查清单


**📋 安全代码Review要点**

✅ **认证授权检查**
- [ ] 是否有未授权的API端点？
- [ ] 权限验证逻辑是否正确？
- [ ] 敏感操作是否需要二次验证？

✅ **数据处理检查**
- [ ] 用户输入是否经过验证？
- [ ] SQL查询是否使用参数化？
- [ ] 输出数据是否经过编码？

✅ **密钥管理检查**
- [ ] 是否有硬编码的密钥？
- [ ] 敏感信息是否加密存储？
- [ ] 密钥是否定期轮换？

### 7.3 安全配置管理


**⚙️ 生产环境安全配置**
```yaml
# application-prod.yml
server:
  # 隐藏服务器版本信息
  server-header: ""
  
spring:
  # 禁用开发工具
  devtools:
    enabled: false
    
  # 数据库连接安全
  datasource:
    url: jdbc:mysql://db-server:3306/myapp?useSSL=true&requireSSL=true
    hikari:
      leak-detection-threshold: 60000
      
  # Redis安全配置  
  redis:
    password: ${REDIS_PASSWORD}
    ssl: true
    
# 日志安全配置
logging:
  level:
    # 生产环境关闭调试日志
    org.springframework.security: WARN
    org.springframework.web: WARN
  pattern:
    # 避免在日志中输出敏感信息
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    
# 管理端点安全
management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized
```

### 7.4 安全监控与告警


**📊 安全监控指标**
```java
@Component
public class SecurityMetrics {
    
    private final MeterRegistry meterRegistry;
    private final Counter failedLoginCounter;
    private final Counter sqlInjectionAttemptCounter;
    
    public SecurityMetrics(MeterRegistry meterRegistry) {
        this.meterRegistry = meterRegistry;
        this.failedLoginCounter = Counter.builder("security.login.failed")
                .description("Failed login attempts")
                .register(meterRegistry);
        this.sqlInjectionAttemptCounter = Counter.builder("security.sql.injection.attempt")
                .description("SQL injection attempts")
                .register(meterRegistry);
    }
    
    public void recordFailedLogin(String username, String ip) {
        failedLoginCounter.increment(
            Tags.of("username", username, "ip", ip)
        );
    }
    
    public void recordSqlInjectionAttempt(String ip, String userAgent) {
        sqlInjectionAttemptCounter.increment(
            Tags.of("ip", ip, "user_agent", userAgent)
        );
    }
}

// 安全事件告警
@Service
public class SecurityAlertService {
    
    private final NotificationService notificationService;
    
    @EventListener
    public void handleSecurityEvent(SecurityEvent event) {
        
        switch (event.getType()) {
            case FAILED_LOGIN_THRESHOLD_EXCEEDED:
                // 连续登录失败超过阈值
                sendAlert("可疑登录活动", event.getDetails());
                break;
                
            case SQL_INJECTION_DETECTED:
                // 检测到SQL注入尝试
                sendUrgentAlert("SQL注入攻击", event.getDetails());
                break;
                
            case UNUSUAL_API_USAGE:
                // 异常API使用模式
                sendAlert("异常API访问", event.getDetails());
                break;
        }
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的安全概念


```
🔐 API安全三要素：机密性、完整性、可用性
🔑 密钥管理：生成、存储、使用、轮换、销毁全生命周期
✍️ 请求签名：防篡改、防重放的核心机制
🛡️ 输入验证：所有用户输入都是不可信的
🔒 输出编码：防止XSS等注入攻击的关键
```

### 8.2 关键实施要点


**🔹 开发阶段**
```
设计安全：从架构设计阶段考虑安全
编码安全：遵循安全编码规范
测试安全：包含安全测试用例
```

**🔹 部署阶段**
```
环境隔离：开发、测试、生产环境分离
配置管理：敏感配置加密存储
权限最小：只给必要的最小权限
```

**🔹 运维阶段**
```
监控告警：实时监控安全事件
日志审计：记录关键操作日志
应急响应：制定安全事件处理流程
```

### 8.3 新手学习路径建议


**📚 学习顺序推荐**
```
第一阶段：理解基础概念
├─ HTTPS工作原理
├─ 常见攻击类型
└─ 安全防护原理

第二阶段：实践基础防护
├─ Spring Security配置
├─ 输入验证实现
└─ SQL注入防护

第三阶段：高级安全特性
├─ API签名机制
├─ 限流防护
└─ 安全监控
```

**💡 实用学习建议**
- 从简单的验证开始，逐步增加复杂性
- 多动手实践，光看理论是不够的
- 关注安全漏洞案例，从失败中学习
- 定期关注安全社区动态，了解新威胁

> 🎯 **核心记忆**
> 
> API安全不是一次性工作，而是需要在整个开发和运维过程中持续关注的重要议题。安全防护要做到"纵深防御"，多层保护，单点失效不影响整体安全。

**最后的话**：安全是一个不断学习和改进的过程。随着技术发展和威胁演变，我们需要持续更新安全知识和防护手段。记住：没有100%的安全，但我们要努力做到99.9%！