---
title: 2、生产安全配置清单
---
## 📚 目录

1. [生产环境安全概述](#1-生产环境安全概述)
2. [配置文件安全](#2-配置文件安全)
3. [默认设置安全加固](#3-默认设置安全加固)
4. [服务与端口管理](#4-服务与端口管理)
5. [网络安全策略](#5-网络安全策略)
6. [防火墙配置实战](#6-防火墙配置实战)
7. [入侵检测与防护](#7-入侵检测与防护)
8. [安全基线标准](#8-安全基线标准)
9. [生产部署检查清单](#9-生产部署检查清单)

---

## 1. 🛡️ 生产环境安全概述


### 1.1 为什么生产环境安全如此重要


**直白理解**：开发环境就像你自己家里，随意一点没关系；生产环境就像银行金库，必须严防死守。

```
开发环境 vs 生产环境的区别：

🏠 开发环境：
- 数据是假的测试数据
- 出问题只影响开发人员
- 配置可以随意调试
- 密码可以用简单的

🏦 生产环境：
- 数据是真实用户数据
- 出问题影响所有用户
- 配置必须严格把控
- 密码必须复杂且定期更换
```

### 1.2 生产安全的核心目标


**🎯 三大安全目标**：

| 安全目标 | 含义解释 | 实际例子 |
|---------|---------|---------|
| **保密性** | 数据只能被授权的人看到 | 用户密码、银行卡号不能泄露 |
| **完整性** | 数据不能被篡改 | 订单金额不能被恶意修改 |
| **可用性** | 系统随时可以正常使用 | 用户随时能登录、下单 |

> 💡 **新手理解**：这就像你的手机一样 - 别人不能看你的隐私(保密性)、不能改你的照片(完整性)、你随时都能用(可用性)。

---

## 2. 🔐 配置文件安全


### 2.1 配置文件为什么是安全重点


**问题场景**：很多新手会把包含密码的配置文件直接上传到GitHub，结果被黑客扫描到，数据库就被攻击了！

```
❌ 常见的危险做法：

application.yml 文件内容：
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root
    password: root123  # 直接明文写密码

然后把这个文件上传到GitHub公开仓库
→ 黑客用工具扫描GitHub
→ 发现你的数据库密码
→ 入侵你的系统
```

### 2.2 配置文件安全的正确做法


**🔹 方案一：使用环境变量（推荐新手）**

```yaml
# application-prod.yml
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/userdb
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
```

```bash
# 在服务器上设置环境变量（不会被上传到代码仓库）
export DB_HOST=192.168.1.100
export DB_USERNAME=prod_user
export DB_PASSWORD=Abc@123!XyZ
```

> 🔥 **关键理解**：配置文件里只写占位符 `${变量名}`，真实的值存在服务器环境变量里，这样代码可以公开，密码还是安全的。

**🔹 方案二：使用配置中心（进阶方案）**

```
流程理解：
1. 敏感配置存储在专门的配置中心（如Nacos、Apollo）
2. 应用启动时从配置中心读取
3. 配置中心本身有严格的权限控制

好处：
✅ 配置集中管理
✅ 修改配置不需要重启应用
✅ 有完整的操作审计日志
```

### 2.3 敏感信息加密存储


**加密配置示例**：

```yaml
# 使用Jasypt加密工具
spring:
  datasource:
    password: ENC(加密后的密文)  # 不是明文密码

# 应用启动时提供解密密钥
java -jar app.jar --jasypt.encryptor.password=解密密钥
```

**📋 配置文件安全检查表**：

- [ ] 所有密码都使用环境变量或加密存储
- [ ] 配置文件不包含IP地址（使用域名）
- [ ] API密钥、Token等敏感信息已移除
- [ ] `.gitignore` 文件已配置，排除敏感配置
- [ ] 生产配置文件权限设置为 `600`（只有所有者可读写）

---

## 3. 🔑 默认设置安全加固


### 3.1 默认密码的巨大风险


**真实案例**：很多系统被攻击，就是因为使用了默认密码！

```
常见的默认密码陷阱：

❌ MySQL默认密码：root/root
❌ Redis默认密码：无密码
❌ RabbitMQ默认密码：guest/guest
❌ Nacos默认密码：nacos/nacos
❌ Spring Boot Actuator默认不需要密码
```

> ⚠️ **警告**：黑客有专门的工具，会自动扫描互联网上使用默认密码的服务，一旦发现就立即攻击！

### 3.2 默认密码修改实战


**🔸 MySQL密码修改**：

```sql
-- 修改root密码为复杂密码
ALTER USER 'root'@'localhost' IDENTIFIED BY 'Mysql@2025!Secure';

-- 创建专用数据库用户，不使用root
CREATE USER 'app_user'@'%' IDENTIFIED BY 'App@Pass!2025';
GRANT SELECT, INSERT, UPDATE, DELETE ON mydb.* TO 'app_user'@'%';
```

**🔸 Redis安全配置**：

```conf
# redis.conf
requirepass YourVeryStrongPassword@2025

# 禁用危险命令
rename-command FLUSHDB ""
rename-command FLUSHALL ""
rename-command CONFIG ""
```

**🔸 Spring Boot Actuator安全配置**：

```yaml
management:
  endpoints:
    web:
      exposure:
        include: health,info  # 只暴露必要的端点
  endpoint:
    health:
      show-details: when-authorized  # 只对授权用户显示详情
  
# 添加安全认证
spring:
  security:
    user:
      name: admin
      password: ${ACTUATOR_PASSWORD}  # 从环境变量读取
```

### 3.3 默认端口修改


**为什么要修改默认端口**：黑客会扫描默认端口，修改端口可以增加攻击难度。

```
常见服务默认端口：
3306 → MySQL
6379 → Redis
8080 → Tomcat/Spring Boot
27017 → MongoDB

修改建议：
3306 → 33306（加10000）
6379 → 16379
8080 → 18080
```

```yaml
# application.yml
server:
  port: 18080  # 不使用默认8080
```

---

## 4. 🚪 服务与端口管理


### 4.1 最小化服务原则


**核心理念**：服务器上只运行必要的服务，关闭一切不需要的服务。

```
服务检查流程：

步骤1：列出所有运行的服务
systemctl list-units --type=service --state=running

步骤2：识别不需要的服务
常见可以关闭的：
- cups (打印服务，服务器不需要)
- bluetooth (蓝牙服务)
- avahi-daemon (网络发现服务)

步骤3：停止并禁用
systemctl stop cups
systemctl disable cups
```

> 💡 **形象理解**：这就像你家里的门窗，用不到的门就锁死，窗户能封死就封死，只留必须的出入口，这样小偷进来的路就少了。

### 4.2 端口开放管理


**🔹 端口开放检查**：

```bash
# 查看所有监听的端口
netstat -tuln | grep LISTEN

# 或使用ss命令（更现代）
ss -tuln
```

**端口分类管理**：

| 端口类型 | 开放范围 | 典型端口 | 安全策略 |
|---------|---------|---------|---------|
| **公网端口** | 对所有人开放 | 80(HTTP), 443(HTTPS) | 必须有安全防护 |
| **内网端口** | 只对内网开放 | 8080(应用), 3306(MySQL) | 防火墙限制IP |
| **管理端口** | 只对运维人员 | 22(SSH) | IP白名单+密钥认证 |

**🔹 端口绑定安全配置**：

```yaml
# ❌ 错误：绑定到所有网卡（0.0.0.0）
server:
  address: 0.0.0.0  # 危险！任何人都能访问
  port: 8080

# ✅ 正确：只绑定内网IP
server:
  address: 192.168.1.100  # 只能内网访问
  port: 8080
```

### 4.3 不必要服务识别与关闭


**📋 生产环境服务检查清单**：

```
✅ 必须运行的服务：
- sshd (远程管理，但要加固)
- java应用服务
- nginx/apache (Web服务器)
- 数据库服务
- 监控agent

❌ 建议关闭的服务：
- telnet (不安全的远程协议)
- ftp (使用sftp替代)
- rpcbind (非必要的RPC服务)
- postfix (非邮件服务器不需要)
```

**关闭服务脚本示例**：

```bash
#!/bin/bash
# 关闭不必要的服务

services=("telnet" "rpcbind" "postfix" "cups")

for service in "${services[@]}"
do
    if systemctl is-active --quiet $service; then
        echo "正在停止 $service..."
        systemctl stop $service
        systemctl disable $service
        echo "$service 已停止并禁用"
    fi
done
```

---

## 5. 🌐 网络安全策略


### 5.1 网络隔离设计


**网络分层架构**：

```
互联网层 (DMZ区)
    ↓ 防火墙
┌─────────────────────┐
│   Nginx反向代理      │ ← 只暴露80/443端口
│   (公网可访问)       │
└─────────────────────┘
    ↓ 内网防火墙
┌─────────────────────┐
│   应用服务器集群     │ ← 内网IP，外网无法直接访问
│   (10.0.1.x)        │
└─────────────────────┘
    ↓ 数据库防火墙
┌─────────────────────┐
│   数据库服务器       │ ← 只允许应用服务器访问
│   (10.0.2.x)        │
└─────────────────────┘
```

> 🔥 **关键理解**：这就像古代城堡的防御体系 - 外层城墙(DMZ)、内城(应用层)、核心堡垒(数据层)，层层防护。

### 5.2 访问控制策略


**🔸 基于IP的访问控制**：

```yaml
# Spring Security IP白名单配置
spring:
  security:
    filter:
      # 管理接口只允许特定IP访问
      dispatcher-types: request
      
security:
  ipwhitelist:
    admin-ips:
      - 192.168.1.100  # 运维机器IP
      - 10.0.1.0/24    # 内网网段
```

```java
// IP白名单过滤器（简化示例）
@Component
public class IPWhitelistFilter implements Filter {
    
    @Value("${security.ipwhitelist.admin-ips}")
    private List<String> allowedIps;
    
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, 
                        FilterChain chain) {
        String clientIp = request.getRemoteAddr();
        
        if (isAdminPath(request) && !isAllowedIp(clientIp)) {
            // 拒绝访问
            ((HttpServletResponse)response).setStatus(403);
            return;
        }
        
        chain.doFilter(request, response);
    }
}
```

### 5.3 SSL/TLS加密传输


**HTTPS配置要点**：

```
SSL证书配置流程：

步骤1：申请SSL证书
- 免费证书：Let's Encrypt
- 商业证书：DigiCert、GlobalSign

步骤2：Nginx配置HTTPS
server {
    listen 443 ssl http2;
    server_name api.example.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    # 强制使用强加密套件
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
}

步骤3：HTTP自动跳转HTTPS
server {
    listen 80;
    server_name api.example.com;
    return 301 https://$server_name$request_uri;
}
```

---

## 6. 🔥 防火墙配置实战


### 6.1 防火墙基础概念


**什么是防火墙**：防火墙就像小区的保安，检查每个进出的人（数据包），只放行安全的，拦截危险的。

```
Linux常用防火墙工具：

iptables（传统工具）
    ↓ 替代
firewalld（CentOS 7+）
    ↓ 替代
ufw（Ubuntu推荐）
```

### 6.2 Firewalld配置实战


**🔹 基础操作**：

```bash
# 启动防火墙
systemctl start firewalld
systemctl enable firewalld

# 查看防火墙状态
firewall-cmd --state

# 查看已开放的端口
firewall-cmd --list-ports
```

**🔹 端口开放配置**：

```bash
# 开放HTTPS端口（永久生效）
firewall-cmd --permanent --add-port=443/tcp

# 开放应用端口（仅本次运行）
firewall-cmd --add-port=8080/tcp

# 重载配置使永久规则生效
firewall-cmd --reload
```

**🔹 IP白名单配置**：

```bash
# 只允许特定IP访问MySQL端口
firewall-cmd --permanent --add-rich-rule='
  rule family="ipv4" 
  source address="192.168.1.100" 
  port protocol="tcp" port="3306" 
  accept'

# 允许整个内网段访问
firewall-cmd --permanent --add-rich-rule='
  rule family="ipv4" 
  source address="10.0.1.0/24" 
  port protocol="tcp" port="8080" 
  accept'
```

### 6.3 防火墙安全规则设计


**🎯 防火墙规则设计原则**：

```
原则1：默认拒绝策略（最安全）
所有端口默认关闭，只开放必要的

原则2：最小权限原则
数据库端口只对应用服务器开放，不对公网

原则3：分层防护
外层防火墙 + 应用层防火墙 + 数据库防火墙
```

**生产环境标准规则模板**：

```bash
#!/bin/bash
# 生产环境防火墙配置脚本

# 1. 开放Web服务端口
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https

# 2. SSH只对运维IP开放
firewall-cmd --permanent --add-rich-rule='
  rule family="ipv4" 
  source address="运维机IP" 
  port protocol="tcp" port="22" 
  accept'

# 3. 应用端口只对内网开放
firewall-cmd --permanent --add-rich-rule='
  rule family="ipv4" 
  source address="10.0.0.0/16" 
  port protocol="tcp" port="8080" 
  accept'

# 4. 数据库端口只对应用服务器开放
firewall-cmd --permanent --add-rich-rule='
  rule family="ipv4" 
  source address="应用服务器IP" 
  port protocol="tcp" port="3306" 
  accept'

# 5. 重载配置
firewall-cmd --reload

echo "防火墙配置完成！"
```

---

## 7. 🛡️ 入侵检测与防护


### 7.1 入侵检测系统（IDS）基础


**什么是入侵检测**：就像安装了监控摄像头，实时观察有没有可疑行为，发现异常立即报警。

```
入侵检测的工作原理：

步骤1：收集系统日志和网络流量
步骤2：分析是否有异常行为模式
步骤3：发现异常立即告警
步骤4：记录详细信息供调查

常见异常行为：
🚨 短时间大量失败登录尝试（暴力破解）
🚨 访问不存在的URL（扫描漏洞）
🚨 SQL注入特征字符
🚨 异常大流量（DDoS攻击）
```

### 7.2 Fail2Ban入侵防护实战


**Fail2Ban工作机制**：

```
监控日志 → 发现异常 → 自动封禁IP

例如：
1. 监控SSH登录日志
2. 发现某IP连续5次登录失败
3. 自动将该IP加入防火墙黑名单
4. 10分钟后自动解封（可配置）
```

**🔹 Fail2Ban安装配置**：

```bash
# 安装
yum install fail2ban

# 配置SSH保护
cat > /etc/fail2ban/jail.local << EOF
[sshd]
enabled = true
port = ssh
filter = sshd
logpath = /var/log/secure
maxretry = 3        # 最多尝试3次
bantime = 3600      # 封禁1小时
findtime = 600      # 10分钟内
EOF

# 启动服务
systemctl start fail2ban
systemctl enable fail2ban
```

**🔹 查看封禁状态**：

```bash
# 查看被封禁的IP
fail2ban-client status sshd

# 手动解封IP
fail2ban-client set sshd unbanip 192.168.1.100
```

### 7.3 应用层入侵检测


**🔸 Spring Security异常检测**：

```java
// 登录失败监控
@Component
public class LoginFailureListener {
    
    private Map<String, Integer> failureCount = new ConcurrentHashMap<>();
    
    @EventListener
    public void onLoginFailure(AuthenticationFailureBadCredentialsEvent event) {
        String username = event.getAuthentication().getName();
        String ip = getClientIp();
        
        // 记录失败次数
        int count = failureCount.merge(ip, 1, Integer::sum);
        
        // 超过5次，封禁IP
        if (count >= 5) {
            blockIp(ip);
            log.warn("IP {} 登录失败{}次，已封禁", ip, count);
        }
    }
}
```

**📋 应用层检测要点**：

- [ ] 监控登录失败频率
- [ ] 检测SQL注入特征
- [ ] 识别异常API调用模式
- [ ] 记录所有敏感操作日志
- [ ] 异常行为实时告警

---

## 8. 📏 安全基线标准


### 8.1 什么是安全基线


**通俗解释**：安全基线就是系统安全的"及格线"，达到这个标准才算基本安全。

```
形象比喻：
安全基线 = 汽车出厂标准

汽车必须有：
✅ 安全带
✅ 刹车系统
✅ 安全气囊
这些是基线，缺一不可

系统也必须有：
✅ 强密码策略
✅ 防火墙配置
✅ 日志审计
这些是安全基线
```

### 8.2 企业级安全基线标准


**🔹 操作系统安全基线**：

| 检查项 | 基线要求 | 检查方法 |
|--------|---------|---------|
| **密码策略** | 长度≥12位，包含大小写+数字+特殊字符 | 查看 `/etc/security/pwquality.conf` |
| **账户管理** | 禁用root远程登录，使用sudo | `grep PermitRootLogin /etc/ssh/sshd_config` |
| **补丁更新** | 每月至少更新一次系统补丁 | `yum updateinfo list security` |
| **审计日志** | 启用审计日志，保留≥90天 | `systemctl status auditd` |

**🔹 应用安全基线**：

```yaml
# Spring Boot应用安全基线配置
spring:
  security:
    # 1. 强制HTTPS
    require-ssl: true
    
    # 2. Session安全
    session:
      cookie:
        secure: true      # Cookie只在HTTPS传输
        http-only: true   # 禁止JavaScript访问
        same-site: strict # 防止CSRF
    
    # 3. 密码策略
    password:
      min-length: 12
      require-special-char: true
      
  # 4. 禁用不安全功能
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.h2.H2ConsoleAutoConfiguration
```

### 8.3 数据库安全基线


**MySQL安全基线配置**：

```sql
-- 1. 删除匿名用户
DELETE FROM mysql.user WHERE User='';

-- 2. 禁止root远程登录
DELETE FROM mysql.user WHERE User='root' AND Host NOT IN ('localhost', '127.0.0.1');

-- 3. 删除test数据库
DROP DATABASE IF EXISTS test;

-- 4. 创建专用账户，使用最小权限
CREATE USER 'app_user'@'应用服务器IP' IDENTIFIED BY '复杂密码';
GRANT SELECT, INSERT, UPDATE, DELETE ON app_db.* TO 'app_user'@'应用服务器IP';

-- 5. 刷新权限
FLUSH PRIVILEGES;
```

**数据库配置文件加固**：

```ini
# my.cnf
[mysqld]
# 只监听内网IP
bind-address = 10.0.2.100

# 禁用本地文件加载（防止SQL注入读取系统文件）
local-infile = 0

# 启用慢查询日志（发现异常查询）
slow_query_log = 1
long_query_time = 2
```

---

## 9. ✅ 生产部署检查清单


### 9.1 部署前安全检查


**📋 上线前必查项目（逐项确认）**：

```
🔐 配置安全检查：
□ 所有密码已从代码中移除
□ 敏感配置使用环境变量或加密存储
□ 配置文件权限设置为600
□ .gitignore已配置，排除敏感文件
□ 生产配置文件已单独管理

🔑 认证授权检查：
□ 默认密码已全部修改
□ 密码复杂度符合安全策略（≥12位）
□ 启用多因素认证（MFA）
□ JWT过期时间设置合理（≤1小时）
□ 刷新Token机制已实现

🚪 服务端口检查：
□ 不必要的服务已关闭
□ 危险端口已关闭（Telnet/FTP）
□ 数据库端口不对公网开放
□ 管理端口仅对内网或VPN开放

🌐 网络安全检查：
□ 防火墙规则已配置
□ 仅必要端口对外开放
□ SSH采用密钥认证
□ 启用HTTPS，HTTP自动跳转
□ SSL证书有效期检查（>30天）

🛡️ 防护措施检查：
□ Fail2Ban或类似工具已安装
□ 入侵检测系统已配置
□ 限流策略已启用
□ 防火墙日志已启用
□ 异常告警已配置
```

### 9.2 部署后验证检查


**🔹 安全验证测试**：

```bash
# 1. 端口扫描检查（使用nmap）
nmap -sV 服务器IP

# 预期结果：只显示必要的开放端口
# ✅ 80/tcp   open  http
# ✅ 443/tcp  open  https
# ❌ 3306/tcp closed mysql （数据库端口应该关闭）

# 2. SSL安全检查
curl -I https://your-domain.com

# 预期结果：
# ✅ Strict-Transport-Security: max-age=31536000
# ✅ X-Content-Type-Options: nosniff
# ✅ X-Frame-Options: DENY

# 3. 敏感信息泄露检查
curl https://your-domain.com/actuator/env

# 预期结果：
# ❌ 401 Unauthorized （需要认证才能访问）
```

### 9.3 持续安全监控


**📊 日常监控要点**：

| 监控项 | 检查频率 | 异常阈值 | 处理措施 |
|--------|---------|---------|---------|
| **登录失败** | 实时 | 5分钟内5次失败 | 自动封禁IP |
| **API异常调用** | 实时 | 1分钟内100次 | 限流+告警 |
| **CPU使用率** | 每分钟 | >80%持续5分钟 | 告警+扩容 |
| **磁盘空间** | 每小时 | >85% | 告警+清理 |
| **SSL证书** | 每天 | <30天过期 | 提前续期 |

**🔹 安全巡检脚本**：

```bash
#!/bin/bash
# 每日安全巡检脚本

echo "===== 安全巡检开始 $(date) ====="

# 1. 检查是否有新增用户
echo "检查系统用户..."
users=$(cat /etc/passwd | grep -v nologin | wc -l)
echo "当前用户数：$users"

# 2. 检查防火墙状态
echo "检查防火墙..."
firewall-cmd --state

# 3. 检查失败登录记录
echo "检查登录失败记录..."
lastb | head -10

# 4. 检查磁盘使用率
echo "检查磁盘空间..."
df -h | grep -vE '^Filesystem|tmpfs|cdrom' | awk '{ print $5 " " $1 }' | while read output;
do
  usage=$(echo $output | awk '{print $1}' | sed 's/%//g')
  partition=$(echo $output | awk '{print $2}')
  if [ $usage -ge 85 ]; then
    echo "警告：分区 $partition 使用率 $usage%"
  fi
done

# 5. 检查进程异常
echo "检查CPU使用Top5进程..."
ps aux | sort -nrk 3,3 | head -5

echo "===== 巡检完成 ====="
```

---

## 🎯 核心要点总结


### 必须掌握的安全知识


**🔸 配置安全的本质**：
```
配置文件 = 系统的钥匙
密码泄露 = 把钥匙给了小偷
环境变量 = 把钥匙藏在安全的地方
```

**🔸 默认设置的危险性**：
```
默认密码就像：门锁用的是厂家默认密码"123456"
黑客第一个就试这个密码
必须改成复杂的个人密码
```

**🔸 防火墙的作用**：
```
防火墙 = 保安
检查每个进出的人（数据包）
只放行安全的，拦截危险的
没有防火墙 = 门户大开
```

**🔸 入侵检测的意义**：
```
入侵检测 = 监控摄像头
发现可疑行为立即报警
没有监控 = 小偷进来也不知道
```

### 生产部署记忆口诀


```
上线之前先检查，密码配置要分家
默认设置全修改，不用服务赶紧杀
防火墙来把好门，只放必要来往宾
监控告警不能少，异常发现早处理
```

### 新手常犯错误


```
❌ 错误1：配置文件包含明文密码上传GitHub
✅ 正确：使用环境变量，敏感信息不进代码库

❌ 错误2：数据库端口3306对公网开放
✅ 正确：只对应用服务器IP开放

❌ 错误3：使用默认密码，觉得"测试环境无所谓"
✅ 正确：任何环境都要用强密码

❌ 错误4：关闭防火墙"方便调试"
✅ 正确：永远不要关闭防火墙，通过规则控制

❌ 错误5：不看日志，出问题才知道
✅ 正确：配置监控告警，主动发现问题
```

> 🔥 **终极提醒**：安全无小事，一个小疏忽就可能导致整个系统被攻击。生产环境的安全配置，宁可麻烦一点，也不要心存侥幸！