---
title: 4、现代认证协议
---
## 📚 目录

1. [OAuth 2.0授权框架](#1-OAuth-2-0授权框架)
2. [OpenID Connect身份层](#2-OpenID-Connect身份层)
3. [SAML联合身份](#3-SAML联合身份)
4. [WebAuthn标准](#4-WebAuthn标准)
5. [协议对比与选择](#5-协议对比与选择)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔑 OAuth 2.0授权框架


### 1.1 OAuth 2.0是什么


**🔸 核心概念**
```
OAuth 2.0 = 授权框架，不是认证协议
作用：让第三方应用安全获取用户资源的访问权限
目标：用户不需要把密码告诉第三方应用

通俗理解：
就像酒店房卡系统 - 你（资源所有者）授权给朋友（第三方应用）
一张临时房卡（访问令牌），朋友可以进入你的房间（访问资源）
但朋友不需要知道你的房间密码
```

**💡 解决的问题**
```
传统方式的问题：
用户想让手机App访问微信头像 → 需要把微信密码告诉App
风险：App可以用你的密码做任何事情

OAuth 2.0方式：
用户 → 微信确认授权 → 给App一个临时令牌 → App用令牌获取头像
好处：App永远不知道你的微信密码
```

### 1.2 OAuth 2.0的四个角色


**🎭 角色解释**
```
📱 客户端（Client）
- 就是第三方应用（比如某个手机App）
- 想要访问用户资源的应用

👤 资源所有者（Resource Owner）  
- 就是用户本人
- 决定是否授权给第三方应用

🏢 资源服务器（Resource Server）
- 存储用户资源的服务器（比如微信的头像服务器）
- 验证令牌并提供资源

🔐 授权服务器（Authorization Server）
- 负责用户认证和颁发令牌的服务器
- 通常和资源服务器是同一家公司
```

**🌰 生活化例子**
```
场景：用美团App点外卖，需要获取你的微信头像

👤 你 = 资源所有者（决定是否授权）
📱 美团App = 客户端（想要头像）
🏢 微信头像服务器 = 资源服务器（存储头像）
🔐 微信授权服务器 = 授权服务器（验证身份，发令牌）
```

### 1.3 OAuth 2.0授权流程


**📋 标准授权码流程**
```
用户操作流程：

第1步：点击"用微信登录"
美团App → 跳转到微信授权页面

第2步：微信确认授权
你 → 在微信页面点击"同意授权"

第3步：获取授权码
微信 → 跳转回美团App，带上授权码

第4步：换取访问令牌
美团App → 用授权码向微信换取访问令牌

第5步：访问用户资源
美团App → 用访问令牌获取你的微信头像
```

**🔄 详细技术流程**
```
客户端                    授权服务器                  资源服务器
   |                         |                          |
   |--[1]授权请求------------>|                          |
   |   /authorize?            |                          |
   |   client_id=xxx          |                          |
   |   redirect_uri=xxx       |                          |
   |   scope=read_profile     |                          |
   |                         |                          |
   |<-[2]返回授权码-----------|                          |
   |   code=abc123            |                          |
   |                         |                          |
   |--[3]令牌请求------------>|                          |
   |   code=abc123            |                          |
   |   client_secret=xxx      |                          |
   |                         |                          |
   |<-[4]返回访问令牌---------|                          |
   |   access_token=xyz789    |                          |
   |                         |                          |
   |--[5]资源请求-------------------------->|
   |   Authorization: Bearer xyz789         |
   |                                        |
   |<-[6]返回资源数据-----------------------|
   |   {"name":"张三","avatar":"xxx.jpg"}   |
```

### 1.4 OAuth 2.0的四种授权模式


**🎯 授权码模式（最安全，最常用）**
```
适用场景：Web应用、手机App
安全性：最高（授权码和令牌分离）
流程：用户授权 → 获取授权码 → 换取令牌

为什么安全？
- 授权码只能用一次
- 访问令牌不通过浏览器传输
- 需要客户端密钥验证
```

**⚡ 简化模式（适合前端应用）**
```
适用场景：纯前端应用（JavaScript）
安全性：较低（令牌直接返回）
流程：用户授权 → 直接获取令牌

使用场景：
- 单页应用（SPA）
- 无法保存客户端密钥的应用
```

**🔑 密码模式（不推荐）**
```
适用场景：高度信任的应用
安全性：低（需要用户密码）
流程：用户提供密码 → 直接获取令牌

为什么不推荐？
- 违背OAuth 2.0设计初衷
- 第三方应用知道用户密码
```

**🏢 客户端模式（机器对机器）**
```
适用场景：服务器间通信
安全性：中等（无用户参与）
流程：客户端凭证 → 直接获取令牌

使用场景：
- API调用
- 服务间认证
```

### 1.5 令牌类型与管理


**🎫 访问令牌（Access Token）**
```
作用：访问资源的凭证
特点：
- 有时效性（通常几小时）
- 可以设置访问范围（scope）
- 不可逆（无法解析出用户信息）

格式示例：
Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**🔄 刷新令牌（Refresh Token）**
```
作用：获取新的访问令牌
特点：
- 有效期长（几天到几个月）
- 一次性使用（用完即作废）
- 更安全（不用重新授权）

使用场景：
当访问令牌过期时，自动续期而不打断用户
```

**📊 令牌管理最佳实践**
```
安全存储：
✅ 服务端：数据库加密存储
✅ 客户端：安全存储（Keychain、加密SharedPreferences）
❌ 避免：localStorage明文存储

令牌使用：
✅ HTTPS传输
✅ 设置合理过期时间
✅ 及时清理过期令牌
❌ 避免：令牌泄露到日志
```

---

## 2. 🆔 OpenID Connect身份层


### 2.1 OpenID Connect是什么


**🔸 核心概念**
```
OpenID Connect = OAuth 2.0 + 身份认证
简称：OIDC

关系理解：
OAuth 2.0：回答"你能做什么"（授权）
OIDC：回答"你是谁"（认证）+ "你能做什么"（授权）

通俗比喻：
OAuth 2.0像门禁卡 - 能进哪些门
OIDC像身份证+门禁卡 - 证明你是谁，还能进哪些门
```

**💡 解决的认证问题**
```
OAuth 2.0的局限：
- 只能授权访问资源
- 无法确认用户身份
- 第三方应用不知道"登录的是谁"

OIDC的解决方案：
- 在OAuth 2.0基础上增加身份信息
- 提供ID令牌（ID Token）
- 标准化用户信息格式
```

### 2.2 OIDC的核心组件


**🎫 ID令牌（ID Token）**
```
作用：包含用户身份信息的令牌
格式：JWT（JSON Web Token）
内容：用户基本信息（姓名、邮箱等）

ID Token示例：
{
  "sub": "248289761001",      // 用户唯一标识
  "name": "张三",             // 用户姓名
  "email": "zhangsan@qq.com", // 邮箱
  "iat": 1516239022,          // 颁发时间
  "exp": 1516242622           // 过期时间
}
```

**📋 用户信息端点（UserInfo Endpoint）**
```
作用：获取详细用户信息的API接口
访问方式：使用访问令牌请求
返回内容：标准化的用户属性

标准用户属性：
- sub：用户唯一标识
- name：全名
- given_name：名字
- family_name：姓氏
- email：邮箱地址
- picture：头像URL
```

**🔍 发现端点（Discovery Endpoint）**
```
作用：自动发现OIDC服务配置
URL格式：/.well-known/openid_configuration
返回内容：所有OIDC端点URL和支持的功能

配置信息示例：
{
  "authorization_endpoint": "https://auth.example.com/authorize",
  "token_endpoint": "https://auth.example.com/token",
  "userinfo_endpoint": "https://auth.example.com/userinfo",
  "jwks_uri": "https://auth.example.com/jwks"
}
```

### 2.3 OIDC认证流程


**🔄 授权码+OIDC流程**
```
完整认证流程：

第1步：用户点击登录
App → 跳转到OIDC授权页面
参数：scope=openid profile email

第2步：用户确认授权
用户 → 同意分享身份信息

第3步：获取授权码
OIDC服务 → 返回授权码

第4步：换取令牌
App → 用授权码换取访问令牌和ID令牌

第5步：验证ID令牌
App → 验证ID令牌签名和内容

第6步：获取用户信息
App → 可选，用访问令牌获取详细用户信息
```

**🎯 与OAuth 2.0的关键区别**
```
请求参数差异：
OAuth 2.0：scope=read_profile
OIDC：scope=openid profile email

返回内容差异：
OAuth 2.0：只返回access_token
OIDC：返回access_token + id_token

使用目的差异：
OAuth 2.0：访问API资源
OIDC：确认用户身份 + 访问API资源
```

### 2.4 OIDC的三种流程


**📱 授权码流程（推荐）**
```
适用：Web应用、移动App
安全性：最高
特点：ID令牌不经过浏览器

流程要点：
1. 用授权码换取令牌
2. 服务端验证ID令牌
3. 获取用户信息登录
```

**⚡ 隐式流程（不推荐）**
```
适用：纯前端应用
安全性：较低
特点：ID令牌直接返回

为什么不推荐？
- ID令牌暴露在URL中
- 无法验证客户端身份
- 令牌可能被截获
```

**🔀 混合流程（复杂场景）**
```
适用：需要前后端协作的应用
安全性：中等
特点：同时使用授权码和隐式流程

使用场景：
- 前端需要立即获取ID令牌
- 后端需要长期访问资源
```

---

## 3. 🏛️ SAML联合身份


### 3.1 SAML是什么


**🔸 核心概念**
```
SAML = Security Assertion Markup Language
中文：安全断言标记语言
本质：基于XML的身份联合标准

作用：让不同组织间安全共享身份信息
目标：单点登录（SSO）和身份联合

通俗理解：
就像国际驾照 - 在一个国家考的驾照，
在其他国家也能开车，不用重新考试
```

**💡 解决的问题**
```
企业身份管理痛点：
- 员工需要记住多套账号密码
- IT部门需要管理多个用户系统
- 合作伙伴访问需要单独开账号

SAML解决方案：
- 一次登录，处处可用
- 统一身份管理
- 跨组织身份信任
```

### 3.2 SAML的核心角色


**🎭 三个重要角色**
```
👤 用户（User/Principal）
- 需要访问服务的人
- 例：员工小李

🏛️ 身份提供商（Identity Provider, IdP）
- 验证用户身份的服务
- 例：公司的AD域控服务器

🏢 服务提供商（Service Provider, SP）
- 提供业务服务的系统
- 例：公司的OA系统、邮件系统
```

**🌰 企业场景例子**
```
场景：员工访问公司各种系统

小李（用户）想用OA系统：
1. 小李访问OA系统
2. OA系统发现小李未登录
3. OA系统重定向到公司AD域控
4. 小李在AD域控输入工号密码
5. AD域控验证成功，生成SAML断言
6. 浏览器带着断言返回OA系统
7. OA系统验证断言，允许小李访问

好处：小李只需要登录一次，就能访问所有系统
```

### 3.3 SAML断言（Assertion）


**📜 断言的含义**
```
SAML断言 = 身份证明书
内容：用户身份信息和权限声明
格式：XML文档，经过数字签名

断言包含的信息：
- 用户是谁（身份声明）
- 用户能做什么（权限声明）
- 断言的有效期
- 颁发机构签名
```

**🔍 断言结构示例**
```xml
<saml:Assertion ID="123456" IssueInstant="2024-01-01T10:00:00Z">
  <!-- 颁发者信息 -->
  <saml:Issuer>https://company.com/idp</saml:Issuer>
  
  <!-- 用户身份信息 -->
  <saml:Subject>
    <saml:NameID Format="email">zhangsan@company.com</saml:NameID>
  </saml:Subject>
  
  <!-- 属性声明 -->
  <saml:AttributeStatement>
    <saml:Attribute Name="Department">
      <saml:AttributeValue>IT部门</saml:AttributeValue>
    </saml:Attribute>
    <saml:Attribute Name="Role">
      <saml:AttributeValue>系统管理员</saml:AttributeValue>
    </saml:Attribute>
  </saml:AttributeStatement>
  
  <!-- 数字签名（确保不被篡改） -->
  <ds:Signature>...</ds:Signature>
</saml:Assertion>
```

### 3.4 SAML SSO流程


**🔄 SP发起的SSO流程**
```
最常见的单点登录流程：

用户操作：
第1步：用户访问业务系统（SP）
第2步：系统发现用户未登录
第3步：重定向到身份认证系统（IdP）
第4步：用户输入用户名密码
第5步：认证成功后跳转回业务系统
第6步：业务系统验证身份，允许访问

技术细节：
SP → 生成SAML认证请求
IdP → 验证用户身份
IdP → 生成SAML断言
SP → 验证断言签名
SP → 解析用户信息
SP → 建立用户会话
```

**📊 详细交互流程**
```
浏览器                SP系统              IdP系统
   |                    |                    |
   |--[1]访问保护资源--->|                    |
   |                    |                    |
   |<-[2]重定向到IdP-----|                    |
   |  SAML认证请求       |                    |
   |                    |                    |
   |--[3]提交认证请求------------------>|
   |                                   |
   |<-[4]返回登录页面-------------------|
   |                                   |
   |--[5]提交用户凭证------------------>|
   |                                   |
   |<-[6]返回SAML断言-------------------|
   |  重定向到SP                        |
   |                    |                    |
   |--[7]提交SAML断言--->|                    |
   |                    |                    |
   |<-[8]返回保护资源-----|                    |
   |  设置会话cookie     |                    |
```

### 3.5 SAML的安全机制


**🔒 安全保障措施**
```
数字签名：
- 确保断言没有被篡改
- 验证断言的真实来源
- 使用公钥密码学技术

时间窗口：
- 断言有明确的有效期
- 防止重放攻击
- 通常几分钟内有效

传输安全：
- 使用HTTPS加密传输
- 防止断言被截获
- 保护用户隐私
```

**⚠️ 常见安全问题**
```
配置风险：
❌ 不验证断言签名
❌ 信任所有来源的断言
❌ 不检查断言有效期

防护措施：
✅ 严格验证断言签名
✅ 维护受信任的IdP列表
✅ 检查断言时间戳
✅ 使用HTTPS传输
```

---

## 4. 🔐 WebAuthn标准


### 4.1 WebAuthn是什么


**🔸 核心概念**
```
WebAuthn = Web Authentication API
中文：Web身份验证API
本质：无密码身份验证标准

作用：用生物识别或硬件密钥代替密码
目标：更安全、更便捷的身份验证

通俗理解：
就像用指纹解锁手机 - 不需要记密码，
更安全（指纹很难伪造），更方便（一触即解）
```

**💡 解决的密码问题**
```
传统密码的痛点：
❌ 容易忘记
❌ 容易被盗
❌ 弱密码不安全
❌ 重复使用风险高

WebAuthn的优势：
✅ 无需记忆
✅ 难以伪造
✅ 本地验证
✅ 抗钓鱼攻击
```

### 4.2 WebAuthn的工作原理


**🔑 公钥密码学基础**
```
核心概念：每个用户生成一对密钥
- 私钥：保存在用户设备上（永不传输）
- 公钥：注册时发送给服务器保存

登录过程：
1. 服务器发送挑战（随机数）
2. 设备用私钥签名挑战
3. 服务器用公钥验证签名
4. 验证成功则登录

安全性：
- 私钥永不离开设备
- 每次挑战都不同
- 无法重放攻击
```

**🎭 三个重要角色**
```
👤 用户（User）
- 想要登录的人
- 拥有支持WebAuthn的设备

🌐 依赖方（Relying Party, RP）
- 需要验证用户身份的网站
- 例：银行网站、电商网站

🔐 认证器（Authenticator）
- 生成和保存密钥的设备
- 例：手机指纹、USB安全密钥、Face ID
```

### 4.3 认证器类型


**📱 平台认证器（内置认证器）**
```
特点：设备内置的生物识别
常见类型：
- 指纹识别（Touch ID、指纹传感器）
- 面部识别（Face ID、Windows Hello）
- 虹膜识别
- 语音识别

优势：
✅ 用户体验好（无需额外设备）
✅ 速度快
✅ 成本低

限制：
❌ 绑定特定设备
❌ 设备丢失需要重新注册
```

**🔑 跨平台认证器（外部认证器）**
```
特点：独立的硬件安全密钥
常见类型：
- USB安全密钥（YubiKey）
- NFC安全密钥
- 蓝牙安全密钥

优势：
✅ 可在多设备使用
✅ 安全性极高
✅ 便携

限制：
❌ 需要单独购买
❌ 可能丢失
❌ 用户接受度较低
```

### 4.4 WebAuthn注册流程


**📝 首次注册过程**
```
用户体验流程：
第1步：用户在网站选择"设置无密码登录"
第2步：网站提示"请使用指纹验证"
第3步：用户按指纹或面部识别
第4步：设备生成密钥对
第5步：网站保存公钥，完成注册

技术实现流程：
1. 网站调用 navigator.credentials.create()
2. 浏览器显示认证器选择界面
3. 用户选择认证器（指纹/Face ID/安全密钥）
4. 认证器验证用户身份（生物识别）
5. 认证器生成新的密钥对
6. 私钥安全存储在设备中
7. 公钥返回给网站保存
```

**💻 注册代码示例**
```javascript
// 1. 创建注册选项
const createOptions = {
  challenge: new Uint8Array(32), // 服务器生成的随机挑战
  rp: {
    name: "示例银行",
    id: "bank.example.com"
  },
  user: {
    id: new TextEncoder().encode("user123"),
    name: "zhangsan@example.com",
    displayName: "张三"
  },
  pubKeyCredParams: [{
    type: "public-key",
    alg: -7 // ES256算法
  }],
  timeout: 60000,
  authenticatorSelection: {
    userVerification: "required" // 需要用户验证
  }
};

// 2. 调用WebAuthn API
const credential = await navigator.credentials.create({
  publicKey: createOptions
});

// 3. 发送公钥到服务器保存
fetch('/register', {
  method: 'POST',
  body: JSON.stringify({
    publicKey: credential.response.publicKey,
    credentialId: credential.id
  })
});
```

### 4.5 WebAuthn登录流程


**🔓 登录验证过程**
```
用户体验流程：
第1步：用户访问网站，输入用户名
第2步：网站提示"请验证身份"
第3步：用户按指纹或面部识别
第4步：验证成功，直接登录

技术实现流程：
1. 网站调用 navigator.credentials.get()
2. 浏览器显示可用的认证器
3. 用户选择认证器
4. 认证器验证用户身份
5. 认证器用私钥签名挑战
6. 网站用公钥验证签名
7. 验证成功则登录
```

**💻 登录代码示例**
```javascript
// 1. 创建登录选项
const getOptions = {
  challenge: new Uint8Array(32), // 新的随机挑战
  allowCredentials: [{
    type: "public-key",
    id: userCredentialId // 用户注册时的凭证ID
  }],
  timeout: 60000,
  userVerification: "required"
};

// 2. 调用WebAuthn API
const assertion = await navigator.credentials.get({
  publicKey: getOptions
});

// 3. 发送签名到服务器验证
const loginResult = await fetch('/login', {
  method: 'POST',
  body: JSON.stringify({
    credentialId: assertion.id,
    signature: assertion.response.signature,
    authenticatorData: assertion.response.authenticatorData,
    clientDataJSON: assertion.response.clientDataJSON
  })
});

if (loginResult.ok) {
  console.log('登录成功！');
}
```

### 4.6 WebAuthn的安全优势


**🛡️ 抗攻击能力**
```
防钓鱼攻击：
- 每个网站的密钥都不同
- 恶意网站无法使用其他网站的凭证
- 域名绑定确保安全

防中间人攻击：
- 签名包含当前网站信息
- 无法在其他网站重复使用
- TLS传输层保护

防重放攻击：
- 每次挑战都是随机的
- 签名结果每次都不同
- 时间戳验证
```

**🎯 用户体验优势**
```
便捷性：
✅ 无需记住密码
✅ 登录速度快（几秒钟）
✅ 支持多种认证方式

安全性：
✅ 生物特征难以伪造
✅ 私钥永不传输
✅ 设备本地验证

兼容性：
✅ 主流浏览器支持
✅ 移动设备支持
✅ 可与传统密码共存
```

---

## 5. ⚖️ 协议对比与选择


### 5.1 四种协议特点对比


| 协议 | **主要用途** | **安全性** | **复杂度** | **适用场景** |
|------|------------|----------|-----------|-------------|
| 🔑 **OAuth 2.0** | `授权访问资源` | `中等` | `中等` | `第三方应用授权` |
| 🆔 **OpenID Connect** | `身份认证+授权` | `高` | `中等` | `现代Web/移动应用登录` |
| 🏛️ **SAML** | `企业单点登录` | `高` | `高` | `企业内部系统集成` |
| 🔐 **WebAuthn** | `无密码认证` | `极高` | `中等` | `高安全要求的用户认证` |

### 5.2 技术架构对比


**📱 移动互联网时代选择**
```
消费级应用（C端）：
首选：OpenID Connect
- 用户体验好
- 开发简单
- 安全性足够

备选：OAuth 2.0
- 只需要授权功能
- 不需要用户身份信息
```

**🏢 企业级应用选择**
```
企业内部系统：
首选：SAML
- 成熟稳定
- 与企业AD集成好
- 支持复杂的权限管理

新建系统：
考虑：OpenID Connect
- 更现代的标准
- JSON格式易处理
- 移动端友好
```

**🔒 高安全场景选择**
```
金融、医疗等行业：
推荐：WebAuthn + OpenID Connect
- WebAuthn提供强身份认证
- OIDC提供授权和会话管理
- 双重保障

政府、军工等：
推荐：SAML + 硬件安全密钥
- 满足合规要求
- 高度安全
- 完整审计链
```

### 5.3 实际项目选择指南


**🎯 选择决策流程**
```
第1步：明确需求
- 只需要授权？ → OAuth 2.0
- 需要身份认证？ → 继续判断

第2步：确定用户类型
- 消费者用户？ → OpenID Connect
- 企业用户？ → 继续判断

第3步：评估安全要求
- 一般安全要求？ → SAML或OIDC
- 极高安全要求？ → WebAuthn + SAML/OIDC

第4步：考虑技术约束
- 纯Web应用？ → SAML或OIDC
- 移动应用？ → OIDC
- 物联网设备？ → 简化的OAuth 2.0
```

**💡 混合使用策略**
```
大型系统常见组合：

企业级平台：
SAML（企业SSO） + OIDC（对外API） + WebAuthn（高级用户）

互联网平台：
OIDC（主要登录） + OAuth 2.0（第三方接入） + WebAuthn（敏感操作）

移动应用：
OIDC（用户登录） + WebAuthn（支付验证）
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的基本概念


```
🔸 OAuth 2.0：授权框架，解决第三方应用访问资源问题
🔸 OpenID Connect：在OAuth 2.0基础上增加身份认证
🔸 SAML：企业级联合身份标准，主要用于SSO
🔸 WebAuthn：无密码认证标准，提供强身份验证
```

### 6.2 关键理解要点


**🔹 OAuth 2.0核心思想**
```
核心价值：授权而非认证
设计理念：用户不需要把密码告诉第三方
安全机制：临时令牌代替密码
适用场景：第三方应用集成
```

**🔹 OpenID Connect扩展价值**
```
解决问题：OAuth 2.0无法确认"用户是谁"
核心增加：ID令牌（包含用户身份信息）
标准化：统一的用户信息格式
现代选择：移动互联网时代的首选方案
```

**🔹 SAML企业特色**
```
设计背景：企业级身份管理需求
技术特点：基于XML，功能完整
主要价值：跨组织的身份信任
适用环境：企业内部系统集成
```

**🔹 WebAuthn未来趋势**
```
技术突破：无密码身份验证
安全升级：生物识别+公钥密码学
用户体验：更安全也更便捷
发展方向：未来身份认证的主流
```

### 6.3 实际应用指导


**🎯 项目实施建议**
```
新项目选择：
✅ 移动应用 → OpenID Connect
✅ 企业应用 → SAML或OIDC
✅ 高安全场景 → WebAuthn组合方案
✅ API开放平台 → OAuth 2.0

技术迁移策略：
✅ 渐进式升级，保持向后兼容
✅ 先实现核心功能，再优化体验
✅ 重视安全配置和最佳实践
✅ 建立完善的监控和审计机制
```

**🔒 安全实践要点**
```
通用安全原则：
✅ 使用HTTPS传输所有认证信息
✅ 验证所有令牌和断言的签名
✅ 设置合理的令牌有效期
✅ 实施完整的日志和监控

具体安全措施：
✅ OAuth 2.0：使用授权码模式，保护客户端密钥
✅ OIDC：验证ID令牌，实施nonce防重放
✅ SAML：严格验证断言来源和时效性
✅ WebAuthn：启用用户验证，使用可信认证器
```

**核心记忆**：
- 现代认证协议各有专长，需要根据场景选择
- OAuth 2.0解决授权，OIDC增加认证，SAML适合企业，WebAuthn面向未来
- 安全性和用户体验需要平衡，技术选择要考虑长远发展
- 实际项目中常常需要多种协议组合使用