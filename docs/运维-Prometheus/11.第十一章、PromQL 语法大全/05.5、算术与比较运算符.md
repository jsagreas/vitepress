---
title: 5、算术与比较运算符
---
## 📚 目录

1. [什么是PromQL运算符](#1-什么是promql运算符)
2. [算术运算符详解](#2-算术运算符详解)
3. [比较运算符应用](#3-比较运算符应用)
4. [逻辑运算符使用](#4-逻辑运算符使用)
5. [向量匹配机制](#5-向量匹配机制)
6. [运算符优先级与最佳实践](#6-运算符优先级与最佳实践)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🤔 什么是PromQL运算符


### 1.1 运算符的作用


💭 **想象一下**：如果你要监控服务器的CPU使用率，单纯看原始数据是不够的，你需要：
- 计算平均值、总和（算术运算）
- 判断是否超过阈值（比较运算）  
- 组合多个条件（逻辑运算）

**🏷️ PromQL运算符**：就是帮你对监控数据进行各种计算和判断的工具

```
简单理解：
原始数据：cpu_usage{server="web1"} = 0.8
运算后：cpu_usage > 0.7  (判断是否超过70%)
结果：true (触发告警)
```

### 1.2 运算符分类概览


```
PromQL运算符家族：
├── 算术运算符    ← 做数学计算（+、-、*、/、%、^）
├── 比较运算符    ← 做条件判断（>、<、==、!=）  
└── 逻辑运算符    ← 组合条件（and、or、unless）
```

🎯 **学习目标**：掌握这三类运算符，能够灵活组合使用进行复杂的监控查询

---

## 2. 🧮 算术运算符详解


### 2.1 基础算术操作


**📋 算术运算符清单**：

| 运算符 | **含义** | **示例** | **实际应用场景** |
|--------|----------|----------|------------------|
| `+` | 加法 | `cpu_usage + 10` | 数值偏移、求和 |
| `-` | 减法 | `memory_total - memory_used` | 计算剩余量 |
| `*` | 乘法 | `bytes_per_second * 8` | 单位转换 |
| `/` | 除法 | `used_memory / total_memory` | 计算比例 |
| `%` | 取模 | `time() % 3600` | 时间周期计算 |
| `^` | 幂运算 | `value ^ 2` | 平方、指数计算 |

### 2.2 实际应用举例


**🌰 计算内存使用率**：
```promql
# 基础公式：使用率 = 已使用内存 / 总内存 * 100
(node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes) / 
node_memory_MemTotal_bytes * 100
```

💡 **这里发生了什么**：
1. `node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes` ← 计算已使用内存
2. 除以总内存得到比例
3. 乘以100转换为百分比

**🌰 网络流量单位转换**：
```promql
# 从字节/秒转换为比特/秒
network_bytes_per_second * 8

# 从字节转换为MB
disk_usage_bytes / 1024 / 1024
```

### 2.3 向量与标量运算


🔍 **深入理解**：PromQL中的运算有两种情况

**标量运算**（简单情况）：
```promql
# 所有CPU使用率都加10%
cpu_usage + 10
# 结果：每个时间序列的值都增加10
```

**向量运算**（复杂情况）：
```promql
# 两个指标相减
metric_a - metric_b
# 注意：需要标签匹配才能运算！
```

---

## 3. ⚖️ 比较运算符应用


### 3.1 比较运算符类型


**📊 比较运算符功能表**：

```
条件判断工具箱：
==  相等      ← 精确匹配
!=  不等      ← 排除条件  
>   大于      ← 超过阈值
<   小于      ← 低于阈值
>=  大于等于  ← 达到标准
<=  小于等于  ← 不超标准
```

### 3.2 告警规则中的应用


**🚨 CPU使用率告警**：
```promql
# 查找CPU使用率超过80%的服务器
cpu_usage > 0.8

# 查找内存使用率超过90%的实例
memory_usage_ratio > 0.9
```

**✅ 健康检查**：
```promql
# 查找响应时间小于100ms的服务（表示正常）
http_request_duration_seconds < 0.1

# 查找错误率为0的服务
error_rate == 0
```

### 3.3 过滤与筛选


💡 **实际应用场景**：

**筛选高负载服务器**：
```promql
# 只显示负载超过阈值的机器
load_average_5m > 2.0
```

**识别异常实例**：
```promql
# 找出磁盘使用率不等于预期值的服务器
disk_usage_ratio != 0.8
```

---

## 4. 🧠 逻辑运算符使用


### 4.1 逻辑运算符概念


**🏷️ 逻辑运算符**：用来组合多个条件，就像日常语言中的"并且"、"或者"、"除了"

```
逻辑关系表达：
and     ← 两个条件都成立
or      ← 任一条件成立  
unless  ← 排除某种情况
```

### 4.2 AND运算应用


**🎯 组合条件监控**：
```promql
# 既要CPU高又要内存高的服务器
(cpu_usage > 0.8) and (memory_usage > 0.9)

# 既要响应时间长又要错误率高的服务
(response_time > 1) and (error_rate > 0.05)
```

💭 **理解要点**：`and`要求**两边的条件都满足**，结果只包含同时满足两个条件的时间序列

### 4.3 OR运算应用


**🔄 多选一条件**：
```promql
# CPU高或者内存高的服务器（任一条件满足即可）
(cpu_usage > 0.8) or (memory_usage > 0.9)

# 磁盘空间不足或IO异常的服务器
(disk_free < 1000000000) or (disk_io_wait > 10)
```

### 4.4 UNLESS运算应用


**❌ 排除特定情况**：
```promql
# 所有HTTP请求，但排除200状态码
http_requests_total unless (http_status == 200)

# 所有服务器，但排除维护中的
server_status unless server_status{status="maintenance"}
```

🤔 **换句话说**：`unless`就是"除了...之外"的意思

---

## 5. 🔗 向量匹配机制


### 5.1 为什么需要向量匹配


💭 **思考一下**：当你要计算`metric_a - metric_b`时，Prometheus怎么知道哪个`metric_a`要减去哪个`metric_b`？

```
问题场景：
metric_a{server="web1", env="prod"} = 100
metric_a{server="web2", env="test"} = 200
metric_b{server="web1", env="prod"} = 30
metric_b{server="web2", env="prod"} = 40

哪个减哪个？需要匹配规则！
```

**🏷️ 向量匹配**：Prometheus通过标签来匹配哪些时间序列可以进行运算

### 5.2 一对一匹配（默认）


**✅ 标签完全相同的才能运算**：
```promql
# 计算每台服务器的内存使用率
memory_used{server="web1"} / memory_total{server="web1"}
# 只有server标签相同的才会相除
```

**匹配规则图示**：
```
memory_used{server="web1", zone="a"}  ←→  memory_total{server="web1", zone="a"}  ✅ 匹配
memory_used{server="web2", zone="b"}  ←→  memory_total{server="web2", zone="b"}  ✅ 匹配
memory_used{server="web1", zone="a"}  ←→  memory_total{server="web2", zone="b"}  ❌ 不匹配
```

### 5.3 使用on指定匹配标签


**🎯 只关心特定标签**：
```promql
# 只根据server标签匹配，忽略其他标签
memory_used / on(server) memory_total

# 根据多个标签匹配
cpu_usage * on(server, zone) server_capacity
```

💡 **实际效果**：即使其他标签不同，只要指定的标签相同就能匹配

### 5.4 使用ignoring忽略标签


**🔧 忽略干扰标签**：
```promql
# 忽略instance标签的差异
rate(http_requests_total[5m]) / ignoring(instance) 
rate(http_requests_duration[5m])
```

🔄 **对比理解**：
- `on(labels)`：**只看**这些标签
- `ignoring(labels)`：**不看**这些标签

### 5.5 一对多和多对一匹配


**🏗️ 复杂匹配场景**：

**一对多匹配（group_left）**：
```promql
# 一个服务器配置信息匹配多个性能指标
server_info * on(server) group_left(cpu_cores, memory_gb) server_metrics
```

**多对一匹配（group_right）**：
```promql
# 多个服务实例匹配一个服务配置
instance_metrics * on(service) group_right service_config
```

💡 **什么时候用**：
- `group_left`：左边少，右边多（通常用于添加标签信息）
- `group_right`：左边多，右边少（较少使用）

---

## 6. ⚡ 运算符优先级与最佳实践


### 6.1 运算符优先级


**📊 优先级排序**（从高到低）：

```
优先级排序：
1. ^     ← 幂运算（最高）
2. *, /, %  ← 乘除模
3. +, -     ← 加减
4. ==, !=, <, <=, >, >=  ← 比较
5. and      ← 逻辑与
6. or, unless  ← 逻辑或（最低）
```

**🔧 使用括号确保顺序**：
```promql
# 不建议（可能产生歧义）
cpu_usage + memory_usage > 1.5

# 推荐（明确表达意图）
(cpu_usage + memory_usage) > 1.5
```

### 6.2 性能优化建议


**⚡ 提高查询效率**：

```promql
# ✅ 好的做法：先用标签过滤
rate(http_requests_total{status="200"}[5m]) > 100

# ❌ 避免：后用条件过滤（效率低）
rate(http_requests_total[5m]) and status == "200"
```

**📈 复杂查询的组织**：
```promql
# 使用变量分步计算（在Grafana中）
$cpu_high = cpu_usage > 0.8
$memory_high = memory_usage > 0.9
$cpu_high and $memory_high
```

### 6.3 常见错误避免


**🚨 标签匹配问题**：
```promql
# ❌ 错误：标签不匹配导致无结果
metric_a{env="prod"} / metric_b{env="test"}

# ✅ 正确：确保标签能够匹配
metric_a{env="prod"} / metric_b{env="prod"}
```

**💥 类型混用问题**：
```promql
# ❌ 错误：向量和标量运算顺序
5 > cpu_usage  # 可能不是你想要的

# ✅ 正确：明确表达意图
cpu_usage > 5
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 算术运算符：+、-、*、/、%、^ 用于数值计算
🔸 比较运算符：>、<、==、!= 用于条件判断
🔸 逻辑运算符：and、or、unless 用于组合条件
🔸 向量匹配：默认要求标签完全相同才能运算
🔸 匹配控制：on()指定标签，ignoring()忽略标签
🔸 多对一：group_left/group_right处理不对等匹配
```

### 7.2 实际应用要点


**🎯 运算符选择原则**：
```
数值计算 → 算术运算符
阈值判断 → 比较运算符
多条件组合 → 逻辑运算符
复杂匹配 → on/ignoring/group_*
```

**⚡ 性能优化记忆**：
- 先过滤再计算
- 使用括号明确优先级
- 标签匹配要精确

### 7.3 学习建议


**📚 循序渐进**：
1. 先掌握基础算术和比较运算
2. 再学习逻辑运算符的组合使用
3. 最后深入向量匹配的高级用法

**🛠️ 实践建议**：
- 在Prometheus Web UI中多练习
- 从简单查询开始，逐步增加复杂度
- 关注查询结果的标签变化

**📝 一句话总结**：PromQL运算符是处理监控数据的瑞士军刀，掌握了它们就能写出强大的监控查询语句

**🔑 记忆技巧**：
- **算术**：日常数学计算
- **比较**：设置告警阈值  
- **逻辑**：组合多个条件
- **匹配**：指定哪些数据可以运算

**核心理解**：运算符让你从被动看数据变为主动分析数据，是从监控新手到监控专家的重要一步！