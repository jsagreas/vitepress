---
title: 6、相关工具类
---
## 📚 目录

1. [Arrays工具类](#1-Arrays工具类)
2. [Collections工具类](#2-Collections工具类)
3. [Objects工具类](#3-Objects工具类)
4. [Properties工具类](#4-Properties工具类)
5. [核心要点总结](#5-核心要点总结)

---

## 1. 🔢 Arrays工具类


### 1.1 Arrays基本概念


**🔸 核心定义**
```
Arrays：Java提供的数组操作工具类，位于java.util包中
特点：提供静态方法操作数组，包括排序、查找、复制、填充等功能
设计理念：简化数组操作，提供高效的数组处理方法
```

### 1.2 排序方法


**📊 基本排序操作**
```java
// 基本类型数组排序
Arrays.sort(int[] a);                 // 升序排序整个数组
Arrays.sort(int[] a, int fromIndex, int toIndex);  // 排序指定范围

// 对象数组排序
Arrays.sort(Object[] a);              // 使用自然顺序
Arrays.sort(T[] a, Comparator<? super T> c);  // 使用比较器
```

**💡 排序算法特点**
```
基本类型数组：
- 使用双轴快速排序（Dual-Pivot Quicksort）
- 时间复杂度：平均O(n log n)，最坏O(n²)
- 不保证稳定性

对象数组：
- 使用改进的归并排序（TimSort）
- 时间复杂度：O(n log n)
- 保证稳定性
```

### 1.3 查找方法


**🔍 二分查找**
```java
// 二分查找（要求数组已排序）
int index = Arrays.binarySearch(int[] a, int key);          // 返回索引，未找到返回负值
int index = Arrays.binarySearch(T[] a, T key, Comparator<? super T> c);  // 使用比较器
```

**⚠️ 二分查找注意事项**
```
前提条件：
- 数组必须已经排序
- 未排序的数组结果不可预测

返回值规则：
- 找到：返回元素的索引位置
- 未找到：返回 -(insertion point) - 1
- insertion point：应该插入的位置
```

### 1.4 复制方法


**📋 数组复制操作**
```java
// 复制数组
int[] copy = Arrays.copyOf(int[] original, int newLength);  // 可指定新长度
// 复制指定范围
int[] range = Arrays.copyOfRange(int[] original, int from, int to);
```

**🔸 复制方法特点**
```
Arrays.copyOf()：
- 复制整个数组或指定长度
- 新长度可以大于或小于原数组
- 大于：多余位置填充默认值
- 小于：截断原数组

Arrays.copyOfRange()：
- 复制指定范围的元素
- 左闭右开区间 [from, to)
- 范围可以超出原数组长度
```

### 1.5 填充方法


**🎨 数组填充操作**
```java
// 填充整个数组
Arrays.fill(int[] a, int val);
// 填充指定范围
Arrays.fill(int[] a, int fromIndex, int toIndex, int val);
```

**💡 填充应用场景**
```
初始化数组：
- 快速将数组所有元素设为特定值
- 比循环赋值更高效

重置数组：
- 清空数组内容
- 准备重新使用
```

### 1.6 比较方法


**⚖️ 数组比较操作**
```java
// 比较两个数组是否相等
boolean equals = Arrays.equals(int[] a, int[] b);
// 深度比较（多维数组）
boolean deepEquals = Arrays.deepEquals(Object[] a1, Object[] a2);
```

**🔸 比较方法说明**
```
Arrays.equals()：
- 比较一维数组内容是否相等
- 逐个比较对应位置的元素
- 不适用于多维数组

Arrays.deepEquals()：
- 递归比较多维数组
- 可以处理嵌套数组结构
- 适用于复杂数组结构
```

### 1.7 转换方法


**🔄 数组转换操作**
```java
// 数组转列表（固定大小）
List<String> list = Arrays.asList("a", "b", "c");
// 数组转流（Java 8+）
IntStream stream = Arrays.stream(int[] array);
Stream<String> stream = Arrays.stream(String[] array);

// 数组转字符串
String str = Arrays.toString(int[] a);  // 一维数组
// 多维数组转字符串
String deepStr = Arrays.deepToString(Object[][] a);  // 多维数组
```

**⚠️ Arrays.asList()注意事项**
```
特殊限制：
- 返回的List是固定大小的
- 不能添加或删除元素
- 只能修改已有元素
- 对原数组的修改会影响List
```

---

## 2. 📚 Collections工具类


### 2.1 Collections基本概念


**🔸 核心定义**
```
Collections：Java提供的集合操作工具类，位于java.util包中
特点：提供静态方法操作Collection和List，包括排序、查找、同步等功能
设计理念：简化集合操作，提供线程安全包装和不可变视图
```

### 2.2 排序与反转


**📊 排序相关操作**
```java
List<Integer> list = new ArrayList<>(Arrays.asList(3, 1, 2));
Collections.sort(list);           // 升序排序：[1, 2, 3]
Collections.sort(list, Collections.reverseOrder()); // 降序排序：[3, 2, 1]

// 列表排序
Collections.sort(List<T> list);                              // 使用自然顺序
Collections.sort(List<T> list, Comparator<? super T> c);     // 使用比较器
Collections.shuffle(List<?> list);                           // 随机打乱顺序
Collections.reverse(List<?> list);                           // 反转顺序
Collections.rotate(List<?> list, int distance);              // 旋转元素
```

**🔸 排序方法特点**
```
Collections.sort()：
- 内部调用Arrays.sort()
- 保证稳定排序
- 时间复杂度O(n log n)

Collections.shuffle()：
- 随机打乱元素顺序
- 使用默认随机数生成器
- 可以指定Random对象

Collections.rotate()：
- 循环移动元素
- 正数向右移动，负数向左移动
- distance可以大于列表长度
```

### 2.3 查找与统计


**🔍 查找统计操作**
```java
List<String> list = Arrays.asList("A", "B", "A", "C");

int index = Collections.binarySearch(list, "B");  // 查找 B 的索引
int freq = Collections.frequency(list, "A");      // A 出现 2 次

String max = Collections.max(list);               // 返回最大元素
boolean disjoint = Collections.disjoint(list, Arrays.asList("X", "Y")); // true 判断两集合是否无交集
```

**💡 查找方法说明**
```
Collections.binarySearch()：
- 要求列表已排序
- 返回值规则同Arrays.binarySearch()
- 基于List.get()随机访问

Collections.frequency()：
- 统计指定元素出现次数
- 使用equals()方法比较
- 时间复杂度O(n)

Collections.disjoint()：
- 判断两个集合是否没有交集
- 有交集返回false，无交集返回true
- 优化：在较小集合上迭代
```

### 2.4 填充与替换


**🎨 集合内容操作**
```java
List<String> list = new ArrayList<>(Arrays.asList("A", "B", "C"));

Collections.fill(list, "X");                      // 所有元素变成 "X"

Collections.replaceAll(list, "X", "Y");           // 替换为 "Y"

List<String> dest = new ArrayList<>(Arrays.asList("", "", ""));
Collections.copy(dest, Arrays.asList("A", "B", "C")); // 复制元素
```

**⚠️ Collections.copy()注意事项**
```
使用限制：
- 目标列表大小必须 >= 源列表大小
- 不会自动扩展目标列表
- 只复制内容，不创建新列表
- 目标列表原有元素会被覆盖
```

### 2.5 最值操作


**📈 最值查找**
```java
T max = Collections.max(Collection<? extends T>);                             // 查找最大元素
T min = Collections.min(Collection<? extends T>);                             // 查找最小元素
T max = Collections.max(Collection<? extends T>, Comparator<? super T>);      // 使用比较器
```

**🔸 最值方法特点**
```
基本要求：
- 集合不能为空
- 元素必须实现Comparable或提供Comparator
- 所有元素必须可以相互比较

性能特点：
- 时间复杂度O(n)
- 一次遍历找到最值
- 对于已排序集合可以优化
```

### 2.6 同步包装（线程安全）


**🔒 线程安全集合**
```java
// 返回线程安全的集合
List<T> syncList = Collections.synchronizedList(new ArrayList<>());
Set<T> syncSet = Collections.synchronizedSet(new HashSet<>());
Map<K,V> syncMap = Collections.synchronizedMap(new HashMap<>());
```

**⚠️ 同步包装注意事项**
```
同步范围：
- 只对单个方法调用同步
- 迭代操作需要手动同步
- 不保证复合操作的原子性

正确用法：
synchronized(syncList) {
    Iterator<T> i = syncList.iterator();
    while (i.hasNext()) {
        // 处理元素
    }
}

性能考虑：
- 比并发集合（如ConcurrentHashMap）性能差
- 推荐使用java.util.concurrent包中的集合
```

### 2.7 不可变视图


**🛡️ 只读集合视图**
```java
// 返回不可修改的集合视图
List<T> unmodList = Collections.unmodifiableList(list);
Set<T> unmodSet = Collections.unmodifiableSet(set);
Map<K,V> unmodMap = Collections.unmodifiableMap(map);
```

**🔸 不可变视图特点**
```
安全保障：
- 防止意外修改集合内容
- 抛出UnsupportedOperationException
- 原集合修改仍会反映到视图

应用场景：
- 返回只读的集合给调用者
- 保护内部数据结构
- 实现防御性编程
```

### 2.8 创建特殊集合


**🏗️ 特殊集合创建**
```java
// 创建只有一个元素的集合
List<T> singletonList = Collections.singletonList(element);
Set<T> singleton = Collections.singleton(element);
Map<K,V> singletonMap = Collections.singletonMap(key, value);

// 返回不可变的空集合
List<T> emptyList = Collections.emptyList();
Set<T> emptySet = Collections.emptySet();
Map<K,V> emptyMap = Collections.emptyMap();

List<String> copies = Collections.nCopies(3, "Hi");        // [Hi, Hi, Hi]
```

**💡 特殊集合说明**
```
单例集合：
- 只包含一个元素
- 不可修改
- 内存高效，对象复用

空集合：
- 不包含任何元素
- 不可修改
- 避免返回null

重复集合：
- 包含n个相同元素的列表
- 不可修改
- 延迟创建，节省内存
```

---

## 3. 🔧 Objects工具类


### 3.1 Objects基本概念


**🔸 核心定义**
```
Objects：Java 7引入的对象操作工具类，位于java.util包中
特点：提供静态方法处理对象的null值判断、比较、哈希等操作
设计理念：简化对象操作，提供null安全的方法，避免NullPointerException
```

### 3.2 null值判断


**⚠️ 空值检查操作**
```java
// null值判断
boolean isNull = Objects.isNull(obj);        // 判断是否为null
boolean nonNull = Objects.nonNull(obj);      // 判断是否不为null

// null值检查（抛异常）
T result = Objects.requireNonNull(obj);      // obj为null时抛NullPointerException
T result = Objects.requireNonNull(obj, "参数不能为null");  // 自定义错误信息
T result = Objects.requireNonNull(obj, () -> "延迟计算的错误信息");  // 延迟计算错误信息
```

**💡 null检查应用场景**
```
参数验证：
- 构造方法参数检查
- public方法参数验证
- 关键业务逻辑入口校验

条件判断：
- 替代 obj == null 的写法
- 提高代码可读性
- 配合Stream API使用
```

### 3.3 对象比较


**⚖️ 安全比较操作**
```java
// 安全的equals比较（处理null）
boolean equals = Objects.equals(obj1, obj2);  // 两个都为null返回true，一个为null返回false

// 深度比较
boolean deepEquals = Objects.deepEquals(obj1, obj2);  // 递归比较数组和对象

// 比较操作
int result = Objects.compare(obj1, obj2, comparator);  // 使用comparator比较，处理null值
```

**🔸 比较方法特点**
```
Objects.equals()：
- null安全的equals比较
- 避免NullPointerException
- 相当于：(a == b) || (a != null && a.equals(b))

Objects.deepEquals()：
- 适用于数组和复杂对象
- 递归比较嵌套结构
- 比Arrays.deepEquals()更通用

Objects.compare()：
- 提供null值的比较策略
- 可以指定null值的排序位置
- 常与Comparator.nullsFirst()或nullsLast()配合
```

### 3.4 哈希值计算


**🔢 哈希码操作**
```java
// 单个对象哈希值（null安全）
int hash = Objects.hashCode(obj);          // obj为null返回0

// 多个对象组合哈希值
int hash = Objects.hash(obj1, obj2, obj3); // 相当于Arrays.hashCode(new Object[]{obj1, obj2, obj3})
```

**💡 哈希值计算说明**
```
Objects.hashCode()：
- null安全的hashCode()调用
- obj为null时返回0
- obj不为null时返回obj.hashCode()

Objects.hash()：
- 计算多个对象的组合哈希值
- 内部使用Arrays.hashCode()
- 常用于重写hashCode()方法
```

### 3.5 字符串转换


**🔤 字符串操作**
```java
// 安全的toString操作
String str = Objects.toString(obj);           // obj为null返回"null"
String str = Objects.toString(obj, "默认值"); // obj为null返回指定默认值
```

**🔸 toString方法特点**
```
Objects.toString(obj)：
- null安全的toString()调用
- obj为null时返回字符串"null"
- obj不为null时返回obj.toString()

Objects.toString(obj, nullDefault)：
- 提供null值的默认字符串
- 避免返回"null"字符串
- 适用于需要特定默认值的场景
```

### 3.6 典型应用示例


**🏗️ 实际使用示例**
```java
public class Person {
    private String name;
    private Integer age;
    private String email;

    public Person(String name, Integer age, String email) {
        // 参数验证
        this.name = Objects.requireNonNull(name, "姓名不能为null");
        this.age = Objects.requireNonNull(age, "年龄不能为null");
        this.email = email; // email可以为null
    }

    @Override
    public boolean equals(Object obj) {
        if (this == obj) return true;
        if (!(obj instanceof Person)) return false;
        Person person = (Person) obj;
        
        // 使用Objects.equals进行null安全比较
        return Objects.equals(name, person.name) &&
               Objects.equals(age, person.age) &&
               Objects.equals(email, person.email);
    }

    @Override
    public int hashCode() {
        // 使用Objects.hash计算组合哈希值
        return Objects.hash(name, age, email);
    }

    @Override
    public String toString() {
        return "Person{" +
                "name='" + name + '\'' +
                ", age=" + age +
                ", email='" + Objects.toString(email, "未设置") + '\'' +
                '}';
    }

    // 业务方法中的null检查
    public void updateEmail(String newEmail) {
        if (Objects.nonNull(newEmail)) {
            this.email = newEmail;
        }
    }
}
```

---

## 4. ⚙️ Properties工具类


### 4.1 Properties基本概念


**🔸 核心定义**
```
Properties：Java提供的配置文件处理类，继承自Hashtable<Object,Object>
特点：专门用于处理key=value格式的配置文件
设计理念：简化配置文件的读取、写入和管理操作
位置：java.util.Properties
```

**💡 Properties文件格式**
```
# 这是注释行
! 这也是注释行
username=admin
password=123456
database.host=localhost
database.port=3306

# 支持多行值（使用反斜杠）
long.message=这是一个很长的消息，\
             可以分多行书写

# 支持Unicode转义
chinese.name=\u4e2d\u6587\u540d\u79f0
```

### 4.2 创建和基本操作


**🏗️ Properties创建方式**
```java
// 创建空的Properties对象
Properties props = new Properties();

// 设置属性
props.setProperty("username", "admin");
props.setProperty("password", "123456");
props.setProperty("timeout", "30");

// 获取属性
String username = props.getProperty("username");           // 返回 "admin"
String timeout = props.getProperty("timeout", "60");      // 不存在时返回默认值 "60"
String unknown = props.getProperty("unknown");            // 返回 null

// 获取所有属性名
Set<String> propertyNames = props.stringPropertyNames();  // 返回所有key的集合
```

**⚠️ Properties vs Map的区别**
```
Properties特点：
- 专门处理String类型的键值对
- 提供配置文件的读写方法
- 支持默认值和属性继承
- 线程安全（继承自Hashtable）

Map使用限制：
- 虽然可以存储Object，但推荐只存储String
- getProperty()只能获取String值
- setProperty()只能设置String键值对
```

### 4.3 文件读取操作


**📖 从文件加载Properties**
```java
// 方法1：从文件路径加载
Properties props = new Properties();
try (FileInputStream fis = new FileInputStream("config.properties")) {
    props.load(fis);  // 使用系统默认编码
} catch (IOException e) {
    e.printStackTrace();
}

// 方法2：从类路径加载
Properties props = new Properties();
try (InputStream is = MyClass.class.getResourceAsStream("/config.properties")) {
    props.load(is);
} catch (IOException e) {
    e.printStackTrace();
}

// 方法3：从Reader加载（推荐，可指定编码）
Properties props = new Properties();
try (InputStreamReader reader = new InputStreamReader(
        new FileInputStream("config.properties"), "UTF-8")) {
    props.load(reader);
} catch (IOException e) {
    e.printStackTrace();
}

// 方法4：加载XML格式的properties
Properties props = new Properties();
try (FileInputStream fis = new FileInputStream("config.xml")) {
    props.loadFromXML(fis);
} catch (IOException e) {
    e.printStackTrace();
}
```

### 4.4 文件写入操作


**💾 保存Properties到文件**
```java
Properties props = new Properties();
props.setProperty("app.name", "MyApplication");
props.setProperty("app.version", "1.0.0");
props.setProperty("app.author", "开发者");

// 方法1：保存为properties格式
try (FileOutputStream fos = new FileOutputStream("output.properties")) {
    props.store(fos, "应用配置文件");  // 第二个参数是注释
} catch (IOException e) {
    e.printStackTrace();
}

// 方法2：使用Writer保存（推荐，可指定编码）
try (OutputStreamWriter writer = new OutputStreamWriter(
        new FileOutputStream("output.properties"), "UTF-8")) {
    props.store(writer, "UTF-8编码的配置文件");
} catch (IOException e) {
    e.printStackTrace();
}

// 方法3：保存为XML格式
try (FileOutputStream fos = new FileOutputStream("output.xml")) {
    props.storeToXML(fos, "XML格式的配置文件", "UTF-8");
} catch (IOException e) {
    e.printStackTrace();
}
```

### 4.5 高级特性


**🔗 属性继承（默认Properties）**
```java
// 创建默认属性
Properties defaultProps = new Properties();
defaultProps.setProperty("timeout", "30");
defaultProps.setProperty("retries", "3");
defaultProps.setProperty("debug", "false");

// 创建继承默认属性的Properties
Properties props = new Properties(defaultProps);
props.setProperty("username", "admin");  // 新增属性
props.setProperty("timeout", "60");      // 覆盖默认属性

// 获取属性时会自动查找继承链
String timeout = props.getProperty("timeout");   // 返回 "60"（覆盖的值）
String retries = props.getProperty("retries");   // 返回 "3"（继承的值）
String debug = props.getProperty("debug");       // 返回 "false"（继承的值）
```

**📝 属性列表和遍历**
```java
Properties props = new Properties();
props.setProperty("db.host", "localhost");
props.setProperty("db.port", "3306");
props.setProperty("db.name", "mydb");

// 方法1：使用stringPropertyNames()遍历
for (String key : props.stringPropertyNames()) {
    String value = props.getProperty(key);
    System.out.println(key + " = " + value);
}

// 方法2：使用forEach遍历（Java 8+）
props.forEach((key, value) -> {
    System.out.println(key + " = " + value);
});

// 方法3：打印所有属性到控制台（调试用）
props.list(System.out);

// 方法4：打印所有属性到PrintStream
try (PrintStream ps = new PrintStream("props.txt", "UTF-8")) {
    props.list(ps);
} catch (IOException e) {
    e.printStackTrace();
}
```

### 4.6 实际应用示例


**🏗️ 配置管理工具类**
```java
public class ConfigManager {
    private Properties properties;
    private String configFile;
    
    public ConfigManager(String configFile) {
        this.configFile = configFile;
        this.properties = new Properties();
        loadConfig();
    }
    
    /**
     * 加载配置文件
     */
    private void loadConfig() {
        try (InputStream is = getClass().getResourceAsStream("/" + configFile)) {
            if (is != null) {
                properties.load(is);
                System.out.println("配置文件加载成功: " + configFile);
            } else {
                System.err.println("配置文件不存在: " + configFile);
            }
        } catch (IOException e) {
            System.err.println("加载配置文件失败: " + e.getMessage());
        }
    }
    
    /**
     * 获取字符串配置
     */
    public String getString(String key) {
        return properties.getProperty(key);
    }
    
    /**
     * 获取字符串配置（带默认值）
     */
    public String getString(String key, String defaultValue) {
        return properties.getProperty(key, defaultValue);
    }
    
    /**
     * 获取整数配置
     */
    public int getInt(String key, int defaultValue) {
        String value = properties.getProperty(key);
        if (value != null) {
            try {
                return Integer.parseInt(value.trim());
            } catch (NumberFormatException e) {
                System.err.println("配置项 " + key + " 不是有效的整数: " + value);
            }
        }
        return defaultValue;
    }
    
    /**
     * 获取布尔配置
     */
    public boolean getBoolean(String key, boolean defaultValue) {
        String value = properties.getProperty(key);
        if (value != null) {
            return Boolean.parseBoolean(value.trim());
        }
        return defaultValue;
    }
    
    /**
     * 设置配置项
     */
    public void setProperty(String key, String value) {
        properties.setProperty(key, value);
    }
    
    /**
     * 保存配置到文件
     */
    public void saveConfig() {
        try (OutputStreamWriter writer = new OutputStreamWriter(
                new FileOutputStream(configFile), "UTF-8")) {
            properties.store(writer, "Updated at " + new Date());
            System.out.println("配置文件保存成功");
        } catch (IOException e) {
            System.err.println("保存配置文件失败: " + e.getMessage());
        }
    }
    
    /**
     * 重新加载配置
     */
    public void reload() {
        properties.clear();
        loadConfig();
    }
    
    /**
     * 获取所有配置项
     */
    public Set<String> getAllKeys() {
        return properties.stringPropertyNames();
    }
}

// 使用示例
public class ConfigExample {
    public static void main(String[] args) {
        ConfigManager config = new ConfigManager("app.properties");
        
        // 读取配置
        String appName = config.getString("app.name", "默认应用");
        int port = config.getInt("server.port", 8080);
        boolean debug = config.getBoolean("debug.enabled", false);
        
        System.out.println("应用名称: " + appName);
        System.out.println("服务端口: " + port);
        System.out.println("调试模式: " + debug);
        
        // 修改配置并保存
        config.setProperty("last.access", String.valueOf(System.currentTimeMillis()));
        config.saveConfig();
    }
}
```

### 4.7 数据库配置示例


**🗄️ 数据库连接配置管理**
```java
public class DatabaseConfig {
    private Properties dbProps;
    
    public DatabaseConfig() {
        dbProps = new Properties();
        loadDatabaseConfig();
    }
    
    private void loadDatabaseConfig() {
        try (InputStream is = getClass().getResourceAsStream("/db.properties")) {
            dbProps.load(is);
        } catch (IOException e) {
            throw new RuntimeException("无法加载数据库配置文件", e);
        }
    }
    
    public String getJdbcUrl() {
        String host = dbProps.getProperty("db.host", "localhost");
        String port = dbProps.getProperty("db.port", "3306");
        String database = dbProps.getProperty("db.name", "test");
        return String.format("jdbc:mysql://%s:%s/%s", host, port, database);
    }
    
    public String getUsername() {
        return dbProps.getProperty("db.username");
    }
    
    public String getPassword() {
        return dbProps.getProperty("db.password");
    }
    
    public Properties getConnectionProperties() {
        Properties connProps = new Properties();
        connProps.setProperty("user", getUsername());
        connProps.setProperty("password", getPassword());
        connProps.setProperty("useUnicode", 
            dbProps.getProperty("db.useUnicode", "true"));
        connProps.setProperty("characterEncoding", 
            dbProps.getProperty("db.characterEncoding", "UTF-8"));
        return connProps;
    }
}

// db.properties 文件内容：
/*
# 数据库连接配置
db.host=localhost
db.port=3306
db.name=myapp
db.username=root
db.password=password123

# 连接参数
db.useUnicode=true
db.characterEncoding=UTF-8
db.autoReconnect=true
db.maxReconnects=3
*/
```

### 4.8 系统属性操作


**⚙️ 系统属性管理**
```java
// 获取系统Properties
Properties sysProps = System.getProperties();

// 常用系统属性
String javaVersion = System.getProperty("java.version");      // Java版本
String osName = System.getProperty("os.name");               // 操作系统名称
String userHome = System.getProperty("user.home");           // 用户主目录
String userDir = System.getProperty("user.dir");             // 当前工作目录
String fileSeparator = System.getProperty("file.separator"); // 文件分隔符
String lineSeparator = System.getProperty("line.separator"); // 行分隔符

// 设置系统属性
System.setProperty("my.custom.property", "自定义值");

// 从命令行参数设置系统属性
// java -Dapp.env=production -Dlog.level=INFO MyApp

// 在代码中获取命令行设置的属性
String environment = System.getProperty("app.env", "development");
String logLevel = System.getProperty("log.level", "DEBUG");

System.out.println("运行环境: " + environment);
System.out.println("日志级别: " + logLevel);
```

### 4.9 Properties的注意事项


**⚠️ 使用注意事项**
```java
编码问题：
❌ 直接使用FileInputStream可能导致中文乱码
✅ 使用InputStreamReader指定UTF-8编码
✅ 或者使用Properties.loadFromXML()处理XML格式

线程安全：
✅ Properties继承自Hashtable，是线程安全的
⚠️ 但并发修改时仍需要额外同步
⚠️ 推荐使用ConcurrentHashMap替代

性能考虑：
✅ Properties适合配置数据，不适合大量数据
✅ 频繁读取的配置可以缓存到变量中
❌ 不要在循环中重复加载配置文件

键值对格式：
✅ 键名推荐使用点分隔命名：app.database.host
✅ 值中的特殊字符需要转义：\n, \r, \t, \\, \=, \:
⚠️ 空格在键名中有特殊含义，建议避免使用
```

**🛠️ 最佳实践**
```java
配置文件组织：
✅ 按环境分离：app-dev.properties, app-prod.properties
✅ 按模块分离：database.properties, redis.properties
✅ 使用层级命名：logging.level.root, logging.level.com.myapp

错误处理：
✅ 提供合理的默认值
✅ 记录配置加载失败的日志
✅ 验证关键配置项的有效性

配置更新：
✅ 提供配置重载机制
✅ 监听配置文件变化
✅ 热更新非关键配置项
```

---

## 5. 📋 核心要点总结


### 5.1 必须掌握的核心概念


```
🔸 Arrays工具类：专门操作数组的静态方法集合，包括排序、查找、复制、填充
🔸 Collections工具类：专门操作集合的静态方法集合，包括排序、同步、不可变视图
🔸 Objects工具类：处理对象的null安全操作，包括判断、比较、哈希、字符串转换
🔸 Properties工具类：专门处理配置文件的类，支持key=value格式和XML格式
🔸 线程安全：Collections提供同步包装器，Properties继承自Hashtable
🔸 不可变视图：提供只读访问，防止意外修改
🔸 算法复杂度：了解各种操作的时间复杂度和适用场景
```

### 5.2 关键理解要点


**🔹 四大工具类的区别**
```
Arrays：
- 专门处理数组（Array）
- 方法参数和返回值都是数组
- 提供基本类型和对象数组的处理
- 不涉及Collection接口

Collections：
- 专门处理集合（Collection）
- 方法参数和返回值都是集合接口
- 提供List、Set、Map的操作
- 基于Collection框架设计

Objects：
- 专门处理对象的通用操作
- 提供null安全的方法
- 简化equals、hashCode、toString的实现
- 避免NullPointerException

Properties：
- 专门处理配置文件
- 继承自Hashtable<Object,Object>
- 支持文件加载和保存
- 提供类型转换和默认值
```

**🔹 性能考虑**
```
排序性能：
- Arrays.sort()：直接操作数组，性能最高
- Collections.sort()：内部转换为数组再排序
- List.sort()：Java 8+推荐，语法简洁

线程安全性能：
- Collections同步包装：性能较差，简单粗暴
- 并发集合类：性能优秀，推荐使用
- 如：ConcurrentHashMap > synchronizedMap

查找性能：
- 二分查找：O(log n)，要求已排序
- 线性查找：O(n)，适用于未排序
- 频率统计：O(n)，需要遍历整个集合
```

**🔹 使用场景选择**
```
数组操作首选Arrays：
- 基本类型数组处理
- 高性能排序需求
- 数组与其他类型转换

集合操作首选Collections：
- 复杂集合操作
- 需要线程安全包装
- 创建特殊集合（空集合、单例集合）

对象操作首选Objects：
- null安全的对象操作
- 实现equals、hashCode、toString
- 参数验证和null检查

配置管理首选Properties：
- 应用配置文件处理
- 系统属性管理
- 键值对数据存储

现代Java推荐：
- Stream API：函数式处理
- 并发集合：高并发场景
- Java 9+ 集合工厂方法：List.of(), Set.of(), Map.of()
```

### 5.3 实际应用指导


**💡 最佳实践建议**
```
选择合适的工具：
✅ 数组操作用Arrays
✅ 集合操作用Collections
✅ 对象操作用Objects
✅ 配置管理用Properties
✅ 高并发用并发集合
✅ 函数式处理用Stream

避免常见陷阱：
❌ Arrays.asList()返回固定大小List
❌ 忘记二分查找需要排序
❌ 同步包装器的迭代需要手动同动
❌ 不可变视图不等于不可变对象
❌ Properties文件编码问题
❌ 配置文件路径错误
❌ 忘记提供默认值

性能优化技巧：
✅ 对大数组使用Arrays.sort()
✅ 频繁查找用二分查找
✅ 批量操作比单个操作效率高
✅ 预估集合大小，避免频繁扩容
✅ 使用Objects.hash()简化hashCode实现
✅ Properties使用UTF-8编码
✅ 配置文件提供合理默认值
```

**⚠️ 常见错误和解决方案**
```
Arrays.asList()误用：
❌ List<String> list = Arrays.asList("a", "b");
   list.add("c"); // UnsupportedOperationException
✅ List<String> list = new ArrayList<>(Arrays.asList("a", "b"));
   list.add("c"); // 正确

同步包装器误用：
❌ List<String> syncList = Collections.synchronizedList(list);
   for(String s : syncList) { ... } // 不安全
✅ synchronized(syncList) {
     for(String s : syncList) { ... } // 安全
   }

二分查找误用：
❌ List<Integer> list = Arrays.asList(3, 1, 2);
   Collections.binarySearch(list, 2); // 错误，未排序
✅ Collections.sort(list);
   Collections.binarySearch(list, 2); // 正确

null值处理误用：
❌ if (obj != null && obj.equals(other)) { ... }  // 冗长且容易出错
✅ if (Objects.equals(obj, other)) { ... }        // 简洁且安全

hashCode实现误用：
❌ @Override
   public int hashCode() {
       return name.hashCode() + age.hashCode(); // 可能NullPointerException
   }
✅ @Override
   public int hashCode() {
       return Objects.hash(name, age); // null安全且标准
   }

Properties使用误用：
❌ Properties props = new Properties();
   FileInputStream fis = new FileInputStream("config.properties");
   props.load(fis); // 使用系统默认编码，可能乱码
✅ Properties props = new Properties();
   InputStreamReader reader = new InputStreamReader(
       new FileInputStream("config.properties"), "UTF-8");
   props.load(reader); // 指定UTF-8编码

配置默认值误用：
❌ String host = props.getProperty("db.host");
   if (host == null) host = "localhost"; // 冗长的null检查
✅ String host = props.getProperty("db.host", "localhost"); // 简洁的默认值
```

### 5.4 工具类方法分类总结


```
Arrays核心方法分类：
📊 排序：sort()
🔍 查找：binarySearch()
📋 复制：copyOf(), copyOfRange()
🎨 填充：fill()
⚖️ 比较：equals(), deepEquals()
🔄 转换：asList(), toString(), stream()

Collections核心方法分类：
📊 排序：sort(), reverse(), shuffle(), rotate()
🔍 查找：binarySearch(), max(), min(), frequency()
🎨 操作：fill(), replaceAll(), copy()
🔒 安全：synchronizedXxx(), unmodifiableXxx()
🏗️ 创建：emptyXxx(), singletonXxx(), nCopies()

Objects核心方法分类：
⚠️ 判断：isNull(), nonNull(), requireNonNull()
⚖️ 比较：equals(), deepEquals(), compare()
🔢 哈希：hashCode(), hash()
🔤 转换：toString()

Properties核心方法分类：
📖 加载：load(), loadFromXML()
💾 保存：store(), storeToXML()
🔍 获取：getProperty(), stringPropertyNames()
✏️ 设置：setProperty()
```

### 5.5 现代Java替代方案


```
Java 8+ Stream API：
✅ 更强大的数据处理能力
✅ 函数式编程风格
✅ 并行处理支持
✅ 链式操作更简洁

Java 9+ 集合工厂：
✅ List.of(), Set.of(), Map.of()
✅ 创建不可变集合更简单
✅ 性能更好，内存更少

Java 14+ Records：
✅ 自动生成equals、hashCode、toString
✅ 减少Objects工具类的使用
✅ 更简洁的数据类定义

Java 21+ 模式匹配：
✅ switch表达式增强
✅ instanceof模式匹配
✅ 更安全的类型处理

现代配置管理：
✅ Spring Boot的application.properties
✅ YAML格式配置文件
✅ 环境变量和系统属性集成
✅ 配置热更新和监听
```