---
title: 1、File类
---
## 📚 目录

1. [File类概述](#1-File类概述)
2. [File类构造方法](#2-File类构造方法)
3. [File类判断功能方法](#3-File类判断功能方法)
4. [File类获取功能方法](#4-File类获取功能方法)
5. [File类创建与删除操作](#5-File类创建与删除操作)
6. [File类重命名与移动](#6-File类重命名与移动)
7. [File类目录操作](#7-File类目录操作)
8. [文件路径操作辅助方法](#8-文件路径操作辅助方法)
9. [Properties类详解](#9-Properties类详解)
10. [ResourceBundle类详解](#10-ResourceBundle类详解)
11. [RandomAccessFile类详解](#11-RandomAccessFile类详解)
12. [核心要点总结](#12-核心要点总结)

---

## 1. 📁 File类概述


### 1.1 File类基本概念


**🔸 核心定义**
```
File类（java.io.File）：代表文件或目录的路径名的抽象表示
关键理解：File对象本身不表示文件/目录是否真实存在，只是路径的封装
实际作用：路径操作、文件元数据获取、目录遍历等
```

> **⚠️ 重要提醒**：构造方法本质上不会创建文件或目录，需显式调用相关方法

### 1.2 File类在IO体系中的位置


```
Java IO 体系结构：
┌─────────────────────────────────┐
│        File 类（路径抽象）           │ ← 文件路径操作
├─────────────────────────────────┤
│    字节流（InputStream/OutputStream） │ ← 二进制数据读写
├─────────────────────────────────┤
│      字符流（Reader/Writer）        │ ← 文本数据读写
├─────────────────────────────────┤
│         缓冲流（Buffered）          │ ← 高效IO操作
└─────────────────────────────────┘
```

---

## 2. 🔧 File类构造方法


### 2.1 构造方法详解


| 构造方法 | **说明** | **使用场景** |
|---------|----------|-------------|
| `File(String pathname)` | `根据路径字符串构建` | `简单路径创建` |
| `File(String parent, String child)` | `父路径 + 子路径` | `路径拼接` |
| `File(File parent, String child)` | `父对象 + 子路径` | `基于已有File对象扩展` |

### 2.2 构造方法使用示例


```java
// 方式1：直接路径字符串
File file1 = new File("test.txt");
File file2 = new File("C:\\Users\\data\\test.txt");

// 方式2：父路径字符串 + 子路径
File file3 = new File("C:\\Users", "test.txt");

// 方式3：父File对象 + 子路径  
File parentDir = new File("C:\\Users");
File file4 = new File(parentDir, "test.txt");
```

**💡 路径分隔符处理**
```java
// 跨平台路径分隔符
String separator = File.separator;  // Windows: \  Linux/Mac: /
File file = new File("data" + File.separator + "config.txt");
```

---

## 3. 🔍 File类判断功能方法


### 3.1 存在性与类型判断


| 方法名 | **功能描述** | **返回值** |
|--------|-------------|-----------|
| `boolean exists()` | `是否存在` | `true/false` |
| `boolean isFile()` | `是否是文件` | `文件返回true` |
| `boolean isDirectory()` | `是否是目录` | `目录返回true` |

### 3.2 权限与属性判断


| 方法名 | **功能描述** | **使用场景** |
|--------|-------------|-------------|
| `boolean canRead()` | `是否可读` | `权限检查` |
| `boolean canWrite()` | `是否可写` | `写入前检查` |
| `boolean canExecute()` | `是否可执行（Java 6+）` | `脚本文件检查` |
| `boolean isHidden()` | `是否是隐藏文件` | `文件过滤` |

### 3.3 判断方法使用示例


```java
File file = new File("config.txt");

// 基本判断
if (file.exists()) {
    if (file.isFile()) {
        System.out.println("这是一个文件");
    } else if (file.isDirectory()) {
        System.out.println("这是一个目录");
    }
    
    // 权限检查
    System.out.println("可读：" + file.canRead());
    System.out.println("可写：" + file.canWrite());
    System.out.println("可执行：" + file.canExecute());
}
```

---

## 4. 📊 File类获取功能方法


### 4.1 路径信息获取


| 方法名 | **功能描述** | **示例返回值** |
|--------|-------------|---------------|
| `String getAbsolutePath()` | `获取绝对路径` | `C:\Users\data\test.txt` |
| `String getPath()` | `获取构造时传入的路径` | `data\test.txt` |
| `String getName()` | `获取名称（文件名或目录名）` | `test.txt` |

### 4.2 父路径与文件信息


| 方法名 | **功能描述** | **注意事项** |
|--------|-------------|-------------|
| `String getParent()` | `获取父路径字符串` | `可能返回null` |
| `File getParentFile()` | `获取父路径的File对象` | `可能返回null` |
| `long length()` | `获取文件大小（字节）` | `目录返回0` |
| `long lastModified()` | `获取最后修改时间（毫秒时间戳）` | `可配合Date使用` |

### 4.3 获取方法使用示例


```java
File file = new File("C:\\Users\\data\\config.txt");

System.out.println("绝对路径：" + file.getAbsolutePath());
System.out.println("相对路径：" + file.getPath());
System.out.println("文件名：" + file.getName());
System.out.println("父目录：" + file.getParent());

if (file.exists() && file.isFile()) {
    System.out.println("文件大小：" + file.length() + " 字节");
    
    // 时间戳转换
    long lastModified = file.lastModified();
    Date date = new Date(lastModified);
    System.out.println("最后修改时间：" + date);
}
```

---

## 5. ⚡ File类创建与删除操作


### 5.1 创建操作方法


| 方法名 | **功能描述** | **注意事项** |
|--------|-------------|-------------|
| `boolean createNewFile()` | `创建新文件（文件不存在时）` | `需要父目录存在` |
| `boolean mkdir()` | `创建单层目录` | `只能创建一级目录` |
| `boolean mkdirs()` | `创建多层目录` | `可创建多级目录` |

### 5.2 删除操作方法


| 方法名 | **功能描述** | **限制** |
|--------|-------------|----------|
| `boolean delete()` | `删除文件或目录` | `不能删除非空目录` |

> **📌 重要提示**：创建文件时若路径不存在，会失败；删除非空目录需递归处理。

### 5.3 创建与删除示例


```java
// 创建文件
File file = new File("data/config.txt");
try {
    // 先创建父目录
    File parentDir = file.getParentFile();
    if (!parentDir.exists()) {
        parentDir.mkdirs();  // 创建多级目录
    }
    
    // 创建文件
    if (file.createNewFile()) {
        System.out.println("文件创建成功");
    }
} catch (IOException e) {
    e.printStackTrace();
}

// 删除操作
if (file.exists()) {
    if (file.delete()) {
        System.out.println("删除成功");
    }
}
```

---

## 6. 🔄 File类重命名与移动


### 6.1 renameTo方法


**🔸 方法签名**
```java
boolean renameTo(File dest)
```

**💡 功能说明**
- 可以用于**重命名**文件/目录
- 也可实现**移动**操作
- **注意**：不覆盖目标文件，操作失败返回false

### 6.2 使用示例


```java
// 重命名文件
File oldFile = new File("old_name.txt");
File newFile = new File("new_name.txt");

if (oldFile.renameTo(newFile)) {
    System.out.println("重命名成功");
} else {
    System.out.println("重命名失败");
}

// 移动文件到其他目录
File sourceFile = new File("source/file.txt");
File targetFile = new File("target/file.txt");

if (sourceFile.renameTo(targetFile)) {
    System.out.println("移动成功");
}
```

---

## 7. 📂 File类目录操作


### 7.1 目录遍历方法


| 方法名 | **功能描述** | **返回类型** |
|--------|-------------|-------------|
| `String[] list()` | `返回当前目录下的子文件/目录名数组` | `String[]` |
| `File[] listFiles()` | `返回子文件/目录的File对象数组` | `File[]` |
| `File[] listFiles(FilenameFilter filter)` | `带文件名过滤器地列出文件` | `File[]` |
| `File[] listFiles(FileFilter filter)` | `自定义过滤器列出文件` | `File[]` |

### 7.2 目录遍历示例


```java
File directory = new File("data");

if (directory.exists() && directory.isDirectory()) {
    // 获取所有子项名称
    String[] names = directory.list();
    for (String name : names) {
        System.out.println("文件名：" + name);
    }
    
    // 获取所有子项File对象
    File[] files = directory.listFiles();
    for (File file : files) {
        if (file.isFile()) {
            System.out.println("文件：" + file.getName());
        } else if (file.isDirectory()) {
            System.out.println("目录：" + file.getName());
        }
    }
    
    // 使用过滤器（只显示.txt文件）
    File[] txtFiles = directory.listFiles(new FilenameFilter() {
        @Override
        public boolean accept(File dir, String name) {
            return name.endsWith(".txt");
        }
    });
}
```

---

## 8. 🛠️ 文件路径操作辅助方法


### 8.1 Java NIO.2 路径操作（推荐）


| 方法 | **作用** | **示例** |
|------|---------|----------|
| `Paths.get(...)` | `创建路径对象` | `Path path = Paths.get("file.txt");` |
| `Files.exists(...)` | `判断文件或目录是否存在` | `boolean exists = Files.exists(path);` |
| `Files.copy(...)` | `拷贝文件` | `Files.copy(sourcePath, targetPath);` |
| `Files.move(...)` | `移动或重命名文件` | `Files.move(sourcePath, targetPath);` |
| `Files.delete(...)` | `删除文件或目录` | `Files.delete(path);` |

### 8.2 NIO.2 vs 传统File类对比


```
传统File类                    Java NIO.2 (推荐)
┌─────────────────┐         ┌─────────────────┐
│ File file = ... │   VS    │ Path path = ... │
│ file.exists()   │         │ Files.exists()  │
│ file.delete()   │         │ Files.delete()  │
│ 操作复杂        │         │ 操作简洁        │
└─────────────────┘         └─────────────────┘
```

---

## 9. 🔧 Properties类详解


### 9.1 Properties类概述


**🔸 核心定义**
```
Properties类：Java标准库中的配置文件管理工具类
继承关系：继承自Hashtable<Object, Object>
专门用途：处理String类型的键值对配置数据
线程安全：是（因继承自Hashtable）
```

**🏗️ 继承关系图**
```
java.lang.Object
  ↳ java.util.Dictionary<K,V>
    ↳ java.util.Hashtable<Object,Object>
      ↳ java.util.Properties
```

### 9.2 Properties主要特点


```
🔸 专门用于读写配置文件(通常是.properties文件)
🔸 键和值都是字符串类型
🔸 支持默认值机制
🔸 提供多种存储和加载方式(包括基于流、文件、XML等)
🔸 线程安全(因继承自Hashtable)
```

### 9.3 典型应用场景


- **配置文件管理**：数据库连接、系统设置
- **国际化资源**：配合ResourceBundle使用
- **环境变量配置**：系统属性管理
- **用户偏好设置**：应用程序配置存储

### 9.4 常用构造方法


```java
// 创建空的Properties对象
public Properties()

// 创建带默认值的Properties对象
Properties defaults = new Properties();
Properties properties = new Properties(defaults);
```

### 9.5 常用方法详解


| 方法 | **说明** | **使用场景** |
|------|----------|-------------|
| `setProperty(String key, String value)` | `设置属性值` | `配置写入` |
| `getProperty(String key)` | `获取指定key的值` | `配置读取` |
| `getProperty(String key, String defaultValue)` | `获取值，若不存在返回默认值` | `安全读取` |
| `load(InputStream inStream)` | `从输入流读取.properties文件` | `文件加载` |
| `load(Reader reader)` | `从字符输入流读取（推荐用于处理中文）` | `中文配置` |
| `store(OutputStream out, String comments)` | `将属性写入输出流` | `配置保存` |
| `store(Writer writer, String comments)` | `将属性写入字符流` | `配置保存` |
| `list(PrintStream out)` | `以可读方式输出全部键值对` | `调试查看` |
| `stringPropertyNames()` | `获取所有key的Set<String>` | `遍历所有键` |

### 9.6 基本操作示例


```java
// 创建与基本操作
Properties properties = new Properties();

// 设置属性
properties.setProperty("database.url", "jdbc:mysql://localhost:3306/mydb");
properties.setProperty("database.user", "admin");

// 获取属性
String url = properties.getProperty("database.url");
String password = properties.getProperty("database.password");  // 不存在，返回null
String timeout = properties.getProperty("timeout", "60");  // 使用默认值

// 检查属性
boolean hasUser = properties.containsKey("database.user");

// 移除属性
properties.remove("database.user");

// 清空所有属性
properties.clear();
```

### 9.7 文件读写操作


```java
// 读取配置文件
Properties props = new Properties();
try (FileReader reader = new FileReader("config.properties")) {
    props.load(reader);
    String url = props.getProperty("db.url");
    System.out.println("数据库地址：" + url);
} catch (IOException e) {
    e.printStackTrace();
}

// 写入配置文件
Properties props = new Properties();
props.setProperty("db.url", "jdbc:mysql://localhost:3306/mydb");
props.setProperty("db.user", "root");

try (FileWriter writer = new FileWriter("config.properties")) {
    props.store(writer, "数据库配置");
} catch (IOException e) {
    e.printStackTrace();
}
```

### 9.8 Properties vs Map对比


| 方面 | **Properties** | **HashMap/HashTable** |
|------|---------------|----------------------|
| `类型限制` | `仅支持String类型的key和value` | `支持任意Object类型` |
| `典型用途` | `配置文件读写` | `普通数据结构` |
| `特性` | `内建支持加载/保存文件` | `不支持直接持久化到文件` |
| `是否线程安全` | `是（继承自Hashtable）` | `HashMap非线程安全` |

### 9.9 注意事项与建议


> **⚠️ 编码问题**：默认读取是ISO-8859-1编码，中文字符需转码（或使用InputStreamReader指定UTF-8）
> 
> **📄 文件格式**：文件必须使用key=value或key:value结构
> 
> **💬 注释支持**：可使用#或!添加注释行
> 
> **🔤 类型建议**：避免使用非字符串类型作为key或value

---

## 10. 🌍 ResourceBundle类详解


### 10.1 ResourceBundle概述


**🔸 核心定义**
```
ResourceBundle：Java国际化(i18n)的核心类
包路径：java.util.ResourceBundle
主要作用：管理特定区域(Locale)的资源
设计理念：将用户界面文本与代码分离，支持多语言应用开发
```

### 10.2 资源文件命名规范


**🔸 命名格式**
```java
基础名_语言_国家.properties
```

| 文件名 | **区域含义** | **优先级** |
|--------|-------------|----------|
| `Messages.properties` | `默认（英文或通用）` | `最低` |
| `Messages_zh.properties` | `中文（通用）` | `中等` |
| `Messages_zh_CN.properties` | `简体中文（中国）` | `最高` |
| `Messages_en_US.properties` | `美式英文` | `最高` |

### 10.3 ResourceBundle查找机制


**🔍 降级策略（从最具体到最通用）**
```
查找顺序：
1. Messages_zh_CN.properties  ← 最具体
2. Messages_zh.properties     ← 语言级别
3. Messages.properties        ← 默认
4. 找不到 → MissingResourceException
```

### 10.4 常用方法说明


| 方法 | **说明** | **使用场景** |
|------|----------|-------------|
| `getBundle(String baseName)` | `加载默认Locale的资源文件` | `简单加载` |
| `getBundle(String baseName, Locale locale)` | `按指定语言区域加载资源` | `多语言应用` |
| `getString(String key)` | `获取字符串资源` | `文本获取` |
| `getObject(String key)` | `获取任意对象（包括字符串、数组等）` | `复杂资源` |
| `keySet()` | `获取所有key集合` | `遍历所有键` |

### 10.5 实际使用示例


```java
// 文件内容 Messages_zh_CN.properties
welcome=欢迎光临
exit=退出系统

// Java 加载示例
public class TestResourceBundle {
    public static void main(String[] args) {
        Locale locale = new Locale("zh", "CN");
        ResourceBundle bundle = ResourceBundle.getBundle("Messages", locale);

        String welcome = bundle.getString("welcome");
        String exit = bundle.getString("exit");

        System.out.println(welcome); // 欢迎光临
        System.out.println(exit);    // 退出系统
    }
}

// 与 Locale 的配合使用
Locale zhCN = new Locale("zh", "CN");  // 简体中文
Locale enUS = new Locale("en", "US");  // 美式英语
ResourceBundle zhBundle = ResourceBundle.getBundle("Messages", zhCN);
ResourceBundle enBundle = ResourceBundle.getBundle("Messages", enUS);
```

### 10.6 注意事项


| 事项 | **说明** | **解决方案** |
|------|----------|-------------|
| `编码问题` | `.properties默认使用ISO-8859-1` | `需要转义或使用UTF-8 Reader` |
| `文件名规则` | `必须以基名+Locale命名` | `放在classpath中` |
| `缺失资源` | `找不到key会抛出异常` | `MissingResourceException处理` |
| `线程安全` | `ResourceBundle是线程安全的` | `可安全并发使用` |

---

## 11. 💾 RandomAccessFile类详解


### 11.1 RandomAccessFile概述


**🔸 核心定义**
```
RandomAccessFile：Java IO中的特殊类，支持文件随机访问
包路径：java.io.RandomAccessFile
设计目的：支持在文件中任意位置进行读写操作
本质：融合了输入流和输出流功能的类，用于文件的低层级读写操作
```

### 11.2 主要特点与应用场景


**⚡ 核心特点**
- 同时支持**读取和写入**
- 可**任意移动**文件指针位置
- 支持**随机访问**模式

**🎯 典型应用场景**
- 大型文件的局部读写（如日志文件、视频流）
- 数据库文件分页处理
- 文件修改（插入、覆盖）
- 多线程文件操作（可配合FileChannel使用）

### 11.3 创建与访问模式


**🔧 构造方法**
```java
public RandomAccessFile(String name, String mode)
public RandomAccessFile(File file, String mode)

// 使用文件名创建
RandomAccessFile raf = new RandomAccessFile("data.dat", "rw");

// 使用File对象创建
File file = new File("data.dat");
RandomAccessFile raf = new RandomAccessFile(file, "r");
```

### 11.4 文件访问模式详解


| 模式 | **说明** | **权限** | **使用场景** |
|------|----------|----------|-------------|
| `"r"` | `只读模式` | `只能读取` | `数据查询` |
| `"rw"` | `读写模式` | `读取+写入` | `常规文件操作` |
| `"rws"` | `读写模式，同步内容和元数据` | `读写+立即同步` | `关键数据写入` |
| `"rwd"` | `读写模式，同步内容但不同步元数据` | `读写+内容同步` | `日志写入` |

```java
// 只读模式 - 尝试写入将抛出IOException
RandomAccessFile readOnly = new RandomAccessFile("file.txt", "r");
// readOnly.write(65); // 抛出IOException

// 标准读写模式 - 支持读写操作
RandomAccessFile readWrite = new RandomAccessFile("file.txt", "rw");
readWrite.write(65);  // 正常执行

// 同步读写模式 - 每次写入立即同步到磁盘
RandomAccessFile syncReadWrite = new RandomAccessFile("file.txt", "rws");
syncReadWrite.write(65);  // 写入并立即同步到磁盘
```

### 11.5 常用方法详解


| 方法类别 | **方法名** | **说明** |
|---------|-----------|----------|
| **写入操作** | `write(int b)` | `写一个字节` |
| | `write(byte[] b)` | `写字节数组` |
| | `writeUTF(String str)` | `写入UTF格式字符串` |
| **读取操作** | `read()` | `读取一个字节（返回int）` |
| | `read(byte[] b)` | `读取字节数组` |
| | `readUTF()` | `读取UTF格式字符串` |
| **指针操作** | `getFilePointer()` | `获取当前指针位置` |
| | `seek(long pos)` | `移动文件指针到指定位置` |
| **文件操作** | `length()` | `获取文件长度` |
| | `setLength(long newLength)` | `设置文件长度（可截断或扩展）` |

### 11.6 随机访问示例


```java
// 写入文件内容
RandomAccessFile raf = new RandomAccessFile("data.txt", "rw");
raf.writeUTF("Hello");
raf.writeInt(100);
raf.close();

// 读取文件内容
RandomAccessFile raf = new RandomAccessFile("data.txt", "r");
String msg = raf.readUTF();
int number = raf.readInt();
System.out.println(msg + " - " + number);
raf.close();

// 随机跳转读取
RandomAccessFile raf = new RandomAccessFile("data.txt", "r");
raf.seek(5); // 假设跳过字符串长度和内容，读取 int 值
int num = raf.readInt();
System.out.println(num);
raf.close();
```

### 11.7 典型应用场景示例


```java
// 文件尾部追加内容
RandomAccessFile raf = new RandomAccessFile("log.txt", "rw");
raf.seek(raf.length()); // 指针移至文件末尾
raf.writeUTF("新日志行");
raf.close();

// 修改文件某一段数据
RandomAccessFile raf = new RandomAccessFile("data.bin", "rw");
raf.seek(10); // 假设第10字节开始是目标字段
raf.writeInt(999); // 修改为新值
raf.close();

// 配合 FileChannel 使用（更高级）
RandomAccessFile raf = new RandomAccessFile("bigfile.txt", "rw");
FileChannel channel = raf.getChannel();
// 可用 channel.map() 创建内存映射，提高大文件访问性能
```

### 11.8 注意事项与对比


**⚠️ 重要注意事项**

| 项目 | **说明** | **解决方案** |
|------|----------|-------------|
| `编码问题` | `writeUTF()和readUTF()使用特定格式编码` | `含长度信息，注意配对使用` |
| `并发访问` | `多线程写入需加锁` | `使用NIO或同步机制` |
| `指针越界` | `使用seek()必须小心避免越界` | `检查文件长度` |
| `覆盖数据` | `指针位置写入将覆盖原数据` | `非插入操作，需谨慎` |

**🔄 与其他IO类的区别**

| 类 | **特点** | **适用场景** |
|----|----------|-------------|
| `FileInputStream/FileOutputStream` | `顺序读取或写入` | `简单文件操作` |
| `BufferedReader/Writer` | `高效缓冲文本操作` | `文本文件处理` |
| `RandomAccessFile` | `可随机访问、读写混合` | `复杂文件处理` |
| `FileChannel（NIO）` | `更高效的文件映射与传输` | `可替代RandomAccessFile` |

---

## 12. 📋 核心要点总结


### 12.1 必须掌握的核心概念


```
🔸 File类：路径抽象表示，不涉及文件内容读写
🔸 Properties类：配置文件管理，键值对存储
🔸 ResourceBundle类：国际化资源管理，多语言支持
🔸 RandomAccessFile类：随机文件访问，支持任意位置读写
```

### 12.2 关键理解要点


**🔹 File类的设计理念**
```
核心概念：
- File对象 ≠ 实际文件
- 只是路径的封装和操作接口
- 真正的文件操作需要其他IO类配合
```

**🔹 Properties vs ResourceBundle**
```
Properties：
✓ 通用配置文件管理
✓ 简单键值对存储
✓ 手动编码处理

ResourceBundle：
✓ 专门国际化支持
✓ 自动locale匹配
✓ 更好的多语言支持
```

**🔹 RandomAccessFile的独特价值**
```
传统流：
- 顺序访问，无法回退
- 读写分离，操作单一

RandomAccessFile：
- 任意位置访问
- 读写一体化
- 适合复杂文件操作
```

### 12.3 实际应用指导


**📝 使用场景总结**

| 需求场景 | **推荐方案** | **理由** |
|---------|-------------|----------|
| `路径操作、文件信息获取` | `File类` | `专门的路径抽象` |
| `应用配置管理` | `Properties类` | `简单易用` |
| `多语言应用` | `ResourceBundle类` | `国际化专用` |
| `大文件随机访问` | `RandomAccessFile类` | `高效随机访问` |
| `现代化文件操作` | `Java NIO.2` | `功能更强大` |

### 12.4 最佳实践建议


**🔧 编程实践要点**
```
File操作：
- 使用File.separator处理跨平台路径
- 创建文件前检查父目录是否存在
- 及时关闭资源，使用try-with-resources

配置管理：
- Properties用于简单配置
- ResourceBundle用于国际化
- 注意中文编码问题

文件访问：
- 小文件用传统IO
- 大文件用RandomAccessFile或NIO
- 考虑并发安全问题
```

**核心记忆口诀**：
- File类管路径，不管内容读和写
- Properties配置好，键值对来存储  
- ResourceBundle国际化，多语言来支持
- RandomAccessFile功能强，随机访问效率高