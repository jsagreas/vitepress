---
title: 4、流程语句
---
## 📚 目录

1. [if-else条件语句](#1-if-else条件语句)
2. [switch分支语句](#2-switch分支语句)
3. [for循环语句](#3-for循环语句)
4. [while循环语句](#4-while循环语句)
5. [嵌套循环语句](#5-嵌套循环语句)
6. [递归语句](#6-递归语句)
7. [break和continue控制](#7-break和continue控制)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔀 if-else条件语句


### 1.1 条件语句概览


**🔸 条件语句的本质**
条件语句是程序根据不同条件执行不同代码分支的控制结构，是编程逻辑的基础。

```
程序执行流程：
        开始
         ↓
    [条件判断]
     ↙    ↘
   true   false
    ↓      ↓
  执行A    执行B
    ↓      ↓
        结束
```

### 1.2 单个if语句


**🔸 基本语法**
```java
if (条件表达式) {
    // 条件为 true 时执行的代码块
}
```

**💡 使用场景**
```java
// 简单条件检查
int age = 18;
if (age >= 18) {
    System.out.println("已成年，可以投票");
}

// 安全检查
String name = null;
if (name != null) {
    System.out.println("姓名长度：" + name.length());
}
```

### 1.3 if-else双分支


**🔸 基本语法**
```java
if (条件表达式) {
    // 条件为 true 时执行
} else {
    // 条件为 false 时执行
}
```

**💡 决策流程图**
```
    条件判断
     ↙    ↘
   true   false
    ↓      ↓
  分支A    分支B
    ↓      ↓
    合并点
```

**🔧 实际应用**
```java
int score = 85;
if (score >= 60) {
    System.out.println("考试通过");
} else {
    System.out.println("考试不及格");
}

// 三元运算符替代简单if-else
String result = (score >= 60) ? "通过" : "不及格";
```

### 1.4 多分支if-else if-else


**🔸 基本语法**
```java
if (条件1) {
    // 条件1为true时执行
} else if (条件2) {
    // 条件1为false且条件2为true时执行
} else if (条件3) {
    // 前面条件都为false且条件3为true时执行
} else {
    // 所有条件都为false时执行
}
```

**⚠️ 执行特点**
```
关键理解：
✅ 从上到下依次判断条件
✅ 只要某个条件为true，执行对应代码块后跳出
✅ 即使后续条件也为true，也不会执行
✅ else是可选的，用于处理所有条件都不满足的情况
```

**🔧 代码示例**
```java
int x = 5;
if (x > 10) {
    System.out.println("条件1成立");
} else if (x > 0) {
    System.out.println("条件2成立"); // 执行这里
} else if (x > -5) {
    System.out.println("条件3成立"); // 虽然也为true，但不会执行
} else {
    System.out.println("都不满足");
}
```

### 1.5 if语句最佳实践


**🎯 编程建议**
```
代码规范：
✅ 即使只有一行代码，也建议使用花括号{}
✅ 条件表达式要简洁明了，避免过于复杂
✅ 多重条件优先使用逻辑运算符&&、||
✅ 复杂逻辑考虑提取为独立方法

性能优化：
✅ 将最可能为true的条件放在前面
✅ 避免在条件中进行复杂计算
✅ 使用switch替代过多的else if
```

---

## 2. 🎛️ switch分支语句


### 2.1 switch语句概览


**🔸 switch vs if-else**

| 特性 | switch | if-else |
|------|--------|---------|
| **适用场景** | 等值比较、多分支选择 | 范围比较、复杂条件 |
| **性能** | 编译器优化，跳转表 | 顺序判断 |
| **可读性** | 多分支时更清晰 | 复杂条件更灵活 |
| **支持类型** | 有限类型 | 任意boolean表达式 |

### 2.2 支持的数据类型


**🔸 类型支持表**

| 数据类型 | 支持版本 | 示例 | 说明 |
|----------|----------|------|------|
| `byte` | JDK 1.0+ | `switch (byteValue)` | 8位整数 |
| `short` | JDK 1.0+ | `switch (shortValue)` | 16位整数 |
| `int` | JDK 1.0+ | `switch (intValue)` | 32位整数 |
| `char` | JDK 1.0+ | `switch (charValue)` | 16位字符 |
| `String` | **JDK 7+** | `switch (stringValue)` | 字符串类型 |
| `enum` | **JDK 5+** | `switch (enumValue)` | 枚举类型 |

**❌ 不支持的类型**
```java
// 以下类型不能用于switch
long longValue = 100L;        // ❌ 不支持
float floatValue = 3.14f;     // ❌ 不支持  
double doubleValue = 3.14;    // ❌ 不支持
boolean boolValue = true;     // ❌ 不支持
```

### 2.3 switch语法结构


**🔸 基本语法**
```java
switch (表达式) {
    case 值1:
        // 执行语句1
        break;
    case 值2:
        // 执行语句2
        break;
    case 值3:
    case 值4:
        // 值3和值4共享相同逻辑
        break;
    default:
        // 所有case不匹配时执行
        break; // default的break可选
}
```

### 2.4 case穿透机制


**⚠️ 穿透现象（Fall-through）**
```java
public class SwitchFallThrough {
    public static void main(String[] args) {
        int num = 2;

        switch (num) {
            case 1:
                System.out.println("匹配 1");
            case 2:
                System.out.println("匹配 2");    // 执行
            case 3:
                System.out.println("匹配 3");    // 也执行！
            default:
                System.out.println("默认执行");   // 也执行！
        }
    }
}

// 输出结果：
// 匹配 2
// 匹配 3  
// 默认执行
```

**💡 穿透机制的合理利用**
```java
// 多个case共享相同逻辑
char grade = 'B';
switch (grade) {
    case 'A':
    case 'a':
        System.out.println("优秀");
        break;
    case 'B':
    case 'b':
        System.out.println("良好");
        break;
    case 'C':
    case 'c':
        System.out.println("及格");
        break;
    default:
        System.out.println("不及格");
}
```

### 2.5 各类型示例


**🔸 整数类型示例**
```java
int day = 3;
switch (day) {
    case 1: System.out.println("星期一"); break;
    case 2: System.out.println("星期二"); break;
    case 3: System.out.println("星期三"); break;
    case 4: System.out.println("星期四"); break;
    case 5: System.out.println("星期五"); break;
    default: System.out.println("周末");
}
```

**🔸 字符类型示例**
```java
char grade = 'B';
switch (grade) {
    case 'A': System.out.println("优秀"); break;
    case 'B': System.out.println("良好"); break;
    case 'C': System.out.println("及格"); break;
    default: System.out.println("其他等级");
}
```

**🔸 字符串类型示例（Java 7+）**
```java
String role = "admin";
switch (role) {
    case "admin": 
        System.out.println("管理员权限"); 
        break;
    case "user": 
        System.out.println("普通用户权限"); 
        break;
    case "guest":
        System.out.println("访客权限");
        break;
    default: 
        System.out.println("未知身份");
}
```

**🔸 枚举类型示例（Java 5+）**
```java
public class EnumSwitchExample {
    enum Status { ACTIVE, INACTIVE, PENDING }
    
    public static void main(String[] args) {
        Status current = Status.ACTIVE;
        
        switch (current) {
            case ACTIVE:
                System.out.println("状态: 活跃");
                break;
            case INACTIVE:
                System.out.println("状态: 不活跃");
                break;
            case PENDING:
                System.out.println("状态: 待处理");
                break;
            default:
                System.out.println("未知状态");
        }
    }
}
```

### 2.6 switch使用要点


**🎯 最佳实践**
```
编码规范：
✅ 每个case后都要有break（除非故意穿透）
✅ 即使最后一个case也建议加break
✅ default分支建议始终包含，增强代码健壮性
✅ case的值必须是编译时常量

性能考虑：
✅ switch通常比多个if-else性能更好
✅ 编译器会优化为跳转表或二分查找
✅ case标签数量影响优化策略
```

---

## 3. 🔄 for循环语句


### 3.1 for循环概览


**🔸 for循环的优势**
```
for循环特点：
✅ 结构清晰，初始化、条件、更新集中管理
✅ 适合已知循环次数的场景
✅ 变量作用域限制在循环内部
✅ 支持多种灵活的变体写法
```

### 3.2 标准for循环


**🔸 基本语法**
```java
for (初始化表达式; 条件表达式; 更新表达式) {
    // 循环体
}
```

**🔸 执行流程图**
```
    开始
     ↓
  [初始化] (只执行一次)
     ↓
  [条件判断] ←─────┐
   ↙     ↘      │
 true    false   │
  ↓       ↓      │
[执行循环体]    结束
  ↓             │
 [更新表达式]    │
  ↓             │
  └─────────────┘
```

**🔧 经典示例**
```java
// 基本计数循环
for (int i = 0; i < 5; i++) {
    System.out.println("i = " + i);
}
// 输出：i = 0, i = 1, i = 2, i = 3, i = 4

// 倒序循环
for (int i = 10; i >= 1; i--) {
    System.out.println("倒计时：" + i);
}

// 步长不为1的循环
for (int i = 0; i < 20; i += 3) {
    System.out.println("i = " + i); // 0, 3, 6, 9, 12, 15, 18
}
```

### 3.3 增强型for循环（for-each）


**🔸 基本语法**
```java
for (元素类型 元素变量 : 数组或集合) {
    // 使用元素变量
}
```

**🔸 适用场景对比**

| 特性 | 标准for循环 | 增强for循环 |
|------|-------------|-------------|
| **语法简洁性** | 相对复杂 | 非常简洁 |
| **访问索引** | ✅ 支持 | ❌ 不支持 |
| **修改元素** | ✅ 支持 | ❌ 不支持（基本类型） |
| **性能** | 一般 | 更优 |
| **适用类型** | 数组、索引集合 | 数组、所有Collection |

**🔧 使用示例**
```java
// 遍历数组
int[] numbers = {10, 20, 30, 40, 50};
for (int num : numbers) {
    System.out.println(num);
}

// 遍历集合
List<String> names = Arrays.asList("Alice", "Bob", "Charlie");
for (String name : names) {
    System.out.println("Hello, " + name);
}

// 注意：无法修改基本类型元素
int[] arr = {1, 2, 3};
for (int num : arr) {
    num = num * 2; // ❌ 这不会修改原数组
}
```

### 3.4 for循环变体


**🔸 省略部分的for循环**

```java
// 示例1：省略初始化（变量在外部定义）
int i = 0;
for (; i < 3; i++) {
    System.out.println("i = " + i);
}
// 使用场景：循环结束后还要使用循环变量

// 示例2：省略更新表达式（手动控制更新）
for (int i = 0; i < 3;) {
    System.out.println("i = " + i);
    i++; // 手动更新
}
// 使用场景：复杂的更新逻辑

// 示例3：省略条件表达式（无限循环）
for (int i = 0; ; i++) {
    System.out.println("i = " + i);
    if (i >= 2) break; // 必须手动终止
}
// 使用场景：动态条件判断

// 示例4：完全省略（等价于while(true)）
for (;;) {
    System.out.println("无限循环");
    if (someCondition) break;
}
// 使用场景：服务器监听、游戏主循环
```

**🔸 多变量for循环**
```java
// 多个初始化变量
for (int i = 0, j = 10; i < j; i++, j--) {
    System.out.println("i=" + i + ", j=" + j);
}
// 输出：i=0,j=10  i=1,j=9  i=2,j=8  i=3,j=7  i=4,j=6
```

### 3.5 for循环最佳实践


**🎯 性能优化技巧**
```java
// ✅ 好的做法：缓存数组长度
int[] arr = new int[1000000];
int len = arr.length;
for (int i = 0; i < len; i++) {
    // 处理arr[i]
}

// ❌ 避免的做法：每次都获取长度
for (int i = 0; i < arr.length; i++) {
    // 每次循环都要访问arr.length
}

// ✅ 好的做法：使用增强for循环遍历
for (int element : arr) {
    // 简洁且高效
}
```

---

## 4. 🔃 while循环语句


### 4.1 while循环概览


**🔸 while vs for循环**

| 特性 | while循环 | for循环 |
|------|-----------|---------|
| **适用场景** | 条件不确定、次数未知 | 已知循环次数 |
| **语法结构** | 简洁，只有条件判断 | 复杂，包含初始化和更新 |
| **变量作用域** | 变量在循环外定义 | 变量可限制在循环内 |
| **使用频率** | 相对较少 | 更常用 |

### 4.2 标准while循环


**🔸 基本语法**
```java
while (条件表达式) {
    // 条件为true时执行的循环体
}
```

**🔸 执行流程图**
```
    开始
     ↓
  [条件判断] ←─────┐
   ↙     ↘      │
 true    false   │
  ↓       ↓      │
[执行循环体]    结束
  ↓             │
  └─────────────┘
```

**🔧 典型应用场景**
```java
// 场景1：用户输入验证
Scanner scanner = new Scanner(System.in);
int num;
System.out.print("请输入一个正数：");
while ((num = scanner.nextInt()) <= 0) {
    System.out.print("输入错误，请重新输入正数：");
}

// 场景2：文件读取
BufferedReader reader = new BufferedReader(new FileReader("file.txt"));
String line;
while ((line = reader.readLine()) != null) {
    System.out.println(line);
}

// 场景3：条件未知的计算
int num = 1234;
int digitCount = 0;
while (num > 0) {
    num /= 10;
    digitCount++;
}
System.out.println("位数：" + digitCount);
```

### 4.3 do-while循环


**🔸 基本语法**
```java
do {
    // 先执行一次循环体
} while (条件表达式); // 注意：结尾要加分号
```

**🔸 与while的关键区别**
```
执行次数对比：
while循环：可能0次（条件一开始就为false）
do-while：至少1次（先执行再判断）

使用场景：
while：条件驱动的循环  
do-while：至少执行一次的逻辑
```

**🔧 do-while典型应用**
```java
// 场景1：菜单选择（至少显示一次菜单）
Scanner scanner = new Scanner(System.in);
int choice;
do {
    System.out.println("=== 菜单 ===");
    System.out.println("1. 选项一");
    System.out.println("2. 选项二");
    System.out.println("0. 退出");
    System.out.print("请选择：");
    choice = scanner.nextInt();
    
    switch (choice) {
        case 1: System.out.println("执行选项一"); break;
        case 2: System.out.println("执行选项二"); break;
        case 0: System.out.println("退出程序"); break;
        default: System.out.println("无效选择");
    }
} while (choice != 0);

// 场景2：随机数生成（至少生成一次）
Random random = new Random();
int randomNum;
do {
    randomNum = random.nextInt(100);
    System.out.println("生成的随机数：" + randomNum);
} while (randomNum != 50); // 直到生成50为止
```

### 4.4 while循环注意事项


**⚠️ 常见问题与解决**
```java
// 问题1：无限循环
int i = 0;
while (i < 10) {
    System.out.println(i);
    // ❌ 忘记更新i，导致无限循环
}

// ✅ 正确做法
int i = 0;
while (i < 10) {
    System.out.println(i);
    i++; // 确保条件会发生变化
}

// 问题2：条件永远不满足
int i = 10;
while (i < 5) { // ❌ 条件一开始就为false
    System.out.println(i);
}

// 问题3：浮点数比较精度问题
double d = 0.1;
while (d != 1.0) { // ❌ 可能因精度问题无限循环
    d += 0.1;
}

// ✅ 正确做法：使用范围比较
double d = 0.1;
while (d < 1.0 - 0.001) { // 允许小误差
    d += 0.1;
}
```

---

## 5. 🌀 嵌套循环语句


### 5.1 嵌套循环概念


**🔸 嵌套循环结构**
```java
for (外层控制变量) {           // 外层循环
    for (内层控制变量) {       // 内层循环
        // 内层循环体
    }
    // 外层循环体的其他语句
}
```

**🔸 执行机制理解**
```
执行顺序：
1. 外层循环初始化
2. 外层循环条件判断
3. 进入外层循环体
4. 内层循环完整执行（所有迭代）
5. 外层循环更新
6. 回到步骤2，直到外层条件为false

时间复杂度：
外层循环n次 × 内层循环m次 = O(n×m)
```

### 5.2 嵌套循环经典应用


**🔸 九九乘法表**
```java
public class MultiplicationTable {
    public static void main(String[] args) {
        for (int i = 1; i <= 9; i++) {           // 外层控制行（被乘数）
            for (int j = 1; j <= i; j++) {       // 内层控制列（乘数）
                System.out.print(j + "×" + i + "=" + (i * j) + "\t");
            }
            System.out.println(); // 每行结束后换行
        }
    }
}

// 输出结果：
// 1×1=1
// 1×2=2    2×2=4
// 1×3=3    2×3=6    3×3=9
// ...
```

**🔸 图形绘制**
```java
// 打印直角三角形
public class TrianglePattern {
    public static void main(String[] args) {
        int n = 5;
        for (int i = 1; i <= n; i++) {        // 控制行数
            for (int j = 1; j <= i; j++) {    // 控制每行星号数
                System.out.print("* ");
            }
            System.out.println();
        }
    }
}

// 输出：
// * 
// * * 
// * * * 
// * * * * 
// * * * * *
```

**🔸 二维数组遍历**
```java
public class Array2DTraversal {
    public static void main(String[] args) {
        int[][] matrix = {
            {1, 2, 3},
            {4, 5, 6},
            {7, 8, 9}
        };
        
        // 方法1：标准嵌套for循环
        for (int i = 0; i < matrix.length; i++) {           // 遍历行
            for (int j = 0; j < matrix[i].length; j++) {    // 遍历列
                System.out.print(matrix[i][j] + " ");
            }
            System.out.println();
        }
        
        // 方法2：增强for循环
        for (int[] row : matrix) {
            for (int element : row) {
                System.out.print(element + " ");
            }
            System.out.println();
        }
    }
}
```

### 5.3 嵌套循环控制


**🔸 break和continue在嵌套循环中**
```java
// break只跳出当前循环
for (int i = 1; i <= 3; i++) {
    for (int j = 1; j <= 3; j++) {
        if (j == 2) break; // 只跳出内层循环
        System.out.print("(" + i + "," + j + ") ");
    }
    System.out.println();
}
// 输出：
// (1,1) 
// (2,1) 
// (3,1)

// 使用标签跳出多层循环
outer: for (int i = 1; i <= 3; i++) {
    for (int j = 1; j <= 3; j++) {
        if (i == 2 && j == 2) {
            break outer; // 跳出外层循环
        }
        System.out.print("(" + i + "," + j + ") ");
    }
    System.out.println();
}
// 输出：
// (1,1) (1,2) (1,3) 
// (2,1)
```

### 5.4 嵌套循环性能优化


**🎯 优化策略**
```java
// ✅ 优化技巧1：减少内层循环次数
// 例如：查找二维数组中的最大值
int max = matrix[0][0];
for (int i = 0; i < matrix.length; i++) {
    for (int j = 0; j < matrix[i].length; j++) {
        if (matrix[i][j] > max) {
            max = matrix[i][j];
        }
    }
}

// ✅ 优化技巧2：避免重复计算
// ❌ 低效写法
for (int i = 0; i < matrix.length; i++) {
    for (int j = 0; j < matrix[i].length; j++) {
        // 每次都计算matrix[i].length
    }
}

// ✅ 高效写法
for (int i = 0; i < matrix.length; i++) {
    int rowLength = matrix[i].length; // 缓存长度
    for (int j = 0; j < rowLength; j++) {
        // 使用缓存的长度
    }
}
```

---

## 6. 🔄 递归语句


### 6.1 递归基本概念


**🔸 递归定义**
递归是方法自己调用自己的编程技术，通常用于解决可以分解为相似子问题的复杂问题。

**🔸 递归的数学基础**
```
数学递归定义示例：
阶乘：n! = n × (n-1)!  (n > 1)
     1! = 1            (基础情况)

斐波那契：F(n) = F(n-1) + F(n-2)  (n > 2)
         F(1) = F(2) = 1          (基础情况)
```

### 6.2 递归的组成要素


**🔸 递归三要素**

| 要素 | 说明 | 重要性 |
|------|------|--------|
| **终止条件（Base Case）** | 递归的"出口"，停止递归调用 | ⭐⭐⭐ 最重要 |
| **递归调用（Recursive Call）** | 方法调用自身，处理子问题 | ⭐⭐⭐ |
| **参数变化** | 每次调用参数向终止条件靠近 | ⭐⭐⭐ |

**🔸 递归方法通用模板**
```java
返回类型 递归方法名(参数) {
    // 1. 终止条件
    if (终止条件) {
        return 终止结果;
    }
    
    // 2. 递归调用（参数必须变化）
    return 递归方法名(参数的较小值);
}
```

### 6.3 递归执行机制


**🔸 递归调用栈**
```
递归执行过程：
1. 方法调用 → 压栈（保存当前状态）
2. 执行递归调用 → 继续压栈
3. 到达终止条件 → 开始出栈
4. 依次返回结果 → 完成计算

栈结构特点：
• 后进先出（LIFO）
• 每层递归都有独立的变量空间
• 栈空间有限，深度递归可能栈溢出
```

### 6.4 递归经典示例


**🔸 阶乘计算**
```java
public class FactorialExample {
    // 计算 n! = n × (n-1) × ... × 1
    public static int factorial(int n) {
        // 终止条件
        if (n <= 1) {
            return 1;
        }
        
        // 递归调用
        return n * factorial(n - 1);
    }

    public static void main(String[] args) {
        System.out.println("5! = " + factorial(5)); // 输出：120
    }
}

// 执行过程追踪：
factorial(5)
├── 5 * factorial(4)
    ├── 4 * factorial(3)
        ├── 3 * factorial(2)
            ├── 2 * factorial(1)
                └── 1 (终止条件)
            └── 2 * 1 = 2
        └── 3 * 2 = 6
    └── 4 * 6 = 24
└── 5 * 24 = 120
```

**🔸 斐波那契数列**
```java
public class FibonacciExample {
    // F(n) = F(n-1) + F(n-2)，F(1)=F(2)=1
    public static int fibonacci(int n) {
        // 终止条件
        if (n <= 2) {
            return 1;
        }
        
        // 递归调用
        return fibonacci(n - 1) + fibonacci(n - 2);
    }

    public static void main(String[] args) {
        System.out.println("F(6) = " + fibonacci(6)); // 输出：8
    }
}

// 递归调用树：
fibonacci(6)
├── fibonacci(5)
│   ├── fibonacci(4)
│   │   ├── fibonacci(3)
│   │   │   ├── fibonacci(2) → 1
│   │   │   └── fibonacci(1) → 1
│   │   └── fibonacci(2) → 1
│   └── fibonacci(3)
│       ├── fibonacci(2) → 1
│       └── fibonacci(1) → 1
└── fibonacci(4) [重复计算]
```

### 6.5 递归vs循环对比


**🔸 特性对比表**

| 特性 | 递归 | 循环 |
|------|------|------|
| **代码可读性** | 接近数学定义，直观 | 相对复杂 |
| **内存使用** | 每次调用压栈，内存开销大 | 固定内存空间 |
| **执行效率** | 函数调用开销，相对较慢 | 执行效率高 |
| **实现难度** | 思路清晰，实现简单 | 需要手动管理状态 |
| **适用场景** | 树形结构、分治算法、回溯 | 简单重复操作 |
| **栈溢出风险** | 深度递归可能栈溢出 | 无此风险 |

**🔸 递归改循环示例**
```java
// 递归版本：简洁但效率低
public static int fibRecursive(int n) {
    if (n <= 2) return 1;
    return fibRecursive(n - 1) + fibRecursive(n - 2);
}

// 循环版本：效率高但相对复杂
public static int fibIterative(int n) {
    if (n <= 2) return 1;
    
    int prev2 = 1, prev1 = 1;
    for (int i = 3; i <= n; i++) {
        int current = prev1 + prev2;
        prev2 = prev1;
        prev1 = current;
    }
    return prev1;
}

// 性能对比：
// fibRecursive(40)：时间复杂度O(2^n)，非常慢
// fibIterative(40)：时间复杂度O(n)，很快
```

### 6.6 递归注意事项


**⚠️ 常见问题与解决**

| 问题 | 描述 | 解决方案 |
|------|------|----------|
| **栈溢出** | 递归深度过大 | 1. 增加JVM栈大小 `-Xss`<br>2. 改用循环实现<br>3. 尾递归优化 |
| **性能问题** | 重复计算（如斐波那契） | 1. 使用记忆化递归<br>2. 动态规划<br>3. 改用循环 |
| **无限递归** | 缺少终止条件或参数不变化 | 1. 确保有明确终止条件<br>2. 确保参数向终止条件收敛 |

**🔧 递归优化技巧**
```java
// 记忆化递归（避免重复计算）
public class MemoizedFibonacci {
    private static Map<Integer, Integer> memo = new HashMap<>();
    
    public static int fibonacci(int n) {
        if (n <= 2) return 1;
        
        // 检查是否已计算过
        if (memo.containsKey(n)) {
            return memo.get(n);
        }
        
        // 计算并存储结果
        int result = fibonacci(n - 1) + fibonacci(n - 2);
        memo.put(n, result);
        return result;
    }
}
```

---

## 7. 🛑 break和continue控制


### 7.1 流程控制语句概览


**🔸 控制语句作用**
```
break：    终止循环或switch，跳出当前结构
continue： 跳过当前迭代，继续下次循环
return：   终止方法执行，返回结果
throw：    抛出异常，中断正常流程
```

### 7.2 break语句详解


**🔸 break的使用场景**

| 使用位置 | 作用 | 示例场景 |
|----------|------|----------|
| **for循环** | 终止整个循环 | 找到目标值后提前结束 |
| **while循环** | 终止整个循环 | 满足退出条件时结束 |
| **do-while循环** | 终止整个循环 | 错误处理或用户退出 |
| **switch语句** | 跳出switch分支 | 防止case穿透 |

**🔧 break在循环中的应用**
```java
// 示例1：查找数组中的元素
public static int findElement(int[] arr, int target) {
    for (int i = 0; i < arr.length; i++) {
        if (arr[i] == target) {
            System.out.println("找到目标值：" + target + "，位置：" + i);
            break; // 找到后立即终止循环
        }
    }
    return -1; // 未找到
}

// 示例2：输入验证循环
Scanner scanner = new Scanner(System.in);
while (true) {
    System.out.print("请输入密码：");
    String password = scanner.nextLine();
    
    if ("123456".equals(password)) {
        System.out.println("密码正确！");
        break; // 密码正确，退出循环
    } else {
        System.out.println("密码错误，请重试");
    }
}
```

**🔸 break在嵌套循环中**
```java
// break只跳出当前层循环
public class BreakNested {
    public static void main(String[] args) {
        for (int i = 1; i <= 3; i++) {
            System.out.println("外层循环：" + i);
            for (int j = 1; j <= 3; j++) {
                if (j == 2) {
                    break; // 只跳出内层循环
                }
                System.out.println("  内层循环：" + j);
            }
        }
    }
}

// 输出：
// 外层循环：1
//   内层循环：1
// 外层循环：2  
//   内层循环：1
// 外层循环：3
//   内层循环：1
```

**🔸 标签break（跳出多层循环）**
```java
public class LabeledBreak {
    public static void main(String[] args) {
        outer: for (int i = 1; i <= 3; i++) {
            for (int j = 1; j <= 3; j++) {
                if (i == 2 && j == 2) {
                    break outer; // 跳出外层循环
                }
                System.out.print("(" + i + "," + j + ") ");
            }
            System.out.println();
        }
        System.out.println("循环结束");
    }
}

// 输出：
// (1,1) (1,2) (1,3) 
// (2,1) 
// 循环结束
```

### 7.3 continue语句详解


**🔸 continue的使用场景**

| 场景 | 说明 | 示例 |
|------|------|------|
| **跳过特定条件** | 满足某条件时跳过当前迭代 | 跳过偶数，只处理奇数 |
| **错误处理** | 处理失败时继续下一个 | 文件处理失败继续下一个 |
| **过滤数据** | 筛选符合条件的数据 | 只处理有效数据 |

**🔧 continue基本用法**
```java
// 示例1：跳过偶数，只打印奇数
for (int i = 1; i <= 10; i++) {
    if (i % 2 == 0) {
        continue; // 跳过偶数
    }
    System.out.println("奇数：" + i);
}
// 输出：奇数：1  奇数：3  奇数：5  奇数：7  奇数：9

// 示例2：处理数组，跳过负数
int[] numbers = {1, -2, 3, -4, 5, -6};
for (int num : numbers) {
    if (num < 0) {
        continue; // 跳过负数
    }
    System.out.println("正数：" + num);
}
// 输出：正数：1  正数：3  正数：5
```

**🔸 continue在嵌套循环中**
```java
public class ContinueNested {
    public static void main(String[] args) {
        for (int i = 1; i <= 3; i++) {
            System.out.println("外层循环：" + i);
            for (int j = 1; j <= 3; j++) {
                if (j == 2) {
                    continue; // 只跳过内层当前迭代
                }
                System.out.println("  内层循环：" + j);
            }
        }
    }
}

// 输出：
// 外层循环：1
//   内层循环：1
//   内层循环：3
// 外层循环：2
//   内层循环：1  
//   内层循环：3
// 外层循环：3
//   内层循环：1
//   内层循环：3
```

### 7.4 break vs continue对比


**🔸 行为对比**

| 特性 | break | continue |
|------|-------|----------|
| **作用范围** | 终止整个循环或switch | 跳过当前迭代，继续下次 |
| **执行效果** | 立即退出循环体 | 跳转到循环条件判断 |
| **适用场景** | 找到结果、错误终止 | 过滤条件、跳过处理 |
| **在switch中** | ✅ 防止穿透 | ❌ 不能使用 |

**🔧 对比示例**
```java
public class BreakVsContinue {
    public static void main(String[] args) {
        System.out.println("=== break示例 ===");
        for (int i = 1; i <= 5; i++) {
            if (i == 3) {
                break; // 遇到3就终止循环
            }
            System.out.println("i = " + i);
        }
        // 输出：i = 1  i = 2
        
        System.out.println("\n=== continue示例 ===");
        for (int i = 1; i <= 5; i++) {
            if (i == 3) {
                continue; // 遇到3就跳过，继续循环
            }
            System.out.println("i = " + i);
        }
        // 输出：i = 1  i = 2  i = 4  i = 5
    }
}
```

### 7.5 流程控制最佳实践


**🎯 使用建议**
```
适当使用：
✅ 在复杂条件判断中简化逻辑
✅ 处理异常情况或边界条件
✅ 提高代码可读性和性能

避免滥用：
❌ 不要过度使用，影响代码逻辑清晰度
❌ 复杂嵌套中的break/continue会降低可读性
❌ 可以用简单条件判断替代时不要使用
```

**🔧 重构建议**
```java
// ❌ 过度使用continue的例子
for (int i = 0; i < arr.length; i++) {
    if (arr[i] == null) continue;
    if (arr[i].isEmpty()) continue;
    if (arr[i].startsWith("#")) continue;
    // 处理逻辑
    processData(arr[i]);
}

// ✅ 更清晰的写法
for (int i = 0; i < arr.length; i++) {
    if (arr[i] != null && !arr[i].isEmpty() && !arr[i].startsWith("#")) {
        processData(arr[i]);
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 条件控制：if-else实现程序分支逻辑
🔸 多分支选择：switch语句处理多值匹配
🔸 循环控制：for、while实现重复执行逻辑
🔸 嵌套结构：多层循环处理复杂数据结构
🔸 递归思想：方法自调用解决分治问题
🔸 流程控制：break、continue精确控制执行流程
```

### 8.2 关键理解要点


**🔹 条件语句选择策略**
```
选择原则：
• 简单真假判断 → if-else
• 多值精确匹配 → switch
• 复杂条件逻辑 → if-else if
• 简单条件赋值 → 三元运算符
```

**🔹 循环语句选择策略**
```
选择原则：
• 已知循环次数 → for循环
• 条件驱动循环 → while循环  
• 至少执行一次 → do-while循环
• 遍历集合数组 → 增强for循环
```

**🔹 递归使用时机**
```
适用场景：
✅ 树形数据结构遍历
✅ 分治算法实现
✅ 回溯算法求解
✅ 数学递推公式计算

不适用场景：
❌ 简单重复操作（用循环更好）
❌ 深度很大的递归（栈溢出风险）
❌ 有大量重复计算的递归
```

**🔹 嵌套循环注意事项**
```
性能考虑：
• 时间复杂度：外层n次 × 内层m次 = O(n×m)
• 尽量减少内层循环的迭代次数
• 考虑缓存外层循环中的计算结果
• 合理使用break减少不必要的迭代
```

### 8.3 实际编程应用


**🎯 性能优化技巧**
```
循环优化：
✅ 缓存数组长度：int len = arr.length
✅ 减少方法调用：避免在循环条件中调用方法
✅ 使用增强for循环：遍历集合时更高效
✅ 合理使用break：找到结果后立即退出

递归优化：
✅ 记忆化递归：缓存计算结果避免重复
✅ 尾递归：某些语言可优化为循环
✅ 迭代替代：简单递归改为循环实现
```

**🔧 代码质量提升**
```
可读性：
✅ 合理的缩进和空行
✅ 有意义的变量命名
✅ 适当的注释说明
✅ 避免过深的嵌套（建议不超过3层）

健壮性：
✅ 边界条件检查
✅ 异常情况处理
✅ 避免无限循环
✅ 递归终止条件验证
```

**⚠️ 常见陷阱避免**
```
循环陷阱：
• 循环条件永远为true导致无限循环
• 循环变量忘记更新
• 浮点数精度问题导致条件异常
• 集合遍历时的并发修改异常

递归陷阱：
• 缺少终止条件导致栈溢出
• 参数不向终止条件收敛
• 重复计算导致性能问题
• 递归深度过大超出栈空间

流程控制陷阱：
• switch语句忘记break导致穿透
• break/continue作用范围理解错误
• 标签使用不当影响代码可读性
```

### 8.4 学习建议


**📚 掌握顺序**
```
1. 基础条件语句（if-else）
2. 基础循环语句（for、while）
3. 分支选择语句（switch）
4. 嵌套循环处理
5. 流程控制（break、continue）
6. 递归思想和应用
```

**🔨 实践项目**
```
初级练习：
• 计算器程序（条件分支）
• 数组遍历和查找（循环）
• 简单图形绘制（嵌套循环）

中级练习：
• 排序算法实现（多种循环结合）
• 树形结构遍历（递归）
• 游戏逻辑实现（综合应用）

高级练习：
• 算法问题求解（递归+动态规划）
• 数据结构实现（综合流程控制）
• 复杂业务逻辑处理（设计模式结合）
```

**核心记忆**：
- 条件分支switch选，循环for-while各有专
- 嵌套递归需谨慎，流程控制break-continue
- 性能可读两兼顾，边界异常要处理