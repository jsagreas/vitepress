---
title: 11、OOP常用类
---
## 📚 目录

1. [基础通用类](#1-基础通用类)
   - [Object类](#11-object类)
   - [System类](#12-system类)
   - [Runtime类](#13-runtime类)
   - [Scanner类](#14-scanner类)
   - [Random类](#15-random类)
2. [时间日期处理](#2-时间日期处理)
   - [Date类](#21-date类)
   - [Calendar类](#22-calendar类)
   - [SimpleDateFormat类](#23-simpledateformat类)
   - [DateTimeFormatter类](#24-datetimeformatter类)
   - [LocalDate类](#25-localdate类)
   - [LocalTime类](#26-localtime类)
   - [LocalDateTime类](#27-localdatetime类)
   - [ZonedDateTime类](#28-zoneddatetime类)
   - [Duration类](#29-duration类)
   - [Period类](#210-period类)
   - [Instant类](#211-instant类)
3. [字符串相关](#3-字符串相关)
   - [String类](#31-string类)
   - [StringBuilder类](#32-stringbuilder类)
   - [StringBuffer类](#33-stringbuffer类)
   - [StringJoiner类](#34-stringjoiner类)
4. [包装类](#4-包装类)
   - [Wrapper类](#41-wrapper类)
5. [其他常用辅助](#5-其他常用辅助)
   - [Arrays类](#51-arrays类)
   - [Math类](#52-math类)
   - [BigDecimal类](#53-bigdecimal类)
   - [BigInteger类](#54-biginteger类)
   - [Pattern类](#55-pattern类)
   - [Matcher类](#56-matcher类)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔧 基础通用类


### 1.1 Object类


**🔸 核心定义**
```
Object类：所有Java类的超类（父类），每个类都隐式地继承自Object类
位置：java.lang包，默认导入
特点：任何一个类如果没有显式继承父类，都会隐式继承Object类
```

**🔸 Object类核心方法**

| **方法** | **说明** | **示例** |
|---------|----------|----------|
| `toString()` | 返回对象的字符串表示 | `System.out.println(person)` |
| `equals(Object obj)` | 判断两个对象是否相等 | `if (a.equals(b))` |
| `hashCode()` | 返回对象的哈希码 | `int code = obj.hashCode()` |
| `clone()` | 返回对象的副本 | `Object copy = obj.clone()` |
| `getClass()` | 获取对象的类 | `Class<?> clazz = obj.getClass()` |
| `finalize()` | 垃圾回收前调用，但已过时，不推荐使用 | `// 过时，不推荐使用` |

### 1.2 System类


**🔸 System类基本介绍**

| **特性** | **说明** |
|---------|----------|
| **所属包** | `java.lang`，默认导入，可直接使用 |
| **静态方法和字段** | 所有成员都是静态的，可直接通过类名调用 |
| **`final` 修饰** | 无法继承，确保类的安全性和稳定性 |
| **私有构造方法** | 不能实例化，仅能通过静态方法访问功能 |
| **提供系统级功能** | 标准输入/输出流、系统属性、内存管理、数组操作等 |

**🔸 System类核心功能**

| **功能类别** | **方法** | **说明** |
|-------------|----------|----------|
| **标准IO流** | `System.in`, `System.out`, `System.err` | 处理输入、输出、错误流 |
| **时间管理** | `currentTimeMillis()`, `nanoTime()` | 获取当前时间（毫秒级、纳秒级） |
| **垃圾回收** | `gc()` | 提示 JVM 进行垃圾回收 |
| **系统退出** | `exit(int status)` | 终止 JVM，返回状态码 |
| **数组操作** | `arrayCopy(src, srcPos, dest, destPos, length)` | 复制数组元素，提高效率 |
| **系统属性获取** | `getProperty(String key)` | 获取系统属性（如 `os.name`, `user.home`） |

**🔸 System.in - 标准输入流**
- **类型**：InputStream
- **作用**：默认与键盘输入相连，用于读取用户输入的数据
- **常用场景**：读取用户输入，通常通过 Scanner 或 BufferedReader 来读取

```java
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter your name: ");
        String name = scanner.nextLine();
        System.out.println("Hello, " + name);
    }
}
```

**🔸 System.out - 标准输出流**
- **类型**：PrintStream
- **作用**：默认与控制台输出相连，用于将信息输出到屏幕（控制台）
- **常用场景**：在控制台打印信息，常用 println() 打印输出信息

```java
public class Main {
    public static void main(String[] args) {
        System.out.println("Hello, World!");  // 输出到控制台
    }
}

// print(String s)：输出字符串，不换行
// println(String s)：输出字符串，并换行
```

**🔸 System.err - 标准错误流**
- **类型**：PrintStream
- **作用**：与标准错误流连接，通常用于打印错误信息
- **常用场景**：输出错误信息，通常与 System.out 区分，用于错误日志等

```java
public class Main {
    public static void main(String[] args) {
        System.err.println("This is an error message.");
    }
}
```

**🔸 System类常用方法实例**

```java
// System.currentTimeMillis() - 获取当前时间的毫秒数
// 作用：返回当前时间与 1970年1月1日00:00:00 UTC 的毫秒差（Unix 时间戳）
// 常用场景：用于计算程序的运行时间，或获取当前时间戳
public class Main {
    public static void main(String[] args) {
        long startTime = System.currentTimeMillis();
        
        // 模拟一些处理
        for (int i = 0; i < 1000; i++) {
            Math.random();
        }
        
        long endTime = System.currentTimeMillis();
        System.out.println("Time taken: " + (endTime - startTime) + " milliseconds");
    }
}

// System.nanoTime() - 获取当前时间的纳秒数
// 作用：返回一个高精度的时间值，用于度量时间间隔 
// 常用场景：精确测量短时间间隔，如算法性能测试
public class Main {
    public static void main(String[] args) {
        long startTime = System.nanoTime();
        
        // 模拟一些处理
        for (int i = 0; i < 1000; i++) {
            Math.random();
        }
        
        long endTime = System.nanoTime();
        System.out.println("Time taken: " + (endTime - startTime) + " nanoseconds");
    }
}

// System.arraycopy() - 数组复制 (比循环复制更高效的本地方法)
// 作用：将一个数组的内容复制到另一个数组中，效率高于手动循环复制
// 常用场景：高效的数组元素复制，常用于数组操作
int[] source = {1, 2, 3, 4, 5};
int[] dest = new int[5];
// arraycopy(源数组, 源起始位置, 目标数组, 目标起始位置, 复制长度)
System.arraycopy(source, 0, dest, 0, source.length);

// 部分复制
int[] partial = new int[3];
System.arraycopy(source, 1, partial, 0, 3); // partial = {2, 3, 4}

// System.gc() - 请求垃圾回收
// 作用：建议 JVM 执行垃圾回收，但并不保证一定会执行。
// 常用场景：在内存使用较高时手动请求垃圾回收 
public class Main {
    public static void main(String[] args) {
        System.gc();  // 请求垃圾回收
        System.out.println("Garbage collection requested.");
    }
}

// System.exit() - 退出程序
// 作用：终止当前的 Java 程序并退出 JVM。
// 常用场景：在程序执行完毕或遇到错误时退出程序
public class Main {
    public static void main(String[] args) {
        System.out.println("Program will exit now.");
        System.exit(0);  // 退出程序，0表示正常退出
    }
}

// 解释：System.exit(0) 终止当前 Java 程序，0 表示正常退出，非零表示异常退出。
```

**🔸 标准输入输出流的重定向**

```java
// System 类提供了 System.setIn()、System.setOut() 和 System.setErr() 方法
// 允许开发者重定向标准输入、输出和错误流
// 重定向输出流到文件
PrintStream ps = new PrintStream("output.txt");
System.setOut(ps);

// 重定向错误流
System.setErr(new PrintStream("error.log"));

// 重定向输入流
System.setIn(new FileInputStream("input.txt"));
```

### 1.3 Runtime类


**🔸 Runtime类概述**

| **特性** | **说明** |
|---------|----------|
| **所属包** | `java.lang`，默认导入，可直接使用 |
| **代表运行时环境** | 提供 Java 程序执行期间的系统交互 |
| **单例模式** | 不能实例化，使用 `Runtime.getRuntime()` 获取唯一实例 |
| **与 JVM 交互** | 提供内存管理、垃圾回收、进程控制等功能 |
| **执行外部命令** | 可以调用 `exec()` 运行系统命令 |
| **系统信息获取** | 获取JVM 内存状态、处理器数量等数据 |

**🔸 Runtime类常用方法**

```java
// getRuntime()：获取当前运行时环境实例
// 作用：返回当前 Java 应用程序的运行时对象，允许访问 JVM 相关的资源。
// 常用场景：获取并操作当前应用程序的 Runtime 对象，例如获取内存信息、执行系统命令等。
public class Main {
    public static void main(String[] args) {
        // 获取当前 Java 应用程序的运行时对象
        Runtime runtime = Runtime.getRuntime();
        System.out.println("Available processors: " + runtime.availableProcessors());
        System.out.println("Max memory: " + runtime.maxMemory() + " bytes");
        System.out.println("Total memory: " + runtime.totalMemory() + " bytes");
        System.out.println("Free memory: " + runtime.freeMemory() + " bytes");
    }
}
// availableProcessors()：返回 JVM 可用的处理器数
// maxMemory()：返回 JVM 最大内存大小（单位：字节）
// totalMemory()：返回 JVM 当前可用内存总量（单位：字节）
// freeMemory()：返回 JVM 中当前空闲内存的大小（单位：字节）

// exec()：执行系统命令
// 作用：执行外部操作系统的命令（如启动外部程序、执行脚本等）  
// 常用场景：通过 Java 程序执行外部命令或程序，通常用于系统级操作，如调用外部工具、批处理文件等 
public class Main {
    public static void main(String[] args) {
        try {
            // 执行一个系统命令（Windows上执行"notepad.exe"）
            Runtime runtime = Runtime.getRuntime();
            Process process = runtime.exec("notepad.exe");

            // 等待外部进程执行完毕
            process.waitFor();
            System.out.println("Notepad launched and closed.");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
// 解释：exec() 方法启动一个外部程序，返回一个 Process 对象，我们可以用它来与外部进程交互

// exit(int status)：退出当前应用程序
// 作用：终止当前 Java 程序并退出 JVM，status 参数为退出状态码，0 通常表示正常退出，非 0 表示异常退出。
// 常用场景：在程序执行完毕或出现异常时，退出程序
public class Main {
    public static void main(String[] args) {
        System.out.println("Program will exit now.");
        Runtime.getRuntime().exit(0);  // 退出程序，0 表示正常退出
    }
}
// 解释：exit(0) 终止当前程序，退出时返回状态码 0

// gc()：建议进行垃圾回收
// 作用：请求 JVM 执行垃圾回收操作，但并不保证立即执行。
// 常用场景：手动触发垃圾回收（通常不推荐手动触发，JVM 会自动管理垃圾回收）
public class Main {
    public static void main(String[] args) {
        // 请求垃圾回收
        Runtime.getRuntime().gc();
        System.out.println("Garbage collection requested.");
    }
}
// 解释：gc() 方法提示 JVM 进行垃圾回收，通常 JDK 会根据内存使用情况自动触发垃圾回收，手动调用通常不需要
```

### 1.4 Scanner类


**🔸 Scanner类概述**

| **特性** | **说明** |
|---------|----------|
| **所属包** | `java.util`，需手动导入 `import java.util.Scanner;` |
| **主要功能** | 解析基本类型和字符串，用于输入数据扫描 |
| **输入源** | 支持键盘输入、文件、字符串、流对象等数据来源 |
| **分隔方式** | 使用正则表达式作为分隔符，灵活解析文本 |
| **接口实现** | 继承 `Iterator` 和 `Closeable`，可用于迭代与关闭资源 |

**🔸 创建Scanner对象**

```java
// 从标准输入创建 Scanner
Scanner scanner = new Scanner(System.in);

// 从字符串创建 Scanner
String data = "John Smith 25 3.5";
Scanner scanner = new Scanner(data);

// 从文件创建 Scanner
try {
    Scanner scanner = new Scanner(new File("data.txt"));
    // 使用完后记得关闭
    scanner.close();
} catch (FileNotFoundException e) {
    e.printStackTrace();
}

// 从网络流创建
URL url = new URL("http://example.com");
Scanner scanner = new Scanner(url.openStream());

// 从字节数组创建
byte[] bytes = "Hello World".getBytes();
Scanner scanner = new Scanner(new ByteArrayInputStream(bytes));
```

**🔸 Scanner类常用构造方法**

```java
// Scanner(InputStream source) - 从标准输入读取数据
// 作用：创建一个从指定输入流（如 System.in）读取数据的 Scanner 实例
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);  // 从标准输入读取
        System.out.print("Enter your name: ");
        String name = scanner.nextLine();  // 读取整行文本
        System.out.println("Hello, " + name);
        scanner.close();  // 关闭Scanner
    }
}

// Scanner(File source) - 从文件读取数据
// 作用：创建一个从指定文件读取数据的 Scanner 实例 
import java.io.File;
import java.util.Scanner;
public class Main {
    public static void main(String[] args) throws Exception {
        File file = new File("data.txt");
        Scanner scanner = new Scanner(file);  // 从文件读取
        while (scanner.hasNextLine()) {
            String line = scanner.nextLine();  // 读取每一行
            System.out.println(line);
        }
        scanner.close();
    }
}

// Scanner(String source) - 从字符串读取数据
// 作用：创建一个从指定字符串读取数据的 Scanner 实例
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        String text = "Hello 123 45.67 true";
        Scanner scanner = new Scanner(text);  // 从字符串读取
        System.out.println(scanner.next());  // 输出：Hello
        System.out.println(scanner.nextInt());  // 输出：123
        System.out.println(scanner.nextDouble());  // 输出：45.67
        System.out.println(scanner.nextBoolean());  // 输出：true
        scanner.close();
    }
}
```

**🔸 读取字符串**

```java
// 读取下一个字符串标记（以空白分隔）
String token = scanner.next();

// 读取一整行文本（包括空白）
String line = scanner.nextLine();

// 注意：在 nextInt() 等方法后调用 nextLine() 时的特殊情况
// 通常需要额外调用一次 nextLine() 来消耗换行符
int value = scanner.nextInt();
scanner.nextLine(); // 消耗换行符
String input = scanner.nextLine(); // 现在才真正读取输入行
```

**🔸 使用正则表达式**

```java
// 使用正则表达式读取
String pattern = scanner.next("\\d+"); // 只读取数字

// 使用正则表达式查找
scanner.findInLine("\\d{3}-\\d{2}-\\d{4}"); // 查找类似 SSN 的格式

// 跳过与模式匹配的输入
scanner.skip("\\s*,\\s*"); // 跳过逗号和周围空白
```

**🔸 Scanner类常用方法**

```java
// next() - 读取下一个词（以空白字符分隔）
// 作用：读取下一个单词（以空格、换行、制表符等分隔）并返回它的字符串表示 
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter a word: ");
        String word = scanner.next();  // 读取单个词
        System.out.println("You entered: " + word);
        scanner.close();
    }
}

// nextLine() - 读取一整行文本
// 作用：读取一整行文本，直到遇到换行符（\n）
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter a sentence: ");
        String sentence = scanner.nextLine();  // 读取整行文本
        System.out.println("You entered: " + sentence);
        scanner.close();
    }
}

// nextInt() - 读取下一个整数
// 作用：读取下一个整数值 
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter an integer: ");
        int number = scanner.nextInt();  // 读取整数
        System.out.println("You entered: " + number);
        scanner.close();
    }
}
```

**🔸 Scanner类常见功能**
- **hasNext()**：判断是否还有下一个输入（可以是单词、行等）
- **hasNextLine()**：判断是否还有下一行
- **hasNextInt()**：判断下一个输入是否为整数
- **hasNextDouble()**：判断下一个输入是否为双精度浮点数

```java
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter a number: ");
        if (scanner.hasNextInt()) {
            int number = scanner.nextInt();
            System.out.println("You entered: " + number);
        } else {
            System.out.println("That's not a valid number.");
        }
        scanner.close();
    }
}
```

**🔸 跳过不需要的输入**

```java
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Enter text with numbers: ");
        scanner.skip("[^0-9]+");  // 跳过所有非数字字符
        System.out.println("Remaining input: " + scanner.nextLine());
        scanner.close();
    }
}
```

### 1.5 Random类


**🔸 Random类概述**

| **特性** | **说明** |
|---------|----------|
| **所属包** | `java.util`，需手动导入 `import java.util.Random;` |
| **主要用途** | 生成伪随机数（整数、浮点数、布尔值等） |
| **随机数算法** | 基于线性同余算法 (LCG)进行计算 |
| **种子值 (`seed`)** | 若指定相同 `seed`，随机数序列将一致 |
| **非线程安全** | 在多线程环境中建议使用 `ThreadLocalRandom` 或 `SecureRandom` |

**🔸 Random类常用构造方法**

```java
// Random() - 默认构造方法
// 作用：使用当前时间作为种子，创建一个新的 Random 对象
Random random = new Random();  // 使用当前时间戳作为种子
System.out.println(random.nextInt());  // 输出随机整数

// Random(long seed) - 使用指定种子
// 作用：使用指定的种子来创建一个 Random 对象。
// 这样可以确保生成的随机数序列是可重复的（即相同种子下生成的随机数序列相同）
Random random = new Random(12345L);  // 使用指定的种子
System.out.println(random.nextInt());  // 输出随机整数
// 解释：相同的种子 12345L 会生成相同的随机数序列
```

**🔸 Random类常用方法**

```java
Random random = new Random();
System.out.println(random.nextInt());  // 生成一个随机整数

Random random = new Random();
int randomNumber = random.nextInt(100);  // 生成 0 到 99 之间的随机整数
System.out.println(randomNumber);

Random random = new Random();
double randomDouble = random.nextDouble();  // 生成一个 [0.0, 1.0) 范围的随机数
System.out.println(randomDouble);

Random random = new Random();
boolean randomBoolean = random.nextBoolean();  // 生成随机布尔值
System.out.println(randomBoolean);

// 生成随机字节数组
byte[] bytes = new byte[10];
random.nextBytes(bytes);
```

**🔸 随机数流（Java 8+）**

```java
// 生成无限的随机int流
IntStream randomInts = random.ints();
// 生成指定数量的随机int流
IntStream limitedRandomInts = random.ints(5); // 5个随机int
// 生成指定范围和数量的随机int流
IntStream rangedRandomInts = random.ints(5, 1, 101); // 5个1-100之间的随机int
```

**🔸 种子值的影响**

```java
// 具有相同种子的两个Random实例将产生相同的随机数序列
Random random1 = new Random(42);
Random random2 = new Random(42);
for (int i = 0; i < 5; i++) {
    System.out.println(random1.nextInt(100) + " == " + random2.nextInt(100));
}

// 重置种子
Random random = new Random();
// 生成一些随机数...

// 重置种子以重新开始序列
random.setSeed(42);
```

**🔸 Random的替代方案**

```java
//  Math.random()
// 生成0.0到1.0之间的随机double值（不包括1.0）
double random = Math.random();
// 生成指定范围的int值
int min = 5;
int max = 10;
int randomInt = (int)(Math.random() * (max - min)) + min;

// SecureRandom（更安全）
// 创建SecureRandom实例（适用于安全要求较高的场景）
SecureRandom secureRandom = new SecureRandom();
// 使用方法与Random类似
int randomInt = secureRandom.nextInt(100);

// ThreadLocalRandom（更高效）
import java.util.concurrent.ThreadLocalRandom;
// 在多线程环境中使用（比共享Random实例更高效）
int random = ThreadLocalRandom.current().nextInt(100);
```

**🔸 Random类的应用场景**

| **应用场景** | **说明** |
|-------------|----------|
| **生成随机数** | 用于游戏随机事件、模拟实验、测试数据生成等 |
| **随机选择元素** | 生成随机索引用于列表/数组中随机选取元素 |
| **模拟和概率控制** | 适用于概率模拟，如骰子投掷、抽奖、随机路径选择等 |
| **密码学应用** | Random不是密码级安全，但适用于一般随机应用 |

**🔸 Random类的注意事项**

| **注意点** | **说明** |
|-----------|----------|
| **种子值 (`seed`) 影响随机序列** | 如果 `Random` 使用相同种子 (`new Random(seed)`)，则生成的随机数序列相同 |
| **线程安全性** | `Random` 不是线程安全，建议在多线程环境使用 `ThreadLocalRandom` 或 `SecureRandom` |
| **非安全性用途** | 由于 `Random` 生成伪随机数，不适用于密码学级别安全需求，需改用 `SecureRandom` |

**🔸 ThreadLocalRandom类与Random的区别**
- **ThreadLocalRandom** 是为多线程环境设计的，它避免了多线程使用 Random 时的共享问题，通常具有更好的性能
- **Random** 是单线程安全的，但如果在多线程中共享 Random 实例，可能会导致性能下降

```java
// 在多线程环境中使用 ThreadLocalRandom
int randomValue = ThreadLocalRandom.current().nextInt(0, 100);  // 生成 0 到 100 之间的随机整数
System.out.println(randomValue);
```

---

## 2. 📅 时间日期处理


### 2.1 Date类


```java
// 创建 Date 对象
Date date = new Date();  // 获取当前时间

// 获取时间戳（毫秒）
long timestamp = now.getTime();

// 获取当前时间戳
long timestamp = System.currentTimeMillis();  // 返回当前时间的毫秒值

// 使用时间戳创建Date
Date dateFromTimestamp = new Date(timestamp);

// 格式化日期（使用 SimpleDateFormat）
SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
String formattedDate = sdf.format(new Date());

// 比较日期
Date date1 = new Date();
Date date2 = new Date();
boolean isBefore = date1.before(date2);  // 判断 date1 是否在 date2 之前
boolean isAfter = date1.after(date2);    // 判断 date1 是否在 date2 之后

// 转换为字符串（使用 toString()）
String dateStr = new Date().toString();  // 输出日期的字符串表示

// 设置日期
Date date = new Date();
date.setTime(1633036800000L);  // 设置为特定时间戳
```

### 2.2 Calendar类


```java
// 获取 Calendar 实例
Calendar calendar = Calendar.getInstance();  // 获取当前日期和时间的 Calendar 实例

// 转换为 Date 对象
Date date = calendar.getTime();  // 获取 Calendar 对象对应的 Date 对象

// 使用Date设置Calendar
Date date = new Date();
calendar.setTime(date);

// 获取和设置日期
int year = calendar.get(Calendar.YEAR);  // 获取当前年份
int month = calendar.get(Calendar.MONTH);  // 获取当前月份 (注意：0-11, 1月是 0)
int day = calendar.get(Calendar.DAY_OF_MONTH);  // 获取当前日
calendar.set(Calendar.YEAR, 2025);  // 设置年份为 2025
calendar.set(Calendar.MONTH, Calendar.JANUARY);  // 设置为 1月

// 设置年月日时分秒
calendar.set(Calendar.YEAR, 2023);
calendar.set(Calendar.MONTH, Calendar.SEPTEMBER);  // 月份从0开始计数
calendar.set(Calendar.DAY_OF_MONTH, 15);
calendar.set(Calendar.HOUR_OF_DAY, 14);  // 24小时制
calendar.set(Calendar.MINUTE, 30);
calendar.set(Calendar.SECOND, 0);

// 一次性设置年月日
calendar.set(2023, Calendar.SEPTEMBER, 15);

// 日期加减操作
calendar.add(Calendar.DATE, 5);  // 当前日期加 5 天
calendar.add(Calendar.MONTH, -1);  // 当前日期减 1 个月

// 获取当前时间戳
long timestamp = calendar.getTimeInMillis();  // 获取当前时间的毫秒表示

// 获取日期组件
int year = calendar.get(Calendar.YEAR);
int month = calendar.get(Calendar.MONTH);            // 0-11表示1-12月
int day = calendar.get(Calendar.DAY_OF_MONTH);
int hour = calendar.get(Calendar.HOUR_OF_DAY);       // 24小时制
int minute = calendar.get(Calendar.MINUTE);
int second = calendar.get(Calendar.SECOND);
int dayOfWeek = calendar.get(Calendar.DAY_OF_WEEK);  // 1(周日)到7(周六)

// 日期比较
Calendar calendar1 = Calendar.getInstance();
Calendar calendar2 = Calendar.getInstance();
boolean isBefore = calendar1.before(calendar2);  // 判断 calendar1 是否在 calendar2 之前
boolean isAfter = calendar1.after(calendar2);    // 判断 calendar1 是否在 calendar2 之后
```

### 2.3 SimpleDateFormat类


```java
// 创建 SimpleDateFormat 实例
SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");  // 定义日期格式

// 格式化日期
Date date = new Date();
String formattedDate = sdf.format(date);  // 将 Date 转换为指定格式的字符串

// Date 转字符串
Date date = new Date();
String dateStr = sdf.format(date);  // 例如: "2023-09-15 14:30:00"

// 解析字符串为日期
String dateString = "2025-04-28 15:30:00";
try {
    Date date = sdf.parse(dateString);  // 将字符串解析为 Date 对象
} catch (ParseException e) {
    e.printStackTrace();
}

// 更改日期格式
SimpleDateFormat newSdf = new SimpleDateFormat("dd/MM/yyyy");  // 设置不同的格式
String newFormattedDate = newSdf.format(new Date());

// 常用日期格式模式
SimpleDateFormat dateOnly = new SimpleDateFormat("yyyy-MM-dd");
SimpleDateFormat timeOnly = new SimpleDateFormat("HH:mm:ss");
SimpleDateFormat dateTimeWithMillis = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss.SSS");
SimpleDateFormat customFormat = new SimpleDateFormat("yyyy年MM月dd日 HH时mm分ss秒");

// 设置时区
SimpleDateFormat sdfWithTZ = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
sdfWithTZ.setTimeZone(TimeZone.getTimeZone("GMT+8"));  // 设置为东八区

// 线程安全使用方式 (SimpleDateFormat 非线程安全)
private static final ThreadLocal<SimpleDateFormat> dateFormatThreadLocal = 
    ThreadLocal.withInitial(() -> new SimpleDateFormat("yyyy-MM-dd HH:mm:ss"));

// 然后在多线程环境中这样使用:
SimpleDateFormat sdfThreadSafe = dateFormatThreadLocal.get();
String threadSafeDateStr = sdfThreadSafe.format(new Date());

// 注意: SimpleDateFormat 不是线程安全的
// 在多线程环境中应该使用 ThreadLocal 或 Java 8+ 的 DateTimeFormatter 替代
```

### 2.4 DateTimeFormatter类


```java
// 创建 DateTimeFormatter 实例
LocalDateTime now = LocalDateTime.now();
String formattedDate = formatter.format(now);  // 将 LocalDateTime 格式化为字符串

// 格式化日期时间
LocalDateTime now = LocalDateTime.now();
String formattedDate = formatter.format(now);  // 将 LocalDateTime 格式化为字符串

// 常用内置格式器
DateTimeFormatter isoFormatter = DateTimeFormatter.ISO_LOCAL_DATE_TIME;  // 内置 ISO 格式

// 创建 DateTimeFormatter
DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");

// 使用预定义格式
DateTimeFormatter isoDate = DateTimeFormatter.ISO_DATE;          // 2023-09-15
DateTimeFormatter isoDateTime = DateTimeFormatter.ISO_DATE_TIME; // 2023-09-15T14:30:00
DateTimeFormatter basicIsoDate = DateTimeFormatter.BASIC_ISO_DATE; // 20230915

// 日期时间类型转字符串
LocalDate date = LocalDate.now();
LocalTime time = LocalTime.now();
LocalDateTime dateTime = LocalDateTime.now();
String dateStr = date.format(DateTimeFormatter.ofPattern("yyyy-MM-dd"));
String timeStr = time.format(DateTimeFormatter.ofPattern("HH:mm:ss"));
String dateTimeStr = dateTime.format(formatter);

// 字符串转日期时间类型
String dateString = "2025-04-28 15:30:00";
LocalDateTime dateTime = LocalDateTime.parse(dateString, formatter);  // 将字符串解析为 LocalDateTime
LocalDate parsedDate = LocalDate.parse("2023-09-15", DateTimeFormatter.ofPattern("yyyy-MM-dd"));
LocalTime parsedTime = LocalTime.parse("14:30:00", DateTimeFormatter.ofPattern("HH:mm:ss"));
LocalDateTime parsedDateTime = LocalDateTime.parse("2023-09-15 14:30:00", formatter);

// 自定义格式
DateTimeFormatter customFormatter = DateTimeFormatter.ofPattern("yyyy年MM月dd日 HH时mm分ss秒");
String customFormatted = dateTime.format(customFormatter);

// 设置地区
DateTimeFormatter formatterWithLocale = DateTimeFormatter.ofPattern("yyyy-MM-dd").withLocale(Locale.CHINA);

// 设置时区
DateTimeFormatter formatterWithZone = formatter.withZone(ZoneId.of("Asia/Shanghai"));

// 格式化带时区的日期时间
ZonedDateTime zonedDateTime = ZonedDateTime.now();
String zonedDateTimeStr = zonedDateTime.format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss z"));

// DateTimeFormatter 线程安全
```

### 2.5 LocalDate类


```java
// 获取当前日期
LocalDate today = LocalDate.now();

// 指定日期
LocalDate date = LocalDate.of(2025, 4, 28);  // 指定年、月、日
LocalDate date2 = LocalDate.of(2023, Month.SEPTEMBER, 15);

// 获取年月日
int year = today.getYear();
int month = today.getMonthValue();  // 1-12
int day = today.getDayOfMonth();

// 加减日期
LocalDate nextWeek = today.plusWeeks(1);  // 加一周
LocalDate lastMonth = today.minusMonths(1);  // 减一个月

// 日期比较
boolean isBefore = today.isBefore(date);
boolean isAfter = today.isAfter(date);

// 字符串解析与格式化
LocalDate parsedDate = LocalDate.parse("2025-04-28");  // 按默认ISO格式解析
LocalDate customParsed = LocalDate.parse("2023/09/15", DateTimeFormatter.ofPattern("yyyy/MM/dd"));

// 获取年月日
int year = date.getYear();        // 2023
int month = date.getMonthValue(); // 9
int day = date.getDayOfMonth();   // 15
DayOfWeek dayOfWeek = date.getDayOfWeek();  // FRIDAY
int dayOfYear = date.getDayOfYear();        // 258

// 日期计算
LocalDate tomorrow = today.plusDays(1);
LocalDate nextWeek = today.plusWeeks(1);
LocalDate nextMonth = today.plusMonths(1);
LocalDate nextYear = today.plusYears(1);

LocalDate yesterday = today.minusDays(1);
LocalDate lastWeek = today.minusWeeks(1);
LocalDate lastMonth = today.minusMonths(1);
LocalDate lastYear = today.minusYears(1);

// 日期修改
LocalDate firstDayOfMonth = today.withDayOfMonth(1);
LocalDate withYear = today.withYear(2025);

// 获取特定日期
LocalDate firstDayOfYear = today.with(TemporalAdjusters.firstDayOfYear());
LocalDate lastDayOfMonth = today.with(TemporalAdjusters.lastDayOfMonth());
LocalDate nextMonday = today.with(TemporalAdjusters.next(DayOfWeek.MONDAY));

// 日期比较
boolean isBefore = date1.isBefore(date2);
boolean isAfter = date1.isAfter(date2);
boolean isEqual = date1.isEqual(date2);

// 计算日期间隔
Period period = Period.between(date1, date2);
int years = period.getYears();
int months = period.getMonths();
int days = period.getDays();
long totalDays = ChronoUnit.DAYS.between(date1, date2);

// 格式化日期
String formatted = today.format(DateTimeFormatter.ofPattern("yyyy-MM-dd"));
String formatted2 = today.format(DateTimeFormatter.ISO_DATE);

// 检查闰年
boolean isLeapYear = today.isLeapYear();

// LocalDate 表示不带时间的日期，它是不可变的且线程安全，适合处理生日、纪念日等纯日期信息 
```

### 2.6 LocalTime类


```java
// 获取当前时间
LocalTime now = LocalTime.now();

// 创建特定时间
LocalTime time = LocalTime.of(14, 30, 15);            // 时分秒
LocalTime time2 = LocalTime.of(14, 30, 15, 500000000); // 时分秒纳秒

// 获取时分秒
int hour = time.getHour();      // 14
int minute = time.getMinute();  // 30
int second = time.getSecond();  // 15
int nano = time.getNano();      // 0

// 加减时间
LocalTime later = now.plusHours(2);  // 加2小时
LocalTime earlier = now.minusMinutes(15);  // 减15分钟

// 时间比较
boolean isBefore = now.isBefore(time);
boolean isAfter = now.isAfter(time);

// 字符串解析与格式化
LocalTime parsedTime = LocalTime.parse("14:30:00");  // 默认ISO格式
LocalTime customParsed = LocalTime.parse("14:30:15", DateTimeFormatter.ofPattern("HH:mm:ss"));

// 格式化时间
String formatted = now.format(DateTimeFormatter.ofPattern("HH:mm:ss"));
String formatted2 = now.format(DateTimeFormatter.ISO_TIME);

// 检查是否在特定时间范围内
boolean isInRange = time.isAfter(LocalTime.of(9, 0)) && 
                    time.isBefore(LocalTime.of(17, 0));

// 计算时间间隔
long hoursBetween = ChronoUnit.HOURS.between(time1, time2);
long minutesBetween = ChronoUnit.MINUTES.between(time1, time2);
long secondsBetween = ChronoUnit.SECONDS.between(time1, time2);

// 时间修改
LocalTime withHour = now.withHour(10);
LocalTime withMinute = now.withMinute(15);
LocalTime withSecond = now.withSecond(30);
LocalTime withNano = now.withNano(500000000);

// 获取一天的特定时间点
LocalTime midnight = LocalTime.MIDNIGHT;  // 00:00
LocalTime noon = LocalTime.NOON;          // 12:00
LocalTime min = LocalTime.MIN;            // 00:00
LocalTime max = LocalTime.MAX;            // 23:59:59.999999999

// 时间计算
LocalTime later = now.plusHours(1);
LocalTime later2 = now.plusMinutes(30);
LocalTime later3 = now.plusSeconds(20);
LocalTime later4 = now.plusNanos(1000000);

LocalTime earlier = now.minusHours(1);
LocalTime earlier2 = now.minusMinutes(30);
LocalTime earlier3 = now.minusSeconds(20);

// LocalTime 只表示时间（时、分、秒）;线程安全，它是不可变的且线程安全，适合处理一天中的时间点 
```

### 2.7 LocalDateTime类


```java
// 获取当前日期时间
LocalDateTime now = LocalDateTime.now();

// 创建特定日期时间
LocalDateTime dateTime = LocalDateTime.of(2023, 9, 15, 14, 30, 15); // 年,月,日,时,分,秒
LocalDateTime dateTime2 = LocalDateTime.of(LocalDate.of(2023, 9, 15), LocalTime.of(14, 30, 15));

// 从字符串解析
LocalDateTime parsed = LocalDateTime.parse("2023-09-15T14:30:15");
LocalDateTime customParsed = LocalDateTime.parse(
    "2023-09-15 14:30:15", 
    DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")
);

// 获取日期和时间部分
LocalDate date = now.toLocalDate();
LocalTime time = now.toLocalTime();

// 获取年月日时分秒
int year = now.getYear();
int month = now.getMonthValue();
int day = now.getDayOfMonth();
int hour = now.getHour();
int minute = now.getMinute();
int second = now.getSecond();
DayOfWeek dayOfWeek = now.getDayOfWeek();

// 加减日期时间
LocalDateTime future = now.plusDays(1).plusHours(2);  // 加1天2小时
LocalDateTime past = now.minusWeeks(1);  // 减1周

// 日期时间计算
LocalDateTime future = now.plusDays(1).plusHours(2);
LocalDateTime past = now.minusDays(1).minusHours(2);
LocalDateTime plusDateTime = now.plus(1, ChronoUnit.DAYS);

// 修改日期时间的某个部分
LocalDateTime withYear = now.withYear(2025);
LocalDateTime withMonth = now.withMonth(10);
LocalDateTime withDayOfMonth = now.withDayOfMonth(20);
LocalDateTime withHour = now.withHour(10);

// 使用时间调整器
LocalDateTime firstDayOfMonth = now.with(TemporalAdjusters.firstDayOfMonth());
LocalDateTime lastDayOfYear = now.with(TemporalAdjusters.lastDayOfYear());
LocalDateTime nextMonday = now.with(TemporalAdjusters.next(DayOfWeek.MONDAY));

// 日期时间比较
boolean isBefore = dateTime1.isBefore(dateTime2);
boolean isAfter = dateTime1.isAfter(dateTime2);
boolean isEqual = dateTime1.isEqual(dateTime2);

// 计算间隔
long days = ChronoUnit.DAYS.between(dateTime1, dateTime2);
long hours = ChronoUnit.HOURS.between(dateTime1, dateTime2);
long minutes = ChronoUnit.MINUTES.between(dateTime1, dateTime2);

// 格式化日期时间
String formatted = now.format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
String formatted2 = now.format(DateTimeFormatter.ISO_DATE_TIME);

// 字符串解析与格式化
LocalDateTime parsedDateTime = LocalDateTime.parse("2025-04-28T14:30:00");  // 默认ISO格式

// 转换为时间戳
long millis = now.atZone(ZoneId.systemDefault()).toInstant().toEpochMilli();

// 从时间戳创建
LocalDateTime fromEpochMilli = LocalDateTime.ofInstant(
    Instant.ofEpochMilli(millis), 
    ZoneId.systemDefault()
);

// LocalDateTime 表示日期和时间的组合，它不包含时区信息，是不可变的且线程安全，适合存储特定时间点的信息。
```

### 2.8 ZonedDateTime类


```java
// 获取当前日期时间(系统默认时区)
ZonedDateTime now = ZonedDateTime.now();

// 获取特定时区的当前日期时间
ZonedDateTime nowInTokyo = ZonedDateTime.now(ZoneId.of("Asia/Tokyo"));

// 创建特定日期时间和时区
ZonedDateTime dateTime = ZonedDateTime.of(2023, 9, 15, 14, 30, 0, 0, ZoneId.of("Europe/Paris"));
ZonedDateTime dateTime2 = ZonedDateTime.of(LocalDateTime.of(2023, 9, 15, 14, 30), ZoneId.of("Europe/Paris"));

// 从字符串解析
ZonedDateTime parsed = ZonedDateTime.parse("2023-09-15T14:30:00+02:00[Europe/Paris]");
ZonedDateTime customParsed = ZonedDateTime.parse(
    "2023-09-15 14:30:00 Europe/Paris", 
    DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss z")
);

// 加减时间
ZonedDateTime future = now.plusHours(5);
ZonedDateTime past = now.minusDays(2);

// 获取组件
LocalDate date = now.toLocalDate();
LocalTime time = now.toLocalTime();
LocalDateTime localDateTime = now.toLocalDateTime();
ZoneId zone = now.getZone();
ZoneOffset offset = now.getOffset();

// 日期时间计算(保持时区不变)
ZonedDateTime future = now.plusDays(1).plusHours(2);
ZonedDateTime past = now.minusDays(1).minusHours(2);

// 获取年月日时分秒和时区
int year = now.getYear();
int month = now.getMonthValue();
int day = now.getDayOfMonth();
ZoneId zone = now.getZone();  // 获取时区

// 修改日期时间的某个部分
ZonedDateTime withYear = now.withYear(2025);
ZonedDateTime withMonth = now.withMonth(10);
ZonedDateTime withDayOfMonth = now.withDayOfMonth(20);
ZonedDateTime withHour = now.withHour(10);

// 时区转换
ZonedDateTime tokyoTime = now.withZoneSameInstant(ZoneId.of("Asia/Tokyo"));
ZonedDateTime newYorkTime = now.withZoneSameInstant(ZoneId.of("America/New_York"));

// 日期时间比较
boolean isBefore = dateTime1.isBefore(dateTime2);
boolean isAfter = dateTime1.isAfter(dateTime2);
boolean isEqual = dateTime1.isEqual(dateTime2);

// 计算间隔
long days = ChronoUnit.DAYS.between(dateTime1, dateTime2);
long hours = ChronoUnit.HOURS.between(dateTime1, dateTime2);

// 格式化
String formatted = now.format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss z"));
String formatted2 = now.format(DateTimeFormatter.ISO_ZONED_DATE_TIME);

// 转换为Instant和时间戳
Instant instant = now.toInstant();
long millis = instant.toEpochMilli();

// 从时间戳创建
ZonedDateTime fromEpochMilli = ZonedDateTime.ofInstant(
    Instant.ofEpochMilli(millis), 
    ZoneId.systemDefault()
);

// 处理夏令时
boolean isDST = now.getZone().getRules().isDaylightSavings(now.toInstant());

// ZonedDateTime = 日期 + 时间 + 时区，支持完整的全球时间处理 
// 适合处理跨时区的时间点以及计算不同时区之间的时间转换 
// 线程安全，常配合 DateTimeFormatter 使用
```

### 2.9 Duration类


```java
// 创建Duration
Duration duration = Duration.ofDays(1);         // 1天
Duration duration1 = Duration.ofHours(2);       // 2小时
Duration duration2 = Duration.ofMinutes(30);    // 30分钟
Duration duration3 = Duration.ofSeconds(45);    // 45秒
Duration duration4 = Duration.ofMillis(200);    // 200毫秒
Duration duration5 = Duration.ofNanos(1000);    // 1000纳秒

// 从时间单位创建
Duration duration6 = Duration.of(2, ChronoUnit.DAYS);  // 2天

// 从两个时间点创建
Duration between1 = Duration.between(LocalTime.of(8, 0), LocalTime.of(12, 0));  // 4小时
Duration between2 = Duration.between(Instant.now(), Instant.now().plusSeconds(60));  // 60秒
Duration between3 = Duration.between(LocalDateTime.now(), LocalDateTime.now().plusHours(3));  // 3小时

// 计算两个时间差
LocalTime start = LocalTime.of(10, 0);
LocalTime end = LocalTime.of(12, 30);
Duration diff = Duration.between(start, end);  // 计算时间差

// 加减 Duration
Duration added = duration.plusMinutes(30);  // 加30分钟
Duration subtracted = duration.minusSeconds(600);  // 减600秒

// 判断是否为负数/是否为零
boolean isNegative = duration.isNegative();
boolean isZero = duration.isZero();

// 解析字符串
Duration parsed = Duration.parse("P1DT2H30M");  // 1天2小时30分钟
Duration parsed2 = Duration.parse("PT15M");     // 15分钟

// 获取时间量
long days = duration.toDays();
long hours = duration.toHours();
long minutes = duration.toMinutes();
long seconds = duration.getSeconds();
long millis = duration.toMillis();
long nanos = duration.toNanos();

// 提取部分时间量
long hoursPart = duration.toHoursPart();
long minutesPart = duration.toMinutesPart();
long secondsPart = duration.toSecondsPart();
int nanosPart = duration.toNanosPart();

// 计算操作
Duration added = duration1.plus(duration2);

// Duration 表示时间量（精确到秒或纳秒），常用于计算时间差或进行时间运算
// 主要用于 LocalTime、LocalDateTime、Instant 等时间类型之间的差值处理
```

### 2.10 Period类


```java
// 创建Period对象
Period period1 = Period.ofYears(1);           // 1年
Period period2 = Period.ofMonths(6);          // 6个月
Period period3 = Period.ofWeeks(2);           // 2周(14天)
Period period4 = Period.ofDays(10);           // 10天
Period period5 = Period.of(1, 6, 15);         // 1年6个月15天

// 从字符串解析
Period parsed = Period.parse("P1Y6M15D");     // 1年6个月15天

// 计算两个日期之间的Period
Period between = Period.between(
    LocalDate.of(2022, 1, 1), 
    LocalDate.of(2023, 6, 15)
);  // 1年5个月14天

LocalDate startDate = LocalDate.of(2024, 1, 1);
LocalDate endDate = LocalDate.of(2025, 4, 28);
Period between = Period.between(startDate, endDate);  // 计算年/月/日差

// 获取年、月、日
int years = period.getYears();
int months = period.getMonths();
int days = period.getDays();

// 获取Period中的值
int years = period.getYears();
int months = period.getMonths();
int days = period.getDays(); 

// 修改Period
Period withYears = period.withYears(2);
Period withMonths = period.withMonths(3);
Period withDays = period.withDays(20);

// 加减计算操作
Period added = period1.plus(period2);
Period subtracted = period1.minus(period2);
Period multiplied = period1.multipliedBy(2);

Period added = period.plusMonths(3);  // 加3个月
Period subtracted = period.minusDays(10);  // 减10天

// 判断Period
boolean isZero = period.isZero();
boolean isNegative = period.isNegative();

// 正规化Period
Period normalized = period.normalized();  // 将月标准化为年(如果可能)

// 应用Period到日期
LocalDate date = LocalDate.of(2023, 1, 1);
LocalDate newDate = date.plus(period);

// Period 表示基于年/月/日的时间量（适合处理日历日期差）。
// 常用于 LocalDate 之间的时间计算，比如年龄、到期日等
```

### 2.11 Instant类


```java
// 获取当前时间戳（UTC时间）
Instant now = Instant.now();

// 从纪元秒(1970-01-01T00:00:00Z)创建
Instant instant1 = Instant.ofEpochSecond(1631700000);           // 秒
Instant instant2 = Instant.ofEpochSecond(1631700000, 500000000); // 秒+纳秒
Instant instant3 = Instant.ofEpochMilli(1631700000000L);        // 毫秒
Instant fromEpochMilli = Instant.ofEpochMilli(System.currentTimeMillis());  // 从毫秒

// 从字符串解析
Instant parsed = Instant.parse("2023-09-15T10:15:30.00Z");

// 获取时间戳
long epochSecond = now.getEpochSecond();  // 秒
long epochMilli = now.toEpochMilli();     // 毫秒
int nano = now.getNano();                 // 纳秒部分

// 时间计算
Instant future = now.plusSeconds(3600);    // 加1小时
Instant future2 = now.plusMillis(1800000); // 加30分钟
Instant future3 = now.plusNanos(1000000);  // 加1毫秒

Instant past = now.minusSeconds(3600);     // 减1小时
Instant past2 = now.minusMillis(1800000);  // 减30分钟
Instant past3 = now.minusNanos(1000000);   // 减1毫秒

// 使用Duration进行计算
Instant later = now.plus(Duration.ofHours(2));
Instant earlier = now.minus(Duration.ofHours(2));

// 时间比较
boolean isBefore = instant1.isBefore(instant2);
boolean isAfter = instant1.isAfter(instant2);
int compareResult = instant1.compareTo(instant2);

// 计算两个时间点之间的差异
Duration duration = Duration.between(instant1, instant2);
long secondsBetween = duration.getSeconds();
long millisBetween = duration.toMillis();

// 转换为其他日期时间类型
LocalDateTime dateTime = LocalDateTime.ofInstant(now, ZoneId.systemDefault());
ZonedDateTime zonedDateTime = Instant.now().atZone(ZoneId.of("Europe/Paris"));
OffsetDateTime offsetDateTime = Instant.now().atOffset(ZoneOffset.of("+02:00"));

// 检查是否在给定时间范围内
boolean isInRange = now.isAfter(start) && now.isBefore(end);

// 获取特殊时间点
Instant epoch = Instant.EPOCH;  // 1970-01-01T00:00:00Z

// Instant 表示绝对时间点（以 UTC 为标准），适合处理时间戳
// 常用于高精度时间计算、存储时间戳、系统日志记录等
```

---

## 3. 🔤 字符串相关


### 3.1 String类


**🔸 String类常见用法**

```java
// 创建字符串
String str1 = "Hello";                     // 字符串字面量
String str2 = new String("Hello");         // 使用构造函数
String str3 = String.valueOf(123);         // 从其他类型转换
String str4 = String.join(", ", "A", "B", "C"); // 连接多个字符串

// 长度和判空
int length = str1.length();                // 获取长度
boolean isEmpty = str1.isEmpty();          // 是否为空字符串
boolean isBlank = str1.isBlank();          // 是否为空白字符串(Java 11+)

// 比较
boolean equals = str1.equals(str2);        // 比较内容是否相等
boolean equalsIgnoreCase = str1.equalsIgnoreCase("hello"); // 忽略大小写比较
int compResult = str1.compareTo(str2);     // 按字典顺序比较

// 查找
char charAt = str1.charAt(1);              // 获取指定位置字符
int indexOf = str1.indexOf('e');           // 查找字符首次出现位置
int indexOf2 = str1.indexOf("el");         // 查找子串首次出现位置
int indexOf3 = str1.indexOf('l', 3);       // 从指定位置开始查找
int lastIndexOf = str1.lastIndexOf('l');   // 查找字符最后出现位置
boolean contains = str1.contains("el");    // 是否包含子串
boolean startsWith = str1.startsWith("He"); // 是否以指定前缀开始
boolean endsWith = str1.endsWith("lo");    // 是否以指定后缀结束

// 截取
String substring = str1.substring(1);      // 从索引1到末尾的子串
String substring2 = str1.substring(1, 4);  // 从索引1到3的子串

// 修改
String upper = str1.toUpperCase();         // 转大写
String lower = str1.toLowerCase();         // 转小写
String trimmed = "  Hello  ".trim();       // 去除前后空白
String stripped = "  Hello  ".strip();     // 去除前后空白(支持Unicode空白,Java 11+)
String replaced = str1.replace('e', 'a');  // 替换所有指定字符
String replaced2 = str1.replace("el", "AL"); // 替换所有指定子串
String replaced3 = str1.replaceAll("e.", "X"); // 正则替换

// 分割与合并
String[] split = "a,b,c".split(",");       // 分割字符串
String result = "Hello" + " " + "World";  // 字符串拼接
String joined = String.join("-", split);   // 合并字符串数组

// 转换
char[] chars = str1.toCharArray();         // 转换为字符数组
byte[] bytes = str1.getBytes();            // 转换为字节数组
byte[] bytes2 = str1.getBytes("UTF-8");    // 指定编码转换为字节数组

// 格式化(静态方法)
String formatted = String.format("Name: %s, Age: %d", "John", 30);
String formatted2 = "Name: %s, Age: %d".formatted("John", 30); // 实例方法(Java 15+)

// StringBuilder/StringBuffer替代频繁字符串连接
StringBuilder sb = new StringBuilder();
sb.append("Hello").append(" ").append("World");
String result = sb.toString();

// 多行字符串(Java 15+)
String multiline = """
    Hello
    World
    """;

// String 是不可变的，所有修改操作都会创建新字符串
// 对于频繁的字符串操作，应使用 StringBuilder(非线程安全)或 StringBuffer(线程安全)以提高性能
```

**🔸 String类的创建情况与内存分析**

```java
// 字符串常量池
// 创建方式：在 Java 中，字符串常量池（String Pool）是一个特殊的内存区域，用于存储字面量字符串
// 通过字面量创建的字符串，会自动放入常量池中
// 由于常量池的存在，相同的字面量会共享同一个对象，而不是每次创建新对象
// 字符串字面量（例如 String str = "hello";）存储在常量池中。常量池是由 JVM 维护的，属于方法区的内存区域
String str1 = "Hello";  // 字面量方式创建，"Hello" 会被存入常量池
String str2 = "Hello";  // 直接引用常量池中的 "Hello"

// 通过 new 创建字符串
// 创建方式： 使用 new 关键字创建字符串时，字符串不会直接放入常量池
// 相反，它会在堆内存中创建一个新的 String 对象
// 即使 "Hello" 已经存在于常量池中，new 关键字创建的字符串会在堆内存中创建一个新的 String 对象
// 使用 new 关键字创建的字符串对象存储在堆内存中，并且可以通过该对象引用常量池中的字符串
String str3 = new String("Hello");  // 通过 new 创建，会在堆内存中创建新对象

// 通过 new 创建的字符串对象会被存储在堆内存中，但它会首先检查常量池中是否存在该字符串
// 如果常量池中已有该字面量，则 new 会创建一个新的引用指向常量池中的字符串，而不是重新存储它； 例如：
String str1 = "Hello";  // 在常量池中
String str2 = new String("Hello");  // 在堆中创建新的对象，str2 会引用常量池中的 "Hello"
```

### 3.2 StringBuilder类


**🔸 StringBuilder类常见用法**

```java
// 创建StringBuilder
StringBuilder sb = new StringBuilder();  // 空builder，默认容量16，扩容策略是(capacity*2)+2
StringBuilder sb2 = new StringBuilder("Hello");   // 初始内容为"Hello"
StringBuilder sb3 = new StringBuilder(50);        // 指定初始容量

// 添加内容
sb.append("Hello");                    // 添加字符串
sb.append(123);                        // 添加数字
sb.append(true);                       // 添加布尔值
sb.append('A');                        // 添加字符
sb.append(new char[]{'a', 'b', 'c'});  // 添加字符数组
sb.append("World").append(123);        // 链式调用

// 插入内容
sb.insert(5, " World");                // 在指定位置插入
sb.insert(0, "Start: ");               // 在开头插入

// 删除内容
sb.deleteCharAt(0);                    // 删除指定位置字符
sb.delete(0, 5);                       // 删除指定范围的字符[0,5)

// 替换内容
sb.replace(0, 5, "Hi");                // 替换指定范围的字符

// 反转
sb.reverse();                          // 反转整个字符串

// 设置长度
sb.setLength(10);                      // 截断或用null填充至指定长度

// 获取长度和容量
int length = sb.length();              // 当前长度
int capacity = sb.capacity();          // 当前容量

// 确保容量
sb.ensureCapacity(100);                // 确保至少有指定容量

// 获取/设置单个字符
char c = sb.charAt(1);                 // 获取指定位置的字符
sb.setCharAt(1, 'X');                  // 设置指定位置的字符

// 获取子串
String substring = sb.substring(2);    // 获取从索引2到末尾的子串
String substring2 = sb.substring(2, 5); // 获取索引2到4的子串

// 查找
int index = sb.indexOf("World");       // 查找子串的首次出现位置
int lastIndex = sb.lastIndexOf("o");   // 查找子串的最后出现位置

// 转换为String
String result = sb.toString();         // 转换为不可变的String对象

// StringBuilder 用于可变的字符串操作，比 String 更高效，尤其在频繁拼接字符串时
```

**🔸 注意事项和易错点**

```java
// 注意事项：StringBuilder 是非线程安全的
// 如果多个线程同时访问同一个 StringBuilder 实例并修改它，可能会导致数据不一致
StringBuffer sb = new StringBuffer(); // 多线程环境下可以考虑使用 StringBuffer

// 容量管理
// 易错点：StringBuilder 默认初始容量为 16。如果频繁拼接很长的字符串，可能导致多次扩容，从而增加不必要的内存开销
// 可以通过在构造时指定初始容量来避免这种情况
StringBuilder sb = new StringBuilder(100);  // 设置初始容量为 100
```

### 3.3 StringBuffer类


```java
// StringBuffer 与 StringBuilder 的API完全相同
// 但StringBuffer是线程安全的(同步的)，适用于多线程环境
// 单线程场景用StringBuilder更高效，StringBuffer方法加了synchronized

// 共享对象的同步问题
// 方法同步不保证对象引用共享的安全性
// 需要额外同步来确保引用安全

// 线程安全的边界
// 仅单个方法是线程安全的，多个方法组合操作不保证原子性
StringBuffer sb = new StringBuffer("Hello");
// 线程A和线程B同时执行这些操作时可能相互干扰:
// 线程A: if(sb.length() > 0) { sb.delete(0, sb.length()); }
// 线程B: sb.append("World");
```

### 3.4 StringJoiner类


**🔸 基本用法**

```java
// 基本创建与使用
StringJoiner joiner = new StringJoiner(", ");          // 只指定分隔符
joiner.add("Apple").add("Banana").add("Orange");       // 添加元素
String result = joiner.toString();                     // 结果: "Apple, Banana, Orange"

// 指定前缀和后缀
StringJoiner joiner2 = new StringJoiner(", ", "[", "]"); // 分隔符、前缀、后缀
joiner2.add("Apple").add("Banana").add("Orange");
String result2 = joiner2.toString();                   // 结果: "[Apple, Banana, Orange]"

// 空StringJoiner
StringJoiner empty = new StringJoiner(",");
String emptyResult = empty.toString();                 // 结果: "" (空字符串)

// 设置空值的默认结果
StringJoiner joiner3 = new StringJoiner(",", "[", "]");
joiner3.setEmptyValue("EMPTY");                        // 设置空值的默认结果
String emptyResult2 = joiner3.toString();              // 结果: "EMPTY"

// 获取长度
int length = joiner.length();                          // 字符串的总长度

// 合并两个StringJoiner
StringJoiner joiner4 = new StringJoiner(", ");
joiner4.add("Red").add("Green");
StringJoiner joiner5 = new StringJoiner(", ");
joiner5.add("Blue").add("Yellow");
StringJoiner merged = joiner4.merge(joiner5);          // 合并两个joiner
String mergedResult = merged.toString();               // 结果: "Red, Green, Blue, Yellow"

// 与其他集合结合
List<String> fruits = Arrays.asList("Apple", "Banana", "Cherry");
StringJoiner sj3 = new StringJoiner(", ");
fruits.forEach(sj3::add);  // 遍历集合并添加到 StringJoiner 中

// 使用String.join简化(Java 8+)
String joined = String.join(", ", "Apple", "Banana", "Orange"); // 结果: "Apple, Banana, Orange"
String joined2 = String.join(", ", Arrays.asList("Apple", "Banana")); // 从集合创建

// StringJoiner 是 Java 8 引入的用于高效连接字符串的工具类，特别适合需要分隔符、前缀和后缀的场景
```

**🔸 注意事项和易错点**

```java
// 1. 前缀后缀仅应用一次
StringJoiner joiner = new StringJoiner(", ", "[", "]");
joiner.add("A").add("B");
// 结果是"[A, B]"而非"[A], [B]"

// 2. 空StringJoiner的行为
StringJoiner joiner = new StringJoiner(",", "[", "]");
String result = joiner.toString(); // 结果是"[]"而非""
// 使用setEmptyValue可修改空值行为
joiner.setEmptyValue("EMPTY");

// 3. null值处理
StringJoiner joiner = new StringJoiner(",");
// joiner.add(null); // NullPointerException!
// 正确做法:
joiner.add("null"); // 或通过判断处理null

// 4. merge方法不改变原对象
StringJoiner j1 = new StringJoiner(",");
StringJoiner j2 = new StringJoiner(";");
StringJoiner merged = j1.merge(j2);
// merged != j1 且 merged != j2
// 应使用: j1 = j1.merge(j2);

// 5. setEmptyValue与前缀后缀关系
StringJoiner sj = new StringJoiner(",", "[", "]");
sj.setEmptyValue("EMPTY");
// 当没有元素时返回"EMPTY"而非"[]"
// 一旦添加元素，前缀后缀就会生效

// 6. 合并不同分隔符的StringJoiner
StringJoiner j1 = new StringJoiner(",");
StringJoiner j2 = new StringJoiner(";");
// 合并后使用第一个joiner的分隔符、前缀和后缀

// 7. length()返回结果字符串长度，不是元素数量
// 要获取元素数量，需自行计数

// 8. 与Collectors.joining的区别
// joiner = 手动构建
// Collectors.joining = 流操作的终端操作

// 关键注意点：添加null值会抛异常；merge返回新对象而不修改原对象；空Joiner的默认输出包含前缀后缀；
// 合并保留第一个Joiner的分隔符和前后缀；length()是字符长度不是元素数量。
```

---

## 4. 📦 包装类


### 4.1 Wrapper类


```java
// 自动装箱和拆箱
Integer num = 100;  // 自动装箱
int n = num;        // 自动拆箱

// 基本类型转换
String str = "123";
int i = Integer.parseInt(str);      // 字符串转int
double d = Double.parseDouble("3.14"); // 字符串转double
String numStr = Integer.toString(i); // int转字符串

// 值比较
Integer a = 127;
Integer b = 127;
System.out.println(a == b);    // true（-128到127缓存）

Integer c = 200;
Integer d = 200;
System.out.println(c == d);    // false（超出缓存范围）
System.out.println(c.equals(d)); // true（始终比较值）

// 常用静态值
Integer maxValue = Integer.MAX_VALUE;  // 2147483647
Integer minValue = Integer.MIN_VALUE;  // -2147483648

// 类型转换
Integer i = 42;
Double d = i.doubleValue();  // 转为double
Long l = i.longValue();      // 转为long

// 集合使用
ArrayList<Integer> list = new ArrayList<>();  // 泛型必须用包装类

// valueOf方法
Integer i = Integer.valueOf(42);    // 优先使用缓存
Integer j = Integer.valueOf("42");  // 字符串转包装类

// Integer
String str = "123";
int i = Integer.parseInt(str);        // 字符串转整数
Integer integerObj = Integer.valueOf(i);  // int 转 Integer
int value = integerObj.intValue();     // 获取 int 值

// Double
String strDouble = "123.45";
double d = Double.parseDouble(strDouble);  // 字符串转 double
Double doubleObj = Double.valueOf(d);      // double 转 Double
double doubleValue = doubleObj.doubleValue();  // 获取 double 值

// Character
char c = 'A';
boolean isDigit = Character.isDigit(c);  // 检查是否是数字
boolean isLetter = Character.isLetter(c);  // 检查是否是字母
char charValue = c; // 获取 char 值 (直接使用基本类型)

// Boolean
String strBool = "true";
boolean b = Boolean.parseBoolean(strBool);  // 字符串转 boolean
Boolean booleanObj = Boolean.valueOf(b);    // boolean 转 Boolean
boolean booleanValue = booleanObj.booleanValue();  // 获取 boolean 值
```

---

## 5. 🔧 其他常用辅助


### 5.1 Arrays类


**🔸 需要注意的事项**

| **方法** | **注意事项** |
|---------|-------------|
| `Arrays.sort()` | 默认升序排序，对象数组需实现 `Comparable` 接口，否则可能抛 `ClassCastException` |
| `Arrays.binarySearch()` | 数组必须先排序，否则搜索结果可能不准确 |
| `Arrays.fill()` | 填充对象数组时，所有元素引用相同对象（浅拷贝），可能导致数据共享问题 |
| `Arrays.equals()` | 仅比较数组元素引用，不是深度比较，且两数组必须长度相同 |
| `Arrays.copyOf()` | 若目标数组长度大于原数组，新增元素会被初始化（数字为 `0`，对象为 `null`） |

```java
// 排序
int[] numbers = {5, 2, 9, 1, 5};
Arrays.sort(numbers);                       // 升序排序
Arrays.sort(numbers, 0, 3);                 // 部分排序(从索引0到2)
Arrays.sort(objects, Comparator.comparing(Object::toString)); // 使用比较器

// 二分查找(数组必须已排序)
int index = Arrays.binarySearch(numbers, 5);     // 返回元素位置或负值
int index2 = Arrays.binarySearch(numbers, 0, 3, 5); // 指定范围内查找

// 填充
Arrays.fill(numbers, 0);                    // 全部填充为0
Arrays.fill(numbers, 0, 3, 9);              // 部分填充(从索引0到2填充为9)

// 数组复制
int[] copy = Arrays.copyOf(numbers, numbers.length);        // 完整复制
int[] copy2 = Arrays.copyOf(numbers, 10);                   // 复制并扩容
int[] copy3 = Arrays.copyOfRange(numbers, 1, 4);            // 部分复制(索引1到3)

// 数组比较
boolean isEqual = Arrays.equals(array1, array2);            // 一维数组内容比较
boolean isDeepEqual = Arrays.deepEquals(array1, array2);    // 多维数组内容比较

// 转换为List(返回的List是固定大小的)
List<Integer> list = Arrays.asList(1, 2, 3, 4, 5);          // 数组转List
List<String> strList = Arrays.asList("a", "b", "c");

// 转换为Stream
IntStream stream = Arrays.stream(numbers);    // 数组转流
Stream<String> strStream = Arrays.stream(new String[]{"a", "b", "c"});

// 数组转字符串 toString方法
String str = Arrays.toString(numbers);                      // 一维数组转字符串
String deepStr = Arrays.deepToString(new int[][]{{1,2},{3,4}}); // 多维数组转字符串

// hashCode
int hashCode = Arrays.hashCode(numbers);                    // 一维数组哈希码
int deepHashCode = Arrays.deepHashCode(new int[][]{{1,2},{3,4}}); // 多维数组哈希码

// 并行排序(大数组效率更高)
Arrays.parallelSort(numbers);
Arrays.parallelSort(numbers, 0, 3);

// 设置范围的值
Arrays.setAll(numbers, i -> i * 2);                         // 设置每个元素为索引的2倍

// Java 9+: 创建不可变List
List<String> immutableList = Arrays.asList("a", "b", "c");  // 固定大小但可修改元素
List<String> immutable = List.of("a", "b", "c");            // 完全不可变(Java 9+)

// Arrays 类包含操作数组的各种静态方法，如排序、搜索、比较、填充、复制等
// 主要用于简化数组的常见操作，提高代码效率 
```

### 5.2 Math类


```java
// 常量
double pi = Math.PI;                 // π (3.141592653589793)
double e = Math.E;                   // 自然对数的底 (2.718281828459045)

// 基本运算
double sqrt = Math.sqrt(16);  // 平方根
double abs = Math.abs(-5);  // 绝对值
double max = Math.max(10, 20);  // 最大值
double min = Math.min(10, 20);  // 最小值

// 向上/向下取整
double ceilValue = Math.ceil(10.1);  // 向上取整: 11.0
double floorValue = Math.floor(10.9); // 向下取整: 10.0
long roundValue = Math.round(10.5);  // 四舍五入: 11

// 幂运算
double power = Math.pow(2, 3);       // 2的3次方: 8.0
double squareRoot = Math.sqrt(16);   // 平方根: 4.0
double cubeRoot = Math.cbrt(27);     // 立方根: 3.0

// 指数和对数
double expValue = Math.exp(1);       // e的1次方: 2.718...
double logValue = Math.log(10);      // 自然对数(底为e): 2.302...
double log10Value = Math.log10(100); // 以10为底的对数: 2.0

// 随机数
double random = Math.random();       // 0.0到1.0之间的随机数(不包括1.0)

// 符号函数
int sign = Math.signum(-10.5);       // 返回数字的符号: -1.0

// 舍入函数
int floor = Math.floorDiv(10, 3);    // 向下整除: 3
int mod = Math.floorMod(10, 3);      // 向下取模: 1

// 加减乘除不溢出
int addExact = Math.addExact(a, b);  // 溢出会抛ArithmeticException
int mulExact = Math.multiplyExact(a, b);
```

### 5.3 BigDecimal类


```java
// 创建 BigDecimal
BigDecimal bd1 = new BigDecimal("123.456");  // 从字符串创建
BigDecimal bd2 = BigDecimal.valueOf(123.456);  // 从 double 创建

// 基本算术操作
BigDecimal sum = bd1.add(bd2);  // 加法
BigDecimal diff = bd1.subtract(bd2);  // 减法
BigDecimal prod = bd1.multiply(bd2);  // 乘法
BigDecimal quotient = bd1.divide(bd2, 2, RoundingMode.HALF_UP);  // 除法（保留两位小数）

// 设置精度与舍入
BigDecimal bd3 = bd1.setScale(2, RoundingMode.HALF_UP);  // 设置两位小数并四舍五入

// 比较大小
int result = bd1.compareTo(bd2);  // 比较两个 BigDecimal（返回 -1, 0, 1）

// 转为字符串 
String str = bd1.toString();   

// 判断是否为零
boolean isZero = bd1.equals(BigDecimal.ZERO);   

// 幂和开方
BigDecimal power = bd1.pow(2);                        // 幂运算(bd1^2)
BigDecimal sqrt = bd1.sqrt(new MathContext(10));      // 平方根(指定精度)

// 取余和取模
BigDecimal remainder = bd1.remainder(bd2);            // 取余

// 移动小数点
BigDecimal moved = bd1.movePointLeft(2);              // 小数点左移2位(除以100)
BigDecimal moved2 = bd1.movePointRight(2);            // 小数点右移2位(乘以100)

// 判断
boolean isZero = bd1.signum() == 0;                   // 是否为0
boolean isNegative = bd1.signum() < 0;                // 是否为负数
boolean isPositive = bd1.signum() > 0;                // 是否为正数

// BigDecimal 用于高精度小数运算，避免 float 和 double 精度丢失 
// 提供加、减、乘、除、比较、舍入等常见数学操作，适用于金融、科学计算等需要高精度的场合
```

### 5.4 BigInteger类


```java
// 创建 BigInteger
BigInteger bigInt1 = new BigInteger("12345678901234567890");  // 从字符串创建
BigInteger bigInt2 = BigInteger.valueOf(1234567890L);  // 从 long 类型创建

// 基本算术操作
BigInteger sum = bigInt1.add(bigInt2);  // 加法
BigInteger diff = bigInt1.subtract(bigInt2);  // 减法
BigInteger prod = bigInt1.multiply(bigInt2);  // 乘法
BigInteger quotient = bigInt1.divide(bigInt2);  // 除法

// 取余与模运算
BigInteger remainder = bigInt1.remainder(bigInt2);  // 取余
BigInteger mod = bigInt1.mod(bigInt2);  // 取模

// 大数比较
int result = bigInt1.compareTo(bigInt2);  // 比较两个大整数
if (result > 0) {
    // bigInt1 > bigInt2
} else if (result < 0) {
    // bigInt1 < bigInt2
} else {
    // bigInt1 == bigInt2
}

// 幂运算
BigInteger power = bigInt1.pow(3);  // bigInt1 的 3 次方

// 转为字符串
String str = bigInt1.toString();  // 转换为字符串

// 判断是否为零
boolean isZero = bigInt1.equals(BigInteger.ZERO);  // 判断是否为 0

// BigInteger 用于表示任意精度的整数，适合处理超大整数 
// 提供加、减、乘、除、比较、幂运算等常见数学操作
```

### 5.5 Pattern类


```java
// 创建Pattern对象
Pattern pattern = Pattern.compile("\\w+");                // 基本模式
Pattern patternCase = Pattern.compile("java", Pattern.CASE_INSENSITIVE);  // 忽略大小写
Pattern multiline = Pattern.compile("^start", Pattern.MULTILINE);         // 多行模式
Pattern dotAll = Pattern.compile(".*", Pattern.DOTALL);                   // 点号匹配所有字符(包括换行)

// 常用标志
// Pattern.CASE_INSENSITIVE - 忽略大小写
// Pattern.MULTILINE - 多行模式(^和$匹配行的开始和结束)
// Pattern.DOTALL - 点号匹配所有字符(包括换行符)
// Pattern.UNICODE_CASE - Unicode感知大小写
// Pattern.COMMENTS - 忽略空白和#注释

// 常见的内置模式
Pattern pattern1 = Pattern.compile("\\d+");  // 匹配数字
Pattern pattern2 = Pattern.compile("\\w+");  // 匹配单词字符（字母、数字、下划线）

// 检查正则匹配
boolean isMatch = pattern.matcher("aaab").matches();  // 检查整个字符串是否匹配

// 创建Matcher
Matcher matcher = pattern.matcher("input text");

// 分割字符串
String[] parts = Pattern.compile(",").split("a,b,c");  // 使用逗号分割字符串
String[] parts2 = pattern.split("text to split");       // 基于模式分割
String[] partsLimit = pattern.split("text", 2);        // 限制分割次数

// 静态匹配方法
boolean matches = Pattern.matches("\\d+", "12345");    // 整个字符串是否匹配模式

// 正则表达式检查
boolean isRegexValid = true;
try {
    Pattern.compile("(invalid[regex");
} catch (PatternSyntaxException e) {
    isRegexValid = false;
}

// 查找所有匹配项（使用 Matcher）
Matcher matcher = pattern.matcher("aaab");
while (matcher.find()) {
    System.out.println(matcher.group());  // 打印每个匹配的子串
}

// 快速查找所有匹配(无需Matcher)
Pattern wordPattern = Pattern.compile("\\w+");
wordPattern.matcher("Hello Java World")
    .results()                             // Stream<MatchResult> (Java 9+)
    .map(mr -> mr.group())
    .forEach(System.out::println);

// 预编译常用的正则表达式
public static final Pattern EMAIL_PATTERN = 
    Pattern.compile("^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+$");
public static final Pattern DATE_PATTERN = 
    Pattern.compile("\\d{4}-\\d{2}-\\d{2}");

// 判断是否匹配
boolean isEmail = EMAIL_PATTERN.matcher("user@example.com").matches();

// Pattern 用于编译和管理正则表达式 
// 常与 Matcher 配合，后者执行具体的匹配操作（如查找、替换等） 
// 内置多种正则表达式模式用于不同的匹配任务 
```

### 5.6 Matcher类


```java
// 创建Pattern和Matcher
Pattern pattern = Pattern.compile("\\w+"); // 编译正则表达式
Matcher matcher = pattern.matcher("Hello Java"); // 创建 Matcher 对象

// 查找匹配
boolean found = matcher.find();           // 查找下一个匹配，返回是否找到
boolean foundAgain = matcher.find();      // 再次查找下一个匹配

// 完全匹配
boolean matches = matcher.matches();      // 整个输入是否匹配模式

// 从头匹配
boolean lookingAt = matcher.lookingAt();  // 从开头开始是否匹配

String matched = matcher.group();  // 获取当前匹配的子串

// 获取匹配结果
String group = matcher.group();           // 返回最近一次匹配的子串
String group1 = matcher.group(1);         // 返回第1个捕获组
int groupCount = matcher.groupCount();    // 返回捕获组数量

// 获取匹配位置
int start = matcher.start();              // 最近一次匹配的起始位置
int end = matcher.end();                  // 最近一次匹配的结束位置后一位
int start1 = matcher.start(1);            // 第1个捕获组的起始位置
int end1 = matcher.end(1);                // 第1个捕获组的结束位置后一位

// 重置匹配器
matcher.reset();                          // 重置到初始状态
matcher.reset("New text");                // 重置并使用新文本

// 替换
String result = matcher.replaceFirst("replacement");  // 替换第一个匹配项
String resultAll = matcher.replaceAll("replacement"); // 替换所有匹配项

// 使用分组引用替换
String withGroups = matcher.replaceAll("prefix_$1_suffix");  // $1引用第一个捕获组

// 替换回调
StringBuffer sb = new StringBuffer();
while (matcher.find()) {
    matcher.appendReplacement(sb, matcher.group().toUpperCase());
}
matcher.appendTail(sb);
String processed = sb.toString();

// 使用find的循环
while (matcher.find()) {
    System.out.println("Found: " + matcher.group());
}

// 查找所有匹配项到列表
List<String> allMatches = new ArrayList<>();
while (matcher.find()) {
    allMatches.add(matcher.group());
}

// Matcher 用于执行正则表达式匹配操作
// 常与 Pattern 配合使用，通过 find() 和 group() 获取匹配结果
// 提供了功能如查找、替换、分组等
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 基础通用类：Object是所有类的父类；System提供系统级功能；Scanner处理输入输出
🔸 时间日期处理：Java 8前使用Date/Calendar；Java 8后使用LocalDate/LocalTime等新API
🔸 字符串处理：String不可变；StringBuilder可变且高效；StringJoiner用于连接
🔸 包装类：基本类型与引用类型转换；自动装箱拆箱；缓存机制
🔸 工具辅助类：Arrays数组操作；Math数学运算；BigDecimal高精度计算；Pattern正则表达式
```

### 6.2 关键理解要点


**🔹 字符串处理的选择**
```
String：不可变，适合少量操作
StringBuilder：可变，非线程安全，单线程环境推荐
StringBuffer：可变，线程安全，多线程环境使用
StringJoiner：专门用于连接，支持分隔符和前后缀
```

**🔹 时间API的演进**
```
传统API (Java 8前)：
• Date/Calendar：可变，非线程安全，API设计复杂
• SimpleDateFormat：非线程安全，需要注意

新时间API (Java 8+)：
• LocalDate/LocalTime/LocalDateTime：不可变，线程安全
• DateTimeFormatter：线程安全，替代SimpleDateFormat
• Duration/Period：时间间隔计算
• Instant：时间戳处理
```

**🔹 高精度计算**
```
float/double精度问题：
• 不适合金融计算
• 存在精度丢失

BigDecimal解决方案：
• 任意精度小数运算
• 避免精度丢失
• 适合金融、科学计算

BigInteger：
• 任意精度整数运算
• 处理超大整数
```

### 6.3 最佳实践建议


**🔧 编码规范**
- 优先使用Java 8+的新时间API
- 字符串频繁操作使用StringBuilder
- 金融计算使用BigDecimal，避免double
- 正则表达式预编译Pattern对象
- 使用Arrays类简化数组操作

**⚡ 性能考虑**
- StringBuilder指定初始容量避免扩容
- 缓存常用的Pattern对象
- BigDecimal创建使用字符串构造器
- 合理选择时间API，避免不必要的转换
- 使用并行方法处理大数组

**🛡️ 安全注意**
- SimpleDateFormat非线程安全，使用ThreadLocal或DateTimeFormatter
- 注意包装类缓存范围（-128到127）
- BigDecimal除法指定精度避免异常
- 正则表达式防止ReDoS攻击
- 时区处理使用ZonedDateTime

**核心记忆口诀**：
- Object万类祖，System系统助
- 时间新API，线程安全齐
- 字符串处理，可变不可变要区分
- 包装类装箱，缓存要记清
- 工具类丰富，数组数学正则助