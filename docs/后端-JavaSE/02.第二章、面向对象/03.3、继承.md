---
title: 3、继承
---
## 📚 目录

1. [继承的基本概念](#1-继承的基本概念)
2. [继承的语法与类型](#2-继承的语法与类型)
3. [继承的核心特性](#3-继承的核心特性)
4. [构造方法与继承](#4-构造方法与继承)
5. [访问控制与继承](#5-访问控制与继承)
6. [方法重写机制](#6-方法重写机制)
7. [继承的设计原则](#7-继承的设计原则)
8. [继承与接口对比](#8-继承与接口对比)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🌳 继承的基本概念


### 1.1 继承定义与本质


**🔸 核心定义**
```
继承（Inheritance）：面向对象编程的三大特性之一
本质：子类继承父类的属性和方法，实现代码复用和扩展
关系：建立类之间的"is-a"关系
目标：提高代码复用性，建立类的层次结构
```

### 1.2 继承的概念图示


```
继承关系示例：

        Animal（动物）
          |
    ┌─────┴─────┐
    |           |
  Mammal      Bird
 （哺乳动物）  （鸟类）
    |           |
┌───┴───┐   ┌───┴───┐
|       |   |       |
Dog    Cat Hawk   Eagle
（狗） （猫）（鹰） （老鹰）

关系解读：
- Dog is-a Mammal ✓
- Mammal is-a Animal ✓
- Dog is-a Animal ✓（传递性）
```

### 1.3 继承的基本特征


| 特征项目 | 说明 | 实际意义 |
|---------|------|----------|
| **代码复用** | 子类自动获得父类的功能 | 减少重复代码，提高开发效率 |
| **层次结构** | 形成树状的类层次结构 | 便于代码组织和管理 |
| **is-a关系** | 子类是父类的一种特殊形式 | 符合现实世界的逻辑关系 |
| **单继承限制** | Java类只能继承一个父类 | 避免菱形继承问题，保持简洁 |
| **功能扩展** | 子类可以新增和重写功能 | 在复用基础上实现个性化 |

> **💡 关键理解**：继承是Java实现代码复用和多态的基础机制，它建立了子类与父类的"is-a"关系，但使用时应遵循单一职责、最小权限、组合优先等设计原则。

---

## 2. 🔧 继承的语法与类型


### 2.1 基本继承语法


**🔸 语法格式**
```java
// 父类定义
class 父类名 {
    // 父类成员
}

// 子类继承语法
class 子类名 extends 父类名 {
    // 子类成员
}
```

**💻 基础示例**
```java
// 父类：动物
class Animal {
    protected String name;
    protected int age;
    
    public Animal(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    public void makeSound() {
        System.out.println("动物发出声音");
    }
    
    public void sleep() {
        System.out.println(name + "正在睡觉");
    }
}

// 子类：狗
class Dog extends Animal {
    private String breed; // 子类新增属性
    
    public Dog(String name, int age, String breed) {
        super(name, age); // 调用父类构造器
        this.breed = breed;
    }
    
    @Override
    public void makeSound() { // 重写父类方法
        System.out.println(name + "汪汪叫");
    }
    
    public void wagTail() { // 子类新增方法
        System.out.println(name + "摇尾巴");
    }
}

// 使用示例
public class InheritanceDemo {
    public static void main(String[] args) {
        Dog dog = new Dog("小白", 3, "金毛");
        dog.makeSound(); // 输出：小白汪汪叫（重写的方法）
        dog.sleep();     // 输出：小白正在睡觉（继承的方法）
        dog.wagTail();   // 输出：小白摇尾巴（子类方法）
    }
}
```

### 2.2 继承的类型分类


**🔸 按继承层次分类**

| 继承类型 | 特点描述 | 示例关系 |
|---------|----------|----------|
| **单继承** | 一个子类只能继承一个父类 | `class Dog extends Animal` |
| **多层继承** | 形成继承链，支持传递性 | `Animal → Mammal → Dog` |
| **树形继承** | 一个父类可被多个子类继承 | `Animal ← {Dog, Cat, Bird}` |

**📊 继承层次图示**
```
多层继承示例：

        Object（根类）
          |
        Animal
          |
        Mammal
          |
    ┌─────┴─────┐
    |           |
   Dog         Cat
    |           |
 GoldenDog   PersianCat

继承链分析：
- GoldenDog extends Dog extends Mammal extends Animal extends Object
- 每个类都继承了上层类的所有非私有成员
- 继承具有传递性：GoldenDog也是Animal的子类
```

### 2.3 继承关键字详解


**🔸 extends关键字**
```java
// 类继承类
class 子类 extends 父类 {
    // 子类实现
}

// 接口继承接口
interface 子接口 extends 父接口 {
    // 子接口定义
}
```

**🔸 implements关键字**
```java
// 类实现接口（可多实现）
class 实现类 implements 接口1, 接口2, 接口3 {
    // 必须实现所有接口的抽象方法
}

// 抽象类也可以实现接口
abstract class 抽象类 implements 接口 {
    // 可以不实现接口方法，留给子类实现
}
```

---

## 3. ⚡ 继承的核心特性


### 3.1 成员继承规则


**🔸 继承内容清单**

| 成员类型 | **public** | **protected** | **默认** | **private** |
|---------|------------|--------------|----------|-------------|
| **属性字段** | ✅ 继承 | ✅ 继承 | ✅ 同包继承 | ❌ 不继承 |
| **实例方法** | ✅ 继承 | ✅ 继承 | ✅ 同包继承 | ❌ 不继承 |
| **静态方法** | ✅ 继承 | ✅ 继承 | ✅ 同包继承 | ❌ 不继承 |
| **构造方法** | ❌ 不继承 | ❌ 不继承 | ❌ 不继承 | ❌ 不继承 |
| **内部类** | ✅ 继承 | ✅ 继承 | ✅ 同包继承 | ❌ 不继承 |

> **⚠️ 重要提醒**：构造方法永远不会被继承，但子类可以通过`super()`调用父类构造器。

### 3.2 方法继承与重写


**🔸 方法继承示例**
```java
class Vehicle {
    protected String brand;
    
    public void start() {
        System.out.println("车辆启动");
    }
    
    public void stop() {
        System.out.println("车辆停止");
    }
    
    public final void showBrand() {
        System.out.println("品牌：" + brand);
    }
}

class Car extends Vehicle {
    @Override
    public void start() {
        System.out.println("汽车点火启动");
    }
    
    // stop()方法被继承，可以直接使用
    // showBrand()是final方法，不能重写
    
    public void openDoor() {
        System.out.println("打开车门");
    }
}
```

### 3.3 super关键字详解


**🔸 super的三种用法**

| 用法 | 语法 | 说明 |
|------|------|------|
| **调用父类构造器** | `super(参数列表)` | 必须在子类构造器第一行 |
| **访问父类方法** | `super.方法名()` | 调用父类被重写的方法 |
| **访问父类属性** | `super.属性名` | 访问父类的属性（较少使用） |

**💻 super使用示例**
```java
class Animal {
    protected String name;
    
    public Animal(String name) {
        this.name = name;
        System.out.println("Animal构造器执行");
    }
    
    public void move() {
        System.out.println(name + "在移动");
    }
}

class Bird extends Animal {
    private int wingspan;
    
    public Bird(String name, int wingspan) {
        super(name); // 调用父类构造器
        this.wingspan = wingspan;
        System.out.println("Bird构造器执行");
    }
    
    @Override
    public void move() {
        super.move(); // 调用父类方法
        System.out.println(name + "正在飞行，翼展" + wingspan + "cm");
    }
}
```

### 3.4 final关键字与继承


**🔸 final的继承限制**

```java
// final类不能被继承
final class FinalClass {
    // 不能有子类
}

class RegularClass {
    // final方法不能被重写
    final void cannotOverride() {
        System.out.println("此方法不能被重写");
    }
    
    // final变量不能被修改
    final int CONSTANT = 100;
}
```

---

## 4. 🏗️ 构造方法与继承


### 4.1 构造器继承规则


**🔸 核心规则**
```
规则1：子类不继承父类的构造方法
规则2：子类构造器必须调用父类构造器
规则3：super()调用必须在子类构造器第一行
规则4：如果不显式调用，编译器自动插入super()
```

### 4.2 构造器调用链


**📊 构造执行顺序图**
```
构造器执行流程：

    创建子类对象
         ↓
    ┌────────────────┐
    │ 1. 加载父类     │
    └────────────────┘
         ↓
    ┌────────────────┐
    │ 2. 执行父类构造器│
    └────────────────┘
         ↓
    ┌────────────────┐
    │ 3. 执行子类构造器│
    └────────────────┘
         ↓
    子类对象创建完成
```

**💻 构造链示例**
```java
class GrandParent {
    public GrandParent() {
        System.out.println("1. GrandParent构造器");
    }
}

class Parent extends GrandParent {
    public Parent() {
        super(); // 可省略，编译器自动添加
        System.out.println("2. Parent构造器");
    }
    
    public Parent(String name) {
        this(); // 调用本类的无参构造器
        System.out.println("3. Parent(String)构造器");
    }
}

class Child extends Parent {
    public Child() {
        super("child"); // 显式调用父类有参构造器
        System.out.println("4. Child构造器");
    }
}

// 测试代码
public class ConstructorChain {
    public static void main(String[] args) {
        Child child = new Child();
        /* 输出顺序：
         * 1. GrandParent构造器
         * 2. Parent构造器
         * 3. Parent(String)构造器
         * 4. Child构造器
         */
    }
}
```

### 4.3 构造器常见问题


**🔸 问题1：父类无无参构造器**
```java
class Parent {
    private String name;
    
    // 只有有参构造器，无默认无参构造器
    public Parent(String name) {
        this.name = name;
    }
}

class Child extends Parent {
    // 编译错误：找不到Parent()构造器
    public Child() {
        // super(); // 编译器自动插入，但找不到父类无参构造器
    }
    
    // 正确做法：显式调用父类有参构造器
    public Child(String name) {
        super(name); // 必须显式调用
    }
}
```

---

## 5. 🔐 访问控制与继承


### 5.1 访问修饰符继承规则


**🔸 访问权限对比表**

| 修饰符 | **同类** | **同包** | **子类** | **不同包** | **继承性** |
|--------|----------|----------|----------|------------|------------|
| `public` | ✅ | ✅ | ✅ | ✅ | **✅ 可继承** |
| `protected` | ✅ | ✅ | ✅ | ❌ | **✅ 可继承** |
| **默认** | ✅ | ✅ | ⚠️ 同包 | ❌ | **⚠️ 同包可继承** |
| `private` | ✅ | ❌ | ❌ | ❌ | **❌ 不可继承** |

### 5.2 跨包继承访问示例


**💻 包结构示例**
```java
// 父类包：com.parent
package com.parent;

public class Animal {
    public String publicField = "public字段";
    protected String protectedField = "protected字段";
    String defaultField = "默认字段";
    private String privateField = "private字段";
    
    public void publicMethod() { }
    protected void protectedMethod() { }
    void defaultMethod() { }
    private void privateMethod() { }
}

// 子类包：com.child
package com.child;
import com.parent.Animal;

public class Dog extends Animal {
    public void accessTest() {
        // 可以访问
        System.out.println(publicField);    // ✅ public
        System.out.println(protectedField); // ✅ protected
        
        // 不能访问（跨包）
        // System.out.println(defaultField);   // ❌ 默认访问级别
        // System.out.println(privateField);   // ❌ private
        
        // 方法访问同理
        publicMethod();    // ✅
        protectedMethod(); // ✅
        // defaultMethod();   // ❌
        // privateMethod();   // ❌
    }
}
```

### 5.3 protected关键字详解


**🔸 protected的特殊性**
```java
package com.example;

class Parent {
    protected int value = 100;
    
    protected void display() {
        System.out.println("父类protected方法");
    }
}

class Child extends Parent {
    public void test() {
        // 子类可以访问继承的protected成员
        this.value = 200;        // ✅ 通过this访问
        super.display();         // ✅ 通过super访问
        
        // 也可以访问其他同类型对象的protected成员
        Child other = new Child();
        other.value = 300;       // ✅ 同类型对象
        
        // 但不能访问父类对象的protected成员
        Parent parent = new Parent();
        // parent.value = 400;   // ❌ 编译错误
    }
}
```

---

## 6. 🔄 方法重写机制


### 6.1 方法重写规则


**🔸 重写条件检查表**

| 检查项目 | **要求** | **说明** |
|---------|----------|----------|
| **方法名** | 必须相同 | 区分大小写 |
| **参数列表** | 必须完全相同 | 类型、个数、顺序 |
| **返回类型** | 相同或协变 | 子类返回类型可以是父类的子类 |
| **访问权限** | 不能降低 | 可以提高权限 |
| **异常声明** | 不能扩大 | 可以缩小或删除 |
| **修饰符限制** | 不能重写final、static、private | 这些方法不参与多态 |

### 6.2 @Override注解详解


**🔸 @Override的作用**
```java
class Animal {
    public void makeSound() {
        System.out.println("动物叫声");
    }
}

class Cat extends Animal {
    @Override // 强烈推荐使用
    public void makeSound() {
        System.out.println("喵喵叫");
    }
    
    // 编译错误示例
    @Override
    public void makesound() { // 方法名拼写错误
        // 编译器会报错：方法不是重写父类方法
    }
}
```

**💡 @Override的好处**
```
1. 编译时检查：确保真正重写了父类方法
2. 代码可读性：明确标识这是重写方法
3. 维护性：父类方法签名改变时会编译错误
4. IDE支持：提供更好的代码提示和错误检查
```

### 6.3 方法重写示例


**💻 完整重写示例**
```java
class Shape {
    protected String color;
    
    public Shape(String color) {
        this.color = color;
    }
    
    // 被重写的方法
    public double getArea() {
        return 0.0;
    }
    
    public void display() {
        System.out.println("这是一个" + color + "的图形");
    }
    
    // final方法不能被重写
    public final void showInfo() {
        System.out.println("图形信息显示");
    }
}

class Circle extends Shape {
    private double radius;
    
    public Circle(String color, double radius) {
        super(color);
        this.radius = radius;
    }
    
    // 重写计算面积方法
    @Override
    public double getArea() {
        return Math.PI * radius * radius;
    }
    
    // 重写显示方法
    @Override
    public void display() {
        super.display(); // 调用父类方法
        System.out.println("半径为：" + radius);
    }
    
    // 不能重写final方法
    // public void showInfo() { } // 编译错误
}

// 测试重写
public class OverrideDemo {
    public static void main(String[] args) {
        Shape shape = new Circle("红色", 5.0);
        shape.display();           // 调用重写的方法
        System.out.println("面积：" + shape.getArea()); // 多态调用
    }
}
```

### 6.4 重写vs重载对比


| 特性 | **重写(Override)** | **重载(Overload)** |
|------|-------------------|-------------------|
| **英文** | Override | Overload |
| **发生范围** | 子类重写父类方法 | 同类中方法名相同 |
| **方法名** | 必须相同 | 必须相同 |
| **参数列表** | 必须相同 | 必须不同 |
| **返回类型** | 相同或协变返回类型 | 可以不同 |
| **访问修饰符** | 不能降低访问权限 | 可以不同 |
| **编译时/运行时** | 运行时确定（动态绑定） | 编译时确定（静态绑定） |

---

## 7. 🎯 继承的设计原则


### 7.1 继承vs组合对比


**🔸 设计选择对比表**

| 设计维度 | **继承(is-a)** | **组合(has-a)** | **推荐** |
|---------|----------------|-----------------|----------|
| **关系类型** | "是一种"关系 | "拥有一个"关系 | 根据实际关系选择 |
| **耦合度** | 高耦合，子类依赖父类 | 低耦合，可替换组件 | **✅ 优先低耦合** |
| **扩展性** | 编译时确定，难修改 | 运行时可替换，灵活 | **✅ 优先组合** |
| **代码复用** | 自动继承全部功能 | 选择性使用功能 | **✅ 有选择性复用** |
| **层次结构** | 形成固定层次树 | 扁平化，易理解 | **✅ 避免深继承** |

### 7.2 组合优于继承示例


**💻 继承方式（不推荐）**
```java
// 不好的设计：强制继承关系
class Bird {
    public void fly() {
        System.out.println("鸟在飞");
    }
}

class Penguin extends Bird {
    @Override
    public void fly() {
        throw new UnsupportedOperationException("企鹅不会飞");
    }
}
```

**💻 组合方式（推荐）**
```java
// 好的设计：使用组合
interface Flyable {
    void fly();
}

class FlyBehavior implements Flyable {
    @Override
    public void fly() {
        System.out.println("正在飞行");
    }
}

class NoFlyBehavior implements Flyable {
    @Override
    public void fly() {
        System.out.println("不会飞行");
    }
}

class Bird {
    private Flyable flyBehavior; // 组合飞行行为
    
    public Bird(Flyable flyBehavior) {
        this.flyBehavior = flyBehavior;
    }
    
    public void performFly() {
        flyBehavior.fly();
    }
    
    // 运行时可以改变行为
    public void setFlyBehavior(Flyable flyBehavior) {
        this.flyBehavior = flyBehavior;
    }
}

// 使用示例
public class CompositionDemo {
    public static void main(String[] args) {
        Bird eagle = new Bird(new FlyBehavior());
        Bird penguin = new Bird(new NoFlyBehavior());
        
        eagle.performFly();   // 正在飞行
        penguin.performFly(); // 不会飞行
        
        // 运行时改变行为
        penguin.setFlyBehavior(new FlyBehavior());
        penguin.performFly(); // 正在飞行
    }
}
```

### 7.3 继承设计原则


**🔸 SOLID原则在继承中的应用**

| 原则 | **说明** | **继承中的体现** |
|------|----------|------------------|
| **S - 单一职责** | 一个类只负责一个功能 | 避免父类功能过于复杂 |
| **O - 开闭原则** | 对扩展开放，对修改关闭 | 通过继承扩展，不修改父类 |
| **L - 里氏替换** | 子类可以替换父类 | 子类不能违反父类约定 |
| **I - 接口隔离** | 不依赖不需要的接口 | 优先使用接口而非继承 |
| **D - 依赖倒置** | 依赖抽象而非具体 | 依赖接口而非具体类 |

**⚠️ 继承使用警告**
```
继承滥用的标志：
❌ 继承层次超过3-4层
❌ 子类只用到父类很少功能
❌ 子类需要重写大量父类方法
❌ 出现"拒绝继承"情况
❌ 为了代码复用而强行继承

正确使用继承：
✅ 真正的is-a关系
✅ 子类是父类的特化
✅ 遵循里氏替换原则
✅ 继承层次保持浅层
✅ 考虑使用接口代替抽象类
```

---

## 8. 🔗 继承与接口对比


### 8.1 继承vs接口全面对比


| 对比维度 | **类继承(extends)** | **接口实现(implements)** |
|---------|-------------------|-------------------------|
| **关键字** | `extends` | `implements` |
| **数量限制** | 只能继承一个类 | 可以实现多个接口 |
| **方法实现** | 可以有具体实现 | 传统上只有抽象方法 |
| **成员变量** | 可以有实例变量 | 只能有public static final常量 |
| **构造方法** | 可以有构造方法 | 不能有构造方法 |
| **访问修饰符** | 支持所有修饰符 | 方法默认public abstract |
| **设计目的** | 代码复用和扩展 | 定义行为规范，实现多继承 |
| **耦合度** | 高耦合 | 低耦合 |

### 8.2 Java8+接口新特性


**🔸 默认方法与静态方法**
```java
interface Drawable {
    // 传统抽象方法
    void draw();
    
    // Java 8+ 默认方法
    default void fillColor(String color) {
        System.out.println("用" + color + "填充颜色");
    }
    
    // Java 8+ 静态方法
    static void showVersion() {
        System.out.println("Drawable接口 v2.0");
    }
    
    // Java 9+ 私有方法
    private void commonLogic() {
        System.out.println("接口内部通用逻辑");
    }
}

class Circle implements Drawable {
    @Override
    public void draw() {
        System.out.println("绘制圆形");
    }
    
    // 可以选择重写默认方法
    @Override
    public void fillColor(String color) {
        System.out.println("圆形特殊填充：" + color);
    }
}
```

### 8.3 多接口实现示例


**💻 多接口实现**
```java
interface Flyable {
    void fly();
    default void takeOff() {
        System.out.println("准备起飞");
    }
}

interface Swimmable {
    void swim();
    default void dive() {
        System.out.println("准备潜水");
    }
}

interface Walkable {
    void walk();
}

// 实现多个接口
class Duck implements Flyable, Swimmable, Walkable {
    @Override
    public void fly() {
        System.out.println("鸭子飞行");
    }
    
    @Override
    public void swim() {
        System.out.println("鸭子游泳");
    }
    
    @Override
    public void walk() {
        System.out.println("鸭子走路");
    }
}

// 测试多接口
public class MultiInterfaceDemo {
    public static void main(String[] args) {
        Duck duck = new Duck();
        
        // 所有行为都可以执行
        duck.fly();     // 鸭子飞行
        duck.swim();    // 鸭子游泳
        duck.walk();    // 鸭子走路
        duck.takeOff(); // 准备起飞（默认方法）
        duck.dive();    // 准备潜水（默认方法）
        
        // 多态使用
        Flyable flyable = new Duck();
        flyable.fly();
    }
}
```

### 8.4 接口冲突解决


**🔸 默认方法冲突处理**
```java
interface A {
    default void hello() {
        System.out.println("Hello from A");
    }
}

interface B {
    default void hello() {
        System.out.println("Hello from B");
    }
}

// 实现多个接口时的冲突解决
class C implements A, B {
    @Override
    public void hello() {
        // 解决方案1：选择其中一个接口的实现
        A.super.hello();
        
        // 解决方案2：提供自己的实现
        // System.out.println("Hello from C");
        
        // 解决方案3：组合调用
        // A.super.hello();
        // B.super.hello();
    }
}
```

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的基本概念


```
🔸 继承本质：子类继承父类的非私有成员，建立is-a关系
🔸 继承语法：extends关键字，Java支持单继承但可多层继承
🔸 方法重写：@Override注解，遵循访问权限不能降低的原则
🔸 构造器规则：子类不继承构造器，但必须调用父类构造器
🔸 super关键字：调用父类构造器、方法和属性的关键字
🔸 访问控制：private不可继承，protected跨包可继承
```

### 9.2 关键理解要点


**🔹 继承的双面性**
```
优势：
✅ 代码复用，减少重复编写
✅ 建立清晰的类层次结构
✅ 支持多态，提高代码灵活性
✅ 符合现实世界的分类逻辑

劣势：
❌ 增加类间耦合度
❌ 破坏封装性
❌ 继承层次过深难以维护
❌ 子类过度依赖父类实现
```

**🔹 设计选择策略**
```
选择继承的时机：
✅ 存在真正的is-a关系
✅ 子类是父类的特化
✅ 需要利用多态特性
✅ 父类设计良好且稳定

选择组合的时机：
✅ 存在has-a关系
✅ 需要运行时改变行为
✅ 避免不必要的继承层次
✅ 提高代码的可测试性
```

**🔹 最佳实践原则**
```
继承设计原则：
1. 组合优于继承（Composition over Inheritance）
2. 里氏替换原则（Liskov Substitution Principle）
3. 接口隔离原则（Interface Segregation Principle）
4. 依赖倒置原则（Dependency Inversion Principle）
5. 开闭原则（Open-Closed Principle）
```

### 9.3 实际应用价值


**🎯 继承的应用场景**
- **框架设计**：建立扩展点，允许用户继承并自定义
- **模板方法**：定义算法骨架，子类实现具体步骤
- **多态实现**：同一接口的不同实现，运行时动态选择
- **代码复用**：公共功能提取到父类，避免重复
- **领域建模**：反映现实世界的分类和层次关系

**🔧 工程实践要点**
```
代码审查检查点：
□ 继承层次是否合理（建议不超过4层）
□ 是否存在真正的is-a关系
□ 子类是否违反了里氏替换原则
□ 是否可以用组合替代继承
□ 接口设计是否优于抽象类
□ 访问修饰符是否合理
□ @Override注解是否正确使用
```

**核心记忆口诀**：
- 继承建立is-a关系，单继承支持多层次
- 子类重写父类方法，访问权限不降低
- 构造器调用有顺序，super必须第一行
- 组合优于继承原则，接口实现多继承
- 里氏替换要遵守，设计模式助编程