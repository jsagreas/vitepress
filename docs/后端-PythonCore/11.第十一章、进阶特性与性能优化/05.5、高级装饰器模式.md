---
title: 5、高级装饰器模式
---
## 📚 目录

1. [装饰器基础回顾](#1-装饰器基础回顾)
2. [带参数装饰器](#2-带参数装饰器)
3. [类装饰器](#3-类装饰器)
4. [装饰器类](#4-装饰器类)
5. [多重装饰器](#5-多重装饰器)
6. [条件装饰器](#6-条件装饰器)
7. [缓存装饰器](#7-缓存装饰器)
8. [同步装饰器](#8-同步装饰器)
9. [装饰器最佳实践](#9-装饰器最佳实践)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🎯 装饰器基础回顾


### 1.1 什么是装饰器


> 💡 **简单理解**：装饰器就像给函数"包装"一层功能，不改变原函数代码，却能增加新功能

**生活类比**：
```
原始房子：能住人                    原始函数：实现基本功能
装修房子：住人 + 美观 + 保温        装饰函数：基本功能 + 额外功能

装修不改变房子结构                  装饰不改变函数结构
只是在外面包了一层                  只是在外面包了一层逻辑
```

### 1.2 装饰器的本质


**核心理解**：装饰器本质上是一个**接收函数作为参数，返回新函数的函数**

```python
# 最简单的装饰器示例
def my_decorator(func):
    def wrapper():
        print("函数执行前的操作")
        result = func()  # 调用原函数
        print("函数执行后的操作")
        return result
    return wrapper

# 使用装饰器
@my_decorator
def say_hello():
    print("Hello!")

# 等价于：say_hello = my_decorator(say_hello)
```

**执行流程**：
```
调用 say_hello()
    ↓
执行 wrapper()
    ↓
打印"函数执行前的操作"
    ↓
调用原始的 say_hello()
    ↓
打印"Hello!"
    ↓
打印"函数执行后的操作"
```

---

## 2. 🔧 带参数装饰器


### 2.1 为什么需要带参数装饰器


**问题场景**：普通装饰器功能固定，无法灵活配置

```python
# 普通装饰器：只能重复3次
def repeat_three_times(func):
    def wrapper(*args, **kwargs):
        for i in range(3):
            result = func(*args, **kwargs)
        return result
    return wrapper

# 想要重复5次怎么办？又要写一个新装饰器？
```

### 2.2 带参数装饰器的实现


**核心思想**：装饰器外面再包一层函数，用来接收参数

```python
def repeat(times):
    """可以指定重复次数的装饰器"""
    def decorator(func):
        def wrapper(*args, **kwargs):
            result = None
            for i in range(times):
                print(f"第{i+1}次执行:")
                result = func(*args, **kwargs)
            return result
        return wrapper
    return decorator

# 使用方式
@repeat(3)
def greet(name):
    print(f"你好, {name}!")

@repeat(5)
def count_sheep():
    print("🐑")
```

**三层结构解析**：
```
第一层：repeat(times)     → 接收装饰器参数，返回真正的装饰器
第二层：decorator(func)   → 接收被装饰的函数，返回包装函数
第三层：wrapper(*args)    → 实际执行的包装函数
```

### 2.3 实用的带参数装饰器示例


**延时执行装饰器**：
```python
import time

def delay(seconds):
    """延时装饰器，让函数延迟指定秒数执行"""
    def decorator(func):
        def wrapper(*args, **kwargs):
            print(f"等待 {seconds} 秒...")
            time.sleep(seconds)
            return func(*args, **kwargs)
        return wrapper
    return decorator

@delay(2)
def important_task():
    print("执行重要任务!")
```

**权限检查装饰器**：
```python
def require_role(role):
    """检查用户权限的装饰器"""
    def decorator(func):
        def wrapper(*args, **kwargs):
            # 假设从某处获取当前用户角色
            current_role = get_current_user_role()
            if current_role != role:
                print(f"权限不足！需要 {role} 权限")
                return None
            return func(*args, **kwargs)
        return wrapper
    return decorator

@require_role("admin")
def delete_user(user_id):
    print(f"删除用户 {user_id}")
```

---

## 3. 🏗️ 类装饰器


### 3.1 什么是类装饰器


> 💡 **核心理解**：类装饰器是用来装饰**整个类**的装饰器，可以给类添加新的属性或方法

**应用场景**：
- 给类添加通用方法
- 自动注册类
- 添加元数据
- 实现设计模式

### 3.2 基础类装饰器示例


```python
def add_toString(cls):
    """给类添加toString方法的装饰器"""
    def toString(self):
        attrs = []
        for key, value in self.__dict__.items():
            attrs.append(f"{key}={value}")
        return f"{cls.__name__}({', '.join(attrs)})"
    
    cls.toString = toString
    return cls

@add_toString
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

# 使用
person = Person("张三", 25)
print(person.toString())  # Person(name=张三, age=25)
```

### 3.3 带参数的类装饰器


```python
def singleton(cls):
    """单例模式装饰器"""
    instances = {}
    def get_instance(*args, **kwargs):
        if cls not in instances:
            instances[cls] = cls(*args, **kwargs)
        return instances[cls]
    return get_instance

@singleton
class Database:
    def __init__(self):
        self.connection = "数据库连接"
        print("创建数据库连接")

# 测试单例
db1 = Database()  # 创建数据库连接
db2 = Database()  # 不会再创建
print(db1 is db2)  # True - 是同一个对象
```

### 3.4 实用类装饰器：自动属性


```python
def auto_property(cls):
    """自动为类的属性添加getter和setter"""
    for name, value in list(cls.__dict__.items()):
        if not name.startswith('_') and not callable(value):
            # 创建私有属性
            private_name = f"_{name}"
            
            # 创建getter和setter
            def make_property(attr_name, private_attr):
                def getter(self):
                    return getattr(self, private_attr)
                def setter(self, value):
                    print(f"设置 {attr_name} = {value}")
                    setattr(self, private_attr, value)
                return property(getter, setter)
            
            # 添加到类中
            setattr(cls, name, make_property(name, private_name))
    
    return cls

@auto_property
class Product:
    name = None
    price = None
    
    def __init__(self, name, price):
        self.name = name
        self.price = price

# 使用
product = Product("手机", 3000)
product.price = 2800  # 设置 price = 2800
```

---

## 4. 🎭 装饰器类


### 4.1 装饰器类 vs 类装饰器


**容易混淆的概念**：

| 概念 | 说明 | 装饰什么 | 语法示例 |
|------|------|----------|----------|
| **类装饰器** | 用装饰器装饰类 | 装饰类 | `@decorator` <br> `class MyClass:` |
| **装饰器类** | 把类当作装饰器使用 | 装饰函数 | `@MyDecoratorClass` <br> `def func():` |

### 4.2 装饰器类的实现


**核心要点**：装饰器类需要实现 `__call__` 方法

```python
class CountCalls:
    """统计函数调用次数的装饰器类"""
    
    def __init__(self, func):
        self.func = func
        self.count = 0
    
    def __call__(self, *args, **kwargs):
        self.count += 1
        print(f"函数 {self.func.__name__} 被调用了 {self.count} 次")
        return self.func(*args, **kwargs)

@CountCalls
def say_hello(name):
    print(f"Hello, {name}!")

# 使用
say_hello("Alice")  # 函数 say_hello 被调用了 1 次
say_hello("Bob")    # 函数 say_hello 被调用了 2 次
```

### 4.3 带参数的装饰器类


```python
class Timer:
    """计时装饰器类"""
    
    def __init__(self, unit='秒'):
        self.unit = unit
    
    def __call__(self, func):
        def wrapper(*args, **kwargs):
            import time
            start = time.time()
            result = func(*args, **kwargs)
            end = time.time()
            
            elapsed = end - start
            if self.unit == '毫秒':
                elapsed *= 1000
            
            print(f"函数 {func.__name__} 执行时间: {elapsed:.2f} {self.unit}")
            return result
        return wrapper

@Timer(unit='毫秒')
def slow_function():
    import time
    time.sleep(0.1)
    return "完成"
```

### 4.4 装饰器类的高级用法


**保持状态的装饰器**：
```python
class RateLimiter:
    """限流装饰器类"""
    
    def __init__(self, max_calls, time_window):
        self.max_calls = max_calls      # 最大调用次数
        self.time_window = time_window  # 时间窗口(秒)
        self.calls = []                 # 调用记录
    
    def __call__(self, func):
        def wrapper(*args, **kwargs):
            import time
            now = time.time()
            
            # 清理过期的调用记录
            self.calls = [call_time for call_time in self.calls 
                         if now - call_time < self.time_window]
            
            # 检查是否超过限制
            if len(self.calls) >= self.max_calls:
                print(f"调用频率过高！请等待 {self.time_window} 秒")
                return None
            
            # 记录这次调用
            self.calls.append(now)
            return func(*args, **kwargs)
        return wrapper

@RateLimiter(max_calls=3, time_window=60)  # 每分钟最多3次
def api_call():
    print("调用API成功")
```

---

## 5. 🔗 多重装饰器


### 5.1 多重装饰器的执行顺序


**重要概念**：多个装饰器的执行顺序是**从下往上**包装，**从外往内**执行

```python
def decorator_a(func):
    print("装饰器A：包装函数")
    def wrapper(*args, **kwargs):
        print("装饰器A：执行前")
        result = func(*args, **kwargs)
        print("装饰器A：执行后")
        return result
    return wrapper

def decorator_b(func):
    print("装饰器B：包装函数")
    def wrapper(*args, **kwargs):
        print("装饰器B：执行前")
        result = func(*args, **kwargs)
        print("装饰器B：执行后")
        return result
    return wrapper

@decorator_a
@decorator_b
def my_function():
    print("原函数执行")

# 等价于：my_function = decorator_a(decorator_b(my_function))
```

**执行结果分析**：
```
包装阶段（从下往上）：
装饰器B：包装函数
装饰器A：包装函数

调用 my_function() 时（从外往内）：
装饰器A：执行前
装饰器B：执行前
原函数执行
装饰器B：执行后
装饰器A：执行后
```

### 5.2 多重装饰器的实际应用


**权限 + 日志 + 缓存组合**：
```python
import time
from functools import lru_cache

def log_calls(func):
    """日志装饰器"""
    def wrapper(*args, **kwargs):
        print(f"[LOG] 调用函数 {func.__name__}")
        result = func(*args, **kwargs)
        print(f"[LOG] 函数 {func.__name__} 执行完成")
        return result
    return wrapper

def require_auth(func):
    """权限检查装饰器"""
    def wrapper(*args, **kwargs):
        if not hasattr(wrapper, 'authenticated'):
            print("[AUTH] 权限验证失败")
            return None
        print("[AUTH] 权限验证通过")
        return func(*args, **kwargs)
    return wrapper

def cache_result(func):
    """简单缓存装饰器"""
    cache = {}
    def wrapper(*args, **kwargs):
        key = str(args) + str(kwargs)
        if key in cache:
            print("[CACHE] 缓存命中")
            return cache[key]
        print("[CACHE] 缓存未命中，计算结果")
        result = func(*args, **kwargs)
        cache[key] = result
        return result
    return wrapper

@log_calls
@require_auth  
@cache_result
def expensive_calculation(n):
    """模拟耗时计算"""
    time.sleep(1)
    return n * n

# 设置认证状态
expensive_calculation.authenticated = True

# 测试
result1 = expensive_calculation(5)  # 完整流程
result2 = expensive_calculation(5)  # 缓存命中
```

---

## 6. 🎛️ 条件装饰器


### 6.1 根据条件决定是否装饰


**应用场景**：开发环境需要调试功能，生产环境不需要

```python
def conditional_decorator(condition):
    """条件装饰器：根据条件决定是否应用装饰逻辑"""
    def decorator(func):
        if condition:
            # 应用装饰逻辑
            def wrapper(*args, **kwargs):
                print(f"[DEBUG] 调用 {func.__name__}")
                result = func(*args, **kwargs)
                print(f"[DEBUG] {func.__name__} 返回: {result}")
                return result
            return wrapper
        else:
            # 不装饰，直接返回原函数
            return func
    return decorator

# 根据环境变量决定是否开启调试
import os
DEBUG = os.getenv('DEBUG', 'False').lower() == 'true'

@conditional_decorator(DEBUG)
def calculate(a, b):
    return a + b

# 在DEBUG=True时会有调试输出，DEBUG=False时没有
```

### 6.2 动态条件装饰器


```python
class SmartLogger:
    """智能日志装饰器：根据函数执行时间决定是否记录"""
    
    def __init__(self, threshold=1.0):
        self.threshold = threshold  # 时间阈值（秒）
    
    def __call__(self, func):
        def wrapper(*args, **kwargs):
            import time
            start = time.time()
            result = func(*args, **kwargs)
            elapsed = time.time() - start
            
            # 只有执行时间超过阈值才记录日志
            if elapsed > self.threshold:
                print(f"⚠️  慢函数警告: {func.__name__} 执行了 {elapsed:.2f} 秒")
            
            return result
        return wrapper

@SmartLogger(threshold=0.5)
def quick_task():
    import time
    time.sleep(0.1)  # 不会触发日志
    return "快速完成"

@SmartLogger(threshold=0.5)  
def slow_task():
    import time
    time.sleep(1.0)  # 会触发日志
    return "慢慢完成"
```

### 6.3 环境感知装饰器


```python
def environment_aware(dev_behavior, prod_behavior):
    """环境感知装饰器"""
    def decorator(func):
        def wrapper(*args, **kwargs):
            import os
            env = os.getenv('ENVIRONMENT', 'development')
            
            if env == 'development':
                print(f"[DEV] {dev_behavior}")
                # 开发环境：详细日志 + 错误追踪
                try:
                    result = func(*args, **kwargs)
                    print(f"[DEV] 函数执行成功: {result}")
                    return result
                except Exception as e:
                    print(f"[DEV] 函数执行失败: {e}")
                    raise
            else:
                print(f"[PROD] {prod_behavior}")
                # 生产环境：简单执行
                return func(*args, **kwargs)
        return wrapper
    return decorator

@environment_aware(
    dev_behavior="开发模式：详细调试",
    prod_behavior="生产模式：高效执行"
)
def process_data(data):
    return len(data)
```

---

## 7. 🗄️ 缓存装饰器


### 7.1 简单内存缓存


**核心思想**：把函数的参数作为key，返回值作为value存储

```python
def simple_cache(func):
    """简单的内存缓存装饰器"""
    cache = {}
    
    def wrapper(*args, **kwargs):
        # 生成缓存key
        key = str(args) + str(sorted(kwargs.items()))
        
        if key in cache:
            print(f"💾 缓存命中: {func.__name__}")
            return cache[key]
        
        print(f"🔄 计算结果: {func.__name__}")
        result = func(*args, **kwargs)
        cache[key] = result
        return result
    
    # 添加缓存管理方法
    wrapper.cache_info = lambda: f"缓存大小: {len(cache)}"
    wrapper.cache_clear = lambda: cache.clear()
    
    return wrapper

@simple_cache
def fibonacci(n):
    """斐波那契数列计算"""
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

# 测试
print(fibonacci(10))  # 第一次计算
print(fibonacci(10))  # 缓存命中
print(fibonacci.cache_info())  # 查看缓存信息
```

### 7.2 带过期时间的缓存


```python
import time

def timed_cache(expire_seconds=60):
    """带过期时间的缓存装饰器"""
    def decorator(func):
        cache = {}
        
        def wrapper(*args, **kwargs):
            key = str(args) + str(sorted(kwargs.items()))
            now = time.time()
            
            # 检查缓存是否存在且未过期
            if key in cache:
                value, timestamp = cache[key]
                if now - timestamp < expire_seconds:
                    print(f"💾 缓存命中: {func.__name__}")
                    return value
                else:
                    print(f"⏰ 缓存过期: {func.__name__}")
                    del cache[key]
            
            # 计算新结果并缓存
            print(f"🔄 计算结果: {func.__name__}")
            result = func(*args, **kwargs)
            cache[key] = (result, now)
            return result
        
        wrapper.cache_info = lambda: {
            'size': len(cache),
            'keys': list(cache.keys())
        }
        
        return wrapper
    return decorator

@timed_cache(expire_seconds=5)  # 5秒过期
def get_weather(city):
    """模拟获取天气信息"""
    import random
    time.sleep(1)  # 模拟网络请求
    return f"{city}: {random.choice(['晴天', '下雨', '多云'])}"

# 测试
print(get_weather("北京"))  # 计算结果
print(get_weather("北京"))  # 缓存命中
time.sleep(6)
print(get_weather("北京"))  # 缓存过期，重新计算
```

### 7.3 LRU缓存实现


```python
class LRUCache:
    """最近最少使用缓存"""
    
    def __init__(self, max_size=128):
        self.max_size = max_size
        self.cache = {}
        self.access_order = []  # 记录访问顺序
    
    def get(self, key):
        if key in self.cache:
            # 更新访问顺序
            self.access_order.remove(key)
            self.access_order.append(key)
            return self.cache[key]
        return None
    
    def put(self, key, value):
        if key in self.cache:
            # 更新现有键
            self.access_order.remove(key)
        elif len(self.cache) >= self.max_size:
            # 删除最久未使用的键
            oldest = self.access_order.pop(0)
            del self.cache[oldest]
        
        self.cache[key] = value
        self.access_order.append(key)

def lru_cache(max_size=128):
    """LRU缓存装饰器"""
    def decorator(func):
        cache = LRUCache(max_size)
        
        def wrapper(*args, **kwargs):
            key = str(args) + str(sorted(kwargs.items()))
            
            result = cache.get(key)
            if result is not None:
                print(f"💾 LRU缓存命中: {func.__name__}")
                return result
            
            print(f"🔄 计算结果: {func.__name__}")
            result = func(*args, **kwargs)
            cache.put(key, result)
            return result
        
        wrapper.cache_info = lambda: f"LRU缓存大小: {len(cache.cache)}"
        return wrapper
    return decorator

@lru_cache(max_size=3)
def expensive_calculation(x):
    import time
    time.sleep(0.5)  # 模拟耗时计算
    return x ** 2
```

---

## 8. 🔄 同步装饰器


### 8.1 线程安全装饰器


**问题场景**：多线程环境下，函数可能被同时调用，导致数据竞争

```python
import threading
import time

def thread_safe(func):
    """线程安全装饰器"""
    lock = threading.Lock()
    
    def wrapper(*args, **kwargs):
        with lock:
            print(f"🔒 线程 {threading.current_thread().name} 获得锁")
            result = func(*args, **kwargs)
            print(f"🔓 线程 {threading.current_thread().name} 释放锁")
            return result
    return wrapper

# 共享计数器
counter = 0

@thread_safe
def increment():
    """线程安全的计数器增加"""
    global counter
    temp = counter
    time.sleep(0.1)  # 模拟一些处理时间
    counter = temp + 1
    print(f"计数器: {counter}")

# 测试多线程
def test_threading():
    threads = []
    for i in range(3):
        thread = threading.Thread(target=increment, name=f"Thread-{i+1}")
        threads.append(thread)
        thread.start()
    
    for thread in threads:
        thread.join()

test_threading()
```

### 8.2 单例模式装饰器


```python
import threading

def singleton_with_lock(cls):
    """线程安全的单例装饰器"""
    instances = {}
    lock = threading.Lock()
    
    def get_instance(*args, **kwargs):
        if cls not in instances:
            with lock:
                # 双重检查锁定模式
                if cls not in instances:
                    print(f"创建 {cls.__name__} 实例")
                    instances[cls] = cls(*args, **kwargs)
        return instances[cls]
    
    return get_instance

@singleton_with_lock
class DatabaseConnection:
    def __init__(self):
        self.connection_id = id(self)
        print(f"建立数据库连接: {self.connection_id}")

# 多线程测试
def create_connection():
    db = DatabaseConnection()
    print(f"线程 {threading.current_thread().name} 得到连接: {db.connection_id}")

# 启动多个线程
threads = []
for i in range(3):
    thread = threading.Thread(target=create_connection, name=f"Worker-{i+1}")
    threads.append(thread)
    thread.start()

for thread in threads:
    thread.join()
```

### 8.3 异步装饰器


```python
import asyncio
import time

def async_timer(func):
    """异步函数计时装饰器"""
    async def wrapper(*args, **kwargs):
        start = time.time()
        result = await func(*args, **kwargs)
        elapsed = time.time() - start
        print(f"异步函数 {func.__name__} 执行时间: {elapsed:.2f} 秒")
        return result
    return wrapper

def sync_to_async(func):
    """将同步函数转换为异步函数"""
    async def wrapper(*args, **kwargs):
        loop = asyncio.get_event_loop()
        # 在线程池中执行同步函数
        result = await loop.run_in_executor(None, func, *args, **kwargs)
        return result
    return wrapper

@async_timer
async def async_task(n):
    """异步任务"""
    await asyncio.sleep(1)
    return f"异步任务完成: {n}"

@sync_to_async
def sync_task(n):
    """同步任务转异步"""
    time.sleep(1)
    return f"同步任务完成: {n}"

# 使用示例
async def main():
    # 并发执行异步任务
    tasks = [
        async_task(1),
        async_task(2),
        sync_task(3)  # 同步函数也能异步执行
    ]
    
    results = await asyncio.gather(*tasks)
    for result in results:
        print(result)

# 运行异步程序
# asyncio.run(main())
```

---

## 9. 🎖️ 装饰器最佳实践


### 9.1 保持函数元信息


**问题**：装饰器会丢失原函数的名称、文档等信息

```python
from functools import wraps

def bad_decorator(func):
    """不好的装饰器：丢失函数信息"""
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs)
    return wrapper

def good_decorator(func):
    """好的装饰器：保持函数信息"""
    @wraps(func)  # 关键：使用wraps保持原函数信息
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs)
    return wrapper

def example_function():
    """这是一个示例函数"""
    pass

# 对比效果
bad_decorated = bad_decorator(example_function)
good_decorated = good_decorator(example_function)

print("坏装饰器结果:")
print(f"函数名: {bad_decorated.__name__}")      # wrapper
print(f"文档: {bad_decorated.__doc__}")         # None

print("\n好装饰器结果:")
print(f"函数名: {good_decorated.__name__}")     # example_function  
print(f"文档: {good_decorated.__doc__}")        # 这是一个示例函数
```

### 9.2 装饰器参数化通用模板


```python
from functools import wraps

def create_decorator(decorator_name):
    """创建装饰器的通用模板"""
    def decorator_factory(*decorator_args, **decorator_kwargs):
        def decorator(func):
            @wraps(func)
            def wrapper(*args, **kwargs):
                # 装饰器逻辑前
                print(f"[{decorator_name}] 执行前准备")
                
                try:
                    # 执行原函数
                    result = func(*args, **kwargs)
                    
                    # 装饰器逻辑后
                    print(f"[{decorator_name}] 执行后清理")
                    return result
                    
                except Exception as e:
                    # 异常处理
                    print(f"[{decorator_name}] 异常处理: {e}")
                    raise
                    
            return wrapper
        return decorator
    return decorator_factory

# 使用模板创建具体装饰器
monitoring = create_decorator("MONITOR")

@monitoring()
def business_function():
    return "业务逻辑执行"
```

### 9.3 装饰器性能优化


```python
import time
from functools import wraps, lru_cache

def optimized_cache(max_size=128, typed=False):
    """优化的缓存装饰器"""
    def decorator(func):
        # 使用Python内置的lru_cache，性能更好
        cached_func = lru_cache(maxsize=max_size, typed=typed)(func)
        
        @wraps(func)
        def wrapper(*args, **kwargs):
            return cached_func(*args, **kwargs)
        
        # 暴露缓存管理接口
        wrapper.cache_info = cached_func.cache_info
        wrapper.cache_clear = cached_func.cache_clear
        wrapper.cache_parameters = {
            'maxsize': max_size,
            'typed': typed
        }
        
        return wrapper
    return decorator

def performance_monitor(func):
    """性能监控装饰器"""
    @wraps(func)
    def wrapper(*args, **kwargs):
        start_time = time.perf_counter()  # 使用高精度计时器
        start_memory = get_memory_usage()  # 假设有内存监控函数
        
        try:
            result = func(*args, **kwargs)
            
            # 记录性能数据
            elapsed_time = time.perf_counter() - start_time
            memory_used = get_memory_usage() - start_memory
            
            # 异步记录日志，不影响主流程
            log_performance_async(func.__name__, elapsed_time, memory_used)
            
            return result
            
        except Exception as e:
            # 即使出错也要记录性能数据
            elapsed_time = time.perf_counter() - start_time
            log_error_async(func.__name__, elapsed_time, str(e))
            raise
            
    return wrapper

def get_memory_usage():
    """获取当前内存使用量（简化实现）"""
    import psutil
    process = psutil.Process()
    return process.memory_info().rss / 1024 / 1024  # MB

def log_performance_async(func_name, time_taken, memory):
    """异步记录性能日志"""
    # 实际应用中可以用队列或异步日志库
    print(f"性能报告: {func_name} 耗时 {time_taken:.4f}s, 内存 {memory:.2f}MB")

def log_error_async(func_name, time_taken, error):
    """异步记录错误日志"""
    print(f"错误报告: {func_name} 耗时 {time_taken:.4f}s, 错误: {error}")
```

### 9.4 装饰器组合的最佳实践


```python
from functools import wraps

class DecoratorChain:
    """装饰器链管理器"""
    
    def __init__(self):
        self.decorators = []
    
    def add(self, decorator):
        """添加装饰器到链中"""
        self.decorators.append(decorator)
        return self
    
    def apply(self, func):
        """应用所有装饰器到函数"""
        result = func
        # 从后往前应用装饰器
        for decorator in reversed(self.decorators):
            result = decorator(result)
        return result

# 预定义常用装饰器组合
def web_api_decorators():
    """Web API常用装饰器组合"""
    return DecoratorChain()\
        .add(performance_monitor)\
        .add(auth_required)\
        .add(rate_limiter(100, 60))\
        .add(json_response)

def data_processing_decorators():
    """数据处理常用装饰器组合"""
    return DecoratorChain()\
        .add(optimized_cache(max_size=1000))\
        .add(performance_monitor)\
        .add(error_handler)

# 使用装饰器组合
api_chain = web_api_decorators()

@api_chain.apply
def get_user_info(user_id):
    return {"user_id": user_id, "name": "张三"}
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


```
🔸 装饰器本质：接收函数，返回新函数的高阶函数
🔸 带参数装饰器：三层结构 - 参数层、装饰器层、包装器层
🔸 类装饰器：装饰整个类，添加类级别功能
🔸 装饰器类：实现__call__方法的类，用作装饰器
🔸 多重装饰器：从下往上包装，从外往内执行
```

### 10.2 实际应用价值


**🎯 常见应用场景**：
- **日志记录**：记录函数调用、参数、返回值
- **性能监控**：统计执行时间、内存使用
- **权限控制**：检查用户权限、角色验证
- **缓存优化**：避免重复计算，提升性能
- **错误处理**：统一异常捕获和处理
- **参数验证**：检查输入参数的有效性

### 10.3 关键理解要点


**🔹 装饰器的执行时机**：
```
定义时期：装饰器代码执行，返回新函数
调用时期：包装器函数执行，调用原函数
```

**🔹 参数传递的最佳实践**：
```python
def decorator(func):
    @wraps(func)  # 保持函数元信息
    def wrapper(*args, **kwargs):  # 接收任意参数
        # 装饰逻辑
        return func(*args, **kwargs)  # 传递所有参数
    return wrapper
```

**🔹 装饰器性能考虑**：
```
轻量级装饰器：简单逻辑，最小开销
缓存装饰器：用内存换时间
同步装饰器：牺牲并发性换取安全性
```

### 10.4 实践指导原则


**✅ 推荐做法**：
- 使用 `@wraps` 保持函数信息
- 用 `*args, **kwargs` 处理参数
- 装饰器功能单一，职责明确
- 提供缓存清理等管理接口
- 考虑线程安全和性能影响

**❌ 避免错误**：
- 不要在装饰器中修改原函数
- 避免过度复杂的装饰器逻辑
- 不要忽略异常处理
- 避免装饰器间的强耦合

### 10.5 学习路径建议


**🎯 掌握顺序**：
1. **基础装饰器** → 理解核心概念和语法
2. **带参数装饰器** → 掌握三层结构
3. **实用装饰器** → 缓存、日志、计时等
4. **高级模式** → 类装饰器、装饰器类
5. **最佳实践** → 性能优化、错误处理

**💡 记忆口诀**：
```
装饰器模式要记牢，函数包装增功能
带参三层要分清，类装饰器改整体
多重装饰从下包，执行顺序要搞懂
保持信息用wraps，性能安全要考虑
```

**🚀 实践建议**：
- 从简单的日志装饰器开始练习
- 尝试实现自己的缓存装饰器
- 练习多重装饰器的组合使用
- 在实际项目中应用装饰器模式
- 关注装饰器的性能影响和调试技巧