---
title: 10、网络编程基础
---
## 📚 目录

1. [网络编程概述](#1-网络编程概述)
2. [HTTP协议理解](#2-HTTP协议理解)
3. [requests库使用](#3-requests库使用)
4. [网页内容抓取](#4-网页内容抓取)
5. [Web API调用](#5-Web-API调用)
6. [简单Web服务器](#6-简单Web服务器)
7. [Socket编程基础](#7-Socket编程基础)
8. [HTTP客户端编程实战](#8-HTTP客户端编程实战)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🌐 网络编程概述


### 1.1 什么是网络编程


**简单理解**：网络编程就是让你的程序能够通过网络和其他程序"对话"

想象一下日常生活：
- 📱 **微信聊天**：你的手机 ↔ 微信服务器 ↔ 朋友的手机
- 🛒 **网购下单**：你的浏览器 ↔ 淘宝服务器 ↔ 商家系统
- 📺 **看视频**：你的播放器 ↔ B站服务器 ↔ 视频文件

网络编程让程序具备了这种"远程对话"的能力。

### 1.2 网络编程的核心概念


**🔸 客户端与服务器**
```
客户端（Client）：主动发起请求的一方
服务器（Server）：被动响应请求的一方

就像打电话：
- 拨号的人 = 客户端
- 接电话的人 = 服务器
```

**🔸 协议**
协议就是双方约定的"对话规则"，就像：
- **中文对话**：双方都用中文交流
- **英文对话**：双方都用英文交流
- **HTTP协议**：浏览器和服务器都按HTTP规则交流

### 1.3 Python网络编程的优势


| 特点 | **说明** | **实际好处** |
|------|---------|-------------|
| 🐍 **简单易学** | `语法简洁，库丰富` | `几行代码就能发送网络请求` |
| 📚 **库生态好** | `requests、urllib等成熟库` | `不用重复造轮子` |
| ⚡ **开发效率高** | `快速实现网络功能` | `专注业务逻辑，不纠结底层` |
| 🔧 **功能强大** | `从简单爬虫到复杂API` | `满足各种网络编程需求` |

---

## 2. 📡 HTTP协议理解


### 2.1 HTTP协议是什么


**通俗解释**：HTTP就像邮局的标准邮件格式

```
普通邮件格式：
┌─────────────────┐
│ 收件人：张三      │ ← 请求地址
│ 发件人：李四      │ ← 来源信息  
│ 主题：问候       │ ← 请求类型
├─────────────────┤
│ 你好，最近好吗？   │ ← 请求内容
└─────────────────┘

HTTP请求格式：
┌─────────────────┐
│ GET /hello HTTP/1.1 │ ← 请求行
│ Host: www.baidu.com │ ← 请求头
│ User-Agent: Python  │
├─────────────────────┤
│ (请求体，GET请求通常为空) │
└─────────────────────┘
```

### 2.2 请求响应机制


**🔄 HTTP工作流程**

```
第1步：客户端发送请求
你的程序 → "我想要首页内容" → 服务器

第2步：服务器处理请求  
服务器 → 查找首页内容 → 准备响应

第3步：服务器返回响应
服务器 → "这是首页内容" → 你的程序

第4步：客户端处理响应
你的程序 → 解析内容 → 显示给用户
```

### 2.3 常用HTTP方法


| 方法 | **作用** | **生活类比** | **使用场景** |
|------|---------|-------------|-------------|
| 📖 **GET** | `获取数据` | `向图书管理员要书` | `浏览网页、查询信息` |
| 📝 **POST** | `提交数据` | `向银行柜台存钱` | `登录、注册、提交表单` |
| ✏️ **PUT** | `更新数据` | `替换文件内容` | `修改用户信息` |
| 🗑️ **DELETE** | `删除数据` | `删除文件` | `删除账号、文章` |

### 2.4 HTTP状态码


**🚦 状态码就像交通信号灯**

```
🟢 2xx 成功类：
200 OK - 绿灯，一切正常
201 Created - 创建成功

🟡 3xx 重定向类：  
301 Moved Permanently - 永久搬家
302 Found - 临时搬家

🔴 4xx 客户端错误：
400 Bad Request - 你说的话我听不懂
401 Unauthorized - 你没有权限
404 Not Found - 找不到你要的东西

💥 5xx 服务器错误：
500 Internal Server Error - 服务器出故障了
503 Service Unavailable - 服务器太忙了
```

---

## 3. 🚀 requests库使用


### 3.1 为什么选择requests


> 💡 **requests库的魅力**  
> Python内置的urllib虽然功能完整，但使用复杂。requests库让HTTP请求变得像写普通Python代码一样简单。

**安装requests库**
```bash
pip install requests
```

### 3.2 基础GET请求


```python
import requests

# 最简单的GET请求
response = requests.get('https://httpbin.org/get')

# 查看响应内容
print(f"状态码: {response.status_code}")
print(f"响应内容: {response.text}")
```

**🔸 带参数的GET请求**
```python
# 方式1: 直接在URL中写参数
url = 'https://httpbin.org/get?name=张三&age=25'
response = requests.get(url)

# 方式2: 使用params参数(推荐)
params = {
    'name': '张三',
    'age': 25,
    'city': '北京'
}
response = requests.get('https://httpbin.org/get', params=params)
print(f"实际请求的URL: {response.url}")
```

### 3.3 POST请求发送数据


```python
# 发送表单数据
data = {
    'username': 'zhangsan',
    'password': '123456'
}
response = requests.post('https://httpbin.org/post', data=data)

# 发送JSON数据
json_data = {
    'name': '张三',
    'email': 'zhangsan@qq.com'
}
response = requests.post('https://httpbin.org/post', json=json_data)
```

### 3.4 处理请求头


```python
# 设置请求头
headers = {
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
    'Accept': 'application/json',
    'Authorization': 'Bearer your_token_here'
}

response = requests.get('https://httpbin.org/headers', headers=headers)
```

**常用请求头说明**：
- **User-Agent**: 告诉服务器你是什么浏览器（防止被识别为爬虫）
- **Accept**: 告诉服务器你希望接收什么格式的数据
- **Authorization**: 身份验证信息

### 3.5 会话管理


**🔸 什么是会话**
会话就像在银行办业务，你只需要验证一次身份，然后可以连续办多项业务。

```python
# 使用会话保持登录状态
session = requests.Session()

# 第一次请求：登录
login_data = {'username': 'zhangsan', 'password': '123456'}
session.post('https://example.com/login', data=login_data)

# 后续请求会自动携带登录信息
profile = session.get('https://example.com/profile')  # 已登录状态
orders = session.get('https://example.com/orders')    # 已登录状态
```

### 3.6 处理响应内容


```python
response = requests.get('https://httpbin.org/json')

# 不同格式的响应处理
print(f"文本内容: {response.text}")           # 字符串格式
print(f"字节内容: {response.content}")        # 字节格式  
print(f"JSON数据: {response.json()}")        # 直接解析JSON
print(f"响应头: {response.headers}")         # 查看响应头
```

### 3.7 错误处理


```python
try:
    response = requests.get('https://httpbin.org/status/404', timeout=5)
    response.raise_for_status()  # 如果状态码不是200会抛出异常
    print("请求成功")
except requests.exceptions.HTTPError:
    print(f"HTTP错误: {response.status_code}")
except requests.exceptions.ConnectionError:
    print("连接错误，请检查网络")
except requests.exceptions.Timeout:
    print("请求超时")
except requests.exceptions.RequestException as e:
    print(f"请求出错: {e}")
```

---

## 4. 🕷️ 网页内容抓取


### 4.1 网页抓取基础概念


**什么是网页抓取**
就像用复印机复印文件一样，网页抓取是把网页内容"复印"到你的程序里。

**🔸 抓取流程**
```
第1步：发送请求 → 访问目标网页
第2步：获取HTML → 得到网页源代码  
第3步：解析内容 → 提取有用信息
第4步：数据处理 → 保存或使用数据
```

### 4.2 简单的网页抓取


```python
import requests
from bs4 import BeautifulSoup

# 抓取网页内容
url = 'https://quotes.toscrape.com/'
response = requests.get(url)

# 解析HTML内容
soup = BeautifulSoup(response.text, 'html.parser')

# 提取所有名言
quotes = soup.find_all('div', class_='quote')

for quote in quotes:
    text = quote.find('span', class_='text').text
    author = quote.find('small', class_='author').text
    print(f"名言: {text}")
    print(f"作者: {author}")
    print("-" * 50)
```

### 4.3 处理常见问题


**🔸 处理中文编码**
```python
response = requests.get(url)
response.encoding = 'utf-8'  # 指定编码
content = response.text
```

**🔸 模拟浏览器访问**
```python
headers = {
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'
}
response = requests.get(url, headers=headers)
```

**🔸 处理反爬虫**
```python
import time
import random

# 添加随机延时
def polite_crawl(urls):
    for url in urls:
        response = requests.get(url, headers=headers)
        # 处理响应...
        
        # 随机等待1-3秒，模拟人类浏览行为
        time.sleep(random.uniform(1, 3))
```

### 4.4 数据保存


```python
import csv
import json

# 保存为CSV文件
def save_to_csv(data, filename):
    with open(filename, 'w', newline='', encoding='utf-8') as file:
        writer = csv.writer(file)
        writer.writerow(['标题', '作者', '内容'])  # 表头
        for item in data:
            writer.writerow([item['title'], item['author'], item['content']])

# 保存为JSON文件  
def save_to_json(data, filename):
    with open(filename, 'w', encoding='utf-8') as file:
        json.dump(data, file, ensure_ascii=False, indent=2)
```

---

## 5. 🔌 Web API调用


### 5.1 什么是Web API


**通俗理解**：API就像餐厅的菜单

```
餐厅菜单类比：
┌─────────────────┐
│ 1. 宫保鸡丁 - 28元 │ ← 菜品名称和价格
│ 2. 红烧肉 - 32元   │
│ 3. 蒸蛋羹 - 12元   │
└─────────────────┘

Web API类比：
┌─────────────────┐
│ GET /users - 获取用户列表 │ ← 接口地址和功能
│ POST /login - 用户登录   │
│ PUT /profile - 更新资料  │
└─────────────────┘
```

**🔸 API的好处**
- **标准化**：大家都按相同规则调用
- **简单化**：不需要知道内部实现
- **复用性**：一个API可以被多个程序使用

### 5.2 调用RESTful API


```python
# GitHub API示例：获取用户信息
def get_github_user(username):
    url = f'https://api.github.com/users/{username}'
    response = requests.get(url)
    
    if response.status_code == 200:
        user_data = response.json()
        return {
            'name': user_data.get('name'),
            'followers': user_data.get('followers'),
            'public_repos': user_data.get('public_repos')
        }
    else:
        print(f"获取用户信息失败: {response.status_code}")
        return None

# 使用示例
user_info = get_github_user('octocat')
if user_info:
    print(f"用户名: {user_info['name']}")
    print(f"粉丝数: {user_info['followers']}")
    print(f"公开仓库: {user_info['public_repos']}")
```

### 5.3 处理API认证


**🔸 API Key认证**
```python
# 天气API示例
def get_weather(city, api_key):
    url = 'https://api.openweathermap.org/data/2.5/weather'
    params = {
        'q': city,
        'appid': api_key,
        'units': 'metric',
        'lang': 'zh_cn'
    }
    
    response = requests.get(url, params=params)
    if response.status_code == 200:
        data = response.json()
        return {
            'city': data['name'],
            'temperature': data['main']['temp'],
            'description': data['weather'][0]['description']
        }
    return None
```

**🔸 Bearer Token认证**
```python
def api_call_with_token(url, token):
    headers = {
        'Authorization': f'Bearer {token}',
        'Content-Type': 'application/json'
    }
    response = requests.get(url, headers=headers)
    return response.json()
```

### 5.4 API错误处理


```python
def robust_api_call(url, max_retries=3):
    for attempt in range(max_retries):
        try:
            response = requests.get(url, timeout=10)
            
            if response.status_code == 200:
                return response.json()
            elif response.status_code == 429:  # 请求过于频繁
                print("请求频率过高，等待60秒...")
                time.sleep(60)
            elif response.status_code == 500:  # 服务器错误
                print("服务器错误，2秒后重试...")
                time.sleep(2)
            else:
                print(f"API调用失败: {response.status_code}")
                break
                
        except requests.exceptions.Timeout:
            print(f"请求超时，第{attempt + 1}次重试...")
            time.sleep(2)
        except requests.exceptions.ConnectionError:
            print("网络连接错误...")
            break
    
    return None
```

---

## 6. 🌐 简单Web服务器


### 6.1 HTTP服务器基础


**什么是Web服务器**
Web服务器就像一个自动售货机：
- 🪙 **投币（请求）**：客户端发送HTTP请求
- 🥤 **出商品（响应）**：服务器返回HTML页面或数据
- 🔄 **重复服务**：可以处理多个客户端请求

### 6.2 使用http.server创建简单服务器


```python
# 最简单的文件服务器
import http.server
import socketserver
import os

# 设置服务器参数
PORT = 8000
DIRECTORY = "web_files"  # 要服务的目录

# 创建目录（如果不存在）
os.makedirs(DIRECTORY, exist_ok=True)

# 切换到目标目录
os.chdir(DIRECTORY)

# 创建请求处理器
Handler = http.server.SimpleHTTPRequestHandler

# 启动服务器
with socketserver.TCPServer(("", PORT), Handler) as httpd:
    print(f"服务器启动在: http://localhost:{PORT}")
    print("按 Ctrl+C 停止服务器")
    httpd.serve_forever()
```

### 6.3 自定义HTTP服务器


```python
import http.server
import json
from urllib.parse import urlparse, parse_qs

class MyHTTPRequestHandler(http.server.BaseHTTPRequestHandler):
    
    def do_GET(self):
        """处理GET请求"""
        # 解析URL
        parsed_path = urlparse(self.path)
        
        if parsed_path.path == '/':
            self.send_homepage()
        elif parsed_path.path == '/api/time':
            self.send_current_time()
        elif parsed_path.path == '/api/hello':
            self.send_hello_message()
        else:
            self.send_404()
    
    def do_POST(self):
        """处理POST请求"""
        if self.path == '/api/echo':
            self.handle_echo()
        else:
            self.send_404()
    
    def send_homepage(self):
        """发送首页"""
        html = """
        <!DOCTYPE html>
        <html>
        <head><title>我的Python服务器</title></head>
        <body>
            <h1>欢迎来到我的Python服务器！</h1>
            <p><a href="/api/time">查看当前时间</a></p>
            <p><a href="/api/hello">获取问候消息</a></p>
        </body>
        </html>
        """
        self.send_response(200)
        self.send_header('Content-type', 'text/html; charset=utf-8')
        self.end_headers()
        self.wfile.write(html.encode('utf-8'))
    
    def send_current_time(self):
        """返回当前时间的API"""
        import datetime
        current_time = datetime.datetime.now().strftime('%Y-%m-%d %H:%M:%S')
        
        response_data = {
            'time': current_time,
            'timestamp': datetime.datetime.now().timestamp()
        }
        
        self.send_json_response(response_data)
    
    def send_hello_message(self):
        """返回问候消息"""
        response_data = {
            'message': '你好，欢迎使用Python服务器！',
            'status': 'success'
        }
        self.send_json_response(response_data)
    
    def handle_echo(self):
        """回显POST数据"""
        content_length = int(self.headers['Content-Length'])
        post_data = self.rfile.read(content_length)
        
        try:
            # 尝试解析JSON数据
            json_data = json.loads(post_data.decode('utf-8'))
            response_data = {
                'echo': json_data,
                'message': '数据接收成功'
            }
        except:
            response_data = {
                'error': '无法解析JSON数据'
            }
        
        self.send_json_response(response_data)
    
    def send_json_response(self, data):
        """发送JSON响应"""
        response_json = json.dumps(data, ensure_ascii=False, indent=2)
        self.send_response(200)
        self.send_header('Content-type', 'application/json; charset=utf-8')
        self.end_headers()
        self.wfile.write(response_json.encode('utf-8'))
    
    def send_404(self):
        """发送404错误"""
        self.send_response(404)
        self.send_header('Content-type', 'text/html; charset=utf-8')
        self.end_headers()
        self.wfile.write(b'<h1>404 - Page Not Found</h1>')

# 启动服务器
if __name__ == '__main__':
    PORT = 8000
    with socketserver.TCPServer(("", PORT), MyHTTPRequestHandler) as httpd:
        print(f"自定义服务器启动在: http://localhost:{PORT}")
        httpd.serve_forever()
```

---

## 7. 🔌 Socket编程基础


### 7.1 Socket概念理解


**什么是Socket**
Socket就像电话系统：
- 📞 **电话机**：Socket对象
- 📱 **电话号码**：IP地址 + 端口号  
- 📡 **电话线路**：网络连接
- 💬 **通话内容**：数据传输

```
Socket通信过程：
客户端Socket                    服务器Socket
     |                              |
  1. 创建Socket                  1. 创建Socket
     |                              |
  2. 连接服务器 ←→ TCP握手 ←→    2. 绑定地址并监听
     |                              |
  3. 发送数据 ←→ 数据传输 ←→      3. 接受连接
     |                              |
  4. 接收响应 ←→ 数据传输 ←→      4. 处理数据
     |                              |
  5. 关闭连接 ←→ TCP挥手 ←→      5. 关闭连接
```

### 7.2 TCP Socket编程


**🔸 TCP服务器端**
```python
import socket
import threading

def handle_client(client_socket, address):
    """处理单个客户端连接"""
    print(f"客户端 {address} 已连接")
    
    try:
        while True:
            # 接收数据
            data = client_socket.recv(1024).decode('utf-8')
            if not data:
                break
            
            print(f"收到消息: {data}")
            
            # 发送响应
            response = f"服务器收到: {data}"
            client_socket.send(response.encode('utf-8'))
    
    except Exception as e:
        print(f"处理客户端 {address} 时出错: {e}")
    
    finally:
        client_socket.close()
        print(f"客户端 {address} 断开连接")

def start_tcp_server():
    # 创建TCP Socket
    server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    
    # 设置端口可重用（避免重启时端口被占用）
    server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
    
    # 绑定地址和端口
    server_address = ('localhost', 8888)
    server_socket.bind(server_address)
    
    # 开始监听（最多5个等待连接）
    server_socket.listen(5)
    print(f"TCP服务器启动在 {server_address}")
    
    try:
        while True:
            # 等待客户端连接
            client_socket, address = server_socket.accept()
            
            # 为每个客户端创建单独的线程
            client_thread = threading.Thread(
                target=handle_client, 
                args=(client_socket, address)
            )
            client_thread.daemon = True  # 守护线程
            client_thread.start()
    
    except KeyboardInterrupt:
        print("\n服务器关闭")
    finally:
        server_socket.close()

if __name__ == '__main__':
    start_tcp_server()
```

**🔸 TCP客户端**
```python
import socket

def tcp_client():
    # 创建TCP Socket
    client_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    
    try:
        # 连接服务器
        server_address = ('localhost', 8888)
        client_socket.connect(server_address)
        print(f"已连接到服务器 {server_address}")
        
        while True:
            # 获取用户输入
            message = input("请输入消息 (输入'quit'退出): ")
            if message.lower() == 'quit':
                break
            
            # 发送消息
            client_socket.send(message.encode('utf-8'))
            
            # 接收响应
            response = client_socket.recv(1024).decode('utf-8')
            print(f"服务器响应: {response}")
    
    except Exception as e:
        print(f"客户端错误: {e}")
    
    finally:
        client_socket.close()
        print("客户端断开连接")

if __name__ == '__main__':
    tcp_client()
```

### 7.3 UDP Socket编程


**🔸 UDP的特点**
- **无连接**：像发短信，不需要先建立连接
- **不可靠**：数据可能丢失，但速度快
- **适用场景**：直播、游戏、DNS查询等对速度要求高的场景

```python
# UDP服务器
import socket

def udp_server():
    # 创建UDP Socket
    server_socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    
    # 绑定地址
    server_address = ('localhost', 9999)
    server_socket.bind(server_address)
    print(f"UDP服务器启动在 {server_address}")
    
    try:
        while True:
            # 接收数据和客户端地址
            data, client_address = server_socket.recvfrom(1024)
            message = data.decode('utf-8')
            print(f"收到来自 {client_address} 的消息: {message}")
            
            # 发送响应
            response = f"服务器收到: {message}"
            server_socket.sendto(response.encode('utf-8'), client_address)
    
    except KeyboardInterrupt:
        print("\nUDP服务器关闭")
    finally:
        server_socket.close()

# UDP客户端
def udp_client():
    client_socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    server_address = ('localhost', 9999)
    
    try:
        while True:
            message = input("请输入消息 (输入'quit'退出): ")
            if message.lower() == 'quit':
                break
            
            # 发送数据
            client_socket.sendto(message.encode('utf-8'), server_address)
            
            # 接收响应
            data, _ = client_socket.recvfrom(1024)
            response = data.decode('utf-8')
            print(f"服务器响应: {response}")
    
    finally:
        client_socket.close()
```

---

## 8. 💻 HTTP客户端编程实战


### 8.1 构建完整的HTTP客户端


```python
import requests
import json
import time
from urllib.parse import urljoin

class HTTPClient:
    """功能完整的HTTP客户端"""
    
    def __init__(self, base_url=None, timeout=30):
        self.session = requests.Session()
        self.base_url = base_url
        self.timeout = timeout
        
        # 设置默认请求头
        self.session.headers.update({
            'User-Agent': 'Python HTTP Client 1.0',
            'Accept': 'application/json',
            'Content-Type': 'application/json'
        })
    
    def get(self, endpoint, params=None, **kwargs):
        """GET请求"""
        url = self._build_url(endpoint)
        return self._make_request('GET', url, params=params, **kwargs)
    
    def post(self, endpoint, data=None, json_data=None, **kwargs):
        """POST请求"""
        url = self._build_url(endpoint)
        if json_data:
            return self._make_request('POST', url, json=json_data, **kwargs)
        else:
            return self._make_request('POST', url, data=data, **kwargs)
    
    def put(self, endpoint, data=None, json_data=None, **kwargs):
        """PUT请求"""
        url = self._build_url(endpoint)
        if json_data:
            return self._make_request('PUT', url, json=json_data, **kwargs)
        else:
            return self._make_request('PUT', url, data=data, **kwargs)
    
    def delete(self, endpoint, **kwargs):
        """DELETE请求"""
        url = self._build_url(endpoint)
        return self._make_request('DELETE', url, **kwargs)
    
    def _build_url(self, endpoint):
        """构建完整URL"""
        if self.base_url:
            return urljoin(self.base_url, endpoint)
        return endpoint
    
    def _make_request(self, method, url, **kwargs):
        """发送HTTP请求"""
        kwargs.setdefault('timeout', self.timeout)
        
        try:
            response = self.session.request(method, url, **kwargs)
            return HTTPResponse(response)
        except requests.exceptions.RequestException as e:
            raise HTTPClientError(f"请求失败: {e}")
    
    def set_auth(self, token=None, username=None, password=None):
        """设置认证"""
        if token:
            self.session.headers['Authorization'] = f'Bearer {token}'
        elif username and password:
            self.session.auth = (username, password)
    
    def add_header(self, key, value):
        """添加请求头"""
        self.session.headers[key] = value
    
    def close(self):
        """关闭会话"""
        self.session.close()

class HTTPResponse:
    """HTTP响应包装器"""
    
    def __init__(self, response):
        self.response = response
        self.status_code = response.status_code
        self.headers = response.headers
        self.url = response.url
    
    @property
    def text(self):
        return self.response.text
    
    @property  
    def content(self):
        return self.response.content
    
    def json(self):
        """解析JSON响应"""
        try:
            return self.response.json()
        except json.JSONDecodeError:
            raise HTTPClientError("响应不是有效的JSON格式")
    
    def raise_for_status(self):
        """检查HTTP状态码"""
        self.response.raise_for_status()
    
    @property
    def ok(self):
        return self.response.ok

class HTTPClientError(Exception):
    """HTTP客户端异常"""
    pass

# 使用示例
def demo_http_client():
    # 创建HTTP客户端
    client = HTTPClient(base_url='https://jsonplaceholder.typicode.com')
    
    try:
        # GET请求获取数据
        print("=== 获取用户列表 ===")
        response = client.get('/users')
        if response.ok:
            users = response.json()
            for user in users[:3]:  # 只显示前3个
                print(f"用户: {user['name']} ({user['email']})")
        
        # POST请求创建数据
        print("\n=== 创建新文章 ===")
        new_post = {
            'title': '我的第一篇文章',
            'body': '这是文章内容...',
            'userId': 1
        }
        response = client.post('/posts', json_data=new_post)
        if response.ok:
            created_post = response.json()
            print(f"创建成功，文章ID: {created_post['id']}")
        
        # PUT请求更新数据
        print("\n=== 更新文章 ===")
        updated_post = {
            'id': 1,
            'title': '更新后的标题',
            'body': '更新后的内容',
            'userId': 1
        }
        response = client.put('/posts/1', json_data=updated_post)
        if response.ok:
            print("文章更新成功")
        
        # DELETE请求删除数据
        print("\n=== 删除文章 ===")
        response = client.delete('/posts/1')
        if response.ok:
            print("文章删除成功")
    
    except HTTPClientError as e:
        print(f"HTTP客户端错误: {e}")
    except Exception as e:
        print(f"未知错误: {e}")
    finally:
        client.close()

if __name__ == '__main__':
    demo_http_client()
```

### 8.2 requests高级用法


**🔸 代理设置**
```python
# 设置代理
proxies = {
    'http': 'http://proxy.example.com:8080',
    'https': 'https://proxy.example.com:8080'
}
response = requests.get('https://httpbin.org/ip', proxies=proxies)
```

**🔸 SSL证书处理**
```python
# 忽略SSL证书验证（仅用于测试）
response = requests.get('https://self-signed-cert.com', verify=False)

# 使用自定义证书
response = requests.get('https://example.com', verify='/path/to/cert.pem')
```

**🔸 文件上传下载**
```python
# 文件上传
with open('file.txt', 'rb') as f:
    files = {'file': f}
    response = requests.post('https://httpbin.org/post', files=files)

# 文件下载
def download_file(url, filename):
    with requests.get(url, stream=True) as response:
        response.raise_for_status()
        with open(filename, 'wb') as f:
            for chunk in response.iter_content(chunk_size=8192):
                f.write(chunk)
    print(f"文件下载完成: {filename}")
```

---

## 9. 📋 核心要点总结


### 9.1 必须掌握的核心概念


```
🔸 网络编程本质：让程序通过网络进行数据交换
🔸 HTTP协议：基于请求-响应模式的应用层协议
🔸 requests库：Python最友好的HTTP客户端库
🔸 Socket编程：网络通信的底层编程接口
🔸 API调用：通过标准接口获取或提交数据
```

### 9.2 学习路径建议


**📚 循序渐进的学习顺序**
```
第1阶段：理解概念
✅ HTTP协议基础 → 了解请求响应机制
✅ 状态码含义 → 知道各种响应状态

第2阶段：基础实践  
✅ requests基本用法 → GET/POST请求
✅ 处理响应数据 → JSON/HTML解析

第3阶段：进阶应用
✅ 网页抓取 → BeautifulSoup解析
✅ API调用 → 认证和错误处理

第4阶段：深入理解
✅ Socket编程 → TCP/UDP通信
✅ 服务器开发 → 简单HTTP服务器
```

### 9.3 实际应用场景


| 应用场景 | **技术选择** | **适用情况** |
|---------|-------------|-------------|
| 🕷️ **网页数据抓取** | `requests + BeautifulSoup` | `获取网站公开信息` |
| 🔌 **API接口调用** | `requests库` | `与第三方服务集成` |
| 📊 **数据收集** | `requests + 定时任务` | `定期获取外部数据` |
| 🌐 **简单Web服务** | `http.server` | `快速搭建测试服务` |
| 💬 **实时通信** | `Socket编程` | `聊天、游戏等应用` |

### 9.4 最佳实践建议


**🎯 网络编程最佳实践**

> ⚠️ **重要提醒**  
> 1. **遵守robots.txt**：爬取网站前检查robots.txt文件
> 2. **控制请求频率**：避免给服务器造成压力  
> 3. **处理异常情况**：网络不稳定时的重试机制
> 4. **保护敏感信息**：API密钥、密码等不要硬编码

```python
# 推荐的网络请求模板
def safe_request(url, max_retries=3, delay=1):
    """安全的网络请求函数"""
    headers = {
        'User-Agent': 'Mozilla/5.0 (compatible; Python Bot)'
    }
    
    for attempt in range(max_retries):
        try:
            response = requests.get(
                url, 
                headers=headers,
                timeout=10,
                verify=True  # 验证SSL证书
            )
            response.raise_for_status()
            return response
            
        except requests.exceptions.RequestException as e:
            if attempt < max_retries - 1:
                print(f"请求失败，{delay}秒后重试... ({e})")
                time.sleep(delay)
                delay *= 2  # 指数退避
            else:
                print(f"请求最终失败: {e}")
                return None
```

### 9.5 常见问题解决


**🔧 问题排查清单**

- [ ] **连接超时**：检查网络连接和防火墙设置
- [ ] **状态码错误**：确认URL正确性和服务器状态  
- [ ] **编码问题**：指定正确的字符编码
- [ ] **SSL错误**：检查证书有效性或使用verify参数
- [ ] **反爬虫限制**：添加User-Agent和请求延时

**核心记忆**：
- 网络编程让程序具备"远程对话"能力
- HTTP协议是Web通信的标准语言
- requests库让网络请求变得简单优雅
- Socket是网络通信的底层基础
- 实践中要平衡功能需求和网络礼仪