---
title: 4、类型检查与转换
---
## 📚 目录

1. [什么是类型检查与转换](#1-什么是类型检查与转换)
2. [type()函数的使用](#2-type函数的使用)
3. [isinstance()智能类型检查](#3-isinstance智能类型检查)
4. [hasattr()属性检查](#4-hasattr属性检查)
5. [隐式类型转换](#5-隐式类型转换)
6. [显式类型转换](#6-显式类型转换)
7. [类型转换异常处理](#7-类型转换异常处理)
8. [类型注解基础](#8-类型注解基础)
9. [动态类型特性理解](#9-动态类型特性理解)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🤔 什么是类型检查与转换


### 1.1 类型检查的含义

**类型检查**就像是"身份验证"，帮我们确认变量到底是什么类型的数据。

```
想象一下现实生活中：
你拿到一个盒子，想知道里面装的是什么
- 摇一摇听声音（类似type()函数）
- 看标签说明（类似isinstance()函数）
- 检查有没有特定功能（类似hasattr()函数）
```

**类型转换**就像是"格式变换"，把一种类型的数据变成另一种类型。

```
日常生活中的转换：
数字 "123" → 数值 123（字符串转整数）
温度 25°C → 77°F（摄氏度转华氏度）
人民币 ¥100 → $14.5（货币转换）
```

### 1.2 为什么需要类型检查

**🔸 避免程序错误**
```python
# 不检查类型可能出错
def add_numbers(a, b):
    return a + b

result = add_numbers("5", 3)  # 得到 "53" 而不是 8
```

**🔸 编写更安全的代码**
```python
# 检查类型后更安全
def safe_add(a, b):
    if isinstance(a, (int, float)) and isinstance(b, (int, float)):
        return a + b
    else:
        return "错误：只能计算数字"
```

### 1.3 Python的类型系统特点

```
Python的类型特色：
🟢 动态类型：变量类型运行时确定
🟢 强类型：不同类型间不会隐式转换  
🟢 鸭子类型："看起来像鸭子，就当鸭子用"

对比其他语言：
Java：静态强类型（编译时确定类型）
JavaScript：动态弱类型（自动转换类型）
C++：静态强类型（类型严格检查）
```

---

## 2. 🔍 type()函数的使用


### 2.1 type()函数基本用法

**type()函数**就像是给数据做"身份证检查"，告诉你这个变量确切是什么类型。

```python
# 基本使用方法
name = "Alice"
age = 25
height = 1.75
is_student = True

print(type(name))      # <class 'str'>
print(type(age))       # <class 'int'>  
print(type(height))    # <class 'float'>
print(type(is_student)) # <class 'bool'>
```

### 2.2 type()检查的详细信息

```python
# type()返回的具体类型信息
numbers = [1, 2, 3]
person = {"name": "Bob", "age": 30}

print(type(numbers))   # <class 'list'>
print(type(person))    # <class 'dict'>

# 获取类型名称字符串
print(type(numbers).__name__)  # list
print(type(person).__name__)   # dict
```

### 2.3 用type()进行类型比较

```python
# 直接比较类型
def check_type_exact(value):
    if type(value) == str:
        return "这是字符串"
    elif type(value) == int:
        return "这是整数"
    elif type(value) == list:
        return "这是列表"
    else:
        return f"这是{type(value).__name__}类型"

# 测试
print(check_type_exact("hello"))  # 这是字符串
print(check_type_exact(42))       # 这是整数
print(check_type_exact([1,2,3]))  # 这是列表
```

### 2.4 type()的局限性

**⚠️ 注意：type()只检查确切类型，不考虑继承关系**

```python
# type()不认识"亲戚关系"
class Animal:
    def speak(self):
        pass

class Dog(Animal):  # Dog继承自Animal
    def speak(self):
        return "汪汪"

my_dog = Dog()

print(type(my_dog) == Dog)     # True  
print(type(my_dog) == Animal)  # False（即使Dog继承自Animal）

# 这就是type()的限制：只认"本人"，不认"家族"
```

---

## 3. 🧠 isinstance()智能类型检查


### 3.1 isinstance()的智能之处

**isinstance()函数**比type()更聪明，它不仅认识"本人"，还认识"家族关系"（继承）。

```python
# isinstance()的基本语法
isinstance(对象, 类型)  # 返回True或False
```

### 3.2 基本类型检查

```python
# 检查基本数据类型
value = 42

print(isinstance(value, int))    # True
print(isinstance(value, str))    # False
print(isinstance(value, (int, float)))  # True（可以检查多个类型）

# 实际应用示例
def process_data(data):
    if isinstance(data, str):
        return f"处理文本：{data.upper()}"
    elif isinstance(data, (int, float)):
        return f"处理数字：{data * 2}"
    elif isinstance(data, list):
        return f"处理列表：共{len(data)}个元素"
    else:
        return "未知数据类型"
```

### 3.3 isinstance()处理继承关系

```python
# isinstance()理解继承关系
class Vehicle:  # 交通工具（父类）
    def move(self):
        pass

class Car(Vehicle):  # 汽车（子类）
    def drive(self):
        return "开车中..."

class Bicycle(Vehicle):  # 自行车（子类）  
    def pedal(self):
        return "骑车中..."

my_car = Car()

print(isinstance(my_car, Car))      # True（是汽车）
print(isinstance(my_car, Vehicle))  # True（也是交通工具）
print(isinstance(my_car, Bicycle)) # False（不是自行车）

# isinstance()的智能：知道汽车也是交通工具
```

### 3.4 多类型检查技巧

```python
# 检查多种可能的类型
def smart_calculator(a, b, operation):
    # 确保输入是数字类型
    if not isinstance(a, (int, float)) or not isinstance(b, (int, float)):
        return "错误：只能计算数字"
    
    # 确保操作符是字符串
    if not isinstance(operation, str):
        return "错误：操作符必须是字符串"
    
    # 执行计算
    if operation == "+":
        return a + b
    elif operation == "-":
        return a - b
    elif operation == "*":
        return a * b
    elif operation == "/":
        return a / b if b != 0 else "错误：不能除以零"
    else:
        return "错误：不支持的操作"

# 测试
print(smart_calculator(10, 5, "+"))    # 15
print(smart_calculator("10", 5, "+"))  # 错误：只能计算数字
```

### 3.5 isinstance() vs type() 对比


| 特性 | **isinstance()** | **type()** |
|------|-----------------|------------|
| 🎯 **继承识别** | ✅ 识别父子关系 | ❌ 只识别确切类型 |
| 🔄 **多类型检查** | ✅ 支持元组参数 | ❌ 需要多次比较 |
| 📝 **代码简洁** | ✅ 更简洁易读 | ❌ 较为繁琐 |
| ⚡ **推荐程度** | 🟢 **推荐使用** | 🟡 特殊情况使用 |

---

## 4. 🔎 hasattr()属性检查


### 4.1 hasattr()的作用原理

**hasattr()函数**就像是"技能检测器"，检查一个对象是否具有某个属性或方法。

```python
# hasattr()语法
hasattr(对象, "属性名")  # 返回True或False
```

### 4.2 检查对象属性

```python
# 创建一个简单的类
class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def study(self):
        return f"{self.name}正在学习"

student = Student("小明", 18)

# 检查属性是否存在
print(hasattr(student, "name"))    # True
print(hasattr(student, "age"))     # True  
print(hasattr(student, "grade"))   # False
print(hasattr(student, "study"))   # True（方法也是属性）
```

### 4.3 实际应用：鸭子类型编程

**🦆 鸭子类型原理：**"如果它走起来像鸭子，叫起来像鸭子，那就把它当鸭子"

```python
# 不同的类，但有相同的方法
class Duck:
    def quack(self):
        return "嘎嘎叫"
    
    def fly(self):
        return "鸭子飞翔"

class Robot:
    def quack(self):
        return "模拟嘎嘎叫"
    
    def fly(self):
        return "机器人飞行"

class Dog:
    def bark(self):
        return "汪汪叫"

# 鸭子类型函数：只要能"quack"就当鸭子用
def make_it_quack(animal):
    if hasattr(animal, "quack"):
        return animal.quack()
    else:
        return "这个动物不会叫"

# 测试
duck = Duck()
robot = Robot()  
dog = Dog()

print(make_it_quack(duck))   # 嘎嘎叫
print(make_it_quack(robot))  # 模拟嘎嘎叫  
print(make_it_quack(dog))    # 这个动物不会叫
```

### 4.4 安全调用方法

```python
# 在调用方法前先检查是否存在
def safe_call_method(obj, method_name, *args):
    """安全调用对象的方法"""
    if hasattr(obj, method_name):
        method = getattr(obj, method_name)  # 获取方法
        if callable(method):  # 确保是可调用的
            return method(*args)
        else:
            return f"{method_name}不是方法"
    else:
        return f"对象没有{method_name}方法"

# 示例使用
class Calculator:
    def add(self, a, b):
        return a + b
    
    value = 100  # 这是属性，不是方法

calc = Calculator()

print(safe_call_method(calc, "add", 5, 3))     # 8
print(safe_call_method(calc, "subtract", 5, 3)) # 对象没有subtract方法
print(safe_call_method(calc, "value", 5, 3))    # value不是方法
```

---

## 5. 🔄 隐式类型转换


### 5.1 什么是隐式类型转换

**隐式类型转换**就是Python"悄悄地"帮你转换类型，你不需要明确要求。

```python
# Python自动进行的转换
result = 5 + 3.14  # int + float → float
print(result)      # 8.14
print(type(result)) # <class 'float'>

# Python把整数5自动转换成5.0，然后计算
```

### 5.2 数值类型的隐式转换

```python
# 数值运算中的自动转换规律
┌─────────────────────────────────────┐
│  Python数值类型转换层次（从低到高）    │
│                                     │
│  bool → int → float → complex       │
│                                     │
│  运算时：低类型自动转换为高类型        │
└─────────────────────────────────────┘

# 实际例子
print(True + 5)        # 6 (bool→int: True=1)
print(2 + 3.0)         # 5.0 (int→float)  
print(1 + 2j)          # (1+2j) (int→complex)
print(3.14 + 1j)       # (3.14+1j) (float→complex)
```

### 5.3 布尔值转换规则

```python
# 布尔值的隐式转换规则
转换为False的值：
- False（布尔假值）
- 0（零）
- 0.0（浮点零）
- ""（空字符串）
- []（空列表）
- {}（空字典）
- ()（空元组）
- None（空值）

转换为True的值：
- 其他所有值都是True

# 实际测试
print(bool(0))      # False
print(bool(""))     # False  
print(bool([]))     # False
print(bool({}))     # False
print(bool(None))   # False

print(bool(1))      # True
print(bool("hi"))   # True
print(bool([1]))    # True
print(bool({"a":1})) # True
```

### 5.4 字符串格式化中的转换

```python
# 字符串格式化时的隐式转换
age = 25
height = 1.75

# Python自动把数字转换为字符串
message = f"我今年{age}岁，身高{height}米"
print(message)  # 我今年25岁，身高1.75米

# 相当于：
message2 = f"我今年{str(age)}岁，身高{str(height)}米"
```

---

## 6. 💫 显式类型转换


### 6.1 什么是显式类型转换

**显式类型转换**就是你明确告诉Python："我要把这个数据转换成那个类型"。

```python
# 常用的转换函数
int()    # 转换为整数
float()  # 转换为浮点数  
str()    # 转换为字符串
bool()   # 转换为布尔值
list()   # 转换为列表
tuple()  # 转换为元组
dict()   # 转换为字典
set()    # 转换为集合
```

### 6.2 数值类型转换

```python
# 字符串转数字
num_str = "123"
num_int = int(num_str)      # 123
num_float = float(num_str)  # 123.0

print(f"原始：{num_str} (类型：{type(num_str).__name__})")
print(f"整数：{num_int} (类型：{type(num_int).__name__})")  
print(f"浮点：{num_float} (类型：{type(num_float).__name__})")

# 浮点数转整数（注意：会截断小数部分）
pi = 3.14159
pi_int = int(pi)  # 3（不是四舍五入，是直接截断）
print(f"{pi} → {pi_int}")

# 不同进制转换
binary_str = "1010"      # 二进制字符串
decimal = int(binary_str, 2)  # 转换为十进制
print(f"二进制{binary_str} = 十进制{decimal}")  # 10
```

### 6.3 字符串转换技巧

```python
# 任何类型都可以转换为字符串
data_list = [1, 2, 3]
data_dict = {"name": "Alice", "age": 25}
data_bool = True

print(str(data_list))  # "[1, 2, 3]"
print(str(data_dict))  # "{'name': 'Alice', 'age': 25}"
print(str(data_bool))  # "True"

# 实用函数：智能转换为字符串
def to_string(value):
    """智能转换任何值为字符串"""
    if isinstance(value, str):
        return value  # 已经是字符串了
    elif isinstance(value, (list, tuple, set)):
        return f"包含{len(value)}个元素的{type(value).__name__}"
    elif isinstance(value, dict):
        return f"包含{len(value)}个键值对的字典"
    else:
        return str(value)

# 测试
print(to_string("hello"))        # hello
print(to_string([1,2,3]))        # 包含3个元素的list
print(to_string({"a":1,"b":2}))  # 包含2个键值对的字典
```

### 6.4 容器类型互转

```python
# 列表、元组、集合之间的转换
original_list = [1, 2, 3, 2, 1]

# 转换示例
to_tuple = tuple(original_list)    # (1, 2, 3, 2, 1)
to_set = set(original_list)        # {1, 2, 3} (去重了)
back_to_list = list(to_set)        # [1, 2, 3] (顺序可能变化)

print(f"原列表：{original_list}")
print(f"转元组：{to_tuple}")  
print(f"转集合：{to_set}")
print(f"转回列表：{back_to_list}")

# 字符串和列表的转换
text = "hello"
char_list = list(text)           # ['h', 'e', 'l', 'l', 'o']
back_to_text = "".join(char_list) # "hello"

print(f"字符串→列表：{text} → {char_list}")
print(f"列表→字符串：{char_list} → {back_to_text}")
```

---

## 7. ⚠️ 类型转换异常处理


### 7.1 常见转换错误

```python
# 这些转换会出错
❌ int("hello")     # ValueError: 无法转换
❌ int("3.14")      # ValueError: 有小数点
❌ float("abc")     # ValueError: 不是数字
❌ int([1, 2, 3])   # TypeError: 不支持的类型

# 正确的转换
✅ int("123")       # 123
✅ int(3.14)        # 3  
✅ float("3.14")    # 3.14
✅ str([1, 2, 3])   # "[1, 2, 3]"
```

### 7.2 安全转换函数

```python
def safe_int(value, default=0):
    """安全转换为整数，失败时返回默认值"""
    try:
        return int(value)
    except (ValueError, TypeError):
        return default

def safe_float(value, default=0.0):
    """安全转换为浮点数，失败时返回默认值"""
    try:
        return float(value)
    except (ValueError, TypeError):
        return default

# 测试安全转换
print(safe_int("123"))      # 123
print(safe_int("abc"))      # 0
print(safe_int("abc", -1))  # -1

print(safe_float("3.14"))   # 3.14  
print(safe_float("hello"))  # 0.0
```

### 7.3 智能类型转换器

```python
def smart_convert(value, target_type):
    """智能类型转换器"""
    try:
        if target_type == "int":
            # 如果是浮点数字符串，先转float再转int
            if isinstance(value, str) and "." in value:
                return int(float(value))
            return int(value)
        
        elif target_type == "float":
            return float(value)
        
        elif target_type == "str":
            return str(value)
        
        elif target_type == "bool":
            if isinstance(value, str):
                return value.lower() in ["true", "1", "yes", "on"]
            return bool(value)
        
        else:
            return f"不支持转换为{target_type}类型"
    
    except Exception as e:
        return f"转换失败：{e}"

# 测试智能转换
print(smart_convert("3.14", "int"))     # 3
print(smart_convert("123", "float"))    # 123.0
print(smart_convert("true", "bool"))    # True
print(smart_convert("false", "bool"))   # False
print(smart_convert(123, "str"))        # "123"
```

### 7.4 转换验证器

```python
def validate_and_convert(value, expected_type):
    """验证并转换数据类型"""
    conversion_map = {
        "int": int,
        "float": float, 
        "str": str,
        "bool": bool
    }
    
    if expected_type not in conversion_map:
        return {"success": False, "error": f"不支持的类型：{expected_type}"}
    
    try:
        converted_value = conversion_map[expected_type](value)
        return {
            "success": True,
            "original": value,
            "converted": converted_value,
            "original_type": type(value).__name__,
            "target_type": expected_type
        }
    except Exception as e:
        return {
            "success": False,
            "original": value,
            "target_type": expected_type,
            "error": str(e)
        }

# 测试验证器
result1 = validate_and_convert("123", "int")
result2 = validate_and_convert("abc", "int")

print("成功转换：", result1)
print("失败转换：", result2)
```

---

## 8. 📝 类型注解基础


### 8.1 什么是类型注解

**类型注解（Type Hints）**就像给变量和函数贴上"标签"，说明它们应该是什么类型。

```python
# 类型注解的基本语法
变量名: 类型 = 值
def 函数名(参数: 类型) -> 返回类型:
    pass

# 注意：类型注解只是"提示"，不强制执行
```

### 8.2 变量类型注解

```python
# 基本类型注解
name: str = "Alice"
age: int = 25  
height: float = 1.75
is_student: bool = True

# 容器类型注解
from typing import List, Dict, Tuple, Optional

numbers: List[int] = [1, 2, 3, 4, 5]
person: Dict[str, str] = {"name": "Bob", "city": "北京"}
coordinates: Tuple[float, float] = (39.9, 116.4)
optional_value: Optional[str] = None  # 可能是str或None
```

### 8.3 函数类型注解

```python
def greet(name: str) -> str:
    """问候函数：输入姓名(字符串)，返回问候语(字符串)"""
    return f"你好，{name}！"

def add_numbers(a: int, b: int) -> int:
    """加法函数：输入两个整数，返回整数结果"""
    return a + b

def process_data(data: List[int]) -> Dict[str, int]:
    """数据处理：输入整数列表，返回统计字典"""
    return {
        "总数": len(data),
        "最大值": max(data) if data else 0,
        "最小值": min(data) if data else 0,
        "总和": sum(data)
    }

# 使用示例
print(greet("小明"))
print(add_numbers(5, 3))
print(process_data([1, 2, 3, 4, 5]))
```

### 8.4 复杂类型注解

```python
from typing import Union, Callable, Any

# Union：多种可能的类型
def format_value(value: Union[int, float, str]) -> str:
    """格式化值：可以是整数、浮点数或字符串"""
    if isinstance(value, (int, float)):
        return f"数字：{value}"
    else:
        return f"文本：{value}"

# Callable：函数类型
def apply_operation(numbers: List[int], 
                   operation: Callable[[int], int]) -> List[int]:
    """对列表中每个数字应用操作函数"""
    return [operation(num) for num in numbers]

# 使用示例
def double(x: int) -> int:
    return x * 2

result = apply_operation([1, 2, 3], double)
print(result)  # [2, 4, 6]
```

### 8.5 类型注解的好处

```
🔸 代码更清晰：一眼看出函数期望什么类型
🔸 IDE支持：编辑器能提供更好的代码提示
🔸 错误检查：类型检查工具能发现潜在问题
🔸 文档作用：类型注解就是活的文档

注意事项：
⚠️ 类型注解不会在运行时检查
⚠️ 即使类型不匹配，代码仍会运行
⚠️ 需要额外工具（如mypy）进行静态检查
```

---

## 9. 🌊 动态类型特性理解


### 9.1 动态类型的含义

**动态类型**意味着变量的类型在运行时确定，而且可以随时改变。

```python
# 同一个变量可以存储不同类型的数据
value = 42          # 开始是整数
print(f"{value} 是 {type(value).__name__}")

value = "hello"     # 变成字符串
print(f"{value} 是 {type(value).__name__}")

value = [1, 2, 3]   # 变成列表
print(f"{value} 是 {type(value).__name__}")

# 输出：
# 42 是 int
# hello 是 str  
# [1, 2, 3] 是 list
```

### 9.2 动态类型的优势

```python
# 优势1：灵活性高
def flexible_function(data):
    """这个函数可以处理多种类型的数据"""
    if isinstance(data, str):
        return f"文本长度：{len(data)}"
    elif isinstance(data, (list, tuple)):
        return f"容器大小：{len(data)}"
    elif isinstance(data, (int, float)):
        return f"数字平方：{data ** 2}"
    else:
        return f"未知类型：{type(data).__name__}"

# 测试
print(flexible_function("Python"))      # 文本长度：6
print(flexible_function([1, 2, 3]))     # 容器大小：3
print(flexible_function(5))             # 数字平方：25
```

### 9.3 动态类型的注意事项

```python
# 注意事项1：运行时才发现错误
def process_number(num):
    return num + 10

# 这些调用在定义时都不会报错，只有运行时才知道
try:
    print(process_number(5))        # 15（正常）
    print(process_number("hello"))  # 错误！运行时才发现
except TypeError as e:
    print(f"类型错误：{e}")

# 注意事项2：变量类型可能出人意料
def get_data(use_string=False):
    if use_string:
        return "no data"
    else:
        return 0

result = get_data(True)  # 返回字符串
# 如果后面把result当数字用就会出错
```

### 9.4 编写健壮的动态类型代码

```python
# 技巧1：充分使用类型检查
def smart_multiply(value, factor):
    """智能乘法：根据类型决定行为"""
    if isinstance(value, str):
        return value * factor  # 字符串重复
    elif isinstance(value, (int, float)):
        return value * factor  # 数字相乘
    elif isinstance(value, (list, tuple)):
        return list(value) * factor  # 列表重复
    else:
        raise TypeError(f"不支持类型{type(value).__name__}的乘法")

# 测试
print(smart_multiply("Hi", 3))      # HiHiHi
print(smart_multiply(5, 4))         # 20
print(smart_multiply([1, 2], 2))    # [1, 2, 1, 2]

# 技巧2：提供类型转换选项
def universal_adder(*args, force_numeric=False):
    """通用加法器"""
    if force_numeric:
        # 尝试转换为数字
        numbers = []
        for arg in args:
            try:
                if isinstance(arg, str):
                    numbers.append(float(arg))
                else:
                    numbers.append(float(arg))
            except (ValueError, TypeError):
                raise ValueError(f"无法将{arg}转换为数字")
        return sum(numbers)
    else:
        # 保持原类型
        result = args[0]
        for arg in args[1:]:
            result += arg
        return result

# 测试
print(universal_adder(1, 2, 3))           # 6
print(universal_adder("a", "b", "c"))     # abc  
print(universal_adder("1", "2", "3", force_numeric=True))  # 6.0
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念

```
🔸 类型检查：type()精确检查，isinstance()智能检查，hasattr()属性检查
🔸 类型转换：隐式转换（自动），显式转换（手动）
🔸 异常处理：转换失败要有备案，不能让程序崩溃
🔸 类型注解：给代码加上类型提示，提高可读性
🔸 动态特性：Python的灵活性和需要注意的地方
```

### 10.2 实用技巧总结


**🔹 类型检查最佳实践**
```python
# ✅ 推荐：使用isinstance()
if isinstance(value, (int, float)):
    # 处理数字

# ❌ 不推荐：使用type()
if type(value) == int or type(value) == float:
    # 处理数字
```

**🔹 安全转换模式**
```python
# 安全转换模板
def safe_convert(value, target_type, default=None):
    try:
        return target_type(value)
    except (ValueError, TypeError):
        return default
```

**🔹 类型注解指导**
```python
# 从简单开始，逐步完善
def process(data: list) -> dict:  # 基础注解
    pass

from typing import List, Dict
def process(data: List[int]) -> Dict[str, int]:  # 详细注解
    pass
```

### 10.3 常见错误与避免方法


| 错误类型 | **常见情况** | **解决方案** |
|---------|------------|-------------|
| 🚫 **类型转换错误** | `int("hello")` | 使用try-except或安全转换函数 |
| 🚫 **属性不存在** | 直接调用不存在的方法 | 用hasattr()先检查 |
| 🚫 **类型判断错误** | 用type()检查继承关系 | 改用isinstance() |
| 🚫 **动态类型困扰** | 变量类型不确定 | 加强类型检查和文档 |

### 10.4 学习进阶建议

```
🎯 入门阶段：
- 熟练使用isinstance()和hasattr()
- 掌握基本的显式类型转换
- 学会处理转换异常

🎯 进阶阶段：  
- 使用类型注解美化代码
- 理解动态类型的优势和陷阱
- 编写类型安全的函数

🎯 高级阶段：
- 使用mypy等工具进行静态类型检查
- 设计灵活且安全的API
- 深入理解Python的类型系统
```

**💡 记忆口诀**：
```
类型检查用isinstance，安全转换用try-except
hasattr检查有没有，类型注解做提示
动态灵活要小心，代码健壮最重要
```

**🔗 相关知识点**：
- 面向对象编程（类的继承关系）
- 异常处理机制（try-except语句）
- 函数式编程（高阶函数和类型）
- 代码规范和最佳实践