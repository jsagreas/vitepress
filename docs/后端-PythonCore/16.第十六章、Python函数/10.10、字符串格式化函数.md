---
title: 10、字符串格式化函数
---
## 📚 目录

1. [字符串格式化概述](#1-字符串格式化概述)
2. [format()函数详解](#2-format函数详解)
3. [repr()函数详解](#3-repr函数详解)
4. [str()函数详解](#4-str函数详解)
5. [三个函数对比分析](#5-三个函数对比分析)
6. [实际应用场景](#6-实际应用场景)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 字符串格式化概述


### 1.1 什么是字符串格式化


**简单理解**：字符串格式化就像是**填空题**，我们先准备一个模板，然后把具体的值填进去。

```
想象一下写信的过程：
模板："亲爱的___，今天天气___，温度___度"
填空后："亲爱的小明，今天天气晴朗，温度25度"
```

在Python中，我们有三个重要的函数来处理字符串：
- **`format()`** - 专门用来格式化字符串，像高级的填空工具
- **`str()`** - 把任何东西转换成人类容易读懂的字符串
- **`repr()`** - 把任何东西转换成程序员调试用的精确字符串

### 1.2 为什么需要字符串格式化


**日常例子**：
```
不格式化的麻烦写法：
name = "张三"
age = 25
message = "姓名:" + name + ",年龄:" + str(age) + "岁"

格式化的简洁写法：
message = "姓名:{},年龄:{}岁".format(name, age)
```

**核心作用**：
- 🎯 **简化代码** - 不用一直拼接字符串
- 📊 **统一格式** - 让输出看起来整齐美观
- 🔧 **方便维护** - 模板和数据分离，修改更容易
- 🌍 **支持多语言** - 可以轻松切换不同语言的模板

---

## 2. ⚡ format()函数详解


### 2.1 基本概念和语法


**什么是format()**：
`format()`是Python中最强大的字符串格式化工具，它可以把变量的值按照我们指定的格式插入到字符串模板中。

**基本语法**：
```python
# 基本用法
"模板字符串{}".format(值)
"模板字符串{0}{1}".format(值1, 值2)
```

### 2.2 基础用法示例


**最简单的占位符**：
```python
# 简单替换
name = "小红"
greeting = "你好，{}！".format(name)
print(greeting)  # 输出：你好，小红！

# 多个占位符
name = "李四"
age = 30
info = "我叫{}，今年{}岁".format(name, age)
print(info)  # 输出：我叫李四，今年30岁
```

**带位置编号的占位符**：
```python
# 按位置填充（从0开始计数）
template = "第{0}名是{1}，第{2}名是{3}"
result = template.format(1, "张三", 2, "李四")
print(result)  # 输出：第1名是张三，第2名是李四

# 可以重复使用同一个位置
template = "{0}喜欢{1}，{0}也喜欢{2}"
result = template.format("小明", "苹果", "香蕉")
print(result)  # 输出：小明喜欢苹果，小明也喜欢香蕉
```

### 2.3 高级格式化选项


**数字格式化**：

| 格式符 | **说明** | **示例** | **输出** |
|--------|---------|----------|---------|
| `{:.2f}` | `保留2位小数` | `"{:.2f}".format(3.14159)` | `3.14` |
| `{:,}` | `千分位分隔符` | `"{:,}".format(1234567)` | `1,234,567` |
| `{:.2%}` | `百分比格式` | `"{:.2%}".format(0.1234)` | `12.34%` |
| `{:>10}` | `右对齐，宽度10` | `"{:>10}".format("hello")` | `"     hello"` |

**实际应用例子**：
```python
# 价格显示
price = 1299.50
discount = 0.15

print("原价：￥{:.2f}".format(price))          # 原价：￥1299.50
print("折扣：{:.1%}".format(discount))         # 折扣：15.0%
print("现价：￥{:.2f}".format(price * (1-discount)))  # 现价：￥1104.58

# 表格对齐
print("{:<10} {:>8} {:>10}".format("商品", "数量", "价格"))
print("{:<10} {:>8} {:>10.2f}".format("苹果", 5, 12.5))
print("{:<10} {:>8} {:>10.2f}".format("香蕉", 10, 8.0))
```

**输出效果**：
```
商品           数量       价格
苹果              5     12.50
香蕉             10      8.00
```

### 2.4 命名占位符


**使用变量名代替位置编号**：
```python
# 更清晰的模板
template = "学生{name}在{subject}考试中得了{score}分"
result = template.format(name="王五", subject="数学", score=95)
print(result)  # 输出：学生王五在数学考试中得了95分

# 使用字典传参
student = {"name": "赵六", "age": 18, "grade": "高三"}
info = "姓名：{name}，年龄：{age}，年级：{grade}".format(**student)
print(info)  # 输出：姓名：赵六，年龄：18，年级：高三
```

---

## 3. 🔍 repr()函数详解


### 3.1 repr()是什么


**简单理解**：`repr()`就像是给程序员看的"身份证"，它会显示一个对象最准确、最详细的信息。

**核心特点**：
- 🎯 **面向开发者** - 主要用于调试和开发
- 📝 **显示细节** - 会显示数据类型、特殊字符等详细信息
- 🔧 **可执行性** - 理想情况下，`repr()`的输出应该能重新创建对象

### 3.2 基础用法示例


**字符串的repr()**：
```python
# 普通字符串
text = "Hello World"
print(repr(text))  # 输出：'Hello World'

# 包含特殊字符的字符串
special = "第一行\n第二行\t制表符"
print(repr(special))  # 输出：'第一行\n第二行\t制表符'
print(special)       # 输出：第一行
                     #       第二行	制表符
```

**数字和其他类型**：
```python
# 各种数据类型
print(repr(42))          # 输出：42
print(repr(3.14))        # 输出：3.14
print(repr([1, 2, 3]))   # 输出：[1, 2, 3]
print(repr({"a": 1}))    # 输出：{'a': 1}
print(repr(None))        # 输出：None
```

### 3.3 repr()的实际用途


**调试程序时的好帮手**：
```python
# 调试字符串问题
user_input = "  hello world  "  # 注意前后有空格
print("用户输入：", user_input)        # 看不出有空格
print("用户输入：", repr(user_input))  # 清楚显示：'  hello world  '

# 调试列表内容
data = ["apple", "", "banana", None]
print("列表内容：", data)        # [['apple', '', 'banana', None]]
print("详细信息：", repr(data))  # ['apple', '', 'banana', None]
```

**检查数据类型**：
```python
# 看起来一样，实际类型不同
num1 = 100
num2 = "100"
print(num1, num2)        # 输出：100 100（看起来一样）
print(repr(num1), repr(num2))  # 输出：100 '100'（类型不同）
```

---

## 4. 📝 str()函数详解


### 4.1 str()是什么


**简单理解**：`str()`就像是给普通用户看的"说明书"，它把任何东西都转换成人类容易理解的文字。

**核心特点**：
- 👥 **面向用户** - 主要给最终用户看
- 📖 **易于阅读** - 格式友好，简洁明了
- 🎨 **美观实用** - 隐藏技术细节，突出有用信息

### 4.2 基础用法示例


**基本转换**：
```python
# 把数字转成字符串
age = 25
age_str = str(age)
print("我今年" + age_str + "岁")  # 我今年25岁

# 把布尔值转成字符串
is_student = True
print("是学生：" + str(is_student))  # 是学生：True

# 把列表转成字符串
fruits = ["苹果", "香蕉", "橙子"]
print("水果清单：" + str(fruits))  # 水果清单：['苹果', '香蕉', '橙子']
```

### 4.3 str()与其他类型转换


**日期时间的友好显示**：
```python
from datetime import datetime

now = datetime.now()
print("现在时间：", str(now))  # 现在时间：2024-01-21 10:30:45.123456
print("调试信息：", repr(now))  # 调试信息：datetime.datetime(2024, 1, 21, 10, 30, 45, 123456)
```

**自定义类的字符串表示**：
```python
class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def __str__(self):
        return f"学生：{self.name}（{self.age}岁）"
    
    def __repr__(self):
        return f"Student('{self.name}', {self.age})"

student = Student("小明", 18)
print(str(student))   # 学生：小明（18岁）
print(repr(student))  # Student('小明', 18)
```

---

## 5. ⚖️ 三个函数对比分析


### 5.1 直观对比


**一个例子看懂三者区别**：
```python
text = "Hello\nWorld"  # 包含换行符的字符串

print("原始输出：", text)
print("str()输出：", str(text))
print("repr()输出：", repr(text))
print("format()输出：", "内容是：{}".format(text))
```

**输出结果**：
```
原始输出：Hello
World
str()输出：Hello
World
repr()输出：'Hello\nWorld'
format()输出：内容是：Hello
World
```

### 5.2 详细对比表格


| **函数** | **目标用户** | **显示风格** | **主要用途** | **典型场景** |
|----------|-------------|-------------|-------------|-------------|
| `str()` | `普通用户` | `友好易读` | `最终显示` | `用户界面显示` |
| `repr()` | `程序员` | `精确详细` | `调试开发` | `错误排查、日志记录` |
| `format()` | `所有人` | `自定义格式` | `格式化输出` | `报表、消息模板` |

### 5.3 使用场景选择指南


**什么时候用format()**：
- ✅ 需要按特定格式显示数据
- ✅ 制作用户消息、报告
- ✅ 需要对齐、精度控制
- ✅ 批量生成相似格式的文本

**什么时候用str()**：
- ✅ 简单的类型转换
- ✅ 用户界面显示
- ✅ 日常的字符串拼接
- ✅ 不需要特殊格式的场合

**什么时候用repr()**：
- ✅ 调试程序时查看数据
- ✅ 写日志文件
- ✅ 检查数据是否正确
- ✅ 需要看到所有细节的时候

---

## 6. 🚀 实际应用场景


### 6.1 数据报告生成


**销售报告示例**：
```python
# 销售数据
sales_data = [
    {"name": "张三", "product": "手机", "amount": 1299.00, "quantity": 2},
    {"name": "李四", "product": "电脑", "amount": 4999.50, "quantity": 1},
    {"name": "王五", "product": "平板", "amount": 899.99, "quantity": 3}
]

print("=" * 50)
print("{:^50}".format("销售报告"))
print("=" * 50)
print("{:<8} {:<8} {:<6} {:>10}".format("销售员", "商品", "数量", "金额"))
print("-" * 50)

total = 0
for sale in sales_data:
    line = "{:<8} {:<8} {:<6} {:>10.2f}".format(
        sale["name"], sale["product"], 
        sale["quantity"], sale["amount"]
    )
    print(line)
    total += sale["amount"]

print("-" * 50)
print("{:<24} {:>10.2f}".format("总计", total))
```

### 6.2 配置文件和模板


**配置文件模板**：
```python
# 数据库配置模板
config_template = """
数据库配置
---------
服务器地址：{host}
端口号：{port}
数据库名：{database}
用户名：{username}
连接超时：{timeout}秒
"""

# 配置数据
db_config = {
    "host": "localhost",
    "port": 3306,
    "database": "myapp",
    "username": "admin",
    "timeout": 30
}

# 生成配置文件
config_text = config_template.format(**db_config)
print(config_text)
```

### 6.3 错误信息和调试


**调试信息收集**：
```python
def debug_info(variable_name, value):
    """收集调试信息"""
    info = {
        "变量名": variable_name,
        "类型": type(value).__name__,
        "str()表示": str(value),
        "repr()表示": repr(value),
        "长度": len(str(value)) if hasattr(value, '__len__') else "N/A"
    }
    
    template = """
调试信息
--------
变量名: {变量名}
类型: {类型}
用户显示: {str()表示}
开发显示: {repr()表示}
长度: {长度}
"""
    
    return template.format(**info)

# 使用示例
data = ["hello", "world", "", None]
print(debug_info("data", data))
```

### 6.4 用户交互界面


**菜单系统**：
```python
def show_menu(options):
    """显示菜单"""
    print("\n" + "=" * 30)
    print("{:^30}".format("主菜单"))
    print("=" * 30)
    
    for i, option in enumerate(options, 1):
        print("{:>2}. {:<25}".format(i, option))
    
    print("-" * 30)
    choice = input("请选择操作（输入数字）：")
    return choice

# 使用示例
menu_options = ["查看商品", "添加商品", "删除商品", "退出系统"]
user_choice = show_menu(menu_options)
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 format()：专业的格式化工具，用于创建美观的输出
🔸 str()：通用的字符串转换，给人看的友好格式
🔸 repr()：开发者工具，显示精确的对象信息
🔸 三者配合：各有用途，互相补充
```

### 7.2 关键理解要点


**🔹 format()的核心价值**：
```
模板思维：
- 分离内容和格式
- 可重复使用
- 批量处理数据
- 国际化支持

格式控制：
- 数字精度：{:.2f}
- 对齐方式：{:>10}, {:<10}, {:^10}
- 千分位：{:,}
- 百分比：{:.2%}
```

**🔹 str()和repr()的区别**：
```
设计目标不同：
str() → 给最终用户看，追求美观易读
repr() → 给开发者看，追求准确完整

使用场景：
str() → 界面显示、用户消息
repr() → 调试、日志、错误报告
```

**🔹 实际开发中的选择原则**：
```
用户看到的 → str() 或 format()
开发调试用 → repr()
需要格式控制 → format()
简单转换 → str()
```

### 7.3 实际应用指导


**最佳实践**：
- 📊 **报表生成**：优先使用format()进行格式控制
- 🎯 **用户界面**：使用str()转换 + format()美化
- 🔧 **调试排错**：使用repr()查看详细信息
- 📝 **日志记录**：混合使用，看具体需求

**常见错误避免**：
- ❌ 不要在用户界面显示repr()的结果
- ❌ 不要忽略format()的格式控制功能
- ❌ 不要把str()和字符串拼接弄混
- ✅ 根据使用场景选择合适的函数

**记忆口诀**：
```
format专格式，模板加数据
str给用户看，友好又美观  
repr给程序员，调试最方便
三者各有用，选对是关键
```

### 7.4 进阶技巧


**f-string（Python 3.6+）**：
```python
# 现代Python推荐的格式化方式
name = "小明"
age = 18
score = 95.67

# 等价于format()，但更简洁
message = f"学生{name}，年龄{age}，成绩{score:.1f}分"
print(message)  # 学生小明，年龄18，成绩95.7分
```

**自定义字符串表示**：
```python
class Product:
    def __init__(self, name, price):
        self.name = name
        self.price = price
    
    def __str__(self):
        return f"{self.name}（￥{self.price:.2f}）"
    
    def __repr__(self):
        return f"Product('{self.name}', {self.price})"

# 用户友好显示 vs 开发者精确信息
product = Product("苹果", 5.5)
print("展示给用户：", str(product))    # 苹果（￥5.50）
print("开发者信息：", repr(product))   # Product('苹果', 5.5)
```