---
title: 28、时间日期函数
---
## 📚 目录

1. [时间日期基础概念](#1-时间日期基础概念)
2. [time模块核心函数](#2-time模块核心函数)
3. [datetime模块核心函数](#3-datetime模块核心函数)
4. [时间格式化与解析](#4-时间格式化与解析)
5. [实际应用场景](#5-实际应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🕐 时间日期基础概念


### 1.1 为什么需要时间日期处理


**🤔 生活中的时间需求**

想象一下这些日常场景：
- 记录你写作业的开始和结束时间
- 设置闹钟提醒自己休息
- 查看今天是星期几
- 计算距离考试还有多少天

在编程中，我们经常需要处理这样的时间问题，Python提供了专门的工具来帮我们处理。

**📝 Python中的两个时间模块**

```
time模块      ←→ 像一个简单的计时器
datetime模块  ←→ 像一个功能丰富的日历+时钟
```

### 1.2 时间戳概念解释


**什么是时间戳？**

时间戳就像是时间的"身份证号"，是一个数字，表示从1970年1月1日0点0分0秒到现在经过了多少秒。

```
生活类比：
就像你的学号一样，每个时刻都有唯一的数字标识

例如：1632150000 表示某个具体的时间点
```

**💡 为什么要用时间戳？**

- **统一标准**：全世界都用同一个起点计算
- **便于计算**：数字比文字更好比较和运算
- **节省空间**：一个数字比"2023年9月21日15点30分"省空间

---

## 2. ⏰ time模块核心函数


### 2.1 time.time() - 获取当前时间戳


**🔸 基本用法**

`time.time()` 就像问："现在是几点了？"，但它回答的是一个数字（时间戳）。

```python
import time

# 获取当前时间戳
now = time.time()
print(now)  # 输出类似：1695312600.123456
```

**💭 通俗理解**

```
想象时间是一条数轴：
1970年1月1日 ────→ 现在 ────→ 未来
     0秒           1695312600秒

time.time() 告诉你现在在数轴的哪个位置
```

**🎯 实际应用场景**

```python
# 计算代码运行时间
start_time = time.time()
# 这里放你要测试的代码
time.sleep(2)  # 模拟耗时操作
end_time = time.time()

print(f"代码运行了 {end_time - start_time:.2f} 秒")
# 输出：代码运行了 2.00 秒
```

### 2.2 time.sleep() - 程序休眠


**🔸 基本概念**

`time.sleep()` 就像给程序按了"暂停键"，让它休息一会儿。

```python
import time

print("开始工作...")
time.sleep(3)  # 暂停3秒
print("休息结束，继续工作！")
```

**📊 休眠时间单位**

| 写法 | 含义 | 实际效果 |
|------|------|----------|
| `time.sleep(1)` | 1秒 | 暂停1秒 |
| `time.sleep(0.5)` | 0.5秒 | 暂停半秒 |
| `time.sleep(60)` | 60秒 | 暂停1分钟 |

**🎮 实际应用例子**

```python
# 制作一个简单的倒计时
for i in range(5, 0, -1):
    print(f"倒计时：{i}")
    time.sleep(1)
print("时间到！")
```

### 2.3 其他time模块函数


**🔸 常用时间显示函数**

```python
import time

# 获取可读的当前时间
readable_time = time.ctime()
print(readable_time)  # 输出：Thu Sep 21 15:30:00 2023

# 获取本地时间（元组格式）
local_time = time.localtime()
print(f"现在是{local_time.tm_year}年{local_time.tm_mon}月{local_time.tm_mday}日")
```

**📋 time模块速查卡**

```
┌─────────────────────────┐
│ time模块常用函数         │
├─────────────────────────┤
│ time.time()    获取时间戳│
│ time.sleep()   程序休眠  │
│ time.ctime()   可读时间  │
│ time.localtime() 本地时间│
└─────────────────────────┘
```

---

## 3. 📅 datetime模块核心函数


### 3.1 datetime.now() - 获取当前日期时间


**🔸 基本概念**

如果说`time.time()`像一个数字时钟，那`datetime.now()`就像一个显示完整信息的日历+时钟。

```python
from datetime import datetime

# 获取当前完整的日期时间
now = datetime.now()
print(now)  # 输出：2023-09-21 15:30:45.123456
```

**💡 datetime对象的组成部分**

```
datetime对象就像一个信息盒子：
┌─────────────────────┐
│ 年：2023           │
│ 月：9              │
│ 日：21             │
│ 时：15             │
│ 分：30             │
│ 秒：45             │
│ 微秒：123456       │
└─────────────────────┘
```

**🔧 获取具体部分**

```python
from datetime import datetime

now = datetime.now()
print(f"今年是：{now.year}年")
print(f"现在是：{now.month}月")
print(f"今天是：{now.day}日")
print(f"现在是：{now.hour}点{now.minute}分")
```

### 3.2 创建特定的日期时间


**🔸 指定具体日期时间**

```python
from datetime import datetime

# 创建特定的日期时间（生日、纪念日等）
birthday = datetime(2000, 5, 15, 14, 30, 0)
print(birthday)  # 输出：2000-05-15 14:30:00

# 只指定日期，时间默认为0点
simple_date = datetime(2023, 12, 25)
print(simple_date)  # 输出：2023-12-25 00:00:00
```

### 3.3 日期时间计算


**🔸 计算时间差**

```python
from datetime import datetime

# 计算两个时间之间的差值
start = datetime(2023, 9, 1)
end = datetime(2023, 9, 21)
difference = end - start

print(f"相差 {difference.days} 天")  # 输出：相差 20 天
```

**📊 时间计算实用例子**

```python
from datetime import datetime, timedelta

now = datetime.now()

# 计算明天
tomorrow = now + timedelta(days=1)
print(f"明天是：{tomorrow.strftime('%Y-%m-%d')}")

# 计算一周前
week_ago = now - timedelta(weeks=1)
print(f"一周前是：{week_ago.strftime('%Y-%m-%d')}")

# 计算3小时后
three_hours_later = now + timedelta(hours=3)
print(f"3小时后是：{three_hours_later.strftime('%H:%M')}")
```

---

## 4. 🎨 时间格式化与解析


### 4.1 datetime.strftime() - 格式化时间


**🔸 什么是格式化？**

格式化就像给时间"化妆"，把它变成你想要的样子显示出来。

```
原始时间：2023-09-21 15:30:45
格式化后：2023年9月21日 下午3点30分
或者：    2023/09/21
或者：    21-Sep-2023
```

**📝 常用格式化代码**

| 格式代码 | 含义 | 示例 |
|---------|------|------|
| `%Y` | 4位年份 | 2023 |
| `%m` | 月份(01-12) | 09 |
| `%d` | 日期(01-31) | 21 |
| `%H` | 小时(00-23) | 15 |
| `%M` | 分钟(00-59) | 30 |
| `%S` | 秒(00-59) | 45 |

**🔧 实际使用例子**

```python
from datetime import datetime

now = datetime.now()

# 不同的显示格式
print(now.strftime("%Y年%m月%d日"))           # 2023年09月21日
print(now.strftime("%Y/%m/%d"))              # 2023/09/21
print(now.strftime("%H:%M:%S"))              # 15:30:45
print(now.strftime("%Y-%m-%d %H:%M"))        # 2023-09-21 15:30
```

**💡 记忆技巧**

```
年(Year) → %Y
月(Month) → %m  
日(Day) → %d
时(Hour) → %H
分(Minute) → %M
秒(Second) → %S
```

### 4.2 datetime.strptime() - 解析时间字符串


**🔸 什么是解析？**

解析就是把文字形式的时间转换成datetime对象，就像把"2023年9月21日"这句话变成程序能理解的时间对象。

```python
from datetime import datetime

# 把字符串转换为datetime对象
date_string = "2023-09-21"
date_object = datetime.strptime(date_string, "%Y-%m-%d")
print(type(date_object))  # <class 'datetime.datetime'>
print(date_object)        # 2023-09-21 00:00:00
```

**🔄 strftime vs strptime 对比**

```
strftime：datetime对象 → 字符串 (格式化)
strptime：字符串 → datetime对象 (解析)

记忆方法：
f = format (格式化，输出)
p = parse (解析，输入)
```

**📚 实用解析例子**

```python
from datetime import datetime

# 解析不同格式的日期字符串
formats_and_strings = [
    ("2023/09/21", "%Y/%m/%d"),
    ("21-09-2023", "%d-%m-%Y"),
    ("2023年9月21日", "%Y年%m月%d日"),
    ("15:30:45", "%H:%M:%S")
]

for date_str, format_str in formats_and_strings:
    parsed = datetime.strptime(date_str, format_str)
    print(f"'{date_str}' 解析为：{parsed}")
```

---

## 5. 🚀 实际应用场景


### 5.1 制作简单的计时器


```python
import time
from datetime import datetime

def simple_timer():
    """简单的学习计时器"""
    print("📚 学习计时器启动！")
    print("按 Enter 开始计时...")
    input()
    
    start_time = time.time()
    start_datetime = datetime.now()
    
    print(f"⏰ 开始时间：{start_datetime.strftime('%H:%M:%S')}")
    print("按 Enter 停止计时...")
    input()
    
    end_time = time.time()
    end_datetime = datetime.now()
    
    duration = end_time - start_time
    print(f"🏁 结束时间：{end_datetime.strftime('%H:%M:%S')}")
    print(f"📊 学习时长：{duration:.1f} 秒")

# 使用示例
# simple_timer()
```

### 5.2 生日倒计时程序


```python
from datetime import datetime, timedelta

def birthday_countdown(birth_month, birth_day):
    """计算距离生日还有多少天"""
    today = datetime.now()
    current_year = today.year
    
    # 今年的生日
    birthday_this_year = datetime(current_year, birth_month, birth_day)
    
    # 如果今年生日已过，计算明年的生日
    if birthday_this_year < today:
        birthday_next = datetime(current_year + 1, birth_month, birth_day)
    else:
        birthday_next = birthday_this_year
    
    days_until = (birthday_next - today).days
    
    print(f"🎂 距离你的生日还有 {days_until} 天！")
    print(f"📅 下次生日是：{birthday_next.strftime('%Y年%m月%d日')}")

# 使用示例
# birthday_countdown(5, 15)  # 5月15日生日
```

### 5.3 日志记录系统


```python
from datetime import datetime

def write_log(message):
    """简单的日志记录功能"""
    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    log_entry = f"[{timestamp}] {message}"
    
    # 写入文件（实际使用中）
    # with open("activity.log", "a", encoding="utf-8") as f:
    #     f.write(log_entry + "\n")
    
    # 这里只是打印示例
    print(log_entry)

# 使用示例
write_log("用户登录系统")
write_log("开始学习Python")
write_log("完成作业提交")
```

### 5.4 工作时间统计


```python
from datetime import datetime, timedelta

def work_time_tracker():
    """简单的工作时间跟踪"""
    sessions = []
    
    print("📊 工作时间跟踪器")
    print("输入工作记录（格式：开始时间-结束时间，如 09:00-12:00）")
    print("输入 'quit' 退出")
    
    while True:
        record = input("请输入：").strip()
        
        if record.lower() == 'quit':
            break
            
        try:
            start_str, end_str = record.split('-')
            start_time = datetime.strptime(start_str.strip(), "%H:%M")
            end_time = datetime.strptime(end_str.strip(), "%H:%M")
            
            duration = end_time - start_time
            hours = duration.total_seconds() / 3600
            
            sessions.append(hours)
            print(f"✅ 记录成功：工作了 {hours:.1f} 小时")
            
        except ValueError:
            print("❌ 格式错误，请使用 HH:MM-HH:MM 格式")
    
    if sessions:
        total_hours = sum(sessions)
        print(f"\n📈 今日总工作时间：{total_hours:.1f} 小时")
        print(f"📊 平均每次工作：{total_hours/len(sessions):.1f} 小时")

# 使用示例
# work_time_tracker()
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的基本概念


```
🔸 时间戳：从1970年开始计算的秒数，全球统一标准
🔸 time模块：简单的时间操作，主要用于计时和休眠
🔸 datetime模块：完整的日期时间处理，功能更丰富
🔸 格式化：把时间对象转换成想要的字符串格式
🔸 解析：把时间字符串转换成程序能处理的对象
```

### 6.2 函数功能对比表


| 函数 | 模块 | 返回类型 | 主要用途 | 难度 |
|------|------|----------|----------|------|
| `time.time()` | time | float | 获取时间戳，计算时间差 | ⭐ |
| `time.sleep()` | time | None | 程序暂停，控制节奏 | ⭐ |
| `datetime.now()` | datetime | datetime | 获取完整时间信息 | ⭐⭐ |
| `strftime()` | datetime | str | 时间格式化显示 | ⭐⭐ |
| `strptime()` | datetime | datetime | 解析时间字符串 | ⭐⭐⭐ |

### 6.3 选择使用指南


**🤔 什么时候用time模块？**

```
✅ 需要计算程序运行时间
✅ 需要让程序暂停一会儿
✅ 简单的时间戳操作
✅ 性能测试和基准测试

示例场景：游戏中的倒计时、程序性能测试
```

**🤔 什么时候用datetime模块？**

```
✅ 需要显示具体的日期时间
✅ 需要格式化时间显示
✅ 需要解析用户输入的时间
✅ 复杂的日期计算

示例场景：日历应用、日志系统、生日提醒
```

### 6.4 常见错误和解决方案


**❌ 常见错误1：格式不匹配**

```python
# 错误示例
date_str = "2023/09/21"
# 这样会报错，因为格式不匹配
datetime.strptime(date_str, "%Y-%m-%d")

# 正确做法
datetime.strptime(date_str, "%Y/%m/%d")  # 格式要完全对应
```

**❌ 常见错误2：时区问题**

```python
# 注意：datetime.now() 获取的是本地时间
# 如果需要UTC时间，使用：
utc_time = datetime.utcnow()
```

### 6.5 学习路径建议


```
📚 学习阶段：

第1步：理解时间戳概念 → 掌握 time.time()
第2步：学会程序控制 → 掌握 time.sleep()  
第3步：处理日期时间 → 掌握 datetime.now()
第4步：美化时间显示 → 掌握 strftime()
第5步：处理用户输入 → 掌握 strptime()

🎯 实践建议：
- 制作一个简单的计时器
- 编写生日倒计时程序
- 创建个人日程管理工具
```

### 6.6 速记口诀


```
💡 记忆技巧：

time模块记忆：
"time点time，sleep要休眠"

datetime记忆：
"now现在，strftime格式化"
"strptime解析，格式要对应"

格式代码记忆：
"Y年m月d日，HMS时分秒"
```

**🎯 核心理解**

时间日期处理就像现实生活中看时间一样：
- **time模块**像数字时钟：简单直接，给你数字
- **datetime模块**像智能手表：功能丰富，信息完整
- **格式化**像给时间穿衣服：同样的时间，不同的展示方式
- **解析**像读懂时间：把别人说的时间变成自己理解的格式

记住：程序中的时间处理，就是为了让计算机像人一样理解和处理时间！