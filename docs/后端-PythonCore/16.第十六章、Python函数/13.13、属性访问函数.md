---
title: 13、属性访问函数
---
## 📚 目录

1. [什么是对象属性操作](#1-什么是对象属性操作)
2. [getattr()获取属性](#2-getattr获取属性)
3. [setattr()设置属性](#3-setattr设置属性)
4. [hasattr()检查属性存在](#4-hasattr检查属性存在)
5. [delattr()删除属性](#5-delattr删除属性)
6. [dir()获取对象属性列表](#6-dir获取对象属性列表)
7. [vars()获取对象属性字典](#7-vars获取对象属性字典)
8. [实际应用场景](#8-实际应用场景)
9. [核心要点总结](#9-核心要点总结)

---

## 1. 🔍 什么是对象属性操作


### 1.1 基本概念理解


**💡 什么是对象属性？**

简单来说，对象属性就像是物品上的标签或特征。比如一个人有姓名、年龄、身高这些属性。

```
现实世界的例子：

一个人：
├─ 姓名：张三
├─ 年龄：25
├─ 身高：175cm
└─ 职业：程序员

Python中的对象：
person = Person()
├─ name = "张三"
├─ age = 25  
├─ height = 175
└─ job = "程序员"
```

**🔸 为什么需要属性操作函数？**

有时候我们不知道对象有什么属性，或者属性名是动态的（比如从用户输入获得），这时就需要这些特殊函数来帮助我们操作属性。

### 1.2 属性操作的核心场景


```
日常开发中的需求：

1. 动态获取属性 → 不知道属性名，需要从变量中获取
2. 安全检查属性 → 访问前先确认属性是否存在
3. 批量设置属性 → 根据配置文件动态设置对象属性
4. 调试查看属性 → 开发时查看对象都有哪些属性
5. 删除无用属性 → 清理对象上不需要的属性
```

---

## 2. 🎯 getattr()获取属性


### 2.1 基本用法详解


**🔸 getattr()是什么？**

`getattr()`就像是一个"智能取物器"，你告诉它要从哪个对象上取什么东西，它就帮你取来。最重要的是，如果没有这个东西，它不会报错，而是给你一个默认值。

**📋 基本语法**
```python
getattr(对象, '属性名', 默认值)
```

### 2.2 通俗易懂的例子


```python
# 创建一个简单的学生类
class Student:
    def __init__(self):
        self.name = "小明"
        self.age = 18

student = Student()

# 🔸 普通方式获取属性（我们都知道属性名）
print(student.name)  # 输出：小明

# 🔸 使用getattr获取属性（属性名可能来自变量）
attr_name = "name"
print(getattr(student, attr_name))  # 输出：小明

# 🔸 获取不存在的属性，设置默认值
print(getattr(student, "score", 0))  # 输出：0（因为score属性不存在）
```

### 2.3 实际应用场景


**💡 场景1：处理配置信息**
```python
class Config:
    def __init__(self):
        self.debug = True
        self.port = 8080

config = Config()

# 从用户输入或配置文件获取要查询的配置项
setting_name = input("请输入要查询的配置:")  # 用户输入 "timeout"

# 安全获取配置，如果不存在则使用默认值
value = getattr(config, setting_name, "配置不存在")
print(f"{setting_name} = {value}")
```

**💡 场景2：动态调用对象方法**
```python
class Calculator:
    def add(self, a, b):
        return a + b
    
    def subtract(self, a, b):
        return a - b

calc = Calculator()
operation = "add"  # 这可能来自用户选择

# 动态获取方法并调用
method = getattr(calc, operation, None)
if method:
    result = method(10, 5)
    print(f"结果: {result}")  # 输出：结果: 15
else:
    print("不支持的操作")
```

### 2.4 与直接访问的对比


| 访问方式 | **何时使用** | **优点** | **缺点** |
|---------|------------|---------|---------|
| `obj.attr` | `属性名确定` | `简单直接，性能好` | `属性不存在会报错` |
| `getattr()` | `属性名动态变化` | `安全，可设默认值` | `写法稍复杂` |

---

## 3. ⚙️ setattr()设置属性


### 3.1 基本概念


**🔸 setattr()是什么？**

`setattr()`就像是一个"万能贴标器"，你可以给任何对象贴上任何标签（属性），哪怕这个标签之前不存在。

**📋 基本语法**
```python
setattr(对象, '属性名', 值)
```

### 3.2 简单易懂的使用方法


```python
class Person:
    pass

person = Person()

# 🔸 使用setattr动态添加属性
setattr(person, "name", "张三")
setattr(person, "age", 25)

print(person.name)  # 输出：张三
print(person.age)   # 输出：25

# 🔸 等价于直接赋值
person.height = 175  # 这和 setattr(person, "height", 175) 效果一样
```

### 3.3 动态批量设置属性


```python
# 💡 实际场景：从配置文件读取用户信息
user_data = {
    "username": "admin",
    "email": "admin@example.com", 
    "is_active": True,
    "login_count": 100
}

class User:
    pass

user = User()

# 批量设置属性
for key, value in user_data.items():
    setattr(user, key, value)

print(user.username)     # 输出：admin
print(user.login_count)  # 输出：100
```

### 3.4 修改已存在的属性


```python
class Product:
    def __init__(self):
        self.price = 100
        self.name = "商品A"

product = Product()
print(f"原价格: {product.price}")  # 输出：原价格: 100

# 动态修改价格
new_price = 120
setattr(product, "price", new_price)
print(f"新价格: {product.price}")  # 输出：新价格: 120
```

---

## 4. ✅ hasattr()检查属性存在


### 4.1 基本理解


**🔸 hasattr()是什么？**

`hasattr()`就像是一个"属性侦探"，它会告诉你某个对象是否拥有你要找的属性，返回True（有）或False（没有）。

**📋 基本语法**
```python
hasattr(对象, '属性名')  # 返回 True 或 False
```

### 4.2 安全的属性检查


```python
class Student:
    def __init__(self):
        self.name = "小红"
        self.age = 20

student = Student()

# 🔸 检查属性是否存在
if hasattr(student, "name"):
    print(f"学生姓名: {student.name}")  # 输出：学生姓名: 小红

if hasattr(student, "score"):
    print(f"学生成绩: {student.score}")
else:
    print("该学生还没有成绩记录")  # 这行会被执行
```

### 4.3 避免错误的安全编程


```python
def get_student_info(student, info_type):
    """安全获取学生信息"""
    
    # 🔸 先检查属性是否存在，避免程序崩溃
    if hasattr(student, info_type):
        return getattr(student, info_type)
    else:
        return f"该学生没有{info_type}信息"

# 使用示例
student = Student()
print(get_student_info(student, "name"))    # 输出：小红
print(get_student_info(student, "phone"))   # 输出：该学生没有phone信息
```

### 4.4 结合其他操作使用


```python
class GameCharacter:
    def __init__(self):
        self.health = 100
        self.level = 1

character = GameCharacter()

# 🔸 检查并安全操作属性
attributes_to_check = ["health", "mana", "level", "experience"]

for attr in attributes_to_check:
    if hasattr(character, attr):
        value = getattr(character, attr)
        print(f"✅ {attr}: {value}")
    else:
        print(f"❌ {attr}: 属性不存在")

# 输出：
# ✅ health: 100
# ❌ mana: 属性不存在  
# ✅ level: 1
# ❌ experience: 属性不存在
```

---

## 5. 🗑️ delattr()删除属性


### 5.1 基本概念


**🔸 delattr()是什么？**

`delattr()`就像是一个"属性橡皮擦"，它可以把对象上不需要的属性完全擦掉，就像这个属性从来没有存在过一样。

**📋 基本语法**
```python
delattr(对象, '属性名')
```

### 5.2 基本使用方法


```python
class TempData:
    def __init__(self):
        self.temp_file = "临时文件"
        self.important_data = "重要数据"
        self.cache = "缓存数据"

data = TempData()
print(hasattr(data, "temp_file"))  # 输出：True

# 🔸 删除不需要的临时属性
delattr(data, "temp_file")
print(hasattr(data, "temp_file"))  # 输出：False

# 重要数据依然存在
print(data.important_data)  # 输出：重要数据
```

### 5.3 安全删除属性


```python
def safe_delete_attr(obj, attr_name):
    """安全删除属性，如果属性不存在也不会报错"""
    if hasattr(obj, attr_name):
        delattr(obj, attr_name)
        print(f"✅ 已删除属性: {attr_name}")
    else:
        print(f"❌ 属性 {attr_name} 不存在，无需删除")

class TestObj:
    def __init__(self):
        self.data1 = "数据1"
        self.data2 = "数据2"

obj = TestObj()
safe_delete_attr(obj, "data1")      # ✅ 已删除属性: data1
safe_delete_attr(obj, "data3")      # ❌ 属性 data3 不存在，无需删除
```

### 5.4 批量清理属性


```python
class UserSession:
    def __init__(self):
        self.user_id = "12345"
        self.temp_token = "临时令牌"
        self.cache_data = "缓存"
        self.sensitive_info = "敏感信息"

session = UserSession()

# 🔸 清理临时和敏感数据
temp_attrs = ["temp_token", "cache_data", "sensitive_info"]

for attr in temp_attrs:
    if hasattr(session, attr):
        delattr(session, attr)

print(hasattr(session, "user_id"))        # True - 保留
print(hasattr(session, "temp_token"))     # False - 已删除
print(hasattr(session, "sensitive_info")) # False - 已删除
```

---

## 6. 📋 dir()获取对象属性列表


### 6.1 基本概念


**🔸 dir()是什么？**

`dir()`就像是一个"属性清单员"，它会给你列出一个对象拥有的所有属性和方法的名字，就像给物品做清单一样。

**📋 基本语法**
```python
dir(对象)  # 返回属性名列表
```

### 6.2 查看对象的所有属性


```python
class MyClass:
    def __init__(self):
        self.name = "测试"
        self.value = 100
    
    def my_method(self):
        return "这是一个方法"

obj = MyClass()

# 🔸 查看对象的所有属性和方法
all_attrs = dir(obj)
print("对象的所有属性和方法:")
for attr in all_attrs:
    print(f"  - {attr}")
```

### 6.3 过滤用户自定义的属性


```python
def get_user_attributes(obj):
    """获取用户自定义的属性（过滤掉Python内置的）"""
    user_attrs = []
    
    for attr in dir(obj):
        # 🔸 过滤掉以下划线开头的内置属性
        if not attr.startswith('_'):
            user_attrs.append(attr)
    
    return user_attrs

class Student:
    def __init__(self):
        self.name = "小明"
        self.age = 18
        self.grade = "高三"
    
    def study(self):
        return "正在学习"

student = Student()
user_attrs = get_user_attributes(student)
print("用户自定义的属性:", user_attrs)
# 输出：用户自定义的属性: ['age', 'grade', 'name', 'study']
```

### 6.4 动态探索对象


```python
def explore_object(obj, obj_name="对象"):
    """探索对象的属性和方法"""
    print(f"\n🔍 探索 {obj_name} 的属性:")
    
    for attr_name in sorted(dir(obj)):
        if not attr_name.startswith('_'):  # 只看用户属性
            try:
                attr_value = getattr(obj, attr_name)
                attr_type = type(attr_value).__name__
                print(f"  📌 {attr_name}: {attr_type}")
                
                # 如果是简单的数据类型，显示值
                if attr_type in ['str', 'int', 'float', 'bool']:
                    print(f"     值: {attr_value}")
                    
            except Exception as e:
                print(f"  ❌ {attr_name}: 无法访问")

# 使用示例
class Car:
    def __init__(self):
        self.brand = "丰田"
        self.price = 200000
        self.is_running = False
    
    def start(self):
        self.is_running = True

car = Car()
explore_object(car, "汽车对象")
```

---

## 7. 📚 vars()获取对象属性字典


### 7.1 基本概念


**🔸 vars()是什么？**

`vars()`就像是一个"属性字典制作器"，它把对象的属性整理成一个字典格式，属性名作为键，属性值作为值。

**📋 基本语法**
```python
vars(对象)  # 返回 {'属性名': 属性值} 字典
```

### 7.2 基本使用方法


```python
class Person:
    def __init__(self):
        self.name = "李四"
        self.age = 30
        self.city = "北京"

person = Person()

# 🔸 获取对象的属性字典
attr_dict = vars(person)
print("对象属性字典:", attr_dict)
# 输出：对象属性字典: {'name': '李四', 'age': 30, 'city': '北京'}

# 🔸 可以像普通字典一样操作
print(f"姓名: {attr_dict['name']}")
print(f"年龄: {attr_dict['age']}")
```

### 7.3 与dir()的区别


```
                  dir()                    vs                   vars()
    ┌─────────────────────────────┐              ┌─────────────────────────────┐
    │     返回属性名列表             │              │     返回属性字典              │
    │   ['name', 'age', 'city']   │              │  {'name': '李四',            │
    │                             │              │   'age': 30,                │
    │   包含所有属性和方法           │              │   'city': '北京'}           │
    │   (包括内置的)               │              │                             │
    │                             │              │   只包含实例属性             │
    └─────────────────────────────┘              └─────────────────────────────┘
```

### 7.4 实际应用场景


**💡 场景1：对象序列化**
```python
import json

class UserProfile:
    def __init__(self):
        self.username = "user123"
        self.email = "user@example.com"
        self.is_active = True
        self.login_count = 50

user = UserProfile()

# 🔸 将对象属性转换为JSON
user_data = vars(user)
json_str = json.dumps(user_data, ensure_ascii=False)
print("用户数据JSON:", json_str)
```

**💡 场景2：对象属性对比**
```python
def compare_objects(obj1, obj2):
    """比较两个对象的属性差异"""
    attrs1 = vars(obj1)
    attrs2 = vars(obj2)
    
    print("🔍 对象属性对比:")
    
    # 找出所有属性名
    all_attrs = set(attrs1.keys()) | set(attrs2.keys())
    
    for attr in sorted(all_attrs):
        value1 = attrs1.get(attr, "❌不存在")
        value2 = attrs2.get(attr, "❌不存在")
        
        if value1 == value2:
            print(f"  ✅ {attr}: {value1}")
        else:
            print(f"  🔄 {attr}: {value1} → {value2}")

# 使用示例
class Settings:
    pass

old_settings = Settings()
old_settings.debug = False
old_settings.port = 8080

new_settings = Settings()
new_settings.debug = True
new_settings.port = 9000
new_settings.ssl = True

compare_objects(old_settings, new_settings)
```

---

## 8. 🚀 实际应用场景


### 8.1 配置管理系统


```python
class ConfigManager:
    """配置管理器示例"""
    
    def __init__(self):
        self.debug = False
        self.port = 8080
        self.host = "localhost"
    
    def update_config(self, **kwargs):
        """批量更新配置"""
        for key, value in kwargs.items():
            if hasattr(self, key):
                setattr(self, key, value)
                print(f"✅ 更新配置 {key} = {value}")
            else:
                print(f"❌ 未知配置项: {key}")
    
    def get_all_config(self):
        """获取所有配置"""
        return vars(self)
    
    def reset_config(self, config_name):
        """重置特定配置"""
        default_values = {
            'debug': False,
            'port': 8080,
            'host': 'localhost'
        }
        
        if hasattr(self, config_name):
            default_value = default_values.get(config_name)
            setattr(self, config_name, default_value)
            print(f"🔄 重置 {config_name} 为默认值: {default_value}")

# 使用示例
config = ConfigManager()
print("初始配置:", config.get_all_config())

config.update_config(debug=True, port=9000, ssl=True)
print("更新后配置:", config.get_all_config())
```

### 8.2 动态API调用


```python
class APIClient:
    """动态API客户端示例"""
    
    def get_user(self):
        return {"id": 1, "name": "用户1"}
    
    def get_product(self):
        return {"id": 1, "name": "商品1"}
    
    def get_order(self):
        return {"id": 1, "total": 100}

def call_api(client, api_name):
    """动态调用API方法"""
    method_name = f"get_{api_name}"
    
    if hasattr(client, method_name):
        method = getattr(client, method_name)
        return method()
    else:
        return {"error": f"API {api_name} 不存在"}

# 使用示例
client = APIClient()

# 模拟用户输入
api_requests = ["user", "product", "order", "invalid"]

for api in api_requests:
    result = call_api(client, api)
    print(f"调用 {api} API: {result}")
```

### 8.3 对象调试工具


```python
def debug_object(obj, title="对象调试信息"):
    """完整的对象调试工具"""
    print(f"\n{'='*50}")
    print(f"🔧 {title}")
    print(f"{'='*50}")
    
    # 基本信息
    print(f"📌 对象类型: {type(obj).__name__}")
    
    # 所有属性列表
    all_attrs = dir(obj)
    print(f"📋 总属性数量: {len(all_attrs)}")
    
    # 用户自定义属性
    user_attrs = [attr for attr in all_attrs if not attr.startswith('_')]
    print(f"👤 用户属性数量: {len(user_attrs)}")
    
    # 实例属性详情
    if hasattr(obj, '__dict__'):
        instance_attrs = vars(obj)
        print(f"\n📊 实例属性详情:")
        for name, value in instance_attrs.items():
            print(f"  🔹 {name}: {type(value).__name__} = {value}")
    
    # 方法列表
    methods = [attr for attr in user_attrs 
              if callable(getattr(obj, attr, None))]
    print(f"\n🛠️ 可调用方法 ({len(methods)}个):")
    for method in methods:
        print(f"  🔧 {method}()")

# 使用示例
class SampleClass:
    def __init__(self):
        self.name = "示例对象"
        self.number = 42
        self.items = [1, 2, 3]
    
    def method1(self):
        pass
    
    def method2(self):
        pass

sample = SampleClass()
debug_object(sample, "示例对象调试")
```

---

## 9. 📋 核心要点总结


### 9.1 六大函数功能对比


| 函数 | **主要用途** | **返回值** | **典型场景** |
|------|------------|-----------|-------------|
| `getattr()` | **获取属性值** | `属性值或默认值` | `动态获取配置、API调用` |
| `setattr()` | **设置属性值** | `None` | `批量配置、动态赋值` |
| `hasattr()` | **检查属性存在** | `True/False` | `安全检查、条件判断` |
| `delattr()` | **删除属性** | `None` | `清理缓存、移除临时数据` |
| `dir()` | **列出所有属性名** | `属性名列表` | `调试、属性探索` |
| `vars()` | **获取属性字典** | `{名:值}字典` | `序列化、属性导出` |

### 9.2 使用建议和最佳实践


**🔸 安全性原则**
```python
# ✅ 推荐：安全的属性操作
if hasattr(obj, 'attr_name'):
    value = getattr(obj, 'attr_name')
    
# ❌ 避免：直接访问可能不存在的属性
# value = obj.unknown_attr  # 可能报错
```

**🔸 性能考虑**
```python
# ✅ 推荐：属性名确定时用直接访问
value = obj.known_attr

# ⚠️ 注意：属性名动态变化时才用getattr
attr_name = get_dynamic_name()
value = getattr(obj, attr_name, default)
```

**🔸 代码可读性**
```python
# ✅ 推荐：有意义的默认值
user_name = getattr(user, 'name', '匿名用户')
max_retries = getattr(config, 'max_retries', 3)

# ❌ 避免：无意义的默认值
value = getattr(obj, 'attr', None)  # None通常没有业务含义
```

### 9.3 常见错误避免


**⚠️ 错误1：忘记处理属性不存在的情况**
```python
# ❌ 错误写法
name = getattr(user, 'name')  # 如果name不存在会报错

# ✅ 正确写法  
name = getattr(user, 'name', '未知')
```

**⚠️ 错误2：删除不存在的属性**
```python
# ❌ 错误写法
delattr(obj, 'maybe_not_exist')  # 可能报错

# ✅ 正确写法
if hasattr(obj, 'maybe_not_exist'):
    delattr(obj, 'maybe_not_exist')
```

### 9.4 记忆口诀


```
📝 属性操作六兄弟，各有用途记心里：

🔍 getattr取属性，安全获取不出错
⚙️ setattr设属性，动态赋值很灵活  
✅ hasattr查存在，先检查来后操作
🗑️ delattr删属性，清理垃圾保整洁
📋 dir列属性名，调试探索全靠它
📚 vars取字典值，序列化时很有用
```

### 9.5 学习进阶建议


1. **🎯 基础练习**：先掌握每个函数的基本用法
2. **🔧 组合应用**：学会多个函数配合使用
3. **🚀 实际项目**：在真实项目中应用这些技巧
4. **📊 性能优化**：了解什么时候用直接访问，什么时候用动态访问

**核心记忆**：
- 属性操作函数是Python灵活性的体现
- 安全第一，先检查再操作
- 动态场景用动态方法，确定场景用直接方法
- 这些函数让你的代码更加智能和健壮