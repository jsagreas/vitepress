---
title: 17、字符串与编码操作
---
## 📚 目录

1. [字符与编码基础概念](#1-字符与编码基础概念)
2. [字符转换函数详解](#2-字符转换函数详解)
3. [字符串表示与格式化](#3-字符串表示与格式化)
4. [代码执行相关函数](#4-代码执行相关函数)
5. [实际应用场景](#5-实际应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔤 字符与编码基础概念


### 1.1 什么是字符编码


> 💡 **简单理解**：计算机只认识数字，所以需要给每个字符分配一个数字编号

**字符编码就像给文字建立身份证**：
```
字符 'A' ←→ 编号 65
字符 '中' ←→ 编号 20013
字符 '😀' ←→ 编号 128512

就像每个人都有身份证号一样，每个字符都有编码数字
```

### 1.2 常见编码标准


| 编码标准 | **覆盖范围** | **用途** | **特点** |
|---------|-------------|---------|----------|
| 🔸 **ASCII** | `英文字符` | `基础英文` | `0-127，最简单` |
| 🔸 **Unicode** | `全世界字符` | `国际化应用` | `统一标准，最全面` |
| 🔸 **UTF-8** | `Unicode实现` | `网络传输` | `变长编码，兼容ASCII` |

### 1.3 为什么需要了解编码


```
实际开发中的问题：
• 网页显示乱码 → 编码不匹配
• 文件读取错误 → 编码格式不对
• 国际化应用 → 需要支持多语言
• 数据传输 → 编码转换不当

掌握编码知识 = 解决这些问题的基础
```

---

## 2. 🔄 字符转换函数详解


### 2.1 chr() - 数字转字符


**🎯 作用**：把数字编码转换成对应的字符

> **生活类比**：就像用身份证号查找对应的人

```python
# 基本用法
print(chr(65))    # 输出：A
print(chr(97))    # 输出：a
print(chr(20013)) # 输出：中
print(chr(128512)) # 输出：😀
```

**💡 常用场景**：
```python
# 生成字母表
alphabet = [chr(i) for i in range(65, 91)]
print(alphabet)  # ['A', 'B', 'C', ..., 'Z']

# 生成密码字符
password_chars = [chr(i) for i in range(33, 127)]
print(''.join(password_chars[:10]))  # !"#$%&'()*
```

### 2.2 ord() - 字符转数字


**🎯 作用**：把字符转换成对应的数字编码

> **生活类比**：就像查看身份证号码

```python
# 基本用法
print(ord('A'))    # 输出：65
print(ord('a'))    # 输出：97
print(ord('中'))   # 输出：20013
print(ord('😀'))   # 输出：128512
```

**💡 实用技巧**：
```python
# 判断字符类型
def char_type(char):
    code = ord(char)
    if 65 <= code <= 90:
        return "大写字母"
    elif 97 <= code <= 122:
        return "小写字母"
    elif 48 <= code <= 57:
        return "数字"
    else:
        return "其他字符"

print(char_type('A'))  # 大写字母
print(char_type('9'))  # 数字
```

### 2.3 chr()和ord()的对称关系


```
字符 ←chr()→ 数字 ←ord()→ 字符

验证对称性：
chr(ord('A')) == 'A'  # True
ord(chr(65)) == 65    # True

这就像翻译：中文→英文→中文，结果不变
```

### 2.4 ascii() - 获取ASCII表示


**🎯 作用**：返回对象的ASCII可读表示，非ASCII字符会被转义

```python
# 基本用法
print(ascii('Hello'))     # 'Hello'
print(ascii('中文'))      # '\u4e2d\u6587'
print(ascii(['a', '中']))  # ['a', '\u4e2d']
```

**实际用途**：
```python
# 调试时查看特殊字符
text = "Hello\n世界\t"
print(ascii(text))  # 'Hello\n\u4e16\u754c\t'

# 在不支持Unicode的环境中显示
data = {'name': '张三', 'age': 25}
print(ascii(data))  # {'name': '\u5f20\u4e09', 'age': 25}
```

---

## 3. 📝 字符串表示与格式化


### 3.1 repr() - 对象的"官方"表示


**🎯 作用**：返回对象的官方字符串表示，通常可以用来重新创建对象

> **理解要点**：repr()返回的是"给程序员看的"，str()返回的是"给用户看的"

```python
# 字符串的repr vs str
text = "Hello\nWorld"
print(str(text))   # Hello
                   # World
print(repr(text))  # 'Hello\nWorld'

# 日期时间的表示
from datetime import datetime
now = datetime(2023, 12, 25, 10, 30)
print(str(now))   # 2023-12-25 10:30:00
print(repr(now))  # datetime.datetime(2023, 12, 25, 10, 30)
```

**📊 repr() vs str() 对比**：

| 函数 | **目的** | **用户** | **特点** |
|------|---------|---------|----------|
| `str()` | `用户友好显示` | `最终用户` | `易读、美观` |
| `repr()` | `开发调试` | `程序员` | `精确、可执行` |

### 3.2 format() - 高级字符串格式化


**🎯 作用**：对值进行格式化，支持复杂的格式控制

```python
# 基本数字格式化
print(format(123.456, '.2f'))      # 123.46 (保留2位小数)
print(format(1000000, ','))        # 1,000,000 (千分位)
print(format(42, '08d'))           # 00000042 (8位补零)

# 进制转换
print(format(255, 'x'))            # ff (十六进制)
print(format(255, 'b'))            # 11111111 (二进制)
print(format(255, 'o'))            # 377 (八进制)
```

**💡 实用格式化技巧**：
```python
# 百分比格式
ratio = 0.8563
print(format(ratio, '.1%'))        # 85.6%

# 科学计数法
big_number = 1234567890
print(format(big_number, '.2e'))   # 1.23e+09

# 居中对齐
name = "Python"
print(format(name, '^20'))         # "       Python       "
```

---

## 4. ⚡ 代码执行相关函数


### 4.1 eval() - 执行表达式


**🎯 作用**：执行字符串形式的Python表达式并返回结果

> ⚠️ **重要提醒**：eval()很强大但也很危险，只用于可信的代码

```python
# 数学表达式计算
result = eval("2 + 3 * 4")
print(result)  # 14

# 调用函数
result = eval("max([1, 5, 3])")
print(result)  # 5

# 访问变量
x = 10
result = eval("x * 2")
print(result)  # 20
```

**⚠️ 安全风险示例**：
```python
# 危险！！！绝对不要这样做
# user_input = "import os; os.system('rm -rf /')"
# eval(user_input)  # 这会删除整个系统！

# 安全的替代方案
def safe_calc(expression):
    # 只允许数字、运算符和括号
    allowed = set('0123456789+-*/().')
    if not all(c in allowed for c in expression):
        return "不安全的表达式"
    return eval(expression)

print(safe_calc("2+3*4"))  # 14
print(safe_calc("print('hack')"))  # 不安全的表达式
```

### 4.2 exec() - 执行代码块


**🎯 作用**：执行字符串形式的Python代码，没有返回值

```python
# 执行简单代码
code = """
x = 5
y = 10
print(f"x + y = {x + y}")
"""
exec(code)  # 输出：x + y = 15

# 动态创建函数
func_code = """
def greet(name):
    return f"Hello, {name}!"
"""
exec(func_code)
print(greet("Alice"))  # Hello, Alice!
```

### 4.3 compile() - 编译代码


**🎯 作用**：将字符串编译成代码对象，提高重复执行的效率

```python
# 编译表达式
expr = compile("2 + 3 * 4", "<string>", "eval")
result = eval(expr)
print(result)  # 14

# 编译代码块
code = compile("print('Hello World')", "<string>", "exec")
exec(code)  # Hello World

# 性能优化示例
import time

# 方案1：每次都解析
start = time.time()
for i in range(10000):
    eval("2 + 3 * 4")
time1 = time.time() - start

# 方案2：预编译
compiled_expr = compile("2 + 3 * 4", "<string>", "eval")
start = time.time()
for i in range(10000):
    eval(compiled_expr)
time2 = time.time() - start

print(f"直接执行: {time1:.4f}秒")
print(f"预编译: {time2:.4f}秒")
print(f"性能提升: {time1/time2:.2f}倍")
```

### 4.4 代码执行函数对比


| 函数 | **用途** | **返回值** | **安全性** |
|------|---------|-----------|-----------|
| `eval()` | `执行表达式` | `表达式结果` | `⚠️ 需谨慎` |
| `exec()` | `执行代码块` | `None` | `⚠️ 需谨慎` |
| `compile()` | `编译代码` | `代码对象` | `相对安全` |

---

## 5. 🚀 实际应用场景


### 5.1 配置文件处理


```python
def load_config(config_string):
    """安全地加载配置"""
    config = {}
    
    # 定义安全的执行环境
    safe_dict = {
        "__builtins__": {},  # 禁用内置函数
        "True": True,
        "False": False,
        "None": None
    }
    
    exec(config_string, safe_dict, config)
    return config

# 使用示例
config_text = """
database_host = "localhost"
database_port = 5432
debug_mode = True
max_connections = 100
"""

config = load_config(config_text)
print(config)  # {'database_host': 'localhost', ...}
```

### 5.2 数据验证和清理


```python
def validate_username(username):
    """验证用户名字符"""
    for char in username:
        code = ord(char)
        # 只允许字母、数字、下划线
        if not ((65 <= code <= 90) or      # A-Z
                (97 <= code <= 122) or     # a-z  
                (48 <= code <= 57) or      # 0-9
                char == '_'):
            return False, f"包含非法字符: {repr(char)}"
    return True, "用户名有效"

# 测试
print(validate_username("user_123"))      # (True, "用户名有效")
print(validate_username("user@123"))      # (False, "包含非法字符: '@'")
print(validate_username("用户123"))       # (False, "包含非法字符: '用'")
```

### 5.3 文本处理和转换


```python
def text_analysis(text):
    """文本字符分析"""
    stats = {
        'ascii_chars': 0,
        'chinese_chars': 0,
        'other_chars': 0,
        'total_chars': len(text)
    }
    
    for char in text:
        code = ord(char)
        if code < 128:  # ASCII字符
            stats['ascii_chars'] += 1
        elif 0x4e00 <= code <= 0x9fff:  # 中文字符范围
            stats['chinese_chars'] += 1
        else:
            stats['other_chars'] += 1
    
    return stats

# 使用示例
text = "Hello 世界! 🌍"
analysis = text_analysis(text)
print(f"ASCII字符: {analysis['ascii_chars']}")    # 8
print(f"中文字符: {analysis['chinese_chars']}")    # 2  
print(f"其他字符: {analysis['other_chars']}")      # 2
```

### 5.4 密码强度检测


```python
def password_strength(password):
    """检测密码强度"""
    score = 0
    feedback = []
    
    # 检查长度
    if len(password) >= 8:
        score += 1
    else:
        feedback.append("密码长度至少8位")
    
    # 检查字符类型
    has_upper = any(65 <= ord(c) <= 90 for c in password)
    has_lower = any(97 <= ord(c) <= 122 for c in password)  
    has_digit = any(48 <= ord(c) <= 57 for c in password)
    has_special = any(ord(c) in range(33, 48) for c in password)
    
    if has_upper: score += 1
    else: feedback.append("需要大写字母")
        
    if has_lower: score += 1
    else: feedback.append("需要小写字母")
        
    if has_digit: score += 1
    else: feedback.append("需要数字")
        
    if has_special: score += 1
    else: feedback.append("需要特殊字符")
    
    # 评级
    if score >= 4:
        strength = "强"
    elif score >= 3:
        strength = "中等"
    else:
        strength = "弱"
    
    return {
        'strength': strength,
        'score': score,
        'feedback': feedback
    }

# 测试
print(password_strength("123456"))           # 弱
print(password_strength("Password123!"))     # 强
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 字符编码：计算机用数字表示文字的方法
🔸 chr/ord：数字和字符的相互转换
🔸 repr/ascii：获取对象的字符串表示
🔸 format：强大的格式化工具
🔸 eval/exec：执行字符串代码（需谨慎使用）
🔸 compile：编译代码提高性能
```

### 6.2 关键理解要点


**🔹 字符编码的本质**
```
理解要点：
• 计算机只认识数字，所以文字需要编码
• ASCII覆盖英文，Unicode覆盖全世界
• chr()和ord()是编码转换的基础工具
```

**🔹 字符串表示的区别**
```
记忆方法：
• str()：给用户看的，美观易读
• repr()：给程序员看的，精确完整  
• ascii()：强制转成ASCII可显示格式
```

**🔹 代码执行的安全性**
```
安全原则：
• eval()和exec()很强大但很危险
• 永远不要执行不可信的代码
• 使用compile()可以提高重复执行的性能
```

### 6.3 实际应用价值


**🎯 日常开发场景**：
- **文本处理**：字符分析、编码转换、格式验证
- **配置管理**：动态加载配置文件
- **数据验证**：用户输入检查、密码强度检测  
- **国际化**：多语言支持、字符集处理

**🔧 问题解决能力**：
- **编码问题**：解决乱码、文件读取错误
- **格式化需求**：数字显示、报表生成
- **动态执行**：配置解析、脚本执行
- **安全防护**：输入验证、代码审查

### 6.4 学习建议


**📚 循序渐进**：
1. **先掌握基础**：chr/ord的基本用法
2. **理解编码**：ASCII、Unicode的概念
3. **学会格式化**：format的各种用法
4. **谨慎使用执行**：eval/exec的安全问题

**💡 实践要点**：
- 多做字符处理的小练习
- 关注编码相关的错误处理
- 学会使用format美化输出
- 永远不要执行用户输入的代码

**核心记忆口诀**：
```
🎯 一句话总结：
chr转字符ord转码，repr显示format格式好
eval执行要谨慎，compile优化性能高
编码理解是基础，安全意识不能少
```