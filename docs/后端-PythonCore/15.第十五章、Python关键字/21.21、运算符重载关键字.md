---
title: 21、运算符重载关键字
---
## 📚 目录

1. [运算符重载基础概念](#1-运算符重载基础概念)
2. [算术运算符重载](#2-算术运算符重载)
3. [比较运算符重载](#3-比较运算符重载)
4. [运算符重载实战应用](#4-运算符重载实战应用)
5. [最佳实践与注意事项](#5-最佳实践与注意事项)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 运算符重载基础概念


### 1.1 什么是运算符重载


> **💡 核心理解**  
> 运算符重载就是让你自己写的类也能使用`+`、`-`、`==`这些符号，就像内置类型一样自然

**🔍 生活类比**：
```
想象你有一个"钱包"类：
- 两个钱包相加 = 钱数相加
- 比较两个钱包 = 比较钱数多少
- 这就是运算符重载的作用！
```

**基本原理**：
```python
# 没有运算符重载时
wallet1 = Wallet(100)
wallet2 = Wallet(50)
total = wallet1.add(wallet2)  # 麻烦！

# 有了运算符重载后
total = wallet1 + wallet2     # 自然！
```

### 1.2 特殊方法命名规律


> **📌 重要规律**  
> Python中所有特殊方法都是`__方法名__`的格式，这叫做"魔法方法"

**命名模式**：
```
运算符    →    特殊方法    →    调用时机
   +      →    __add__     →    obj1 + obj2
   -      →    __sub__     →    obj1 - obj2
   ==     →    __eq__      →    obj1 == obj2
   <      →    __lt__      →    obj1 < obj2
```

**📊 记忆技巧**：
- `add` = addition（加法）
- `sub` = subtraction（减法）
- `eq` = equal（相等）
- `lt` = less than（小于）

---

## 2. ➕ 算术运算符重载


### 2.1 加法运算符 `__add__`


**🎯 核心作用**：定义两个对象相加时的行为

```python
class Money:
    def __init__(self, amount):
        self.amount = amount
    
    def __add__(self, other):
        """定义 + 运算符的行为"""
        if isinstance(other, Money):
            return Money(self.amount + other.amount)
        elif isinstance(other, (int, float)):
            return Money(self.amount + other)
        else:
            return NotImplemented
    
    def __str__(self):
        return f"${self.amount}"

# 使用示例
money1 = Money(100)
money2 = Money(50)
total = money1 + money2        # 调用 __add__
print(total)                   # $150

cash = money1 + 25             # 也支持加数字
print(cash)                    # $125
```

> **⚠️ 常见误区**  
> `__add__`方法要返回一个新对象，不要修改原对象

### 2.2 减法运算符 `__sub__`


```python
class Money:
    # ... 前面的代码 ...
    
    def __sub__(self, other):
        """定义 - 运算符的行为"""
        if isinstance(other, Money):
            return Money(self.amount - other.amount)
        elif isinstance(other, (int, float)):
            return Money(self.amount - other)
        else:
            return NotImplemented

# 使用示例
money1 = Money(100)
money2 = Money(30)
remaining = money1 - money2    # 调用 __sub__
print(remaining)               # $70
```

### 2.3 乘法和除法运算符


```python
class Money:
    # ... 前面的代码 ...
    
    def __mul__(self, other):
        """定义 * 运算符（乘法）"""
        if isinstance(other, (int, float)):
            return Money(self.amount * other)
        return NotImplemented
    
    def __truediv__(self, other):
        """定义 / 运算符（真除法）"""
        if isinstance(other, (int, float)) and other != 0:
            return Money(self.amount / other)
        return NotImplemented
    
    def __mod__(self, other):
        """定义 % 运算符（取余）"""
        if isinstance(other, (int, float)) and other != 0:
            return Money(self.amount % other)
        return NotImplemented
    
    def __pow__(self, other):
        """定义 ** 运算符（幂运算）"""
        if isinstance(other, (int, float)):
            return Money(self.amount ** other)
        return NotImplemented

# 使用示例
money = Money(100)
doubled = money * 2            # $200
half = money / 2               # $50
remainder = money % 30         # $10
squared = money ** 2           # $10000
```

### 2.4 算术运算符对照表


| **运算符** | **方法名** | **说明** | **使用示例** |
|------------|------------|----------|--------------|
| `+` | `__add__` | 加法运算 | `a + b` |
| `-` | `__sub__` | 减法运算 | `a - b` |
| `*` | `__mul__` | 乘法运算 | `a * b` |
| `/` | `__truediv__` | 真除法 | `a / b` |
| `//` | `__floordiv__` | 整数除法 | `a // b` |
| `%` | `__mod__` | 取余运算 | `a % b` |
| `**` | `__pow__` | 幂运算 | `a ** b` |

---

## 3. 🔍 比较运算符重载


### 3.1 相等比较 `__eq__`


**🎯 最重要的比较运算符**：决定两个对象是否相等

```python
class Student:
    def __init__(self, name, student_id):
        self.name = name
        self.student_id = student_id
    
    def __eq__(self, other):
        """定义 == 运算符的行为"""
        if isinstance(other, Student):
            # 学号相同就认为是同一个学生
            return self.student_id == other.student_id
        return False
    
    def __str__(self):
        return f"{self.name}({self.student_id})"

# 使用示例
student1 = Student("张三", "2024001")
student2 = Student("张三", "2024001")  # 同一个学号
student3 = Student("李四", "2024002")

print(student1 == student2)  # True （学号相同）
print(student1 == student3)  # False（学号不同）
```

> **💡 核心理解**  
> `__eq__`定义了"什么情况下两个对象相等"，这个定义完全由你决定

### 3.2 不等比较 `__ne__`


```python
class Student:
    # ... 前面的代码 ...
    
    def __ne__(self, other):
        """定义 != 运算符的行为"""
        # 通常直接返回 __eq__ 的相反结果
        return not self.__eq__(other)

# 使用示例
print(student1 != student3)  # True
```

> **📌 小提示**  
> 现代Python中，如果你定义了`__eq__`，Python会自动提供`__ne__`，所以通常不需要手动定义

### 3.3 大小比较运算符


```python
class Score:
    def __init__(self, points):
        self.points = points
    
    def __lt__(self, other):
        """定义 < 运算符（小于）"""
        if isinstance(other, Score):
            return self.points < other.points
        elif isinstance(other, (int, float)):
            return self.points < other
        return NotImplemented
    
    def __le__(self, other):
        """定义 <= 运算符（小于等于）"""
        if isinstance(other, Score):
            return self.points <= other.points
        elif isinstance(other, (int, float)):
            return self.points <= other
        return NotImplemented
    
    def __gt__(self, other):
        """定义 > 运算符（大于）"""
        if isinstance(other, Score):
            return self.points > other.points
        elif isinstance(other, (int, float)):
            return self.points > other
        return NotImplemented
    
    def __ge__(self, other):
        """定义 >= 运算符（大于等于）"""
        if isinstance(other, Score):
            return self.points >= other.points
        elif isinstance(other, (int, float)):
            return self.points >= other
        return NotImplemented
    
    def __str__(self):
        return f"Score({self.points})"

# 使用示例
score1 = Score(85)
score2 = Score(92)

print(score1 < score2)   # True
print(score1 <= 85)      # True
print(score2 > score1)   # True
print(score2 >= 90)      # True
```

### 3.4 比较运算符对照表


| **运算符** | **方法名** | **说明** | **记忆技巧** |
|------------|------------|----------|--------------|
| `==` | `__eq__` | 等于 | **eq**ual |
| `!=` | `__ne__` | 不等于 | **n**ot **e**qual |
| `<` | `__lt__` | 小于 | **l**ess **t**han |
| `<=` | `__le__` | 小于等于 | **l**ess **e**qual |
| `>` | `__gt__` | 大于 | **g**reater **t**han |
| `>=` | `__ge__` | 大于等于 | **g**reater **e**qual |

---

## 4. 🚀 运算符重载实战应用


### 4.1 完整示例：向量类


```python
class Vector:
    """二维向量类 - 演示完整的运算符重载"""
    
    def __init__(self, x, y):
        self.x = x
        self.y = y
    
    # 算术运算符
    def __add__(self, other):
        """向量加法：(x1,y1) + (x2,y2) = (x1+x2, y1+y2)"""
        if isinstance(other, Vector):
            return Vector(self.x + other.x, self.y + other.y)
        return NotImplemented
    
    def __sub__(self, other):
        """向量减法"""
        if isinstance(other, Vector):
            return Vector(self.x - other.x, self.y - other.y)
        return NotImplemented
    
    def __mul__(self, scalar):
        """向量数乘：向量乘以一个数"""
        if isinstance(scalar, (int, float)):
            return Vector(self.x * scalar, self.y * scalar)
        return NotImplemented
    
    # 比较运算符
    def __eq__(self, other):
        """向量相等：坐标完全相同"""
        if isinstance(other, Vector):
            return self.x == other.x and self.y == other.y
        return False
    
    def __lt__(self, other):
        """向量大小比较：按长度比较"""
        if isinstance(other, Vector):
            return self.length() < other.length()
        return NotImplemented
    
    # 辅助方法
    def length(self):
        """计算向量长度"""
        return (self.x**2 + self.y**2)**0.5
    
    def __str__(self):
        return f"Vector({self.x}, {self.y})"
    
    def __repr__(self):
        return self.__str__()

# 实战演示
v1 = Vector(3, 4)
v2 = Vector(1, 2)

print(f"v1 = {v1}")           # Vector(3, 4)
print(f"v2 = {v2}")           # Vector(1, 2)
print(f"v1 + v2 = {v1 + v2}") # Vector(4, 6)
print(f"v1 - v2 = {v1 - v2}") # Vector(2, 2)
print(f"v1 * 2 = {v1 * 2}")   # Vector(6, 8)
print(f"v1 == v2: {v1 == v2}") # False
print(f"v1 < v2: {v1 < v2}")   # False (v1长度更大)
```

### 4.2 实用示例：时间类


```python
class Time:
    """时间类 - 支持时间运算"""
    
    def __init__(self, hours, minutes):
        self.hours = hours
        self.minutes = minutes
        self._normalize()  # 标准化时间格式
    
    def _normalize(self):
        """将分钟数标准化（超过60分钟转换为小时）"""
        if self.minutes >= 60:
            self.hours += self.minutes // 60
            self.minutes = self.minutes % 60
        elif self.minutes < 0:
            hours_to_subtract = (-self.minutes - 1) // 60 + 1
            self.hours -= hours_to_subtract
            self.minutes += hours_to_subtract * 60
    
    def __add__(self, other):
        """时间相加"""
        if isinstance(other, Time):
            return Time(self.hours + other.hours, 
                       self.minutes + other.minutes)
        return NotImplemented
    
    def __sub__(self, other):
        """时间相减"""
        if isinstance(other, Time):
            return Time(self.hours - other.hours, 
                       self.minutes - other.minutes)
        return NotImplemented
    
    def __eq__(self, other):
        """时间相等比较"""
        if isinstance(other, Time):
            return self.hours == other.hours and self.minutes == other.minutes
        return False
    
    def __lt__(self, other):
        """时间早晚比较"""
        if isinstance(other, Time):
            if self.hours != other.hours:
                return self.hours < other.hours
            return self.minutes < other.minutes
        return NotImplemented
    
    def __str__(self):
        return f"{self.hours:02d}:{self.minutes:02d}"

# 使用示例
work_start = Time(9, 0)      # 9:00
work_hours = Time(8, 30)     # 8小时30分钟
work_end = work_start + work_hours

print(f"上班时间: {work_start}")     # 09:00
print(f"工作时长: {work_hours}")     # 08:30
print(f"下班时间: {work_end}")       # 17:30
```

---

## 5. ⚡ 最佳实践与注意事项


### 5.1 设计原则


> **🔔 重要原则**
> 1. **直观性**：运算符的行为要符合直觉
> 2. **一致性**：相似的运算符行为要保持一致
> 3. **对称性**：`a + b`和`b + a`应该等价

**✅ 好的设计**：
```python
# 直观：数字相加
money1 + money2  # 返回金额相加的新Money对象

# 一致：所有比较运算符都基于同一个属性
student1 < student2  # 基于成绩比较
student1 == student2 # 基于学号比较（一致的逻辑）
```

**❌ 不好的设计**：
```python
# 不直观：加法返回字符串？
money1 + money2  # 返回"总金额是150元"这样的字符串

# 不一致：比较运算符使用不同逻辑
student1 < student2  # 基于成绩比较
student1 == student2 # 基于姓名比较（逻辑不一致）
```

### 5.2 类型检查最佳实践


```python
def __add__(self, other):
    # ✅ 推荐做法
    if isinstance(other, self.__class__):
        # 同类型运算
        return self.__class__(self.value + other.value)
    elif isinstance(other, (int, float)):
        # 与数字运算
        return self.__class__(self.value + other)
    else:
        # 不支持的类型
        return NotImplemented  # 让Python尝试其他方式
```

> **📌 关键提示**  
> 返回`NotImplemented`而不是抛出异常，这样Python会尝试调用另一个对象的运算符方法

### 5.3 常见陷阱与解决方案


**陷阱1：忘记返回新对象**
```python
# ❌ 错误：修改了原对象
def __add__(self, other):
    self.value += other.value
    return self  # 不应该修改self

# ✅ 正确：返回新对象
def __add__(self, other):
    return MyClass(self.value + other.value)
```

**陷阱2：比较运算符不完整**
```python
# ❌ 只定义了部分比较运算符
class Score:
    def __eq__(self, other):
        return self.points == other.points
    # 缺少其他比较运算符

# ✅ 使用functools.total_ordering装饰器
from functools import total_ordering

@total_ordering
class Score:
    def __eq__(self, other):
        return self.points == other.points
    
    def __lt__(self, other):
        return self.points < other.points
    # 其他比较运算符会自动生成
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 运算符重载：让自定义类支持+、-、==等运算符
🔸 魔法方法：以__开头和结尾的特殊方法名
🔸 返回新对象：运算符重载通常返回新对象，不修改原对象
🔸 类型检查：使用isinstance检查参数类型
🔸 NotImplemented：不支持的运算返回这个特殊值
```

### 6.2 运算符方法速查表


| **类别** | **运算符** | **方法** | **记忆要点** |
|----------|------------|----------|--------------|
| **算术** | `+` `-` `*` `/` | `__add__` `__sub__` `__mul__` `__truediv__` | 基础数学运算 |
| **比较** | `==` `!=` `<` `>` | `__eq__` `__ne__` `__lt__` `__gt__` | equal, not equal, less than, greater than |
| **其他** | `%` `**` `<=` `>=` | `__mod__` `__pow__` `__le__` `__ge__` | 取余、幂运算、小于等于、大于等于 |

### 6.3 学习检查清单


**🔧 实践检查清单**：
- [ ] 理解运算符重载的基本概念
- [ ] 掌握算术运算符的实现方法
- [ ] 掌握比较运算符的实现方法  
- [ ] 了解`NotImplemented`的作用
- [ ] 能够设计直观一致的运算符行为
- [ ] 会使用`isinstance`进行类型检查

**💡 核心理解要点**：
- 运算符重载让代码更自然、更易读
- 设计时要考虑直观性和一致性
- 返回新对象而不是修改原对象
- 合理的类型检查让代码更健壮

**🎯 实际应用价值**：
- **数学计算类**：向量、矩阵、复数运算
- **时间日期类**：时间段的加减运算
- **业务对象类**：金额、分数、评级的比较
- **数据结构类**：让自定义容器支持运算符

**核心记忆口诀**：
> 魔法方法双下划线，运算符号变自然  
> 加减乘除eq和lt，返回新值莫改原  
> 类型检查用isinstance，NotImplemented是关键