---
title: 22、属性访问控制关键字
---
## 📚 目录

1. [属性访问控制概述](#1-属性访问控制概述)
2. [属性获取控制方法](#2-属性获取控制方法)
3. [属性设置与删除控制](#3-属性设置与删除控制)
4. [对象信息获取属性](#4-对象信息获取属性)
5. [实际应用场景](#5-实际应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 属性访问控制概述


### 1.1 什么是属性访问控制


**简单理解**：就像门卫管理进出一样，Python可以控制对象属性的访问权限

```
生活中的例子：
银行账户 → 你不能随便查看别人的余额
手机通讯录 → 有些联系人可能设为私密

Python对象中：
obj.name → 获取属性值时可以进行控制
obj.age = 25 → 设置属性值时可以进行验证
del obj.temp → 删除属性时可以进行拦截
```

**🔸 核心作用**
- **数据保护**：防止重要属性被随意修改
- **输入验证**：确保设置的值符合要求
- **动态计算**：属性值可以根据其他数据动态生成
- **访问记录**：记录属性的访问情况

### 1.2 属性访问的基本流程


```
属性访问过程：
用户访问 obj.name
     ↓
Python检查是否有特殊方法
     ↓
调用相应的控制方法
     ↓
返回处理后的结果
```

---

## 2. 🔍 属性获取控制方法


### 2.1 `__getattr__` - 属性获取兜底机制


**🔸 基本含义**
- 当访问**不存在的属性**时，Python会调用这个方法
- 类似"找不到东西时的备用方案"

```python
class SmartDict:
    def __init__(self):
        self.data = {'name': '张三', 'age': 25}
    
    def __getattr__(self, key):
        # 当访问不存在的属性时调用
        print(f"正在查找属性: {key}")
        if key in self.data:
            return self.data[key]
        return f"没有找到属性 {key}"

# 使用示例
person = SmartDict()
print(person.name)      # 输出: 张三 (存在于data中)
print(person.height)    # 输出: 没有找到属性 height
```

**💡 实际应用场景**
- **配置对象**：动态访问配置项
- **API包装**：包装第三方API调用
- **数据代理**：代理访问数据库或文件

### 2.2 `__getattribute__` - 全能属性拦截器


**🔸 基本含义**
- **每次访问任何属性**都会调用这个方法
- 比`__getattr__`更强大，但也更危险

```python
class MonitoredObject:
    def __init__(self):
        self.name = "监控对象"
        self.count = 0
    
    def __getattribute__(self, name):
        # 每次访问属性都会执行
        print(f"访问了属性: {name}")
        
        # 必须调用父类方法，否则会无限递归
        return super().__getattribute__(name)

# 使用示例
obj = MonitoredObject()
print(obj.name)   # 输出: 访问了属性: name \n 监控对象
print(obj.count)  # 输出: 访问了属性: count \n 0
```

**⚠️ 重要注意事项**
```python
class 错误示例:
    def __getattribute__(self, name):
        # 错误！这会导致无限递归
        return self.data[name]  # 访问self.data又会调用__getattribute__

class 正确示例:
    def __getattribute__(self, name):
        # 正确！使用super()避免递归
        return super().__getattribute__(name)
```

### 2.3 两者的区别对比


| 特性对比 | `__getattr__` | `__getattribute__` |
|---------|---------------|-------------------|
| **调用时机** | 属性不存在时 | 每次访问属性时 |
| **使用难度** | 🟢 简单安全 | 🟡 需要小心使用 |
| **性能影响** | 🟢 只在需要时调用 | 🟡 每次都调用 |
| **适用场景** | 🎯 动态属性、默认值 | 🎯 属性访问监控 |

---

## 3. ✏️ 属性设置与删除控制


### 3.1 `__setattr__` - 属性设置控制器


**🔸 基本含义**
- 当设置对象属性时调用：`obj.name = value`
- 可以验证数据、记录变化、阻止修改等

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def __setattr__(self, name, value):
        # 年龄验证
        if name == 'age':
            if not isinstance(value, int) or value < 0:
                raise ValueError("年龄必须是非负整数")
            if value > 150:
                raise ValueError("年龄不能超过150岁")
        
        # 姓名验证
        if name == 'name':
            if not isinstance(value, str) or len(value.strip()) == 0:
                raise ValueError("姓名不能为空")
        
        # 通过验证后设置属性
        super().__setattr__(name, value)
        print(f"已设置 {name} = {value}")

# 使用示例
person = Person("张三", 25)
# 输出: 已设置 name = 张三
#      已设置 age = 25

person.age = 30      # 输出: 已设置 age = 30
# person.age = -5    # 报错: 年龄必须是非负整数
```

**🎯 实际应用**
- **数据验证**：确保属性值符合业务规则
- **类型转换**：自动转换数据类型
- **变化通知**：属性改变时通知其他组件

### 3.2 `__delattr__` - 属性删除控制器


**🔸 基本含义**
- 当删除对象属性时调用：`del obj.name`
- 可以阻止删除重要属性

```python
class ProtectedObject:
    def __init__(self):
        self.important_data = "重要信息"
        self.temp_data = "临时信息"
    
    def __delattr__(self, name):
        # 保护重要属性
        if name == 'important_data':
            raise AttributeError("重要数据不能删除")
        
        print(f"正在删除属性: {name}")
        super().__delattr__(name)

# 使用示例
obj = ProtectedObject()
del obj.temp_data        # 输出: 正在删除属性: temp_data
# del obj.important_data # 报错: 重要数据不能删除
```

---

## 4. 📋 对象信息获取属性


### 4.1 `__dict__` - 实例属性字典


**🔸 基本含义**
- 存储对象实例属性的字典
- 类似对象的"个人档案袋"

```python
class Student:
    def __init__(self, name, age, grade):
        self.name = name
        self.age = age
        self.grade = grade

student = Student("小明", 16, "高一")

# 查看所有实例属性
print(student.__dict__)
# 输出: {'name': '小明', 'age': 16, 'grade': '高一'}

# 动态添加属性
student.hobby = "篮球"
print(student.__dict__)
# 输出: {'name': '小明', 'age': 16, 'grade': '高一', 'hobby': '篮球'}
```

**💡 实际用途**
```python
# 批量设置属性
data = {'math': 95, 'english': 88, 'physics': 92}
for subject, score in data.items():
    setattr(student, subject, score)

# 属性序列化
import json
student_json = json.dumps(student.__dict__, ensure_ascii=False)
print(student_json)
# 输出: {"name": "小明", "age": 16, "grade": "高一", "hobby": "篮球", "math": 95, "english": 88, "physics": 92}
```

### 4.2 `__class__` - 对象类型信息


**🔸 基本含义**
- 获取对象所属的类
- 相当于查看"出生证明"

```python
class Dog:
    species = "犬科动物"

class Cat:
    species = "猫科动物"

dog = Dog()
cat = Cat()

print(dog.__class__)        # 输出: <class '__main__.Dog'>
print(dog.__class__.__name__)  # 输出: Dog
print(dog.__class__.species)   # 输出: 犬科动物

# 判断对象类型
if cat.__class__.__name__ == 'Cat':
    print("这是一只猫")  # 会执行
```

### 4.3 `__module__` - 模块信息


**🔸 基本含义**
- 显示类定义在哪个模块中
- 用于调试和代码组织

```python
class MyClass:
    pass

obj = MyClass()
print(obj.__class__.__module__)  # 输出: __main__ (当前模块)

# 导入的类会显示其模块名
import datetime
now = datetime.datetime.now()
print(now.__class__.__module__)  # 输出: datetime
```

### 4.4 `__name__` - 对象名称


**🔸 基本含义**
- 获取类或函数的名称
- 主要用于类和函数对象

```python
class Calculator:
    pass

def add_numbers(a, b):
    return a + b

print(Calculator.__name__)  # 输出: Calculator
print(add_numbers.__name__) # 输出: add_numbers

# 在类内部获取类名
class SelfAware:
    def get_class_name(self):
        return self.__class__.__name__

obj = SelfAware()
print(obj.get_class_name())  # 输出: SelfAware
```

---

## 5. 🚀 实际应用场景


### 5.1 智能配置管理器


```python
class Config:
    """智能配置管理，支持点号访问和验证"""
    
    def __init__(self, **kwargs):
        self._config = kwargs
        self._readonly = set()  # 只读配置项
    
    def __getattr__(self, key):
        if key.startswith('_'):
            raise AttributeError(f"私有属性 {key} 不可访问")
        return self._config.get(key, None)
    
    def __setattr__(self, key, value):
        if key.startswith('_'):
            super().__setattr__(key, value)
            return
        
        if hasattr(self, '_readonly') and key in self._readonly:
            raise AttributeError(f"配置项 {key} 是只读的")
        
        if hasattr(self, '_config'):
            self._config[key] = value
        else:
            super().__setattr__(key, value)
    
    def make_readonly(self, *keys):
        """设置只读配置项"""
        self._readonly.update(keys)

# 使用示例
config = Config(
    database_url="localhost:5432",
    debug=True,
    max_connections=100
)

print(config.database_url)  # 输出: localhost:5432
config.debug = False        # 正常设置
config.make_readonly('database_url')
# config.database_url = "new_url"  # 报错: 配置项 database_url 是只读的
```

### 5.2 属性访问监控器


```python
class AccessMonitor:
    """监控属性访问情况"""
    
    def __init__(self):
        self._data = {}
        self._access_log = []
    
    def __getattribute__(self, name):
        if not name.startswith('_'):
            self._access_log.append(f"读取: {name}")
        return super().__getattribute__(name)
    
    def __setattr__(self, name, value):
        if not name.startswith('_'):
            if hasattr(self, '_access_log'):
                self._access_log.append(f"设置: {name} = {value}")
        super().__setattr__(name, value)
    
    def get_access_log(self):
        return self._access_log.copy()

# 使用示例
monitor = AccessMonitor()
monitor.username = "admin"
monitor.password = "secret"
print(monitor.username)

print("访问记录:")
for log in monitor.get_access_log():
    print(f"  {log}")
```

### 5.3 对象信息调试工具


```python
def object_info(obj):
    """获取对象的详细信息"""
    
    info = {
        'class_name': obj.__class__.__name__,
        'module': obj.__class__.__module__,
        'attributes': {},
        'methods': []
    }
    
    # 获取属性信息
    if hasattr(obj, '__dict__'):
        info['attributes'] = obj.__dict__.copy()
    
    # 获取方法信息
    for attr_name in dir(obj):
        if not attr_name.startswith('_'):
            attr_value = getattr(obj, attr_name)
            if callable(attr_value):
                info['methods'].append(attr_name)
    
    return info

# 使用示例
class Person:
    def __init__(self, name):
        self.name = name
        self.age = 25
    
    def say_hello(self):
        return f"Hello, I'm {self.name}"

person = Person("张三")
info = object_info(person)

print(f"类名: {info['class_name']}")
print(f"模块: {info['module']}")
print(f"属性: {info['attributes']}")
print(f"方法: {info['methods']}")
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 属性访问控制：Python提供了完整的属性访问控制机制
🔸 获取控制：__getattr__处理不存在的属性，__getattribute__拦截所有访问
🔸 设置控制：__setattr__控制属性设置，支持验证和转换
🔸 删除控制：__delattr__控制属性删除，保护重要数据
🔸 对象信息：__dict__, __class__, __module__, __name__提供对象元信息
```

### 6.2 关键理解要点


**🔹 方法调用时机**
```
属性访问流程：
1. 访问存在的属性 → 直接返回
2. 访问不存在的属性 → 调用 __getattr__
3. 设置任何属性 → 调用 __setattr__
4. 删除任何属性 → 调用 __delattr__
5. 启用 __getattribute__ → 拦截所有访问
```

**🔹 使用场景选择**
```
数据验证 → 使用 __setattr__
默认值处理 → 使用 __getattr__
访问监控 → 使用 __getattribute__
属性保护 → 使用 __delattr__
对象调试 → 使用 __dict__, __class__
```

**🔹 安全注意事项**
```
避免无限递归：
- __getattribute__ 中必须使用 super()
- __setattr__ 中不能直接赋值 self.xxx

性能考虑：
- __getattribute__ 每次都调用，影响性能
- 优先使用 __getattr__ 处理特殊情况
```

### 6.3 实际应用价值


**🎯 开发场景**
- **框架开发**：ORM框架中的动态属性访问
- **配置管理**：灵活的配置读取和验证
- **API包装**：简化第三方API的调用方式
- **调试工具**：对象状态监控和分析

**🔧 编程实践**
- **数据模型**：自动验证和转换数据类型
- **代理模式**：代理访问远程对象或资源
- **装饰器模式**：为对象添加额外功能
- **观察者模式**：监控属性变化并通知

**🚀 进阶应用**
- **元编程**：动态创建和修改类的行为
- **DSL设计**：创建特定领域的语言接口
- **性能优化**：缓存计算结果，延迟加载
- **安全控制**：实现细粒度的访问权限控制

**核心记忆口诀**：
- 属性访问有门卫，获取设置可拦截
- getattr兜底用，getattribute全拦截  
- setattr验证强，delattr保护好
- dict存属性，class知类型，调试开发都重要