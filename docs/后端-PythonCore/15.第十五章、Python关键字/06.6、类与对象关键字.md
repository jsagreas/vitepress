---
title: 6、类与对象关键字
---
## 📚 目录

1. [类与对象基础概念](#1-类与对象基础概念)
2. [class关键字详解](#2-class关键字详解)
3. [self关键字详解](#3-self关键字详解)
4. [super关键字详解](#4-super关键字详解)
5. [实际应用场景](#5-实际应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🏗️ 类与对象基础概念


### 1.1 什么是类和对象


**🎯 用生活例子理解**

想象一下制作月饼的过程：
```
月饼模具 = 类(class)
用模具做出的月饼 = 对象(object)

一个模具可以做很多个月饼
每个月饼都有相同的形状，但可以有不同的馅料
```

**📋 编程中的类与对象**
- **类**：就像一个模板或蓝图，定义了对象应该有什么属性和能做什么事
- **对象**：根据类这个模板创建出来的具体实例

**💡 为什么需要类？**
```
问题：如果要管理100个学生的信息怎么办？

传统方式：
student1_name = "张三"
student1_age = 18
student2_name = "李四"
student2_age = 19
...太麻烦了！

用类的方式：
创建一个Student类模板
然后批量创建student对象
代码整洁，易于管理
```

### 1.2 类与对象的关系图示

```
类(Class) - Student模板
├── 属性：姓名、年龄、学号
├── 方法：学习、考试、毕业
└── 创建对象 ↓

对象(Object) - 具体的学生
├── 张三(姓名="张三", 年龄=18)
├── 李四(姓名="李四", 年龄=19)  
└── 王五(姓名="王五", 年龄=20)
```

---

## 2. 🔧 class关键字详解


### 2.1 class是什么


**📖 基本定义**
`class`是Python中定义类的关键字，相当于说"我要创建一个模板"。

**🎯 通俗理解**
```
class就像在说：
"我要设计一种新的东西，这种东西应该具备哪些特征和功能"

比如设计汽车类：
- 特征：颜色、品牌、价格
- 功能：启动、刹车、加速
```

### 2.2 class的基本语法


**📝 标准格式**
```python
class 类名:
    """类的说明文档"""
    
    def __init__(self):
        # 初始化方法，创建对象时自动执行
        pass
    
    def 方法名(self):
        # 类的方法（功能）
        pass
```

**🌟 实际例子**
```python
class Dog:
    """狗狗类 - 定义狗狗的基本特征和行为"""
    
    def __init__(self, name, breed):
        self.name = name      # 狗的名字
        self.breed = breed    # 狗的品种
    
    def bark(self):
        return f"{self.name}在汪汪叫！"
    
    def eat(self):
        return f"{self.name}正在吃狗粮"

# 使用这个类创建具体的狗对象
my_dog = Dog("旺财", "金毛")
print(my_dog.bark())  # 输出：旺财在汪汪叫！
```

### 2.3 类的组成部分


**🏗️ 类的结构**
```
类的组成：
┌─────────────────────┐
│ 类名：给类起个名字   │
├─────────────────────┤  
│ 属性：存储数据信息   │
├─────────────────────┤
│ 方法：定义功能行为   │
└─────────────────────┘
```

**📊 属性vs方法对比**

| 组成部分 | **作用** | **例子** | **特点** |
|---------|---------|---------|---------|
| 🏷️ **属性** | `存储数据` | `狗的名字、年龄` | `名词，描述特征` |
| ⚡ **方法** | `执行功能` | `吃饭、睡觉` | `动词，描述行为` |

### 2.4 类命名规范


**✅ 正确的类命名**
```python
class Student:        # 首字母大写
class BankAccount:    # 多个单词用驼峰命名
class CarEngine:      # 清晰表达类的用途
```

**❌ 错误的类命名**
```python
class student:        # 首字母应该大写
class bank_account:   # 不推荐下划线
class A:              # 名称太简单不清楚
```

---

## 3. 🎯 self关键字详解


### 3.1 self是什么


**💭 通俗理解**
```
self就像是"我自己"的意思

当你说"我的名字"时，这个"我"指的就是你自己
当对象说"self.name"时，这个"self"指的就是对象自己
```

**📖 专业定义**
`self`是类方法的第一个参数，代表调用该方法的对象实例。

### 3.2 为什么需要self


**🤔 问题场景**
```python
# 假设有多个学生对象
student1 = Student("张三", 18)
student2 = Student("李四", 19)

# 当调用方法时，Python怎么知道是哪个学生在说话？
student1.introduce()  # 应该说"我是张三"
student2.introduce()  # 应该说"我是李四"
```

**💡 self的作用**
```python
class Student:
    def __init__(self, name, age):
        self.name = name  # self.name表示"这个学生对象的名字"
        self.age = age
    
    def introduce(self):
        # self让方法知道是哪个具体的学生在介绍自己
        return f"我是{self.name}，今年{self.age}岁"

# 创建两个不同的学生
zhang_san = Student("张三", 18)
li_si = Student("李四", 19)

# self自动指向调用方法的对象
print(zhang_san.introduce())  # self指向zhang_san对象
print(li_si.introduce())      # self指向li_si对象
```

### 3.3 self的工作原理


**🔄 self的传递过程**
```
调用过程：
zhang_san.introduce()

实际上Python内部是这样处理的：
Student.introduce(zhang_san)
                  ↑
                 这个就是self
```

**📋 详细示例**
```python
class Phone:
    def __init__(self, brand, model):
        self.brand = brand    # 这个手机的品牌
        self.model = model    # 这个手机的型号
        self.is_on = False    # 这个手机的开机状态
    
    def power_on(self):
        self.is_on = True     # 让这个手机开机
        print(f"{self.brand} {self.model} 已开机")
    
    def make_call(self, number):
        if self.is_on:        # 检查这个手机是否开机
            print(f"用{self.brand}手机拨打{number}")
        else:
            print("请先开机")

# 创建两部不同的手机
iphone = Phone("苹果", "iPhone 15")
xiaomi = Phone("小米", "小米14")

# 每个手机都有自己的状态
iphone.power_on()    # 苹果手机开机
xiaomi.power_on()    # 小米手机开机
```

### 3.4 self的使用注意事项


**⚠️ 常见错误**
```python
class Car:
    def __init__(self, brand):
        brand = brand    # ❌ 错误：没有self，只是局部变量
        self.brand = brand    # ✅ 正确：保存到对象中
    
    def start():    # ❌ 错误：忘记写self参数
        print("启动汽车")
    
    def start(self):    # ✅ 正确：第一个参数必须是self
        print(f"{self.brand}汽车启动")
```

**✨ 记忆技巧**
```
记住：
1. 类的方法第一个参数必须是self
2. 访问对象的属性要用self.属性名  
3. self就是"我自己"的意思
```

---

## 4. 🚀 super关键字详解


### 4.1 super是什么


**🏠 家族关系类比**
```
想象家族关系：
爷爷 → 爸爸 → 儿子

在编程中：
父类(Parent) → 子类(Child)

super()就像儿子说"我要用爸爸的方法"
```

**📖 专业定义**
`super()`用于在子类中调用父类的方法，特别是在继承关系中。

### 4.2 为什么需要super


**🎯 继承的场景**
```python
# 父类：动物
class Animal:
    def __init__(self, name):
        self.name = name
    
    def speak(self):
        print(f"{self.name}发出声音")

# 子类：狗（继承自动物）
class Dog(Animal):
    def __init__(self, name, breed):
        # 狗除了有名字，还有品种
        # 怎么重用父类已经写好的初始化代码？
        super().__init__(name)  # 调用父类的__init__
        self.breed = breed
    
    def speak(self):
        super().speak()  # 先调用父类的speak
        print("汪汪汪！")  # 再添加狗特有的叫声
```

### 4.3 super的使用场景


**📊 常见使用情况**

| 场景 | **用途** | **例子** |
|------|---------|---------|
| 🏗️ **初始化** | `重用父类初始化代码` | `super().__init__()` |
| 🔧 **方法扩展** | `在父类方法基础上增加功能` | `super().method() + 新功能` |
| 🔄 **方法重写** | `保留父类功能，添加新特性` | `调用父类+自定义逻辑` |

**🌟 详细示例**
```python
class Vehicle:
    """交通工具父类"""
    def __init__(self, brand, max_speed):
        self.brand = brand
        self.max_speed = max_speed
        print(f"创建了{brand}交通工具")
    
    def start(self):
        print("交通工具启动了")
    
    def stop(self):
        print("交通工具停止了")

class Car(Vehicle):
    """汽车子类"""
    def __init__(self, brand, max_speed, fuel_type):
        # 使用super调用父类初始化
        super().__init__(brand, max_speed)
        self.fuel_type = fuel_type
        print(f"这是一辆{fuel_type}汽车")
    
    def start(self):
        print("插入钥匙...")
        super().start()  # 调用父类的启动方法
        print("汽车引擎启动！")

# 创建汽车对象
my_car = Car("丰田", 180, "汽油")
my_car.start()

# 输出：
# 创建了丰田交通工具
# 这是一辆汽油汽车  
# 插入钥匙...
# 交通工具启动了
# 汽车引擎启动！
```

### 4.4 super的工作原理


**🔄 方法查找顺序**
```
子类调用super()时的查找过程：

Dog.speak() 
    ↓
调用 super().speak()
    ↓
查找父类 Animal.speak()
    ↓
执行父类方法
    ↓
返回子类继续执行
```

**💡 实际调用流程**
```python
class Animal:
    def make_sound(self):
        print("动物发出声音")

class Dog(Animal):
    def make_sound(self):
        print("小狗准备叫...")
        super().make_sound()    # 调用父类方法
        print("汪汪汪！")
        print("叫完了")

dog = Dog()
dog.make_sound()

# 执行顺序：
# 1. 小狗准备叫...
# 2. 动物发出声音      ← super()调用的父类方法
# 3. 汪汪汪！
# 4. 叫完了
```

---

## 5. 🎯 实际应用场景


### 5.1 员工管理系统


**💼 业务场景**
设计一个公司员工管理系统，不同类型的员工有不同的特点。

```python
class Employee:
    """员工基类"""
    def __init__(self, name, employee_id, salary):
        self.name = name
        self.employee_id = employee_id  
        self.salary = salary
    
    def work(self):
        return f"{self.name}正在工作"
    
    def get_info(self):
        return f"姓名：{self.name}，工号：{self.employee_id}"

class Developer(Employee):
    """程序员类"""
    def __init__(self, name, employee_id, salary, programming_language):
        super().__init__(name, employee_id, salary)
        self.programming_language = programming_language
    
    def work(self):
        base_work = super().work()
        return f"{base_work}，正在用{self.programming_language}编程"
    
    def code_review(self):
        return f"{self.name}正在做代码审查"

class Manager(Employee):
    """经理类"""
    def __init__(self, name, employee_id, salary, team_size):
        super().__init__(name, employee_id, salary)
        self.team_size = team_size
    
    def work(self):
        base_work = super().work()
        return f"{base_work}，管理{self.team_size}人的团队"
    
    def hold_meeting(self):
        return f"{self.name}正在主持会议"

# 创建不同类型的员工
dev = Developer("张三", "D001", 15000, "Python")
mgr = Manager("李四", "M001", 25000, 8)

print(dev.work())           # 张三正在工作，正在用Python编程
print(mgr.work())           # 李四正在工作，管理8人的团队
print(dev.code_review())    # 张三正在做代码审查
```

### 5.2 图形绘制系统


**🎨 图形设计场景**
```python
class Shape:
    """图形基类"""
    def __init__(self, color):
        self.color = color
    
    def get_info(self):
        return f"这是一个{self.color}的图形"

class Rectangle(Shape):
    """矩形类"""
    def __init__(self, color, width, height):
        super().__init__(color)
        self.width = width
        self.height = height
    
    def get_area(self):
        return self.width * self.height
    
    def get_info(self):
        base_info = super().get_info()
        return f"{base_info}，矩形，面积：{self.get_area()}"

class Circle(Shape):
    """圆形类"""
    def __init__(self, color, radius):
        super().__init__(color)
        self.radius = radius
    
    def get_area(self):
        return 3.14 * self.radius * self.radius
    
    def get_info(self):
        base_info = super().get_info()
        return f"{base_info}，圆形，面积：{self.get_area():.2f}"

# 创建不同的图形
rect = Rectangle("红色", 5, 3)
circle = Circle("蓝色", 4)

print(rect.get_info())    # 这是一个红色的图形，矩形，面积：15
print(circle.get_info())  # 这是一个蓝色的图形，圆形，面积：50.24
```

### 5.3 实际应用总结


**📋 设计原则**
```
使用类和继承的好处：
┌─────────────────────┐
│ 1. 代码重用         │ ← 父类代码可以被多个子类使用
├─────────────────────┤
│ 2. 易于维护         │ ← 修改父类，所有子类自动受益  
├─────────────────────┤
│ 3. 扩展性好         │ ← 添加新类型很容易
├─────────────────────┤
│ 4. 结构清晰         │ ← 层次分明，逻辑清楚
└─────────────────────┘
```

---

## 6. 📋 核心要点总结


### 6.1 三个关键字的作用


**🔑 关键字速览**

| 关键字 | **作用** | **通俗理解** | **使用场景** |
|-------|---------|-------------|-------------|
| 🏗️ **class** | `定义类模板` | `设计图纸` | `创建新的数据类型` |
| 🎯 **self** | `代表对象自己` | `我自己` | `访问对象的属性和方法` |
| 🚀 **super** | `调用父类方法` | `用爸爸的方法` | `继承中重用父类功能` |

### 6.2 记忆口诀


**📚 记忆技巧**
```
class定模板，就像做月饼的模具
self是自己，方法里面必须有
super找爸爸，继承关系要用到
```

### 6.3 使用注意事项


**⚠️ 常见错误避免**

✅ **正确做法**：
- class名首字母大写
- 方法第一个参数写self  
- 使用super()调用父类方法

❌ **错误做法**：
- 忘记写self参数
- 直接修改类变量而不是实例变量
- 不理解继承关系乱用super

### 6.4 学习建议


**📈 学习路径**
```
入门阶段：
1. 理解类和对象的概念
2. 练习定义简单的类
3. 掌握self的使用

进阶阶段：  
4. 学习继承的概念
5. 练习使用super()
6. 设计简单的继承体系

实战阶段：
7. 完成小项目练习
8. 理解面向对象设计原则
```

**💡 实践建议**
- 多动手写代码，理论结合实践
- 从生活中的例子理解抽象概念  
- 先掌握基础语法，再学设计模式
- 遇到问题多思考为什么这样设计

**✨ 核心理解**
```
面向对象编程的本质：
把现实世界的事物抽象成程序中的类和对象
让程序更接近人类的思维方式
提高代码的可读性和可维护性
```