---
title: 1、类定义与实例化
---
## 📚 目录

1. [什么是类和对象](#1-什么是类和对象)
2. [类的定义语法](#2-类的定义语法)
3. [对象的创建过程](#3-对象的创建过程)
4. [属性系统详解](#4-属性系统详解)
5. [对象身份与比较](#5-对象身份与比较)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🏗️ 什么是类和对象


### 1.1 生活中的类与对象概念


**🔸 通俗理解**
```
生活类比：
汽车设计图纸（类）     →     具体的汽车（对象）
房屋建筑图纸（类）     →     真实的房子（对象）
手机产品规格（类）     →     你手里的手机（对象）

关键理解：
• 类 = 模板/蓝图，定义了"规格"和"功能"
• 对象 = 根据模板制造出来的"实体"
• 一个类可以创建无数个对象
```

**💡 编程中的含义**
```
类（Class）：
定义：一种自定义的数据类型，描述了对象应该有什么属性和方法
作用：代码复用的模板，封装相关的数据和功能

对象（Object）：
定义：类的具体实例，拥有真实的数据和可执行的方法
作用：程序运行时实际操作的实体
```

### 1.2 Python中类与对象的关系


```
现实世界映射到编程世界：

现实概念              Python概念
────────────        ────────────
学生这个群体    →    Student类
张三这个学生    →    zhang_san对象
李四这个学生    →    li_si对象

每个学生都有：       每个Student对象都有：
• 姓名、年龄        • name、age属性
• 学习、考试        • study()、exam()方法
```

---

## 2. 📝 类的定义语法


### 2.1 class关键字基础语法


**🔸 最简单的类定义**
```python
# 最基础的类定义
class Student:
    pass  # pass表示暂时不写具体内容

# 创建对象（实例化）
xiaoming = Student()
print(type(xiaoming))  # <class '__main__.Student'>
```

**🔸 带属性和方法的类**
```python
class Student:
    # 类属性：所有学生共同拥有的属性
    school = "北京大学"
    
    # 初始化方法：创建对象时自动调用
    def __init__(self, name, age):
        self.name = name    # 实例属性
        self.age = age      # 实例属性
    
    # 实例方法：对象可以调用的功能
    def introduce(self):
        return f"我是{self.name}，今年{self.age}岁"
    
    def study(self, subject):
        return f"{self.name}正在学习{subject}"
```

### 2.2 类命名约定


**📋 Python类命名规范**
```python
# ✅ 正确的类命名（首字母大写，驼峰命名）
class Student:         # 学生类
class BankAccount:     # 银行账户类  
class DatabaseManager: # 数据库管理器类

# ❌ 不推荐的命名
class student:         # 小写开头
class bank_account:    # 下划线分隔（更适合函数/变量）
class database-mgr:    # 包含连字符（语法错误）
```

**🎯 命名建议**
- **首字母大写**：类名每个单词首字母都大写
- **驼峰命名**：`StudentInfo`, `GamePlayer`, `FileManager`
- **含义明确**：一看就知道这个类是做什么的
- **避免缩写**：除非是广为人知的缩写（如`URL`, `HTML`）

### 2.3 self参数的含义


**🔍 self到底是什么**
```python
class Student:
    def __init__(self, name):
        self.name = name
    
    def say_hello(self):
        print(f"你好，我是{self.name}")

# 创建两个不同的学生对象
student1 = Student("张三")
student2 = Student("李四")

# self会自动指向调用方法的那个对象
student1.say_hello()  # self指向student1，输出：你好，我是张三
student2.say_hello()  # self指向student2，输出：你好，我是李四
```

**💭 深入理解self**
```
为什么需要self？

情况分析：
• Python需要知道你在操作"哪个具体的对象"
• 同一个类可能创建了很多对象
• 方法被调用时，必须明确是"谁"在调用

工作机制：
student1.say_hello()  实际上等价于  Student.say_hello(student1)
                     ↑                                    ↑
                  调用者                            self参数
```

---

## 3. ⚙️ 对象的创建过程


### 3.1 实例化的完整过程


**🔄 对象创建的内部流程**
```
对象创建步骤（自动进行）：

第1步：调用 __new__ 方法
• 在内存中分配空间
• 创建一个"空白"的对象
• 返回对象的内存地址

第2步：调用 __init__ 方法  
• 接收第1步创建的对象
• 给对象设置初始属性值
• 进行必要的初始化工作

第3步：返回初始化完成的对象
• 将对象赋值给变量
• 对象正式可用
```

**💻 代码演示创建过程**
```python
class Student:
    def __new__(cls, name, age):
        print(f"第1步：__new__被调用，准备创建{cls}的实例")
        # 调用父类的__new__方法创建对象
        instance = super().__new__(cls)
        print(f"第2步：对象创建完成，内存地址：{id(instance)}")
        return instance
    
    def __init__(self, name, age):
        print(f"第3步：__init__被调用，开始初始化")
        self.name = name
        self.age = age
        print(f"第4步：初始化完成，学生姓名：{self.name}")

# 创建对象，观察整个过程
print("开始创建学生对象...")
student = Student("小明", 18)
print("对象创建完成！")
```

### 3.2 __new__ 与 __init__ 的区别


**📊 两个方法的对比**

| 方法 | **调用时机** | **主要作用** | **返回值** | **使用频率** |
|------|------------|-------------|-----------|-------------|
| `__new__` | `对象创建前` | `分配内存空间` | `对象实例` | `很少自定义` |
| `__init__` | `对象创建后` | `初始化属性` | `None` | `几乎必用` |

**🎯 实际使用建议**
```python
# 大多数情况下，你只需要关心 __init__
class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age

# __new__ 主要用于特殊场景（如单例模式）
class Singleton:
    _instance = None
    
    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance  # 始终返回同一个对象
```

### 3.3 实例化的多种方式


**🔧 创建对象的不同写法**
```python
class Student:
    def __init__(self, name="未知", age=0):
        self.name = name
        self.age = age

# 方式1：提供完整参数
student1 = Student("张三", 20)

# 方式2：使用默认参数
student2 = Student("李四")      # age使用默认值0
student3 = Student()           # 全部使用默认值

# 方式3：关键字参数（推荐）
student4 = Student(age=19, name="王五")  # 参数顺序可以改变

# 方式4：混合使用
student5 = Student("赵六", age=21)
```

---

## 4. 📦 属性系统详解


### 4.1 实例属性 vs 类属性


**🔸 属性类型对比**
```python
class Student:
    # 类属性：属于整个类，所有对象共享
    school = "北京大学"
    student_count = 0  # 记录学生总数
    
    def __init__(self, name, age):
        # 实例属性：属于具体对象，每个对象独有
        self.name = name
        self.age = age
        # 修改类属性
        Student.student_count += 1

# 创建学生对象
student1 = Student("张三", 20)
student2 = Student("李四", 19)

print(f"学校：{Student.school}")           # 北京大学
print(f"学生总数：{Student.student_count}")  # 2
print(f"学生1姓名：{student1.name}")        # 张三
print(f"学生2姓名：{student2.name}")        # 李四
```

**💡 属性访问优先级**
```python
class Student:
    name = "默认姓名"  # 类属性
    
    def __init__(self, name):
        self.name = name  # 实例属性（同名会覆盖类属性）

student = Student("小明")
print(student.name)      # 小明（实例属性优先）
print(Student.name)      # 默认姓名（类属性不变）

# 删除实例属性后，类属性重新可见
del student.name
print(student.name)      # 默认姓名（现在访问的是类属性）
```

### 4.2 属性访问机制


**🔍 属性查找顺序**
```
Python属性查找规则：

第1步：查找实例属性
• 在对象的 __dict__ 中查找
• 如果找到，直接返回

第2步：查找类属性  
• 在类的 __dict__ 中查找
• 如果找到，返回类属性

第3步：查找父类属性
• 按照方法解析顺序（MRO）查找
• 在父类及其父类中查找

第4步：抛出异常
• 如果都没找到，抛出 AttributeError
```

**💻 属性查找演示**
```python
class Student:
    school = "北京大学"  # 类属性
    
    def __init__(self, name):
        self.name = name  # 实例属性

student = Student("小红")

# 查看对象的属性字典
print("实例属性：", student.__dict__)    # {'name': '小红'}
print("类属性：", Student.__dict__.keys())  # 包含 'school'

# 属性访问
print(student.name)     # 实例属性：小红
print(student.school)   # 类属性：北京大学
# print(student.grade)  # AttributeError: 'Student' object has no attribute 'grade'
```

### 4.3 动态添加和删除属性


**🔧 运行时操作属性**
```python
class Student:
    def __init__(self, name):
        self.name = name

student = Student("小刚")

# 动态添加实例属性
student.age = 18
student.hobby = "篮球"
print(f"{student.name}今年{student.age}岁，爱好{student.hobby}")

# 动态添加类属性
Student.school = "清华大学"
print(f"学校：{Student.school}")

# 删除属性
del student.age
# print(student.age)  # AttributeError

# 检查属性是否存在
print(hasattr(student, 'name'))    # True
print(hasattr(student, 'age'))     # False

# 安全获取属性（提供默认值）
age = getattr(student, 'age', 0)   # 如果没有age属性，返回0
print(f"年龄：{age}")
```

---

## 5. 🆔 对象身份与比较


### 5.1 对象身份（Identity）


**🔸 对象的唯一标识**
```python
class Student:
    def __init__(self, name):
        self.name = name

# 创建两个学生对象
student1 = Student("张三")
student2 = Student("张三")  # 名字相同但是不同的对象
student3 = student1        # 指向同一个对象

# 对象身份检查
print(f"student1的ID：{id(student1)}")
print(f"student2的ID：{id(student2)}")  
print(f"student3的ID：{id(student3)}")

# is 运算符检查对象身份
print(student1 is student2)  # False，不是同一个对象
print(student1 is student3)  # True，是同一个对象
```

**📋 身份 vs 相等性**
```
概念区分：

身份（Identity）：
• 检查是否为同一个对象
• 使用 is 运算符
• 比较内存地址

相等性（Equality）：
• 检查内容是否相同
• 使用 == 运算符  
• 比较对象的值
```

### 5.2 对象相等性比较


**⚖️ 自定义相等性判断**
```python
class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    # 定义相等性比较规则
    def __eq__(self, other):
        if not isinstance(other, Student):
            return False
        return self.name == other.name and self.age == other.age
    
    # 定义字符串表示（便于调试）
    def __repr__(self):
        return f"Student('{self.name}', {self.age})"

# 测试相等性
student1 = Student("张三", 20)
student2 = Student("张三", 20)  # 属性完全相同
student3 = Student("李四", 20)  # 属性不同

print(student1 == student2)  # True，内容相同
print(student1 is student2)  # False，不是同一对象
print(student1 == student3)  # False，内容不同
```

### 5.3 对象的字符串表示


**📝 定制对象的显示方式**
```python
class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    # 用户友好的字符串表示
    def __str__(self):
        return f"学生：{self.name}（{self.age}岁）"
    
    # 开发者友好的字符串表示
    def __repr__(self):
        return f"Student(name='{self.name}', age={self.age})"

student = Student("王五", 19)

# 不同的字符串表示方式
print(str(student))   # 学生：王五（19岁）
print(repr(student))  # Student(name='王五', age=19)
print(student)        # 默认调用 __str__，输出：学生：王五（19岁）
```

**🎯 __str__ vs __repr__ 的区别**

| 方法 | **目标用户** | **使用场景** | **设计原则** |
|------|------------|-------------|-------------|
| `__str__` | `终端用户` | `print()、str()` | `可读性优先` |
| `__repr__` | `开发者` | `调试、repr()` | `准确性优先` |

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 类与对象关系：类是模板，对象是实例
🔸 class语法：class 类名: 定义类的基本结构  
🔸 实例化过程：__new__ 创建对象，__init__ 初始化对象
🔸 self参数：指向调用方法的具体对象实例
🔸 属性系统：实例属性（对象独有）vs 类属性（对象共享）
🔸 对象身份：is 检查身份，== 检查相等性
```

### 6.2 关键理解要点


**🔹 面向对象的核心思想**
```
封装思维：
• 把相关的数据和操作放在一起
• 通过类来组织代码，提高复用性
• 隐藏内部实现细节，提供清晰接口

抽象思维：
• 从具体事物中提取共同特征
• 忽略不重要的细节，突出关键属性
• 建立通用的模型来处理同类问题
```

**🔹 属性访问的查找机制**
```
记忆要点：
实例属性 → 类属性 → 父类属性 → 异常

实际意义：
• 实例属性优先级最高（对象个性）
• 类属性作为默认值（对象共性）  
• 灵活的属性查找让代码更加简洁
```

**🔹 对象创建的完整理解**
```
深层理解：
• 每次实例化都会在内存中创建新对象
• __init__ 不是构造函数，只是初始化函数
• 真正的构造函数是 __new__（通常不需要自定义）
```

### 6.3 实际应用价值


**💼 编程实践指导**
- **类设计原则**：一个类只做一件事，职责单一
- **命名规范**：类名首字母大写，方法名小写下划线
- **属性管理**：优先使用实例属性，类属性用于共享数据
- **方法设计**：每个方法都要有明确的输入输出和职责

**🚀 进阶学习方向**
- **继承机制**：类之间的父子关系和方法重写
- **多态特性**：同一接口的不同实现方式
- **特殊方法**：魔法方法让对象更加智能
- **设计模式**：面向对象的经典解决方案

### 6.4 常见问题避坑


**⚠️ 新手容易犯的错误**
```python
# ❌ 忘记self参数
class Student:
    def say_hello():  # 缺少self
        print("Hello")

# ❌ 类属性使用可变对象
class Student:
    hobbies = []  # 所有实例共享同一个列表！
    
# ✅ 正确的做法
class Student:
    def __init__(self):
        self.hobbies = []  # 每个实例有自己的列表
```

**💡 最佳实践建议**
- **初始化完整**：`__init__`中设置所有必要属性
- **方法简洁**：每个方法只做一件事情
- **异常处理**：对可能出错的操作进行保护
- **文档字符串**：为类和方法添加说明文档

**🧠 核心记忆**：
- 类是蓝图对象是房子，self是房子的门牌号
- 先有类定义后有对象实例，实例属性覆盖类属性
- 对象身份用is，对象相等用==，想要相等需定义规则
- 面向对象三特性：封装数据隐细节，继承复用抽象化，多态接口变实现