---
title: 5、JSON数据处理
---
## 📚 目录

1. [JSON基础概念](#1-JSON基础概念)
2. [json模块详解](#2-json模块详解)
3. [JSON与Python类型对应](#3-JSON与Python类型对应)
4. [序列化操作](#4-序列化操作)
5. [反序列化操作](#5-反序列化操作)
6. [自定义编码器](#6-自定义编码器)
7. [JSON格式化输出](#7-JSON格式化输出)
8. [JSON性能优化](#8-JSON性能优化)
9. [JSON数据验证](#9-JSON数据验证)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🌐 JSON基础概念


### 1.1 什么是JSON


**JSON的全称**：JavaScript Object Notation（JavaScript对象表示法）

```
简单理解：
JSON就像是数据的"通用语言"，让不同的程序能够互相理解对方的数据
就像人类用英语做国际交流一样，程序用JSON做数据交流
```

**🔸 JSON的核心特点**
- **轻量级**：比XML更简洁，传输更快
- **易读性**：人类可以直接阅读和编写
- **跨平台**：几乎所有编程语言都支持
- **标准化**：有严格的格式规范

### 1.2 JSON长什么样


**典型的JSON示例**：
```json
{
  "name": "张三",
  "age": 25,
  "is_student": false,
  "hobbies": ["读书", "游泳", "编程"],
  "address": {
    "city": "北京",
    "district": "朝阳区"
  },
  "phone": null
}
```

**🔍 结构解析**：
- `{}` 表示对象（类似Python字典）
- `[]` 表示数组（类似Python列表）
- `"key": value` 表示键值对
- 支持字符串、数字、布尔值、null

### 1.3 为什么要学JSON


> 💡 **实际应用场景**：
> - 网页与服务器数据交换
> - 配置文件存储
> - API接口数据传输
> - 数据库数据导入导出

---

## 2. 📦 json模块详解


### 2.1 导入json模块


Python内置了处理JSON的模块，直接导入即可：

```python
import json
```

> 📝 **注意**：json是Python标准库，无需额外安装

### 2.2 json模块的四个核心函数


```
json模块就像一个"翻译官"，提供四个主要功能：

📤 导出功能（序列化）：
├── dumps()  → 把Python数据转为JSON字符串
└── dump()   → 把Python数据转为JSON并写入文件

📥 导入功能（反序列化）：
├── loads() → 把JSON字符串转为Python数据
└── load()  → 从文件读取JSON并转为Python数据
```

**🎯 记忆技巧**：
- `dumps` = dump string（转为字符串）
- `dump` = dump to file（转到文件）
- `loads` = load from string（从字符串加载）
- `load` = load from file（从文件加载）

---

## 3. 🔄 JSON与Python类型对应


### 3.1 数据类型转换表


| **JSON类型** | **Python类型** | **示例** | **说明** |
|-------------|---------------|----------|----------|
| `object` | `dict` | `{"name": "张三"}` | 对象变字典 |
| `array` | `list` | `[1, 2, 3]` | 数组变列表 |
| `string` | `str` | `"hello"` | 字符串保持不变 |
| `number` | `int/float` | `42, 3.14` | 整数或浮点数 |
| `true/false` | `bool` | `True/False` | 布尔值 |
| `null` | `None` | `None` | 空值 |

### 3.2 转换示例演示


```python
# JSON → Python 转换示例
json_data = '''
{
    "name": "小明",
    "scores": [85, 90, 78],
    "graduated": true,
    "remarks": null
}
'''

python_data = json.loads(json_data)
print(type(python_data))  # <class 'dict'>
print(python_data["scores"])  # [85, 90, 78] (list类型)
```

**🔸 转换注意事项**：
- JSON的key必须是字符串，Python字典的key可以是多种类型
- JSON不支持注释，Python字典可以有注释
- JSON数字没有整数/浮点数区分，Python会自动判断

---

## 4. 📤 序列化操作


### 4.1 dumps()方法 - 转为字符串


**基本用法**：将Python数据转换为JSON字符串

```python
# 基础数据序列化
student = {
    "name": "李华",
    "age": 20,
    "subjects": ["数学", "英语", "Python"]
}

json_string = json.dumps(student)
print(json_string)
# 输出：{"name": "李华", "age": 20, "subjects": ["数学", "英语", "Python"]}
```

**常用参数配置**：

```python
# 中文显示 + 格式化
json_string = json.dumps(student, 
                        ensure_ascii=False,  # 显示中文
                        indent=2)            # 缩进格式化
print(json_string)
```

### 4.2 dump()方法 - 写入文件


**直接保存到文件**：

```python
student_data = {"name": "王五", "grade": 95}

# 写入JSON文件
with open("student.json", "w", encoding="utf-8") as f:
    json.dump(student_data, f, ensure_ascii=False, indent=2)
```

> ⚠️ **编码提醒**：处理中文时记得设置 `encoding="utf-8"`

### 4.3 序列化参数详解


| **参数** | **作用** | **示例值** | **使用场景** |
|---------|---------|-----------|-------------|
| `ensure_ascii` | 是否转义中文 | `False` | 显示中文字符 |
| `indent` | 缩进格式化 | `2, 4` | 美化输出 |
| `sort_keys` | 按key排序 | `True` | 统一key顺序 |
| `separators` | 自定义分隔符 | `(',', ':')` | 压缩输出 |

---

## 5. 📥 反序列化操作


### 5.1 loads()方法 - 从字符串解析


**基本解析**：

```python
# JSON字符串
json_text = '{"name": "赵六", "age": 30, "city": "上海"}'

# 转换为Python字典
data = json.loads(json_text)
print(data["name"])  # 输出：赵六
print(type(data))    # <class 'dict'>
```

**错误处理**：

```python
# 处理格式错误的JSON
try:
    bad_json = '{"name": "张三", "age":}'  # 缺少age的值
    data = json.loads(bad_json)
except json.JSONDecodeError as e:
    print(f"JSON格式错误：{e}")
```

### 5.2 load()方法 - 从文件读取


**读取JSON文件**：

```python
# 从文件加载JSON数据
try:
    with open("student.json", "r", encoding="utf-8") as f:
        student_data = json.load(f)
        print(f"学生姓名：{student_data['name']}")
except FileNotFoundError:
    print("文件不存在")
except json.JSONDecodeError:
    print("JSON格式错误")
```

### 5.3 常见解析错误


```
常见JSON格式错误：

❌ 单引号：{'name': 'Tom'}        → 应该用双引号
❌ 末尾逗号：{"age": 20,}         → 不能有末尾逗号  
❌ 注释：{"name": "Tom" // 注释}  → JSON不支持注释
❌ 未闭合：{"name": "Tom"        → 缺少右括号
```

---

## 6. 🎨 自定义编码器


### 6.1 为什么需要自定义编码器


Python的某些数据类型JSON不直接支持：

```python
import datetime

data = {
    "name": "测试",
    "time": datetime.datetime.now(),  # datetime对象
    "price": decimal.Decimal("99.99") # Decimal对象
}

# 直接序列化会报错
# json.dumps(data)  # TypeError: Object of type datetime is not JSON serializable
```

### 6.2 创建自定义编码器


**方法一：使用default参数**

```python
import datetime
import decimal

def custom_encoder(obj):
    """自定义JSON编码器"""
    if isinstance(obj, datetime.datetime):
        return obj.strftime("%Y-%m-%d %H:%M:%S")
    elif isinstance(obj, decimal.Decimal):
        return float(obj)
    
    # 如果不是特殊类型，抛出异常
    raise TypeError(f"Object of type {type(obj)} is not JSON serializable")

# 使用自定义编码器
data = {
    "name": "订单信息", 
    "time": datetime.datetime.now(),
    "price": decimal.Decimal("199.99")
}

json_str = json.dumps(data, default=custom_encoder, ensure_ascii=False)
print(json_str)
```

**方法二：继承JSONEncoder类**

```python
class CustomJSONEncoder(json.JSONEncoder):
    def default(self, obj):
        if isinstance(obj, datetime.datetime):
            return obj.isoformat()  # ISO格式时间
        elif isinstance(obj, set):
            return list(obj)        # 集合转列表
        
        return super().default(obj)

# 使用自定义编码器类
data = {"tags": {"Python", "JSON", "编程"}}
json_str = json.dumps(data, cls=CustomJSONEncoder, ensure_ascii=False)
print(json_str)  # {"tags": ["Python", "JSON", "编程"]}
```

---

## 7. ✨ JSON格式化输出


### 7.1 美化JSON输出


**基础格式化**：

```python
data = {"name": "Python课程", "lessons": [{"title": "基础语法", "duration": 120}, {"title": "面向对象", "duration": 180}]}

# 压缩格式（默认）
compact = json.dumps(data, ensure_ascii=False)
print("压缩格式：", compact)

# 美化格式
pretty = json.dumps(data, ensure_ascii=False, indent=4)
print("美化格式：\n", pretty)
```

### 7.2 自定义格式化参数


```python
# 完全自定义的格式化
formatted_json = json.dumps(
    data,
    ensure_ascii=False,      # 显示中文
    indent=2,                # 2空格缩进
    sort_keys=True,          # 按键排序
    separators=(',', ': ')   # 自定义分隔符
)
```

### 7.3 在线JSON工具


> 🔧 **实用工具推荐**：
> - 在线格式化：JSONLint、JSON Formatter
> - Python工具：`python -m json.tool file.json`
> - IDE插件：VSCode JSON格式化快捷键

---

## 8. ⚡ JSON性能优化


### 8.1 性能对比测试


```python
import time
import json

# 准备测试数据
large_data = {"users": [{"id": i, "name": f"user_{i}"} for i in range(10000)]}

# 测试序列化性能
start_time = time.time()
json_string = json.dumps(large_data)
end_time = time.time()
print(f"序列化耗时：{end_time - start_time:.4f}秒")

# 测试反序列化性能
start_time = time.time()
parsed_data = json.loads(json_string)
end_time = time.time()
print(f"反序列化耗时：{end_time - start_time:.4f}秒")
```

### 8.2 优化技巧


**🔸 压缩输出**：
```python
# 去除不必要的空格
compact_json = json.dumps(data, separators=(',', ':'))
```

**🔸 流式处理大文件**：
```python
import ijson  # 需要额外安装：pip install ijson

# 处理大型JSON文件，逐个解析对象
def parse_large_json(filename):
    with open(filename, 'rb') as f:
        parser = ijson.parse(f)
        for prefix, event, value in parser:
            if prefix.endswith('.name'):
                print(f"找到名字：{value}")
```

### 8.3 性能优化建议


```
性能优化策略：

📊 数据结构优化：
• 减少嵌套层级
• 避免重复数据
• 使用简洁的key名

💾 传输优化：
• 压缩JSON字符串
• 分页处理大数据
• 缓存常用JSON

🔧 代码优化：
• 重用json编码器
• 避免频繁序列化
• 考虑使用更快的库（如orjson）
```

---

## 9. ✅ JSON数据验证


### 9.1 基础验证


**检查JSON格式**：

```python
def is_valid_json(json_string):
    """检查字符串是否为有效JSON"""
    try:
        json.loads(json_string)
        return True
    except json.JSONDecodeError:
        return False

# 测试
test_data = '{"name": "测试", "age": 25}'
if is_valid_json(test_data):
    print("JSON格式正确")
else:
    print("JSON格式错误")
```

### 9.2 数据内容验证


**验证必需字段**：

```python
def validate_user_data(json_data):
    """验证用户数据完整性"""
    required_fields = ["name", "age", "email"]
    
    try:
        data = json.loads(json_data) if isinstance(json_data, str) else json_data
        
        # 检查必需字段
        missing_fields = [field for field in required_fields if field not in data]
        
        if missing_fields:
            return False, f"缺少字段：{missing_fields}"
        
        # 检查数据类型
        if not isinstance(data["age"], int) or data["age"] < 0:
            return False, "年龄必须是正整数"
        
        if "@" not in data["email"]:
            return False, "邮箱格式错误"
        
        return True, "验证通过"
    
    except Exception as e:
        return False, f"验证错误：{e}"

# 使用示例
user_json = '{"name": "张三", "age": 25, "email": "zhangsan@example.com"}'
is_valid, message = validate_user_data(user_json)
print(f"验证结果：{message}")
```

### 9.3 使用第三方验证库


**jsonschema库验证**：

```python
# 安装：pip install jsonschema
from jsonschema import validate, ValidationError

# 定义JSON模式
user_schema = {
    "type": "object",
    "properties": {
        "name": {"type": "string", "minLength": 1},
        "age": {"type": "integer", "minimum": 0},
        "email": {"type": "string", "format": "email"}
    },
    "required": ["name", "age", "email"]
}

# 验证数据
user_data = {"name": "李四", "age": 30, "email": "lisi@test.com"}

try:
    validate(instance=user_data, schema=user_schema)
    print("数据验证通过")
except ValidationError as e:
    print(f"验证失败：{e.message}")
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


```
🔸 JSON本质：轻量级的数据交换格式，程序间通信的"通用语言"
🔸 四大操作：dumps(转字符串)、dump(写文件)、loads(读字符串)、load(读文件)  
🔸 类型映射：JSON对象→dict、数组→list、字符串→str、数字→int/float
🔸 中文处理：ensure_ascii=False 显示中文字符
🔸 错误处理：使用try-except捕获JSONDecodeError异常
```

### 10.2 关键理解要点


**🔹 序列化vs反序列化**
```
简单理解：
序列化 = 打包：把Python数据"打包"成JSON字符串，方便传输
反序列化 = 解包：把JSON字符串"解包"成Python数据，方便使用

就像寄快递：
发送前要打包（序列化） → 传输 → 收到后要拆包（反序列化）
```

**🔹 文件操作vs字符串操作**
```
记忆方法：
• 带s的函数处理字符串：dumps、loads
• 不带s的函数处理文件：dump、load

实际应用：
• API接口：通常用dumps/loads处理字符串
• 配置文件：通常用dump/load处理文件
```

**🔹 常见错误避免**
```
JSON格式要求：
✅ 必须用双引号："name"
✅ 不能有注释
✅ 不能有末尾逗号
✅ 必须正确嵌套和闭合

Python转JSON注意：
✅ datetime需要自定义编码器
✅ 中文记得设置ensure_ascii=False
✅ 文件操作记得设置encoding="utf-8"
```

### 10.3 实际应用价值


**🎯 真实应用场景**：
- **Web开发**：前后端数据交换，API接口设计
- **配置管理**：应用程序配置文件存储
- **数据持久化**：简单数据的存储和读取
- **日志系统**：结构化日志数据记录
- **数据分析**：数据导入导出和格式转换

**🔧 最佳实践**：
```python
# 推荐的JSON处理模板
def safe_json_operation(data, filename=None):
    """安全的JSON操作模板"""
    try:
        if filename:
            # 文件操作
            with open(filename, 'w', encoding='utf-8') as f:
                json.dump(data, f, ensure_ascii=False, indent=2)
        else:
            # 字符串操作
            return json.dumps(data, ensure_ascii=False, indent=2)
    except (TypeError, ValueError) as e:
        print(f"JSON操作失败：{e}")
        return None
```

**核心记忆口诀**：
```
JSON轻量又通用，四个方法要记清
dumps转串loads解析，dump写文件load来读
中文显示False设置，错误捕获要谨慎
自定义编码解难题，验证数据保安全
```

### 10.4 进阶学习建议


**🚀 深入方向**：
1. **性能优化**：学习orjson、ujson等高性能JSON库
2. **数据验证**：掌握jsonschema进行复杂数据验证
3. **流式处理**：学习ijson处理超大JSON文件
4. **Web应用**：结合Flask/Django学习API开发

**💡 实践项目**：
- 制作个人信息管理系统（用JSON存储数据）
- 开发简单的配置文件管理工具
- 创建数据转换小工具（CSV↔JSON）
- 实现简单的API接口