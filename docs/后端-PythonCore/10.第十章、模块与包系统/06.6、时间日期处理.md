---
title: 6、时间日期处理
---
## 📚 目录

1. [时间日期处理概述](#1-时间日期处理概述)
2. [datetime模块核心详解](#2-datetime模块核心详解)
3. [时间差计算与运算](#3-时间差计算与运算)
4. [时区处理与转换技巧](#4-时区处理与转换技巧)
5. [time模块实用操作](#5-time模块实用操作)
6. [calendar日历模块](#6-calendar日历模块)
7. [时间格式化详解](#7-时间格式化详解)
8. [时间戳转换技巧](#8-时间戳转换技巧)
9. [实际应用场景](#9-实际应用场景)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🕐 时间日期处理概述


### 1.1 为什么需要时间日期处理


**生活中的时间应用**：
```
日常场景举例：
• 订票系统：显示发车时间、计算行程时长
• 考勤系统：记录上下班时间、计算工作时长
• 日志系统：记录操作时间、分析时间段
• 定时任务：每天凌晨执行数据备份
```

💡 **核心概念理解**
> 在编程中，时间就像生活中的时钟和日历，我们需要：
> - **获取时间**：现在几点了？
> - **计算时间**：从A时间到B时间过了多久？
> - **格式化时间**：把时间显示成用户喜欢的格式
> - **转换时间**：不同时区的时间转换

### 1.2 Python时间处理模块全景


```
Python时间处理模块体系：
┌─────────────────┐
│   datetime      │ ← 主力模块，面向对象设计
├─────────────────┤
│   time          │ ← 基础模块，系统时间操作
├─────────────────┤
│   calendar      │ ← 日历功能，日期计算
└─────────────────┘
```

📊 **模块对比选择**
| 模块 | **主要用途** | **适用场景** | **难易程度** |
|------|-------------|-------------|-------------|
| **datetime** | `日期时间对象操作` | `业务开发，数据处理` | ⭐⭐⭐ 中等 |
| **time** | `系统时间操作` | `性能测试，系统编程` | ⭐⭐ 简单 |
| **calendar** | `日历计算` | `日历展示，日期计算` | ⭐ 很简单 |

---

## 2. 📅 datetime模块核心详解


### 2.1 datetime模块基本概念


**什么是datetime？**
> datetime就像一个智能的日历加时钟，它可以精确到微秒，还能处理时区。就像你手机上显示的"2025年9月21日 15:30:25"这样的完整时间信息。

```python
# 导入datetime模块
from datetime import datetime, date, time, timedelta

# 📝 学习检查点
# [ ] 理解date、time、datetime的区别
# [x] 学会基本的时间获取方法
```

### 2.2 date类详解


**date类：只管日期，不管时间**

🏠 **生活类比**
> date就像日历，只告诉你今天是几号，不告诉你现在几点

```python
from datetime import date

# 1. 获取今天的日期
today = date.today()
print(f"今天是: {today}")  # 2025-09-21

# 2. 创建指定日期
birthday = date(1990, 5, 15)
print(f"生日: {birthday}")  # 1990-05-15

# 3. 访问日期的各个部分
print(f"年份: {today.year}")    # 2025
print(f"月份: {today.month}")   # 9
print(f"日期: {today.day}")     # 21
```

🔧 **实用方法**
```python
# 获取星期几（0=周一，6=周日）
weekday = today.weekday()
print(f"今天是周{weekday + 1}")

# 格式化显示
print(today.strftime("%Y年%m月%d日"))  # 2025年09月21日
```

### 2.3 time类详解


**time类：只管时间，不管日期**

🏠 **生活类比**
> time就像钟表，只告诉你现在几点几分，不告诉你今天是几号

```python
from datetime import time

# 1. 创建时间对象
morning = time(8, 30, 0)        # 8:30:00
afternoon = time(14, 15, 30)    # 14:15:30

print(f"上午时间: {morning}")     # 08:30:00
print(f"下午时间: {afternoon}")   # 14:15:30

# 2. 访问时间的各个部分
print(f"小时: {morning.hour}")      # 8
print(f"分钟: {morning.minute}")    # 30
print(f"秒数: {morning.second}")    # 0
```

### 2.4 datetime类详解（重点）


**datetime类：日期+时间的完整组合**

🏠 **生活类比**
> datetime就像你手机屏幕上显示的完整时间，既有日期又有时间

```python
from datetime import datetime

# 1. 获取当前完整时间
now = datetime.now()
print(f"现在时间: {now}")  # 2025-09-21 15:30:25.123456

# 2. 创建指定的日期时间
meeting = datetime(2025, 12, 25, 14, 30, 0)
print(f"会议时间: {meeting}")  # 2025-12-25 14:30:00

# 3. 访问各个组成部分
print(f"年: {now.year}")
print(f"月: {now.month}")  
print(f"日: {now.day}")
print(f"时: {now.hour}")
print(f"分: {now.minute}")
print(f"秒: {now.second}")
```

💡 **常用实用方法**
```python
# 只获取日期部分
today_date = now.date()
print(f"今天日期: {today_date}")

# 只获取时间部分  
current_time = now.time()
print(f"当前时间: {current_time}")

# 替换某个部分
new_year = now.replace(year=2026)
print(f"明年同一时间: {new_year}")
```

---

## 3. ⏱️ 时间差计算与运算


### 3.1 timedelta概念理解


**什么是timedelta？**
> timedelta就像一个"时间段"，比如"3天2小时30分钟"这样的时间长度。就像你说"电影播放了2小时30分钟"中的这个时间长度。

🏠 **生活类比**
```
生活中的时间差：
• 从家到公司需要30分钟
• 这个项目要做3个月
• 飞机延误了2小时
• 还有5天就放假了
```

### 3.2 创建时间差对象


```python
from datetime import timedelta

# 1. 创建各种时间差
one_day = timedelta(days=1)
one_week = timedelta(weeks=1)  
two_hours = timedelta(hours=2)
thirty_minutes = timedelta(minutes=30)

# 2. 组合创建复杂时间差
work_time = timedelta(days=5, hours=8)  # 5天8小时
print(f"工作时长: {work_time}")

# 3. 创建精确的时间差
precise_time = timedelta(days=1, hours=2, minutes=30, seconds=45)
print(f"精确时间: {precise_time}")
```

### 3.3 时间运算实战


**日期时间的加减运算**

```python
from datetime import datetime, timedelta

# 当前时间
now = datetime.now()
print(f"现在: {now}")

# 1. 时间加法：计算未来时间
tomorrow = now + timedelta(days=1)
next_week = now + timedelta(weeks=1)
in_two_hours = now + timedelta(hours=2)

print(f"明天: {tomorrow}")
print(f"下周: {next_week}")
print(f"2小时后: {in_two_hours}")

# 2. 时间减法：计算过去时间
yesterday = now - timedelta(days=1)
last_month = now - timedelta(days=30)

print(f"昨天: {yesterday}")
print(f"上个月: {last_month}")
```

💪 **实践挑战**
```python
# 计算年龄（天数）
from datetime import date

birth_date = date(1990, 5, 15)
today = date.today()
age_days = today - birth_date

print(f"你已经活了 {age_days.days} 天")
print(f"大约 {age_days.days // 365} 岁")
```

### 3.4 时间差的实用计算


🔍 **深入思考**
> 为什么程序需要计算时间差？
> - 计算任务耗时
> - 判断是否超期
> - 计算年龄或工龄
> - 定时任务调度

```python
# 实用案例：计算代码执行时间
start_time = datetime.now()

# 模拟一些工作
import time
time.sleep(2)  # 等待2秒

end_time = datetime.now()
execution_time = end_time - start_time

print(f"代码执行耗时: {execution_time}")
print(f"总秒数: {execution_time.total_seconds()}")
```

---

## 4. 🌍 时区处理与转换技巧


### 4.1 时区概念理解


**什么是时区？**
> 时区就像地球上的"时间邮编"，不同地方有不同的本地时间。比如北京时间比伦敦时间快8小时。

```
全球时区示意：
伦敦时间: 09:00  |  北京时间: 17:00  |  纽约时间: 04:00
    ↓                    ↓                    ↓
   GMT                 GMT+8               GMT-5
```

### 4.2 timezone基础操作


```python
from datetime import datetime, timezone, timedelta

# 1. 创建时区对象
utc = timezone.utc                           # UTC时区
beijing = timezone(timedelta(hours=8))       # 北京时区(UTC+8)
newyork = timezone(timedelta(hours=-5))      # 纽约时区(UTC-5)

# 2. 获取带时区的当前时间
utc_now = datetime.now(utc)
beijing_now = datetime.now(beijing)

print(f"UTC时间: {utc_now}")
print(f"北京时间: {beijing_now}")
```

### 4.3 时区转换技巧


**不同时区之间的转换**

```python
# 1. 创建一个UTC时间
utc_time = datetime(2025, 9, 21, 10, 0, 0, tzinfo=timezone.utc)
print(f"UTC时间: {utc_time}")

# 2. 转换到不同时区
beijing_time = utc_time.astimezone(beijing)
newyork_time = utc_time.astimezone(newyork)

print(f"北京时间: {beijing_time}")
print(f"纽约时间: {newyork_time}")
```

⚠️ **常见误区**
```python
# ❌ 错误做法：直接用本地时间
local_time = datetime.now()  # 没有时区信息

# ✅ 正确做法：明确指定时区
aware_time = datetime.now(timezone.utc)  # 有时区信息
```

🎯 **记忆技巧**
> **"astimezone"** = **"as timezone"** = 转换到指定时区

### 4.4 实际应用场景


```python
# 场景：国际会议时间协调
def schedule_meeting(utc_time_str):
    """安排国际会议，显示各地时间"""
    # 解析UTC时间
    utc_time = datetime.fromisoformat(utc_time_str).replace(tzinfo=timezone.utc)
    
    # 转换到各个时区
    beijing = utc_time.astimezone(timezone(timedelta(hours=8)))
    london = utc_time.astimezone(timezone(timedelta(hours=1)))
    newyork = utc_time.astimezone(timezone(timedelta(hours=-5)))
    
    print("🌍 国际会议时间安排:")
    print(f"  北京: {beijing.strftime('%Y-%m-%d %H:%M')}")
    print(f"  伦敦: {london.strftime('%Y-%m-%d %H:%M')}")  
    print(f"  纽约: {newyork.strftime('%Y-%m-%d %H:%M')}")

# 使用示例
schedule_meeting("2025-09-21T14:00:00")
```

---

## 5. ⏰ time模块实用操作


### 5.1 time模块概述


**time模块的特点**
> time模块更接近操作系统，主要处理时间戳和系统时间。就像汽车的仪表盘，显示最基础的时间信息。

```python
import time

# 1. 获取当前时间戳
timestamp = time.time()
print(f"时间戳: {timestamp}")  # 1695299425.123456

# 2. 暂停程序执行
print("开始等待...")
time.sleep(2)  # 等待2秒
print("等待结束!")
```

### 5.2 时间戳相关操作


**什么是时间戳？**
> 时间戳就像一个全球通用的"时间身份证号"，是从1970年1月1日开始到现在的秒数。

```python
import time

# 1. 获取当前时间戳
now_timestamp = time.time()
print(f"当前时间戳: {now_timestamp}")

# 2. 时间戳转换为可读格式
readable_time = time.ctime(now_timestamp)
print(f"可读时间: {readable_time}")

# 3. 结构化时间对象
struct_time = time.localtime(now_timestamp)
print(f"年: {struct_time.tm_year}")
print(f"月: {struct_time.tm_mon}")
print(f"日: {struct_time.tm_mday}")
```

### 5.3 性能测试应用


```python
import time

def measure_performance():
    """测量代码执行时间"""
    start = time.time()
    
    # 模拟一些计算工作
    total = 0
    for i in range(1000000):
        total += i
    
    end = time.time()
    duration = end - start
    
    print(f"计算耗时: {duration:.3f} 秒")
    return duration

# 使用示例
measure_performance()
```

---

## 6. 📆 calendar日历模块


### 6.1 calendar模块基础


**calendar模块的作用**
> calendar模块就像一本日历，可以告诉你某年某月的日历布局，哪天是星期几。

```python
import calendar

# 1. 显示某年某月的日历
print("2025年9月日历:")
print(calendar.month(2025, 9))

# 2. 判断是否闰年
is_leap = calendar.isleap(2024)
print(f"2024年是闰年吗? {is_leap}")

# 3. 获取某月有多少天
days_in_month = calendar.monthrange(2025, 2)
print(f"2025年2月有 {days_in_month[1]} 天")
```

### 6.2 实用日历计算


```python
# 获取某年的所有月份日历
def show_year_calendar(year):
    """显示全年日历"""
    print(f"\n{year}年日历:")
    print(calendar.calendar(year))

# 计算两个日期之间有多少个工作日
def count_weekdays(start_year, start_month, end_year, end_month):
    """计算工作日数量"""
    weekdays = 0
    # 这里可以实现具体的计算逻辑
    return weekdays

# 示例使用
show_year_calendar(2025)
```

---

## 7. 🎨 时间格式化详解


### 7.1 strftime格式化输出


**什么是strftime？**
> strftime就像时间的"化妆师"，可以把时间打扮成你想要的样子。比如把"2025-09-21 15:30:25"变成"2025年9月21日 下午3点30分"。

```python
from datetime import datetime

now = datetime.now()

# 常用格式化方式
formats = {
    "标准格式": now.strftime("%Y-%m-%d %H:%M:%S"),
    "中文格式": now.strftime("%Y年%m月%d日 %H时%M分"),
    "美式格式": now.strftime("%m/%d/%Y %I:%M %p"),
    "简短格式": now.strftime("%y%m%d"),
    "文件名格式": now.strftime("%Y%m%d_%H%M%S")
}

print("🎨 时间格式化展示:")
for name, formatted_time in formats.items():
    print(f"  {name}: {formatted_time}")
```

📋 **格式化代码速查表**
| 代码 | **含义** | **示例** |
|------|---------|---------|
| `%Y` | 4位年份 | 2025 |
| `%m` | 月份(01-12) | 09 |
| `%d` | 日期(01-31) | 21 |
| `%H` | 小时(00-23) | 15 |
| `%M` | 分钟(00-59) | 30 |
| `%S` | 秒(00-59) | 25 |
| `%A` | 星期几全名 | Monday |
| `%B` | 月份全名 | September |

### 7.2 strptime解析字符串


**什么是strptime？**
> strptime是strftime的反向操作，就像"时间翻译器"，把文字描述的时间转换成程序能理解的时间对象。

```python
from datetime import datetime

# 解析不同格式的时间字符串
time_strings = [
    ("2025-09-21 15:30:25", "%Y-%m-%d %H:%M:%S"),
    ("2025年9月21日", "%Y年%m月%d日"),
    ("09/21/2025", "%m/%d/%Y"),
    ("21-Sep-2025", "%d-%b-%Y")
]

print("🔍 字符串解析示例:")
for time_str, format_str in time_strings:
    parsed_time = datetime.strptime(time_str, format_str)
    print(f"  '{time_str}' → {parsed_time}")
```

💡 **记忆技巧**
> - **strftime** = **str**ing **f**rom **time** (时间→字符串)
> - **strptime** = **str**ing **p**arse **time** (字符串→时间)

### 7.3 实际应用案例


```python
def log_formatter(message):
    """日志格式化器"""
    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    return f"[{timestamp}] {message}"

def parse_log_time(log_line):
    """解析日志时间"""
    # 假设日志格式: [2025-09-21 15:30:25] 用户登录
    time_part = log_line.split(']')[0][1:]  # 提取时间部分
    return datetime.strptime(time_part, "%Y-%m-%d %H:%M:%S")

# 使用示例
log_entry = log_formatter("用户登录成功")
print(f"日志: {log_entry}")

parsed_time = parse_log_time(log_entry)
print(f"解析的时间: {parsed_time}")
```

---

## 8. 🔢 时间戳转换技巧


### 8.1 Unix时间戳理解


**什么是Unix时间戳？**
> Unix时间戳就像一个全球统一的"时间计数器"，从1970年1月1日开始数秒数。就像奥运会从某个起点开始计时一样。

🏠 **生活类比**
```
时间戳就像:
• 汽车里程表: 从出厂开始累计公里数
• 手机计步器: 从今天0点开始累计步数
• Unix时间戳: 从1970年开始累计秒数
```

### 8.2 时间戳转换实操


```python
from datetime import datetime
import time

# 1. 获取当前时间戳
current_timestamp = time.time()
print(f"当前时间戳: {current_timestamp}")

# 2. 时间戳转换为datetime对象
dt_object = datetime.fromtimestamp(current_timestamp)
print(f"转换后的时间: {dt_object}")

# 3. datetime对象转换为时间戳  
dt = datetime(2025, 9, 21, 15, 30, 25)
timestamp = dt.timestamp()
print(f"指定时间的时间戳: {timestamp}")

# 4. 时间戳转换为字符串
time_string = datetime.fromtimestamp(current_timestamp).strftime("%Y-%m-%d %H:%M:%S")
print(f"格式化时间: {time_string}")
```

### 8.3 时间戳应用场景


```python
# 应用案例1: 文件备份命名
def create_backup_filename(original_name):
    """创建带时间戳的备份文件名"""
    timestamp = int(time.time())
    name, ext = original_name.rsplit('.', 1)
    return f"{name}_backup_{timestamp}.{ext}"

backup_name = create_backup_filename("data.txt")
print(f"备份文件名: {backup_name}")

# 应用案例2: 缓存过期检查
def is_cache_expired(cache_timestamp, expire_seconds=3600):
    """检查缓存是否过期(默认1小时)"""
    current_time = time.time()
    return (current_time - cache_timestamp) > expire_seconds

# 模拟缓存时间戳(1小时前)
old_cache = time.time() - 3700
print(f"缓存是否过期: {is_cache_expired(old_cache)}")
```

### 8.4 时间戳转换工具函数


```python
class TimeConverter:
    """时间戳转换工具类"""
    
    @staticmethod
    def timestamp_to_readable(timestamp):
        """时间戳转可读格式"""
        return datetime.fromtimestamp(timestamp).strftime("%Y-%m-%d %H:%M:%S")
    
    @staticmethod
    def readable_to_timestamp(time_string, format_str="%Y-%m-%d %H:%M:%S"):
        """可读格式转时间戳"""
        dt = datetime.strptime(time_string, format_str)
        return dt.timestamp()
    
    @staticmethod
    def current_timestamp():
        """获取当前时间戳"""
        return time.time()

# 使用示例
converter = TimeConverter()
print(f"当前时间戳: {converter.current_timestamp()}")
print(f"转换为可读: {converter.timestamp_to_readable(time.time())}")
```

---

## 9. 🚀 实际应用场景


### 9.1 日志系统时间处理


```python
import os
from datetime import datetime

class Logger:
    """简单的日志记录器"""
    
    def __init__(self, log_file="app.log"):
        self.log_file = log_file
    
    def log(self, level, message):
        """记录日志"""
        timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
        log_entry = f"[{timestamp}] [{level}] {message}\n"
        
        with open(self.log_file, 'a', encoding='utf-8') as f:
            f.write(log_entry)
        
        print(log_entry.strip())
    
    def info(self, message):
        self.log("INFO", message)
    
    def error(self, message):
        self.log("ERROR", message)

# 使用示例
logger = Logger()
logger.info("程序启动")
logger.error("发生错误")
```

### 9.2 定时任务调度


```python
from datetime import datetime, timedelta
import time

class SimpleScheduler:
    """简单的任务调度器"""
    
    def __init__(self):
        self.tasks = []
    
    def schedule_daily(self, hour, minute, task_func, task_name):
        """每日定时任务"""
        next_run = datetime.now().replace(hour=hour, minute=minute, second=0, microsecond=0)
        
        # 如果今天的时间已过，安排到明天
        if next_run <= datetime.now():
            next_run += timedelta(days=1)
        
        self.tasks.append({
            'name': task_name,
            'next_run': next_run,
            'function': task_func,
            'type': 'daily'
        })
        
        print(f"任务 '{task_name}' 已安排到 {next_run}")
    
    def run(self):
        """执行调度器"""
        print("调度器启动...")
        while True:
            now = datetime.now()
            
            for task in self.tasks:
                if now >= task['next_run']:
                    print(f"执行任务: {task['name']}")
                    task['function']()
                    
                    # 安排下次执行(每日任务)
                    if task['type'] == 'daily':
                        task['next_run'] += timedelta(days=1)
            
            time.sleep(60)  # 每分钟检查一次

# 示例任务
def backup_task():
    print("执行数据备份...")

def report_task():
    print("生成日报...")

# 使用示例
scheduler = SimpleScheduler()
scheduler.schedule_daily(9, 0, backup_task, "每日备份")
scheduler.schedule_daily(18, 0, report_task, "每日报告")
```

### 9.3 性能监控工具


```python
import time
from datetime import datetime

class PerformanceMonitor:
    """性能监控工具"""
    
    def __init__(self):
        self.start_times = {}
        self.records = []
    
    def start_timer(self, task_name):
        """开始计时"""
        self.start_times[task_name] = time.time()
        print(f"⏱️ 开始监控: {task_name}")
    
    def end_timer(self, task_name):
        """结束计时"""
        if task_name not in self.start_times:
            print(f"❌ 错误: 未找到任务 {task_name}")
            return
        
        start_time = self.start_times[task_name]
        end_time = time.time()
        duration = end_time - start_time
        
        record = {
            'task': task_name,
            'start_time': datetime.fromtimestamp(start_time),
            'end_time': datetime.fromtimestamp(end_time),
            'duration': duration
        }
        
        self.records.append(record)
        print(f"✅ 任务完成: {task_name}, 耗时: {duration:.3f}秒")
        
        del self.start_times[task_name]
        return duration
    
    def get_report(self):
        """生成性能报告"""
        if not self.records:
            print("📊 暂无性能数据")
            return
        
        print("\n📊 性能监控报告:")
        print("-" * 50)
        for record in self.records:
            print(f"任务: {record['task']}")
            print(f"开始: {record['start_time'].strftime('%H:%M:%S')}")
            print(f"结束: {record['end_time'].strftime('%H:%M:%S')}")
            print(f"耗时: {record['duration']:.3f}秒")
            print("-" * 30)

# 使用示例
monitor = PerformanceMonitor()

# 模拟任务监控
monitor.start_timer("数据处理")
time.sleep(2)  # 模拟工作
monitor.end_timer("数据处理")

monitor.start_timer("文件上传")
time.sleep(1)  # 模拟工作
monitor.end_timer("文件上传")

monitor.get_report()
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


```
🔸 datetime模块三大类：
  • date: 只有日期(年月日)
  • time: 只有时间(时分秒)
  • datetime: 完整日期时间

🔸 时间差计算：
  • timedelta: 表示时间段长度
  • 支持加减运算：datetime ± timedelta

🔸 时区处理：
  • timezone: 时区对象
  • astimezone(): 时区转换
  • 区分有时区(aware)和无时区(naive)时间

🔸 格式化转换：
  • strftime(): 时间 → 字符串
  • strptime(): 字符串 → 时间
  • timestamp(): 时间 → 时间戳
```

### 10.2 关键理解要点


**🔹 什么时候用哪个模块？**
```
选择标准：
• 需要面向对象操作 → datetime模块
• 需要系统级时间操作 → time模块  
• 需要日历计算 → calendar模块
• 需要性能测试计时 → time.time()
```

**🔹 时区处理的重要性**
```
为什么重要：
• 全球化应用必备
• 避免时间混乱
• 正确的时间计算

记住原则：
• 存储用UTC时间
• 显示用本地时间
• 转换用astimezone()
```

**🔹 时间戳的应用价值**
```
时间戳优势：
• 全球统一标准
• 便于存储和传输
• 计算简单高效
• 不受时区影响
```

### 10.3 实际应用指导


🎯 **一分钟掌握**
核心要记住的3个要点：
1. **datetime.now()** - 获取当前时间
2. **strftime()** - 格式化显示
3. **timedelta()** - 时间计算

⭐ **必须理解**
时间处理的本质就是：**获取→计算→格式化→应用**

🚨 **注意事项**
- 处理时区时要明确是否带时区信息
- 时间戳计算要注意精度（秒 vs 毫秒）
- 格式化字符串要与数据格式匹配

### 10.4 学习路径建议


```
学习进度规划：
第1步：掌握datetime基础操作 (1天)
第2步：学会时间差计算 (1天)  
第3步：理解时区转换 (2天)
第4步：熟练格式化操作 (1天)
第5步：实际项目应用 (3天)
```

📚 **扩展学习**
- 深入学习：[Python官方文档 - datetime](https://docs.python.org)
- 实战项目：构建个人时间管理工具
- 进阶主题：多线程环境下的时间处理

### 10.5 常见问题解答


**Q: datetime和time模块有什么区别？**
A: datetime更高级，面向对象设计，功能更丰富；time更基础，接近系统调用

**Q: 为什么要处理时区？**  
A: 因为全球用户在不同时区，需要显示本地时间，避免时间混乱

**Q: 时间戳有什么用？**
A: 时间戳是全球统一的时间表示，便于存储、传输和比较

**Q: 如何选择时间格式？**
A: 根据用户习惯和业务需求，常用ISO格式便于程序处理

🎯 **核心记忆口诀**
> datetime管日期，time管系统调
> 时区转换astimezone，格式化用strftime巧
> 时间戳存储传输好，timedelta计算不可少
> 实际应用多练习，时间处理难不倒