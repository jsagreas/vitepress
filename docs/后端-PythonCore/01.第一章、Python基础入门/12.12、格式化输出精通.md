---
title: 12、格式化输出精通
---
## 📚 目录

1. [格式化输出概述](#1-格式化输出概述)
2. [% 格式化操作符](#2-格式化操作符)
3. [str.format() 方法](#3-strformat-方法)
4. [f-string 格式化](#4-f-string-格式化)
5. [格式规范语法详解](#5-格式规范语法详解)
6. [数字格式化技巧](#6-数字格式化技巧)
7. [日期时间格式化](#7-日期时间格式化)
8. [对齐与填充美化](#8-对齐与填充美化)
9. [格式化性能对比](#9-格式化性能对比)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🎯 格式化输出概述


### 1.1 什么是格式化输出


**简单理解**：就像做菜时要摆盘一样，格式化输出就是把数据按照你想要的样子"摆盘"展示出来。

```
原始数据：name="张三", age=25, score=98.5
普通输出：张三 25 98.5
格式化输出：姓名：张三，年龄：25岁，成绩：98.5分
```

**为什么需要格式化**：
- 🎨 **美观展示**：让数据看起来更整齐、更专业
- 📊 **信息清晰**：突出重要信息，提高可读性  
- 🔧 **灵活控制**：控制小数位数、对齐方式等
- 📱 **用户友好**：符合用户阅读习惯

### 1.2 Python中的三种主要方式


```
历史发展脉络：
% 格式化 (古老方式) → str.format() (改进方式) → f-string (现代方式)

就像交通工具的发展：
马车 → 汽车 → 高铁
都能到达目的地，但速度和体验不同
```

**选择建议**：
- 🟢 **推荐**：f-string（Python 3.6+）- 简洁、快速、易读
- 🟡 **备选**：str.format() - 复杂格式化、兼容性需求
- 🔴 **了解**：% 格式化 - 维护老代码时可能遇到

---

## 2. 📝 % 格式化操作符


### 2.1 基本概念和语法


**% 格式化**就像填空题一样，先写好模板，再往空白处填内容。

```python
# 基本语法：模板字符串 % 数据
"我叫%s，今年%d岁" % ("张三", 25)
# 输出：我叫张三，今年25岁
```

### 2.2 常用格式符号


| 格式符 | **含义** | **举例** | **输出** |
|--------|----------|----------|----------|
| `%s` | 字符串 | `"姓名：%s" % "张三"` | `姓名：张三` |
| `%d` | 整数 | `"年龄：%d" % 25` | `年龄：25` |
| `%f` | 浮点数 | `"成绩：%f" % 98.5` | `成绩：98.500000` |
| `%.2f` | 保留2位小数 | `"成绩：%.2f" % 98.567` | `成绩：98.57` |
| `%x` | 十六进制 | `"颜色：#%x" % 255` | `颜色：#ff` |

### 2.3 实际应用示例


```python
# 学生成绩单示例
name = "李明"
chinese = 85
math = 92.5
english = 78

# 使用 % 格式化
report = """
=== 学生成绩单 ===
姓名：%s
语文：%d分
数学：%.1f分  
英语：%d分
平均分：%.2f分
""" % (name, chinese, math, english, (chinese + math + english) / 3)

print(report)
```

**输出效果**：
```
=== 学生成绩单 ===
姓名：李明
语文：85分
数学：92.5分  
英语：78分
平均分：85.17分
```

### 2.4 % 格式化的优缺点


**优点**：
- ✅ 语法简单，容易理解
- ✅ 兼容性好，老版本Python都支持
- ✅ 类似C语言printf，熟悉的人容易上手

**缺点**：
- ❌ 功能有限，复杂格式化困难
- ❌ 容易出错，参数顺序必须对应
- ❌ 可读性差，模板和数据分离

---

## 3. 🔧 str.format() 方法


### 3.1 基本概念


**str.format()** 就像升级版的填空题，不仅能填空，还能给空白处编号、命名，更加灵活。

```python
# 基本语法
"模板字符串 {占位符}".format(数据)

# 简单示例
"我叫{}，今年{}岁".format("张三", 25)
# 输出：我叫张三，今年25岁
```

### 3.2 占位符的三种方式


**位置参数**（按顺序填入）：
```python
msg = "今天是{}月{}日，天气{}".format(12, 25, "晴朗")
print(msg)  # 今天是12月25日，天气晴朗
```

**索引参数**（指定位置）：
```python
msg = "我喜欢{1}和{0}，但更喜欢{1}".format("苹果", "香蕉")
print(msg)  # 我喜欢香蕉和苹果，但更喜欢香蕉
```

**关键字参数**（用名字指定）：
```python
msg = "我叫{name}，今年{age}岁，住在{city}".format(
    name="张三", 
    age=25, 
    city="北京"
)
print(msg)  # 我叫张三，今年25岁，住在北京
```

### 3.3 格式控制语法


**数字格式化**：
```python
price = 1234.5678

print("价格：{:.2f}元".format(price))     # 价格：1234.57元
print("价格：{:,.2f}元".format(price))    # 价格：1,234.57元
print("价格：{:>10.2f}元".format(price))  # 价格：   1234.57元
```

**字符串对齐**：
```python
name = "张三"

print("姓名：{:<10}".format(name))   # 左对齐：张三       
print("姓名：{:>10}".format(name))   # 右对齐：       张三
print("姓名：{:^10}".format(name))   # 居中：   张三    
```

### 3.4 实际应用：制作表格


```python
# 商品销售报表
products = [
    {"name": "iPhone", "price": 6999, "sales": 150},
    {"name": "iPad", "price": 3999, "sales": 89},
    {"name": "MacBook", "price": 12999, "sales": 45}
]

# 表头
print("{:^10} {:^8} {:^6} {:^10}".format("商品", "价格", "销量", "销售额"))
print("-" * 40)

# 数据行
for product in products:
    revenue = product["price"] * product["sales"]
    print("{:^10} {:>8} {:>6} {:>10,}".format(
        product["name"],
        product["price"],
        product["sales"],
        revenue
    ))
```

**输出效果**：
```
   商品     价格     销量      销售额   
----------------------------------------
  iPhone      6999    150   1,049,850
   iPad       3999     89     355,911
 MacBook     12999     45     584,955
```

---

## 4. 🚀 f-string 格式化


### 4.1 什么是 f-string


**f-string** 是Python 3.6推出的"超级武器"，就像智能手机一样，把之前需要多步完成的事情一步搞定。

```python
# 传统方式需要分开写
name = "张三"
age = 25
msg = "我叫{}，今年{}岁".format(name, age)

# f-string 一步到位
msg = f"我叫{name}，今年{age}岁"
```

**f-string 的神奇之处**：
- 🎯 **直接嵌入变量**：大括号里直接写变量名
- ⚡ **性能最快**：编译时就处理好了
- 📖 **代码清晰**：看代码就知道输出什么

### 4.2 基本使用方法


**变量嵌入**：
```python
name = "李明"
score = 95.5
subject = "数学"

# 直接在大括号中使用变量
result = f"{name}的{subject}成绩是{score}分"
print(result)  # 李明的数学成绩是95.5分
```

**表达式计算**：
```python
a = 10
b = 20

# 大括号内可以写任何Python表达式
print(f"{a} + {b} = {a + b}")           # 10 + 20 = 30
print(f"{a} 的平方是 {a ** 2}")         # 10 的平方是 100
print(f"较大的数是 {max(a, b)}")         # 较大的数是 20
```

**函数调用**：
```python
import datetime

name = "张三"
now = datetime.datetime.now()

print(f"你好 {name.upper()}!")                    # 你好 ZHANG三!
print(f"当前时间：{now.strftime('%Y-%m-%d')}")     # 当前时间：2025-09-21
print(f"字符串长度：{len(name)}")                   # 字符串长度：2
```

### 4.3 格式化控制


**数字格式化**：
```python
pi = 3.14159265
money = 12345.67

print(f"圆周率：{pi:.2f}")        # 圆周率：3.14
print(f"圆周率：{pi:.4f}")        # 圆周率：3.1416
print(f"金额：{money:,.2f}元")    # 金额：12,345.67元
print(f"百分比：{0.8567:.1%}")    # 百分比：85.7%
```

**字符串对齐**：
```python
items = ["苹果", "香蕉", "橙子"]
prices = [3.5, 2.8, 4.2]

for item, price in zip(items, prices):
    print(f"{item:^6} {price:>6.1f}元")
```

**输出**：
```
  苹果     3.5元
  香蕉     2.8元
  橙子     4.2元
```

### 4.4 高级技巧


**多行字符串**：
```python
student = {
    "name": "王小明",
    "age": 20,
    "major": "计算机科学",
    "gpa": 3.85
}

profile = f"""
学生信息档案
========================
姓名：{student['name']}
年龄：{student['age']}岁
专业：{student['major']}
GPA：{student['gpa']:.2f}
========================
"""
print(profile)
```

**条件表达式**：
```python
score = 85
grade = f"成绩：{score}分，等级：{'优秀' if score >= 90 else '良好' if score >= 80 else '及格' if score >= 60 else '不及格'}"
print(grade)  # 成绩：85分，等级：良好
```

---

## 5. 📐 格式规范语法详解


### 5.1 通用格式语法结构


**完整语法**：`{值:填充字符对齐方式宽度精度类型}`

```
语法结构解析：
{value:fill align width .precision type}

就像填写表格：
- value: 要填入的内容
- fill: 用什么字符填空白（默认空格）
- align: 对齐方式（左中右）
- width: 总宽度
- precision: 精度（小数位数）
- type: 数据类型
```

### 5.2 对齐方式详解


```python
text = "Python"

print(f"'{text:<10}'")    # 'Python    ' 左对齐
print(f"'{text:>10}'")    # '    Python' 右对齐  
print(f"'{text:^10}'")    # '  Python  ' 居中
print(f"'{text:*^10}'")   # '**Python**' 用*填充居中
```

**对齐方式记忆法**：
- `<` ：箭头指向左边 → 左对齐
- `>` ：箭头指向右边 → 右对齐  
- `^` ：帽子在中间 → 居中对齐

### 5.3 数字类型标识


| 类型 | **含义** | **示例** | **输出** |
|------|----------|----------|----------|
| `d` | 十进制整数 | `f"{255:d}"` | `255` |
| `x` | 十六进制小写 | `f"{255:x}"` | `ff` |
| `X` | 十六进制大写 | `f"{255:X}"` | `FF` |
| `o` | 八进制 | `f"{255:o}"` | `377` |
| `b` | 二进制 | `f"{255:b}"` | `11111111` |
| `f` | 固定小数 | `f"{3.14:f}"` | `3.140000` |
| `e` | 科学计数法小写 | `f"{1234:e}"` | `1.234000e+03` |
| `%` | 百分比 | `f"{0.85:%}"` | `85.000000%` |

### 5.4 实际格式化案例


**制作收据**：
```python
def print_receipt(items):
    print("=" * 40)
    print(f"{'商品':^15} {'数量':^6} {'单价':^8} {'金额':^8}")
    print("-" * 40)
    
    total = 0
    for item in items:
        amount = item['quantity'] * item['price']
        total += amount
        print(f"{item['name']:^15} {item['quantity']:^6d} {item['price']:^8.2f} {amount:^8.2f}")
    
    print("-" * 40)
    print(f"{'总计':>32} {total:>6.2f}")
    print("=" * 40)

# 使用示例
shopping_list = [
    {"name": "苹果", "quantity": 3, "price": 5.5},
    {"name": "牛奶", "quantity": 2, "price": 12.8},
    {"name": "面包", "quantity": 1, "price": 8.0}
]

print_receipt(shopping_list)
```

---

## 6. 🔢 数字格式化技巧


### 6.1 小数精度控制


**保留指定小数位数**：
```python
pi = 3.14159265359

print(f"默认：{pi}")           # 默认：3.14159265359
print(f"1位小数：{pi:.1f}")    # 1位小数：3.1
print(f"2位小数：{pi:.2f}")    # 2位小数：3.14
print(f"4位小数：{pi:.4f}")    # 4位小数：3.1416
```

**四舍五入规则**：
```python
numbers = [3.145, 3.146, 3.144, 3.149]

for num in numbers:
    print(f"{num} → {num:.2f}")

# 输出：
# 3.145 → 3.14  (Python使用银行家舍入法)
# 3.146 → 3.15
# 3.144 → 3.14  
# 3.149 → 3.15
```

### 6.2 千分位分隔符


**让大数字更易读**：
```python
big_numbers = [1234, 12345, 123456, 1234567]

for num in big_numbers:
    print(f"原数字：{num:>8} → 格式化：{num:>10,}")

# 输出：
# 原数字：    1234 → 格式化：     1,234
# 原数字：   12345 → 格式化：    12,345
# 原数字：  123456 → 格式化：   123,456
# 原数字： 1234567 → 格式化： 1,234,567
```

**金额显示**：
```python
salary = 234567.89
bonus = 50000

print(f"基本工资：￥{salary:,.2f}")        # 基本工资：￥234,567.89
print(f"年终奖金：￥{bonus:,.2f}")         # 年终奖金：￥50,000.00
print(f"总收入：￥{salary + bonus:,.2f}")  # 总收入：￥284,567.89
```

### 6.3 百分比格式化


**百分比显示**：
```python
scores = [0.856, 0.923, 0.789, 0.654]

print("学生成绩百分比：")
for i, score in enumerate(scores, 1):
    print(f"学生{i}：{score:.1%}")

# 输出：
# 学生1：85.6%
# 学生2：92.3%
# 学生3：78.9%
# 学生4：65.4%
```

### 6.4 正负数符号控制


```python
numbers = [42, -42, 0]

for num in numbers:
    print(f"默认：{num:d}")      # 默认：42, -42, 0
    print(f"强制显示符号：{num:+d}")  # 强制显示符号：+42, -42, +0
    print(f"正数空格：{num: d}")      # 正数空格： 42, -42,  0
```

---

## 7. 📅 日期时间格式化


### 7.1 基本日期时间格式化


```python
import datetime

now = datetime.datetime.now()
today = datetime.date.today()

# 使用 strftime 方法格式化
print(f"完整日期时间：{now.strftime('%Y-%m-%d %H:%M:%S')}")
print(f"中文日期：{now.strftime('%Y年%m月%d日')}")
print(f"12小时制：{now.strftime('%Y-%m-%d %I:%M:%S %p')}")
print(f"星期几：{now.strftime('%A')}")
```

### 7.2 常用日期格式代码


| 代码 | **含义** | **示例** |
|------|----------|----------|
| `%Y` | 四位年份 | `2025` |
| `%y` | 两位年份 | `25` |
| `%m` | 月份数字 | `09` |
| `%B` | 月份英文全名 | `September` |
| `%b` | 月份英文缩写 | `Sep` |
| `%d` | 日期 | `21` |
| `%H` | 24小时制小时 | `14` |
| `%I` | 12小时制小时 | `02` |
| `%M` | 分钟 | `30` |
| `%S` | 秒 | `45` |
| `%A` | 星期英文全名 | `Sunday` |
| `%a` | 星期英文缩写 | `Sun` |

### 7.3 实际应用示例


**日志时间戳**：
```python
import datetime

def log_message(message):
    timestamp = datetime.datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    print(f"[{timestamp}] {message}")

# 使用示例
log_message("系统启动")
log_message("用户登录")
log_message("数据保存完成")

# 输出：
# [2025-09-21 14:30:45] 系统启动
# [2025-09-21 14:30:45] 用户登录
# [2025-09-21 14:30:45] 数据保存完成
```

**生日倒计时**：
```python
import datetime

def birthday_countdown(birthday_str):
    # 解析生日字符串
    birthday = datetime.datetime.strptime(birthday_str, '%Y-%m-%d').date()
    today = datetime.date.today()
    
    # 计算今年的生日
    this_year_birthday = birthday.replace(year=today.year)
    if this_year_birthday < today:
        # 如果今年生日已过，计算明年的
        this_year_birthday = birthday.replace(year=today.year + 1)
    
    days_left = (this_year_birthday - today).days
    
    print(f"距离 {birthday.strftime('%m月%d日')} 生日还有 {days_left} 天")

# 使用示例
birthday_countdown("2000-12-25")  # 距离 12月25日 生日还有 95 天
```

---

## 8. 🎨 对齐与填充美化


### 8.1 基本对齐方式


**文本对齐演示**：
```python
names = ["张三", "李四", "王五", "赵六"]
ages = [25, 30, 28, 35]

print("学员信息表")
print("=" * 20)

for name, age in zip(names, ages):
    print(f"{name:<6} {age:>3}岁")

# 输出：
# 张三     25岁
# 李四     30岁
# 王五     28岁  
# 赵六     35岁
```

### 8.2 填充字符美化


**使用不同填充字符**：
```python
title = "Python学习"
content = "格式化输出"

print(f"{title:=^20}")     # ====Python学习=====
print(f"{content:*^20}")   # ***格式化输出****
print(f"{'开始':->18}")     # ---------------开始
print(f"{'结束':-<18}")     # 结束---------------
```

**制作分隔线**：
```python
def print_section(title, width=40):
    print()
    print("=" * width)
    print(f"{title:^{width}}")
    print("=" * width)

# 使用示例
print_section("第一章：基础语法")
print("这里是章节内容...")
print_section("第二章：数据类型")
print("这里是章节内容...")
```

### 8.3 制作精美表格


```python
def print_score_table(students):
    """打印学生成绩表格"""
    
    # 表头
    print("┌" + "─" * 50 + "┐")
    print("│" + "学生成绩统计表".center(48) + "│")
    print("├" + "─" * 50 + "┤")
    print(f"│ {'姓名':^8} │ {'语文':^6} │ {'数学':^6} │ {'英语':^6} │ {'总分':^6} │")
    print("├" + "─" * 50 + "┤")
    
    # 数据行
    for student in students:
        total = student['chinese'] + student['math'] + student['english']
        print(f"│ {student['name']:^8} │ {student['chinese']:^6} │ "
              f"{student['math']:^6} │ {student['english']:^6} │ {total:^6} │")
    
    print("└" + "─" * 50 + "┘")

# 测试数据
students_data = [
    {"name": "张三", "chinese": 85, "math": 92, "english": 78},
    {"name": "李四", "chinese": 90, "math": 88, "english": 85},
    {"name": "王五", "chinese": 78, "math": 95, "english": 89}
]

print_score_table(students_data)
```

---

## 9. ⚡ 格式化性能对比


### 9.1 性能测试对比


```python
import time

def performance_test(iterations=100000):
    """测试三种格式化方法的性能"""
    
    name = "张三"
    age = 25
    score = 95.5
    
    # 测试 % 格式化
    start = time.time()
    for _ in range(iterations):
        result = "姓名：%s，年龄：%d，分数：%.2f" % (name, age, score)
    percent_time = time.time() - start
    
    # 测试 str.format()
    start = time.time()
    for _ in range(iterations):
        result = "姓名：{}，年龄：{}，分数：{:.2f}".format(name, age, score)
    format_time = time.time() - start
    
    # 测试 f-string
    start = time.time()
    for _ in range(iterations):
        result = f"姓名：{name}，年龄：{age}，分数：{score:.2f}"
    fstring_time = time.time() - start
    
    # 打印结果
    print(f"性能测试结果（{iterations:,} 次循环）：")
    print(f"% 格式化：   {percent_time:.4f} 秒")
    print(f"str.format()：{format_time:.4f} 秒")
    print(f"f-string：   {fstring_time:.4f} 秒")
    
    # 计算相对性能
    baseline = fstring_time
    print(f"\n相对于 f-string 的性能：")
    print(f"% 格式化慢了：{percent_time/baseline:.1f} 倍")
    print(f"str.format()慢了：{format_time/baseline:.1f} 倍")

# 运行测试
performance_test()
```

**典型输出结果**：
```
性能测试结果（100,000 次循环）：
% 格式化：   0.0856 秒
str.format()：0.1234 秒  
f-string：   0.0445 秒

相对于 f-string 的性能：
% 格式化慢了：1.9 倍
str.format()慢了：2.8 倍
```

### 9.2 性能优化建议


**选择原则**：
```
🚀 首选 f-string：
- 性能最快（编译时优化）
- 代码最清晰
- Python 3.6+ 推荐

⚖️ 备选 % 格式化：
- 简单场景下性能尚可
- 兼容老版本Python
- 语法最简洁

🔧 特殊情况用 str.format()：
- 复杂格式化需求
- 动态格式字符串
- 模板重用场景
```

**实际选择建议**：
```python
# ✅ 推荐：简单清晰的 f-string
name = "张三"
score = 95.5
message = f"{name}的成绩是{score:.1f}分"

# ✅ 可以：复杂格式化用 str.format()
template = "学生{name}的{subject}成绩为{score:.2f}分，等级为{grade}"
message = template.format(name="李四", subject="数学", score=92.5, grade="优秀")

# ❌ 不推荐：除非维护老代码
message = "%s的成绩是%.1f分" % (name, score)
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


**三种格式化方法对比**：

| 方法 | **语法示例** | **优点** | **缺点** | **适用场景** |
|------|-------------|----------|----------|-------------|
| **% 格式化** | `"姓名：%s" % name` | 简单直接 | 功能有限 | 维护老代码 |
| **str.format()** | `"姓名：{}".format(name)` | 功能强大 | 语法复杂 | 复杂格式化 |
| **f-string** | `f"姓名：{name}"` | 快速清晰 | 需Python3.6+ | 日常开发首选 |

### 10.2 关键语法要点


**格式化语法结构**：
```
{值:填充字符对齐方式宽度.精度类型}

记忆口诀：
值在前，冒号分，
填充对齐定宽度，
点号精度类型跟，
格式清晰效果好
```

**常用格式代码**：
- 🔢 **数字**：`:d` 整数、`:.2f` 两位小数、`:,` 千分位、`:.1%` 百分比
- 📝 **文本**：`:^10` 居中10位、`:<8` 左对齐8位、`:>6` 右对齐6位
- 📅 **日期**：`strftime('%Y-%m-%d %H:%M:%S')` 标准格式

### 10.3 实际应用建议


**日常开发推荐**：
1. 🥇 **优先使用 f-string**：性能最好，代码最清晰
2. 🥈 **复杂场景用 str.format()**：动态模板、复杂对齐
3. 🥉 **了解 % 格式化**：维护老代码时可能遇到

**格式化最佳实践**：
```python
# ✅ 好的做法
price = 1234.56
quantity = 5

# 清晰明了的变量嵌入
total = f"总价：￥{price * quantity:,.2f}"

# 合理的格式控制
header = f"{'商品':^10} {'单价':>8} {'数量':>4} {'小计':>10}"

# ❌ 避免的做法
# 过度复杂的格式化
bad_example = f"{name:*^{width if width > 10 else 10}.{precision if precision < 5 else 5}}"
```

**记忆要点**：
- **f-string 是王道**：现代Python开发的标准选择
- **格式语法很规律**：掌握基本结构，举一反三
- **实用性第一**：不追求复杂，够用就好
- **代码要清晰**：让别人（包括未来的自己）能看懂

### 10.4 进阶学习方向


**深入掌握**：
- 🎯 **自定义格式化类**：实现 `__format__` 方法
- 🎯 **本地化格式化**：多语言、多地区格式支持
- 🎯 **模板引擎**：Jinja2等模板系统的格式化
- 🎯 **性能优化**：大量数据的格式化性能调优

**核心记忆**：
- 格式化让数据更美观，三种方法各有长
- f-string 性能佳，日常开发是首选
- 语法规律要掌握，对齐精度类型清
- 实用为主不求全，清晰易读是关键