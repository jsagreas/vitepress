---
title: 2、文本样式
---
## 📚 目录

1. [文字颜色设置](#1-文字颜色设置)
2. [文字对齐方式](#2-文字对齐方式)
3. [文字装饰效果](#3-文字装饰效果)
4. [文字省略处理](#4-文字省略处理)
5. [行高与字间距](#5-行高与字间距)
6. [常用文字效果](#6-常用文字效果)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎨 文字颜色设置


### 1.1 color属性基础


**🔸 基本语法**
```css
.text {
    color: red;        /* 设置文字颜色 */
}
```

### 1.2 颜色表示方法


**🌈 常用颜色写法**
```css
/* 方法1：颜色名称 (最简单) */
.text1 { color: red; }
.text2 { color: blue; }
.text3 { color: green; }

/* 方法2：十六进制 (最常用) */
.text4 { color: #ff0000; }  /* 红色 */
.text5 { color: #333333; }  /* 深灰色 */
.text6 { color: #fff; }     /* 白色(简写) */

/* 方法3：RGB值 */
.text7 { color: rgb(255, 0, 0); }     /* 红色 */
.text8 { color: rgba(255, 0, 0, 0.5); } /* 半透明红色 */
```

### 1.3 常用颜色推荐


**📋 实用颜色表**
| 颜色用途 | **十六进制** | **效果** | **使用场景** |
|---------|-------------|---------|-------------|
| 🖤 **主要文字** | `#333` | <span style="color:#333">深灰色</span> | `正文内容` |
| 🔘 **次要文字** | `#666` | <span style="color:#666">中灰色</span> | `辅助信息` |
| ⚪ **提示文字** | `#999` | <span style="color:#999">浅灰色</span> | `占位符、说明` |
| 🔵 **链接文字** | `#007bff` | <span style="color:#007bff">蓝色</span> | `超链接` |
| 🔴 **错误提示** | `#dc3545` | <span style="color:#dc3545">红色</span> | `错误信息` |
| 🟢 **成功提示** | `#28a745` | <span style="color:#28a745">绿色</span> | `成功状态` |

**💻 实际应用示例**
```html
<div class="content">
    <h1>文章标题</h1>           <!-- 使用 #333 -->
    <p class="meta">发布时间：2024-01-01</p>  <!-- 使用 #666 -->
    <p>这是正文内容...</p>       <!-- 使用 #333 -->
    <span class="tip">温馨提示</span>  <!-- 使用 #999 -->
</div>

<style>
.content h1 { color: #333; }
.content .meta { color: #666; }
.content p { color: #333; }
.content .tip { color: #999; }
</style>
```

---

## 2. 📍 文字对齐方式


### 2.1 text-align属性


**🔸 基本语法**
```css
.text {
    text-align: left;    /* 文字对齐方式 */
}
```

### 2.2 对齐方式详解


**↔️ 四种对齐方式**
```css
/* 左对齐 (默认) */
.left-text {
    text-align: left;
}

/* 居中对齐 (最常用) */
.center-text {
    text-align: center;
}

/* 右对齐 */
.right-text {
    text-align: right;
}

/* 两端对齐 */
.justify-text {
    text-align: justify;
}
```

### 2.3 对齐效果演示


**📝 效果对比**
```
左对齐效果：
这是一段左对齐的文字内容
文字从左边开始排列
每行开头都对齐

居中对齐效果：
    这是一段居中对齐的文字内容
      文字在容器中间位置
        常用于标题

右对齐效果：
                  这是一段右对齐的文字内容
                      文字从右边开始排列
                            常用于价格显示
```

### 2.4 实际应用场景


**🎯 使用场景推荐**
```css
/* 文章标题 - 居中 */
.article-title {
    text-align: center;
    font-size: 24px;
    margin-bottom: 20px;
}

/* 价格显示 - 右对齐 */
.price {
    text-align: right;
    color: #e74c3c;
    font-weight: bold;
}

/* 按钮文字 - 居中 */
.button {
    text-align: center;
    padding: 10px 20px;
    background: #007bff;
    color: white;
}
```

---

## 3. ✨ 文字装饰效果


### 3.1 text-decoration属性


**🔸 基本语法**
```css
.text {
    text-decoration: none;    /* 文字装饰 */
}
```

### 3.2 装饰类型详解


**🎨 装饰效果对比**
```css
/* 无装饰 (默认) */
.normal-text {
    text-decoration: none;
}

/* 下划线 */
.underline-text {
    text-decoration: underline;
}

/* 删除线 */
.line-through-text {
    text-decoration: line-through;
}

/* 上划线 (很少用) */
.overline-text {
    text-decoration: overline;
}
```

### 3.3 实际应用场景


**🔗 链接样式处理**
```css
/* 移除链接默认下划线 */
a {
    text-decoration: none;
    color: #007bff;
}

/* 鼠标悬停时显示下划线 */
a:hover {
    text-decoration: underline;
}
```

**💰 价格划线效果**
```css
/* 原价划线 */
.old-price {
    text-decoration: line-through;
    color: #999;
    margin-right: 10px;
}

/* 现价正常显示 */
.new-price {
    color: #e74c3c;
    font-weight: bold;
}
```

**💻 HTML示例**
```html
<div class="price-box">
    <span class="old-price">￥199</span>
    <span class="new-price">￥99</span>
</div>
```

---

## 4. ✂️ 文字省略处理


### 4.1 为什么需要文字省略


**🤔 实际问题**
```
网页布局中经常遇到：
- 文字内容太长，超出容器宽度
- 破坏页面布局的整齐性
- 移动端屏幕空间有限

解决方案：文字超出部分显示省略号(...)
```

### 4.2 单行文字省略


**📝 经典三件套**
```css
.single-line-ellipsis {
    width: 200px;                    /* 1. 限制宽度 */
    white-space: nowrap;             /* 2. 强制不换行 */
    overflow: hidden;                /* 3. 隐藏超出部分 */
    text-overflow: ellipsis;         /* 4. 显示省略号 */
}
```

**🔍 代码详解**
```css
/* 每个属性的作用 */
width: 200px;           /* 必须设置宽度限制 */
white-space: nowrap;    /* 禁止文字换行 */
overflow: hidden;       /* 隐藏超出的文字 */
text-overflow: ellipsis; /* 用...代替超出文字 */
```

### 4.3 多行文字省略


**📱 移动端常用**
```css
.multi-line-ellipsis {
    width: 300px;
    display: -webkit-box;            /* 使用弹性盒模型 */
    -webkit-box-orient: vertical;    /* 垂直排列 */
    -webkit-line-clamp: 3;           /* 显示3行 */
    overflow: hidden;                /* 隐藏超出部分 */
    text-overflow: ellipsis;         /* 显示省略号 */
}
```

### 4.4 省略效果对比


**👀 效果演示**
```html
<!-- 原始长文字 -->
<p class="original">这是一段很长很长的文字内容，在小容器中会显示不完整，需要进行省略处理来保持页面美观</p>

<!-- 单行省略效果 -->
<p class="single-line">这是一段很长很长的文字内容，在小容器中会显示不完整...</p>

<!-- 多行省略效果 -->
<p class="multi-line">这是一段很长很长的文字内容，
在小容器中会显示不完整，需要进行省略
处理来保持页面美观...</p>
```

---

## 5. 📏 行高与字间距


### 5.1 line-height行高设置


**🔸 基本概念**
```
行高 = 文字高度 + 上下空白
作用：控制行与行之间的距离，影响文字的垂直间距
```

**📐 行高设置方法**
```css
/* 方法1：像素值 (固定大小) */
.text1 {
    font-size: 16px;
    line-height: 24px;      /* 固定24px行高 */
}

/* 方法2：倍数 (最推荐) */
.text2 {
    font-size: 16px;
    line-height: 1.5;       /* 字号的1.5倍 = 24px */
}

/* 方法3：百分比 */
.text3 {
    font-size: 16px;
    line-height: 150%;      /* 字号的150% = 24px */
}
```

**💡 推荐用法**
```css
/* 最佳实践：使用倍数 */
body {
    font-size: 16px;
    line-height: 1.6;       /* 行高是字号的1.6倍 */
}

/* 常用行高推荐 */
.title { line-height: 1.2; }    /* 标题紧凑一些 */
.content { line-height: 1.6; }  /* 正文舒适阅读 */
.small { line-height: 1.4; }    /* 小字稍紧凑 */
```

### 5.2 letter-spacing字间距


**🔸 调整字符间距**
```css
/* 字符间距调整 */
.normal { letter-spacing: normal; }     /* 默认 */
.loose { letter-spacing: 2px; }         /* 增加2px间距 */
.tight { letter-spacing: -1px; }        /* 减少1px间距 */
```

**📝 实际应用**
```css
/* 标题字间距放大 */
.logo {
    font-size: 24px;
    letter-spacing: 3px;    /* 让标题更有设计感 */
}

/* 按钮文字间距 */
.button {
    letter-spacing: 1px;    /* 让按钮文字更清晰 */
}
```

---

## 6. 🎭 常用文字效果


### 6.1 文字粗细控制


**🔸 font-weight属性**
```css
/* 文字粗细级别 */
.thin { font-weight: 100; }      /* 极细 */
.light { font-weight: 300; }     /* 细体 */
.normal { font-weight: 400; }    /* 正常 (默认) */
.bold { font-weight: 700; }      /* 粗体 */
.black { font-weight: 900; }     /* 极粗 */

/* 也可以用关键字 */
.bold-text { font-weight: bold; }
.normal-text { font-weight: normal; }
```

### 6.2 文字大小写转换


**🔸 text-transform属性**
```css
/* 大小写控制 */
.uppercase { text-transform: uppercase; }   /* 全部大写 */
.lowercase { text-transform: lowercase; }   /* 全部小写 */
.capitalize { text-transform: capitalize; } /* 首字母大写 */
```

**💻 效果演示**
```html
<p class="uppercase">hello world</p>     <!-- 显示：HELLO WORLD -->
<p class="lowercase">Hello World</p>     <!-- 显示：hello world -->
<p class="capitalize">hello world</p>    <!-- 显示：Hello World -->
```

### 6.3 文字阴影效果


**🔸 text-shadow属性**
```css
.shadow-text {
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    /* 参数：水平偏移 垂直偏移 模糊半径 颜色 */
}
```

**🎨 常用阴影效果**
```css
/* 浅色阴影 */
.light-shadow {
    text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
}

/* 立体效果 */
.3d-text {
    color: #fff;
    text-shadow: 
        1px 1px 0px #ccc,
        2px 2px 0px #c9c9c9,
        3px 3px 0px #bbb;
}

/* 发光效果 */
.glow-text {
    color: #fff;
    text-shadow: 0 0 10px #00f;
}
```

### 6.4 实用文字组合效果


**🎯 常用组合示例**
```css
/* 卡片标题样式 */
.card-title {
    font-size: 18px;
    font-weight: bold;
    color: #333;
    line-height: 1.4;
    margin-bottom: 10px;
}

/* 按钮文字样式 */
.btn-text {
    color: #fff;
    font-weight: 600;
    text-align: center;
    letter-spacing: 1px;
    text-transform: uppercase;
}

/* 价格标签样式 */
.price-tag {
    color: #e74c3c;
    font-size: 20px;
    font-weight: bold;
    text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
}
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的属性


**🎯 核心五件套**
```css
.text-basic {
    color: #333;              /* 文字颜色 */
    text-align: center;       /* 文字对齐 */
    text-decoration: none;    /* 文字装饰 */
    line-height: 1.6;        /* 行高 */
    font-weight: normal;      /* 文字粗细 */
}
```

### 7.2 文字省略万能代码


**📋 单行省略 (背下来)**
```css
.ellipsis {
    width: 200px;                /* 设置宽度 */
    white-space: nowrap;         /* 不换行 */
    overflow: hidden;            /* 隐藏超出 */
    text-overflow: ellipsis;     /* 显示省略号 */
}
```

### 7.3 常用颜色速记


**🎨 颜色记忆口诀**
```
主要文字 #333，次要文字 #666
提示信息 #999，链接蓝色 #007bff
错误红色 #dc3545，成功绿色 #28a745
```

### 7.4 实用技巧汇总


**💡 开发技巧**
```css
/* 技巧1：链接去下划线 */
a { text-decoration: none; }

/* 技巧2：行高用倍数 */
body { line-height: 1.6; }

/* 技巧3：颜色用十六进制 */
.text { color: #333; }

/* 技巧4：文字居中对齐 */
.center { text-align: center; }

/* 技巧5：文字省略三件套 */
.ellipsis {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}
```

**核心记忆**：
- **color设置文字颜色**，#333是万能色
- **text-align控制对齐**，center最常用
- **text-decoration管装饰**，none去下划线
- **文字省略三件套**：nowrap + hidden + ellipsis
- **line-height用倍数**，1.6是黄金比例