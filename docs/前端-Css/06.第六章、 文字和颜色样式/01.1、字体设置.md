---
title: 1、字体设置
---
## 📚 目录

1. [字体大小设置](#1-字体大小设置)
2. [字体家族选择](#2-字体家族选择)
3. [字体粗细控制](#3-字体粗细控制)
4. [行高设置技巧](#4-行高设置技巧)
5. [网页安全字体](#5-网页安全字体)
6. [字体简写属性](#6-字体简写属性)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 📏 字体大小设置


### 1.1 font-size基本用法


**🔸 常用单位说明**
```css
.text {
    font-size: 16px;    /* 像素，最常用 */
    font-size: 1.2em;   /* 相对父元素 */
    font-size: 1.2rem;  /* 相对根元素 */
    font-size: 120%;    /* 百分比 */
}
```

### 1.2 字体大小实用建议


**📊 常见字体大小参考**
```css
/* 网页标准字体大小 */
body { font-size: 16px; }      /* 正文基础大小 */
h1 { font-size: 32px; }        /* 主标题 */
h2 { font-size: 24px; }        /* 二级标题 */
h3 { font-size: 20px; }        /* 三级标题 */
.small { font-size: 12px; }    /* 小字体 */
.large { font-size: 18px; }    /* 大字体 */
```

**💡 移动端字体建议**
```css
/* 移动端最小字体不要小于12px */
.mobile-text {
    font-size: 14px;    /* 移动端推荐最小值 */
}

/* 老人友好字体大小 */
.senior-friendly {
    font-size: 18px;    /* 方便老年人阅读 */
}
```

### 1.3 响应式字体大小


**📱 不同设备的字体适配**
```css
/* 桌面端 */
.title { font-size: 24px; }

/* 平板端 */
@media (max-width: 768px) {
    .title { font-size: 20px; }
}

/* 手机端 */
@media (max-width: 480px) {
    .title { font-size: 18px; }
}
```

---

## 2. 🎨 字体家族选择


### 2.1 font-family基本语法


**🔸 字体设置原理**
```css
.text {
    /* 从左到右依次尝试，找到第一个可用字体 */
    font-family: "Microsoft YaHei", Arial, sans-serif;
}
```

**字体查找流程图示：**
```
用户系统查找字体：
Microsoft YaHei → 有 ✅ 使用
Microsoft YaHei → 无 → Arial → 有 ✅ 使用  
Microsoft YaHei → 无 → Arial → 无 → sans-serif ✅ 浏览器默认
```

### 2.2 中文字体设置


**🇨🇳 常用中文字体**
```css
/* 方案1：微软雅黑优先 */
.chinese-text {
    font-family: "Microsoft YaHei", "微软雅黑", sans-serif;
}

/* 方案2：苹果系统优先 */
.apple-chinese {
    font-family: -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif;
}

/* 方案3：全平台兼容 */
.universal-chinese {
    font-family: 
        -apple-system,           /* 苹果系统字体 */
        BlinkMacSystemFont,      /* Chrome on macOS */
        "Segoe UI",             /* Windows 10 */
        "Microsoft YaHei",       /* Windows 中文 */
        "Helvetica Neue",        /* macOS */
        Arial, sans-serif;       /* 兜底字体 */
}
```

### 2.3 英文字体设置


**🔤 常用英文字体**
```css
/* 无衬线字体 - 现代感强 */
.sans-serif-text {
    font-family: "Helvetica Neue", Arial, sans-serif;
}

/* 有衬线字体 - 传统优雅 */
.serif-text {
    font-family: Georgia, "Times New Roman", serif;
}

/* 等宽字体 - 代码专用 */
.code-text {
    font-family: "Consolas", "Monaco", monospace;
}
```

### 2.4 字体类型对比


| 字体类型 | **特点** | **适用场景** | **示例** |
|---------|---------|-------------|---------|
| 🔤 **sans-serif** | `无衬线，现代` | `网页正文，界面` | `Arial, 微软雅黑` |
| 📰 **serif** | `有衬线，传统` | `报纸，书籍` | `Times, 宋体` |
| 💻 **monospace** | `等宽字体` | `代码，数据` | `Consolas, 等线` |

---

## 3. 💪 字体粗细控制


### 3.1 font-weight常用值


**🔸 数字值vs关键字**
```css
.text {
    font-weight: 100;        /* 最细 */
    font-weight: 400;        /* 正常 = normal */
    font-weight: 700;        /* 加粗 = bold */
    font-weight: 900;        /* 最粗 */
}

/* 关键字写法 */
.text {
    font-weight: normal;     /* 默认粗细 = 400 */
    font-weight: bold;       /* 加粗 = 700 */
    font-weight: lighter;    /* 比父元素更细 */
    font-weight: bolder;     /* 比父元素更粗 */
}
```

### 3.2 实际应用示例


**📝 不同场景的字体粗细**
```css
/* 标题加粗 */
h1, h2, h3 {
    font-weight: bold;       /* 或 700 */
}

/* 正文正常 */
p, div {
    font-weight: normal;     /* 或 400 */
}

/* 强调文字 */
.highlight {
    font-weight: 600;        /* 中等加粗 */
}

/* 次要信息 */
.secondary {
    font-weight: 300;        /* 较细 */
}
```

### 3.3 字体粗细可视化对比


**📊 粗细程度对比**
```
font-weight: 100  →  很细的文字
font-weight: 300  →  较细的文字  
font-weight: 400  →  正常的文字 (默认)
font-weight: 600  →  较粗的文字
font-weight: 700  →  加粗的文字 (bold)
font-weight: 900  →  很粗的文字
```

---

## 4. 📐 行高设置技巧


### 4.1 line-height基本概念


**🔸 什么是行高**
```
行高 = 一行文字占据的总高度
包含：文字高度 + 上下间距

视觉效果：
┌─────────────────┐ ← 行高 line-height
│     上间距       │
│   文字内容       │ ← 字体大小 font-size  
│     下间距       │
└─────────────────┘
```

### 4.2 行高设置方法


**📏 不同单位的行高**
```css
.text {
    font-size: 16px;
    
    line-height: 24px;       /* 固定像素 */
    line-height: 1.5;        /* 倍数，推荐！ */
    line-height: 150%;       /* 百分比 */
    line-height: 1.5em;      /* em单位 */
}
```

**💡 推荐使用倍数的原因**
```css
/* ✅ 推荐：倍数写法，灵活适应 */
.text {
    line-height: 1.5;        /* 自动适应字体大小 */
}

/* ❌ 不推荐：固定像素，不够灵活 */
.text {
    line-height: 24px;       /* 字体变大时行高可能不够 */
}
```

### 4.3 行高实用设置


**📖 不同场景的行高建议**
```css
/* 正文段落 - 舒适阅读 */
p {
    line-height: 1.6;        /* 较松散，易阅读 */
}

/* 标题 - 紧凑排版 */
h1, h2, h3 {
    line-height: 1.2;        /* 较紧密 */
}

/* 按钮文字 - 垂直居中 */
.btn {
    height: 40px;
    line-height: 40px;       /* 等于高度，垂直居中 */
}

/* 代码块 - 清晰对齐 */
pre, code {
    line-height: 1.4;        /* 适中间距 */
}
```

### 4.4 行高的妙用技巧


**🎯 垂直居中技巧**
```css
/* 单行文字垂直居中 */
.center-text {
    height: 50px;
    line-height: 50px;       /* 行高等于容器高度 */
    text-align: center;      /* 水平居中 */
}
```

**📱 移动端行高建议**
```css
/* 移动端触摸友好的行高 */
.mobile-text {
    line-height: 1.8;        /* 较大行高，方便点击 */
}
```

---

## 5. 🛡️ 网页安全字体


### 5.1 什么是网页安全字体


**🔸 定义**
```
网页安全字体：绝大多数用户系统都安装的字体
好处：确保网页在任何设备上都能正常显示
```

### 5.2 Windows系统安全字体


**🪟 Windows常见字体**
```css
/* 中文字体 */
.windows-chinese {
    font-family: 
        "Microsoft YaHei",      /* 微软雅黑 - Win7+ */
        "SimSun",               /* 宋体 - 所有Windows */
        sans-serif;
}

/* 英文字体 */
.windows-english {
    font-family: 
        Arial,                  /* 所有Windows */
        "Times New Roman",      /* 所有Windows */
        "Courier New",          /* 等宽字体 */
        sans-serif;
}
```

### 5.3 macOS系统安全字体


**🍎 macOS常见字体**
```css
/* 中文字体 */
.mac-chinese {
    font-family: 
        "PingFang SC",          /* 苹方 - macOS 10.11+ */
        "Hiragino Sans GB",     /* 冬青黑 - 较老macOS */
        sans-serif;
}

/* 英文字体 */
.mac-english {
    font-family: 
        "Helvetica Neue",       /* macOS默认 */
        Helvetica,              /* 经典Mac字体 */
        Arial, sans-serif;
}
```

### 5.4 跨平台安全字体方案


**🌍 全平台兼容方案**
```css
/* 最佳实践：系统字体优先 */
.system-font {
    font-family: 
        -apple-system,          /* iOS/macOS系统字体 */
        BlinkMacSystemFont,     /* macOS Chrome */
        "Segoe UI",             /* Windows 10 */
        Roboto,                 /* Android */
        "Microsoft YaHei",      /* Windows中文 */
        "Helvetica Neue",       /* macOS英文 */
        Arial, sans-serif;      /* 最终兜底 */
}
```

### 5.5 字体加载失败的兜底策略


**🔧 字体回退机制**
```css
/* 分级回退策略 */
.safe-font {
    font-family: 
        "Custom Font",          /* 自定义字体 */
        "Microsoft YaHei",      /* 首选安全字体 */
        Arial,                  /* 次选字体 */
        sans-serif;             /* 浏览器默认 */
}

/* 避免字体加载失败导致的问题 */
.fallback-font {
    font-family: Arial, sans-serif;  /* 简单可靠 */
    font-display: swap;               /* 快速显示文字 */
}
```

---

## 6. ✏️ 字体简写属性


### 6.1 font简写语法


**🔸 完整语法格式**
```css
/* 标准语法：font: [style] [weight] size[/line-height] family */
.text {
    font: normal bold 16px/1.5 Arial, sans-serif;
}

/* 等价于分开写 */
.text {
    font-style: normal;
    font-weight: bold;
    font-size: 16px;
    line-height: 1.5;
    font-family: Arial, sans-serif;
}
```

### 6.2 简写属性实用示例


**📝 常用简写组合**
```css
/* 标题字体 */
h1 {
    font: bold 24px/1.2 "Microsoft YaHei", sans-serif;
}

/* 正文字体 */
p {
    font: normal 16px/1.6 Arial, sans-serif;
}

/* 小字体 */
.small {
    font: normal 12px/1.4 Arial, sans-serif;
}

/* 代码字体 */
code {
    font: normal 14px/1.4 "Consolas", monospace;
}
```

### 6.3 简写属性注意事项


**⚠️ 使用注意点**
```css
/* ✅ 正确：必须包含size和family */
.good {
    font: 16px Arial;               /* 最简写法 */
    font: bold 16px Arial;          /* 加上weight */
    font: 16px/1.5 Arial;          /* 加上line-height */
}

/* ❌ 错误：缺少必需属性 */
.bad {
    font: bold Arial;               /* 缺少size */
    font: 16px;                     /* 缺少family */
}
```

---

## 7. 📋 核心要点总结


### 7.1 字体设置最佳实践


**🎯 实用建议总结**
```css
/* 万能字体方案 */
body {
    font: normal 16px/1.6 
          -apple-system, BlinkMacSystemFont,
          "Segoe UI", "Microsoft YaHei", 
          Arial, sans-serif;
}

/* 标题优化 */
h1, h2, h3 {
    font-weight: bold;
    line-height: 1.2;
}

/* 代码字体 */
code, pre {
    font-family: "Consolas", "Monaco", monospace;
}
```

### 7.2 字体大小选择指南


**📏 推荐字体大小**
```
移动端：
- 正文：14px-16px
- 标题：18px-24px  
- 小字：12px以上

桌面端：
- 正文：16px-18px
- 标题：24px-32px
- 小字：12px-14px
```

### 7.3 行高设置规律


**📐 行高推荐值**
```
文本类型          推荐行高
正文段落          1.6 - 1.8
标题             1.2 - 1.3  
按钮文字          1.0 (等于高度)
代码文字          1.4 - 1.5
```

### 7.4 常见问题快速解决


**🔧 问题排查清单**
```
字体不显示：
1. 检查字体名称拼写
2. 检查是否有兜底字体
3. 使用开发者工具查看实际字体

字体太小/太大：
1. 检查font-size设置
2. 检查是否有媒体查询覆盖
3. 确认单位使用是否正确

行高不合适：
1. 使用倍数而不是固定像素
2. 正文用1.6，标题用1.2
3. 按钮行高等于容器高度
```

**核心记忆**：
- **字体大小16px起步**，移动端不小于12px
- **字体家族要兜底**，系统字体最保险  
- **行高用倍数最灵活**，正文1.6标题1.2
- **font简写要谨慎**，size和family不能少