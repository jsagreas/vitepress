---
title: 3、伪类和伪元素
---
## 📚 目录

1. [伪类与伪元素基本概念](#1-伪类与伪元素基本概念)
2. [交互状态伪类](#2-交互状态伪类)
3. [结构性伪类](#3-结构性伪类)
4. [伪元素详解](#4-伪元素详解)
5. [实际应用场景](#5-实际应用场景)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎭 伪类与伪元素基本概念


### 1.1 什么是伪类和伪元素


**🔸 伪类（Pseudo-class）**
```
定义：选择器的特殊状态，表示元素的某种状态
语法：选择器:伪类名
作用：给元素的不同状态添加样式
```

**🔸 伪元素（Pseudo-element）**
```
定义：创建虚拟的页面元素，实际HTML中不存在
语法：选择器::伪元素名 (CSS3规范，也可用单冒号)
作用：插入内容或装饰元素
```

### 1.2 区别和联系


**📊 伪类 vs 伪元素对比表**

| 类型 | **语法** | **作用** | **举例** | **实际效果** |
|------|---------|---------|---------|-------------|
| 🎯 **伪类** | `单冒号 :` | `选择元素状态` | `:hover` | `鼠标悬停时的样式` |
| 🎨 **伪元素** | `双冒号 ::` | `创建虚拟元素` | `::before` | `在元素前插入内容` |

**💡 简单理解**
```css
/* 伪类：给现有元素的某种状态加样式 */
button:hover { background: blue; }  /* 按钮悬停时变蓝 */

/* 伪元素：创建一个新的虚拟元素 */
.box::before { content: "★"; }     /* 在box前面加个星号 */
```

---

## 2. 🖱️ 交互状态伪类


### 2.1 鼠标交互伪类


**🔸 :hover - 鼠标悬停**
```css
/* 最常用的交互效果 */
.button {
    background: #3498db;
    transition: all 0.3s;  /* 平滑过渡 */
}

.button:hover {
    background: #2980b9;   /* 鼠标悬停时变深蓝 */
    transform: scale(1.05); /* 轻微放大 */
}
```

**🔸 :active - 鼠标按下**
```css
.button:active {
    background: #1f618d;   /* 按下时更深的蓝色 */
    transform: scale(0.95); /* 按下时缩小 */
}
```

**🔸 :focus - 获得焦点**
```css
/* 主要用于表单元素 */
input:focus {
    border-color: #3498db;
    outline: none;          /* 去掉默认的蓝色轮廓 */
    box-shadow: 0 0 5px rgba(52, 152, 219, 0.5);
}

textarea:focus {
    border-color: #e74c3c;
}
```

### 2.2 交互状态组合使用


**⚡ 完整的按钮交互示例**
```css
.modern-button {
    padding: 12px 24px;
    background: #3498db;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.2s ease;
}

/* 悬停效果 */
.modern-button:hover {
    background: #2980b9;
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}

/* 按下效果 */
.modern-button:active {
    background: #1f618d;
    transform: translateY(1px);  /* 按下时下移1px */
}

/* 获得焦点效果 */
.modern-button:focus {
    outline: 2px solid #74b9ff;
    outline-offset: 2px;
}
```

---

## 3. 🏗️ 结构性伪类


### 3.1 子元素位置伪类


**🔸 :first-child - 第一个子元素**
```css
/* 选中第一个子元素 */
.menu li:first-child {
    border-top: none;       /* 第一个不要上边框 */
    margin-top: 0;
}

/* 实际应用：导航菜单第一项特殊样式 */
.nav-item:first-child {
    border-radius: 5px 0 0 5px;  /* 左侧圆角 */
}
```

**🔸 :last-child - 最后一个子元素**
```css
/* 选中最后一个子元素 */
.menu li:last-child {
    border-bottom: none;    /* 最后一个不要下边框 */
    margin-bottom: 0;
}

/* 实际应用：卡片列表最后一项不要下边距 */
.card:last-child {
    margin-bottom: 0;
}
```

**🔸 :nth-child(n) - 第n个子元素**
```css
/* 选择奇数行 - 斑马纹效果 */
.table tr:nth-child(odd) {
    background: #f8f9fa;
}

/* 选择偶数行 */
.table tr:nth-child(even) {
    background: white;
}

/* 选择前3个 */
.item:nth-child(-n+3) {
    color: red;
}

/* 选择第3个开始的所有元素 */
.item:nth-child(n+3) {
    color: blue;
}
```

### 3.2 nth-child常用公式


**📊 nth-child公式表**

| 公式 | **选择范围** | **实际应用** | **说明** |
|------|-------------|-------------|---------|
| `odd` 或 `2n+1` | `1,3,5,7...` | `表格斑马纹` | `奇数行` |
| `even` 或 `2n` | `2,4,6,8...` | `表格斑马纹` | `偶数行` |
| `3n` | `3,6,9,12...` | `三列布局` | `每3个一组` |
| `-n+3` | `1,2,3` | `限制显示数量` | `前3个` |
| `n+4` | `4,5,6,7...` | `跳过前几个` | `第4个开始` |

**💻 实际应用示例**
```css
/* 三列卡片布局，每行最后一个不要右边距 */
.card:nth-child(3n) {
    margin-right: 0;
}

/* 新闻列表，前3条新闻标题红色 */
.news-item:nth-child(-n+3) .title {
    color: #e74c3c;
}
```

---

## 4. 🎨 伪元素详解


### 4.1 ::before 和 ::after 基础


**🔸 基本语法要求**
```css
.element::before {
    content: "";         /* 必须属性，不能省略 */
    display: block;      /* 设置显示方式 */
    /* 其他样式属性 */
}

.element::after {
    content: "★";        /* 可以是文字、空字符串等 */
    position: absolute;  /* 常用定位 */
    /* 其他样式属性 */
}
```

**⚠️ 重要提醒**
```
1. content属性是必须的，没有content伪元素不会显示
2. 伪元素默认是inline，通常需要设置display
3. 伪元素在HTML中不存在，只能用CSS控制
```

### 4.2 ::before 实际应用


**🔸 添加装饰图标**
```css
/* 在链接前添加图标 */
.download-link::before {
    content: "⬇";
    margin-right: 5px;
    color: #3498db;
}

/* 在重要提示前添加警告图标 */
.warning::before {
    content: "⚠";
    color: #f39c12;
    font-weight: bold;
    margin-right: 8px;
}
```

**🔸 创建几何图形**
```css
/* 创建三角形箭头 */
.arrow::before {
    content: "";
    position: absolute;
    left: -10px;
    top: 50%;
    transform: translateY(-50%);
    width: 0;
    height: 0;
    border: 5px solid transparent;
    border-right-color: #3498db;
}
```

### 4.3 ::after 实际应用


**🔸 清除浮动（经典用法）**
```css
.clearfix::after {
    content: "";
    display: table;
    clear: both;
}
```

**🔸 创建遮罩层**
```css
.image-overlay {
    position: relative;
}

.image-overlay::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0,0,0,0.5);  /* 半透明黑色遮罩 */
    opacity: 0;
    transition: opacity 0.3s;
}

.image-overlay:hover::after {
    opacity: 1;  /* 悬停时显示遮罩 */
}
```

### 4.4 伪元素组合使用


**⭐ 创建复杂装饰效果**
```css
.fancy-box {
    position: relative;
    padding: 20px;
    background: white;
}

/* 左上角装饰 */
.fancy-box::before {
    content: "";
    position: absolute;
    top: -5px;
    left: -5px;
    width: 20px;
    height: 20px;
    background: #3498db;
    border-radius: 50%;
}

/* 右下角装饰 */
.fancy-box::after {
    content: "";
    position: absolute;
    bottom: -5px;
    right: -5px;
    width: 20px;
    height: 20px;
    background: #e74c3c;
    border-radius: 50%;
}
```

---

## 5. 🚀 实际应用场景


### 5.1 网站导航菜单


**🔸 响应式导航栏**
```css
.nav {
    display: flex;
}

.nav-item {
    padding: 10px 20px;
    transition: all 0.3s;
}

/* 悬停效果 */
.nav-item:hover {
    background: #ecf0f1;
    transform: translateY(-2px);
}

/* 当前页面高亮 */
.nav-item.active {
    background: #3498db;
    color: white;
}

/* 第一个和最后一个圆角 */
.nav-item:first-child {
    border-radius: 5px 0 0 5px;
}

.nav-item:last-child {
    border-radius: 0 5px 5px 0;
}
```

### 5.2 卡片组件设计


**🔸 现代卡片效果**
```css
.card {
    background: white;
    border-radius: 8px;
    padding: 20px;
    margin: 10px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}

/* 悬停时卡片提升 */
.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.15);
}

/* 卡片左侧彩色边条 */
.card::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 4px;
    background: linear-gradient(to bottom, #3498db, #2ecc71);
}

/* 卡片标题前的小圆点 */
.card-title::before {
    content: "●";
    color: #3498db;
    margin-right: 8px;
}
```

### 5.3 表单增强体验


**🔸 现代表单设计**
```css
.form-group {
    margin-bottom: 20px;
    position: relative;
}

.form-input {
    width: 100%;
    padding: 12px;
    border: 2px solid #ddd;
    border-radius: 4px;
    transition: all 0.3s;
}

/* 获得焦点时的效果 */
.form-input:focus {
    border-color: #3498db;
    box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
}

/* 必填字段标记 */
.required::after {
    content: " *";
    color: #e74c3c;
    font-weight: bold;
}

/* 输入验证成功 */
.form-input.valid {
    border-color: #2ecc71;
}

.form-input.valid::after {
    content: "✓";
    position: absolute;
    right: 10px;
    color: #2ecc71;
}
```

### 5.4 内容列表美化


**🔸 文章列表设计**
```css
.article-list {
    list-style: none;
    padding: 0;
}

.article-item {
    padding: 15px;
    border-bottom: 1px solid #eee;
    transition: background 0.2s;
}

/* 悬停高亮 */
.article-item:hover {
    background: #f8f9fa;
}

/* 最后一项不要下边框 */
.article-item:last-child {
    border-bottom: none;
}

/* 奇偶行不同背景 */
.article-item:nth-child(even) {
    background: #fafafa;
}

/* 文章序号 */
.article-item::before {
    content: counter(article-counter);
    counter-increment: article-counter;
    display: inline-block;
    width: 30px;
    height: 30px;
    line-height: 30px;
    text-align: center;
    background: #3498db;
    color: white;
    border-radius: 50%;
    margin-right: 15px;
    font-size: 12px;
}

/* 初始化计数器 */
.article-list {
    counter-reset: article-counter;
}
```

---

## 6. 📋 核心要点总结


### 6.1 伪类使用要点


**🎯 交互伪类记忆**
```
:hover   → 鼠标悬停（最常用）
:active  → 鼠标按下（瞬间效果）  
:focus   → 获得焦点（主要用于表单）
```

**🏗️ 结构伪类记忆**
```
:first-child  → 老大（第一个）
:last-child   → 老幺（最后一个）
:nth-child(n) → 排队的第n个
```

### 6.2 伪元素使用要点


**📐 伪元素三要素**
```
1. content属性是必须的（不能省略）
2. 通常需要设置display属性  
3. 需要定位时设置position属性
```

**🎨 伪元素常用场景**
```
::before → 添加图标、装饰、几何图形
::after  → 清除浮动、遮罩层、装饰
```

### 6.3 实战开发建议


**💡 最佳实践**
```css
/* ✅ 推荐：添加过渡效果让交互更流畅 */
.button {
    transition: all 0.3s ease;
}

.button:hover {
    background: #2980b9;
}

/* ✅ 推荐：伪元素记得设置content */
.icon::before {
    content: "";  /* 必须有 */
    display: block;
}

/* ❌ 避免：过度使用伪类嵌套 */
.menu li:first-child:hover:active {
    /* 太复杂了 */
}
```

### 6.4 兼容性说明


**🌐 浏览器支持**
```
伪类：所有现代浏览器都支持
伪元素：
- ::before/::after 所有现代浏览器支持
- 旧版IE需要用单冒号 :before/:after
```

### 6.5 常见问题解决


**🚫 伪元素不显示的原因**
```
1. 忘记写content属性
2. content为空但没有设置宽高
3. 父元素设置了overflow:hidden
4. 伪元素被其他元素遮挡
```

**🔧 调试技巧**
```css
/* 调试伪元素：给个明显的背景色 */
.element::before {
    content: "";
    background: red;  /* 调试用，确认伪元素存在 */
}
```

**核心记忆**：
- **伪类选状态**（:hover悬停，:focus焦点，:first-child老大）
- **伪元素造元素**（::before前面加，::after后面补）
- **content属性必须有**，伪元素才能显示
- **交互加过渡**，用户体验更流畅