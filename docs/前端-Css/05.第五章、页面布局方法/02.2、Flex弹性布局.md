---
title: 2、Flex弹性布局
---
## 📚 目录

1. [Flex布局是什么](#1-Flex布局是什么)
2. [Flex容器基本设置](#2-Flex容器基本设置)
3. [主轴对齐控制](#3-主轴对齐控制)
4. [交叉轴对齐控制](#4-交叉轴对齐控制)
5. [排列方向控制](#5-排列方向控制)
6. [Flex项目属性](#6-Flex项目属性)
7. [常见布局案例](#7-常见布局案例)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🤔 Flex布局是什么


### 1.1 为什么需要Flex布局


**😭 传统布局的痛点**
```css
/* 传统方式实现水平居中 */
.container {
    text-align: center;      /* 只能居中文字 */
}

.box {
    margin: 0 auto;          /* 只能居中块级元素 */
    width: 200px;            /* 必须设置宽度 */
}

/* 垂直居中更麻烦 */
.parent {
    position: relative;
    height: 300px;
}
.child {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);  /* 复杂的代码 */
}
```

**😊 Flex布局的简单**
```css
/* Flex方式实现水平垂直居中 */
.container {
    display: flex;
    justify-content: center;  /* 水平居中 */
    align-items: center;      /* 垂直居中 */
}
/* 就这么简单！ */
```

### 1.2 Flex布局基本概念


**🔸 核心思想**
```
Flex = Flexible 弹性的
把容器变成"弹性盒子"，子元素自动排列
就像一个有弹性的盒子，能自动调整子元素的位置和大小
```

**📊 Flex布局结构图**
```
┌─────────────────────────────────┐ ← Flex容器(父元素)
│  ┌───┐  ┌───┐  ┌───┐  ┌───┐    │
│  │ 1 │  │ 2 │  │ 3 │  │ 4 │    │ ← Flex项目(子元素)
│  └───┘  └───┘  └───┘  └───┘    │
└─────────────────────────────────┘
    ←────── 主轴(main axis) ────→
```

**🎯 两个核心角色**
- **Flex容器**：设置了`display: flex`的父元素
- **Flex项目**：Flex容器的直接子元素

---

## 2. 📦 Flex容器基本设置


### 2.1 开启Flex布局


**⚡ 基本语法**
```css
.container {
    display: flex;        /* 开启Flex布局 */
}

/* 或者 */
.container {
    display: inline-flex; /* 行内Flex容器 */
}
```

### 2.2 Flex轴线概念


**📏 主轴与交叉轴**
```
默认情况：
┌─────────────────────────────────┐
│  ┌───┐  ┌───┐  ┌───┐            │
│  │ 1 │  │ 2 │  │ 3 │            │ ← 交叉轴
│  └───┘  └───┘  └───┘            │   (cross axis)
└─────────────────────────────────┘
    ←────── 主轴(main axis) ────→

主轴：Flex项目排列的方向 (默认水平)
交叉轴：垂直于主轴的方向 (默认垂直)
```

### 2.3 立即看到效果


**💻 快速体验代码**
```html
<!DOCTYPE html>
<html>
<head>
<style>
.container {
    display: flex;           /* 开启Flex */
    background: #f0f0f0;
    height: 200px;
}

.item {
    background: #4CAF50;
    color: white;
    padding: 20px;
    margin: 5px;
}
</style>
</head>
<body>
    <div class="container">
        <div class="item">项目1</div>
        <div class="item">项目2</div>
        <div class="item">项目3</div>
    </div>
</body>
</html>
```

**🎉 效果对比**
```
没有Flex时：3个div垂直排列(块级元素特性)
有了Flex后：3个div水平排列(自动变成弹性项目)
```

---

## 3. ↔️ 主轴对齐控制


### 3.1 justify-content属性详解


**🔸 控制主轴对齐方式**
```css
.container {
    display: flex;
    justify-content: ?;  /* 主轴对齐方式 */
}
```

### 3.2 六种对齐方式图解


**📊 对齐方式可视化**
```
flex-start (默认) - 左对齐
┌─────────────────────────────────┐
│ [1][2][3]                       │
└─────────────────────────────────┘

flex-end - 右对齐  
┌─────────────────────────────────┐
│                       [1][2][3] │
└─────────────────────────────────┘

center - 居中对齐
┌─────────────────────────────────┐
│           [1][2][3]             │
└─────────────────────────────────┘

space-between - 两端对齐
┌─────────────────────────────────┐
│ [1]           [2]           [3] │
└─────────────────────────────────┘

space-around - 环绕对齐
┌─────────────────────────────────┐
│  [1]     [2]     [3]            │
└─────────────────────────────────┘

space-evenly - 平均分布
┌─────────────────────────────────┐
│   [1]     [2]     [3]           │
└─────────────────────────────────┘
```

### 3.3 实际应用示例


**💻 导航菜单布局**
```css
/* 导航菜单居中 */
.nav {
    display: flex;
    justify-content: center;  /* 菜单居中 */
}

/* 导航菜单两端对齐 */
.header {
    display: flex;
    justify-content: space-between;  /* LOGO左边，菜单右边 */
}
```

```html
<div class="header">
    <div class="logo">LOGO</div>
    <nav class="nav">
        <a href="#">首页</a>
        <a href="#">产品</a>
        <a href="#">关于</a>
    </nav>
</div>
```

---

## 4. ↕️ 交叉轴对齐控制


### 4.1 align-items属性详解


**🔸 控制交叉轴对齐方式**
```css
.container {
    display: flex;
    align-items: ?;  /* 交叉轴对齐方式 */
    height: 200px;   /* 容器需要有高度才能看到效果 */
}
```

### 4.2 五种对齐方式图解


**📊 垂直对齐可视化**
```
flex-start - 顶部对齐
┌─────────────────┐
│ ┌─┐ ┌─┐ ┌─┐     │
│ │1│ │2│ │3│     │
│ └─┘ └─┘ └─┘     │
│                 │
│                 │
└─────────────────┘

flex-end - 底部对齐
┌─────────────────┐
│                 │
│                 │
│ ┌─┐ ┌─┐ ┌─┐     │
│ │1│ │2│ │3│     │
│ └─┘ └─┘ └─┘     │
└─────────────────┘

center - 垂直居中
┌─────────────────┐
│                 │
│ ┌─┐ ┌─┐ ┌─┐     │
│ │1│ │2│ │3│     │
│ └─┘ └─┘ └─┘     │
│                 │
└─────────────────┘

stretch - 拉伸填满(默认)
┌─────────────────┐
│ ┌─┐ ┌─┐ ┌─┐     │
│ │1│ │2│ │3│     │
│ │ │ │ │ │ │     │
│ │ │ │ │ │ │     │
│ └─┘ └─┘ └─┘     │
└─────────────────┘

baseline - 基线对齐
┌─────────────────┐
│     ┌─┐         │
│ ┌─┐ │2│ ┌─┐     │
│ │1│ │ │ │3│     │
│ └─┘ └─┘ └─┘     │
└─────────────────┘
```

### 4.3 万能居中技巧


**⭐ 水平垂直居中神器**
```css
.center-container {
    display: flex;
    justify-content: center;  /* 水平居中 */
    align-items: center;      /* 垂直居中 */
    height: 100vh;           /* 全屏高度 */
}

.center-box {
    /* 不需要设置任何定位和宽高 */
    /* 自动居中 */
}
```

```html
<div class="center-container">
    <div class="center-box">我在正中间！</div>
</div>
```

---

## 5. 🔄 排列方向控制


### 5.1 flex-direction属性


**🔸 改变主轴方向**
```css
.container {
    display: flex;
    flex-direction: ?;  /* 排列方向 */
}
```

### 5.2 四种排列方向


**📊 方向对比图**
```
row (默认) - 水平从左到右
┌─────────────────────────────────┐
│ [1] → [2] → [3]                 │
└─────────────────────────────────┘

row-reverse - 水平从右到左
┌─────────────────────────────────┐
│                 [3] ← [2] ← [1] │
└─────────────────────────────────┘

column - 垂直从上到下
┌─────────┐
│   [1]   │
│    ↓    │
│   [2]   │
│    ↓    │
│   [3]   │
└─────────┘

column-reverse - 垂直从下到上
┌─────────┐
│   [3]   │
│    ↑    │
│   [2]   │
│    ↑    │
│   [1]   │
└─────────┘
```

### 5.3 实际应用场景


**💻 手机端垂直导航**
```css
/* 移动端垂直菜单 */
.mobile-nav {
    display: flex;
    flex-direction: column;  /* 垂直排列 */
    height: 100vh;
}

.nav-item {
    flex: 1;  /* 平均分配高度 */
    text-align: center;
}
```

**💻 卡片翻转效果**
```css
.card-list {
    display: flex;
    flex-direction: row-reverse;  /* 从右到左排列 */
}
```

---

## 6. 🎯 Flex项目属性


### 6.1 flex-grow 扩展比例


**🔸 控制项目放大比例**
```css
.item {
    flex-grow: 1;  /* 扩展比例，默认0不扩展 */
}
```

**📊 扩展效果图解**
```
三个项目，flex-grow都是1：
┌─────────────────────────────────┐
│ ┌─────┐ ┌─────┐ ┌─────┐         │  平均分配剩余空间
│ │  1  │ │  2  │ │  3  │         │
│ └─────┘ └─────┘ └─────┘         │
└─────────────────────────────────┘

第二个项目flex-grow是2：
┌─────────────────────────────────┐
│ ┌───┐ ┌─────────┐ ┌───┐         │  项目2占用更多空间
│ │ 1 │ │    2    │ │ 3 │         │
│ └───┘ └─────────┘ └───┘         │
└─────────────────────────────────┘
```

### 6.2 flex-shrink 收缩比例


**🔸 控制项目缩小比例**
```css
.item {
    flex-shrink: 1;  /* 收缩比例，默认1允许收缩 */
}

.no-shrink {
    flex-shrink: 0;  /* 不允许收缩 */
}
```

### 6.3 flex 简写属性


**⚡ 最常用的flex简写**
```css
.item {
    flex: 1;  /* 等同于 flex-grow: 1; flex-shrink: 1; flex-basis: 0; */
}

/* 其他常用值 */
.item {
    flex: none;  /* 不扩展不收缩 */
    flex: auto;  /* 自动扩展收缩 */
}
```

**💡 实际应用**
```css
/* 三栏布局：侧边栏固定，主内容自适应 */
.layout {
    display: flex;
    height: 100vh;
}

.sidebar {
    width: 200px;    /* 固定宽度 */
    flex-shrink: 0;  /* 不收缩 */
}

.main {
    flex: 1;         /* 占用剩余空间 */
}
```

---

## 7. 🎨 常见布局案例


### 7.1 经典三栏布局


**💻 左右固定，中间自适应**
```html
<div class="layout">
    <aside class="sidebar">侧边栏</aside>
    <main class="content">主内容区</main>
    <aside class="ads">广告栏</aside>
</div>
```

```css
.layout {
    display: flex;
    min-height: 100vh;
}

.sidebar, .ads {
    width: 200px;
    background: #f0f0f0;
    flex-shrink: 0;  /* 不收缩 */
}

.content {
    flex: 1;         /* 占用剩余空间 */
    background: white;
    padding: 20px;
}
```

### 7.2 均分布局


**💻 等宽卡片布局**
```html
<div class="card-container">
    <div class="card">卡片1</div>
    <div class="card">卡片2</div>
    <div class="card">卡片3</div>
    <div class="card">卡片4</div>
</div>
```

```css
.card-container {
    display: flex;
    gap: 20px;      /* 卡片间距 */
}

.card {
    flex: 1;        /* 等宽分布 */
    padding: 20px;
    background: #4CAF50;
    color: white;
    text-align: center;
    border-radius: 8px;
}
```

### 7.3 底部固定布局


**💻 页脚始终在底部**
```html
<div class="page">
    <header class="header">页头</header>
    <main class="main">主内容</main>
    <footer class="footer">页脚</footer>
</div>
```

```css
.page {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

.header, .footer {
    background: #333;
    color: white;
    padding: 20px;
    flex-shrink: 0;  /* 固定高度 */
}

.main {
    flex: 1;         /* 占用剩余空间 */
    padding: 20px;
}
```

### 7.4 响应式导航


**💻 移动端自适应菜单**
```html
<nav class="nav">
    <div class="logo">LOGO</div>
    <div class="menu">
        <a href="#">首页</a>
        <a href="#">产品</a>
        <a href="#">服务</a>
        <a href="#">联系</a>
    </div>
</nav>
```

```css
.nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    background: #333;
}

.logo {
    color: white;
    font-size: 20px;
    font-weight: bold;
}

.menu {
    display: flex;
    gap: 20px;
}

.menu a {
    color: white;
    text-decoration: none;
    padding: 10px 15px;
    border-radius: 4px;
    transition: background 0.3s;
}

.menu a:hover {
    background: #555;
}

/* 移动端垂直排列 */
@media (max-width: 768px) {
    .nav {
        flex-direction: column;
        gap: 10px;
    }
    
    .menu {
        width: 100%;
        justify-content: center;
    }
}
```

### 7.5 居中登录框


**💻 完美居中的登录表单**
```html
<div class="login-page">
    <form class="login-form">
        <h2>用户登录</h2>
        <input type="text" placeholder="用户名">
        <input type="password" placeholder="密码">
        <button type="submit">登录</button>
    </form>
</div>
```

```css
.login-page {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.login-form {
    display: flex;
    flex-direction: column;
    gap: 15px;
    padding: 40px;
    background: white;
    border-radius: 10px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    min-width: 300px;
}

.login-form h2 {
    text-align: center;
    margin-bottom: 20px;
    color: #333;
}

.login-form input, .login-form button {
    padding: 12px;
    border: 1px solid #ddd;
    border-radius: 6px;
    font-size: 16px;
}

.login-form button {
    background: #4CAF50;
    color: white;
    border: none;
    cursor: pointer;
}
```

---

## 8. 📋 核心要点总结


### 8.1 Flex布局思维模式


**🧠 理解关键**
```
1. Flex容器 = 弹性盒子 (父元素设置display: flex)
2. Flex项目 = 盒子里的物品 (子元素自动变弹性)
3. 主轴 = 排列方向 (默认水平)
4. 交叉轴 = 垂直于主轴 (默认垂直)
```

### 8.2 必记属性速查表


| **属性** | **作用** | **常用值** | **记忆技巧** |
|---------|---------|-----------|-------------|
| `display: flex` | 开启Flex布局 | `flex` | 万物的开始 |
| `justify-content` | 主轴对齐 | `center`, `space-between` | justify=调整 |
| `align-items` | 交叉轴对齐 | `center`, `stretch` | align=对齐 |
| `flex-direction` | 排列方向 | `row`, `column` | direction=方向 |
| `flex` | 项目伸缩 | `1`, `none` | 分配剩余空间 |

### 8.3 万能布局公式


**⭐ 居中布局万能公式**
```css
.container {
    display: flex;
    justify-content: center;  /* 水平居中 */
    align-items: center;      /* 垂直居中 */
}
```

**⭐ 自适应布局万能公式**
```css
.container {
    display: flex;
}
.fixed {
    width: 200px;      /* 固定宽度 */
    flex-shrink: 0;    /* 不收缩 */
}
.flexible {
    flex: 1;           /* 占剩余空间 */
}
```

### 8.4 快速记忆口诀


**🎵 Flex布局顺口溜**
```
display flex是起点，容器项目要分清
justify主轴来对齐，align交叉不能停  
direction改变方向性，flex属性管伸缩
center居中最常用，space-between两端行
```

### 8.5 实战开发建议


**💡 使用技巧**
1. **优先考虑Flex**：现代布局首选Flex，简单直观
2. **容器项目分清**：搞清楚谁是容器谁是项目
3. **axis轴线概念**：理解主轴和交叉轴的方向
4. **gap替代margin**：用`gap`属性设置间距更方便
5. **移动端友好**：Flex天生适合响应式布局

**⚠️ 注意事项**
- Flex项目的`float`、`clear`、`vertical-align`会失效
- 注意浏览器兼容性（IE10+支持）
- 避免过度嵌套Flex容器

**核心记忆**：
- **display: flex** 开启弹性布局
- **justify-content** 控制水平方向
- **align-items** 控制垂直方向  
- **flex: 1** 让元素占满剩余空间
- Flex是现代CSS布局的**首选方案**