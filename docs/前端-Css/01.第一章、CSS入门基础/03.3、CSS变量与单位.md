---
title: 3、CSS变量与单位
---
## 📚 目录

1. [CSS变量基础](#1-CSS变量基础)
2. [CSS变量实战应用](#2-CSS变量实战应用)
3. [CSS单位详解](#3-CSS单位详解)
4. [单位选择指南](#4-单位选择指南)
5. [响应式单位](#5-响应式单位)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎨 CSS变量基础


### 1.1 什么是CSS变量


**🔸 简单理解**
```
CSS变量 = 可以重复使用的值
就像编程语言中的变量一样，定义一次，到处使用
```

**💡 为什么要用CSS变量**
```
传统方式的问题：
/* 如果要改主题色，需要找到所有地方一个个修改 */
.header { background: #3498db; }
.button { background: #3498db; }
.link { color: #3498db; }

CSS变量的优势：
/* 定义一次，修改一个地方，全站生效 */
:root { --main-color: #3498db; }
.header { background: var(--main-color); }
.button { background: var(--main-color); }
.link { color: var(--main-color); }
```

### 1.2 CSS变量语法


**📝 基本语法格式**
```css
/* 第一步：定义变量 */
:root {
    --变量名: 变量值;
}

/* 第二步：使用变量 */
.元素 {
    属性: var(--变量名);
}
```

**🔧 实际示例**
```css
/* 定义变量 */
:root {
    --primary-color: #007bff;
    --text-size: 16px;
    --spacing: 20px;
}

/* 使用变量 */
.button {
    background: var(--primary-color);
    font-size: var(--text-size);
    padding: var(--spacing);
}
```

### 1.3 :root的作用


**🌟 :root详解**
```css
/* :root = html元素，但权重更高 */
:root {
    --main-color: blue;  /* 全局变量 */
}

/* 等同于，但:root权重更高 */
html {
    --main-color: blue;
}
```

**🔍 变量作用域**
```css
/* 全局变量 - 整个网站都能用 */
:root {
    --global-color: red;
}

/* 局部变量 - 只在.card内部生效 */
.card {
    --local-color: blue;
}

.card .title {
    color: var(--local-color);  /* 可以用 */
}

.header {
    color: var(--local-color);  /* 用不了，会失效 */
}
```

---

## 2. 🛠️ CSS变量实战应用


### 2.1 主题切换实战


**🎨 亮色/暗色主题**
```css
/* 定义主题变量 */
:root {
    /* 亮色主题 */
    --bg-color: #ffffff;
    --text-color: #333333;
    --border-color: #e1e1e1;
}

/* 暗色主题 */
[data-theme="dark"] {
    --bg-color: #1a1a1a;
    --text-color: #ffffff;
    --border-color: #404040;
}

/* 使用主题变量 */
body {
    background: var(--bg-color);
    color: var(--text-color);
}

.card {
    border: 1px solid var(--border-color);
}
```

**⚡ JavaScript切换主题**
```javascript
// 切换主题的简单方法
function toggleTheme() {
    const body = document.body;
    const currentTheme = body.getAttribute('data-theme');
    
    if (currentTheme === 'dark') {
        body.setAttribute('data-theme', 'light');
    } else {
        body.setAttribute('data-theme', 'dark');
    }
}
```

### 2.2 组件化设计


**🧩 按钮组件变量**
```css
/* 定义按钮组件的变量 */
.btn {
    /* 默认变量值 */
    --btn-bg: #007bff;
    --btn-color: white;
    --btn-padding: 10px 20px;
    --btn-radius: 4px;
    
    /* 使用变量 */
    background: var(--btn-bg);
    color: var(--btn-color);
    padding: var(--btn-padding);
    border-radius: var(--btn-radius);
    border: none;
    cursor: pointer;
}

/* 不同类型的按钮 */
.btn-danger {
    --btn-bg: #dc3545;
}

.btn-success {
    --btn-bg: #28a745;
}

.btn-large {
    --btn-padding: 15px 30px;
}
```

### 2.3 响应式变量


**📱 根据屏幕大小调整变量**
```css
:root {
    --container-width: 1200px;
    --font-size: 16px;
    --spacing: 20px;
}

/* 平板尺寸 */
@media (max-width: 768px) {
    :root {
        --container-width: 100%;
        --font-size: 14px;
        --spacing: 15px;
    }
}

/* 手机尺寸 */
@media (max-width: 480px) {
    :root {
        --font-size: 12px;
        --spacing: 10px;
    }
}

/* 使用响应式变量 */
.container {
    max-width: var(--container-width);
    padding: var(--spacing);
    font-size: var(--font-size);
}
```

---

## 3. 📏 CSS单位详解


### 3.1 绝对单位 vs 相对单位


**🔸 单位分类图示**
```
CSS单位
├── 绝对单位 (固定大小)
│   ├── px (像素) ⭐⭐⭐⭐⭐
│   ├── pt (磅)   ⭐
│   └── cm (厘米) ⭐
│
└── 相对单位 (会变化的大小)
    ├── em (相对父元素) ⭐⭐⭐⭐
    ├── rem (相对根元素) ⭐⭐⭐⭐⭐
    ├── % (百分比) ⭐⭐⭐⭐⭐
    ├── vw (视口宽度) ⭐⭐⭐⭐
    └── vh (视口高度) ⭐⭐⭐⭐

⭐数量 = 重要程度和使用频率
```

### 3.2 px - 像素单位


**🖥️ px的特点**
```css
/* px = 屏幕上的一个像素点 */
.box {
    width: 300px;      /* 固定300像素宽 */
    height: 200px;     /* 固定200像素高 */
    font-size: 16px;   /* 固定16像素字体 */
}

优点：精确控制，所见即所得
缺点：不会随屏幕大小变化，响应式较差
```

**📊 px使用场景**
```css
/* ✅ 适合用px的场景 */
.border { border: 1px solid #ccc; }    /* 边框 */
.shadow { box-shadow: 2px 2px 4px; }   /* 阴影 */
.small-spacing { margin: 5px; }        /* 小间距 */

/* ❌ 不太适合用px的场景 */
.container { width: 1200px; }          /* 容器宽度 */
.text { font-size: 18px; }              /* 正文字体 */
```

### 3.3 em - 相对父元素


**📏 em的计算方式**
```css
/* em = 相对于父元素的font-size */
.parent {
    font-size: 20px;
}

.child {
    font-size: 1.5em;  /* 1.5 × 20px = 30px */
    margin: 1em;       /* 1 × 30px = 30px (注意：相对自己的字体大小) */
}
```

**🔄 em的层层嵌套**
```css
body { font-size: 16px; }

.level1 {
    font-size: 1.2em;  /* 1.2 × 16px = 19.2px */
}

.level1 .level2 {
    font-size: 1.2em;  /* 1.2 × 19.2px = 23.04px */
}

.level1 .level2 .level3 {
    font-size: 1.2em;  /* 1.2 × 23.04px = 27.65px */
}

/* 问题：嵌套越深，字体越大，难以控制 */
```

### 3.4 rem - 相对根元素


**🌟 rem的优势**
```css
/* rem = 相对于根元素(html)的font-size */
html { font-size: 16px; }

.title { font-size: 2rem; }     /* 2 × 16px = 32px */
.text { font-size: 1rem; }      /* 1 × 16px = 16px */
.small { font-size: 0.875rem; } /* 0.875 × 16px = 14px */

/* 优点：不管嵌套多深，都是相对于根元素计算 */
```

**⚡ rem实战技巧**
```css
/* 设置根字体为10px，方便计算 */
html { font-size: 10px; }

.h1 { font-size: 2.4rem; }  /* 24px */
.h2 { font-size: 2rem; }    /* 20px */
.p { font-size: 1.6rem; }   /* 16px */
.small { font-size: 1.2rem; } /* 12px */

/* 这样rem就很好算了！ */
```

---

## 4. 🎯 单位选择指南


### 4.1 em vs rem 对比


| 特性 | **em** | **rem** | **推荐场景** |
|------|--------|---------|-------------|
| **参考对象** | `父元素font-size` | `根元素font-size` | `rem更稳定` |
| **嵌套问题** | `会层层累积` | `不会累积` | `rem避免嵌套问题` |
| **使用场景** | `组件内部间距` | `全局字体、间距` | `rem用于主体，em用于细节` |
| **计算难度** | `较复杂` | `简单` | `rem更好计算` |

### 4.2 实际开发建议


**🏗️ 推荐的单位使用策略**
```css
/* ✅ 推荐使用rem的场景 */
html { font-size: 16px; }

.container { max-width: 75rem; }  /* 容器宽度 */
.title { font-size: 2rem; }       /* 标题字体 */
.text { font-size: 1rem; }        /* 正文字体 */
.spacing { margin: 1.5rem; }     /* 主要间距 */

/* ✅ 推荐使用em的场景 */
.button {
    font-size: 1rem;
    padding: 0.5em 1em;    /* 按钮内边距随字体大小变化 */
}

/* ✅ 推荐使用px的场景 */
.border { border: 1px solid #ccc; }  /* 边框 */
.shadow { box-shadow: 0 2px 4px; }   /* 阴影 */
```

### 4.3 百分比单位


**📊 百分比的用法**
```css
/* % = 相对于父元素的对应属性 */
.parent {
    width: 800px;
    height: 600px;
}

.child {
    width: 50%;      /* 50% × 800px = 400px */
    height: 30%;     /* 30% × 600px = 180px */
    margin-left: 25%; /* 25% × 800px = 200px */
}
```

**⚠️ 百分比的注意事项**
```css
/* 百分比参考的是不同的属性 */
.parent { width: 400px; height: 300px; }

.child {
    width: 50%;        /* 相对于父元素width: 200px */
    height: 50%;       /* 相对于父元素height: 150px */
    padding: 10%;      /* 相对于父元素width: 40px (注意不是height!) */
    margin: 5%;        /* 相对于父元素width: 20px */
}
```

---

## 5. 📱 响应式单位


### 5.1 视口单位详解


**🖼️ 视口单位说明**
```
vw = viewport width  (视口宽度)
vh = viewport height (视口高度)
vmin = 视口宽高中较小的那个
vmax = 视口宽高中较大的那个

1vw = 视口宽度的1%
1vh = 视口高度的1%
```

**📐 视口单位示例**
```css
/* 假设浏览器窗口是 1200px × 800px */

.full-width {
    width: 100vw;    /* 100% × 1200px = 1200px */
}

.half-screen {
    width: 50vw;     /* 50% × 1200px = 600px */
    height: 50vh;    /* 50% × 800px = 400px */
}

.square {
    width: 50vmin;   /* 50% × 800px = 400px (取较小值) */
    height: 50vmin;  /* 50% × 800px = 400px */
}
```

### 5.2 vw/vh实战应用


**🎨 全屏背景**
```css
/* 铺满整个屏幕的背景 */
.hero-section {
    width: 100vw;
    height: 100vh;
    background: linear-gradient(45deg, #007bff, #6610f2);
    display: flex;
    align-items: center;
    justify-content: center;
}
```

**📱 响应式字体**
```css
/* 字体大小随屏幕变化 */
.responsive-title {
    font-size: calc(2rem + 2vw);
    /* 基础2rem + 屏幕宽度的2% */
}

/* 更精确的响应式字体 */
.fluid-text {
    font-size: clamp(1rem, 4vw, 3rem);
    /* 最小1rem，理想4vw，最大3rem */
}
```

### 5.3 calc()函数


**🧮 calc()的强大功能**
```css
/* calc() = 可以混合不同单位计算 */
.sidebar {
    width: calc(100% - 300px);     /* 总宽度减去固定宽度 */
    height: calc(100vh - 60px);    /* 总高度减去头部高度 */
    padding: calc(1rem + 10px);    /* rem加px */
}

/* 响应式间距 */
.container {
    padding: calc(1rem + 2vw);     /* 基础间距 + 响应式间距 */
    margin: calc(50% - 400px);     /* 居中的另一种方法 */
}
```

**⚡ calc()实际案例**
```css
/* 完美的三栏布局 */
.layout {
    display: flex;
}

.sidebar {
    width: 250px;                  /* 固定宽度 */
}

.main {
    width: calc(100% - 500px);     /* 总宽度 - 两个侧边栏 */
}

.aside {
    width: 250px;                  /* 固定宽度 */
}
```

---

## 6. 📋 核心要点总结


### 6.1 CSS变量最佳实践


**🎨 变量命名规范**
```css
:root {
    /* 颜色变量 */
    --color-primary: #007bff;
    --color-secondary: #6c757d;
    --color-success: #28a745;
    --color-danger: #dc3545;
    
    /* 尺寸变量 */
    --spacing-xs: 0.25rem;
    --spacing-sm: 0.5rem;
    --spacing-md: 1rem;
    --spacing-lg: 1.5rem;
    --spacing-xl: 3rem;
    
    /* 字体变量 */
    --font-size-sm: 0.875rem;
    --font-size-base: 1rem;
    --font-size-lg: 1.25rem;
    --font-size-xl: 1.5rem;
}
```

### 6.2 单位选择速查表


| 用途 | **首选单位** | **备选单位** | **原因** |
|------|-------------|-------------|---------|
| **字体大小** | `rem` | `em` | `响应式，好计算` |
| **间距(margin/padding)** | `rem` | `em, px` | `统一比例` |
| **容器宽度** | `%`, `vw` | `rem` | `响应式布局` |
| **固定元素** | `px` | `无` | `精确控制` |
| **边框阴影** | `px` | `无` | `视觉效果` |
| **高度** | `vh`, `%` | `rem` | `适应屏幕` |

### 6.3 响应式单位组合


**💡 黄金组合**
```css
/* 完美的响应式设计 */
:root {
    --container-width: min(90vw, 1200px);  /* 响应式容器 */
    --font-fluid: clamp(1rem, 4vw, 2rem);  /* 流体字体 */
    --spacing-fluid: calc(1rem + 1vw);     /* 流体间距 */
}

.container {
    width: var(--container-width);
    font-size: var(--font-fluid);
    padding: var(--spacing-fluid);
}
```

### 6.4 避免常见错误


**⚠️ 常见问题**
```css
/* ❌ 错误：em嵌套累积 */
.parent { font-size: 1.2em; }
.child { font-size: 1.2em; }    /* 会累积变大 */

/* ✅ 正确：使用rem */
.parent { font-size: 1.2rem; }
.child { font-size: 1.2rem; }   /* 保持一致 */

/* ❌ 错误：100vw导致横向滚动条 */
.full-width { width: 100vw; }   /* 可能超出页面 */

/* ✅ 正确：使用100% */
.full-width { width: 100%; }    /* 适应父元素 */
```

**核心记忆**：
- **CSS变量**：定义一次，到处使用，`:root`做全局，`var()`来使用
- **rem最常用**：相对根元素，不累积，好计算
- **vw/vh做响应式**：相对视口，自动适应屏幕
- **px做细节**：边框、阴影等精确控制
- **组合使用更强大**：`calc()`混合计算，`clamp()`限制范围