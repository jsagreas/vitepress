---
title: 2、ldap.toml LDAP认证配置
---
## 📚 目录

1. [LDAP认证基础概念](#1-LDAP认证基础概念)
2. [ldap.toml文件结构解析](#2-ldaptoml文件结构解析)
3. [服务器连接配置详解](#3-服务器连接配置详解)
4. [用户搜索与过滤配置](#4-用户搜索与过滤配置)
5. [属性映射配置](#5-属性映射配置)
6. [组权限映射配置](#6-组权限映射配置)
7. [实际配置示例](#7-实际配置示例)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🔐 LDAP认证基础概念


### 1.1 什么是LDAP认证


**LDAP简单理解**：
想象一下公司的通讯录，里面记录着每个员工的姓名、部门、职位等信息。LDAP就像是一个**电子化的企业通讯录**，不仅存储用户信息，还能验证用户身份。

```
传统登录方式：
用户 → Grafana自己的用户数据库 → 验证密码

LDAP认证方式：
用户 → Grafana → 公司LDAP服务器 → 验证密码 → 返回用户信息
```

**为什么要用LDAP认证？**
- ✅ **统一管理**：一套账号走遍公司所有系统
- ✅ **安全性高**：密码统一管理，降低泄露风险  
- ✅ **权限继承**：LDAP中的组织架构直接映射到Grafana权限
- ✅ **维护简单**：人员变动只需在LDAP中操作一次

### 1.2 LDAP中的核心概念


**DN（Distinguished Name）**：
就像家庭住址一样，DN是LDAP中每个条目的**唯一地址**。

```
人员DN示例：
cn=张三,ou=开发部,ou=技术中心,dc=company,dc=com

解读：
- cn=张三：具体的人（Common Name）
- ou=开发部：所在部门（Organizational Unit）  
- ou=技术中心：上级部门
- dc=company,dc=com：公司域名
```

**搜索基础（Search Base）**：
告诉Grafana从LDAP的哪个"文件夹"开始找用户，就像告诉别人"去技术中心找人"。

---

## 2. 📄 ldap.toml文件结构解析


### 2.1 配置文件整体结构


`ldap.toml`文件是Grafana连接LDAP服务器的**说明书**，告诉Grafana：
- 🔗 怎么连接LDAP服务器
- 🔍 怎么搜索用户信息  
- 🏷️ 怎么理解用户属性
- 👥 怎么分配用户权限

```
文件结构概览：
┌─ [[servers]]           ← LDAP服务器配置
│  ├─ 连接配置（host、port等）
│  ├─ 认证配置（bind_dn、密码等）
│  ├─ 搜索配置（用户和组搜索）
│  └─ 映射配置（属性和权限映射）
└─ 可配置多个服务器实现冗余
```

### 2.2 配置文件的工作逻辑


**配置文件工作流程**：
```
用户登录 → Grafana读取ldap.toml → 连接LDAP服务器 → 搜索用户 → 
验证密码 → 获取用户属性 → 分配权限 → 登录成功
```

---

## 3. 🌐 服务器连接配置详解


### 3.1 基本连接参数


**`[[servers]]`** - 服务器定义
```toml
[[servers]]
# 可以配置多个服务器，Grafana会依次尝试连接
```
> **通俗理解**：这就像在通讯录里记录多个联系方式，如果第一个打不通，就试第二个。

**`host`** - LDAP服务器地址
```toml
host = "ldap.company.com"
# 或者用IP地址：host = "192.168.1.100"
```
> **实际含义**：告诉Grafana"LDAP服务器在哪里"，就像告诉快递员你的地址。

**`port`** - 连接端口
```toml
port = 389        # 标准LDAP端口
# port = 636      # LDAPS(SSL)端口
```

### 3.2 安全连接配置


**`use_ssl`** - 是否使用SSL加密
```toml
use_ssl = false   # 标准LDAP连接
# use_ssl = true  # 使用LDAPS加密连接
```
> **安全提示**：生产环境建议开启SSL，就像银行转账要用加密通道一样。

**`start_tls`** - 启动TLS加密
```toml
start_tls = false
# start_tls = true  # 在标准连接基础上启动加密
```

**`ssl_skip_verify`** - 跳过SSL证书验证
```toml
ssl_skip_verify = false
# ssl_skip_verify = true  # 测试环境可以跳过，生产环境不建议
```

### 3.3 认证凭据配置


**`bind_dn`** - 绑定DN（服务账号）
```toml
bind_dn = "cn=grafana-service,ou=service-accounts,dc=company,dc=com"
```
> **通俗解释**：这是Grafana用来连接LDAP的"服务账号"，就像酒店前台用特殊钥匙访问客人信息一样。

**`bind_password`** - 绑定密码
```toml
bind_password = "service_account_password"
```

---

## 4. 🔍 用户搜索与过滤配置


### 4.1 用户搜索配置


**`search_filter`** - 用户搜索过滤器
```toml
search_filter = "(cn=%s)"
# 或更复杂的过滤器
search_filter = "(&(objectClass=person)(|(cn=%s)(mail=%s)))"
```

**过滤器语法解读**：
```
(cn=%s)                    ← 按用户名搜索
(&(条件1)(条件2))          ← 逻辑AND：两个条件都要满足
(|(条件1)(条件2))          ← 逻辑OR：满足任一条件即可
(objectClass=person)       ← 限定搜索人员对象
%s                         ← 用户输入的登录名占位符
```

**实际应用示例**：
```toml
# 简单搜索：只按用户名
search_filter = "(cn=%s)"

# 灵活搜索：支持用户名或邮箱登录
search_filter = "(|(cn=%s)(mail=%s)(sAMAccountName=%s))"
```

**`search_base_dns`** - 搜索基础DN
```toml
search_base_dns = ["ou=people,dc=company,dc=com"]
# 可以指定多个搜索基础
search_base_dns = [
  "ou=employees,dc=company,dc=com",
  "ou=contractors,dc=company,dc=com"
]
```
> **形象比喻**：告诉Grafana去哪几个"部门"找人，而不是在整个公司乱找。

### 4.2 组搜索配置


**`group_search_filter`** - 组搜索过滤器
```toml
group_search_filter = "(&(objectClass=posixGroup)(memberUid=%s))"
```

**`group_search_base_dns`** - 组搜索基础
```toml
group_search_base_dns = ["ou=groups,dc=company,dc=com"]
```

---

## 5. 🏷️ 属性映射配置


### 5.1 用户属性映射


**`[servers.attributes]`** - 属性映射部分
```toml
[servers.attributes]
name      = "givenName"     # 名字字段
surname   = "sn"           # 姓氏字段  
username  = "cn"           # 用户名字段
member_of = "memberOf"     # 组成员关系字段
email     = "mail"         # 邮箱字段
```

**属性映射的作用**：
```
LDAP服务器说："这个人的givenName是张，sn是三"
Grafana理解为："这个人的名字是张，姓氏是三"

┌─ LDAP原始数据 ─┐    映射    ┌─ Grafana理解的数据 ─┐
│ givenName: 张  │   ────→   │ name: 张           │
│ sn: 三         │   ────→   │ surname: 三        │  
│ mail: xxx@com  │   ────→   │ email: xxx@com     │
└───────────────┘          └───────────────────┘
```

### 5.2 常见属性字段对照表


| Grafana字段 | 常见LDAP字段 | 说明 |
|------------|-------------|------|
| `name` | `givenName`, `firstName` | 用户名字 |
| `surname` | `sn`, `lastName` | 用户姓氏 |
| `username` | `cn`, `uid`, `sAMAccountName` | 登录用户名 |
| `email` | `mail`, `email` | 邮箱地址 |
| `member_of` | `memberOf`, `groups` | 所属组信息 |

---

## 6. 👥 组权限映射配置


### 6.1 组映射基本概念


**组映射的目的**：
将LDAP中的用户组（如"开发部"、"运维组"）转换为Grafana中的权限角色（如"管理员"、"编辑者"）。

```
LDAP组织架构 → Grafana权限体系

技术总监组     →    Grafana管理员
开发部         →    编辑者权限  
运维组         →    编辑者权限
普通员工       →    查看者权限
```

### 6.2 组映射配置语法


**`[[servers.group_mappings]]`** - 组映射配置
```toml
[[servers.group_mappings]]
group_dn = "cn=技术总监,ou=groups,dc=company,dc=com"
org_role = "Admin"           # Grafana角色
grafana_admin = true         # 是否为Grafana超级管理员
org_id = 1                   # 组织ID

[[servers.group_mappings]]  
group_dn = "cn=开发部,ou=groups,dc=company,dc=com"
org_role = "Editor"          # 编辑者权限
org_id = 1

[[servers.group_mappings]]
group_dn = "cn=普通员工,ou=groups,dc=company,dc=com"  
org_role = "Viewer"          # 查看者权限
org_id = 1
```

### 6.3 Grafana权限角色说明


| 角色名称 | 权限范围 | 适用场景 |
|---------|---------|---------|
| **`Admin`** | 完全管理权限 | 部门负责人、技术总监 |
| **`Editor`** | 可创建、编辑仪表板 | 开发人员、运维人员 |
| **`Viewer`** | 只能查看仪表板 | 普通员工、业务人员 |

**`grafana_admin`特殊说明**：
- `grafana_admin = true`：拥有Grafana系统级管理权限
- 可以管理用户、组织、插件等全局配置
- 通常只给最高级别的管理员

---

## 7. 📋 实际配置示例


### 7.1 典型企业LDAP配置


```toml
# Grafana LDAP配置示例 - 适用于中小企业

[[servers]]
# === 服务器连接配置 ===
host = "ldap.company.com"
port = 389
use_ssl = false
start_tls = true                    # 启动TLS加密
ssl_skip_verify = false

# === 服务账号配置 ===  
bind_dn = "cn=grafana-service,ou=service-accounts,dc=company,dc=com"
bind_password = "your_service_password"

# === 用户搜索配置 ===
search_filter = "(|(cn=%s)(mail=%s))"    # 支持用户名或邮箱登录
search_base_dns = ["ou=employees,dc=company,dc=com"]

# === 组搜索配置 ===
group_search_filter = "(&(objectClass=group)(member=%s))"
group_search_base_dns = ["ou=groups,dc=company,dc=com"]

# === 用户属性映射 ===
[servers.attributes]
name      = "givenName"
surname   = "sn"  
username  = "cn"
member_of = "memberOf"
email     = "mail"

# === 权限组映射 ===
# 技术总监 → Grafana超级管理员
[[servers.group_mappings]]
group_dn = "cn=技术总监,ou=groups,dc=company,dc=com"
org_role = "Admin"
grafana_admin = true
org_id = 1

# 开发部 → 编辑者权限
[[servers.group_mappings]]
group_dn = "cn=开发部,ou=groups,dc=company,dc=com"  
org_role = "Editor"
org_id = 1

# 运维组 → 编辑者权限
[[servers.group_mappings]]
group_dn = "cn=运维组,ou=groups,dc=company,dc=com"
org_role = "Editor"  
org_id = 1

# 普通员工 → 查看者权限
[[servers.group_mappings]]
group_dn = "cn=all-employees,ou=groups,dc=company,dc=com"
org_role = "Viewer"
org_id = 1
```

### 7.2 多服务器冗余配置


```toml
# 主LDAP服务器
[[servers]]
host = "ldap-primary.company.com"
port = 389
# ... 其他配置相同

# 备用LDAP服务器  
[[servers]]
host = "ldap-backup.company.com"
port = 389
# ... 配置与主服务器保持一致
```

### 7.3 配置验证清单


**连接测试步骤**：
1. ✅ 确认LDAP服务器地址和端口可达
2. ✅ 验证服务账号密码正确
3. ✅ 测试用户搜索过滤器能找到用户
4. ✅ 检查属性映射获取正确的用户信息
5. ✅ 验证组映射分配合适的权限

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 LDAP认证：统一身份验证，一套账号多系统使用
🔸 DN地址：LDAP中每个条目的唯一标识，类似家庭住址
🔸 搜索基础：告诉Grafana去哪里找用户和组信息
🔸 属性映射：将LDAP字段翻译成Grafana能理解的信息
🔸 组映射：将企业组织架构映射为Grafana权限体系
```

### 8.2 配置文件关键要点


**🔹 连接配置要点**：
```
安全连接：生产环境必须启用SSL/TLS
服务账号：需要专门的LDAP服务账号，不要用个人账号
多服务器：配置主备服务器提高可用性
```

**🔹 搜索配置要点**：
```
过滤器设计：支持多种登录方式（用户名、邮箱）
搜索范围：精确指定搜索基础，提高查询效率
权限最小：服务账号只给必要的LDAP读取权限
```

**🔹 映射配置要点**：
```
属性映射：确保所有需要的用户信息都能正确映射
权限分层：合理设计组权限映射，避免权限过大或过小
组织对应：LDAP组织架构与Grafana权限体系相匹配
```

### 8.3 常见问题排查


**连接问题**：
- 检查网络连通性和防火墙配置
- 验证LDAP服务器地址和端口
- 确认SSL/TLS配置正确

**认证问题**：
- 验证bind_dn和密码正确性
- 检查服务账号的LDAP权限
- 确认用户搜索过滤器语法

**权限问题**：
- 检查组DN是否准确
- 验证用户确实属于相应的LDAP组
- 确认org_id配置正确

### 8.4 实际应用价值


- **企业级用户管理**：实现统一身份认证，提升安全性
- **权限继承**：LDAP组织架构直接映射到监控权限
- **运维简化**：人员变动自动同步到Grafana权限
- **审计合规**：用户访问行为可追溯，满足安全审计要求

**核心记忆要点**：
- LDAP配置就像搭建Grafana与企业通讯录的桥梁
- 配置的核心是"连接→搜索→映射→授权"四个步骤
- 安全配置和权限分层是生产环境的重要考虑因素
- 合理的组映射设计能大大简化用户权限管理工作