---
title: 6、策略模式代码实战
---
## 📚 目录

1. [策略模式核心回顾](#1-策略模式核心回顾)
2. [基础代码实现](#2-基础代码实现)
3. [多策略实现对比](#3-多策略实现对比)
4. [动态策略切换](#4-动态策略切换)
5. [配置驱动演示](#5-配置驱动演示)
6. [电商系统应用实战](#6-电商系统应用实战)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 策略模式核心回顾


### 1.1 什么是策略模式

> 💡 **通俗理解**  
> 策略模式就像选择不同的交通工具去同一个目的地：坐地铁、打车、骑车都能到达，但方式不同。程序中也是如此，同一个任务可以用不同的"策略"来完成。

**🔸 核心思想**
```
问题场景：
需要完成同一件事，但有多种不同的做法

传统做法：
if (类型A) {
    做法A的代码
} else if (类型B) {
    做法B的代码
} else if (类型C) {
    做法C的代码
}

策略模式：
把每种做法封装成独立的"策略类"
需要时直接调用对应策略
```

### 1.2 策略模式的三个角色


```
📋 角色分工图：

    客户端(Context)
         |
         | 使用
         ↓
    策略接口(Strategy) ←── 定义统一规范
         ↑
         | 实现
    ┌────┴────┬────────┐
    |         |        |
 策略A    策略B    策略C
(ConcreteStrategy)
```

**🎯 角色说明**
- **策略接口**：定义所有策略的统一标准（就像"交通工具"这个概念）
- **具体策略**：实现接口的各种具体做法（地铁、出租车、自行车）
- **上下文**：使用策略的客户端（需要出行的人）

---

## 2. 💻 基础代码实现


### 2.1 最简单的策略模式示例


**场景：计算器的不同运算策略**

> 🧠 **学习思路**  
> 我们先从最简单的例子开始，理解策略模式的基本结构

```java
// 第一步：定义策略接口
interface CalculateStrategy {
    double calculate(double a, double b);
}

// 第二步：实现具体策略
class AddStrategy implements CalculateStrategy {
    @Override
    public double calculate(double a, double b) {
        return a + b;  // 加法策略
    }
}

class MultiplyStrategy implements CalculateStrategy {
    @Override
    public double calculate(double a, double b) {
        return a * b;  // 乘法策略
    }
}

// 第三步：创建上下文类
class Calculator {
    private CalculateStrategy strategy;
    
    // 设置使用哪种策略
    public void setStrategy(CalculateStrategy strategy) {
        this.strategy = strategy;
    }
    
    // 执行计算
    public double execute(double a, double b) {
        return strategy.calculate(a, b);
    }
}
```

**💡 使用示例**
```java
public class StrategyDemo {
    public static void main(String[] args) {
        Calculator calc = new Calculator();
        
        // 使用加法策略
        calc.setStrategy(new AddStrategy());
        System.out.println("5 + 3 = " + calc.execute(5, 3));
        
        // 切换到乘法策略
        calc.setStrategy(new MultiplyStrategy());
        System.out.println("5 × 3 = " + calc.execute(5, 3));
    }
}
```

**🔍 代码分析**
- **策略接口**：`CalculateStrategy` 定义了计算的统一方法
- **具体策略**：`AddStrategy`、`MultiplyStrategy` 实现不同算法
- **上下文**：`Calculator` 负责调用策略，不关心具体实现
- **关键优势**：新增减法、除法策略时，无需修改现有代码

---

## 3. 🔄 多策略实现对比


### 3.1 传统if-else方式 vs 策略模式


> 📊 **对比分析**  
> 让我们通过实际例子看看两种方式的差别

**❌ 传统方式：会员折扣计算**
```java
class TraditionalDiscount {
    public double calculateDiscount(String memberType, double amount) {
        if ("BRONZE".equals(memberType)) {
            return amount * 0.95;  // 95折
        } else if ("SILVER".equals(memberType)) {
            return amount * 0.90;  // 9折
        } else if ("GOLD".equals(memberType)) {
            return amount * 0.85;  // 85折
        } else if ("PLATINUM".equals(memberType)) {
            return amount * 0.80;  // 8折
        }
        return amount;  // 无折扣
    }
}
```

**✅ 策略模式方式**
```java
// 折扣策略接口
interface DiscountStrategy {
    double applyDiscount(double amount);
    String getDescription();
}

// 青铜会员策略
class BronzeDiscount implements DiscountStrategy {
    @Override
    public double applyDiscount(double amount) {
        return amount * 0.95;
    }
    
    @Override
    public String getDescription() {
        return "青铜会员95折优惠";
    }
}

// 黄金会员策略
class GoldDiscount implements DiscountStrategy {
    @Override
    public double applyDiscount(double amount) {
        return amount * 0.85;
    }
    
    @Override
    public String getDescription() {
        return "黄金会员85折优惠";
    }
}

// 折扣计算器
class DiscountCalculator {
    private DiscountStrategy strategy;
    
    public void setDiscountStrategy(DiscountStrategy strategy) {
        this.strategy = strategy;
    }
    
    public double calculate(double amount) {
        return strategy.applyDiscount(amount);
    }
    
    public String getDiscountInfo() {
        return strategy.getDescription();
    }
}
```

### 3.2 两种方式的优缺点对比


| 对比维度 | **传统if-else** | **策略模式** |
|---------|----------------|-------------|
| 🔧 **代码维护** | `需要修改主类，容易出错` | `新增策略类，无需修改现有代码` |
| 📖 **代码可读性** | `逻辑混杂在一起` | `每个策略职责清晰` |
| 🔄 **扩展性** | `添加新类型需要修改if-else` | `新增策略类即可` |
| 🧪 **测试性** | `需要测试整个方法` | `可以单独测试每个策略` |
| 💾 **内存占用** | `较少（只有一个类）` | `较多（多个策略对象）` |

> 🎯 **选择建议**  
> - 策略类型少于3个：可以考虑if-else
> - 策略类型多于3个：建议使用策略模式
> - 策略逻辑复杂：推荐策略模式
> - 需要频繁扩展：必须使用策略模式

---

## 4. 🔄 动态策略切换


### 4.1 运行时策略切换示例


> 💭 **实际场景**  
> 想象一个音乐播放器，用户可以随时切换播放模式：顺序播放、随机播放、单曲循环

```java
// 播放策略接口
interface PlayStrategy {
    String play(List<String> playlist, int currentIndex);
    String getModeName();
}

// 顺序播放策略
class SequentialPlay implements PlayStrategy {
    @Override
    public String play(List<String> playlist, int currentIndex) {
        int nextIndex = (currentIndex + 1) % playlist.size();
        return playlist.get(nextIndex);
    }
    
    @Override
    public String getModeName() {
        return "顺序播放";
    }
}

// 随机播放策略
class RandomPlay implements PlayStrategy {
    private Random random = new Random();
    
    @Override
    public String play(List<String> playlist, int currentIndex) {
        int randomIndex = random.nextInt(playlist.size());
        return playlist.get(randomIndex);
    }
    
    @Override
    public String getModeName() {
        return "随机播放";
    }
}

// 单曲循环策略
class RepeatOnePlay implements PlayStrategy {
    @Override
    public String play(List<String> playlist, int currentIndex) {
        return playlist.get(currentIndex);  // 重复当前歌曲
    }
    
    @Override
    public String getModeName() {
        return "单曲循环";
    }
}
```

**🎵 音乐播放器实现**
```java
class MusicPlayer {
    private List<String> playlist;
    private int currentIndex;
    private PlayStrategy playStrategy;
    
    public MusicPlayer(List<String> playlist) {
        this.playlist = playlist;
        this.currentIndex = 0;
        this.playStrategy = new SequentialPlay();  // 默认顺序播放
    }
    
    // 切换播放模式
    public void changePlayMode(PlayStrategy strategy) {
        this.playStrategy = strategy;
        System.out.println("🎵 切换到：" + strategy.getModeName());
    }
    
    // 播放下一首
    public void playNext() {
        String nextSong = playStrategy.play(playlist, currentIndex);
        System.out.println("🎶 正在播放：" + nextSong);
        
        // 更新当前索引（简化处理）
        currentIndex = playlist.indexOf(nextSong);
    }
}
```

**🎮 交互式演示**
```java
public class MusicPlayerDemo {
    public static void main(String[] args) {
        // 创建播放列表
        List<String> songs = Arrays.asList(
            "《晴天》", "《稻香》", "《青花瓷》", "《听妈妈的话》"
        );
        
        MusicPlayer player = new MusicPlayer(songs);
        
        // 顺序播放几首
        System.out.println("=== 顺序播放模式 ===");
        player.playNext();
        player.playNext();
        
        // 切换到随机播放
        System.out.println("\n=== 切换播放模式 ===");
        player.changePlayMode(new RandomPlay());
        player.playNext();
        player.playNext();
        
        // 切换到单曲循环
        player.changePlayMode(new RepeatOnePlay());
        player.playNext();
        player.playNext();
    }
}
```

**📱 运行效果**
```
=== 顺序播放模式 ===
🎶 正在播放：《稻香》
🎶 正在播放：《青花瓷》

=== 切换播放模式 ===
🎵 切换到：随机播放
🎶 正在播放：《晴天》
🎶 正在播放：《听妈妈的话》

🎵 切换到：单曲循环
🎶 正在播放：《听妈妈的话》
🎶 正在播放：《听妈妈的话》
```

---

## 5. ⚙️ 配置驱动演示


### 5.1 基于配置文件的策略选择


> 🔧 **实用技巧**  
> 在实际项目中，策略选择往往需要根据配置文件动态决定，这样可以不重启程序就能改变行为

**📄 配置文件示例（application.yml）**
```yaml
# 数据处理配置
data-processor:
  strategy: "compress"  # 可选：compress, encrypt, validate
  
# 文件存储配置  
file-storage:
  strategy: "cloud"     # 可选：local, cloud, database
```

**🏭 策略工厂模式**
```java
// 数据处理策略接口
interface DataProcessStrategy {
    String process(String data);
    String getStrategyName();
}

// 压缩策略
class CompressStrategy implements DataProcessStrategy {
    @Override
    public String process(String data) {
        return "[压缩后]" + data.substring(0, Math.min(10, data.length())) + "...";
    }
    
    @Override
    public String getStrategyName() {
        return "数据压缩策略";
    }
}

// 加密策略
class EncryptStrategy implements DataProcessStrategy {
    @Override
    public String process(String data) {
        return "[加密]" + data.replaceAll(".", "*");
    }
    
    @Override
    public String getStrategyName() {
        return "数据加密策略";
    }
}

// 验证策略
class ValidateStrategy implements DataProcessStrategy {
    @Override
    public String process(String data) {
        boolean isValid = data.length() > 5;
        return "[验证]" + data + " -> " + (isValid ? "✅有效" : "❌无效");
    }
    
    @Override
    public String getStrategyName() {
        return "数据验证策略";
    }
}
```

**🏗️ 策略工厂**
```java
class StrategyFactory {
    private static final Map<String, DataProcessStrategy> strategies = new HashMap<>();
    
    // 注册所有策略
    static {
        strategies.put("compress", new CompressStrategy());
        strategies.put("encrypt", new EncryptStrategy());
        strategies.put("validate", new ValidateStrategy());
    }
    
    // 根据配置创建策略
    public static DataProcessStrategy createStrategy(String strategyName) {
        DataProcessStrategy strategy = strategies.get(strategyName);
        if (strategy == null) {
            throw new IllegalArgumentException("未知策略：" + strategyName);
        }
        return strategy;
    }
    
    // 获取所有可用策略
    public static Set<String> getAvailableStrategies() {
        return strategies.keySet();
    }
}
```

**⚙️ 配置驱动的处理器**
```java
class ConfigurableDataProcessor {
    private DataProcessStrategy strategy;
    private String currentStrategyName;
    
    // 根据配置初始化
    public void initializeFromConfig(String configStrategy) {
        this.currentStrategyName = configStrategy;
        this.strategy = StrategyFactory.createStrategy(configStrategy);
        System.out.println("📋 已加载策略：" + strategy.getStrategyName());
    }
    
    // 处理数据
    public String processData(String data) {
        if (strategy == null) {
            throw new IllegalStateException("策略未初始化，请先调用initializeFromConfig()");
        }
        return strategy.process(data);
    }
    
    // 热切换策略（无需重启）
    public void switchStrategy(String newStrategyName) {
        System.out.println("🔄 策略切换：" + currentStrategyName + " -> " + newStrategyName);
        initializeFromConfig(newStrategyName);
    }
    
    // 获取当前策略信息
    public String getCurrentStrategyInfo() {
        return strategy != null ? strategy.getStrategyName() : "未设置策略";
    }
}
```

**🧪 配置驱动演示**
```java
public class ConfigDrivenDemo {
    public static void main(String[] args) {
        ConfigurableDataProcessor processor = new ConfigurableDataProcessor();
        String testData = "这是一段需要处理的重要数据内容";
        
        // 模拟从配置文件读取策略
        String configStrategy = "compress";  // 实际项目中从配置文件读取
        
        // 初始化策略
        processor.initializeFromConfig(configStrategy);
        System.out.println("📊 处理结果：" + processor.processData(testData));
        
        // 运行时切换策略
        System.out.println("\n=== 运行时策略切换 ===");
        processor.switchStrategy("encrypt");
        System.out.println("📊 处理结果：" + processor.processData(testData));
        
        processor.switchStrategy("validate");
        System.out.println("📊 处理结果：" + processor.processData(testData));
        
        // 显示可用策略
        System.out.println("\n📋 可用策略：" + StrategyFactory.getAvailableStrategies());
    }
}
```

**📱 运行结果**
```
📋 已加载策略：数据压缩策略
📊 处理结果：[压缩后]这是一段需要处理的...

=== 运行时策略切换 ===
🔄 策略切换：compress -> encrypt
📋 已加载策略：数据加密策略
📊 处理结果：[加密]***************

🔄 策略切换：encrypt -> validate
📋 已加载策略：数据验证策略
📊 处理结果：[验证]这是一段需要处理的重要数据内容 -> ✅有效

📋 可用策略：[compress, encrypt, validate]
```

---

## 6. 🛒 电商系统应用实战


### 6.1 复杂业务场景：订单处理系统


> 🏢 **真实场景**  
> 电商平台需要处理不同类型的订单：普通订单、秒杀订单、预售订单。每种订单的处理流程都不一样。

**📦 订单策略体系设计**

```
电商订单处理体系：

    订单处理接口
         ↑
    ┌────┴────┬─────────┬─────────┐
    |         |         |         |
 普通订单   秒杀订单   预售订单   团购订单
 处理策略   处理策略   处理策略   处理策略
```

**🎯 订单处理策略接口**
```java
// 订单信息类
class Order {
    private String orderId;
    private String productName;
    private double amount;
    private int quantity;
    private String customerInfo;
    
    // 构造函数和getter/setter
    public Order(String orderId, String productName, double amount, int quantity) {
        this.orderId = orderId;
        this.productName = productName;
        this.amount = amount;
        this.quantity = quantity;
    }
    
    // 省略getter/setter方法...
    public String getOrderId() { return orderId; }
    public String getProductName() { return productName; }
    public double getAmount() { return amount; }
    public int getQuantity() { return quantity; }
}

// 订单处理结果
class OrderProcessResult {
    private boolean success;
    private String message;
    private String processType;
    
    public OrderProcessResult(boolean success, String message, String processType) {
        this.success = success;
        this.message = message;
        this.processType = processType;
    }
    
    // getter方法...
    public boolean isSuccess() { return success; }
    public String getMessage() { return message; }
    public String getProcessType() { return processType; }
}

// 订单处理策略接口
interface OrderProcessStrategy {
    OrderProcessResult processOrder(Order order);
    String getStrategyDescription();
    boolean validateOrder(Order order);
}
```

### 6.2 不同订单类型的策略实现


**🛍️ 普通订单处理策略**
```java
class NormalOrderStrategy implements OrderProcessStrategy {
    
    @Override
    public OrderProcessResult processOrder(Order order) {
        if (!validateOrder(order)) {
            return new OrderProcessResult(false, "订单验证失败", "普通订单");
        }
        
        // 普通订单处理流程
        System.out.println("📦 普通订单处理开始...");
        System.out.println("   ✅ 检查库存");
        System.out.println("   ✅ 计算价格（无特殊优惠）");
        System.out.println("   ✅ 生成订单");
        System.out.println("   ✅ 发送确认邮件");
        
        return new OrderProcessResult(true, 
            "普通订单处理成功，预计3-5天发货", "普通订单");
    }
    
    @Override
    public boolean validateOrder(Order order) {
        return order.getAmount() > 0 && order.getQuantity() > 0;
    }
    
    @Override
    public String getStrategyDescription() {
        return "普通订单：标准处理流程，3-5天发货";
    }
}
```

**⚡ 秒杀订单处理策略**
```java
class SeckillOrderStrategy implements OrderProcessStrategy {
    
    @Override
    public OrderProcessResult processOrder(Order order) {
        if (!validateOrder(order)) {
            return new OrderProcessResult(false, "秒杀订单验证失败", "秒杀订单");
        }
        
        // 秒杀订单特殊处理
        System.out.println("⚡ 秒杀订单处理开始...");
        System.out.println("   🔥 高优先级库存锁定");
        System.out.println("   💰 应用秒杀价格");
        System.out.println("   🚀 快速生成订单");
        System.out.println("   📱 即时推送通知");
        System.out.println("   🎯 加入优先发货队列");
        
        return new OrderProcessResult(true, 
            "秒杀订单处理成功，24小时内发货！", "秒杀订单");
    }
    
    @Override
    public boolean validateOrder(Order order) {
        // 秒杀订单额外验证：限购数量
        return order.getAmount() > 0 && 
               order.getQuantity() > 0 && 
               order.getQuantity() <= 2;  // 限购2件
    }
    
    @Override
    public String getStrategyDescription() {
        return "秒杀订单：优先处理，限购2件，24小时发货";
    }
}
```

**🔮 预售订单处理策略**
```java
class PreSaleOrderStrategy implements OrderProcessStrategy {
    
    @Override
    public OrderProcessResult processOrder(Order order) {
        if (!validateOrder(order)) {
            return new OrderProcessResult(false, "预售订单验证失败", "预售订单");
        }
        
        // 预售订单特殊处理
        System.out.println("🔮 预售订单处理开始...");
        System.out.println("   📅 预留库存配额");
        System.out.println("   💳 收取定金（30%）");
        System.out.println("   📝 创建预售记录");
        System.out.println("   ⏰ 设置发货提醒");
        System.out.println("   🎁 赠送预售礼品");
        
        return new OrderProcessResult(true, 
            "预售订单处理成功，预计30天后发货", "预售订单");
    }
    
    @Override
    public boolean validateOrder(Order order) {
        // 预售订单需要更高的金额门槛
        return order.getAmount() >= 100 && order.getQuantity() > 0;
    }
    
    @Override
    public String getStrategyDescription() {
        return "预售订单：收取定金，30天后发货，有预售礼品";
    }
}
```

### 6.3 电商订单处理系统


**🏪 订单处理中心**
```java
class OrderProcessCenter {
    private OrderProcessStrategy currentStrategy;
    private Map<String, OrderProcessStrategy> strategies;
    
    public OrderProcessCenter() {
        initializeStrategies();
    }
    
    private void initializeStrategies() {
        strategies = new HashMap<>();
        strategies.put("NORMAL", new NormalOrderStrategy());
        strategies.put("SECKILL", new SeckillOrderStrategy());
        strategies.put("PRESALE", new PreSaleOrderStrategy());
    }
    
    // 根据订单类型选择策略
    public void selectStrategy(String orderType) {
        OrderProcessStrategy strategy = strategies.get(orderType.toUpperCase());
        if (strategy == null) {
            throw new IllegalArgumentException("不支持的订单类型：" + orderType);
        }
        
        this.currentStrategy = strategy;
        System.out.println("🔄 已切换到：" + strategy.getStrategyDescription());
    }
    
    // 处理订单
    public OrderProcessResult handleOrder(Order order) {
        if (currentStrategy == null) {
            throw new IllegalStateException("请先选择订单处理策略");
        }
        
        System.out.println("\n🏪 开始处理订单：" + order.getOrderId());
        System.out.println("📋 商品：" + order.getProductName() + 
                          " | 数量：" + order.getQuantity() + 
                          " | 金额：￥" + order.getAmount());
        
        OrderProcessResult result = currentStrategy.processOrder(order);
        
        System.out.println("📊 处理结果：" + 
                          (result.isSuccess() ? "✅成功" : "❌失败") + 
                          " | " + result.getMessage());
        
        return result;
    }
    
    // 获取所有可用策略
    public void showAvailableStrategies() {
        System.out.println("\n📋 可用订单处理策略：");
        strategies.forEach((key, strategy) -> 
            System.out.println("   " + key + ": " + strategy.getStrategyDescription())
        );
    }
}
```

### 6.4 完整的电商应用演示


```java
public class ECommerceDemo {
    public static void main(String[] args) {
        OrderProcessCenter center = new OrderProcessCenter();
        
        // 显示所有可用策略
        center.showAvailableStrategies();
        
        // 创建不同类型的订单
        Order normalOrder = new Order("ORD001", "iPhone 15", 7999.0, 1);
        Order seckillOrder = new Order("SEC001", "小米手机", 1999.0, 2);
        Order presaleOrder = new Order("PRE001", "PlayStation 6", 3999.0, 1);
        
        System.out.println("\n" + "=".repeat(50));
        System.out.println("🛒 电商订单处理系统演示");
        System.out.println("=".repeat(50));
        
        // 处理普通订单
        center.selectStrategy("NORMAL");
        center.handleOrder(normalOrder);
        
        System.out.println("\n" + "-".repeat(50));
        
        // 处理秒杀订单
        center.selectStrategy("SECKILL");
        center.handleOrder(seckillOrder);
        
        System.out.println("\n" + "-".repeat(50));
        
        // 处理预售订单
        center.selectStrategy("PRESALE");
        center.handleOrder(presaleOrder);
        
        // 演示订单验证失败的情况
        System.out.println("\n" + "-".repeat(50));
        Order invalidOrder = new Order("INV001", "超限商品", 50.0, 5);
        center.selectStrategy("SECKILL");  // 秒杀限购2件
        center.handleOrder(invalidOrder);
    }
}
```

**📱 运行效果展示**
```
📋 可用订单处理策略：
   NORMAL: 普通订单：标准处理流程，3-5天发货
   SECKILL: 秒杀订单：优先处理，限购2件，24小时发货
   PRESALE: 预售订单：收取定金，30天后发货，有预售礼品

==================================================
🛒 电商订单处理系统演示
==================================================
🔄 已切换到：普通订单：标准处理流程，3-5天发货

🏪 开始处理订单：ORD001
📋 商品：iPhone 15 | 数量：1 | 金额：￥7999.0
📦 普通订单处理开始...
   ✅ 检查库存
   ✅ 计算价格（无特殊优惠）
   ✅ 生成订单
   ✅ 发送确认邮件
📊 处理结果：✅成功 | 普通订单处理成功，预计3-5天发货

--------------------------------------------------
🔄 已切换到：秒杀订单：优先处理，限购2件，24小时发货

🏪 开始处理订单：SEC001
📋 商品：小米手机 | 数量：2 | 金额：￥1999.0
⚡ 秒杀订单处理开始...
   🔥 高优先级库存锁定
   💰 应用秒杀价格
   🚀 快速生成订单
   📱 即时推送通知
   🎯 加入优先发货队列
📊 处理结果：✅成功 | 秒杀订单处理成功，24小时内发货！

--------------------------------------------------
🔄 已切换到：预售订单：收取定金，30天后发货，有预售礼品

🏪 开始处理订单：PRE001
📋 商品：PlayStation 6 | 数量：1 | 金额：￥3999.0
🔮 预售订单处理开始...
   📅 预留库存配额
   💳 收取定金（30%）
   📝 创建预售记录
   ⏰ 设置发货提醒
   🎁 赠送预售礼品
📊 处理结果：✅成功 | 预售订单处理成功，预计30天后发货

--------------------------------------------------
🏪 开始处理订单：INV001
📋 商品：超限商品 | 数量：5 | 金额：￥50.0
📊 处理结果：❌失败 | 秒杀订单验证失败
```

---

## 7. 📋 核心要点总结


### 7.1 策略模式的核心价值


> 🎯 **关键理解**  
> 策略模式的核心是将"算法的选择"与"算法的使用"分离，让程序更灵活、更易扩展。

**🔸 解决的核心问题**
```
❌ 传统问题：
- 大量的if-else判断
- 新增功能需要修改现有代码
- 代码耦合度高，难以维护
- 无法在运行时动态切换逻辑

✅ 策略模式解决：
- 每个策略独立封装
- 新增策略无需修改现有代码
- 松耦合设计，易于维护
- 支持运行时动态切换
```

### 7.2 实际应用的最佳实践


**📋 实施检查清单**
- [ ] **策略接口设计**：方法简洁明确，参数合理
- [ ] **策略命名规范**：见名知意，统一后缀如Strategy
- [ ] **工厂模式配合**：方便策略的创建和管理
- [ ] **配置文件支持**：支持不重启切换策略
- [ ] **异常处理机制**：策略失败时的降级方案
- [ ] **性能考虑**：避免频繁创建策略对象

**🎯 选择策略模式的时机**
```
适合使用：
✅ 同一行为有多种实现方式
✅ 需要在运行时选择算法
✅ 算法逻辑复杂且独立
✅ 需要屏蔽算法实现细节

不适合使用：
❌ 策略类型固定且少于3个
❌ 策略逻辑过于简单
❌ 不需要动态切换
❌ 性能要求极高的场景
```

### 7.3 与其他模式的关系


```
策略模式组合使用：

策略模式 + 工厂模式：
- 工厂负责创建策略对象
- 策略负责执行具体算法

策略模式 + 模板方法：
- 模板方法定义算法骨架
- 策略实现具体步骤

策略模式 + 状态模式：
- 状态模式：对象状态改变时行为改变
- 策略模式：算法选择时行为改变
```

### 7.4 记忆口诀


> 🧠 **策略模式记忆法**  
> 
> **"一个接口多实现，策略选择很简单"**  
> **"工厂配置来驱动，运行切换不重启"**  
> **"if-else说再见，扩展维护都轻松"**

**🔑 核心关键词**
- **Strategy**：策略接口统一标准
- **Context**：上下文使用策略
- **Factory**：工厂创建策略
- **Config**：配置驱动选择
- **Dynamic**：动态运行时切换

**💡 实战记忆点**
1. **先定接口**：确定所有策略的统一行为
2. **再写实现**：每个策略专注自己的逻辑
3. **工厂管理**：统一创建和获取策略
4. **配置驱动**：让策略选择可配置化
5. **动态切换**：支持运行时更改策略

---

> 🎓 **学习建议**  
> 策略模式是最容易理解和应用的设计模式之一。建议从简单的例子开始练习，逐步过渡到复杂的业务场景。记住：策略模式的精髓是"把变化的部分提取出来，让它可以独立变化"。