---
title: 4、策略模式实际应用
---
## 📚 目录

1. [策略模式在实际开发中的价值](#1-策略模式在实际开发中的价值)
2. [排序算法策略](#2-排序算法策略)
3. [支付方式策略](#3-支付方式策略)
4. [折扣计算策略](#4-折扣计算策略)
5. [数据压缩策略](#5-数据压缩策略)
6. [路由算法策略](#6-路由算法策略)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 策略模式在实际开发中的价值


### 1.1 为什么要学习策略模式的实际应用


**生活中的策略选择**：
```
出行策略：
🚶‍♂️ 步行 - 距离近，不赶时间
🚲 骑车 - 中等距离，环保
🚌 公交 - 距离远，省钱
🚗 开车 - 距离远，舒适快捷
✈️ 飞机 - 超远距离，时间紧

根据不同情况（距离、时间、预算）选择不同的出行方式
```

**编程中的策略模式**：
- **核心思想**：把算法/行为封装成独立的策略类
- **主要好处**：可以在运行时动态切换不同的处理方式
- **解决问题**：避免大量`if-else`判断，让代码更清晰

### 1.2 策略模式的实际价值


> 📌 **核心概念**  
> 策略模式就像一个"工具箱"，里面有各种工具（策略），根据不同的任务需求，选择最合适的工具来完成工作。

**企业开发中的优势**：
- **可维护性**：新增策略不影响原有代码
- **可测试性**：每个策略可以独立测试
- **可扩展性**：轻松添加新的业务规则
- **代码复用**：同一个策略可以在多个地方使用

---

## 2. 📊 排序算法策略


### 2.1 现实需求场景


**不同数据量需要不同排序算法**：
```
小数据量（< 100）：插入排序 - 简单高效
中等数据量（100-10000）：快速排序 - 平均性能好  
大数据量（> 10000）：归并排序 - 稳定性能
特殊需求：堆排序 - 内存限制严格时
```

### 2.2 传统写法的问题


**❌ 不好的写法**：
```java
public void sortData(int[] data, String type) {
    if (type.equals("quick")) {
        // 快速排序代码...
    } else if (type.equals("merge")) {
        // 归并排序代码...
    } else if (type.equals("insertion")) {
        // 插入排序代码...
    }
    // 添加新算法需要修改这个方法
}
```

**问题分析**：
- 🚫 **违反开闭原则**：每次新增算法都要修改原代码
- 🚫 **代码臃肿**：所有算法逻辑堆积在一个方法里
- 🚫 **难以测试**：无法单独测试某个排序算法

### 2.3 策略模式解决方案


**✅ 策略模式实现**：

```java
// 排序策略接口 - 定义统一规范
interface SortStrategy {
    void sort(int[] data);
    String getName();
}

// 具体策略：快速排序
class QuickSortStrategy implements SortStrategy {
    @Override
    public void sort(int[] data) {
        System.out.println("使用快速排序");
        // 快速排序实现
        quickSort(data, 0, data.length - 1);
    }
    
    @Override
    public String getName() {
        return "快速排序";
    }
    
    private void quickSort(int[] arr, int low, int high) {
        // 快速排序核心逻辑（简化版）
        if (low < high) {
            int pi = partition(arr, low, high);
            quickSort(arr, low, pi - 1);
            quickSort(arr, pi + 1, high);
        }
    }
}

// 具体策略：归并排序
class MergeSortStrategy implements SortStrategy {
    @Override
    public void sort(int[] data) {
        System.out.println("使用归并排序");
        mergeSort(data, 0, data.length - 1);
    }
    
    @Override
    public String getName() {
        return "归并排序";
    }
    
    private void mergeSort(int[] arr, int left, int right) {
        // 归并排序核心逻辑
        if (left < right) {
            int mid = (left + right) / 2;
            mergeSort(arr, left, mid);
            mergeSort(arr, mid + 1, right);
            merge(arr, left, mid, right);
        }
    }
}

// 环境类：排序器
class DataSorter {
    private SortStrategy strategy;
    
    public void setStrategy(SortStrategy strategy) {
        this.strategy = strategy;
    }
    
    public void performSort(int[] data) {
        if (strategy == null) {
            throw new IllegalStateException("请先设置排序策略");
        }
        
        System.out.println("数据量: " + data.length);
        System.out.println("选择策略: " + strategy.getName());
        
        long startTime = System.currentTimeMillis();
        strategy.sort(data);
        long endTime = System.currentTimeMillis();
        
        System.out.println("排序完成，耗时: " + (endTime - startTime) + "ms");
    }
}
```

**📱 实际使用示例**：
```java
public class SortingExample {
    public static void main(String[] args) {
        DataSorter sorter = new DataSorter();
        int[] smallData = {5, 2, 8, 1, 9};
        int[] largeData = new int[10000]; // 假设是大数据集
        
        // 小数据用插入排序
        sorter.setStrategy(new InsertionSortStrategy());
        sorter.performSort(smallData);
        
        // 大数据用归并排序
        sorter.setStrategy(new MergeSortStrategy());
        sorter.performSort(largeData);
    }
}
```

### 2.4 智能策略选择器


**🤖 自动选择最优策略**：
```java
class SmartSortSelector {
    public static SortStrategy selectOptimalStrategy(int dataSize) {
        if (dataSize < 100) {
            return new InsertionSortStrategy(); // 小数据量
        } else if (dataSize < 10000) {
            return new QuickSortStrategy();     // 中等数据量
        } else {
            return new MergeSortStrategy();     // 大数据量
        }
    }
}
```

> 💡 **实用技巧**  
> 在实际项目中，可以根据数据量自动选择排序策略，用户无需关心具体用的是哪种算法，系统会自动优化性能。

---

## 3. 💳 支付方式策略


### 3.1 电商支付场景


**多种支付方式的现实需求**：
```
🏪 电商平台支付选择：
💳 信用卡支付 - 国际通用，手续费高
💰 支付宝 - 国内主流，优惠多
💸 微信支付 - 社交便捷，普及率高
🏦 银行转账 - 大额安全，到账慢
🎁 积分支付 - 会员专享，有限额度
```

### 3.2 支付策略设计


**支付策略接口设计**：
```java
// 支付策略接口
interface PaymentStrategy {
    PaymentResult pay(double amount, PaymentInfo info);
    String getPaymentMethod();
    boolean isAvailable(double amount);
}

// 支付结果类
class PaymentResult {
    private boolean success;
    private String message;
    private String transactionId;
    
    // 构造函数和getter/setter...
}

// 支付信息类
class PaymentInfo {
    private String userId;
    private String orderId;
    private Map<String, String> extraInfo;
    
    // 构造函数和getter/setter...
}
```

**具体支付策略实现**：
```java
// 支付宝支付策略
class AlipayStrategy implements PaymentStrategy {
    @Override
    public PaymentResult pay(double amount, PaymentInfo info) {
        System.out.println("=== 支付宝支付 ===");
        System.out.println("支付金额: ¥" + amount);
        
        // 模拟支付宝支付流程
        if (amount > 0 && amount <= 50000) {
            return new PaymentResult(true, "支付宝支付成功", "ALP" + System.currentTimeMillis());
        } else {
            return new PaymentResult(false, "支付宝单笔限额50000元", null);
        }
    }
    
    @Override
    public String getPaymentMethod() {
        return "支付宝";
    }
    
    @Override
    public boolean isAvailable(double amount) {
        return amount > 0 && amount <= 50000;
    }
}

// 微信支付策略
class WechatPayStrategy implements PaymentStrategy {
    @Override
    public PaymentResult pay(double amount, PaymentInfo info) {
        System.out.println("=== 微信支付 ===");
        System.out.println("支付金额: ¥" + amount);
        
        if (amount > 0 && amount <= 20000) {
            return new PaymentResult(true, "微信支付成功", "WX" + System.currentTimeMillis());
        } else {
            return new PaymentResult(false, "微信单笔限额20000元", null);
        }
    }
    
    @Override
    public String getPaymentMethod() {
        return "微信支付";
    }
    
    @Override
    public boolean isAvailable(double amount) {
        return amount > 0 && amount <= 20000;
    }
}

// 信用卡支付策略
class CreditCardStrategy implements PaymentStrategy {
    @Override
    public PaymentResult pay(double amount, PaymentInfo info) {
        System.out.println("=== 信用卡支付 ===");
        System.out.println("支付金额: ¥" + amount);
        System.out.println("手续费: ¥" + (amount * 0.03)); // 3%手续费
        
        return new PaymentResult(true, "信用卡支付成功", "CC" + System.currentTimeMillis());
    }
    
    @Override
    public String getPaymentMethod() {
        return "信用卡";
    }
    
    @Override
    public boolean isAvailable(double amount) {
        return amount > 0; // 信用卡无限额（简化）
    }
}
```

**支付处理器**：
```java
class PaymentProcessor {
    private PaymentStrategy strategy;
    
    public void setPaymentStrategy(PaymentStrategy strategy) {
        this.strategy = strategy;
    }
    
    public PaymentResult processPayment(double amount, PaymentInfo info) {
        if (strategy == null) {
            throw new IllegalStateException("请选择支付方式");
        }
        
        if (!strategy.isAvailable(amount)) {
            return new PaymentResult(false, 
                strategy.getPaymentMethod() + "不支持该金额", null);
        }
        
        System.out.println("开始处理支付...");
        PaymentResult result = strategy.pay(amount, info);
        
        if (result.isSuccess()) {
            System.out.println("✅ 支付成功！交易号: " + result.getTransactionId());
        } else {
            System.out.println("❌ 支付失败: " + result.getMessage());
        }
        
        return result;
    }
}
```

### 3.3 智能支付推荐


**💡 根据金额推荐最优支付方式**：
```java
class PaymentRecommender {
    public static PaymentStrategy recommend(double amount) {
        System.out.println("💡 为您推荐最优支付方式:");
        
        if (amount <= 100) {
            System.out.println("小额支付推荐: 微信支付（便捷快速）");
            return new WechatPayStrategy();
        } else if (amount <= 5000) {
            System.out.println("中额支付推荐: 支付宝（优惠多）");
            return new AlipayStrategy();
        } else {
            System.out.println("大额支付推荐: 信用卡（无限额）");
            return new CreditCardStrategy();
        }
    }
}
```

**🛒 实际购物场景**：
```java
public class ShoppingExample {
    public static void main(String[] args) {
        PaymentProcessor processor = new PaymentProcessor();
        PaymentInfo info = new PaymentInfo("user123", "order456");
        
        // 场景1：小额购物
        double amount1 = 50.0;
        processor.setPaymentStrategy(PaymentRecommender.recommend(amount1));
        processor.processPayment(amount1, info);
        
        System.out.println("\n" + "=".repeat(50) + "\n");
        
        // 场景2：大额购物
        double amount2 = 8000.0;
        processor.setPaymentStrategy(PaymentRecommender.recommend(amount2));
        processor.processPayment(amount2, info);
    }
}
```

---

## 4. 🏷️ 折扣计算策略


### 4.1 电商促销场景


**多样化的优惠策略**：
```
🎯 电商平台常见优惠：
🆕 新用户专享：首单9折
👑 VIP会员折扣：全场8.5折
💰 满减优惠：满200减50
🔢 买N送N活动：买2送1
📅 节日促销：双11全场7折
🎁 积分抵扣：1000积分抵10元
```

### 4.2 折扣策略设计


**折扣策略接口**：
```java
// 折扣策略接口
interface DiscountStrategy {
    double calculateDiscount(OrderInfo order);
    String getDiscountDescription();
    boolean isApplicable(OrderInfo order);
}

// 订单信息类
class OrderInfo {
    private double originalPrice;
    private String userType;
    private int quantity;
    private int userPoints;
    private boolean isFirstOrder;
    
    // 构造函数和getter/setter...
    public OrderInfo(double price, String userType, int quantity, 
                    int points, boolean isFirst) {
        this.originalPrice = price;
        this.userType = userType;
        this.quantity = quantity;
        this.userPoints = points;
        this.isFirstOrder = isFirst;
    }
    
    // getter方法...
}
```

**具体折扣策略**：
```java
// 新用户折扣策略
class NewUserDiscountStrategy implements DiscountStrategy {
    @Override
    public double calculateDiscount(OrderInfo order) {
        if (isApplicable(order)) {
            return order.getOriginalPrice() * 0.1; // 9折优惠，返回优惠金额
        }
        return 0;
    }
    
    @Override
    public String getDiscountDescription() {
        return "🆕 新用户专享9折优惠";
    }
    
    @Override
    public boolean isApplicable(OrderInfo order) {
        return order.isFirstOrder();
    }
}

// VIP会员折扣策略
class VipDiscountStrategy implements DiscountStrategy {
    @Override
    public double calculateDiscount(OrderInfo order) {
        if (isApplicable(order)) {
            return order.getOriginalPrice() * 0.15; // 8.5折优惠
        }
        return 0;
    }
    
    @Override
    public String getDiscountDescription() {
        return "👑 VIP会员专享8.5折优惠";
    }
    
    @Override
    public boolean isApplicable(OrderInfo order) {
        return "VIP".equals(order.getUserType());
    }
}

// 满减策略
class FullReductionStrategy implements DiscountStrategy {
    private double threshold;
    private double reduction;
    
    public FullReductionStrategy(double threshold, double reduction) {
        this.threshold = threshold;
        this.reduction = reduction;
    }
    
    @Override
    public double calculateDiscount(OrderInfo order) {
        if (isApplicable(order)) {
            int times = (int)(order.getOriginalPrice() / threshold);
            return times * reduction;
        }
        return 0;
    }
    
    @Override
    public String getDiscountDescription() {
        return String.format("💰 满%.0f减%.0f优惠", threshold, reduction);
    }
    
    @Override
    public boolean isApplicable(OrderInfo order) {
        return order.getOriginalPrice() >= threshold;
    }
}

// 积分抵扣策略
class PointsDiscountStrategy implements DiscountStrategy {
    @Override
    public double calculateDiscount(OrderInfo order) {
        if (isApplicable(order)) {
            // 100积分抵1元，最多抵扣订单金额的50%
            double maxDiscount = order.getOriginalPrice() * 0.5;
            double pointsDiscount = order.getUserPoints() / 100.0;
            return Math.min(pointsDiscount, maxDiscount);
        }
        return 0;
    }
    
    @Override
    public String getDiscountDescription() {
        return "🎁 积分抵扣优惠（100积分=1元）";
    }
    
    @Override
    public boolean isApplicable(OrderInfo order) {
        return order.getUserPoints() >= 100;
    }
}
```

### 4.3 折扣计算器


**多策略组合计算**：
```java
class DiscountCalculator {
    private List<DiscountStrategy> strategies = new ArrayList<>();
    
    public void addStrategy(DiscountStrategy strategy) {
        strategies.add(strategy);
    }
    
    public DiscountResult calculateBestDiscount(OrderInfo order) {
        double maxDiscount = 0;
        DiscountStrategy bestStrategy = null;
        
        System.out.println("🔍 正在为您寻找最优优惠...");
        System.out.println("原价: ¥" + order.getOriginalPrice());
        System.out.println();
        
        for (DiscountStrategy strategy : strategies) {
            if (strategy.isApplicable(order)) {
                double discount = strategy.calculateDiscount(order);
                System.out.printf("✅ %s: 优惠¥%.2f%n", 
                    strategy.getDiscountDescription(), discount);
                
                if (discount > maxDiscount) {
                    maxDiscount = discount;
                    bestStrategy = strategy;
                }
            } else {
                System.out.printf("❌ %s: 不满足条件%n", 
                    strategy.getDiscountDescription());
            }
        }
        
        System.out.println();
        if (bestStrategy != null) {
            double finalPrice = order.getOriginalPrice() - maxDiscount;
            System.out.printf("🏆 最优选择: %s%n", bestStrategy.getDiscountDescription());
            System.out.printf("💰 优惠金额: ¥%.2f%n", maxDiscount);
            System.out.printf("🎯 最终价格: ¥%.2f%n", finalPrice);
            
            return new DiscountResult(maxDiscount, finalPrice, bestStrategy);
        } else {
            System.out.println("😔 很抱歉，暂无可用优惠");
            return new DiscountResult(0, order.getOriginalPrice(), null);
        }
    }
}

// 折扣结果类
class DiscountResult {
    private double discountAmount;
    private double finalPrice;
    private DiscountStrategy appliedStrategy;
    
    public DiscountResult(double discountAmount, double finalPrice, 
                         DiscountStrategy appliedStrategy) {
        this.discountAmount = discountAmount;
        this.finalPrice = finalPrice;
        this.appliedStrategy = appliedStrategy;
    }
    
    // getter方法...
}
```

**🛍️ 购物场景演示**：
```java
public class ShoppingDiscountExample {
    public static void main(String[] args) {
        DiscountCalculator calculator = new DiscountCalculator();
        
        // 添加各种折扣策略
        calculator.addStrategy(new NewUserDiscountStrategy());
        calculator.addStrategy(new VipDiscountStrategy());
        calculator.addStrategy(new FullReductionStrategy(200, 50));
        calculator.addStrategy(new PointsDiscountStrategy());
        
        // 场景1：新用户购物
        System.out.println("=== 场景1：新用户首次购物 ===");
        OrderInfo newUserOrder = new OrderInfo(300, "NORMAL", 1, 500, true);
        calculator.calculateBestDiscount(newUserOrder);
        
        System.out.println("\n" + "=".repeat(50) + "\n");
        
        // 场景2：VIP用户购物
        System.out.println("=== 场景2：VIP用户购物 ===");
        OrderInfo vipOrder = new OrderInfo(300, "VIP", 2, 1500, false);
        calculator.calculateBestDiscount(vipOrder);
    }
}
```

---

## 5. 🗜️ 数据压缩策略


### 5.1 文件压缩需求场景


**不同文件类型需要不同压缩策略**：
```
📁 文件压缩策略选择：
📝 文本文件 → ZIP压缩：通用性好，压缩率中等
🖼️ 图片文件 → JPEG压缩：有损压缩，文件更小
🎵 音频文件 → MP3压缩：音质可控，大小适中
🎬 视频文件 → H.264压缩：高质量，广泛支持
📄 PDF文档 → 专用压缩：保持格式，减小体积
```

### 5.2 压缩策略设计


**压缩策略接口**：
```java
// 压缩策略接口
interface CompressionStrategy {
    CompressResult compress(FileData fileData);
    String getCompressionType();
    boolean supports(String fileType);
    double getExpectedCompressionRatio();
}

// 文件数据类
class FileData {
    private String fileName;
    private String fileType;
    private byte[] data;
    private long originalSize;
    
    public FileData(String fileName, String fileType, byte[] data) {
        this.fileName = fileName;
        this.fileType = fileType;
        this.data = data;
        this.originalSize = data.length;
    }
    
    // getter方法...
}

// 压缩结果类
class CompressResult {
    private byte[] compressedData;
    private long originalSize;
    private long compressedSize;
    private String compressionType;
    private long compressionTime;
    
    public CompressResult(byte[] compressedData, long originalSize, 
                         String compressionType, long compressionTime) {
        this.compressedData = compressedData;
        this.originalSize = originalSize;
        this.compressedSize = compressedData.length;
        this.compressionType = compressionType;
        this.compressionTime = compressionTime;
    }
    
    public double getCompressionRatio() {
        return ((double)(originalSize - compressedSize) / originalSize) * 100;
    }
    
    // getter方法...
}
```

**具体压缩策略**：
```java
// ZIP压缩策略
class ZipCompressionStrategy implements CompressionStrategy {
    @Override
    public CompressResult compress(FileData fileData) {
        System.out.println("🗜️ 使用ZIP压缩: " + fileData.getFileName());
        
        long startTime = System.currentTimeMillis();
        
        // 模拟ZIP压缩过程
        byte[] compressed = simulateZipCompression(fileData.getData());
        
        long endTime = System.currentTimeMillis();
        long compressionTime = endTime - startTime;
        
        return new CompressResult(compressed, fileData.getOriginalSize(), 
                                "ZIP", compressionTime);
    }
    
    @Override
    public String getCompressionType() {
        return "ZIP压缩";
    }
    
    @Override
    public boolean supports(String fileType) {
        return true; // ZIP支持所有文件类型
    }
    
    @Override
    public double getExpectedCompressionRatio() {
        return 30.0; // 预期30%的压缩率
    }
    
    private byte[] simulateZipCompression(byte[] data) {
        // 模拟压缩：实际项目中使用java.util.zip
        return new byte[(int)(data.length * 0.7)]; // 模拟70%大小
    }
}

// GZIP压缩策略
class GzipCompressionStrategy implements CompressionStrategy {
    @Override
    public CompressResult compress(FileData fileData) {
        System.out.println("⚡ 使用GZIP压缩: " + fileData.getFileName());
        
        long startTime = System.currentTimeMillis();
        byte[] compressed = simulateGzipCompression(fileData.getData());
        long endTime = System.currentTimeMillis();
        
        return new CompressResult(compressed, fileData.getOriginalSize(), 
                                "GZIP", endTime - startTime);
    }
    
    @Override
    public String getCompressionType() {
        return "GZIP压缩";
    }
    
    @Override
    public boolean supports(String fileType) {
        // GZIP特别适合文本文件
        return fileType.equals("txt") || fileType.equals("log") || 
               fileType.equals("csv") || fileType.equals("json");
    }
    
    @Override
    public double getExpectedCompressionRatio() {
        return 50.0; // 文本文件压缩率更高
    }
    
    private byte[] simulateGzipCompression(byte[] data) {
        return new byte[(int)(data.length * 0.5)]; // 模拟50%大小
    }
}

// 图片专用压缩策略
class ImageCompressionStrategy implements CompressionStrategy {
    @Override
    public CompressResult compress(FileData fileData) {
        System.out.println("🖼️ 使用图片专用压缩: " + fileData.getFileName());
        
        long startTime = System.currentTimeMillis();
        byte[] compressed = simulateImageCompression(fileData.getData());
        long endTime = System.currentTimeMillis();
        
        return new CompressResult(compressed, fileData.getOriginalSize(), 
                                "图片压缩", endTime - startTime);
    }
    
    @Override
    public String getCompressionType() {
        return "图片专用压缩";
    }
    
    @Override
    public boolean supports(String fileType) {
        return fileType.equals("jpg") || fileType.equals("png") || 
               fileType.equals("bmp") || fileType.equals("gif");
    }
    
    @Override
    public double getExpectedCompressionRatio() {
        return 60.0; // 图片压缩率很高
    }
    
    private byte[] simulateImageCompression(byte[] data) {
        return new byte[(int)(data.length * 0.4)]; // 模拟40%大小
    }
}
```

### 5.3 智能压缩选择器


**🤖 自动选择最优压缩策略**：
```java
class CompressionSelector {
    private List<CompressionStrategy> strategies = new ArrayList<>();
    
    public CompressionSelector() {
        strategies.add(new ZipCompressionStrategy());
        strategies.add(new GzipCompressionStrategy());
        strategies.add(new ImageCompressionStrategy());
    }
    
    public CompressionStrategy selectBestStrategy(FileData fileData) {
        System.out.println("🔍 为文件选择最优压缩策略: " + fileData.getFileName());
        
        List<CompressionStrategy> supportedStrategies = strategies.stream()
            .filter(strategy -> strategy.supports(fileData.getFileType()))
            .collect(Collectors.toList());
        
        if (supportedStrategies.isEmpty()) {
            System.out.println("⚠️ 使用默认ZIP压缩");
            return new ZipCompressionStrategy();
        }
        
        // 选择预期压缩率最高的策略
        CompressionStrategy bestStrategy = supportedStrategies.stream()
            .max(Comparator.comparing(CompressionStrategy::getExpectedCompressionRatio))
            .orElse(new ZipCompressionStrategy());
        
        System.out.printf("💡 推荐策略: %s (预期压缩率: %.1f%%)%n", 
                         bestStrategy.getCompressionType(), 
                         bestStrategy.getExpectedCompressionRatio());
        
        return bestStrategy;
    }
}
```

**📁 文件压缩管理器**：
```java
class FileCompressor {
    private CompressionSelector selector = new CompressionSelector();
    
    public CompressResult compressFile(FileData fileData) {
        System.out.println("=" .repeat(50));
        System.out.println("📁 开始压缩文件: " + fileData.getFileName());
        System.out.println("📊 原始大小: " + formatFileSize(fileData.getOriginalSize()));
        
        CompressionStrategy strategy = selector.selectBestStrategy(fileData);
        CompressResult result = strategy.compress(fileData);
        
        System.out.println("✅ 压缩完成!");
        System.out.println("📉 压缩后大小: " + formatFileSize(result.getCompressedSize()));
        System.out.printf("📈 压缩率: %.1f%%%n", result.getCompressionRatio());
        System.out.println("⏱️ 耗时: " + result.getCompressionTime() + "ms");
        
        return result;
    }
    
    private String formatFileSize(long bytes) {
        if (bytes < 1024) return bytes + " B";
        if (bytes < 1024 * 1024) return String.format("%.1f KB", bytes / 1024.0);
        return String.format("%.1f MB", bytes / (1024.0 * 1024.0));
    }
}
```

**💾 压缩演示场景**：
```java
public class CompressionExample {
    public static void main(String[] args) {
        FileCompressor compressor = new FileCompressor();
        
        // 压缩不同类型的文件
        FileData textFile = new FileData("document.txt", "txt", new byte[50000]);
        FileData imageFile = new FileData("photo.jpg", "jpg", new byte[200000]);
        FileData dataFile = new FileData("backup.zip", "zip", new byte[1000000]);
        
        compressor.compressFile(textFile);
        compressor.compressFile(imageFile);
        compressor.compressFile(dataFile);
    }
}
```

---

## 6. 🗺️ 路由算法策略


### 6.1 网络路由场景


**不同场景需要不同路由策略**：
```
🌐 网络路由策略选择：
⚡ 最短路径 → 延迟最低，适合实时通信
💰 最低成本 → 费用最少，适合大数据传输
🚦 负载均衡 → 分散流量，避免拥堵
🔄 容错路由 → 自动绕行，提高可靠性
📊 动态路由 → 实时调整，适应网络变化
```

### 6.2 路由策略设计


**路由策略接口**：
```java
// 网络节点类
class NetworkNode {
    private String nodeId;
    private String nodeName;
    
    public NetworkNode(String nodeId, String nodeName) {
        this.nodeId = nodeId;
        this.nodeName = nodeName;
    }
    
    // getter方法...
    @Override
    public String toString() {
        return nodeName + "(" + nodeId + ")";
    }
}

// 路由路径类
class RoutePath {
    private List<NetworkNode> nodes;
    private double totalCost;
    private double totalDistance;
    private int hopCount;
    
    public RoutePath() {
        this.nodes = new ArrayList<>();
    }
    
    public void addNode(NetworkNode node) {
        nodes.add(node);
        hopCount = nodes.size() - 1;
    }
    
    // getter/setter方法...
    @Override
    public String toString() {
        return nodes.stream()
                   .map(NetworkNode::toString)
                   .collect(Collectors.joining(" → "));
    }
}

// 路由策略接口
interface RoutingStrategy {
    RoutePath findRoute(NetworkNode source, NetworkNode destination, 
                       NetworkTopology topology);
    String getStrategyName();
    String getDescription();
}

// 网络拓扑类（简化版）
class NetworkTopology {
    private Map<String, Map<String, Double>> distanceMap;
    private Map<String, Map<String, Double>> costMap;
    private Map<String, Double> nodeLoadMap;
    
    public NetworkTopology() {
        initializeNetwork();
    }
    
    private void initializeNetwork() {
        // 模拟网络拓扑数据
        distanceMap = new HashMap<>();
        costMap = new HashMap<>();
        nodeLoadMap = new HashMap<>();
        
        // 简化的网络数据初始化
        // 实际项目中会从配置文件或数据库读取
    }
    
    // 获取两节点间距离
    public double getDistance(String from, String to) {
        return distanceMap.getOrDefault(from, new HashMap<>())
                         .getOrDefault(to, Double.MAX_VALUE);
    }
    
    // 获取两节点间成本
    public double getCost(String from, String to) {
        return costMap.getOrDefault(from, new HashMap<>())
                     .getOrDefault(to, Double.MAX_VALUE);
    }
    
    // 获取节点负载
    public double getNodeLoad(String nodeId) {
        return nodeLoadMap.getOrDefault(nodeId, 0.0);
    }
}
```

**具体路由策略**：
```java
// 最短路径策略
class ShortestPathStrategy implements RoutingStrategy {
    @Override
    public RoutePath findRoute(NetworkNode source, NetworkNode destination, 
                              NetworkTopology topology) {
        System.out.println("🚀 使用最短路径算法寻找路由");
        
        RoutePath path = new RoutePath();
        path.addNode(source);
        
        // 模拟Dijkstra算法寻找最短路径
        // 实际实现会更复杂
        path.addNode(new NetworkNode("node2", "中转节点2"));
        path.addNode(destination);
        
        path.setTotalDistance(100.0);
        path.setTotalCost(50.0);
        
        System.out.println("✅ 找到最短路径: " + path);
        System.out.println("📏 总距离: " + path.getTotalDistance() + "km");
        
        return path;
    }
    
    @Override
    public String getStrategyName() {
        return "最短路径策略";
    }
    
    @Override
    public String getDescription() {
        return "优先选择物理距离最短的路径，延迟最低";
    }
}

// 最低成本策略
class LowestCostStrategy implements RoutingStrategy {
    @Override
    public RoutePath findRoute(NetworkNode source, NetworkNode destination, 
                              NetworkTopology topology) {
        System.out.println("💰 使用最低成本算法寻找路由");
        
        RoutePath path = new RoutePath();
        path.addNode(source);
        
        // 模拟成本优先的路径计算
        path.addNode(new NetworkNode("node3", "廉价节点3"));
        path.addNode(new NetworkNode("node4", "廉价节点4"));
        path.addNode(destination);
        
        path.setTotalDistance(150.0);
        path.setTotalCost(20.0);
        
        System.out.println("✅ 找到最低成本路径: " + path);
        System.out.println("💵 总成本: ¥" + path.getTotalCost());
        
        return path;
    }
    
    @Override
    public String getStrategyName() {
        return "最低成本策略";
    }
    
    @Override
    public String getDescription() {
        return "优先选择传输成本最低的路径，节省费用";
    }
}

// 负载均衡策略
class LoadBalancingStrategy implements RoutingStrategy {
    @Override
    public RoutePath findRoute(NetworkNode source, NetworkNode destination, 
                              NetworkTopology topology) {
        System.out.println("⚖️ 使用负载均衡算法寻找路由");
        
        RoutePath path = new RoutePath();
        path.addNode(source);
        
        // 选择负载较轻的节点
        path.addNode(new NetworkNode("node5", "轻负载节点5"));
        path.addNode(destination);
        
        path.setTotalDistance(120.0);
        path.setTotalCost(35.0);
        
        System.out.println("✅ 找到负载均衡路径: " + path);
        System.out.println("📊 平均负载: 35%");
        
        return path;
    }
    
    @Override
    public String getStrategyName() {
        return "负载均衡策略";
    }
    
    @Override
    public String getDescription() {
        return "选择负载较轻的路径，避免网络拥塞";
    }
}
```

### 6.3 智能路由器


**🤖 根据需求自动选择路由策略**：
```java
class SmartRouter {
    private Map<String, RoutingStrategy> strategies;
    
    public SmartRouter() {
        strategies = new HashMap<>();
        strategies.put("shortest", new ShortestPathStrategy());
        strategies.put("cheapest", new LowestCostStrategy());
        strategies.put("balanced", new LoadBalancingStrategy());
    }
    
    public RoutePath route(NetworkNode source, NetworkNode destination, 
                          String priority, NetworkTopology topology) {
        
        System.out.println("🗺️ 智能路由器开始计算路径");
        System.out.println("📍 起点: " + source);
        System.out.println("🎯 终点: " + destination);
        System.out.println("🎯 优先级: " + priority);
        System.out.println();
        
        RoutingStrategy strategy = selectStrategy(priority);
        
        System.out.println("💡 选择策略: " + strategy.getStrategyName());
        System.out.println("📝 策略说明: " + strategy.getDescription());
        System.out.println();
        
        RoutePath result = strategy.findRoute(source, destination, topology);
        
        System.out.println("\n🎉 路由计算完成!");
        return result;
    }
    
    private RoutingStrategy selectStrategy(String priority) {
        switch (priority.toLowerCase()) {
            case "speed":
            case "fast":
                return strategies.get("shortest");
            case "cost":
            case "cheap":
                return strategies.get("cheapest");
            case "balance":
            case "stable":
                return strategies.get("balanced");
            default:
                System.out.println("⚠️ 未知优先级，使用默认最短路径策略");
                return strategies.get("shortest");
        }
    }
}
```

**📡 路由场景演示**：
```java
public class RoutingExample {
    public static void main(String[] args) {
        SmartRouter router = new SmartRouter();
        NetworkTopology topology = new NetworkTopology();
        
        NetworkNode source = new NetworkNode("A", "服务器A");
        NetworkNode destination = new NetworkNode("Z", "客户端Z");
        
        // 场景1：游戏数据传输（要求速度）
        System.out.println("=== 场景1：在线游戏数据传输 ===");
        router.route(source, destination, "speed", topology);
        
        System.out.println("\n" + "=".repeat(60) + "\n");
        
        // 场景2：文件备份（要求成本低）
        System.out.println("=== 场景2：大文件备份传输 ===");
        router.route(source, destination, "cost", topology);
        
        System.out.println("\n" + "=".repeat(60) + "\n");
        
        // 场景3：正常业务（要求稳定）
        System.out.println("=== 场景3：日常业务数据传输 ===");
        router.route(source, destination, "balance", topology);
    }
}
```

---

## 7. 📋 核心要点总结


### 7.1 策略模式实际应用的核心价值


> 📌 **核心理解**  
> 策略模式就像一个"万能工具箱"，里面有各种专用工具（策略），根据不同的工作需求（业务场景），选择最合适的工具来完成任务。

**🎯 实际开发中的价值**：
- **业务灵活性**：新增业务规则不影响现有代码
- **代码可维护性**：每个策略独立，便于调试和修改
- **系统可扩展性**：轻松适应业务变化和新需求
- **团队协作效率**：不同策略可以并行开发

### 7.2 五大应用场景的共同模式


| 应用场景 | **核心目标** | **策略选择依据** | **实际收益** |
|---------|-------------|-----------------|-------------|
| 🔢 **排序算法** | `性能优化` | `数据量大小` | `提升处理效率` |
| 💳 **支付方式** | `用户体验` | `金额和偏好` | `提高成功率` |
| 🏷️ **折扣计算** | `营销效果` | `用户类型和订单` | `增加销售额` |
| 🗜️ **数据压缩** | `存储优化` | `文件类型和大小` | `节省存储成本` |
| 🗺️ **路由算法** | `网络性能` | `优先级需求` | `优化传输效果` |

### 7.3 策略模式的最佳实践


**✅ 设计原则**：
```markdown
1. 接口设计简洁：策略接口方法不宜过多
2. 策略独立性：每个策略应该完全独立
3. 参数标准化：统一的输入输出格式
4. 错误处理：每个策略都要有异常处理
5. 性能考虑：避免频繁的策略切换开销
```

**🔄 实施步骤**：
```
第一步：识别变化点
↓ 找出代码中经常变化的算法或规则

第二步：提取策略接口  
↓ 定义统一的策略接口和方法

第三步：实现具体策略
↓ 将不同算法封装成策略类

第四步：创建环境类
↓ 负责策略的选择和调用

第五步：智能选择器
↓ 根据条件自动选择最优策略
```

### 7.4 避免常见陷阱


> ⚠️ **注意事项**  
> 策略模式不是万能的，过度使用会增加系统复杂度。只有在真正需要频繁切换算法或规则的场景下才应该使用。

**❌ 常见误区**：
- **过度设计**：为简单的`if-else`使用策略模式
- **策略爆炸**：创建过多细粒度的策略类
- **忽略性能**：频繁创建策略对象的开销
- **缺乏文档**：策略选择逻辑没有清晰说明

**✅ 正确做法**：
- **适度抽象**：确实有多种算法才使用策略模式
- **合理粒度**：策略粒度要适中，不要过细
- **对象复用**：策略对象可以设计为单例复用
- **清晰文档**：详细说明何时选择哪种策略

### 7.5 学习检验清单


**🎓 自检清单**：
- [ ] 理解策略模式解决的核心问题
- [ ] 能识别适合使用策略模式的场景  
- [ ] 掌握策略接口的设计原则
- [ ] 会实现具体的策略类
- [ ] 能设计智能的策略选择器
- [ ] 理解策略模式的优缺点
- [ ] 知道何时不应该使用策略模式

**📚 进阶学习建议**：
- 结合工厂模式创建策略对象
- 使用注解和反射实现策略自动注册
- 在Spring框架中使用策略模式
- 学习状态模式与策略模式的区别

**🔤 记忆口诀**：
```
策略模式像工具箱，不同场景选不同
算法封装各自立，运行时刻来决定
支付折扣和压缩，路由排序都能用
接口统一易扩展，代码清晰好维护
```

---

> 💡 **学习提示**  
> 策略模式是设计模式中最实用的模式之一，在实际项目中应用非常广泛。掌握了策略模式，你就掌握了面向对象设计的精髓之一：将变化封装起来，让系统更加灵活和可维护。