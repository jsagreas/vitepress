---
title: 1、策略模式概念原理
---
## 📚 目录

1. [策略模式基本概念](#1-策略模式基本概念)
2. [算法封装问题的解决](#2-算法封装问题的解决)
3. [运行时算法切换机制](#3-运行时算法切换机制)
4. [应用场景深度分析](#4-应用场景深度分析)
5. [优缺点全面对比](#5-优缺点全面对比)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 策略模式基本概念


### 1.1 什么是策略模式


**🔸 生活中的策略选择**
```
出行方式的选择：
目的地：公司（10公里）

策略选择：
🚗 开车：舒适但可能堵车
🚇 地铁：稳定但需要换乘  
🚲 骑车：环保但比较累
🚶 步行：健康但太慢

根据不同情况（天气、时间、心情）选择不同策略
```

**📋 策略模式的正式定义**
> 💡 **策略模式**：定义一系列算法，把它们一个个封装起来，并且使它们可以相互替换。策略模式让算法的变化独立于使用算法的客户端。

**🔑 核心理念**
```
传统方式的问题：
if (情况A) {
    执行算法A
} else if (情况B) {
    执行算法B  
} else if (情况C) {
    执行算法C
}
→ 代码臃肿，难以维护，违反开闭原则

策略模式的解决：
选择具体策略 → 执行对应算法 → 获得结果
→ 算法独立封装，可以自由切换和扩展
```

### 1.2 策略模式的组成结构


**🏗 三大核心角色**

```
策略模式结构图：

    客户端
      ↓
   ┌─────────┐      使用      ┌─────────────┐
   │ Context │ ─────────────→ │  Strategy   │ ← 抽象策略
   │ 环境类   │                │   接口      │
   └─────────┘                └─────────────┘
                                      ↑
                              ┌───────┼───────┐
                              │       │       │
                     ┌────────────┐ ┌────────────┐ ┌────────────┐
                     │Strategy A  │ │Strategy B  │ │Strategy C  │
                     │具体策略A   │ │具体策略B   │ │具体策略C   │  
                     └────────────┘ └────────────┘ └────────────┘
```

**角色详解**：

**🎭 Context（环境类）**
- **作用**：持有策略对象的引用，负责调用策略
- **职责**：不知道具体策略细节，只知道如何使用策略接口
- **比喻**：就像司机，不需要知道发动机内部构造，只需要知道如何启动

**🎯 Strategy（抽象策略）** 
- **作用**：定义所有策略的统一接口
- **职责**：规定策略的调用方式和参数
- **比喻**：就像遥控器的统一按钮，不同品牌电视都能用

**⚙️ ConcreteStrategy（具体策略）**
- **作用**：实现具体的算法逻辑
- **职责**：封装特定的算法实现
- **比喻**：就像不同的出行工具，都能到达目的地但方式不同

### 1.3 策略模式的工作流程


**🔄 执行流程图**
```
开始
  ↓
客户端创建Context
  ↓  
客户端选择具体策略
  ↓
Context设置策略对象
  ↓
Context调用策略方法
  ↓
具体策略执行算法
  ↓
返回执行结果
  ↓
结束
```

**💡 核心思想**
- **分离关注点**：算法实现与算法使用分离
- **面向接口编程**：依赖抽象而不是具体实现
- **运行时决策**：可以在程序运行时动态选择算法

---

## 2. 🔧 算法封装问题的解决


### 2.1 传统方式的问题分析


**❌ 不使用策略模式的代码**
```java
// 计算器类 - 传统方式
public class Calculator {
    public double calculate(double a, double b, String operation) {
        if ("add".equals(operation)) {
            return a + b;
        } else if ("subtract".equals(operation)) {
            return a - b;
        } else if ("multiply".equals(operation)) {
            return a * b;
        } else if ("divide".equals(operation)) {
            if (b != 0) {
                return a / b;
            } else {
                throw new IllegalArgumentException("除数不能为0");
            }
        }
        throw new IllegalArgumentException("不支持的运算类型");
    }
}
```

**🚨 存在的问题**
```
1. 违反开闭原则：
   - 新增运算类型需要修改calculate方法
   - 每次修改都可能引入新bug

2. 违反单一职责原则：
   - Calculator类承担了多种运算的职责
   - 职责过于复杂，难以测试

3. 代码难以维护：
   - if-else分支过多，逻辑混乱
   - 算法逻辑与调用逻辑耦合

4. 可扩展性差：
   - 添加复杂算法会让方法变得巨大
   - 无法在运行时动态添加新算法
```

### 2.2 策略模式的改进方案


**✅ 使用策略模式重构**

**第一步：定义策略接口**
```java
// 运算策略接口
public interface CalculationStrategy {
    double calculate(double a, double b);
    String getOperationName(); // 获取运算名称
}
```

**第二步：实现具体策略**
```java
// 加法策略
public class AddStrategy implements CalculationStrategy {
    @Override
    public double calculate(double a, double b) {
        return a + b;
    }
    
    @Override
    public String getOperationName() {
        return "加法";
    }
}

// 除法策略
public class DivideStrategy implements CalculationStrategy {
    @Override
    public double calculate(double a, double b) {
        if (b == 0) {
            throw new IllegalArgumentException("除数不能为0");
        }
        return a / b;
    }
    
    @Override
    public String getOperationName() {
        return "除法";
    }
}
```

**第三步：创建环境类**
```java
// 计算器环境类
public class Calculator {
    private CalculationStrategy strategy;
    
    // 设置计算策略
    public void setStrategy(CalculationStrategy strategy) {
        this.strategy = strategy;
    }
    
    // 执行计算
    public double calculate(double a, double b) {
        if (strategy == null) {
            throw new IllegalStateException("请先设置计算策略");
        }
        return strategy.calculate(a, b);
    }
    
    // 获取当前策略信息
    public String getCurrentOperation() {
        return strategy != null ? strategy.getOperationName() : "未设置";
    }
}
```

### 2.3 改进效果对比


**📊 改进前后对比表**

| 对比维度 | **改进前** | **改进后** |
|---------|-----------|-----------|
| **扩展性** | `需要修改Calculator类` | `只需新增策略类` |
| **维护性** | `if-else分支复杂` | `每个算法独立维护` |
| **测试性** | `需要测试所有分支` | `可以单独测试每个策略` |
| **复用性** | `算法与类绑定` | `策略可以在其他地方复用` |
| **职责** | `一个类多个职责` | `每个类单一职责` |

**🎯 核心改进**
```
封装变化：
- 每种算法都被封装在独立的策略类中
- 算法的变化不会影响其他部分

易于扩展：  
- 新增算法只需要新增策略类
- 不需要修改现有代码

运行时切换：
- 可以在程序运行时动态切换算法
- 提供了更大的灵活性
```

---

## 3. 🔄 运行时算法切换机制


### 3.1 动态策略切换的实现


**🎮 策略切换演示**
```java
public class StrategyDemo {
    public static void main(String[] args) {
        Calculator calculator = new Calculator();
        double a = 10, b = 5;
        
        // 动态切换加法策略
        calculator.setStrategy(new AddStrategy());
        System.out.println(a + " + " + b + " = " + calculator.calculate(a, b));
        // 输出：10.0 + 5.0 = 15.0
        
        // 动态切换减法策略
        calculator.setStrategy(new SubtractStrategy());
        System.out.println(a + " - " + b + " = " + calculator.calculate(a, b));
        // 输出：10.0 - 5.0 = 5.0
        
        // 动态切换乘法策略
        calculator.setStrategy(new MultiplyStrategy());
        System.out.println(a + " × " + b + " = " + calculator.calculate(a, b));
        // 输出：10.0 × 5.0 = 50.0
    }
}
```

### 3.2 基于条件的策略选择


**🎯 智能策略选择器**
```java
public class StrategySelector {
    
    // 根据运算符选择策略
    public static CalculationStrategy selectByOperator(String operator) {
        switch (operator) {
            case "+": return new AddStrategy();
            case "-": return new SubtractStrategy();
            case "*": return new MultiplyStrategy();
            case "/": return new DivideStrategy();
            default: 
                throw new IllegalArgumentException("不支持的运算符: " + operator);
        }
    }
    
    // 根据数据特征选择最优策略
    public static CalculationStrategy selectOptimalStrategy(double a, double b) {
        // 如果是整数且较小，选择简单算法
        if (isSmallInteger(a) && isSmallInteger(b)) {
            return new SimpleCalculationStrategy();
        }
        // 如果是大数，选择高精度算法  
        else if (isLargeNumber(a) || isLargeNumber(b)) {
            return new PrecisionCalculationStrategy();
        }
        // 默认使用标准算法
        else {
            return new StandardCalculationStrategy();
        }
    }
    
    private static boolean isSmallInteger(double num) {
        return num == (int)num && Math.abs(num) < 1000;
    }
    
    private static boolean isLargeNumber(double num) {
        return Math.abs(num) > 1000000;
    }
}
```

### 3.3 策略缓存与性能优化


**⚡ 策略缓存机制**
```java
public class CachedCalculator {
    // 策略缓存，避免重复创建对象
    private static final Map<String, CalculationStrategy> strategyCache = new HashMap<>();
    
    static {
        // 预加载常用策略
        strategyCache.put("add", new AddStrategy());
        strategyCache.put("subtract", new SubtractStrategy());
        strategyCache.put("multiply", new MultiplyStrategy());
        strategyCache.put("divide", new DivideStrategy());
    }
    
    private CalculationStrategy currentStrategy;
    
    // 高效的策略切换
    public void setStrategy(String strategyName) {
        this.currentStrategy = strategyCache.get(strategyName);
        if (this.currentStrategy == null) {
            throw new IllegalArgumentException("不支持的策略: " + strategyName);
        }
    }
    
    public double calculate(double a, double b) {
        return currentStrategy.calculate(a, b);
    }
}
```

**💡 性能优化要点**
> 📌 **缓存策略对象**：避免频繁创建销毁对象的开销
> 
> 🚀 **延迟加载**：只在需要时才创建具体策略实例
>
> ⚡ **策略预热**：系统启动时预加载常用策略

---

## 4. 🎯 应用场景深度分析


### 4.1 经典应用场景


**💰 电商系统的促销策略**
```
业务场景：
不同用户享受不同的优惠策略

用户类型：
🥉 普通用户：无折扣
🥈 VIP用户：9折优惠  
🥇 超级VIP：8折优惠
💎 钻石用户：7折优惠 + 满减

策略实现思路：
interface DiscountStrategy {
    double calculateDiscount(double originalPrice, User user);
}
```

**🚗 出行路径规划**
```
应用场景：地图导航的路径选择

路径策略：
🏃 最短路径：距离最短，适合步行
⚡ 最快路径：时间最短，适合赶时间
💰 最省钱路径：费用最低，适合经济出行
🌿 风景路径：风景优美，适合休闲旅行

根据用户偏好和实时情况动态选择最佳策略
```

**📊 数据排序算法选择**
```
根据数据特征选择最适合的排序算法：

数据量小 (< 100)：插入排序
数据量中等 (100-10000)：快速排序  
数据量大 (> 10000)：归并排序
数据基本有序：冒泡排序
数据范围固定：计数排序
```

### 4.2 实际项目中的应用


**🏪 电商支付方式选择**
```java
// 支付策略接口
public interface PaymentStrategy {
    PaymentResult pay(Order order, double amount);
    boolean isAvailable(User user);
    String getPaymentMethodName();
}

// 支付环境类
public class PaymentProcessor {
    private PaymentStrategy paymentStrategy;
    
    public void setPaymentMethod(PaymentStrategy strategy) {
        this.paymentStrategy = strategy;
    }
    
    public PaymentResult processPayment(Order order, double amount) {
        if (!paymentStrategy.isAvailable(order.getUser())) {
            throw new IllegalStateException("当前支付方式不可用");
        }
        return paymentStrategy.pay(order, amount);
    }
}

// 使用示例
PaymentProcessor processor = new PaymentProcessor();

// 根据用户选择设置支付方式
if (userChoice.equals("alipay")) {
    processor.setPaymentMethod(new AlipayStrategy());
} else if (userChoice.equals("wechat")) {
    processor.setPaymentMethod(new WechatPayStrategy());
} else if (userChoice.equals("credit")) {
    processor.setPaymentMethod(new CreditCardStrategy());
}

PaymentResult result = processor.processPayment(order, totalAmount);
```

### 4.3 策略模式的适用判断


**✅ 适合使用策略模式的情况**
```
🎯 算法多样性：
- 同一个问题有多种解决方案
- 不同方案适用于不同场景

🔄 运行时切换：
- 需要在程序运行时选择算法
- 算法选择依赖于用户输入或系统状态

📈 易于扩展：
- 算法种类可能会增加
- 需要支持第三方算法插件

🧹 简化复杂分支：
- 现有代码有大量if-else或switch
- 条件判断逻辑复杂且难以维护
```

**❌ 不适合使用策略模式的情况**
```
🔒 算法固定：
- 算法数量少且基本不变
- 业务逻辑非常稳定

🎯 算法简单：
- 算法逻辑非常简单
- 使用策略模式反而增加复杂度

💰 性能要求极高：
- 对性能要求极其严格
- 不能承受多态调用的开销
```

---

## 5. ⚖️ 优缺点全面对比


### 5.1 策略模式的优点


**🎯 优点详细分析**

| 优点类别 | **具体表现** | **实际好处** |
|---------|-------------|-------------|
| **🔧 符合开闭原则** | `新增策略不需修改现有代码` | `降低维护风险，提高系统稳定性` |
| **🎭 单一职责原则** | `每个策略类只负责一种算法` | `代码结构清晰，易于理解和测试` |
| **🔄 运行时灵活性** | `可以动态切换算法` | `适应不同场景需求，用户体验更好` |
| **♻️ 提高代码复用** | `策略可以在多个地方使用` | `减少重复代码，提高开发效率` |
| **🧪 便于测试** | `每个策略可以独立测试` | `提高代码质量，减少bug数量` |

**💡 具体优势举例**
```
开闭原则体现：
- 原有策略：加减乘除四种运算
- 新增需求：增加求幂运算
- 策略模式：只需新增PowerStrategy类
- 传统方式：需要修改Calculator类的if-else逻辑

单一职责体现：
- AddStrategy：只负责加法运算
- DivideStrategy：只负责除法运算和零除检查
- Calculator：只负责调用策略，不关心算法细节

测试便利性：
- 可以单独测试DivideStrategy的零除情况
- 可以Mock策略对象测试Calculator
- 新增策略不影响现有测试用例
```

### 5.2 策略模式的缺点


**⚠️ 缺点分析与应对**

**🔸 客户端必须了解所有策略**
```
问题描述：
客户端需要知道每个策略的适用场景才能正确选择

解决方案：
1. 提供策略选择器工具类
2. 在环境类中封装选择逻辑
3. 提供策略推荐机制

示例：
// 不好的方式
if (数据量小) {
    calculator.setStrategy(new InsertionSortStrategy());
} else if (数据量大) {
    calculator.setStrategy(new MergeSortStrategy());
}

// 更好的方式  
SortStrategy strategy = StrategySelector.selectOptimal(data);
calculator.setStrategy(strategy);
```

**🔸 策略类数量增加**
```
问题描述：
每个算法都需要一个类，可能导致类的数量快速增长

影响评估：
✅ 可接受：算法逻辑复杂，独立类有价值
❌ 需优化：算法逻辑简单，类过多反而复杂

优化方案：
1. 将简单策略合并到枚举或配置中
2. 使用lambda表达式简化简单策略
3. 按功能模块分包组织策略类
```

**🔸 运行时开销**
```
性能影响：
- 多态方法调用比直接调用略慢
- 策略对象创建和管理有内存开销

优化策略：
1. 策略对象缓存和复用
2. 使用单例模式（如果策略无状态）
3. 在性能关键路径考虑直接调用

基准测试对比：
直接调用：1000万次运算 100ms
策略模式：1000万次运算 120ms  
开销增加：约20%（通常可接受）
```

### 5.3 与其他模式的对比


**🔄 策略模式 vs 状态模式**
```
相同点：
- 都使用组合和委托
- 都能在运行时改变对象行为

不同点：
策略模式：
- 关注算法的选择和切换
- 客户端主动选择策略
- 策略之间相互独立

状态模式：
- 关注对象状态的变化
- 状态自动转换
- 状态之间有转换关系
```

**🏭 策略模式 vs 工厂模式**
```
策略模式：
- 重点：算法的封装和切换
- 用途：运行时选择不同算法
- 结构：环境类持有策略对象

工厂模式：
- 重点：对象的创建
- 用途：根据条件创建不同对象
- 结构：工厂方法返回产品对象

联合使用：
工厂模式创建策略对象，策略模式使用这些对象
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 策略模式本质：将算法封装成独立的策略类，使它们可以相互替换
🔸 核心思想：分离算法的定义、封装和使用，提高代码的灵活性
🔸 主要角色：Context环境类、Strategy策略接口、ConcreteStrategy具体策略
🔸 关键特性：运行时切换、算法独立、易于扩展
🔸 设计原则：开闭原则、单一职责原则、依赖倒置原则
```

### 6.2 关键理解要点


**🔹 策略模式解决的核心问题**
```
问题根源：
- if-else分支过多，代码难以维护
- 算法与使用者紧耦合，扩展困难
- 算法无法在运行时动态切换

解决方案：
- 将算法抽象为策略接口
- 每种算法实现为独立的策略类
- 环境类通过组合使用策略对象
```

**🔹 何时使用策略模式**
```
使用时机：
✅ 有多种方式处理同一问题
✅ 需要在运行时选择算法
✅ 想要消除复杂的条件语句
✅ 希望算法独立变化

避免滥用：
❌ 算法非常简单且固定
❌ 策略数量很少且不会增长
❌ 性能要求极其严格的场景
```

**🔹 实现策略模式的最佳实践**
```
设计建议：
1. 策略接口保持简单，职责单一
2. 策略实现类保持无状态（如果可能）
3. 提供策略选择的辅助工具
4. 考虑策略的缓存和复用
5. 为复杂场景提供组合策略
```

### 6.3 学习检查清单


**📝 掌握程度自检**
- [ ] 能够识别适合使用策略模式的场景
- [ ] 理解策略模式的三个核心角色及其职责
- [ ] 会设计策略接口和实现具体策略类
- [ ] 能够实现运行时的策略切换
- [ ] 了解策略模式的优缺点和适用场景
- [ ] 可以区分策略模式与其他相似模式

**🎯 实战练习建议**
```
入门练习：
实现一个文件压缩工具，支持ZIP、RAR、7Z三种压缩格式

进阶练习：
设计一个游戏角色的技能系统，不同职业有不同的技能策略

高级练习：
开发一个多渠道消息推送系统，支持邮件、短信、微信等推送方式
```

### 6.4 记忆要点


**🧠 核心记忆口诀**
```
策略模式记忆法：
"算法多样需切换，策略模式是首选
接口定义共同点，具体实现各不同  
环境持有策略引用，运行时刻可替换
开闭原则得体现，扩展容易维护简"
```

**💫 关键词提取**
- **封装算法**：每种算法独立封装
- **可替换性**：策略可以相互替换
- **运行时切换**：程序运行时动态选择
- **开闭原则**：对扩展开放，对修改关闭
- **消除分支**：减少if-else和switch语句

**📚 扩展学习方向**
- **行为型模式**：状态模式、命令模式、模板方法模式
- **设计原则**：SOLID原则的深入理解和应用
- **架构模式**：插件架构、微服务架构中的策略应用
- **性能优化**：策略模式在高性能场景下的优化技巧

---

> 💡 **学习建议**：策略模式是设计模式中最容易理解和应用的模式之一。建议先通过简单的例子理解核心思想，然后在实际项目中寻找可以应用策略模式的场景进行练习。记住，好的设计不是为了炫技，而是为了解决实际问题，让代码更加清晰和易于维护。