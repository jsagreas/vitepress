---
title: 4、建造者模式实际应用
---
## 📚 目录

1. [建造者模式核心回顾](#1-建造者模式核心回顾)
2. [SQL查询构建器](#2-SQL查询构建器)
3. [HTTP请求构建器](#3-HTTP请求构建器)
4. [配置对象构建](#4-配置对象构建)
5. [UI组件构建器](#5-UI组件构建器)
6. [测试数据构建器](#6-测试数据构建器)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 建造者模式核心回顾


### 1.1 什么是建造者模式

**简单理解**：就像搭积木一样，一步一步地组装复杂对象

```
生活中的例子：
🏠 盖房子：先打地基 → 建墙体 → 装屋顶 → 内部装修
🍔 做汉堡：先拿面包 → 放生菜 → 加肉饼 → 放芝士 → 盖上面包
📱 组装手机：先装主板 → 加内存 → 装屏幕 → 加电池 → 装外壳
```

**核心思想**：
- **分步构建**：复杂对象分成多个简单步骤
- **可选配置**：每个步骤都是可选的，想要什么就加什么
- **链式调用**：一个方法接一个方法，写起来很流畅

### 1.2 为什么需要建造者模式


**🤔 想象一个场景**：
你要创建一个用户对象，有姓名、年龄、邮箱、电话、地址、爱好等信息

```
传统方式的问题：
User user = new User("张三", 25, "zhangsan@email.com", "13800138000", 
                    "北京市朝阳区", "读书,游泳,编程");
// 😵 参数太多了，容易搞错顺序
// 😵 如果只想设置姓名和邮箱怎么办？
// 😵 代码读起来不清楚每个参数是什么意思

建造者模式的解决：
User user = new UserBuilder()
    .setName("张三")
    .setAge(25)
    .setEmail("zhangsan@email.com")
    .setPhone("13800138000")
    .build();
// ✅ 清楚明白每个属性的作用
// ✅ 想设置什么就设置什么
// ✅ 代码读起来像说话一样自然
```

---

## 2. 🗄️ SQL查询构建器


### 2.1 SQL查询的复杂性


**传统SQL拼接的痛苦**：
```java
// 😱 传统方式：字符串拼接
String sql = "SELECT * FROM users WHERE 1=1";
if (name != null) {
    sql += " AND name = '" + name + "'";
}
if (age > 0) {
    sql += " AND age = " + age;
}
if (city != null) {
    sql += " AND city = '" + city + "'";
}
sql += " ORDER BY create_time DESC LIMIT 10";

// 问题：
// 1. 容易写错SQL语法
// 2. 字符串拼接容易出错
// 3. SQL注入安全隐患
// 4. 代码难读难维护
```

### 2.2 SQL建造者模式实现


**🔧 核心设计思路**：
```
查询步骤分解：
1. 选择字段 (SELECT)
2. 指定表名 (FROM) 
3. 添加条件 (WHERE)
4. 分组聚合 (GROUP BY)
5. 排序规则 (ORDER BY)
6. 限制数量 (LIMIT)
```

**💻 简化实现示例**：
```java
public class SQLBuilder {
    private StringBuilder sql = new StringBuilder();
    private List<String> selectFields = new ArrayList<>();
    private String tableName;
    private List<String> whereConditions = new ArrayList<>();
    private String orderBy;
    private Integer limit;
    
    // 选择字段
    public SQLBuilder select(String... fields) {
        for (String field : fields) {
            selectFields.add(field);
        }
        return this; // 返回自己，支持链式调用
    }
    
    // 指定表名
    public SQLBuilder from(String table) {
        this.tableName = table;
        return this;
    }
    
    // 添加WHERE条件
    public SQLBuilder where(String condition) {
        whereConditions.add(condition);
        return this;
    }
    
    // 排序
    public SQLBuilder orderBy(String column, String direction) {
        this.orderBy = column + " " + direction;
        return this;
    }
    
    // 限制数量
    public SQLBuilder limit(int count) {
        this.limit = count;
        return this;
    }
    
    // 构建最终SQL
    public String build() {
        sql.append("SELECT ");
        
        // 处理字段
        if (selectFields.isEmpty()) {
            sql.append("*");
        } else {
            sql.append(String.join(", ", selectFields));
        }
        
        // 处理表名
        sql.append(" FROM ").append(tableName);
        
        // 处理WHERE条件
        if (!whereConditions.isEmpty()) {
            sql.append(" WHERE ");
            sql.append(String.join(" AND ", whereConditions));
        }
        
        // 处理排序
        if (orderBy != null) {
            sql.append(" ORDER BY ").append(orderBy);
        }
        
        // 处理限制
        if (limit != null) {
            sql.append(" LIMIT ").append(limit);
        }
        
        return sql.toString();
    }
}
```

### 2.3 实际使用效果


**🎯 使用示例**：
```java
// 构建复杂查询
String sql = new SQLBuilder()
    .select("name", "age", "email")           // 选择字段
    .from("users")                           // 指定表名
    .where("age >= 18")                      // 添加条件
    .where("city = '北京'")                   // 再加条件
    .orderBy("create_time", "DESC")          // 排序
    .limit(20)                               // 限制数量
    .build();

// 生成的SQL：
// SELECT name, age, email FROM users 
// WHERE age >= 18 AND city = '北京' 
// ORDER BY create_time DESC LIMIT 20

// 简单查询
String simpleSQL = new SQLBuilder()
    .from("products")
    .where("price < 100")
    .build();
// 生成：SELECT * FROM products WHERE price < 100
```

**✅ 建造者模式的优势**：
- **可读性强**：代码像英语句子一样易读
- **灵活组合**：想要什么条件就加什么条件
- **避免错误**：不会因为参数顺序错误而出问题
- **易于扩展**：添加新的SQL功能很容易

---

## 3. 🌐 HTTP请求构建器


### 3.1 HTTP请求的复杂性


**现代HTTP请求的要素**：
```
HTTP请求组成部分：
📌 请求方法：GET、POST、PUT、DELETE等
📌 请求URL：协议 + 域名 + 路径 + 参数
📌 请求头：认证信息、内容类型、自定义头等
📌 请求体：JSON数据、表单数据、文件等
📌 超时设置：连接超时、读取超时
📌 重试策略：失败重试次数和间隔
```

**传统方式的麻烦**：
```java
// 😫 传统方式：各种配置分散
HttpURLConnection conn = (HttpURLConnection) url.openConnection();
conn.setRequestMethod("POST");
conn.setRequestProperty("Content-Type", "application/json");
conn.setRequestProperty("Authorization", "Bearer " + token);
conn.setRequestProperty("User-Agent", "MyApp/1.0");
conn.setDoOutput(true);
conn.setConnectTimeout(5000);
conn.setReadTimeout(10000);
// ... 还有更多配置
```

### 3.2 HTTP建造者实现


**🔧 设计思路**：
```
请求构建步骤：
1. 设置基础URL
2. 选择HTTP方法
3. 添加请求头
4. 设置请求参数
5. 配置请求体
6. 设置超时等选项
7. 执行请求
```

**💻 实现示例**：
```java
public class HttpRequestBuilder {
    private String url;
    private String method = "GET";
    private Map<String, String> headers = new HashMap<>();
    private Map<String, String> params = new HashMap<>();
    private String body;
    private int connectTimeout = 5000;
    private int readTimeout = 10000;
    
    public HttpRequestBuilder url(String url) {
        this.url = url;
        return this;
    }
    
    public HttpRequestBuilder get() {
        this.method = "GET";
        return this;
    }
    
    public HttpRequestBuilder post() {
        this.method = "POST";
        return this;
    }
    
    // 添加请求头
    public HttpRequestBuilder header(String name, String value) {
        headers.put(name, value);
        return this;
    }
    
    // 添加认证头
    public HttpRequestBuilder auth(String token) {
        return header("Authorization", "Bearer " + token);
    }
    
    // 设置JSON内容
    public HttpRequestBuilder json() {
        return header("Content-Type", "application/json");
    }
    
    // 添加URL参数
    public HttpRequestBuilder param(String name, String value) {
        params.put(name, value);
        return this;
    }
    
    // 设置请求体
    public HttpRequestBuilder body(String body) {
        this.body = body;
        return this;
    }
    
    // 设置超时
    public HttpRequestBuilder timeout(int connectTimeout, int readTimeout) {
        this.connectTimeout = connectTimeout;
        this.readTimeout = readTimeout;
        return this;
    }
    
    // 执行请求
    public String execute() throws IOException {
        // 构建完整URL（包含参数）
        String fullUrl = buildFullUrl();
        
        // 创建连接并配置
        HttpURLConnection conn = (HttpURLConnection) new URL(fullUrl).openConnection();
        conn.setRequestMethod(method);
        conn.setConnectTimeout(connectTimeout);
        conn.setReadTimeout(readTimeout);
        
        // 设置请求头
        for (Map.Entry<String, String> header : headers.entrySet()) {
            conn.setRequestProperty(header.getKey(), header.getValue());
        }
        
        // 设置请求体
        if (body != null) {
            conn.setDoOutput(true);
            try (OutputStream os = conn.getOutputStream()) {
                os.write(body.getBytes());
            }
        }
        
        // 读取响应
        try (BufferedReader reader = new BufferedReader(
                new InputStreamReader(conn.getInputStream()))) {
            StringBuilder response = new StringBuilder();
            String line;
            while ((line = reader.readLine()) != null) {
                response.append(line);
            }
            return response.toString();
        }
    }
    
    private String buildFullUrl() {
        if (params.isEmpty()) {
            return url;
        }
        
        StringBuilder urlBuilder = new StringBuilder(url);
        urlBuilder.append("?");
        
        for (Map.Entry<String, String> param : params.entrySet()) {
            urlBuilder.append(param.getKey())
                     .append("=")
                     .append(param.getValue())
                     .append("&");
        }
        
        // 移除最后的&
        return urlBuilder.substring(0, urlBuilder.length() - 1);
    }
}
```

### 3.3 实际使用效果


**🎯 各种场景的使用**：
```java
// 场景1：简单GET请求
String response1 = new HttpRequestBuilder()
    .url("https://api.example.com/users")
    .get()
    .execute();

// 场景2：带参数的GET请求
String response2 = new HttpRequestBuilder()
    .url("https://api.example.com/search")
    .get()
    .param("keyword", "java")
    .param("page", "1")
    .execute();
// 实际请求：https://api.example.com/search?keyword=java&page=1

// 场景3：POST JSON数据
String jsonData = "{\"name\":\"张三\",\"age\":25}";
String response3 = new HttpRequestBuilder()
    .url("https://api.example.com/users")
    .post()
    .json()                                    // 设置JSON头
    .auth("your-token-here")                   // 添加认证
    .body(jsonData)                           // 设置请求体
    .timeout(3000, 15000)                     // 自定义超时
    .execute();

// 场景4：复杂的API调用
String response4 = new HttpRequestBuilder()
    .url("https://api.example.com/upload")
    .post()
    .header("User-Agent", "MyApp/2.0")
    .header("X-API-Version", "v1")
    .auth("bearer-token")
    .param("type", "image")
    .body(fileData)
    .execute();
```

**🔍 对比传统方式**：

| **建造者模式** | **传统方式** |
|---------------|-------------|
| `代码清晰易读，像说话一样` | `配置分散，难以理解` |
| `链式调用，写起来流畅` | `需要记住很多API细节` |
| `可选配置，用什么设什么` | `所有参数都要考虑` |
| `容易扩展新功能` | `修改现有代码风险大` |

---

## 4. ⚙️ 配置对象构建


### 4.1 配置对象的常见场景


**实际开发中的配置需求**：
```
应用配置的复杂性：
🔧 数据库配置：主机、端口、用户名、密码、连接池设置
🔧 缓存配置：服务器地址、过期时间、最大连接数
🔧 日志配置：日志级别、输出格式、文件路径
🔧 邮件配置：SMTP服务器、认证方式、发送者信息
🔧 文件上传配置：允许类型、大小限制、存储路径
```

**配置对象的挑战**：
- **参数众多**：一个配置类可能有十几个甚至几十个属性
- **可选属性**：很多参数有默认值，不是所有都需要设置
- **参数验证**：某些参数组合是无效的，需要校验
- **环境差异**：开发、测试、生产环境的配置不同

### 4.2 数据库配置建造者


**💻 实际案例：数据库连接配置**：
```java
public class DatabaseConfig {
    private String host;
    private int port;
    private String database;
    private String username;
    private String password;
    private int maxConnections;
    private int connectionTimeout;
    private boolean useSSL;
    private String charset;
    
    // 私有构造函数，只能通过Builder创建
    private DatabaseConfig(Builder builder) {
        this.host = builder.host;
        this.port = builder.port;
        this.database = builder.database;
        this.username = builder.username;
        this.password = builder.password;
        this.maxConnections = builder.maxConnections;
        this.connectionTimeout = builder.connectionTimeout;
        this.useSSL = builder.useSSL;
        this.charset = builder.charset;
    }
    
    // 内部建造者类
    public static class Builder {
        // 必填参数
        private String host;
        private String database;
        private String username;
        private String password;
        
        // 可选参数及默认值
        private int port = 3306;                    // MySQL默认端口
        private int maxConnections = 10;            // 默认连接池大小
        private int connectionTimeout = 30000;      // 默认30秒超时
        private boolean useSSL = false;             // 默认不使用SSL
        private String charset = "UTF-8";          // 默认字符集
        
        // 构造函数要求必填参数
        public Builder(String host, String database, String username, String password) {
            this.host = host;
            this.database = database;
            this.username = username;
            this.password = password;
        }
        
        public Builder port(int port) {
            if (port <= 0 || port > 65535) {
                throw new IllegalArgumentException("端口号必须在1-65535之间");
            }
            this.port = port;
            return this;
        }
        
        public Builder maxConnections(int maxConnections) {
            if (maxConnections <= 0) {
                throw new IllegalArgumentException("最大连接数必须大于0");
            }
            this.maxConnections = maxConnections;
            return this;
        }
        
        public Builder connectionTimeout(int timeout) {
            if (timeout < 1000) {
                throw new IllegalArgumentException("连接超时至少1秒");
            }
            this.connectionTimeout = timeout;
            return this;
        }
        
        public Builder enableSSL() {
            this.useSSL = true;
            return this;
        }
        
        public Builder charset(String charset) {
            this.charset = charset;
            return this;
        }
        
        // 构建最终对象
        public DatabaseConfig build() {
            // 可以在这里做最终验证
            validateConfig();
            return new DatabaseConfig(this);
        }
        
        private void validateConfig() {
            if (host == null || host.trim().isEmpty()) {
                throw new IllegalArgumentException("数据库主机不能为空");
            }
            if (database == null || database.trim().isEmpty()) {
                throw new IllegalArgumentException("数据库名不能为空");
            }
            // 更多验证逻辑...
        }
    }
    
    // getter方法
    public String getHost() { return host; }
    public int getPort() { return port; }
    public String getDatabase() { return database; }
    // ... 其他getter
    
    @Override
    public String toString() {
        return String.format("数据库配置: %s:%d/%s (用户:%s, 连接池:%d)", 
                           host, port, database, username, maxConnections);
    }
}
```

### 4.3 使用示例


**🎯 不同环境的配置**：
```java
// 开发环境：简单配置
DatabaseConfig devConfig = new DatabaseConfig.Builder(
        "localhost", "myapp_dev", "dev_user", "dev_password")
    .build();

// 测试环境：增加连接池
DatabaseConfig testConfig = new DatabaseConfig.Builder(
        "test-server", "myapp_test", "test_user", "test_password")
    .port(3307)                        // 自定义端口
    .maxConnections(20)                // 更大连接池
    .connectionTimeout(15000)          // 15秒超时
    .build();

// 生产环境：完整配置
DatabaseConfig prodConfig = new DatabaseConfig.Builder(
        "prod-cluster.example.com", "myapp_prod", "prod_user", "secure_password")
    .port(3306)
    .maxConnections(100)               // 生产环境大连接池
    .connectionTimeout(10000)          // 10秒超时
    .enableSSL()                       // 启用SSL
    .charset("UTF-8")
    .build();

System.out.println(devConfig);
// 输出：数据库配置: localhost:3306/myapp_dev (用户:dev_user, 连接池:10)
```

### 4.4 配置建造者的优势


**📊 与传统方式对比**：

```
传统方式的问题：
❌ 构造函数参数过多：new Config(host, port, db, user, pwd, max, timeout, ssl, charset)
❌ 参数顺序容易搞错：容易把端口和超时搞反
❌ 可选参数处理复杂：需要多个重载构造函数
❌ 验证逻辑分散：在构造函数里验证会很乱

建造者模式的解决：
✅ 参数含义清晰：每个方法名都说明了参数作用
✅ 可选配置灵活：想设置什么就设置什么
✅ 验证逻辑集中：在build()方法中统一验证
✅ 代码易读易维护：修改配置不影响其他代码
```

---

## 5. 🎨 UI组件构建器


### 5.1 UI组件的复杂性


**现代UI组件的特点**：
```
UI组件的属性众多：
🎨 外观属性：颜色、字体、大小、边框、阴影
🎨 布局属性：位置、对齐方式、间距、内边距
🎨 交互属性：点击事件、悬停效果、动画
🎨 状态属性：可见性、可用性、选中状态
🎨 内容属性：文本、图标、子组件
```

**传统UI创建的痛点**：
```java
// 😫 传统方式：属性设置分散
JButton button = new JButton();
button.setText("确认");
button.setSize(100, 30);
button.setBackground(Color.BLUE);
button.setForeground(Color.WHITE);
button.setFont(new Font("宋体", Font.BOLD, 14));
button.setBorder(BorderFactory.createRaisedBorder());
button.addActionListener(e -> doConfirm());
button.setToolTipText("点击确认操作");
// ... 还有更多属性设置
```

### 5.2 按钮组件建造者


**💻 实现思路**：
```java
public class ButtonBuilder {
    private String text = "";
    private Color backgroundColor = Color.LIGHT_GRAY;
    private Color textColor = Color.BLACK;
    private Font font = new Font("微软雅黑", Font.PLAIN, 12);
    private int width = 80;
    private int height = 30;
    private String toolTip;
    private ActionListener clickHandler;
    private boolean enabled = true;
    private Border border;
    
    public ButtonBuilder text(String text) {
        this.text = text;
        return this;
    }
    
    public ButtonBuilder size(int width, int height) {
        this.width = width;
        this.height = height;
        return this;
    }
    
    public ButtonBuilder backgroundColor(Color color) {
        this.backgroundColor = color;
        return this;
    }
    
    public ButtonBuilder textColor(Color color) {
        this.textColor = color;
        return this;
    }
    
    public ButtonBuilder font(String name, int style, int size) {
        this.font = new Font(name, style, size);
        return this;
    }
    
    public ButtonBuilder onClick(ActionListener handler) {
        this.clickHandler = handler;
        return this;
    }
    
    public ButtonBuilder toolTip(String tip) {
        this.toolTip = tip;
        return this;
    }
    
    public ButtonBuilder disabled() {
        this.enabled = false;
        return this;
    }
    
    // 预设样式
    public ButtonBuilder primaryStyle() {
        return backgroundColor(new Color(0, 123, 255))
               .textColor(Color.WHITE)
               .font("微软雅黑", Font.BOLD, 14);
    }
    
    public ButtonBuilder dangerStyle() {
        return backgroundColor(new Color(220, 53, 69))
               .textColor(Color.WHITE)
               .font("微软雅黑", Font.BOLD, 14);
    }
    
    public ButtonBuilder successStyle() {
        return backgroundColor(new Color(40, 167, 69))
               .textColor(Color.WHITE)
               .font("微软雅黑", Font.BOLD, 14);
    }
    
    public JButton build() {
        JButton button = new JButton(text);
        
        // 设置外观
        button.setSize(width, height);
        button.setPreferredSize(new Dimension(width, height));
        button.setBackground(backgroundColor);
        button.setForeground(textColor);
        button.setFont(font);
        
        // 设置行为
        button.setEnabled(enabled);
        if (toolTip != null) {
            button.setToolTipText(toolTip);
        }
        if (clickHandler != null) {
            button.addActionListener(clickHandler);
        }
        if (border != null) {
            button.setBorder(border);
        }
        
        return button;
    }
}
```

### 5.3 表单构建器


**🎯 实际应用：用户注册表单**：
```java
public class FormBuilder {
    private JPanel panel;
    private GridBagLayout layout;
    private GridBagConstraints constraints;
    private int currentRow = 0;
    
    public FormBuilder() {
        panel = new JPanel();
        layout = new GridBagLayout();
        panel.setLayout(layout);
        constraints = new GridBagConstraints();
        constraints.insets = new Insets(5, 5, 5, 5); // 设置组件间距
    }
    
    public FormBuilder addField(String labelText, JComponent component) {
        // 添加标签
        JLabel label = new JLabel(labelText);
        constraints.gridx = 0;
        constraints.gridy = currentRow;
        constraints.anchor = GridBagConstraints.EAST;
        panel.add(label, constraints);
        
        // 添加组件
        constraints.gridx = 1;
        constraints.anchor = GridBagConstraints.WEST;
        constraints.fill = GridBagConstraints.HORIZONTAL;
        panel.add(component, constraints);
        
        currentRow++;
        return this;
    }
    
    public FormBuilder addButton(JButton button) {
        constraints.gridx = 1;
        constraints.gridy = currentRow;
        constraints.anchor = GridBagConstraints.CENTER;
        constraints.fill = GridBagConstraints.NONE;
        panel.add(button, constraints);
        currentRow++;
        return this;
    }
    
    public FormBuilder title(String title) {
        JLabel titleLabel = new JLabel(title);
        titleLabel.setFont(new Font("微软雅黑", Font.BOLD, 16));
        titleLabel.setHorizontalAlignment(JLabel.CENTER);
        
        constraints.gridx = 0;
        constraints.gridy = currentRow;
        constraints.gridwidth = 2; // 占两列
        constraints.anchor = GridBagConstraints.CENTER;
        panel.add(titleLabel, constraints);
        
        constraints.gridwidth = 1; // 恢复默认
        currentRow++;
        return this;
    }
    
    public JPanel build() {
        return panel;
    }
}
```

### 5.4 使用示例


**🎯 创建各种风格的按钮**：
```java
// 主要操作按钮
JButton saveButton = new ButtonBuilder()
    .text("保存")
    .primaryStyle()                          // 蓝色主题
    .size(100, 35)
    .onClick(e -> saveData())
    .toolTip("保存当前数据")
    .build();

// 危险操作按钮
JButton deleteButton = new ButtonBuilder()
    .text("删除")
    .dangerStyle()                           // 红色警告
    .size(100, 35)
    .onClick(e -> confirmDelete())
    .toolTip("删除选中项目")
    .build();

// 禁用按钮
JButton disabledButton = new ButtonBuilder()
    .text("暂不可用")
    .backgroundColor(Color.GRAY)
    .textColor(Color.DARK_GRAY)
    .disabled()                              // 设为禁用
    .build();

// 构建完整表单
JPanel registrationForm = new FormBuilder()
    .title("用户注册")
    .addField("用户名:", new JTextField(20))
    .addField("密码:", new JPasswordField(20))
    .addField("确认密码:", new JPasswordField(20))
    .addField("邮箱:", new JTextField(20))
    .addField("手机:", new JTextField(20))
    .addButton(saveButton)
    .build();
```

**✨ UI建造者的优势**：
- **代码整洁**：UI创建代码读起来像配置文件
- **样式复用**：预设样式可以在多个地方使用
- **易于调整**：修改样式不影响其他代码
- **链式流畅**：一行代码设置多个属性

---

## 6. 🧪 测试数据构建器


### 6.1 测试数据的挑战


**单元测试中的数据准备**：
```
测试数据的复杂性：
🧪 对象属性多：用户对象可能有十几个属性
🧪 关联关系复杂：用户-订单-商品的多层关联
🧪 测试场景多样：正常数据、边界数据、异常数据
🧪 数据重复创建：每个测试方法都需要准备数据
🧪 维护困难：业务对象变化导致测试数据失效
```

**传统测试数据创建的问题**：
```java
// 😫 传统方式：数据创建冗长
@Test
public void testUserService() {
    // 创建测试用户 - 代码很长很乱
    User user = new User();
    user.setId(1L);
    user.setName("张三");
    user.setAge(25);
    user.setEmail("zhangsan@test.com");
    user.setPhone("13800138000");
    user.setCity("北京");
    user.setCreateTime(new Date());
    user.setStatus("ACTIVE");
    user.setScore(100);
    
    // 创建订单
    Order order = new Order();
    order.setId(1001L);
    order.setUserId(1L);
    order.setAmount(new BigDecimal("299.99"));
    order.setStatus("PAID");
    order.setCreateTime(new Date());
    
    // 测试逻辑...
}
```

### 6.2 用户数据建造者


**💻 测试数据建造者实现**：
```java
public class UserTestDataBuilder {
    private Long id = 1L;
    private String name = "测试用户";
    private Integer age = 25;
    private String email = "test@example.com";
    private String phone = "13800138000";
    private String city = "北京";
    private Date createTime = new Date();
    private String status = "ACTIVE";
    private Integer score = 100;
    private BigDecimal balance = BigDecimal.ZERO;
    
    // 设置ID
    public UserTestDataBuilder id(Long id) {
        this.id = id;
        return this;
    }
    
    // 设置姓名
    public UserTestDataBuilder name(String name) {
        this.name = name;
        return this;
    }
    
    // 设置年龄
    public UserTestDataBuilder age(Integer age) {
        this.age = age;
        return this;
    }
    
    // 设置邮箱
    public UserTestDataBuilder email(String email) {
        this.email = email;
        return this;
    }
    
    // 预设场景：未成年用户
    public UserTestDataBuilder asMinor() {
        return age(16)
               .name("小明")
               .email("xiaoming@test.com");
    }
    
    // 预设场景：VIP用户
    public UserTestDataBuilder asVip() {
        return score(1000)
               .balance(new BigDecimal("5000.00"))
               .status("VIP");
    }
    
    // 预设场景：新注册用户
    public UserTestDataBuilder asNewUser() {
        return score(0)
               .balance(BigDecimal.ZERO)
               .createTime(new Date())
               .status("PENDING");
    }
    
    // 预设场景：异常数据
    public UserTestDataBuilder withInvalidData() {
        return name("")                        // 空姓名
               .age(-1)                        // 负数年龄
               .email("invalid-email");        // 无效邮箱
    }
    
    // 构建用户对象
    public User build() {
        User user = new User();
        user.setId(id);
        user.setName(name);
        user.setAge(age);
        user.setEmail(email);
        user.setPhone(phone);
        user.setCity(city);
        user.setCreateTime(createTime);
        user.setStatus(status);
        user.setScore(score);
        user.setBalance(balance);
        return user;
    }
    
    // 批量创建
    public List<User> buildList(int count) {
        List<User> users = new ArrayList<>();
        for (int i = 0; i < count; i++) {
            users.add(this.id((long) (i + 1))
                         .name("用户" + (i + 1))
                         .email("user" + (i + 1) + "@test.com")
                         .build());
        }
        return users;
    }
}
```

### 6.3 复杂对象建造者


**🎯 订单测试数据建造者**：
```java
public class OrderTestDataBuilder {
    private Long id = 1001L;
    private Long userId = 1L;
    private BigDecimal amount = new BigDecimal("99.99");
    private String status = "PENDING";
    private Date createTime = new Date();
    private List<OrderItem> items = new ArrayList<>();
    private String shippingAddress;
    
    public OrderTestDataBuilder id(Long id) {
        this.id = id;
        return this;
    }
    
    public OrderTestDataBuilder userId(Long userId) {
        this.userId = userId;
        return this;
    }
    
    public OrderTestDataBuilder amount(String amount) {
        this.amount = new BigDecimal(amount);
        return this;
    }
    
    public OrderTestDataBuilder status(String status) {
        this.status = status;
        return this;
    }
    
    // 添加订单项
    public OrderTestDataBuilder addItem(String productName, int quantity, String price) {
        OrderItem item = new OrderItem();
        item.setProductName(productName);
        item.setQuantity(quantity);
        item.setPrice(new BigDecimal(price));
        items.add(item);
        return this;
    }
    
    // 预设场景：小额订单
    public OrderTestDataBuilder asSmallOrder() {
        return amount("29.99")
               .addItem("笔记本", 1, "29.99")
               .status("PAID");
    }
    
    // 预设场景：大额订单
    public OrderTestDataBuilder asLargeOrder() {
        return amount("2999.99")
               .addItem("笔记本电脑", 1, "2999.99")
               .status("PENDING")
               .shippingAddress("北京市朝阳区xxx路123号");
    }
    
    // 预设场景：已完成订单
    public OrderTestDataBuilder asCompletedOrder() {
        return status("COMPLETED")
               .createTime(getDateDaysAgo(7));  // 7天前的订单
    }
    
    public Order build() {
        Order order = new Order();
        order.setId(id);
        order.setUserId(userId);
        order.setAmount(amount);
        order.setStatus(status);
        order.setCreateTime(createTime);
        order.setItems(items);
        order.setShippingAddress(shippingAddress);
        return order;
    }
    
    private Date getDateDaysAgo(int days) {
        Calendar cal = Calendar.getInstance();
        cal.add(Calendar.DAY_OF_MONTH, -days);
        return cal.getTime();
    }
}
```

### 6.4 测试中的实际使用


**🧪 清爽的测试代码**：
```java
public class UserServiceTest {
    
    @Test
    public void testCreateUser() {
        // 准备测试数据 - 简洁明了
        User user = new UserTestDataBuilder()
            .name("张三")
            .age(25)
            .email("zhangsan@test.com")
            .build();
        
        // 执行测试
        User savedUser = userService.create(user);
        
        // 验证结果
        assertThat(savedUser.getId()).isNotNull();
        assertThat(savedUser.getName()).isEqualTo("张三");
    }
    
    @Test
    public void testVipUserDiscount() {
        // VIP用户测试
        User vipUser = new UserTestDataBuilder()
            .asVip()                    // 使用预设场景
            .name("VIP客户")
            .build();
        
        Order order = new OrderTestDataBuilder()
            .userId(vipUser.getId())
            .asLargeOrder()             // 大额订单
            .build();
        
        // 测试VIP折扣逻辑
        BigDecimal discount = discountService.calculateDiscount(vipUser, order);
        assertThat(discount).isGreaterThan(BigDecimal.ZERO);
    }
    
    @Test
    public void testBatchUserCreation() {
        // 批量创建测试数据
        List<User> users = new UserTestDataBuilder()
            .asNewUser()
            .buildList(10);             // 创建10个新用户
        
        // 批量保存测试
        List<User> savedUsers = userService.batchCreate(users);
        assertThat(savedUsers).hasSize(10);
    }
    
    @Test
    public void testInvalidUserData() {
        // 异常数据测试
        User invalidUser = new UserTestDataBuilder()
            .withInvalidData()          // 使用无效数据
            .build();
        
        // 验证异常处理
        assertThatThrownBy(() -> userService.create(invalidUser))
            .isInstanceOf(ValidationException.class);
    }
}
```

**📊 测试数据建造者的优势**：

| **建造者模式** | **传统方式** |
|---------------|-------------|
| `代码简洁，专注测试逻辑` | `数据准备代码冗长` |
| `预设场景复用性强` | `每次都要重新创建` |
| `测试意图清晰明了` | `数据细节掩盖测试重点` |
| `维护成本低` | `对象变化影响所有测试` |

---

## 7. 📋 核心要点总结


### 7.1 建造者模式的应用价值


**🎯 核心理解**：
建造者模式就像**组装积木**，把复杂对象的创建过程分解成一个个简单步骤，让代码像说话一样自然流畅。

**💡 适用场景判断**：
```
什么时候用建造者模式？
✅ 对象参数很多（超过4-5个）
✅ 有很多可选参数
✅ 参数之间有依赖关系
✅ 需要不同的对象配置组合
✅ 希望代码可读性强

什么时候不用？
❌ 对象很简单（1-3个参数）
❌ 参数都是必填的
❌ 对象创建逻辑很简单
❌ 性能要求极高的场景
```

### 7.2 各个应用场景对比


**📊 应用场景特点总结**：

| **应用场景** | **主要优势** | **解决的核心问题** | **实际价值** |
|-------------|-------------|------------------|-------------|
| `SQL查询构建` | `避免字符串拼接错误` | `复杂查询的可读性` | `减少SQL注入风险` |
| `HTTP请求构建` | `配置清晰，链式调用` | `HTTP参数设置繁琐` | `提高网络编程效率` |
| `配置对象构建` | `参数验证集中化` | `配置参数过多混乱` | `降低配置错误率` |
| `UI组件构建` | `样式复用，代码整洁` | `UI属性设置分散` | `提升界面开发效率` |
| `测试数据构建` | `数据准备简洁明了` | `测试数据创建繁琐` | `提高测试代码质量` |

### 7.3 实现建造者模式的关键要点


**🔧 设计原则**：
```
1. 必填 vs 可选参数分离
   • 构造函数处理必填参数
   • 方法链处理可选参数

2. 方法命名要清晰
   • 动词命名：setName() → name()
   • 语义明确：让代码像英语句子

3. 返回this支持链式调用
   • 每个设置方法都返回builder自身
   • 保证方法链的流畅性

4. 验证逻辑放在build()
   • 延迟验证，避免中间状态检查
   • 集中验证，逻辑清晰

5. 提供预设场景方法
   • 常用配置组合封装成方法
   • 提高代码复用性
```

### 7.4 建造者模式的变种形式


**🔄 常见变种**：
```
1. 简单建造者
   • 只有基本的属性设置
   • 适合参数不太复杂的场景

2. 分步建造者
   • 强制按顺序设置参数
   • 适合有依赖关系的参数

3. 类型安全建造者
   • 利用泛型确保类型安全
   • 编译期检查参数完整性

4. 流式接口建造者
   • 提供更自然的英语语法
   • 如：query().select().from().where()
```

### 7.5 实际项目中的应用建议


**🎯 最佳实践**：
```
项目应用指导：

📌 从简单场景开始
• 先在配置类中尝试建造者模式
• 积累经验后再用于复杂业务对象

📌 工具类优先考虑
• SQL构建器、HTTP客户端等工具类是很好的起点
• 这些类通常参数多、使用频繁

📌 测试代码中大量使用
• 测试数据构建是建造者模式的最佳应用场景
• 能显著提高测试代码的可读性

📌 API设计时考虑建造者
• 对外提供的API如果参数复杂，建议使用建造者
• 能降低API使用者的学习成本
```

**🧠 记忆要点**：
- **建造者模式**：复杂对象分步构建，参数设置更清晰
- **核心价值**：代码可读性强，参数设置灵活
- **适用场景**：参数多、可选参数多、需要不同配置组合
- **实现关键**：链式调用、参数验证、预设场景
- **最佳应用**：工具类构建、配置对象、测试数据准备

**一句话总结**：建造者模式让复杂对象的创建像搭积木一样简单，代码读起来像说话一样自然！