---
title: 3、享元模式实现机制
---
## 📚 目录


1. [享元对象池核心机制](#1-享元对象池核心机制)
2. [工厂缓存策略详解](#2-工厂缓存策略详解)
3. [状态分离技术原理](#3-状态分离技术原理)
4. [弱引用的巧妙使用](#4-弱引用的巧妙使用)
5. [内存管理优化技巧](#5-内存管理优化技巧)
6. [完整实现案例](#6-完整实现案例)
7. [核心要点总结](#7-核心要点总结)

---

# 📖 前言



> 💡 **学习指导**  
> 享元模式的实现机制是这个模式的"心脏"部分。本章将带你深入理解享元模式是如何通过巧妙的技术手段，实现内存的高效利用。我们会用通俗的语言和生动的例子，让你彻底掌握这些看似复杂的技术细节。

**🎯 难度等级**：★★★☆☆  
**📖 预计阅读时间**：25分钟  
**🏷️ 标签**：`#结构型模式` `#内存优化` `#对象池` `#缓存策略`

---

## 1. 🏊 享元对象池核心机制



### 1.1 什么是享元对象池



**通俗理解**：想象一个图书馆，如果每个读者都要买一本书，那成本太高了。图书馆的做法是买少量的书，让所有读者共享。享元对象池就是软件世界的"图书馆"。

```
传统方式：每次需要对象就创建新的
用户A需要"红色圆形" → 创建新对象
用户B需要"红色圆形" → 又创建新对象
用户C需要"红色圆形" → 再创建新对象
结果：内存中有3个相同的对象 ❌

享元方式：共享相同的对象
用户A需要"红色圆形" → 创建对象并放入池中
用户B需要"红色圆形" → 从池中取出同一个对象
用户C需要"红色圆形" → 还是那同一个对象
结果：内存中只有1个对象 ✅
```

### 1.2 对象池的基本结构



```java
// 享元对象池的核心思想
public class FlyweightPool {
    // 这个Map就是我们的"对象仓库"
    private Map<String, Flyweight> pool = new HashMap<>();
    
    // 获取享元对象的方法
    public Flyweight getFlyweight(String key) {
        // 先看看仓库里有没有这个对象
        Flyweight flyweight = pool.get(key);
        
        if (flyweight == null) {
            // 没有就创建一个新的，然后放入仓库
            flyweight = new ConcreteFlyweight(key);
            pool.put(key, flyweight);
            System.out.println("创建新的享元对象：" + key);
        } else {
            System.out.println("复用已有享元对象：" + key);
        }
        
        return flyweight;
    }
}
```

**🔍 关键理解**：
- **HashMap作为仓库**：用键值对存储享元对象
- **先查后建**：优先使用已有对象，没有才创建新的
- **透明使用**：客户端不需要知道对象是新建的还是复用的

### 1.3 对象池的生命周期



```
对象池生命周期示意图：

初始化阶段    使用阶段         清理阶段
    |           |              |
 ┌─────┐    ┌─────────┐    ┌─────────┐
 │创建池│ →  │存取对象 │ →  │清理资源 │
 └─────┘    └─────────┘    └─────────┘
    ↓           ↓              ↓
  空的Map   逐渐填充Map     释放内存
```

---

## 2. 🏭 工厂缓存策略详解



### 2.1 缓存策略的必要性



**为什么需要缓存策略**？  
想象你是一个蛋糕师，如果每次客户要蛋糕都从头开始做，效率很低。聪明的做法是：把常用的蛋糕胚子提前做好存起来，客户要的时候直接装饰就行。

```
没有缓存策略的问题：
┌─客户请求─┐ → ┌─创建对象─┐ → ┌─返回对象─┐
│每次都要│     │消耗时间│     │增加内存│
└────────┘     └────────┘     └────────┘
    ↓              ↓              ↓
   频繁           CPU消耗        内存浪费

有缓存策略的优势：
┌─客户请求─┐ → ┌─检查缓存─┐ → ┌─直接返回─┐
│只需查找│     │几乎瞬间│     │共享内存│
└────────┘     └────────┘     └────────┘
    ↓              ↓              ↓
   高效           CPU友好        内存节省
```

### 2.2 常见缓存策略对比



| 策略类型 | **工作原理** | **适用场景** | **优点** | **缺点** |
|---------|------------|-------------|---------|---------|
| 🔄 **LRU缓存** | `最近最少使用淘汰` | `内存有限场景` | `自动清理，防止内存泄漏` | `实现复杂，有额外开销` |
| ⏰ **TTL缓存** | `超时自动过期` | `数据时效性要求` | `自动刷新，保证数据新鲜` | `需要定时清理机制` |
| 📊 **固定大小** | `超出容量直接拒绝` | `严格内存控制` | `简单可控，内存可预测` | `可能拒绝有效请求` |
| ♾️ **无限缓存** | `永久保存所有对象` | `对象种类有限` | `命中率最高，实现简单` | `可能内存泄漏` |

### 2.3 LRU缓存策略实现



```java
// 基于LinkedHashMap的LRU享元工厂
public class LRUFlyweightFactory {
    private final int maxSize;
    
    // LinkedHashMap天然支持LRU顺序
    private final Map<String, Flyweight> cache = 
        new LinkedHashMap<String, Flyweight>(16, 0.75f, true) {
            @Override
            protected boolean removeEldestEntry(Map.Entry<String, Flyweight> eldest) {
                return size() > maxSize; // 超过最大容量就淘汰最老的
            }
        };
    
    public LRUFlyweightFactory(int maxSize) {
        this.maxSize = maxSize;
    }
    
    public Flyweight getFlyweight(String intrinsicState) {
        return cache.computeIfAbsent(intrinsicState, key -> {
            System.out.println("创建新享元：" + key);
            return new ConcreteFlyweight(key);
        });
    }
}
```

**🎯 理解要点**：
- **LinkedHashMap的access-order**：设置为true时，访问过的元素会移到末尾
- **removeEldestEntry方法**：超出容量时自动调用，返回true就删除最老的元素
- **computeIfAbsent方法**：如果key不存在就计算并放入，存在就直接返回

---

## 3. 🔧 状态分离技术原理



### 3.3 状态分离的核心思想



**什么是状态分离**？  
想象你是一个演员，你的"角色设定"（身高、声音、演技）是不变的，但"演出内容"（台词、动作、服装）每场戏都不同。状态分离就是把"不变的部分"和"变化的部分"分开管理。

```
传统对象设计（未分离）：
┌─────────────────┐
│    游戏角色     │
├─────────────────┤
│角色类型：法师   │ ← 内在状态（不变）
│技能：火球术     │ ← 内在状态（不变）
│当前位置：(10,20)│ ← 外在状态（变化）
│当前血量：80     │ ← 外在状态（变化）
└─────────────────┘
问题：每个角色实例都包含所有信息

享元设计（状态分离）：
┌─────────────────┐    ┌─────────────────┐
│  享元对象（共享） │    │  上下文对象     │
├─────────────────┤    ├─────────────────┤
│角色类型：法师   │    │当前位置：(10,20)│
│技能：火球术     │    │当前血量：80     │
│外观：蓝色法袍   │    │所属玩家：张三   │
└─────────────────┘    └─────────────────┘
      ↑                        ↑
   一个实例共享          每个角色独有
```

### 3.2 内在状态与外在状态



```java
// 享元接口：定义操作方法
public interface CharacterFlyweight {
    // 关键：外在状态通过参数传入，不存储在享元对象中
    void performAction(String action, Position position, int health);
}

// 具体享元：只存储内在状态
public class MageFlyweight implements CharacterFlyweight {
    private final String characterType; // 内在状态：角色类型
    private final String[] skills;      // 内在状态：技能列表
    private final String appearance;    // 内在状态：外观描述
    
    public MageFlyweight() {
        this.characterType = "法师";
        this.skills = new String[]{"火球术", "冰锥术", "闪现"};
        this.appearance = "蓝色法袍，手持法杖";
    }
    
    @Override
    public void performAction(String action, Position position, int health) {
        // 使用内在状态和外在状态共同完成操作
        System.out.println(
            characterType + "(" + appearance + ")" +
            "在位置" + position + 
            "执行" + action + 
            "，当前血量:" + health
        );
    }
}

// 外在状态封装类
public class GameContext {
    private Position position;  // 外在状态：位置
    private int health;        // 外在状态：血量
    private String playerName; // 外在状态：玩家名
    
    // 构造函数和getter/setter方法...
}
```

### 3.3 状态分离的判断标准



**如何判断哪些是内在状态，哪些是外在状态**？

```
判断标准口诀：
🔸 不变的、共享的 → 内在状态
🔸 变化的、独特的 → 外在状态

具体判断方法：
✅ 内在状态特征：
  - 对象创建后不会改变
  - 多个对象可以共享
  - 与具体使用环境无关
  
❌ 外在状态特征：
  - 会频繁变化
  - 每个对象都不同
  - 依赖具体使用环境
```

**📋 实际案例分析**：

```
文本编辑器中的字符对象：

字符'A'分析：
├─ 字体类型（宋体）     → 内在状态 ✅（多个A可以共享）
├─ 字符大小（12号）     → 内在状态 ✅（同样大小的A可以共享）
├─ 字符颜色（黑色）     → 内在状态 ✅（同样颜色的A可以共享）
├─ 文档中的位置         → 外在状态 ❌（每个A位置都不同）
└─ 选中状态             → 外在状态 ❌（状态会变化）

结论：(宋体,12号,黑色)的字符'A'可以作为一个享元对象
```

---

## 4. 🔗 弱引用的巧妙使用



### 4.1 为什么需要弱引用



**内存泄漏的风险**：  
普通的强引用就像"永不放手的握手"，即使对象不再需要，享元工厂仍然紧紧抓住它，导致内存无法释放。

```
强引用的问题示意：
                    ┌─────────────┐
享元工厂 ───强引用───→│  享元对象   │←── 还有其他引用吗？
                    │ (无法释放)  │     没有了...
                    └─────────────┘     但工厂不放手！

弱引用的解决方案：
                    ┌─────────────┐
享元工厂 ───弱引用───→│  享元对象   │←── 还有其他引用吗？
                    │ (可以释放)  │     没有了！
                    └─────────────┘     那就可以被GC回收
```

### 4.2 WeakHashMap的使用



```java
import java.util.WeakHashMap;

// 使用弱引用的享元工厂
public class WeakReferenceFlyweightFactory {
    // WeakHashMap：当key没有其他强引用时，会自动删除entry
    private final WeakHashMap<String, Flyweight> flyweights = new WeakHashMap<>();
    
    public Flyweight getFlyweight(String intrinsicState) {
        Flyweight flyweight = flyweights.get(intrinsicState);
        
        if (flyweight == null) {
            flyweight = new ConcreteFlyweight(intrinsicState);
            flyweights.put(intrinsicState, flyweight);
            System.out.println("创建享元对象：" + intrinsicState);
        } else {
            System.out.println("复用享元对象：" + intrinsicState);
        }
        
        return flyweight;
    }
    
    // 查看当前缓存的对象数量
    public int getCacheSize() {
        return flyweights.size();
    }
}
```

### 4.3 弱引用的效果演示



```java
// 演示弱引用的自动清理效果
public class WeakReferenceDemo {
    public static void main(String[] args) {
        WeakReferenceFlyweightFactory factory = new WeakReferenceFlyweightFactory();
        
        // 创建一些享元对象
        Flyweight f1 = factory.getFlyweight("type1");
        Flyweight f2 = factory.getFlyweight("type2");
        Flyweight f3 = factory.getFlyweight("type1"); // 复用f1
        
        System.out.println("创建对象后缓存大小：" + factory.getCacheSize()); // 输出：2
        
        // 删除强引用
        f1 = null;
        f3 = null;
        
        // 建议进行垃圾回收
        System.gc();
        
        // 稍等一下让GC完成
        try { Thread.sleep(100); } catch (InterruptedException e) {}
        
        System.out.println("GC后缓存大小：" + factory.getCacheSize()); // 可能输出：1
        // type1对应的享元可能已经被回收，只剩下type2
    }
}
```

**🔍 理解要点**：
- **WeakHashMap的特性**：key被回收时，对应的entry也会自动删除
- **垃圾回收时机**：GC不是立即发生的，可能需要等待
- **适用场景**：对象生命周期不确定，希望自动清理的情况

---

## 5. 🧠 内存管理优化技巧



### 5.1 内存使用监控



```java
// 内存监控的享元工厂
public class MonitoredFlyweightFactory {
    private final Map<String, Flyweight> flyweights = new ConcurrentHashMap<>();
    private final AtomicLong createCount = new AtomicLong(0);
    private final AtomicLong reuseCount = new AtomicLong(0);
    
    public Flyweight getFlyweight(String intrinsicState) {
        return flyweights.computeIfAbsent(intrinsicState, key -> {
            createCount.incrementAndGet();
            System.out.println("创建新享元，总创建数：" + createCount.get());
            return new ConcreteFlyweight(key);
        });
    }
    
    // 获取内存使用统计
    public MemoryStats getMemoryStats() {
        long totalMemory = Runtime.getRuntime().totalMemory();
        long freeMemory = Runtime.getRuntime().freeMemory();
        long usedMemory = totalMemory - freeMemory;
        
        return new MemoryStats(
            flyweights.size(),           // 享元对象数量
            createCount.get(),           // 创建次数
            reuseCount.get(),           // 复用次数
            usedMemory                  // 已用内存
        );
    }
}

// 内存统计信息类
public class MemoryStats {
    private final int flyweightCount;
    private final long createCount;
    private final long reuseCount;
    private final long usedMemory;
    
    // 构造函数和getter方法...
    
    public void printStats() {
        System.out.println("=== 内存使用统计 ===");
        System.out.println("享元对象数量：" + flyweightCount);
        System.out.println("创建次数：" + createCount);
        System.out.println("复用次数：" + reuseCount);
        System.out.println("复用率：" + (reuseCount * 100.0 / (createCount + reuseCount)) + "%");
        System.out.println("已用内存：" + (usedMemory / 1024 / 1024) + "MB");
    }
}
```

### 5.2 容量限制与清理策略



```java
// 带容量限制的享元工厂
public class BoundedFlyweightFactory {
    private final int maxCapacity;
    private final Map<String, Flyweight> flyweights = new LinkedHashMap<>();
    
    public BoundedFlyweightFactory(int maxCapacity) {
        this.maxCapacity = maxCapacity;
    }
    
    public synchronized Flyweight getFlyweight(String intrinsicState) {
        Flyweight flyweight = flyweights.get(intrinsicState);
        
        if (flyweight != null) {
            // 移动到末尾（LRU策略）
            flyweights.remove(intrinsicState);
            flyweights.put(intrinsicState, flyweight);
            return flyweight;
        }
        
        // 检查容量限制
        if (flyweights.size() >= maxCapacity) {
            // 删除最久未使用的享元
            String oldestKey = flyweights.keySet().iterator().next();
            flyweights.remove(oldestKey);
            System.out.println("容量已满，清理享元：" + oldestKey);
        }
        
        // 创建新的享元
        flyweight = new ConcreteFlyweight(intrinsicState);
        flyweights.put(intrinsicState, flyweight);
        
        return flyweight;
    }
}
```

### 5.3 预热策略



```java
// 支持预热的享元工厂
public class PrewarmFlyweightFactory {
    private final Map<String, Flyweight> flyweights = new ConcurrentHashMap<>();
    
    // 预热常用的享元对象
    public void prewarm(String[] commonIntrinsicStates) {
        System.out.println("开始预热享元对象...");
        
        for (String state : commonIntrinsicStates) {
            flyweights.put(state, new ConcreteFlyweight(state));
            System.out.println("预热创建：" + state);
        }
        
        System.out.println("预热完成，共创建 " + commonIntrinsicStates.length + " 个享元对象");
    }
    
    public Flyweight getFlyweight(String intrinsicState) {
        return flyweights.computeIfAbsent(intrinsicState, key -> {
            System.out.println("运行时创建享元：" + key);
            return new ConcreteFlyweight(key);
        });
    }
}
```

---

## 6. 🎮 完整实现案例



### 6.1 游戏角色系统



```java
// 游戏角色享元接口
public interface GameCharacterFlyweight {
    void performAction(String action, GameContext context);
    String getCharacterInfo();
}

// 具体角色享元：战士
public class WarriorFlyweight implements GameCharacterFlyweight {
    private final String characterType = "战士";
    private final String[] skills = {"重击", "旋风斩", "战吼"};
    private final String appearance = "重甲战士，手持双手剑";
    
    @Override
    public void performAction(String action, GameContext context) {
        System.out.println(String.format(
            "%s(%s)在位置(%d,%d)执行%s，血量:%d/%d",
            characterType, appearance,
            context.getX(), context.getY(),
            action,
            context.getCurrentHealth(), context.getMaxHealth()
        ));
    }
    
    @Override
    public String getCharacterInfo() {
        return characterType + " - " + String.join(",", skills);
    }
}

// 游戏上下文（外在状态）
public class GameContext {
    private int x, y;                    // 位置
    private int currentHealth, maxHealth; // 血量
    private String playerName;           // 玩家名
    private String equipment;            // 装备
    
    public GameContext(int x, int y, int maxHealth, String playerName) {
        this.x = x;
        this.y = y;
        this.maxHealth = maxHealth;
        this.currentHealth = maxHealth;
        this.playerName = playerName;
    }
    
    // getter和setter方法...
}

// 游戏角色工厂
public class GameCharacterFactory {
    private static final Map<String, GameCharacterFlyweight> flyweights = new HashMap<>();
    
    public static GameCharacterFlyweight getCharacter(String characterType) {
        return flyweights.computeIfAbsent(characterType, type -> {
            switch (type.toLowerCase()) {
                case "warrior": return new WarriorFlyweight();
                case "mage": return new MageFlyweight();
                case "archer": return new ArcherFlyweight();
                default: throw new IllegalArgumentException("未知角色类型：" + type);
            }
        });
    }
    
    public static void printStatistics() {
        System.out.println("=== 享元统计 ===");
        System.out.println("享元类型数量：" + flyweights.size());
        flyweights.forEach((type, flyweight) -> 
            System.out.println("- " + flyweight.getCharacterInfo())
        );
    }
}
```

### 6.2 使用示例



```java
public class GameDemo {
    public static void main(String[] args) {
        // 创建100个战士角色
        List<GameContext> warriors = new ArrayList<>();
        GameCharacterFlyweight warriorFlyweight = GameCharacterFactory.getCharacter("warrior");
        
        for (int i = 0; i < 100; i++) {
            GameContext context = new GameContext(
                i * 10, i * 5,           // 不同位置
                100,                     // 血量
                "玩家" + i               // 不同玩家名
            );
            warriors.add(context);
        }
        
        // 所有战士执行攻击动作
        for (GameContext context : warriors) {
            warriorFlyweight.performAction("重击", context);
        }
        
        // 创建一些其他类型角色
        GameCharacterFlyweight mage = GameCharacterFactory.getCharacter("mage");
        GameCharacterFlyweight archer = GameCharacterFactory.getCharacter("archer");
        
        // 打印统计信息
        GameCharacterFactory.printStatistics();
        
        System.out.println("\n=== 内存优化效果 ===");
        System.out.println("如果不使用享元模式：100个战士 = 100个对象");
        System.out.println("使用享元模式：100个战士 = 1个享元对象 + 100个上下文对象");
        System.out.println("内存节省：战士享元对象只需要1个，节省了99个对象的内存");
    }
}
```

---

## 7. 📋 核心要点总结



### 7.1 必须掌握的核心概念



```
🔸 享元对象池：共享对象的"仓库"，先查后建
🔸 工厂缓存策略：LRU、TTL、固定大小等不同缓存方案
🔸 状态分离技术：内在状态（共享）vs 外在状态（独特）
🔸 弱引用使用：自动内存清理，防止内存泄漏
🔸 内存管理优化：监控、限制、预热等策略
```

### 7.2 关键理解要点



**🔹 对象池的本质**
```
核心思想：
- 缓存复用 → 减少对象创建开销
- 共享机制 → 降低内存使用
- 透明使用 → 客户端无感知

记忆口诀：
先查仓库有没有，没有再造新的放
有的直接拿出来，一物多用省内存
```

**🔹 状态分离的判断技巧**
```
判断方法：
- 问自己：这个属性多个对象能共享吗？
- 能共享 → 内在状态，放入享元对象
- 不能共享 → 外在状态，通过参数传递

实用技巧：
内在状态像"身份证信息"（固定不变）
外在状态像"当前状态"（经常变化）
```

**🔹 内存优化的平衡艺术**
```
优化目标：
- 最大化对象复用
- 最小化内存占用
- 保持合理的性能

平衡考虑：
缓存大小 vs 内存限制
复用率 vs 查找开销
自动清理 vs 手动管理
```

### 7.3 实现机制的价值



**🎯 解决的核心问题**
- **内存爆炸**：大量相似对象导致的内存不足
- **创建开销**：频繁创建对象的性能问题
- **资源浪费**：重复存储相同信息的空间浪费

**🔧 提供的技术手段**
- **对象池管理**：高效的缓存和复用机制
- **状态分离**：巧妙的数据组织方式
- **内存优化**：多种策略保障系统稳定

**📈 带来的实际价值**
- **性能提升**：减少对象创建时间
- **内存节省**：大幅降低内存使用
- **系统稳定**：避免内存溢出问题

### 7.4 学习建议



**🎓 掌握顺序**
1. **理解对象池概念** → 从"图书馆"比喻开始
2. **练习状态分离** → 多做判断练习
3. **实现缓存策略** → 从简单到复杂
4. **优化内存管理** → 结合实际项目需求

**🛠️ 实践要点**
- 先实现基本版本，再考虑优化
- 多测试不同缓存策略的效果
- 注意线程安全问题
- 监控内存使用情况

**核心记忆**：
- 享元实现靠对象池，缓存策略要选好
- 状态分离是关键，内外状态分清楚
- 弱引用防泄漏，内存优化有技巧
- 理论实践要结合，项目应用见真章