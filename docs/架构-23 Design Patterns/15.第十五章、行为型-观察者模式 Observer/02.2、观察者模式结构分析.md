---
title: 2、观察者模式结构分析
---
## 📚 目录

1. [什么是观察者模式](#1-什么是观察者模式)
2. [观察者模式的核心角色](#2-观察者模式的核心角色)
3. [抽象主题接口设计](#3-抽象主题接口设计)
4. [具体主题实现](#4-具体主题实现)
5. [抽象观察者接口设计](#5-抽象观察者接口设计)
6. [具体观察者实现](#6-具体观察者实现)
7. [通知机制设计详解](#7-通知机制设计详解)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 什么是观察者模式


### 1.1 生活中的观察者模式


**🔸 订阅报纸的例子**
```
报社（主题）     ←→     读者们（观察者）
    |                       |
发布新报纸               收到新报纸通知
    |                       |
通知所有订阅者          各自做出反应
```

想象你订阅了一份报纸：
- **报社**就是**主题**（被观察的对象）
- **你和其他读者**就是**观察者**
- 当报社**发布新报纸**时，会**自动通知**所有订阅者
- 每个订阅者收到通知后，可以**各自决定**要不要去买

### 1.2 观察者模式的核心思想


**🎯 核心定义**：观察者模式定义了对象之间的**一对多依赖关系**，当一个对象的状态发生改变时，所有依赖它的对象都会收到通知并自动更新。

**💡 简单理解**：
- 就像**微信朋友圈**，你发动态（主题状态改变）
- 你的朋友们（观察者）会在朋友圈看到（收到通知）
- 他们可以点赞、评论或忽略（各自做出反应）

### 1.3 为什么需要观察者模式


**❌ 没有观察者模式的问题**：
```
传统方式的问题：
主题对象需要知道所有依赖它的对象
→ 紧耦合，难以维护
→ 添加新的依赖对象需要修改主题代码
→ 违反开闭原则
```

**✅ 观察者模式的优势**：
- **解耦**：主题和观察者互相不知道对方的具体实现
- **动态关系**：可以在运行时添加或删除观察者
- **广播通信**：一次状态改变可以通知多个对象
- **开闭原则**：添加新观察者无需修改现有代码

---

## 2. 🏗️ 观察者模式的核心角色


### 2.1 四个核心角色


```
观察者模式结构图：

    Subject（抽象主题）
         |
         | 实现
         ↓
 ConcreteSubject（具体主题）
         |
         | 通知
         ↓
    Observer（抽象观察者）
         |
         | 实现
         ↓
ConcreteObserver（具体观察者）
```

**🔸 角色说明**：

| 角色 | **中文名** | **作用** | **比喻** |
|------|------------|----------|----------|
| `Subject` | 抽象主题 | 定义添加、删除、通知观察者的接口 | 报社的通用规范 |
| `ConcreteSubject` | 具体主题 | 实现具体的业务逻辑和状态管理 | 某个具体的报社 |
| `Observer` | 抽象观察者 | 定义观察者的通用更新接口 | 订阅者的通用行为 |
| `ConcreteObserver` | 具体观察者 | 实现具体的更新逻辑 | 具体的读者 |

### 2.2 角色间的关系


**🔄 工作流程**：
```
1. 具体主题状态发生变化
   ↓
2. 调用通知方法
   ↓  
3. 遍历观察者列表
   ↓
4. 调用每个观察者的更新方法
   ↓
5. 观察者根据新状态做出响应
```

---

## 3. 📋 抽象主题接口设计


### 3.1 抽象主题的职责


**🎯 核心职责**：
- **注册观察者**：添加新的观察者到列表中
- **移除观察者**：从列表中删除观察者
- **通知观察者**：状态改变时通知所有观察者

### 3.2 抽象主题接口实现


```java
/**
 * 抽象主题接口
 * 定义了观察者管理的基本操作
 */
public interface Subject {
    
    /**
     * 添加观察者
     * @param observer 要添加的观察者
     */
    void addObserver(Observer observer);
    
    /**
     * 移除观察者  
     * @param observer 要移除的观察者
     */
    void removeObserver(Observer observer);
    
    /**
     * 通知所有观察者
     * 当主题状态发生变化时调用
     */
    void notifyObservers();
}
```

### 3.3 设计要点解析


**🔸 接口设计原则**：

> **单一职责**：只负责观察者的管理，不涉及具体业务
> 
> **开闭原则**：对扩展开放，对修改关闭
> 
> **依赖倒置**：依赖抽象而不是具体实现

**💡 为什么使用接口**：
- **标准化**：所有主题都必须实现这些基本操作
- **多态性**：可以用统一的方式处理不同的具体主题
- **灵活性**：具体实现可以根据需要自定义

---

## 4. 🔧 具体主题实现


### 4.1 具体主题的职责


**🎯 核心职责**：
- **继承抽象主题**：实现观察者管理的基本操作
- **管理业务状态**：维护自己的业务数据
- **触发通知**：状态改变时主动通知观察者

### 4.2 具体主题实现示例


```java
/**
 * 天气数据主题 - 具体主题实现
 */
public class WeatherData implements Subject {
    
    // 观察者列表
    private List<Observer> observers;
    
    // 业务状态数据
    private float temperature;    // 温度
    private float humidity;       // 湿度  
    private float pressure;       // 气压
    
    public WeatherData() {
        observers = new ArrayList<>();
    }
    
    @Override
    public void addObserver(Observer observer) {
        observers.add(observer);
        System.out.println("添加了一个新的观察者");
    }
    
    @Override
    public void removeObserver(Observer observer) {
        if (observers.contains(observer)) {
            observers.remove(observer);
            System.out.println("移除了一个观察者");
        }
    }
    
    @Override
    public void notifyObservers() {
        // 通知所有观察者
        for (Observer observer : observers) {
            observer.update(temperature, humidity, pressure);
        }
    }
    
    /**
     * 设置天气数据 - 业务方法
     */
    public void setWeatherData(float temperature, float humidity, float pressure) {
        this.temperature = temperature;
        this.humidity = humidity;
        this.pressure = pressure;
        
        // 状态改变，通知观察者
        notifyObservers();
    }
    
    // getter 方法
    public float getTemperature() { return temperature; }
    public float getHumidity() { return humidity; }
    public float getPressure() { return pressure; }
}
```

### 4.3 实现要点解析


**🔸 关键设计细节**：

**观察者列表管理**：
```java
private List<Observer> observers = new ArrayList<>();
```
- 使用`ArrayList`存储观察者
- 支持动态添加和删除
- 遍历时通知所有观察者

**状态改变触发通知**：
```java
public void setWeatherData(...) {
    // 1. 更新状态
    this.temperature = temperature;
    // 2. 立即通知
    notifyObservers();
}
```
- **先更新状态，再通知**
- **每次状态改变都通知**
- **自动化的通知机制**

---

## 5. 👁️ 抽象观察者接口设计


### 5.1 抽象观察者的职责


**🎯 核心职责**：
- **定义更新接口**：规定观察者如何接收通知
- **统一通信协议**：主题和观察者的通信标准

### 5.2 抽象观察者接口实现


```java
/**
 * 抽象观察者接口
 * 定义观察者接收通知的标准
 */
public interface Observer {
    
    /**
     * 更新方法 - 接收主题的通知
     * @param temperature 温度
     * @param humidity 湿度
     * @param pressure 气压
     */
    void update(float temperature, float humidity, float pressure);
}
```

### 5.3 接口设计考虑


**🔸 参数传递方式**：

**推模式（Push）**：
```java
// 主题主动推送所有数据
void update(float temperature, float humidity, float pressure);
```
- **优点**：观察者无需主动获取数据
- **缺点**：可能传递不需要的数据

**拉模式（Pull）**：
```java
// 只传递主题引用，观察者主动拉取需要的数据
void update(Subject subject);
```
- **优点**：观察者只获取需要的数据
- **缺点**：需要知道主题的具体类型

> **💡 本例选择推模式**：简单直观，适合演示观察者模式的核心思想

---

## 6. 👥 具体观察者实现


### 6.1 具体观察者的职责


**🎯 核心职责**：
- **实现更新接口**：响应主题的状态变化
- **处理业务逻辑**：根据新状态执行具体操作
- **保持独立性**：与其他观察者相互独立

### 6.2 具体观察者实现示例


**📱 手机APP显示观察者**：
```java
/**
 * 手机APP - 具体观察者实现
 */
public class PhoneDisplay implements Observer {
    
    private String appName;
    
    public PhoneDisplay(String appName) {
        this.appName = appName;
    }
    
    @Override
    public void update(float temperature, float humidity, float pressure) {
        System.out.println("=== " + appName + " 收到天气更新 ===");
        System.out.println("温度: " + temperature + "°C");
        System.out.println("湿度: " + humidity + "%");
        System.out.println("气压: " + pressure + " hPa");
        
        // 具体的业务逻辑
        if (temperature > 30) {
            System.out.println("📱 推送：今天很热，注意防暑！");
        }
        System.out.println();
    }
}
```

**🖥️ 网站显示观察者**：
```java
/**
 * 网站显示 - 具体观察者实现
 */
public class WebDisplay implements Observer {
    
    @Override
    public void update(float temperature, float humidity, float pressure) {
        System.out.println("=== 网站页面更新 ===");
        
        // 不同的显示格式
        displayAsTable(temperature, humidity, pressure);
        
        // 特有的业务逻辑
        generateWeatherReport(temperature, humidity, pressure);
        System.out.println();
    }
    
    private void displayAsTable(float temp, float humidity, float pressure) {
        System.out.println("┌─────────┬─────────┐");
        System.out.println("│ 项目    │ 数值    │");
        System.out.println("├─────────┼─────────┤");
        System.out.println("│ 温度    │ " + temp + "°C   │");
        System.out.println("│ 湿度    │ " + humidity + "%     │");
        System.out.println("│ 气压    │ " + pressure + " hPa │");
        System.out.println("└─────────┴─────────┘");
    }
    
    private void generateWeatherReport(float temp, float humidity, float pressure) {
        System.out.println("🌐 生成详细天气报告...");
        if (pressure < 1000) {
            System.out.println("   ⚠️ 低气压，可能有降雨");
        }
    }
}
```

### 6.3 多样性体现


**🔸 不同的观察者可以**：
- **不同的显示方式**：APP推送 vs 网页表格
- **不同的业务逻辑**：高温提醒 vs 降雨预警  
- **不同的处理重点**：有的关注温度，有的关注气压
- **独立的处理时机**：可以异步处理

> **核心思想**：每个观察者都是独立的，可以根据同一份数据做出不同的响应

---

## 7. 🔔 通知机制设计详解


### 7.1 通知机制的核心流程


```
通知机制工作流程：

1. 主题状态改变
   └── WeatherData.setWeatherData()
       
2. 触发通知方法  
   └── this.notifyObservers()
   
3. 遍历观察者列表
   └── for (Observer observer : observers)
   
4. 调用每个观察者的更新方法
   └── observer.update(temperature, humidity, pressure)
   
5. 观察者各自处理
   ├── PhoneDisplay: 推送提醒
   └── WebDisplay: 更新网页
```

### 7.2 通知机制的关键特点


**🔸 广播性质**：
```java
public void notifyObservers() {
    for (Observer observer : observers) {
        observer.update(temperature, humidity, pressure);
    }
}
```
- **一次通知，多个接收**
- **顺序通知**：按照列表顺序依次通知
- **同步通知**：当前实现是同步的

**🔸 解耦特性**：
- 主题**不知道**观察者的具体类型
- 主题**不关心**观察者如何处理数据
- 观察者**不知道**其他观察者的存在

### 7.3 通知时机设计


**⏰ 何时触发通知**：

**方式一：状态改变时立即通知**
```java
public void setTemperature(float temperature) {
    this.temperature = temperature;
    notifyObservers();  // 立即通知
}
```

**方式二：批量更新后统一通知**
```java
public void setWeatherData(float temp, float humidity, float pressure) {
    this.temperature = temp;
    this.humidity = humidity;
    this.pressure = pressure;
    notifyObservers();  // 批量更新后通知
}
```

> **💡 推荐方式二**：避免频繁通知，提高效率

### 7.4 通知机制的扩展


**🚀 高级通知机制**：

**异步通知**：
```java
public void notifyObservers() {
    for (Observer observer : observers) {
        // 异步通知，不阻塞主线程
        CompletableFuture.runAsync(() -> {
            observer.update(temperature, humidity, pressure);
        });
    }
}
```

**选择性通知**：
```java
public void notifyObservers(String eventType) {
    for (Observer observer : observers) {
        if (observer.isInterestedIn(eventType)) {
            observer.update(temperature, humidity, pressure);
        }
    }
}
```

---

## 8. 📋 核心要点总结


### 8.1 观察者模式的核心结构


```
观察者模式四大角色：

Subject（抽象主题）
├── 定义观察者管理接口
├── addObserver() / removeObserver()
└── notifyObservers()

ConcreteSubject（具体主题）  
├── 实现观察者管理
├── 维护业务状态
└── 状态改变时通知观察者

Observer（抽象观察者）
└── 定义update()接口

ConcreteObserver（具体观察者）
├── 实现update()方法
└── 处理具体业务逻辑
```

### 8.2 关键设计原则


**🔸 核心设计思想**：
- **一对多依赖**：一个主题对应多个观察者
- **状态同步**：主题状态改变，观察者自动更新
- **松散耦合**：主题和观察者相互独立
- **动态关系**：运行时可以添加/删除观察者

**🔸 实现要点**：
- **接口分离**：抽象接口定义标准，具体类实现细节
- **列表管理**：用集合存储和管理观察者
- **通知机制**：状态改变时主动通知所有观察者
- **业务分离**：通知机制与业务逻辑分离

### 8.3 使用场景判断


**✅ 适用场景**：
- **一个对象的改变需要通知多个其他对象**
- **需要在运行时动态地添加或删除依赖关系**
- **希望两个对象松散耦合**
- **需要广播通信的场景**

**❌ 不适用场景**：
- **只有一个观察者的简单场景**
- **对性能要求极高的场景**（频繁通知）
- **观察者之间有强依赖关系**

### 8.4 核心记忆要点


> **🎯 核心理念**：**发布-订阅机制**，一个发布者对应多个订阅者
> 
> **🔧 实现方式**：**接口 + 列表 + 遍历通知**
> 
> **💡 设计精髓**：**主题不关心观察者的具体实现，观察者不知道其他观察者的存在**
> 
> **🚀 核心价值**：**解耦 + 扩展性 + 广播通信**

**记忆口诀**：
- 一对多依赖，状态变化通知
- 接口定标准，列表管观察者  
- 主题发通知，观察者各响应
- 松散耦合好，动态添删易