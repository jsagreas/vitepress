---
title: 1、迭代器模式概念原理
---
## 📚 目录

1. [什么是迭代器模式](#1-什么是迭代器模式)
2. [为什么需要迭代器模式](#2-为什么需要迭代器模式)
3. [迭代器模式的核心结构](#3-迭代器模式的核心结构)
4. [统一访问接口详解](#4-统一访问接口详解)
5. [典型应用场景](#5-典型应用场景)
6. [优缺点全面分析](#6-优缺点全面分析)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🤔 什么是迭代器模式


### 1.1 生活中的迭代器


想象你去图书馆找书，不同的书架有不同的排列方式：

```
小说区书架：     技术书架：        杂志架：
书1 书2 书3      书A               杂志①
书4 书5 书6      书B               杂志②  
书7 书8 书9      书C               杂志③

你需要一个"导览员"来帮你按顺序查看每本书，
不管书架怎么排列，导览员都能带你遍历所有书籍
```

### 1.2 编程中的迭代器模式


**🔸 官方定义**：提供一种方法顺序访问一个容器对象中的各个元素，而又不需要暴露该对象的内部表示。

**💡 通俗理解**：
- 就像给不同的数据容器（数组、链表、树等）配一个**统一的遥控器**
- 这个遥控器有固定的按钮：`下一个`、`是否还有`、`当前元素`
- 不管容器内部结构多复杂，使用方式都一样简单

### 1.3 核心思想


```
使用者                     迭代器                    数据容器
  |                         |                        |
  |--"给我下一个"----------->|                        |
  |                         |--内部复杂逻辑---------->|数组/链表/树
  |<--返回具体元素----------|<-----------------------|
  |                         |                        |
  
关键：使用者不需要知道容器内部是怎么存储的
```

---

## 2. 🚨 为什么需要迭代器模式


### 2.1 没有迭代器的痛苦


假设你要遍历不同类型的数据结构：

```java
// 遍历数组 - 用索引
String[] books = {"Java", "Python", "C++"};
for (int i = 0; i < books.length; i++) {
    System.out.println(books[i]);
}

// 遍历链表 - 用节点
LinkedList<String> bookList = new LinkedList<>();
Node current = bookList.head;
while (current != null) {
    System.out.println(current.data);
    current = current.next;
}

// 遍历树 - 用递归
Tree bookTree = new Tree();
traverseTree(bookTree.root);
```

**😵 问题显而易见**：
- 每种数据结构的遍历方式**完全不同**
- 使用者必须**了解内部结构**
- 换个数据结构就要**重写遍历代码**
- 代码**耦合度极高**，难以维护

### 2.2 迭代器模式的解决方案


```java
// 统一的遍历方式，不管什么数据结构
Iterator<String> iterator = container.getIterator();
while (iterator.hasNext()) {
    String book = iterator.next();
    System.out.println(book);
}
```

**✨ 一套代码走天下**：
- 不管是数组、链表还是树，**使用方式完全一致**
- 使用者**不需要关心内部结构**
- 更换数据结构**不影响遍历代码**

### 2.3 容器遍历的本质问题


**🎯 核心矛盾**：
```
数据容器的职责：存储和管理数据
遍历访问的职责：按某种顺序访问数据

如果把遍历逻辑放在容器内部 → 容器变得复杂臃肿
如果让使用者直接访问内部结构 → 破坏封装性
```

**💡 迭代器的智慧**：
- 把**遍历职责**从容器中**分离出来**
- 创建专门的**迭代器对象**负责遍历
- 容器**保持简洁**，遍历**逻辑独立**

---

## 3. 🏗️ 迭代器模式的核心结构


### 3.1 角色分工图


```
┌─────────────────┐    使用    ┌─────────────────┐
│   客户端代码     │ ────────> │   迭代器接口     │
│  (Client)       │           │  (Iterator)     │
└─────────────────┘           └─────────────────┘
        │                            ↑
        │ 获取迭代器                   │ 实现
        ↓                            │
┌─────────────────┐    创建    ┌─────────────────┐
│   容器接口       │ ────────> │  具体迭代器      │
│ (Container)     │           │(ConcreteIterator)│
└─────────────────┘           └─────────────────┘
        ↑                            │
        │ 实现                        │ 访问
        │                            ↓
┌─────────────────┐           ┌─────────────────┐
│   具体容器       │ <──────── │   内部数据       │
│(ConcreteContainer)│   包含    │                │
└─────────────────┘           └─────────────────┘
```

### 3.2 核心接口设计


**🔸 迭代器接口**：
```java
public interface Iterator<T> {
    boolean hasNext();  // 还有下一个元素吗？
    T next();          // 获取下一个元素
    void remove();     // 删除当前元素（可选）
}
```

**🔸 容器接口**：
```java
public interface Container<T> {
    Iterator<T> getIterator();  // 获取迭代器
}
```

### 3.3 具体实现示例


**📋 书架容器实现**：
```java
public class BookShelf implements Container<String> {
    private String[] books;
    private int count = 0;
    
    public BookShelf(int size) {
        books = new String[size];
    }
    
    public void addBook(String book) {
        if (count < books.length) {
            books[count++] = book;
        }
    }
    
    @Override
    public Iterator<String> getIterator() {
        return new BookIterator();
    }
    
    // 内部迭代器类
    private class BookIterator implements Iterator<String> {
        private int position = 0;
        
        @Override
        public boolean hasNext() {
            return position < count;
        }
        
        @Override
        public String next() {
            if (hasNext()) {
                return books[position++];
            }
            return null;
        }
    }
}
```

### 3.4 使用方式


```java
// 创建书架并添加书籍
BookShelf shelf = new BookShelf(5);
shelf.addBook("设计模式");
shelf.addBook("Java编程");
shelf.addBook("算法导论");

// 统一的遍历方式
Iterator<String> iterator = shelf.getIterator();
while (iterator.hasNext()) {
    String book = iterator.next();
    System.out.println("正在阅读: " + book);
}
```

---

## 4. 🔄 统一访问接口详解


### 4.1 接口统一的价值


**🎯 一个接口，多种实现**：

```
统一的迭代器接口
        │
        ├─ 数组迭代器    → 按索引顺序访问
        ├─ 链表迭代器    → 按节点链接访问  
        ├─ 树迭代器      → 按深度优先访问
        └─ 哈希表迭代器  → 按哈希桶访问
```

**💡 使用者的体验**：
- **学习成本低**：只需要掌握一套API
- **代码可复用**：同样的遍历逻辑适用于所有容器
- **维护成本低**：修改遍历逻辑只需要改一处

### 4.2 接口方法详解


| 方法 | **作用** | **返回值** | **使用场景** |
|------|---------|-----------|-------------|
| `hasNext()` | `检查是否还有下一个元素` | `boolean` | `while循环条件判断` |
| `next()` | `获取下一个元素并移动指针` | `T` | `获取具体数据` |
| `remove()` | `删除当前元素（可选）` | `void` | `遍历时删除特定元素` |

### 4.3 内部状态管理


```java
public class ArrayIterator<T> implements Iterator<T> {
    private T[] array;
    private int position;     // 当前位置指针
    private int lastPosition; // 上次返回元素的位置
    
    public ArrayIterator(T[] array) {
        this.array = array;
        this.position = 0;
        this.lastPosition = -1;
    }
    
    @Override
    public boolean hasNext() {
        return position < array.length && array[position] != null;
    }
    
    @Override
    public T next() {
        if (!hasNext()) {
            throw new NoSuchElementException();
        }
        lastPosition = position;
        return array[position++];
    }
    
    @Override
    public void remove() {
        if (lastPosition < 0) {
            throw new IllegalStateException();
        }
        // 移除逻辑...
        lastPosition = -1;
    }
}
```

### 4.4 不同数据结构的统一访问


**🔸 链表迭代器**：
```java
private class LinkedListIterator implements Iterator<T> {
    private Node current = head;
    
    public boolean hasNext() {
        return current != null;
    }
    
    public T next() {
        T data = current.data;
        current = current.next;
        return data;
    }
}
```

**🔸 树结构迭代器（中序遍历）**：
```java
private class TreeIterator implements Iterator<T> {
    private Stack<TreeNode> stack = new Stack<>();
    
    public TreeIterator(TreeNode root) {
        pushLeft(root);
    }
    
    private void pushLeft(TreeNode node) {
        while (node != null) {
            stack.push(node);
            node = node.left;
        }
    }
    
    public boolean hasNext() {
        return !stack.isEmpty();
    }
    
    public T next() {
        TreeNode node = stack.pop();
        pushLeft(node.right);
        return node.data;
    }
}
```

---

## 5. 🎯 典型应用场景


### 5.1 Java集合框架


**📋 最经典的应用**：
```java
// List接口的迭代器
List<String> list = new ArrayList<>();
list.add("Apple");
list.add("Banana");

Iterator<String> iter = list.iterator();
while (iter.hasNext()) {
    System.out.println(iter.next());
}

// 增强for循环的本质也是迭代器
for (String item : list) {
    System.out.println(item);  // 编译器自动转换为迭代器调用
}
```

### 5.2 数据库结果集遍历


```java
// JDBC ResultSet本质上就是迭代器模式
ResultSet rs = statement.executeQuery("SELECT * FROM users");
while (rs.next()) {  // hasNext() + next()的组合
    String name = rs.getString("name");
    int age = rs.getInt("age");
    System.out.println(name + ", " + age);
}
```

### 5.3 文件系统遍历


```java
// 目录迭代器
public class DirectoryIterator implements Iterator<File> {
    private File[] files;
    private int position = 0;
    
    public DirectoryIterator(File directory) {
        this.files = directory.listFiles();
    }
    
    public boolean hasNext() {
        return position < files.length;
    }
    
    public File next() {
        return files[position++];
    }
}

// 使用
File dir = new File("/path/to/directory");
Iterator<File> fileIter = new DirectoryIterator(dir);
while (fileIter.hasNext()) {
    File file = fileIter.next();
    System.out.println(file.getName());
}
```

### 5.4 自定义数据结构


**🔸 分页数据迭代器**：
```java
public class PageIterator<T> implements Iterator<T> {
    private DataSource<T> dataSource;
    private int currentPage = 0;
    private int pageSize;
    private List<T> currentPageData;
    private int positionInPage = 0;
    
    public PageIterator(DataSource<T> dataSource, int pageSize) {
        this.dataSource = dataSource;
        this.pageSize = pageSize;
        loadNextPage();
    }
    
    private void loadNextPage() {
        currentPageData = dataSource.getPage(currentPage, pageSize);
        positionInPage = 0;
    }
    
    public boolean hasNext() {
        if (positionInPage < currentPageData.size()) {
            return true;
        }
        // 尝试加载下一页
        if (currentPageData.size() == pageSize) {
            currentPage++;
            loadNextPage();
            return positionInPage < currentPageData.size();
        }
        return false;
    }
    
    public T next() {
        if (!hasNext()) {
            throw new NoSuchElementException();
        }
        return currentPageData.get(positionInPage++);
    }
}
```

### 5.5 应用场景总结


| 场景类型 | **具体应用** | **优势** |
|---------|-------------|---------|
| **集合遍历** | `ArrayList、LinkedList、TreeSet` | `统一访问方式，隐藏内部结构` |
| **数据库访问** | `ResultSet、Cursor` | `逐行处理，节省内存` |
| **文件处理** | `目录遍历、日志文件读取` | `顺序访问，支持大文件` |
| **网络数据** | `分页API、流式数据` | `按需加载，提高性能` |
| **自定义容器** | `缓存系统、配置管理` | `封装复杂逻辑，简化使用` |

---

## 6. ⚖️ 优缺点全面分析


### 6.1 优点详解


**✅ 访问统一性**
```
好处：不管什么数据结构，使用方式都一样
实际价值：
- 学习成本：只需要学会一套API
- 开发效率：复用遍历代码逻辑
- 代码维护：统一的编程风格
```

**✅ 封装性保护**
```
好处：使用者不需要了解容器内部结构
实际价值：
- 信息隐藏：内部实现可以随意修改
- 安全性：防止外部直接修改内部数据
- 接口稳定：内部优化不影响外部调用
```

**✅ 职责分离**
```
好处：遍历逻辑从容器中独立出来
实际价值：
- 容器专注：只负责数据存储和管理
- 迭代器专注：只负责遍历逻辑
- 各司其职：代码结构更清晰
```

**✅ 多种遍历方式**
```java
// 同一个容器可以有多种迭代器
public class BookShelf {
    public Iterator<Book> getIterator() {
        return new ForwardIterator();  // 正向遍历
    }
    
    public Iterator<Book> getReverseIterator() {
        return new ReverseIterator();  // 反向遍历
    }
    
    public Iterator<Book> getRandomIterator() {
        return new RandomIterator();   // 随机遍历
    }
}
```

### 6.2 缺点分析


**❌ 性能开销**
```
问题：相比直接访问，迭代器有额外开销
具体表现：
- 对象创建：每次遍历都要创建迭代器对象
- 方法调用：hasNext()和next()的方法调用开销
- 内存占用：迭代器需要维护状态信息

适用建议：
- 性能要求不高的场景可以忽略
- 高性能场景可以考虑直接访问
```

**❌ 复杂性增加**
```
问题：简单的遍历变得复杂
具体表现：
- 类的数量：每个容器都需要对应的迭代器类
- 代码量：相比直接for循环代码更多
- 理解成本：需要理解迭代器模式的概念

适用建议：
- 简单的一次性遍历可以直接使用for循环
- 复杂的遍历逻辑建议使用迭代器
```

**❌ 并发修改问题**
```java
// 经典的并发修改异常
List<String> list = new ArrayList<>();
list.add("A");
list.add("B");

Iterator<String> iter = list.iterator();
while (iter.hasNext()) {
    String item = iter.next();
    if ("A".equals(item)) {
        list.remove(item);  // ❌ 抛出ConcurrentModificationException
    }
}

// 正确做法
Iterator<String> iter = list.iterator();
while (iter.hasNext()) {
    String item = iter.next();
    if ("A".equals(item)) {
        iter.remove();  // ✅ 使用迭代器的remove方法
    }
}
```

### 6.3 使用决策指南


```
选择迭代器模式的情况：
✅ 容器内部结构复杂
✅ 需要多种遍历方式
✅ 遍历逻辑可能经常变化
✅ 需要统一的访问接口
✅ 代码复用性要求高

选择直接访问的情况：
✅ 容器结构简单（如数组）
✅ 性能要求极高
✅ 只有简单的一次性遍历
✅ 不需要封装内部结构
✅ 代码量要求尽可能少
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 迭代器模式本质：为不同容器提供统一的遍历接口
🔸 核心价值：封装遍历逻辑，隐藏内部结构，统一访问方式
🔸 基本角色：迭代器接口、具体迭代器、容器接口、具体容器
🔸 关键方法：hasNext()检查、next()获取、remove()删除
🔸 应用广泛：Java集合、数据库结果集、文件系统等
```

### 7.2 关键理解要点


**🔹 为什么需要迭代器**
```
核心问题：不同数据结构的遍历方式不同
解决方案：提供统一的遍历接口
实际价值：代码复用、封装保护、职责分离
```

**🔹 迭代器vs直接访问**
```
迭代器优势：统一接口、封装性好、支持多种遍历
直接访问优势：性能更高、代码更简单
选择原则：根据具体需求权衡利弊
```

**🔹 内部迭代器vs外部迭代器**
```
内部迭代器：容器内部控制遍历过程（如forEach）
外部迭代器：客户端控制遍历过程（如Iterator）
各有优势：内部迭代器简单，外部迭代器灵活
```

### 7.3 实际应用指导


**🎯 何时使用迭代器模式**：
- 数据容器**内部结构复杂**，不想暴露给使用者
- 需要为**同一个容器提供多种遍历方式**
- 希望**统一不同容器的访问接口**
- 遍历逻辑可能**频繁变化或扩展**

**🔧 实现要点**：
- 迭代器接口要**简洁明了**，通常只需要hasNext()和next()
- 迭代器要**维护自己的状态**，不影响容器本身
- 考虑**并发修改的安全性**，提供安全的删除方法
- 支持**多个迭代器同时工作**，互不干扰

**⚠️ 注意事项**：
- 避免在遍历过程中**直接修改容器**
- 注意迭代器的**生命周期管理**
- 考虑**性能开销**，在高性能场景下权衡使用
- 保证迭代器的**状态一致性**

**核心记忆**：
- 迭代器像遥控器，统一操作不同设备
- 封装遍历细节，提供简洁访问接口  
- Java集合框架是最佳学习实例
- 平衡封装性与性能，按需选择使用