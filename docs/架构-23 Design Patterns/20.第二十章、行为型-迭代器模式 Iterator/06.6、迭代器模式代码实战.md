---
title: 6、迭代器模式代码实战
---
## 📚 目录

1. [迭代器模式实战概览](#1-迭代器模式实战概览)
2. [自定义容器实现](#2-自定义容器实现)
3. [多种迭代器对比](#3-多种迭代器对比)
4. [性能测试分析](#4-性能测试分析)
5. [并发安全实现](#5-并发安全实现)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 迭代器模式实战概览


### 1.1 什么是迭代器模式的代码实战


**💡 简单理解**：就像遥控器可以控制不同品牌的电视一样，迭代器模式让我们用**统一的方式**来遍历各种不同的容器。

```
生活中的例子：
📺 遥控器(迭代器) → 可以操作 → 各种电视(容器)
🎵 播放器控制按钮 → 可以播放 → 不同格式音乐
🚌 公交车路线 → 按顺序停靠 → 各个站点

编程中的迭代器：
Iterator接口 → 可以遍历 → List、Set、Map等容器
```

### 1.2 为什么需要实际编码实战


**🔸 理论 vs 实践的差距**
```
只看理论：知道迭代器有next()、hasNext()方法
动手实战：
  ✅ 理解内部是怎么记住当前位置的
  ✅ 明白为什么要这样设计
  ✅ 学会处理边界情况和异常
  ✅ 掌握性能优化技巧
```

### 1.3 本章实战内容预览


| 实战内容 | **核心价值** | **难度等级** |
|---------|-------------|-------------|
| 🏗️ **自定义容器** | `理解迭代器工作原理` | ⭐⭐☆☆☆ |
| 🔄 **多种迭代器** | `掌握不同遍历策略` | ⭐⭐⭐☆☆ |
| ⚡ **性能测试** | `学会性能分析方法` | ⭐⭐⭐⭐☆ |
| 🔒 **并发安全** | `解决多线程问题` | ⭐⭐⭐⭐⭐ |

---

## 2. 🏗️ 自定义容器实现


### 2.1 设计一个简单的书架容器


**💭 设计思路**：想象一个书架，我们要能够按顺序取出每本书来阅读。

```
书架的样子：
┌─────────────────┐
│  📚 Java编程     │ ← 第0本
├─────────────────┤
│  📘 设计模式     │ ← 第1本  
├─────────────────┤
│  📗 算法导论     │ ← 第2本
└─────────────────┘

我们需要一个"取书助手"(迭代器)，能够：
1️⃣ 记住现在看到第几本书
2️⃣ 告诉我们还有没有下一本
3️⃣ 帮我们取出下一本书
```

### 2.2 基础容器类实现


```java
// 书本类 - 我们要存储的数据
class Book {
    private String title;
    private String author;
    
    public Book(String title, String author) {
        this.title = title;
        this.author = author;
    }
    
    @Override
    public String toString() {
        return title + " - " + author;
    }
}

// 书架容器类 - 自定义的数据结构
class BookShelf {
    private Book[] books;      // 存书的数组
    private int currentIndex;  // 当前放到第几个位置了
    private int capacity;      // 书架总容量
    
    public BookShelf(int capacity) {
        this.capacity = capacity;
        this.books = new Book[capacity];
        this.currentIndex = 0;
    }
    
    // 往书架上放书
    public void addBook(Book book) {
        if (currentIndex >= capacity) {
            System.out.println("❌ 书架满了，放不下更多书！");
            return;
        }
        books[currentIndex] = book;
        currentIndex++;
        System.out.println("✅ 成功放入：" + book);
    }
    
    // 获取书架上书的总数
    public int getBookCount() {
        return currentIndex;
    }
    
    // 根据索引获取书（内部使用）
    Book getBookAt(int index) {
        if (index >= 0 && index < currentIndex) {
            return books[index];
        }
        return null;
    }
}
```

### 2.3 自定义迭代器实现


```java
// 书架迭代器 - 专门用来遍历书架的工具
class BookShelfIterator {
    private BookShelf bookShelf;  // 要遍历的书架
    private int currentPosition;  // 当前读到第几本书
    
    public BookShelfIterator(BookShelf bookShelf) {
        this.bookShelf = bookShelf;
        this.currentPosition = 0;  // 从第一本书开始
    }
    
    // 检查是否还有下一本书
    public boolean hasNext() {
        return currentPosition < bookShelf.getBookCount();
    }
    
    // 获取下一本书
    public Book next() {
        if (!hasNext()) {
            System.out.println("📚 已经是最后一本书了！");
            return null;
        }
        
        Book book = bookShelf.getBookAt(currentPosition);
        currentPosition++;  // 移动到下一个位置
        return book;
    }
    
    // 重置到开始位置
    public void reset() {
        currentPosition = 0;
        System.out.println("🔄 迭代器已重置到开始位置");
    }
}
```

### 2.4 让书架支持迭代器


```java
// 改进版书架 - 内置迭代器支持
class BookShelfImproved {
    private Book[] books;
    private int currentIndex;
    private int capacity;
    
    public BookShelfImproved(int capacity) {
        this.capacity = capacity;
        this.books = new Book[capacity];
        this.currentIndex = 0;
    }
    
    // ... 其他方法保持不变 ...
    
    // 🎯 关键方法：创建迭代器
    public BookShelfIterator createIterator() {
        return new BookShelfIterator(this);
    }
    
    // 🎯 支持增强for循环的方法（实现Iterable接口）
    public Iterator<Book> iterator() {
        return new Iterator<Book>() {
            private int position = 0;
            
            @Override
            public boolean hasNext() {
                return position < currentIndex;
            }
            
            @Override
            public Book next() {
                if (!hasNext()) {
                    throw new NoSuchElementException("没有更多书籍了");
                }
                return books[position++];
            }
        };
    }
}
```

### 2.5 使用示例


```java
public class BookShelfDemo {
    public static void main(String[] args) {
        // 1️⃣ 创建书架并添加书籍
        BookShelfImproved myShelf = new BookShelfImproved(5);
        myShelf.addBook(new Book("Java编程思想", "Bruce Eckel"));
        myShelf.addBook(new Book("设计模式", "GoF"));
        myShelf.addBook(new Book("算法导论", "CLRS"));
        
        System.out.println("\n📖 开始遍历书架：");
        
        // 2️⃣ 使用自定义迭代器
        BookShelfIterator iterator = myShelf.createIterator();
        while (iterator.hasNext()) {
            Book book = iterator.next();
            System.out.println("正在阅读：" + book);
        }
        
        // 3️⃣ 使用增强for循环（更简洁）
        System.out.println("\n🔄 使用增强for循环：");
        for (Book book : myShelf) {
            System.out.println("发现好书：" + book);
        }
    }
}
```

---

## 3. 🔄 多种迭代器对比


### 3.1 不同遍历策略的迭代器


**💡 核心思想**：同一个书架，可以有不同的读书方式

```
正序阅读：📚 → 📘 → 📗 (从第一本到最后一本)
倒序阅读：📗 → 📘 → 📚 (从最后一本到第一本)  
隔本阅读：📚 → 📗 (跳过中间的书)
随机阅读：📘 → 📚 → 📗 (随机顺序)
```

### 3.2 正序迭代器（Forward Iterator）


```java
// 正序迭代器 - 从头到尾遍历
class ForwardIterator {
    private BookShelf bookShelf;
    private int currentPosition;
    
    public ForwardIterator(BookShelf bookShelf) {
        this.bookShelf = bookShelf;
        this.currentPosition = 0;
    }
    
    public boolean hasNext() {
        return currentPosition < bookShelf.getBookCount();
    }
    
    public Book next() {
        if (!hasNext()) return null;
        return bookShelf.getBookAt(currentPosition++);
    }
    
    public String getDirection() {
        return "🔜 正序遍历 (从前到后)";
    }
}
```

### 3.3 倒序迭代器（Reverse Iterator）


```java
// 倒序迭代器 - 从尾到头遍历
class ReverseIterator {
    private BookShelf bookShelf;
    private int currentPosition;
    
    public ReverseIterator(BookShelf bookShelf) {
        this.bookShelf = bookShelf;
        this.currentPosition = bookShelf.getBookCount() - 1; // 从最后开始
    }
    
    public boolean hasNext() {
        return currentPosition >= 0;  // 注意：这里是 >= 0
    }
    
    public Book next() {
        if (!hasNext()) return null;
        return bookShelf.getBookAt(currentPosition--);  // 注意：这里是 --
    }
    
    public String getDirection() {
        return "🔙 倒序遍历 (从后到前)";
    }
}
```

### 3.4 步进迭代器（Step Iterator）


```java
// 步进迭代器 - 按指定步长遍历
class StepIterator {
    private BookShelf bookShelf;
    private int currentPosition;
    private int stepSize;  // 步长
    
    public StepIterator(BookShelf bookShelf, int stepSize) {
        this.bookShelf = bookShelf;
        this.currentPosition = 0;
        this.stepSize = Math.max(1, stepSize); // 步长至少为1
    }
    
    public boolean hasNext() {
        return currentPosition < bookShelf.getBookCount();
    }
    
    public Book next() {
        if (!hasNext()) return null;
        
        Book book = bookShelf.getBookAt(currentPosition);
        currentPosition += stepSize;  // 按步长前进
        return book;
    }
    
    public String getDirection() {
        return "⏩ 步进遍历 (步长: " + stepSize + ")";
    }
}
```

### 3.5 过滤迭代器（Filter Iterator）


```java
// 过滤迭代器 - 只遍历符合条件的元素
class FilterIterator {
    private BookShelf bookShelf;
    private int currentPosition;
    private String keywordFilter;  // 过滤关键词
    
    public FilterIterator(BookShelf bookShelf, String keyword) {
        this.bookShelf = bookShelf;
        this.currentPosition = 0;
        this.keywordFilter = keyword.toLowerCase();
    }
    
    public boolean hasNext() {
        // 寻找下一个符合条件的书
        while (currentPosition < bookShelf.getBookCount()) {
            Book book = bookShelf.getBookAt(currentPosition);
            if (book.toString().toLowerCase().contains(keywordFilter)) {
                return true;  // 找到了符合条件的书
            }
            currentPosition++;
        }
        return false;
    }
    
    public Book next() {
        if (!hasNext()) return null;
        return bookShelf.getBookAt(currentPosition++);
    }
    
    public String getDirection() {
        return "🔍 过滤遍历 (关键词: " + keywordFilter + ")";
    }
}
```

### 3.6 迭代器对比测试


```java
public class IteratorComparison {
    public static void main(String[] args) {
        // 准备测试数据
        BookShelf shelf = new BookShelf(6);
        shelf.addBook(new Book("Java基础", "张三"));
        shelf.addBook(new Book("Python入门", "李四"));
        shelf.addBook(new Book("Java高级", "王五"));
        shelf.addBook(new Book("数据结构", "赵六"));
        shelf.addBook(new Book("Java设计模式", "田七"));
        
        System.out.println("📚 书架内容准备完成，开始对比不同迭代器：\n");
        
        // 测试正序迭代器
        testIterator("正序", new ForwardIterator(shelf));
        
        // 测试倒序迭代器  
        testIterator("倒序", new ReverseIterator(shelf));
        
        // 测试步进迭代器
        testIterator("步进", new StepIterator(shelf, 2));
        
        // 测试过滤迭代器
        testIterator("过滤", new FilterIterator(shelf, "Java"));
    }
    
    // 通用测试方法
    private static void testIterator(String type, Object iterator) {
        System.out.println("=== " + type + "迭代器测试 ===");
        
        // 使用反射调用hasNext和next方法（简化代码）
        try {
            Method hasNext = iterator.getClass().getMethod("hasNext");
            Method next = iterator.getClass().getMethod("next");
            Method getDirection = iterator.getClass().getMethod("getDirection");
            
            System.out.println(getDirection.invoke(iterator));
            
            int count = 0;
            while ((Boolean) hasNext.invoke(iterator)) {
                Book book = (Book) next.invoke(iterator);
                System.out.println("  " + (++count) + ". " + book);
            }
            System.out.println("遍历完成，共 " + count + " 本书\n");
            
        } catch (Exception e) {
            System.out.println("测试出错：" + e.getMessage());
        }
    }
}
```

---

## 4. ⚡ 性能测试分析


### 4.1 性能测试的重要性


**💡 为什么要做性能测试？**

```
实际开发中的考虑：
🚀 速度：遍历100万条数据要多久？
💾 内存：迭代器本身占用多少内存？
🔄 并发：多个线程同时迭代会怎样？
📊 对比：不同实现方式的性能差异？
```

### 4.2 性能测试框架搭建


```java
// 性能测试工具类
class PerformanceTestUtils {
    
    // 测试迭代器遍历性能
    public static long testIterationTime(String testName, Runnable testCode) {
        System.out.println("🧪 开始测试：" + testName);
        
        // 预热JVM
        for (int i = 0; i < 1000; i++) {
            testCode.run();
        }
        
        // 正式测试
        long startTime = System.nanoTime();
        testCode.run();
        long endTime = System.nanoTime();
        
        long duration = endTime - startTime;
        System.out.println("⏱️  耗时：" + formatTime(duration));
        return duration;
    }
    
    // 测试内存使用
    public static void testMemoryUsage(String testName, Supplier<Object> objectCreator) {
        Runtime runtime = Runtime.getRuntime();
        
        // 强制垃圾回收
        System.gc();
        long beforeMemory = runtime.totalMemory() - runtime.freeMemory();
        
        // 创建对象
        Object testObject = objectCreator.get();
        
        System.gc();
        long afterMemory = runtime.totalMemory() - runtime.freeMemory();
        
        long memoryUsed = afterMemory - beforeMemory;
        System.out.println("💾 " + testName + " 内存使用：" + formatMemory(memoryUsed));
    }
    
    private static String formatTime(long nanos) {
        if (nanos < 1000) return nanos + " ns";
        if (nanos < 1000000) return String.format("%.2f μs", nanos / 1000.0);
        if (nanos < 1000000000) return String.format("%.2f ms", nanos / 1000000.0);
        return String.format("%.2f s", nanos / 1000000000.0);
    }
    
    private static String formatMemory(long bytes) {
        if (bytes < 1024) return bytes + " B";
        if (bytes < 1024 * 1024) return String.format("%.2f KB", bytes / 1024.0);
        return String.format("%.2f MB", bytes / (1024.0 * 1024.0));
    }
}
```

### 4.3 大数据量性能测试


```java
public class LargeDataPerformanceTest {
    
    public static void main(String[] args) {
        System.out.println("📊 迭代器性能测试开始\n");
        
        // 创建大容量书架
        int[] testSizes = {1000, 10000, 100000, 1000000};
        
        for (int size : testSizes) {
            System.out.println("📈 测试数据量：" + size + " 本书");
            testWithSize(size);
            System.out.println();
        }
    }
    
    private static void testWithSize(int size) {
        // 准备测试数据
        List<Book> bookList = new ArrayList<>();
        for (int i = 0; i < size; i++) {
            bookList.add(new Book("书籍" + i, "作者" + i));
        }
        
        // 测试ArrayList迭代器
        PerformanceTestUtils.testIterationTime(
            "ArrayList迭代器", 
            () -> iterateList(bookList)
        );
        
        // 测试增强for循环
        PerformanceTestUtils.testIterationTime(
            "增强for循环", 
            () -> enhancedForLoop(bookList)
        );
        
        // 测试传统for循环
        PerformanceTestUtils.testIterationTime(
            "传统for循环", 
            () -> traditionalForLoop(bookList)
        );
        
        // 测试流式API
        PerformanceTestUtils.testIterationTime(
            "Stream API", 
            () -> streamIteration(bookList)
        );
    }
    
    // 使用迭代器遍历
    private static void iterateList(List<Book> books) {
        Iterator<Book> iterator = books.iterator();
        int count = 0;
        while (iterator.hasNext()) {
            Book book = iterator.next();
            count++; // 模拟处理
        }
    }
    
    // 增强for循环
    private static void enhancedForLoop(List<Book> books) {
        int count = 0;
        for (Book book : books) {
            count++; // 模拟处理
        }
    }
    
    // 传统for循环
    private static void traditionalForLoop(List<Book> books) {
        int count = 0;
        for (int i = 0; i < books.size(); i++) {
            Book book = books.get(i);
            count++; // 模拟处理
        }
    }
    
    // Stream API
    private static void streamIteration(List<Book> books) {
        long count = books.stream().count();
    }
}
```

### 4.4 内存效率测试


```java
public class MemoryEfficiencyTest {
    
    public static void main(String[] args) {
        System.out.println("💾 内存效率测试\n");
        
        final int TEST_SIZE = 10000;
        
        // 测试不同容器的内存使用
        PerformanceTestUtils.testMemoryUsage(
            "ArrayList", 
            () -> createArrayList(TEST_SIZE)
        );
        
        PerformanceTestUtils.testMemoryUsage(
            "LinkedList", 
            () -> createLinkedList(TEST_SIZE)
        );
        
        PerformanceTestUtils.testMemoryUsage(
            "自定义书架", 
            () -> createBookShelf(TEST_SIZE)
        );
        
        // 测试迭代器本身的内存开销
        ArrayList<Book> testList = createArrayList(TEST_SIZE);
        
        PerformanceTestUtils.testMemoryUsage(
            "ArrayList迭代器", 
            () -> testList.iterator()
        );
    }
    
    private static ArrayList<Book> createArrayList(int size) {
        ArrayList<Book> list = new ArrayList<>();
        for (int i = 0; i < size; i++) {
            list.add(new Book("书" + i, "作者" + i));
        }
        return list;
    }
    
    private static LinkedList<Book> createLinkedList(int size) {
        LinkedList<Book> list = new LinkedList<>();
        for (int i = 0; i < size; i++) {
            list.add(new Book("书" + i, "作者" + i));
        }
        return list;
    }
    
    private static BookShelf createBookShelf(int size) {
        BookShelf shelf = new BookShelf(size);
        for (int i = 0; i < size; i++) {
            shelf.addBook(new Book("书" + i, "作者" + i));
        }
        return shelf;
    }
}
```

### 4.5 性能优化建议


| 场景 | **推荐方案** | **性能特点** | **注意事项** |
|------|-------------|-------------|-------------|
| 🚀 **大量数据遍历** | `传统for循环` | `最快` | `只适用于List` |
| 🔄 **通用遍历** | `增强for循环` | `较快且简洁` | `底层使用迭代器` |
| 🎯 **复杂处理** | `Stream API` | `功能强大` | `小数据量有开销` |
| 📊 **自定义容器** | `实现Iterator接口` | `标准化` | `需要考虑fail-fast` |

---

## 5. 🔒 并发安全实现


### 5.1 并发环境下的问题


**💭 想象场景**：多个人同时在图书馆借书

```
问题场景：
👨‍💼 张三正在浏览书架第3本书
👩‍💼 李四同时也在浏览书架，刚好也到第3本
📚 此时管理员把第2本书拿走了
❓ 结果：张三和李四可能会看到不一致的状态

编程中的问题：
🧵 线程A正在遍历List
🧵 线程B同时修改了List（添加/删除元素）
💥 结果：ConcurrentModificationException 异常
```

### 5.2 传统迭代器的并发问题


```java
// 演示并发修改异常
public class ConcurrentProblemDemo {
    
    public static void main(String[] args) {
        List<String> books = new ArrayList<>();
        books.add("Java编程");
        books.add("设计模式");
        books.add("算法导论");
        books.add("数据结构");
        
        System.out.println("❌ 演示并发修改问题：");
        
        try {
            // 开始遍历
            Iterator<String> iterator = books.iterator();
            
            while (iterator.hasNext()) {
                String book = iterator.next();
                System.out.println("正在阅读：" + book);
                
                // 模拟在遍历过程中修改集合
                if ("设计模式".equals(book)) {
                    books.add("新书：Spring实战");  // 💥 这里会引发异常
                }
            }
        } catch (ConcurrentModificationException e) {
            System.out.println("💥 发生并发修改异常：" + e.getMessage());
        }
    }
}
```

### 5.3 Fail-Fast 机制解析


**🔸 什么是Fail-Fast？**
```
Fail-Fast = "快速失败"
原理：迭代器检测到集合被修改时，立即抛出异常
目的：及早发现并发问题，避免数据不一致

检测机制：
集合维护 modCount (修改次数计数器)
迭代器创建时记录 expectedModCount
每次调用next()时检查：modCount == expectedModCount
如果不相等，说明集合被修改了，抛出异常
```

### 5.4 线程安全的迭代器实现


```java
// 线程安全的书架实现
class ThreadSafeBookShelf {
    private final List<Book> books;
    private final ReadWriteLock lock;  // 读写锁
    
    public ThreadSafeBookShelf() {
        this.books = new ArrayList<>();
        this.lock = new ReentrantReadWriteLock();
    }
    
    // 添加书籍（写操作）
    public void addBook(Book book) {
        lock.writeLock().lock();
        try {
            books.add(book);
            System.out.println("✅ 线程 " + Thread.currentThread().getName() + 
                             " 添加了书籍：" + book);
        } finally {
            lock.writeLock().unlock();
        }
    }
    
    // 创建线程安全的迭代器
    public SafeIterator createSafeIterator() {
        return new SafeIterator();
    }
    
    // 线程安全的迭代器内部类
    private class SafeIterator {
        private final List<Book> snapshot;  // 快照副本
        private int currentIndex = 0;
        
        public SafeIterator() {
            lock.readLock().lock();
            try {
                // 创建当前状态的快照
                this.snapshot = new ArrayList<>(books);
            } finally {
                lock.readLock().unlock();
            }
        }
        
        public boolean hasNext() {
            return currentIndex < snapshot.size();
        }
        
        public Book next() {
            if (!hasNext()) {
                throw new NoSuchElementException("没有更多元素");
            }
            
            Book book = snapshot.get(currentIndex++);
            System.out.println("📖 线程 " + Thread.currentThread().getName() + 
                             " 正在阅读：" + book);
            return book;
        }
    }
}
```

### 5.5 基于CopyOnWrite的实现


```java
// 使用CopyOnWriteArrayList的线程安全方案
class CopyOnWriteBookShelf {
    private final CopyOnWriteArrayList<Book> books;
    
    public CopyOnWriteBookShelf() {
        this.books = new CopyOnWriteArrayList<>();
    }
    
    public void addBook(Book book) {
        books.add(book);
        System.out.println("📚 添加书籍：" + book + 
                         " (线程：" + Thread.currentThread().getName() + ")");
    }
    
    // CopyOnWriteArrayList的迭代器天然线程安全
    public Iterator<Book> iterator() {
        return books.iterator();
    }
    
    public void printAllBooks(String readerName) {
        System.out.println("\n👤 " + readerName + " 开始阅读：");
        Iterator<Book> iterator = books.iterator();
        
        while (iterator.hasNext()) {
            Book book = iterator.next();
            System.out.println("  📖 " + readerName + " 读到：" + book);
            
            // 模拟阅读时间
            try {
                Thread.sleep(100);
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
            }
        }
        System.out.println("  ✅ " + readerName + " 阅读完成");
    }
}
```

### 5.6 并发测试验证


```java
public class ConcurrentSafetyTest {
    
    public static void main(String[] args) throws InterruptedException {
        testCopyOnWriteSafety();
        Thread.sleep(2000);
        testCustomSafety();
    }
    
    // 测试CopyOnWrite方案
    private static void testCopyOnWriteSafety() throws InterruptedException {
        System.out.println("🧪 测试CopyOnWrite线程安全性\n");
        
        CopyOnWriteBookShelf shelf = new CopyOnWriteBookShelf();
        
        // 初始化一些书籍
        shelf.addBook(new Book("Java基础", "张三"));
        shelf.addBook(new Book("设计模式", "李四"));
        
        // 创建多个读者线程
        Thread reader1 = new Thread(() -> shelf.printAllBooks("读者1"));
        Thread reader2 = new Thread(() -> shelf.printAllBooks("读者2"));
        
        // 创建写入线程
        Thread writer = new Thread(() -> {
            try {
                Thread.sleep(150);  // 等待读者开始阅读
                shelf.addBook(new Book("算法导论", "王五"));
                Thread.sleep(150);
                shelf.addBook(new Book("数据结构", "赵六"));
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
            }
        });
        
        // 启动所有线程
        reader1.start();
        reader2.start();
        writer.start();
        
        // 等待所有线程完成
        reader1.join();
        reader2.join();
        writer.join();
        
        System.out.println("🎉 CopyOnWrite测试完成，无异常发生\n");
    }
    
    // 测试自定义线程安全方案
    private static void testCustomSafety() throws InterruptedException {
        System.out.println("🧪 测试自定义线程安全方案\n");
        
        ThreadSafeBookShelf shelf = new ThreadSafeBookShelf();
        
        // 初始化书籍
        shelf.addBook(new Book("Spring实战", "田七"));
        shelf.addBook(new Book("MyBatis教程", "孙八"));
        
        // 创建并发读写任务
        ExecutorService executor = Executors.newFixedThreadPool(4);
        
        // 提交读取任务
        for (int i = 1; i <= 3; i++) {
            final int readerId = i;
            executor.submit(() -> {
                ThreadSafeBookShelf.SafeIterator iterator = shelf.createSafeIterator();
                while (iterator.hasNext()) {
                    iterator.next();
                    try {
                        Thread.sleep(50);
                    } catch (InterruptedException e) {
                        Thread.currentThread().interrupt();
                    }
                }
            });
        }
        
        // 提交写入任务
        executor.submit(() -> {
            try {
                Thread.sleep(100);
                shelf.addBook(new Book("Redis实战", "新作者"));
                Thread.sleep(100);
                shelf.addBook(new Book("Kafka指南", "另一作者"));
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
            }
        });
        
        executor.shutdown();
        executor.awaitTermination(5, TimeUnit.SECONDS);
        
        System.out.println("🎉 自定义线程安全测试完成\n");
    }
}
```

### 5.7 并发安全方案对比


| 方案 | **优点** | **缺点** | **适用场景** |
|------|---------|---------|-------------|
| 🔒 **同步锁** | `数据一致性强` | `性能开销大，可能死锁` | `写操作频繁` |
| 📋 **快照模式** | `读性能好，无锁` | `内存占用增加` | `读多写少` |
| 🔄 **CopyOnWrite** | `读写分离，简单` | `写时复制开销大` | `读多写极少` |
| ⚡ **无锁算法** | `性能最好` | `实现复杂` | `高性能要求` |

---

## 6. 📋 核心要点总结


### 6.1 迭代器模式实战收获


**🎯 核心理解**
- **统一接口**：`hasNext()` + `next()` 就能遍历任何容器
- **职责分离**：容器负责存储，迭代器负责遍历
- **多样化策略**：同一容器可以有多种遍历方式
- **性能考量**：不同实现方式有不同的性能特点

### 6.2 关键技术点掌握


**🔸 实现要点**
```
基础实现：
✅ 维护当前位置指针
✅ 实现hasNext()判断逻辑  
✅ 实现next()获取元素逻辑
✅ 处理边界条件和异常

进阶技巧：
✅ 支持多种遍历策略
✅ 实现reset()重置功能
✅ 考虑内存和性能优化
✅ 解决并发安全问题
```

### 6.3 实际应用指导


**🚀 选择建议**

**性能优先场景**：
- 大数据量 → 传统for循环
- 复杂逻辑 → Stream API  
- 通用遍历 → 增强for循环

**并发安全场景**：
- 读多写少 → CopyOnWriteArrayList
- 读写均衡 → synchronized或Lock
- 高并发 → 无锁数据结构

### 6.4 常见陷阱避免


**⚠️ 注意事项**
```
并发修改：
❌ 遍历时直接修改集合
✅ 使用Iterator.remove()或并发安全容器

性能问题：
❌ 在循环中创建新迭代器
✅ 复用迭代器对象

内存泄漏：
❌ 长期持有迭代器引用
✅ 及时释放迭代器资源
```

### 6.5 学习进阶路径


**📚 后续学习建议**
1. **深入源码**：研究JDK中Iterator的实现
2. **框架应用**：了解Spring、MyBatis中的迭代器使用
3. **自定义容器**：尝试实现更复杂的数据结构
4. **性能调优**：学习JVM调优和并发编程

**核心记忆**：
- 迭代器让遍历变得简单统一
- 实现时要考虑性能和线程安全
- 不同场景选择合适的迭代策略
- 动手实践比理论学习更重要