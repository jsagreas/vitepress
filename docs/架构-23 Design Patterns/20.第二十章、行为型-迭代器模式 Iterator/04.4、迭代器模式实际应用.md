---
title: 4、迭代器模式实际应用
---
## 📚 目录

1. [迭代器模式在Java集合框架中的应用](#1-迭代器模式在Java集合框架中的应用)
2. [数据库结果集中的迭代器应用](#2-数据库结果集中的迭代器应用)
3. [文件系统遍历的迭代器实现](#3-文件系统遍历的迭代器实现)
4. [树形结构遍历的迭代器模式](#4-树形结构遍历的迭代器模式)
5. [自定义容器设计与迭代器](#5-自定义容器设计与迭代器)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🔄 迭代器模式在Java集合框架中的应用


### 1.1 什么是Java集合框架中的迭代器


**通俗理解**：就像你在图书馆里一本一本地翻阅书籍，Java的迭代器就是帮你一个一个地访问集合中元素的"翻页器"。

```
现实生活中的遍历：              Java集合中的遍历：
图书馆看书                     ArrayList遍历
┌─────────────┐               ┌─────────────┐
│  📖 书本1   │               │   元素1     │
│  📖 书本2   │  ←翻页器       │   元素2     │  ←Iterator
│  📖 书本3   │               │   元素3     │
│  📖 书本4   │               │   元素4     │
└─────────────┘               └─────────────┘
```

### 1.2 Java迭代器的核心接口


**Iterator接口**：这是Java提供的标准迭代器"合同"，规定了迭代器必须会做的事情。

| 方法名 | **作用** | **通俗解释** |
|--------|---------|-------------|
| `hasNext()` | `检查是否还有下一个元素` | `就像问"还有书吗？"` |
| `next()` | `获取下一个元素并移动指针` | `拿起下一本书来看` |
| `remove()` | `删除当前元素` | `把这本书放回书架` |

### 1.3 实际使用示例


**🟢入门级** - ArrayList的迭代器使用：

```java
// 创建一个装水果的列表
List<String> fruits = new ArrayList<>();
fruits.add("苹果");
fruits.add("香蕉");
fruits.add("橘子");

// 获取迭代器（就像拿到了遍历这个列表的"遥控器"）
Iterator<String> iterator = fruits.iterator();

// 使用迭代器遍历
while (iterator.hasNext()) {  // 还有水果吗？
    String fruit = iterator.next();  // 拿出下一个水果
    System.out.println("当前水果：" + fruit);
}
```

**🟡进阶级** - 不同集合类型的迭代器：

```java
// HashSet的迭代器（无序集合）
Set<Integer> numbers = new HashSet<>();
numbers.add(1); numbers.add(3); numbers.add(2);

for (Integer num : numbers) {  // 增强for循环本质上就是迭代器
    System.out.println(num);  // 输出顺序不固定
}

// LinkedList的迭代器（可双向遍历）
LinkedList<String> cities = new LinkedList<>();
cities.add("北京"); cities.add("上海"); cities.add("广州");

ListIterator<String> listIter = cities.listIterator();
// 可以前进也可以后退
while (listIter.hasNext()) {
    System.out.println("前进：" + listIter.next());
}
```

### 1.4 为什么Java要用迭代器模式


**🔸 统一的访问方式**
```
不用迭代器的痛苦：
ArrayList → 用下标：for(int i=0; i<list.size(); i++)
LinkedList → 用节点：复杂的指针操作
HashSet → 完全不知道怎么遍历

有了迭代器的便利：
所有集合 → 统一方式：iterator.hasNext() + iterator.next()
```

**🔸 安全的遍历**
- **并发修改检测**：如果在遍历过程中有其他地方修改了集合，迭代器会立即发现并报错
- **内存安全**：不会访问到不存在的元素

---

## 2. 🗄️ 数据库结果集中的迭代器应用


### 2.1 ResultSet就是迭代器模式


**通俗理解**：当你查询数据库时，结果可能有成千上万条记录。ResultSet就像是一个"智能书签"，帮你一条一条地读取结果，而不是一次性把所有数据都加载到内存中。

```
数据库查询结果：
┌─────────────────────────────────┐
│ ID │  姓名  │   年龄  │   城市  │
├─────────────────────────────────┤
│ 1  │  张三  │   25   │   北京  │  ← ResultSet指针位置
│ 2  │  李四  │   30   │   上海  │
│ 3  │  王五  │   28   │   广州  │
│ 4  │  赵六  │   35   │   深圳  │
│ ...│  ...   │   ...  │   ...   │
└─────────────────────────────────┘
```

### 2.2 ResultSet的迭代器特性


**核心方法对比**：

| ResultSet方法 | **迭代器等价概念** | **实际作用** |
|--------------|------------------|-------------|
| `next()` | `hasNext() + next()` | `移动到下一行并返回是否成功` |
| `getString()` | `获取当前元素` | `从当前行获取指定列的数据` |
| `isAfterLast()` | `!hasNext()` | `检查是否已经超过最后一行` |

### 2.3 实际代码示例


**🟢基础用法**：

```java
// 执行SQL查询
String sql = "SELECT name, age FROM users WHERE age > 20";
ResultSet rs = statement.executeQuery(sql);

// 使用迭代器模式遍历结果
while (rs.next()) {  // 相当于 hasNext() && moveToNext()
    String name = rs.getString("name");
    int age = rs.getInt("age");
    System.out.println(name + " - " + age + "岁");
}
```

### 2.4 为什么数据库要用迭代器模式


**🔸 内存效率**
```
不用迭代器：查询100万条记录 → 一次性加载到内存 → 内存爆炸 💥
使用迭代器：查询100万条记录 → 每次只加载一条 → 内存稳定 ✅
```

**🔸 网络效率**
- 数据库可以分批发送数据，不用一次性传输所有结果
- 如果用户只看前10条记录，就不需要传输后面的99万条

---

## 3. 📁 文件系统遍历的迭代器实现


### 3.1 文件系统遍历的挑战


**通俗理解**：想象你要整理一个超级复杂的文件夹，里面有文件夹套文件夹，就像俄罗斯套娃一样。迭代器模式可以帮你有序地一个一个处理，不会漏掉任何文件。

```
文件系统结构：
📁 根目录
├── 📄 文件1.txt
├── 📁 子目录A
│   ├── 📄 文件2.txt
│   └── 📁 子目录A1
│       └── 📄 文件3.txt
└── 📁 子目录B
    ├── 📄 文件4.txt
    └── 📄 文件5.txt
```

### 3.2 自定义文件迭代器实现


**🟡进阶级** - 深度优先遍历的文件迭代器：

```java
public class FileSystemIterator implements Iterator<File> {
    private Stack<File> fileStack;  // 用栈来实现深度优先
    
    public FileSystemIterator(File rootDir) {
        fileStack = new Stack<>();
        if (rootDir.exists()) {
            fileStack.push(rootDir);
        }
    }
    
    @Override
    public boolean hasNext() {
        return !fileStack.isEmpty();
    }
    
    @Override
    public File next() {
        if (!hasNext()) {
            throw new NoSuchElementException("没有更多文件了");
        }
        
        File current = fileStack.pop();
        
        // 如果是目录，把子文件和子目录加入栈中
        if (current.isDirectory()) {
            File[] children = current.listFiles();
            if (children != null) {
                // 倒序添加，保证正确的遍历顺序
                for (int i = children.length - 1; i >= 0; i--) {
                    fileStack.push(children[i]);
                }
            }
        }
        
        return current;
    }
}
```

### 3.3 使用文件迭代器


**实际应用场景**：

```java
// 遍历指定目录下的所有文件
File rootDir = new File("C:/MyDocuments");
FileSystemIterator iterator = new FileSystemIterator(rootDir);

while (iterator.hasNext()) {
    File file = iterator.next();
    if (file.isFile()) {
        System.out.println("找到文件：" + file.getName());
        // 可以在这里进行文件处理，比如计算大小、备份等
    } else {
        System.out.println("进入目录：" + file.getName());
    }
}
```

### 3.4 Java 8的现代化实现


**🔴高级** - 使用Stream API：

```java
// Java 8提供的现代化文件遍历
Path startPath = Paths.get("C:/MyDocuments");

Files.walk(startPath)  // 这里内部就使用了迭代器模式
     .filter(Files::isRegularFile)  // 只要文件，不要目录
     .filter(path -> path.toString().endsWith(".txt"))  // 只要txt文件
     .forEach(path -> System.out.println("找到txt文件：" + path));
```

---

## 4. 🌲 树形结构遍历的迭代器模式


### 4.1 树形结构遍历的复杂性


**通俗理解**：树形结构就像家族族谱，有爷爷、爸爸、儿子这样的层级关系。遍历树就是要按照某种规则拜访家族中的每一个人。

```
              爷爷(根节点)
             /           \
          爸爸A          爸爸B
         /    \         /    \
      儿子1   儿子2   儿子3   儿子4
```

### 4.2 三种经典遍历方式


**树的三种拜访顺序**：

| 遍历方式 | **拜访顺序** | **实际用途** |
|---------|-------------|-------------|
| **前序遍历** | `爷爷 → 爸爸A → 儿子1 → 儿子2 → 爸爸B → 儿子3 → 儿子4` | `复制目录结构` |
| **中序遍历** | `儿子1 → 爸爸A → 儿子2 → 爷爷 → 儿子3 → 爸爸B → 儿子4` | `二叉搜索树排序` |
| **后序遍历** | `儿子1 → 儿子2 → 爸爸A → 儿子3 → 儿子4 → 爸爸B → 爷爷` | `删除目录结构` |

### 4.3 前序遍历迭代器实现


**🟡进阶级** - 最常用的前序遍历：

```java
public class PreOrderIterator<T> implements Iterator<TreeNode<T>> {
    private Stack<TreeNode<T>> stack;
    
    public PreOrderIterator(TreeNode<T> root) {
        stack = new Stack<>();
        if (root != null) {
            stack.push(root);
        }
    }
    
    @Override
    public boolean hasNext() {
        return !stack.isEmpty();
    }
    
    @Override
    public TreeNode<T> next() {
        TreeNode<T> current = stack.pop();
        
        // 先压入右子树，再压入左子树（因为栈是后进先出）
        if (current.right != null) {
            stack.push(current.right);
        }
        if (current.left != null) {
            stack.push(current.left);
        }
        
        return current;
    }
}
```

### 4.4 实际应用场景


**📁 文件系统应用**：
```java
// 使用树迭代器遍历文件目录
TreeNode<String> fileTree = buildFileTree("C:/Projects");
PreOrderIterator<String> iterator = new PreOrderIterator<>(fileTree);

while (iterator.hasNext()) {
    TreeNode<String> node = iterator.next();
    System.out.println("处理：" + node.data);
    // 可以在这里执行复制、压缩等操作
}
```

**🎯 为什么用迭代器遍历树**：
- **内存控制**：不需要递归，避免栈溢出
- **可中断**：可以随时停止遍历
- **统一接口**：无论什么树结构，遍历方式都一样

---

## 5. 📦 自定义容器设计与迭代器


### 5.1 为什么要设计自定义容器


**实际场景**：假设你要管理一个班级的学生信息，需要按照学号、姓名、成绩等不同方式来遍历，普通的ArrayList无法满足这种复杂需求。

### 5.2 设计一个学生管理容器


**🟢基础设计** - 支持多种遍历方式的容器：

```java
public class StudentContainer implements Iterable<Student> {
    private List<Student> students;
    
    public StudentContainer() {
        this.students = new ArrayList<>();
    }
    
    public void addStudent(Student student) {
        students.add(student);
    }
    
    // 默认迭代器：按添加顺序遍历
    @Override
    public Iterator<Student> iterator() {
        return students.iterator();
    }
    
    // 按学号遍历
    public Iterator<Student> studentIdIterator() {
        List<Student> sortedList = new ArrayList<>(students);
        sortedList.sort((s1, s2) -> s1.getId().compareTo(s2.getId()));
        return sortedList.iterator();
    }
    
    // 按成绩遍历
    public Iterator<Student> scoreIterator() {
        List<Student> sortedList = new ArrayList<>(students);
        sortedList.sort((s1, s2) -> Double.compare(s2.getScore(), s1.getScore()));
        return sortedList.iterator();
    }
}
```

### 5.3 实际使用示例


```java
// 创建学生容器
StudentContainer container = new StudentContainer();
container.addStudent(new Student("001", "张三", 85.5));
container.addStudent(new Student("003", "李四", 92.0));
container.addStudent(new Student("002", "王五", 78.5));

// 按添加顺序遍历
System.out.println("📝 按添加顺序：");
for (Student s : container) {  // 使用默认迭代器
    System.out.println(s.getName() + " - " + s.getScore());
}

// 按学号遍历
System.out.println("🔢 按学号排序：");
Iterator<Student> idIter = container.studentIdIterator();
while (idIter.hasNext()) {
    Student s = idIter.next();
    System.out.println(s.getId() + " - " + s.getName());
}

// 按成绩遍历
System.out.println("🏆 按成绩排序：");
Iterator<Student> scoreIter = container.scoreIterator();
while (scoreIter.hasNext()) {
    Student s = scoreIter.next();
    System.out.println(s.getName() + " - " + s.getScore());
}
```

### 5.4 高级特性：延迟计算迭代器


**🔴高级** - 内存友好的大数据处理：

```java
public class LazyDataIterator implements Iterator<ProcessedData> {
    private List<RawData> rawDataList;
    private int currentIndex;
    
    public LazyDataIterator(List<RawData> rawDataList) {
        this.rawDataList = rawDataList;
        this.currentIndex = 0;
    }
    
    @Override
    public boolean hasNext() {
        return currentIndex < rawDataList.size();
    }
    
    @Override
    public ProcessedData next() {
        RawData raw = rawDataList.get(currentIndex++);
        // 只在需要时才进行数据处理（延迟计算）
        return processData(raw);  // 这个方法可能很耗时
    }
    
    private ProcessedData processData(RawData raw) {
        // 复杂的数据处理逻辑
        // 只有当用户真正需要这个数据时才执行
        return new ProcessedData(raw);
    }
}
```

### 5.5 自定义容器的最佳实践


**⚡ 性能优化建议**：

| 场景 | **推荐做法** | **原因** |
|------|-------------|---------|
| **小数据集** | `直接排序后返回迭代器` | `简单直接，性能影响小` |
| **大数据集** | `使用延迟计算` | `节省内存，按需处理` |
| **频繁遍历** | `缓存排序结果` | `避免重复排序开销` |
| **实时数据** | `动态迭代器` | `反映数据的最新状态` |

---

## 6. 📋 核心要点总结


### 6.1 迭代器模式的实际价值


**🔸 核心作用总结**
```
统一访问：不管什么容器，遍历方式都一样
内存安全：按需加载，不会内存溢出
代码简洁：复杂的遍历逻辑被封装起来
扩展性强：可以轻松添加新的遍历方式
```

### 6.2 五大应用场景对比


| 应用场景 | **核心优势** | **使用频率** | **难度等级** |
|---------|-------------|-------------|-------------|
| **Java集合** | `标准化，开箱即用` | `🔥🔥🔥🔥🔥` | `🟢简单` |
| **数据库结果** | `内存效率，处理大数据` | `🔥🔥🔥🔥` | `🟢简单` |
| **文件遍历** | `处理复杂目录结构` | `🔥🔥🔥` | `🟡中等` |
| **树形遍历** | `算法标准化` | `🔥🔥` | `🟡中等` |
| **自定义容器** | `业务定制化` | `🔥` | `🔴复杂` |

### 6.3 记忆要点


**💡 理解核心**：
- 迭代器就像是遥控器，帮你控制如何"换台"（遍历元素）
- 不同的容器可以用同样的遥控器操作
- 遥控器很智能，知道什么时候没有下一个台了

**🎯 应用原则**：
- **简单场景**：直接用Java提供的迭代器
- **复杂需求**：设计自定义迭代器
- **大数据量**：考虑延迟加载和内存优化
- **性能要求高**：缓存和预处理

**⚡ 最佳实践**：
1. 优先使用标准库提供的迭代器
2. 自定义迭代器要考虑线程安全
3. 大数据场景要实现延迟计算
4. 提供多种遍历方式满足不同需求

**核心记忆口诀**：
```
迭代器模式用处广，统一遍历不慌张
集合数据和文件，树形结构都能装
内存安全性能好，大数据处理不卡顿
自定义容器灵活用，业务需求全满足
```