---
title: 1、Vue 3 + Vite 完整开发实战
---
## 📚 目录

1. [项目创建与初始化](#1-项目创建与初始化)
2. [核心功能开发](#2-核心功能开发)
3. [路由与状态管理](#3-路由与状态管理)
4. [开发工具与优化](#4-开发工具与优化)
5. [核心要点总结](#5-核心要点总结)

---

## 1. 🚀 项目创建与初始化


### 1.1 Vue 3 + Vite 项目搭建


**🔸 什么是 Vue 3 + Vite 组合**
```
Vue 3：最新版本的 Vue 框架
- 更快的渲染速度
- 更好的 TypeScript 支持
- 全新的 Composition API

Vite：现代化的前端构建工具
- 秒级启动速度
- 闪电般的热更新
- 原生 ESM 支持

两者结合 = 极佳的开发体验！
```

**💻 三种项目创建方式**

#### 方式一：使用 create-vite（推荐）

```bash
# 创建项目
npm create vite@latest my-vue-app -- --template vue

# 进入项目目录
cd my-vue-app

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

#### 方式二：使用 Vue 官方 CLI

```bash
# 创建项目（Vue CLI 4.5+）
vue create my-vue-app

# 选择 Vue 3 + Vite 配置
```

#### 方式三：手动创建

```bash
# 创建目录
mkdir my-vue-app && cd my-vue-app

# 初始化 package.json
npm init -y

# 安装核心依赖
npm install vue@next
npm install -D vite @vitejs/plugin-vue
```

> 💡 **新手建议**：推荐使用方式一，最简单直接！

### 1.2 项目结构解析


**🏗️ 标准项目结构**
```
my-vue-app/
├── public/           # 静态资源目录
│   └── favicon.ico   # 网站图标
├── src/              # 源码目录
│   ├── components/   # 组件目录
│   ├── views/        # 页面目录
│   ├── router/       # 路由配置
│   ├── store/        # 状态管理
│   ├── assets/       # 资源文件
│   ├── App.vue       # 根组件
│   └── main.js       # 入口文件
├── package.json      # 项目配置
└── vite.config.js    # Vite 配置文件
```

### 1.3 核心配置文件


**📄 vite.config.js 基础配置**
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()], // 启用 Vue 3 支持
  server: {
    port: 3000,       // 开发服务器端口
    open: true        // 自动打开浏览器
  }
})
```

> 📖 **概念解释**：`@vitejs/plugin-vue` 是让 Vite 能够理解和处理 `.vue` 文件的插件

---

## 2. 🛠️ 核心功能开发


### 2.1 单文件组件（SFC）详解


**🔸 什么是单文件组件**

单文件组件（Single File Component，简称 SFC）就是把一个组件的**模板**、**逻辑**、**样式**都写在一个 `.vue` 文件里。

```
想象一下：
传统开发 = 分别写 HTML + JS + CSS 三个文件
SFC = 一个 .vue 文件包含所有内容，更整洁！
```

**📝 SFC 基本结构**
```vue
<template>
  <!-- HTML 模板部分 -->
  <div class="hello">
    <h1>{{ message }}</h1>
    <button @click="changeMessage">点击我</button>
  </div>
</template>

<script>
// JavaScript 逻辑部分
export default {
  name: 'HelloWorld',
  data() {
    return {
      message: '你好，Vue 3!'
    }
  },
  methods: {
    changeMessage() {
      this.message = '消息已改变！'
    }
  }
}
</script>

<style scoped>
/* CSS 样式部分 */
.hello {
  color: #2c3e50;
  text-align: center;
}
</style>
```

> 💡 **理解要点**：
> - `<template>` = 页面长什么样
> - `<script>` = 页面怎么工作  
> - `<style>` = 页面怎么美化

### 2.2 Script Setup 语法糖


**🔸 什么是 Script Setup**

`<script setup>` 是 Vue 3 的新语法，让代码写起来更简洁。

```
传统写法 = 需要 export default、return 等繁琐语法
Script Setup = 直接写逻辑，自动处理导出
```

**⚡ 传统写法 vs Script Setup 对比**

```vue
<!-- 传统写法 -->
<script>
import { ref } from 'vue'

export default {
  setup() {
    const count = ref(0)
    const increment = () => {
      count.value++
    }
    
    return {
      count,
      increment
    }
  }
}
</script>
```

```vue
<!-- Script Setup 写法 -->
<script setup>
import { ref } from 'vue'

const count = ref(0)
const increment = () => {
  count.value++
}
</script>
```

> 🚀 **优势明显**：代码量减少 50%，逻辑更清晰！

### 2.3 Composition API 核心概念


**🔸 什么是 Composition API**

Composition API 是 Vue 3 的新编程方式，让组件逻辑更好组织。

```
Options API（旧方式）：
data() { ... }     // 数据放这里
methods: { ... }   // 方法放这里
computed: { ... }  // 计算属性放这里

Composition API（新方式）：
所有相关逻辑可以写在一起，更容易维护！
```

**💻 响应式数据基础**

```vue
<template>
  <div>
    <p>计数: {{ count }}</p>
    <p>双倍: {{ doubleCount }}</p>
    <button @click="increment">+1</button>
  </div>
</template>

<script setup>
import { ref, computed } from 'vue'

// ref：创建响应式变量
const count = ref(0)

// computed：计算属性
const doubleCount = computed(() => count.value * 2)

// 普通函数
const increment = () => {
  count.value++
}
</script>
```

> 📖 **核心概念**：
> - `ref()` = 创建一个响应式的变量
> - `computed()` = 根据其他数据自动计算的值
> - `.value` = 在 JS 中访问 ref 的值需要 .value

### 2.4 组件热更新机制


**🔸 什么是热更新**

热更新（Hot Module Replacement，HMR）就是修改代码后，页面**不刷新**但内容**自动更新**。

```
传统开发流程：
修改代码 → 保存 → 浏览器刷新 → 重新加载整个页面

Vite 热更新：
修改代码 → 保存 → 页面瞬间更新（不刷新）
```

**⚡ 热更新配置**
```javascript
// vite.config.js
export default defineConfig({
  plugins: [vue()],
  server: {
    hmr: {
      port: 24678  // 热更新端口
    }
  }
})
```

> ✅ **开发体验**：修改组件样式、逻辑，保存后立即看到效果，不丢失页面状态！

---

## 3. 🛣️ 路由与状态管理


### 3.1 Vue Router 路由系统


**🔸 什么是路由**

路由就是**不同网址对应不同页面**的机制。

```
传统网站：
/home     → 首页 HTML
/about    → 关于页面 HTML
/contact  → 联系页面 HTML

Vue 单页应用：
/home     → 首页组件
/about    → 关于组件  
/contact  → 联系组件
```

**📦 安装与基础配置**

```bash
npm install vue-router@4
```

```javascript
// router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import Home from '../views/Home.vue'
import About from '../views/About.vue'

const routes = [
  {
    path: '/',
    name: 'Home',
    component: Home
  },
  {
    path: '/about',
    name: 'About',
    component: About
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

```javascript
// main.js
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

createApp(App).use(router).mount('#app')
```

**🔗 路由使用**
```vue
<template>
  <div>
    <!-- 路由导航 -->
    <router-link to="/">首页</router-link>
    <router-link to="/about">关于</router-link>
    
    <!-- 路由内容显示区域 -->
    <router-view />
  </div>
</template>
```

> 💡 **理解要点**：
> - `router-link` = 导航链接（相当于 `<a>` 标签）
> - `router-view` = 页面内容显示区域

### 3.2 Pinia 状态管理


**🔸 什么是状态管理**

状态管理就是**在多个组件之间共享数据**的机制。

```
没有状态管理的问题：
组件A有数据 → 想传给组件C → 需要通过组件B → 很麻烦

有状态管理的好处：
所有组件都可以直接访问共享的数据 → 简单！
```

**📦 Pinia 基础使用**

```bash
npm install pinia
```

```javascript
// main.js
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'

const app = createApp(App)
app.use(createPinia())
app.mount('#app')
```

```javascript
// stores/counter.js
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useCounterStore = defineStore('counter', () => {
  // 状态（数据）
  const count = ref(0)
  
  // 计算属性
  const doubleCount = computed(() => count.value * 2)
  
  // 方法（操作数据）
  const increment = () => {
    count.value++
  }
  
  return {
    count,
    doubleCount,
    increment
  }
})
```

```vue
<!-- 在组件中使用 -->
<template>
  <div>
    <p>计数: {{ counter.count }}</p>
    <p>双倍: {{ counter.doubleCount }}</p>
    <button @click="counter.increment">+1</button>
  </div>
</template>

<script setup>
import { useCounterStore } from '@/stores/counter'

const counter = useCounterStore()
</script>
```

> 🚀 **Pinia 优势**：比 Vuex 更简单，写法更接近 Composition API

### 3.3 路由与状态管理集成


**🔄 数据流示意图**
```
用户操作 → 组件 → Pinia Store → 数据更新 → 组件重新渲染
    ↑                                            ↓
    ←──── 路由跳转 ←──── 业务逻辑 ←──── 组件响应 ←────
```

**💻 实际集成示例**
```javascript
// stores/user.js
export const useUserStore = defineStore('user', () => {
  const userInfo = ref(null)
  const isLoggedIn = computed(() => !!userInfo.value)
  
  const login = async (credentials) => {
    // 登录逻辑
    userInfo.value = await loginAPI(credentials)
    return userInfo.value
  }
  
  const logout = () => {
    userInfo.value = null
  }
  
  return { userInfo, isLoggedIn, login, logout }
})
```

```vue
<!-- Login.vue -->
<script setup>
import { useUserStore } from '@/stores/user'
import { useRouter } from 'vue-router'

const userStore = useUserStore()
const router = useRouter()

const handleLogin = async () => {
  await userStore.login(credentials)
  if (userStore.isLoggedIn) {
    router.push('/dashboard')  // 登录成功跳转
  }
}
</script>
```

---

## 4. 🔧 开发工具与优化


### 4.1 组件库集成


**🔸 什么是组件库**

组件库就是**别人写好的 UI 组件集合**，我们可以直接使用，不用自己从零开始写。

```
没有组件库：
需要自己写按钮、表格、弹窗等 → 费时费力

有组件库：
import Button from 'element-plus' → 直接使用美观的按钮
```

**📦 Element Plus 集成示例**

```bash
npm install element-plus
```

```javascript
// main.js - 完整导入
import { createApp } from 'vue'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import App from './App.vue'

const app = createApp(App)
app.use(ElementPlus)
app.mount('#app')
```

**⚡ 按需导入（推荐）**
```bash
npm install -D unplugin-vue-components unplugin-auto-import
```

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
})
```

```vue
<!-- 直接使用，无需手动导入 -->
<template>
  <div>
    <el-button type="primary">主要按钮</el-button>
    <el-input v-model="input" placeholder="请输入内容"></el-input>
  </div>
</template>

<script setup>
import { ref } from 'vue'
const input = ref('')
</script>
```

> 💡 **按需导入好处**：只打包使用的组件，减小最终文件大小

### 4.2 Vue DevTools 调试


**🔸 什么是 Vue DevTools**

Vue DevTools 是浏览器插件，专门用来**调试 Vue 应用**。

```
没有调试工具：
console.log 满天飞 → 难以跟踪数据变化

有 Vue DevTools：
直观看到组件树、数据状态、路由信息 → 调试神器！
```

**🛠️ 安装与使用**
```bash
# Chrome 扩展商店搜索：Vue.js devtools
# 或者访问：https://chrome.google.com/webstore
```

**📊 主要功能面板**
```
┌─ Components ─────────────┐
│ 查看组件树和 props/data  │
├─ Timeline ──────────────┤  
│ 查看事件时间线          │
├─ Routes ────────────────┤
│ 查看路由信息            │
├─ Pinia ─────────────────┤
│ 查看状态管理数据        │
└─ Performance ───────────┘
│ 性能分析                │
```

> 🚀 **调试技巧**：开发时保持 DevTools 开启，实时查看数据变化

### 4.3 构建优化配置


**🔸 什么是构建优化**

构建优化就是让最终的项目文件**更小、加载更快**。

```
优化前：
打包文件 2MB → 加载慢 → 用户体验差

优化后：
打包文件 500KB → 加载快 → 用户体验好
```

**⚡ Vite 构建优化配置**
```javascript
// vite.config.js
export default defineConfig({
  plugins: [vue()],
  build: {
    // 代码分割
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'vue-router'],
          elementUI: ['element-plus']
        }
      }
    },
    // 压缩配置
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true  // 生产环境移除 console
      }
    }
  },
  // 路径别名
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  }
})
```

**📊 构建优化效果对比**

| 优化项目 | 优化前 | 优化后 | 提升 |
|---------|--------|--------|------|
| **首屏加载** | `2.5s` | `1.2s` | `52%` |
| **包体积** | `2.1MB` | `980KB` | `53%` |
| **构建速度** | `45s` | `18s` | `60%` |

> ✅ **最佳实践**：
> - 生产环境移除调试代码
> - 合理分割代码块
> - 使用 CDN 加载大型库

---

## 5. 📋 核心要点总结


### 5.1 必须掌握的核心概念


```
🔸 项目搭建：create-vite 快速创建，@vitejs/plugin-vue 核心插件
🔸 SFC 开发：template + script + style 三位一体
🔸 Script Setup：Vue 3 最简洁的语法糖
🔸 Composition API：ref、computed、响应式编程思维
🔸 路由系统：vue-router 单页应用导航
🔸 状态管理：Pinia 组件间数据共享
🔸 开发工具：组件库集成、DevTools 调试、构建优化
```

### 5.2 关键理解要点


**🔹 为什么选择 Vue 3 + Vite**
```
开发体验：
- 秒级启动：告别等待，即开即用
- 热更新：修改代码瞬间生效
- 现代语法：享受最新 JavaScript 特性

生产表现：
- 包体积更小：Tree-shaking 自动优化
- 运行更快：Vue 3 性能全面提升
- 维护性更好：Composition API 逻辑清晰
```

**🔹 单文件组件的核心价值**
```
组件化思维：
一个 .vue 文件 = 一个完整的功能模块
template（UI）+ script（逻辑）+ style（样式）

开发效率：
- 代码集中：相关代码在一个文件
- 作用域隔离：样式和逻辑不会冲突
- 热更新友好：修改立即生效
```

**🔹 状态管理的使用场景**
```
需要状态管理：
✅ 用户登录信息（多个页面都需要）
✅ 购物车数据（添加商品、结算页面）
✅ 主题设置（全局生效）

不需要状态管理：
❌ 表单输入数据（仅当前组件使用）
❌ 列表展开状态（局部 UI 状态）
❌ 临时提示信息（短暂显示）
```

### 5.3 实际开发流程


**🚀 标准开发工作流**
```
1. 项目初始化
   ├── create-vite 创建项目
   ├── 配置 vite.config.js
   └── 安装必要依赖

2. 基础架构搭建  
   ├── 配置路由（vue-router）
   ├── 配置状态管理（pinia）
   └── 集成组件库（element-plus）

3. 功能开发
   ├── 创建页面组件
   ├── 编写业务逻辑
   └── 状态管理集成

4. 开发调试
   ├── Vue DevTools 调试
   ├── 热更新测试
   └── 性能优化

5. 构建部署
   ├── 生产构建优化
   ├── 静态资源处理
   └── 部署配置
```

### 5.4 常见问题与解决


> ⚠️ **注意事项**：
> - **ref.value**：Composition API 中访问响应式数据需要 `.value`
> - **路由懒加载**：大型应用建议使用动态导入组件
> - **样式作用域**：使用 `scoped` 避免样式污染
> - **环境变量**：使用 `.env` 文件管理不同环境配置

**💡 最佳实践建议**：
```
代码组织：
- 按功能模块划分目录结构
- 组件命名使用 PascalCase
- 文件命名使用 kebab-case

性能优化：
- 合理使用 computed 替代复杂计算
- 避免在模板中使用复杂表达式
- 大列表使用虚拟滚动

开发效率：
- 善用 Vue DevTools 调试
- 配置 ESLint + Prettier 代码规范
- 使用 TypeScript 增强类型安全
```

**核心记忆**：
- Vue 3 + Vite = 现代前端开发的最佳组合
- SFC + Composition API = 清晰的组件开发模式  
- Router + Pinia = 完整的应用状态管理
- 热更新 + DevTools = 极致的开发体验