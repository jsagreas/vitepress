---
title: 2、React 18 + Vite 完整开发实战
---
## 📚 目录

1. [项目创建与配置](#1-项目创建与配置)
2. [JSX 语法与开发](#2-jsx-语法与开发)
3. [路由与状态管理](#3-路由与状态管理)
4. [开发工具与优化](#4-开发工具与优化)
5. [生产环境优化](#5-生产环境优化)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🚀 项目创建与配置


### 1.1 什么是 React + Vite

**通俗理解**：React 是做网页的工具，Vite 是让开发更快的辅助工具

```
传统开发流程：
写代码 → 等待编译（很慢）→ 看效果 → 继续写代码

Vite + React 流程：
写代码 → 瞬间看效果（超快）→ 继续写代码

就像从老式电脑升级到高配电脑，开发体验大幅提升！
```

### 1.2 快速创建项目


**🔧 一键创建项目**
```bash
# 使用官方模板快速创建
npm create vite@latest my-react-app -- --template react

# 进入项目目录
cd my-react-app

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**💡 项目结构一览**
```
my-react-app/
├── src/                   ← 你的代码放这里
│   ├── App.jsx           ← 主组件
│   ├── main.jsx          ← 程序入口
│   └── index.css         ← 样式文件
├── public/               ← 静态资源
├── index.html            ← 页面模板
├── vite.config.js        ← Vite 配置文件
└── package.json          ← 项目信息
```

### 1.3 核心配置文件详解


**📝 vite.config.js 配置**
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],      // 让 Vite 支持 React
  server: {
    port: 3000,           // 开发服务器端口
    open: true            // 自动打开浏览器
  }
})
```

**🔸 @vitejs/plugin-react 作用**
- **JSX 转换**：把 JSX 语法转成浏览器能懂的 JavaScript
- **热更新**：代码改了立即在浏览器看到效果
- **错误提示**：代码有问题时友好地告诉你哪里错了

---

## 2. 📝 JSX 语法与开发


### 2.1 JSX 到底是什么


**通俗解释**：JSX 就是在 JavaScript 里写 HTML，让你用写网页的方式来写 React 组件

```jsx
// 传统方式：分开写 HTML 和 JavaScript
// HTML: <div id="app"></div>
// JS: document.getElementById('app').innerHTML = '<h1>Hello</h1>'

// JSX 方式：一起写，更直观
function App() {
  const name = "小明"
  return <h1>你好，{name}！</h1>    // 看起来像 HTML，实际是 JavaScript
}
```

### 2.2 JSX 语法核心要点


**🔸 基本规则**
```jsx
function MyComponent() {
  const user = { name: "张三", age: 25 }
  
  return (
    <div>
      {/* 这是注释，用花括号包 JavaScript 代码 */}
      <h1>欢迎 {user.name}</h1>                    {/* 显示变量 */}
      <p>年龄：{user.age > 18 ? "成年" : "未成年"}</p>  {/* 条件判断 */}
      <button onClick={() => alert("点击了")}>      {/* 事件处理 */}
        点我
      </button>
    </div>
  )
}
```

**⚠️ 重要注意事项**
- 组件名**必须大写开头**（`MyComponent`，不是 `myComponent`）
- **只能返回一个根元素**（用 `<div>` 或 `<>` 包裹）
- **属性用驼峰命名**（`onClick` 不是 `onclick`）

### 2.3 Fast Refresh 热更新


**💡 什么是 Fast Refresh**
简单说就是"改代码立即生效"，不用手动刷新浏览器

```jsx
// 体验 Fast Refresh
function Counter() {
  const [count, setCount] = useState(0)
  
  return (
    <div>
      <h1>计数：{count}</h1>
      <button onClick={() => setCount(count + 1)}>
        加 1
      </button>
      {/* 试试改这里的文字，保存后立即看到效果！ */}
    </div>
  )
}
```

**🚀 Fast Refresh 的好处**
- **状态保持**：改代码时，你点击的次数不会丢失
- **即时反馈**：打字同时就能看到效果
- **错误恢复**：修复错误后自动恢复正常

---

## 3. 🛣️ 路由与状态管理


### 3.1 React Router 路由配置


**🔸 什么是路由**
路由就是"不同网址显示不同页面"，比如：
- `/` 显示首页
- `/about` 显示关于页面
- `/user/123` 显示用户详情

**📦 安装和基本配置**
```bash
npm install react-router-dom
```

```jsx
// App.jsx - 路由配置
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom'

function App() {
  return (
    <BrowserRouter>
      {/* 导航菜单 */}
      <nav>
        <Link to="/">首页</Link> | 
        <Link to="/about">关于</Link> | 
        <Link to="/contact">联系</Link>
      </nav>
      
      {/* 路由匹配的页面内容 */}
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/about" element={<AboutPage />} />
        <Route path="/contact" element={<ContactPage />} />
      </Routes>
    </BrowserRouter>
  )
}

// 简单的页面组件
function HomePage() {
  return <h1>这是首页</h1>
}

function AboutPage() {
  return <h1>这是关于页面</h1>
}
```

### 3.2 状态管理基础


**🔸 什么是状态管理**
状态就是"数据"，状态管理就是"数据如何在不同组件间共享"

**💡 简单状态管理示例**
```jsx
import { useState } from 'react'

// 父组件管理状态
function App() {
  const [user, setUser] = useState({ name: "", isLogin: false })
  
  return (
    <div>
      <Header user={user} />                    {/* 传递给头部 */}
      <LoginForm onLogin={setUser} />           {/* 传递给登录表单 */}
    </div>
  )
}

// 头部组件显示用户信息
function Header({ user }) {
  return (
    <div>
      {user.isLogin ? `欢迎，${user.name}` : "请登录"}
    </div>
  )
}

// 登录表单组件
function LoginForm({ onLogin }) {
  const handleLogin = () => {
    onLogin({ name: "张三", isLogin: true })  // 更新父组件状态
  }
  
  return <button onClick={handleLogin}>登录</button>
}
```

### 3.3 状态管理方案选择


| 场景 | **推荐方案** | **特点** |
|------|-------------|----------|
| **小项目** | `useState + props` | `简单直接，够用就行` |
| **中型项目** | `Context API` | `React 内置，无需额外库` |
| **大型项目** | `Redux Toolkit` | `功能强大，生态完善` |
| **轻量需求** | `Zustand` | `简单易用，体积小` |

---

## 4. 🔧 开发工具与优化


### 4.1 UI 组件库集成


**🎨 为什么要用 UI 组件库**
就像装修房子用现成的家具，比自己做更快更好看

**📦 Ant Design 集成示例**
```bash
npm install antd
```

```jsx
import { Button, DatePicker, Space } from 'antd'
import 'antd/dist/reset.css'  // 引入样式

function App() {
  return (
    <div style={{ padding: 20 }}>
      <Space>
        <Button type="primary">主要按钮</Button>
        <Button>普通按钮</Button>
        <DatePicker placeholder="选择日期" />
      </Space>
    </div>
  )
}
```

**🔧 按需引入优化**
```javascript
// vite.config.js - 只打包用到的组件
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  css: {
    preprocessorOptions: {
      less: {
        modifyVars: {
          '@primary-color': '#1DA57A',  // 自定义主题色
        },
        javascriptEnabled: true,
      },
    },
  },
})
```

### 4.2 React DevTools 使用


**🔍 什么是 React DevTools**
就像浏览器的"开发者工具"，但专门为 React 设计

**💡 主要功能**
```
组件树查看：
└── App
    ├── Header
    │   └── UserInfo (props: {name: "张三"})
    ├── MainContent
    │   ├── ArticleList (state: {articles: [...]})
    │   └── Sidebar
    └── Footer

你可以：
✅ 查看每个组件的 props 和 state
✅ 修改数据立即看到效果
✅ 跟踪组件重新渲染的原因
✅ 检查性能问题
```

**🚀 实用技巧**
- **快速定位组件**：点击页面元素，在 DevTools 中高亮对应组件
- **状态修改测试**：直接在 DevTools 中修改 state 测试不同情况
- **性能分析**：查看哪些组件渲染次数过多

---

## 5. 🎯 生产环境优化


### 5.1 构建优化配置


**⚡ 打包命令和结果**
```bash
# 构建生产版本
npm run build

# 构建结果
dist/
├── index.html           ← 优化后的 HTML
├── assets/
│   ├── index-abc123.js  ← 压缩的 JavaScript（带哈希）
│   └── index-def456.css ← 压缩的 CSS（带哈希）
└── ...
```

**🔧 Vite 构建优化配置**
```javascript
// vite.config.js
export default defineConfig({
  plugins: [react()],
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          // 第三方库单独打包
          vendor: ['react', 'react-dom'],
          antd: ['antd']
        }
      }
    },
    // 生成 source map 便于调试
    sourcemap: true,
    // 压缩配置
    minify: 'esbuild'
  }
})
```

### 5.2 性能优化实践


**🚀 React 性能优化要点**

```jsx
// ✅ 使用 React.memo 避免不必要的重渲染
const UserCard = React.memo(function UserCard({ user }) {
  return (
    <div>
      <h3>{user.name}</h3>
      <p>{user.email}</p>
    </div>
  )
})

// ✅ 使用 useCallback 缓存函数
function TodoList() {
  const [todos, setTodos] = useState([])
  
  // 缓存函数，避免子组件重渲染
  const handleAddTodo = useCallback((text) => {
    setTodos(prev => [...prev, { id: Date.now(), text }])
  }, [])
  
  return (
    <div>
      <AddTodo onAdd={handleAddTodo} />
      {todos.map(todo => 
        <TodoItem key={todo.id} todo={todo} />
      )}
    </div>
  )
}
```

**📊 性能监控指标**

| 指标 | **目标值** | **优化方法** |
|------|-----------|-------------|
| **首屏加载时间** | `< 3秒` | `代码分割、资源压缩` |
| **包体积大小** | `< 500KB` | `按需引入、Tree Shaking` |
| **页面切换速度** | `< 200ms` | `懒加载、缓存优化` |

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 项目搭建：Vite + React 快速创建和配置
🔸 JSX 语法：在 JavaScript 中写 HTML 的语法糖
🔸 路由管理：React Router 实现页面跳转
🔸 状态管理：组件间数据共享的解决方案
🔸 开发工具：DevTools 和热更新提升开发效率
🔸 生产优化：构建配置和性能优化实践
```

### 6.2 关键理解要点


**🔹 为什么选择 Vite + React**
```
传统工具（webpack）：
启动慢（30s+）→ 修改代码 → 等待编译（5s+）→ 看效果

Vite + React：
启动快（<3s）→ 修改代码 → 瞬间看效果（<100ms）

开发体验提升 10 倍以上！
```

**🔹 JSX 的本质理解**
```
JSX 语法：
<h1>Hello {name}</h1>

编译后的 JavaScript：
React.createElement('h1', null, 'Hello ', name)

JSX 只是让代码更好写更好读的语法糖
```

**🔹 状态管理的选择原则**
```
项目规模决定方案：
小项目：useState + props 传递
中项目：Context API 全局状态
大项目：Redux/Zustand 专业状态管理

记住：够用就好，不要过度设计
```

### 6.3 实际开发建议


**🎯 新手开发流程**
```
第一步：用官方模板创建项目
第二步：写几个简单组件熟悉 JSX
第三步：添加路由实现页面跳转
第四步：集成 UI 组件库快速开发
第五步：使用 DevTools 调试和优化
```

**⚠️ 常见问题避免**
- **组件名首字母大写**：`function MyComponent()` ✅，`function myComponent()` ❌
- **JSX 返回单一根元素**：用 `<div>` 或 `<>` 包裹多个元素
- **事件处理函数**：`onClick={handleClick}` ✅，`onClick={handleClick()}` ❌
- **状态更新**：使用 `setState` 而不是直接修改 state

**🚀 进阶学习路径**
1. **掌握 Hooks**：useState、useEffect、useContext
2. **深入路由**：嵌套路由、路由守卫、参数传递
3. **状态管理**：Redux Toolkit、Zustand 选择一个深入
4. **性能优化**：懒加载、代码分割、缓存策略
5. **测试开发**：Jest + React Testing Library

**核心记忆**：
- Vite 让 React 开发体验飞起来
- JSX 是写组件的便捷语法
- 路由管理页面跳转，状态管理数据共享
- 工具和优化让开发更高效
- 从简单开始，逐步进阶