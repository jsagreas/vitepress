---
title: 1、生产构建配置与策略
---
## 📚 目录

1. [什么是生产构建配置](#1-什么是生产构建配置)
2. [基础构建配置详解](#2-基础构建配置详解)
3. [代码分割配置策略](#3-代码分割配置策略)
4. [文件命名与版本控制](#4-文件命名与版本控制)
5. [高级构建配置](#5-高级构建配置)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 什么是生产构建配置


### 1.1 为什么需要生产构建


**简单理解**：就像装修房子一样，开发时可以杂乱无章，但交付给用户时必须整洁有序

```
开发环境：快速预览，可以乱一点
   ↓
生产环境：给用户用的，必须优化好
```

**构建的作用**：
- 🔸 **压缩代码**：去掉空格、注释，让文件更小
- 🔸 **合并文件**：把多个文件打包成几个，减少网络请求
- 🔸 **优化性能**：让网站加载更快，用户体验更好
- 🔸 **兼容处理**：让老浏览器也能正常使用

### 1.2 开发环境 vs 生产环境


```
开发环境（你写代码时）：
├── src/
│   ├── main.js
│   ├── App.vue
│   └── components/
│       ├── Header.vue
│       └── Footer.vue
└── 几十个小文件，方便调试

生产环境（给用户的）：
├── index.html
├── assets/
│   ├── index.123abc.js    ← 压缩合并后的文件
│   └── style.456def.css
└── 几个大文件，加载快
```

---

## 2. ⚙️ 基础构建配置详解


### 2.1 build 配置选项核心概念


**什么是 build 配置**：告诉 Vite 怎么把你的代码打包成最终的网站文件

```javascript
// vite.config.js
export default {
  build: {
    // 这里放各种构建规则
  }
}
```

### 2.2 构建目标配置


**target 是什么**：指定你的网站要支持哪些浏览器

```javascript
export default {
  build: {
    target: 'es2015',  // 支持较新浏览器，文件更小
    // target: 'es5'   // 支持老旧浏览器，文件更大
  }
}
```

**通俗解释**：
- `es2015`：只支持较新浏览器，代码更简洁高效
- `es5`：支持老浏览器，但代码会被转换得复杂一些

### 2.3 输出目录设置


**outDir 概念**：指定打包后的文件放在哪个文件夹

```javascript
export default {
  build: {
    outDir: 'dist',      // 默认放在 dist 文件夹
    // outDir: 'build'   // 也可以叫 build 文件夹
  }
}
```

**目录结构示例**：
```
项目根目录/
├── src/           ← 你写代码的地方
├── dist/          ← 构建后的文件
│   ├── index.html
│   └── assets/
└── vite.config.js
```

### 2.4 基础路径设置


**base 是什么**：告诉浏览器从哪个路径加载你的网站文件

```javascript
export default {
  base: '/',              // 网站根目录访问
  // base: '/my-app/'     // 子目录访问
}
```

**实际场景**：
- 根目录部署：`https://mysite.com/` → `base: '/`
- 子目录部署：`https://mysite.com/my-app/` → `base: '/my-app/'`

---

## 3. 📦 代码分割配置策略


### 3.1 什么是代码分割


**通俗理解**：就像搬家时把东西分类装箱，而不是全塞进一个大箱子

```
不分割（一个大文件）：
[所有代码] 1.5MB  ← 用户要下载整个文件才能看到页面

分割后（多个小文件）：
[主要代码] 300KB  ← 用户先看到主要内容
[第三方库] 800KB  ← 后台慢慢加载
[其他页面] 400KB  ← 需要时才加载
```

### 3.2 基础代码分割配置


```javascript
export default {
  build: {
    rollupOptions: {
      output: {
        // 手动指定哪些代码要分开打包
        manualChunks: {
          // 把 Vue 相关的库单独打包
          'vue-vendor': ['vue', 'vue-router'],
          // 把工具库单独打包
          'utils': ['lodash', 'axios']
        }
      }
    }
  }
}
```

### 3.3 智能分割策略


```javascript
export default {
  build: {
    rollupOptions: {
      output: {
        // 根据文件大小和使用频率自动分割
        manualChunks(id) {
          // 第三方库单独打包
          if (id.includes('node_modules')) {
            return 'vendor'
          }
          // 组件库单独打包
          if (id.includes('/components/')) {
            return 'components'
          }
        }
      }
    }
  }
}
```

### 3.4 分割效果对比


| 策略 | **文件数量** | **首屏加载** | **缓存效果** | **适用场景** |
|------|-------------|-------------|-------------|-------------|
| **不分割** | `1个大文件` | `慢，但简单` | `差，改一点全重新下载` | `小项目` |
| **基础分割** | `3-5个文件` | `中等` | `好，第三方库不常变` | `中型项目` |
| **精细分割** | `多个小文件` | `快，按需加载` | `很好，精确控制` | `大型项目` |

---

## 4. 🏷️ 文件命名与版本控制


### 4.1 哈希命名的作用


**为什么要哈希命名**：让浏览器知道文件有没有更新

```
没有哈希：
app.js  ← 浏览器不知道文件是否更新，可能用缓存

有哈希：
app.abc123.js  ← 文件内容变了，哈希就变，浏览器知道要重新下载
```

### 4.2 文件命名配置


```javascript
export default {
  build: {
    rollupOptions: {
      output: {
        // JS 文件命名规则
        entryFileNames: 'js/[name].[hash].js',
        // CSS 文件命名规则
        assetFileNames: (assetInfo) => {
          if (assetInfo.name.endsWith('.css')) {
            return 'css/[name].[hash].css'
          }
          return 'assets/[name].[hash].[ext]'
        }
      }
    }
  }
}
```

### 4.3 哈希类型说明


```javascript
// 不同的哈希方式
export default {
  build: {
    rollupOptions: {
      output: {
        // [hash] - 内容变了就变
        entryFileNames: 'js/[name].[hash].js',
        
        // [chunkhash] - 这个模块变了就变
        chunkFileNames: 'js/[name].[chunkhash].js'
      }
    }
  }
}
```

**选择建议**：
- **一般情况**：用 `[hash]` 就够了
- **大项目**：用 `[chunkhash]` 更精确

### 4.4 长期缓存策略


**核心思想**：让不变的文件一直被缓存，变化的文件立即更新

```
缓存策略设计：
第三方库（很少变）→ vendor.123abc.js  ← 哈希很稳定
业务代码（经常变）→ main.456def.js    ← 哈希经常变
样式文件（偶尔变）→ style.789ghi.css  ← 哈希偶尔变
```

---

## 5. 🔧 高级构建配置


### 5.1 Source Map 配置


**什么是 Source Map**：就像代码的"翻译对照表"，帮你调试压缩后的代码

```javascript
export default {
  build: {
    sourcemap: false,        // 生产环境一般关闭
    // sourcemap: true,      // 开发时可以开启
    // sourcemap: 'hidden'   // 生成但不暴露给用户
  }
}
```

**各种模式对比**：

| 模式 | **文件大小** | **调试能力** | **安全性** | **生产建议** |
|------|-------------|-------------|-----------|-------------|
| `false` | `最小` | `无` | `最安全` | `✅ 推荐` |
| `true` | `较大` | `完整` | `代码暴露` | `❌ 不建议` |
| `'hidden'` | `较大` | `有（需工具）` | `相对安全` | `🟡 可选` |

### 5.2 浏览器兼容性设置


**target 详细配置**：

```javascript
export default {
  build: {
    target: [
      'es2015',      // 支持现代浏览器
      'chrome58',    // Chrome 58+
      'firefox57'    // Firefox 57+
    ]
  }
}
```

**兼容性选择指南**：
```
目标用户群体分析：
现代用户（90%+）→ es2015    ← 文件小，性能好
广泛兼容（95%+）→ es5       ← 文件大，但兼容好
特定需求        → 自定义    ← 按实际情况
```

### 5.3 rollupOptions 核心配置


**external 配置**：告诉 Vite 哪些库不要打包进来

```javascript
export default {
  build: {
    rollupOptions: {
      // 这些库不打包，从外部引入
      external: ['vue'],
      output: {
        globals: {
          vue: 'Vue'  // 全局变量名
        }
      }
    }
  }
}
```

**使用场景**：
- 🔸 **CDN 优化**：大型库从 CDN 加载更快
- 🔸 **减小包体积**：避免重复打包
- 🔸 **版本统一**：多个项目共用同一个库版本

### 5.4 构建分析配置


```javascript
export default {
  build: {
    // 生成构建分析报告
    reportCompressedSize: true,
    
    // 设置 chunk 大小警告阈值
    chunkSizeWarningLimit: 500,  // 单位 KB
    
    rollupOptions: {
      output: {
        // 控制 chunk 拆分
        manualChunks: (id) => {
          if (id.includes('node_modules')) {
            // 大型库单独拆分
            if (id.includes('element-plus')) {
              return 'element-plus'
            }
            return 'vendor'
          }
        }
      }
    }
  }
}
```

### 5.5 完整的生产配置示例


```javascript
// vite.config.js - 生产优化配置
export default {
  base: '/',
  build: {
    outDir: 'dist',
    target: 'es2015',
    sourcemap: false,
    
    rollupOptions: {
      output: {
        // 文件命名策略
        entryFileNames: 'js/[name].[hash].js',
        chunkFileNames: 'js/[name].[hash].js',
        assetFileNames: (assetInfo) => {
          const info = assetInfo.name.split('.')
          const extType = info[info.length - 1]
          if (/\.(mp4|webm|ogg|mp3|wav|flac|aac)(\?.*)?$/i.test(assetInfo.name)) {
            return `media/[name].[hash].${extType}`
          }
          if (/\.(png|jpe?g|gif|svg)(\?.*)?$/i.test(assetInfo.name)) {
            return `img/[name].[hash].${extType}`
          }
          if (extType === 'css') {
            return `css/[name].[hash].${extType}`
          }
          return `assets/[name].[hash].${extType}`
        },
        
        // 智能代码分割
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router'],
          'ui-vendor': ['element-plus'],
          'utils-vendor': ['axios', 'lodash']
        }
      }
    },
    
    // 压缩配置
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,  // 删除 console.log
        drop_debugger: true  // 删除 debugger
      }
    }
  }
}
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的基本概念


```
🔸 生产构建：把开发代码转换成用户可用的优化版本
🔸 代码分割：把大文件拆成小文件，提高加载速度
🔸 哈希命名：给文件加版本号，解决缓存更新问题
🔸 Source Map：调试工具，生产环境一般关闭
🔸 兼容性配置：决定支持哪些浏览器版本
```

### 6.2 配置选择策略


**🔹 项目规模决定配置复杂度**
```
小项目（<50个组件）：
✅ 基础配置就够了
✅ 简单的代码分割
✅ 标准哈希命名

大项目（>200个组件）：
✅ 精细的代码分割
✅ 复杂的文件命名规则
✅ 详细的性能分析
```

**🔹 用户群体决定兼容性**
```
现代项目：target: 'es2015'  ← 文件小，性能好
企业项目：target: 'es5'     ← 兼容性好
移动端：target: 'es2017'    ← 平衡性能和兼容性
```

### 6.3 常见配置模板


**基础项目配置**：
```javascript
export default {
  build: {
    outDir: 'dist',
    target: 'es2015',
    sourcemap: false
  }
}
```

**中型项目配置**：
```javascript
export default {
  build: {
    outDir: 'dist',
    target: 'es2015',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'vue-router'],
          utils: ['axios', 'lodash']
        }
      }
    }
  }
}
```

### 6.4 性能优化要点


**🎯 优化目标排序**：
1. **首屏加载速度** - 最重要，影响用户第一印象
2. **缓存利用率** - 减少重复下载
3. **包体积大小** - 节省带宽和存储
4. **构建速度** - 提高开发效率

**⚡ 实用优化技巧**：
- 🔸 **第三方库分离**：vendor chunk 独立，利用缓存
- 🔸 **路由级分割**：页面级别的懒加载
- 🔸 **资源压缩**：图片、字体等静态资源优化
- 🔸 **无用代码删除**：Tree Shaking 和 Dead Code Elimination

**核心记忆**：
- 构建配置像装修，开发随意，生产精细
- 代码分割像分类，相关的放一起，不常变的单独放
- 哈希命名像版本号，内容变了名字就变
- 配置复杂度跟项目规模成正比，小项目别过度优化