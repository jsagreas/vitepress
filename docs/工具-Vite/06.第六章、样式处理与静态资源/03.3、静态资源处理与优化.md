---
title: 3、静态资源处理与优化
---
## 📚 目录

1. [资源目录理解](#1-资源目录理解)
2. [图片资源处理](#2-图片资源处理)  
3. [多媒体资源处理](#3-多媒体资源处理)
4. [资源路径与引用](#4-资源路径与引用)
5. [核心要点总结](#5-核心要点总结)

---

## 1. 📁 资源目录理解


### 1.1 两个资源目录的本质区别


**简单理解：**
- **`public/`目录** = 直接复制，不经过任何处理
- **`src/`目录** = 智能处理，可以压缩优化

```
项目结构示例：
my-project/
├── public/           ← 原样复制到网站根目录
│   ├── favicon.ico   
│   ├── logo.png      
│   └── data.json     
├── src/              ← 会被 Vite 分析处理  
│   ├── assets/       
│   │   ├── img/      
│   │   ├── fonts/    
│   │   └── icons/    
│   └── components/   
```

### 1.2 public 目录 - "傻瓜式"资源


**💡 什么是 public 目录？**
> 想象 public 目录就像一个"传送带"，里面的文件会原封不动地搬到最终网站里

**使用场景：**
```markdown
✅ **适合放在 public/** 
- favicon.ico（网站图标）
- robots.txt（搜索引擎规则）  
- 第三方库的静态文件
- 不需要处理的大文件

❌ **不适合放在 public/**
- 需要压缩的图片
- 需要版本控制的资源
- 组件中使用的图片
```

**访问方式：**
```html
<!-- public/logo.png 这样引用 -->
<img src="/logo.png" alt="logo">

<!-- 注意：路径开头的 / 表示网站根目录 -->
<link rel="icon" href="/favicon.ico">
```

### 1.3 src 目录 - "智能化"资源


**💡 什么是 src 目录资源？**
> src 目录的资源像"原材料"，Vite 会对它们进行加工处理

**Vite 的智能处理：**
```
原始文件: logo.png (100KB)
         ↓ Vite 处理
处理后: logo-abc123.png (60KB)  ← 自动压缩+添加版本号
```

**使用方式：**
```vue
<template>
  <!-- 需要 import 导入 -->
  <img :src="logoImg" alt="logo">
</template>

<script>
// 导入图片，Vite 会处理
import logoImg from '@/assets/img/logo.png'

export default {
  data() {
    return {
      logoImg  // 实际是处理后的路径
    }
  }
}
</script>
```

### 1.4 两者对比总结


| 对比项 | **public/ 目录** | **src/ 目录** |
|---------|-----------------|--------------|
| 🎯 **处理方式** | `直接复制，不处理` | `智能分析处理` |
| 📁 **引用方式** | `绝对路径 /file.png` | `import 导入` |
| ⚡ **性能优化** | `无优化` | `自动压缩、版本控制` |
| 🔧 **适用场景** | `静态文件、第三方资源` | `项目内部资源` |
| 📦 **构建结果** | `原样复制` | `优化后复制` |

> 💡 **记忆口诀：** public 是"原样搬家"，src 是"精装修后搬家"

---

## 2. 🖼️ 图片资源处理


### 2.1 图片导入基础用法


**在 Vue 组件中使用图片：**
```vue
<template>
  <div>
    <!-- 方法一：import 导入 -->
    <img :src="profileImg" alt="用户头像">
    
    <!-- 方法二：动态导入 -->
    <img :src="getImageUrl('avatar.jpg')" alt="头像">
  </div>
</template>

<script>
// 静态导入
import profileImg from '@/assets/img/profile.png'

export default {
  data() {
    return {
      profileImg
    }
  },
  methods: {
    // 动态导入图片
    getImageUrl(name) {
      return new URL(`../assets/img/${name}`, import.meta.url).href
    }
  }
}
</script>
```

### 2.2 小图片自动 Base64 转换


**💡 什么是 Base64 转换？**
> 把小图片直接"嵌入"到代码里，减少网络请求次数

**Vite 的自动处理：**
```javascript
// 小于 4KB 的图片会自动转换
import smallIcon from '@/assets/icons/star.png'  // 2KB
console.log(smallIcon)  
// 输出：data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA...

import bigImage from '@/assets/img/banner.png'    // 50KB  
console.log(bigImage)   
// 输出：/assets/banner-a1b2c3.png
```

**转换规则：**
```
图片大小 < 4KB  →  转为 Base64（嵌入代码）
图片大小 ≥ 4KB  →  保持文件形式（网络加载）
```

**自定义转换阈值：**
```javascript
// vite.config.js
export default {
  build: {
    assetsInlineLimit: 8192  // 改为 8KB
  }
}
```

### 2.3 图片格式优化建议


**📊 不同格式的特点：**

| 格式 | **适用场景** | **特点** | **建议用途** |
|------|-------------|----------|-------------|
| 🖼️ **PNG** | `透明背景图片` | `无损压缩，支持透明` | `Logo、图标` |
| 📷 **JPEG** | `照片、复杂图像` | `有损压缩，文件小` | `产品图、背景图` |
| 🎨 **WebP** | `现代浏览器` | `压缩率更高` | `新项目首选` |
| 📐 **SVG** | `矢量图形` | `无限缩放不失真` | `图标、简单图形` |

**优化实践：**
```vue
<template>
  <!-- 响应式图片，支持多格式 -->
  <picture>
    <source srcset="/img/banner.webp" type="image/webp">
    <source srcset="/img/banner.jpg" type="image/jpeg">
    <img src="/img/banner.jpg" alt="横幅">
  </picture>
</template>
```

---

## 3. 🎨 多媒体资源处理


### 3.1 字体文件导入配置


**字体文件的使用方式：**
```css
/* 方法一：在 CSS 中导入 */
@font-face {
  font-family: 'MyCustomFont';
  src: url('@/assets/fonts/custom-font.woff2') format('woff2'),
       url('@/assets/fonts/custom-font.woff') format('woff');
  font-weight: normal;
  font-style: normal;
}

.custom-text {
  font-family: 'MyCustomFont', Arial, sans-serif;
}
```

**在组件中使用：**
```vue
<template>
  <h1 class="custom-title">自定义字体标题</h1>
</template>

<style scoped>
.custom-title {
  font-family: 'MyCustomFont', '微软雅黑', sans-serif;
  font-size: 2rem;
}
</style>
```

**💡 字体格式选择：**
```markdown
推荐加载顺序：
1. WOFF2 (最佳压缩)
2. WOFF  (兼容性好) 
3. TTF   (备用方案)
```

### 3.2 SVG 图标使用最佳实践


**方法一：作为图片使用**
```vue
<template>
  <!-- 简单引用 -->
  <img :src="iconSvg" alt="图标">
</template>

<script>
import iconSvg from '@/assets/icons/heart.svg'

export default {
  data() {
    return { iconSvg }
  }
}
</script>
```

**方法二：内联 SVG（推荐）**
```vue
<template>
  <!-- 可以控制颜色、大小 -->
  <svg class="icon-heart">
    <use href="#icon-heart"></use>
  </svg>
</template>

<style>
.icon-heart {
  width: 24px;
  height: 24px;
  fill: #ff6b6b;  /* 可以改颜色 */
}
</style>
```

**方法三：SVG 组件化**
```vue
<!-- HeartIcon.vue -->
<template>
  <svg :width="size" :height="size" :fill="color" viewBox="0 0 24 24">
    <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5..."/>
  </svg>
</template>

<script>
export default {
  name: 'HeartIcon',
  props: {
    size: { type: Number, default: 24 },
    color: { type: String, default: '#000' }
  }
}
</script>
```

### 3.3 媒体文件（音视频）处理


**音频文件使用：**
```vue
<template>
  <audio controls>
    <source :src="audioFile" type="audio/mpeg">
    您的浏览器不支持音频播放
  </audio>
</template>

<script>
import audioFile from '@/assets/audio/background.mp3'

export default {
  data() {
    return { audioFile }
  }
}
</script>
```

**视频文件使用：**
```vue
<template>
  <video controls width="100%">
    <source :src="videoFile" type="video/mp4">
    您的浏览器不支持视频播放
  </video>
</template>

<script>
import videoFile from '@/assets/video/intro.mp4'

export default {
  data() {
    return { videoFile }
  }
}
</script>
```

---

## 4. 🔗 资源路径与引用


### 4.1 资源路径解析规则


**路径类型详解：**
```javascript
// 绝对路径：从网站根目录开始
'/logo.png'          → 指向 public/logo.png

// 相对路径：从当前文件开始  
'./logo.png'         → 当前目录的 logo.png
'../images/logo.png' → 上级目录的 images/logo.png

// 别名路径：使用配置的别名
'@/assets/logo.png'  → src/assets/logo.png
```

**路径解析流程：**
```
用户输入路径
     ↓
Vite 检查路径类型
     ↓
┌─────────────┬─────────────┐
│  以 / 开头   │  其他路径    │
│  (绝对路径)  │  (相对路径)  │
│     ↓       │     ↓       │
│ public 目录  │ src 目录    │
│  直接复制    │  智能处理    │
└─────────────┴─────────────┘
     ↓             ↓
   最终网站       优化后资源
```

### 4.2 不同资源的引入方式


**🖼️ 图片引入对比：**
```vue
<template>
  <!-- public 目录图片 -->
  <img src="/static-logo.png" alt="静态logo">
  
  <!-- src 目录图片 -->
  <img :src="dynamicLogo" alt="动态logo">
  
  <!-- CSS 背景图片 -->
  <div class="bg-image"></div>
</template>

<script>
import dynamicLogo from '@/assets/img/logo.png'

export default {
  data() {
    return { dynamicLogo }
  }
}
</script>

<style>
.bg-image {
  background-image: url('@/assets/img/background.jpg');
  background-size: cover;
}
</style>
```

**🎵 多媒体资源引入：**
```vue
<template>
  <div>
    <!-- 字体文件在 CSS 中引入 -->
    <h1 class="custom-font">标题</h1>
    
    <!-- 音频文件 -->
    <audio :src="bgMusic" controls></audio>
    
    <!-- SVG 图标 -->
    <img :src="starIcon" alt="星星">
  </div>
</template>

<script>
import bgMusic from '@/assets/audio/bg.mp3'
import starIcon from '@/assets/icons/star.svg'

export default {
  data() {
    return {
      bgMusic,
      starIcon
    }
  }
}
</script>

<style>
@import '@/assets/fonts/custom.css';

.custom-font {
  font-family: 'CustomFont', sans-serif;
}
</style>
```

### 4.3 构建后的资源路径处理


**💡 什么是构建后的路径？**
> 开发时是一个路径，构建后（打包）是另一个路径

**路径变化示例：**
```
开发环境：
http://localhost:3000/src/assets/img/logo.png

构建后：
https://mysite.com/assets/logo-a1b2c3.png  ← 自动添加版本号
```

**Vite 自动处理：**
```vue
<script>
// 开发时
import logo from '@/assets/img/logo.png'
console.log(logo)  // /src/assets/img/logo.png

// 构建后  
console.log(logo)  // /assets/logo-a1b2c3.png
</script>
```

**配置资源路径：**
```javascript
// vite.config.js
export default {
  base: '/my-app/',  // 设置基础路径
  build: {
    assetsDir: 'static',  // 资源文件夹名称
    rollupOptions: {
      output: {
        assetFileNames: 'assets/[name]-[hash][extname]'  // 文件命名规则
      }
    }
  }
}
```

**🔄 路径处理流程图：**
```
开发阶段资源引用
        ↓
   Vite 路径解析  
        ↓
┌───────────────┬───────────────┐
│   public/     │    src/       │
│   资源        │    资源       │
│      ↓        │       ↓       │
│  直接访问      │   import 导入  │
└───────────────┴───────────────┘
        ↓               ↓
        构建打包阶段
        ↓               ↓
┌───────────────┬───────────────┐
│   原样复制     │   处理+重命名  │
│      ↓        │       ↓       │
│ /logo.png     │ /assets/       │
│              │ logo-abc.png   │
└───────────────┴───────────────┘
```

---

## 5. 📋 核心要点总结


### 5.1 必须掌握的核心概念


```
🔸 目录区别：public/ 直接复制，src/ 智能处理
🔸 引用方式：public/ 用绝对路径，src/ 用 import
🔸 图片处理：小图自动转 Base64，大图保持文件
🔸 路径解析：开发和构建环境路径会变化
🔸 资源优化：Vite 自动压缩、添加版本号
```

### 5.2 关键理解要点


**🔹 为什么要区分两个目录？**
```
性能考虑：
- public/ : 快速访问，不需要处理的文件
- src/    : 优化处理，减小文件体积

开发效率：
- public/ : 第三方资源，不常变化
- src/    : 项目资源，经常修改
```

**🔹 什么时候用哪种方式？**
```
选择 public/ ：
✅ 网站图标 favicon.ico
✅ 机器人文件 robots.txt  
✅ 大的媒体文件
✅ 第三方静态资源

选择 src/ ：
✅ 组件中的图片
✅ 需要优化的资源
✅ 经常修改的文件
✅ 需要版本控制的资源
```

**🔹 Base64 转换的意义？**
```
优点：
- 减少 HTTP 请求次数
- 小图标加载更快

缺点：  
- 增加代码体积
- 不能单独缓存

Vite 智能选择：小图才转换
```

### 5.3 实际应用指南


**📁 推荐的资源组织结构：**
```
src/
├── assets/
│   ├── img/          ← 图片资源
│   │   ├── icons/    ← 小图标
│   │   ├── photos/   ← 照片
│   │   └── bg/       ← 背景图
│   ├── fonts/        ← 字体文件
│   ├── audio/        ← 音频文件
│   └── video/        ← 视频文件
└── components/

public/
├── favicon.ico       ← 网站图标
├── robots.txt        ← SEO文件
└── data/             ← 静态数据
    └── api.json
```

**🎯 最佳实践总结：**
- **小图标** → src/assets/icons/ → 自动 Base64
- **产品图** → src/assets/img/ → 压缩优化  
- **字体文件** → src/assets/fonts/ → CSS 引入
- **媒体文件** → 根据大小选择目录
- **第三方资源** → public/ → 原样使用

**核心记忆：**
- public 是"直通车"，src 是"加工厂"
- 小文件嵌入代码，大文件保持独立
- 开发用相对路径，构建后自动优化
- 合理组织资源，提升加载性能