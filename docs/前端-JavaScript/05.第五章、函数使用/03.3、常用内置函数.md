---
title: 3、常用内置函数
---
## 📚 目录

1. [数字转换函数](#1-数字转换函数)
2. [Math数学计算](#2-Math数学计算)
3. [Date日期对象](#3-Date日期对象)
4. [字符串处理方法](#4-字符串处理方法)
5. [核心要点总结](#5-核心要点总结)

---

## 1. 🔢 数字转换函数


### 1.1 parseInt() - 转换为整数


**🔸 核心概念**
```
parseInt()：把字符串转换成整数
作用：从字符串中提取整数部分，遇到非数字就停止
返回：整数或NaN（转换失败时）
```

**💡 基本用法**
```javascript
// 基础转换
parseInt("123")        // 123
parseInt("123.45")     // 123 (只取整数部分)
parseInt("123abc")     // 123 (遇到字母停止)
parseInt("abc123")     // NaN (开头不是数字)

// 指定进制（常用）
parseInt("10", 10)     // 10 (十进制)
parseInt("10", 2)      // 2  (二进制)
parseInt("ff", 16)     // 255 (十六进制)
```

### 1.2 parseFloat() - 转换为小数


**🔸 核心概念**
```
parseFloat()：把字符串转换成浮点数
作用：提取字符串中的小数，包含小数点
返回：浮点数或NaN
```

**💡 基本用法**
```javascript
parseFloat("123.45")      // 123.45
parseFloat("123.45abc")   // 123.45 (遇到非数字停止)
parseFloat("123")         // 123
parseFloat("abc")         // NaN
```

### 1.3 Number() - 严格转换


**🔸 对比三种转换方式**
```javascript
let str = "123.45abc";

parseInt(str)      // 123   (取整数部分)
parseFloat(str)    // 123.45 (取小数部分)
Number(str)        // NaN   (严格转换，有非数字就失败)

// 实际使用场景
let userAge = parseInt(prompt("请输入年龄"));     // 用户输入转年龄
let price = parseFloat("19.99");                 // 价格字符串转小数
let score = Number("85");                        // 严格数字转换
```

---

## 2. 🧮 Math数学计算


### 2.1 四舍五入函数组


**🔸 核心概念**
```
Math.round()：标准四舍五入
Math.floor()：向下取整（地板）
Math.ceil()：向上取整（天花板）
```

**💡 三种取整对比**
```javascript
let num = 4.7;

Math.round(num)    // 5   (四舍五入)
Math.floor(num)    // 4   (向下取整)
Math.ceil(num)     // 5   (向上取整)

let num2 = 4.3;
Math.round(num2)   // 4   (四舍五入)
Math.floor(num2)   // 4   (向下取整)  
Math.ceil(num2)    // 5   (向上取整)
```

**🎯 实际应用场景**
```javascript
// 计算页数（向上取整）
let totalItems = 23;
let pageSize = 10;
let totalPages = Math.ceil(totalItems / pageSize);  // 3页

// 商品评分显示（四舍五入）
let rating = 4.67;
let displayRating = Math.round(rating * 10) / 10;   // 4.7

// 倒计时秒数（向下取整）
let timeLeft = 59.8;
let seconds = Math.floor(timeLeft);                 // 59秒
```

### 2.2 Math.random() - 随机数生成


**🔸 核心概念**
```
Math.random()：生成0到1之间的随机小数（不包括1）
返回：0 ≤ 结果 < 1
用途：抽奖、游戏、随机选择等
```

**💡 常用随机数公式**
```javascript
// 基础随机数
Math.random()              // 0.0 到 0.999...

// 0-9的随机整数
Math.floor(Math.random() * 10)

// 1-10的随机整数  
Math.floor(Math.random() * 10) + 1

// 通用公式：min到max的随机整数
function randomInt(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

randomInt(5, 15)           // 5到15的随机整数
```

**🎯 实际应用**
```javascript
// 随机选择数组元素
let colors = ["red", "blue", "green", "yellow"];
let randomColor = colors[Math.floor(Math.random() * colors.length)];

// 生成随机ID
let randomId = Math.floor(Math.random() * 1000000);

// 抽奖概率
let isWinner = Math.random() < 0.1;  // 10%中奖率
```

### 2.3 其他常用Math方法


```javascript
// 最大值和最小值
Math.max(1, 3, 2)          // 3
Math.min(1, 3, 2)          // 1

// 绝对值
Math.abs(-5)               // 5

// 平方根
Math.sqrt(16)              // 4

// 幂运算
Math.pow(2, 3)             // 8 (2的3次方)
```

---

## 3. 📅 Date日期对象


### 3.1 创建日期对象


**🔸 核心概念**
```
Date对象：处理日期和时间
创建方式：new Date()
用途：显示时间、计算时差、格式化日期
```

**💡 创建Date的方法**
```javascript
// 当前时间
let now = new Date();                    // 现在的日期时间

// 指定日期
let birthday = new Date("2000-01-01");   // 2000年1月1日
let meeting = new Date(2024, 0, 15);     // 2024年1月15日(月份从0开始)

console.log(now);                        // Mon Jul 28 2025 16:30:00...
```

### 3.2 获取日期信息


**📊 常用获取方法**
```javascript
let date = new Date();

// 年月日
date.getFullYear()     // 2025 (完整年份)
date.getMonth()        // 6    (月份0-11，6表示7月)
date.getDate()         // 28   (日期1-31)

// 时分秒
date.getHours()        // 16   (小时0-23)
date.getMinutes()      // 30   (分钟0-59)
date.getSeconds()      // 0    (秒数0-59)

// 星期
date.getDay()          // 1    (星期0-6，0是周日)
```

### 3.3 实用日期操作


**🎯 格式化显示**
```javascript
let now = new Date();

// 简单格式化
function formatDate(date) {
    let year = date.getFullYear();
    let month = date.getMonth() + 1;  // +1因为月份从0开始
    let day = date.getDate();
    
    return `${year}-${month}-${day}`;
}

console.log(formatDate(now));  // "2025-7-28"

// 补零版本
function formatDateWithZero(date) {
    let year = date.getFullYear();
    let month = String(date.getMonth() + 1).padStart(2, '0');
    let day = String(date.getDate()).padStart(2, '0');
    
    return `${year}-${month}-${day}`;
}

console.log(formatDateWithZero(now));  // "2025-07-28"
```

**⏰ 时间计算**
```javascript
// 几天后的日期
let today = new Date();
let threeDaysLater = new Date(today.getTime() + 3 * 24 * 60 * 60 * 1000);

// 时间差计算
let startTime = new Date("2025-01-01");
let endTime = new Date("2025-12-31");
let diffDays = (endTime - startTime) / (1000 * 60 * 60 * 24);
console.log(diffDays);  // 相差多少天
```

---

## 4. 📝 字符串处理方法


### 4.1 查找和截取


**🔸 indexOf() - 查找位置**
```javascript
let text = "Hello World";

text.indexOf("o")        // 4    (第一个o的位置)
text.indexOf("o", 5)     // 7    (从位置5开始找o)
text.indexOf("xyz")      // -1   (没找到返回-1)

// 实际应用：检查是否包含
let email = "user@gmail.com";
if (email.indexOf("@") !== -1) {
    console.log("这是有效邮箱格式");
}
```

**🔸 substring() - 截取字符串**
```javascript
let text = "JavaScript";

text.substring(0, 4)     // "Java" (从0到4，不包括4)
text.substring(4)        // "Script" (从4到结尾)
text.substring(4, 8)     // "Scri" (从4到8)

// 实际应用：提取文件扩展名
let filename = "photo.jpg";
let dotIndex = filename.indexOf(".");
let extension = filename.substring(dotIndex + 1);  // "jpg"
```

### 4.2 大小写转换


**🔸 核心概念**
```
toLowerCase()：转换为小写
toUpperCase()：转换为大写
用途：统一格式、不区分大小写比较
```

**💡 基本用法**
```javascript
let name = "JavaScript";

name.toLowerCase()       // "javascript"
name.toUpperCase()       // "JAVASCRIPT"

// 实际应用：不区分大小写搜索
function searchIgnoreCase(text, keyword) {
    return text.toLowerCase().indexOf(keyword.toLowerCase()) !== -1;
}

searchIgnoreCase("Hello World", "WORLD");  // true
```

### 4.3 去除空格和分割


**🔸 trim() - 去除首尾空格**
```javascript
let userInput = "  hello world  ";

userInput.trim()         // "hello world" (去除首尾空格)

// 实际应用：处理用户输入
function validateInput(input) {
    let cleanInput = input.trim();
    if (cleanInput === "") {
        return "输入不能为空";
    }
    return cleanInput;
}
```

**🔸 split() - 分割字符串**
```javascript
let fruits = "apple,banana,orange";

fruits.split(",")        // ["apple", "banana", "orange"]
fruits.split("")         // ["a","p","p","l","e",",","b"...] (按字符分割)

// 实际应用：处理CSV数据
let csvLine = "张三,25,程序员";
let [name, age, job] = csvLine.split(",");
console.log(name);       // "张三"
```

### 4.4 字符串模板（ES6补充）


**🔸 模板字符串**
```javascript
// 传统拼接
let name = "小明";
let age = 18;
let msg1 = "我叫" + name + "，今年" + age + "岁";

// 模板字符串（推荐）
let msg2 = `我叫${name}，今年${age}岁`;

// 多行文本
let html = `
    <div>
        <h1>${name}</h1>
        <p>年龄：${age}</p>
    </div>
`;
```

---

## 5. 📋 核心要点总结


### 5.1 必须掌握的函数分类


```
🔸 数字转换：parseInt()、parseFloat()、Number()
🔸 数学计算：Math.round()、Math.floor()、Math.ceil()、Math.random()
🔸 日期处理：new Date()、getFullYear()、getMonth()、getDate()
🔸 字符串操作：indexOf()、substring()、toLowerCase()、trim()、split()
```

### 5.2 使用场景记忆法


**🔹 数字处理场景**
```
用户输入年龄 → parseInt()
价格计算 → parseFloat()
页数计算 → Math.ceil()
评分显示 → Math.round()
抽奖系统 → Math.random()
```

**🔹 字符串处理场景**
```
邮箱验证 → indexOf("@")
文件扩展名 → substring()
搜索功能 → toLowerCase()
表单验证 → trim()
CSV处理 → split(",")
```

**🔹 日期处理场景**
```
显示当前时间 → new Date()
格式化日期 → getFullYear()、getMonth()、getDate()
计算天数差 → 时间戳相减
```

### 5.3 常见错误与注意事项


**⚠️ 容易出错的地方**
```javascript
// 1. 月份从0开始
let date = new Date(2024, 0, 1);  // 2024年1月1日，不是0月

// 2. parseInt要指定进制
parseInt("08", 10);  // 8，避免八进制问题

// 3. 数组索引可能为-1
let index = text.indexOf("x");
if (index !== -1) {  // 要检查是否找到
    // 找到了才处理
}

// 4. 字符串是不可变的
let str = "hello";
str.toUpperCase();   // 不会改变原字符串
str = str.toUpperCase();  // 要重新赋值
```

### 5.4 实际开发技巧


**💡 常用工具函数**
```javascript
// 随机整数
function random(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

// 格式化日期
function formatDate(date) {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');
    return `${year}-${month}-${day}`;
}

// 安全的数字转换
function toNumber(value, defaultValue = 0) {
    const num = parseFloat(value);
    return isNaN(num) ? defaultValue : num;
}
```

**核心记忆**：
- 内置函数是JavaScript提供的现成工具
- 数字转换要选对方法：parseInt取整、parseFloat取小数
- Math对象提供数学计算：round四舍五入、random生成随机数
- Date对象处理时间：注意月份从0开始
- 字符串方法解决文本处理：查找用indexOf、截取用substring