---
title: 1、函数的定义和调用
---
## 📚 目录

1. [函数的定义和调用](#1-函数的定义和调用)
2. [函数参数和作用域](#2-函数参数和作用域)
3. [this关键字详解](#3-this关键字详解)
4. [常用内置函数](#4-常用内置函数)
5. [核心要点总结](#5-核心要点总结)

---

## 1. 🔧 函数的定义和调用


### 1.1 函数是什么


**🔸 通俗理解**
```
函数就像一个"小工厂"：
• 输入：原材料（参数）
• 处理：工厂流程（函数体）
• 输出：产品（返回值）

就像榨汁机：放入水果 → 榨汁过程 → 得到果汁
```

**💡 为什么要用函数**
- **避免重复**：相同的代码写一次就够了
- **代码整洁**：复杂逻辑封装起来
- **方便调试**：问题定位更准确
- **团队协作**：功能模块化分工

### 1.2 函数声明（最常用）


**🔸 基本语法**
```javascript
function 函数名(参数1, 参数2) {
    // 函数体
    return 结果;
}
```

**💻 实际例子**
```javascript
// 计算两个数的和
function add(a, b) {
    return a + b;
}

// 调用函数
let result = add(5, 3);  // 结果是8
console.log(result);     // 输出：8
```

**📋 函数声明特点**
```
✅ 会被提升：可以在声明前调用
✅ 有名字：方便调试
✅ 最常用：推荐日常开发使用

sayHello();  // 可以正常调用

function sayHello() {
    console.log("Hello!");
}
```

### 1.3 函数表达式


**🔸 什么是函数表达式**
```javascript
// 把函数赋值给变量
let add = function(a, b) {
    return a + b;
};

// 调用方式一样
let result = add(10, 20);  // 结果是30
```

**⚡ 函数声明 vs 函数表达式**
```javascript
// 函数声明：会提升，可以先调用后声明
sayHi();  // 正常工作

function sayHi() {
    console.log("Hi!");
}

// 函数表达式：不会提升，必须先声明后调用
sayBye();  // ❌ 报错！

let sayBye = function() {
    console.log("Bye!");
};
```

### 1.4 箭头函数（现代写法）


**🔸 基本语法**
```javascript
// 完整写法
let add = (a, b) => {
    return a + b;
};

// 简写：只有一行时可以省略{}和return
let add = (a, b) => a + b;

// 只有一个参数时可以省略()
let double = x => x * 2;
```

**💻 实际对比**
```javascript
// 传统函数
function multiply(x, y) {
    return x * y;
}

// 箭头函数
let multiply = (x, y) => x * y;

// 效果一样
console.log(multiply(3, 4));  // 12
```

**📊 什么时候用箭头函数**
```
✅ 简单的计算逻辑
✅ 数组操作（map、filter等）
✅ 代码更简洁

❌ 需要this指向的情况（后面会讲）
❌ 复杂的函数逻辑
```

### 1.5 函数调用和返回值


**🔸 函数调用**
```javascript
function greet(name) {
    console.log("Hello, " + name);
}

// 调用函数
greet("张三");  // 输出：Hello, 张三
greet("李四");  // 输出：Hello, 李四
```

**🔸 返回值的作用**
```javascript
// 有返回值：函数有结果
function getAge() {
    return 25;
}
let age = getAge();  // age = 25

// 无返回值：函数只做事情
function showMessage() {
    alert("操作完成");
    // 没有return，默认返回undefined
}
let result = showMessage();  // result = undefined
```

**💡 return的特点**
```javascript
function checkAge(age) {
    if (age < 18) {
        return "未成年";  // 函数结束，后面代码不执行
    }
    return "成年人";
}

console.log(checkAge(16));  // "未成年"
console.log(checkAge(20));  // "成年人"
```

---

## 2. 📦 函数参数和作用域


### 2.1 参数传递


**🔸 基本参数**
```javascript
function introduce(name, age) {
    console.log(`我叫${name}，今年${age}岁`);
}

introduce("小明", 18);  // 我叫小明，今年18岁
```

**🔸 默认参数（ES6新特性）**
```javascript
function greet(name = "朋友") {
    console.log(`你好，${name}！`);
}

greet();        // 你好，朋友！
greet("小红");   // 你好，小红！
```

**🔸 剩余参数（处理不定数量参数）**
```javascript
function sum(...numbers) {
    let total = 0;
    for (let num of numbers) {
        total += num;
    }
    return total;
}

console.log(sum(1, 2, 3));       // 6
console.log(sum(1, 2, 3, 4, 5)); // 15
```

### 2.2 作用域（变量的生存范围）


**🔸 全局作用域**
```javascript
let globalVar = "我是全局变量";

function test() {
    console.log(globalVar);  // 可以访问
}

test();  // 输出：我是全局变量
```

**🔸 函数作用域**
```javascript
function test() {
    let localVar = "我是局部变量";
    console.log(localVar);  // 函数内部可以访问
}

test();  // 输出：我是局部变量
// console.log(localVar);  // ❌ 外部无法访问，会报错
```

**🔸 块级作用域（let和const）**
```javascript
function demo() {
    if (true) {
        let blockVar = "块级变量";
        var functionVar = "函数变量";
    }
    
    // console.log(blockVar);    // ❌ 无法访问
    console.log(functionVar);    // ✅ 可以访问
}
```

**💡 作用域链（变量查找规则）**
```
查找顺序：
1. 当前作用域
2. 上一级作用域  
3. 再上一级...
4. 全局作用域
5. 找不到就报错

像俄罗斯套娃一样，从里往外找
```

### 2.3 闭包（重要概念）


**🔸 什么是闭包**
```javascript
function outer() {
    let count = 0;
    
    function inner() {
        count++;
        console.log(count);
    }
    
    return inner;
}

let counter = outer();
counter();  // 1
counter();  // 2
counter();  // 3
```

**📋 闭包的作用**
```
✅ 数据私有化：外部无法直接访问count
✅ 状态保持：每次调用都记住上次的值
✅ 模块化：创建独立的功能模块
```

**💻 实用例子：计数器**
```javascript
function createCounter() {
    let count = 0;
    
    return {
        increment: () => ++count,
        decrement: () => --count,
        getCount: () => count
    };
}

let counter = createCounter();
console.log(counter.increment());  // 1
console.log(counter.increment());  // 2
console.log(counter.getCount());   // 2
```

---

## 3. 🎯 this关键字详解


### 3.1 this是什么


**🔸 通俗理解**
```
this就像是"我"这个代词：
• 在不同场合，"我"指向不同的人
• 在不同环境，this指向不同的对象

关键是：谁调用函数，this就指向谁
```

### 3.2 普通函数中的this


**🔸 全局环境**
```javascript
function showThis() {
    console.log(this);  // 浏览器中指向window对象
}

showThis();  // this = window（浏览器环境）
```

**🔸 对象方法中的this**
```javascript
let person = {
    name: "小明",
    age: 18,
    sayHello: function() {
        console.log(`我是${this.name}，今年${this.age}岁`);
    }
};

person.sayHello();  // this = person对象
// 输出：我是小明，今年18岁
```

**⚠️ this指向会变化**
```javascript
let person = {
    name: "小明",
    sayHello: function() {
        console.log(this.name);
    }
};

person.sayHello();      // "小明"（this = person）

let hello = person.sayHello;
hello();                // undefined（this = window）
```

### 3.3 箭头函数中的this


**🔸 箭头函数的特点**
```javascript
let person = {
    name: "小明",
    
    // 普通函数：this指向调用者
    sayHello: function() {
        console.log(this.name);  // "小明"
    },
    
    // 箭头函数：this继承外层
    sayBye: () => {
        console.log(this.name);  // undefined（this = window）
    }
};

person.sayHello();  // "小明"
person.sayBye();    // undefined
```

**💡 箭头函数的使用场景**
```javascript
let person = {
    name: "小明",
    hobbies: ["游戏", "音乐", "运动"],
    
    showHobbies: function() {
        // 使用箭头函数，this保持指向person
        this.hobbies.forEach(hobby => {
            console.log(`${this.name}喜欢${hobby}`);
        });
    }
};

person.showHobbies();
// 小明喜欢游戏
// 小明喜欢音乐  
// 小明喜欢运动
```

### 3.4 改变this指向


**🔸 call方法**
```javascript
function greet() {
    console.log(`Hello, ${this.name}`);
}

let person1 = { name: "小明" };
let person2 = { name: "小红" };

greet.call(person1);  // Hello, 小明
greet.call(person2);  // Hello, 小红
```

**🔸 apply方法**
```javascript
function introduce(age, city) {
    console.log(`我是${this.name}，${age}岁，住在${city}`);
}

let person = { name: "小明" };

// apply用数组传参数
introduce.apply(person, [18, "北京"]);
// 我是小明，18岁，住在北京
```

**🔸 bind方法**
```javascript
function greet() {
    console.log(`Hello, ${this.name}`);
}

let person = { name: "小明" };

// bind返回新函数，不立即执行
let boundGreet = greet.bind(person);
boundGreet();  // Hello, 小明
```

**📊 call、apply、bind对比**

| 方法 | **使用方式** | **执行时机** | **常用场景** |
|------|------------|-------------|-------------|
| `call` | `func.call(thisArg, arg1, arg2)` | `立即执行` | `参数较少时` |
| `apply` | `func.apply(thisArg, [args])` | `立即执行` | `参数是数组时` |
| `bind` | `func.bind(thisArg)` | `返回新函数` | `需要保存函数时` |

---

## 4. 🛠️ 常用内置函数


### 4.1 数字处理函数


**🔸 字符串转数字**
```javascript
// parseInt：转整数
console.log(parseInt("123"));     // 123
console.log(parseInt("123.45"));  // 123
console.log(parseInt("123abc"));  // 123

// parseFloat：转小数
console.log(parseFloat("123.45")); // 123.45
console.log(parseFloat("123"));    // 123

// Number：严格转换
console.log(Number("123"));     // 123
console.log(Number("123abc"));  // NaN
```

**🔸 Math对象常用方法**
```javascript
// 四舍五入
console.log(Math.round(4.7));   // 5
console.log(Math.round(4.3));   // 4

// 向上取整
console.log(Math.ceil(4.1));    // 5

// 向下取整
console.log(Math.floor(4.9));   // 4

// 随机数 0-1
console.log(Math.random());     // 0.xxx

// 随机整数 1-10
let randomNum = Math.floor(Math.random() * 10) + 1;
```

### 4.2 字符串处理函数


**🔸 常用字符串方法**
```javascript
let str = "Hello World";

// 查找位置
console.log(str.indexOf("o"));      // 4
console.log(str.lastIndexOf("o"));  // 7

// 截取字符串
console.log(str.substring(0, 5));   // "Hello"
console.log(str.slice(6));          // "World"

// 替换
console.log(str.replace("World", "JavaScript")); // "Hello JavaScript"

// 大小写转换
console.log(str.toLowerCase());     // "hello world"
console.log(str.toUpperCase());     // "HELLO WORLD"

// 去除空格
let text = "  hello  ";
console.log(text.trim());           // "hello"
```

### 4.3 Date对象基础


**🔸 获取当前时间**
```javascript
let now = new Date();
console.log(now);  // 当前完整时间

// 获取各部分
console.log(now.getFullYear());  // 年份：2024
console.log(now.getMonth());     // 月份：0-11（注意从0开始）
console.log(now.getDate());      // 日期：1-31
console.log(now.getHours());     // 小时：0-23
console.log(now.getMinutes());   // 分钟：0-59
```

**🔸 格式化时间**
```javascript
function formatDate(date) {
    let year = date.getFullYear();
    let month = (date.getMonth() + 1).toString().padStart(2, '0');
    let day = date.getDate().toString().padStart(2, '0');
    
    return `${year}-${month}-${day}`;
}

console.log(formatDate(new Date()));  // "2024-01-28"
```

---

## 5. 📋 核心要点总结


### 5.1 必须掌握的核心概念


```
🔸 函数定义：function声明、函数表达式、箭头函数
🔸 函数调用：参数传递、返回值、作用域
🔸 this指向：普通函数vs箭头函数、call/apply/bind
🔸 闭包概念：数据私有、状态保持
🔸 内置函数：数字转换、字符串处理、时间操作
```

### 5.2 关键理解要点


**🔹 函数的本质**
```
函数 = 代码封装 + 复用工具
• 输入（参数）→ 处理（函数体）→ 输出（返回值）
• 避免重复代码，提高开发效率
• 模块化思维的基础
```

**🔹 this的核心规律**
```
谁调用函数，this就指向谁：
• obj.func() → this指向obj
• func() → this指向window
• 箭头函数 → this继承外层
• call/apply/bind → 手动指定this
```

**🔹 作用域的查找规则**
```
变量查找：从内到外，层层向上
• 当前作用域 → 上级作用域 → 全局作用域
• 闭包：内部函数可以访问外部变量
• 块级作用域：let/const有块级限制
```

### 5.3 实际应用指导


**🎯 函数选择策略**
```
函数声明：
✅ 一般情况的首选
✅ 需要函数提升时
✅ 复杂逻辑函数

箭头函数：
✅ 简单计算逻辑
✅ 数组方法回调
✅ 不需要this时

函数表达式：
✅ 条件性定义函数
✅ 作为参数传递时
```

**🔧 常见应用场景**
```javascript
// 工具函数
function formatMoney(amount) {
    return `¥${amount.toFixed(2)}`;
}

// 事件处理
button.onclick = function() {
    console.log("按钮被点击");
};

// 数组处理
let prices = [10, 20, 30];
let total = prices.reduce((sum, price) => sum + price, 0);

// 异步回调
setTimeout(() => {
    console.log("3秒后执行");
}, 3000);
```

### 5.4 避免常见错误


**⚠️ this指向问题**
```javascript
// ❌ 错误：this丢失
let obj = {
    name: "test",
    sayHello: function() { console.log(this.name); }
};
let fn = obj.sayHello;
fn(); // this指向window

// ✅ 正确：保持this指向
let fn = obj.sayHello.bind(obj);
fn(); // this指向obj
```

**⚠️ 闭包内存泄漏**
```javascript
// ❌ 注意：避免循环引用
function createHandler(element) {
    return function() {
        element.style.color = "red";
    };
}

// ✅ 使用完及时清理
let handler = createHandler(element);
element.onclick = handler;
// 不用时：element.onclick = null;
```

**核心记忆**：
- 函数是代码复用的基本工具
- this指向取决于调用方式，箭头函数除外
- 作用域决定变量的访问范围
- 闭包可以创建私有变量和持久状态
- 熟练使用内置函数提高开发效率