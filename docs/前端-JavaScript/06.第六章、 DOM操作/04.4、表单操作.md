---
title: 4、表单操作
---
## 📚 目录

1. [表单操作概述](#1-表单操作概述)
2. [获取表单元素](#2-获取表单元素)
3. [获取和设置输入值](#3-获取和设置输入值)
4. [处理选中状态](#4-处理选中状态)
5. [控制元素状态](#5-控制元素状态)
6. [表单验证基础](#6-表单验证基础)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 📝 表单操作概述


### 1.1 什么是表单操作


**🔸 核心定义**
```
表单操作：用JavaScript获取、修改、验证网页表单内容
目的：实现用户交互，处理用户输入的数据
应用场景：登录注册、搜索框、在线问卷、购物车等
```

### 1.2 为什么需要表单操作


**💡 实际需求**
```
用户场景：
• 检查用户是否填写了用户名
• 验证邮箱格式是否正确
• 获取用户选择的城市
• 禁用/启用提交按钮
• 实时显示输入内容

JavaScript的作用：
让表单变得"聪明"，能响应用户操作
```

### 1.3 常见表单元素


**📋 表单元素类型**
```
文本输入：<input type="text">、<textarea>
密码输入：<input type="password">
单选按钮：<input type="radio">
复选框：<input type="checkbox">
下拉菜单：<select>
提交按钮：<input type="submit">、<button>
```

---

## 2. 🎯 获取表单元素


### 2.1 通过ID获取


**🔸 最常用方法**
```html
<input type="text" id="username" placeholder="请输入用户名">
<button id="loginBtn">登录</button>
```

```javascript
// 获取输入框
const usernameInput = document.getElementById('username');

// 获取按钮
const loginBtn = document.getElementById('loginBtn');

console.log(usernameInput); // 输出：<input type="text" id="username" ...>
```

### 2.2 通过选择器获取


**🔧 灵活的获取方式**
```html
<form class="login-form">
    <input type="text" name="username">
    <input type="password" name="password">
    <button type="submit">提交</button>
</form>
```

```javascript
// 通过类名获取表单
const form = document.querySelector('.login-form');

// 通过name属性获取
const usernameInput = document.querySelector('input[name="username"]');
const passwordInput = document.querySelector('input[name="password"]');

// 获取表单内所有输入框
const allInputs = document.querySelectorAll('.login-form input');
```

### 2.3 通过表单elements集合


**⚡ 表单专用方法**
```html
<form id="myForm">
    <input name="username" type="text">
    <input name="email" type="email">
</form>
```

```javascript
const form = document.getElementById('myForm');

// 通过name直接访问
const username = form.elements.username;
const email = form.elements.email;

// 或者使用数组方式
const username2 = form.elements['username'];
```

---

## 3. 📥 获取和设置输入值


### 3.1 文本输入框操作


**🔸 value属性是关键**
```html
<input type="text" id="name" value="默认值">
<input type="password" id="pwd">
<textarea id="content">默认内容</textarea>
```

```javascript
const nameInput = document.getElementById('name');
const pwdInput = document.getElementById('pwd');
const contentArea = document.getElementById('content');

// 获取值
console.log(nameInput.value);    // "默认值"
console.log(pwdInput.value);     // ""
console.log(contentArea.value);  // "默认内容"

// 设置值
nameInput.value = "张三";
pwdInput.value = "123456";
contentArea.value = "新的内容";
```

### 3.2 实时获取用户输入


**💡 监听输入变化**
```html
<input type="text" id="search" placeholder="搜索...">
<div id="result"></div>
```

```javascript
const searchInput = document.getElementById('search');
const resultDiv = document.getElementById('result');

// 实时显示用户输入
searchInput.addEventListener('input', function() {
    const userInput = searchInput.value;
    resultDiv.textContent = `你输入了：${userInput}`;
});
```

### 3.3 常用输入验证


**✅ 基础验证示例**
```html
<input type="text" id="phone" placeholder="手机号">
<span id="phoneMsg"></span>
```

```javascript
const phoneInput = document.getElementById('phone');
const phoneMsg = document.getElementById('phoneMsg');

phoneInput.addEventListener('blur', function() {
    const phone = phoneInput.value;
    
    if (phone.length === 0) {
        phoneMsg.textContent = "请输入手机号";
        phoneMsg.style.color = "red";
    } else if (phone.length !== 11) {
        phoneMsg.textContent = "手机号应为11位";
        phoneMsg.style.color = "red";
    } else {
        phoneMsg.textContent = "格式正确";
        phoneMsg.style.color = "green";
    }
});
```

---

## 4. ☑️ 处理选中状态


### 4.1 复选框操作


**🔸 checked属性控制选中**
```html
<input type="checkbox" id="agree"> 我同意用户协议
<input type="checkbox" id="subscribe" checked> 订阅邮件
```

```javascript
const agreeBox = document.getElementById('agree');
const subscribeBox = document.getElementById('subscribe');

// 获取选中状态
console.log(agreeBox.checked);     // false
console.log(subscribeBox.checked); // true

// 设置选中状态
agreeBox.checked = true;           // 选中
subscribeBox.checked = false;      // 取消选中
```

### 4.2 单选按钮操作


**🔸 同name的单选按钮互斥**
```html
<input type="radio" name="gender" value="male" id="male"> 男
<input type="radio" name="gender" value="female" id="female"> 女
```

```javascript
const maleRadio = document.getElementById('male');
const femaleRadio = document.getElementById('female');

// 设置选中
maleRadio.checked = true;

// 获取选中的值
function getSelectedGender() {
    if (maleRadio.checked) {
        return maleRadio.value;  // "male"
    } else if (femaleRadio.checked) {
        return femaleRadio.value; // "female"
    } else {
        return null; // 都没选中
    }
}
```

### 4.3 下拉菜单操作


**🔸 select元素的value和selectedIndex**
```html
<select id="city">
    <option value="">请选择城市</option>
    <option value="beijing">北京</option>
    <option value="shanghai">上海</option>
    <option value="guangzhou">广州</option>
</select>
```

```javascript
const citySelect = document.getElementById('city');

// 获取选中的值
console.log(citySelect.value); // 例如："beijing"

// 获取选中选项的文本
const selectedOption = citySelect.options[citySelect.selectedIndex];
console.log(selectedOption.text); // 例如："北京"

// 设置选中项
citySelect.value = "shanghai"; // 选中上海

// 监听选择变化
citySelect.addEventListener('change', function() {
    console.log('用户选择了：', citySelect.value);
});
```

---

## 5. 🎛️ 控制元素状态


### 5.1 禁用和启用元素


**🔸 disabled属性控制**
```html
<input type="text" id="username">
<input type="password" id="password">
<button id="submitBtn" disabled>登录</button>
```

```javascript
const usernameInput = document.getElementById('username');
const passwordInput = document.getElementById('password');
const submitBtn = document.getElementById('submitBtn');

// 检查输入，控制按钮状态
function checkInput() {
    const username = usernameInput.value.trim();
    const password = passwordInput.value.trim();
    
    // 用户名和密码都不为空时，启用按钮
    if (username.length > 0 && password.length > 0) {
        submitBtn.disabled = false;
        submitBtn.style.opacity = '1';
    } else {
        submitBtn.disabled = true;
        submitBtn.style.opacity = '0.5';
    }
}

// 监听输入变化
usernameInput.addEventListener('input', checkInput);
passwordInput.addEventListener('input', checkInput);
```

### 5.2 只读状态控制


**🔸 readonly属性设置只读**
```html
<input type="text" id="orderNo" value="ORDER-20250728" readonly>
<input type="text" id="editableField">
<button onclick="toggleReadonly()">切换只读</button>
```

```javascript
const editableField = document.getElementById('editableField');

function toggleReadonly() {
    if (editableField.readOnly) {
        editableField.readOnly = false;
        editableField.style.backgroundColor = 'white';
    } else {
        editableField.readOnly = true;
        editableField.style.backgroundColor = '#f5f5f5';
    }
}
```

### 5.3 显示隐藏元素


**🔸 控制元素可见性**
```html
<input type="checkbox" id="showAdvanced"> 显示高级选项
<div id="advancedOptions" style="display: none;">
    <input type="text" placeholder="高级设置1">
    <input type="text" placeholder="高级设置2">
</div>
```

```javascript
const showAdvancedBox = document.getElementById('showAdvanced');
const advancedOptions = document.getElementById('advancedOptions');

showAdvancedBox.addEventListener('change', function() {
    if (showAdvancedBox.checked) {
        advancedOptions.style.display = 'block';
    } else {
        advancedOptions.style.display = 'none';
    }
});
```

---

## 6. ✅ 表单验证基础


### 6.1 非空验证


**🔸 检查必填字段**
```html
<form id="registerForm">
    <input type="text" id="username" placeholder="用户名" required>
    <input type="email" id="email" placeholder="邮箱" required>
    <input type="password" id="password" placeholder="密码" required>
    <button type="submit">注册</button>
</form>
```

```javascript
const form = document.getElementById('registerForm');

function validateForm() {
    const username = document.getElementById('username').value.trim();
    const email = document.getElementById('email').value.trim();
    const password = document.getElementById('password').value.trim();
    
    // 检查非空
    if (!username) {
        alert('请输入用户名');
        return false;
    }
    
    if (!email) {
        alert('请输入邮箱');
        return false;
    }
    
    if (!password) {
        alert('请输入密码');
        return false;
    }
    
    return true; // 验证通过
}

// 阻止表单默认提交，进行验证
form.addEventListener('submit', function(event) {
    event.preventDefault(); // 阻止默认提交
    
    if (validateForm()) {
        console.log('验证通过，可以提交');
        // 这里可以发送数据到服务器
    }
});
```

### 6.2 格式验证


**🔸 常用格式检查**
```html
<input type="text" id="phone" placeholder="手机号">
<input type="email" id="email" placeholder="邮箱">
<div id="errorMsg"></div>
```

```javascript
const phoneInput = document.getElementById('phone');
const emailInput = document.getElementById('email');
const errorMsg = document.getElementById('errorMsg');

// 手机号验证
function validatePhone(phone) {
    const phoneRegex = /^1[3-9]\d{9}$/;
    return phoneRegex.test(phone);
}

// 邮箱验证（简单版）
function validateEmail(email) {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return emailRegex.test(email);
}

// 实时验证
phoneInput.addEventListener('blur', function() {
    const phone = phoneInput.value.trim();
    if (phone && !validatePhone(phone)) {
        errorMsg.textContent = '手机号格式不正确';
        errorMsg.style.color = 'red';
    } else {
        errorMsg.textContent = '';
    }
});

emailInput.addEventListener('blur', function() {
    const email = emailInput.value.trim();
    if (email && !validateEmail(email)) {
        errorMsg.textContent = '邮箱格式不正确';
        errorMsg.style.color = 'red';
    } else {
        errorMsg.textContent = '';
    }
});
```

### 6.3 密码确认验证


**🔸 两次密码输入一致性检查**
```html
<input type="password" id="password" placeholder="密码">
<input type="password" id="confirmPassword" placeholder="确认密码">
<span id="passwordMsg"></span>
```

```javascript
const passwordInput = document.getElementById('password');
const confirmPasswordInput = document.getElementById('confirmPassword');
const passwordMsg = document.getElementById('passwordMsg');

function checkPasswordMatch() {
    const password = passwordInput.value;
    const confirmPassword = confirmPasswordInput.value;
    
    if (confirmPassword.length === 0) {
        passwordMsg.textContent = '';
        return;
    }
    
    if (password === confirmPassword) {
        passwordMsg.textContent = '密码一致';
        passwordMsg.style.color = 'green';
    } else {
        passwordMsg.textContent = '两次密码不一致';
        passwordMsg.style.color = 'red';
    }
}

// 监听两个密码输入框
passwordInput.addEventListener('input', checkPasswordMatch);
confirmPasswordInput.addEventListener('input', checkPasswordMatch);
```

### 6.4 完整表单验证示例


**🎯 综合验证案例**
```html
<form id="completeForm">
    <div>
        <input type="text" id="name" placeholder="姓名" required>
        <span class="error" id="nameError"></span>
    </div>
    <div>
        <input type="tel" id="mobile" placeholder="手机号" required>
        <span class="error" id="mobileError"></span>
    </div>
    <div>
        <input type="checkbox" id="terms" required>
        <label for="terms">我同意用户协议</label>
        <span class="error" id="termsError"></span>
    </div>
    <button type="submit">提交</button>
</form>
```

```javascript
const completeForm = document.getElementById('completeForm');

// 验证规则
const validators = {
    name: (value) => {
        if (!value.trim()) return '请输入姓名';
        if (value.trim().length < 2) return '姓名至少2个字符';
        return '';
    },
    
    mobile: (value) => {
        if (!value.trim()) return '请输入手机号';
        if (!/^1[3-9]\d{9}$/.test(value)) return '手机号格式不正确';
        return '';
    },
    
    terms: (checked) => {
        if (!checked) return '请同意用户协议';
        return '';
    }
};

// 验证单个字段
function validateField(fieldName, value, isCheckbox = false) {
    const errorElement = document.getElementById(`${fieldName}Error`);
    const fieldValue = isCheckbox ? value : value;
    const errorMessage = validators[fieldName](fieldValue);
    
    errorElement.textContent = errorMessage;
    errorElement.style.color = 'red';
    
    return errorMessage === ''; // 返回是否验证通过
}

// 表单提交验证
completeForm.addEventListener('submit', function(event) {
    event.preventDefault();
    
    const name = document.getElementById('name').value;
    const mobile = document.getElementById('mobile').value;
    const terms = document.getElementById('terms').checked;
    
    // 验证所有字段
    const isNameValid = validateField('name', name);
    const isMobileValid = validateField('mobile', mobile);
    const isTermsValid = validateField('terms', terms, true);
    
    // 如果所有字段都通过验证
    if (isNameValid && isMobileValid && isTermsValid) {
        alert('提交成功！');
        console.log('提交的数据:', { name, mobile, terms });
    }
});
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 获取表单元素：getElementById、querySelector、form.elements
🔸 value属性：获取和设置输入框的值
🔸 checked属性：处理复选框和单选按钮的选中状态
🔸 disabled属性：控制元素的禁用和启用
🔸 事件监听：input、change、blur、submit事件
🔸 表单验证：非空检查、格式验证、实时反馈
```

### 7.2 关键理解要点


**🔹 表单元素的value和checked**
```
文本输入：element.value 获取和设置文本内容
复选框：element.checked 获取和设置选中状态
单选按钮：name相同的互斥，通过checked判断选中
下拉菜单：select.value 获取选中选项的value值
```

**🔹 表单验证的时机**
```
实时验证：input事件 - 用户输入时立即反馈
失焦验证：blur事件 - 用户离开输入框时检查
提交验证：submit事件 - 表单提交前最终检查
```

**🔹 用户体验考虑**
```
及时反馈：输入错误立即提示
状态控制：根据输入启用/禁用按钮
友好提示：清晰的错误信息和正确提示
阻止误操作：验证不通过阻止表单提交
```

### 7.3 实际应用价值


- **用户注册登录**：验证用户名、密码、邮箱格式
- **在线表单**：问卷调查、申请表单的数据收集
- **电商购物**：收货地址、支付信息的验证
- **搜索功能**：实时搜索、关键词提示
- **设置页面**：用户偏好设置、开关控制

**核心记忆**：
- 表单操作就是获取用户输入，验证数据格式
- value获取文本，checked获取选中状态
- 及时验证提升用户体验，submit前最终检查
- disabled控制状态，addEventListener监听变化