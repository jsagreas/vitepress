---
title: 3、创建和删除元素
---
## 📚 目录

1. [为什么要动态创建删除元素](#1-为什么要动态创建删除元素)
2. [创建新元素](#2-创建新元素)
3. [添加元素到页面](#3-添加元素到页面)
4. [删除页面元素](#4-删除页面元素)
5. [插入元素到指定位置](#5-插入元素到指定位置)
6. [克隆复制元素](#6-克隆复制元素)
7. [实用技巧总结](#7-实用技巧总结)

---

## 1. 🤔 为什么要动态创建删除元素


### 1.1 实际应用场景

**日常开发中经常遇到的情况：**
```
用户点击按钮 → 添加一个新的待办事项
用户点击删除 → 移除某个商品
用户提交表单 → 显示一个成功提示框
页面加载数据 → 动态生成商品列表
```

### 1.2 静态HTML的局限性

```html
<!-- 静态HTML：写死的内容 -->
<ul>
  <li>任务1</li>
  <li>任务2</li>
</ul>

<!-- 无法处理：用户要添加任务3、任务4... -->
```

**动态操作的价值：**
- ✅ **响应用户操作** - 根据用户行为添加/删除内容
- ✅ **处理数据变化** - 服务器返回的数据动态显示
- ✅ **提升用户体验** - 无需刷新页面就能更新内容

---

## 2. 🛠️ 创建新元素


### 2.1 createElement() 创建元素


**🔸 核心概念**
```
createElement() = 在内存中创建一个新的HTML元素
特点：创建后还没有显示在页面上，需要添加到DOM中
```

### 2.2 基本语法

```javascript
// 语法：document.createElement('标签名')
let newDiv = document.createElement('div');
let newP = document.createElement('p');
let newImg = document.createElement('img');
```

### 2.3 创建并设置内容

```javascript
// 创建一个完整的列表项
let newLi = document.createElement('li');
newLi.textContent = '新任务';
newLi.className = 'task-item';

// 创建带属性的图片
let newImg = document.createElement('img');
newImg.src = 'photo.jpg';
newImg.alt = '照片描述';
```

### 2.4 实际应用示例

```javascript
// 场景：用户点击按钮添加新任务
function addTask() {
    // 1. 创建新元素
    let taskItem = document.createElement('div');
    taskItem.className = 'task';
    
    // 2. 设置内容
    taskItem.innerHTML = `
        <span>新任务</span>
        <button onclick="deleteTask(this)">删除</button>
    `;
    
    // 3. 添加到页面（下一节详细讲）
    document.getElementById('task-list').appendChild(taskItem);
}
```

---

## 3. ➕ 添加元素到页面


### 3.1 appendChild() 添加子元素


**🔸 核心概念**
```
appendChild() = 把创建的元素添加为某个父元素的最后一个子元素
理解：就像往盒子里放东西，新东西放在最下面
```

### 3.2 基本用法

```javascript
// 语法：父元素.appendChild(子元素)
let parent = document.getElementById('container');
let child = document.createElement('p');
child.textContent = '我是新段落';

parent.appendChild(child);  // 添加到container的末尾
```

### 3.3 完整的创建添加流程

```javascript
// 完整示例：动态添加商品到购物车
function addToCart(productName, price) {
    // 1. 获取购物车容器
    let cartList = document.getElementById('cart-list');
    
    // 2. 创建商品项
    let productDiv = document.createElement('div');
    productDiv.className = 'cart-item';
    
    // 3. 设置商品内容
    productDiv.innerHTML = `
        <span class="name">${productName}</span>
        <span class="price">¥${price}</span>
        <button class="remove-btn">删除</button>
    `;
    
    // 4. 添加到购物车
    cartList.appendChild(productDiv);
}

// 使用
addToCart('苹果', 5);
addToCart('香蕉', 3);
```

### 3.4 批量添加的技巧

```javascript
// 高效方式：先创建容器，再一次性添加
function addMultipleItems(items) {
    let container = document.getElementById('list');
    let fragment = document.createDocumentFragment(); // 文档片段
    
    items.forEach(item => {
        let div = document.createElement('div');
        div.textContent = item;
        fragment.appendChild(div);  // 先添加到片段
    });
    
    container.appendChild(fragment);  // 一次性添加到页面
}
```

---

## 4. ❌ 删除页面元素


### 4.1 removeChild() 删除子元素


**🔸 核心概念**
```
removeChild() = 父元素删除它的某个子元素
特点：需要知道父元素，然后删除子元素
语法：父元素.removeChild(要删除的子元素)
```

### 4.2 基本删除方法

```javascript
// 方法1：通过父元素删除
let parent = document.getElementById('container');
let child = document.getElementById('item-1');
parent.removeChild(child);

// 方法2：元素删除自己（常用）
let element = document.getElementById('item-1');
element.parentNode.removeChild(element);
```

### 4.3 现代浏览器的简单方法

```javascript
// ES6新方法：直接删除（推荐）
let element = document.getElementById('item-1');
element.remove();  // 简单直接！
```

### 4.4 实际应用：删除功能实现

```javascript
// 场景：删除待办事项
function deleteTask(deleteBtn) {
    // 获取要删除的任务项（按钮的父元素）
    let taskItem = deleteBtn.parentNode;
    
    // 确认删除
    if (confirm('确定要删除这个任务吗？')) {
        taskItem.remove();  // 删除元素
    }
}

// HTML中的使用
// <div class="task">
//   <span>任务内容</span>
//   <button onclick="deleteTask(this)">删除</button>
// </div>
```

### 4.5 批量删除技巧

```javascript
// 删除所有某类元素
function clearAllTasks() {
    let tasks = document.querySelectorAll('.task');
    tasks.forEach(task => task.remove());
}

// 删除容器内所有子元素
function clearContainer() {
    let container = document.getElementById('container');
    container.innerHTML = '';  // 最简单的清空方法
}
```

---

## 5. 📍 插入元素到指定位置


### 5.1 insertBefore() 在指定位置插入


**🔸 核心概念**
```
insertBefore() = 在某个元素前面插入新元素
appendChild()只能添加到最后，insertBefore()可以插入到任意位置
语法：父元素.insertBefore(新元素, 参考元素)
```

### 5.2 基本用法

```javascript
// 在指定元素前插入
let parent = document.getElementById('list');
let newItem = document.createElement('li');
newItem.textContent = '我是新项目';

let referenceItem = document.getElementById('item-3');
parent.insertBefore(newItem, referenceItem);  // 插入到item-3前面
```

### 5.3 插入到开头的技巧

```javascript
// 插入到容器的最前面
function addToTop(content) {
    let container = document.getElementById('list');
    let newItem = document.createElement('div');
    newItem.textContent = content;
    
    let firstChild = container.firstElementChild;
    if (firstChild) {
        container.insertBefore(newItem, firstChild);  // 插到第一个前面
    } else {
        container.appendChild(newItem);  // 如果没有子元素就直接添加
    }
}
```

### 5.4 现代浏览器的新方法

```javascript
// 更直观的插入方法
let element = document.getElementById('target');
let newElement = document.createElement('div');

// 插入到目标元素的不同位置
element.insertAdjacentElement('beforebegin', newElement);  // 前面
element.insertAdjacentElement('afterbegin', newElement);   // 内部开头
element.insertAdjacentElement('beforeend', newElement);    // 内部末尾
element.insertAdjacentElement('afterend', newElement);     // 后面
```

---

## 6. 📋 克隆复制元素


### 6.1 cloneNode() 克隆元素


**🔸 核心概念**
```
cloneNode() = 复制一个现有元素
用途：基于现有元素创建相似的新元素，避免重复写代码
参数：true(深克隆-包含子元素) / false(浅克隆-只克隆当前元素)
```

### 6.2 基本克隆方法

```javascript
// 获取要克隆的元素
let original = document.getElementById('template-item');

// 浅克隆：只克隆元素本身
let shallowCopy = original.cloneNode(false);

// 深克隆：克隆元素及其所有子元素（常用）
let deepCopy = original.cloneNode(true);
```

### 6.3 实际应用：模板复制

```javascript
// 场景：基于模板添加新的表单项
function addFormItem() {
    // 1. 获取模板元素
    let template = document.getElementById('form-item-template');
    
    // 2. 克隆模板
    let newItem = template.cloneNode(true);
    
    // 3. 修改克隆元素的内容
    newItem.id = 'form-item-' + Date.now();  // 给新ID
    newItem.style.display = 'block';  // 显示元素
    
    // 4. 清空输入框的值
    let inputs = newItem.querySelectorAll('input');
    inputs.forEach(input => input.value = '');
    
    // 5. 添加到页面
    document.getElementById('form-container').appendChild(newItem);
}
```

### 6.4 克隆的注意事项

```javascript
// ⚠️ 克隆时要注意的问题
function cloneExample() {
    let original = document.getElementById('item-1');
    let clone = original.cloneNode(true);
    
    // 问题1：ID重复了！需要修改
    clone.id = 'item-copy';
    
    // 问题2：事件监听器不会被克隆
    clone.addEventListener('click', handleClick);  // 需要重新绑定
    
    // 添加到页面
    document.body.appendChild(clone);
}
```

---

## 7. 💡 实用技巧总结


### 7.1 常用操作模式对比


| **操作需求** | **推荐方法** | **使用场景** |
|-------------|-------------|-------------|
| `添加到末尾` | `appendChild()` | `添加新留言、新商品` |
| `添加到开头` | `insertBefore()` | `最新消息置顶` |
| `删除元素` | `element.remove()` | `删除按钮功能` |
| `基于模板创建` | `cloneNode(true)` | `复制表单项` |
| `清空容器` | `innerHTML = ''` | `清空购物车` |

### 7.2 性能优化技巧

```javascript
// ✅ 高效方式：使用文档片段
function addManyItems(items) {
    let fragment = document.createDocumentFragment();
    
    items.forEach(item => {
        let div = document.createElement('div');
        div.textContent = item;
        fragment.appendChild(div);
    });
    
    document.getElementById('container').appendChild(fragment);
}

// ❌ 低效方式：逐个添加
function addManyItemsSlow(items) {
    let container = document.getElementById('container');
    items.forEach(item => {
        let div = document.createElement('div');
        div.textContent = item;
        container.appendChild(div);  // 每次都操作DOM
    });
}
```

### 7.3 实用工具函数

```javascript
// 创建带内容的元素
function createElement(tag, content, className) {
    let element = document.createElement(tag);
    if (content) element.textContent = content;
    if (className) element.className = className;
    return element;
}

// 使用示例
let button = createElement('button', '点击我', 'btn btn-primary');

// 安全删除元素
function safeRemove(selector) {
    let element = document.querySelector(selector);
    if (element) {
        element.remove();
    }
}
```

### 7.4 现代DOM操作总结

```javascript
// 现代浏览器推荐的DOM操作方式

// 创建并添加
let div = document.createElement('div');
div.textContent = '内容';
document.body.append(div);  // append比appendChild更灵活

// 删除
div.remove();  // 比removeChild简单

// 插入
div.before(newElement);   // 前面插入
div.after(newElement);    // 后面插入
div.prepend(newElement);  // 内部开头插入
div.append(newElement);   // 内部末尾插入
```

### 7.5 调试技巧

```javascript
// 检查元素是否成功创建和添加
function debugDOM() {
    let newDiv = document.createElement('div');
    console.log('元素已创建:', newDiv);
    
    document.body.appendChild(newDiv);
    console.log('元素已添加到页面');
    
    // 检查元素是否在页面中
    console.log('是否在页面中:', document.body.contains(newDiv));
}
```

**核心记忆：**
- **创建**：`createElement()` 在内存中创建
- **添加**：`appendChild()` 添加到末尾
- **删除**：`remove()` 直接删除
- **插入**：`insertBefore()` 指定位置插入
- **克隆**：`cloneNode(true)` 复制元素
