---
title: 2、修改元素内容和样式
---
## 📚 目录

1. [修改元素内容](#1-修改元素内容)
2. [修改元素样式](#2-修改元素样式)
3. [操作CSS类名](#3-操作CSS类名)
4. [实用技巧与注意事项](#4-实用技巧与注意事项)
5. [核心要点总结](#5-核心要点总结)

---

## 1. 📝 修改元素内容


### 1.1 innerHTML - 修改HTML内容


**🔸 核心概念**
```
innerHTML：获取或设置元素内部的HTML内容
作用：可以插入HTML标签，会被浏览器解析渲染
特点：功能强大，但要注意安全性
```

**💡 基本用法**
```javascript
// 获取元素
const box = document.getElementById('box');

// 修改内容（包含HTML标签）
box.innerHTML = '<h2>新标题</h2><p>这是段落</p>';

// 获取当前内容
console.log(box.innerHTML);
```

**⚡ 实际应用场景**
```javascript
// 动态生成列表
const list = document.getElementById('list');
const fruits = ['苹果', '香蕉', '橙子'];

list.innerHTML = fruits.map(fruit => `<li>${fruit}</li>`).join('');

// 结果：
// <li>苹果</li>
// <li>香蕉</li>
// <li>橙子</li>
```

### 1.2 textContent - 修改纯文本


**🔸 核心概念**
```
textContent：获取或设置元素的纯文本内容
作用：只处理文本，HTML标签会被当作普通文字
特点：更安全，适合显示用户输入的内容
```

**💡 基本用法**
```javascript
const title = document.getElementById('title');

// 修改文本内容
title.textContent = '这是新标题';

// HTML标签会被当作普通文字显示
title.textContent = '<h1>标题</h1>'; // 页面显示：<h1>标题</h1>
```

### 1.3 innerHTML vs textContent 对比


| 属性 | **处理方式** | **安全性** | **使用场景** |
|------|------------|-----------|-------------|
| 🔧 **innerHTML** | `解析HTML标签` | `需要注意XSS攻击` | `需要插入HTML结构时` |
| 📝 **textContent** | `纯文本显示` | `更安全` | `显示用户输入或纯文本时` |

**🚨 安全性示例**
```javascript
const userInput = '<script>alert("危险代码")</script>';

// ❌ 危险：会执行脚本
element.innerHTML = userInput;

// ✅ 安全：只显示文本
element.textContent = userInput;
```

---

## 2. 🎨 修改元素样式


### 2.1 style.property - 直接修改样式


**🔸 核心概念**
```
style.property：直接修改元素的内联样式
作用：动态改变元素外观
特点：优先级高，会覆盖CSS文件中的样式
```

**💡 基本语法**
```javascript
const box = document.getElementById('box');

// 修改单个样式
box.style.color = 'red';
box.style.fontSize = '20px';
box.style.backgroundColor = 'yellow';

// CSS属性名转换规则：background-color → backgroundColor
box.style.borderRadius = '10px';
```

**⚡ 常用样式操作**
```javascript
const element = document.querySelector('.box');

// 颜色相关
element.style.color = '#333';
element.style.backgroundColor = '#f0f0f0';

// 尺寸相关
element.style.width = '200px';
element.style.height = '100px';

// 位置相关
element.style.position = 'absolute';
element.style.top = '50px';
element.style.left = '100px';

// 显示隐藏
element.style.display = 'none';    // 隐藏
element.style.display = 'block';   // 显示
element.style.visibility = 'hidden'; // 隐藏但占位
```

### 2.2 批量设置样式


**🔧 使用cssText**
```javascript
const box = document.getElementById('box');

// 一次性设置多个样式
box.style.cssText = 'color: red; font-size: 20px; background: yellow;';

// 或者使用模板字符串
box.style.cssText = `
  width: 200px;
  height: 100px;
  background-color: lightblue;
  border: 2px solid blue;
`;
```

---

## 3. 🏷️ 操作CSS类名


### 3.1 className - 传统类名操作


**🔸 核心概念**
```
className：获取或设置元素的class属性
作用：通过修改类名来改变样式
特点：操作的是完整的class字符串
```

**💡 基本用法**
```javascript
const button = document.getElementById('btn');

// 设置类名
button.className = 'btn btn-primary';

// 获取类名
console.log(button.className); // 'btn btn-primary'

// 添加类名（需要手动拼接）
button.className += ' active';
```

### 3.2 classList - 现代类名操作


**🔸 核心概念**
```
classList：专门操作class的API集合
作用：提供了增删改查类名的便捷方法
特点：更方便、更安全、功能更强大
```

**💡 常用方法**
```javascript
const element = document.querySelector('.box');

// 添加类名
element.classList.add('active');
element.classList.add('highlight', 'selected'); // 一次添加多个

// 删除类名
element.classList.remove('active');
element.classList.remove('highlight', 'selected');

// 切换类名（有就删除，没有就添加）
element.classList.toggle('active');

// 检查是否包含某个类
if (element.classList.contains('active')) {
    console.log('元素有active类');
}
```

**⚡ 实际应用场景**
```javascript
// 按钮点击切换激活状态
const button = document.getElementById('toggle-btn');

button.addEventListener('click', function() {
    this.classList.toggle('active');
    
    // 根据状态改变文字
    if (this.classList.contains('active')) {
        this.textContent = '已激活';
    } else {
        this.textContent = '未激活';
    }
});
```

### 3.3 className vs classList 对比


```
传统方式 className：
┌─────────────────────────┐
│ 'btn primary active'    │ ← 完整字符串操作
└─────────────────────────┘
         ↕ 需要手动处理字符串

现代方式 classList：
┌─────┬─────────┬────────┐
│ btn │ primary │ active │ ← 单独操作每个类
└─────┴─────────┴────────┘
         ↕ 提供专门的方法
```

---

## 4. 💡 实用技巧与注意事项


### 4.1 样式操作最佳实践


**✅ 推荐做法**
```javascript
// 优先使用classList操作类名
element.classList.add('highlight');

// 复杂样式定义在CSS中，JS只负责切换类名
.highlight {
    background-color: yellow;
    border: 2px solid orange;
    font-weight: bold;
}
```

**❌ 不推荐做法**
```javascript
// 避免直接设置复杂样式
element.style.backgroundColor = 'yellow';
element.style.border = '2px solid orange';
element.style.fontWeight = 'bold';
```

### 4.2 获取计算后的样式


**🔍 getComputedStyle**
```javascript
const element = document.querySelector('.box');

// 获取元素最终的样式值（包括CSS文件中的样式）
const styles = window.getComputedStyle(element);

console.log(styles.color);        // 实际的颜色值
console.log(styles.fontSize);     // 实际的字体大小
console.log(styles.width);        // 实际的宽度
```

### 4.3 常见应用场景


**📱 响应式显示隐藏**
```javascript
const sidebar = document.querySelector('.sidebar');
const toggleBtn = document.querySelector('.toggle-btn');

toggleBtn.addEventListener('click', function() {
    sidebar.classList.toggle('hidden');
});
```

**🎯 表单验证样式**
```javascript
const input = document.querySelector('#email');

input.addEventListener('blur', function() {
    if (this.value.includes('@')) {
        this.classList.remove('error');
        this.classList.add('success');
    } else {
        this.classList.remove('success');
        this.classList.add('error');
    }
});
```

**🌙 主题切换**
```javascript
const themeBtn = document.querySelector('.theme-toggle');

themeBtn.addEventListener('click', function() {
    document.body.classList.toggle('dark-theme');
    
    // 保存用户偏好
    const isDark = document.body.classList.contains('dark-theme');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
});
```

---

## 5. 📋 核心要点总结


### 5.1 必须掌握的核心概念


```
🔸 内容修改：innerHTML处理HTML，textContent处理纯文本
🔸 样式修改：style直接设置，classList操作类名
🔸 安全考虑：用户输入优先使用textContent
🔸 最佳实践：复杂样式用CSS类，JS负责切换
```

### 5.2 关键API速查


**📝 内容操作**
```javascript
element.innerHTML = '<p>HTML内容</p>';
element.textContent = '纯文本内容';
```

**🎨 样式操作**
```javascript
element.style.color = 'red';
element.style.display = 'none';
```

**🏷️ 类名操作**
```javascript
element.classList.add('class-name');
element.classList.remove('class-name');
element.classList.toggle('class-name');
element.classList.contains('class-name');
```

### 5.3 使用场景指导


```
需要插入HTML结构 → 使用 innerHTML
显示用户输入内容 → 使用 textContent
临时修改单个样式 → 使用 style.property
复杂样式变化 → 使用 classList + CSS类
响应用户交互 → 使用 classList.toggle()
```

### 5.4 注意事项


**⚠️ 安全性**
- 用户输入数据避免直接使用innerHTML
- 使用textContent更安全

**⚡ 性能考虑**
- 频繁样式修改优先使用classList
- 避免重复查询DOM元素

**🎯 维护性**
- 样式定义在CSS中，JS只负责状态切换
- 语义化的类名便于维护

**核心记忆**：
- innerHTML改结构，textContent改文字
- style改样式，classList改类名  
- 安全第一选textContent，灵活操作用classList
- CSS定义样式，JS负责切换状态