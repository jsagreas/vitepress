---
title: 6、新语法与数据结构
---
## 📚 目录

1. [简写对象语法](#1-简写对象语法)
2. [箭头函数深入](#2-箭头函数深入)
3. [Map与Set数据结构](#3-Map与Set数据结构)
4. [WeakMap与WeakSet](#4-WeakMap与WeakSet)
5. [其他实用新特性](#5-其他实用新特性)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 💡 简写对象语法


### 1.1 属性简写


**🔸 核心概念**
当对象的属性名和变量名相同时，可以省略属性值

```javascript
// 传统写法
const name = 'Tom';
const age = 18;
const user = {
  name: name,
  age: age
};

// ES6简写 - 属性名和变量名相同时可以省略
const user = {
  name,  // 等同于 name: name
  age    // 等同于 age: age
};
```

### 1.2 方法简写


**🔸 核心概念**
对象中的方法可以省略`function`关键字

```javascript
// 传统写法
const calculator = {
  add: function(a, b) {
    return a + b;
  },
  multiply: function(a, b) {
    return a * b;
  }
};

// ES6简写
const calculator = {
  add(a, b) {
    return a + b;
  },
  multiply(a, b) {
    return a * b;
  }
};
```

### 1.3 计算属性名


**🔸 核心概念**
可以用表达式作为对象的属性名

```javascript
const prefix = 'user';
const id = 123;

// 动态属性名
const obj = {
  [prefix + 'Name']: 'Tom',      // userName: 'Tom'
  [prefix + 'Id']: id,           // userId: 123
  ['get' + prefix]() {           // getuser: function()
    return this.userName;
  }
};

console.log(obj.userName);  // Tom
console.log(obj.getuser()); // Tom
```

### 1.4 实际应用场景


```javascript
// 接口数据处理
function createUser(name, email, role) {
  return {
    name,     // 简写属性
    email,
    role,
    id: Date.now(),
    
    // 简写方法
    getInfo() {
      return `${this.name} (${this.email})`;
    },
    
    // 计算属性名 - 根据角色设置权限
    [`${role}Permissions`]: ['read', 'write']
  };
}

const admin = createUser('Tom', 'tom@mail.com', 'admin');
console.log(admin.adminPermissions); // ['read', 'write']
```

---

## 2. 🏹 箭头函数深入


### 2.1 基本语法对比


```javascript
// 传统函数
function add(a, b) {
  return a + b;
}

// 箭头函数 - 完整写法
const add = (a, b) => {
  return a + b;
};

// 箭头函数 - 简写（单行表达式）
const add = (a, b) => a + b;

// 单个参数可省略括号
const double = x => x * 2;

// 无参数必须写括号
const random = () => Math.random();
```

### 2.2 this指向的重要区别


**🔸 核心差异**
- **普通函数**: `this`指向调用者
- **箭头函数**: `this`指向定义时的上下文，不会改变

```javascript
const obj = {
  name: 'Tom',
  
  // 普通函数 - this指向obj
  sayHello: function() {
    console.log(`Hello, ${this.name}`);
  },
  
  // 箭头函数 - this指向外层作用域
  sayBye: () => {
    console.log(`Bye, ${this.name}`); // this.name是undefined
  },
  
  // 实际应用：事件处理
  bindEvents: function() {
    document.addEventListener('click', () => {
      // 箭头函数保持this指向obj
      console.log(`${this.name} clicked`);
    });
  }
};

obj.sayHello(); // Hello, Tom
obj.sayBye();   // Bye, undefined
```

### 2.3 不能使用箭头函数的场景


```javascript
// ❌ 不要用作对象方法（会丢失this）
const user = {
  name: 'Tom',
  getName: () => this.name  // this不指向user
};

// ❌ 不要用作构造函数
const Person = (name) => {
  this.name = name;  // 报错：箭头函数不能用new
};

// ❌ 不要用在需要arguments的场景
const sum = () => {
  console.log(arguments); // 报错：箭头函数没有arguments
};

// ✅ 正确的写法
const user = {
  name: 'Tom',
  getName() {
    return this.name;
  }
};
```

---

## 3. 🗺️ Map与Set数据结构


### 3.1 Map映射结构


**🔸 核心概念**
Map是键值对的集合，键可以是任意类型（不只是字符串）

```javascript
// 创建Map
const map = new Map();

// 设置值
map.set('name', 'Tom');
map.set(123, 'number key');
map.set(true, 'boolean key');

// 获取值
console.log(map.get('name'));  // Tom
console.log(map.get(123));     // number key

// 检查是否存在
console.log(map.has('name'));  // true

// 删除
map.delete('name');
console.log(map.has('name'));  // false

// 获取长度
console.log(map.size);  // 2
```

### 3.2 Map vs 普通对象


| 特性 | **Map** | **普通对象** |
|------|---------|-------------|
| **键的类型** | `任意类型` | `字符串/Symbol` |
| **获取长度** | `map.size` | `Object.keys(obj).length` |
| **遍历顺序** | `插入顺序` | `不保证顺序` |
| **原型** | `没有默认键` | `有默认键` |

```javascript
// Map的优势示例
const userRoles = new Map();
const adminUser = {id: 1, name: 'Tom'};
const normalUser = {id: 2, name: 'John'};

// 对象作为键
userRoles.set(adminUser, 'admin');
userRoles.set(normalUser, 'user');

console.log(userRoles.get(adminUser)); // admin

// 遍历Map
userRoles.forEach((role, user) => {
  console.log(`${user.name}: ${role}`);
});
```

### 3.3 Set集合结构


**🔸 核心概念**
Set是值的集合，所有值都是唯一的（自动去重）

```javascript
// 创建Set
const set = new Set();

// 添加值
set.add(1);
set.add(2);
set.add(2);  // 重复值，不会添加
set.add('hello');

console.log(set.size);  // 3（自动去重）

// 检查是否存在
console.log(set.has(1));     // true
console.log(set.has('hi'));  // false

// 删除值
set.delete(1);
console.log(set.has(1));     // false
```

### 3.4 Set的实际应用


```javascript
// 数组去重
const numbers = [1, 2, 2, 3, 3, 4];
const uniqueNumbers = [...new Set(numbers)];
console.log(uniqueNumbers); // [1, 2, 3, 4]

// 字符串去重
const str = 'hello';
const uniqueChars = [...new Set(str)];
console.log(uniqueChars); // ['h', 'e', 'l', 'o']

// 判断两个数组的交集
const arr1 = [1, 2, 3];
const arr2 = [2, 3, 4];
const set1 = new Set(arr1);
const intersection = arr2.filter(x => set1.has(x));
console.log(intersection); // [2, 3]
```

### 3.5 遍历Map和Set


```javascript
const map = new Map([
  ['name', 'Tom'],
  ['age', 18]
]);

const set = new Set([1, 2, 3]);

// Map遍历
for (let [key, value] of map) {
  console.log(`${key}: ${value}`);
}

// Set遍历
for (let value of set) {
  console.log(value);
}

// forEach方法
map.forEach((value, key) => console.log(`${key}: ${value}`));
set.forEach(value => console.log(value));
```

---

## 4. 🔒 WeakMap与WeakSet


### 4.1 WeakMap基础


**🔸 核心概念**
WeakMap是弱引用的Map，键必须是对象，不阻止垃圾回收

```javascript
const weakMap = new WeakMap();
let obj = {name: 'Tom'};

// 设置值（键必须是对象）
weakMap.set(obj, 'some data');
console.log(weakMap.get(obj)); // some data

// 当obj被销毁时，WeakMap中的记录也会自动清除
obj = null; // 对象可以被垃圾回收
```

### 4.2 WeakMap与Map的区别


```
┌─────────────────┬─────────────────┬─────────────────┐
│      特性       │       Map       │    WeakMap      │
├─────────────────┼─────────────────┼─────────────────┤
│   键的类型      │    任意类型      │   只能是对象     │
├─────────────────┼─────────────────┼─────────────────┤
│   垃圾回收      │   阻止回收      │   不阻止回收     │
├─────────────────┼─────────────────┼─────────────────┤
│   可遍历性      │     可遍历      │    不可遍历     │
├─────────────────┼─────────────────┼─────────────────┤
│    获取长度     │   有size属性    │   无size属性    │
└─────────────────┴─────────────────┴─────────────────┘
```

### 4.3 实际应用场景


```javascript
// 为DOM元素存储额外数据
const elementData = new WeakMap();

function attachData(element, data) {
  elementData.set(element, data);
}

function getData(element) {
  return elementData.get(element);
}

// 使用
const button = document.querySelector('#myButton');
attachData(button, {clickCount: 0, lastClicked: null});

// 当DOM元素被删除时，存储的数据会自动清除
```

### 4.4 WeakSet基础


**🔸 核心概念**
WeakSet是弱引用的Set，只能存储对象

```javascript
const weakSet = new WeakSet();
let obj1 = {id: 1};
let obj2 = {id: 2};

// 添加对象
weakSet.add(obj1);
weakSet.add(obj2);

// 检查是否存在
console.log(weakSet.has(obj1)); // true

// 对象被销毁时自动从WeakSet中移除
obj1 = null;
```

---

## 5. ⚡ 其他实用新特性


### 5.1 可选链操作符(?.)


**🔸 核心概念**
安全地访问深层嵌套的对象属性，避免报错

```javascript
const user = {
  name: 'Tom',
  address: {
    street: '123 Main St'
  }
};

// 传统写法 - 需要多层判断
if (user && user.address && user.address.street) {
  console.log(user.address.street);
}

// ES2020 可选链 - 简洁安全
console.log(user?.address?.street);        // 123 Main St
console.log(user?.phone?.number);          // undefined（不报错）
console.log(user?.getName?.());            // undefined（方法也适用）

// 数组中的应用
const users = [
  {name: 'Tom', skills: ['JS', 'CSS']},
  {name: 'John'}
];
console.log(users[0]?.skills?.[0]);        // JS
console.log(users[1]?.skills?.[0]);        // undefined
```

### 5.2 空值合并操作符(??)


**🔸 核心概念**
当左侧为`null`或`undefined`时，返回右侧值

```javascript
// 与 || 的区别
const value1 = 0 || 'default';        // 'default' (0被认为是假值)
const value2 = 0 ?? 'default';        // 0 (只有null/undefined才使用默认值)

const value3 = '' || 'default';       // 'default'
const value4 = '' ?? 'default';       // ''

// 实际应用
function createUser(name, age, role) {
  return {
    name: name ?? 'Unknown',           // 只在null/undefined时使用默认值
    age: age ?? 0,
    role: role ?? 'user'
  };
}

const user1 = createUser('Tom', 25);        // age: 25
const user2 = createUser('John', 0);        // age: 0 (不会被默认值覆盖)
const user3 = createUser('Alice', null);    // age: 0 (null被替换)
```

### 5.3 数值分隔符(_)


**🔸 核心概念**
使用下划线分隔大数字，提高可读性

```javascript
// 大数字的可读性
const million = 1_000_000;           // 等同于 1000000
const billion = 1_000_000_000;       // 等同于 1000000000
const decimal = 123.456_789;         // 等同于 123.456789
const binary = 0b1010_0001;          // 二进制
const hex = 0xFF_EC_DE_5E;           // 十六进制

console.log(million);                // 1000000 (输出时无下划线)

// 实际应用
const salary = 50_000;               // 年薪5万
const population = 1_400_000_000;    // 14亿人口
```

### 5.4 globalThis全局对象


**🔸 核心概念**
统一的全局对象引用，兼容不同环境

```javascript
// 不同环境的全局对象
// 浏览器: window
// Node.js: global  
// Web Worker: self

// ES2020 统一写法
console.log(globalThis);             // 在任何环境都指向全局对象

// 实际应用 - 环境检测
const isNode = typeof globalThis.process !== 'undefined';
const isBrowser = typeof globalThis.window !== 'undefined';

if (isBrowser) {
  console.log('运行在浏览器中');
} else if (isNode) {
  console.log('运行在Node.js中');
}
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的新特性


```
🔸 简写对象：属性简写、方法简写、计算属性名
🔸 箭头函数：语法简洁、this指向固定、适用场景
🔸 Map/Set：新的数据结构、自动去重、任意类型键
🔸 可选链：安全访问嵌套属性，避免报错
🔸 空值合并：精确的默认值处理
```

### 6.2 实际开发建议


**🔹 什么时候用箭头函数**
```javascript
✅ 数组方法回调
const doubled = numbers.map(x => x * 2);

✅ 事件处理（需要保持this）
button.addEventListener('click', () => this.handleClick());

❌ 对象方法
const obj = {
  name: 'Tom',
  getName: () => this.name  // 错误：this不指向obj
};
```

**🔹 Map vs 对象的选择**
```javascript
// 使用Map的场景：
✅ 键不是字符串
✅ 需要频繁添加删除
✅ 需要遍历所有键值

// 使用对象的场景：
✅ 作为配置或选项
✅ 需要JSON序列化
✅ 需要属性访问语法
```

**🔹 现代JavaScript代码风格**
```javascript
// 现代写法示例
const createAPI = (baseURL, timeout = 5000) => ({
  baseURL,
  timeout,
  
  async get(url, params = {}) {
    const response = await fetch(`${this.baseURL}${url}`);
    return response?.json?.() ?? null;
  },
  
  post(url, data) {
    return fetch(`${this.baseURL}${url}`, {
      method: 'POST',
      body: JSON.stringify(data)
    });
  }
});
```

### 6.3 浏览器兼容性提醒


```
ES6基础特性 (2015)：现代浏览器全面支持
ES2017 (async/await)：IE不支持，需要Babel转换  
ES2020 (可选链、空值合并)：较新特性，注意兼容性
ES2021+ (数值分隔符等)：最新特性，按需使用
```
