---
title: 4、模板字符串和解构
---
## 📚 目录

1. [模板字符串基础](#1-模板字符串基础)
2. [变量插入与表达式](#2-变量插入与表达式)
3. [数组解构赋值](#3-数组解构赋值)
4. [对象解构赋值](#4-对象解构赋值)
5. [默认值与重命名](#5-默认值与重命名)
6. [实用技巧总结](#6-实用技巧总结)

---

## 1. 🎯 模板字符串基础


### 1.1 什么是模板字符串


**🔸 核心概念**
```
模板字符串：用反引号(``)包围的字符串，支持变量插入和多行文本
目的：让字符串拼接更简单、更直观
替代：传统的字符串拼接方式
```

**💡 基本语法对比**
```javascript
// 老式写法：麻烦的字符串拼接
let name = "张三";
let age = 25;
let message = "你好，我是" + name + "，今年" + age + "岁";

// 新式写法：模板字符串
let message = `你好，我是${name}，今年${age}岁`;
```

### 1.2 模板字符串的优势


**📊 对比分析**

| 特性 | **传统字符串** | **模板字符串** |
|------|-------------|-------------|
| **语法** | `"字符串" + 变量` | `` `字符串${变量}` `` |
| **可读性** | `差，容易出错` | `好，直观清晰` |
| **多行支持** | `需要\n或拼接` | `直接换行` |
| **表达式** | `需要先计算` | `直接写在${}内` |

---

## 2. 🔧 变量插入与表达式


### 2.1 基本变量插入


**🔸 语法格式**
```javascript
// ${变量名} - 插入变量值
let userName = "小明";
let score = 85;

let result = `${userName}同学的成绩是${score}分`;
console.log(result); // 小明同学的成绩是85分
```

### 2.2 表达式计算


**💡 支持任何JavaScript表达式**
```javascript
let price = 100;
let quantity = 3;

// 可以进行计算
let total = `总价：${price * quantity}元`;

// 可以调用函数
let discount = `折后价：${(price * 0.8).toFixed(2)}元`;

// 可以使用三元运算符
let level = `等级：${score >= 90 ? '优秀' : '良好'}`;
```

### 2.3 多行文本处理


**🌟 多行字符串的优势**
```javascript
// 老式写法：麻烦
let html = '<div class="card">' +
           '  <h2>标题</h2>' +
           '  <p>内容</p>' +
           '</div>';

// 模板字符串：直观
let html = `
<div class="card">
  <h2>标题</h2>
  <p>内容</p>
</div>
`;
```

**⚡ 实际应用场景**
```javascript
// 生成HTML模板
function createUserCard(user) {
  return `
    <div class="user-card">
      <img src="${user.avatar}" alt="${user.name}">
      <h3>${user.name}</h3>
      <p>年龄：${user.age}岁</p>
      <p>邮箱：${user.email}</p>
    </div>
  `;
}
```

---

## 3. 📋 数组解构赋值


### 3.1 什么是解构赋值


**🔸 核心概念**
```
解构赋值：从数组或对象中快速提取值，赋给变量
作用：让数据提取更简单、代码更简洁
原理：按位置(数组)或属性名(对象)进行匹配
```

### 3.2 数组解构基础语法


**💡 基本用法**
```javascript
// 老式写法：一个个取值
let arr = ['苹果', '香蕉', '橙子'];
let first = arr[0];
let second = arr[1];
let third = arr[2];

// 解构写法：一次性取值
let [first, second, third] = ['苹果', '香蕉', '橙子'];
console.log(first);  // 苹果
console.log(second); // 香蕉
console.log(third);  // 橙子
```

### 3.3 数组解构实用技巧


**🎯 跳过元素**
```javascript
let colors = ['红', '绿', '蓝', '黄'];

// 只要第1个和第3个
let [first, , third] = colors;
console.log(first, third); // 红 蓝
```

**🔄 交换变量**
```javascript
let a = 1;
let b = 2;

// 老式写法：需要临时变量
let temp = a;
a = b;
b = temp;

// 解构写法：一行搞定
[a, b] = [b, a];
console.log(a, b); // 2 1
```

**📦 剩余元素**
```javascript
let numbers = [1, 2, 3, 4, 5];

// 第一个单独取，剩下的放在新数组里
let [first, ...rest] = numbers;
console.log(first); // 1
console.log(rest);  // [2, 3, 4, 5]
```

---

## 4. 🎲 对象解构赋值


### 4.1 对象解构基础


**🔸 按属性名提取**
```javascript
let user = {
  name: '张三',
  age: 25,
  city: '北京'
};

// 老式写法
let name = user.name;
let age = user.age;
let city = user.city;

// 解构写法：属性名要一致
let {name, age, city} = user;
console.log(name, age, city); // 张三 25 北京
```

### 4.2 对象解构实用场景


**📱 函数参数解构**
```javascript
// 处理用户信息的函数
function showUserInfo({name, age, email}) {
  console.log(`姓名：${name}`);
  console.log(`年龄：${age}`);
  console.log(`邮箱：${email}`);
}

// 调用时直接传对象
let user = {name: '李四', age: 30, email: 'lisi@qq.com'};
showUserInfo(user);
```

**🎯 从API数据中提取**
```javascript
// 模拟API返回的数据
let apiResponse = {
  status: 200,
  data: {
    id: 1,
    title: '文章标题',
    content: '文章内容'
  },
  message: '获取成功'
};

// 直接提取需要的数据
let {status, data: {title, content}} = apiResponse;
console.log(status, title, content);
```

### 4.3 嵌套对象解构


**🔗 多层数据提取**
```javascript
let student = {
  name: '王五',
  course: {
    name: 'JavaScript',
    teacher: '张老师',
    score: {
      midterm: 85,
      final: 92
    }
  }
};

// 提取嵌套的数据
let {
  name,
  course: {
    name: courseName,
    score: {final}
  }
} = student;

console.log(name, courseName, final); // 王五 JavaScript 92
```

---

## 5. ⚙️ 默认值与重命名


### 5.1 设置默认值


**🔸 数组解构默认值**
```javascript
// 数组长度不够时的默认值
let [a, b, c = '默认值'] = ['第一个', '第二个'];
console.log(a, b, c); // 第一个 第二个 默认值

// 实际应用：处理配置项
let [host = 'localhost', port = 3000] = ['192.168.1.1'];
console.log(host, port); // 192.168.1.1 3000
```

**🔸 对象解构默认值**
```javascript
let user = {name: '张三'};

// 属性不存在时使用默认值
let {name, age = 18, city = '未知'} = user;
console.log(name, age, city); // 张三 18 未知
```

### 5.2 变量重命名


**🏷️ 对象属性重命名**
```javascript
let userInfo = {
  n: '李四',  // 简写的属性名
  a: 28,
  e: 'lisi@email.com'
};

// 重命名为更清晰的变量名
let {n: name, a: age, e: email} = userInfo;
console.log(name, age, email); // 李四 28 lisi@email.com
```

**🎯 实际应用场景**
```javascript
// API返回的数据字段名不友好
let apiData = {
  usr_nm: '用户名',
  usr_age: 25,
  usr_sts: 'active'
};

// 重命名为更好理解的变量名
let {
  usr_nm: userName,
  usr_age: userAge,
  usr_sts: userStatus = 'inactive'  // 重命名+默认值
} = apiData;
```

---

## 6. 💡 实用技巧总结


### 6.1 模板字符串最佳实践


**✅ 推荐用法**
```javascript
// 1. 字符串拼接
let message = `欢迎 ${user.name}，当前时间：${new Date().toLocaleString()}`;

// 2. 生成HTML
let createButton = (text, type) => `
  <button class="btn btn-${type}">
    ${text}
  </button>
`;

// 3. 格式化输出
let formatPrice = (price) => `￥${price.toFixed(2)}`;
```

**❌ 避免的用法**
```javascript
// 不要在简单字符串中使用
let simple = `hello`;  // 直接用 'hello' 就行

// 不要嵌套太深
let complex = `${`${user.name}`}`;  // 过度使用
```

### 6.2 解构赋值最佳实践


**🎯 实用场景总结**
```javascript
// 1. 函数返回多个值
function getWindowSize() {
  return [window.innerWidth, window.innerHeight];
}
let [width, height] = getWindowSize();

// 2. 快速配置对象
let config = {
  host: 'localhost',
  port: 3000,
  debug: true
};
let {host, port, debug = false} = config;

// 3. 数组处理
let numbers = [1, 2, 3, 4, 5];
let [first, second, ...others] = numbers;

// 4. 从复杂数据中提取
let response = {
  data: {users: [{name: 'John', id: 1}]},
  status: 'success'
};
let {data: {users: [firstUser]}} = response;
```

### 6.3 关键要点记忆


```
🔸 模板字符串用反引号(``)，变量用${}包围
🔸 解构按位置(数组)或属性名(对象)进行匹配
🔸 可以设置默认值，防止undefined
🔸 对象解构可以重命名变量
🔸 用解构让代码更简洁，但不要过度使用
```

### 6.4 常见错误避免


**⚠️ 注意事项**
```javascript
// 1. 对象解构变量名必须和属性名一致
let {userName} = {name: '张三'};  // ❌ undefined
let {name} = {name: '张三'};      // ✅ 正确

// 2. 数组解构按顺序，不能跳序号
let [a, c] = [1, 2, 3];  // c是2，不是3

// 3. 模板字符串的${}不能省略
let name = 'Tom';
let msg = `Hello name`;    // ❌ Hello name
let msg = `Hello ${name}`; // ✅ Hello Tom
```

**核心记忆**：
- 模板字符串让字符串拼接像说话一样自然
- 解构赋值让数据提取像拆包裹一样简单
- 都是为了让代码更简洁、更易读的语法糖