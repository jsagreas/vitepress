---
title: 5、扩展运算符和模块
---
## 📚 目录

1. [扩展运算符到底是什么](#1-扩展运算符到底是什么)
2. [数组的展开和复制](#2-数组的展开和复制)
3. [对象的展开和合并](#3-对象的展开和合并)
4. [剩余参数的作用](#4-剩余参数的作用)
5. [模块化基础概念](#5-模块化基础概念)
6. [导出和导入的使用](#6-导出和导入的使用)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🔍 扩展运算符到底是什么


### 1.1 什么是扩展运算符


**扩展运算符就是三个点 `...`**，它的作用很简单：**把一个整体拆开，变成一个一个的单独部分**。

```javascript
// 数组拆开
const arr = [1, 2, 3];
console.log(arr);     // 输出：[1, 2, 3] (整个数组)
console.log(...arr);  // 输出：1 2 3 (拆开的单个数字)
```

**🤔 为什么要拆开？**
很多时候我们需要把数组里的元素一个一个传给函数，或者把两个数组合并，扩展运算符就是专门干这个的。

### 1.2 最简单的理解方式


**把扩展运算符想象成"拆包裹"**：
```
有个包裹：[苹果, 香蕉, 橙子]
拆包裹：...包裹 = 苹果, 香蕉, 橙子
```

```javascript
// 函数需要3个参数
function buyFruit(fruit1, fruit2, fruit3) {
    console.log(`买了${fruit1}、${fruit2}、${fruit3}`);
}

const fruits = ['苹果', '香蕉', '橙子'];

// 不用扩展运算符（麻烦）
buyFruit(fruits[0], fruits[1], fruits[2]);

// 用扩展运算符（简单）
buyFruit(...fruits);  // 自动拆开传参
```

---

## 2. 📋 数组的展开和复制


### 2.1 复制数组


**问题：为什么要复制数组？**
因为直接赋值只是复制了地址，修改新数组会影响原数组。

```javascript
// 错误的复制方式
const original = [1, 2, 3];
const wrong = original;  // 这不是复制，只是指向同一个数组
wrong.push(4);
console.log(original);  // [1, 2, 3, 4] 原数组也被修改了！

// 正确的复制方式
const correct = [...original];  // 这才是真正的复制
correct.push(5);
console.log(original);  // [1, 2, 3] 原数组不受影响
```

**为什么扩展运算符能复制？**
`...original` 把数组拆成 `1, 2, 3`，然后 `[1, 2, 3]` 创建了一个新数组。

### 2.2 合并数组


```javascript
const part1 = [1, 2];
const part2 = [3, 4];
const part3 = [5, 6];

// 老方法（麻烦）
const merged1 = part1.concat(part2).concat(part3);

// 新方法（简单）
const merged2 = [...part1, ...part2, ...part3];
console.log(merged2);  // [1, 2, 3, 4, 5, 6]

// 还能在中间插入
const result = [...part1, 999, ...part2];
console.log(result);  // [1, 2, 999, 3, 4]
```

### 2.3 实用技巧


```javascript
// 找数组最大值
const numbers = [5, 1, 9, 3];
const max = Math.max(...numbers);  // 9
// 相当于：Math.max(5, 1, 9, 3)

// 数组去重
const arr = [1, 2, 2, 3, 3];
const unique = [...new Set(arr)];  // [1, 2, 3]
```

---

## 3. 🏗️ 对象的展开和合并


### 3.1 复制对象


**对象复制的原理和数组一样**：

```javascript
const person = {name: '张三', age: 25};

// 错误复制
const wrong = person;  // 还是指向同一个对象
wrong.age = 30;
console.log(person.age);  // 30，原对象被修改了

// 正确复制
const correct = {...person};  // 创建新对象
correct.age = 35;
console.log(person.age);  // 25，原对象不变
```

### 3.2 合并对象


**后面的属性会覆盖前面的**：

```javascript
const basic = {name: '李四', age: 30};
const extra = {city: '北京', age: 25};  // 注意：age重复了

const result = {...basic, ...extra};
console.log(result);
// {name: '李四', age: 25, city: '北京'}
// age变成25了，因为extra在后面
```

### 3.3 常用场景


**更新对象属性**：
```javascript
const user = {id: 1, name: '王五', email: 'old@qq.com'};

// 只修改email，其他保持不变
const updated = {...user, email: 'new@qq.com'};
console.log(updated);
// {id: 1, name: '王五', email: 'new@qq.com'}
```

**设置默认值**：
```javascript
const defaults = {color: 'red', size: 'medium'};
const custom = {color: 'blue'};  // 只指定了color

const final = {...defaults, ...custom};
console.log(final);
// {color: 'blue', size: 'medium'}
// color用自定义的，size用默认的
```

---

## 4. 🎪 剩余参数的作用


### 4.1 剩余参数是什么


**剩余参数和扩展运算符正好相反**：
- **扩展运算符**：把数组拆开 `[1,2,3]` → `1,2,3`
- **剩余参数**：把零散的收集起来 `1,2,3` → `[1,2,3]`

```javascript
// 不知道会传几个参数的函数
function sum(...numbers) {  // numbers就是剩余参数
    console.log(numbers);  // 自动变成数组
    let total = 0;
    for (let num of numbers) {
        total += num;
    }
    return total;
}

sum(1, 2, 3);        // numbers = [1, 2, 3]
sum(1, 2, 3, 4, 5);  // numbers = [1, 2, 3, 4, 5]
```

### 4.2 混合使用


```javascript
function greet(first, ...others) {
    console.log(`主要打招呼：${first}`);
    if (others.length > 0) {
        console.log(`顺便打招呼：${others.join(', ')}`);
    }
}

greet('张三');  
// 主要打招呼：张三

greet('张三', '李四', '王五');
// 主要打招呼：张三
// 顺便打招呼：李四, 王五
```

### 4.3 数组解构中的剩余参数


```javascript
const [first, ...rest] = [1, 2, 3, 4, 5];
console.log(first);  // 1
console.log(rest);   // [2, 3, 4, 5]

// 对象解构
const {name, ...otherInfo} = {
    name: '张三',
    age: 25,
    city: '北京'
};
console.log(name);       // 张三
console.log(otherInfo);  // {age: 25, city: '北京'}
```

---

## 5. 📦 模块化基础概念


### 5.1 为什么需要模块化


**想象你在整理房间**：
- **不模块化**：所有东西都堆在一个房间里，乱糟糟
- **模块化**：衣服放衣柜，书放书架，工具放工具箱

```javascript
// 不模块化的代码（一个文件几千行）
// 所有函数都混在一起，变量可能重名，难以维护

// 模块化的代码
// utils.js - 工具函数
// user.js - 用户相关
// product.js - 商品相关
// 每个文件职责清晰，便于维护
```

### 5.2 模块的作用域


**每个模块都有自己的"私人空间"**：

```javascript
// math.js 文件
const secret = '这是私有的';  // 外部访问不到
function add(a, b) {
    return a + b;
}

export { add };  // 只有export的才能被外部使用
```

```javascript
// main.js 文件
import { add } from './math.js';
console.log(add(1, 2));  // 可以使用
console.log(secret);     // 报错：secret未定义
```

**核心理解**：模块就像一个有门锁的房间，只有通过export"开门"，外人才能进来拿东西。

---

## 6. 🚀 导出和导入的使用


### 6.1 export导出的两种方式


**🔸 命名导出（常用）**
```javascript
// utils.js
export function formatDate(date) {
    return date.toLocaleDateString();
}

export function isEmail(email) {
    return email.includes('@');
}

export const MAX_SIZE = 100;

// 或者统一导出
function helper1() { return 'help1'; }
function helper2() { return 'help2'; }
export { helper1, helper2 };
```

**🔸 默认导出（一个文件只能有一个）**
```javascript
// Calculator.js
class Calculator {
    add(a, b) { return a + b; }
    subtract(a, b) { return a - b; }
}

export default Calculator;
```

### 6.2 import导入的方式


**🔸 导入命名导出**
```javascript
// 准确导入需要的函数
import { formatDate, isEmail } from './utils.js';

// 重命名导入
import { formatDate as format } from './utils.js';

// 导入全部（不推荐，除非真的都要用）
import * as utils from './utils.js';
utils.formatDate(new Date());
```

**🔸 导入默认导出**
```javascript
// 可以起任意名字
import Calculator from './Calculator.js';
import MyCalculator from './Calculator.js';  // 叫什么都行

const calc = new Calculator();
console.log(calc.add(1, 2));
```

### 6.3 实际使用建议


**什么时候用命名导出？**
- 一个文件有多个函数或变量要导出
- 工具函数、配置常量等

**什么时候用默认导出？**
- 一个文件主要就是一个东西（类、主函数等）
- React组件通常用默认导出

```javascript
// ✅ 推荐：工具函数用命名导出
// utils.js
export function formatDate() { /* ... */ }
export function formatTime() { /* ... */ }

// ✅ 推荐：主要的类用默认导出
// User.js
export default class User { /* ... */ }
```

### 6.4 简单的项目结构


```
src/
├── utils/
│   └── date.js     ← export { formatDate, addDays }
├── components/
│   └── Button.js   ← export default Button
└── main.js         ← import各种需要的东西
```

```javascript
// main.js
import { formatDate } from './utils/date.js';
import Button from './components/Button.js';

console.log(formatDate(new Date()));
const btn = new Button();
```

---

## 7. 📋 核心要点总结


### 7.1 扩展运算符 vs 剩余参数


```
┌─────────────────────────────────────────────────────┐
│                   三个点 ...                        │
├─────────────────────────────────────────────────────┤
│  扩展运算符              │        剩余参数           │
│  (Spread Operator)      │    (Rest Parameters)     │
├─────────────────────────┼─────────────────────────┤
│  把整体拆开              │      把零散收集起来        │
│  [1,2,3] → 1,2,3        │      1,2,3 → [1,2,3]     │
│                         │                          │
│  用于：                  │      用于：               │
│  • 复制数组/对象         │      • 函数参数           │
│  • 合并数组/对象         │      • 数组解构           │
│  • 函数调用传参          │      • 对象解构           │
└─────────────────────────┴─────────────────────────┘
```

### 7.2 export vs import


| **导出类型** | **语法** | **导入语法** | **特点** |
|-------------|---------|-------------|---------|
| **命名导出** | `export { func1, func2 }` | `import { func1 } from './module.js'` | 可以多个，名字固定 |
| **默认导出** | `export default MyClass` | `import MyClass from './module.js'` | 只能一个，名字随意 |

### 7.3 最常用的场景


**🎯 日常开发中90%的使用场景**：

```javascript
// 1. 复制数组
const newArr = [...oldArr];

// 2. 合并数组
const combined = [...arr1, ...arr2];

// 3. 复制对象
const newObj = {...oldObj};

// 4. 更新对象
const updated = {...user, name: '新名字'};

// 5. 函数不定参数
function sum(...numbers) { /* ... */ }

// 6. 导出工具函数
export { formatDate, isEmail };

// 7. 导入需要的函数
import { formatDate } from './utils.js';
```

**💡 记忆口诀**：
```
扩展运算符三个点，拆开合并最方便
剩余参数也三点，收集参数变数组
export导出给人用，import导入自己用
模块分离职责清，代码维护更轻松
```

**核心理解**：
- `...` 的作用就是"拆"和"装"
- 模块化就是"分房间整理东西"
- export/import就是"房间之间传递东西"
- 这些都是为了让代码更好维护