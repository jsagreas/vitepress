---
title: 1、数组的创建和访问
---
## 📚 目录

1. [什么是数组](#1-什么是数组)
2. [数组的创建方法](#2-数组的创建方法)
3. [访问数组元素](#3-访问数组元素)
4. [数组长度操作](#4-数组长度操作)
5. [修改数组元素](#5-修改数组元素)
6. [数组的核心特点](#6-数组的核心特点)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🎯 什么是数组


### 1.1 数组的本质

**简单理解**：数组就是一个**装多个数据的盒子**，每个数据都有自己的**位置编号**

```
现实生活中的例子：
停车场：[奔驰, 宝马, 奥迪, 本田, 丰田]
        0号   1号   2号   3号   4号位

编程中的数组：
let cars = ['奔驰', '宝马', '奥迪', '本田', '丰田'];
           0      1      2      3      4     (索引)
```

### 1.2 为什么需要数组

```javascript
// ❌ 没有数组时：要存储多个学生成绩很麻烦
let score1 = 85;
let score2 = 92;
let score3 = 78;
let score4 = 96;
// ... 想象一下100个学生！

// ✅ 有了数组：一个变量搞定
let scores = [85, 92, 78, 96];
```

---

## 2. 📝 数组的创建方法


### 2.1 最常用：字面量创建

```javascript
// 空数组
let arr = [];

// 有初始值的数组
let fruits = ['苹果', '香蕉', '橘子'];
let numbers = [1, 2, 3, 4, 5];
let mixed = ['张三', 25, true, null]; // 可以混合不同类型
```

### 2.2 其他创建方式

```javascript
// 构造函数创建（了解即可，实际很少用）
let arr1 = new Array();         // 空数组
let arr2 = new Array(3);        // 长度为3的空数组
let arr3 = new Array(1, 2, 3);  // [1, 2, 3]

// 💡 推荐：永远使用 [] 创建数组，简单直接！
```

### 2.3 实际开发场景

```javascript
// 购物车商品
let cart = ['iPhone', '耳机', '充电器'];

// 用户权限
let permissions = ['read', 'write', 'delete'];

// 考试成绩
let scores = [88, 92, 76, 95, 89];
```

---

## 3. 🔍 访问数组元素


### 3.1 索引访问（核心概念）

```
重要：数组索引从 0 开始计数！

数组： ['苹果', '香蕉', '橘子', '葡萄']
索引：    0      1      2      3
```

### 3.2 基本访问语法

```javascript
let fruits = ['苹果', '香蕉', '橘子', '葡萄'];

// 获取元素
console.log(fruits[0]);  // '苹果' (第1个)
console.log(fruits[1]);  // '香蕉' (第2个)
console.log(fruits[3]);  // '葡萄' (第4个)

// 访问不存在的索引
console.log(fruits[10]); // undefined (没有第11个)
```

### 3.3 动态访问

```javascript
let students = ['小明', '小红', '小刚'];

// 使用变量作为索引
let index = 1;
console.log(students[index]); // '小红'

// 获取最后一个元素（常用技巧）
let lastIndex = students.length - 1;
console.log(students[lastIndex]); // '小刚'
```

### 3.4 实际应用场景

```javascript
// 轮播图片切换
let images = ['1.jpg', '2.jpg', '3.jpg'];
let currentIndex = 0;
console.log('当前图片：' + images[currentIndex]);

// 菜单选择
let menus = ['首页', '产品', '关于我们', '联系我们'];
let selectedMenu = menus[2]; // '关于我们'
```

---

## 4. 📏 数组长度操作


### 4.1 获取数组长度

```javascript
let colors = ['红', '绿', '蓝'];

console.log(colors.length); // 3
console.log([].length);     // 0 (空数组长度为0)
```

### 4.2 长度的实际用途

```javascript
let scores = [85, 92, 78, 96, 88];

// 计算平均分
let total = 0;
for (let i = 0; i < scores.length; i++) {
    total += scores[i];
}
let average = total / scores.length;
console.log('平均分：' + average);

// 检查数组是否为空
if (scores.length > 0) {
    console.log('有考试成绩');
} else {
    console.log('还没有考试成绩');
}
```

### 4.3 通过length获取最后一个元素

```javascript
let fruits = ['苹果', '香蕉', '橘子'];

// 常用技巧：获取最后一个元素
let lastFruit = fruits[fruits.length - 1];
console.log(lastFruit); // '橘子'

// 为什么是 length - 1？
// 因为索引从0开始，长度为3的数组，最后一个索引是2
```

---

## 5. ✏️ 修改数组元素


### 5.1 直接赋值修改

```javascript
let fruits = ['苹果', '香蕉', '橘子'];

// 修改指定位置的元素
fruits[1] = '草莓';
console.log(fruits); // ['苹果', '草莓', '橘子']

// 在末尾添加元素（不推荐这种方式）
fruits[3] = '葡萄';
console.log(fruits); // ['苹果', '草莓', '橘子', '葡萄']
```

### 5.2 实际应用场景

```javascript
// 用户信息更新
let userInfo = ['张三', '18岁', '学生'];
userInfo[1] = '19岁'; // 生日了，年龄加1
console.log(userInfo); // ['张三', '19岁', '学生']

// 购物车商品替换
let cart = ['iPhone', '耳机', '充电器'];
cart[0] = 'iPad'; // 改变主意了
console.log(cart); // ['iPad', '耳机', '充电器']
```

### 5.3 批量修改

```javascript
let scores = [60, 70, 80];

// 所有成绩加10分（奖励分）
for (let i = 0; i < scores.length; i++) {
    scores[i] += 10;
}
console.log(scores); // [70, 80, 90]
```

---

## 6. 🔧 数组的核心特点


### 6.1 重要特性总结

```javascript
// ✅ 特性1：索引从0开始
let arr = ['a', 'b', 'c'];
console.log(arr[0]); // 'a' (第一个)

// ✅ 特性2：可以存储不同类型的数据
let mixed = ['文字', 123, true, null];

// ✅ 特性3：长度可以动态变化
let list = [];
list[0] = '第一个';
list[1] = '第二个';
console.log(list.length); // 2

// ✅ 特性4：length属性实时反映数组大小
let numbers = [1, 2, 3];
numbers[10] = 100;
console.log(numbers.length); // 11 (中间的位置会是undefined)
```

### 6.2 数组 vs 对象

```
数组的数据结构：
┌─────┬─────┬─────┬─────┐
│  0  │  1  │  2  │  3  │  ← 索引（数字）
├─────┼─────┼─────┼─────┤
│苹果 │香蕉 │橘子 │葡萄 │  ← 数据
└─────┴─────┴─────┴─────┘

对象的数据结构：
┌────────┬────────┬────────┐
│  name  │  age   │  city  │  ← 键名（字符串）
├────────┼────────┼────────┤
│  张三  │   25   │  北京  │  ← 数据
└────────┴────────┴────────┘

选择原则：
• 数组：存储一组相同类型的数据（如：成绩列表、商品列表）
• 对象：存储一个事物的多个属性（如：用户信息、商品详情）
```

### 6.3 常见误区

```javascript
// ❌ 误区1：认为数组索引从1开始
let arr = ['a', 'b', 'c'];
console.log(arr[1]); // 这是第2个元素，不是第1个！

// ❌ 误区2：直接用length添加元素
let fruits = ['苹果'];
fruits[fruits.length] = '香蕉'; // 虽然可以，但不推荐
// ✅ 推荐用 push() 方法（下一节会学）

// ❌ 误区3：数组一定要连续
let sparse = [];
sparse[0] = 'first';
sparse[5] = 'sixth';
console.log(sparse.length); // 6，但中间都是undefined
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的基本概念

```javascript
// 🔸 创建数组：使用 [] 最简单
let arr = ['元素1', '元素2', '元素3'];

// 🔸 访问元素：索引从0开始
console.log(arr[0]); // 第一个元素

// 🔸 获取长度：使用 .length
console.log(arr.length); // 3

// 🔸 修改元素：直接赋值
arr[0] = '新元素';

// 🔸 获取最后一个：length - 1
let last = arr[arr.length - 1];
```

### 7.2 记忆口诀

```
数组创建用中括号，
索引访问从零开始，
长度属性点length，
修改赋值很简单。
```

### 7.3 实际应用价值

- **数据列表**：商品列表、用户列表、菜单项
- **批量处理**：所有元素执行相同操作
- **顺序存储**：保持数据的插入顺序
- **动态集合**：可以随时增删元素


**核心记忆**：
- 数组是装多个数据的盒子，每个数据有编号
- 编号从0开始，用方括号访问
- length属性告诉你盒子里有多少个数据
- 可以随时修改盒子里的任何数据