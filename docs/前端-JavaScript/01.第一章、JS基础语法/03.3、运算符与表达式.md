---
title: 3、运算符与表达式
---
## 📚 目录

1. [运算符概述](#1-运算符概述)
2. [算术运算符](#2-算术运算符)
3. [赋值运算符](#3-赋值运算符)
4. [比较运算符](#4-比较运算符)
5. [逻辑运算符](#5-逻辑运算符)
6. [自增自减运算符](#6-自增自减运算符)
7. [三元运算符](#7-三元运算符)
8. [运算符优先级](#8-运算符优先级)
9. [实际应用与技巧](#9-实际应用与技巧)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🎯 运算符概述


### 1.1 什么是运算符

**简单理解**：运算符就是用来对数据进行操作的符号

```javascript
// 就像数学里的加减乘除
let a = 5 + 3;  // + 就是运算符
let b = x > 10; // > 也是运算符
```

### 1.2 运算符的分类

```
数学计算：+  -  *  /  %
数据赋值：=  +=  -=  *=
大小比较：>  <  >=  <=  ==  ===
逻辑判断：&&  ||  !
特殊操作：++  --  ? :
```

**💡 核心作用**：
- **算术运算符** → 做数学计算
- **赋值运算符** → 给变量存值
- **比较运算符** → 判断大小相等
- **逻辑运算符** → 组合多个条件

---

## 2. ➕ 算术运算符


### 2.1 基本算术运算

```javascript
let a = 10;
let b = 3;

console.log(a + b);  // 13  加法
console.log(a - b);  // 7   减法
console.log(a * b);  // 30  乘法
console.log(a / b);  // 3.333... 除法
console.log(a % b);  // 1   取余数（求模）
```

### 2.2 取余运算的实用性

```javascript
// 判断奇偶数
let num = 7;
if (num % 2 === 0) {
    console.log("偶数");
} else {
    console.log("奇数");  // 输出这个
}

// 循环显示（轮播图常用）
let index = 0;
let maxIndex = 3;
index = (index + 1) % maxIndex;  // 0,1,2,0,1,2...循环
```

### 2.3 字符串拼接特殊情况

```javascript
// + 号的双重身份
console.log(5 + 3);      // 8    数字相加
console.log("5" + 3);    // "53" 字符串拼接
console.log("hello" + " world");  // "hello world"

// 实用技巧：数字转字符串
let age = 25;
let message = "我今年" + age + "岁";  // "我今年25岁"
```

**⚠️ 常见陷阱**：
```javascript
console.log("10" - 5);   // 5    自动转数字
console.log("10" + 5);   // "105" 字符串拼接
console.log("10" * 5);   // 50   自动转数字
```

---

## 3. 📝 赋值运算符


### 3.1 基本赋值

```javascript
let score = 85;        // 基本赋值
let name = "张三";     // 给变量存值
let isActive = true;   // 布尔值赋值
```

### 3.2 复合赋值运算符

```javascript
let count = 10;

// 传统写法 vs 简化写法
count = count + 5;     // 传统
count += 5;            // 简化，效果一样

count -= 3;            // count = count - 3
count *= 2;            // count = count * 2
count /= 4;            // count = count / 4
count %= 3;            // count = count % 3
```

### 3.3 实际应用场景

```javascript
// 购物车商品数量
let cartCount = 0;
cartCount += 1;        // 添加商品
cartCount -= 1;        // 删除商品

// 游戏分数累加
let score = 0;
score += 100;          // 得分
score *= 2;            // 双倍分数

// 字符串拼接
let message = "Hello";
message += " World";   // "Hello World"
```

---

## 4. ⚖️ 比较运算符


### 4.1 大小比较

```javascript
let a = 10;
let b = 5;

console.log(a > b);    // true  大于
console.log(a < b);    // false 小于
console.log(a >= 10);  // true  大于等于
console.log(b <= 5);   // true  小于等于
```

### 4.2 相等比较（重点！）

```javascript
// == vs === 的区别（面试常考）
console.log(5 == "5");    // true  只比较值
console.log(5 === "5");   // false 比较值和类型

console.log(0 == false);  // true  
console.log(0 === false); // false

console.log(null == undefined);  // true
console.log(null === undefined); // false
```

### 4.3 不等比较

```javascript
console.log(5 != "5");    // false 只比较值
console.log(5 !== "5");   // true  比较值和类型

console.log(10 != 5);     // true
console.log(10 !== 5);    // true
```

### 4.4 最佳实践

```javascript
// ✅ 推荐：总是使用 === 和 !==
if (user.age === 18) {
    console.log("刚好18岁");
}

if (username !== "") {
    console.log("用户名不为空");
}

// ❌ 避免：除非特殊需要，少用 == 和 !=
```

---

## 5. 🔗 逻辑运算符


### 5.1 AND运算符 (&&)

```javascript
// 所有条件都为真时，结果才为真
let age = 20;
let hasLicense = true;

if (age >= 18 && hasLicense) {
    console.log("可以开车");  // 两个条件都满足
}

// 实用场景：表单验证
let username = "admin";
let password = "123456";
if (username !== "" && password.length >= 6) {
    console.log("登录成功");
}
```

### 5.2 OR运算符 (||)

```javascript
// 任意一个条件为真，结果就为真
let isWeekend = true;
let isHoliday = false;

if (isWeekend || isHoliday) {
    console.log("可以休息");  // 其中一个为true即可
}

// 设置默认值（常用技巧）
function greet(name) {
    name = name || "游客";  // 如果name为空，使用"游客"
    console.log("你好，" + name);
}
```

### 5.3 NOT运算符 (!)

```javascript
let isLoggedIn = false;

if (!isLoggedIn) {
    console.log("请先登录");  // 取反：false变true
}

// 双重否定转布尔值
let value = "hello";
console.log(!!value);      // true（非空字符串）

let empty = "";
console.log(!!empty);      // false（空字符串）
```

### 5.4 真值和假值概念

```javascript
// JavaScript中的假值（只有这6个）
console.log(!false);       // true
console.log(!0);           // true
console.log(!"");          // true
console.log(!null);        // true
console.log(!undefined);   // true
console.log(!NaN);         // true

// 其他都是真值
console.log(!"hello");     // false
console.log(!42);          // false
console.log(!{});          // false（空对象也是真值）
console.log(![]);          // false（空数组也是真值）
```

---

## 6. 📈 自增自减运算符


### 6.1 基本用法

```javascript
let count = 5;

// 后置：先使用，再改变
console.log(count++);  // 输出5，然后count变成6
console.log(count);    // 输出6

// 前置：先改变，再使用
count = 5;
console.log(++count);  // count先变成6，然后输出6
console.log(count);    // 输出6
```

### 6.2 实际应用场景

```javascript
// 循环计数器
for (let i = 0; i < 5; i++) {
    console.log(i);  // 0,1,2,3,4
}

// 数组索引递增
let colors = ["红", "绿", "蓝"];
let index = 0;
console.log(colors[index++]);  // "红"，然后index变成1

// 点击次数统计
let clickCount = 0;
function onClick() {
    clickCount++;
    console.log("点击了" + clickCount + "次");
}
```

### 6.3 常见陷阱

```javascript
let a = 5;
let b = a++ + ++a;  // 复杂表达式，避免使用

// 清晰的写法
let a = 5;
let temp1 = a++;    // a先给temp1(5)，然后a变成6
let temp2 = ++a;    // a先变成7，然后给temp2
let b = temp1 + temp2;  // 5 + 7 = 12
```

---

## 7. ❓ 三元运算符


### 7.1 基本语法

```javascript
// 语法：条件 ? 真值 : 假值
let age = 20;
let status = age >= 18 ? "成年人" : "未成年";
console.log(status);  // "成年人"

// 等价于if-else
let status2;
if (age >= 18) {
    status2 = "成年人";
} else {
    status2 = "未成年";
}
```

### 7.2 实用场景

```javascript
// 设置CSS类名
let score = 85;
let className = score >= 60 ? "pass" : "fail";

// 显示不同消息
let messageCount = 3;
let text = messageCount > 0 ? `有${messageCount}条消息` : "无消息";

// 函数参数默认值
function greet(name) {
    name = name ? name : "访客";  // 或者用 name || "访客"
    return "你好，" + name;
}
```

### 7.3 嵌套使用（谨慎）

```javascript
// 简单嵌套还可以
let score = 85;
let grade = score >= 90 ? "A" : score >= 80 ? "B" : "C";

// 复杂情况用if-else更清晰
let weather = "sunny";
let activity;
if (weather === "sunny") {
    activity = "去海边";
} else if (weather === "rainy") {
    activity = "在家看书";
} else {
    activity = "去咖啡厅";
}
```

---

## 8. 🎯 运算符优先级


### 8.1 常用优先级表

```
优先级（高到低）：
1. ()          括号
2. ++ --       自增自减
3. * / %       乘除取余
4. + -         加减
5. > < >= <=   比较
6. == === != !== 相等
7. &&          逻辑与
8. ||          逻辑或
9. ? :         三元运算符
10. = += -= *= /=  赋值
```

### 8.2 实际例子

```javascript
// 没有括号的计算
let result = 2 + 3 * 4;     // 14，不是20
console.log(result);        // 先算3*4=12，再算2+12=14

// 使用括号改变优先级
let result2 = (2 + 3) * 4;  // 20
console.log(result2);       // 先算(2+3)=5，再算5*4=20

// 复杂表达式
let a = 5;
let b = 3;
let c = a > b && a + b < 10;  // true
// 执行顺序：a+b → a>b → && 运算
```

### 8.3 最佳实践

```javascript
// ✅ 推荐：用括号明确优先级
let price = (basePrice + tax) * quantity;
let isValid = (age >= 18) && (hasPermission === true);

// ❌ 避免：依赖记忆优先级
let price = basePrice + tax * quantity;  // 不够清晰
```

---

## 9. 💡 实际应用与技巧


### 9.1 表单验证

```javascript
function validateForm(username, password, email) {
    // 用户名检查
    let isUsernameValid = username !== "" && username.length >= 3;
    
    // 密码检查
    let isPasswordValid = password !== "" && password.length >= 6;
    
    // 邮箱简单检查
    let isEmailValid = email !== "" && email.includes("@");
    
    // 综合判断
    return isUsernameValid && isPasswordValid && isEmailValid;
}

// 使用
let formValid = validateForm("admin", "123456", "admin@test.com");
console.log(formValid ? "表单有效" : "表单无效");
```

### 9.2 数据处理

```javascript
// 价格计算
let originalPrice = 100;
let discount = 0.8;        // 8折
let quantity = 3;

let totalPrice = originalPrice * discount * quantity;
console.log("总价：" + totalPrice);  // 240

// 分页计算
let currentPage = 1;
let pageSize = 10;
let totalItems = 95;

let totalPages = Math.ceil(totalItems / pageSize);  // 10页
let hasNextPage = currentPage < totalPages;         // true
let hasPrevPage = currentPage > 1;                  // false
```

### 9.3 常用判断模式

```javascript
// 范围判断
let age = 25;
let category = age < 18 ? "少年" : age < 60 ? "青年" : "老年";

// 空值处理
function processData(data) {
    data = data || [];  // 如果data为空，使用空数组
    return data.length > 0 ? data[0] : "无数据";
}

// 状态切换
let isShow = false;
function toggle() {
    isShow = !isShow;  // 反转状态
    return isShow ? "显示" : "隐藏";
}
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的运算符

```javascript
// 🔥 最重要的运算符（90%场景）
=   +=  -=                    // 赋值
===  !==  >  <  >=  <=       // 比较（用===，避免==）
&&  ||  !                    // 逻辑
++  --                       // 自增自减
? :                          // 三元运算符
```

### 10.2 关键理解要点


**🔹 === vs == 的选择**
```javascript
// ✅ 总是使用 === 和 !==
if (value === 0) { }
if (name !== "") { }

// ❌ 避免使用 == 和 !=（容易出错）
```

**🔹 逻辑运算的短路特性**
```javascript
// && 短路：前面为false，后面不执行
user && user.getName();  // 常用于安全调用

// || 短路：前面为true，后面不执行
name = inputName || "默认名称";  // 常用于默认值
```

**🔹 真值假值记忆**
```javascript
// 假值：只有6个
false, 0, "", null, undefined, NaN

// 其他都是真值（包括空对象{}、空数组[]）
```

### 10.3 实用开发技巧


**🔧 常用模式**
```javascript
// 1. 条件赋值
let message = isError ? "错误" : "成功";

// 2. 默认值设置  
let config = userConfig || defaultConfig;

// 3. 安全调用
user && user.updateProfile();

// 4. 数值累加
score += getBonus();

// 5. 状态切换
isVisible = !isVisible;
```

**⚠️ 常见错误**
```javascript
// 1. 赋值与比较混淆
if (a = 5) { }     // ❌ 应该是 a === 5

// 2. 运算符优先级不清楚
let result = 2 + 3 * 4;  // ❌ 应该用括号：(2 + 3) * 4

// 3. 类型转换陷阱
console.log("10" + 5);   // ❌ 结果是"105"不是15
```

**核心记忆**：
- 运算符是操作数据的工具，熟练掌握常用的即可
- === 比 == 安全，养成使用习惯
- 逻辑运算符有短路特性，可以巧妙利用
- 复杂表达式用括号，代码更清晰
- 实战中多练习，形成编程直觉