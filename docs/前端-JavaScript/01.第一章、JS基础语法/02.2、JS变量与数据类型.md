---
title: 2、JS变量与数据类型
---
## 📚 目录

1. [变量声明方式](#1-变量声明方式)
2. [JavaScript数据类型](#2-JavaScript数据类型)
3. [类型检测方法](#3-类型检测方法)
4. [类型转换](#4-类型转换)
5. [实用开发技巧](#5-实用开发技巧)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 📦 变量声明方式


### 1.1 三种声明方式对比


**🔸 基本语法**
```javascript
var name = "张三";     // 老式写法
let age = 25;          // 现代写法
const PI = 3.14;       // 常量声明
```

**📊 三者区别对比**

| 声明方式 | **作用域** | **能否重复声明** | **能否修改** | **推荐使用** |
|---------|-----------|----------------|-------------|-------------|
| `var` | `函数作用域` | `✅ 可以` | `✅ 可以` | `❌ 不推荐` |
| `let` | `块级作用域` | `❌ 不可以` | `✅ 可以` | `✅ 推荐` |
| `const` | `块级作用域` | `❌ 不可以` | `❌ 不可以` | `✅ 推荐常量` |

### 1.2 实际使用建议


**💡 最佳实践**
```javascript
// ✅ 推荐写法
const userName = "李四";        // 不变的值用 const
let userAge = 30;              // 会变的值用 let

// ❌ 避免使用
var oldStyle = "不推荐";        // 容易出现作用域问题
```

**🎯 选择原则**
```
数据不会变 → 用 const
数据会改变 → 用 let
避免使用 var → 容易出错
```

---

## 2. 🎲 JavaScript数据类型


### 2.1 数据类型总览


**📋 JavaScript的8种数据类型**
```
基本类型（7种）：
├── string      字符串
├── number      数字
├── boolean     布尔值
├── null        空值
├── undefined   未定义
├── symbol      符号（ES6新增，了解即可）
└── bigint      大整数（ES6新增，了解即可）

引用类型（1种）：
└── object      对象（包括数组、函数等）
```

### 2.2 常用基本类型详解


#### 🔤 字符串 (string)


**🔸 基本使用**
```javascript
let name = "小明";
let message = '你好世界';
let template = `我的名字是${name}`;  // 模板字符串（推荐）

// 常用字符串操作
console.log(name.length);           // 获取长度
console.log(name.toUpperCase());     // 转大写
console.log(message.includes("你好")); // 检查是否包含
```

#### 🔢 数字 (number)


**🔸 基本使用**
```javascript
let age = 25;           // 整数
let price = 9.99;       // 小数
let big = 1e6;          // 科学计数法：1000000

// 常用数字操作
console.log(Math.round(price));     // 四舍五入：10
console.log(Math.floor(price));     // 向下取整：9
console.log(Math.random());         // 随机数：0-1之间
```

**⚠️ 特殊数字值**
```javascript
console.log(1 / 0);         // Infinity（无穷大）
console.log("abc" * 2);     // NaN（Not a Number）
console.log(isNaN("abc"));  // true（检查是否为NaN）
```

#### ✅ 布尔值 (boolean)


**🔸 基本使用**
```javascript
let isLogin = true;
let isVIP = false;

// 布尔值常用场景
if (isLogin) {
    console.log("用户已登录");
}

let canAccess = isLogin && isVIP;  // 逻辑运算
```

#### 🔘 null 和 undefined


**🔸 区别和使用**
```javascript
let empty = null;           // 故意设置为空
let notSet;                 // 声明但未赋值，自动为undefined

console.log(empty);         // null
console.log(notSet);        // undefined

// 实际使用场景
let userInfo = null;        // 用户信息暂时为空
if (userInfo === null) {
    console.log("请先登录");
}
```

**💡 记忆方法**
```
null：     人为设置的"空"
undefined：系统认为的"没有值"
```

### 2.3 对象类型 (object)


**🔸 基本对象**
```javascript
let person = {
    name: "张三",
    age: 30,
    city: "北京"
};
console.log(typeof person);  // "object"
```

**🔸 数组也是对象**
```javascript
let colors = ["红", "绿", "蓝"];
console.log(typeof colors);  // "object"
console.log(Array.isArray(colors));  // true（检查是否为数组）
```

---

## 3. 🔍 类型检测方法


### 3.1 typeof 操作符


**📋 typeof 检测结果**
```javascript
console.log(typeof "hello");     // "string"
console.log(typeof 42);          // "number"
console.log(typeof true);        // "boolean"
console.log(typeof undefined);   // "undefined"
console.log(typeof null);        // "object" ⚠️ 这是bug，但无法修复
console.log(typeof {});          // "object"
console.log(typeof []);          // "object"
console.log(typeof function(){}); // "function"
```

### 3.2 实用检测方法


**🛠️ 常用类型检测函数**
```javascript
// 检测数组
function isArray(value) {
    return Array.isArray(value);
}

// 检测对象（排除null和数组）
function isObject(value) {
    return typeof value === 'object' && value !== null && !Array.isArray(value);
}

// 检测字符串
function isString(value) {
    return typeof value === 'string';
}

// 使用示例
console.log(isArray([1, 2, 3]));       // true
console.log(isObject({name: "张三"}));  // true
console.log(isString("hello"));        // true
```

---

## 4. 🔄 类型转换


### 4.1 自动类型转换


**🔸 字符串拼接**
```javascript
console.log("我今年" + 25 + "岁");        // "我今年25岁"
console.log(10 + 20 + "分");              // "30分"
console.log("分数：" + 10 + 20);          // "分数：1020" ⚠️注意顺序
```

### 4.2 手动类型转换


**🔸 转为字符串**
```javascript
let num = 123;
let str1 = String(num);        // "123"
let str2 = num.toString();     // "123"
let str3 = num + "";           // "123" （简单粗暴）
```

**🔸 转为数字**
```javascript
let str = "123";
let num1 = Number(str);        // 123
let num2 = parseInt(str);      // 123 （只取整数部分）
let num3 = parseFloat("12.34"); // 12.34 （保留小数）
let num4 = +str;               // 123 （简单写法）

// 转换失败的情况
console.log(Number("abc"));    // NaN
console.log(parseInt("abc"));  // NaN
```

**🔸 转为布尔值**
```javascript
// 这些值转为 false
console.log(Boolean(""));      // false （空字符串）
console.log(Boolean(0));       // false （数字0）
console.log(Boolean(null));    // false
console.log(Boolean(undefined)); // false
console.log(Boolean(NaN));     // false

// 其他都是 true
console.log(Boolean("hello")); // true
console.log(Boolean(1));       // true
console.log(Boolean([]));      // true
console.log(Boolean({}));      // true
```

---

## 5. 💡 实用开发技巧


### 5.1 变量命名规范


**✅ 好的命名**
```javascript
const userName = "张三";        // 驼峰命名
const MAX_COUNT = 100;         // 常量用大写
const isLoggedIn = true;       // 布尔值用is开头
```

**❌ 避免的命名**
```javascript
const a = "张三";              // 太简短
const user_name = "李四";      // 下划线风格（不推荐）
const 姓名 = "王五";           // 中文（虽然可以但不推荐）
```

### 5.2 常见实用场景


**🔸 默认值设置**
```javascript
function greet(name) {
    name = name || "客人";      // 如果name为空，使用默认值
    console.log("你好，" + name);
}

// ES6更好的写法
function greetES6(name = "客人") {
    console.log("你好，" + name);
}
```

**🔸 类型安全检查**
```javascript
function addNumbers(a, b) {
    // 检查参数类型
    if (typeof a !== 'number' || typeof b !== 'number') {
        console.log("请传入数字类型的参数");
        return;
    }
    return a + b;
}

console.log(addNumbers(1, 2));      // 3
console.log(addNumbers("1", 2));    // 请传入数字类型的参数
```

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


```
🔸 变量声明：优先使用 const 和 let，避免 var
🔸 基本类型：string、number、boolean、null、undefined
🔸 引用类型：object（包括数组、函数等）
🔸 类型检测：typeof 和 Array.isArray()
🔸 类型转换：Number()、String()、Boolean()
```

### 6.2 实际开发建议


**🎯 变量使用原则**
```
能用 const 就用 const （值不变）
需要改变时用 let
彻底避免 var （作用域容易出错）
```

**🎯 类型处理建议**
```
字符串拼接：使用模板字符串 `${变量}`
数字计算：注意NaN和Infinity
布尔判断：记住falsy值（""、0、null、undefined、NaN）
类型转换：用明确的方法，避免隐式转换导致的bug
```

**🎯 常见错误避免**
```javascript
// ❌ 容易出错
console.log("5" + 3);          // "53" （字符串拼接）
console.log("5" - 3);          // 2   （数字运算）

// ✅ 明确意图
console.log("5" + String(3));   // "53" （明确字符串拼接）
console.log(Number("5") + 3);   // 8   （明确数字运算）
```

**核心记忆**：
- 变量声明优先 const/let，避免 var
- 5种常用类型：string、number、boolean、null、undefined
- typeof 检测类型，Array.isArray() 检测数组
- 类型转换要明确，避免隐式转换的陷阱