---
title: 2、常用鼠标事件
---
## 📚 目录

1. [鼠标事件概述](#1-鼠标事件概述)
2. [基础点击事件](#2-基础点击事件)
3. [鼠标进入退出事件](#3-鼠标进入退出事件)
4. [鼠标按下松开事件](#4-鼠标按下松开事件)
5. [鼠标位置获取](#5-鼠标位置获取)
6. [阻止默认行为](#6-阻止默认行为)
7. [实用补充事件](#7-实用补充事件)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🖱️ 鼠标事件概述


### 1.1 什么是鼠标事件


**🔸 核心概念**
```
鼠标事件：用户通过鼠标与网页元素交互时触发的事件
作用：让网页能够响应用户的鼠标操作
原理：浏览器监听鼠标动作，当满足条件时执行对应代码
```

### 1.2 为什么需要鼠标事件


**💡 实际应用价值**
- **交互反馈**：按钮点击、链接跳转
- **视觉效果**：鼠标悬停高亮、动画效果
- **功能实现**：拖拽操作、右键菜单
- **用户体验**：提供直观的操作反馈

### 1.3 鼠标事件分类


```
操作类事件：click、dblclick、contextmenu
移动类事件：mouseover、mouseout、mousemove
按压类事件：mousedown、mouseup
滚轮类事件：wheel
```

---

## 2. 👆 基础点击事件


### 2.1 click 点击事件


**🔸 核心概念**
- **含义**：用户完成一次完整点击（按下并松开）时触发
- **作用**：最常用的交互事件，实现按钮功能、链接跳转等
- **触发时机**：鼠标按下并在同一元素上松开

**💻 基础语法**
```javascript
// 方式1：直接绑定
element.onclick = function() {
    console.log('按钮被点击了');
};

// 方式2：事件监听（推荐）
element.addEventListener('click', function() {
    console.log('按钮被点击了');
});
```

**🎯 实用示例**
```html
<button id="myBtn">点击我</button>
<div id="result">等待点击...</div>

<script>
const btn = document.getElementById('myBtn');
const result = document.getElementById('result');

btn.addEventListener('click', function() {
    result.textContent = '按钮被点击了！';
    result.style.color = 'green';
});
</script>
```

### 2.2 dblclick 双击事件


**🔸 核心概念**
- **含义**：用户快速点击两次时触发
- **差异**：比click事件要求更快的连续点击
- **应用场景**：文件打开、全屏切换

**💻 简单示例**
```javascript
element.addEventListener('dblclick', function() {
    console.log('双击触发');
});
```

---

## 3. 🏠 鼠标进入退出事件


### 3.1 mouseover 和 mouseout


**🔸 核心概念**
```
mouseover：鼠标移入元素时触发
mouseout：鼠标移出元素时触发
特点：会冒泡，移入子元素也会触发
```

**💻 基础用法**
```javascript
const box = document.getElementById('box');

box.addEventListener('mouseover', function() {
    this.style.backgroundColor = 'lightblue';
    console.log('鼠标移入');
});

box.addEventListener('mouseout', function() {
    this.style.backgroundColor = 'white';
    console.log('鼠标移出');
});
```

### 3.2 mouseenter 和 mouseleave（重要补充）


**🔸 核心概念**
```
mouseenter：鼠标移入元素时触发
mouseleave：鼠标移出元素时触发
特点：不会冒泡，只在目标元素边界触发
```

**⚖️ 两组事件的差异**

| 事件类型 | **冒泡特性** | **子元素影响** | **推荐场景** |
|---------|------------|-------------|-------------|
| `mouseover/mouseout` | `会冒泡` | `移入子元素也触发` | `需要监听所有子元素` |
| `mouseenter/mouseleave` | `不冒泡` | `只在边界触发` | `悬停效果（推荐）` |

**🎯 对比示例**
```html
<div id="parent" style="padding: 20px; background: lightgray;">
    父元素
    <div id="child" style="padding: 10px; background: white;">
        子元素
    </div>
</div>

<script>
const parent = document.getElementById('parent');

// 使用mouseenter/mouseleave（推荐）
parent.addEventListener('mouseenter', function() {
    console.log('鼠标进入父元素'); // 只触发一次
});

parent.addEventListener('mouseleave', function() {
    console.log('鼠标离开父元素'); // 只触发一次
});
</script>
```

---

## 4. ⬇️ 鼠标按下松开事件


### 4.1 mousedown 和 mouseup


**🔸 核心概念**
```
mousedown：鼠标按钮按下时立即触发
mouseup：鼠标按钮松开时触发
作用：可以实现拖拽、长按等复杂交互
```

**💡 与click的差异**
```
时机差异：
mousedown → mouseup → click

应用差异：
• mousedown：立即响应，适合拖拽开始
• mouseup：按钮松开，适合拖拽结束  
• click：完整点击，适合按钮功能
```

**🎯 拖拽基础示例**
```html
<div id="dragBox" style="width: 100px; height: 100px; background: red; position: absolute;">
    拖拽我
</div>

<script>
const box = document.getElementById('dragBox');
let isDragging = false;

box.addEventListener('mousedown', function(e) {
    isDragging = true;
    console.log('开始拖拽');
    box.style.cursor = 'grabbing';
});

document.addEventListener('mouseup', function() {
    if (isDragging) {
        isDragging = false;
        console.log('结束拖拽');
        box.style.cursor = 'grab';
    }
});

document.addEventListener('mousemove', function(e) {
    if (isDragging) {
        box.style.left = e.clientX - 50 + 'px';
        box.style.top = e.clientY - 50 + 'px';
    }
});
</script>
```

### 4.2 鼠标按钮识别


**🔸 核心概念**
- **event.button**：识别按下的是哪个鼠标按钮
- **值含义**：0=左键，1=中键，2=右键

**💻 按钮识别示例**
```javascript
element.addEventListener('mousedown', function(e) {
    switch(e.button) {
        case 0:
            console.log('左键按下');
            break;
        case 1:
            console.log('中键按下');
            break;
        case 2:
            console.log('右键按下');
            break;
    }
});
```

---

## 5. 📍 鼠标位置获取


### 5.1 常用位置属性


**🔸 核心概念**
```
clientX/clientY：相对于浏览器窗口的坐标
pageX/pageY：相对于整个页面的坐标（包含滚动）
offsetX/offsetY：相对于目标元素的坐标
screenX/screenY：相对于屏幕的坐标
```

### 5.2 位置属性对比


```
坐标系统对比：

浏览器窗口 ┌─────────────────┐
          │  (clientX/Y)    │
          │                 │
          │   ┌─────────┐   │
页面元素   │   │(offsetX/Y)│   │
          │   │         │   │  
          │   └─────────┘   │
          └─────────────────┘
整个页面包含滚动区域 (pageX/Y)
```

**🎯 位置获取实例**
```html
<div id="tracker" style="width: 200px; height: 200px; background: lightgreen; margin: 50px;">
    移动鼠标查看坐标
</div>
<div id="info"></div>

<script>
const tracker = document.getElementById('tracker');
const info = document.getElementById('info');

tracker.addEventListener('mousemove', function(e) {
    info.innerHTML = `
        <strong>坐标信息：</strong><br>
        相对窗口：(${e.clientX}, ${e.clientY})<br>
        相对页面：(${e.pageX}, ${e.pageY})<br>
        相对元素：(${e.offsetX}, ${e.offsetY})
    `;
});
</script>
```

### 5.3 实用应用场景


**💡 常见应用**
```javascript
// 跟随鼠标的提示框
document.addEventListener('mousemove', function(e) {
    const tooltip = document.getElementById('tooltip');
    tooltip.style.left = e.clientX + 10 + 'px';
    tooltip.style.top = e.clientY + 10 + 'px';
});

// 图片放大镜效果
img.addEventListener('mousemove', function(e) {
    const x = e.offsetX;
    const y = e.offsetY;
    // 根据鼠标位置显示放大区域
});
```

---

## 6. 🚫 阻止默认行为


### 6.1 什么是默认行为


**🔸 核心概念**
```
默认行为：浏览器对某些事件的内置处理
常见例子：
• 右键弹出菜单
• 链接跳转
• 表单提交
• 图片拖拽
```

### 6.2 preventDefault() 方法


**🔸 核心语法**
```javascript
element.addEventListener('事件类型', function(e) {
    e.preventDefault(); // 阻止默认行为
    // 自定义处理逻辑
});
```

**🎯 实用示例**

```html
<!-- 阻止右键菜单 -->
<div id="noContextMenu" style="width: 200px; height: 100px; background: yellow;">
    右键试试看
</div>

<!-- 阻止链接跳转 -->
<a href="https://www.baidu.com" id="noJump">点击不跳转</a>

<!-- 阻止图片拖拽 -->
<img src="example.jpg" id="noDrag" alt="不能拖拽的图片">

<script>
// 阻止右键菜单
document.getElementById('noContextMenu').addEventListener('contextmenu', function(e) {
    e.preventDefault();
    alert('右键菜单被阻止了');
});

// 阻止链接跳转
document.getElementById('noJump').addEventListener('click', function(e) {
    e.preventDefault();
    alert('链接跳转被阻止了');
});

// 阻止图片拖拽
document.getElementById('noDrag').addEventListener('dragstart', function(e) {
    e.preventDefault();
});
</script>
```

### 6.3 常用阻止场景


**💡 实际应用**
```javascript
// 自定义右键菜单
element.addEventListener('contextmenu', function(e) {
    e.preventDefault();
    showCustomMenu(e.clientX, e.clientY);
});

// 表单验证失败阻止提交
form.addEventListener('submit', function(e) {
    if (!validateForm()) {
        e.preventDefault();
        showError('表单验证失败');
    }
});

// 阻止文字选择
element.addEventListener('selectstart', function(e) {
    e.preventDefault();
});
```

---

## 7. ✨ 实用补充事件


### 7.1 contextmenu 右键菜单事件


**🔸 核心概念**
- **含义**：用户右键点击时触发
- **作用**：实现自定义右键菜单
- **默认行为**：弹出浏览器右键菜单

**💻 自定义右键菜单**
```html
<div id="customArea" style="width: 300px; height: 200px; background: lightcoral;">
    右键点击这里
</div>
<div id="customMenu" style="display: none; position: absolute; background: white; border: 1px solid #ccc;">
    <div onclick="alert('复制')">复制</div>
    <div onclick="alert('粘贴')">粘贴</div>
    <div onclick="alert('删除')">删除</div>
</div>

<script>
const area = document.getElementById('customArea');
const menu = document.getElementById('customMenu');

area.addEventListener('contextmenu', function(e) {
    e.preventDefault();
    menu.style.display = 'block';
    menu.style.left = e.clientX + 'px';
    menu.style.top = e.clientY + 'px';
});

// 点击其他地方隐藏菜单
document.addEventListener('click', function() {
    menu.style.display = 'none';
});
</script>
```

### 7.2 wheel 滚轮事件


**🔸 核心概念**
- **含义**：用户滚动鼠标滚轮时触发
- **作用**：实现自定义滚动效果、缩放功能
- **方向判断**：通过 deltaY 判断滚动方向

**💻 滚轮缩放示例**
```javascript
const zoomTarget = document.getElementById('zoomTarget');
let scale = 1;

zoomTarget.addEventListener('wheel', function(e) {
    e.preventDefault();
    
    if (e.deltaY < 0) {
        scale += 0.1; // 向上滚动，放大
    } else {
        scale -= 0.1; // 向下滚动，缩小
    }
    
    scale = Math.max(0.1, Math.min(3, scale)); // 限制缩放范围
    this.style.transform = `scale(${scale})`;
});
```

### 7.3 mousemove 鼠标移动事件


**🔸 核心概念**
- **含义**：鼠标在元素上移动时持续触发
- **注意**：触发频率很高，需要注意性能
- **应用**：鼠标跟随效果、拖拽操作

**💻 鼠标跟随效果**
```javascript
const follower = document.getElementById('follower');

document.addEventListener('mousemove', function(e) {
    follower.style.left = e.clientX + 'px';
    follower.style.top = e.clientY + 'px';
});
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 基础点击：click最常用，dblclick双击功能
🔸 悬停效果：推荐使用mouseenter/mouseleave（不冒泡）
🔸 按压操作：mousedown开始，mouseup结束，实现拖拽
🔸 位置获取：clientX/Y窗口坐标，offsetX/Y元素坐标
🔸 阻止默认：preventDefault()阻止浏览器默认行为
🔸 右键菜单：contextmenu实现自定义菜单
```

### 8.2 关键理解要点


**🔹 事件选择策略**
```
悬停效果：mouseenter/mouseleave（推荐）
↓
不会因为子元素触发多次，性能更好

拖拽功能：mousedown + mousemove + mouseup
↓
分阶段处理，可以实现复杂交互

位置获取：根据需求选择坐标系
↓
clientX/Y：窗口相对位置（最常用）
offsetX/Y：元素相对位置（精确定位）
```

**🔹 性能优化建议**
```
mousemove事件：
• 触发频率极高，避免复杂计算
• 使用节流（throttle）优化性能
• 只在必要时绑定和解绑

事件委托：
• 利用冒泡机制，在父元素统一处理
• 减少事件监听器数量
• 适合动态生成的元素
```

### 8.3 实际应用价值


**🎯 常见应用场景**
- **交互反馈**：按钮点击效果、悬停高亮
- **拖拽功能**：文件上传、排序操作
- **自定义菜单**：右键菜单、下拉菜单
- **位置跟随**：提示框、放大镜效果
- **手势操作**：滑动、缩放、旋转

**🔧 开发实践建议**
```
事件绑定：
• 优先使用addEventListener
• 避免内联事件处理
• 及时移除不需要的监听器

错误预防：
• 检查元素是否存在
• 处理事件对象可能为空的情况
• 使用try-catch包裹复杂逻辑

用户体验：
• 提供清晰的视觉反馈
• 避免误触和重复操作
• 考虑移动端触摸事件
```

**核心记忆**：
- 鼠标事件让网页"活"起来，提供丰富的用户交互
- mouseenter/mouseleave是悬停效果的最佳选择
- 位置信息配合事件可以实现复杂的交互功能
- preventDefault()是控制浏览器行为的重要工具