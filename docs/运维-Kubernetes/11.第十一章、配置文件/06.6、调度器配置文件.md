---
title: 6、调度器配置文件
---
## 📚 目录

1. [调度器基础概念](#1-调度器基础概念)
2. [调度器配置文件位置与结构](#2-调度器配置文件位置与结构)
3. [核心配置参数详解](#3-核心配置参数详解)
4. [调度策略与插件配置](#4-调度策略与插件配置)
5. [实际配置示例与最佳实践](#5-实际配置示例与最佳实践)
6. [核心要点总结](#6-核心要点总结)

---

## 1. 🎯 调度器基础概念


### 1.1 什么是Kubernetes调度器


> 💡 **通俗理解**  
> 想象你是一个聪明的房屋中介，有很多客户要租房，也有很多房子可以出租。调度器就像这个中介，它的工作就是根据客户的需求（Pod的资源要求），找到最合适的房子（Node节点）来安排入住。

**📋 调度器的核心作用**
```
客户需求(Pod)  ──调度器分析──▶  合适的房子(Node)
    │                           │
    ├─ 需要2核CPU               ├─ 有足够CPU资源
    ├─ 需要4GB内存              ├─ 有足够内存
    ├─ 需要SSD存储              ├─ 有SSD存储
    └─ 需要特定标签              └─ 符合标签要求
```

**🔸 调度器的工作流程**
1. **监听**: 持续监听是否有新的Pod需要调度
2. **筛选**: 找出所有可以运行这个Pod的节点（过滤阶段）
3. **评分**: 对这些节点进行打分，找出最优选择（打分阶段）
4. **绑定**: 将Pod分配到得分最高的节点上

### 1.2 为什么需要调度器配置


**🤔 没有调度器会怎样？**
- Pod无法知道应该在哪个节点运行
- 资源分配不均衡，有的节点满载，有的节点空闲
- 无法根据业务需求进行智能调度

**⚡ 调度器配置的价值**
- **智能分配**: 根据资源需求和节点状态进行最优分配
- **策略定制**: 可以根据业务需求定制调度策略
- **高可用**: 支持多调度器实例，防止单点故障

---

## 2. 📂 调度器配置文件位置与结构


### 2.1 调度器配置文件位置


**📍 主要配置文件位置**
```
/etc/kubernetes/manifests/kube-scheduler.yaml
```

> 🔍 **文件位置说明**  
> 这个文件位于Kubernetes控制平面节点上，是调度器Pod的定义文件。当Kubernetes启动时，kubelet会自动读取这个目录下的yaml文件并创建相应的Pod。

### 2.2 配置文件基本结构


**🏗️ 配置文件组成部分**
```
kube-scheduler.yaml
├── Pod基本信息 (metadata、spec等)
├── 容器配置 (image、command、args)
├── 调度器参数 (启动参数)
├── 存储卷挂载 (配置文件、证书等)
└── 网络配置 (主机网络模式)
```

**📋 典型文件结构示意**
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: kube-scheduler
  namespace: kube-system
spec:
  containers:
  - name: kube-scheduler
    image: k8s.gcr.io/kube-scheduler:v1.28.0
    command:
    - kube-scheduler
    - --config=/etc/kubernetes/scheduler-config.yaml
    - --v=2
```

---

## 3. ⚙️ 核心配置参数详解


### 3.1 基础连接参数


#### --config 调度器配置文件路径


**🔸 参数含义**
```bash
--config=/etc/kubernetes/scheduler-config.yaml
```

> 💡 **通俗解释**  
> 这就像告诉调度器："你的详细工作手册在哪里"。调度器会读取这个配置文件来了解自己该怎么工作。

**📋 配置文件内容示例**
```yaml
apiVersion: kubescheduler.config.k8s.io/v1beta3
kind: KubeSchedulerConfiguration
profiles:
- schedulerName: default-scheduler
clientConnection:
  kubeconfig: /etc/kubernetes/scheduler.conf
```

#### --kube-config kubeconfig文件路径


**🔸 参数含义**
```bash
--kubeconfig=/etc/kubernetes/scheduler.conf
```

> 🔑 **认证说明**  
> 这个文件包含了调度器连接API Server所需的认证信息，就像调度器的"身份证"和"通行证"。

**🔧 kubeconfig文件作用**
- **身份验证**: 证明调度器的合法身份
- **服务器信息**: 记录API Server的地址和端口
- **访问权限**: 定义调度器可以执行哪些操作

### 3.2 高可用相关参数


#### --leader-elect 领导者选举


**🔸 参数含义**
```bash
--leader-elect=true
```

> 🏆 **形象比喻**  
> 当有多个调度器实例时，需要选出一个"队长"来负责实际工作，其他的作为"候补"。这就像班级选班长，只有一个人真正负责，但如果班长请假了，副班长可以顶上。

**📊 选举机制图解**
```
调度器实例A ──竞争──▶ [领导者选举] ◀──竞争── 调度器实例B
      │                    │                    │
      ▼                    ▼                    ▼
   候选者               当前领导者              候选者
 (standby)            (actively working)    (standby)
```

#### --lock-object-name 和 --lock-object-namespace


**🔸 参数含义**
```bash
--lock-object-name=kube-scheduler
--lock-object-namespace=kube-system
```

> 🔒 **锁机制解释**  
> 这就像一个"令牌"，只有拿到这个令牌的调度器才能真正工作。令牌的名字是`kube-scheduler`，存放在`kube-system`命名空间中。

---

## 4. 🧠 调度策略与插件配置


### 4.1 调度算法配置


#### --algorithm-provider 算法提供程序


**🔸 已废弃参数**
```bash
--algorithm-provider=DefaultProvider  # 新版本中已废弃
```

> ⚠️ **版本演进说明**  
> 在新版本的Kubernetes中，这个参数已经被更灵活的插件系统(plugins)替代。就像从老式的固定套餐升级到了自由搭配的菜单。

#### --policy-config-file 调度策略配置文件


**🔸 参数含义**
```bash
--policy-config-file=/etc/kubernetes/scheduler-policy.json
```

> 📜 **策略文件作用**  
> 这个文件定义了调度的详细规则，比如哪些Pod必须运行在一起，哪些Pod不能运行在同一个节点等。

### 4.2 现代插件配置系统


#### profiles 调度配置文件


**🔸 新版本配置方式**
```yaml
profiles:
- schedulerName: default-scheduler
  plugins:
    score:
      enabled:
      - name: NodeResourcesFit
      - name: NodeAffinity
```

> 🔧 **插件系统优势**  
> 新的插件系统就像乐高积木，你可以根据需要组合不同的调度功能，更加灵活和可定制。

### 4.3 核心调度插件详解


#### nodeResourcesFit 节点资源匹配


**🔸 功能说明**
```yaml
plugins:
  filter:
  - name: NodeResourcesFit
  score:
  - name: NodeResourcesFit
```

> 💰 **资源匹配原理**  
> 就像选择酒店房间，要确保房间的设施能满足客人的需求：
> - CPU够不够用？
> - 内存够不够大？
> - 存储空间是否充足？

**📊 资源评分机制**
```
节点A: CPU使用率30% + 内存使用率40% = 总分70分
节点B: CPU使用率60% + 内存使用率70% = 总分65分
节点C: CPU使用率80% + 内存使用率90% = 总分55分

结果: Pod会被调度到节点A（分数最高，资源最充裕）
```

#### nodeAffinity 节点亲和性


**🔸 配置示例**
```yaml
plugins:
  filter:
  - name: NodeAffinity
```

> 🎯 **亲和性理解**  
> 节点亲和性就像是"择校"，你可以指定Pod更喜欢运行在哪类节点上，比如：
> - 必须在SSD硬盘的节点上运行
> - 优先选择内存大于32GB的节点
> - 不能在Windows节点上运行

**🔸 亲和性类型**
- **硬亲和性** (`requiredDuringSchedulingIgnoredDuringExecution`): 必须满足
- **软亲和性** (`preferredDuringSchedulingIgnoredDuringExecution`): 优先满足

#### podAffinity Pod亲和性


**🔸 配置示例**
```yaml
plugins:
  filter:
  - name: InterPodAffinity
```

> 👥 **Pod亲和性应用**  
> Pod亲和性控制Pod之间的"邻居关系"：
> - **亲和性**: Web服务和缓存服务放在同一个节点（减少网络延迟）
> - **反亲和性**: 多个数据库实例分散在不同节点（提高可用性）

---

## 5. 🔧 实际配置示例与最佳实践


### 5.1 完整的调度器配置示例


**📋 /etc/kubernetes/manifests/kube-scheduler.yaml**
```yaml
apiVersion: v1
kind: Pod
metadata:
  labels:
    component: kube-scheduler
    tier: control-plane
  name: kube-scheduler
  namespace: kube-system
spec:
  containers:
  - name: kube-scheduler
    image: registry.k8s.io/kube-scheduler:v1.28.2
    command:
    - kube-scheduler
    - --authentication-kubeconfig=/etc/kubernetes/scheduler.conf
    - --authorization-kubeconfig=/etc/kubernetes/scheduler.conf
    - --bind-address=127.0.0.1
    - --kubeconfig=/etc/kubernetes/scheduler.conf
    - --leader-elect=true
    - --config=/etc/kubernetes/scheduler-config.yaml
    volumeMounts:
    - mountPath: /etc/kubernetes/scheduler.conf
      name: kubeconfig
      readOnly: true
    - mountPath: /etc/kubernetes/scheduler-config.yaml
      name: config
      readOnly: true
  volumes:
  - hostPath:
      path: /etc/kubernetes/scheduler.conf
      type: FileOrCreate
    name: kubeconfig
  - hostPath:
      path: /etc/kubernetes/scheduler-config.yaml
      type: FileOrCreate
    name: config
```

### 5.2 调度器配置文件示例


**📋 /etc/kubernetes/scheduler-config.yaml**
```yaml
apiVersion: kubescheduler.config.k8s.io/v1beta3
kind: KubeSchedulerConfiguration
profiles:
- schedulerName: default-scheduler
  plugins:
    # 过滤阶段插件
    filter:
      enabled:
      - name: NodeResourcesFit
      - name: NodeAffinity
      - name: InterPodAffinity
      - name: VolumeBinding
    # 打分阶段插件
    score:
      enabled:
      - name: NodeResourcesFit
      - name: NodeAffinity
      - name: InterPodAffinity
  pluginConfig:
  - name: NodeResourcesFit
    args:
      scoringStrategy:
        type: LeastAllocated  # 选择资源使用率最低的节点
clientConnection:
  kubeconfig: /etc/kubernetes/scheduler.conf
leaderElection:
  leaderElect: true
  resourceNamespace: kube-system
  resourceName: kube-scheduler
```

### 5.3 调度器配置最佳实践


**✅ 推荐配置**
```yaml
# 1. 启用领导者选举（高可用）
leaderElection:
  leaderElect: true

# 2. 合理配置插件
plugins:
  filter:
    enabled:
    - name: NodeResourcesFit      # 基础资源过滤
    - name: NodeAffinity          # 节点亲和性
    - name: InterPodAffinity      # Pod间亲和性
    - name: VolumeBinding         # 存储卷绑定

# 3. 选择合适的资源分配策略
pluginConfig:
- name: NodeResourcesFit
  args:
    scoringStrategy:
      type: LeastAllocated      # 负载均衡策略
```

**⚠️ 常见配置陷阱**
- 不要禁用核心插件如`NodeResourcesFit`
- 避免过于复杂的亲和性规则
- 确保配置文件语法正确
- 定期备份配置文件

---

## 6. 📋 核心要点总结


### 6.1 必须掌握的核心概念


**🔸 调度器作用**: Pod到Node的智能分配器，确保资源合理利用
**🔸 配置文件**: `/etc/kubernetes/manifests/kube-scheduler.yaml`为主配置
**🔸 工作模式**: 通过过滤+打分两阶段完成调度决策
**🔸 高可用**: 通过领导者选举实现多实例容错
**🔸 插件系统**: 现代Kubernetes使用灵活的插件架构

### 6.2 关键配置参数速查


| 参数类型 | **参数名称** | **作用说明** | **推荐配置** |
|---------|------------|-------------|-------------|
| 🔧 **基础配置** | `--config` | `指定调度器配置文件` | `必须配置` |
| 🔑 **认证配置** | `--kubeconfig` | `API Server连接认证` | `必须配置` |
| 🏆 **高可用配置** | `--leader-elect` | `启用领导者选举` | `生产环境必须` |
| 🔒 **锁配置** | `--lock-object-name` | `选举锁对象名称` | `使用默认值` |
| 🧠 **调度策略** | `profiles.plugins` | `调度插件配置` | `根据需求定制` |

### 6.3 实际运维建议


**🔍 调试与监控**
```bash
# 查看调度器状态
kubectl get pods -n kube-system | grep scheduler

# 查看调度器日志
kubectl logs -n kube-system kube-scheduler-master

# 检查调度器配置
kubectl describe pod -n kube-system kube-scheduler-master
```

**📊 性能优化建议**
- **合理设置插件**: 只启用必要的调度插件
- **避免过度调度**: 不要设置过于严格的亲和性规则
- **监控调度延迟**: 关注Pod调度到运行的时间
- **定期清理**: 清理无用的调度策略和配置

**🛡️ 安全最佳实践**
- 确保kubeconfig文件权限设置为600
- 定期轮换调度器使用的证书
- 限制调度器的API访问权限
- 启用审计日志记录调度决策

### 6.4 学习进阶方向


**📈 深入学习路径**
```
基础调度概念 ──▶ 插件开发 ──▶ 性能调优 ──▶ 多调度器架构
      │              │           │            │
      ▼              ▼           ▼            ▼
   理论基础       实践开发     生产优化     架构设计
```

**🎯 实践建议**
- 在测试环境尝试不同的调度策略
- 编写自定义调度插件解决特定需求  
- 监控和分析调度器的性能指标
- 学习社区最新的调度器发展趋势

**核心记忆要点**：
- 调度器是Pod分配的大脑，配置决定智能程度
- 高可用通过领导者选举，多实例保证服务连续
- 插件系统灵活可定制，根据业务需求合理配置
- 监控和调试是运维关键，日志分析不可或缺