---
title: 2、Webpack安装与环境配置
---
## 📚 目录

1. [Node.js环境准备](#1-nodejs环境准备)
2. [包管理器选择与使用](#2-包管理器选择与使用)
3. [Webpack安装方式详解](#3-webpack安装方式详解)
4. [项目初始化与配置](#4-项目初始化与配置)
5. [目录结构设计规范](#5-目录结构设计规范)
6. [团队开发环境统一](#6-团队开发环境统一)
7. [第一个Webpack项目](#7-第一个webpack项目)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🌱 Node.js环境准备


### 1.1 为什么需要Node.js？


**`Node.js`** 简单说就是让JavaScript能在电脑上运行的环境，而不仅仅是在浏览器里。

```
为什么Webpack需要Node.js？
浏览器中的JS → 只能处理网页交互
Node.js中的JS → 可以处理文件、运行工具

Webpack本质上是一个用JavaScript写的工具
需要在你的电脑上运行，处理你的项目文件
所以必须先装Node.js
```

### 1.2 Node.js版本选择


**🔸 版本兼容性要求**
```
推荐版本：Node.js 16.x 或更高版本
最低要求：Node.js 14.x

为什么要关注版本？
- 太老的版本：可能不支持新的Webpack特性
- 太新的版本：可能有兼容性问题
- LTS版本：长期支持，最稳定
```

**💡 版本检查方法**
```bash
# 检查当前Node.js版本
node --version
# 或者
node -v

# 检查npm版本
npm --version
```

### 1.3 Node.js安装步骤


**🔧 安装流程**
```
步骤1：访问 nodejs.org
步骤2：下载LTS版本（长期支持版）
步骤3：运行安装程序，一路下一步
步骤4：验证安装是否成功
```

**⚠️ 常见问题**
- **权限问题**：Windows需要管理员权限
- **路径问题**：确保Node.js添加到环境变量
- **版本冲突**：如果之前装过老版本，建议先卸载

---

## 2. 📦 包管理器选择与使用


### 2.1 什么是包管理器？


**包管理器**就像是一个"软件商店"，帮你自动下载、安装、管理各种JavaScript工具和库。

```
不用包管理器的痛苦：
1. 手动下载每个库的文件
2. 手动管理版本更新
3. 手动处理库之间的依赖关系
4. 手动解决版本冲突

用了包管理器的便利：
1. 一个命令自动下载安装
2. 自动管理版本
3. 自动处理依赖关系
4. 一键升级所有库
```

### 2.2 npm vs yarn


| 特点 | **npm** | **yarn** |
|------|---------|----------|
| **安装方式** | `Node.js自带` | `需要单独安装` |
| **安装速度** | `较慢` | `较快` |
| **锁定文件** | `package-lock.json` | `yarn.lock` |
| **命令简洁性** | `较长` | `较短` |
| **推荐程度** | `⭐⭐⭐⭐` | `⭐⭐⭐⭐⭐` |

**🎯 新手建议**：先用npm，因为装Node.js就自动有了，等熟悉后再考虑yarn。

### 2.3 npm基础命令


**📋 常用命令对照**
```bash
# 初始化项目
npm init

# 安装包
npm install 包名
npm install 包名 --save-dev  # 开发时用的包

# 安装所有依赖
npm install

# 卸载包
npm uninstall 包名
```

---

## 3. ⚙️ Webpack安装方式详解


### 3.1 全局安装 vs 本地安装


**🌍 全局安装**
```bash
npm install -g webpack webpack-cli
```

**含义**：把Webpack装到电脑的系统目录，所有项目都能用

**优点**：
- ✅ 任何地方都能使用webpack命令
- ✅ 只需要安装一次

**缺点**：
- ❌ 不同项目可能需要不同版本的Webpack
- ❌ 团队协作时版本容易不一致
- ❌ 升级时可能影响所有项目

**🏠 本地安装（推荐）**
```bash
npm install webpack webpack-cli --save-dev
```

**含义**：把Webpack装到当前项目文件夹，只有这个项目能用

**优点**：
- ✅ 每个项目可以使用不同版本
- ✅ 团队协作版本一致
- ✅ 项目独立，互不影响

**缺点**：
- ❌ 不能直接使用webpack命令（需要npx）

### 3.2 webpack-cli的作用


**`webpack-cli`** 就是Webpack的"命令行工具"，让你能在终端使用webpack命令。

```
Webpack = 打包的核心引擎
webpack-cli = 让你能用命令操作这个引擎

类比理解：
Webpack = 汽车发动机
webpack-cli = 方向盘和踏板
```

**为什么要单独安装？**
- Webpack 4.0后，命令行工具被分离出来
- 这样做可以减少核心包的大小
- 给用户更多选择（比如用图形界面代替命令行）

### 3.3 npx命令的使用


**`npx`** 是npm 5.2+自带的工具，专门用来运行本地安装的命令。

```bash
# 不用npx的写法（麻烦）
./node_modules/.bin/webpack --version

# 用npx的写法（简单）
npx webpack --version
```

**npx的作用**：
- 🔍 自动找到项目中安装的webpack
- 🚀 直接运行，不需要写长路径
- ✨ 临时下载运行（如果本地没有）

---

## 4. 📋 项目初始化与配置


### 4.1 package.json文件的作用


**`package.json`** 就像是项目的"身份证"，记录了项目的所有重要信息。

```json
{
  "name": "my-webpack-project",
  "version": "1.0.0",
  "description": "我的第一个Webpack项目",
  "main": "index.js",
  "scripts": {
    "build": "webpack",
    "dev": "webpack --mode development"
  },
  "devDependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^4.10.0"
  }
}
```

**各字段含义**：
- **name**: 项目名称
- **version**: 项目版本号
- **description**: 项目描述
- **main**: 项目入口文件
- **scripts**: 自定义命令脚本
- **devDependencies**: 开发时需要的包

### 4.2 项目初始化步骤


**🔧 完整初始化流程**
```bash
# 第1步：创建项目文件夹
mkdir my-webpack-project
cd my-webpack-project

# 第2步：初始化package.json
npm init -y

# 第3步：安装Webpack
npm install webpack webpack-cli --save-dev

# 第4步：验证安装
npx webpack --version
```

**💡 npm init参数说明**
```bash
npm init          # 交互式创建，会问你很多问题
npm init -y       # 快速创建，全部使用默认值
```

### 4.3 依赖管理最佳实践


**🔸 dependencies vs devDependencies**

```
dependencies（生产依赖）：
- 项目运行时必须的包
- 会被打包到最终产品中
- 例如：react, vue, lodash

devDependencies（开发依赖）：
- 只有开发时才需要的包
- 不会被打包到最终产品中  
- 例如：webpack, babel, eslint
```

**安装命令对比**：
```bash
# 安装到生产依赖
npm install react --save

# 安装到开发依赖
npm install webpack --save-dev

# npm 5+ 版本，--save是默认的，可以省略
npm install react
npm install webpack -D  # -D是--save-dev的简写
```

---

## 5. 📁 目录结构设计规范


### 5.1 标准项目结构


```
my-webpack-project/
├── package.json           ← 项目配置文件
├── package-lock.json      ← 版本锁定文件
├── webpack.config.js      ← Webpack配置文件
├── src/                   ← 源代码目录
│   ├── index.js          ← 项目入口文件
│   ├── components/       ← 组件目录
│   ├── styles/           ← 样式文件目录
│   └── assets/           ← 静态资源目录
├── dist/                  ← 打包输出目录
├── public/                ← 公共资源目录
│   └── index.html        ← HTML模板
└── node_modules/          ← 依赖包目录（自动生成）
```

### 5.2 目录设计原则


**🎯 核心原则**
```
源码分离：src/ 放源代码，dist/ 放打包结果
功能分类：按功能分组，不是按文件类型
扁平结构：避免嵌套太深
命名规范：使用统一的命名风格
```

**📂 各目录作用说明**

| 目录 | **作用** | **重要程度** |
|------|----------|-------------|
| `src/` | `存放你写的源代码` | `⭐⭐⭐⭐⭐` |
| `dist/` | `Webpack打包后的文件` | `⭐⭐⭐⭐` |
| `public/` | `不需要打包的静态文件` | `⭐⭐⭐` |
| `node_modules/` | `npm安装的包` | `⭐⭐⭐⭐` |

### 5.3 gitignore配置


**创建 `.gitignore` 文件**：
```
# 依赖包目录
node_modules/

# 打包输出目录
dist/

# 日志文件
*.log

# 环境配置文件
.env.local
.env.development.local
.env.production.local

# 编辑器配置
.vscode/
.idea/
```

**为什么要忽略这些文件？**
- **node_modules/**: 太大了，可以通过npm install重新安装
- **dist/**: 打包结果，可以重新生成
- **日志文件**: 临时文件，没必要版本控制

---

## 6. 👥 团队开发环境统一


### 6.1 版本锁定机制


**🔒 package-lock.json的作用**

```
问题场景：
小明电脑：webpack 5.75.0
小红电脑：webpack 5.76.0
→ 可能出现不同的打包结果！

解决方案：
package-lock.json 精确记录每个包的版本
团队成员安装时，版本完全一致
```

**最佳实践**：
- ✅ 把 `package-lock.json` 提交到git
- ✅ 团队成员使用 `npm ci` 而不是 `npm install`
- ✅ 定期统一升级依赖版本

### 6.2 Node.js版本管理


**🔧 使用 .nvmrc 文件**
```bash
# 在项目根目录创建 .nvmrc 文件
echo "16.18.0" > .nvmrc

# 团队成员使用指定版本
nvm use
```

### 6.3 统一开发环境检查清单


**☑️ 环境统一检查**
- [ ] Node.js版本一致（建议使用LTS版本）
- [ ] npm/yarn版本记录
- [ ] package-lock.json已提交
- [ ] .nvmrc文件已创建
- [ ] 统一的代码编辑器配置

---

## 7. 🚀 第一个Webpack项目


### 7.1 快速创建流程


**🔧 完整操作步骤**
```bash
# 1. 创建并进入项目目录
mkdir my-first-webpack && cd my-first-webpack

# 2. 初始化npm项目
npm init -y

# 3. 安装Webpack
npm install webpack webpack-cli --save-dev

# 4. 创建基本目录结构
mkdir src dist public

# 5. 创建入口文件
echo "console.log('Hello Webpack!');" > src/index.js

# 6. 第一次打包
npx webpack src/index.js --mode development
```

### 7.2 验证安装是否成功


**✅ 检查项目状态**
```bash
# 查看安装的包
npm list

# 查看Webpack版本
npx webpack --version

# 查看项目结构
ls -la
```

**预期结果**：
```
├── dist/main.js          ← Webpack生成的打包文件
├── src/index.js          ← 你的源代码
├── package.json          ← 项目配置
├── package-lock.json     ← 版本锁定
└── node_modules/         ← 依赖包
```

### 7.3 添加npm脚本


**编辑 package.json**：
```json
{
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development",
    "watch": "webpack --mode development --watch"
  }
}
```

**现在可以使用简短命令**：
```bash
npm run build    # 生产环境打包
npm run dev      # 开发环境打包
npm run watch    # 监听文件变化，自动打包
```

---

## 8. 📋 核心要点总结


### 8.1 安装流程记忆


```
🔸 环境准备：Node.js → npm → 项目文件夹
🔸 项目初始化：npm init → 安装webpack → 创建目录
🔸 验证安装：npx webpack --version → 第一次打包
🔸 环境配置：package.json脚本 → .gitignore → 版本锁定
```

### 8.2 重要概念回顾


**📝 核心概念清单**
- **Node.js**: JavaScript运行环境，Webpack的基础
- **npm**: 包管理器，用来安装和管理JavaScript工具
- **package.json**: 项目配置文件，记录依赖和脚本
- **webpack-cli**: Webpack的命令行工具
- **npx**: 运行本地安装包的工具
- **本地安装**: 推荐方式，每个项目独立版本

### 8.3 常见问题与解决


**🔧 问题排查**
```
❌ 问题：command not found: webpack
✅ 解决：使用 npx webpack 或检查是否正确安装

❌ 问题：版本不兼容
✅ 解决：检查Node.js版本，使用LTS版本

❌ 问题：权限错误
✅ 解决：避免使用sudo，检查npm配置

❌ 问题：网络问题导致安装失败
✅ 解决：配置npm镜像源或使用代理
```

### 8.4 学习检查点


**☑️ 自我检验**
- [ ] 能够独立安装Node.js和npm
- [ ] 理解全局安装和本地安装的区别
- [ ] 会创建package.json并配置基本脚本
- [ ] 能够成功安装webpack和webpack-cli
- [ ] 理解npx命令的作用和使用方法
- [ ] 能够创建标准的项目目录结构
- [ ] 会配置.gitignore忽略不必要的文件

**🎯 下一步学习**
掌握了环境配置，接下来可以学习：
- Webpack基础配置文件编写
- 入口和出口配置
- 基本的打包和构建流程

**💡 记忆要点**
- 本地安装是最佳实践
- package.json是项目的核心配置
- npx解决了本地包的运行问题
- 目录结构要规范，便于团队协作