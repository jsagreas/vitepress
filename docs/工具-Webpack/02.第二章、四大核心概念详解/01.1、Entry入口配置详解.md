---
title: 1、Entry入口配置详解
---
## 📚 目录

1. [Entry入口点是什么](#1-entry入口点是什么)
2. [单入口配置详解](#2-单入口配置详解)
3. [多入口配置详解](#3-多入口配置详解)
4. [动态入口配置](#4-动态入口配置)
5. [路径配置与path模块](#5-路径配置与path模块)
6. [实际应用场景对比](#6-实际应用场景对比)
7. [核心要点总结](#7-核心要点总结)

---

## 1. 🚪 Entry入口点是什么


### 1.1 通俗理解Entry


**🔸 什么是Entry入口点？**

把webpack想象成一个**聪明的打包工人**，Entry就是告诉这个工人**"从哪里开始干活"**。

```
现实类比：
🏠 盖房子 → 从地基开始
📚 看书   → 从目录开始  
🕸️ 爬网页 → 从首页开始
📦 打包   → 从Entry开始
```

**🔸 Entry的工作机制**

```
Entry入口文件 → 分析依赖关系 → 找到所有相关文件 → 打包成bundle

示例流程：
index.js(入口) 
    ↓
引入了 utils.js
    ↓  
引入了 style.css
    ↓
webpack把这些都打包到一起
```

### 1.2 为什么需要Entry？


```
🤔 没有Entry会怎样？
webpack不知道从哪开始 → 无法分析依赖 → 打包失败

✅ 有了Entry：
明确起点 → 自动找到所有依赖 → 完整打包
```

---

## 2. 📝 单入口配置详解


### 2.1 最简单的Entry配置


**🔸 基础语法**

```javascript
// webpack.config.js
module.exports = {
  entry: './src/index.js'  // 👈 最简单的写法
}
```

**💡 这行代码的含义：**
- `entry`：告诉webpack入口在哪
- `'./src/index.js'`：从项目根目录的src文件夹下的index.js开始打包

### 2.2 单入口的完整配置结构


```javascript
// webpack.config.js 完整配置
const path = require('path')

module.exports = {
  entry: './src/index.js',           // 👈 入口文件
  output: {
    path: path.resolve(__dirname, 'dist'),  // 输出目录
    filename: 'bundle.js'                   // 输出文件名
  }
}
```

**📋 配置说明**
```
项目结构：
my-project/
├── src/
│   ├── index.js        ← 入口文件（开始打包的地方）
│   ├── utils.js        ← 被index.js引用的文件
│   └── style.css       ← 被index.js引用的样式
├── dist/
│   └── bundle.js       ← 打包后的文件
└── webpack.config.js   ← webpack配置文件
```

### 2.3 单入口适用场景


**🎯 什么时候用单入口？**

```
✅ 单页应用（SPA）：
- React、Vue等单页应用
- 只有一个HTML页面
- 所有功能都在一个页面内切换

✅ 简单项目：
- 个人博客
- 企业官网
- 学习项目
```

---

## 3. 📚 多入口配置详解


### 3.1 为什么需要多入口？


**🔸 现实场景**

```
🏢 企业网站结构：
├── 首页 (index.html)     ← 需要 index.js
├── 产品页 (product.html)  ← 需要 product.js  
├── 关于页 (about.html)    ← 需要 about.js
└── 后台管理 (admin.html)   ← 需要 admin.js

每个页面有不同功能 → 需要不同的入口文件
```

### 3.2 多入口对象配置


**🔸 基础多入口配置**

```javascript
module.exports = {
  entry: {
    index: './src/index.js',       // 首页入口
    product: './src/product.js',   // 产品页入口
    admin: './src/admin.js'        // 管理页入口
  },
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: '[name].bundle.js'  // 👈 [name]会被替换成入口名
  }
}
```

**📋 打包结果**
```
打包前：
src/
├── index.js
├── product.js  
└── admin.js

打包后：
dist/
├── index.bundle.js     ← 对应index入口
├── product.bundle.js   ← 对应product入口
└── admin.bundle.js     ← 对应admin入口
```

### 3.3 多入口配置进阶


**🔸 数组形式的入口**

```javascript
module.exports = {
  entry: {
    // 👇 一个入口可以包含多个文件
    vendor: ['lodash', 'jquery'],        // 第三方库合并
    app: ['./src/index.js', './src/polyfill.js']  // 应用代码合并
  }
}
```

**💡 什么时候用数组形式？**
- 把多个文件打包到同一个bundle
- 第三方库单独打包
- 添加polyfill垫片

---

## 4. ⚡ 动态入口配置


### 4.1 什么是动态入口？


**🔸 通俗理解**

```
静态入口：写死在配置文件里
entry: './src/index.js'  ← 固定不变

动态入口：根据条件动态生成
entry: getEntries()      ← 运行时决定
```

### 4.2 函数形式的动态入口


**🔸 基础动态配置**

```javascript
module.exports = {
  entry: () => {
    // 👇 根据环境返回不同入口
    if (process.env.NODE_ENV === 'development') {
      return './src/index.dev.js'
    } else {
      return './src/index.prod.js'
    }
  }
}
```

### 4.3 实际应用：自动扫描入口文件


**🔸 自动找到所有页面入口**

```javascript
const glob = require('glob')
const path = require('path')

function getEntries() {
  const entries = {}
  
  // 👇 自动扫描src/pages/下的所有入口文件
  const files = glob.sync('./src/pages/*/index.js')
  
  files.forEach(file => {
    // 提取页面名：./src/pages/home/index.js → home
    const pageName = path.basename(path.dirname(file))
    entries[pageName] = file
  })
  
  return entries
}

module.exports = {
  entry: getEntries()
}
```

**📋 项目结构示例**
```
src/pages/
├── home/index.js       ← 自动识别为 home 入口
├── product/index.js    ← 自动识别为 product 入口
└── about/index.js      ← 自动识别为 about 入口
```

---

## 5. 📁 路径配置与path模块


### 5.1 相对路径 vs 绝对路径


**🔸 相对路径：以当前位置为起点**

```javascript
entry: './src/index.js'    // 👈 相对于webpack.config.js的位置
entry: '../app/main.js'    // 👈 上一级目录的app文件夹
```

**🔸 绝对路径：完整的文件路径**

```javascript
entry: '/Users/username/project/src/index.js'  // 👈 Mac/Linux完整路径
entry: 'C:\\Users\\username\\project\\src\\index.js'  // 👈 Windows完整路径
```

**⚠️ 为什么不推荐绝对路径？**
- 换电脑就不能用了
- 团队合作时路径不一致
- 部署到服务器时路径错误

### 5.2 path模块的作用


**🔸 为什么需要path模块？**

```javascript
// ❌ 硬编码路径（不推荐）
output: {
  path: '/Users/zhang/project/dist'  // 只在我的电脑能用
}

// ✅ 使用path模块（推荐）
const path = require('path')
output: {
  path: path.resolve(__dirname, 'dist')  // 任何电脑都能用
}
```

### 5.3 path模块常用方法


**🔸 path.resolve() - 生成绝对路径**

```javascript
const path = require('path')

// __dirname 表示当前文件所在目录
console.log(__dirname)  // 输出：/Users/zhang/my-project

// path.resolve 把相对路径转成绝对路径
console.log(path.resolve(__dirname, 'dist'))  
// 输出：/Users/zhang/my-project/dist

console.log(path.resolve(__dirname, 'src', 'components'))
// 输出：/Users/zhang/my-project/src/components
```

**🔸 webpack配置中的应用**

```javascript
const path = require('path')

module.exports = {
  entry: './src/index.js',  // 入口用相对路径就够了
  output: {
    path: path.resolve(__dirname, 'dist'),  // 输出用绝对路径
    filename: 'bundle.js'
  }
}
```

---

## 6. 🎯 实际应用场景对比


### 6.1 单页应用（SPA）配置


**🔸 典型的React/Vue项目**

```javascript
// SPA项目的webpack配置
module.exports = {
  entry: './src/main.js',  // 👈 只有一个入口
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'app.bundle.js'
  }
}
```

**📋 SPA特点**
- ✅ **一个HTML页面**：所有内容在一个页面内切换
- ✅ **一个入口文件**：通常是main.js或index.js
- ✅ **路由控制**：用JavaScript控制页面切换

### 6.2 多页应用（MPA）配置


**🔸 传统的多页面网站**

```javascript
// MPA项目的webpack配置
module.exports = {
  entry: {
    index: './src/pages/index/main.js',      // 首页
    product: './src/pages/product/main.js',  // 产品页
    about: './src/pages/about/main.js'       // 关于页
  },
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: '[name].bundle.js'  // 👈 每个页面独立的bundle
  }
}
```

**📋 MPA特点**
- ✅ **多个HTML页面**：每个功能一个页面
- ✅ **多个入口文件**：每个页面有自己的入口
- ✅ **服务器路由**：通过URL跳转不同页面

### 6.3 第三方库分离策略


**🔸 为什么要分离第三方库？**

```
问题：把所有代码打包到一起
├── app.bundle.js (2MB)  ← 包含业务代码+第三方库
    ├── 我的业务代码 (200KB)
    └── lodash+jquery+react (1.8MB)

每次修改业务代码 → 整个2MB都要重新下载 😱
```

**🔸 分离后的配置**

```javascript
module.exports = {
  entry: {
    vendor: ['lodash', 'jquery', 'react'],  // 👈 第三方库单独打包
    app: './src/main.js'                    // 👈 业务代码单独打包
  },
  output: {
    filename: '[name].bundle.js'
  }
}
```

**📋 分离后的优势**
```
打包结果：
├── vendor.bundle.js (1.8MB)  ← 第三方库（很少变化）
└── app.bundle.js (200KB)     ← 业务代码（经常变化）

修改业务代码 → 只需重新下载200KB ✅
```

---

## 7. 📋 核心要点总结


### 7.1 必须掌握的核心概念


```
🔸 Entry入口：webpack开始打包的起点文件
🔸 单入口：适用于SPA单页应用，一个entry指向一个文件
🔸 多入口：适用于MPA多页应用，entry对象包含多个入口
🔸 动态入口：用函数形式根据条件动态生成入口配置
🔸 路径配置：入口用相对路径，输出用path.resolve()生成绝对路径
```

### 7.2 配置选择指导


**🔹 什么时候用什么配置？**

| 项目类型 | Entry配置 | 适用场景 | 示例 |
|---------|-----------|----------|------|
| **单页应用** | `entry: './src/index.js'` | React、Vue项目 | 管理后台、在线工具 |
| **多页网站** | `entry: { home: '...', about: '...' }` | 企业官网、电商网站 | 公司官网、商城首页 |
| **组件库** | `entry: { lib: './src/lib.js' }` | 发布npm包 | UI组件库、工具库 |
| **复杂项目** | `entry: getEntries()` | 大型项目 | 微前端、多团队协作 |

### 7.3 常见问题解决


**🔹 路径问题**
```javascript
// ❌ 常见错误
entry: 'src/index.js'  // 缺少 ./

// ✅ 正确写法  
entry: './src/index.js'  // 相对路径要有 ./
```

**🔹 输出文件名问题**
```javascript
// 多入口时必须用 [name] 占位符
output: {
  filename: '[name].bundle.js'  // ✅ 正确
  // filename: 'bundle.js'      // ❌ 多入口会冲突
}
```

### 7.4 最佳实践建议


**🎯 实际项目建议**

```javascript
// 👍 推荐的基础配置模板
const path = require('path')

module.exports = {
  // 入口配置：根据项目类型选择
  entry: './src/index.js',  // SPA项目
  // entry: { ... },         // MPA项目
  
  // 输出配置：统一使用绝对路径
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js'  // 单入口
    // filename: '[name].bundle.js'  // 多入口
  }
}
```

**核心记忆**：
- Entry是webpack的起点，告诉它从哪开始打包
- 单入口用字符串，多入口用对象，动态入口用函数
- 路径配置：入口相对路径，输出绝对路径
- 选择配置：SPA用单入口，MPA用多入口