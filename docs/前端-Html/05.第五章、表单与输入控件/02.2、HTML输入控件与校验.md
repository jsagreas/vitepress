---
title: 2、HTML输入控件与校验
---
## 📚 目录

1. [基础输入控件](#1-基础输入控件)
2. [选择类控件](#2-选择类控件)
3. [按钮与提交控件](#3-按钮与提交控件)
4. [表单校验机制](#4-表单校验机制)
5. [核心要点总结](#5-核心要点总结)

---

## 1. 📝 基础输入控件


### 1.1 文本输入类型


**🔸 基本文本输入**
```html
<!-- 普通文本 -->
<input type="text" name="username" placeholder="请输入用户名">

<!-- 密码输入 -->
<input type="password" name="password" placeholder="请输入密码">

<!-- 邮箱输入 -->
<input type="email" name="email" placeholder="example@domain.com">

<!-- 数字输入 -->
<input type="number" name="age" min="1" max="120" step="1">

<!-- 电话号码 -->
<input type="tel" name="phone" placeholder="138-0000-0000">

<!-- 网址输入 -->
<input type="url" name="website" placeholder="https://example.com">
```

**📋 输入类型特性对比**

| **类型** | **校验特性** | **移动端键盘** | **常用属性** |
|---------|-------------|--------------|-------------|
| `text` | `无特殊校验` | `普通键盘` | `placeholder, maxlength` |
| `password` | `无特殊校验` | `普通键盘` | `minlength, maxlength` |
| `email` | `邮箱格式校验` | `邮箱键盘(@符号)` | `placeholder, multiple` |
| `number` | `数字校验` | `数字键盘` | `min, max, step` |
| `tel` | `无特殊校验` | `拨号键盘` | `placeholder, pattern` |
| `url` | `URL格式校验` | `URL键盘(.com)` | `placeholder` |

### 1.2 日期时间输入


**🔸 日期时间控件**
```html
<!-- 日期选择 -->
<input type="date" name="birthday" min="1900-01-01" max="2024-12-31">

<!-- 时间选择 -->
<input type="time" name="meeting_time" step="300">

<!-- 日期时间 -->
<input type="datetime-local" name="appointment">

<!-- 月份选择 -->
<input type="month" name="birth_month">

<!-- 周选择 -->
<input type="week" name="vacation_week">
```

### 1.3 其他特殊输入


**🔸 特殊输入类型**
```html
<!-- 文件上传 -->
<input type="file" name="avatar" accept="image/*">
<input type="file" name="documents" accept=".pdf,.doc,.docx" multiple>

<!-- 颜色选择器 -->
<input type="color" name="theme_color" value="#ff0000">

<!-- 范围滑块 -->
<input type="range" name="volume" min="0" max="100" value="50" step="5">

<!-- 搜索框 -->
<input type="search" name="keyword" placeholder="搜索内容...">

<!-- 隐藏字段 -->
<input type="hidden" name="user_id" value="12345">
```

### 1.4 多行文本输入


**🔸 textarea文本域**
```html
<!-- 基本文本域 -->
<textarea name="description" rows="4" cols="50" placeholder="请输入描述..."></textarea>

<!-- 带字数限制 -->
<textarea name="comment" 
          rows="6" 
          maxlength="500" 
          placeholder="最多输入500字..."></textarea>

<!-- 禁用调整大小 -->
<textarea name="feedback" 
          rows="5" 
          style="resize: none;" 
          placeholder="反馈建议..."></textarea>
```

---

## 2. ☑️ 选择类控件


### 2.1 单选按钮


**🔸 radio单选组**
```html
<fieldset>
    <legend>选择性别：</legend>
    
    <label>
        <input type="radio" name="gender" value="male" checked>
        男
    </label>
    
    <label>
        <input type="radio" name="gender" value="female">
        女
    </label>
    
    <label>
        <input type="radio" name="gender" value="other">
        其他
    </label>
</fieldset>
```

**📋 radio特性**
```
分组：相同name属性的radio为一组
单选：一组中只能选择一个
默认选中：checked属性
必需值：value属性定义提交值
```

### 2.2 复选框


**🔸 checkbox多选**
```html
<fieldset>
    <legend>选择爱好：</legend>
    
    <label>
        <input type="checkbox" name="hobbies[]" value="reading">
        阅读
    </label>
    
    <label>
        <input type="checkbox" name="hobbies[]" value="sports" checked>
        运动
    </label>
    
    <label>
        <input type="checkbox" name="hobbies[]" value="music">
        音乐
    </label>
    
    <label>
        <input type="checkbox" name="hobbies[]" value="travel">
        旅行
    </label>
</fieldset>
```

### 2.3 下拉选择框


**🔸 select基本用法**
```html
<!-- 单选下拉框 -->
<label for="city">选择城市：</label>
<select id="city" name="city">
    <option value="">请选择</option>
    <option value="beijing">北京</option>
    <option value="shanghai" selected>上海</option>
    <option value="guangzhou">广州</option>
    <option value="shenzhen">深圳</option>
</select>

<!-- 多选下拉框 -->
<label for="skills">技能选择：</label>
<select id="skills" name="skills[]" multiple size="4">
    <option value="html">HTML</option>
    <option value="css">CSS</option>
    <option value="js" selected>JavaScript</option>
    <option value="react" selected>React</option>
    <option value="vue">Vue</option>
</select>
```

**🔸 optgroup分组**
```html
<label for="course">选择课程：</label>
<select id="course" name="course">
    <optgroup label="前端技术">
        <option value="html">HTML基础</option>
        <option value="css">CSS样式</option>
        <option value="js">JavaScript</option>
    </optgroup>
    
    <optgroup label="后端技术">
        <option value="node">Node.js</option>
        <option value="python">Python</option>
        <option value="java">Java</option>
    </optgroup>
    
    <optgroup label="数据库">
        <option value="mysql">MySQL</option>
        <option value="mongodb">MongoDB</option>
    </optgroup>
</select>
```

### 2.4 选择控件对比


| **控件类型** | **选择方式** | **数据提交** | **适用场景** |
|-------------|-------------|-------------|-------------|
| `radio` | `单选` | `单个值` | `性别、状态选择` |
| `checkbox` | `多选` | `数组值` | `爱好、权限选择` |
| `select单选` | `下拉单选` | `单个值` | `城市、分类选择` |
| `select多选` | `下拉多选` | `数组值` | `技能、标签选择` |

---

## 3. 🔘 按钮与提交控件


### 3.1 input按钮类型


**🔸 input按钮**
```html
<!-- 提交按钮 -->
<input type="submit" value="提交表单">

<!-- 重置按钮 -->
<input type="reset" value="重置表单">

<!-- 普通按钮 -->
<input type="button" value="点击按钮" onclick="doSomething()">

<!-- 图片按钮 -->
<input type="image" src="submit-btn.png" alt="提交" width="100" height="30">
```

### 3.2 button元素


**🔸 button标签优势**
```html
<!-- 提交按钮 -->
<button type="submit">
    <i class="icon-send"></i>
    提交表单
</button>

<!-- 重置按钮 -->
<button type="reset">
    <i class="icon-refresh"></i>
    重置
</button>

<!-- 普通按钮 -->
<button type="button" onclick="handleClick()">
    <i class="icon-save"></i>
    保存草稿
</button>

<!-- 默认是submit类型 -->
<button>提交</button>  <!-- 等同于 type="submit" -->
```

**📋 button vs input对比**
```
button优势：
✅ 可以包含HTML内容（图标、文字等）
✅ 更灵活的样式控制
✅ 更好的语义化
✅ 支持伪元素

input优势：
✅ 语法简洁
✅ 兼容性更好（老版本浏览器）
```

### 3.3 按钮状态控制


**🔸 禁用和只读**
```html
<!-- 禁用按钮 -->
<button type="submit" disabled>提交中...</button>

<!-- 条件禁用 -->
<button type="submit" id="submitBtn">提交</button>
<script>
// JavaScript控制按钮状态
document.getElementById('submitBtn').disabled = true;
</script>

<!-- 表单验证后启用 -->
<form id="myForm">
    <input type="email" required id="email">
    <button type="submit" disabled id="submit">提交</button>
</form>

<script>
document.getElementById('email').addEventListener('input', function() {
    const submitBtn = document.getElementById('submit');
    submitBtn.disabled = !this.validity.valid;
});
</script>
```

---

## 4. ✅ 表单校验机制


### 4.1 HTML5内置校验属性


**🔸 必填校验**
```html
<!-- 必填字段 -->
<input type="text" name="username" required>
<input type="email" name="email" required>
<textarea name="message" required></textarea>
<select name="category" required>
    <option value="">请选择</option>
    <option value="tech">技术</option>
</select>
```

**🔸 长度校验**
```html
<!-- 最小长度 -->
<input type="password" name="password" minlength="8" required>

<!-- 最大长度 -->
<input type="text" name="username" maxlength="20">

<!-- 组合使用 -->
<input type="text" name="nickname" minlength="2" maxlength="10" required>
```

**🔸 数值范围校验**
```html
<!-- 数字范围 -->
<input type="number" name="age" min="18" max="65" required>

<!-- 日期范围 -->
<input type="date" name="start_date" min="2024-01-01" max="2024-12-31">

<!-- 步长控制 -->
<input type="number" name="price" min="0" step="0.01" placeholder="0.00">
```

### 4.2 模式匹配校验


**🔸 pattern属性**
```html
<!-- 手机号码 -->
<input type="tel" 
       name="phone" 
       pattern="^1[3-9]\d{9}$" 
       title="请输入11位手机号码">

<!-- 身份证号 -->
<input type="text" 
       name="id_card" 
       pattern="^(\d{15}|\d{18})$" 
       title="请输入15位或18位身份证号">

<!-- 用户名规则 -->
<input type="text" 
       name="username" 
       pattern="^[a-zA-Z][a-zA-Z0-9_]{3,15}$" 
       title="以字母开头，4-16位字母数字下划线">

<!-- 邮政编码 -->
<input type="text" 
       name="zipcode" 
       pattern="\d{6}" 
       title="请输入6位邮政编码">
```

### 4.3 文件上传校验


**🔸 accept属性**
```html
<!-- 图片文件 -->
<input type="file" 
       name="avatar" 
       accept="image/*" 
       required>

<!-- 特定图片格式 -->
<input type="file" 
       name="photo" 
       accept="image/jpeg,image/png,image/gif">

<!-- 文档文件 -->
<input type="file" 
       name="resume" 
       accept=".pdf,.doc,.docx">

<!-- 多文件上传 -->
<input type="file" 
       name="documents[]" 
       accept=".pdf,.jpg,.png" 
       multiple>
```

### 4.4 校验状态样式


**🔸 CSS伪类选择器**
```html
<style>
/* 必填字段样式 */
input:required {
    border-left: 3px solid #ff6b6b;
}

/* 有效输入样式 */
input:valid {
    border-color: #51cf66;
    background-color: #f8fff8;
}

/* 无效输入样式 */
input:invalid {
    border-color: #ff6b6b;
    background-color: #fff8f8;
}

/* 焦点状态 */
input:focus:invalid {
    box-shadow: 0 0 5px rgba(255, 107, 107, 0.5);
}

/* 可选字段样式 */
input:optional {
    border-left: 3px solid #74c0fc;
}
</style>

<form>
    <input type="email" required placeholder="必填邮箱">
    <input type="tel" placeholder="可选电话">
    <input type="password" minlength="8" required placeholder="至少8位密码">
</form>
```

### 4.5 校验反馈信息


**🔸 自定义校验消息**
```html
<form>
    <input type="email" 
           name="email" 
           required 
           title="请输入有效的邮箱地址">
    
    <input type="password" 
           name="password" 
           required 
           minlength="8" 
           title="密码至少8位字符">
    
    <input type="text" 
           name="username" 
           required 
           pattern="^[a-zA-Z][a-zA-Z0-9_]{3,15}$" 
           title="用户名4-16位，以字母开头">
    
    <button type="submit">注册</button>
</form>
```

**🔸 JavaScript自定义校验**
```html
<script>
// 自定义校验消息
document.querySelector('input[type="email"]').addEventListener('invalid', function(e) {
    if (this.validity.valueMissing) {
        this.setCustomValidity('请输入邮箱地址');
    } else if (this.validity.typeMismatch) {
        this.setCustomValidity('请输入正确的邮箱格式');
    }
});

// 输入时清除自定义消息
document.querySelector('input[type="email"]').addEventListener('input', function() {
    this.setCustomValidity('');
});
</script>
```

---

## 5. 📋 核心要点总结


### 5.1 必知要点


```
🔸 输入类型：text、password、email、number等自带校验
🔸 选择控件：radio单选、checkbox多选、select下拉
🔸 文件上传：type="file" + accept属性限制文件类型
🔸 按钮类型：submit提交、reset重置、button普通
🔸 校验属性：required、pattern、min/max、minlength/maxlength
🔸 CSS伪类：:valid、:invalid、:required用于样式控制
```

### 5.2 输入类型选择指南


**🎯 快速选择输入类型**
```
用户名密码  → text、password
邮箱地址    → email（自动校验）
数字输入    → number（数字键盘）
电话号码    → tel（拨号键盘）
网址输入    → url（URL键盘）
日期选择    → date（日期选择器）
文件上传    → file + accept
多行文本    → textarea
```

### 5.3 校验属性组合


**✅ 常用校验组合**
```html
<!-- 用户名：4-16位，字母开头 -->
<input type="text" 
       name="username" 
       required 
       minlength="4" 
       maxlength="16" 
       pattern="^[a-zA-Z][a-zA-Z0-9_]*$">

<!-- 密码：至少8位，包含字母数字 -->
<input type="password" 
       name="password" 
       required 
       minlength="8" 
       pattern="^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d@$!%*?&]{8,}$">

<!-- 手机号：11位数字 -->
<input type="tel" 
       name="phone" 
       required 
       pattern="^1[3-9]\d{9}$">

<!-- 年龄：18-120岁 -->
<input type="number" 
       name="age" 
       required 
       min="18" 
       max="120">
```

### 5.4 最佳实践


**✅ 表单可用性优化**
```html
<form novalidate>  <!-- 禁用默认校验，使用自定义 -->
    <div class="form-group">
        <label for="email">邮箱 *</label>
        <input type="email" 
               id="email" 
               name="email" 
               required 
               placeholder="example@domain.com"
               aria-describedby="email-help">
        <small id="email-help">用于接收通知和找回密码</small>
    </div>
    
    <div class="form-group">
        <label for="avatar">头像</label>
        <input type="file" 
               id="avatar" 
               name="avatar" 
               accept="image/jpeg,image/png" 
               aria-describedby="avatar-help">
        <small id="avatar-help">支持JPG、PNG格式，最大2MB</small>
    </div>
    
    <button type="submit">提交</button>
</form>
```

**关键记忆**：
- 根据数据类型选择合适的input type
- 移动端会根据类型显示对应键盘
- required、pattern等属性提供基础校验
- CSS伪类:valid/:invalid控制校验样式
- 文件上传用accept限制文件类型