---
title: 2、Shell数组
---
## 📚 目录

1. [数组基本概念](#1-数组基本概念)
2. [索引数组操作](#2-索引数组操作)
3. [关联数组详解](#3-关联数组详解)
4. [数组遍历技巧](#4-数组遍历技巧)
5. [数组高级操作](#5-数组高级操作)
6. [多维数组模拟](#6-多维数组模拟)
7. [实际应用案例](#7-实际应用案例)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 数组基本概念


### 1.1 什么是Shell数组


**数组的本质**：数组就像一个有编号的储物柜，可以把多个数据放在里面，用编号来取用

```
现实类比：
公寓楼房间号 → 数组索引
101: 张三    → array[0]="张三"
102: 李四    → array[1]="李四"  
103: 王五    → array[2]="王五"
```

**Shell数组特点**：
- 🔸 **动态大小**：不用提前声明数组长度，需要多大就多大
- 🔸 **混合类型**：同一个数组可以存放数字、字符串等不同类型
- 🔸 **稀疏数组**：索引可以不连续，比如可以有array[0]和array[5]，中间空着

### 1.2 数组类型区别


| 数组类型 | **索引方式** | **支持版本** | **适用场景** |
|---------|------------|-------------|-------------|
| **索引数组** | `数字编号(0,1,2...)` | `所有bash版本` | `列表数据、批量处理` |
| **关联数组** | `字符串键名` | `bash 4.0+` | `配置项、映射关系` |

**通俗理解**：
- **索引数组**：像学号，按顺序编号 1、2、3...
- **关联数组**：像姓名，用有意义的名字做标识

---

## 2. 📦 索引数组操作


### 2.1 数组定义方法


**方法一：直接赋值**
```bash
# 单个元素赋值
fruits[0]="苹果"
fruits[1]="香蕉"  
fruits[2]="橘子"

echo ${fruits[0]}  # 输出：苹果
```

**方法二：批量定义**
```bash
# 一次性定义多个元素
fruits=("苹果" "香蕉" "橘子" "葡萄")

# 指定索引定义（可以跳过某些索引）
colors=([0]="红色" [2]="蓝色" [5]="绿色")
```

**方法三：动态添加**
```bash
# 从空数组开始
numbers=()
numbers[0]=100
numbers[1]=200
numbers[10]=1000  # 跳过中间索引也可以
```

### 2.2 数组元素访问


**基本访问语法**：`${array[index]}`

```bash
fruits=("苹果" "香蕉" "橘子")

# 访问单个元素
echo ${fruits[0]}     # 输出：苹果
echo ${fruits[1]}     # 输出：香蕉

# 访问所有元素的两种方式
echo ${fruits[@]}     # 输出：苹果 香蕉 橘子
echo ${fruits[*]}     # 输出：苹果 香蕉 橘子
```

> 💡 **重要区别**：`[@]` 和 `[*]` 在加双引号时行为不同
> - `"${array[@]}"` → 每个元素独立（推荐）
> - `"${array[*]}"` → 所有元素合并成一个字符串

### 2.3 数组长度获取


**获取数组长度**：`${#array[@]}`

```bash
fruits=("苹果" "香蕉" "橘子" "葡萄")

echo "水果数量：${#fruits[@]}"    # 输出：4
echo "第一个水果长度：${#fruits[0]}"  # 输出：2（苹果的字符长度）
```

**稀疏数组长度示例**：
```bash
sparse=([0]="第一个" [5]="第六个" [10]="第十一个")
echo "数组长度：${#sparse[@]}"    # 输出：3（只计算有值的元素）
```

### 2.4 数组索引获取


**获取所有索引**：`${!array[@]}`

```bash
colors=([1]="红色" [3]="蓝色" [7]="绿色")

echo "所有索引：${!colors[@]}"    # 输出：1 3 7
```

---

## 3. 🗝️ 关联数组详解


### 3.1 关联数组概念


**什么是关联数组**：用有意义的字符串作为索引，而不是数字

```
传统数组：student[0]="张三"  student[1]="李四" 
关联数组：student["name"]="张三"  student["age"]="20"
```

> ⚠️ **版本要求**：关联数组需要 bash 4.0 或更高版本

### 3.2 关联数组声明和使用


**声明关联数组**：必须先用 `declare -A` 声明

```bash
# 声明关联数组
declare -A student

# 赋值方式一：逐个赋值
student["姓名"]="张三"
student["年龄"]="20"
student["专业"]="计算机"

# 赋值方式二：批量赋值
declare -A config=(
    ["host"]="127.0.0.1"
    ["port"]="3306" 
    ["database"]="mydb"
)
```

**访问关联数组**：
```bash
echo "学生姓名：${student['姓名']}"
echo "数据库配置：${config['host']}:${config['port']}"

# 获取所有键名
echo "配置项：${!config[@]}"  # 输出：host port database
```

### 3.3 关联数组实用场景


**配置文件模拟**：
```bash
declare -A server_config=(
    ["web_port"]="80"
    ["db_host"]="192.168.1.100"
    ["cache_size"]="1024"
    ["log_level"]="info"
)

# 使用配置
echo "启动Web服务，端口：${server_config['web_port']}"
echo "连接数据库：${server_config['db_host']}"
```

---

## 4. 🔄 数组遍历技巧


### 4.1 基本遍历方法


**遍历数组元素**：
```bash
fruits=("苹果" "香蕉" "橘子" "葡萄")

# 方法一：for循环遍历元素
for fruit in "${fruits[@]}"; do
    echo "水果：$fruit"
done

# 方法二：传统for循环
for ((i=0; i<${#fruits[@]}; i++)); do
    echo "第$((i+1))个水果：${fruits[i]}"
done
```

**遍历数组索引**：
```bash
colors=([1]="红色" [3]="蓝色" [7]="绿色")

# 遍历索引和值
for index in "${!colors[@]}"; do
    echo "索引$index：${colors[index]}"
done
```

### 4.2 关联数组遍历


```bash
declare -A person=(
    ["name"]="张三"
    ["age"]="25"  
    ["city"]="北京"
)

# 遍历键值对
for key in "${!person[@]}"; do
    echo "$key: ${person[$key]}"
done
```

### 4.3 带条件的遍历


```bash
numbers=(10 25 33 42 51 66)

echo "大于30的数字："
for num in "${numbers[@]}"; do
    if [ $num -gt 30 ]; then
        echo "  $num"
    fi
done
```

---

## 5. 🛠️ 数组高级操作


### 5.1 数组切片操作


**切片语法**：`${array[@]:start:length}`

```bash
fruits=("苹果" "香蕉" "橘子" "葡萄" "西瓜" "桃子")

# 从索引2开始取3个元素
echo ${fruits[@]:2:3}    # 输出：橘子 葡萄 西瓜

# 从索引1开始取到末尾
echo ${fruits[@]:1}      # 输出：香蕉 橘子 葡萄 西瓜 桃子

# 取最后2个元素
echo ${fruits[@]: -2}    # 输出：西瓜 桃子
```

**实用切片示例**：
```bash
logs=("error.log" "access.log" "debug.log" "system.log" "app.log")

# 取前3个日志文件
recent_logs=(${logs[@]:0:3})
echo "最近的日志：${recent_logs[@]}"
```

### 5.2 数组元素添加


**追加元素**：
```bash
fruits=("苹果" "香蕉")

# 方法一：使用+=
fruits+=("橘子" "葡萄")
echo ${fruits[@]}    # 输出：苹果 香蕉 橘子 葡萄

# 方法二：指定索引
fruits[${#fruits[@]}]="西瓜"  # 在末尾追加
```

**在开头插入**：
```bash
fruits=("香蕉" "橘子")
fruits=("苹果" "${fruits[@]}")  # 在开头插入苹果
echo ${fruits[@]}    # 输出：苹果 香蕉 橘子
```

### 5.3 数组元素删除


**删除特定索引元素**：
```bash
fruits=("苹果" "香蕉" "橘子" "葡萄")
unset fruits[1]  # 删除香蕉

echo ${fruits[@]}      # 输出：苹果 橘子 葡萄
echo ${!fruits[@]}     # 输出：0 2 3（索引1被删除）
```

**删除整个数组**：
```bash
unset fruits    # 删除整个数组
```

### 5.4 数组元素替换


**字符串替换**：
```bash
files=("test.txt" "data.txt" "config.txt")

# 将所有.txt替换为.bak
backup_files=("${files[@]/.txt/.bak}")
echo ${backup_files[@]}  # 输出：test.bak data.bak config.bak

# 只替换第一个匹配
echo ${files[@]/txt/log}  # 输出：test.log data.log config.log
```

---

## 6. 🏗️ 多维数组模拟


### 6.1 二维数组基本实现


**方法一：使用关联数组模拟**：
```bash
declare -A matrix

# 设置二维数组元素 matrix[行,列]
matrix[0,0]="A1"
matrix[0,1]="B1" 
matrix[1,0]="A2"
matrix[1,1]="B2"

# 访问元素
echo ${matrix[0,0]}   # 输出：A1
echo ${matrix[1,1]}   # 输出：B2
```

**方法二：使用索引计算**：
```bash
# 3x3矩阵，用一维数组存储
rows=3
cols=3
matrix=(1 2 3 4 5 6 7 8 9)

# 访问matrix[row][col] = matrix[row * cols + col]
get_element() {
    local row=$1
    local col=$2
    local index=$((row * cols + col))
    echo ${matrix[index]}
}

echo "第1行第2列：$(get_element 1 2)"  # 输出：6
```

### 6.2 复杂数据结构模拟


**学生信息管理**：
```bash
declare -A students

# 存储多个学生信息
students["张三,姓名"]="张三"
students["张三,年龄"]="20"
students["张三,专业"]="计算机"

students["李四,姓名"]="李四" 
students["李四,年龄"]="21"
students["李四,专业"]="电子"

# 查询学生信息
get_student_info() {
    local name=$1
    echo "姓名：${students["$name,姓名"]}"
    echo "年龄：${students["$name,年龄"]}"  
    echo "专业：${students["$name,专业"]}"
}

get_student_info "张三"
```

---

## 7. 💼 实际应用案例


### 7.1 日志文件批处理


```bash
#!/bin/bash

# 需要处理的日志文件列表
log_files=("access.log" "error.log" "debug.log" "system.log")

# 日志处理配置
declare -A log_config=(
    ["backup_dir"]="/backup/logs"
    ["compress"]="true"
    ["keep_days"]="30"
)

# 批量处理日志
process_logs() {
    echo "开始处理 ${#log_files[@]} 个日志文件..."
    
    for log_file in "${log_files[@]}"; do
        if [[ -f "$log_file" ]]; then
            echo "处理文件：$log_file"
            
            # 备份日志
            cp "$log_file" "${log_config['backup_dir']}"
            
            # 压缩处理
            if [[ ${log_config['compress']} == "true" ]]; then
                gzip "${log_config['backup_dir']}/$log_file"
            fi
        else
            echo "警告：文件 $log_file 不存在"
        fi
    done
}

process_logs
```

### 7.2 系统监控脚本


```bash
#!/bin/bash

# 监控指标配置
declare -A thresholds=(
    ["cpu_usage"]="80"
    ["memory_usage"]="85"
    ["disk_usage"]="90"
)

# 当前系统状态
declare -A current_status

# 获取系统状态
get_system_status() {
    current_status["cpu_usage"]=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'%' -f1)
    current_status["memory_usage"]=$(free | grep Mem | awk '{printf("%.0f", $3/$2 * 100.0)}')
    current_status["disk_usage"]=$(df -h / | awk 'NR==2 {print $5}' | cut -d'%' -f1)
}

# 检查告警
check_alerts() {
    for metric in "${!thresholds[@]}"; do
        current_value=${current_status[$metric]}
        threshold=${thresholds[$metric]}
        
        if (( current_value > threshold )); then
            echo "⚠️ 告警：$metric 当前值 ${current_value}% 超过阈值 ${threshold}%"
        else
            echo "✅ 正常：$metric 当前值 ${current_value}%"
        fi
    done
}

get_system_status
check_alerts
```

### 7.3 配置文件管理


```bash
#!/bin/bash

# 应用配置
declare -A app_config

# 从配置文件加载设置
load_config() {
    local config_file="app.conf"
    
    if [[ -f "$config_file" ]]; then
        while IFS='=' read -r key value; do
            # 跳过注释和空行
            [[ $key =~ ^[[:space:]]*# ]] && continue
            [[ -z $key ]] && continue
            
            # 去除首尾空格
            key=$(echo "$key" | xargs)
            value=$(echo "$value" | xargs)
            
            app_config["$key"]="$value"
        done < "$config_file"
    fi
}

# 使用配置
use_config() {
    echo "数据库主机：${app_config['db_host']:-localhost}"
    echo "数据库端口：${app_config['db_port']:-3306}"
    echo "调试模式：${app_config['debug_mode']:-false}"
}

load_config
use_config
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的核心概念


```
🔸 数组类型：索引数组（数字索引）vs 关联数组（字符串键）
🔸 访问语法：${array[index]} 访问元素，${array[@]} 访问所有
🔸 长度获取：${#array[@]} 获取元素个数
🔸 索引获取：${!array[@]} 获取所有索引
🔸 遍历方法：for循环遍历元素和索引
```

### 8.2 关键操作要点


**🔹 数组定义和访问**
```
定义：array=("元素1" "元素2" "元素3")
访问：${array[0]}、${array[@]}
长度：${#array[@]}
索引：${!array[@]}
```

**🔹 关联数组使用**
```
声明：declare -A array
定义：array["key"]="value"  
访问：${array["key"]}
遍历：for key in "${!array[@]}"
```

**🔹 高级操作技巧**
```
切片：${array[@]:start:length}
追加：array+=("新元素")
删除：unset array[index]
替换：${array[@]/old/new}
```

### 8.3 实际应用价值


**批量数据处理**：
- 文件列表批量操作
- 日志文件分析处理
- 系统监控数据收集

**配置管理**：
- 应用程序配置存储
- 环境变量组织管理
- 参数化脚本编写

**数据结构模拟**：
- 简单数据库表模拟
- 多维数据组织
- 复杂数据关系映射

### 8.4 使用建议和注意事项


> 💡 **最佳实践**：
> - 访问数组元素时总是使用双引号：`"${array[@]}"`
> - 关联数组必须先声明：`declare -A array`
> - 检查bash版本，关联数组需要4.0+

> ⚠️ **常见陷阱**：
> - 数组索引从0开始，不是1
> - 删除元素后索引可能不连续
> - `[*]`和`[@]`在加引号时行为不同

> 🚀 **性能提示**：
> - 大数据量时优先考虑外部工具（awk、sed）
> - 避免在循环中频繁计算数组长度
> - 合理使用数组切片减少不必要的遍历

**核心记忆口诀**：
```
数组定义用括号，访问元素花括号
索引从零不是一，长度井号加@号
关联数组先声明，字符索引更直观
遍历循环配引号，切片操作冒号分
```