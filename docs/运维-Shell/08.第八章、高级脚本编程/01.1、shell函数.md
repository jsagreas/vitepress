---
title: 1、shell函数
---
## 📚 目录

1. [函数基础概念](#1-函数基础概念)
2. [函数定义与调用语法](#2-函数定义与调用语法)
3. [函数参数传递机制](#3-函数参数传递机制)
4. [局部变量与作用域](#4-局部变量与作用域)
5. [函数返回值处理](#5-函数返回值处理)
6. [递归函数应用](#6-递归函数应用)
7. [函数重载模拟技巧](#7-函数重载模拟技巧)
8. [函数库设计与管理](#8-函数库设计与管理)
9. [函数编程最佳实践](#9-函数编程最佳实践)
10. [核心要点总结](#10-核心要点总结)

---

## 1. 🔧 函数基础概念


### 1.1 什么是Shell函数


**函数的本质**：函数就像是给一段代码起个名字，方便重复使用

```
生活类比：
做菜流程 → 函数定义
叫外卖   → 函数调用
不同菜品 → 不同参数
菜品结果 → 返回值
```

> 💡 **核心理解**  
> Shell函数是将多条命令组合成一个整体，通过函数名就能执行这一组命令

### 1.2 为什么需要函数


**解决的核心问题**：
- **🔄 代码重复**：相同的操作不用写多遍
- **📦 逻辑封装**：把复杂操作包装成简单调用
- **🧩 模块化**：大脚本拆分成小模块
- **🛠️ 维护性**：修改功能只需改一个地方

**实际应用场景**：
```
常见需求                    函数解决方案
检查文件是否存在           → check_file()
备份重要文件              → backup_file()  
发送通知消息              → send_alert()
格式化日志输出            → log_message()
```

---

## 2. 📝 函数定义与调用语法


### 2.1 函数定义的两种方式


**方式一：function关键字**
```bash
function 函数名() {
    # 函数体
    命令1
    命令2
}
```

**方式二：直接定义（推荐）**
```bash
函数名() {
    # 函数体  
    命令1
    命令2
}
```

> ⚠️ **语法要点**  
> 函数名后面的`()`是必须的，但里面不能写参数名

### 2.2 简单函数示例


```bash
# 定义问候函数
greet() {
    echo "欢迎使用我的脚本！"
    echo "今天是：$(date +%Y-%m-%d)"
}

# 调用函数（直接写函数名即可）
greet
```

**输出结果**：
```
欢迎使用我的脚本！
今天是：2025-09-19
```

### 2.3 函数调用的基本规则


**调用语法**：
```bash
函数名 参数1 参数2 参数3
```

**重要特点**：
- ✅ 函数必须先定义，后调用
- ✅ 调用时直接写函数名，不需要括号
- ✅ 参数用空格分隔，像命令行参数一样

```bash
# 错误示例 - 先调用后定义
say_hello    # ❌ 报错：command not found

say_hello() {
    echo "Hello World"
}

# 正确示例 - 先定义后调用  
say_hello() {
    echo "Hello World"
}
say_hello    # ✅ 正常输出
```

---

## 3. 📨 函数参数传递机制


### 3.1 位置参数的使用


Shell函数通过`$1, $2, $3...`来接收参数，就像脚本接收命令行参数一样

```bash
# 定义带参数的函数
create_file() {
    echo "创建文件：$1"
    echo "文件内容：$2" 
    touch "$1"
    echo "$2" > "$1"
}

# 调用函数并传递参数
create_file "test.txt" "这是测试内容"
```

### 3.2 参数相关的特殊变量


| 变量 | 含义 | 示例说明 |
|------|------|----------|
| `$0` | 脚本名（不是函数名） | `/home/user/script.sh` |
| `$1, $2...` | 第1个、第2个参数 | `$1="hello" $2="world"` |
| `$#` | 参数个数 | `create_file a b c` → `$#=3` |
| `$@` | 所有参数（分开） | `"$1" "$2" "$3"` |
| `$*` | 所有参数（合并） | `"$1 $2 $3"` |

### 3.3 参数处理实用示例


```bash
# 实用的文件操作函数
file_info() {
    # 检查参数数量
    if [ $# -eq 0 ]; then
        echo "用法: file_info <文件名>"
        return 1
    fi
    
    local filename="$1"
    
    # 检查文件是否存在
    if [ ! -f "$filename" ]; then
        echo "错误：文件 $filename 不存在"
        return 1
    fi
    
    # 显示文件信息
    echo "文件名: $filename"
    echo "大小: $(ls -lh "$filename" | awk '{print $5}')"
    echo "修改时间: $(ls -l "$filename" | awk '{print $6,$7,$8}')"
}

# 调用示例
file_info "/etc/passwd"
file_info                    # 显示用法提示
file_info "不存在.txt"        # 显示错误信息
```

### 3.4 处理多个参数的技巧


```bash
# 批量处理文件
batch_backup() {
    echo "开始批量备份，共 $# 个文件"
    
    # 遍历所有参数
    for file in "$@"; do
        if [ -f "$file" ]; then
            cp "$file" "${file}.backup"
            echo "✅ 已备份: $file"
        else
            echo "❌ 文件不存在: $file"
        fi
    done
}

# 使用示例
batch_backup file1.txt file2.txt file3.txt
```

---

## 4. 🏠 局部变量与作用域


### 4.1 变量作用域概念


**全局变量 vs 局部变量**：
```
全局变量：在整个脚本中都能访问
局部变量：只在函数内部有效
```

> 💡 **通俗理解**  
> 想象成房子：全局变量是客厅的东西，每个房间都能用；局部变量是卧室的东西，只有在卧室才能用

### 4.2 local关键字的使用


**语法**：`local 变量名=值`

```bash
# 演示作用域问题
name="小明"  # 全局变量

test_scope() {
    name="小红"        # 修改了全局变量！
    local age=20       # 局部变量
    echo "函数内：name=$name, age=$age"
}

test_scope
echo "函数外：name=$name"  # name被改变了！
# echo "函数外：age=$age"   # 错误！age不存在
```

**输出结果**：
```
函数内：name=小红, age=20
函数外：name=小红
```

### 4.3 正确使用局部变量


```bash
name="小明"  # 全局变量

safe_function() {
    local name="小红"   # 局部变量，不影响全局
    local age=20
    echo "函数内：name=$name, age=$age"
}

safe_function
echo "函数外：name=$name"  # 全局变量未被修改
```

**输出结果**：
```
函数内：name=小红, age=20
函数外：name=小明
```

> ⚠️ **最佳实践**  
> 函数内的变量都加上`local`关键字，避免意外修改全局变量

### 4.4 局部变量的实际应用


```bash
# 计算器函数示例
calculator() {
    local operation="$1"
    local num1="$2" 
    local num2="$3"
    local result
    
    case "$operation" in
        "add")
            result=$((num1 + num2))
            ;;
        "sub") 
            result=$((num1 - num2))
            ;;
        "mul")
            result=$((num1 * num2))
            ;;
        *)
            echo "不支持的操作: $operation"
            return 1
            ;;
    esac
    
    echo "计算结果: $num1 $operation $num2 = $result"
}

# 使用示例
calculator add 10 5    # 输出: 计算结果: 10 add 5 = 15
calculator mul 3 4     # 输出: 计算结果: 3 mul 4 = 12
```

---

## 5. 🔄 函数返回值处理


### 5.1 return语句的作用


**return的两个用途**：
- **🚪 提前退出**：在函数中间就结束执行
- **📊 返回状态码**：告诉调用者函数是否成功

```bash
check_positive() {
    local number="$1"
    
    # 输入检查
    if [ -z "$number" ]; then
        echo "错误：请提供一个数字"
        return 1    # 返回错误状态码
    fi
    
    # 判断是否为正数
    if [ "$number" -gt 0 ]; then
        echo "$number 是正数"
        return 0    # 返回成功状态码
    else
        echo "$number 不是正数" 
        return 2    # 返回特定错误码
    fi
}

# 使用示例
check_positive 5
echo "函数返回状态：$?"  # $? 获取上个命令的返回状态
```

### 5.2 获取函数返回状态


**三种获取方式**：

```bash
# 方式1：直接检查返回状态
if check_positive 10; then
    echo "检查通过"
else
    echo "检查失败"
fi

# 方式2：保存返回状态
check_positive -5
status=$?
case $status in
    0) echo "成功" ;;
    1) echo "参数错误" ;;
    2) echo "不是正数" ;;
esac

# 方式3：在条件中使用
check_positive 0 && echo "这不会执行" || echo "这会执行"
```

### 5.3 通过echo返回数据


由于return只能返回0-255的状态码，要返回实际数据需要用echo：

```bash
# 计算平方数
get_square() {
    local number="$1"
    local result=$((number * number))
    echo "$result"    # 通过echo"返回"结果
}

# 获取函数输出
square=$(get_square 5)
echo "5的平方是: $square"

# 直接在命令中使用
echo "10的平方是: $(get_square 10)"
```

### 5.4 复杂返回值处理


```bash
# 返回多个值的技巧
get_file_stats() {
    local filepath="$1"
    
    if [ ! -f "$filepath" ]; then
        echo "ERROR:文件不存在"
        return 1
    fi
    
    local size=$(ls -l "$filepath" | awk '{print $5}')
    local lines=$(wc -l < "$filepath")
    local words=$(wc -w < "$filepath")
    
    # 返回格式化的多个值
    echo "SIZE:$size LINES:$lines WORDS:$words"
    return 0
}

# 解析返回的多个值
result=$(get_file_stats "/etc/passwd")
if [ $? -eq 0 ]; then
    # 提取各个值
    size=$(echo "$result" | grep -o 'SIZE:[0-9]*' | cut -d: -f2)
    lines=$(echo "$result" | grep -o 'LINES:[0-9]*' | cut -d: -f2)
    words=$(echo "$result" | grep -o 'WORDS:[0-9]*' | cut -d: -f2)
    
    echo "文件大小: $size 字节"
    echo "行数: $lines"  
    echo "单词数: $words"
fi
```

---

## 6. 🔁 递归函数应用


### 6.1 递归函数概念


**递归的本质**：函数调用自己来解决问题

```
递归三要素：
1. 🎯 基准条件：什么时候停止递归
2. 🔄 递归调用：函数调用自己
3. 📈 问题缩小：每次递归问题规模变小
```

### 6.2 简单递归示例 - 阶乘计算


```bash
# 计算阶乘的递归函数
factorial() {
    local n="$1"
    
    # 基准条件：0! = 1, 1! = 1
    if [ "$n" -le 1 ]; then
        echo 1
        return
    fi
    
    # 递归调用：n! = n × (n-1)!
    local prev_result=$(factorial $((n - 1)))
    local result=$((n * prev_result))
    echo "$result"
}

# 使用示例
echo "5的阶乘是: $(factorial 5)"  # 输出: 120
echo "3的阶乘是: $(factorial 3)"  # 输出: 6
```

**执行过程图解**：
```
factorial(5)
├── 5 × factorial(4)
    ├── 4 × factorial(3)  
        ├── 3 × factorial(2)
            ├── 2 × factorial(1)
                └── return 1
            └── return 2
        └── return 6
    └── return 24
└── return 120
```

### 6.3 实用递归示例 - 目录遍历


```bash
# 递归遍历目录树
traverse_dir() {
    local dir="$1"
    local depth="$2"
    
    # 设置默认深度
    if [ -z "$depth" ]; then
        depth=0
    fi
    
    # 创建缩进
    local indent=""
    for ((i=0; i<depth; i++)); do
        indent="  $indent"
    done
    
    echo "${indent}📁 $(basename "$dir")"
    
    # 递归处理子目录和文件
    if [ -d "$dir" ]; then
        for item in "$dir"/*; do
            if [ -d "$item" ]; then
                traverse_dir "$item" $((depth + 1))  # 递归调用
            elif [ -f "$item" ]; then
                echo "${indent}  📄 $(basename "$item")"
            fi
        done
    fi
}

# 使用示例
traverse_dir "/home/user/Documents"
```

> ⚠️ **递归注意事项**  
> 1. 必须有明确的停止条件，否则会无限递归  
> 2. Shell的递归深度有限，不适合深度很大的递归  
> 3. 递归会消耗较多内存，大数据量时要小心

---

## 7. 🎭 函数重载模拟技巧


### 7.1 函数重载概念


**重载的含义**：同一个函数名，根据不同的参数个数或类型，执行不同的操作

> 💡 **Shell的限制**  
> Shell原生不支持函数重载，但可以通过技巧模拟实现

### 7.2 基于参数个数的重载


```bash
# 模拟重载的print函数
print() {
    case $# in
        0)
            # 无参数：打印空行
            echo
            ;;
        1) 
            # 一个参数：普通打印
            echo "$1"
            ;;
        2)
            # 两个参数：格式化打印
            printf "$1\n" "$2"
            ;;
        3)
            # 三个参数：带时间戳打印
            echo "[$(date '+%H:%M:%S')] $1: $2 ($3)"
            ;;
        *)
            # 多个参数：列表打印
            echo "多项内容:"
            local i=1
            for arg in "$@"; do
                echo "  $i. $arg"
                ((i++))
            done
            ;;
    esac
}

# 使用示例
print                           # 打印空行
print "Hello"                   # 普通打印
print "Name: %s" "张三"          # 格式化打印  
print "INFO" "启动成功" "系统"    # 带时间戳
print "苹果" "香蕉" "橙子" "葡萄" # 列表打印
```

### 7.3 基于参数类型的重载


```bash
# 智能处理不同类型的输入
smart_handler() {
    local input="$1"
    
    # 判断输入类型
    if [ -z "$input" ]; then
        echo "❌ 错误：空输入"
        return 1
        
    elif [ -f "$input" ]; then
        # 文件类型
        echo "📄 处理文件: $input"
        echo "   大小: $(ls -lh "$input" | awk '{print $5}')"
        echo "   行数: $(wc -l < "$input")"
        
    elif [ -d "$input" ]; then
        # 目录类型  
        echo "📁 处理目录: $input"
        echo "   文件数: $(find "$input" -type f | wc -l)"
        echo "   子目录数: $(find "$input" -type d | wc -l)"
        
    elif [[ "$input" =~ ^[0-9]+$ ]]; then
        # 数字类型
        echo "🔢 处理数字: $input"
        echo "   平方: $((input * input))"
        echo "   是否偶数: $([ $((input % 2)) -eq 0 ] && echo "是" || echo "否")"
        
    elif [[ "$input" =~ ^https?:// ]]; then
        # URL类型
        echo "🌐 处理URL: $input"
        echo "   检查连接状态..."
        curl -s --head "$input" | head -1
        
    else
        # 普通字符串
        echo "📝 处理文本: $input"
        echo "   长度: ${#input} 字符"
        echo "   单词数: $(echo "$input" | wc -w)"
    fi
}

# 使用示例  
smart_handler "/etc/passwd"           # 文件处理
smart_handler "/home"                 # 目录处理
smart_handler "42"                    # 数字处理
smart_handler "https://www.baidu.com" # URL处理
smart_handler "Hello World"           # 文本处理
```

---

## 8. 📚 函数库设计与管理


### 8.1 函数库的概念


**函数库**：把常用的函数集中在一个文件中，供其他脚本使用

```
函数库的优势：
📦 复用性：一次编写，到处使用
🔧 维护性：统一修改，全局生效  
📋 标准化：统一的接口和规范
🎯 专业性：每个库专注特定领域
```

### 8.2 创建函数库文件


**创建lib/common.sh**：
```bash
#!/bin/bash
# common.sh - 通用函数库
# Version: 1.0
# Author: Your Name

# 日志相关函数
log_info() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] INFO: $*"
}

log_error() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] ERROR: $*" >&2
}

log_warn() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] WARN: $*" >&2
}

# 文件操作函数
backup_file() {
    local file="$1"
    local backup_dir="${2:-./backup}"
    
    if [ ! -f "$file" ]; then
        log_error "文件不存在: $file"
        return 1
    fi
    
    mkdir -p "$backup_dir"
    local backup_name="$(basename "$file").$(date '+%Y%m%d_%H%M%S').backup"
    
    if cp "$file" "$backup_dir/$backup_name"; then
        log_info "备份成功: $file -> $backup_dir/$backup_name"
        return 0
    else
        log_error "备份失败: $file"
        return 1
    fi
}

# 网络相关函数
check_connectivity() {
    local host="${1:-www.baidu.com}"
    local timeout="${2:-5}"
    
    if ping -c 1 -W "$timeout" "$host" >/dev/null 2>&1; then
        log_info "网络连接正常 ($host)"
        return 0
    else
        log_error "网络连接失败 ($host)"
        return 1
    fi
}

# 字符串处理函数
trim_whitespace() {
    local str="$1"
    # 去除前后空白字符
    echo "$str" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
}

is_empty() {
    local str="$1"
    [ -z "$(trim_whitespace "$str")" ]
}
```

### 8.3 使用函数库


**方式一：source命令引入**
```bash
#!/bin/bash
# main_script.sh

# 引入函数库
source lib/common.sh

# 使用函数库中的函数
log_info "脚本开始执行"

if check_connectivity; then
    log_info "网络检查通过"
else
    log_error "网络检查失败"
    exit 1
fi

# 备份重要文件
backup_file "/etc/passwd" "./backup"
log_info "脚本执行完成"
```

**方式二：点操作符引入**
```bash
# . 是source的简写形式
. lib/common.sh

log_info "使用点操作符引入函数库"
```

### 8.4 函数库的组织结构


**推荐的目录结构**：
```
project/
├── lib/                    # 函数库目录
│   ├── common.sh          # 通用函数
│   ├── file_utils.sh      # 文件操作函数
│   ├── network_utils.sh   # 网络相关函数
│   └── string_utils.sh    # 字符串处理函数
├── scripts/               # 具体脚本
│   ├── backup.sh
│   └── monitor.sh  
└── config/               # 配置文件
    └── settings.conf
```

**文件操作函数库示例**：
```bash
# lib/file_utils.sh
#!/bin/bash

# 检查文件是否存在且可读
is_readable_file() {
    [ -f "$1" ] && [ -r "$1" ]
}

# 安全创建目录
safe_mkdir() {
    local dir="$1"
    if [ ! -d "$dir" ]; then
        mkdir -p "$dir" || {
            log_error "无法创建目录: $dir"
            return 1
        }
    fi
}

# 计算文件MD5
get_file_md5() {
    local file="$1"
    if is_readable_file "$file"; then
        md5sum "$file" | awk '{print $1}'
    else
        log_error "无法读取文件: $file"
        return 1
    fi
}
```

---

## 9. 🏆 函数编程最佳实践


### 9.1 函数命名规范


**好的函数名特征**：
- ✅ **动词开头**：`check_file`, `create_backup`
- ✅ **描述清晰**：一看就知道做什么
- ✅ **下划线分隔**：`get_user_info` 而不是 `getUserInfo`
- ✅ **避免缩写**：`calculate` 而不是 `calc`

```bash
# 好的命名示例
check_file_exists()     # 检查文件是否存在
send_email_alert()      # 发送邮件警报
backup_database()       # 备份数据库
validate_user_input()   # 验证用户输入

# 不好的命名示例  
cf()                    # 太简短，不知道干啥
doSomething()          # 没有具体含义
func1()                # 毫无意义的命名
```

### 9.2 函数设计原则


> 🎯 **单一职责原则**  
> 一个函数只做一件事，做好一件事

```bash
# 好的设计 - 职责单一
is_file_writable() {
    [ -w "$1" ]
}

create_backup_dir() {
    local dir="$1"
    mkdir -p "$dir"
}

backup_file_to_dir() {
    local source="$1"
    local target_dir="$2" 
    cp "$source" "$target_dir/"
}

# 不好的设计 - 职责混乱
backup_and_check_and_log() {  
    # 做了太多事情，难以维护
    local file="$1"
    [ -f "$file" ] && mkdir -p backup && cp "$file" backup/ && echo "备份完成"
}
```

### 9.3 错误处理最佳实践


```bash
# 完善的错误处理示例
robust_backup() {
    local source_file="$1"
    local backup_dir="$2"
    
    # 参数验证
    if [ $# -ne 2 ]; then
        log_error "用法: robust_backup <源文件> <备份目录>"
        return 1
    fi
    
    # 源文件检查
    if [ ! -f "$source_file" ]; then
        log_error "源文件不存在: $source_file"
        return 2
    fi
    
    if [ ! -r "$source_file" ]; then
        log_error "源文件不可读: $source_file" 
        return 3
    fi
    
    # 备份目录处理
    if [ ! -d "$backup_dir" ]; then
        log_info "创建备份目录: $backup_dir"
        if ! mkdir -p "$backup_dir"; then
            log_error "无法创建备份目录: $backup_dir"
            return 4
        fi
    fi
    
    # 执行备份
    local backup_file="$backup_dir/$(basename "$source_file").$(date +%Y%m%d_%H%M%S)"
    if cp "$source_file" "$backup_file"; then
        log_info "备份成功: $source_file -> $backup_file"
        return 0
    else
        log_error "备份失败: $source_file"
        return 5
    fi
}
```

### 9.4 函数文档化


```bash
######################################

# 发送邮件通知
# 全局变量:
#   SMTP_SERVER: SMTP服务器地址
#   SMTP_USER: 发送者邮箱
# 参数:
#   $1: 收件人邮箱
#   $2: 邮件主题  
#   $3: 邮件内容
# 返回值:
#   0: 发送成功
#   1: 参数错误
#   2: 配置错误
#   3: 发送失败
# 示例:
#   send_email "admin@company.com" "系统警报" "磁盘空间不足"
######################################

send_email() {
    local recipient="$1"
    local subject="$2" 
    local content="$3"
    
    # 函数实现...
}
```

### 9.5 性能优化建议


```bash
# 避免重复计算
get_system_info() {
    # 不好的做法 - 重复执行命令
    echo "CPU使用率: $(top -bn1 | grep "Cpu(s)" | awk '{print $2}')"
    echo "内存使用率: $(free | grep Mem | awk '{printf("%.2f%%", $3/$2*100)}')"
    echo "磁盘使用率: $(df / | tail -1 | awk '{print $5}')"
    
    # 好的做法 - 缓存计算结果
    local cpu_info=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}')
    local mem_info=$(free | grep Mem | awk '{printf("%.2f%%", $3/$2*100)}')
    local disk_info=$(df / | tail -1 | awk '{print $5}')
    
    echo "CPU使用率: $cpu_info"
    echo "内存使用率: $mem_info" 
    echo "磁盘使用率: $disk_info"
}

# 使用局部变量减少全局污染
process_data() {
    local input="$1"
    local temp_result
    local final_result
    
    # 使用local避免影响全局变量
    temp_result=$(echo "$input" | tr '[:lower:]' '[:upper:]')
    final_result=$(echo "$temp_result" | sed 's/[^A-Z]//g')
    
    echo "$final_result"
}
```

---

## 10. 📋 核心要点总结


### 10.1 必须掌握的核心概念


> 💡 **函数本质理解**  
> 函数是代码的打包和重用机制，让复杂脚本变得模块化和可维护

**🔸 函数定义与调用**：
- 语法：`函数名() { 命令; }`
- 调用：直接写函数名，不需要括号
- 顺序：必须先定义后调用

**🔸 参数传递机制**：
- `$1, $2, $3`：位置参数
- `$#`：参数个数
- `$@`：所有参数（推荐用法）

**🔸 局部变量重要性**：
- `local`关键字保护全局变量
- 避免变量污染和意外修改
- 提高代码安全性

### 10.2 关键理解要点


**🔹 返回值的双重含义**：
```
return语句的作用：
1. 提前退出函数执行
2. 返回状态码(0-255)

返回数据的方法：
- 用echo输出数据
- 用命令替换获取：result=$(函数名)
```

**🔹 递归函数要点**：
```
递归三要素：
1. 基准条件（停止条件）
2. 递归调用（调用自己）  
3. 问题规模缩小

注意事项：
- Shell递归深度有限
- 适合小规模问题
- 必须有明确的停止条件
```

**🔹 函数重载模拟**：
```
实现方式：
1. 基于参数个数：case $# in
2. 基于参数类型：正则表达式判断
3. 基于参数内容：条件分支处理
```

### 10.3 实际应用价值


**📈 提升脚本质量**：
- 模块化设计，易于维护
- 代码复用，减少重复工作
- 统一接口，降低复杂性

**🔧 常见应用场景**：
- **系统管理**：日志记录、文件备份、状态监控
- **自动化脚本**：批量处理、数据转换、环境部署
- **工具开发**：命令行工具、辅助脚本、配置管理

### 10.4 学习建议


**🎯 循序渐进学习路径**：
1. **基础语法**：先掌握定义和调用
2. **参数处理**：理解位置参数的使用
3. **局部变量**：养成使用local的习惯
4. **返回值**：区分状态码和数据返回
5. **高级特性**：递归和重载模拟
6. **工程实践**：函数库设计和管理

**🔨 实践建议**：
- 从简单函数开始，逐步增加复杂度
- 多写多练，在实际项目中应用
- 阅读优秀的Shell脚本代码
- 建立自己的函数库，积累常用功能

> 🏆 **核心记忆口诀**  
> 函数定义先于调用，参数传递用位置  
> 局部变量要记住，返回状态有区别  
> 递归函数需停止，重载模拟有技巧  
> 函数库里集精华，最佳实践要遵循