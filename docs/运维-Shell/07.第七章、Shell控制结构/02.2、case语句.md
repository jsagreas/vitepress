---
title: 2、case语句
---
## 📚 目录

1. [case语句基础概念](#1-case语句基础概念)
2. [case语句结构详解](#2-case语句结构详解)
3. [模式匹配机制](#3-模式匹配机制)
4. [多模式匹配技巧](#4-多模式匹配技巧)
5. [case vs if-elif选择指南](#5-case-vs-if-elif选择指南)
6. [菜单设计实战应用](#6-菜单设计实战应用)
7. [参数处理高级应用](#7-参数处理高级应用)
8. [核心要点总结](#8-核心要点总结)

---

## 1. 🎯 case语句基础概念


### 1.1 什么是case语句

**case语句**：Shell中用于**多分支选择**的控制结构，类似其他编程语言中的switch语句。

> 💡 **通俗理解**：就像生活中的**多选题**，根据不同的选项执行不同的操作

**现实生活类比**：
```
看天气决定穿衣：
- 晴天 → 穿轻便衣服
- 雨天 → 带雨伞
- 雪天 → 穿厚外套  
- 其他 → 正常穿衣
```

### 1.2 为什么需要case语句

**解决的问题**：当有**多个固定选项**需要判断时，比if-elif更简洁清晰

```bash
# 用if-elif写法（繁琐）
if [ "$1" = "start" ]; then
    echo "启动服务"
elif [ "$1" = "stop" ]; then
    echo "停止服务"  
elif [ "$1" = "restart" ]; then
    echo "重启服务"
elif [ "$1" = "status" ]; then
    echo "查看状态"
else
    echo "参数错误"
fi

# 用case写法（简洁）
case "$1" in
    start)   echo "启动服务" ;;
    stop)    echo "停止服务" ;;
    restart) echo "重启服务" ;;
    status)  echo "查看状态" ;;
    *)       echo "参数错误" ;;
esac
```

> 🔥 **核心优势**：代码更简洁、逻辑更清晰、维护更容易

---

## 2. 📋 case语句结构详解


### 2.1 基本语法结构

```bash
case 变量值 in
    模式1)
        命令1
        命令2
        ;;
    模式2)
        命令3
        ;;
    *)
        默认命令
        ;;
esac
```

**结构分解说明**：
```
case 表达式 in        ← 开始关键字，表达式是要匹配的值
    模式1)            ← 匹配模式，后面跟右括号
        执行命令      ← 匹配成功时执行的命令
        ;;            ← 双分号表示此分支结束
    *)                ← 通配符，匹配所有其他情况（可选）
        默认操作      
        ;;
esac                  ← 结束关键字（case倒写）
```

### 2.2 关键元素详解

**🔸 case关键字**
- 作用：开始多分支判断
- 位置：语句开头
- 后面：跟要判断的变量或表达式

**🔸 esac关键字**  
- 作用：结束case语句
- 特点：是case的倒写
- 位置：语句末尾
- **记忆技巧**：case倒过来就是esac

**🔸 双分号;;**
- 作用：表示当前分支结束
- 重要性：**必须有**，否则会继续执行下一个分支
- 位置：每个分支的最后一行

**🔸 通配符\***
- 作用：匹配所有其他情况
- 类似：if-else中的else
- 位置：通常放在最后作为默认选项

### 2.3 实际示例解析

```bash
#!/bin/bash
# 文件操作脚本

echo "请选择操作: [1]查看 [2]编辑 [3]删除"
read choice

case $choice in
    1)
        echo "=== 查看文件 ==="
        ls -la
        ;;
    2)
        echo "=== 编辑文件 ==="
        echo "请输入文件名:"
        read filename
        vi "$filename"
        ;;
    3)
        echo "=== 删除文件 ==="
        echo "危险操作，请确认!"
        ;;
    *)
        echo "❌ 无效选择，请输入1、2或3"
        ;;
esac
```

**执行流程**：
```
用户输入 → case匹配 → 执行对应分支 → ;; 结束 → esac退出
     ↓
   输入1 → 匹配模式1) → 执行ls命令 → ;; → 结束
   输入2 → 匹配模式2) → 执行编辑操作 → ;; → 结束  
   输入x → 没有匹配 → 执行*) → 显示错误 → ;; → 结束
```

---

## 3. 🔍 模式匹配机制


### 3.1 基础模式匹配

**精确匹配**：最常用的匹配方式
```bash
case "$var" in
    "hello")     echo "精确匹配hello" ;;
    "world")     echo "精确匹配world" ;;
    "123")       echo "精确匹配数字123" ;;
esac
```

> 💡 **提示**：建议给变量加双引号，避免空值或特殊字符问题

### 3.2 通配符模式匹配

**🔸 星号\*通配符**
```bash
case "$filename" in
    "*.txt")     echo "这是文本文件" ;;
    "*.jpg")     echo "这是图片文件" ;;
    "log*")      echo "这是日志文件" ;;
    "*backup*")  echo "这是备份文件" ;;
esac
```

**🔸 问号?通配符**
```bash
case "$code" in
    "a?c")       echo "匹配abc、adc、a1c等" ;;
    "??")        echo "匹配任意两个字符" ;;
    "test?")     echo "匹配test1、testA等" ;;
esac
```

**🔸 方括号[]字符集**
```bash
case "$grade" in
    [Aa])        echo "优秀成绩" ;;
    [Bb])        echo "良好成绩" ;;
    [0-9])       echo "数字成绩" ;;
    [a-z])       echo "小写字母" ;;
esac
```

### 3.3 模式匹配实战示例

```bash
#!/bin/bash
# 文件类型识别脚本

echo "请输入文件名："
read filename

case "$filename" in
    *.sh)
        echo "🔧 Shell脚本文件"
        echo "可以执行: chmod +x $filename"
        ;;
    *.txt|*.md)
        echo "📄 文本文档文件"  
        echo "可以查看: cat $filename"
        ;;
    *.jpg|*.png|*.gif)
        echo "🖼️ 图片文件"
        echo "可以查看: eog $filename"
        ;;
    [Dd]ocument*)
        echo "📋 以Document开头的文件"
        ;;
    *.[Ll][Oo][Gg])
        echo "📝 日志文件（大小写不敏感）"
        ;;
    *)
        echo "❓ 未知文件类型"
        ;;
esac
```

**模式匹配总结表格**：

| 模式 | **匹配规则** | **示例** | **说明** |
|------|-------------|----------|----------|
| `abc` | 精确匹配 | `abc` | 只匹配abc |
| `*` | 匹配任意字符串 | `hello`, `world` | 通配所有 |
| `a*` | a开头的字符串 | `apple`, `and` | a开头任意长度 |
| `*c` | c结尾的字符串 | `abc`, `c` | c结尾任意长度 |
| `a?c` | a和c之间一个字符 | `abc`, `a1c` | 固定长度3 |
| `[abc]` | 匹配a、b、c中任意一个 | `a`, `b`, `c` | 字符集合 |
| `[0-9]` | 匹配数字0-9 | `1`, `5`, `9` | 数字范围 |

---

## 4. 🔄 多模式匹配技巧


### 4.1 管道符|多模式匹配

**语法**：用`|`分隔多个模式，表示"或"的关系
```bash
case "$option" in
    start|begin|run)
        echo "🚀 启动操作"
        ;;
    stop|end|quit)
        echo "🛑 停止操作" 
        ;;
    help|h|-h|--help)
        echo "📖 显示帮助信息"
        ;;
esac
```

> ✅ **优势**：一个分支处理多个相似的输入，减少重复代码

### 4.2 复杂模式组合

```bash
#!/bin/bash
# 用户权限检查脚本

echo "请输入用户类型："
read usertype

case "$usertype" in
    admin|administrator|root)
        echo "👑 管理员用户"
        echo "拥有所有权限"
        ;;
    user|normal|guest)
        echo "👤 普通用户" 
        echo "拥有基础权限"
        ;;
    test*|demo*|temp*)
        echo "🧪 测试用户"
        echo "临时权限，限时使用"
        ;;
    [0-9]*|*[0-9])
        echo "🔢 包含数字的用户名"
        echo "需要验证用户名规范"
        ;;
    "")
        echo "❌ 用户类型不能为空"
        ;;
    *)
        echo "❓ 未知用户类型: $usertype"
        ;;
esac
```

### 4.3 多模式匹配最佳实践

**✅ 推荐做法**：
```bash
# 1. 相关选项放在一起
case "$cmd" in
    start|run|begin)     echo "启动" ;;
    stop|quit|end)       echo "停止" ;;
    
# 2. 从特殊到通用的顺序
    --version|-v)        echo "版本信息" ;;
    --help|-h|help)      echo "帮助信息" ;;
    -*)                  echo "未知选项" ;;
    
# 3. 空值单独处理  
    "")                  echo "参数为空" ;;
    *)                   echo "默认处理" ;;
esac
```

**❌ 避免的做法**：
```bash
# 不要模式重复
case "$var" in
    abc)  echo "处理abc" ;;
    abc)  echo "重复了!" ;;  # 永远不会执行
    
# 不要过于复杂的模式
    *[a-z]*[0-9]*[A-Z]*)  # 太复杂，难以理解
        echo "复杂模式"
        ;;
esac
```

---

## 5. ⚖️ case vs if-elif选择指南


### 5.1 什么时候用case语句

**✅ 推荐使用case的场景**：
- **固定选项判断**：有限的几个固定值
- **字符串模式匹配**：需要通配符匹配
- **菜单选择**：用户输入选项处理
- **参数解析**：脚本参数处理
- **文件类型判断**：根据扩展名分类

```bash
# ✅ 适合用case - 菜单选择
case "$choice" in
    1|start)    start_service ;;
    2|stop)     stop_service ;;  
    3|status)   check_status ;;
    q|quit)     exit 0 ;;
    *)          show_help ;;
esac
```

### 5.2 什么时候用if-elif语句  

**✅ 推荐使用if-elif的场景**：
- **数值比较**：大小、范围判断
- **复杂条件**：多个条件组合
- **文件属性检查**：文件存在性、权限等
- **变量状态判断**：空值、类型检查

```bash
# ✅ 适合用if-elif - 数值范围判断
if [ $score -ge 90 ]; then
    echo "优秀"
elif [ $score -ge 80 ]; then
    echo "良好"
elif [ $score -ge 60 ]; then
    echo "及格"  
else
    echo "不及格"
fi
```

### 5.3 性能和可读性对比


**📊 对比分析表**：

| 特性 | **case语句** | **if-elif语句** |
|------|-------------|----------------|
| **适用场景** | 固定选项、模式匹配 | 数值比较、复杂条件 |
| **代码简洁性** | ⭐⭐⭐⭐⭐ 非常简洁 | ⭐⭐⭐ 相对冗长 |
| **可读性** | ⭐⭐⭐⭐⭐ 结构清晰 | ⭐⭐⭐⭐ 逻辑明确 |
| **性能** | ⭐⭐⭐⭐ 较快 | ⭐⭐⭐ 一般 |
| **通配符支持** | ⭐⭐⭐⭐⭐ 原生支持 | ❌ 需要额外处理 |
| **复杂条件** | ❌ 不适合 | ⭐⭐⭐⭐⭐ 很灵活 |

### 5.4 转换示例对比

```bash
# 场景：处理HTTP状态码

# ❌ 用if-elif处理（不够简洁）
if [ "$status" = "200" ] || [ "$status" = "201" ] || [ "$status" = "202" ]; then
    echo "请求成功"
elif [ "$status" = "400" ] || [ "$status" = "401" ] || [ "$status" = "403" ]; then
    echo "客户端错误" 
elif [ "$status" = "500" ] || [ "$status" = "502" ] || [ "$status" = "503" ]; then
    echo "服务器错误"
else
    echo "其他状态码"
fi

# ✅ 用case处理（简洁明了）
case "$status" in
    200|201|202)
        echo "✅ 请求成功"
        ;;
    4[0-9][0-9])
        echo "❌ 客户端错误"
        ;;
    5[0-9][0-9])
        echo "🔥 服务器错误"  
        ;;
    *)
        echo "❓ 其他状态码: $status"
        ;;
esac
```

> 💡 **选择建议**：优先考虑case，如果case无法满足再用if-elif

---

## 6. 🍽️ 菜单设计实战应用


### 6.1 基础菜单框架

```bash
#!/bin/bash
# 系统管理菜单

show_menu() {
    clear
    echo "================================"
    echo "      🔧 系统管理菜单"
    echo "================================"
    echo "1. 📊 查看系统信息"
    echo "2. 👥 用户管理" 
    echo "3. 💾 磁盘管理"
    echo "4. 🔄 进程管理"
    echo "5. ⚙️  服务管理"
    echo "0. 🚪 退出程序"
    echo "================================"
    echo -n "请选择操作 [0-5]: "
}

while true; do
    show_menu
    read choice
    
    case $choice in
        1)
            echo "📊 系统信息："
            uname -a
            df -h
            echo "按回车继续..."
            read
            ;;
        2)
            echo "👥 当前用户："
            who
            echo "按回车继续..."
            read
            ;;
        3)
            echo "💾 磁盘使用情况："
            df -h
            echo "按回车继续..."
            read
            ;;
        0|q|quit|exit)
            echo "👋 再见!"
            break
            ;;
        "")
            echo "❌ 请输入选择"
            sleep 1
            ;;
        *)
            echo "❌ 无效选择: $choice"
            echo "请输入0-5之间的数字"
            sleep 2
            ;;
    esac
done
```

### 6.2 多级菜单设计

```bash
#!/bin/bash
# 文件操作多级菜单

file_menu() {
    while true; do
        echo ""
        echo "📁 文件管理菜单"
        echo "1. 📝 创建文件"
        echo "2. 📖 查看文件"  
        echo "3. ✏️  编辑文件"
        echo "4. 🗑️  删除文件"
        echo "b. ⬅️  返回上级"
        echo -n "选择操作: "
        read file_choice
        
        case $file_choice in
            1) create_file ;;
            2) view_file ;;
            3) edit_file ;;
            4) delete_file ;;
            b|back) break ;;
            *) echo "❌ 无效选择" ;;
        esac
    done
}

create_file() {
    echo -n "📝 输入文件名: "
    read filename
    if [ -z "$filename" ]; then
        echo "❌ 文件名不能为空"
        return
    fi
    
    touch "$filename"
    echo "✅ 文件 $filename 创建成功"
}
```

### 6.3 菜单设计最佳实践


**🎯 设计原则**：
- **数字选项**：1、2、3比字母更直观
- **退出选项**：提供多种退出方式(0、q、quit)
- **输入验证**：处理空输入和无效输入
- **用户友好**：清晰的提示信息和emoji图标
- **循环菜单**：用while循环保持菜单持续显示

**🔧 实用功能**：
```bash
# 带颜色的菜单
RED='\033[0;31m'
GREEN='\033[0;32m'  
BLUE='\033[0;34m'
NC='\033[0m' # No Color

show_colored_menu() {
    echo -e "${BLUE}================================${NC}"
    echo -e "${GREEN}      系统管理菜单${NC}"
    echo -e "${BLUE}================================${NC}"
    echo -e "1. ${GREEN}查看系统${NC}"
    echo -e "0. ${RED}退出${NC}"
}
```

---

## 7. 🔧 参数处理高级应用


### 7.1 脚本参数解析

```bash
#!/bin/bash
# 通用脚本参数处理

script_usage() {
    echo "用法: $0 [选项] [参数]"
    echo "选项:"
    echo "  -h, --help      显示帮助信息"
    echo "  -v, --version   显示版本信息"  
    echo "  -f, --file      指定文件"
    echo "  -d, --debug     开启调试模式"
    echo "  start|stop|restart  服务控制"
}

# 解析所有参数
while [ $# -gt 0 ]; do
    case $1 in
        -h|--help)
            script_usage
            exit 0
            ;;
        -v|--version)
            echo "脚本版本: 1.0.0"
            exit 0
            ;;
        -f|--file)
            if [ -z "$2" ]; then
                echo "❌ -f 选项需要指定文件名"
                exit 1
            fi
            FILE="$2"
            shift 2  # 跳过选项和参数
            ;;
        -d|--debug)
            DEBUG=true
            shift
            ;;
        start|stop|restart|status)
            ACTION="$1"
            shift
            ;;
        -*)
            echo "❌ 未知选项: $1"
            script_usage
            exit 1
            ;;
        *)
            echo "❌ 未知参数: $1"
            script_usage  
            exit 1
            ;;
    esac
done

# 根据解析的参数执行操作
case "$ACTION" in
    start)
        echo "🚀 启动服务..."
        [ "$DEBUG" = true ] && echo "🐛 调试模式开启"
        ;;
    stop)
        echo "🛑 停止服务..."
        ;;
    restart)
        echo "🔄 重启服务..."
        ;;
    status)
        echo "📊 查看服务状态..."
        ;;
    "")
        echo "❌ 请指定操作: start|stop|restart|status"
        script_usage
        exit 1
        ;;
esac
```

### 7.2 配置文件处理

```bash
#!/bin/bash
# 配置文件参数处理

load_config() {
    local config_file="$1"
    
    if [ ! -f "$config_file" ]; then
        echo "❌ 配置文件不存在: $config_file"
        return 1
    fi
    
    while IFS='=' read -r key value; do
        # 跳过注释和空行
        case "$key" in
            '#'*|'') continue ;;
        esac
        
        # 根据配置项设置变量
        case "$key" in
            database_host)
                DB_HOST="$value"
                ;;
            database_port)
                DB_PORT="$value"
                ;;
            log_level)
                case "$value" in
                    debug|info|warn|error)
                        LOG_LEVEL="$value"
                        ;;
                    *)
                        echo "⚠️ 无效日志级别: $value，使用默认值info"
                        LOG_LEVEL="info"
                        ;;
                esac
                ;;
            max_connections)
                if [[ "$value" =~ ^[0-9]+$ ]] && [ "$value" -gt 0 ]; then
                    MAX_CONN="$value"
                else
                    echo "⚠️ 无效连接数: $value，使用默认值100"
                    MAX_CONN="100"
                fi
                ;;
            *)
                echo "⚠️ 未知配置项: $key"
                ;;
        esac
    done < "$config_file"
}
```

### 7.3 错误处理和验证

```bash
#!/bin/bash
# 参数验证和错误处理

validate_file() {
    local file="$1"
    
    case "$file" in
        "")
            echo "❌ 文件名不能为空"
            return 1
            ;;
        /*)
            # 绝对路径
            if [ ! -f "$file" ]; then
                echo "❌ 文件不存在: $file"
                return 1
            fi
            ;;
        *)
            # 相对路径  
            if [ ! -f "./$file" ]; then
                echo "❌ 当前目录下文件不存在: $file"
                return 1
            fi
            ;;
    esac
    
    # 检查文件权限
    case "$file" in
        *.sh)
            if [ ! -x "$file" ]; then
                echo "⚠️ 脚本文件没有执行权限: $file"
                echo "建议执行: chmod +x $file"
            fi
            ;;
        *.txt|*.log)
            if [ ! -r "$file" ]; then
                echo "❌ 文件没有读取权限: $file"
                return 1
            fi
            ;;
    esac
    
    echo "✅ 文件验证通过: $file"
    return 0
}
```

---

## 8. 📋 核心要点总结


### 8.1 必须掌握的基本概念

```
🔸 case语句：多分支选择控制结构，处理固定选项判断
🔸 esac结束：case倒写，表示case语句结束  
🔸 双分号;;：分支结束标识，防止继续执行下个分支
🔸 模式匹配：支持精确匹配和通配符匹配(*、?、[])
🔸 多模式|：一个分支处理多个相似选项
🔸 默认分支*：处理所有未匹配的情况
```

### 8.2 关键语法要点

**🔹 标准语法结构**
```bash
case $变量 in
    模式1)  操作1 ;;
    模式2)  操作2 ;;
    *)      默认操作 ;;
esac
```

**🔹 重要语法规则**  
- **双分号必须**：每个分支都要以`;;`结束
- **右括号必须**：模式后面必须有`)`
- **esac必须**：case语句必须以`esac`结束
- **模式顺序**：从特殊到通用，*通常放最后

### 8.3 使用场景总结


**✅ 推荐使用case的场景**：
- 🎯 **菜单选择**：用户输入1、2、3等固定选项
- 🔧 **参数处理**：脚本命令行参数解析
- 📁 **文件分类**：根据文件扩展名进行不同处理
- 🚦 **状态判断**：start、stop、restart等状态控制
- 📋 **配置解析**：根据配置项做不同处理

**❌ 不推荐使用case的场景**：
- 📊 **数值比较**：大小、范围判断用if更合适
- 🔍 **复杂条件**：多个条件组合判断
- 📂 **文件检查**：文件存在性、权限检查
- 🔢 **计算判断**：数学计算结果的条件判断

### 8.4 最佳实践建议


**🎯 编写技巧**：
```bash
# 1. 加引号防止特殊字符问题
case "$var" in
    
# 2. 相关选项分组
    start|run|begin) echo "启动" ;;
    stop|quit|end)   echo "停止" ;;
    
# 3. 处理空值
    "") echo "参数为空" ;;
    
# 4. 通配符放最后  
    *) echo "默认处理" ;;
esac
```

**🔧 调试技巧**：
- 用`set -x`查看匹配过程
- 用`echo`输出变量值确认匹配内容
- 逐步添加分支，避免一次写太复杂
- 先写简单模式，再添加通配符

**核心记忆口诀**：
- case多分支，esac来结尾
- 双分号分隔，模式要匹配  
- 星号做默认，引号防出错
- 菜单和参数，case最适合

> 💡 **学习建议**：从简单的菜单程序开始练习，逐步掌握复杂的模式匹配和参数处理